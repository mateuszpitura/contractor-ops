packages:
  - "apps/*"
  - "packages/*"

# Minimum release age: reject packages published less than 7 days ago (10080 minutes)
# Protects against supply-chain attacks via freshly published malware packages
minimumReleaseAge: 10080
minimumReleaseAgeStrict: true
