# Persistent Memory (cross-session invariants)

Facts that stay true across GSD phases. Update when architecture or policy changes — not for per-phase task lists (those live in `STATE.md`).

**Last updated:** 2026-07-05

## web-vite UI (current)

- **No `*-container.tsx`** under `apps/web-vite/src/` — wired section + `*View` in co-located files; route screens use `*PageContent` in `pages/**`
- **tRPC boundary** — only `components/{domain}/hooks/use-*.ts` (enforced by `pnpm check:web-vite-data-layer`)
- **`BillingTierGate`** — Stripe subscription tier (`billing/billing-tier-gate.tsx`); distinct from Unleash **`FeatureGate`** (`layout/feature-gate.tsx`)
- **Tests** — assert `*View` with stub props; mock wired import paths, not deleted containers

## API / integration patterns (2026-06)

- **`integrationProcedure`** — integration routers use factory (`packages/api/src/lib/integration-procedure.ts`)
- **`loadOrgIntegrationConnection`** — sole connection loader in integration routers; no inline `findFirst`
- **Integration RBAC** — zero plain `tenantProcedure` in `integrations/*`; teams reads on `integrationSettingsProcedure('read')`

## Money & finance

- **Money rounding** — integer minor units only; derived values round HALF-UP (`Math.round`); skonto discount FLOORS, statutory interest rounds HALF-UP on the accrued claim; never `parseFloat`-then-scale unvalidated money. See [[patterns/money-rounding]] (`.planning/brain/wiki/patterns/money-rounding.md`).

## Shared UI patterns

- **`useListDataTable`** — domain list hooks own sort/column/selection state; reports via `useReportTableState`
- **`EntitySummarySheet`** — side-panel shell (contractor, contract, invoice, payment-run, workflow, approval); `titleVisuallyHidden` + `loadingTitle` for skeleton a11y
- **`useDirection`** — RTL from locale (`ar`); EntitySummarySheet, WizardDialogShell, WorkbenchDataTable
- **Provider sections** — hook + skeleton + view in one `*-provider-section.tsx`

## Authority order

1. In-tree verification (`packages/api/src/root.ts`, `pnpm test`, `pnpm typecheck`)
2. `CLAUDE.md` + `.planning/PROJECT.md`
3. `.planning/codebase/*` maps (commit `70f5782d`)
4. `.planning/intel/*` query index
5. Phase SUMMARYs under `.planning/milestones/` (historical)
6. **Discard** stale session memory, cross-repo handoffs, unverified test counts

## Stack anchors

- **Monorepo:** pnpm 10 + Turborepo; Node 24; TypeScript ESM
- **Staff API:** Fastify `apps/api` → tRPC `appRouter` at `/api/trpc/*`
- **Portal API:** `portalAppRouter` at `/api/trpc/portal` — **not** in `appRouter` (TS inference cost)
- **Router counts:** verify `packages/api/src/root.ts` — **53** always-mounted namespaces + conditional spreads: **8** classification (`module.classification-engine`), **1** us-expansion `taxForm` (`module.us-expansion`), **2** workforce `worker`/`employee` (`module.workforce-employees`) — each also gated by `QA_DEFAULT_ORG_ID`
- **Web UI:** `apps/web-vite` — pages inline `*PageContent` or wired sections → Hook → Component; tRPC only in `hooks/`; **zero** `*-container.tsx` under `components/` (17C2)
- **Auth:** Better Auth `packages/auth`; tenant from session, never from client `organizationId` alone
- **DB:** Prisma 7, PostgreSQL 17; regional `DATABASE_URL_EU` / `_ME` (+ optional `_US`)
- **Flags:** `@contractor-ops/feature-flags` only — keys in `packages/feature-flags/src/registry.ts`

## Non-negotiable patterns

| Pattern | Where enforced |
|---------|----------------|
| `entityIdSchema` for single-entity inputs | `packages/validators/src/common-inputs.ts`, `lint:architecture` |
| `formatMoneyAmount` in web-vite | `apps/web-vite/src/lib/money.ts`, `lint:architecture` |
| No `@contractor-ops/db` in web-vite | `lint:architecture` |
| `writeAuditLog` on sensitive mutations | `packages/api/src/services/audit-writer.ts`, `lint:audit-log` |
| Zod on every tRPC procedure | `packages/api/src/init.ts` middleware stack |
| `semble search` before grep | `CLAUDE.md`, `.cursor/rules/` |
| Read before Edit on existing files | Cursor runtime guard |

## Product / legal

- **Core value:** Invoice → match → approval → payment with full audit trail
- **Current milestone:** v7.0 GTM Expansion (phases 82–101) — see `STATE.md`
- **Legal copy:** DEFERRED sign-off; locked phrases in `packages/validators/src/legal/` — do not duplicate in UI/CMS
- **Deploy posture:** LOCAL-ONLY until legal gates cleared

## Agent discovery commands

```bash
semble search "<behavior>"
node .claude/get-shit-done/bin/gsd-tools.cjs intel query <term>
node .claude/get-shit-done/bin/gsd-tools.cjs graphify query <term>
pnpm check:web-vite-data-layer
pnpm typecheck --filter=@contractor-ops/api
```

## Memory layers (agent stack)

| Layer | Path | Role |
|-------|------|------|
| Live code truth | semble + Read | Symbols, procedures |
| Brownfield snapshot | `.planning/codebase/*` | Structure, conventions (commit pinned) |
| Query index | `.planning/intel/*` | Fast JSON lookup |
| Call graph | `.planning/graphs/graph.json` | AST graphify (verify edges via semble) |
| Domain wiki | `.planning/brain/wiki/` | Why / flows / patterns — **not** symbol lookup |
| Cross-session invariants | This file | Policies that survive phases |

## Graphify note

AST-only extract from repo root: `graphify extract . --no-cluster --out .planning/graphs` (see `.graphifyignore`). Semantic LLM skipped — docs/images/md excluded. Venv: `.planning/.venv-graphify/` (local, not committed). User runs extract; copy `graphify-out/graph.json` when done.

## Wave 16A — workflows container collapse (2026-06-10)

Collapsed all **20** `workflows/` + `workflow/credentials-tab-container.tsx` passthrough/orchestrator files into View+wired exports or `*PageContent` route shells; **zero** workflow `*-container.tsx` remain under `components/workflows/` and `components/workflow/`.

## Wave 17C1 — zatca/admin/auth/peppol container collapse (2026-06-10)

Collapsed **26** `*-container.tsx` in `zatca/` (12), `admin/` (7), `auth/` (4), `peppol/` (3) — View+wired in co-located files; route orchestrators inlined as `*PageContent` in matching `pages/**`; **zero** containers remain in those four folders.

## KB enforcement — doc/graph freshness is gated, not advisory (2026-06-16)

The graphify + semble + wiki knowledge base is now enforced, not hoped for:
- **CI doc-drift gate** — `scripts/check-wiki-brain.mjs` fails when a source file under a wiki page's `verify_with` changes in the diff without that page being updated (NEW drift only, vs branch base; `ci.yml` ci job uses `fetch-depth: 0`). graph.json / BM25 absence downgraded to WARN (local gitignored artifacts).
- **Stop hook block-once** — `.claude/hooks/wiki-brain-inject.sh stop` emits `{"decision":"block"}` once (respects `stop_hook_active`) when `apps/`/`packages/` changed with no wiki update; also emits `GRAPH_WARN` if graph older than newest code commit.
- **Graph auto-rebuild** — `.husky/post-commit` runs incremental AST-only `graphify update` (LLM keys blanked) in background when a commit touches `apps/`/`packages/`.
- **Surfacing** — `core-values.yml` Tools/Workflow now routes graphify (call graph / blast radius) + "never assert project facts from memory"; semble wired in `.mcp.json` + new `.cursor/mcp.json` for Cursor parity.
- **Honest ceiling** — these force *hygiene + freshness*, not *semantic correctness*. No mechanism proves a wiki page is true or that an agent used a result well; an edit-accuracy eval (golden tasks) is the open follow-up.

## US W-form intake + treaty engine (Phase 85, 2026-06-16)

The W-9 / W-8BEN / W-8BEN-E surface captures (does not file) a US tax classification + resolves the treaty claim. Invariants that survive the phase:

- **One treaty engine, one table.** US treaty rows live in the shared `WithholdingTaxRate` table (`sourceCountry='US'`, nullable `treatyArticle` column; `serviceType` reused as the income-type axis = `'business_profits'`). The 4-field `@@unique([sourceCountry, contractorResidency, serviceType, effectiveFrom])` key is load-bearing — adding a 5th field breaks the seed upsert + `calculateWht` lookup. Rates are whole-number percent (30.0 / 0.0 / null). PL/DE/GB/IE/NL → 0% (Article 7); AE/SA have no US treaty → 30%. The same table feeds Phase 87's 1042-S withholding.
- **`TaxFormSubmission` is append-only + supersede-chained**, FK'd to `Contractor` (NOT `Worker` — that abstraction is Theme B/Phase 89). Re-cert flips the prior ACTIVE row to SUPERSEDED then inserts a new ACTIVE row in one `$transaction`; signed rows are never mutated, only DRAFT rows are.
- **Portal-primary self-cert.** The beneficial owner signs in the portal (`portalAppRouter`). ESIGN attestation (ip / actorId / signedAt) is 100% server-derived from the session + headers — the client schema omits all three; identity is unforgeable. The W-9 payload never carries a full SSN (last-4 reference only; the SSN stays in its encrypted column with the `contractorPii:read` reveal gate). `buildFormSnapshot` recursively strips full-SSN/TIN keys as a second guard.
- **Staff get a read/track mirror only** — never an on-behalf signing path (`requestTaxForm` writes an audit event, no record). The staff `taxForm` namespace is a DEDICATED router so `root.ts` can conditionally spread the whole US surface behind `module.us-expansion`; the always-mounted `taxRouter` is never extended for it. Defense-in-depth: boot-gate spread + per-request `assertUsExpansionEnabled` (the flat portal merge can't be conditionally spread, so it self-gates).
- **web-vite layering holds** — the wizard's only tRPC boundary is `hooks/use-tax-form-wizard.ts`; the container + steps + page are presentational (`check:web-vite-data-layer`). Treaty rate/article auto-populate is advisory display announced via `aria-live`; the authoritative resolution + persistence happen server-side.

## AuditLog is DB-level append-only (2026-06-17)

`AuditLog` is append-only enforced in Postgres, not just by convention (migration `20260617000000_auditlog_append_only`): a `BEFORE UPDATE` trigger (`app.reject_auditlog_update`) rejects **every** UPDATE unconditionally, and the RLS `auditlog_delete` policy permits a DELETE **only** inside a transaction that has called `allowAuditPurge(tx)` (`@contractor-ops/db` → `SET LOCAL app.allow_audit_purge='on'`, read by `app.audit_purge_allowed()`). The sole legitimate caller is the GDPR Right-to-Erasure path (`routers/compliance/gdpr.ts`); ordinary writers never set the flag, so their deletes are denied. The old over-broad `auditlog_write FOR ALL` policy was replaced by INSERT-only (`auditlog_insert`). To "correct" an audit row, supersede with a new INSERT — never UPDATE.

## Worker model abstraction (Phase 89, 2026-06-22)

`Worker` is the org-scoped identity root for the workforce union (contractors today, employees from Phase 90). Invariants that survive the phase:

- **Worker base table + one-time backfill.** `Worker` (`packages/db/prisma/schema/worker.prisma`) carries `organizationId` + `workerType WorkerType @default(CONTRACTOR)` + shared `displayName`/`email`/`status` + soft-delete; it is **tenant-owning and absent from `globalModels`** (so `withTenantScope` injects org scope — proven by `worker-tenant-isolation.test.ts`). `Contractor` links via a sidecar `workerId String @unique` 1:1 FK — **`Contractor.id` stays stable** so the 20+ FKs that reference it are never relinked (not a re-key). The link was populated by `packages/db/scripts/backfill-worker.ts`: idempotent (`WHERE workerId IS NULL`, re-run = no-op), reversible (`--rollback` nulls `workerId` then drops orphaned Workers; contractor rows untouched), per-region, audited (one system-actor `worker.backfill.apply` `AuditLog` row per org, written directly via Prisma since `db` sits below `api` in the dep graph — recorded against `ORGANIZATION` as `EntityType` has no `WORKER` member). Two-step migration ordering: Migration A (nullable column + table, additive) → backfill → Migration B (`SET NOT NULL` + FK, applied **LAST** after staging-parity sign-off — running B first rejects every null row). The `contractor.create` sites (`contractor-core.ts`, `import.ts`, `seed-dev.ts`) create+link a `Worker` atomically so new contractors are never orphaned.

- **Contractor reads are `workerType`-scoped by a central extension.** `withWorkerTypeDefault` (`packages/db/src/worker-type.ts`) is chained outermost in the tenant client (`withWorkerTypeDefault(withSoftDelete(withTenantScope(...)))`) and injects `workerType='CONTRACTOR'` on Worker reads **unless the caller passes an explicit `workerType`** (explicit-where-wins — the `worker`/`employee` routers do this to read cross-type). Its blind spot is raw SQL: the 4 known `$queryRaw` `FROM "Contractor"` sites (dashboard active-contractor count, command-palette FTS, two `contractor-shared` facet reads) are **contractor-only-by-table** and annotated `// contractor-only-raw-sql:`; `scripts/check-contractor-rawsql-workertype.ts` (wired into `lint:ci` after `lint:raw-sql`) fails any NEW unannotated raw `FROM "Contractor"` read. Per-type HR-only fields gate on a separate `employee` RBAC resource (4 HR roles, BFLA fence — never a contractor mutation, never auto-granted to `owner`); the whole `worker`/`employee` surface is dark behind `module.workforce-employees` via the three-layer flag-off. Detail: [[domains/worker-foundation]] (`.planning/brain/wiki/domains/worker-foundation.md`).

## US payment rail — payment run is the withholding source of truth (Phase 88, 2026-07-01)

The US payout rail is where the withholding that the tax-form surface only *reports* is actually *deducted*, and where the recorded payout figure becomes the truth the forms aggregate. Invariants that survive the phase:

- **The payment run is the single source of truth for the withheld amount.** The recorded `PaymentRunItem.whtAmountMinor` (written by `applyWithholdingToRun` in `packages/api/src/routers/finance/payment-shared.ts`) is authoritative; the 1099-NEC box-4 and 1042-S box-2 **aggregate the year's actual payment-run withholding — never recompute** the deduction in the forms. Withholding is a payment event: the form reflects what really moved. One HALF-UP round of `whtAmountMinor` at the rate, then `amountMinor = grossAmountMinor − whtAmountMinor` (integer gross/net invariant); the export file carries the net. See [[patterns/money-rounding]].

- **One jurisdiction-agnostic withholding path.** `applyWithholding` (pure, per item) covers SA WHT (unchanged `calculateWht`), US backup withholding 24% (IRC §3406, when `Contractor.backupWithholdingFlagged`), and the 1042-S treaty rate (`applyTreaty`, 30% statutory fallback). The SA branch is byte-preserved and regression-guarded; a US-domestic recipient / 0% treaty / non-withholding jurisdiction returns `null` (item untouched). `createBackupWithholdingFlagWriter` (`tin-match.service.ts`) persists the flag via a tenant-scoped idempotent `updateMany({ id, organizationId })` (boolean only — the TIN never reaches the write).

- **US export formats are hand-rolled and config-driven.** `generateNachaFile` is a hand-rolled zero-dependency NACHA ACH credit file (94-char 1/5/6/8/9 records, entry hash, balanced control totals, block padding, service class 220 + SEC PPD + txn 22 defaults — SEC/txn parameterizable); `generateFedwirePacs008` is an ISO 20022 `pacs.008.001.08` XML (not the retired FAIM flat file), the message handed to the bank. `detectUsFormat` routes USD + US bank → `ACH_NACHA`, above the Same-Day ACH ceiling → `FEDWIRE`; the ceiling is dated **config** (`sameDayAchCeilingMinor(asOf)`: $1M → $10M on 2027-09-17), never a constant. USD is a normal ECB currency — **no `USD=1.0` short-circuit** (it would mask a genuinely missing rate). Settlement FX (`payment-settlement.ts` `convertForSettlement`) is a single HALF-UP delegate to `convertAmount`; a missing rate throws `UNPROCESSABLE_CONTENT`, never a silently zeroed payout.

- **Programmatic ACH + live Plaid are mock-behind-seam, flag-dark; Plaid is advisory fail-open.** The GA path installs **zero external SDKs**. `payment.initiatePayout` (opt-in, `payment:export` + `assertUsExpansionEnabled` + `payments.ach-payouts` flag) originates via the `PayoutInitiationAdapter` (Modern Treasury deterministic mock default; live dark); it is idempotent (Upstash reserve/complete/clear — no double-pay) and masked-audited. The NACHA/Fedwire **file** export stays the always-available default. Plaid verification is advisory: the payout reads the persisted `PaymentRunItem.billingProfile.plaidVerificationStatus` via a tenant-scoped include (never `contractor.billingProfiles[]`); an unverified status warns + audits, never blocks. `payments.plaid-verification` gates ONLY the live Plaid client and is deliberately **non-gated** (not in the v7.0 cohort / signoff registry — the only gated payments prefix is `payments.ach-`). Detail: [[domains/us-payment-rail]] (`.planning/brain/wiki/domains/us-payment-rail.md`).

- **ACH return-code ingestion is the reachable path that flips a bounced payout back to FAILED.** The operator uploads the NACHA return file their bank produced via `payment.ingestAchReturnFile` (`payment-core.ts`, same gate as `initiatePayout`: `payment:export` + `assertUsExpansionEnabled`, applied before any parse/apply, `.strict()` Zod with a bounded `returnFileText`); it delegates to `parseNachaReturnFile` → `applyAchReturns` (`services/ach-return.service.ts`) and returns `{ failed, advisory, skipped, unmatched }` **verbatim**. R01/R02/R03 (+ R-family) → `PaymentRunItem.status='FAILED'` + reason; NOC/COR → advisory (no status change, never fails a payout); unrecognised non-correction code defaults to FAILED (fail-safe). Tenant-scoped + idempotent (an already-FAILED item is skipped, so a re-upload is a no-op and a return can never un-fail an item) + one masked audit row per transition plus an ingestion-summary `payment_run.ach_return_ingested` row (sizes + tallies only, no bank data, no raw file text). The `unmatched` count is the operator-safety signal: `unmatched > 0` distinguishes a mis-uploaded / wrong-run file (a foreign-org run flips nothing — every entry is unmatched) from a clean no-bounce run — never a silent zeros-everywhere no-op; a return-addenda file that parses to nothing is a `BAD_REQUEST` (`PAYMENT_ACH_RETURN_FILE_INVALID`). The live Modern Treasury return-webhook (`PayoutInitiationAdapter.handleWebhook`) is a documented deferred seam (programmatic ACH stays dark), so file upload is the only reachable return path. Detail: [[domains/us-payment-rail]] (`.planning/brain/wiki/domains/us-payment-rail.md`).

- **Plaid verification has a reachable mock write path; the tin-match backup-withholding writer is a recorded defer to Phase 86.** The onboarding write path is `payment.verifyBillingProfilePlaid` (`payment-core.ts`, same gate as `initiatePayout`: `payment:export` + `assertUsExpansionEnabled` + tenant-scoped `.strict()` Zod `billingProfileId`): it runs `MockPlaidIdentityClient.verify` against the profile's **masked** US routing/account + `contractor.legalName` and persists `ContractorBillingProfile.plaidVerificationStatus` + `plaidVerifiedAt` + `plaidAccountId`, so the payout advisory read now has a real non-null status. It is itself advisory fail-open — a non-VERIFIED result is written and returned as `{ status, advisoryWarning }`, never throws/blocks; a foreign-org profile is `NOT_FOUND` (never verified); masked audit `contractor_billing_profile.plaid_verified` (billingProfileId + status only). No Plaid SDK installed — the live Link flow stays flag-dark. Separately, the backup-withholding writer `createBackupWithholdingFlagWriter` / `createDbTinMatchPersistence` (`tin-match.service.ts`, which sets `Contractor.backupWithholdingFlagged` that `applyWithholding` already reads) exists and is correct but has **zero production callers by design** — the year-end IRS TIN-match batch that would trigger it is **owned by Phase 86**, not the Phase 88 payout rail. That is a documented defer (LOCAL-ONLY, no live year-end batch yet), recorded in the phase `deferred-items.md` — not a gap and not a silent omission. Detail: [[domains/us-payment-rail]] + [[integrations/plaid]].

## Employee registry — per-market national-ID PII boundary (Phase 90, 2026-07-01)

The employee registry onboards a `Worker(workerType='EMPLOYEE')` + a tenant-owning 1:1 `EmployeeProfile` (`packages/db/prisma/schema/employee.prisma`, `workerId String @unique` FK — there is **no standalone `Employee` table**). Invariants that survive the phase:

- **National-ID PII lives encrypted in dedicated columns, never in the JSON, with a distinct reveal permission.** PESEL / SSN / Iqama / Emirates-ID are stored as AES-256-GCM `*Encrypted` + masked `*Last4` column pairs on `EmployeeProfile` and are **never** placed in the `countryFields` JSON (each per-market schema in `packages/validators/src/employee-country-fields.ts` is `.strict()`, so any national-ID key is a parse error). The three non-SSN IDs encrypt with a dedicated `EMPLOYEE_PII_ENCRYPTION_KEY` (`packages/api/src/services/employee-pii-crypto.ts` `encryptPii`/`decryptPii`/`maskLast4`) for an independent blast radius; the US SSN reuses the existing `SSN_ENCRYPTION_KEY` + `ssn-crypto`. `register` (`employee:create`) `omit`s every `*Encrypted` column on its return; plaintext is exposed only through `revealPii`, a field-routed decrypt gated on the separate `employeePii:read` resource (owner/admin/hr_admin only) that writes an `employee.<field>.revealed` audit row and is mounted **staff-only** (never on `portalAppRouter`). Audit rows use `resourceType: 'ORGANIZATION'` (`EntityType` has no `EMPLOYEE`/`WORKER` member), `resourceId = EmployeeProfile.id`, `workerId` in metadata. Emirates-ID format is blocking but its checksum is **advisory** (`checksumAdvisory`, never throws). Tax/social IDs (Steuer-IdNr / SV-Nummer / NI / PAYE) are **not** encrypted — they are plaintext-but-RBAC-gated fields inside `countryFields`. Detail: [[domains/employee-registry]] (`.planning/brain/wiki/domains/employee-registry.md`).

- **Government code lists are seeded LOCAL-ONLY data, not live API calls; `EmployeeProfile` is tenant-owning.** The employee reference lists (ZUS oddziały, NFZ, urzędy skarbowe, Krankenkassen — `packages/validators/src/reference-data/*`) and ELStAM (`packages/api/src/services/elstam-stub.ts` `lookupElstam`) are versioned + source-cited + adviser-verify seed tables + a **no-network stub seam** — there is **no live government API** (LOCAL-ONLY posture). `EmployeeProfile` is tenant-owning (`organizationId`) and **absent from `globalModels`** so `withTenantScope` injects org scope on every read — proven by `employee-cross-org-leak.test.ts` (ORG_B never reads/mutates ORG_A's row). Two follow-ups are deferred: the additive `__employee_profile_additive` migration is authored but not applied to the live EU/ME/US databases, and the web-vite RBAC mirror (`use-permissions.ts` / `memberRoles`) does not yet grant `employee`/`employeePii` to the HR roles, so the Register + reveal controls fail closed (absent) until that surface is wired.

## Personnel file — akta osobowe on the Worker identity (Phase 91, 2026-07-01)

The jurisdiction-correct personnel file (PL akta osobowe cz. A/B/C/D per KP art. 94, DE Personalakte, UK personnel file, US I-9 + file) attaches to the `Worker` identity via a 1:1 tenant-owning `PersonnelFile` sidecar (`packages/db/prisma/schema/personnel.prisma`, `workerId String @unique`; `PersonnelFileDocument` references the `Document` stack 1:1 with the 4-section view as an optional `section PersonnelFileSection?` enum-on-link; both **tenant-owning, absent from `globalModels`**). It composes existing primitives — three invariants survive the phase:

- **Per-section RBAC grain = resource-per-section `employeeFileA..D`, never granted to `owner`.** The finer-than-resource grain is four Better-Auth resources `employeeFileA`/`B`/`C`/`D` (each `[read, write]`) on `accessControlStatement` (`packages/auth/src/permissions.ts`) — resource-per-section, not an attribute layer, so `Permission`/`Resource`/`requirePermission`/`permissionToScopes` adapt for free. The section→role matrix wires them into the 4 HR roles (`hr_admin` A/B/C/D r+w; `hr_manager` A/B/D r+w + C read-only; `payroll_officer` C read-only; `leave_approver` A read-only — payroll reaches pay/C without discipline/B) and they are **deliberately kept OUT of the owner `allPermissions` duplicate** (BFLA fence — owner holds no section), mirroring the `employee` carve-out. The `personnelFile` router enforces this at the permission layer via `hasSectionPermission(ctx, section)` (`routers/core/personnel-file/section-access.ts`) — the lock is decided **before** any document query, and a locked section returns its retention posture with **no document payload or count** (never fetch-all-then-filter). Detail: [[patterns/rbac-permissions]] · [[domains/personnel-file]] (`.planning/brain/wiki/domains/personnel-file.md`).

- **Akta retention registers on the SHARED map + a new event-anchor resolver — no parallel engine.** Per-jurisdiction rules register on the same `packages/db/src/retention-policy.ts` `RETENTION_YEARS` map (8 akta tokens: `pl-akta-post2019`=10, `pl-akta-legacy`=50, `de-personalakte-tax`=10, `de-accident-records`=30, `uk-personnel-general`=6, `uk-personnel-financial`=7, `us-i9-post-hire`=3, `us-i9-post-termination`=1 — **years live only on the map, single source**; the register-on-import section+rule registry is `packages/compliance-policy/src/personnel-registry.ts`, PL/DE/UK/US, `resolveSectionForDocumentType`). `getPersonnelRetentionCutoff` (homed in `retention-policy.ts` next to the map; `personnel-retention.ts` is a re-export facade) is event-anchored: each rule declares `HIRE_DATE | TERMINATION_DATE | DOCUMENT_DATE`, computes `anchor + RETENTION_YEARS[token]`, and combines with `max()` (US I-9 `max(hire+3y, termination+1y)`, 8 CFR 274a.2); a missing required anchor (active employee, no termination) makes the section **indefinite** (`retainUntil` null, never erasable — fail-closed). Both existing deletion chokepoints route personnel rows: `softDeleteModels` (always soft-delete at the ORM) + the `apps/cron-worker` data-purge cron (per-row anchor-driven exclusion; children before parents; a `Document` held by an active akta window is excluded from the flat 90-day sweep). Detail: [[structure/key-services]] · [[domains/personnel-file]].

- **Per-employee/per-section/per-jurisdiction statutory-hold erasure never over-claims.** `personnelFile.requestErasure({ workerId })` (`employee:delete`) lifts the org/model GDPR statutory-hold idiom to per-employee + per-section: it resolves each section's window, soft-deletes past-window sections' documents, retains in-window sections with their statutory citation + `retainUntil`, and returns `{ workerId, fullErasureClaimed, sections[] }` where **`fullErasureClaimed = retained.length === 0`** — never true while any hold is active (a single retained section forces `false`; criterion #3 verbatim-locked, and the UI `ErasureResultView` branches STRICTLY on this flag). Every retention-blocked erasure writes `personnel_file.erasure_retained_under_statute` (`resourceType: 'USER'`, `metadata.retainedUnderStatute = { section → citation }`) in the same transaction; `allowAuditPurge` stays **exclusive to the org-grain GDPR path** (personnel erasure only soft-deletes documents). The document classifier is hybrid taxonomy → `killswitch.ai-personnel-classifier` (default-on, `killWhenUnknown`, non-gated) Claude-Vision seam → `PENDING_REVIEW` admin step, and **never blocks the persisted upload**. Detail: [[patterns/audit-log]] · [[patterns/feature-flags]] · [[domains/personnel-file]].

## US expansion — 1042-S filing + worker classification + 1099-K tracker (Phase 87, 2026-07-05)

The chapter-3 foreign-withholding + US-classification surface, all dark behind `module.us-expansion` (`assertUsExpansionEnabled` per request + conditional `root.ts` spread). Invariants that survive the phase:

- **US classification is a pluggable `ClassificationProfile`, advisory-not-verdict.** `UsClassificationProfile` registers as a side-effect plugin (`getProfileForCountry('US')`, no `registry.ts` edit) and scores the federal IRS common-law base + a **dispositive** CA-ABC (AB5) overlay when work-state = CA (`US_WORK_STATE` injected server-side; engagement work-state primary, contractor US-state fallback) + a §530 safe-harbor relief flag. The verdict is decision-support, never a legal determination — the UI keeps a sticky advisory banner + a blocking disclaimer, the AB5 flag amber (never destructive), the §530 chip info. `classification.override` is reason-required + audit-logged into the append-only `AuditLog` (no schema column); the scored outcome stays server-derived. The `US_DETERMINATION_LETTER` react-pdf is deterministic + no-LLM with a **frozen** `ruleSetVersion` (render-from-snapshot), archived append-only. Detail: [[domains/us-classification]].
- **1042-S is a `Form1099Nec`-mirror: immutable + supersede, §875(d)-gated, REPORTED-only.** `form-1042s.service` derives every box server-side (box-2 = settled PAID/USD payouts per recipient per payer-org; box-7 = box-2 × the resolved rate); `resolveBox2Rate` short-circuits to the **30% statutory rate BEFORE any treaty lookup** when the W-8 chain is incomplete (+ escalation flag — never a treaty benefit on an unsubstantiated chain, never a silent skip), else the `applyTreaty` rate + article. A CORRECTED filing supersedes (prior ACTIVE → SUPERSEDED, new ACTIVE `corrected:true`, one `$transaction`); `generateBatch1042S` is idempotent + **REPORTED-only** (zero payment-write call paths — the deduction is the Phase 88 payment rail's job). FTIN is **last-4 only** everywhere (snapshot sanitizer strips forged `ftin`/`tin`/`ssn`; PDF + IRIS XML + DOM); the full reveal is a separate `contractorPii:read` audited procedure. Detail: [[domains/us-tax-forms]].
- **The 1099-K tracker is an informational cron band that NEVER files.** A daily `module.us-expansion` cron sums each contractor's cumulative settled-USD payouts + transaction count and bands SAFE/APPROACHING/OVER against the tax-year-keyed `Tax1099KThreshold` ($20,000 + 200, OBBBA — never a constant, never $600). `OVER` requires **both** the amount and the count crossed; the cron is the **sole writer** of `Form1099KTrackerState`; the read-only `form1099kTracker.getTrackerState` never mutates. No filing/generate/transmit call path exists (the platform is not the settlement TPSO). Detail: [[domains/us-tax-forms]].
- **1042-S IRIS is a sibling `buildIris1042SXml` through the P86 form-parameterized transmit tail.** The 1042-S IRIS builder is a **sibling** of the 1099 builder (distinct Pub 1187 record layout), not a parameterized 1099 builder; it shares the Transmission Manifest + the fast-xml-parser `XMLBuilder` (never string concat) and emits the FTIN last-4 only. `xsdValidate1042S` is form-keyed (`ENTRY_MATCHERS` + per-form memoization, so a missing 1042-S XSD reports missing rather than validating against the 1099 schema) and stays `XSD-BUNDLE-MISSING` until the human-only IRS Pub 1187 XSDs land under `packages/iris/src/schema-bundle/`. The **transmit tail** (download-XML / upload-ack) + the staff filing card + the portal 1042-S consent-gated PDF are a documented **cross-phase HOLD** on the P86 `TaxFilingTransmitter` / `iris-ack-parser` / `iris-status-pill` / `ack-upload-field` / `step-edelivery-consent` / `copy-b-download` seam — reused verbatim once P86 lands, **never rebuilt**. The staff 1042-S batch review UI is mounted independently at `/tax-filing` (flag + `contractor:read` gate). Detail: [[integrations/irs-1042s]] · [[structure/web-vite-domains]].

## US tax year-end filing — TIN-match → 1099-NEC → IRIS e-file (Phase 86, 2026-07-05)

The year-end US information-return loop ships entirely dark behind `module.us-expansion` (+ `module.iris-efile` for A2A). Load-bearing invariants:

- **The 1099-NEC threshold is a tax-year-keyed `Tax1099Threshold` table, never a constant** — $600 TY2025 / $2,000 TY2026 (OBBBA raised it for payments after 2025-12-31). `getBox1ThresholdMinor(db, taxYear)` reads the table; hardcoding `60000`/`200000` is a bug.
- **CORRECTED 1099 = supersede, never mutate.** `form-1099-nec.service.ts` `supersedeCorrected`/`fileCorrection` flips the prior ACTIVE `Form1099Nec` row → SUPERSEDED and inserts a new ACTIVE `corrected:true` row in one tx (mirrors `TaxFormSubmission`). A filed row is never edited. `Form1099Nec` is tenant-owning, absent from `globalModels`, registered in `MODEL_RETENTION_TYPE` `'1099-NEC'` (4yr) + `softDeleteModels` (cross-org leak proven by `tax-filing-tenant-isolation.security.test.ts`).
- **IRIS = ManualDownload default (no TCC) + dark A2A behind `module.iris-efile` (the single gate — no `iris-a2a-transmit` flag); FIRE is doc-only.** `tax-filing-transmitter.ts` factory: `ManualDownload` (build `buildIrisXml` → `xsdValidate` → download the validated XML, upload to IRIS) | `IrisA2A` (dark; SOAP/MTOM send is a documented seam) | `Vendor` (stub).
- **ONE `iris-ack-parser.ts` consumes BOTH the manual-uploaded ack file AND the dark A2A poll result** — all six IRIS statuses → `IrisAckStatus`, plus the Error Information Group + `OriginalReceiptId`. XXE-safe (DOCTYPE rejected; no entity expansion; no network).
- **A TIN mismatch sets the backup-withholding flag + escalates but NEVER hard-blocks** — the 1099 still generates; the staff mismatch UI is amber advisory with no blocking control.
- **A missing IRS IRIS XSD bundle is the expected pre-enablement state, not an error.** `xsdValidate` returns a non-throwing `BUNDLE_UNAVAILABLE` report (validity unproven → nothing files); the `packages/iris` XSD-validation-passes tests skip-and-auto-flip GREEN the moment the SOR `.xsd` files land. `xsdValidate` stays XXE/SSRF-safe (`nonet:true`, default `noent:false`, lazy bundle dir).
- **The year-end reminder cron is notify-only** — it never generates a batch or transmits; generate + file are separate, deliberate, human-initiated staff actions. Full TIN never in the DOM / PDF / IRIS payload / cache / audit — last-4 only; render Copy B only (Copy A goes via the IRIS XML). Detail: [[domains/us-tax-year-end-filing]] · [[integrations/irs-iris]] · [[integrations/irs-eservices-tin-matching]].

## Leave + KP-grade employee time (Phase 92, 2026-07-05)

The employee leave + statutory working-time surface on the [[domains/worker-foundation]] abstraction, gated behind `module.workforce-employees` (6 workforce namespaces now). Invariants that survive the phase:

- **Leave balance = Σ append-only `LeaveLedgerEntry.minutes`; corrections are reversing `ADJUSTMENT` rows, never edits.** `LeaveLedgerEntry` is registered in `APPEND_ONLY_MODELS` (`packages/db/src/tenant.ts`); `computeLeaveBalance` (`services/leave-balance.ts`) folds the ledger and `recomputeBalanceCache` refreshes the `LeaveBalance` cache per (worker, leaveType, year). A missing entitlement never throws. `recordSickAbsence` is a DIRECT negative `DEDUCTION` row + `LEAVE_SICK_RECORDED` dispatch — zero `ApprovalFlow`. Detail: [[domains/leave-and-time]].
- **`EmployeeTimeRecord` is the employee statutory time model keyed on `workerId`, DISTINCT from the contractor-coupled `TimeEntry`.** Day-grain upsert (`@@unique([organizationId, workerId, workDate])`); the web-vite hook calls `employeeTime.*`, NEVER the contractor `time.*` (enforced by `check:web-vite-data-layer` + distinct namespaces). `upsertRecord` returns `{ record, findings }` — the sync `checkWtLimits` breach is a NON-blocking advisory payload with dotted i18n copy-keys, never a thrown error. Detail: [[domains/leave-and-time]] · [[structure/key-services]].
- **`EwidencjaSnapshot` is INSERT-only + DB-trigger-immutable (`app.reject_ewidencja_update`).** Regenerating INSERTs a superseding version (`version+1` + `previousSnapshotId`); a `BEFORE UPDATE` trigger (migration `20260701000000_ewidencja_append_only`) rejects any UPDATE at the DB. The current register is the highest-`version` row (no `status` flip). Old versions stay archived — never deleted (the 10yr KP §94⁴ retention is satisfied by non-deletion; the 3yr immutability floor by the trigger). No edit/hard-delete affordance in the UI. Detail: [[domains/leave-and-time]] · [[structure/prisma-schema-areas]].
- **`LEAVE_REQUEST` extends the generic approval chain at exactly two seams — never a fork.** `submitLeaveRequest` calls `routeToLeaveChain` + `createApprovalFlow({ resourceType:'LEAVE_REQUEST' })`; approve/reject is the SHARED resourceType-gated procedure (`requireAnyPermission({invoice:['approve']},{employee:['approve_leave']})` + body `resourceType→permission` assertion), so a `leave_approver` actions a leave request via `employee:approve_leave` and never gains `invoice:approve` (BFLA fence). Detail: [[domains/approvals-engine]].
- **The WT-limit daily scan fans out over regions with region-prefixed dedup keys.** `runWtLimitScan` (`services/wt-limit-scan.ts`, executed inside the reminders cron) computes the true rolling weekly average and dispatches ONE `employee.wt_limit_breach` digest per recipient/day, deduped by a region-prefixed key (QStash callback carries no tenant ctx). Detail: [[structure/cron-jobs]].

Post-deploy legal checkpoints (non-blocking, LOCAL-ONLY posture): the statutory WT/leave constant values (`compliance-policy`) need Steuerberater/doradca adviser-verify, and the ewidencja 10yr KP §94⁴ retention needs legal sign-off — flagged, not hard-blocking.

## Public API keys / scopes / rate limits + write surface (Phase 99, 2026-07-05)

Theme C makes the public REST API least-privilege + production-safe. Everything ships **double-dark** (`module.public-api` off + `hide:true` on write routes) — Phase 99 builds/scope-enforces/rate-limits/audits, it does NOT flip the flag. Load-bearing invariants:

- **`actingUserId` is ATTRIBUTION, never authorization.** Each `OrganizationApiKey` binds a mutable, active-membership-guarded `actingUserId` (surfaced as `ctx.apiKeyActingUserId`) that fills the non-null user FKs on write-creates (`PaymentRun.createdByUserId`, `WorkflowRun.startedByUserId`). Authorization is `scopes[]` via `requirePermission` apiKey-mode — an executor MUST NOT read `actingUserId` to authorize. Defaults to the creator; rebindable; cross-org/removed/inactive bind → `INVALID_ACTING_USER`. (`Invoice` has no user column — research A1 was wrong.)
- **Every public write carries a mandatory `requirePermission` scope (BFLA)** and audits `writeAuditLog({actorType:'API_KEY', actorId: apiKeyId, ipAddress: sourceIp, userAgent, metadata.actingUserId})` via `routers/public-api/write-shared.ts`. 6 write entities only (contractor/invoice/payment/paymentRun/workflow/workflowTask); `compliance_document.create` + standalone `payment.create` + `_initiatePayoutForRun` are DEFERRED. `.strict()` DTOs reject `organizationId`/`workerType`/server-derived money.
- **Rotation supersedes with a bounded grace window.** `apiKeyRouter.rotate` issues a new key inheriting name/scopes/actingUserId, marks the old `supersededAt`+`graceExpiresAt` (default 24h, max 168h); grace-aware `resolveByPrefix` admits a superseded key ONLY until `graceExpiresAt`, then hard-stops. Revoked wins. No re-rotating a superseded key.
- **Two limiters, two jobs.** Pre-auth flat ~100/min burst (`apps/public-api/rate-limiter.ts`, fail-closed prod) + post-auth per-tier monthly quota (`enforceApiTierQuota` in `apiKeyTenantProcedure`, Upstash `api-quota:{org}:{YYYY-MM}` fixed window; Starter 1k/Pro 10k/Enterprise ∞ short-circuits with no counter). All public keys currently require ENTERPRISE, so Starter/Pro quotas are latent-but-correct.
- **Writes stay DOUBLE-DARK — the flag flip + un-hide + SDK-write-inclusion is Phase 100** (after INTEG-SEC-01 OWASP gate). The `actingUserId`/rotation/`ApiKeyIpEvent` migration is generated + committed but apply is deferred (EXTERNAL-ENABLEMENT #11). Detail: [[domains/public-api-surface]] · [[patterns/rate-limit]] · [[patterns/tenant-and-audit]].

## Employee on/offboarding (Phase 93, 2026-07-05)

- **Employee on/offboarding = extend the generic run + worker-key the coupled saga, NEVER duplicate.** `WorkflowRun`/`DeprovisioningRun` gain a nullable `workerId`; contractor/assignment FKs go nullable; the v1.0 engine, the `assertRunCompletable` gate, the IdP step runner, the token resolver, and the pure `canStartDeprovisioning` cooldown are **reused UNCHANGED**. Contractor coupling lives in exactly 4 places (schema FKs, the `startDeprovisioningRun` trigger, the WorkflowRun start path, the web-vite hook). The single `startWorkflowRun(tx, …)` helper in `workflow-execution-runs.ts` OWNS the only `workflowRun.create` — `workflow.startRun` (discriminated-union `startRunSchema`) and `employeeLifecycleRouter` both delegate; never replicate the create. A defence-in-depth CHECK on both run tables enforces contractor-XOR-worker. Detail: [[domains/worker-onboarding-offboarding]].

- **`EmployeeProfile.terminatedAt` is the dated termination signal feeding the 14-day IdP cooldown** (mirrors `ContractorAssignment.endedAt`). The employee `startDeprovisioningRun` branch synthesizes an `ENDED` status from `terminatedAt`, sources `externalUserId` from `Worker.email`, and `COUNTRY_TZ` gained a `US` entry. `recordTermination` (HR-RBAC + audited) writes it; the web-vite IdP trigger is UX-gated on it but the server re-runs the cooldown regardless (no client bypass).

- **Statutory certs are DRAFT + LOCKED adviser-verify-watermarked, snapshot carries `*Last4` only.** `statutory-cert-pdf.ts` mirrors `form-1099-nec-pdf` (immutable snapshot + CAS `updateMany where pdfArchiveKey:null` → R2 `emp-cert/<orgId>/<id>.pdf`); a recursive strip drops full pesel/ssn/nino/steuerId/iqama/emiratesId. `CERT_ADVISER_VERIFY_EN/DE/PL` are LOCKED const registered PENDING in `signoff-registry.json` and CI-guarded ABSENT from `messages/*.json`. v7.0 subset = świadectwo pracy, PIT-11, simple Arbeitszeugnis, Lohnsteuerbescheinigung, P45, W-2 (qualified Arbeitszeugnis/P11D/COBRA/401k deferred v7.5). Gov interactions (I-9+E-Verify, ZUS ZWUA, Abmeldung SV, HMRC RTI, PIT filing) are network-free `{source:'STUB',available:false}` seams.

- **Per-market employee templates seed `WorkflowTemplate` (NOT `WorkflowRoleTemplate`) on the compound `@@unique([organizationId, jurisdiction, type, seedKey])`.** `@contractor-ops/employee-templates` boot-upserts 8 DRAFT seeds (4 jurisdictions × ONBOARDING/OFFBOARDING) via `upsertEmployeeMarketTemplates` in `runPostOrganizationCreateHooks` (fail-soft); children created only when the template has none (idempotent, never clobbers org edits). Seed tasks are `assigneeMode:'ROLE_BASED'` with `assigneeRole:null` — the P89 HR roles are Better Auth org roles, not the Prisma `UserRole` enum the column accepts, so DRAFT templates materialize unassigned and the org assigns HR on review/activation. The `__phase93_worker_lifecycle` migration is authored un-applied; per-region apply (EU/ME/US) is a deferred deploy-time human gate.

## Outbound webhooks + integration security + write flag-flip (Phase 100, 2026-07-05)

Theme C pushes signed, PII-safe events to hostile customer URLs and performs the inherited P99 write flag-flip. Load-bearing invariants:

- **Outbound webhooks reuse the `OutboxEvent` outbox for FAN-OUT only.** A business write calls `enqueueWebhookEvent(tx, org, event)` → `integration.webhook.publish` OutboxEvent; the shared `/outbox/_drain` handler (`webhooks/fan-out.ts`) resolves matching enabled subs, redacts per-sub, persists one `WebhookDeliveryAttempt` each, enqueues `webhook.deliver` — **NO network I/O in the shared drain** (poison-isolated per sub). Per-delivery retry/DLQ is a DEDICATED drain (`/webhooks-outbound/_deliver` + `webhooks/dispatcher.ts`) with the webhook backoff `[1m,5m,30m,2h,12h,24h]` (max 6) → `webhook_failures` DLQ, per-sub 100/min limit (requeue-not-drop), 5-failures/1h Sentry alert.
- **Customer webhook URLs are hostile — SSRF-guard at BOTH subscribe AND dispatch.** `assertWebhookUrlSafe` blocks private/loopback/link-local/ULA/unspecified/CGNAT/metadata (IPv4/IPv6/IPv4-mapped) + HTTPS-only unless a per-sub override; `webhookAgentLookup` re-resolves + re-classifies at socket connect (DNS-rebind/TOCTOU); redirects never followed (`https.request`). Hand-rolled `lookup` hook — no new dependency.
- **`X-CO-Signature: t=…,v1=…` HMAC-SHA256 over `{t}.{body}`** with a 5-min replay window rejected BEFORE a `timingSafeEqual` compare. Sample verifiers ship in TS/Python/Go/PHP. The `whsec_` secret is AES-256-GCM encrypted at rest (recoverable, re-signed every delivery — NOT one-way hashed), key `WEBHOOK_SECRET_ENCRYPTION_KEY`, revealed once.
- **PII redaction is applied BEFORE the payload snapshot persists** (default `include_pii:false`), so no PII sits in a deliverable/DLQ row an operator could read.
- **Outbound models are NAME-DISTINCT from the inbound `WebhookDelivery`.** `WebhookSubscription`/`WebhookDeliveryAttempt`/`WebhookDeadLetter` (@@map `webhook_failures`) + `OutboundWebhookStatus` enum; never reuse the inbound model/enum/reaper. Live dispatch gated behind default-off `module.outbound-webhooks` (kill switch); everything else is buildable/testable against a local mock.
- **The P99 write flag-flip is GATED on the OWASP suite.** After `owasp-api-gate` (BOLA/BFLA/SSRF/mass-assignment/misconfig/injection) is GREEN, the 11 write routes are un-hidden in code (into the spec/SDK); the per-org `module.public-api` grant stays a MANUAL Unleash act (EXTERNAL-ENABLEMENT #8). `_initiatePayoutForRun` stays deferred. Detail: [[domains/outbound-webhooks]].

## Payroll export adapters (Phase 94, 2026-07-05)

- **`packages/payroll` is a payroll EXPORT profile-registry — a structural clone of `packages/einvoice`, NOT the payment-run bank-file factory** (`_generateExportFileForFormat` / `PaymentExportFormat`). `PayrollExportProfile` (`profileId`/`country`/`displayName`/`flagKey`/`generate`) + `PayrollFeed` DTO + registry + engine; each of 10 targets self-registers. Profiles are **pure `PayrollFeed → {buffer,ext,mime}`** (golden-fixture round-trip tested, no Prisma) — reuse the payment-export *idioms* (exceljs+BOM CSV, `escapeXml` XML builder, BACS/NACHA `padField`/length-guard for DATEV fixed-width) as per-format models, never overload the module. Adapters export employee master data; the incumbent computes gross→net and files (adviser-verify deferred, local-only).
- **The export feed source is a join of 3 already-shipped models — `Worker` + `EmployeeProfile` + `PersonnelFile` — with hire/termination anchors on `PersonnelFile.hireDate`/`.terminatedAt`, NOT EmployeeProfile.** `buildPayrollFeed` masks national IDs to last-4 by market (PL pesel / US ssn / SA iqama / AE emiratesId; DE svNummer + GB niNumber are market refs in `countryFields`); a full ID reveals only via the audited `employeePii:read` path when a format legally requires it (no v7.0 shipping format does — masked ships). No new Prisma model — the export is stateless + `writeAuditLog('payroll.export')`.
- **File-export is the v7.0 floor for all targets; native + strict-validation paths are flag-deferred with buildable-now fallbacks.** Gusto/QuickBooks native OAuth adapters live on `packages/integrations` (creds via `getServerEnv`, `GUSTO_*`/`QUICKBOOKS_*` optional) behind a DI bridge — enabled+connected pushes native, else the CSV export (the shipping path); ADP native → v7.1 (CSV ships); DATEVconnect REST + HMRC RTI XSD are non-throwing dark seams. 9 `payroll.*` flags gate per-adapter (added `payroll.sage-de` — Sage DE ≠ DATEV) on top of `module.workforce-employees`; the `payrollExport` router mounts in `conditionalWorkforceRouters` (METHOD_NOT_FOUND when off). Detail: [[domains/payroll-export]].

## HRIS two-way sync (Phase 95, 2026-07-05)

- **HRIS two-way sync is loop-free by a DISJOINT field partition (Phase 95).** The pull writes ONLY the HRIS-owned allowlist (`HrisWritableEmployeePatch` — invoice/payment/classification/compliance + `*Encrypted`/`*Last4` national-ID keys are physically absent from the type); the push carries ONLY CO-owned business events. No field is written by both directions, so a pull can never trigger a push and a push can never be echoed by a pull. `syncHash` makes the pull idempotent; `assertNotHrisOwnedField` fails loudly on any future overlap. Three corrections baked in: (C1) **`invoice.paid` is NOT an outbox event type** — Phase 95 added THREE new `hris.*.push` types + handlers + wired 3 EMPLOYEE-guarded producers (payment-run-ops + classification-submit); (C2) **one-HRIS-per-org is a raw-SQL PARTIAL unique index**, not a Prisma `@@unique` (which can't filter) — the predicate casts `provider::text IN ('PERSONIO','BAMBOOHR')` to dodge the Postgres new-enum-value-in-same-transaction restriction, P2002 → typed CONFLICT; (C3) the `IntegrationProvider` enum gained both values. Personio = non-OAuth client-credentials bearer (KSeF-shaped); BambooHR = OAuth 2.0 (Jira-shaped) with a `BAMBOOHR_CUSTOM_ATTR_VERIFIED`-gated custom-attribute path. Detail: [[domains/hris-sync]].

## Staff HR dashboard (Phase 97, 2026-07-05)

Theme B ships the read-only staff HR command surface (`hrDashboard.*` → web-vite `/dashboard/hr`), dark behind `module.hr-dashboard` (layered on `module.workforce-employees`). Load-bearing invariants:

- **The HR-dashboard RBAC gate is `requirePermission({ employee: ['read'] })` → the four HR roles ONLY (`hr_admin`, `hr_manager`, `payroll_officer`, `leave_approver`); owner is EXCLUDED** (the BFLA fence omits `employee` from `allPermissions`). It is NOT `report:read`. On the CLIENT this cannot be expressed via `usePermissions().can('employee', ['read'])` — the four HR roles are NOT in the `MemberRole` union (`role-normalization.ts`), so `can('employee', …)` returns `false` for every client role and would lock everyone out. The page + nav instead match the raw active-member role against the HR role set (`apps/web-vite/src/lib/hr-roles.ts` — `isHrDashboardRole`); the nav uses a new additive `NavItem.roles` predicate in `use-nav-items.ts`. The server stays the authoritative RBAC boundary.
- **The HR-DASH aggregation columns were ADDED additively in 97-01, NOT P90-promoted.** `EmployeeProfile.department`/`employmentType`(new `EmploymentType` enum)/`contractEndDate`/`probationEndsAt` + `PersonnelFileDocument.expiresAt`/`docCategory`(new `EmployeeDocCategory` enum), all nullable + org-composite-indexed, via the un-applied `__hr_dashboard_columns` migration (per-region apply DEFERRED — EXTERNAL-ENABLEMENT).
- **`getDocumentExpiry` composes the pure `@contractor-ops/compliance-policy` `daysUntilExpiryInTz` math (NOT the contractor `compliance-reminder-scan` cron)** and section-filters each row by `hasSectionPermission(ctx, section)` BEFORE deriving bands — payroll_officer sees only section C, leave_approver only A; a null (unclassified) section shows only to a caller who can read ALL four sections. The web-vite doc-expiry widget is a DUMB renderer of the server's already-filtered set — no client section logic.
- **The Gulf nationalisation rate is manual-input only (F3 anti-feature), for BOTH KSA and UAE.** `getNationalisationRollup` derives the rate ONLY from the manual `SaudiHeadcount` (never an `EmployeeProfile` groupBy); the band is read-through, never inferred. A country is present only when its manual headcount exists → the UI shows the "record manual headcount" prompt otherwise, never a computed rate. UAE returns `undefined` at HEAD (no UAE manual-headcount store — `SaudiHeadcount` has no country discriminator; the service supports UAE, the router surfaces the prompt). Detail: [[domains/hr-dashboard]].
