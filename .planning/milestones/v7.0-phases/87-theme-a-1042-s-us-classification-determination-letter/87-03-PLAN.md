---
phase: 87-theme-a-1042-s-us-classification-determination-letter
plan: 03
type: execute
wave: 3
depends_on: [87-01, 87-02]
files_modified:
  - packages/classification/src/profiles/us/rule-set.ts
  - packages/classification/src/profiles/us/scoring.ts
  - packages/classification/src/profiles/us/index.ts
  - packages/classification/src/types/outcome.ts
  - packages/classification/src/index.ts
  - packages/api/src/routers/compliance/classification-override.ts
autonomous: true
requirements: [US-CLASS-01, US-CLASS-02]

must_haves:
  truths:
    - "getProfileForCountry('US') resolves a registered US ClassificationProfile"
    - "Scoring combines the IRS common-law three-category federal base, a dispositive CA-ABC overlay when work-state=CA, and a §530 relief-eligibility flag"
    - "AB5 auto-flags on the engagement work-state (CA), falling back to the contractor P84 US state when work-state is unset"
    - "Classification override is reason-required and audit-logged"
    - "The outcome is advisory decision-support, never a legal verdict"
    - "Implements D-03 (US profile registerProfile), D-04 (AB5 work-state trigger), D-05 (advisory not-verdict), D-15 (audit + idempotency)"
  artifacts:
    - path: "packages/classification/src/profiles/us/scoring.ts"
      provides: "Pure scoreUsClassification (federal base + CA-ABC overlay + §530 flag)"
      contains: "scoreUsClassification"
    - path: "packages/classification/src/profiles/us/index.ts"
      provides: "UsClassificationProfile + registerProfile side-effect"
      contains: "registerProfile"
    - path: "packages/classification/src/types/outcome.ts"
      provides: "UsClassificationOutcome added to the Outcome union"
      contains: "US_CLASSIFICATION"
  key_links:
    - from: "packages/classification/src/profiles/us/index.ts"
      to: "packages/classification/src/registry.ts"
      via: "registerProfile(new UsClassificationProfile()) side-effect on import"
      pattern: "registerProfile\\(new UsClassificationProfile"
    - from: "packages/classification/src/profiles/us/scoring.ts"
      to: "engagement work-state"
      via: "AB5 overlay reads work-state, falls back to contractor US state"
      pattern: "workState"
---

<objective>
Author the US `ClassificationProfile`: the rule set (federal IRS common-law factors + CA ABC prongs + §530 conditions, with case-law citations and i18n prompts), the pure scoring (federal base → dispositive CA-ABC overlay when work-state=CA → §530 relief flag), and the registry plugin. Wire the AB5 work-state trigger + a reason-required, audit-logged override.

Purpose: US-CLASS-01 (the US rule set) + US-CLASS-02 (CA AB5 watchlist + stricter ABC + audited override). Turns Plan 01's `profiles/us` RED scaffolds GREEN.
Output: a registered US profile + the outcome union extension + override audit wiring.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-PATTERNS.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-RESEARCH.md

<interfaces>
<!-- Verified contracts — use directly, no codebase exploration needed. -->

ClassificationProfile (packages/classification/src/types/profile.ts:12-30):
  readonly profileId: string;  readonly country: string;  readonly displayName: string;
  readonly ruleSetVersion: string;  // frozen on submit (snapshot)
  buildAssessment(engagementId: string): AssessmentShell;
  scoreAssessment(answers: AnswerMap): Outcome;   // PURE, server-only — never import in a client bundle
  renderOutcome(assessment: Assessment): OutcomeView;

IR35Profile registration (profiles/ir35/index.ts:15-19, 62-63) — the shape to mirror:
  class IR35Profile implements ClassificationProfile { profileId='ir35'; country='GB'; ... }
  registerProfile(new IR35Profile());   // side-effect on import

Dispositive-first ordering (profiles/ir35/scoring.ts:89-137) — ORDER IS LOAD-BEARING:
  if (subVerdict === 'strong-inside' || mooVerdict === 'strong-inside') { overall='inside'; /*dispositive*/ }
  else if (subVerdict === 'strong-outside') { overall='outside'; }
  else { /* composite count of leaning signals */ }
  // reasoning string MUST cite the triggering rule verbatim; computedAt: new Date().toISOString()

Outcome union (types/outcome.ts:72-73):
  export type Outcome = Ir35Outcome | ScheinselbstandigkeitOutcome;  // + UsClassificationOutcome

getAnswerSchemaForType (schemas/answers.ts:27-44): exhaustive never-switch over AnswerType.
  REUSE 'yes-no' / 'score-0-3' / 'likert-5' / 'rationale' — do NOT add a new AnswerType (Pitfall 7).

applyTreaty override audit idiom (treaty-rate.service.ts) + writeAuditLog (audit-writer.ts) —
  override requires a non-empty reason → auditRequired=true → caller writeAuditLog.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: US rule set + outcome union + pure scoring</name>
  <files>packages/classification/src/profiles/us/rule-set.ts, packages/classification/src/profiles/us/scoring.ts, packages/classification/src/types/outcome.ts</files>
  <behavior>
    - scoreUsClassification(answers) returns { outcome: { kind:'US_CLASSIFICATION', ruleSetVersion, verdict, federal factor results, ab5Flag, section530ReliefEligible, computedAt }, reasoning }.
    - Federal base = IRS common-law three-category weighted composite (behavioral / financial / relationship) — NOT the DOL 2024 economic-reality rule (Pitfall 3; DOL surfaced only as adviser-context).
    - When work-state = CA, the CA-ABC overlay is dispositive: outcome defaults to likely-employee unless all three ABC prongs pass.
    - §530 surfaces as section530ReliefEligible FLAG on the result — it does NOT change the verdict (Pitfall 5).
    - reasoning cites the triggering rule verbatim (audit defence).
  </behavior>
  <read_first>
    - packages/classification/src/profiles/ir35/rule-set.ts (RULE_SET_VERSION const, CITE_* named citation consts, *_YES_DIRECTION influence map, *_QUESTIONS append-only IDs, prompt.en/pl/de with REVIEW: tokens)
    - packages/classification/src/profiles/scheinselbstandigkeit/rule-set.ts (a second weighted-criteria authoring example)
    - packages/classification/src/profiles/ir35/scoring.ts (dispositive-first ordering, ScoreIr35Result 59-62, computedAt 153)
    - packages/classification/src/types/outcome.ts (Outcome union 72-73; Ir35Outcome shape; OutcomeView verdict union)
    - packages/classification/src/schemas/answers.ts (getAnswerSchemaForType exhaustive switch — reuse existing AnswerTypes)
    - packages/classification/src/profiles/us/__tests__/rule-set.test.ts + scoring.test.ts (the RED scaffolds from Plan 01)
  </read_first>
  <action>
    Create `rule-set.ts`: `export const RULE_SET_VERSION = 'US-2026-COMMONLAW-AB5' as const` (frozen on submit). Named citation consts: IRS common-law (tc762 / SS-8), CA Labor Code 2775-2785 + 2776 (B2B, 12 criteria), §530 Revenue Act 1978. A `US_QUESTIONS` array with stable append-only IDs grouped into federal behavioral / financial / relationship factors, the three CA ABC prongs (A: free from control, B: outside usual course of business, C: independently established trade), and the §530 conditions (reporting consistency, substantive consistency, reasonable basis); reuse `yes-no`/`score-0-3`/`likert-5`/`rationale` answer types. `prompt.en` authoritative + en-US thin override + `prompt.de`/`prompt.pl`/`prompt.ar` (D-14 parity; REVIEW: tokens where adviser sign-off pending). A `*_YES_DIRECTION` influence map. Annotate every federal factor / AB5 prong / §530 condition adviser-verify + "subject to active rulemaking" where relevant (Pitfalls 3/4/5). Model AB5 exemptions (§2776 B2B / professional-services) as adviser-confirm FLAGS, never a single `ab5Exempt` boolean / auto-determination (Pitfall 5).
    Extend `types/outcome.ts`: add `UsClassificationOutcome` with `kind: 'US_CLASSIFICATION'`, `ruleSetVersion`, `verdict` (likely-employee | indeterminate | likely-contractor), `federalFactors` results, `ab5Flag`, `section530ReliefEligible`, `computedAt`. Add it to the `Outcome` union; widen `OutcomeView.verdict` if needed.
    Create `scoring.ts`: pure `scoreUsClassification(answers): { outcome, reasoning }`. Order is load-bearing: (1) compute the federal IRS common-law composite, (2) apply the CA-ABC overlay dispositively when work-state=CA (default employee unless all three prongs pass), (3) set `section530ReliefEligible` as a relief flag (never a verdict change). `reasoning` cites the triggering rule verbatim; `computedAt: new Date().toISOString()`. Pure + server-only — no DB, no client imports.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/classification test -- profiles/us</automated>
  </verify>
  <acceptance_criteria>
    - `profiles/us/scoring.test.ts` + `rule-set.test.ts` GREEN.
    - `grep -c "US_CLASSIFICATION" packages/classification/src/types/outcome.ts` ≥ 1; UsClassificationOutcome in the Outcome union.
    - scoring asserts: federal base is IRS common-law (not DOL 2024); CA-ABC overlay dispositive when work-state=CA; §530 is a flag, not a verdict change; reasoning cites the rule verbatim.
    - No new AnswerType added (grep confirms the exhaustive switch in answers.ts is unchanged).
    - `pnpm --filter @contractor-ops/classification i18n:parity` (or the repo i18n gate) passes for new keys.
  </acceptance_criteria>
  <done>US rule set + scoring + outcome union land; profiles/us tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Register the US profile + wire AB5 work-state trigger + audited override</name>
  <files>packages/classification/src/profiles/us/index.ts, packages/classification/src/index.ts, packages/api/src/routers/compliance/classification-override.ts</files>
  <read_first>
    - packages/classification/src/profiles/ir35/index.ts (the class + registerProfile side-effect shape)
    - packages/classification/src/index.ts (where profile modules are imported for side-effect registration)
    - packages/classification/src/registry.ts (getProfileForCountry resolution 47-60)
    - packages/api/src/routers/compliance/classification-shared.ts (getProfileForCountry(assignment.contractor.countryCode) — already multi-country, line ~157-193; resolveAssignmentAndProfile)
    - packages/api/src/routers/compliance/classification-draft.ts (createDraft writes countryCode + ruleSetVersion)
    - packages/api/src/services/audit-writer.ts (writeAuditLog signature; pass tx when in a transaction)
    - packages/api/src/services/treaty-rate.service.ts (the override-with-reason + auditRequired idiom to mirror)
  </read_first>
  <action>
    Create `profiles/us/index.ts`: `class UsClassificationProfile implements ClassificationProfile` with `profileId='us-classification'`, `country='US'`, `displayName='US Worker Classification (federal + CA AB5 + §530)'`, `ruleSetVersion=RULE_SET_VERSION`; `buildAssessment` returns the US questions shell; `scoreAssessment` delegates to `scoreUsClassification`; `renderOutcome` guards `outcome.kind !== 'US_CLASSIFICATION'`. End with `registerProfile(new UsClassificationProfile())` (side-effect on import). Add the import to `packages/classification/src/index.ts` so the profile registers at module load (mirror how ir35/schein register). No `registry.ts` change — `getProfileForCountry('US')` auto-resolves.
    AB5 work-state input: the engagement work-state (ContractorAssignment.workState from Plan 02) is the PRIMARY AB5 trigger; fall back to the contractor's P84 US state when work-state is unset (D-04, Pitfall 4 — AB5 governs work performed in CA, not residence). Feed this into the scoring answers/context where `resolveAssignmentAndProfile` selects the assignment + contractor (classification-shared.ts already selects `contractor.countryCode`; extend the select to include `workState` + the contractor US state).
    Override: add/extend a `classification-override` procedure (or extend the existing classification mutation) so an admin override requires a non-empty reason and calls `writeAuditLog({ action: 'classification.override', ... reason })`, mirroring the treaty/reverse-charge override-with-reason+audit pattern. Gate behind `classificationProcedure` (module.classification-engine) AND `assertUsExpansionEnabled` (Open Q1 — defense-in-depth, no new flag) for the US country.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/classification test && pnpm --filter @contractor-ops/api test -- classification && pnpm typecheck --filter=@contractor-ops/classification && pnpm typecheck --filter=@contractor-ops/api</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "registerProfile(new UsClassificationProfile" packages/classification/src/profiles/us/index.ts` ≥ 1.
    - A unit/integration test proves `getProfileForCountry('US')` resolves the US profile and that AB5 auto-flags when work-state=CA, falling back to contractor US state when work-state is unset.
    - The override path requires a non-empty reason and writes `writeAuditLog` with `action: 'classification.override'` (assert in test).
    - `pnpm typecheck --filter=@contractor-ops/classification` passes (the Outcome union extension is self-consistent in its own package).
    - `pnpm typecheck --filter=@contractor-ops/api` passes (Outcome union widening doesn't break consumers).
  </acceptance_criteria>
  <done>US profile registered + AB5 work-state trigger + audited override; classification + api suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → classification mutation | Override reason + outcome must not be client-asserted as a determination |
| classification scoring | Pure server-only; must never reach a client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-03-01 | Repudiation | AB5 override / classification change | mitigate | reason-required + `writeAuditLog({action:'classification.override'})` with reason + actor + tenant |
| T-87-03-02 | (compliance/liability) | Presenting advisory output as a legal verdict | mitigate | `SOFTWARE_NOT_LEGAL_ADVICE_EN` posture (D-05); AB5/§530 framed as flags; outcome never asserted as final determination |
| T-87-03-03 | Tampering / Spoofing | Mass-assignment of classification outcome via client | mitigate | outcome server-derived from `scoreAssessment`; `.strict()` Zod inputs; client cannot set the verdict |
| T-87-03-04 | Information Disclosure | Pure scoring leaking into a client bundle | mitigate | scoreAssessment stays in `packages/classification` server path; not imported by web-vite |
| T-87-03-SC | Tampering | npm/pip/cargo installs | accept | No new external packages |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/classification test` green (US profile + scoring + rule set).
- `pnpm --filter @contractor-ops/api test -- classification` green (AB5 trigger + audited override).
- `pnpm typecheck --filter=@contractor-ops/classification` + `--filter=@contractor-ops/api` green.
</verification>

<success_criteria>
- US-CLASS-01: US rule set scored deterministically (federal + CA-ABC overlay + §530 flag).
- US-CLASS-02: AB5 auto-flags on work-state=CA (contractor-state fallback); override reason-required + audit-logged.
</success_criteria>

<output>
Create `.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-03-SUMMARY.md` when done.
</output>
