---
phase: 87-theme-a-1042-s-us-classification-determination-letter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/classification/src/profiles/us/__tests__/rule-set.test.ts
  - packages/classification/src/profiles/us/__tests__/scoring.test.ts
  - packages/api/src/services/__tests__/form-1042s.service.test.ts
  - packages/api/src/services/__tests__/form-1042s-pdf.test.ts
  - packages/api/src/pdf-templates/__tests__/us-determination-letter.test.tsx
  - packages/api/src/services/__tests__/form-1099k-tracker.service.test.ts
  - packages/iris/src/__tests__/generator-1042s.test.ts
  - packages/iris/src/__tests__/validator-1042s.test.ts
  - packages/iris/src/schema-bundle/source.txt
  - packages/iris/src/schema-bundle/checksums.txt
autonomous: false
requirements: [US-FORM-06, US-CLASS-01, US-CLASS-02, US-CLASS-03, US-CLASS-04]

must_haves:
  truths:
    - "Every Phase 87 deterministic surface has a RED scaffold test that fails by resolution (the implementation does not exist yet)"
    - "The 1042-S IRIS Pub 1187 XSD is present in the iris schema-bundle and checksum-pinned before any 1042-S XML generator/validator is built"
    - "Implements D-01 (determination-letter scaffold), D-06 (Form1042S scaffold), D-08 (IRIS-reuse XSD pin)"
  artifacts:
    - path: "packages/classification/src/profiles/us/__tests__/rule-set.test.ts"
      provides: "RED scaffold for the US rule set (RULE_SET_VERSION, append-only question IDs, case-law/statute citations, adviser-verify annotations)"
      contains: "RULE_SET_VERSION"
    - path: "packages/classification/src/profiles/us/__tests__/scoring.test.ts"
      provides: "RED scaffold for US classification scoring (federal base + CA-ABC overlay + §530 flag)"
      contains: "scoreUsClassification"
    - path: "packages/api/src/services/__tests__/form-1042s.service.test.ts"
      provides: "RED scaffold for 1042-S box aggregation + §875(d) treaty gate + supersede + idempotency"
      contains: "form-1042s"
    - path: "packages/api/src/services/__tests__/form-1042s-pdf.test.ts"
      provides: "RED scaffold for the 1042-S recipient PDF render-from-snapshot + FTIN masking + CAS archive guard"
      contains: "1042-s"
    - path: "packages/api/src/pdf-templates/__tests__/us-determination-letter.test.tsx"
      provides: "RED scaffold for the byte-stable Determination-Letter template (TEMPLATE_VERSION/RENDERER_SLUG, no LLM path)"
      contains: "us-determination-letter"
    - path: "packages/api/src/services/__tests__/form-1099k-tracker.service.test.ts"
      provides: "RED scaffold for the 1099-K band tracker ($20,000 + 200 config, never-file invariant)"
      contains: "Form1099KBand"
    - path: "packages/iris/src/__tests__/generator-1042s.test.ts"
      provides: "RED scaffold for buildIris1042SXml (blocked on XSD download)"
      contains: "buildIris1042SXml"
    - path: "packages/iris/src/__tests__/validator-1042s.test.ts"
      provides: "RED scaffold for the 1042-S XSD validator (libxmljs2 nonet:true; XSD-blocked until Task 3)"
      contains: "nonet"
    - path: "packages/iris/src/schema-bundle/checksums.txt"
      provides: "1042-S XSD checksum entry pinned after human SOR download"
  key_links:
    - from: "packages/iris/src/__tests__/generator-1042s.test.ts"
      to: "packages/iris/src/generator.ts"
      via: "import buildIris1042SXml (resolution-fails until built)"
      pattern: "buildIris1042SXml"
---

<objective>
Lay the Wave-0 RED test scaffolds for every deterministic Phase 87 surface, and resolve the one human-only blocker: download + checksum-pin the 1042-S IRIS XSD (IRS Pub 1187) into `packages/iris/src/schema-bundle/`.

Purpose: Nyquist anchors. Each downstream plan turns its scaffold GREEN. The 1042-S XML generator/validator (Plan 07) cannot be GREEN until the Pub 1187 XSD is on disk and pinned — that download is IRS-SOR-login-only, so it is a human-action checkpoint here, mirroring P86's 86-01 XSD checkpoint.
Output: 8 failing test files (terminal-RED accepted) + an updated schema-bundle with the 1042-S XSD and a pinned checksum.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-RESEARCH.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-VALIDATION.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-PATTERNS.md

<interfaces>
<!-- RED-scaffold contracts the executor must encode, extracted from PATTERNS.md + verified scaffolds -->

P86 IRIS scaffold (the analog these mirror) — packages/iris/src/__tests__/generator.test.ts:
  import { buildIrisXml } from '../generator';  // resolution-fails until built
  // emits a Transmission Manifest carrying schema { versionNum, versionDt };
  // writes payee B-record; uses fast-xml-parser XMLBuilder (never string-concat); full TIN never leaks.

US ClassificationProfile scoring contract — mirror packages/classification/src/profiles/ir35/scoring.ts:
  scoreUsClassification(answers: AnswerMap): { outcome: UsClassificationOutcome; reasoning: string }
  // UsClassificationOutcome: { kind: 'US_CLASSIFICATION', ruleSetVersion, verdict, federal factor results, ab5Flag, section530ReliefEligible, computedAt }

Form1042S service contract — mirror packages/api/src/services/form-1099-nec.service.ts:
  // box-2 rate resolved via applyTreaty (§875(d) gate: W-8 chain complete → treaty rate, else 30% statutory)
  // CORRECTED = supersede ACTIVE→SUPERSEDED then insert ACTIVE in one $transaction
  // generateBatch idempotent via reserve/complete/clear; FTIN last-4 only in snapshot
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED scaffolds for classification + 1042-S + 1099-K + determination letter</name>
  <files>packages/classification/src/profiles/us/__tests__/rule-set.test.ts, packages/classification/src/profiles/us/__tests__/scoring.test.ts, packages/api/src/services/__tests__/form-1042s.service.test.ts, packages/api/src/services/__tests__/form-1042s-pdf.test.ts, packages/api/src/pdf-templates/__tests__/us-determination-letter.test.tsx, packages/api/src/services/__tests__/form-1099k-tracker.service.test.ts</files>
  <read_first>
    - packages/classification/src/profiles/ir35/scoring.ts (the scoring + dispositive-first ordering analog; ScoreIr35Result shape at lines 59-62)
    - packages/classification/src/profiles/ir35/__tests__ (existing classification test idiom)
    - packages/api/src/services/form-1099-nec.service.ts (1042-S service analog: supersede, idempotent batch, snapshot sanitizer)
    - packages/api/src/services/form-1099-nec-pdf.ts (recipient-PDF render/archive + CAS guard analog)
    - packages/api/src/pdf-templates/ir35-sds.tsx (determination-letter template analog; TEMPLATE_VERSION/RENDERER_SLUG)
    - apps/cron-worker/src/jobs/handlers/classification-economic-dependency.ts + the economic-dependency-scan service (1099-K tracker band/notification analog)
    - packages/api/src/services/treaty-rate.service.ts (applyTreaty decision shape for the §875(d) gate test)
  </read_first>
  <action>
    Create six failing test files that import the not-yet-built implementations so they fail by module resolution (terminal-RED, per VALIDATION.md Wave 0).
    - rule-set.test.ts: assert the US rule set exports `RULE_SET_VERSION` (a frozen const), a stable append-only question-ID array, and that case-law/statute citations (IRS common-law tc762/SS-8, CA Labor Code 2775-2785 / 2776, §530 Revenue Act 1978) are present; assert every federal factor / AB5 prong / §530 condition carries an adviser-verify annotation. Reuse existing AnswerTypes (`yes-no`/`score-0-3`/`likert-5`/`rationale`) — do NOT add a new AnswerType (Pitfall 7).
    - scoring.test.ts: assert `scoreUsClassification(answers)` returns `{ outcome: { kind: 'US_CLASSIFICATION', verdict, ab5Flag, section530ReliefEligible }, reasoning }`; assert the federal base is the IRS common-law three-category test (behavioral/financial/relationship) NOT the DOL 2024 rule; assert the CA-ABC overlay is dispositive when work-state=CA (defaults to employee unless all three prongs pass); assert §530 is a relief FLAG, not a verdict change; assert the reasoning string cites the triggering rule verbatim.
    - form-1042s.service.test.ts: assert box-2 rate is resolved via `applyTreaty` with a §875(d) gate (W-8 chain complete → treaty rate; incomplete → 30% statutory); assert CORRECTED = supersede ACTIVE→SUPERSEDED then insert ACTIVE in one `$transaction`; assert `generateBatch` is idempotent (reserve/complete/clear); assert the snapshot keeps FTIN last-4 only and strips forged full-identifier keys; assert routing reads `TaxFormSubmission.formType` (W-8 → 1042-S) not nationality (Pitfall 6).
    - form-1042s-pdf.test.ts: assert the recipient copy renders from the stored immutable snapshot (never a live recompute), FTIN masked to last-4, archive key prefix `1042-s/<orgId>/<id>.pdf`, and a CAS guard (`updateMany where pdfArchiveKey: null`, count===0 short-circuit).
    - us-determination-letter.test.tsx: assert a byte-stable render given a stable `renderedAt`, exports `TEMPLATE_VERSION` + `RENDERER_SLUG = 'us-determination-letter'`, renders verdict + per-factor evidence + AB5 (amber) + §530 (info) flags + citations, and includes the `SOFTWARE_NOT_LEGAL_ADVICE_EN` footer imported from `@contractor-ops/validators` (NOT a translation file); assert no LLM call path.
    - form-1099k-tracker.service.test.ts: assert band transition (SAFE→APPROACHING→OVER) against a tax-year-keyed config of $20,000 + 200 transactions (NOT the stale $5K/$600); assert same-band re-fire suppression via lastReminderAt; assert an invariant test that the scan NEVER files a 1099-K (informational only).
    These are RED scaffolds — they MUST fail (module not found / undefined export). Do not stub the implementations.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/classification test -- profiles/us 2>&1 | grep -qiE "cannot find module|failed to resolve|no test found|does not exist" && pnpm --filter @contractor-ops/api test -- form-1042s 2>&1 | grep -qiE "cannot find module|failed to resolve|does not exist"</automated>
  </verify>
  <acceptance_criteria>
    - All six test files exist and FAIL by module resolution (the targeted implementations are not yet on disk).
    - scoring.test.ts asserts the IRS common-law three-category base (not DOL 2024) and a dispositive CA-ABC overlay; form-1042s.service.test.ts asserts the §875(d) treaty/30% gate and the one-`$transaction` supersede.
    - No implementation files were created (RED-only).
  </acceptance_criteria>
  <done>Six RED scaffold tests committed; all fail by resolution; no implementations stubbed.</done>
</task>

<task type="auto">
  <name>Task 2: RED scaffolds for the 1042-S IRIS generator + validator (XSD-gated)</name>
  <files>packages/iris/src/__tests__/generator-1042s.test.ts, packages/iris/src/__tests__/validator-1042s.test.ts</files>
  <read_first>
    - packages/iris/src/__tests__/generator.test.ts (the P86 1099 RED scaffold — buildIrisXml contract: Transmission Manifest schema {versionNum,versionDt}, payee B-record, fast-xml-parser XMLBuilder, full TIN never leaks)
    - packages/iris/src/__tests__/validator.test.ts (the P86 validator RED scaffold)
    - packages/iris/src/schema-bundle/source.txt + README.md + checksums.txt (current bundle = 1099-NEC payload + Transmission Manifest only; 1042-S XSD is NOT present)
  </read_first>
  <action>
    Create two failing test files for the 1042-S IRIS path, mirroring the P86 1099 scaffolds but targeting a sibling `buildIris1042SXml` (Open Q2 recommendation: sibling builder, not a parameterized 1099 builder, because the 1042-S record layout differs materially — chapter 3/4 status codes, income codes, treaty fields).
    - generator-1042s.test.ts: import `buildIris1042SXml` from `../generator` (resolution-fails until built); assert it emits a Transmission Manifest carrying the schema version, writes a 1042-S payee record with the box fields (income code, gross income box 2, ch3/ch4 exemption+rate, federal tax withheld box 7, recipient 13j/13k status, 13n LOB, treaty article), uses `fast-xml-parser` XMLBuilder (never string-concat), and never leaks a full FTIN (last-4 only).
    - validator-1042s.test.ts: import the 1042-S validator (resolution-fails until built); assert it validates against the 1042-S XSD with `libxmljs2 { nonet: true }` (no SSRF/XXE) and returns structured errors on a malformed payload. Add a `// TODO(blocked): RED until the Pub 1187 1042-S XSD is bundled + pinned (Task 3)` marker so the executor knows this stays RED until Task 3 lands the XSD.
    These are RED scaffolds — both fail by resolution until Plan 07 builds the generator/validator AND Task 3 lands the XSD.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/iris test -- 1042s 2>&1 | grep -qiE "cannot find module|failed to resolve|does not exist"</automated>
  </verify>
  <acceptance_criteria>
    - generator-1042s.test.ts + validator-1042s.test.ts exist and FAIL by module resolution.
    - The validator scaffold targets the 1042-S XSD with `{ nonet: true }` and references the Pub 1187 bundle (not the 1099 XSD).
    - No generator/validator implementation files were created.
  </acceptance_criteria>
  <done>Two iris RED scaffolds committed; both fail by resolution; validator scaffold flagged XSD-blocked.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3 (checkpoint): Download + checksum-pin the 1042-S IRIS XSD (Pub 1187)</name>
  <action>HUMAN ACTION (no CLI/API path — IRS-SOR login required). The human performs the numbered steps in how-to-verify: download the Pub 1187 1042-S XSD from IRS-SOR, place it in `packages/iris/src/schema-bundle/`, update `source.txt`, and run the checksum-pin script so `checksums.txt` gains the 1042-S entry. Claude cannot fetch the login-gated file.</action>
  <what-built>The RED scaffolds reference the 1042-S IRIS XSD (Pub 1187), which is NOT in the schema-bundle (`source.txt` documents the current bundle as 1099-NEC payload + Transmission Manifest only). This XSD is a separate IRS-SOR-login-only download — Claude cannot fetch it.</what-built>
  <how-to-verify>
    1. Log in to the IRS Secure Object Repository (IRS-SOR / IRIS) with your IRS account.
    2. Download the **1042-S IRIS schema package (Publication 1187, filing-year 2026/TY2026)** — the 1042-S payload XSD + its Transmission Manifest XSD (distinct from the 1099-series Pub 1220/IRIS-1099 schema already bundled).
    3. Place the XSD file(s) under `packages/iris/src/schema-bundle/` alongside the existing 1099 schema files.
    4. Update `packages/iris/src/schema-bundle/source.txt` to record the 1042-S package provenance (IRS-SOR URL, download date, filing-year, "adviser-verify / re-verify TY2026 schema ~Nov 2026").
    5. Pin the checksum: `pnpm --filter @contractor-ops/iris exec tsx scripts/verify-iris-schema-checksums.ts --write` (mirror the P86 86-01 procedure) so `checksums.txt` gains a 1042-S XSD entry.
    6. Confirm `checksums.txt` now lists the 1042-S XSD with a SHA-256.
  </how-to-verify>
  <resume-signal>Type "approved" once the 1042-S XSD is in the bundle and `checksums.txt` lists it, or describe a blocker (e.g. SOR access pending — in which case Plan 07's generator/validator stay RED and the deterministic core proceeds without them).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IRS-SOR download → repo schema-bundle | Untrusted external schema file enters the build; supply-chain risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01-01 | Tampering | 1042-S XSD in schema-bundle | mitigate | SHA-256 checksum-pin in `checksums.txt`; CI guard fails on mismatch/missing/unlisted (mirror P86 validator-bundle) |
| T-87-01-SC | Tampering | npm/pip/cargo installs | accept | No new external packages — deterministic core composes existing workspace deps (`@react-pdf/renderer`, `fast-xml-parser`, `libxmljs2`, `zod`); RESEARCH Package Legitimacy Audit = none-new |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/classification test -- profiles/us` → RED (resolution)
- `pnpm --filter @contractor-ops/api test -- form-1042s` → RED (resolution)
- `pnpm --filter @contractor-ops/iris test -- 1042s` → RED (resolution)
- After the human checkpoint: `checksums.txt` contains a 1042-S XSD SHA-256 entry.
</verification>

<success_criteria>
- 8 RED scaffold tests on disk, all failing by resolution (terminal-RED accepted).
- 1042-S Pub 1187 XSD bundled + checksum-pinned (or blocker recorded, gating Plan 07 only).
</success_criteria>

<output>
Create `.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-01-SUMMARY.md` when done.
</output>
