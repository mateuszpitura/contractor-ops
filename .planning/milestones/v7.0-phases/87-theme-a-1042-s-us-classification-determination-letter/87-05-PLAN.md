---
phase: 87-theme-a-1042-s-us-classification-determination-letter
plan: 05
type: execute
wave: 4
depends_on: [87-01, 87-02, 87-03]
files_modified:
  - packages/api/src/pdf-templates/us-determination-letter.tsx
  - packages/api/src/services/classification-document-render.ts
  - packages/api/src/routers/compliance/classification-document.tsx
autonomous: true
requirements: [US-CLASS-04]

must_haves:
  truths:
    - "A Classification Determination Letter PDF generates deterministically from the frozen US classification assessment snapshot (no LLM)"
    - "The letter renders the verdict, scored federal factors, CA-AB5 + §530 flags, and case-law citations"
    - "The letter carries the SOFTWARE_NOT_LEGAL_ADVICE_EN advisory footer"
    - "The document is stored append-only as a US_DETERMINATION_LETTER ClassificationDocument with ruleSetVersion frozen, and letter generation is audit-logged"
    - "Implements D-01 (deterministic React-PDF no-LLM), D-02 (US_DETERMINATION_LETTER kind), D-05 (advisory footer), D-15 (audit)"
  artifacts:
    - path: "packages/api/src/pdf-templates/us-determination-letter.tsx"
      provides: "deterministic Determination-Letter react-pdf template (mirror ir35-sds)"
      contains: "us-determination-letter"
    - path: "packages/api/src/services/classification-document-render.ts"
      provides: "renderDeterminationLetterPdfBuffer (mirror renderSdsPdfBuffer)"
      contains: "US_DETERMINATION_LETTER"
  key_links:
    - from: "packages/api/src/services/classification-document-render.ts"
      to: "packages/api/src/pdf-templates/us-determination-letter.tsx"
      via: "renderToBuffer(UsDeterminationLetterDocument(...))"
      pattern: "UsDeterminationLetterDocument"
    - from: "packages/api/src/pdf-templates/us-determination-letter.tsx"
      to: "@contractor-ops/validators SOFTWARE_NOT_LEGAL_ADVICE_EN"
      via: "imported locked disclaimer phrase"
      pattern: "SOFTWARE_NOT_LEGAL_ADVICE_EN"
---

<objective>
Build the Classification Determination Letter as a deterministic react-pdf template mirroring the v5.0 UK SDS generator (D-01 — no LLM). Render verdict + scored factors + AB5/§530 flags + citations from the frozen US assessment snapshot, archive it as a `US_DETERMINATION_LETTER` ClassificationDocument with `ruleSetVersion` frozen, audit-log generation, and wire the document router for the new kind.

Purpose: US-CLASS-04. Turns Plan 01's `us-determination-letter` RED scaffold GREEN. Depends on Plan 03's `UsClassificationOutcome` (the snapshot shape it renders).
Output: the letter template + render/archive extension + document-router wiring.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-PATTERNS.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-UI-SPEC.md

<interfaces>
<!-- Verified analog idioms — use directly. -->

ir35-sds.tsx module contract (1-19):
  export const TEMPLATE_VERSION = 1 as const;
  export const RENDERER_SLUG = 'ir35-sds' as const;
  // Reads ONLY assessment.outcome + questionsSnapshot + engagement context.
  // NEVER imports live rule-set constants (only TYPES). Locked phrases from @contractor-ops/validators.
  // Byte stability: caller provides a stable renderedAt. VERDICT_PILLS / AREA_PILLS / styles 43-120.

classification-document-render.ts renderSdsPdfBuffer (78-187):
  findFirstOrThrow assessment (org-scoped) → guard status==='COMPLETED' && questionsSnapshot !== null
  → guard outcome.kind === <discriminant> → const { renderToBuffer } = await import('@react-pdf/renderer') (lazy)
  → buffer = await renderToBuffer(<Document>(...)) → sha256 → buildClassificationDocumentKey({ kind, ruleSetVersion, sha256 })
  → append-only ClassificationDocument row (kind/pdfKey/sha256Hash/byteSize/rendererVersion/ruleSetVersion/generatedByUserId)
  // REACT_PDF_VERSION = '3.4.5' (line 45) is a STALE constant label — the real dep is @react-pdf/renderer ^4.5.1.
  // sdsApproval lookup (104-110) — the SDS server approval gate analog.

disclaimers.ts: SOFTWARE_NOT_LEGAL_ADVICE_EN (82-94) + SDS_APPROVAL_STATEMENT_EN (64-68).
  LOCKED phrases — MUST NOT be translated or moved into messages/*.json (CI guard locked-phrases-guard.test.ts).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: us-determination-letter.tsx template (deterministic, no LLM)</name>
  <files>packages/api/src/pdf-templates/us-determination-letter.tsx</files>
  <behavior>
    - Byte-stable render given a stable renderedAt.
    - Exports TEMPLATE_VERSION + RENDERER_SLUG = 'us-determination-letter'.
    - Renders verdict pill (likely-employee=destructive tone / indeterminate=warning / likely-contractor=success), per-factor evidence for the federal categories, a CA-AB5 amber chip, a §530 info chip, and case-law citations.
    - Footer = SOFTWARE_NOT_LEGAL_ADVICE_EN imported from @contractor-ops/validators (NOT a translation file).
    - No LLM call path; reads ONLY assessment.outcome + questionsSnapshot + engagement context (TYPES only, no live rule-set constants).
  </behavior>
  <read_first>
    - packages/api/src/pdf-templates/ir35-sds.tsx (verbatim shape: TEMPLATE_VERSION/RENDERER_SLUG 1-19, VERDICT_PILLS/AREA_PILLS/styles 43-120)
    - packages/validators/src/legal/disclaimers.ts (SOFTWARE_NOT_LEGAL_ADVICE_EN 82-94; SDS_APPROVAL_STATEMENT_EN 64-68; locked-phrase guard 5-8)
    - packages/classification/src/types/outcome.ts (UsClassificationOutcome shape from Plan 03 — verdict, federalFactors, ab5Flag, section530ReliefEligible)
    - packages/api/src/pdf-templates/__tests__/us-determination-letter.test.tsx (the RED scaffold from Plan 01)
    - .planning/phases/87-.../87-UI-SPEC.md (verdict tone reconciliation: AB5 amber never red, §530 info blue)
  </read_first>
  <action>
    Create `us-determination-letter.tsx` mirroring `ir35-sds.tsx`. Export `TEMPLATE_VERSION = 1 as const` + `RENDERER_SLUG = 'us-determination-letter' as const`. Verdict-first layout: a verdict pill using the v5.0 `toneForOutcome` mapping (likely-employee=destructive, indeterminate=warning, likely-contractor=success — a likely-employee outcome is a genuine risk signal, consistent with IR35 `inside`); per-factor evidence sections for the federal behavioral/financial/relationship categories; a CA-AB5 amber `warning` chip (NEVER destructive — D-04/UI-SPEC) and a §530 `info` blue relief chip beside the verdict; case-law/statute citations (IRS common-law, CA Labor Code 2775-2785/2776, §530). Footer = `SOFTWARE_NOT_LEGAL_ADVICE_EN` imported from `@contractor-ops/validators` (locked phrase — never a translation file). Add the D-13 adviser-verify note. Reads ONLY `assessment.outcome` + `questionsSnapshot` + engagement context (TYPES only — never import live rule-set constants). `@react-pdf/renderer ^4.5.1` API (not v3). NO LLM (D-01 — explicitly forbidden).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- us-determination-letter && pnpm --filter @contractor-ops/validators test -- locked-phrases-guard</automated>
  </verify>
  <acceptance_criteria>
    - `us-determination-letter.test.tsx` GREEN (byte-stable render given a stable renderedAt).
    - `grep -c "SOFTWARE_NOT_LEGAL_ADVICE_EN" packages/api/src/pdf-templates/us-determination-letter.tsx` ≥ 1; imported from `@contractor-ops/validators`.
    - `grep -c "RENDERER_SLUG = 'us-determination-letter'" packages/api/src/pdf-templates/us-determination-letter.tsx` ≥ 1.
    - No LLM/network call in the template; locked-phrases-guard still passes.
  </acceptance_criteria>
  <done>Determination-letter template GREEN; byte-stable; locked footer; no LLM.</done>
</task>

<task type="auto">
  <name>Task 2: renderDeterminationLetterPdfBuffer + document-router wiring</name>
  <files>packages/api/src/services/classification-document-render.ts, packages/api/src/routers/compliance/classification-document.tsx</files>
  <read_first>
    - packages/api/src/services/classification-document-render.ts (renderSdsPdfBuffer 78-187 — the verbatim render/archive idiom; buildClassificationDocumentKey; sdsApproval lookup 104-110)
    - packages/api/src/routers/compliance/classification-document.tsx (the existing document router — generate/list procedures; how the SDS kind is generated + listed)
    - packages/api/src/services/audit-writer.ts (writeAuditLog)
    - packages/api/src/middleware/require-us-expansion-flag.ts (assertUsExpansionEnabled)
  </read_first>
  <action>
    Add `renderDeterminationLetterPdfBuffer` to `classification-document-render.ts` mirroring `renderSdsPdfBuffer`: org-scoped `findFirstOrThrow` the assessment (include engagement/contractor/organization), guard `status === 'COMPLETED' && questionsSnapshot !== null`, guard `outcome.kind === 'US_CLASSIFICATION'`, lazy `const { renderToBuffer } = await import('@react-pdf/renderer')`, `renderToBuffer(UsDeterminationLetterDocument({ assessment, engagement, contractor, organization, renderedAt }))`, sha256 → `buildClassificationDocumentKey({ kind: 'US_DETERMINATION_LETTER', ruleSetVersion: assessment.ruleSetVersion, sha256 })` → append-only `ClassificationDocument` row (kind/pdfKey/sha256Hash/byteSize/rendererVersion/ruleSetVersion/generatedByUserId). `ruleSetVersion` is frozen from the assessment (the snapshot), not recomputed. `writeAuditLog({ action: 'classification.determinationLetter.generate' })`. Decide the approval gate: mirror the SDS server approval lookup (the UI-SPEC adds an SDS-mirror typed-name + checkbox gate at the button layer in Plan 08; if the SDS path requires a server approval row, mirror it — otherwise the typed-name gate stays UI-side and the server records `generatedByUserId`).
    Wire `classification-document.tsx` to handle the `US_DETERMINATION_LETTER` kind in its generate + list procedures (staff-generated + downloadable like the SDS; no contractor self-generation), gated behind `classificationProcedure` AND `assertUsExpansionEnabled` for the US country.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- classification-document-render && pnpm typecheck --filter=@contractor-ops/api && pnpm --filter @contractor-ops/api lint:audit-log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "US_DETERMINATION_LETTER" packages/api/src/services/classification-document-render.ts` ≥ 1.
    - A test proves the letter row is append-only, `ruleSetVersion` is frozen from the assessment, and generation writes `writeAuditLog` with the determination-letter action.
    - The document router lists + generates the `US_DETERMINATION_LETTER` kind (staff-only) gated by us-expansion.
    - `pnpm typecheck --filter=@contractor-ops/api` + `lint:audit-log` pass.
  </acceptance_criteria>
  <done>Render/archive + document-router wiring GREEN; append-only + frozen ruleSetVersion + audit proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → letter-generate procedure | Staff-only; no contractor self-generation; ruleSetVersion server-frozen |
| assessment snapshot → PDF | Letter must render from the frozen snapshot, never a live re-score |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-05-01 | Tampering | Letter content diverging from the assessed outcome | mitigate | renders from the frozen assessment snapshot + frozen ruleSetVersion; append-only ClassificationDocument |
| T-87-05-02 | Repudiation | Unaudited letter generation | mitigate | `writeAuditLog({action:'classification.determinationLetter.generate'})` with actor + tenant |
| T-87-05-03 | (compliance/liability) | Letter asserted as a final legal determination | mitigate | `SOFTWARE_NOT_LEGAL_ADVICE_EN` footer (locked phrase) + adviser-verify note; AB5/§530 framed as flags; NO LLM-asserted verdict (D-01) |
| T-87-05-04 | Elevation | Contractor self-generating a letter | mitigate | staff-router only; classificationProcedure + assertUsExpansionEnabled gate |
| T-87-05-SC | Tampering | npm/pip/cargo installs | accept | No new external packages |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- us-determination-letter` + `classification-document-render` green.
- `pnpm --filter @contractor-ops/validators test -- locked-phrases-guard` green.
- `pnpm typecheck --filter=@contractor-ops/api` + `lint:audit-log` green.
</verification>

<success_criteria>
- US-CLASS-04: a deterministic Determination Letter PDF generates from the frozen assessment (verdict + factors + AB5/§530 flags + citations + locked advisory footer), stored append-only with a frozen ruleSetVersion, audit-logged.
</success_criteria>

<output>
Create `.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-05-SUMMARY.md` when done.
</output>
