---
phase: 87-theme-a-1042-s-us-classification-determination-letter
plan: 07
type: execute
wave: 4
depends_on: [87-01, 87-02, 87-04]
files_modified:
  - packages/iris/src/generator.ts
  - packages/iris/src/validator.ts
  - packages/iris/src/index.ts
  - packages/api/src/services/form-1042s-transmit.service.ts
  - packages/api/src/routers/finance/form-1042s-router.ts
autonomous: true
requirements: [US-FORM-06]

must_haves:
  truths:
    - "buildIris1042SXml produces a 1042-S IRIS payload (Transmission Manifest + 1042-S payee record) using fast-xml-parser XMLBuilder, never string concatenation"
    - "The 1042-S payload validates against the Pub 1187 XSD with libxmljs2 { nonet: true } (no SSRF/XXE)"
    - "1042-S transmit routes through the P86 TaxFilingTransmitter seam (ManualDownload default / IrisA2A dark behind module.iris-efile / Vendor stub) and the single ack parser, parameterized by form-type"
    - "Full FTIN never leaks into the XML — last-4 only"
    - "1042-S transmit is idempotent and audit-logged"
    - "Implements D-08 (reuse P86 IRIS transmitter seam, A2A dark), D-12 (us-expansion gating), D-15 (idempotency)"
  artifacts:
    - path: "packages/iris/src/generator.ts"
      provides: "buildIris1042SXml sibling builder"
      contains: "buildIris1042SXml"
    - path: "packages/iris/src/validator.ts"
      provides: "1042-S XSD validation ({ nonet: true })"
      contains: "nonet"
    - path: "packages/api/src/services/form-1042s-transmit.service.ts"
      provides: "1042-S transmit via the P86 transmitter seam + ack parser (form-type parameterized)"
      contains: "TaxFilingTransmitter"
  key_links:
    - from: "packages/api/src/services/form-1042s-transmit.service.ts"
      to: "packages/api/src/services/tax-filing-transmitter.ts (P86)"
      via: "form-type-parameterized transmit tail + iris-ack-parser"
      pattern: "TaxFilingTransmitter"
    - from: "packages/iris/src/validator.ts"
      to: "packages/iris/src/schema-bundle (1042-S Pub 1187 XSD)"
      via: "libxmljs2 { nonet: true } validation against the checksum-pinned XSD"
      pattern: "nonet"
---

<objective>
Build the 1042-S IRIS XML generator + validator (sibling `buildIris1042SXml` against the Pub 1187 XSD bundled in Plan 01) and the form-type-parameterized transmit reuse of the P86 `TaxFilingTransmitter` seam + ack parser.

Purpose: US-FORM-06 (the IRS-file half). Turns Plan 01's `generator-1042s` + `validator-1042s` RED scaffolds GREEN.

**CROSS-PHASE DEPENDENCY (declared, load-bearing):** The P86 transmitter seam + ack parser + the base IRIS generator are NOT on disk — they are P86 Wave-0 `it.todo` / RED scaffolds (`packages/iris/src/generator.ts buildIrisXml`, `validator.ts`, `packages/api/src/services/tax-filing-transmitter.ts`, `iris-ack-parser.ts`). `IrisSubmission`/`IrisAck` Prisma models DO exist. This plan's TRANSMIT task (Task 2) requires those P86 seams to land GREEN (P86 Wave 2+) first. The GENERATOR/VALIDATOR task (Task 1) also requires the Plan 01 human XSD checkpoint to be resolved. If either is unresolved at execution time, the orchestrator holds Task 2 (transmit) — the deterministic core (Plans 03–06) and the 1042-S model/service/PDF (Plan 04) are already GREEN regardless. Do NOT duplicate/rebuild the P86 seam — parameterize it by form-type when it lands.

Output: the 1042-S XML generator/validator + the transmit service + router wiring.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-PATTERNS.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-RESEARCH.md

<interfaces>
<!-- Contracts from the P86 RED scaffolds + the 1042-S RED scaffolds (Plan 01). The P86 generator/transmitter/ack-parser may not be on disk yet — see the cross-phase note. -->

P86 generator scaffold (packages/iris/src/__tests__/generator.test.ts):
  import { buildIrisXml } from '../generator';  // 1099 builder
  // Transmission Manifest carrying schema { versionNum, versionDt }; payee B-record;
  // fast-xml-parser XMLBuilder (never string-concat); full TIN never leaks (last-4 only).

P86 validator scaffold: libxmljs2 { nonet: true } (no SSRF/XXE), noent:false (no XXE), self-contained bundle.

P86 transmitter scaffold (packages/api/src/services/__tests__/tax-filing-transmitter.test.ts — it.todo):
  TaxFilingTransmitter seam — ManualDownload (default, no TCC) / IrisA2A (dark, module.iris-efile) / Vendor (stub).

P86 ack-parser scaffold (iris-ack-parser.test.ts — it.todo):
  single parser → 6 IrisAckStatus + Error Information Group + OriginalReceiptId → IrisAck row + audit.

IrisSubmission / IrisAck / IrisAckStatus Prisma models EXIST (tax.prisma:180-217).

1042-S RED scaffolds (Plan 01): packages/iris/src/__tests__/generator-1042s.test.ts + validator-1042s.test.ts.
1042-S XSD: bundled + checksum-pinned in Plan 01 Task 3 (human IRS-SOR download). RED until that resolves.

idempotency.ts (reserve/complete/clear) + writeAuditLog (audit-writer.ts) — for transmit dedupe + audit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: buildIris1042SXml + 1042-S XSD validator (sibling builder)</name>
  <files>packages/iris/src/generator.ts, packages/iris/src/validator.ts, packages/iris/src/index.ts</files>
  <read_first>
    - packages/iris/src/__tests__/generator-1042s.test.ts + validator-1042s.test.ts (the Plan 01 RED scaffolds — the contract)
    - packages/iris/src/__tests__/generator.test.ts + validator.test.ts (the P86 1099 scaffolds — XMLBuilder + { nonet: true } idiom; if P86's generator.ts/validator.ts have landed, read them for the shared Transmission Manifest helper)
    - packages/iris/src/schema-bundle/ (the 1042-S Pub 1187 XSD + checksums.txt from Plan 01 Task 3)
    - packages/einvoice validator-bundle (the checksum-guard precedent if needed)
  </read_first>
  <action>
    Implement a sibling `buildIris1042SXml` in `packages/iris/src/generator.ts` (Open Q2 recommendation — sibling builder, NOT a parameterized 1099 builder, because the 1042-S record layout differs materially: chapter 3/4 status codes, income codes, treaty fields). Share the Transmission Manifest helper with the P86 `buildIrisXml` (if it has landed) but use a separate 1042-S B-record builder. Use `fast-xml-parser ^5.7.3` XMLBuilder (NEVER string concatenation — entity-escape bugs surface as opaque XSD failures). Emit the box fields (income code, gross income box 2, ch3/ch4 exemption + rate, federal tax withheld box 7, recipient 13j/13k status, 13n LOB, treaty article). Full FTIN never in the XML — last-4 only. Income/status/exemption code numbers are adviser-verify at coding time (A1/A2 — verify against i1042s Appendix B/C).
    Implement the 1042-S validation in `packages/iris/src/validator.ts` against the bundled Pub 1187 XSD using `libxmljs2 ^0.37.0 { nonet: true }` (no SSRF) + `noent: false` (no XXE), reading the checksum-pinned schema-bundle. Returns structured errors on a malformed payload. Export both from `packages/iris/src/index.ts`.
    **Gate:** if Plan 01 Task 3's human XSD checkpoint is unresolved (no 1042-S XSD in the bundle), this task stays RED — surface that to the orchestrator and do not fabricate an XSD.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/iris test -- 1042s</automated>
  </verify>
  <acceptance_criteria>
    - `generator-1042s.test.ts` + `validator-1042s.test.ts` GREEN (requires the Plan 01 XSD checkpoint resolved).
    - `grep -c "buildIris1042SXml" packages/iris/src/generator.ts` ≥ 1; `grep -c "nonet" packages/iris/src/validator.ts` ≥ 1.
    - The generator uses `fast-xml-parser` XMLBuilder (no string-concatenated XML); full FTIN never appears in the emitted payload (last-4 only — assert in test).
  </acceptance_criteria>
  <done>1042-S XML generator + validator GREEN (XSD-gated); XMLBuilder + { nonet: true } + last-4 proven.</done>
</task>

<task type="auto">
  <name>Task 2: 1042-S transmit via the P86 TaxFilingTransmitter seam (form-type parameterized)</name>
  <files>packages/api/src/services/form-1042s-transmit.service.ts, packages/api/src/routers/finance/form-1042s-router.ts</files>
  <read_first>
    - packages/api/src/services/__tests__/tax-filing-transmitter.test.ts (the P86 seam contract — ManualDownload/IrisA2A/Vendor; if tax-filing-transmitter.ts has landed, read it)
    - packages/api/src/services/__tests__/iris-ack-parser.test.ts (the ack-parser contract — 6 statuses, Error Information Group, OriginalReceiptId; if iris-ack-parser.ts has landed, read it)
    - packages/db/prisma/schema/tax.prisma (IrisSubmission / IrisAck / IrisAckStatus 180-217)
    - packages/api/src/lib/idempotency.ts + packages/api/src/services/audit-writer.ts
    - packages/api/src/routers/finance/form-1042s-router.ts (the staff router from Plan 04 — add the transmit + ack-upload procedures here)
    - packages/api/src/middleware/require-us-expansion-flag.ts
  </read_first>
  <action>
    **CROSS-PHASE GATE:** Confirm the P86 `tax-filing-transmitter.ts` + `iris-ack-parser.ts` are on disk + GREEN. If they are NOT (still `it.todo`), STOP and surface to the orchestrator — do NOT rebuild the P86 seam; this task is held until P86 lands them. (The deterministic core is already GREEN, so the phase is not blocked overall.)
    Once the seam is GREEN: create `form-1042s-transmit.service.ts` that routes 1042-S through the SAME `TaxFilingTransmitter` seam parameterized by form-type (ManualDownload default → download the validated XML; IrisA2A dark behind `module.iris-efile`; Vendor stub) and the SAME single ack parser (6 `IrisAckStatus`). Persist via the existing `IrisSubmission` / `IrisAck` models. Transmit is idempotent (`idempotency.ts` reserve/complete/clear) + `writeAuditLog({ action: 'form1042s.transmit' })`. Build the 1042-S payload via `buildIris1042SXml` (Task 1) + validate via the 1042-S validator before transmit; a validation failure surfaces the structured errors and never transmits.
    Add the transmit + ack-upload procedures to `form-1042s-router.ts` (Zod `.strict()`, tenant from session, `assertUsExpansionEnabled`). The ManualDownload path is the default (no TCC); A2A stays dark behind `module.iris-efile` (no new flag, D-08).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- form-1042s-transmit && pnpm typecheck --filter=@contractor-ops/api && pnpm --filter @contractor-ops/api lint:audit-log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TaxFilingTransmitter" packages/api/src/services/form-1042s-transmit.service.ts` ≥ 1 (reuses the P86 seam, not a rebuild).
    - A test proves: ManualDownload default produces the validated XML; transmit is idempotent + writes `writeAuditLog({action:'form1042s.transmit'})`; a validation failure surfaces errors and never transmits; the ack parser maps the 6 statuses to an IrisAck row.
    - If the P86 seam is not on disk, the SUMMARY records the cross-phase HOLD (no rebuild) and the rest of the phase remains GREEN.
    - `pnpm typecheck --filter=@contractor-ops/api` + `lint:audit-log` pass.
  </acceptance_criteria>
  <done>1042-S transmit reuses the P86 seam (form-type parameterized) + ack parser; idempotent + audited; OR cross-phase HOLD recorded if the P86 seam is unlanded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 1042-S XSD bundle → validation | Tampered/unpinned XSD silently changes "valid" |
| XSD `<xs:import schemaLocation>` → network | XXE / SSRF on validation |
| client → transmit procedure | Box amounts already server-derived; transmit must be idempotent + audited |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-07-01 | Tampering | 1042-S XSD silently changed | mitigate | SHA-256 checksum-pin (Plan 01); CI guard fails on mismatch/missing/unlisted |
| T-87-07-02 | Info Disclosure / SSRF | XXE / SSRF via XSD import | mitigate | `libxmljs2 { nonet: true }` (no network) + `noent: false` (no entity expansion); self-contained bundle (P86 pattern, safe-by-construction) |
| T-87-07-03 | Information Disclosure | Full FTIN in the IRIS XML | mitigate | last-4 only in the payload; assert in generator test |
| T-87-07-04 | Tampering / Repudiation | Double-transmit on retry | mitigate | `idempotency.ts` reserve/complete/clear; `writeAuditLog({action:'form1042s.transmit'})` |
| T-87-07-05 | Tampering | String-concatenated XML entity-escape bugs | mitigate | `fast-xml-parser` XMLBuilder only — no string concat |
| T-87-07-SC | Tampering | npm/pip/cargo installs | accept | No new external packages — reuse fast-xml-parser ^5.7.3 + libxmljs2 ^0.37.0 already in packages/iris (P86) |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/iris test -- 1042s` green (generator + validator; XSD-gated).
- `pnpm --filter @contractor-ops/api test -- form-1042s-transmit` green (or cross-phase HOLD recorded).
- `pnpm typecheck --filter=@contractor-ops/api` + `lint:audit-log` green.
</verification>

<success_criteria>
- US-FORM-06 (IRS-file half): a validated 1042-S IRIS payload builds + validates against the Pub 1187 XSD and transmits through the P86 seam (ManualDownload default, A2A dark), idempotent + audited — OR a clean cross-phase HOLD on the unlanded P86 seam with the deterministic core intact.
</success_criteria>

<output>
Create `.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-07-SUMMARY.md` when done.
</output>
