---
phase: 87-theme-a-1042-s-us-classification-determination-letter
plan: 02
type: execute
wave: 2
depends_on: [87-01]
files_modified:
  - packages/db/prisma/schema/tax.prisma
  - packages/db/prisma/schema/classification.prisma
  - packages/db/prisma/schema/contractor.prisma
  - packages/db/prisma/seed/tax-1099k-threshold.ts
  - packages/db/src/tenant.ts
  - packages/db/src/__tests__/cross-org-leak-1042s.test.ts
autonomous: false
requirements: [US-FORM-06, US-CLASS-01, US-CLASS-02, US-CLASS-03, US-CLASS-04]

must_haves:
  truths:
    - "Form1042S exists as an immutable + supersede-chain model with box-2 withholding and box-5/treaty-article snapshot, FTIN last-4 only"
    - "ClassificationDocument supports a US_DETERMINATION_LETTER kind"
    - "ContractorAssignment carries an engagement work-state field (AB5 trigger)"
    - "Form1099KTrackerState (per contractor per tax-year) and a tax-year-keyed Tax1099KThreshold config exist"
    - "Every new tenant-owning model is absent from globalModels and has a cross-org leak test"
    - "A multi-region migration (EU/ME/US) is applied at a human gate before downstream plans build against the models"
    - "Implements D-02 (US_DETERMINATION_LETTER kind), D-04 (workState AB5 trigger), D-06 (Form1042S immutable-supersede), D-10 (Form1099KTrackerState), D-11 (Tax1099KThreshold config), D-13 (immutable adviser-verify)"
  artifacts:
    - path: "packages/db/prisma/schema/tax.prisma"
      provides: "Form1042S, Form1099KTrackerState, Tax1099KThreshold models + Form1042SStatus enum"
      contains: "model Form1042S"
    - path: "packages/db/prisma/schema/classification.prisma"
      provides: "US_DETERMINATION_LETTER ClassificationDocumentKind + any US-assessment fields"
      contains: "US_DETERMINATION_LETTER"
    - path: "packages/db/prisma/schema/contractor.prisma"
      provides: "ContractorAssignment work-state field"
      contains: "workState"
    - path: "packages/db/src/__tests__/cross-org-leak-1042s.test.ts"
      provides: "cross-org isolation test for Form1042S + Form1099KTrackerState"
      contains: "organizationId"
  key_links:
    - from: "packages/db/prisma/schema/tax.prisma Form1042S"
      to: "packages/db/src/tenant.ts globalModels"
      via: "NEGATIVE assertion — Form1042S/Form1099KTrackerState must NOT be in globalModels"
      pattern: "globalModels"
---

<objective>
Add the phase's Prisma surface and apply the multi-region migration: `Form1042S` (immutable + supersede, box-2 withholding + treaty-article snapshot), the `US_DETERMINATION_LETTER` ClassificationDocument kind, an engagement work-state field on `ContractorAssignment` (AB5 trigger, D-04), `Form1099KTrackerState` (band state per contractor/tax-year, D-10), and a tax-year-keyed `Tax1099KThreshold` config (D-11). Apply the additive migration across EU/ME/US at a human gate.

Purpose: every Wave-2+ deterministic surface (classification AB5, 1042-S service, 1099-K tracker, determination letter) builds against these models. The `ContractorAssignment.workState` field (D-04) is the persistence foundation for the US-CLASS-01 AB5 work-state trigger consumed in Plan 03. The migration mutates live regional DBs, so it stays a human gate (P83–86 convention).
Output: extended schema files + seed + cross-org leak tests + applied multi-region migration.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-PATTERNS.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-RESEARCH.md

<interfaces>
<!-- Verified analog shapes — extracted from PATTERNS.md + filesystem. Use directly. -->

Form1099Nec (tax.prisma:141-176) — the immutable + supersede idiom to mirror:
  id, organizationId, payerOrgId, recipientId, taxYear, status Form1099Status @default(DRAFT),
  box1AmountMinor Int, currency @default("USD") @db.Char(3), corrected Boolean @default(false),
  snapshotJson Json (last-4 TIN only), pdfArchiveKey String?, supersededById String? @unique,
  deletedAt DateTime?, @@index([organizationId, payerOrgId, recipientId, taxYear, status])

ClassificationDocumentKind enum (classification.prisma ~line 49) — add US_DETERMINATION_LETTER.
ClassificationDocument carries ruleSetVersion (frozen at generation) — APPEND_ONLY guard note at line ~74.

EconomicDependencyAlertState (classification.prisma:151-175) — band-state idiom to mirror:
  contractorAssignmentId @unique, currentBand <enum> @default(SAFE), lastScannedAt, lastCrossedAt, lastReminderAt

Tax1099Threshold (tax.prisma:221-227) — config-table idiom: taxYear @unique + threshold fields.

ContractorAssignment (contractor.prisma:175) — the engagement model; add a nullable work-state field
  (additive, mirrors the Phase 76 `endedAt` / Phase 79 `isSaudi` nullable-additive pattern).

globalModels (packages/db/src/tenant.ts:41) — the Set new tenant-owning models MUST NOT join.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Form1042S, Form1099KTrackerState, Tax1099KThreshold, US_DETERMINATION_LETTER, and the engagement work-state field</name>
  <files>packages/db/prisma/schema/tax.prisma, packages/db/prisma/schema/classification.prisma, packages/db/prisma/schema/contractor.prisma, packages/db/prisma/seed/tax-1099k-threshold.ts</files>
  <read_first>
    - packages/db/prisma/schema/tax.prisma (Form1099Nec lines 141-176; Tax1099Threshold 221-227; WithholdingTaxRate ~23 for treatyRate column type; IrisSubmission/IrisAck 180-217)
    - packages/db/prisma/schema/classification.prisma (ClassificationDocumentKind enum ~49; ClassificationDocument ~55 + ruleSetVersion + APPEND_ONLY note ~74; EconomicDependencyAlertState 151-175)
    - packages/db/prisma/schema/contractor.prisma (ContractorAssignment 175-214; the nullable-additive endedAt/isSaudi precedent)
    - packages/db/prisma/seed (the existing Tax1099Threshold seed for the config-table seeding idiom)
  </read_first>
  <action>
    Add the following models/fields. Prisma enum values UPPER_SNAKE_CASE (db:audit-enum-casing). All new models tenant-owning with `organizationId` + the org relation; box amounts in minor units (USD cents); rate columns as `Decimal(5,2)` consistent with `WithholdingTaxRate.treatyRate`.
    - tax.prisma `Form1042S` (mirror Form1099Nec): `enum Form1042SStatus { DRAFT ACTIVE SUPERSEDED }`; fields `box1IncomeCode` (short string), `box2GrossIncomeMinor Int`, `box3aChap3ExemptionCode`, `box3bChap3Rate Decimal @db.Decimal(5,2)`, `box4aChap4ExemptionCode`, `box4bChap4Rate Decimal @db.Decimal(5,2)`, `box7FederalTaxWithheldMinor Int`, `recipientChap3StatusCode` (13j), `recipientChap4StatusCode` (13k), `recipientLobCode` (13n), `treatyArticle String?`, `snapshotJson Json` (FTIN last-4 only), `pdfArchiveKey String?`, `corrected Boolean @default(false)`, `supersededById String? @unique` (self-relation), `deletedAt DateTime?`, `payerOrgId`, `recipientId`, `taxYear Int`, `currency @default("USD") @db.Char(3)`, `@@index([organizationId, payerOrgId, recipientId, taxYear, status])`. Annotate the income/status/exemption/LOB code columns "adviser-verify exact i1042s Appendix B/C values at coding time" (real domain comment, no breadcrumb IDs).
    - tax.prisma `Form1099KTrackerState` (mirror EconomicDependencyAlertState): `contractorId`, `taxYear Int`, `@@unique([contractorId, taxYear])`, `enum Form1099KBand { SAFE APPROACHING OVER }` `currentBand @default(SAFE)`, `cumulativePayoutMinor Int @default(0)`, `transactionCount Int @default(0)`, `lastScannedAt DateTime?`, `lastCrossedAt DateTime?`, `lastReminderAt DateTime?`, `organizationId` + org relation.
    - tax.prisma `Tax1099KThreshold` (mirror Tax1099Threshold): `taxYear Int @unique`, `amountThresholdMinor Int`, `transactionCountThreshold Int`. NOT a constant (D-11).
    - classification.prisma: add `US_DETERMINATION_LETTER` to `ClassificationDocumentKind`. (ClassificationAssessment already carries countryCode — confirm no new column needed for US; add a US-assessment field only if the scoring outcome shape requires persistence beyond `outcome Json`.)
    - contractor.prisma `ContractorAssignment`: add a nullable `workState String?` (US state code; AB5 primary trigger, D-04 — falls back to contractor P84 US state when unset; comment "AB5 governs work performed in CA, not residence").
    - Seed `tax-1099k-threshold.ts`: upsert TY2026 row `amountThresholdMinor = 2000000` ($20,000) + `transactionCountThreshold = 200` (OBBBA — NOT the stale $5K/$600), annotated adviser-verify / local-only.
    Run `pnpm --filter @contractor-ops/db db:generate` after schema edits (generated client is tracked in-repo per P85 — commit it with the schema).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db db:generate && pnpm --filter @contractor-ops/db exec prisma validate && pnpm --filter @contractor-ops/db db:audit-enum-casing</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' packages/db/prisma/schema/tax.prisma | grep -c "model Form1042S\b"` ≥ 1; `box2GrossIncomeMinor`, `box7FederalTaxWithheldMinor`, `treatyArticle`, `supersededById` present on Form1042S.
    - `grep -c "US_DETERMINATION_LETTER" packages/db/prisma/schema/classification.prisma` ≥ 1.
    - `grep -c "workState" packages/db/prisma/schema/contractor.prisma` ≥ 1.
    - `model Form1099KTrackerState` with `@@unique([contractorId, taxYear])` and `model Tax1099KThreshold` with `taxYear Int @unique` present.
    - `prisma validate` passes; `db:audit-enum-casing` passes; generated client regenerated.
    - The 1099-K seed row is $20,000 + 200 (2000000 minor + 200), not $5K/$600.
  </acceptance_criteria>
  <done>Schema + seed + regenerated client committed; prisma validate + enum-casing audit green.</done>
</task>

<task type="auto">
  <name>Task 2: Keep new models out of globalModels + cross-org leak tests</name>
  <files>packages/db/src/tenant.ts, packages/db/src/__tests__/cross-org-leak-1042s.test.ts</files>
  <read_first>
    - packages/db/src/tenant.ts (globalModels Set at line 41; the tenant-scoping middleware at ~161)
    - packages/db/src/rls.ts (globalModels rationale comment ~143)
    - any existing per-model cross-org leak test in packages/db/src/__tests__ (the repo convention to mirror — two-org isolation)
  </read_first>
  <action>
    Confirm `Form1042S`, `Form1099KTrackerState`, and `Tax1099KThreshold` are NOT added to `globalModels` (tenant-owning models stay out — the IDOR landmine, V4). `Tax1099KThreshold` is org-agnostic config like `Tax1099Threshold` — match whatever `Tax1099Threshold` does (if it is in globalModels as shared config, mirror that; if not, leave it tenant-scoped) — do NOT default new tenant-owning rows into globalModels.
    Write `cross-org-leak-1042s.test.ts`: create two orgs, insert a `Form1042S` + a `Form1099KTrackerState` under org A, assert an org-B-scoped tenant client `findMany`/`findFirst` returns zero rows from org A (mirror the repo's existing per-model leak test). Cover both new tenant-owning models.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db test -- cross-org-leak-1042s</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' packages/db/src/tenant.ts | grep -E "Form1042S|Form1099KTrackerState"` returns nothing inside the globalModels Set (negative assertion).
    - cross-org-leak-1042s.test.ts passes: an org-B tenant client sees zero org-A Form1042S and zero org-A Form1099KTrackerState rows.
  </acceptance_criteria>
  <done>globalModels confirmed clean; cross-org leak test GREEN for both new models.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3 (checkpoint): Apply the multi-region 1042-S/classification/1099-K migration</name>
  <action>HUMAN ACTION (mutates live regional DBs — human gate per the P83–86 convention). The human performs the numbered steps in how-to-verify: review the schema diff, apply the additive migration per region (EU/ME/US) using the repo convention (or the migrate-dev/db-push fallback on history drift), seed the Tax1099KThreshold TY2026 row per region, and confirm the new tables + columns exist (or record per-region apply as deferred-by-design).</action>
  <what-built>Schema models + the regenerated Prisma client are committed. The additive migration that creates `Form1042S` / `Form1099KTrackerState` / `Tax1099KThreshold` + the `US_DETERMINATION_LETTER` enum value + the `ContractorAssignment.workState` column must be applied to the live regional databases (EU/ME/US). This mutates live regional DBs — a human gate, per the P83–86 convention.</what-built>
  <how-to-verify>
    1. Review the diff in `packages/db/prisma/schema/*.prisma` and the regenerated client.
    2. Apply the additive migration per region using the repo convention (`pnpm db:migrate:all` EU then ME then US, or the `migrate dev`/`db push` fallback if migration-history drift blocks `migrate` — the P82–86 posture). The enum value add is `ALTER TYPE ... ADD VALUE IF NOT EXISTS` (non-breaking).
    3. Run `pnpm --filter @contractor-ops/db db:seed` for the `Tax1099KThreshold` TY2026 row in each region.
    4. Confirm `Form1042S`, `Form1099KTrackerState`, `Tax1099KThreshold` tables exist and `ContractorAssignment.workState` column exists in each applied region (or record per-region apply as deferred-by-design, matching the open P82–86 migration-apply items in STATE.md Deferred Items).
  </how-to-verify>
  <resume-signal>Type "approved" once the migration is applied (or recorded as deferred per the P82–86 posture), or describe a blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → tRPC → DB write | Box amounts / organizationId must be server-derived, never client-set (mass-assignment) |
| org A data ↔ org B query | Tenant isolation on new tenant-owning models |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-02-01 | Information Disclosure / Elevation | Form1042S / Form1099KTrackerState cross-org read | mitigate | tenant-owning, absent from globalModels; per-model cross-org leak test (Task 2); RLS + org-scoped queries enforced by tenant middleware |
| T-87-02-02 | Information Disclosure | Full FTIN/TIN in snapshotJson | mitigate | snapshotJson schema constrained to FTIN last-4 only; full value never persisted (enforced by the service sanitizer in Plan 04) |
| T-87-02-03 | Tampering | Additive migration on live regional DBs | mitigate | human-gate apply; additive-only (new tables + `ADD VALUE IF NOT EXISTS` + nullable column) — zero data migration, reversible |
| T-87-02-SC | Tampering | npm/pip/cargo installs | accept | No new external packages — schema-only change |
</threat_model>

<verification>
- `prisma validate` + `db:audit-enum-casing` green.
- `pnpm --filter @contractor-ops/db test -- cross-org-leak-1042s` green.
- Migration applied per region (or recorded deferred).
</verification>

<success_criteria>
- All five schema additions land additively; client regenerated; enum casing clean.
- New tenant-owning models out of globalModels with passing cross-org leak tests.
- Multi-region migration applied (or deferred per P82–86 posture) at the human gate.
</success_criteria>

<output>
Create `.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-02-SUMMARY.md` when done.
</output>
