---
phase: 87-theme-a-1042-s-us-classification-determination-letter
plan: 06
type: execute
wave: 3
depends_on: [87-01, 87-02]
files_modified:
  - packages/api/src/services/form-1099k-tracker.service.ts
  - apps/cron-worker/src/jobs/handlers/form-1099k-tracker.ts
  - apps/cron-worker/src/jobs/registry.ts
  - apps/cron-worker/src/env.ts
  - .env.example
  - packages/api/src/routers/finance/form-1099k-tracker-router.ts
autonomous: true
requirements: [US-CLASS-03]

must_haves:
  truths:
    - "A periodic cron sums cumulative tax-year payouts + transaction count per contractor and transitions bands (SAFE → APPROACHING → OVER) against the tax-year-keyed $20,000 + 200 config"
    - "An up-crossing fires a proactive heads-up notification with same-band re-fire suppressed"
    - "The tracker is purely informational — it NEVER files a 1099-K"
    - "The band state surfaces read-only on the contractor profile"
    - "Implements D-10 (cron band state never-files), D-11 (tax-year-keyed $20k+200 config), D-12 (us-expansion gating)"
  artifacts:
    - path: "packages/api/src/services/form-1099k-tracker.service.ts"
      provides: "band-scan service (mirror economic-dependency-scan)"
      contains: "Form1099KBand"
    - path: "apps/cron-worker/src/jobs/handlers/form-1099k-tracker.ts"
      provides: "cron handler with flag short-circuit + runScan"
      contains: "createCronLogger"
    - path: "packages/api/src/routers/finance/form-1099k-tracker-router.ts"
      provides: "read-only profile band query (us-expansion gated)"
      contains: "assertUsExpansionEnabled"
  key_links:
    - from: "apps/cron-worker/src/jobs/registry.ts"
      to: "form-1099k-tracker handler"
      via: "getJobDefinitions env CRON_FORM_1099K_TRACKER_SCHEDULE + meta entry"
      pattern: "CRON_FORM_1099K_TRACKER_SCHEDULE"
    - from: "packages/api/src/services/form-1099k-tracker.service.ts"
      to: "Tax1099KThreshold config"
      via: "tax-year-keyed threshold read, never a constant"
      pattern: "Tax1099KThreshold"
    - from: "packages/api/src/routers/finance/form-1099k-tracker-router.ts"
      to: "packages/api/src/root.ts"
      via: "isUsExpansionRegistered() conditional spread mounts the read-only router"
      pattern: "isUsExpansionRegistered"
---

<objective>
Build the informational 1099-K threshold tracker: a periodic cron that sums cumulative tax-year payouts + transaction count per contractor, transitions bands against the tax-year-keyed `$20,000 + 200` config, and fires a proactive heads-up notification on an up-crossing — purely informational, never a filing. Surface the band read-only for the contractor profile.

Purpose: US-CLASS-03. Turns Plan 01's `form-1099k-tracker.service` RED scaffold GREEN. Mirrors the v5.0 `EconomicDependencyAlertState` cron/band pattern.
Output: the tracker service + cron handler + registry wiring + a read-only profile router.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-PATTERNS.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-RESEARCH.md

<interfaces>
<!-- Verified analog idioms — use directly. -->

economic-dependency-scan.ts (the band-scan idiom to mirror):
  createCronLogger('…') (line 50) — NO console.*
  pure bandFor(value): Band + bandIndex(b) (66-82) for transition comparison
  updateBandState (179-248): next>prev → up-cross fire; next<prev → resolved; same non-safe band + lastReminderAt≥cadence → re-fire; upsert keyed on the @unique column
  dispatch({ organizationId, type, recipientUserIds, title, body, entityType, entityId, metadata }) (340-353); recipients from resolveRbacRecipients(orgId, 'contractor:read')
  bounded fan-out pLimit(10) (294-295); metrics.gauge (370-372)

handlers/classification-economic-dependency.ts (18-56):
  evaluate(flag, FLAG_CTX) short-circuit → CRON_SKIPPED_FLAG_OFF; try runScan → { ok, durationMs, details }; catch → Sentry.captureException({ tags: { 'cron.job': … } })

registry.ts (29-42, 71-77): getJobDefinitions env param + a { meta: { name, schedule }, handler } entry.

Tax1099KThreshold (from Plan 02): taxYear @unique, amountThresholdMinor ($20,000 = 2000000), transactionCountThreshold (200).
Form1099KTrackerState (from Plan 02): @@unique([contractorId, taxYear]), Form1099KBand { SAFE APPROACHING OVER }, cumulativePayoutMinor, transactionCount, lastScannedAt, lastCrossedAt, lastReminderAt.

require-us-expansion-flag.ts: assertUsExpansionEnabled / isUsExpansionRegistered (the cron uses module.us-expansion via evaluate(); root.ts mounts the read router via isUsExpansionRegistered() conditional spread).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: form-1099k-tracker.service — band scan against the tax-year-keyed threshold</name>
  <files>packages/api/src/services/form-1099k-tracker.service.ts</files>
  <behavior>
    - runScan sums cumulative tax-year payout minor + transaction count per contractor (read the payment domain).
    - bandFor compares against the tax-year-keyed Tax1099KThreshold ($20,000 + 200) — NEVER a constant; APPROACHING is a configurable proximity below the threshold; OVER is at/above either dimension.
    - updateBandState: up-cross fires a notification; same non-safe band + lastReminderAt ≥ cadence re-fires; down-cross resolves; upsert keyed on (contractorId, taxYear).
    - The scan NEVER files a 1099-K (assert this invariant explicitly in a test — no Form1042S/IRIS/filing call path).
    - createCronLogger — no console.*.
  </behavior>
  <read_first>
    - the economic-dependency-scan service (createCronLogger 50, bandFor/bandIndex 66-82, updateBandState 179-248, dispatch 340-353, pLimit 294-295, metrics.gauge 370-372)
    - apps/cron-worker/src/jobs/handlers/classification-economic-dependency.ts (the handler wrapper idiom)
    - packages/api/src/services/__tests__/form-1099k-tracker.service.test.ts (the RED scaffold from Plan 01)
    - packages/db/prisma/schema/tax.prisma (Form1099KTrackerState + Tax1099KThreshold from Plan 02)
    - the payment domain read path (cumulative settled USD payouts + transaction count per contractor per tax-year)
  </read_first>
  <action>
    Create `form-1099k-tracker.service.ts` mirroring the economic-dependency scan. `createCronLogger('form-1099k-tracker')` (no console.*). `runScan`: for each US-org contractor with tax-year payouts, sum `cumulativePayoutMinor` + `transactionCount` (read the settled-payment domain). Pure `bandFor(payoutMinor, txnCount, threshold): Form1099KBand` reading the tax-year-keyed `Tax1099KThreshold` ($20,000 + 200 — never a constant; D-11); SAFE / APPROACHING (configurable proximity) / OVER (at/above either dimension). `updateBandState` mirrors the economic-dependency upsert keyed on `(contractorId, taxYear)`: up-cross → proactive heads-up notification via `dispatch(...)` (recipients via `resolveRbacRecipients`); same non-safe band + `lastReminderAt ≥ cadence` → re-fire; down-cross → resolved. Bounded fan-out `pLimit(10)`. INVARIANT: the scan NEVER creates a Form1042S / triggers IRIS / files anything — purely informational (D-10). Annotate adviser-verify / informational-only.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- form-1099k-tracker.service && pnpm --filter @contractor-ops/api lint:logs</automated>
  </verify>
  <acceptance_criteria>
    - `form-1099k-tracker.service.test.ts` GREEN.
    - Tests prove: band transition against the tax-year-keyed $20,000 + 200 config (not a constant); same-band re-fire suppression via lastReminderAt; the never-file invariant (no filing call path).
    - `grep -c "Tax1099KThreshold" packages/api/src/services/form-1099k-tracker.service.ts` ≥ 1; `grep -c "console\." packages/api/src/services/form-1099k-tracker.service.ts` == 0 (lint:logs passes).
  </acceptance_criteria>
  <done>Tracker service GREEN; band transitions + never-file invariant + no-console proven.</done>
</task>

<task type="auto">
  <name>Task 2: Cron handler + registry + env, and the read-only profile router</name>
  <files>apps/cron-worker/src/jobs/handlers/form-1099k-tracker.ts, apps/cron-worker/src/jobs/registry.ts, apps/cron-worker/src/env.ts, .env.example, packages/api/src/routers/finance/form-1099k-tracker-router.ts</files>
  <read_first>
    - apps/cron-worker/src/jobs/handlers/classification-economic-dependency.ts (evaluate flag short-circuit → CRON_SKIPPED_FLAG_OFF; runScan; Sentry tags)
    - apps/cron-worker/src/jobs/registry.ts (getJobDefinitions env params 29-42; job-definition entries 71-77)
    - apps/cron-worker/src/env.ts (the cron env schema — where CRON_*_SCHEDULE vars are validated)
    - .env.example (the CRON_*_SCHEDULE convention)
    - packages/api/src/root.ts (the isUsExpansionRegistered() conditional-spread mount point for US-expansion routers)
    - the P85/P86 finance read-only router for the procedure shape + assertUsExpansionEnabled gating
    - packages/api/src/middleware/require-us-expansion-flag.ts
  </read_first>
  <action>
    Create `handlers/form-1099k-tracker.ts` mirroring `classification-economic-dependency.ts`: `evaluate('module.us-expansion', FLAG_CTX)` short-circuit → `CRON_SKIPPED_FLAG_OFF`; `try runScan → { ok, durationMs, details }`; `catch → Sentry.captureException({ tags: { 'cron.job': 'form-1099k-tracker' } })`.
    Register the job in `registry.ts`: add `CRON_FORM_1099K_TRACKER_SCHEDULE` to the `getJobDefinitions` env param + a `{ meta: { name: 'form-1099k-tracker', schedule: env.CRON_FORM_1099K_TRACKER_SCHEDULE }, handler }` entry. Add `CRON_FORM_1099K_TRACKER_SCHEDULE` to the cron-worker env schema (`apps/cron-worker/src/env.ts`) and `.env.example` (with a sensible default cadence, e.g. daily).
    Create `form-1099k-tracker-router.ts`: a single read-only `getTrackerState` query (the cron writes state; the UI only surfaces it), Zod `.strict()` input, tenant from session, `assertUsExpansionEnabled` per-request. Returns the band + cumulative payout + transaction count + the tax-year threshold for the profile band. Mount conditionally in `packages/api/src/root.ts` via `isUsExpansionRegistered()` conditional spread (or fold into an existing US finance namespace if one exists) — mirror the P85/P86 US-expansion router-mount idiom.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/cron-worker test 2>/dev/null; pnpm typecheck --filter=@contractor-ops/cron-worker && pnpm typecheck --filter=@contractor-ops/api && pnpm check:no-process-env 2>/dev/null || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CRON_FORM_1099K_TRACKER_SCHEDULE" apps/cron-worker/src/jobs/registry.ts apps/cron-worker/src/env.ts .env.example` ≥ 3 (registered, validated, documented).
    - The handler short-circuits on `module.us-expansion` off (CRON_SKIPPED_FLAG_OFF) — assert in a handler test if the cron-worker test harness supports it.
    - The profile router is read-only and us-expansion gated (`grep -c "assertUsExpansionEnabled" packages/api/src/routers/finance/form-1099k-tracker-router.ts` ≥ 1) and mounted in `root.ts` via `isUsExpansionRegistered()` conditional spread.
    - `pnpm typecheck --filter=@contractor-ops/cron-worker` + `--filter=@contractor-ops/api` pass.
  </acceptance_criteria>
  <done>Cron handler + registry + env + read-only profile router wired; flag short-circuit + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron → DB band-state write | The cron is the sole writer of Form1099KTrackerState |
| client → profile router | Read-only; tenant-scoped; us-expansion gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-06-01 | (compliance) | Tracker mistaken for a filing obligation | mitigate | never-file invariant asserted in a unit test; UI copy "we don't file 1099-K"; no filing call path in the scan |
| T-87-06-02 | Elevation / Info Disclosure | Cross-org read of tracker state | mitigate | tenant-owning model + per-model leak test (Plan 02); read router org-scoped + us-expansion gated |
| T-87-06-03 | Tampering | Client setting band/payout via the router | mitigate | router is read-only; band state written exclusively by the cron |
| T-87-06-04 | DoS | Unbounded scan fan-out | mitigate | `pLimit(10)` bounded fan-out (mirror economic-dependency) |
| T-87-06-SC | Tampering | npm/pip/cargo installs | accept | No new external packages |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- form-1099k-tracker.service` green (band + never-file invariant).
- `pnpm typecheck --filter=@contractor-ops/cron-worker` + `--filter=@contractor-ops/api` green; `lint:logs` clean.
</verification>

<success_criteria>
- US-CLASS-03: the 1099-K tracker transitions bands against the tax-year-keyed $20,000 + 200 config, fires a proactive heads-up on up-cross, never files, and surfaces read-only for the contractor profile.
</success_criteria>

<output>
Create `.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-06-SUMMARY.md` when done.
</output>
