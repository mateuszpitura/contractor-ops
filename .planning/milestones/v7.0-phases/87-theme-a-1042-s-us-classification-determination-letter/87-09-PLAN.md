---
phase: 87-theme-a-1042-s-us-classification-determination-letter
plan: 09
type: execute
wave: 6
depends_on: [87-04, 87-07, 87-08]
files_modified:
  - apps/web-vite/src/components/contractors/tax-filing/tax-1042s-batch-panel.tsx
  - apps/web-vite/src/components/contractors/tax-filing/tax-1042s-batch-summary.tsx
  - apps/web-vite/src/components/contractors/tax-filing/tax-1042s-filing-card.tsx
  - apps/web-vite/src/components/contractors/tax-filing/treaty-rate-caption.tsx
  - apps/web-vite/src/components/contractors/tax-filing/hooks/use-1042s-batch.ts
  - apps/web-vite/src/components/contractors/tax-filing/hooks/use-1042s-filing.ts
  - apps/web-vite/src/components/portal/tax-forms/copy-1042s-download.tsx
  - apps/web-vite/src/messages/en.json
  - apps/web-vite/src/messages/en-US.json
  - apps/web-vite/src/messages/de.json
  - apps/web-vite/src/messages/pl.json
  - apps/web-vite/src/messages/ar.json
autonomous: true
requirements: [US-FORM-06]

must_haves:
  truths:
    - "Staff can generate a 1042-S batch (review-before-file) and see per-recipient treaty-rate-vs-30% withholding"
    - "A recipient without a complete W-8 chain is shown at the 30% statutory rate (amber caption), never blocked from filing"
    - "TIN/FTIN is last-4 only everywhere; new strings i18n at en/en-US/de/pl/ar parity"
    - "CONDITIONAL (P86 UI present): staff can download the validated 1042-S XML and upload the IRS acknowledgement with IRIS ack status via the reused P86 status pill, AND the portal 1042-S recipient PDF download is gated on the SAME P86 affirmative IRS e-delivery consent (no consent / no portal seat → paper/manual messaging). If the P86 UI components (iris-status-pill, ack-upload-field, step-edelivery-consent/use-edelivery-consent, copy-b-download) are NOT on disk at execution, Tasks 2-3 HOLD and the SUMMARY records a cross-phase HOLD — they are NEVER rebuilt, only reused once P86 lands."
    - "Implements D-09 (recipient PDF + P86 consent gate), D-14 (i18n parity en/en-US/de/pl/ar)"
  artifacts:
    - path: "apps/web-vite/src/components/contractors/tax-filing/tax-1042s-batch-panel.tsx"
      provides: "wired 1042-S batch panel (4-state) with treaty-rate column — deliverable independently of P86"
      contains: "Skeleton"
    - path: "apps/web-vite/src/components/contractors/tax-filing/tax-1042s-filing-card.tsx"
      provides: "IRIS filing status + download XML + ack upload (P86 status pill reuse) — HOLDS if the P86 pill/ack-upload are absent"
      contains: "iris-status-pill"
    - path: "apps/web-vite/src/components/portal/tax-forms/copy-1042s-download.tsx"
      provides: "consent-gated portal 1042-S recipient PDF download (P86 consent-gate reuse) — HOLDS if the P86 consent components are absent"
      contains: "consent"
  key_links:
    - from: "apps/web-vite/src/components/portal/tax-forms/copy-1042s-download.tsx"
      to: "use-edelivery-consent hook (P86)"
      via: "the SAME consent gate gates the 1042-S download (REUSE — never rebuilt; Task 3 HOLDS if absent)"
      pattern: "use-edelivery-consent"
    - from: "apps/web-vite/src/components/contractors/tax-filing/hooks/use-1042s-batch.ts"
      to: "form-1042s-router (Plan 04/07)"
      via: "sole tRPC boundary for the batch/filing surface"
      pattern: "useTRPC"
---

<objective>
Build the staff 1042-S filing surface (batch panel + summary + filing card + treaty-rate caption) extending the P86 tax-filing idioms, and the portal 1042-S recipient PDF download gated on the SAME P86 affirmative IRS e-delivery consent. Review-before-file; 30%-vs-treaty as an advisory caption (never a block); FTIN last-4 only.

Purpose: the UI half of US-FORM-06. Sequenced AFTER Plan 08 (depends_on: 87-08) because both edit the shared web-vite message files — Plan 08 owns the UsClassification/Form1099KTracker namespaces, Plan 09 adds the Tax1042S* namespaces to the same files (no concurrent write).

**CROSS-PHASE NOTE (conditional delivery):** The P86 `tax-1099-*` filing components (`tax-1099-batch-panel`, `iris-status-pill`, `ack-upload-field`, `step-edelivery-consent`, `use-edelivery-consent`, `use-iris-filing`, `copy-b-download`) are the analogs this mirrors — they are built in P86 (in-flight) and may still be Wave-0 RED / not on disk. Reuse them VERBATIM where they exist (the consent step + status pill + ack-upload are explicitly "reuse the SAME P86 component"); NEVER rebuild the P86 seam.
- **Task 1** (staff 1042-S batch panel + summary + treaty caption + hook) reuses only the generic 4-state idiom + the Plan 04/07 `form-1042s-router` — it is **GREEN and deliverable independently** of the P86 UI components.
- **Tasks 2-3** reuse the P86 `iris-status-pill` / `ack-upload-field` / `step-edelivery-consent` / `use-edelivery-consent` / `copy-b-download`. If those components are **not on disk at execution time**, surface the dependency to the orchestrator and **HOLD Tasks 2-3** (do not rebuild the P86 seam); the SUMMARY records a **cross-phase HOLD** for them. Once P86 lands the components, Tasks 2-3 reuse them verbatim and complete.

Output: the staff filing components + portal download + hooks + i18n for the 1042-S namespaces (Tasks 2-3 conditional on the P86 UI seam being on disk).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-UI-SPEC.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
<!-- UI layering + the P85/P86 components to reuse/extend (per UI-SPEC Component Inventory). -->

Layering: Page → wired section (4-state, no direct tRPC) → hook (sole tRPC boundary) → presentational. NO *-container.tsx.

Reuse verbatim (P86 — may be in-flight / not yet on disk; reuse, don't rebuild — HOLD Tasks 2-3 if absent):
  - tax-1099-batch-panel (loading/empty/error idiom for the batch panel)
  - iris-status-pill.tsx (STATUS_MAP / FormStatusPill — the 6 IRIS ack states; variant + icon + tooltip + aria-label)
  - ack-upload-field.tsx (Input type=file + safeParse in the hook/server)
  - step-edelivery-consent.tsx + use-edelivery-consent.ts (the SAME affirmative IRS e-delivery consent gate; mirrors P85 step-attest)
  - copy-b-download.tsx (the consent-gated recipient-PDF download idiom)
  - correction-dialog (P86 supersede confirm) — reuse or mirror with DialogBody/DialogFooter
Extend:
  - treaty-claim-caption.tsx (P85) → treaty-rate-caption.tsx (per-recipient treaty-vs-30% + §875(d) explanation)
  - SsnMaskedReveal (FTIN last-4 only, contractorPii:read gated)

Color (UI-SPEC): treaty rate applied = success green; statutory 30% (W-8 incomplete) = warning amber (advisory, NOT a block);
  manual override = info blue. IRIS ack states map to the P86 STATUS_MAP variants. Never block filing on missing W-8.

Consent contract (UI-SPEC IRS Electronic-Delivery section): affirmative opt-in checkbox (unchecked default), button disabled
  until checked; timestamp/IP/actorId 100% server-derived; consent gates *furnishing* (the PDF is archived either way);
  no consent → paper-copy messaging; no portal seat → paper/manual messaging; withdrawable; FTIN last-4 only.

i18n: Plan 08 already added UsClassification + Form1099KTracker keys to the message files; Plan 09 ADDS the
  Tax1042SBatch / Tax1042SFiling / Tax1042SConsent namespaces to the same files (no concurrent edit — 09 runs after 08).

Gates: check:web-vite-data-layer, check:web-vite-dialog-pattern, i18n:parity, lint:no-hardcoded-strings.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Staff 1042-S batch panel + summary + treaty-rate caption + hook</name>
  <files>apps/web-vite/src/components/contractors/tax-filing/tax-1042s-batch-panel.tsx, apps/web-vite/src/components/contractors/tax-filing/tax-1042s-batch-summary.tsx, apps/web-vite/src/components/contractors/tax-filing/treaty-rate-caption.tsx, apps/web-vite/src/components/contractors/tax-filing/hooks/use-1042s-batch.ts</files>
  <read_first>
    - the frontend-design plugin + `semble search` for the P86 tax-1099 filing components before editing (CLAUDE.md [ui] binding)
    - the P86 tax-1099-batch-panel (the 4-state loading/empty/error idiom + recipient list) — reuse the structure
    - apps/web-vite/src/components/contractors/tax-filing/.../treaty-claim-caption.tsx (P85 — the caption to extend)
    - SsnMaskedReveal (FTIN last-4 reveal on the recipient row)
    - .planning/phases/87-.../87-UI-SPEC.md (batch panel copy, treaty-vs-30% column, review-before-file invariant, never-hard-block invariant)
  </read_first>
  <action>
    NOTE: this task is GREEN and deliverable independently of the P86 UI seam — it reuses only the generic 4-state idiom + the Plan 04/07 `form-1042s-router`. (The P86-dependent surfaces are Tasks 2-3.)
    Write the hook FIRST (`hooks/use-1042s-batch.ts`) — the sole tRPC boundary calling the Plan 04 `form-1042s-router` generateBatch + read. Then `tax-1042s-batch-panel.tsx` (wired, 4-state: Skeleton/empty with the UI-SPEC copy + "Generate 1042-S batch" CTA/error role=alert + Reload/loaded). The loaded state lists foreign recipients with: name (Bdi), FTIN via SsnMaskedReveal (last-4), treaty article + withholding rate (box 3b), and a 30%-statutory column. `tax-1042s-batch-summary.tsx` shows recipients / total box-2 gross / total box-7 withheld / taxYear with `font-mono` amounts. `treaty-rate-caption.tsx` extends the P85 caption: per-recipient "Treaty rate {rate} applied under {article}" (success green) vs "No complete W-8 on file — 30% statutory" (warning amber, advisory — §875(d) gate explanation), NEVER a block (D-07 never-hard-block invariant). Review-before-file: "Generate 1042-S batch" produces a reviewable summary; filing is a separate action (Task 2). All strings i18n.
  </action>
  <verify>
    <automated>pnpm check:web-vite-data-layer; pnpm --filter @contractor-ops/web-vite test -- tax-1042s-batch</automated>
  </verify>
  <acceptance_criteria>
    - `check:web-vite-data-layer` passes (the hook is the sole tRPC boundary).
    - The 30%-statutory caption is amber `warning` and the panel does NOT block filing for a missing W-8 (grep: no disabled/blocked filing on the statutory branch).
    - The batch panel branches all four states; FTIN renders via SsnMaskedReveal (last-4).
  </acceptance_criteria>
  <done>Staff 1042-S batch panel + summary + treaty caption + hook; review-before-file + never-block proven (independent of the P86 UI seam).</done>
</task>

<task type="auto">
  <name>Task 2: Staff 1042-S filing card (download XML + ack upload + status pill) + hook — HOLDS if the P86 pill/ack-upload are absent</name>
  <files>apps/web-vite/src/components/contractors/tax-filing/tax-1042s-filing-card.tsx, apps/web-vite/src/components/contractors/tax-filing/hooks/use-1042s-filing.ts</files>
  <read_first>
    - the P86 iris-status-pill.tsx (STATUS_MAP / FormStatusPill — reuse verbatim) + ack-upload-field.tsx + use-iris-filing.ts
    - the P86 correction-dialog (supersede confirm — reuse or mirror with DialogBody/DialogFooter)
    - apps/web-vite/src/components/contractors/tax-filing/.../tax-1042s-batch-panel.tsx (Task 1 — same domain)
    - .planning/phases/87-.../87-UI-SPEC.md (filing card states + error copy for XSD-validation-failed / ack-parse-failed / Rejected)
  </read_first>
  <action>
    **GATE (cross-phase reuse — blocking-soft):** first confirm the P86 `iris-status-pill` + `ack-upload-field` are on disk (`semble search` / Glob). If EITHER is absent, do NOT rebuild it — surface the dependency to the orchestrator and HOLD this task; record the HOLD in the SUMMARY (Task 1 still ships). Proceed only once the P86 components exist; then reuse them VERBATIM.
    Write `hooks/use-1042s-filing.ts` (sole tRPC boundary) calling the Plan 07 transmit + ack-upload procedures (idempotency surfaced). Then `tax-1042s-filing-card.tsx` (wired, 4-state) that REUSES the P86 `iris-status-pill` over the same 6 IRIS ack states (1042-S is a different form-type through the same transmit tail, D-08): "Download validated 1042-S XML" primary CTA (ManualDownload default), the reused `ack-upload-field` to "Upload IRS acknowledgement" (safeParse in the hook/server), and the supersede correction confirm (reuse P86 correction-dialog or mirror with DialogBody/DialogFooter). Error states use the UI-SPEC copy: XSD-validation-failed (lists the validation errors), ack-parse-failed, Rejected (per-recipient error detail → file a correction). aria-live="polite" on status change (assertive on Rejected). All strings i18n.
  </action>
  <verify>
    <automated>pnpm check:web-vite-data-layer; pnpm --filter @contractor-ops/web-vite test -- tax-1042s-filing</automated>
  </verify>
  <acceptance_criteria>
    - EITHER: the P86 `iris-status-pill` + `ack-upload-field` are absent → this task is recorded as a cross-phase HOLD in the SUMMARY (no rebuild), OR (P86 present):
    - `grep -c "iris-status-pill\|IrisStatusPill\|FormStatusPill" apps/web-vite/src/components/contractors/tax-filing/tax-1042s-filing-card.tsx` ≥ 1 (reuses the P86 pill, not a rebuild).
    - The filing card branches all four states; the Rejected state announces via aria-live and offers "File a correction".
    - `check:web-vite-data-layer` passes; the hook is the sole tRPC boundary.
  </acceptance_criteria>
  <done>Staff 1042-S filing card + hook reusing the P86 status pill + ack upload (download/transmit/correct wired) — OR a cross-phase HOLD recorded if the P86 seam is absent.</done>
</task>

<task type="auto">
  <name>Task 3: Portal 1042-S recipient PDF download (consent-gated) + i18n — HOLDS if the P86 consent components are absent</name>
  <files>apps/web-vite/src/components/portal/tax-forms/copy-1042s-download.tsx, apps/web-vite/src/messages/en.json, apps/web-vite/src/messages/en-US.json, apps/web-vite/src/messages/de.json, apps/web-vite/src/messages/pl.json, apps/web-vite/src/messages/ar.json</files>
  <read_first>
    - the P86 step-edelivery-consent.tsx + use-edelivery-consent.ts (the SAME consent gate — reuse) + copy-b-download.tsx (the consent-gated download idiom to mirror)
    - apps/web-vite/src/components/portal/tax-forms (the portal layout + portalAppRouter hook idiom)
    - apps/web-vite/src/messages/en.json (the namespaces Plan 08 already added — append Tax1042S* without disturbing them)
    - .planning/phases/87-.../87-UI-SPEC.md (IRS Electronic-Delivery Consent Contract — the 5 points; the Copywriting Contract Tax1042SConsent/Tax1042SBatch/Tax1042SFiling namespaces)
  </read_first>
  <action>
    **GATE (cross-phase reuse — blocking-soft):** first confirm the P86 `step-edelivery-consent` + `use-edelivery-consent` + `copy-b-download` are on disk (`semble search` / Glob). If any is absent, do NOT rebuild the consent step — surface the dependency to the orchestrator and HOLD the `copy-1042s-download.tsx` portal download (record the HOLD in the SUMMARY). The i18n sub-step (adding the Tax1042S* namespaces) MAY still proceed independently of the P86 seam (it is pure message-file authoring). Build the consent-gated download only once the P86 consent components exist; then reuse them VERBATIM.
    `copy-1042s-download.tsx` (portal): a consent-gated 1042-S recipient PDF download that REUSES the SAME P86 `step-edelivery-consent` + `use-edelivery-consent` hook (the same gate gates a second form type — timestamp/IP/actorId 100% server-derived). With consent → the download is offered (FTIN last-4 only on any on-screen identifier); without consent → paper-copy messaging; no portal seat → paper/manual messaging (UI-SPEC copy). Withdrawable. The PDF is archived regardless (the gate is on furnishing). Mirror P86 `copy-b-download` — reuse, don't rebuild the consent step.
    i18n: ADD the `Tax1042SBatch` / `Tax1042SFiling` / `Tax1042SConsent` namespaces (UI-SPEC canonical en + en-US thin override + de/pl/ar parity, ar RTL; mark machine-translated for native review per the standing i18n_review deferred item) WITHOUT disturbing the UsClassification/Form1099KTracker keys Plan 08 added to the same files. The locked disclaimer phrase stays in `@contractor-ops/validators` (never a translation key).
  </action>
  <verify>
    <automated>pnpm check:web-vite-data-layer; pnpm --filter @contractor-ops/web-vite i18n:parity && pnpm lint:no-hardcoded-strings 2>/dev/null || true</automated>
  </verify>
  <acceptance_criteria>
    - EITHER: the P86 consent components are absent → the `copy-1042s-download.tsx` portal download is recorded as a cross-phase HOLD in the SUMMARY (no consent-step rebuild; the i18n namespaces may still land), OR (P86 present):
    - `grep -c "use-edelivery-consent\|useEdeliveryConsent" apps/web-vite/src/components/portal/tax-forms/copy-1042s-download.tsx` ≥ 1 (reuses the P86 consent hook — no rebuild).
    - The download is offered only with consent; no-consent and no-portal-seat show the paper/manual messaging; FTIN last-4 only (no full value in the DOM).
    - `i18n:parity` passes; the `Tax1042S*` namespaces exist in all five message files; the Plan-08 namespaces are intact; the locked phrase is not a translation key.
  </acceptance_criteria>
  <done>Portal consent-gated 1042-S download reusing the P86 consent gate + 1042-S i18n namespaces at parity — OR a cross-phase HOLD recorded for the portal download if the P86 consent seam is absent (i18n namespaces still landed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| portal client → consent → download | Consent timestamp/IP/actorId server-derived; recipient scoped to ctx.contractorId (IDOR) |
| staff client → batch/filing hook | Box amounts already server-derived; ack file safeParsed |
| recipient identifier → DOM | FTIN last-4 only, never full value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-09-01 | Elevation / IDOR | Portal 1042-S PDF download | mitigate | recipient scoped to ctx.contractorId (never client-supplied); consent gate; reuse the P86 portal scoping (mirror P85/P86 IDOR pattern) |
| T-87-09-02 | Information Disclosure | Full FTIN in the DOM / PDF | mitigate | FTIN last-4 only via SsnMaskedReveal + the masked snapshot; full value never in the DOM |
| T-87-09-03 | Repudiation / Spoofing | Forged consent timestamp/IP/actor | mitigate | consent timestamp/IP/actorId 100% server-derived (deriveClientIp / ctx.contractorId / new Date()); client schema omits all three |
| T-87-09-04 | Tampering | Malicious ack-file upload | mitigate | `safeParse` the uploaded ack in the hook/server (P86 ack-parser); no unsafe `as` |
| T-87-09-05 | (compliance) | Blocking a recipient without a W-8 | mitigate | never-hard-block — 30% statutory amber caption, filing proceeds (D-07) |
| T-87-09-SC | Tampering | shadcn registry / npm installs | accept | UI-SPEC Registry Safety: only installed primitives + reused P85/P86 components; no new registry blocks |
</threat_model>

<verification>
- `pnpm check:web-vite-data-layer` + `check:web-vite-dialog-pattern` green.
- `pnpm --filter @contractor-ops/web-vite i18n:parity` + `lint:no-hardcoded-strings` green.
- Scoped web-vite tests for the new components green (NEVER the full unscoped suite — RAM).
- Tasks 2-3 verified GREEN only when the P86 UI seam is on disk; otherwise their HOLD is recorded in the SUMMARY (Task 1 + the i18n namespaces still ship).
</verification>

<success_criteria>
- US-FORM-06 (UI): staff 1042-S batch (review-before-file) + filing card (download XML / ack upload / status pill) with treaty-vs-30% advisory captions, and a portal recipient PDF gated on the same P86 e-delivery consent — FTIN last-4 only throughout. Task 1 ships independently; the P86-dependent filing card + portal download complete once the reused P86 components are on disk (cross-phase HOLD recorded otherwise — never rebuilt).
</success_criteria>

<output>
Create `.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-09-SUMMARY.md` when done. If Tasks 2-3 HOLD because the P86 UI seam is absent, the SUMMARY must record the cross-phase HOLD (which components, that they are reused-not-rebuilt, and that Task 1 + i18n shipped).
</output>
