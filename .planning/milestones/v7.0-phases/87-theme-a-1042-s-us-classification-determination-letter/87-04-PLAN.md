---
phase: 87-theme-a-1042-s-us-classification-determination-letter
plan: 04
type: execute
wave: 3
depends_on: [87-01, 87-02]
files_modified:
  - packages/api/src/services/form-1042s.service.ts
  - packages/api/src/services/form-1042s-pdf.ts
  - packages/api/src/pdf-templates/form-1042s-recipient-copy.tsx
  - packages/api/src/routers/finance/form-1042s-router.ts
  - packages/api/src/root.ts
autonomous: true
requirements: [US-FORM-06]

must_haves:
  truths:
    - "A 1042-S is generated for a foreign (W-8) recipient with box-2 withholding sourced from the P85 treaty table and the treaty article snapshotted"
    - "When the W-8 chain is complete the treaty rate applies; otherwise the 30% statutory rate applies (§875(d) gate)"
    - "Form1042S is append-only; a CORRECTED form supersedes the prior in one $transaction; the filed row is never mutated"
    - "Batch generation is idempotent and audit-logged"
    - "The recipient PDF renders from the immutable snapshot with FTIN last-4 only, archived to the US R2 tax bucket"
    - "Routing is W-8 → 1042-S vs W-9 → 1099-NEC derived from the form on file, never nationality"
    - "Implements D-06 (Form1042S generation), D-07 (reported-only §875(d)), D-09 (recipient PDF consent + FTIN last-4), D-12 (us-expansion gating), D-13 (immutable), D-15 (audit + idempotency)"
  artifacts:
    - path: "packages/api/src/services/form-1042s.service.ts"
      provides: "box aggregation + §875(d) treaty snapshot + supersede + idempotent batch"
      contains: "applyTreaty"
    - path: "packages/api/src/services/form-1042s-pdf.ts"
      provides: "recipient-copy render/archive (CAS guard, last-4 masking)"
      contains: "renderAndArchive"
    - path: "packages/api/src/pdf-templates/form-1042s-recipient-copy.tsx"
      provides: "substitute recipient-copy react-pdf template"
      contains: "1042"
    - path: "packages/api/src/routers/finance/form-1042s-router.ts"
      provides: "staff 1042-S generate/correct/recipient-copy router (us-expansion gated)"
      contains: "assertUsExpansionEnabled"
  key_links:
    - from: "packages/api/src/services/form-1042s.service.ts"
      to: "packages/api/src/services/treaty-rate.service.ts applyTreaty"
      via: "box-2 rate + treaty article resolution with §875(d) gate"
      pattern: "applyTreaty"
    - from: "packages/api/src/root.ts"
      to: "form-1042s-router"
      via: "conditional spread via isUsExpansionRegistered"
      pattern: "isUsExpansionRegistered"
---

<objective>
Build the 1042-S deterministic core: the `form-1042s.service` (box aggregation, §875(d)-gated treaty-rate snapshot via `applyTreaty`, immutable supersede, idempotent audited batch), the recipient-copy PDF render/archive (mirror Copy-B), and the staff router wiring (us-expansion gated). This is independent of the P86 transmit seam — transmit is Plan 07.

Purpose: US-FORM-06 non-transmit core. Turns Plan 01's `form-1042s.service` + `form-1042s-pdf` RED scaffolds GREEN.
Output: the 1042-S service + recipient PDF + staff router; no IRIS XML/transmit (Plan 07).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-PATTERNS.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-RESEARCH.md

<interfaces>
<!-- Verified analog idioms — use directly. -->

form-1099-nec.service.ts (the verbatim structure to mirror):
  - FORBIDDEN_SNAPSHOT_KEYS set drops full ssn/tin (232-315); buildSnapshot keeps recipientTinLast4 only;
    sanitizeSnapshotValue strips forged full-identifier keys.
  - supersedeCorrected (359-395): updateMany {status:'ACTIVE'} → {status:'SUPERSEDED'} THEN create {status:'ACTIVE', corrected:true}; supersede BEFORE insert; in one $transaction.
  - fileCorrection (630-662): supersede + writeAuditLog({action:'form1099.correction'}).
  - generateBatch (486-595): reserve(key,TTL) → HIT/PENDING/MISS; complete(key,result,TTL); clear(key) on error; deterministic batchIdempotencyKey (406-412); writeAuditLog({action:'form1099.generate'}).
  - box amounts in minor units; threshold/figures from config table, never constants.

treaty-rate.service.ts applyTreaty (144-175):
  applyTreaty({ contractorResidency, asOf, override }) → { rate, article, source:'treaty'|'override'|'statutory_30', autoRate, autoArticle, auditRequired }
  STATUTORY_RATE = 30; US_INCOME_TYPE = 'business_profits'; hasTreatyRow = non-XX row with non-null treatyRate.
  override branch requires non-empty reason + auditRequired=true → caller writeAuditLog.

form-1099-nec-pdf.ts renderAndArchiveCopyB (39-138):
  lazy import('@react-pdf/renderer') + lazy template import; renders from stored immutable snapshot (never recompute);
  CAS guard updateMany where { id, pdfArchiveKey: null }; count===0 → already-claimed short-circuit;
  archive key `1099-nec/<orgId>/<id>.pdf`; putObjectAndSignDownload({ ttlSeconds: 60 }).

tax-form-routing (P85): read TaxFormSubmission.formType — W-8BEN/W-8BEN-E → 1042-S; W-9 → 1099-NEC (Pitfall 6).

require-us-expansion-flag.ts: assertUsExpansionEnabled(orgId, region) (per-request FORBIDDEN US_EXPANSION_DISABLED) + isUsExpansionRegistered() (conditional root.ts spread; QA_DEFAULT_ORG_ID force-registers).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: form-1042s.service — box mapping, §875(d) treaty snapshot, supersede, idempotent batch</name>
  <files>packages/api/src/services/form-1042s.service.ts</files>
  <behavior>
    - Routing reads TaxFormSubmission.formType: W-8 → 1042-S (W-9 recipients are skipped — they belong to 1099-NEC); never routes on nationality.
    - box-2 rate + treaty article resolved via applyTreaty, §875(d)-gated: W-8 chain complete → treaty rate (box3bChap3Rate + treatyArticle); incomplete → 30% statutory.
    - A missing/invalid W-form + US-source income → 30% statutory 1042-S path + escalation, never a silent skip.
    - CORRECTED = supersede ACTIVE→SUPERSEDED then insert ACTIVE in one $transaction; the filed row is never mutated.
    - generateBatch is idempotent (reserve/complete/clear) keyed `form1042s:batch:{org}:{payerOrg}:{taxYear}`; writeAuditLog({action:'form1042s.generate'}); fileCorrection writeAuditLog({action:'form1042s.correction'}).
    - snapshotJson keeps FTIN last-4 only; the sanitizer strips forged full-identifier keys.
  </behavior>
  <read_first>
    - packages/api/src/services/form-1099-nec.service.ts (verbatim structure: snapshot sanitizer 232-315, supersedeCorrected 359-395, fileCorrection 630-662, generateBatch 486-595, batchIdempotencyKey 406-412)
    - packages/api/src/services/treaty-rate.service.ts (applyTreaty 144-175; STATUTORY_RATE 24; US_INCOME_TYPE)
    - packages/api/src/lib/idempotency.ts (reserve/complete/clear)
    - packages/api/src/services/audit-writer.ts (writeAuditLog; pass tx in transactions)
    - the P85 tax-form-routing helper (W-8 → 1042-S vs W-9 → 1099-NEC; read TaxFormSubmission.formType)
    - packages/api/src/services/__tests__/form-1042s.service.test.ts (the RED scaffold from Plan 01)
    - packages/db/prisma/schema/tax.prisma (Form1042S model from Plan 02)
  </read_first>
  <action>
    Create `form-1042s.service.ts` mirroring `form-1099-nec.service.ts` structure with the D-07 differences. Routing: read `TaxFormSubmission.formType` (W-8BEN/W-8BEN-E → 1042-S; W-9 recipients skipped); a missing/invalid W-form + US-source income escalates to the 30% statutory path (never silent skip). Treaty snapshot: resolve box-2 rate + article via `applyTreaty({ contractorResidency, asOf, override })`, then apply a §875(d) gate — only use the treaty rate when the contractor's W-8 chain is complete; otherwise force 30% statutory. Snapshot the resolved rate onto `box3bChap3Rate` + `treatyArticle`. REPORTED-only — never mutate payouts (Phase 88 owns the deduction). Box amounts in minor units (USD cents); income/status/exemption/LOB codes adviser-verify at coding time (A1/A2). `snapshotJson` keeps FTIN last-4 only via a sanitizer mirroring `FORBIDDEN_SNAPSHOT_KEYS` / `sanitizeSnapshotValue`. CORRECTED supersede in one `$transaction` (supersede BEFORE insert). `generateBatch` idempotent via `idempotency.ts` keyed `form1042s:batch:{org}:{payerOrg}:{taxYear}`; `writeAuditLog({action:'form1042s.generate'})`; `fileCorrection` joins supersede + `writeAuditLog({action:'form1042s.correction'})`. Persistence writers + payment-data reads as injected ports where the live DB writer is supplied by the router (mirror the P86 form-1099-nec injected-port pattern so the deterministic core is unit-testable without a live DB).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- form-1042s.service && pnpm typecheck --filter=@contractor-ops/api</automated>
  </verify>
  <acceptance_criteria>
    - `form-1042s.service.test.ts` GREEN.
    - Tests prove: §875(d) gate (W-8 complete → treaty rate; incomplete → 30% statutory); routing reads formType not nationality; CORRECTED supersede in one `$transaction`; idempotent batch; snapshot FTIN last-4 only.
    - `grep -c "form1042s.generate\|form1042s.correction" packages/api/src/services/form-1042s.service.ts` ≥ 2 (audit actions present).
    - No payout-mutation call path (REPORTED-only).
  </acceptance_criteria>
  <done>form-1042s.service GREEN; treaty gate + supersede + idempotency + audit proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 1042-S recipient-copy PDF render/archive</name>
  <files>packages/api/src/services/form-1042s-pdf.ts, packages/api/src/pdf-templates/form-1042s-recipient-copy.tsx</files>
  <behavior>
    - renderForm1042SRecipientCopy(snapshot) renders from the stored immutable snapshot (never a live recompute), FTIN masked to last-4.
    - renderAndArchive uses a CAS guard (updateMany where pdfArchiveKey: null; count===0 → already-claimed short-circuit); archive key `1042-s/<orgId>/<id>.pdf`; putObjectAndSignDownload ttl 60s.
    - the template shows box 1a-1c income, box 2 gross, box 3a/3b ch3, box 7 withheld, recipient 13j/13k status, 13n LOB, treaty article; substitute black-ink (Pub 1179 idiom).
  </behavior>
  <read_first>
    - packages/api/src/services/form-1099-nec-pdf.ts (renderAndArchiveCopyB 39-138: lazy react-pdf import, CAS guard, archive key, signed URL)
    - packages/api/src/pdf-templates/form-1099-nec-copy-b.tsx (the recipient-copy template layout + last-4 masking)
    - packages/api/src/services/__tests__/form-1042s-pdf.test.ts (the RED scaffold from Plan 01)
    - the R2 region bucket helper (US tax-archive bucket; P83 R2_BUCKET_NAME_US routing)
  </read_first>
  <action>
    Create `pdf-templates/form-1042s-recipient-copy.tsx` mirroring `form-1099-nec-copy-b.tsx`: substitute black-ink recipient copy with the 1042-S box layout (box 1a-1c income code/gross, box 2, box 3a/3b ch3 exemption+rate, box 4a/4b ch4, box 7 federal tax withheld, recipient 13j ch3-status / 13k ch4-status / 13n LOB, treaty article). Renders ONLY from the stored snapshot; FTIN masked to last-4 (never full value in the PDF). `@react-pdf/renderer ^4.5.1` — do not assume the v3 API (the `REACT_PDF_VERSION = '3.4.5'` constant elsewhere is a stale label).
    Create `form-1042s-pdf.ts` mirroring `form-1099-nec-pdf.ts`: `renderForm1042SRecipientCopy(snapshot)` (lazy `await import('@react-pdf/renderer')` + lazy template import) + `renderAndArchive(db, formId)` with the CAS guard (`updateMany where { id, pdfArchiveKey: null }`, `count === 0` short-circuit), archive key `1042-s/<orgId>/<id>.pdf` (org-scoped), `putObjectAndSignDownload({ ttlSeconds: 60 })` to the US R2 tax bucket. The PDF is archived regardless of consent (D-09 — the consent gate is on *furnishing*, handled in the portal UI Plan 08), so generation does not check consent.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- form-1042s-pdf</automated>
  </verify>
  <acceptance_criteria>
    - `form-1042s-pdf.test.ts` GREEN.
    - Tests prove: render from the stored snapshot (no live recompute), FTIN last-4 only, archive key prefix `1042-s/<orgId>/`, CAS guard short-circuit on a claimed slot.
    - `grep -c "1042-s/" packages/api/src/services/form-1042s-pdf.ts` ≥ 1 (org-scoped key).
  </acceptance_criteria>
  <done>1042-S recipient-copy PDF render/archive GREEN; last-4 masking + CAS guard proven.</done>
</task>

<task type="auto">
  <name>Task 3: Staff 1042-S router (generate / correct / recipient-copy) — us-expansion gated</name>
  <files>packages/api/src/routers/finance/form-1042s-router.ts, packages/api/src/root.ts</files>
  <read_first>
    - packages/api/src/root.ts (the conditional-spread idiom: isUsExpansionRegistered() spreads US routers at boot; the 50-namespace appRouter)
    - packages/api/src/middleware/require-us-expansion-flag.ts (assertUsExpansionEnabled per-request; isUsExpansionRegistered conditional spread)
    - the P86 form-1099-nec staff router (or the nearest finance router) for the procedure shape, Zod .strict() inputs, and CONTRACTOR_PII:READ-gated full-FTIN reveal
    - packages/auth/src/permissions.ts (CONTRACTOR_PII:READ)
  </read_first>
  <action>
    Create `form-1042s-router.ts`: staff procedures `generateBatch`, `fileCorrection`, `getRecipientCopyUrl` (and read/list as needed), each with Zod `.strict()` inputs, tenant from session, `assertUsExpansionEnabled(organizationId, region)` per-request, and `writeAuditLog` on generate/correct (delegated to the service). The router supplies the live DB writers + payment-data reads to the injected-port service from Task 1. Full FTIN reveal stays behind `CONTRACTOR_PII:READ`, staff-router only; the recipient-copy URL surface uses the masked snapshot. Box amounts are computed server-side — never client-supplied (mass-assignment). Mount it conditionally in `root.ts` via `isUsExpansionRegistered()` (METHOD_NOT_FOUND when the US surface is off), mirroring P85/P86. Add the namespace to the appRouter spread.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- form-1042s && pnpm typecheck --filter=@contractor-ops/api && pnpm --filter @contractor-ops/api lint:audit-log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "assertUsExpansionEnabled" packages/api/src/routers/finance/form-1042s-router.ts` ≥ 1.
    - The router is spread conditionally via `isUsExpansionRegistered()` in root.ts (METHOD_NOT_FOUND when off — assert in test).
    - Inputs are Zod `.strict()`; box amounts are not in the client input schema (mass-assignment guard).
    - `pnpm typecheck --filter=@contractor-ops/api` + `lint:audit-log` pass.
  </acceptance_criteria>
  <done>Staff 1042-S router wired + conditionally spread; us-expansion gate + .strict() inputs + audit verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → 1042-S router → service | Box amounts, organizationId, rate must be server-derived |
| service → R2 archive | Recipient PDF must never contain a full FTIN |
| treaty table → snapshot | §875(d) gate must not silently grant a treaty rate without a complete W-8 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-04-01 | Information Disclosure | Full FTIN in snapshot/PDF/log | mitigate | snapshot sanitizer (FORBIDDEN keys); FTIN last-4 only in snapshot + PDF; full reveal behind CONTRACTOR_PII:READ staff-router only |
| T-87-04-02 | Tampering | Mass-assignment of box amounts / organizationId | mitigate | `.strict()` Zod DTOs; box figures computed server-side from settled payments; tenant from session |
| T-87-04-03 | Tampering | Immutability — editing a filed 1042-S | mitigate | append-only model; CORRECTED supersedes in one `$transaction`; the filed row is never updated |
| T-87-04-04 | Tampering / Repudiation | Double-file on retry | mitigate | `idempotency.ts` reserve/complete/clear; CAS compare-and-swap on the PDF archive slot |
| T-87-04-05 | (compliance) | Treaty rate applied without a complete W-8 chain | mitigate | §875(d) gate forces 30% statutory when the W-8 chain is incomplete |
| T-87-04-06 | Elevation / Info Disclosure | Cross-org read of Form1042S | mitigate | org-scoped findFirst + tenant middleware; per-model leak test (Plan 02) |
| T-87-04-SC | Tampering | npm/pip/cargo installs | accept | No new external packages — reuse @react-pdf/renderer, idempotency, audit-writer |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- form-1042s` green (service + pdf + router).
- `pnpm typecheck --filter=@contractor-ops/api` + `lint:audit-log` green.
</verification>

<success_criteria>
- US-FORM-06 (non-transmit core): 1042-S generated for a foreign W-8 recipient with §875(d)-gated box-2 withholding + treaty-article snapshot, immutable supersede, idempotent audited batch, and a consent-archived recipient PDF (FTIN last-4 only).
</success_criteria>

<output>
Create `.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-04-SUMMARY.md` when done.
</output>
