---
phase: 87-theme-a-1042-s-us-classification-determination-letter
plan: 08
type: execute
wave: 5
depends_on: [87-03, 87-05, 87-06]
files_modified:
  - apps/web-vite/src/components/contractors/classification/us-classification-result.tsx
  - apps/web-vite/src/components/contractors/classification/ab5-watchlist-flag.tsx
  - apps/web-vite/src/components/contractors/classification/classification-override-dialog.tsx
  - apps/web-vite/src/components/contractors/classification/hooks/use-us-classification.ts
  - apps/web-vite/src/components/contractors/classification-documents/generate-determination-letter-button.tsx
  - apps/web-vite/src/components/contractors/classification-documents/hooks/use-generate-determination-letter.ts
  - apps/web-vite/src/components/contractors/form-1099k-band.tsx
  - apps/web-vite/src/components/contractors/hooks/use-1099k-tracker.ts
  - apps/web-vite/src/messages/en.json
  - apps/web-vite/src/messages/en-US.json
  - apps/web-vite/src/messages/de.json
  - apps/web-vite/src/messages/pl.json
  - apps/web-vite/src/messages/ar.json
autonomous: true
requirements: [US-CLASS-01, US-CLASS-02, US-CLASS-03, US-CLASS-04]

must_haves:
  truths:
    - "Staff see the US classification result (verdict + scored factors + AB5 amber flag + §530 info flag + citations) with the sticky advisory banner and blocking disclaimer"
    - "Staff can override the classification with a required reason (audit-logged)"
    - "Staff can generate + download a Determination Letter via the SDS-mirror approval gate"
    - "The contractor profile shows a read-only informational 1099-K band (SAFE/APPROACHING/OVER), amber at most, with no filing affordance"
    - "All new strings are i18n keys at en/en-US/de/pl/ar parity; AB5 is amber, §530 is info-blue, the 1099-K band is never destructive"
    - "Implements D-03/D-05 (advisory classification surface), D-14 (i18n parity en/en-US/de/pl/ar)"
  artifacts:
    - path: "apps/web-vite/src/components/contractors/classification/hooks/use-us-classification.ts"
      provides: "sole tRPC boundary for the US classification section"
      contains: "useTRPC"
    - path: "apps/web-vite/src/components/contractors/classification/ab5-watchlist-flag.tsx"
      provides: "amber advisory AB5 flag (never destructive)"
      contains: "warning"
    - path: "apps/web-vite/src/components/contractors/form-1099k-band.tsx"
      provides: "read-only informational 1099-K band"
      contains: "Skeleton"
  key_links:
    - from: "apps/web-vite/src/components/contractors/classification/us-classification-result.tsx"
      to: "use-us-classification hook"
      via: "wired section consumes the hook; no direct tRPC in the section"
      pattern: "use-us-classification"
    - from: "apps/web-vite/src/components/contractors/form-1099k-band.tsx"
      to: "use-1099k-tracker hook"
      via: "read-only band query"
      pattern: "use-1099k-tracker"
---

<objective>
Build the staff US classification result + override surface, the Determination-Letter generate/download button (SDS-mirror approval gate), and the read-only informational 1099-K band on the contractor profile — extending v5.0/P84/P85 idioms per the approved 87-UI-SPEC. All strings i18n at en/en-US/de/pl/ar parity.

Purpose: the UI half of US-CLASS-01/02/03/04. The 1042-S filing + portal UI is Plan 09 (it mirrors P86 components not yet on disk).
Output: classification result + override dialog + AB5 flag + letter button + 1099-K band + hooks + i18n.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-UI-SPEC.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
<!-- UI layering + the v5.0/P84/P85 components to extend (per UI-SPEC Component Inventory). -->

Layering (CLAUDE.md + UI-SPEC): Page (thin composer, Suspense/permissions, no tRPC) → wired section
  (4-state loading/empty/error/loaded branch, no direct tRPC) → hook (components/{domain}/hooks/use-*.ts,
  SOLE tRPC boundary) → presentational (props in / JSX out). NO new *-container.tsx.

Extend / reuse verbatim:
  - classification-tile.tsx toneForOutcome (employee=destructive, indeterminate=warning, contractor=success)
  - ClassificationAdvisoryBanner (sticky amber role="note"); ClassificationDisclaimerDialog (blocking AlertDialog, no dismiss)
  - LegalReferenceCollapsible (citations); reui Stepper + ClassificationProgressBar (the multi-country wizard renders US)
  - generate-sds-button.tsx (the SDS-mirror approval gate: typed name + checkbox → primary CTA → opens signed R2 URL; aria-busy; role=alert error)
  - SsnMaskedReveal (FTIN/SSN last-4 only, contractorPii:read gated, audit-logged)
  - AnimateIn (delay 0|1|2|... staggered; TiltCard entrance={false} inside)
  - Dialog body/footer convention: DialogContent → DialogBody (scroll) + DialogFooter (sticky)

Color rules (UI-SPEC): AB5 flag = warning amber NEVER destructive; §530 flag = info blue; verdict pill may use
  destructive for likely-employee; 1099-K band = amber at most, NEVER destructive, NO filing CTA.

Gates: check:web-vite-data-layer (hook+section first), check:web-vite-page-shells, check:web-vite-dialog-pattern,
  i18n:parity, lint:no-hardcoded-strings. WCAG: visible --ring focus; Label-in-Name; aria-live on verdict/AB5 change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: US classification result + AB5 flag + override dialog + hook</name>
  <files>apps/web-vite/src/components/contractors/classification/us-classification-result.tsx, apps/web-vite/src/components/contractors/classification/ab5-watchlist-flag.tsx, apps/web-vite/src/components/contractors/classification/classification-override-dialog.tsx, apps/web-vite/src/components/contractors/classification/hooks/use-us-classification.ts</files>
  <read_first>
    - the frontend-design plugin SKILL.md + run `semble search` for the classification UI before editing (CLAUDE.md [ui] binding)
    - apps/web-vite/src/components/contractors/classification/classification-tile.tsx (toneForOutcome; Bdi; the 4-state branch)
    - the existing ClassificationAdvisoryBanner + ClassificationDisclaimerDialog + LegalReferenceCollapsible
    - apps/web-vite/ARCHITECTURE.md (Page → section → hook → presentational; the data-layer rule)
    - .planning/phases/87-.../87-UI-SPEC.md (Component Inventory + color reconciliation + interaction contract)
    - an existing override dialog (the treaty/reverse-charge override-with-reason UI, if present) for the required-reason + DialogBody/DialogFooter idiom
  </read_first>
  <action>
    Write the hook FIRST (`hooks/use-us-classification.ts`) as the sole tRPC boundary — read the US assessment/outcome + the override mutation (the routers are already multi-country; reuse the generic classification procedures wired in Plan 03). Then the wired section `us-classification-result.tsx` (4-state: Skeleton loading with aria-busy/aria-live; empty with the UI-SPEC copy + primary CTA; role=alert error + Reload; loaded). Loaded shows the verdict pill (toneForOutcome — likely-employee=destructive, indeterminate=warning, likely-contractor=success), the scored federal factors via a `dl` grid + LegalReferenceCollapsible citations, and beside the verdict an `ab5-watchlist-flag.tsx` (amber `warning` Badge + Tooltip — NEVER destructive, D-04) and a §530 info-blue chip. Mount the sticky `ClassificationAdvisoryBanner` (role="note") and the blocking `ClassificationDisclaimerDialog` before the outcome. `classification-override-dialog.tsx` uses DialogBody (scroll) + DialogFooter (sticky) + a required-reason Textarea + checkbox (the override is audit-logged server-side). Announce verdict + AB5-flag changes via aria-live="polite". All strings are i18n keys. Use AnimateIn (title delay 0, banner/stepper delay 1, body delay 2; TiltCard entrance={false}). Bdi for engagement/recipient names (RTL).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite exec tsx ../../scripts/check-web-vite-data-layer.ts 2>/dev/null || pnpm check:web-vite-data-layer; pnpm check:web-vite-page-shells; pnpm --filter @contractor-ops/web-vite test -- classification/us-classification-result</automated>
  </verify>
  <acceptance_criteria>
    - `check:web-vite-data-layer` passes (no direct tRPC in the section; the hook is the sole boundary).
    - `check:web-vite-page-shells` passes (the wired section conforms to the page-shell convention).
    - `grep -c "warning" apps/web-vite/src/components/contractors/classification/ab5-watchlist-flag.tsx` ≥ 1 AND `grep -c "destructive" apps/web-vite/src/components/contractors/classification/ab5-watchlist-flag.tsx` == 0 (AB5 never destructive).
    - The section branches all four states (Skeleton/empty/error role=alert/loaded); the override dialog requires a non-empty reason.
    - No hardcoded user-facing strings (`lint:no-hardcoded-strings` passes for the new files).
  </acceptance_criteria>
  <done>US classification result + AB5 flag + override dialog + hook; data-layer + page-shells + AB5-amber proven.</done>
</task>

<task type="auto">
  <name>Task 2: Determination-Letter button + 1099-K profile band + hooks</name>
  <files>apps/web-vite/src/components/contractors/classification-documents/generate-determination-letter-button.tsx, apps/web-vite/src/components/contractors/classification-documents/hooks/use-generate-determination-letter.ts, apps/web-vite/src/components/contractors/form-1099k-band.tsx, apps/web-vite/src/components/contractors/hooks/use-1099k-tracker.ts</files>
  <read_first>
    - run `semble search` for generate-sds-button + the document-history panel before editing
    - apps/web-vite/src/components/contractors/classification-documents/generate-sds-button.tsx (the verbatim approval-gate mirror: amber card, typed name + checkbox → primary CTA → opens signed R2 URL; aria-busy; role=alert)
    - the document-history-list / classification-documents-panel (to add the US_DETERMINATION_LETTER kind row)
    - a P84/P85 profile-card component (the band-card idiom for form-1099k-band)
    - .planning/phases/87-.../87-UI-SPEC.md (1099-K band copy SAFE/APPROACHING/OVER; informational-only invariant; font-mono for figures)
  </read_first>
  <action>
    `generate-determination-letter-button.tsx` = a verbatim mirror of `generate-sds-button.tsx`: an SDS-mirror approval gate (amber card, typed recipient/client name + checkbox confirming the figures were reviewed) → the "Generate determination letter" primary CTA → opens the signed R2 URL in a new tab; `aria-busy` while generating; `role="alert"` on error. Its hook `use-generate-determination-letter.ts` mirrors `useGenerateSds` (the sole tRPC boundary calling the Plan 05 document router). Add the `US_DETERMINATION_LETTER` kind row to the existing document-history list (reuse, do not rebuild).
    `form-1099k-band.tsx` = a read-only wired band (Card/inline band + Badge + Skeleton) for the contractor profile: SAFE (neutral/secondary), APPROACHING (amber `warning` + TrendingUp), OVER (amber `warning` + AlertTriangle — NEVER destructive). `font-mono` for cumulative payout + transaction count vs the {taxYear} threshold. Copy from the UI-SPEC ("this is a heads-up — we don't file 1099-K"). NO filing/generate/fix affordance (D-10 informational-only invariant). Its hook `use-1099k-tracker.ts` is read-only (the cron writes state; the UI only surfaces it) — the sole tRPC boundary calling the Plan 06 read router. All strings i18n.
  </action>
  <verify>
    <automated>pnpm check:web-vite-data-layer; pnpm check:web-vite-page-shells; pnpm --filter @contractor-ops/web-vite test -- form-1099k-band</automated>
  </verify>
  <acceptance_criteria>
    - `check:web-vite-page-shells` passes (the wired band section conforms to the page-shell convention).
    - `grep -c "destructive" apps/web-vite/src/components/contractors/form-1099k-band.tsx` == 0 (band never destructive).
    - The 1099-K band has NO "file"/"generate"/"fix" button (grep the file for those CTA labels → absent); it is read-only.
    - The letter button gates generation behind a typed-name + checkbox approval; `aria-busy` on generate, `role="alert"` on error.
    - `check:web-vite-data-layer` passes; hooks are the sole tRPC boundary.
  </acceptance_criteria>
  <done>Determination-letter button + 1099-K read-only band + hooks; informational-only + approval-gate proven.</done>
</task>

<task type="auto">
  <name>Task 3: i18n parity (en/en-US/de/pl/ar) for all new namespaces</name>
  <files>apps/web-vite/src/messages/en.json, apps/web-vite/src/messages/en-US.json, apps/web-vite/src/messages/de.json, apps/web-vite/src/messages/pl.json, apps/web-vite/src/messages/ar.json</files>
  <read_first>
    - the existing message files (the namespace structure + how en-US thin overrides work — fallbackPeers per P84)
    - .planning/phases/87-.../87-UI-SPEC.md (Copywriting Contract — the canonical en source + namespaces: UsClassification, Tax1042SBatch/Filing, Form1099KTracker, Tax1042SConsent)
  </read_first>
  <action>
    Add the `UsClassification` + `Form1099KTracker` namespaces (and the determination-letter approval keys) with the UI-SPEC canonical en copy, an en-US thin override where US-idiomatic wording differs, and de/pl/ar translations at full parity (ar is RTL; mark machine-translated strings for native review per the standing i18n_review deferred item — no split needed, the ar strings ship at key-parity but are tagged for native review). Do NOT translate the locked `SOFTWARE_NOT_LEGAL_ADVICE_EN` phrase — it is imported from `@contractor-ops/validators`, never a translation key. (The `Tax1042SBatch`/`Tax1042SFiling`/`Tax1042SConsent` namespaces are added in Plan 09 alongside their components.)
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite i18n:parity && pnpm --filter @contractor-ops/web-vite lint:no-hardcoded-strings 2>/dev/null || pnpm lint:no-hardcoded-strings</automated>
  </verify>
  <acceptance_criteria>
    - `i18n:parity` passes (en/de/pl/ar key parity; en-US fallback-aware).
    - The `UsClassification` + `Form1099KTracker` keys exist in all five files.
    - The machine-translated `ar` strings are tagged/marked for native review per the standing i18n_review deferred item (i18n:parity stays green — the marking is a review flag, not a parity gap).
    - The locked disclaimer phrase is NOT present as a translation key (it stays in @contractor-ops/validators).
  </acceptance_criteria>
  <done>i18n parity for UsClassification + Form1099KTracker; ar tagged for native review; locked phrase kept out of messages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → tRPC hook | All data crosses the hook (sole boundary); no full FTIN in the DOM |
| classification outcome → UI | Must read as advisory, never a legal verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-08-01 | Information Disclosure | Full FTIN/SSN in the DOM | mitigate | SsnMaskedReveal last-4 only, contractorPii:read gated, audit-logged; full value never reaches the DOM |
| T-87-08-02 | (compliance/liability) | Classification UI read as a legal verdict | mitigate | sticky advisory banner (role=note) + blocking disclaimer + AB5 amber + §530 info + adviser-verify note (D-05) |
| T-87-08-03 | (compliance) | 1099-K band implying a filing obligation | mitigate | informational-only invariant — no filing CTA, amber at most, "we don't file 1099-K" copy (D-10) |
| T-87-08-04 | Repudiation | Override without a reason | mitigate | override dialog requires a non-empty reason; server audit-logs (Plan 03) |
| T-87-08-SC | Tampering | shadcn registry / npm installs | accept | UI-SPEC Registry Safety: only already-installed local primitives + v5.0/P84/P85 components; no new registry blocks pulled |
</threat_model>

<verification>
- `pnpm check:web-vite-data-layer` + `check:web-vite-page-shells` + `check:web-vite-dialog-pattern` green.
- `pnpm --filter @contractor-ops/web-vite i18n:parity` + `lint:no-hardcoded-strings` green.
- Scoped web-vite tests for the new components green (NEVER the full unscoped suite — RAM).
</verification>

<success_criteria>
- US-CLASS-01/02/04 (UI): staff classification result + AB5 amber flag + §530 info flag + reason-required override + SDS-mirror determination-letter button, all advisory-framed.
- US-CLASS-03 (UI): read-only informational 1099-K band on the profile, amber at most, no filing affordance.
</success_criteria>

<output>
Create `.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-08-SUMMARY.md` when done.
</output>
