---
phase: 96-theme-b-employee-self-service-portal
plan: 04
type: execute
wave: 4
depends_on: ["01", "02", "03"]
files_modified:
  - packages/api/src/routers/portal/portal-employee-router.ts
  - packages/api/src/portal-root.ts
  - packages/validators/src/portal-employee.ts
  - packages/validators/src/index.ts
autonomous: true
requirements: [EMP-PORTAL-02]

must_haves:
  truths:
    - "`portalEmployee` router is mounted on `portalAppRouter` ONLY when `module.employee-portal` is registered (dark-mount, mirror `conditionalWorkforceRouters`); every procedure re-asserts the flag per request"
    - "`getLeaveBalance` / `listMyLeaveRequests` / `getMyTime` / `getMyEwidencja` read ONLY the session subject's rows (`where: { workerId: ctx.workerId, organizationId: ctx.organizationId }`) — no client `workerId` accepted"
    - "`submitTimeOffRequest` derives `workerId` from `ctx.workerId`, reuses `computeLeaveBalance` + `createApprovalFlow`/`routeToLeaveChain` (no reimplemented business logic), and `writeAuditLog`s in the same transaction; its Zod input is `.strict()` and has NO `workerId` field"
    - "Document upload uses the server-key `PendingUpload(purpose='PORTAL_DOC_VERSION')` flow — no client-supplied storageKey — scoped to the session worker"
    - "The 96-03 RED tests portal-employee-idor / portal-timeoff-request / portal-root-gating flip GREEN"
  artifacts:
    - path: "packages/api/src/routers/portal/portal-employee-router.ts"
      provides: "portalEmployeeRouter — self-scoped leave/time reads + time-off write + upload"
      contains: "submitTimeOffRequest"
    - path: "packages/validators/src/portal-employee.ts"
      provides: "portalTimeOffRequestInput (NO workerId) + upload inputs (.strict())"
      contains: "portalTimeOffRequestInput"
  key_links:
    - from: "portal-employee-router.ts submitTimeOffRequest"
      to: "services/approval-engine.ts createApprovalFlow + routeToLeaveChain"
      via: "portal write injects ctx.workerId then calls the shared leave services in a tx"
      pattern: "routeToLeaveChain"
---

<objective>
Build the employee-self backend surface (EMP-PORTAL-02): a `portalEmployee` tRPC namespace whose reads are session-scoped and whose one write (time-off request + document upload) reuses the shipped P92/P91 domain services, flipping the 96-03 RED net (employee-IDOR, time-off, gating) GREEN.

Purpose: the data boundary the employee dashboard (96-07) consumes. Every read filters on `ctx.workerId` (RESEARCH Pattern 1); the time-off write injects `ctx.workerId` and calls `computeLeaveBalance` + `createApprovalFlow`/`routeToLeaveChain` (Pattern 2, correction C5) — it reuses the services, never the staff `workerId`-from-input procedure. Upload uses the server-key `PendingUpload` flow (Pattern 4). Manager surface + akta/pay-stub are separate plans (96-06, 96-05).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-CONTEXT.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-RESEARCH.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-VALIDATION.md

<interfaces>
- `packages/api/src/middleware/portal-auth.ts` (96-02) — `portalEmployeeProcedure` (ctx.workerId, employeeProfile, db, organizationId, region).
- `packages/api/src/services/leave-balance.ts:31-44` — `computeLeaveBalance({ jurisdiction, leaveKind, tenureYears, etat })` + `recomputeBalanceCache`.
- `packages/api/src/services/approval-engine.ts` — `createApprovalFlow`, `routeToLeaveChain` (called by staff `leave.submitLeaveRequest`).
- `packages/api/src/routers/workforce/leave.ts:108-120` — the staff submit shape to mirror MINUS the client `workerId`.
- `packages/validators/src/leave.ts` — `submitLeaveRequestInput` (has `workerId` — the portal input omits it).
- `packages/api/src/routers/workforce/employee-time.ts` + `ewidencja.ts` — the read shapes to expose read-only.
- `packages/db/prisma/schema/portal.prisma:97-123` — `PendingUpload` (`PORTAL_DOC_VERSION`) server-key upload.
- `packages/api/src/routers/portal/portal-tax-1099-router.ts:91-109` — `writeAuditLog({ actorType, actorId, ... })` precedent (use `actorType:'EMPLOYEE'` / actorId `ctx.workerId`; add the enum value if absent).
- `packages/api/src/root.ts:185-196,262` — the `conditionalWorkforceRouters` dark-mount pattern to mirror on `portalAppRouter`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portal-employee Zod inputs (no workerId)</name>
  <files>packages/validators/src/portal-employee.ts, packages/validators/src/index.ts</files>
  <read_first>
    - `packages/validators/src/leave.ts` (submitLeaveRequestInput) + `96-RESEARCH.md` § Corrections C5
  </read_first>
  <action>
    Create `portal-employee.ts`: `portalTimeOffRequestInput` = the staff `submitLeaveRequestInput` shape with `workerId` REMOVED (`leaveTypeId`, `startDate`, `endDate`, `requestedMinutes`, optional `note`), `.strict()` + the same start≤end refine; `portalLeaveBalanceQueryInput` (`leaveTypeId`, optional `year`) `.strict()`; `portalAktaUploadRequestInput` / `portalAktaUploadConfirmInput` (`documentId` + mime/size — server-key flow, NO `storageKey`) `.strict()`. Export from `index.ts`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/validators test 2>&1 | tail -10 && grep -c "workerId" packages/validators/src/portal-employee.ts</automated>
  </verify>
  <acceptance_criteria>
    - `portalTimeOffRequestInput` has NO `workerId` (grep count 0); all inputs `.strict()`.
  </acceptance_criteria>
  <done>The portal write inputs cannot carry another worker's id.</done>
</task>

<task type="auto">
  <name>Task 2: portalEmployeeRouter — self-scoped reads + time-off write + upload</name>
  <files>packages/api/src/routers/portal/portal-employee-router.ts, packages/api/src/portal-root.ts</files>
  <read_first>
    - `portal-auth.ts` (96-02 procedures) + `leave.ts:108-120` + `leave-balance.ts:31-44` + `approval-engine.ts` + `portal.prisma:97-123` + `root.ts:185-196,262`
  </read_first>
  <action>
    Create `portalEmployeeRouter`:
    - `getDashboard` — a compact aggregate (balance summary + next approval + recent time) for the landing card, all `ctx.workerId`-scoped.
    - `getLeaveBalance(input: portalLeaveBalanceQueryInput)` — `computeLeaveBalance` for the caller's `employeeProfile` (jurisdiction from countryCode, etat from the profile), self-scoped.
    - `listMyLeaveRequests` — `ctx.db.leaveRequest.findMany({ where: { workerId: ctx.workerId, organizationId } })`.
    - `getMyTime` / `getMyEwidencja` — read-only projections of the employee-time / ewidencja rows for `ctx.workerId`.
    - `submitTimeOffRequest(input: portalTimeOffRequestInput)` — assert the portal flag; open `ctx.db.$transaction`; inject `workerId: ctx.workerId`; `computeLeaveBalance` guard; `createApprovalFlow` + `routeToLeaveChain` (the same services staff uses); `writeAuditLog({ actorType:'EMPLOYEE', actorId: ctx.workerId, action:'LEAVE_REQUEST_SUBMITTED', resourceType:'LEAVE_REQUEST', resourceId, tx })`.
    - `requestAktaUpload` / `confirmAktaUpload` — the `PendingUpload(purpose='PORTAL_DOC_VERSION')` server-key flow scoped to `ctx.workerId`; audit on confirm.
    Mount on `portalAppRouter` behind the dark-mount gate (mirror `conditionalWorkforceRouters` — include `portalEmployee` only when `module.employee-portal` is registered), and re-assert the flag per procedure. Use `plain()`/`select` projections so router output types never reference the generated Prisma client (TS2742).
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test portal-employee-idor portal-timeoff-request portal-root-gating 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Every read `where` includes `workerId: ctx.workerId` + `organizationId`; no procedure accepts a `workerId`.
    - `submitTimeOffRequest` calls `createApprovalFlow`/`routeToLeaveChain` (grep) and `writeAuditLog` in-tx; no reimplemented leave logic.
    - 96-03 RED tests employee-idor / timeoff-request / root-gating now GREEN.
  </acceptance_criteria>
  <done>The employee-self data + write surface is live, self-scoped, and IDOR-fenced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Session subject → data rows | Every employee read/write is scoped to `ctx.workerId`; a client id is never trusted. |
| Client → storage | Upload uses a server-minted key; no client `storageKey`. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-04-01 | Information Disclosure | reading another worker's balance/time | mitigate | Self-scoped `where` (Task 2); `portal-employee-idor` GREEN. |
| T-96-04-02 | Tampering | submitting time-off for another worker | mitigate | `.strict()` no-`workerId` input + session-derived workerId (Tasks 1,2); `portal-timeoff-request` GREEN. |
| T-96-04-03 | Tampering | cross-tenant document path injection on upload | mitigate | `PendingUpload` server-key flow (Task 2); no client storageKey. |
| T-96-04-04 | Repudiation | an unaudited portal write | mitigate | `writeAuditLog` in-tx on submit + confirm (Task 2); `lint:audit-log` green. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test portal-employee-idor portal-timeoff-request portal-root-gating` GREEN; `pnpm typecheck --filter=@contractor-ops/api` green.
- `pnpm lint:audit-log` + `pnpm lint:schema` + `pnpm lint:logs` + `pnpm lint:silent-catch` green.
- Wiki: `structure/api-routers-catalog.md` (new `portalEmployee` namespace) + the portal domain page (employee reads/writes + upload flow) updated in this change set; verify `portal-root.ts`.
</verification>

<success_criteria>
`portalEmployee` is a dark-mounted, flag-gated namespace whose reads are `ctx.workerId`-scoped and whose time-off write + upload reuse the P92/P91 services (no reimplemented logic) under audit. The 96-03 employee-IDOR, time-off, and gating tests are GREEN. Akta/pay-stub (96-05) and manager (96-06) are separate.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-04-SUMMARY.md` when done.
</output>
