---
phase: 96-theme-b-employee-self-service-portal
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - packages/api/src/routers/portal/__tests__/portal-employee-idor.test.ts
  - packages/api/src/routers/portal/__tests__/portal-manager-idor.test.ts
  - packages/api/src/routers/portal/__tests__/portal-timeoff-request.test.ts
  - packages/api/src/routers/portal/__tests__/portal-akta-selfview.test.ts
  - packages/api/src/routers/portal/__tests__/portal-paystub-unavailable.test.ts
  - packages/api/src/routers/portal/__tests__/portal-root-gating.test.ts
  - packages/api/src/routers/portal/__tests__/portal-fixtures.ts
autonomous: true
requirements: [EMP-PORTAL-01, EMP-PORTAL-02, EMP-PORTAL-03]

must_haves:
  truths:
    - "The two-employee IDOR test exists and is RED: employee A's portal session cannot read B's leave balance / akta / employee-time; a client-supplied workerId is rejected by `.strict()`; a cross-org token reads nothing"
    - "The manager IDOR test exists and is RED: a manager cannot read or approve for a worker who is not their report; a non-manager has no /team surface; cross-org blocked"
    - "leave-from-portal, entitlement-scoped akta, pay-stub-unavailable, and dark-flag gating tests exist and are RED (they reference the not-yet-built portalEmployee/portalManager routers)"
    - "Every RED test drives a portalEmployee/portalManager caller through `portalEmployeeProcedure`/`portalManagerProcedure` (96-02) with a real two-employee, two-org fixture"
    - "The RED scaffolds do not brick `tsc` — `packages/api` already excludes `src/**/__tests__/**`"
  artifacts:
    - path: "packages/api/src/routers/portal/__tests__/portal-employee-idor.test.ts"
      provides: "the load-bearing two-employee + cross-org IDOR fence (RED)"
      contains: "portal-employee-idor"
    - path: "packages/api/src/routers/portal/__tests__/portal-fixtures.ts"
      provides: "two-employee + manager + two-org portal test fixture (Worker/EmployeeProfile/PortalSession/leave/akta)"
      contains: "makePortalEmployeeFixture"
  key_links:
    - from: "portal-employee-idor.test.ts"
      to: "portalEmployeeProcedure (96-02)"
      via: "constructs an employee caller and asserts self-only reads; fails until 96-04/05 land the router"
      pattern: "portalEmployee"
---

<objective>
Seed the RED net — the security-first failing tests that Wave 4 flips GREEN — BEFORE any employee data router exists. The load-bearing test is the **two-employee IDOR fence**: an employee portal session sees ONLY its own records (D-03, the CONTEXT's non-negotiable). Alongside it: manager-non-report + cross-org IDOR, leave-request-from-portal session-scoping, entitlement-scoped akta, pay-stub-unavailable, and dark-flag gating.

Purpose: make the security invariants executable and RED so an executor cannot mark Wave 4 done without them going GREEN. These tests reference the not-yet-built `portalEmployee`/`portalManager` procedures (RED via missing router); `packages/api` excludes `__tests__` from `tsc`, so they do not brick the package typecheck. No product code in this plan.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-CONTEXT.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-RESEARCH.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-VALIDATION.md

<interfaces>
- `packages/api/src/middleware/portal-auth.ts` (96-02) — `portalEmployeeProcedure`/`portalManagerProcedure` the tests construct callers against.
- `packages/api/src/routers/portal/portal-profile-router.ts:389-410` — the self-scoping precedent the tests assert (`{ workerId: ctx.workerId, organizationId }`).
- `packages/api/src/routers/__tests__/*` + `packages/api/vitest.config.ts` — the existing tRPC caller-factory + DB fixture harness to mirror.
- `packages/db/prisma/schema/{worker,employee,personnel,leave}.prisma` — the models the fixture seeds.
- `96-RESEARCH.md` § The employee-IDOR fence, § Validation Architecture; `96-VALIDATION.md` § Wave 3 Requirements.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Portal test fixture (two employees, a manager, two orgs)</name>
  <files>packages/api/src/routers/portal/__tests__/portal-fixtures.ts</files>
  <read_first>
    - the existing DB-fixture pattern in `packages/api/src/routers/__tests__/*` + `96-RESEARCH.md` § The employee-IDOR fence
  </read_first>
  <action>
    Create `makePortalEmployeeFixture()` seeding (in the test DB / mocked prisma per the package harness): org A with employee A (Worker EMPLOYEE + EmployeeProfile + PortalSession subjectType EMPLOYEE) and employee B (a peer, not A's report); a manager M in org A whose reports include A (A.managerWorkerId = M.workerId) but NOT B; org C with employee X. Seed each with a leave balance/ledger row, a PersonnelFile with a document in SECTION_A and one in SECTION_C, and an employee-time record. Export helpers to build a `portalEmployeeProcedure` caller and a `portalManagerProcedure` caller for a given session subject.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test portal-fixtures 2>&1 | tail -8 || echo "fixture module compiles (no test file yet)"</automated>
  </verify>
  <acceptance_criteria>
    - Two employees (peer, not manager/report), a manager over exactly one, a second org.
    - Section A + Section C documents seeded (for the allowlist test).
  </acceptance_criteria>
  <done>Every RED test builds real two-subject, two-org callers from one fixture.</done>
</task>

<task type="auto">
  <name>Task 2: The two-employee IDOR test (the load-bearing fence) — RED</name>
  <files>packages/api/src/routers/portal/__tests__/portal-employee-idor.test.ts</files>
  <read_first>
    - `96-RESEARCH.md` § The employee-IDOR fence + `portal-profile-router.ts:389-410`
  </read_first>
  <action>
    Write (RED): employee A's caller reads `portalEmployee.getLeaveBalance` / `getMyAkta` / `getMyTime` and gets ONLY A's rows; there is NO `workerId` argument to inject (assert the input schema is `.strict()` and rejects `{ workerId: B }`); a caller built from org C's session reads nothing of org A (cross-org). Assert the read `where` is session-derived (A never sees B's balance/akta/time even by id).
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test portal-employee-idor 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Test RED (missing `portalEmployee` router) — the failure is "router/procedure not found", not a syntax error.
    - Covers self-only, no-workerId-injection, cross-org.
  </acceptance_criteria>
  <done>The load-bearing IDOR fence is executable and RED.</done>
</task>

<task type="auto">
  <name>Task 3: Manager IDOR + leave-from-portal + akta + pay-stub + gating — RED</name>
  <files>packages/api/src/routers/portal/__tests__/portal-manager-idor.test.ts, packages/api/src/routers/portal/__tests__/portal-timeoff-request.test.ts, packages/api/src/routers/portal/__tests__/portal-akta-selfview.test.ts, packages/api/src/routers/portal/__tests__/portal-paystub-unavailable.test.ts, packages/api/src/routers/portal/__tests__/portal-root-gating.test.ts</files>
  <read_first>
    - `96-VALIDATION.md` § Per-Task Verification Map + `96-RESEARCH.md` § Widget Gating Map, § Corrections C3/C4/C5
  </read_first>
  <action>
    Write (all RED):
    - `portal-manager-idor` — manager M can `listReportLeaveRequests`/`approveReportLeaveRequest` for A (a report) but NOT for B (non-report → FORBIDDEN/NOT_FOUND); a non-manager employee has no `portalManager` surface (procedure rejects); cross-org manager blocked.
    - `portal-timeoff-request` — `portalEmployee.submitTimeOffRequest` input has NO `workerId`; the handler derives it from the session, computes balance, creates the approval flow, and writes an audit row in-tx; a `{ workerId }` in the payload is a `.strict()` rejection.
    - `portal-akta-selfview` — `getMyAkta` returns A's own SECTION_A doc but NEVER the SECTION_C doc (allowlist); no client `workerId`/`section` trusted.
    - `portal-paystub-unavailable` — `getPayStubAvailability` returns `{ available:false, reason:'EXTERNAL_PAYROLL' }`.
    - `portal-root-gating` — with `module.employee-portal` OFF, `portalEmployee`/`portalManager` are absent from `portalAppRouter` (METHOD_NOT_FOUND) and each procedure re-asserts the flag.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test portal-manager-idor portal-timeoff-request portal-akta-selfview portal-paystub-unavailable portal-root-gating 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All five RED via missing routers (not syntax errors).
    - Manager test asserts report-only; time-off test asserts no-workerId-input; akta test asserts section-C exclusion; gating test asserts METHOD_NOT_FOUND when dark.
  </acceptance_criteria>
  <done>The full security/behavior RED net is seeded before any router exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test net → impl | The RED net is the executable contract Wave 4 must satisfy; a green Wave 4 with a RED IDOR test is a hard stop. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-03-01 | Information Disclosure | employee A reading B's records (the core IDOR) | mitigate | `portal-employee-idor` proves self-only + no-injection + cross-org (Task 2); RED until 96-04/05. |
| T-96-03-02 | Elevation of Privilege | a manager acting for a non-report | mitigate | `portal-manager-idor` proves report-only (Task 3); RED until 96-06. |
| T-96-03-03 | Tampering | a portal caller submitting time-off for another worker | mitigate | `portal-timeoff-request` proves session-derived workerId + `.strict()` no-workerId (Task 3). |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test portal-employee-idor portal-manager-idor portal-timeoff-request portal-akta-selfview portal-paystub-unavailable portal-root-gating` — all RED (missing routers), no typecheck break.
- `pnpm typecheck --filter=@contractor-ops/api` green (the `__tests__` exclusion holds — confirm, do not re-add a broad include).
- No wiki change required (test-only files are doc-exempt); note the RED net in `96-03-SUMMARY.md`.
</verification>

<success_criteria>
The RED net — two-employee IDOR (the load-bearing fence), manager-non-report + cross-org IDOR, leave-from-portal session-scoping, entitlement-scoped akta, pay-stub-unavailable, dark-flag gating — exists, is RED via missing routers, and does not brick `tsc`. Wave 4 flips each GREEN.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-03-SUMMARY.md` when done.
</output>
