---
phase: 96-theme-b-employee-self-service-portal
plan: 09
type: execute
wave: 6
depends_on: ["07", "08"]
files_modified:
  - apps/web-vite/src/i18n/locales/en/portal.json
  - apps/web-vite/src/i18n/locales/pl/portal.json
  - apps/web-vite/src/i18n/locales/de/portal.json
  - apps/web-vite/src/i18n/locales/ar/portal.json
  - apps/web-vite/src/i18n/locales/en-US/portal.json
  - apps/web-vite/src/components/layout/portal-top-bar.tsx
  - .planning/brain/wiki/domains/employee-portal.md
  - .planning/brain/wiki/structure/web-vite-domains.md
  - .planning/brain/wiki/structure/api-routers-catalog.md
  - .planning/MEMORY.md
  - .planning/EXTERNAL-ENABLEMENT.md
autonomous: true
requirements: [EMP-PORTAL-04, EMP-PORTAL-01]

must_haves:
  truths:
    - "Every `Portal.employee.*` and `Portal.employee.team.*` key exists at 5-locale parity (en canonical; de/pl machine-assisted; ar RTL; en-US); `pnpm i18n:parity` passes and no hardcoded strings remain in the new surfaces"
    - "The portal top-bar nav exposes the employee (and, for managers, the /team) entries for an EMPLOYEE subject and is unchanged for a CONTRACTOR subject"
    - "Arabic renders RTL and German uses the formal-Sie register on the new routes (built; native review flagged in EXTERNAL-ENABLEMENT)"
    - "Documentation-follows-code is closed in the SAME change set: a new employee-portal domain wiki page + web-vite-domains + api-routers-catalog + MEMORY invariant + EXTERNAL-ENABLEMENT native-review entry; `pnpm check:wiki-brain` passes"
  artifacts:
    - path: ".planning/brain/wiki/domains/employee-portal.md"
      provides: "the employee-portal domain page (purpose, flow, entry points, UI surface, agent mistakes, verify_with)"
      contains: "portalEmployee"
    - path: "apps/web-vite/src/i18n/locales/en/portal.json"
      provides: "canonical English employee-portal keys"
      contains: "employee"
  key_links:
    - from: ".planning/brain/wiki/domains/employee-portal.md"
      to: "packages/api/src/routers/portal/portal-employee-router.ts"
      via: "verify_with binds the domain page to the portal routers so drift is CI-gated"
      pattern: "verify_with"
---

<objective>
Close the phase: 5-locale i18n parity for the new employee + manager surfaces, portal top-bar nav wiring for the EMPLOYEE subject, RTL + Sie verification, and documentation-follows-code (wiki domain page + indexes + MEMORY invariant + EXTERNAL-ENABLEMENT native-review entry + graph/BM25 refresh) — all in one change set (EMP-PORTAL-04 + the D-08 doc mandate).

Purpose: make the portal ship at the parity gate (not advisory) and leave the wiki a living compass so the next agent does not hallucinate the new subject-discriminated session, the reporting-line edge, or the self-view entitlement. This is the only plan that touches i18n locale files + wiki + nav; the UI plans left English source keys + a domain-page stub.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-CONTEXT.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-RESEARCH.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-VALIDATION.md

<interfaces>
- `apps/web-vite/src/i18n/locales/{en,pl,de,ar,en-US}/portal.json` — the 5 locale files to extend at parity.
- `apps/web-vite/src/components/layout/portal-top-bar.tsx` — the nav to extend for the employee (and manager) subject.
- `.planning/brain/wiki/domains/` + `structure/web-vite-domains.md` + `structure/api-routers-catalog.md` + `wiki/hot.md` + `wiki/log.md` — the wiki compass to update.
- `.planning/MEMORY.md` — the invariant registry (subject-discriminated portal session; employee-IDOR fence).
- `.planning/EXTERNAL-ENABLEMENT.md:19` — the de/pl/ar native-review register row to extend.
- `CLAUDE.md` § Documentation follows code — the mandatory update matrix + refresh pipeline commands.
- `96-RESEARCH.md` § Widget Gating Map, § Route + Namespace Inventory.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5-locale parity for the new surfaces</name>
  <files>apps/web-vite/src/i18n/locales/en/portal.json, apps/web-vite/src/i18n/locales/pl/portal.json, apps/web-vite/src/i18n/locales/de/portal.json, apps/web-vite/src/i18n/locales/ar/portal.json, apps/web-vite/src/i18n/locales/en-US/portal.json</files>
  <read_first>
    - the existing `portal.json` structure in each locale + `CLAUDE.md` (i18n parity is a gate)
  </read_first>
  <action>
    Add the `employee.*` + `employee.team.*` key trees to all 5 locale files at parity: en canonical (the source keys the UI plans introduced); pl + de machine-assisted (de in the formal-Sie register); ar as RTL-safe translations; en-US mirroring en with US spelling/labels. Cover every string the 96-07/96-08 views reference (section headings, empty/unavailable/forbidden states, the time-off form, approve/reject, pay-stub-unavailable copy, doc-expiry labels). Run `pnpm i18n:parity` until green; run the hardcoded-string check.
  </action>
  <verify>
    <automated>pnpm i18n:parity 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm i18n:parity` passes (all 5 locales carry every new key); no hardcoded strings in the new surfaces.
    - de uses Sie; ar is RTL-safe.
  </acceptance_criteria>
  <done>The portal renders at 5-locale parity for the new surfaces (native review deferred).</done>
</task>

<task type="auto">
  <name>Task 2: Portal top-bar nav for the employee/manager subject</name>
  <files>apps/web-vite/src/components/layout/portal-top-bar.tsx</files>
  <read_first>
    - `portal-top-bar.tsx` (the current contractor nav) + `96-RESEARCH.md` § Route + Namespace Inventory
  </read_first>
  <action>
    Extend the top-bar so an EMPLOYEE subject sees the employee nav (Dashboard, Leave, Time, Documents, Pay) and, when the caller is a manager (from the manager-overview forbidden flag / a lightweight `isManager` signal), the Team entry; a CONTRACTOR subject sees the unchanged contractor nav. Drive visibility from the session subject-type surfaced by the portal session query — never from a client toggle. Localized labels via the Task-1 keys.
  </action>
  <verify>
    <automated>pnpm check:web-vite-presentational 2>&1 | tail -6 && pnpm --filter @contractor-ops/web-vite test src/components/layout/portal-top-bar 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - Employee subject sees employee nav; manager additionally sees /team; contractor nav unchanged.
    - Visibility derives from the server subject-type, not client input.
  </acceptance_criteria>
  <done>The new surfaces are reachable from the shell nav for the right subject.</done>
</task>

<task type="auto">
  <name>Task 3: Documentation-follows-code + refresh pipeline</name>
  <files>.planning/brain/wiki/domains/employee-portal.md, .planning/brain/wiki/structure/web-vite-domains.md, .planning/brain/wiki/structure/api-routers-catalog.md, .planning/MEMORY.md, .planning/EXTERNAL-ENABLEMENT.md</files>
  <read_first>
    - `CLAUDE.md` § Documentation follows code (the update matrix + pipeline) + an existing `wiki/domains/*` page for the frontmatter/verify_with shape
  </read_first>
  <action>
    Write `wiki/domains/employee-portal.md` (Purpose; Flow — magic-link → subject-discriminated session → /portal/employee/*; Entry points — `portalEmployee`/`portalManager` routers + the web-vite pages; UI surface; Agent mistakes — "an employee is NOT a Contractor; never add a synthetic Contractor; reads are session-workerId-scoped; section C is excluded from self-view; pay stubs are external in v7.0"; `verify_with` binding the page to `portal-employee-router.ts`, `portal-manager-router.ts`, `middleware/portal-auth.ts`, `services/portal-session.ts`). Add the portal/employee + portal/employee/team domains to `structure/web-vite-domains.md`; add `portalEmployee`/`portalManager` to `structure/api-routers-catalog.md` (verify against `portal-root.ts`). Append the phase invariants to `MEMORY.md` (subject-discriminated portal session + one-of CHECK; employee-IDOR fence = session-workerId only; reporting-line edge `managerWorkerId`; self-view section allowlist; pay-stub external in v7.0). Add an EXTERNAL-ENABLEMENT row for the de/pl/ar native review of the new portal surfaces (machine-translated → native-review-before-GA). Run the refresh pipeline: `wiki/log.md` + overwrite `wiki/hot.md` + BM25 rebuild + `graphify` graph refresh + bump `source_commit` on touched pages; `pnpm check:wiki-brain`.
  </action>
  <verify>
    <automated>pnpm check:wiki-brain 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The employee-portal domain page exists with `verify_with` binding the portal routers; web-vite-domains + api-routers-catalog updated; MEMORY + EXTERNAL-ENABLEMENT appended.
    - `pnpm check:wiki-brain` passes; hot/log/BM25/graph refreshed.
  </acceptance_criteria>
  <done>The wiki tracks the code; the next agent finds the subject model, the IDOR fence, and the reuse seams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Nav visibility → subject | The nav derives from the server subject-type; a client cannot reveal the employee/manager nav for a contractor. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-09-01 | Elevation of Privilege | client-toggled nav exposing manager routes | mitigate | Nav visibility from the server subject-type + manager signal (Task 2); server enforces every access (96-04/06). |
| T-96-09-02 | Information Disclosure | machine-translated legal/HR copy misstating an entitlement | accept (flagged) | Native review deferred + registered in EXTERNAL-ENABLEMENT (Task 3); en/en-US canonical; local-only posture. |
</threat_model>

<verification>
- `pnpm i18n:parity` passes; `pnpm check:web-vite-presentational` + `check:web-vite-page-shells` + `check:web-vite-data-layer` pass.
- `pnpm --filter @contractor-ops/web-vite test src/components/layout/portal-top-bar` GREEN (scoped).
- `pnpm check:wiki-brain` passes; `pnpm standards:check` + `pnpm lint:no-breadcrumbs` green.
- Full scoped api + web-vite portal suites green (the RED net all GREEN) before `/gsd:verify-work`.
</verification>

<success_criteria>
The employee + manager portal surfaces render at 5-locale parity (en canonical; de/pl machine-assisted; ar RTL; en-US), are reachable from the subject-aware top-bar nav, and are fully documented in the wiki (domain page + indexes + MEMORY invariant + EXTERNAL-ENABLEMENT native-review row) with `check:wiki-brain` green — closing Phase 96.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-09-SUMMARY.md` when done.
</output>
