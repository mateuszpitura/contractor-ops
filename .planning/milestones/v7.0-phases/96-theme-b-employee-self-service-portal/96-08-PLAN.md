---
phase: 96-theme-b-employee-self-service-portal
plan: 08
type: execute
wave: 5
depends_on: ["06"]
files_modified:
  - apps/web-vite/src/router/portal-routes.tsx
  - apps/web-vite/src/pages/portal/employee/team/index.tsx
  - apps/web-vite/src/pages/portal/employee/team/approvals.tsx
  - apps/web-vite/src/pages/portal/employee/team/documents.tsx
  - apps/web-vite/src/components/portal/employee/team/hooks/use-manager-overview.ts
  - apps/web-vite/src/components/portal/employee/team/hooks/use-manager-approvals.ts
  - apps/web-vite/src/components/portal/employee/team/hooks/use-manager-documents.ts
  - apps/web-vite/src/components/portal/employee/team/manager-overview.tsx
  - apps/web-vite/src/components/portal/employee/team/manager-approvals.tsx
  - apps/web-vite/src/components/portal/employee/team/manager-documents.tsx
autonomous: true
requirements: [EMP-PORTAL-03, EMP-PORTAL-04]

must_haves:
  truths:
    - "The manager surface is a distinct `/portal/employee/team/*` route group (RESEARCH Open Q3) rendered inside the existing portal shell; it is only reachable when the caller has ≥1 report (a non-manager gets a forbidden/empty state, never a crash)"
    - "The tRPC boundary is ONLY the `components/portal/employee/team/hooks/use-*.ts` hooks; pages are thin; views are presentational — the three web-vite layer checks pass"
    - "The approvals surface lists reports' leave requests + time entries with approve/reject actions calling `portalManager.*`; every list is server-scoped (no report id from the client)"
    - "The document-expiry surface lists reports' expiry flags read-only; each section renders loading/empty/error/forbidden states"
    - "All copy is i18n keys under `Portal.employee.team.*` (no hardcoded strings); DataTable + Dialog conventions followed"
  artifacts:
    - path: "apps/web-vite/src/components/portal/employee/team/manager-approvals.tsx"
      provides: "ManagerApprovalsView + wired ManagerApprovals (approve/reject)"
      contains: "ManagerApprovals"
    - path: "apps/web-vite/src/components/portal/employee/team/hooks/use-manager-approvals.ts"
      provides: "reports' leave/time approval hook (sole tRPC boundary)"
      contains: "usePortalTRPC"
  key_links:
    - from: "manager-approvals.tsx"
      to: "hooks/use-manager-approvals.ts"
      via: "wired section consumes the hook; approve/reject post portalManager mutations"
      pattern: "use-manager-approvals"
---

<objective>
Build the manager dashboard UI (EMP-PORTAL-03 + EMP-PORTAL-04): a distinct `/portal/employee/team/*` route group in the existing portal shell for a team overview, reports' leave + time approvals, and reports' document-expiry flags, consuming the 96-06 `portalManager` backend.

Purpose: give a line manager a polished, accessible, localized approval surface following the web-vite layering + frontend-design/impeccable standard, permission-loaded so a non-manager never mounts it (RESEARCH Open Q3). The IDOR fence is server-side (96-06); the UI adds the forbidden/empty/loading states and the approve/reject flows. i18n parity is finalized in 96-09.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-CONTEXT.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-RESEARCH.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-VALIDATION.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
- `apps/web-vite/src/router/portal-routes.tsx` (extended in 96-07) — add the `/portal/employee/team/*` entries.
- `apps/web-vite/src/components/portal/hooks/use-portal-equipment.ts:1-16` — the `usePortalTRPC` boundary pattern.
- `packages/ui` `DataTable` (workbench primitive, client-pagination mode) — the approvals + reports tables.
- `apps/web-vite/src/components/portal/employee/*` (96-07) — the sibling employee surface + shared i18n namespace.
- `96-RESEARCH.md` § Route + Namespace Inventory, § Widget Gating Map, § Open Q3.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manager section hooks (sole tRPC boundary)</name>
  <files>apps/web-vite/src/components/portal/employee/team/hooks/use-manager-overview.ts, apps/web-vite/src/components/portal/employee/team/hooks/use-manager-approvals.ts, apps/web-vite/src/components/portal/employee/team/hooks/use-manager-documents.ts</files>
  <read_first>
    - `use-portal-equipment.ts:1-16` + `ARCHITECTURE.md` (Hook return shape)
  </read_first>
  <action>
    Write the hooks (the only `usePortalTRPC` callers under `team/`): `use-manager-overview` (getTeamOverview → per-report summary props + `isForbidden` when the caller is not a manager, from a FORBIDDEN error); `use-manager-approvals` (listReportLeaveRequests + listReportTimeToApprove + approve/reject + time-approve mutations via `useResourceMutation` with toasts + invalidation); `use-manager-documents` (listReportDocumentExpiry). Each returns a props bag + `isLoading`/`isEmpty`/`isForbidden` flags.
  </action>
  <verify>
    <automated>pnpm check:web-vite-data-layer 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `usePortalTRPC`/`useQuery`/`useMutation` only in these hooks; `isForbidden` flag surfaced.
  </acceptance_criteria>
  <done>The manager UI has a single data boundary per section.</done>
</task>

<task type="auto">
  <name>Task 2: Wired manager sections + views (approve/reject)</name>
  <files>apps/web-vite/src/components/portal/employee/team/manager-overview.tsx, apps/web-vite/src/components/portal/employee/team/manager-approvals.tsx, apps/web-vite/src/components/portal/employee/team/manager-documents.tsx</files>
  <read_first>
    - `ARCHITECTURE.md` (Wired + View + Canonical DataTable) + the 96-07 employee sections for the shared visual language
  </read_first>
  <action>
    Write `*View` (props-only) + wired `*` sections branching loading (skeleton) / empty / error / forbidden / success. `ManagerOverview` shows a per-report summary grid. `ManagerApprovals` renders the reports' pending leave + time entries in a `DataTable` (client pagination) with per-row approve/reject actions (a confirm Dialog with body/footer convention; reject captures an optional reason); posts `portalManager.approve*`/`reject*` and invalidates. `ManagerDocuments` renders reports' expiry flags with status pills. All copy via `useTranslations('Portal')` under `employee.team.*` (English source now). WCAG: keyboard-operable row actions, focus management on the Dialog, contrast on the pills.
  </action>
  <verify>
    <automated>pnpm check:web-vite-presentational 2>&1 | tail -6 && pnpm --filter @contractor-ops/web-vite test src/components/portal/employee/team 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Views props-only; wired sections own variants incl. `isForbidden`; approvals use `DataTable` + a confirm Dialog.
    - No hardcoded strings; raw shadcn `<Table>` not used (DataTable only).
  </acceptance_criteria>
  <done>The manager approval + reports surfaces render all states to standard.</done>
</task>

<task type="auto">
  <name>Task 3: Team routes + thin pages + nav visibility</name>
  <files>apps/web-vite/src/router/portal-routes.tsx, apps/web-vite/src/pages/portal/employee/team/index.tsx, apps/web-vite/src/pages/portal/employee/team/approvals.tsx, apps/web-vite/src/pages/portal/employee/team/documents.tsx</files>
  <read_first>
    - `portal-routes.tsx` (the array, extended in 96-07)
  </read_first>
  <action>
    Add lazy imports + `portalRoutes` entries `portal/employee/team`, `portal/employee/team/approvals`, `portal/employee/team/documents`. Each page is a thin `Suspense` composer of the wired manager root. The team nav entry is shown only when `use-manager-overview` reports the caller is a manager (a non-manager never sees the `/team` nav and a direct nav renders a forbidden/empty state — not a crash). No `useTRPC`/`useQuery`/`useMutation` in pages.
  </action>
  <verify>
    <automated>pnpm check:web-vite-page-shells 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Pages thin; routes inside the authenticated shell; non-manager gets a forbidden/empty state.
    - All three web-vite layer checks pass.
  </acceptance_criteria>
  <done>The manager surface is routable + permission-visible inside the existing shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Non-manager → manager UI | The surface is permission-loaded; a non-manager sees a forbidden/empty state; the server enforces ≥1 report (96-06). |
| UI → approve action | The UI passes a report id validated server-side against the reporting-line edge (96-06); a spoofed id is rejected. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-08-01 | Elevation of Privilege | a non-manager reaching the team UI | mitigate | `isForbidden` gate + server ≥1-report assertion (96-06); forbidden state. |
| T-96-08-02 | Tampering | approving for a non-report via a spoofed row id | mitigate | Server `assertIsDirectReport` (96-06); the UI cannot bypass it. |
</threat_model>

<verification>
- `pnpm check:web-vite-data-layer` + `pnpm check:web-vite-page-shells` + `pnpm check:web-vite-presentational` pass.
- `pnpm --filter @contractor-ops/web-vite test src/components/portal/employee/team` GREEN (scoped — NEVER unscoped).
- web-vite typecheck green.
- Wiki: `structure/web-vite-domains.md` (portal/employee/team) + the portal domain page (manager UI surface) updated in this change set.
</verification>

<success_criteria>
The manager dashboard renders inside the existing portal shell at `/portal/employee/team/*` with an overview, reports' leave + time approvals (approve/reject), and reports' document-expiry — permission-loaded (a non-manager gets a forbidden state), with the tRPC boundary confined to hooks, thin pages, presentational views, English source i18n keys, and the web-vite layer checks passing.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-08-SUMMARY.md` when done.
</output>
