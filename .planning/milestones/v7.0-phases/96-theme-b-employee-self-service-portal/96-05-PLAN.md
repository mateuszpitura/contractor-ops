---
phase: 96-theme-b-employee-self-service-portal
plan: 05
type: execute
wave: 4
depends_on: ["01", "02", "03"]
files_modified:
  - packages/api/src/routers/portal/portal-employee-akta.ts
  - packages/api/src/routers/portal/portal-employee-router.ts
autonomous: true
requirements: [EMP-PORTAL-02]

must_haves:
  truths:
    - "`getMyAkta` returns documents from the CALLER'S OWN PersonnelFile only (`workerId: ctx.workerId, organizationId`) — no client-supplied workerId is accepted"
    - "The self-view is filtered to `PERSONNEL_FILE_SELF_VIEW_SECTIONS` (96-01) — a document in an excluded section (e.g. SECTION_C) NEVER enters the response, decided BEFORE the document rows are read (not fetch-then-hide)"
    - "`getPayStubAvailability` returns `{ available:false, reason:'EXTERNAL_PAYROLL' }` in v7.0 — there is no payslip surface (RESEARCH C3); it never fabricates a stub"
    - "The 96-03 RED tests portal-akta-selfview and portal-paystub-unavailable flip GREEN"
  artifacts:
    - path: "packages/api/src/routers/portal/portal-employee-akta.ts"
      provides: "getMyAkta (own-file, section-allowlist) + getPayStubAvailability read models"
      contains: "PERSONNEL_FILE_SELF_VIEW_SECTIONS"
  key_links:
    - from: "portal-employee-akta.ts getMyAkta"
      to: "portal-self-view-sections.ts PERSONNEL_FILE_SELF_VIEW_SECTIONS"
      via: "the section filter is applied to the WHERE clause so excluded sections never read"
      pattern: "isSelfViewableSection"
---

<objective>
Build the two read-only surfaces that need a bespoke entitlement (EMP-PORTAL-02): the employee's **personal akta view** (own file, section-allowlist filtered — RESEARCH C4) and the **pay-stub availability** read model (graceful-unavailable — RESEARCH C3), flipping the 96-03 RED tests `portal-akta-selfview` and `portal-paystub-unavailable` GREEN.

Purpose: the akta view is where the CONTEXT's "personnel-file documents they're entitled to" becomes concrete — an employee sees their OWN file, filtered to `PERSONNEL_FILE_SELF_VIEW_SECTIONS` (section C / pay+PII excluded), never a client workerId or section. The pay-stub widget has no v7.0 data surface (P94 is export-only), so it returns `{ available:false }` for a real "your pay stubs live in the payroll system" empty state. These live in the same `portalEmployee` namespace as 96-04.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-CONTEXT.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-RESEARCH.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-VALIDATION.md

<interfaces>
- `packages/api/src/routers/portal/portal-self-view-sections.ts` (96-01) — `PERSONNEL_FILE_SELF_VIEW_SECTIONS` + `isSelfViewableSection`.
- `packages/api/src/routers/core/personnel-file/read.ts` — the staff read shape (document projection + section grouping) to mirror read-only.
- `packages/db/prisma/schema/personnel.prisma:34-79` — `PersonnelFile` (workerId anchor) + `PersonnelFileDocument.section`.
- `packages/api/src/middleware/portal-auth.ts` (96-02) — `portalEmployeeProcedure` (ctx.workerId, employeeProfile).
- `.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-CONTEXT.md:6-27` — the export-only reality (no payslip surface).
- `96-RESEARCH.md` § Corrections C3/C4, § Widget Gating Map.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: getMyAkta — own-file, section-allowlist read</name>
  <files>packages/api/src/routers/portal/portal-employee-akta.ts</files>
  <read_first>
    - `portal-self-view-sections.ts` (96-01) + `personnel-file/read.ts` + `personnel.prisma:34-79`
  </read_first>
  <action>
    In a new `portal-employee-akta.ts` (a router fragment `portalEmployeeAktaProcedures` merged into `portalEmployeeRouter`), add `getMyAkta`: resolve the caller's `PersonnelFile` by `{ workerId: ctx.workerId, organizationId }`; query its documents with `where: { section: { in: [...PERSONNEL_FILE_SELF_VIEW_SECTIONS] } }` (the allowlist is applied IN the query so excluded-section rows never read — not fetched-then-filtered); project to a masked, download-safe shape (filename, section short code, uploadedAt, a portal download link resolver — reuse `mapPortalDocLink`-style mapping); group by section for the UI. NEVER accept a client `workerId` or `section`. Unclassified documents (`section: null`) are excluded (pending classification).
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test portal-akta-selfview 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The document query filters on `PERSONNEL_FILE_SELF_VIEW_SECTIONS` in the `where` (grep `isSelfViewableSection`/the constant); SECTION_C never returned.
    - Own-file only (`workerId: ctx.workerId`); no client id/section input.
    - `portal-akta-selfview` GREEN.
  </acceptance_criteria>
  <done>An employee sees only their own file, only the entitled sections.</done>
</task>

<task type="auto">
  <name>Task 2: getPayStubAvailability — graceful-unavailable read model</name>
  <files>packages/api/src/routers/portal/portal-employee-akta.ts, packages/api/src/routers/portal/portal-employee-router.ts</files>
  <read_first>
    - `94-CONTEXT.md:6-27` + `96-RESEARCH.md` § Corrections C3
  </read_first>
  <action>
    Add `getPayStubAvailability`: return `{ available: false as const, reason: 'EXTERNAL_PAYROLL' as const }` — a forward-compatible read model (a future payslip surface flips `available:true` + a list). A code comment states WHY (pay stubs are computed + owned by the external payroll system in v7.0 — no phase/decision ID). Wire both `getMyAkta` and `getPayStubAvailability` into `portalEmployeeRouter`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test portal-paystub-unavailable 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `getPayStubAvailability` returns `{ available:false, reason:'EXTERNAL_PAYROLL' }`; no stub fabrication.
    - `portal-paystub-unavailable` GREEN; comment carries no breadcrumb ID (`lint:no-breadcrumbs` green).
  </acceptance_criteria>
  <done>The pay-stub widget has a truthful unavailable read model for the UI empty state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Employee → own file sections | The allowlist is applied in the query; an excluded section's rows never load into a response. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-05-01 | Information Disclosure | an employee reading the pay/PII section (C) of their file | mitigate | `PERSONNEL_FILE_SELF_VIEW_SECTIONS` excludes C, applied in the `where` (Task 1). |
| T-96-05-02 | Information Disclosure | reading another worker's akta | mitigate | Own-file `where: { workerId: ctx.workerId }`; no client workerId (Task 1). |
| T-96-05-03 | Tampering | a fabricated pay stub misleading an employee | mitigate | `getPayStubAvailability` returns `available:false` — no stub data path (Task 2). |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test portal-akta-selfview portal-paystub-unavailable` GREEN; `pnpm typecheck --filter=@contractor-ops/api` green.
- `pnpm lint:audit-log` (reads are unaudited by design; a full-document reveal, if added later, must audit) + `pnpm lint:no-breadcrumbs` green.
- Wiki: the portal domain page (akta self-view entitlement + pay-stub-unavailable) + `structure/api-routers-catalog.md` updated in this change set.
</verification>

<success_criteria>
`getMyAkta` returns the caller's own file filtered to the self-view section allowlist (section C excluded, decided in the query), and `getPayStubAvailability` returns a truthful `{ available:false }` model. The 96-03 akta + pay-stub RED tests are GREEN.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-05-SUMMARY.md` when done.
</output>
