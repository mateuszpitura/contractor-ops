---
phase: 96-theme-b-employee-self-service-portal
plan: 07
type: execute
wave: 5
depends_on: ["04", "05"]
files_modified:
  - apps/web-vite/src/router/portal-routes.tsx
  - apps/web-vite/src/pages/portal/employee/index.tsx
  - apps/web-vite/src/pages/portal/employee/leave.tsx
  - apps/web-vite/src/pages/portal/employee/time.tsx
  - apps/web-vite/src/pages/portal/employee/documents.tsx
  - apps/web-vite/src/pages/portal/employee/pay.tsx
  - apps/web-vite/src/components/portal/employee/hooks/use-employee-dashboard.ts
  - apps/web-vite/src/components/portal/employee/hooks/use-employee-leave.ts
  - apps/web-vite/src/components/portal/employee/hooks/use-employee-time.ts
  - apps/web-vite/src/components/portal/employee/hooks/use-employee-akta.ts
  - apps/web-vite/src/components/portal/employee/employee-dashboard.tsx
  - apps/web-vite/src/components/portal/employee/employee-leave-section.tsx
  - apps/web-vite/src/components/portal/employee/employee-time-section.tsx
  - apps/web-vite/src/components/portal/employee/employee-documents-section.tsx
  - apps/web-vite/src/components/portal/employee/employee-pay-section.tsx
autonomous: true
requirements: [EMP-PORTAL-02, EMP-PORTAL-04]

must_haves:
  truths:
    - "The `/portal/employee/*` routes render inside the EXISTING authenticated `PortalShell` (no new shell); the employee dashboard composes leave-balance + time-off + ewidencja + akta + pay-stub sections"
    - "The tRPC boundary is ONLY the `components/portal/employee/hooks/use-*.ts` hooks (`usePortalTRPC`); pages are thin Suspense composers; views are presentational — `check:web-vite-data-layer` + `check:web-vite-page-shells` + `check:web-vite-presentational` pass"
    - "Every section renders loading / empty / error states; a dark widget (D-02) renders a real unavailable state; the pay-stub section renders the `available:false` empty state (never a fake stub)"
    - "The time-off request is a validated form that calls `portalEmployee.submitTimeOffRequest` (no workerId) and surfaces balance + success/error"
    - "All copy is i18n keys under `Portal.employee.*` (no hardcoded strings); the entrance animation + Dialog body/footer conventions are followed"
  artifacts:
    - path: "apps/web-vite/src/components/portal/employee/employee-dashboard.tsx"
      provides: "EmployeeDashboardView + wired EmployeeDashboard"
      contains: "EmployeeDashboard"
    - path: "apps/web-vite/src/components/portal/employee/hooks/use-employee-leave.ts"
      provides: "leave balance + time-off request hook (sole tRPC boundary)"
      contains: "usePortalTRPC"
  key_links:
    - from: "employee-leave-section.tsx"
      to: "hooks/use-employee-leave.ts"
      via: "wired section consumes the hook's props bag + flags; the form posts submitTimeOffRequest"
      pattern: "use-employee-leave"
---

<objective>
Build the employee self-service dashboard UI (EMP-PORTAL-02 + EMP-PORTAL-04) on the existing v2.0 portal shell: `/portal/employee/*` routes, section hooks (the sole tRPC boundary), wired sections, and presentational views for leave balance + time-off request, employee-time/ewidencja, personal akta view + upload, and the pay-stub unavailable state.

Purpose: turn the 96-04/96-05 backend into a polished, accessible, localized surface following the web-vite layering (page → wired section → hook → view), the frontend-design + impeccable standard (D-08), and D-02 graceful degradation (each widget renders a real empty/unavailable state when its surface is dark). No manager UI (96-08); i18n parity + nav wiring is finalized in 96-09.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-CONTEXT.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-RESEARCH.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-VALIDATION.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
- `apps/web-vite/src/router/portal-routes.tsx:31-50` — the `portalRoutes` array to extend with `/portal/employee/*`.
- `apps/web-vite/src/components/layout/portal-shell.tsx` + `apps/web-vite/src/router.tsx:146-157` — the authenticated shell the new routes render inside.
- `apps/web-vite/src/components/portal/hooks/use-portal-equipment.ts:1-16` — the `usePortalTRPC` hook pattern (sole tRPC boundary; `inferRouterOutputs<PortalAppRouter>`).
- `apps/web-vite/src/components/leave/leave-balance-card.tsx` — an existing presentational balance card to reuse/adapt.
- `apps/web-vite/src/i18n/useTranslations.ts` + the `Portal` message namespace — i18n.
- `.planning/brain/wiki/patterns/` (web-vite entrance animation, Dialog body/footer conventions) — mandatory conventions.
- `96-RESEARCH.md` § Route + Namespace Inventory, § Widget Gating Map, Pattern 6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section hooks — the sole tRPC boundary</name>
  <files>apps/web-vite/src/components/portal/employee/hooks/use-employee-dashboard.ts, apps/web-vite/src/components/portal/employee/hooks/use-employee-leave.ts, apps/web-vite/src/components/portal/employee/hooks/use-employee-time.ts, apps/web-vite/src/components/portal/employee/hooks/use-employee-akta.ts</files>
  <read_first>
    - `use-portal-equipment.ts:1-16` (the hook pattern) + `ARCHITECTURE.md` (Hook return shape)
  </read_first>
  <action>
    Write the domain hooks (the ONLY `usePortalTRPC` callers): `use-employee-dashboard` (getDashboard + getPayStubAvailability → props bag + `isLoading`/`isEmpty`/flags); `use-employee-leave` (getLeaveBalance + listMyLeaveRequests + a `submitTimeOffRequest` mutation via `useResourceMutation` with success/error toasts + query invalidation); `use-employee-time` (getMyTime + getMyEwidencja); `use-employee-akta` (getMyAkta + the requestAktaUpload/confirmAktaUpload upload flow). Each returns a props bag + variant flags (never raw query objects). Handle the dark-widget case: a `MODULE`/`METHOD_NOT_FOUND` error maps to an `isUnavailable` flag, not a thrown error.
  </action>
  <verify>
    <automated>pnpm check:web-vite-data-layer 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `usePortalTRPC`/`useQuery`/`useMutation` appear ONLY in these hook files.
    - Each hook returns a props bag + `isLoading`/`isEmpty`/`isUnavailable` flags.
  </acceptance_criteria>
  <done>The employee UI has a single, testable data boundary per section.</done>
</task>

<task type="auto">
  <name>Task 2: Wired sections + presentational views</name>
  <files>apps/web-vite/src/components/portal/employee/employee-dashboard.tsx, apps/web-vite/src/components/portal/employee/employee-leave-section.tsx, apps/web-vite/src/components/portal/employee/employee-time-section.tsx, apps/web-vite/src/components/portal/employee/employee-documents-section.tsx, apps/web-vite/src/components/portal/employee/employee-pay-section.tsx</files>
  <read_first>
    - `ARCHITECTURE.md` (Wired + View convention, anti-patterns) + `leave-balance-card.tsx`
  </read_first>
  <action>
    For each section write a `*View` (props-only) + a wired `*Section` that consumes its hook and branches loading (section skeleton) / empty / error / unavailable / success. `EmployeeDashboard` composes the sections. The leave section includes a time-off request Dialog (content in `DialogBody`, actions in `DialogFooter` per convention) with a validated form (react-hook-form + the portal input schema mirror), showing the current balance and posting `submitTimeOffRequest`. The documents section lists akta by section short-code with a download action + the upload flow. The pay section renders the `available:false` empty state ("your pay stubs live in your payroll system"). Follow the entrance-animation convention (AnimateIn; TiltCard entrance=false inside). All copy via `useTranslations('Portal')` keys under `employee.*` (add English source keys now; parity in 96-09). WCAG: semantic headings, keyboard/focus, contrast, `aria` on the form.
  </action>
  <verify>
    <automated>pnpm check:web-vite-presentational 2>&1 | tail -6 && pnpm --filter @contractor-ops/web-vite test src/components/portal/employee 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Views are props-only; wired sections own every variant; pay section renders the unavailable state.
    - Dialog uses body/footer convention; entrance animation followed; no hardcoded strings.
  </acceptance_criteria>
  <done>Each employee widget renders all states to standard, localized.</done>
</task>

<task type="auto">
  <name>Task 3: Routes + thin pages</name>
  <files>apps/web-vite/src/router/portal-routes.tsx, apps/web-vite/src/pages/portal/employee/index.tsx, apps/web-vite/src/pages/portal/employee/leave.tsx, apps/web-vite/src/pages/portal/employee/time.tsx, apps/web-vite/src/pages/portal/employee/documents.tsx, apps/web-vite/src/pages/portal/employee/pay.tsx</files>
  <read_first>
    - `portal-routes.tsx:31-50` (the route array + `page()` helper)
  </read_first>
  <action>
    Add lazy imports + `portalRoutes` entries `portal/employee`, `portal/employee/leave`, `portal/employee/time`, `portal/employee/documents`, `portal/employee/pay`. Each page is a thin `Suspense` composer rendering the wired root (`<EmployeeDashboard />` etc.) — NO `useTRPC`/`useQuery`/`useMutation`. Guard the group with the portal flag via `useFlag('module.employee-portal')` at the page/section boundary (a contractor subject or dark org sees a forbidden/empty state, not a crash).
  </action>
  <verify>
    <automated>pnpm check:web-vite-page-shells 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Pages are thin (no data boundary); routes registered inside the authenticated shell.
    - `check:web-vite-page-shells` + `check:web-vite-data-layer` + `check:web-vite-presentational` pass.
  </acceptance_criteria>
  <done>The `/portal/employee/*` surface is routable inside the existing shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → portal API | The UI never supplies a workerId; the session scopes every read/write (enforced server-side in 96-04/05). |
| Dark widget → UI | A dark surface renders an unavailable state, never leaks partial data or crashes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-07-01 | Information Disclosure | a contractor subject reaching the employee UI | mitigate | `useFlag`/subject gate at the page boundary + server-side subject rejection (96-02); forbidden state, not a crash. |
| T-96-07-02 | Tampering | a form injecting a workerId | mitigate | The submit input has no workerId (96-04); the UI mirrors that schema. |
</threat_model>

<verification>
- `pnpm check:web-vite-data-layer` + `pnpm check:web-vite-page-shells` + `pnpm check:web-vite-presentational` all pass.
- `pnpm --filter @contractor-ops/web-vite test src/components/portal/employee` GREEN (scoped — NEVER unscoped).
- `pnpm --filter @contractor-ops/web-vite typecheck` (or the web-vite typecheck task) green.
- Wiki: `structure/web-vite-domains.md` (new portal/employee domain) + the portal domain page (UI surface) updated in this change set. i18n parity finalized in 96-09.
</verification>

<success_criteria>
The employee dashboard renders inside the existing portal shell at `/portal/employee/*` with leave, time, akta, and pay sections — each with loading/empty/error/unavailable states, the tRPC boundary confined to hooks, thin pages, presentational views, and English source i18n keys. The web-vite layer checks pass.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-07-SUMMARY.md` when done.
</output>
