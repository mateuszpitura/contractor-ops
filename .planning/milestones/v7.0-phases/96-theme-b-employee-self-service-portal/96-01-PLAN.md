---
phase: 96-theme-b-employee-self-service-portal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema/portal.prisma
  - packages/db/prisma/schema/employee.prisma
  - packages/db/prisma/schema/migrations/__portal_employee_subject/migration.sql
  - packages/db/prisma/schema/migrations/__portal_employee_subject/down.sql
  - packages/feature-flags/src/flags-core.ts
  - packages/feature-flags/src/signoff-registry-flags.json
  - packages/api/src/routers/portal/portal-self-view-sections.ts
  - packages/api/src/routers/portal/__tests__/portal-self-view-sections.test.ts
autonomous: true
requirements: [EMP-PORTAL-01, EMP-PORTAL-03]

must_haves:
  truths:
    - "`PortalSession` can represent an EMPLOYEE subject: `contractorId` is nullable, a nullable `workerId` FK + a `subjectType` (`CONTRACTOR`|`EMPLOYEE`) column exist, and a DB CHECK constraint enforces EXACTLY one subject (contractor xor employee) — a both/neither row is rejected by the database"
    - "Existing `PortalSession` rows backfill to `subjectType='CONTRACTOR'` so the contractor path is untouched (the migration is additive + reversible)"
    - "`EmployeeProfile` carries a nullable self-relation `managerWorkerId` (FK → Worker.id, same-org, indexed) — the reporting-line edge direct-reports resolve off; it did NOT exist before this plan"
    - "A new `module.employee-portal` flag is registered (category module, default false, owner workforce-platform, signoff PENDING) as the portal-surface gate"
    - "`PERSONNEL_FILE_SELF_VIEW_SECTIONS` is a server-fixed, conservative allowlist constant (section C excluded) the akta self-view filters on — pure, unit-tested"
  artifacts:
    - path: "packages/db/prisma/schema/migrations/__portal_employee_subject/migration.sql"
      provides: "PortalSession subject columns + one-of CHECK; EmployeeProfile.managerWorkerId FK + index"
      contains: "subjectType"
    - path: "packages/feature-flags/src/flags-core.ts"
      provides: "module.employee-portal flag definition + registration"
      contains: "module.employee-portal"
    - path: "packages/api/src/routers/portal/portal-self-view-sections.ts"
      provides: "PERSONNEL_FILE_SELF_VIEW_SECTIONS allowlist + isSelfViewableSection()"
      contains: "PERSONNEL_FILE_SELF_VIEW_SECTIONS"
  key_links:
    - from: "employee.prisma EmployeeProfile.managerWorkerId"
      to: "worker.prisma Worker.id"
      via: "nullable same-org self-relation FK; reports = EmployeeProfile where managerWorkerId = caller workerId"
      pattern: "managerWorkerId"
---

<objective>
Lay the DB + flag + entitlement foundation the whole employee portal rests on, WITHOUT any router or UI yet. Three additive, reversible pieces:

1. Make the `PortalSession` row able to be an EMPLOYEE subject (Option A of RESEARCH § The Load-Bearing Decision): nullable `contractorId`, new nullable `workerId` FK, a `subjectType` discriminator, and a raw-SQL one-of CHECK constraint (Prisma cannot express a multi-column CHECK — same posture as the P95 partial-index correction). Existing rows backfill to `CONTRACTOR` so the contractor login is untouched.
2. Add the manager→direct-reports **reporting-line edge** that CONTEXT D-03 assumed but does not exist (RESEARCH C2): a nullable `managerWorkerId` self-relation on `EmployeeProfile`.
3. Register the `module.employee-portal` gate (RESEARCH Open Q1) and the `PERSONNEL_FILE_SELF_VIEW_SECTIONS` self-view allowlist (RESEARCH C4).

Purpose: every later plan imports these — the session shape (96-02), the reports edge (96-06), the flag gate (all portal procedures), and the akta allowlist (96-05). This ships GREEN (a foundation gate, not a RED plan). No `validatePortalSession` change, no procedures, no UI in this plan.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-CONTEXT.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-RESEARCH.md
@.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-VALIDATION.md

<interfaces>
- `packages/db/prisma/schema/portal.prisma:4-23` — `PortalSession` (currently `contractorId String` required FK; the row to extend).
- `packages/db/prisma/schema/employee.prisma:12-63` — `EmployeeProfile` (the manager edge target; `organizationId`, `workerId @unique`).
- `packages/db/prisma/schema/worker.prisma:17-45` — `Worker` (FK target for `managerWorkerId`; `workerType`).
- `packages/db/prisma/schema/integration.prisma:124-128` — the documented precedent that Prisma `@@unique`/schema cannot express a filtered/CHECK constraint → raw SQL in the migration body.
- `packages/feature-flags/src/flags-core.ts:230-238` — `module.workforce-employees` flag (the exact shape to mirror for `module.employee-portal`).
- `packages/feature-flags/src/signoff-registry-flags.json:118-125` — the PENDING signoff registry pattern (add `module.employee-portal`).
- `packages/api/src/routers/core/personnel-file/section-access.ts:22-44` — `PersonnelFileSection` enum + `PERSONNEL_FILE_SECTIONS` (the section vocabulary the self-view allowlist is a subset of).
- `96-RESEARCH.md` § The Load-Bearing Decision (Option A schema shape), § Corrections C2/C4, § Open Q1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PortalSession subject discrimination — schema + migration</name>
  <files>packages/db/prisma/schema/portal.prisma, packages/db/prisma/schema/migrations/__portal_employee_subject/migration.sql, packages/db/prisma/schema/migrations/__portal_employee_subject/down.sql</files>
  <read_first>
    - `packages/db/prisma/schema/portal.prisma:4-23` (the row) + `integration.prisma:124-128` (raw-SQL-constraint precedent)
    - `96-RESEARCH.md` § The Load-Bearing Decision (Option A)
  </read_first>
  <action>
    In `portal.prisma`, edit `PortalSession`: make `contractorId String?` nullable; add `workerId String?`; add `subjectType PortalSubjectType @default(CONTRACTOR)`; add the `worker Worker? @relation(fields: [workerId], references: [id])` (and the back-relation on `Worker`); keep `contractor Contractor? @relation(...)` (now optional). Add `enum PortalSubjectType { CONTRACTOR EMPLOYEE }`. Add `@@index([workerId, organizationId])`. In `worker.prisma`, add `portalSessions PortalSession[]` back-relation.
    Author the `__`-prefixed migration by hand (the `__` prefix keeps it out of `prisma migrate deploy` until applied at deploy — the drift-blocked posture): (a) `ALTER TABLE "PortalSession" ALTER COLUMN "contractorId" DROP NOT NULL;` (b) `CREATE TYPE "PortalSubjectType" AS ENUM ('CONTRACTOR','EMPLOYEE');` (c) `ALTER TABLE "PortalSession" ADD COLUMN "workerId" TEXT, ADD COLUMN "subjectType" "PortalSubjectType" NOT NULL DEFAULT 'CONTRACTOR';` (d) the FK `ADD CONSTRAINT "PortalSession_workerId_fkey" FOREIGN KEY ("workerId") REFERENCES "Worker"("id");` (e) the index; (f) the one-of CHECK: `ALTER TABLE "PortalSession" ADD CONSTRAINT "PortalSession_subject_one_of" CHECK ( (subjectType = 'CONTRACTOR' AND "contractorId" IS NOT NULL AND "workerId" IS NULL) OR (subjectType = 'EMPLOYEE' AND "workerId" IS NOT NULL AND "contractorId" IS NULL) );`. Write the paired `down.sql` (drop CHECK, FK, columns, enum; restore NOT NULL). Then `pnpm db:generate`.
  </action>
  <verify>
    <automated>pnpm db:generate 2>&1 | tail -5 && grep -c "subjectType\|PortalSession_subject_one_of" packages/db/prisma/schema/migrations/__portal_employee_subject/migration.sql</automated>
  </verify>
  <acceptance_criteria>
    - `contractorId` nullable; `workerId` + `subjectType` present; the one-of CHECK is in the migration body (not a Prisma `@@`).
    - `down.sql` reverses every step; existing rows default to `CONTRACTOR`.
    - Prisma client regenerates clean (`pnpm db:generate`).
  </acceptance_criteria>
  <done>The session row can be an employee subject, DB-enforced one-of, contractor rows untouched.</done>
</task>

<task type="auto">
  <name>Task 2: EmployeeProfile reporting-line edge (managerWorkerId)</name>
  <files>packages/db/prisma/schema/employee.prisma, packages/db/prisma/schema/migrations/__portal_employee_subject/migration.sql, packages/db/prisma/schema/migrations/__portal_employee_subject/down.sql</files>
  <read_first>
    - `packages/db/prisma/schema/employee.prisma:12-63` + `worker.prisma:17-45`
    - `96-RESEARCH.md` § Corrections C2
  </read_first>
  <action>
    Add `managerWorkerId String?` to `EmployeeProfile` + `managerWorker Worker? @relation("EmployeeManager", fields: [managerWorkerId], references: [id])` and the matching back-relation on `Worker` (`managedEmployees EmployeeProfile[] @relation("EmployeeManager")`). Add `@@index([organizationId, managerWorkerId])`. Extend the SAME `__portal_employee_subject` migration: `ADD COLUMN "managerWorkerId" TEXT`, the FK to `Worker(id)`, the index. (Same-org integrity is enforced in the resolver, not the FK — Worker rows are org-scoped and the reports query always filters `organizationId`; document this in the resolver, not here.) Extend `down.sql`.
  </action>
  <verify>
    <automated>pnpm db:generate 2>&1 | tail -3 && grep -c "managerWorkerId" packages/db/prisma/schema/employee.prisma packages/db/prisma/schema/migrations/__portal_employee_subject/migration.sql</automated>
  </verify>
  <acceptance_criteria>
    - `managerWorkerId` nullable FK + index present in schema + migration + down.
    - `pnpm db:generate` clean; `pnpm db:audit-enum-casing` unaffected (no new enum here).
  </acceptance_criteria>
  <done>The reporting-line edge exists; direct reports are resolvable server-side in 96-06.</done>
</task>

<task type="auto">
  <name>Task 3: module.employee-portal flag + signoff registration</name>
  <files>packages/feature-flags/src/flags-core.ts, packages/feature-flags/src/signoff-registry-flags.json</files>
  <read_first>
    - `packages/feature-flags/src/flags-core.ts:230-238` (mirror `module.workforce-employees`) + `signoff-registry-flags.json:118-125`
    - `96-RESEARCH.md` § Open Q1
  </read_first>
  <action>
    Register `module.employee-portal`: `{ key: 'module.employee-portal', description: 'Employee self-service portal (Theme B): /portal/employee/* employee + manager surfaces. Requires module.workforce-employees for data. Ship dark — signoff PENDING→APPROVED before enabling per-org.', default: false, category: 'module', jurisdiction: 'ANY', owner: 'workforce-platform' }`. Add it to any module-flag key list (the array around `flags-core.ts:424`). Add a PENDING signoff entry mirroring the workforce entry in `signoff-registry-flags.json`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/feature-flags test 2>&1 | tail -15 && grep -c "module.employee-portal" packages/feature-flags/src/flags-core.ts packages/feature-flags/src/signoff-registry-flags.json</automated>
  </verify>
  <acceptance_criteria>
    - Flag registered, default false, category module, signoff PENDING.
    - Feature-flags package tests green (registry shape/signoff parity checks).
  </acceptance_criteria>
  <done>The portal surface has its own dark gate; per-widget flags layer on top in later plans.</done>
</task>

<task type="auto">
  <name>Task 4: Personnel-file self-view section allowlist (pure)</name>
  <files>packages/api/src/routers/portal/portal-self-view-sections.ts, packages/api/src/routers/portal/__tests__/portal-self-view-sections.test.ts</files>
  <read_first>
    - `packages/api/src/routers/core/personnel-file/section-access.ts:22-44` (the section enum + order)
    - `96-RESEARCH.md` § Corrections C4
  </read_first>
  <action>
    Create `portal-self-view-sections.ts`: `export const PERSONNEL_FILE_SELF_VIEW_SECTIONS = ['SECTION_A','SECTION_B','SECTION_D'] as const satisfies readonly PersonnelFileSection[];` (section C — pay/PII — deliberately excluded; a code comment states WHY: an employee self-serve view excludes the payroll/PII section pending legal review — no phase/decision ID in the comment) and `export function isSelfViewableSection(section: PersonnelFileSection): boolean` (membership test). Import the `PersonnelFileSection` type from `@contractor-ops/compliance-policy` (the source `section-access.ts` uses). Write the GREEN test: the allowlist excludes `SECTION_C`; `isSelfViewableSection` is true for A/B/D and false for C; the constant is a strict subset of `PERSONNEL_FILE_SECTIONS`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test portal-self-view-sections 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `SECTION_C` absent from the allowlist; `isSelfViewableSection('SECTION_C')` false.
    - Pure module (no Prisma, no I/O); tests GREEN.
    - Comment explains the exclusion WHY with no breadcrumb ID (`pnpm lint:no-breadcrumbs` green).
  </acceptance_criteria>
  <done>The akta self-view (96-05) has a server-fixed, legal-conservative section allowlist to filter on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Session row → subject identity | The CHECK constraint is the DB-level guarantee that a session is exactly one subject; a malformed row cannot exist. |
| Employee → own personnel sections | The self-view allowlist is the section boundary; section C can never enter a self-view response. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01-01 | Elevation of Privilege | a session row claiming both a contractor and a worker (dual subject) | mitigate | The one-of CHECK rejects both/neither at insert (Task 1); regression-tested in 96-02. |
| T-96-01-02 | Information Disclosure | employee self-view leaking the pay/PII section | mitigate | `PERSONNEL_FILE_SELF_VIEW_SECTIONS` excludes section C (Task 4); enforced at the read in 96-05. |
| T-96-01-03 | Tampering | a manager edge pointing across orgs | mitigate | The reports resolver always filters `organizationId` (documented on the resolver in 96-06); Worker rows are org-scoped. |
</threat_model>

<verification>
- `pnpm db:generate` clean; `pnpm -F @contractor-ops/api test portal-self-view-sections` GREEN; `pnpm -F @contractor-ops/feature-flags test` GREEN.
- `pnpm typecheck --filter=@contractor-ops/db --filter=@contractor-ops/api` green.
- `pnpm lint:raw-sql` acknowledges the intentional raw-SQL CHECK/FK in the migration; `pnpm lint:no-breadcrumbs` green; `pnpm standards:check` green.
- Wiki: `structure/prisma-schema-areas.md` (PortalSession subject + managerWorkerId) + `patterns/feature-flags.md` (module.employee-portal) updated in this change set.
</verification>

<success_criteria>
`PortalSession` can be an EMPLOYEE subject (nullable contractorId + workerId + subjectType + one-of CHECK, contractor rows backfilled), `EmployeeProfile.managerWorkerId` exists, `module.employee-portal` is a registered dark flag, and `PERSONNEL_FILE_SELF_VIEW_SECTIONS` is a GREEN-tested allowlist. No `validatePortalSession` change, no procedures, no UI. Downstream plans import all four.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/96-theme-b-employee-self-service-portal/96-01-SUMMARY.md` when done.
</output>
