---
phase: 92-theme-b-leave-management-kp-grade-time-tracking
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/compliance-policy/src/types.ts
  - packages/compliance-policy/src/leave-registry.ts
  - packages/compliance-policy/src/wt-registry.ts
  - packages/compliance-policy/src/index.ts
  - packages/compliance-policy/src/policies/pl.ts
  - packages/compliance-policy/src/policies/de.ts
  - packages/compliance-policy/src/policies/uk.ts
  - packages/compliance-policy/src/policies/us.ts
  - packages/compliance-policy/src/policies/uae.ts
  - packages/compliance-policy/src/policies/ksa.ts
autonomous: true
requirements: [LEAVE-01, TIME-EMP-02]

must_haves:
  truths:
    - "resolveLeaveAccrual(jurisdiction, leaveKind) returns market-correct statutory entitlement for PL/DE/UK/US/UAE/KSA via register-on-import (no parallel rules engine)"
    - "resolveWtLimits(jurisdiction) returns per-jurisdiction daily/weekly/night/overtime limits for PL/DE/UK/US/UAE/KSA"
    - "Every statutory rule value carries an adviser-verify draftLegalText annotation (local-only / legal-deferred)"
  artifacts:
    - path: "packages/compliance-policy/src/leave-registry.ts"
      provides: "registerLeaveAccrualRule + resolveLeaveAccrual + listLeaveAccrualRules (mirrors registry.ts)"
      exports: ["registerLeaveAccrualRule", "resolveLeaveAccrual"]
    - path: "packages/compliance-policy/src/wt-registry.ts"
      provides: "registerWorkingTimeLimit + resolveWtLimits + listWorkingTimeLimits"
      exports: ["registerWorkingTimeLimit", "resolveWtLimits"]
  key_links:
    - from: "packages/compliance-policy/src/policies/pl.ts"
      to: "leave-registry + wt-registry"
      via: "registerLeaveAccrualRule + registerWorkingTimeLimit called at import side-effect (index.ts:7)"
      pattern: "registerLeaveAccrualRule|registerWorkingTimeLimit"
---

<objective>
Add two NEW register-on-import registries to compliance-policy (`LeaveAccrualRule`, `WorkingTimeLimitRule`) alongside the existing `PolicyRule` registry — mirroring `registry.ts` (register/resolve/list) keyed on the existing `Jurisdiction` type. Register the cited statutory values for all six markets in `policies/<cc>.ts` (side-effect wired via index.ts:7). Do NOT extend `PolicyRule` (it is document-centric; leave/WT have no document — D-02 "no parallel rules engine" = reuse the register-on-import idiom, not the doc row shape).

Purpose: The pure, DB-free statutory brain for LEAVE-01 entitlement + TIME-EMP-02 limits. Turns the leave-registry + wt-registry Wave-0 tests GREEN.
Output: 2 new registries + 1 types edit + 1 barrel edit + 6 per-market policy modules.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-RESEARCH.md
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-PATTERNS.md

<interfaces>
From packages/compliance-policy/src/registry.ts:15 — registerPolicyRule (deepFreeze + dup-throw) / resolvePolicyRules(ctx) / listPolicyRules()
From packages/compliance-policy/src/types.ts:6 — type Jurisdiction = 'UK'|'DE'|'PL'|'US'|'KSA'|'UAE' ; :46 interface PolicyRule
From packages/compliance-policy/src/index.ts:7 — side-effect imports of ./policies/{uk,pl,de,us,ksa,uae}
From packages/compliance-policy/src/jurisdiction-resolver.ts:28 — mapIsoToJurisdiction
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registry machinery — types, leave-registry, wt-registry, barrel</name>
  <files>packages/compliance-policy/src/types.ts, packages/compliance-policy/src/leave-registry.ts, packages/compliance-policy/src/wt-registry.ts, packages/compliance-policy/src/index.ts</files>
  <read_first>
    - packages/compliance-policy/src/registry.ts:6 (REGISTRY array + REGISTERED_IDS Set + deepFreeze + dup-throw + resolve filter + list — the skeleton to copy)
    - packages/compliance-policy/src/types.ts:46 (PolicyRule interface — place the two new interfaces next to it)
    - packages/compliance-policy/src/index.ts:47 (registry re-export block)
    - 92-RESEARCH.md § Pattern 2 (LeaveAccrualRule + WorkingTimeLimitRule field lists)
  </read_first>
  <action>Add interfaces `LeaveAccrualRule` (jurisdiction, leaveKind 'ANNUAL'|'PARENTAL'|'BEREAVEMENT'|'STUDY'|'SICK', `baseEntitlementDays(ctx:{tenureYears})` resolver, `proRataByEtat` bool, `carryoverPolicy {maxDays, expiresMonthsIntoNextYear}`, `draftLegalText`) and `WorkingTimeLimitRule` (jurisdiction, maxDailyMinutes, maxDailyHardCeilingMinutes, weeklyAvgMaxMinutes, weeklyWindowWeeks, weeklyOptOutAllowed, nightWindow {startHour,endHour}|null, overtimePremium? {standardPct,premiumPct}, draftLegalText) to `types.ts`. Create `leave-registry.ts` mirroring registry.ts: module-level array + Set, `registerLeaveAccrualRule` (deepFreeze + duplicate-key throw on jurisdiction+leaveKind), pure `resolveLeaveAccrual(jurisdiction, leaveKind)`, `listLeaveAccrualRules()`. Create `wt-registry.ts` similarly: `registerWorkingTimeLimit`, `resolveWtLimits(jurisdiction)`, `listWorkingTimeLimits()`. Re-export the register/resolve/list functions + interfaces from `index.ts` next to the existing registry exports. Filter keyed on the existing `Jurisdiction` type — do NOT add a new jurisdiction type.</action>
  <verify>
    <automated>pnpm typecheck --filter @contractor-ops/compliance-policy 2>&1 | grep -qiE "error" && echo HAS-ERR || echo REGISTRY-OK</automated>
  </verify>
  <acceptance_criteria>
    - `leave-registry.ts` + `wt-registry.ts` export register/resolve/list; both re-exported from `index.ts`.
    - The two interfaces live in `types.ts`; `PolicyRule` is NOT extended (separate registries).
    - `registerLeaveAccrualRule`/`registerWorkingTimeLimit` deep-freeze + throw on duplicate (mirror registry.ts).
    - `pnpm typecheck --filter @contractor-ops/compliance-policy` passes.
  </acceptance_criteria>
  <done>Two new registries + interfaces + barrel exports exist and typecheck.</done>
</task>

<task type="auto">
  <name>Task 2: EU market rules — PL, DE, UK (cited statutory values)</name>
  <files>packages/compliance-policy/src/policies/pl.ts, packages/compliance-policy/src/policies/de.ts, packages/compliance-policy/src/policies/uk.ts</files>
  <read_first>
    - packages/compliance-policy/src/policies/pl.ts:9 (registerPolicyRule call convention + draftLegalText "PENDING legal review")
    - 92-RESEARCH.md § Statutory Rule Values PL/DE/UK (KP art.154/129/131/151; BUrlG §3 / ArbZG §3/§6; WTR 1998 reg 13/4/6) + § Code Examples (exact PL calls)
  </read_first>
  <action>In each of pl/de/uk, add `registerLeaveAccrualRule(...)` + `registerWorkingTimeLimit(...)` alongside the existing `registerPolicyRule(...)` calls. PL: ANNUAL 20 (<10yr)/26 (≥10yr) KP art.154 §1, proRata rounds up, carryover expiresMonthsIntoNextYear≈9 (Sep 30); WT maxDailyMinutes 480, weeklyAvgMaxMinutes 2880, weeklyWindowWeeks 16, night 21–07, overtimePremium 50/100. DE: BUrlG §3 min 20 Arbeitstage (5-day week) with override note, proRata true; WT maxDailyMinutes 480 + maxDailyHardCeilingMinutes 600 (ArbZG §3 6-mo avg), night 23–06, no fixed statutory OT premium (annotate). UK: 5.6 weeks / 28-day cap (WTR reg 13/13A); WT weeklyAvgMaxMinutes 2880 weeklyWindowWeeks 17, weeklyOptOutAllowed true, night ≤8h/24h no opt-out, night worker window 23–06, no statutory OT premium. Every value carries a `draftLegalText` with the RESEARCH `[CITED]`/`[ASSUMED, adviser-verify]` tag + "PENDING legal review" (constraint 8, local-only posture). No breadcrumb comments; statute IDs (KP art.154, BUrlG §3, WTR reg 4) are real domain IDs and belong in draftLegalText/WHY.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/compliance-policy test leave-registry wt-registry 2>&1 | grep -qiE "pass|✓" && echo EU-RULES-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `resolveLeaveAccrual('PL','ANNUAL')` yields 20/26 by tenure; `resolveWtLimits('PL').overtimePremium` is {50,100}; `resolveWtLimits('DE').maxDailyHardCeilingMinutes===600`; `resolveWtLimits('UK').weeklyOptOutAllowed===true` — the Wave-0 leave-registry + wt-registry tests turn GREEN for PL/DE/UK.
    - Every registered rule has a non-empty `draftLegalText` carrying the cited statute + PENDING annotation.
  </acceptance_criteria>
  <done>PL/DE/UK leave + WT rules registered, cited, GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: US + Gulf market rules — US, UAE, KSA</name>
  <files>packages/compliance-policy/src/policies/us.ts, packages/compliance-policy/src/policies/uae.ts, packages/compliance-policy/src/policies/ksa.ts</files>
  <read_first>
    - packages/compliance-policy/src/policies/pl.ts:9 (the call convention, again — for consistency)
    - 92-RESEARCH.md § Statutory Rule Values US/UAE/KSA (FLSA 29 USC §207 >40h/wk; UAE FDL 33/2021 art.29 30-day + art.17 8h/48h; KSA Labor Law art.109 21/30-day + art.98 8h/48h + Ramadan)
  </read_first>
  <action>US: no federal statutory paid annual leave — register ANNUAL with a zero statutory floor + a free-text/per-state note (mirror EMP-REG-US-01 10-state approach); WT weeklyAvgMaxMinutes as the FLSA >40h/wk overtime threshold (2400) with `overtimePremium` reflecting 1.5× >40h, no federal daily OT, exempt-classification note; annotate per-state variation (CA daily OT) as adviser-verify. UAE: ANNUAL 30 calendar days after 1yr (2 days/month for 6mo–1yr) FDL 33/2021 art.29; WT maxDailyMinutes 480 / weekly 2880 art.17, Ramadan reduction note. KSA: ANNUAL 21 days (30 after 5 continuous years) Labor Law art.109; WT maxDailyMinutes 480 / weekly 2880 art.98, Ramadan 6h/day for Muslim workers note. All values carry `draftLegalText` with the cited statute + `[CITED]`/`[ASSUMED, adviser-verify]` + PENDING annotation. proRataByEtat true for UAE/KSA/US where applicable.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/compliance-policy test leave-registry wt-registry 2>&1 | grep -qiE "pass|✓" && echo ALL-MARKETS-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `resolveLeaveAccrual('UAE','ANNUAL')` reflects 30 days; `resolveLeaveAccrual('KSA','ANNUAL')` reflects 21/30 by tenure; `resolveWtLimits('US')` reflects the FLSA >40h weekly threshold — the full six-market Wave-0 registry tests turn GREEN.
    - Every registered rule carries a cited + PENDING `draftLegalText`.
    - Ramadan / per-state / DE-Bundesland variations are annotated adviser-verify (not silently omitted).
  </acceptance_criteria>
  <done>US/UAE/KSA leave + WT rules registered, cited, all six markets GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| statutory values → runtime entitlement/limit decisions | wrong values mis-compute leave or mis-fire alerts (compliance exposure) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-05-01 | Tampering (integrity) | wrong/uncited statutory value silently encoded | mitigate | Every rule carries a cited draftLegalText; Wave-0 vectors assert headline values; adviser-verify post-deploy checkpoint |
| T-92-05-02 | Repudiation | rule value changes with no provenance | accept | draftLegalText citation + git history; legal sign-off deferred (local-only posture) |
| T-92-05-SC | Tampering | dependency installs | mitigate | Zero new deps (date-fns already present) |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/compliance-policy test` GREEN (leave-registry + wt-registry).
- `pnpm typecheck --filter @contractor-ops/compliance-policy` passes.
- Post-deploy legal checkpoint recorded for all statutory values (do NOT hard-block).
</verification>

<success_criteria>
Two register-on-import registries resolve market-correct leave + WT rules for all six markets, every value adviser-verify annotated; Wave-0 registry tests GREEN.
</success_criteria>

<output>
Create `.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-05-SUMMARY.md` (list the statutory values + their post-deploy adviser-verify checkpoint).
</output>
