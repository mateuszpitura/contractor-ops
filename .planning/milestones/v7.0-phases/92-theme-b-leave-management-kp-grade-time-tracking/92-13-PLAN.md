---
phase: 92-theme-b-leave-management-kp-grade-time-tracking
plan: 13
type: execute
wave: 7
depends_on: ["92-11", "92-12"]
files_modified:
  - apps/web-vite/src/components/leave/hooks/use-team-calendar.ts
  - apps/web-vite/src/components/leave/team-calendar-section.tsx
  - apps/web-vite/src/components/leave/team-calendar/team-calendar-view.tsx
  - apps/web-vite/src/components/leave/team-calendar/calendar-month-grid.tsx
  - apps/web-vite/src/components/leave/team-calendar/capacity-cell.tsx
  - apps/web-vite/src/components/leave/team-calendar/conflict-marker.tsx
  - apps/web-vite/src/components/leave/team-calendar/capacity-legend.tsx
autonomous: true
requirements: [LEAVE-03]

must_haves:
  truths:
    - "The team calendar shows month + quarter capacity with a heatmap band and flags conflicts on overlapping same-team requests (icon + text, never color alone)"
    - "The calendar is built from packages/ui primitives + a div grid (no third-party block); it mirrors correctly in RTL"
    - "Day cells meet the 44px pointer-target size and are keyboard-navigable"
  artifacts:
    - path: "apps/web-vite/src/components/leave/team-calendar/team-calendar-view.tsx"
      provides: "month/quarter capacity + conflict view (presentational)"
    - path: "apps/web-vite/src/components/leave/hooks/use-team-calendar.ts"
      provides: "sole tRPC boundary — trpc.leave.listTeamCalendar"
  key_links:
    - from: "capacity-cell.tsx"
      to: "contractors/insights/proportion-bar.tsx"
      via: "color-mix(in oklch, var(--status-*)) band idiom"
      pattern: "color-mix"
---

<objective>
Ship the S2 team calendar (LEAVE-03) — a NEW build (no reusable team-calendar exists). Month + quarter capacity heatmap + conflict warnings on overlapping same-team requests, built from `packages/ui` primitives + a div grid (RTL-correct), reusing only the `proportion-bar.tsx` `color-mix` band idiom. Consumes the i18n keys from Plan 12 (no message-file edits).

**Binding UI stack:** `frontend-design` + `impeccable` + web-vite layering. Follow 92-UI-SPEC.md § Team Calendar exactly (44px day cells; conflict = TriangleAlert + text; success/warning/danger bands; keyboard nav; RTL mirror).

Purpose: LEAVE-03 capacity + conflict surface. Turns the team-calendar Wave-0 test GREEN.
Output: hook + wired section + 5 presentational calendar components.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-UI-SPEC.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
tRPC (Plan 09): trpc.leave.listTeamCalendar({from,to,teamId?}) → per-day {teamId, capacityPct, conflict, holidays, requests}
Idiom: apps/web-vite/src/components/contractors/insights/proportion-bar.tsx:34 (color-mix band + aria-pressed segments)
packages/ui: card, tooltip, badge, skeleton, AtelierEmptyState, QueryErrorPanel; icon TriangleAlert (lucide)
i18n (Plan 12): EmployeeTime/Leave calendar keys (Month/Quarter/Conflict/Available/Busy/Over capacity/empty/error)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Team-calendar hook + wired section</name>
  <files>apps/web-vite/src/components/leave/hooks/use-team-calendar.ts, apps/web-vite/src/components/leave/team-calendar-section.tsx</files>
  <read_first>
    - apps/web-vite/src/components/approvals/hooks/use-approval-queue.ts (sole-boundary hook idiom + nuqs URL state)
    - 92-UI-SPEC.md § S2 + § Interaction & State Contract (grid skeleton / empty CalendarRange / QueryErrorPanel; Month/Quarter toggle)
  </read_first>
  <action>Create `use-team-calendar.ts` as the SOLE tRPC boundary: `trpc.leave.listTeamCalendar` query keyed on month/quarter range + optional teamId; `nuqs` URL state for view mode (month|quarter) + focused date; return a props bundle for the view. Create `team-calendar-section.tsx` (wired section) owning loading (grid skeleton of month cells), empty (`AtelierEmptyState variant="page"`, icon CalendarRange), and error (`QueryErrorPanel`) states, and the Month/Quarter toggle (segmented control, teal active). No tRPC in the view components.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite run check:web-vite-data-layer 2>&1 | grep -qiE "pass|ok|✓" && echo CAL-BOUNDARY-OK</automated>
  </verify>
  <acceptance_criteria>
    - `use-team-calendar.ts` is the only tRPC caller for this surface (`check:web-vite-data-layer` passes).
    - The wired section owns loading/empty/error + the Month/Quarter toggle.
  </acceptance_criteria>
  <done>Calendar hook + section wired with all three states.</done>
</task>

<task type="auto">
  <name>Task 2: Calendar presentational components + team-calendar test GREEN</name>
  <files>apps/web-vite/src/components/leave/team-calendar/team-calendar-view.tsx, apps/web-vite/src/components/leave/team-calendar/calendar-month-grid.tsx, apps/web-vite/src/components/leave/team-calendar/capacity-cell.tsx, apps/web-vite/src/components/leave/team-calendar/conflict-marker.tsx, apps/web-vite/src/components/leave/team-calendar/capacity-legend.tsx</files>
  <read_first>
    - apps/web-vite/src/components/contractors/insights/proportion-bar.tsx:34 (color-mix band + aria-pressed idiom to reuse)
    - 92-UI-SPEC.md § Team Calendar visual spec (7-col month grid; quarter = 3 month strips; 44px cells; success→warning→danger bands; conflict TriangleAlert; holiday/weekend muted; legend always visible; RTL mirror)
    - apps/web-vite/src/components/leave/__tests__/team-calendar.test.tsx (RED contract from Plan 01)
  </read_first>
  <action>Build the presentational tree from packages/ui primitives + a div grid: `TeamCalendarView` (month/quarter switch), `CalendarMonthGrid` (7-column week grid; quarter = 3 stacked month strips), `CapacityCell` (date top-start tabular-nums + capacity band via `color-mix(in oklch, var(--status-*) …)` success/warning/danger by % team out + up to N avatar/initials chips + "+K more"; ≥44px tall; focusable with `aria-label`= date+capacity+count; keyboard arrow-nav + Enter), `ConflictMarker` (`TriangleAlert` `--status-danger` + accessible "Conflict" tooltip "N overlapping requests" — icon + text, never color alone), `CapacityLegend` (Available/Busy/Over capacity bands, always visible). Holidays/weekends muted (`--muted`) with a seeded-holiday dot + name tooltip. RTL: logical properties only; the month grid mirrors (week starts on the right via dir-aware layout). Turn the team-calendar test GREEN (scoped run only — never unscoped, RAM).</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite test team-calendar 2>&1 | grep -qiE "pass|✓" && pnpm --filter @contractor-ops/web-vite run check:web-vite-presentational 2>&1 | grep -qiE "pass|ok|✓" && echo CAL-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/web-vite test team-calendar` GREEN (conflict marker icon+text + capacity band aggregation).
    - No third-party calendar block; built from packages/ui + div grid; RTL mirrors.
    - Day cells ≥44px, keyboard-navigable, `aria-label` carries date+capacity+count.
    - `check:web-vite-presentational` passes (components are props-in → JSX-out).
  </acceptance_criteria>
  <done>Team calendar renders capacity + conflict; test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → tRPC | calendar reads only via the hook; org-scoped server-side |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-13-01 | Information Disclosure | calendar shows another org's/team's leave | accept | Server tenant-scopes listTeamCalendar (Plan 09); UI renders returned data |
| T-92-13-02 | Tampering | conflict/capacity conveyed by color alone (a11y failure) | mitigate | Conflict = icon + text; legend always visible; non-color cue contract |
| T-92-13-03 | Tampering | third-party calendar block with fetch/eval | mitigate | No third-party block; local primitives only; registry vetting gate not triggered |
| T-92-13-SC | Tampering | dependency installs | mitigate | Zero new deps |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/web-vite test team-calendar` GREEN; `check:web-vite-data-layer` + `check:web-vite-presentational` pass.
- WCAG: 44px targets, keyboard nav, non-color cues, RTL mirror.
</verification>

<success_criteria>
Month + quarter team calendar shows capacity heatmap + same-team conflict markers, built from local primitives, RTL-correct, keyboard-accessible; the team-calendar test is GREEN.
</success_criteria>

<output>
Create `.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-13-SUMMARY.md` when done.
</output>
