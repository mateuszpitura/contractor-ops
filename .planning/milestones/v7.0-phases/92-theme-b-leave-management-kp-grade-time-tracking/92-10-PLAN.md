---
phase: 92-theme-b-leave-management-kp-grade-time-tracking
plan: 10
type: execute
wave: 4
depends_on: ["92-05", "92-08"]
files_modified:
  - packages/api/src/services/wt-limit-scan.ts
  - apps/cron-worker/src/jobs/handlers/reminders/wt-limit-scan.ts
  - apps/cron-worker/src/jobs/handlers/reminders/index.ts
autonomous: true
requirements: [TIME-EMP-02]

must_haves:
  truths:
    - "The daily WT scan fans out over SUPPORTED_REGIONS (EU+ME) with getRegionalClient — no ME employee is silently excluded"
    - "The scan detects rolling weekly-average 48h breaches and emits ONE dedup-gated digest per recipient/day via claimCronNotificationDedup + dispatch()"
    - "Dedup keys are region-prefixed so one org's employee data never leaks into another region's notifications"
  artifacts:
    - path: "packages/api/src/services/wt-limit-scan.ts"
      provides: "runWtLimitScan (region fan-out) + per-client two-pass digest (mirrors compliance-reminder-scan)"
      exports: ["runWtLimitScan"]
    - path: "apps/cron-worker/src/jobs/handlers/reminders/wt-limit-scan.ts"
      provides: "cron sub-job wiring under the reminders advisory lock"
  key_links:
    - from: "packages/api/src/services/wt-limit-scan.ts"
      to: "compliance-reminder-scan runComplianceReminderScan"
      via: "region fan-out + two-pass per-recipient digest twin"
      pattern: "SUPPORTED_REGIONS|claimCronNotificationDedup"
---

<objective>
Build the daily working-time-limit batch scan (TIME-EMP-02 rolling-window half) as a verbatim twin of `compliance-reminder-scan` / `economic-dependency-scan`: region fan-out over SUPPORTED_REGIONS, per-client two-pass collect→digest, one dedup-gated digest per recipient/day, dispatch() to in-app/email/Slack/Teams. Wire it as a reminders cron sub-job under the existing advisory lock. Region-prefix every dedup key (Pitfall 3 — a tenant-frame-less scan must fan out; under-filtering excludes ME employees).

Purpose: The rolling weekly-average 48h breach detection the synchronous on-save check (Plan 08) cannot do. Turns the wt-limit-scan Wave-0 test GREEN.
Output: 1 scan service + 1 cron sub-job + the reminders index wiring.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-RESEARCH.md
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-PATTERNS.md

<interfaces>
From packages/api/src/services/compliance-reminder-scan.ts:167 — runComplianceReminderScan (for-region getRegionalClient + skip on unavailable) ; :204 per-client two-pass (collectPendingFires → dispatchDigests) ; :37 createCronLogger
From Plan 05: resolveWtLimits(jurisdiction) ; Plan 08: checkWtLimits (reuse the pure classifier for daily-band; the scan adds the rolling window)
From packages/api/src/services/notification-service.ts:270 — dispatch ; claimCronNotificationDedup (region-prefixed key)
From apps/cron-worker/src/jobs/handlers/reminders/index.ts:383 — Promise.all batch under tryAcquireXactLock(tx,'cron','reminders') ; :401 executeComplianceReminderScan wiring precedent
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WT-limit daily scan service (region fan-out + two-pass digest)</name>
  <files>packages/api/src/services/wt-limit-scan.ts</files>
  <behavior>
    - Test: a worker whose rolling weekly-average exceeds 48h fires a breach
    - Test: fan-out covers EU AND ME regions (no ME exclusion)
    - Test: exactly ONE digest per recipient per day (dedup)
    - Test: dedup keys are region-prefixed
  </behavior>
  <read_first>
    - packages/api/src/services/compliance-reminder-scan.ts:167 (runComplianceReminderScan region fan-out — copy verbatim) + :204 (per-client two-pass) + :14 (the comment: flat one-notification-per-breach was tried and produced fatigue spam — keep the digest layer)
    - packages/api/src/services/economic-dependency-scan.ts (the threshold-scan twin)
    - 92-RESEARCH.md § Pitfall 3 (region fan-out, region-prefix dedup) + § Pitfall 5 (rolling window per jurisdiction weeklyWindowWeeks)
    - packages/api/src/services/__tests__/wt-limit-scan.test.ts (RED contract)
  </read_first>
  <action>Create `wt-limit-scan.ts` mirroring `runComplianceReminderScan`: `runWtLimitScan(now)` loops `SUPPORTED_REGIONS`, `getRegionalClient(region)` (skip + log.warn on unavailable — never throw), calls a per-region worker that (pass 1) sums `EmployeeTimeRecord.workedMinutes` across each worker's jurisdiction rolling window (`resolveWtLimits(jurisdiction).weeklyWindowWeeks`) and collects breaches per recipient (manager/HR), then (pass 2) `dispatchDigests` — ONE digest per recipient/day gated by `claimCronNotificationDedup` with a REGION-PREFIXED key (`wt:${region}:${recipient}:${day}`). Notification title/body are dotted i18n copy-keys resolved by `resolveEventCopy` (entityType `EMPLOYEE_TIME_RECORD`). Logger `createCronLogger('wt-limit-scan')`; no console.*. Do NOT remove or collapse the digest layer.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test wt-limit-scan 2>&1 | grep -qiE "pass|✓" && echo WT-SCAN-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test wt-limit-scan` GREEN (rolling breach + EU+ME fan-out + one digest/recipient/day + region-prefixed dedup).
    - The scan uses its own regional clients (not a single EU client); a missing region is skipped with a warning, never a throw.
    - Notification copy is dotted i18n keys, not hardcoded strings.
  </acceptance_criteria>
  <done>WT daily scan GREEN with region fan-out + digest throttle.</done>
</task>

<task type="auto">
  <name>Task 2: Cron sub-job wiring under the reminders advisory lock</name>
  <files>apps/cron-worker/src/jobs/handlers/reminders/wt-limit-scan.ts, apps/cron-worker/src/jobs/handlers/reminders/index.ts</files>
  <read_first>
    - apps/cron-worker/src/jobs/handlers/reminders/index.ts:383 (the Promise.all batch + :401 executeComplianceReminderScan wiring + :397 crash-isolation comment: use own prismaRaw connections, dedup unique index is the real idempotency guard) + :367 tryAcquireXactLock
    - apps/cron-worker/src/jobs/handlers/reminders/ (drv-clearance-expiries.ts sub-job precedent)
  </read_first>
  <action>Create a `wt-limit-scan.ts` cron sub-job in the reminders handler dir that calls `runWtLimitScan()` and returns a result summary, following the `executeComplianceReminderScan` precedent (own connections, not the lock-holding tx — the dedup unique index is the idempotency guard). Wire it into the reminders `Promise.all` batch in `index.ts` under the existing `tryAcquireXactLock(tx,'cron','reminders')` tick. Use `createCronLogger`; no console.*.</action>
  <verify>
    <automated>grep -q "runWtLimitScan\|wt-limit-scan" apps/cron-worker/src/jobs/handlers/reminders/index.ts && pnpm typecheck --filter @contractor-ops/cron-worker 2>&1 | grep -qiE "error" && echo HAS-ERR || echo CRON-WIRED</automated>
  </verify>
  <acceptance_criteria>
    - The WT scan sub-job is invoked from the reminders batch under the advisory lock.
    - It uses its own DB connections (crash-isolated), matching the compliance-scan precedent.
    - `pnpm typecheck --filter @contractor-ops/cron-worker` passes.
  </acceptance_criteria>
  <done>WT scan runs daily under the reminders cron lock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| cron (tenant-frame-less) → regional DBs | scan runs without a tenant frame; must fan out per region + region-prefix keys |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-10-01 | Information Disclosure | one org's employee data leaks into another region's notification (shared dedup key) | mitigate | Region-prefixed dedup keys (`wt:${region}:…`); per-region client fan-out |
| T-92-10-02 | Information Disclosure | ME (UAE/KSA) employees silently excluded (EU-only client) | mitigate | SUPPORTED_REGIONS fan-out + getRegionalClient (Pitfall 3) |
| T-92-10-03 | Denial of Service | notification fatigue (one alert per breach) | mitigate | Two-pass per-recipient/day digest via claimCronNotificationDedup (keep the digest layer) |
| T-92-10-04 | Denial of Service | cron double-fire | mitigate | tryAcquireXactLock('reminders') advisory lock + dedup unique index |
| T-92-10-SC | Tampering | dependency installs | mitigate | Zero new deps |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test wt-limit-scan` GREEN.
- `pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/cron-worker` passes; `pnpm lint:logs` clean (no console.*).
</verification>

<success_criteria>
The daily WT scan detects rolling weekly-average breaches, fans out over EU+ME, and emits one region-scoped digest per recipient/day under the reminders cron lock.
</success_criteria>

<output>
Create `.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-10-SUMMARY.md` when done.
</output>
