---
phase: 92-theme-b-leave-management-kp-grade-time-tracking
plan: 11
type: execute
wave: 5
depends_on: ["92-08", "92-09", "92-10"]
files_modified:
  - packages/api/src/routers/workforce/employee-time.ts
  - packages/api/src/routers/workforce/ewidencja.ts
  - packages/api/src/root.ts
autonomous: true
requirements: [TIME-EMP-01, TIME-EMP-02, TIME-EMP-03]

must_haves:
  truths:
    - "Saving an employee time record runs the synchronous WT check and returns any breach findings alongside the saved row (non-blocking warning)"
    - "The ewidencja router generates + lists snapshots via the INSERT-only builder with audit"
    - "leave, employeeTime, ewidencja are mounted in workforceRouters behind module.workforce-employees; flag OFF → METHOD_NOT_FOUND"
  artifacts:
    - path: "packages/api/src/routers/workforce/employee-time.ts"
      provides: "upsertRecord (+sync WT check), listRecords, weekSummary"
      exports: ["employeeTimeRouter"]
    - path: "packages/api/src/routers/workforce/ewidencja.ts"
      provides: "generate, list, get (INSERT-only supersede via builder)"
      exports: ["ewidencjaRouter"]
  key_links:
    - from: "packages/api/src/root.ts"
      to: "workforceRouters"
      via: "leave/employeeTime/ewidencja added to the conditional-spread const"
      pattern: "workforceRouters"
---

<objective>
Build the employee-time + ewidencja tRPC routers and mount all three new workforce routers (leave, employeeTime, ewidencja) behind `module.workforce-employees`. The time-save procedure runs the synchronous WT check (Plan 08) and returns non-blocking breach findings. The ewidencja router generates via the INSERT-only builder (Plan 08). Mounting is the three-layer flag-off defense: conditional spread in root.ts + per-request `assertWorkforceEnabled`.

Purpose: TIME-EMP-01 (record) + TIME-EMP-02 (on-save check) + TIME-EMP-03 (ewidencja generate) surfaces, reachable only when the flag is on. Turns the workforce-flag Wave-0 test GREEN for the new routers.
Output: 2 new routers + the root.ts mount.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-PATTERNS.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
From Plan 08: checkWtLimits(jurisdiction, record, recentWeekMinutes), buildEwidencjaSnapshot, supersedeAndInsertEwidencja
From packages/validators/src/employee-time.ts (Plan 03): upsertEmployeeTimeRecordInput
From packages/api/src/middleware/require-workforce-flag.ts:25 — assertWorkforceEnabled ; :51 isWorkforceRegistered
From packages/api/src/root.ts:175 — const workforceRouters = { worker, employee } as const ; conditionalWorkforceRouters = isWorkforceRegistered() ? workforceRouters : {}
From Plan 09: leaveRouter (workforce/leave.ts)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: employee-time router (upsert + sync WT check) + ewidencja router</name>
  <files>packages/api/src/routers/workforce/employee-time.ts, packages/api/src/routers/workforce/ewidencja.ts</files>
  <behavior>
    - Test: upsertRecord saves a day-grain EmployeeTimeRecord and returns wt-check findings (breach → warning payload, not an error)
    - Test: ewidencja.generate produces a new snapshot; regenerate INSERTs a superseding version
    - Test: both routers are HR-RBAC + flag gated + audited on mutation
  </behavior>
  <read_first>
    - packages/api/src/routers/core/time.ts:23 (contractor time router shape — analog, but DISTINCT model/hook)
    - Plan 08 services (checkWtLimits, buildEwidencjaSnapshot, supersedeAndInsertEwidencja)
    - packages/api/src/middleware/require-workforce-flag.ts:25 (assertWorkforceEnabled)
    - packages/api/src/services/audit-writer.ts:118 (writeAuditLog + tx)
  </read_first>
  <action>Create `workforce/employee-time.ts` `employeeTimeRouter`: `upsertRecord` (input `upsertEmployeeTimeRecordInput`) upserts the day-grain `EmployeeTimeRecord` keyed `(organizationId, workerId, workDate)`, then runs `checkWtLimits(jurisdiction, record, recentWeekMinutes)` and returns `{record, findings}` — findings are a NON-BLOCKING warning (UI-A3), never thrown; `writeAuditLog` on the mutation. Add `listRecords` + `weekSummary` reads. Create `workforce/ewidencja.ts` `ewidencjaRouter`: `generate({workerId, periodStart, periodEnd})` calls `buildEwidencjaSnapshot` + `supersedeAndInsertEwidencja` (INSERT-only) + `writeAuditLog(action:'ewidencja.generated', tx)`; `list` + `get` reads returning the version chain (latest ACTIVE by version). Both routers: `assertWorkforceEnabled` first, HR-RBAC (`payroll_officer`/`hr_admin`), Zod inputs, no console.*.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test employee-time-record ewidencja-builder 2>&1 | grep -qiE "pass|✓" && pnpm typecheck --filter @contractor-ops/api 2>&1 | grep -qiE "error" && echo HAS-ERR || echo TIME-ROUTERS-OK</automated>
  </verify>
  <acceptance_criteria>
    - `upsertRecord` returns `{record, findings}`; a breach is a warning payload, not a thrown error.
    - `ewidencja.generate` uses INSERT-only supersede + writeAuditLog; no prior-row UPDATE.
    - Both routers gate with `assertWorkforceEnabled` + HR RBAC + Zod; `pnpm typecheck --filter @contractor-ops/api` passes.
  </acceptance_criteria>
  <done>employee-time + ewidencja routers complete and gated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount all three workforce routers + flag-off test GREEN</name>
  <files>packages/api/src/root.ts</files>
  <behavior>
    - Test: with module.workforce-employees ON, leave/employeeTime/ewidencja procedures resolve
    - Test: with the flag OFF, they are absent from appRouter (METHOD_NOT_FOUND) — contractor.* unaffected
  </behavior>
  <read_first>
    - packages/api/src/root.ts:175 (workforceRouters const + conditionalWorkforceRouters spread — add three keys)
    - packages/api/src/__tests__/ (the existing workforce-flag test to extend for the new namespaces)
    - 92-PATTERNS.md § Feature-flag gate (two-layer defense)
  </read_first>
  <action>Extend the `workforceRouters` const in root.ts to add `leave: leaveRouter, employeeTime: employeeTimeRouter, ewidencja: ewidencjaRouter` alongside `worker`/`employee`, keeping the `isWorkforceRegistered()` conditional-spread so the TYPE stays constant across branches (clients always see the namespaces; runtime absence when OFF). Extend the existing workforce-flag test to assert the three new namespaces are METHOD_NOT_FOUND when the flag is OFF and resolve when ON; contractor.* stays unaffected.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test workforce-flag 2>&1 | grep -qiE "pass|✓" && grep -q "employeeTime" packages/api/src/root.ts && grep -q "ewidencja" packages/api/src/root.ts && echo MOUNT-OK</automated>
  </verify>
  <acceptance_criteria>
    - `leave`, `employeeTime`, `ewidencja` appear in `workforceRouters`; the conditional spread is preserved.
    - `pnpm --filter @contractor-ops/api test workforce-flag` GREEN (OFF → METHOD_NOT_FOUND for all three; ON → resolve).
    - contractor.* route shapes unchanged.
  </acceptance_criteria>
  <done>All three routers mounted behind the flag; flag-off proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → workforce routers | reachable only when module.workforce-employees is on |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-11-01 | Elevation of Privilege | dark surface reached with flag off | mitigate | Two-layer: conditional spread (METHOD_NOT_FOUND) + per-request assertWorkforceEnabled; flag-off test GREEN |
| T-92-11-02 | Information Disclosure | time/ewidencja read across orgs | mitigate | tenantProcedure org scope; routers never widen the org filter |
| T-92-11-03 | Repudiation | ewidencja generate / time save without audit | mitigate | writeAuditLog on both mutations with tx |
| T-92-11-04 | Denial of Service | WT breach hard-blocks a legitimate save | mitigate | Findings are a non-blocking warning payload (UI-A3), never thrown |
| T-92-11-SC | Tampering | dependency installs | mitigate | Zero new deps |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test employee-time-record ewidencja-builder workforce-flag` GREEN.
- `pnpm typecheck --filter @contractor-ops/api` passes; contractor.* shape unchanged.
</verification>

<success_criteria>
Time + ewidencja routers ship (on-save WT warning + INSERT-only ewidencja generate), all three workforce routers mounted behind the flag with METHOD_NOT_FOUND when off.
</success_criteria>

<output>
Create `.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-11-SUMMARY.md` when done.
</output>
