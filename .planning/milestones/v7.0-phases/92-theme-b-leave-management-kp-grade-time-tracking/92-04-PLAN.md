---
phase: 92-theme-b-leave-management-kp-grade-time-tracking
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema/ewidencja.prisma
  - packages/db/prisma/schema/migrations/20260701000000_ewidencja_append_only/migration.sql
  - packages/db/prisma/schema/migrations/20260701000000_ewidencja_append_only/down.sql
  - packages/db/src/retention-policy.ts
autonomous: true
requirements: [TIME-EMP-03]

must_haves:
  truths:
    - "EwidencjaSnapshot is an INSERT-only immutable table: every UPDATE raises a DB restrict_violation (never blocked by app convention alone)"
    - "Supersession is INSERT-only — a regeneration inserts a NEW row with an incremented version + previousSnapshotId back-pointer; prior rows are NEVER updated"
    - "'KP-ewidencja' is registered in RETENTION_YEARS (3) + MODEL_RETENTION_TYPE, but NO purge/auto-delete path is wired (3yr = DB-immutability floor, 10yr KP §94⁴ satisfied by non-deletion)"
  artifacts:
    - path: "packages/db/prisma/schema/ewidencja.prisma"
      provides: "EwidencjaSnapshot (append-only, versioned supersede chain) + EwidencjaStatus enum"
      contains: "model EwidencjaSnapshot"
    - path: "packages/db/prisma/schema/migrations/20260701000000_ewidencja_append_only/migration.sql"
      provides: "reject_ewidencja_update BEFORE UPDATE trigger + insert-only RLS + gated delete"
      contains: "reject_ewidencja_update"
  key_links:
    - from: "packages/db/src/retention-policy.ts"
      to: "EwidencjaSnapshot"
      via: "MODEL_RETENTION_TYPE + RETENTION_YEARS['KP-ewidencja']=3 (NO purge cascade)"
      pattern: "KP-ewidencja"
    - from: "migration.sql"
      to: "packages/db/prisma/schema/migrations/20260617000000_auditlog_append_only/migration.sql"
      via: "mirrors reject_auditlog_update trigger + RLS pattern"
      pattern: "restrict_violation"
---

<objective>
Add the PL KP §149 ewidencja immutable archive: a dedicated `EwidencjaSnapshot` table whose immutability is DB-enforced (a BEFORE UPDATE trigger that rejects ALL updates + insert-only RLS), mirroring the AuditLog append-only migration — the only existing trigger-enforced append-only store. Resolve OQ2 as **INSERT-only, zero mutation of prior rows**: supersession inserts a NEW row (incremented `version` + `previousSnapshotId` back-pointer set AT INSERT), so the strict UPDATE-reject trigger never conflicts with a supersede flip. Register retention as a 3-year immutability floor with NO purge path (OQ1: the 10yr KP §94⁴ statutory retention is satisfied by non-deletion; append-only + no delete handler = never deleted).

Purpose: The evidentiary record-of-record for TIME-EMP-03 (criterion-4), tamper-resistant against even BYPASSRLS/owner UPDATE.
Output: 1 schema file + a raw-SQL migration (up + down) + a retention-policy edit.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-RESEARCH.md
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-PATTERNS.md

<interfaces>
From packages/db/prisma/schema/migrations/20260617000000_auditlog_append_only/migration.sql:33 —
  create function app.reject_auditlog_update() ... raise exception '... append-only: UPDATE is not permitted' using errcode='restrict_violation';
  create trigger auditlog_no_update before update on "AuditLog" ...
  create policy auditlog_insert ... for insert with check (app.org_match("organizationId") and app.can_write_ops());
  create policy auditlog_delete ... for delete using (... and app.audit_purge_allowed());
From packages/db/src/retention-policy.ts:13 — RETENTION_YEARS = { '1099-NEC':4, 'backup-withholding':7 } ; :29 MODEL_RETENTION_TYPE = { Form1099Nec:'1099-NEC' }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EwidencjaSnapshot schema (INSERT-only versioned supersede chain)</name>
  <files>packages/db/prisma/schema/ewidencja.prisma</files>
  <read_first>
    - packages/db/prisma/schema/audit.prisma:9 (AuditLog — the append-only analog)
    - packages/api/src/services/tax-form.service.ts:198 (TaxFormSubmission ACTIVE/SUPERSEDED supersede — but adapt to INSERT-only per OQ2)
    - packages/db/prisma/schema/worker.prisma:17 (tenant-owning header + @@index)
    - 92-RESEARCH.md § Architecture Diagram EWIDENCJA (D-06) + § Open Questions Q2 (INSERT-only resolution)
  </read_first>
  <action>Create `ewidencja.prisma` `EwidencjaSnapshot` tenant-owning model: `organizationId`, `workerId`, `periodStart`/`periodEnd @db.Date`, `periodKey String` (e.g. YYYY-MM), `version Int` (increments per regeneration), `previousSnapshotId String?` (self-relation back-pointer set at INSERT — never a forward UPDATE), `status EwidencjaStatus` default ACTIVE, `snapshotJson Json` (frozen KP §149 field set), `generatedByUserId String?`, `createdAt`. Enum `EwidencjaStatus { ACTIVE SUPERSEDED }` — but the "active/current" record is resolved as the latest row by `(organizationId, workerId, periodKey, version desc)`, NOT by a status UPDATE. `@@unique([organizationId, workerId, periodKey, version])`, `@@index([organizationId, workerId, periodKey])`. Tenant-owning — MUST NOT be in globalModels. No `@updatedAt` column (immutable). No breadcrumb comments; KP §149 is a real domain ID and may appear in a WHY comment.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db exec prisma validate 2>&1 | grep -qiE "valid|schema loaded" && grep -q "previousSnapshotId" packages/db/prisma/schema/ewidencja.prisma && grep -q "version" packages/db/prisma/schema/ewidencja.prisma && echo EWIDENCJA-SCHEMA-OK</automated>
  </verify>
  <acceptance_criteria>
    - `EwidencjaSnapshot` has `version` + nullable `previousSnapshotId` self-pointer; no `updatedAt`.
    - `@@unique([organizationId, workerId, periodKey, version])` present.
    - Model is tenant-owning (has organizationId) and NOT global.
    - `prisma validate` + `db:audit-enum-casing` clean.
  </acceptance_criteria>
  <done>ewidencja.prisma validates with the INSERT-only versioned supersede shape.</done>
</task>

<task type="auto">
  <name>Task 2: Append-only trigger migration + retention registration (no purge)</name>
  <files>packages/db/prisma/schema/migrations/20260701000000_ewidencja_append_only/migration.sql, packages/db/prisma/schema/migrations/20260701000000_ewidencja_append_only/down.sql, packages/db/src/retention-policy.ts</files>
  <read_first>
    - packages/db/prisma/schema/migrations/20260617000000_auditlog_append_only/migration.sql:33 (the exact trigger + RLS SQL to mirror)
    - packages/db/src/rls.ts:32 (allowAuditPurge — the transaction-local purge-flag idiom for the gated delete policy)
    - packages/db/src/retention-policy.ts:13 (RETENTION_YEARS) + :29 (MODEL_RETENTION_TYPE) + :16 (the comment inviting new record types)
    - 92-RESEARCH.md § Open Questions Q1 (3yr immutability floor vs 10yr KP §94⁴ — NO purge)
  </read_first>
  <action>Author the raw-SQL migration mirroring `20260617000000_auditlog_append_only`: `create function app.reject_ewidencja_update()` raising `'EwidencjaSnapshot is append-only: UPDATE is not permitted'` with `errcode='restrict_violation'`; `create trigger ewidencja_no_update before update on "EwidencjaSnapshot" for each row execute function app.reject_ewidencja_update()`; an insert-only RLS policy `ewidencja_insert` (`app.org_match("organizationId") and app.can_write_ops()`); and a delete policy gated behind a transaction-local purge flag (mirror `auditlog_delete` / `allowAuditPurge`). Write a paired `down.sql` (drop trigger + function + policies). Edit `retention-policy.ts`: add `'KP-ewidencja': 3` to `RETENTION_YEARS` and `EwidencjaSnapshot: 'KP-ewidencja'` to `MODEL_RETENTION_TYPE`. Add an adviser-verify WHY comment at the registration site documenting the 3yr-immutability-window vs 10yr KP §94⁴ retention distinction (real domain IDs allowed). CRITICAL (OQ1): ship NO purge/auto-delete path for EwidencjaSnapshot — if `RETENTION_YEARS` feeds any active data-purge cascade, EXCLUDE EwidencjaSnapshot from that delete cascade (append-only + no delete handler = never deleted; 10yr statutory retention satisfied by non-deletion). Do NOT apply the migration here — the live per-region apply is the [BLOCKING] Plan 06.</action>
  <verify>
    <automated>grep -q "reject_ewidencja_update" packages/db/prisma/schema/migrations/20260701000000_ewidencja_append_only/migration.sql && grep -q "restrict_violation" packages/db/prisma/schema/migrations/20260701000000_ewidencja_append_only/migration.sql && grep -q "KP-ewidencja" packages/db/src/retention-policy.ts && echo MIGRATION-AUTHORED</automated>
  </verify>
  <acceptance_criteria>
    - `migration.sql` contains the `reject_ewidencja_update` function + `ewidencja_no_update` BEFORE UPDATE trigger + insert-only RLS + gated delete; `down.sql` reverses all of it.
    - `RETENTION_YEARS['KP-ewidencja']===3` and `MODEL_RETENTION_TYPE.EwidencjaSnapshot==='KP-ewidencja'`.
    - No purge/delete cascade references EwidencjaSnapshot (verify the data-purge cron does not include it).
    - An adviser-verify comment documents the 3yr-vs-10yr distinction at the registration site.
  </acceptance_criteria>
  <done>Trigger migration authored (un-applied), retention registered as a no-purge immutability floor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| app/raw-SQL/owner → EwidencjaSnapshot row | evidentiary record must resist UPDATE from ANY path, including BYPASSRLS/owner |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-04-01 | Tampering | edit an archived ewidencja to rewrite hours | mitigate | BEFORE UPDATE trigger raises restrict_violation for every row (blocks even owner/raw SQL); asserted by ewidencja-immutable DB test (Plan 08) |
| T-92-04-02 | Tampering | supersede flip UPDATEs the prior row (would be blocked by the strict trigger, or bypasses immutability) | mitigate | OQ2 INSERT-only: regeneration INSERTs a new version row + previousSnapshotId; no UPDATE of prior rows ever occurs |
| T-92-04-03 | Repudiation / Compliance | 3yr purge under-retains a 10yr statutory record | mitigate | OQ1: no purge path; EwidencjaSnapshot excluded from any delete cascade; adviser-verify comment + post-deploy legal checkpoint |
| T-92-04-04 | Information Disclosure | cross-org read of another org's ewidencja | mitigate | Tenant-owning (not global) → withTenantScope; insert-only RLS org_match |
| T-92-04-SC | Tampering | dependency installs | mitigate | Zero new deps |
</threat_model>

<verification>
- `prisma validate` passes; migration + down.sql authored; `retention-policy.ts` registers 'KP-ewidencja':3 with no purge wiring.
- The runtime immutability proof (`pnpm --filter @contractor-ops/db test ewidencja-immutable`) turns GREEN in Plan 08 after the migration is applied in Plan 06.
</verification>

<success_criteria>
EwidencjaSnapshot is DB-immutable (trigger authored), INSERT-only supersede (OQ2), retention registered as a 3yr floor with NO purge (OQ1). Post-deploy legal checkpoint recorded (do NOT hard-block).
</success_criteria>

<output>
Create `.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-04-SUMMARY.md` (record OQ1 no-purge + OQ2 insert-only decisions + the post-deploy legal checkpoint).
</output>
