---
phase: 92-theme-b-leave-management-kp-grade-time-tracking
plan: 07
type: execute
wave: 3
depends_on: ["92-05", "92-06"]
files_modified:
  - packages/api/src/services/leave-balance.ts
  - packages/api/src/services/approval-engine.ts
  - packages/api/src/middleware/rbac.ts
  - packages/api/src/routers/core/approval-shared.ts
  - packages/api/src/routers/core/approval-queue.ts
autonomous: true
requirements: [LEAVE-01, LEAVE-02]

must_haves:
  truths:
    - "Leave balance = Σ signed ledger minutes; entitlement = round_up(baseEntitlement(tenure) × etat), null etat treated as 1.00 with a logged warning (never throws)"
    - "The generic approval-chain routes + finalizes LEAVE_REQUEST via a branch at exactly the two invoice-coupled seams (route + finalize) — no forked engine"
    - "Approving a leave flow inserts a DEDUCTION ledger row + updates the balance cache in the SAME transaction + writeAuditLog"
    - "A leave_approver-only session (employee:approve_leave, NO invoice:approve) CAN approve/reject/bulk a LEAVE_REQUEST step; an invoice-only approver still CANNOT approve leave and a leave_approver still CANNOT approve an invoice — the shared approval procedures use a resourceType-aware OR-set gate (no over-grant)"
    - "ORG_A never reads ORG_B leave/time/ewidencja rows (cross-org leak test GREEN)"
  artifacts:
    - path: "packages/api/src/services/leave-balance.ts"
      provides: "computeLeaveBalance (Σ ledger), accrue, carryover, proRata(etat), finalizeApprovedLeave helper input"
      exports: ["computeLeaveBalance"]
  key_links:
    - from: "packages/api/src/routers/core/approval-queue.ts"
      to: "finalizeApprovedLeave"
      via: "branch on step.approvalFlow.resourceType === 'LEAVE_REQUEST' at both advanceResult.completed sites"
      pattern: "resourceType === 'LEAVE_REQUEST'"
    - from: "packages/api/src/routers/core/approval-queue.ts"
      to: "assertApprovalActionPermission (approval-shared) + requireAnyPermission (rbac)"
      via: "coarse OR-set middleware ({invoice:['approve']} OR {employee:['approve_leave']}) + a per-request resourceType→permission body assertion after the step is fetched"
      pattern: "assertApprovalActionPermission"
    - from: "packages/api/src/services/leave-balance.ts"
      to: "resolveLeaveAccrual"
      via: "compliance-policy registry (Plan 05)"
      pattern: "resolveLeaveAccrual"
---

<objective>
Build the leave-balance domain service (D-01 append-only ledger; balance = Σ ledger; etat pro-rata + carryover) and extend the generic approval-chain for `LEAVE_REQUEST` at exactly the TWO invoice-coupled seams (route + finalize) — never a forked engine. Make the shared approval procedures' RBAC gate resourceType-aware so the P89 HR roles can actually approve leave (D-03). Prove cross-org isolation.

Purpose: The correctness core for LEAVE-01 (balance) + the approval mechanics for LEAVE-02 (finalize → ledger deduction) + the RBAC gate that makes "managers approve leave" real for the exact roles P89 built. Turns the leave-balance, leave-approval-rbac, and leave-time-cross-org-leak Wave-0 tests GREEN.
Output: 1 new service + approval-seam edits + a resourceType-aware RBAC gate.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-RESEARCH.md
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-PATTERNS.md

<interfaces>
From packages/compliance-policy (Plan 05): resolveLeaveAccrual(jurisdiction, leaveKind): LeaveAccrualRule
From packages/db (Plan 06 client): tx.leaveLedgerEntry, tx.leaveBalance, tx.leaveRequest, EmployeeProfile.etat Decimal?
From packages/api/src/services/approval-engine.ts:123 — routeToChain(tx, orgId, {totalMinor}) ; createApprovalFlow(tx, {resourceType:'INVOICE', resourceId, chainConfig, createdByUserId}) ; :336 checkComplianceHoldAtFinalStep returns null for resourceType !== 'INVOICE'
From packages/api/src/routers/core/approval-shared.ts:255 — finalizeApprovedInvoice(tx, {resourceId, organizationId, db, userId}) ; :323 include:{approvalFlow:true}
From packages/api/src/routers/core/approval-queue.ts:206/340/427/476/502/538 — approve/reject/delegate/requestClarification/bulkApprove/bulkReject, EACH currently `.use(requirePermission({ invoice: ['approve'] }))` ; :525 advanceFlow → if completed → finalizeApprovedInvoice ; :571 bulkReject → invoice.update status REJECTED
From packages/api/src/middleware/rbac.ts:19 — requirePermission(permission) static gate (handles both apiKey scopes + Better Auth session; NO resourceType branching)
From packages/auth/src/permissions.ts:49 — employee:['create','read','update','delete','approve_leave'] ; roles.ts:216 leave_approver = employee:['read','approve_leave'] (NO invoice:approve)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Leave-balance service (Σ ledger + etat pro-rata + carryover)</name>
  <files>packages/api/src/services/leave-balance.ts</files>
  <behavior>
    - Test: computeLeaveBalance == Σ signed ledger minutes (accrual/carryover +, deduction −)
    - Test: entitlement = round_up(baseEntitlement(tenure) × etat); PL etat=0.5 rounds UP to full day
    - Test: null etat → treated as 1.00 + a logged warning; never throws
    - Test: carryover capped per carryoverPolicy.maxDays; expiresMonthsIntoNextYear honored
  </behavior>
  <read_first>
    - packages/api/src/services/tax-form.service.ts:181 (supersede/transaction discipline for the cache update)
    - 92-RESEARCH.md § Pattern 4 (proRata rounds up, carryover) + § Pitfall 6 (PL rounding + Sep-30 deadline) + § Pitfall 7 (null etat → 1.00, never throw)
    - packages/api/src/services/__tests__/leave-balance.test.ts (the RED contract from Plan 01)
    - packages/db/prisma/schema/employee.prisma:44 (etat Decimal(3,2) nullable)
  </read_first>
  <action>Create `leave-balance.ts`: pure `computeLeaveBalance(ledgerRows)` returning Σ signed `minutes`; `resolveEntitlementMinutes({jurisdiction, leaveKind, tenureYears, etat})` calling `resolveLeaveAccrual` then applying `round_up(baseEntitlementDays × etat) × minutesPerDay` — treat a null/undefined `etat` as `1.00` and emit a `createLogger` warning (never throw, per Pitfall 7); PL partial-day rounds UP (Pitfall 6). `accrueAnnual(tx, {worker, year})` inserts an ACCRUAL ledger row + recomputes the LeaveBalance cache in the same tx. `applyCarryover(tx, ...)` inserts a capped CARRYOVER row per `carryoverPolicy`. Export a `recomputeBalanceCache(tx, {worker, leaveTypeId, year})` (Σ ledger → cache) usable as the reconciliation oracle. Use `@contractor-ops/logger` (no console.*). date math via date-fns (already a dep).</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test leave-balance 2>&1 | grep -qiE "pass|✓" && echo LEAVE-BALANCE-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test leave-balance` GREEN (Σ ledger + pro-rata round-up + carryover + null-etat-no-throw).
    - Null `etat` path logs a warning and returns full-time entitlement (no throw).
    - No `console.*`; date math via date-fns.
  </acceptance_criteria>
  <done>leave-balance service GREEN with the D-01 ledger semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Approval-chain LEAVE_REQUEST seams (route + finalize + reject branch) + resourceType-aware RBAC gate + cross-org leak GREEN</name>
  <files>packages/api/src/services/approval-engine.ts, packages/api/src/middleware/rbac.ts, packages/api/src/routers/core/approval-shared.ts, packages/api/src/routers/core/approval-queue.ts</files>
  <read_first>
    - packages/api/src/services/approval-engine.ts:123 (routeToChain + createApprovalFlow — widen the resourceType literal; :336 compliance-hold already null for non-INVOICE)
    - packages/api/src/middleware/rbac.ts:19-58 (requirePermission — extract the "does the caller hold this Permission" check for reuse: it already handles BOTH apiKey scopes AND Better Auth session)
    - packages/auth/src/permissions.ts:49 (employee:['…','approve_leave']) + roles.ts:191-218 (hr_admin/hr_manager/payroll_officer/leave_approver — NONE hold invoice:approve; hr_admin+leave_approver hold employee:approve_leave)
    - packages/api/src/routers/core/approval-shared.ts:255 (finalizeApprovedInvoice — add sibling finalizeApprovedLeave) + :304/:323 (processBulkApprovalSteps already includes approvalFlow, so step.approvalFlow.resourceType is available in the bulk callback)
    - packages/api/src/routers/core/approval-queue.ts:206/340/427/476/502/538 (the six invoice-gated procedures) + :257/:527 (single-approve + bulkApprove finalize sites) + :571 (bulkReject)
    - packages/api/src/services/audit-writer.ts:118 (writeAuditLog signature + tx)
    - packages/api/src/__tests__/leave-time-cross-org-leak.test.ts + leave-approval-rbac.test.ts (the RED contracts from Plan 01)
    - 92-PATTERNS.md § the two invoice-coupled seams (verified excerpts)
  </read_first>
  <action>THREE coupled edits, invoice functional behavior unchanged throughout:

(1) SEAMS. Widen `createApprovalFlow`'s `resourceType: 'INVOICE'` literal type to accept `'LEAVE_REQUEST'` (Pitfall 1 warning sign). Add `routeToLeaveChain(tx, orgId, {leaveType, requestedMinutes})` querying `ApprovalChainConfig where resourceType='LEAVE_REQUEST'` — default org chain (mirror the invoice default-chain resolution; OQ3 RESOLVED: a single default LEAVE_REQUEST chain for v7.0, duration/type-conditioned routing is OUT of scope — note in the summary). Add `finalizeApprovedLeave(tx, {resourceId, organizationId, db, userId})` in approval-shared.ts: set `LeaveRequest.status='APPROVED'`, insert a `DEDUCTION` LeaveLedgerEntry, `recomputeBalanceCache` in the same tx, optionally set `EmployeeProfile.employmentStatus`, and `writeAuditLog({action:'leave.approved', resourceType:'LEAVE_REQUEST', tx})`. In approval-queue.ts, branch BOTH `advanceResult.completed` sites (single-approve + bulkApprove) on `step.approvalFlow.resourceType === 'LEAVE_REQUEST'` → `finalizeApprovedLeave` else `finalizeApprovedInvoice`; and in `bulkReject` add a leave branch (set `LeaveRequest.status='REJECTED'`, NO ledger deduction, `writeAuditLog action:'leave.rejected'`).

(2) RBAC GATE (the fix that makes a leave_approver able to approve leave, WITHOUT over-granting invoice:approve). In rbac.ts, factor the permission check out of `requirePermission` into a reusable `hasPermission(ctx, permission): Promise<boolean>` (same apiKey-scope + Better Auth session logic), and add a coarse-OR middleware `requireAnyPermission(...permissions: Permission[])` that passes when the caller holds ANY of the supplied permission objects (rejects a caller holding none). Keep the existing `requirePermission` byte-unchanged (used everywhere else). In approval-shared.ts add `assertApprovalActionPermission(ctx, resourceType)` mapping `resourceType` → required Permission (`'INVOICE' → {invoice:['approve']}`, `'LEAVE_REQUEST' → {employee:['approve_leave']}`) and throwing `TRPCError FORBIDDEN` (message `E.PERMISSION_DENIED`) via `hasPermission` when unheld — this is the fine-grained enforcement that prevents cross-resource over-grant. In approval-queue.ts, swap the SIX procedures' `.use(requirePermission({ invoice: ['approve'] }))` → `.use(requireAnyPermission({ invoice: ['approve'] }, { employee: ['approve_leave'] }))`, and call `await assertApprovalActionPermission(ctx, step.approvalFlow.resourceType)` immediately after the step is fetched and BEFORE any decision/mutation (single approve/reject; and inside the `processBulkApprovalSteps` callback for bulkApprove/bulkReject — per step). `delegate` (:427) + `requestClarification` (:476) currently fetch the step WITHOUT `include:{approvalFlow:true}` — add the include so `resourceType` is available, then apply the same assertion. Net effect: an invoice approver (invoice:approve, no approve_leave) enters via the OR gate and the body assertion admits INVOICE steps only; a leave_approver (approve_leave, no invoice:approve) enters via the OR gate and the body assertion admits LEAVE_REQUEST steps only — no over-grant either direction. `approver ≠ requester` is still enforced by the existing `validateStepForAction`.

(3) WARNING-3 note (do NOT change behavior). The single-`approve` intermediate-advance notify branch (:296) only fires `dispatchNextApproverNotification` when `result.invoice` is truthy, and `result.invoice` comes from an invoice-specific `tx.invoice.findUnique({where:{id: resourceId}})` (:268) that returns null for a LEAVE_REQUEST — so the "notify the next approver" branch is inert for leave. Under OQ3's single-step default LEAVE_REQUEST chain there is no intermediate step, so this is unreachable, NOT a live bug. Add a one-line code comment at the notify branch (WHY only, no IDs) stating the next-approver dispatch is invoice-specific and a future multi-step leave chain must resolve the resource via a flow-level lookup rather than `tx.invoice.findUnique`; record the same limitation in the summary.

Turn the leave-time-cross-org-leak + leave-approval-rbac tests GREEN (models + client from Plan 06 make the tenantStore.run + role-session assertions resolve).</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test leave-time-cross-org-leak leave-approval-rbac 2>&1 | grep -qiE "pass|✓" && pnpm typecheck --filter @contractor-ops/api 2>&1 | grep -qiE "error" && echo HAS-ERR || echo SEAMS-AND-GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `createApprovalFlow` accepts `resourceType:'LEAVE_REQUEST'`; `pnpm typecheck --filter @contractor-ops/api` passes.
    - Both approve sites + bulkReject branch on `resourceType==='LEAVE_REQUEST'`; the invoice path is byte-unchanged in behavior.
    - `finalizeApprovedLeave` writes the DEDUCTION + cache update + audit inside the caller's tx.
    - The six shared approval procedures gate on `requireAnyPermission({invoice:['approve']},{employee:['approve_leave']})` + a body `assertApprovalActionPermission(ctx, resourceType)`; delegate + requestClarification now `include:{approvalFlow:true}`.
    - `leave-approval-rbac.test.ts` GREEN: a `leave_approver`-only session APPROVES a LEAVE_REQUEST step (mutation resolves + DEDUCTION written); an invoice-only approver is FORBIDDEN on a LEAVE_REQUEST; a `leave_approver` is FORBIDDEN on an INVOICE step.
    - `pnpm --filter @contractor-ops/api test leave-time-cross-org-leak` GREEN for the leave-ledger/request, employee-time, ewidencja families.
  </acceptance_criteria>
  <done>The two invoice seams carry a LEAVE_REQUEST branch, the shared approval gate is resourceType-aware (leave_approver can approve leave with no over-grant), and cross-org isolation is proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| approver → leave finalize | approval mutates the ledger/balance; must be tenant-scoped + audited |
| leave_approver → invoice approve (and invoice approver → leave approve) | the shared approval procedures must gate per resourceType so neither role reaches the other's resource |
| any org → another org's leave/time/ewidencja | reads must auto-scope |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-07-01 | Information Disclosure (IDOR) | cross-org read of leave/time/ewidencja | mitigate | withTenantScope (non-global) + leak test GREEN (Task 2) |
| T-92-07-02 | Tampering | finalize deducts wrong amount / outside tx | mitigate | DEDUCTION + cache recompute in one $transaction; recomputeBalanceCache oracle |
| T-92-07-03 | Repudiation | approve/reject without audit | mitigate | writeAuditLog(action:'leave.approved'/'leave.rejected', tx) at both branches |
| T-92-07-04 | Denial of Service | null etat throws in balance math | mitigate | null etat → 1.00 + logged warning, never throws (Pitfall 7) |
| T-92-07-05 | Elevation of Privilege | leave flow hits contractor-compliance hold | accept | checkComplianceHoldAtFinalStep already returns null for non-INVOICE (verified) |
| T-92-07-06 | Elevation of Privilege (BFLA / over-grant) | leave_approver approves an INVOICE, or invoice approver approves a LEAVE_REQUEST, via the shared procedures | mitigate | coarse `requireAnyPermission({invoice:['approve']},{employee:['approve_leave']})` gate + fine-grained body `assertApprovalActionPermission(ctx, resourceType)` maps resourceType→exact permission; leave-approval-rbac.test.ts asserts BOTH the allowed and the forbidden directions |
| T-92-07-SC | Tampering | dependency installs | mitigate | Zero new deps |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test leave-balance leave-time-cross-org-leak leave-approval-rbac` GREEN.
- `pnpm typecheck --filter @contractor-ops/api` passes; invoice approval path unchanged.
- `pnpm lint:audit-log` clean for the new mutations.
</verification>

<success_criteria>
Balance = Σ ledger with etat pro-rata (null-safe); LEAVE_REQUEST routes + finalizes through the generic chain at the two seams with audit; the shared approval gate is resourceType-aware so a leave_approver can approve leave without gaining invoice:approve; cross-org isolation proven.
</success_criteria>

<output>
Create `.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-07-SUMMARY.md` (record OQ3 default-chain decision, duration-conditioned routing deferred; the resourceType-aware OR-set gate mechanism; and the WARNING-3 next-approver-notify limitation — invoice-specific lookup, must generalize for a future multi-step leave chain).
</output>
