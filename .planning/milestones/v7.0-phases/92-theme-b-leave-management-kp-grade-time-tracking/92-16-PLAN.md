---
phase: 92-theme-b-leave-management-kp-grade-time-tracking
plan: 16
type: execute
wave: 8
depends_on: ["92-09", "92-10", "92-11", "92-13", "92-14", "92-15"]
files_modified:
  - .planning/brain/wiki/domains/leave-and-time.md
  - .planning/brain/wiki/structure/prisma-schema-areas.md
  - .planning/brain/wiki/structure/key-services.md
  - .planning/brain/wiki/structure/api-routers-catalog.md
  - .planning/brain/wiki/structure/cron-jobs.md
  - .planning/brain/wiki/structure/web-vite-domains.md
  - .planning/brain/wiki/patterns/approval-chain.md
  - .planning/brain/wiki/log.md
  - .planning/brain/wiki/hot.md
  - .planning/MEMORY.md
autonomous: true
requirements: [LEAVE-01, LEAVE-02, LEAVE-03, TIME-EMP-01, TIME-EMP-02, TIME-EMP-03]

must_haves:
  truths:
    - "The wiki tracks the shipped leave + employee-time + ewidencja code in the same milestone: a leave-and-time domain page + structure catalogs (prisma/services/routers/cron/web-vite) + the approval-chain reuse pattern"
    - "MEMORY.md carries the new invariants (append-only leave ledger; employee-time distinct from B2B TimeEntry; INSERT-only DB-immutable ewidencja; region-prefixed WT-scan dedup)"
    - "pnpm check:wiki-brain passes (no NEW doc-drift for the touched source files)"
  artifacts:
    - path: ".planning/brain/wiki/domains/leave-and-time.md"
      provides: "Purpose / Flow / Entry points / UI surface / Agent mistakes for the leave+time domain"
    - path: ".planning/MEMORY.md"
      provides: "Phase-92 invariants appended"
  key_links:
    - from: "wiki pages"
      to: "the shipped Phase-92 source"
      via: "verify_with frontmatter pointing at the new files"
      pattern: "verify_with"
---

<objective>
Documentation-follows-code closure for Phase 92 (MANDATORY, same milestone): the wiki + indexes + MEMORY must track the shipped leave-balance engine, leave-request workflow, team calendar, employee statutory time model, WT-limit alerts, and ewidencja immutable archive. A plan that shipped product code without the matching wiki task is INCOMPLETE — this plan closes that.

Purpose: Keep the compass in sync so the next agent does not hallucinate. Satisfies the CLAUDE.md doc-drift gate.
Output: 1 new domain page + 6 structure/pattern refreshes + log/hot + MEMORY invariants + a passing check:wiki-brain.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-CONTEXT.md
@.planning/brain/wiki/hot.md
@.planning/brain/wiki/meta/refresh-triggers.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Leave+time domain page + structure/pattern catalogs</name>
  <files>.planning/brain/wiki/domains/leave-and-time.md, .planning/brain/wiki/structure/prisma-schema-areas.md, .planning/brain/wiki/structure/key-services.md, .planning/brain/wiki/structure/api-routers-catalog.md, .planning/brain/wiki/structure/cron-jobs.md, .planning/brain/wiki/structure/web-vite-domains.md, .planning/brain/wiki/patterns/approval-chain.md</files>
  <read_first>
    - .planning/brain/wiki/domains/worker-foundation.md (the P89 domain page — structure + frontmatter + verify_with convention to mirror)
    - .planning/brain/wiki/structure/prisma-schema-areas.md + key-services.md + api-routers-catalog.md + cron-jobs.md (the catalogs to extend)
    - .planning/brain/wiki/meta/refresh-triggers.md (the trigger matrix — which page each change type requires)
    - 92-CONTEXT.md § canonical_refs (the exact seams to describe)
  </read_first>
  <action>Create `domains/leave-and-time.md` (Purpose, Flow, Entry points, UI surface, Agent mistakes) covering: the append-only leave ledger + balance engine (D-01), the LEAVE_REQUEST approval-chain extension at the two seams (D-03) + sick-direct, the compliance-policy leave/WT registries (D-02/D-05), the distinct EmployeeTimeRecord (D-04), the WT-limit sync check + daily region-fan-out scan (D-05), and the INSERT-only DB-immutable ewidencja (D-06). Extend `prisma-schema-areas.md` (leave.prisma ledger + employee-time.prisma + ewidencja.prisma + PublicHoliday + the append-only trigger), `key-services.md` (leave-balance, wt-limit-check, wt-limit-scan, ewidencja-builder), `api-routers-catalog.md` (leave/employeeTime/ewidencja under workforceRouters), `cron-jobs.md` (the WT-limit daily scan), `web-vite-domains.md` (leave + employee-time domains), and `patterns/approval-chain.md` (the resource-agnostic-engine + branch-on-resourceType reuse). Set each page's `verify_with` frontmatter to the concrete shipped source files. No operational text in the wiki (durable domain facts only).</action>
  <verify>
    <automated>test -f .planning/brain/wiki/domains/leave-and-time.md && grep -q "verify_with" .planning/brain/wiki/domains/leave-and-time.md && grep -q "ewidencja\|EmployeeTimeRecord\|leave" .planning/brain/wiki/structure/prisma-schema-areas.md && echo WIKI-PAGES-OK</automated>
  </verify>
  <acceptance_criteria>
    - `domains/leave-and-time.md` exists with Purpose/Flow/Entry points/UI surface/Agent mistakes + `verify_with` pointing at shipped source.
    - The 5 structure catalogs + the approval-chain pattern page each carry the Phase-92 additions.
    - No operational/round-status text; durable code/domain facts only.
  </acceptance_criteria>
  <done>Domain page + structure/pattern catalogs track the shipped code.</done>
</task>

<task type="auto">
  <name>Task 2: MEMORY invariants + log/hot + rebuild pipeline + check:wiki-brain</name>
  <files>.planning/MEMORY.md, .planning/brain/wiki/log.md, .planning/brain/wiki/hot.md</files>
  <read_first>
    - .planning/MEMORY.md (append convention + prior worker-foundation invariants)
    - .planning/brain/wiki/log.md + hot.md (the log-append + hot-overwrite convention)
    - CLAUDE.md § Documentation follows code (the rebuild sequence: contextual-prefix + bm25 + check:wiki-brain)
  </read_first>
  <action>Append the Phase-92 invariants to `.planning/MEMORY.md`: (1) leave balance = Σ append-only LeaveLedgerEntry (in APPEND_ONLY_MODELS); corrections are reversing ADJUSTMENT rows; null etat → 1.00 (never throw); (2) EmployeeTimeRecord is the employee statutory time model on workerId, DISTINCT from the contractor-coupled TimeEntry; (3) EwidencjaSnapshot is DB-trigger-immutable (reject_ewidencja_update) with INSERT-only versioned supersession + a 3yr immutability floor and NO purge (10yr KP §94⁴ satisfied by non-deletion); (4) LEAVE_REQUEST extends the generic approval-chain at exactly two seams (route + finalize); (5) the WT-limit daily scan fans out over regions with region-prefixed dedup keys. Add a log.md entry + overwrite hot.md. Run the brain rebuild pipeline (contextual-prefix + bm25 index) and `pnpm check:wiki-brain`. Flag the statutory-value adviser-verify + ewidencja 10yr-retention as post-deploy legal checkpoints (do NOT hard-block).</action>
  <verify>
    <automated>pnpm check:wiki-brain 2>&1 | grep -qiE "pass|ok|✓|no drift" && grep -qi "ewidencja\|LeaveLedger\|EmployeeTimeRecord" .planning/MEMORY.md && echo WIKI-BRAIN-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/MEMORY.md` carries the 5 Phase-92 invariants; `log.md` appended; `hot.md` overwritten.
    - `pnpm check:wiki-brain` passes (no NEW doc-drift for the Phase-92 source files).
    - Post-deploy legal checkpoints (statutory values + ewidencja 10yr retention) recorded, not hard-blocking.
  </acceptance_criteria>
  <done>MEMORY invariants + log/hot updated; check:wiki-brain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| code → wiki | stale docs mislead the next agent |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-16-01 | Repudiation | doc drift — shipped code without matching wiki | mitigate | check:wiki-brain gate; verify_with pins each page to source |
| T-92-16-02 | Information Disclosure | operational/PII text written into durable wiki | mitigate | Durable domain facts only; no round-status/PII (MEMORY convention) |
| T-92-16-SC | Tampering | dependency installs | mitigate | Zero new deps |
</threat_model>

<verification>
- `pnpm check:wiki-brain` passes; the domain page + catalogs carry the Phase-92 surfaces; MEMORY invariants appended.
</verification>

<success_criteria>
The wiki + MEMORY track the shipped leave/time/ewidencja code in the same milestone; check:wiki-brain is green; post-deploy legal checkpoints recorded.
</success_criteria>

<output>
Create `.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-16-SUMMARY.md` when done. This is the phase-closure plan.
</output>
