---
phase: 92-theme-b-leave-management-kp-grade-time-tracking
plan: 12
type: execute
wave: 6
depends_on: ["92-11"]
files_modified:
  - apps/web-vite/messages/en.json
  - apps/web-vite/messages/de.json
  - apps/web-vite/messages/pl.json
  - apps/web-vite/messages/ar.json
  - apps/web-vite/src/components/leave/hooks/use-leave-queue.ts
  - apps/web-vite/src/components/leave/leave-queue-section.tsx
  - apps/web-vite/src/components/leave/leave-balance-card.tsx
  - apps/web-vite/src/routes/leave.tsx
autonomous: true
requirements: [LEAVE-02, LEAVE-01]

must_haves:
  truths:
    - "The Leave/EmployeeTime/Ewidencja i18n namespaces exist at full parity across en/de/pl/ar (check:i18n passes; no hardcoded strings)"
    - "The leave-request queue reuses the generic approval-queue (branched cell renderers for LEAVE_REQUEST rows) with the balance-after figure as the side-panel anchor"
    - "The hook is the sole tRPC boundary (check:web-vite-data-layer passes); the page is a thin composer"
  artifacts:
    - path: "apps/web-vite/src/components/leave/hooks/use-leave-queue.ts"
      provides: "sole tRPC boundary for the leave queue + balance-after"
    - path: "apps/web-vite/src/components/leave/leave-balance-card.tsx"
      provides: "balance + entitlement + carryover presentational card"
  key_links:
    - from: "leave-queue-section.tsx"
      to: "approvals/approval-queue + use-approval-actions"
      via: "extend ApprovalQueueRow union for resourceType==='LEAVE_REQUEST'"
      pattern: "LEAVE_REQUEST"
---

<objective>
Ship the S1 leave-request queue + review side-panel + manual sick-entry action, reusing the generic approval-queue verbatim (branched cell renderers), and add ALL Phase-92 i18n namespaces (Leave / EmployeeTime / Ewidencja + Approvals leave-row-variant keys) at full en/de/pl/ar parity. This plan OWNS `messages/*.json` — the other UI plans (13/14/15) consume these keys and do not edit the message files.

**Binding UI stack:** `frontend-design` + `impeccable` + web-vite layering (Page thin composer → wired section branches loading/empty/error → `hooks/use-*.ts` sole tRPC boundary → presentational View). This is a product dashboard — never `design-taste-frontend`. Follow 92-UI-SPEC.md exactly (4 sizes / 2 weights; teal accent reserved list; status tokens; balance-after anchor).

Purpose: LEAVE-02 staff/manager review surface + LEAVE-01 balance display + the i18n foundation for the phase.
Output: 4 message files + leave-queue hook/section + balance card + the /leave route.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-UI-SPEC.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
Reuse verbatim: apps/web-vite/src/components/approvals/approval-queue/{data-table,columns,data-table-toolbar,data-table-bulk-actions,side-panel}.tsx ; approvals/{sla-badge,chain-tracker}.tsx ; hooks/use-approval-actions.ts ; hooks/use-approval-queue.ts (boundary idiom)
tRPC (Plan 09/11): trpc.leave.listRequests, trpc.leave.getBalance, trpc.leave.submitLeaveRequest, trpc.leave.recordSickAbsence + approval actions on the generic chain
packages/ui: DataTable, AtelierPageHeader, AtelierEmptyState, QueryErrorPanel, SummaryCard(+skeleton), EntitySummarySheet
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Leave / EmployeeTime / Ewidencja i18n namespaces (en/de/pl/ar parity)</name>
  <files>apps/web-vite/messages/en.json, apps/web-vite/messages/de.json, apps/web-vite/messages/pl.json, apps/web-vite/messages/ar.json</files>
  <read_first>
    - apps/web-vite/messages/en.json (existing Approvals + Time namespaces — key structure + tone)
    - 92-UI-SPEC.md § Copywriting Contract (all S1–S5 strings) + § Internationalization/RTL
  </read_first>
  <action>Add `Leave`, `EmployeeTime`, and `Ewidencja` namespaces to all four message files at full key parity, plus leave-row-variant keys under `Approvals` (queue row context). Cover every S1–S5 string from the UI-SPEC copywriting contract: queue CTAs, empty/error/destructive copy, sick-entry dialog confirm, team-calendar view toggle + conflict + legend + empty, time-entry field labels + on-save warning, ewidencja generate/regenerate/archived/superseded copy, WT-limit banner. Also add the notification dotted-key copy resolved by `resolveEventCopy` (leave APPROVAL_REQUEST, sick notification, WT-limit alert). ar is RTL — provide translated strings (flag machine-translated ar for native review post-deploy, do NOT hard-block). No hardcoded strings anywhere in the new components; every user-facing string is keyed.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite run check:i18n 2>&1 | grep -qiE "pass|parity|ok|✓" && echo I18N-PARITY-OK</automated>
  </verify>
  <acceptance_criteria>
    - `Leave`, `EmployeeTime`, `Ewidencja` namespaces present in en/de/pl/ar at identical key sets (`check:i18n` passes).
    - Notification dotted-keys exist for the leave APPROVAL_REQUEST, sick, and WT-limit alert copy.
    - No hardcoded user-facing strings introduced.
  </acceptance_criteria>
  <done>All Phase-92 i18n namespaces at four-locale parity.</done>
</task>

<task type="auto">
  <name>Task 2: Leave-queue hook + wired section (reuse approval-queue) + /leave route</name>
  <files>apps/web-vite/src/components/leave/hooks/use-leave-queue.ts, apps/web-vite/src/components/leave/leave-queue-section.tsx, apps/web-vite/src/routes/leave.tsx</files>
  <read_first>
    - apps/web-vite/src/components/approvals/hooks/use-approval-queue.ts (sole-tRPC-boundary idiom: useTRPC + queryOptions + useResourceMutation + nuqs URL state; returns queueSectionProps/sidePanelProps)
    - apps/web-vite/src/components/approvals/approval-queue/columns.tsx (ApprovalQueueRow union — extend for LEAVE_REQUEST)
    - apps/web-vite/src/components/approvals/hooks/use-approval-actions.ts (reuse verbatim for leave chains)
    - 92-UI-SPEC.md § S1 + § Interaction & State Contract (loading/empty/error owned by the wired section; balance-after anchor)
  </read_first>
  <action>Create `use-leave-queue.ts` as the SOLE tRPC boundary: `trpc.leave.listRequests` query, the balance-after query (`trpc.leave.getBalance`), `recordSickAbsence` + submit mutations via `useResourceMutation` with invalidate + i18n success/error messages; URL state via `nuqs`; return a `queueSectionProps` + `sidePanelProps` bundle. Create `leave-queue-section.tsx` (wired section) that owns loading (DataTable skeleton + side-panel Skeleton), empty (`AtelierEmptyState variant="page"`, icon CalendarClock), and error (`QueryErrorPanel`) states, extends the `ApprovalQueueRow` union so `resourceType==='LEAVE_REQUEST'` rows render leave fields (type, date range, days, balance-after, blackout/conflict flag) via branched cell renderers on the SAME `DataTable`, and adds a "Record sick leave" header action (opens the direct-entry dialog — NOT approval). Reuse `sla-badge`/`chain-tracker`/`use-approval-actions` verbatim (engine is generic). Create the thin `/leave` route (Suspense + permissions only; no useTRPC). Run the web-vite gates.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite run check:web-vite-data-layer 2>&1 | grep -qiE "pass|ok|✓" && pnpm --filter @contractor-ops/web-vite run check:web-vite-page-shells 2>&1 | grep -qiE "pass|ok|✓" && echo LAYERING-OK</automated>
  </verify>
  <acceptance_criteria>
    - `use-leave-queue.ts` is the only file issuing tRPC calls for this surface (`check:web-vite-data-layer` passes).
    - The queue reuses the generic approval `DataTable` with branched LEAVE_REQUEST renderers (not a rebuilt table).
    - The wired section owns all three of loading/empty/error; the route is a thin composer (`check:web-vite-page-shells` passes).
    - Sick-entry dialog uses `DialogBody`/`DialogFooter` (`check:web-vite-dialog-pattern`).
  </acceptance_criteria>
  <done>Leave queue + sick action wired, layered, i18n-keyed.</done>
</task>

<task type="auto">
  <name>Task 3: Leave-balance card (side-panel anchor)</name>
  <files>apps/web-vite/src/components/leave/leave-balance-card.tsx</files>
  <read_first>
    - apps/web-vite/src/components/approvals/approval-queue/side-panel.tsx (statusBadgeColors + 480px panel + balance-after placement)
    - 92-UI-SPEC.md § S1 balance-after (available − requested = remaining; negative → status-danger + "Insufficient balance" blocks submit) + § Typography (Display 28px for the primary balance figure)
  </read_first>
  <action>Create `leave-balance-card.tsx` presentational component (props in → JSX out; data via the hook): render entitlement / used / carryover / remaining with `tabular-nums`, the primary balance figure at Display size (28px, font-display, the teal-emphasised anchor), and the balance-after preview (`available − requested = remaining`) — when remaining < 0 render `--status-danger` + "Insufficient balance" and signal submit-blocked. Status via icon + text (never color alone). No tRPC in the component.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite run check:web-vite-presentational 2>&1 | grep -qiE "pass|ok|✓" && echo PRESENTATIONAL-OK</automated>
  </verify>
  <acceptance_criteria>
    - Balance card is presentational (no tRPC; `check:web-vite-presentational` passes).
    - The primary balance figure is the Display-size anchor; balance-after negative → status-danger + submit-blocked signal.
    - Status uses icon + text, tabular-nums for numerals.
  </acceptance_criteria>
  <done>Balance card renders the side-panel anchor per UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → tRPC | reads/writes only through the hook; no client-trusted org/status |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-12-01 | Elevation of Privilege | client submits with a forged status/org | mitigate | Server sets status; org from session; hook passes only validated inputs |
| T-92-12-02 | Information Disclosure | balance/queue rendered for another org | accept | Server tenant-scopes all reads (Plan 09); UI renders what the API returns |
| T-92-12-03 | Tampering | hardcoded strings bypass i18n review | mitigate | check:i18n parity gate; no hardcoded user-facing strings |
| T-92-12-SC | Tampering | third-party UI registry block | mitigate | No third-party block (local packages/ui only); registry vetting gate not triggered |
</threat_model>

<verification>
- `check:i18n`, `check:web-vite-data-layer`, `check:web-vite-page-shells`, `check:web-vite-presentational`, `check:web-vite-dialog-pattern` all pass.
- WCAG: keyboard reachable, focus-visible, non-color status cues, RTL logical properties.
</verification>

<success_criteria>
S1 leave queue + sick action + balance card ship with the balance-after anchor, on the generic approval-queue, fully i18n-keyed at four-locale parity; the hook is the sole tRPC boundary.
</success_criteria>

<output>
Create `.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-12-SUMMARY.md` when done.
</output>
