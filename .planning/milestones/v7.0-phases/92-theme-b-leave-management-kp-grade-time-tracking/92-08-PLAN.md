---
phase: 92-theme-b-leave-management-kp-grade-time-tracking
plan: 08
type: execute
wave: 3
depends_on: ["92-05", "92-06"]
files_modified:
  - packages/api/src/services/wt-limit-check.ts
  - packages/api/src/services/ewidencja-builder.ts
autonomous: true
requirements: [TIME-EMP-02, TIME-EMP-03]

must_haves:
  truths:
    - "The synchronous on-save WT check resolves per-jurisdiction limits and flags a daily-ceiling / current-week heuristic breach (PL >8h, DE 10h ceiling), suppressing a UK 48h breach when wtOptOut is set"
    - "buildEwidencjaSnapshot assembles the KP §149 field set from Σ EmployeeTimeRecord + leave and freezes it to JSON"
    - "Ewidencja supersession is INSERT-only: a new version row + previousSnapshotId back-pointer; prior rows are never updated; an UPDATE raises the DB trigger"
  artifacts:
    - path: "packages/api/src/services/wt-limit-check.ts"
      provides: "checkWtLimits(jurisdiction, record + recent window) → breach findings (pure, synchronous)"
      exports: ["checkWtLimits"]
    - path: "packages/api/src/services/ewidencja-builder.ts"
      provides: "buildEwidencjaSnapshot + supersedeAndInsertEwidencja (INSERT-only)"
      exports: ["buildEwidencjaSnapshot", "supersedeAndInsertEwidencja"]
  key_links:
    - from: "packages/api/src/services/wt-limit-check.ts"
      to: "resolveWtLimits"
      via: "compliance-policy wt-registry (Plan 05)"
      pattern: "resolveWtLimits"
    - from: "packages/api/src/services/ewidencja-builder.ts"
      to: "tax-form.service buildFormSnapshot"
      via: "frozen-JSON snapshot discipline (adapted INSERT-only)"
      pattern: "snapshotJson"
---

<objective>
Build the two time-domain services: the synchronous per-jurisdiction working-time check (TIME-EMP-02 on-save half) and the KP §149 ewidencja snapshot builder (TIME-EMP-03). The ewidencja builder mirrors `buildFormSnapshot` (frozen JSON) but uses INSERT-only supersession (OQ2) so the append-only trigger never conflicts.

Purpose: The correctness cores for TIME-EMP-02 (sync check) + TIME-EMP-03 (immutable snapshot). Turns the wt-limit-check + ewidencja-builder + ewidencja-immutable (DB) Wave-0 tests GREEN.
Output: 2 new services.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-RESEARCH.md
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-PATTERNS.md

<interfaces>
From packages/compliance-policy (Plan 05): resolveWtLimits(jurisdiction): WorkingTimeLimitRule
From packages/db (Plan 06 client): tx.employeeTimeRecord, tx.ewidencjaSnapshot, tx.leaveLedgerEntry
From packages/api/src/services/tax-form.service.ts:108 — buildFormSnapshot (frozen JSON) ; :181 supersedeAndInsert (ACTIVE/SUPERSEDED)
From packages/api/src/services/compliance-reminder-scan.ts:48 — bandFor (pure band classifier — the on-save heuristic analog)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Synchronous working-time-limit check</name>
  <files>packages/api/src/services/wt-limit-check.ts</files>
  <behavior>
    - Test: PL record with workedMinutes > 480 → daily-ceiling breach finding
    - Test: DE record over the 600-minute hard ceiling → breach; under → ok
    - Test: UK current-week > 48h → breach UNLESS wtOptOut=true (then suppressed)
    - Test: pure function — no DB access, deterministic on inputs
  </behavior>
  <read_first>
    - packages/api/src/services/compliance-reminder-scan.ts:48 (bandFor — pure classifier idiom)
    - 92-RESEARCH.md § Pitfall 5 (sync check = fast daily-ceiling + current-week heuristic; the scan does the true rolling average) + § Statutory Rule Values (PL 480, DE 600 ceiling, UK 2880 opt-outable)
    - packages/api/src/services/__tests__/wt-limit-check.test.ts (RED contract from Plan 01)
  </read_first>
  <action>Create `wt-limit-check.ts`: pure `checkWtLimits({jurisdiction, record, recentWeekMinutes})` resolving `resolveWtLimits(jurisdiction)` and returning structured findings (level 'approaching'|'breach', dimension 'daily'|'weekly'|'night', limit, actual, i18n copy-key). Enforce daily ceiling (PL maxDailyMinutes, DE maxDailyHardCeilingMinutes), a current-week heuristic vs weeklyAvgMaxMinutes, and suppress the weekly breach when `record.wtOptOut && rule.weeklyOptOutAllowed` (UK). No DB access (pure); the true rolling-window average is the daily scan's job (Plan 10). Return copy-keys, not hardcoded strings (constraint 7).</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test wt-limit-check 2>&1 | grep -qiE "pass|✓" && echo WT-CHECK-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test wt-limit-check` GREEN (PL daily / DE ceiling / UK opt-out suppression).
    - The function is pure (no Prisma import); findings carry i18n copy-keys, not literal user strings.
  </acceptance_criteria>
  <done>Synchronous WT check GREEN and pure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ewidencja snapshot builder (KP §149 fields, INSERT-only supersede) + DB immutability GREEN</name>
  <files>packages/api/src/services/ewidencja-builder.ts</files>
  <behavior>
    - Test: buildEwidencjaSnapshot(worker, period) contains the KP §149 field set (hours+start/end, night, OT, days-off-with-type, dyżur place, zwolnienia, absences)
    - Test: regenerate INSERTs a new version row (version+1, previousSnapshotId set) — prior rows unchanged
    - Test (db pkg): a raw UPDATE on EwidencjaSnapshot raises restrict_violation
  </behavior>
  <read_first>
    - packages/api/src/services/tax-form.service.ts:108 (buildFormSnapshot frozen JSON) + :181 (supersedeAndInsert — adapt to INSERT-only per OQ2)
    - packages/db/prisma/schema/migrations/20260701000000_ewidencja_append_only/migration.sql (the trigger the immutability test proves)
    - 92-RESEARCH.md § Statutory Rule Values PL (KP §149 + Rozp. 10.12.2018 §6 field list) + § Open Questions Q2 (INSERT-only)
    - packages/api/src/services/__tests__/ewidencja-builder.test.ts + packages/db/src/__tests__/ewidencja-immutable.test.ts (RED contracts)
  </read_first>
  <action>Create `ewidencja-builder.ts`: `buildEwidencjaSnapshot({tx, organizationId, workerId, periodStart, periodEnd})` reads Σ `EmployeeTimeRecord` for the period + the worker's leave ledger and freezes the KP §149 field set (hours worked + start/end; night-shift hours; overtime hours; days-off-with-type; on-call dyżur hours + start/end + place; zwolnienia; other justified absences; unjustified absences) into a `snapshotJson`. `supersedeAndInsertEwidencja(tx, input)` — INSERT-only per OQ2: read the current max `version` for `(organizationId, workerId, periodKey)`, INSERT a new row with `version+1`, `previousSnapshotId` = prior ACTIVE row id, `status='ACTIVE'`; NEVER `updateMany` a prior row (the strict trigger blocks UPDATE). `writeAuditLog(action:'ewidencja.generated', resourceType:'LEAVE_REQUEST' or a suitable EntityType, tx)`. Freeze JSON deterministically (stable key order) so regenerate diffs are meaningful.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test ewidencja-builder 2>&1 | grep -qiE "pass|✓" && pnpm --filter @contractor-ops/db test ewidencja-immutable 2>&1 | grep -qiE "pass|✓|restrict_violation" && echo EWIDENCJA-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test ewidencja-builder` GREEN (KP §149 fields + INSERT-only supersede).
    - `pnpm --filter @contractor-ops/db test ewidencja-immutable` GREEN (UPDATE raises restrict_violation).
    - No `updateMany`/`update` of a prior EwidencjaSnapshot row anywhere in the builder.
    - Generation is audited via writeAuditLog with `tx`.
  </acceptance_criteria>
  <done>Ewidencja builder GREEN; DB immutability proven; supersession is INSERT-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| time-record input → WT check | must not throw on partial data; findings advisory |
| generate → EwidencjaSnapshot | evidentiary write; immutable + audited |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-08-01 | Tampering | ewidencja regenerate mutates prior row / bypasses immutability | mitigate | INSERT-only supersede (OQ2) + DB trigger; ewidencja-immutable test GREEN |
| T-92-08-02 | Repudiation | ewidencja generated without audit | mitigate | writeAuditLog(action:'ewidencja.generated', tx) |
| T-92-08-03 | Tampering (integrity) | WT check under-fires (wrong limit) | mitigate | resolveWtLimits from cited registry; Wave-0 vectors assert PL/DE/UK |
| T-92-08-04 | Information Disclosure | ewidencja snapshot reads another org's time rows | mitigate | tx is tenant-scoped (withTenantScope); builder never widens org filter |
| T-92-08-SC | Tampering | dependency installs | mitigate | Zero new deps |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test wt-limit-check ewidencja-builder` GREEN; `pnpm --filter @contractor-ops/db test ewidencja-immutable` GREEN.
- `pnpm typecheck --filter @contractor-ops/api` passes; `pnpm lint:audit-log` clean.
</verification>

<success_criteria>
Synchronous WT check flags per-jurisdiction breaches (opt-out-aware); ewidencja snapshot carries KP §149 fields and is DB-immutable with INSERT-only supersession.
</success_criteria>

<output>
Create `.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-08-SUMMARY.md` when done.
</output>
