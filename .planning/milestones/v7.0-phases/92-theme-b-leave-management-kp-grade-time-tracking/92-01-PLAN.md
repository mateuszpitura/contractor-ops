---
phase: 92-theme-b-leave-management-kp-grade-time-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/services/__tests__/leave-balance.test.ts
  - packages/compliance-policy/src/__tests__/leave-registry.test.ts
  - packages/compliance-policy/src/__tests__/wt-registry.test.ts
  - packages/api/src/__tests__/leave-approval.test.ts
  - packages/api/src/__tests__/leave-sick-direct.test.ts
  - packages/api/src/__tests__/leave-blackout.test.ts
  - packages/api/src/services/__tests__/wt-limit-check.test.ts
  - packages/api/src/services/__tests__/wt-limit-scan.test.ts
  - packages/api/src/services/__tests__/ewidencja-builder.test.ts
  - packages/api/src/services/__tests__/employee-time-record.test.ts
  - packages/db/src/__tests__/ewidencja-immutable.test.ts
  - packages/api/src/__tests__/leave-time-cross-org-leak.test.ts
  - apps/web-vite/src/components/leave/__tests__/team-calendar.test.tsx
  - packages/api/src/__tests__/fixtures/employee-leave-fixtures.ts
autonomous: true
requirements: [LEAVE-01, LEAVE-02, LEAVE-03, TIME-EMP-01, TIME-EMP-02, TIME-EMP-03]

must_haves:
  truths:
    - "Every phase-92 requirement has at least one RED (or describe.skip HOLD) test file registered before implementation"
    - "P90-gated tests register + skip cleanly (describe.skip HOLD-until-P90) and never brick tsc"
  artifacts:
    - path: "packages/api/src/services/__tests__/leave-balance.test.ts"
      provides: "LEAVE-01 balance = Σ ledger + etat pro-rata + carryover RED contract"
    - path: "packages/db/src/__tests__/ewidencja-immutable.test.ts"
      provides: "TIME-EMP-03 DB trigger UPDATE-reject RED contract"
    - path: "packages/api/src/__tests__/leave-time-cross-org-leak.test.ts"
      provides: "Cross-org isolation RED contract mirroring employee-cross-org-leak.test.ts"
    - path: "packages/api/src/__tests__/fixtures/employee-leave-fixtures.ts"
      provides: "Seeded PublicHoliday rows + Worker(EMPLOYEE)+EmployeeProfile{etat} factory"
  key_links:
    - from: "leave-time-cross-org-leak.test.ts"
      to: "packages/api/src/__tests__/employee-cross-org-leak.test.ts"
      via: "same tenantStore.run + injected where assertion idiom"
      pattern: "tenantStore\\.run"
---

<objective>
Lay all Wave-0 RED test scaffolds from 92-VALIDATION.md so every LEAVE-01..03 / TIME-EMP-01..03 behavior has an automated feedback contract BEFORE any implementation lands. Nyquist validation is enabled — implementation waves turn these RED scaffolds GREEN.

**EXECUTION IS BLOCKED UNTIL PHASE 90 COMPLETES.** Leave + time attach to `Worker(EMPLOYEE)` via `workerId` and read `etat` from P90 `EmployeeProfile`. At plan time P90 is mid-execution. P90-gated tests (anything importing a not-yet-created leave/time module OR needing an `EmployeeProfile{etat}` row) are laid as terminal-RED (Cannot-find-module) or `describe.skip` with a `HOLD-until-P90` marker — mirroring the 90-01 scaffold discipline — so they register and skip cleanly without bricking `tsc`.

Purpose: A provable regression net + the shared employee/holiday fixture factory the whole phase reuses.
Output: 13 test files + 1 fixture module, all RED or HOLD-skipped.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-VALIDATION.md
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-RESEARCH.md
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: compliance-policy + leave/time service RED scaffolds</name>
  <files>packages/compliance-policy/src/__tests__/leave-registry.test.ts, packages/compliance-policy/src/__tests__/wt-registry.test.ts, packages/api/src/services/__tests__/leave-balance.test.ts, packages/api/src/services/__tests__/wt-limit-check.test.ts, packages/api/src/services/__tests__/employee-time-record.test.ts</files>
  <read_first>
    - packages/compliance-policy/src/__tests__/ (existing registry test for the describe/expect idiom)
    - packages/compliance-policy/src/registry.ts:15 (registerPolicyRule / resolvePolicyRules — the resolve shape the new registries mirror)
    - 92-RESEARCH.md § Statutory Rule Values (the PL 20/26, KP art.154; PL 8h/48h, DE 10h ceiling, UK 48h opt-out cited vectors these tests assert)
    - 92-RESEARCH.md § Code Examples (PL registerLeaveAccrualRule / registerWorkingTimeLimit expected values)
  </read_first>
  <action>Create RED unit scaffolds importing the not-yet-existing modules `@contractor-ops/compliance-policy` `resolveLeaveAccrual`/`resolveWtLimits` and `@contractor-ops/api` `computeLeaveBalance`/`checkWtLimits`. Assert the cited statutory vectors: `resolveLeaveAccrual('PL','ANNUAL')` yields 20 days for tenureYears<10 and 26 for ≥10; PL part-time `etat=0.5` pro-rata rounds UP; carryover cap honored; `computeLeaveBalance` == Σ signed ledger `minutes`; `resolveWtLimits('PL').maxDailyMinutes===480`, `resolveWtLimits('DE').maxDailyHardCeilingMinutes===600`, `resolveWtLimits('UK').weeklyOptOutAllowed===true`; `checkWtLimits` flags PL daily >8h and suppresses a UK breach when `wtOptOut` is set. `employee-time-record.test.ts` asserts the `EmployeeTimeRecord` shape is `workerId`-keyed and distinct from `TimeEntry` (no `contractorId`/`contractId`). Terminal-RED via Cannot-find-module is the accepted terminal state for this wave — do NOT stub the missing modules. Do not use `console.*`; no watch flags.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/compliance-policy test leave-registry 2>&1 | grep -qiE "Cannot find module|resolveLeaveAccrual|FAIL" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist and are collected by vitest (they appear in the run output, not skipped-by-typo).
    - Each fails RED with a Cannot-find-module or unresolved-symbol error against the target module (implementation waves 1/3 turn them GREEN).
    - No file imports a P90 `EmployeeProfile` runtime row (pure statutory/ledger math — P90-independent).
  </acceptance_criteria>
  <done>leave-registry, wt-registry, leave-balance, wt-limit-check, employee-time-record scaffolds registered RED.</done>
</task>

<task type="auto">
  <name>Task 2: API integration + DB immutability + cross-org + flag-off scaffolds + shared fixtures</name>
  <files>packages/api/src/__tests__/leave-approval.test.ts, packages/api/src/__tests__/leave-sick-direct.test.ts, packages/api/src/__tests__/leave-blackout.test.ts, packages/api/src/services/__tests__/wt-limit-scan.test.ts, packages/api/src/services/__tests__/ewidencja-builder.test.ts, packages/db/src/__tests__/ewidencja-immutable.test.ts, packages/api/src/__tests__/leave-time-cross-org-leak.test.ts, packages/api/src/__tests__/fixtures/employee-leave-fixtures.ts</files>
  <read_first>
    - packages/api/src/__tests__/employee-cross-org-leak.test.ts:101 (the exact tenantStore.run + injected-where + P2025 + no-context assertions to mirror)
    - packages/db/prisma/schema/migrations/20260617000000_auditlog_append_only/migration.sql:33 (reject_auditlog_update — the trigger the ewidencja-immutable test asserts for)
    - packages/api/src/services/compliance-reminder-scan.ts:167 (region fan-out + per-recipient digest the wt-limit-scan test asserts: EU+ME fan-out, one digest/recipient/day)
    - packages/api/src/services/tax-form.service.ts:181 (supersedeAndInsert — the append-only supersede the ewidencja-builder test asserts)
    - packages/db/prisma/schema/employee.prisma:12 (EmployeeProfile shape the fixture factory builds; etat:44 nullable)
  </read_first>
  <action>Create the integration + DB + isolation scaffolds. `leave-approval.test.ts`: a leave request creates an `ApprovalFlow` with `resourceType==='LEAVE_REQUEST'` and approval inserts a `DEDUCTION` `LeaveLedgerEntry` + decrements the balance cache. `leave-sick-direct.test.ts`: `recordSickAbsence` writes a direct ledger row + a notification and creates NO `ApprovalFlow`. `leave-blackout.test.ts`: an overlapping `BlackoutPeriod` rejects a vacation submit. `wt-limit-scan.test.ts`: the rolling weekly-avg 48h breach fires, region fan-out covers EU+ME, exactly one digest per recipient/day (dedup). `ewidencja-builder.test.ts`: the snapshot carries the KP §149 field set and regenerate supersedes prior (append-only). `ewidencja-immutable.test.ts` (db package): an UPDATE on `EwidencjaSnapshot` raises Postgres `restrict_violation`. `leave-time-cross-org-leak.test.ts`: mirror `employee-cross-org-leak.test.ts` for the leave-ledger/request, employee-time, and ewidencja model families — seed rowA(ORG_A)+rowB(ORG_B), read rowB.id under `tenantStore.run({organizationId: ORG_A})`, assert null + injected `organizationId: ORG_A` where + cross-org mutation throws P2025 + no-context throws 'Tenant context not initialized'. Build `employee-leave-fixtures.ts`: a `makeEmployeeWithProfile({etat})` factory + seeded `PublicHoliday` rows for PL/DE/UK/US/UAE/KSA. Wrap any assertion that needs a live `EmployeeProfile{etat}` row in `describe.skip` with a `HOLD-until-P90` comment marker; the fixture factory itself imports no P90 module (pure builders) so it registers clean.</action>
  <behavior>
    - Test: leave approval → Flow(resourceType=LEAVE_REQUEST) + DEDUCTION ledger row
    - Test: recordSickAbsence → direct ledger row + notification, zero ApprovalFlow rows
    - Test: blackout overlap → submit rejected
    - Test: wt scan → weekly-avg breach fires once/recipient/day across EU+ME
    - Test: ewidencja UPDATE → restrict_violation raised by DB trigger
    - Test: cross-org read of ORG_B row under ORG_A frame → null
  </behavior>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test leave-time-cross-org-leak 2>&1 | grep -qiE "Cannot find module|LeaveLedgerEntry|FAIL|skip" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - All 7 test files + the fixture module exist and are collected by vitest.
    - P90-gated blocks are `describe.skip('… HOLD-until-P90')` — they register (visible in the run) but do not execute or fail on missing rows.
    - `leave-time-cross-org-leak.test.ts` structurally mirrors `employee-cross-org-leak.test.ts` (tenantStore.run + injected-where + P2025 + no-context assertions present).
    - `ewidencja-immutable.test.ts` is in the `@contractor-ops/db` package test set and asserts a `restrict_violation` on UPDATE.
  </acceptance_criteria>
  <done>All Wave-0 integration/DB/isolation scaffolds + fixtures registered; RED or HOLD-skipped; tsc not bricked.</done>
</task>

<task type="auto">
  <name>Task 3: web-vite team-calendar RED scaffold</name>
  <files>apps/web-vite/src/components/leave/__tests__/team-calendar.test.tsx</files>
  <read_first>
    - apps/web-vite/src/components/contractors/insights/proportion-bar.tsx:34 (the capacity-band idiom the calendar reuses; the test asserts band + conflict marker)
    - 92-UI-SPEC.md § Team Calendar (S2 conflict = TriangleAlert + text; capacity success/warning/danger bands; "same team" grouping)
  </read_first>
  <action>Create a RED component scaffold importing the not-yet-existing `TeamCalendarView`/`CalendarMonthGrid` from `components/leave/team-calendar`. Assert: overlapping same-team approved leave renders a conflict marker (role/icon + accessible "Conflict" text, never color alone); the capacity band aggregates % of team out into success/warning/danger. Keep it a scoped web-vite test (never trigger an unscoped suite — kills RAM per MEMORY). Terminal-RED via missing component import is accepted.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite test team-calendar 2>&1 | grep -qiE "Cannot find module|TeamCalendarView|FAIL" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - The test file exists under `components/leave/__tests__/` and is collected by the scoped web-vite run.
    - It fails RED on the missing `TeamCalendarView` import (Plan 13 turns it GREEN).
    - Conflict assertion checks icon + text, not color alone (WCAG non-color-cue contract).
  </acceptance_criteria>
  <done>team-calendar.test.tsx registered RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → generated Prisma client | scaffolds must not import P90-unlanded modules at load-time in a way that bricks tsc |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01-01 | Tampering | RED scaffolds silently green (false pass) | mitigate | Each scaffold asserts a concrete cited vector or Cannot-find-module; verify greps for the RED/skip signal |
| T-92-01-02 | Denial of Service | unscoped web-vite test run | accept | Scoped `--filter @contractor-ops/web-vite test team-calendar` only (RAM guard, MEMORY) |
| T-92-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (research confirms zero new deps) |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test leave` and `--filter @contractor-ops/compliance-policy test` and `--filter @contractor-ops/db test ewidencja-immutable` all COLLECT the new files (RED or skip).
- `pnpm typecheck --filter @contractor-ops/db --filter @contractor-ops/api` is NOT bricked (db/api tsconfig already excludes `src/**/__tests__/**`).
</verification>

<success_criteria>
All 13 test files + 1 fixture module exist, are collected, and are RED or HOLD-skipped; the cross-org + DB-immutability + flag-off scaffolds mirror their named analogs; no tsc regression.
</success_criteria>

<output>
Create `.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-01-SUMMARY.md` when done.
</output>
