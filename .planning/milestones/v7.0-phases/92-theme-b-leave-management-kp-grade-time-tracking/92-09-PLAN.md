---
phase: 92-theme-b-leave-management-kp-grade-time-tracking
plan: 09
type: execute
wave: 4
depends_on: ["92-03", "92-07"]
files_modified:
  - packages/api/src/routers/workforce/leave.ts
autonomous: true
requirements: [LEAVE-02, LEAVE-03, LEAVE-01]

must_haves:
  truths:
    - "submitLeaveRequest validates blackout overlap + sufficient balance, routes to the LEAVE_REQUEST chain, and sets status PENDING"
    - "recordSickAbsence writes a DIRECT ledger row + a notification and creates NO ApprovalFlow (sick = notification, not a request)"
    - "A blackout period rejects an overlapping vacation request"
    - "Approve/reject of a leave request is 100% delegated to the shared approval procedures (Plan 07) — which are now resourceType-gated so a leave_approver can action a LEAVE_REQUEST; the leaveRouter defines NO approve/reject of its own"
    - "Every procedure is flag-gated (assertWorkforceEnabled), HR-RBAC gated, and Zod-validated; sensitive mutations writeAuditLog"
  artifacts:
    - path: "packages/api/src/routers/workforce/leave.ts"
      provides: "submitLeaveRequest, recordSickAbsence, listRequests, getBalance, leaveType CRUD, blackout CRUD, listTeamCalendar"
      exports: ["leaveRouter"]
  key_links:
    - from: "packages/api/src/routers/workforce/leave.ts"
      to: "routeToLeaveChain + createApprovalFlow (Plan 07)"
      via: "submit seam mirroring approval-submit.ts submitForApproval"
      pattern: "routeToLeaveChain"
    - from: "packages/api/src/routers/workforce/leave.ts"
      to: "assertWorkforceEnabled"
      via: "per-request flag gate at the top of every procedure"
      pattern: "assertWorkforceEnabled"
---

<objective>
Build the leave tRPC router: the leave-request submit seam (mirrors `approval-submit.ts` submitForApproval), the direct manual sick-absence entry (no chain — sick is a notification), leave-type + blackout org-config CRUD, balance read, request list, and the team-calendar aggregation read (same-team overlap conflicts + capacity, LEAVE-03). Every procedure is flag-gated + HR-RBAC gated + Zod-validated + audit-logged on mutations. Approve/reject is NOT re-implemented here — it is delegated to the shared approval procedures (Plan 07), which are now resourceType-gated so the P89 HR roles can action a LEAVE_REQUEST.

Purpose: The LEAVE-02 request/sick surface + LEAVE-03 calendar read. Turns leave-approval, leave-sick-direct, leave-blackout Wave-0 tests GREEN.
Output: 1 new router (`workforce/leave.ts`).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-PATTERNS.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
From packages/api/src/routers/core/approval-submit.ts:19 — submitForApproval (tx → findOrThrow resource → routeToChain → createApprovalFlow → set status → post-commit dispatch APPROVAL_REQUEST at :95)
From Plan 07: routeToLeaveChain, createApprovalFlow({resourceType:'LEAVE_REQUEST'}), finalizeApprovedLeave, computeLeaveBalance/resolveEntitlementMinutes
From Plan 07 (RBAC gate): the shared approval procedures (approval-queue.ts approve/reject/delegate/requestClarification/bulkApprove/bulkReject) now gate on `requireAnyPermission({invoice:['approve']},{employee:['approve_leave']})` + a body `assertApprovalActionPermission(ctx, resourceType)` — so a `leave_approver`/`hr_admin` session (employee:approve_leave) can approve/reject a LEAVE_REQUEST while an invoice-only approver cannot, and vice-versa. This leaveRouter therefore adds NO approve/reject procedures.
From packages/api/src/middleware/require-workforce-flag.ts:25 — assertWorkforceEnabled(orgId, region)
From packages/validators/src/leave.ts (Plan 03): submitLeaveRequestInput, recordSickAbsenceInput, leaveTypeUpsertInput, blackoutPeriodUpsertInput
From packages/api/src/services/notification-service.ts:270 — dispatch({type, entityType, entityId, ...}) ; sick uses a plain notification type, NOT APPROVAL_REQUEST
From packages/auth/src/roles.ts:191-218 — hr_admin (employee:create/read/update/delete/approve_leave) / hr_manager (employee:read/update) / payroll_officer (employee:read) / leave_approver (employee:read/approve_leave). NONE hold invoice:approve or any contractor mutation — the BFLA fence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: submitLeaveRequest + recordSickAbsence + balance/list reads</name>
  <files>packages/api/src/routers/workforce/leave.ts</files>
  <behavior>
    - Test: submit creates ApprovalFlow(resourceType=LEAVE_REQUEST) + LeaveRequest PENDING
    - Test: submit with an overlapping BlackoutPeriod → rejected (BAD_REQUEST)
    - Test: submit with insufficient balance → rejected
    - Test: recordSickAbsence → direct ledger row + notification, ZERO ApprovalFlow rows
  </behavior>
  <read_first>
    - packages/api/src/routers/core/approval-submit.ts:19 (submit seam) + :95 (dispatch APPROVAL_REQUEST — sick uses a plain notification instead)
    - packages/api/src/routers/core/approval.ts:1 (barrel/router assembly convention)
    - packages/api/src/middleware/require-workforce-flag.ts:25 (assertWorkforceEnabled)
    - packages/api/src/middleware/rbac.ts:19 (requirePermission — HR-RBAC gate for THIS router's own mutating procedures; approve/reject stay on the Plan-07 shared procedures)
    - packages/api/src/services/audit-writer.ts:118 (writeAuditLog + tx)
    - packages/api/src/__tests__/leave-approval.test.ts + leave-sick-direct.test.ts + leave-blackout.test.ts (RED contracts)
  </read_first>
  <action>Create `workforce/leave.ts` `leaveRouter` on `tenantProcedure.use(requirePermission(...))` with the P89 HR roles (submit/config mutations require the employee resource actions the HR roles hold; do NOT gate this router on invoice:approve). Every procedure begins with `assertWorkforceEnabled(ctx.organizationId, ctx.region)`. `submitLeaveRequest` (input `submitLeaveRequestInput`): in one `$transaction` — find the worker, check `BlackoutPeriod` overlap (reject BAD_REQUEST on overlap), compute available balance via `computeLeaveBalance` and reject if `requestedMinutes` exceeds it, `routeToLeaveChain`, `createApprovalFlow({resourceType:'LEAVE_REQUEST'})`, set `LeaveRequest.status='PENDING'` + `approvalFlowId`; post-commit `dispatch({type:'APPROVAL_REQUEST', entityType:'LEAVE_REQUEST', entityId})` to the approver. `recordSickAbsence` (input `recordSickAbsenceInput`): write a DIRECT `LeaveLedgerEntry` (sick absence) + `writeAuditLog(action:'leave.sick.recorded', tx)` + `dispatch` a plain notification (NOT APPROVAL_REQUEST, NO createApprovalFlow) — sick is a notification, not a request (e-ZLA/eAU auto-pull is v7.5, out of scope). Add `getBalance` (read) + `listRequests` (paginated read). Do NOT add approve/reject procedures — the S1 UI (Plan 12) drives approvals through the shared, now-resourceType-gated approval procedures. Use `@contractor-ops/logger`; no console.*.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test leave-approval leave-sick-direct leave-blackout 2>&1 | grep -qiE "pass|✓" && echo LEAVE-ROUTER-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test leave-approval leave-sick-direct leave-blackout` GREEN.
    - `recordSickAbsence` creates zero ApprovalFlow rows (asserted) and dispatches a non-APPROVAL_REQUEST notification.
    - Blackout overlap + insufficient balance both reject with BAD_REQUEST.
    - The leaveRouter defines NO approve/reject procedure (delegated to Plan-07 shared procedures); every procedure calls `assertWorkforceEnabled` first; mutations call `writeAuditLog` with `tx`.
  </acceptance_criteria>
  <done>Leave submit + sick-direct + balance/list procedures GREEN and gated.</done>
</task>

<task type="auto">
  <name>Task 2: LeaveType + BlackoutPeriod CRUD + team-calendar aggregation read (LEAVE-03)</name>
  <files>packages/api/src/routers/workforce/leave.ts</files>
  <read_first>
    - packages/db/prisma/schema/organization.prisma:212 (Team — same-team grouping key per OQ4) + Member:166 teamId:171
    - packages/validators/src/leave.ts (leaveTypeUpsertInput, blackoutPeriodUpsertInput)
    - 92-UI-SPEC.md § Team Calendar (capacity band + conflict on overlapping same-team requests)
  </read_first>
  <action>Extend `leaveRouter` with org-config CRUD: `leaveType.list/upsert/archive` + `blackout.list/upsert/delete` (all flag-gated + HR-RBAC + audited on mutation). Add `listTeamCalendar({from, to, teamId?})` returning per-day capacity aggregation grouped by the OQ4 same-team key: `LeaveRequest.teamId` (existing Team model) — when `teamId` is null the rows fall into an "Unassigned" bucket. For each day, count approved + pending leave per team, flag a `conflict` when ≥2 overlapping same-team requests, and include seeded `PublicHoliday` rows for weekend/holiday marking. This is a read (no mutation, no audit needed). Record the OQ4 decision (Team via LeaveRequest.teamId; EmployeeProfile has no direct teamId) in the summary.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test leave 2>&1 | grep -qiE "pass|✓" && pnpm typecheck --filter @contractor-ops/api 2>&1 | grep -qiE "error" && echo HAS-ERR || echo LEAVE-CRUD-OK</automated>
  </verify>
  <acceptance_criteria>
    - `leaveType` + `blackout` CRUD procedures exist, flag-gated, HR-RBAC gated, audited on mutation.
    - `listTeamCalendar` returns per-day capacity + a `conflict` flag on overlapping same-team requests, keyed on `LeaveRequest.teamId`; null → Unassigned bucket.
    - `pnpm typecheck --filter @contractor-ops/api` passes.
  </acceptance_criteria>
  <done>Leave-type/blackout CRUD + team-calendar aggregation read complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → leaveRouter | leave/sick/config inputs cross here; Zod + flag + RBAC gate |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-09-01 | Elevation of Privilege (BFLA) | employee approves own leave / approver mutates the wrong resource | mitigate | Approve/reject is delegated to the Plan-07 shared procedures, which gate on `requireAnyPermission({invoice:['approve']},{employee:['approve_leave']})` + a body `assertApprovalActionPermission(ctx, resourceType)`: a `leave_approver` (employee:approve_leave) can action a LEAVE_REQUEST but NOT an INVOICE; an invoice-only approver cannot action a LEAVE_REQUEST. `approver ≠ requester` is enforced by the shared `validateStepForAction`. This leaveRouter's own mutations are HR-RBAC gated on the `employee` resource and never expose contractor mutations (the HR roles hold no contractor write). Verified by leave-approval-rbac.test.ts (Plan 07). |
| T-92-09-02 | Spoofing/EoP | dark surface reached with flag off | mitigate | assertWorkforceEnabled at the top of every procedure (per-request), plus the conditional mount (Plan 11) |
| T-92-09-03 | Tampering | sick entry inflates balance without audit | mitigate | writeAuditLog(action:'leave.sick.recorded', tx); ledger is append-only |
| T-92-09-04 | Information Disclosure | listTeamCalendar leaks another org's team leave | mitigate | tenantProcedure org scope; team query never widens org filter |
| T-92-09-05 | EoP | mass-assign LeaveRequest.status on submit | mitigate | status is server-set PENDING; Zod input omits status |
| T-92-09-SC | Tampering | dependency installs | mitigate | Zero new deps |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test leave` GREEN (approval/sick/blackout).
- `pnpm typecheck --filter @contractor-ops/api` passes; `pnpm lint:audit-log` clean.
</verification>

<success_criteria>
Leave router delivers submit (chain), sick-direct (notification), balance/list, config CRUD, and the same-team calendar read — all flag+RBAC+Zod gated and audited; approve/reject delegated to the resourceType-gated shared procedures.
</success_criteria>

<output>
Create `.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-09-SUMMARY.md` (record OQ4 same-team-grouping decision).
</output>
