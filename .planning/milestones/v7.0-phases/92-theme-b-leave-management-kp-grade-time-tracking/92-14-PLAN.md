---
phase: 92-theme-b-leave-management-kp-grade-time-tracking
plan: 14
type: execute
wave: 7
depends_on: ["92-11", "92-12"]
files_modified:
  - apps/web-vite/src/components/employee-time/hooks/use-employee-time.ts
  - apps/web-vite/src/components/employee-time/employee-time-section.tsx
  - apps/web-vite/src/components/employee-time/employee-time-entry-view.tsx
  - apps/web-vite/src/components/employee-time/wt-limit-warning-banner.tsx
  - apps/web-vite/src/components/employee-time/wt-limit-alert-banner.tsx
  - apps/web-vite/src/routes/employee-time.tsx
autonomous: true
requirements: [TIME-EMP-01, TIME-EMP-02]

must_haves:
  truths:
    - "The day-grain employee time entry form captures hours/night/overtime/weekend-holiday/on-call/absence and saves via the distinct employee-time tRPC (not the contractor Time.* procedures)"
    - "On save, a WT-limit breach shows a NON-BLOCKING inline warning with 'Save anyway' (UI-A3); the three KPI summary cards anchor above the form"
    - "The hook is the sole tRPC boundary; the dialog uses DialogBody/DialogFooter"
  artifacts:
    - path: "apps/web-vite/src/components/employee-time/hooks/use-employee-time.ts"
      provides: "sole tRPC boundary — trpc.employeeTime.upsertRecord/listRecords/weekSummary"
    - path: "apps/web-vite/src/components/employee-time/wt-limit-warning-banner.tsx"
      provides: "on-save non-blocking WT breach warning (S5 shared banner)"
  key_links:
    - from: "use-employee-time.ts"
      to: "trpc.employeeTime.upsertRecord"
      via: "distinct employee-time procedures (NOT contractor Time.*)"
      pattern: "employeeTime"
---

<objective>
Ship the S3 employee time entry + S5 WT-limit alert banner (TIME-EMP-01/02 staff side). A day-grain form (adapts `time/single-entry-form.tsx` shape — night/OT/weekend/on-call/absence fields) saving via the DISTINCT employee-time tRPC, with the on-save WT check surfaced as a non-blocking inline warning ("Save anyway", UI-A3) and three KPI summary cards as the anchor. Consumes Plan-12 i18n keys (no message-file edits).

**Binding UI stack:** `frontend-design` + `impeccable` + web-vite layering. Follow 92-UI-SPEC.md § S3/S5 exactly.

Purpose: TIME-EMP-01 record + TIME-EMP-02 on-save warning surface.
Output: hook + wired section + entry view + two banners + the /employee-time route.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-UI-SPEC.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
Adapt: apps/web-vite/src/components/time/single-entry-form.tsx:139 (Dialog + DialogBody/DialogFooter + per-field Label/error + local useState + validate + onSubmit prop) ; time/time-summary-stats.tsx (3 KPI cards)
tRPC (Plan 11): trpc.employeeTime.upsertRecord → {record, findings}, listRecords, weekSummary — DISTINCT from contractor trpc.time.*
i18n (Plan 12): EmployeeTime field labels + on-save warning + S5 banner keys
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Employee-time hook + wired section + KPI summary anchor</name>
  <files>apps/web-vite/src/components/employee-time/hooks/use-employee-time.ts, apps/web-vite/src/components/employee-time/employee-time-section.tsx, apps/web-vite/src/routes/employee-time.tsx</files>
  <read_first>
    - apps/web-vite/src/components/time/time-summary-stats.tsx (3 KPI cards analog)
    - apps/web-vite/src/components/approvals/hooks/use-approval-queue.ts (sole-boundary idiom)
    - 92-UI-SPEC.md § S3 (3 KPI cards anchor: hours this week / overtime this month / limit status) + § Interaction & State Contract
  </read_first>
  <action>Create `use-employee-time.ts` as the SOLE tRPC boundary: `trpc.employeeTime.weekSummary` + `listRecords` queries, `upsertRecord` mutation via `useResourceMutation` returning `{record, findings}` with invalidate + i18n messages — do NOT reuse the contractor `trpc.time.*` procedures. Create `employee-time-section.tsx` (wired section) owning loading (`SummaryCardSkeleton` ×3 + form skeleton), empty (`AtelierEmptyState variant="subview"` inside the grid card), error (inline `QueryErrorPanel`), and rendering the 3 KPI summary cards (hours this week / overtime this month / limit status) as the anchor above the day form. Thin `/employee-time` route (composer only). No tRPC in views.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite run check:web-vite-data-layer 2>&1 | grep -qiE "pass|ok|✓" && pnpm --filter @contractor-ops/web-vite run check:web-vite-page-shells 2>&1 | grep -qiE "pass|ok|✓" && echo TIME-LAYERING-OK</automated>
  </verify>
  <acceptance_criteria>
    - `use-employee-time.ts` is the only tRPC caller and uses `employeeTime.*` (never contractor `time.*`).
    - The wired section owns loading/empty/error + the 3 KPI anchor; the route is a thin composer.
    - `check:web-vite-data-layer` + `check:web-vite-page-shells` pass.
  </acceptance_criteria>
  <done>Employee-time hook + section + KPI anchor wired.</done>
</task>

<task type="auto">
  <name>Task 2: Day-entry form + on-save WT warning banner + S5 alert banner</name>
  <files>apps/web-vite/src/components/employee-time/employee-time-entry-view.tsx, apps/web-vite/src/components/employee-time/wt-limit-warning-banner.tsx, apps/web-vite/src/components/employee-time/wt-limit-alert-banner.tsx</files>
  <read_first>
    - apps/web-vite/src/components/time/single-entry-form.tsx:139 (Dialog + DialogBody/DialogFooter + field/label/error idiom; local state + validate + onSubmit prop)
    - 92-UI-SPEC.md § S3 copy (field labels Date/Hours worked/Night hours/Overtime/Weekend-holiday/On-call/Absence type/Notes) + § S5 (banner status-warning approaching / status-danger breach; aria-live) + UI-A3 (non-blocking "Save anyway")
  </read_first>
  <action>Build `employee-time-entry-view.tsx` (presentational; data via the hook): a day-grain form (Dialog → DialogBody space-y-4 → DialogFooter) with fields Date, Hours worked, Night hours, Overtime, Weekend/holiday, On-call (dyżur), Absence type, Notes; per-field Label + control + inline `text-destructive` error; local `useState` + `validate()` + `onSubmit` prop (data flows through the hook). Build `wt-limit-warning-banner.tsx` — the inline on-save non-blocking warning rendering `findings` from `upsertRecord` with a "Save anyway" affordance (UI-A3), status-warning (approaching) / status-danger (breach), icon + text (never color alone), `aria-live="polite"` (breach `assertive`). Build `wt-limit-alert-banner.tsx` (S5 shared) — the status-colored banner headline as the single focal element + "View time record" secondary CTA deep-linking to the time record. All copy via Plan-12 i18n keys; delete-draft confirm pre-lock only.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite run check:web-vite-dialog-pattern 2>&1 | grep -qiE "pass|ok|✓" && pnpm --filter @contractor-ops/web-vite run check:web-vite-presentational 2>&1 | grep -qiE "pass|ok|✓" && echo TIME-FORM-OK</automated>
  </verify>
  <acceptance_criteria>
    - The entry dialog uses `DialogBody`/`DialogFooter` (`check:web-vite-dialog-pattern` passes).
    - The WT warning is non-blocking (Save anyway), icon + text, `aria-live` announced.
    - Components are presentational (`check:web-vite-presentational` passes); all strings i18n-keyed.
  </acceptance_criteria>
  <done>Day-entry form + non-blocking WT banners ship per UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → tRPC | time saves only via the hook; server enforces org + RBAC + audit |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-14-01 | Denial of Service | WT breach hard-blocks a legitimate save | mitigate | Non-blocking warning + "Save anyway" (UI-A3); server never throws on breach |
| T-92-14-02 | Tampering | UI reuses contractor Time.* (wrong model) | mitigate | Distinct employeeTime.* procedures; check:web-vite-data-layer enforces the boundary |
| T-92-14-03 | Tampering | breach conveyed by color alone (a11y) | mitigate | icon + text + aria-live; non-color cue contract |
| T-92-14-SC | Tampering | dependency installs | mitigate | Zero new deps |
</threat_model>

<verification>
- `check:web-vite-data-layer`, `check:web-vite-page-shells`, `check:web-vite-dialog-pattern`, `check:web-vite-presentational` pass.
- WCAG: keyboard, focus, aria-live warnings, non-color cues.
</verification>

<success_criteria>
Day-grain employee time entry (distinct model) with a non-blocking on-save WT warning + KPI anchor + S5 alert banner, fully layered and i18n-keyed.
</success_criteria>

<output>
Create `.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-14-SUMMARY.md` when done.
</output>
