---
phase: 92-theme-b-leave-management-kp-grade-time-tracking
plan: 15
type: execute
wave: 7
depends_on: ["92-11", "92-12"]
files_modified:
  - apps/web-vite/src/components/employee-time/ewidencja/hooks/use-ewidencja.ts
  - apps/web-vite/src/components/employee-time/ewidencja/ewidencja-report-section.tsx
  - apps/web-vite/src/components/employee-time/ewidencja/ewidencja-report-view.tsx
  - apps/web-vite/src/components/employee-time/ewidencja/ewidencja-snapshot-table.tsx
  - apps/web-vite/src/components/employee-time/ewidencja/immutable-badge.tsx
  - apps/web-vite/src/components/employee-time/ewidencja/supersede-chain-row.tsx
autonomous: true
requirements: [TIME-EMP-03]

must_haves:
  truths:
    - "The ewidencja report generates + lists KP §149 snapshots over DataTable; the active snapshot shows an Archived/Lock immutable badge and superseded rows are dimmed"
    - "Regenerate confirms it creates a NEW immutable snapshot and supersedes the current one (the old record stays in the archive)"
    - "The hook is the sole tRPC boundary; the report is presentational"
  artifacts:
    - path: "apps/web-vite/src/components/employee-time/ewidencja/ewidencja-report-view.tsx"
      provides: "KP §149 report over DataTable with immutable + supersede-chain treatment"
    - path: "apps/web-vite/src/components/employee-time/ewidencja/immutable-badge.tsx"
      provides: "Archived/Lock badge with the 3-year immutable tooltip"
  key_links:
    - from: "use-ewidencja.ts"
      to: "trpc.ewidencja.generate/list/get"
      via: "sole tRPC boundary"
      pattern: "ewidencja"
---

<objective>
Ship the S4 ewidencja czasu pracy report (TIME-EMP-03 staff surface): generate/regenerate + list KP §149 snapshots over `DataTable`, with the active snapshot's Archived/Lock immutable badge as the active-vs-historical anchor, dimmed superseded rows, and a regenerate confirm making the append-only/supersede semantics explicit. Consumes Plan-12 i18n keys (no message-file edits).

**Binding UI stack:** `frontend-design` + `impeccable` + web-vite layering. Follow 92-UI-SPEC.md § S4 exactly.

Purpose: TIME-EMP-03 report + immutable-archive surface (criterion-4 staff side).
Output: hook + wired section + report view + snapshot table + immutable badge + supersede-chain row.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-UI-SPEC.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
tRPC (Plan 11): trpc.ewidencja.generate({workerId, periodStart, periodEnd}), trpc.ewidencja.list, trpc.ewidencja.get (version chain)
packages/ui: DataTable (renderSubRow for supersede chain), AtelierPageHeader, AtelierEmptyState, QueryErrorPanel, badge; icon Lock/FileText (lucide)
i18n (Plan 12): Ewidencja keys (Generate report/Regenerate/Export/Archived/Superseded/empty/error/regenerate-confirm)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ewidencja hook + wired section</name>
  <files>apps/web-vite/src/components/employee-time/ewidencja/hooks/use-ewidencja.ts, apps/web-vite/src/components/employee-time/ewidencja/ewidencja-report-section.tsx</files>
  <read_first>
    - apps/web-vite/src/components/approvals/hooks/use-approval-queue.ts (sole-boundary idiom + useResourceMutation)
    - 92-UI-SPEC.md § S4 + § Interaction & State Contract (DataTable skeleton + header skeleton; empty page icon FileText with primary "Generate report"; regenerate confirm)
  </read_first>
  <action>Create `use-ewidencja.ts` as the SOLE tRPC boundary: `trpc.ewidencja.list`/`get` queries keyed on worker + period; `generate` mutation via `useResourceMutation` with invalidate + i18n success/error; expose a `regenerate` path that surfaces the supersede confirm. Create `ewidencja-report-section.tsx` (wired section) owning loading (`DataTable` skeleton + header skeleton), empty (`AtelierEmptyState variant="page"`, icon FileText, primary "Generate report"), and error (`QueryErrorPanel`). No tRPC in views.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite run check:web-vite-data-layer 2>&1 | grep -qiE "pass|ok|✓" && echo EWIDENCJA-BOUNDARY-OK</automated>
  </verify>
  <acceptance_criteria>
    - `use-ewidencja.ts` is the only tRPC caller for this surface (`check:web-vite-data-layer` passes).
    - The wired section owns loading/empty/error with the "Generate report" empty-state primary.
  </acceptance_criteria>
  <done>Ewidencja hook + section wired with all three states.</done>
</task>

<task type="auto">
  <name>Task 2: Report view + snapshot table + immutable badge + supersede-chain row</name>
  <files>apps/web-vite/src/components/employee-time/ewidencja/ewidencja-report-view.tsx, apps/web-vite/src/components/employee-time/ewidencja/ewidencja-snapshot-table.tsx, apps/web-vite/src/components/employee-time/ewidencja/immutable-badge.tsx, apps/web-vite/src/components/employee-time/ewidencja/supersede-chain-row.tsx</files>
  <read_first>
    - 92-UI-SPEC.md § S4 copy (Archived badge + Lock tooltip "Immutable KP §149 record — cannot be edited (retained 3 years)"; superseded dimmed + "Superseded {date}"; regenerate confirm "Regenerating creates a new immutable snapshot and marks the current one as superseded…")
    - packages/ui DataTable (renderSubRow for expanding the supersede chain)
  </read_first>
  <action>Build `ewidencja-report-view.tsx` (presentational; data via the hook): the KP §149 snapshot over `EwidencjaSnapshotTable` (`DataTable`), the active row carrying an `ImmutableBadge` (Archived + Lock icon + the 3-year immutable tooltip) as the active-vs-historical anchor; superseded rows dimmed with a "Superseded {date}" chip and expandable via `renderSubRow` into `SupersedeChainRow` (the version chain). Regenerate opens a confirm dialog (DialogBody/DialogFooter) making the append-only supersede explicit ("the old record stays in the archive"); Export offers PDF + CSV. `ImmutableBadge` + `SupersedeChainRow` are presentational. Status via icon + text; tabular-nums for hours/dates; RTL logical properties. All copy via Plan-12 i18n keys.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite run check:web-vite-presentational 2>&1 | grep -qiE "pass|ok|✓" && pnpm --filter @contractor-ops/web-vite run check:web-vite-dialog-pattern 2>&1 | grep -qiE "pass|ok|✓" && echo EWIDENCJA-VIEW-OK</automated>
  </verify>
  <acceptance_criteria>
    - Active snapshot shows the Archived/Lock immutable badge + 3-year tooltip; superseded rows dimmed with a "Superseded {date}" chip + expandable chain.
    - Regenerate confirm (DialogBody/DialogFooter) states the new-snapshot-supersedes-prior semantics; no hard-delete affordance for archived snapshots.
    - Components are presentational (`check:web-vite-presentational` passes); all strings i18n-keyed.
  </acceptance_criteria>
  <done>Ewidencja report renders the immutable + supersede-chain treatment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → tRPC | generate/list only via the hook; server enforces immutability + audit |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-15-01 | Tampering | UI offers an "edit/delete archived snapshot" affordance | mitigate | No edit/hard-delete affordance; only Generate/Regenerate(→supersede)/Export; immutability enforced server + DB |
| T-92-15-02 | Repudiation | user regenerates without understanding supersede | mitigate | Explicit regenerate confirm stating the old record stays archived |
| T-92-15-03 | Information Disclosure | ewidencja of another org/worker rendered | accept | Server tenant-scopes reads (Plan 11); UI renders returned data |
| T-92-15-SC | Tampering | dependency installs | mitigate | Zero new deps |
</threat_model>

<verification>
- `check:web-vite-data-layer`, `check:web-vite-presentational`, `check:web-vite-dialog-pattern` pass.
- WCAG: keyboard, focus, non-color status (Archived = badge + Lock icon + text), RTL.
</verification>

<success_criteria>
The ewidencja report generates/regenerates + lists KP §149 snapshots with the immutable badge anchor + dimmed superseded chain + supersede-explicit confirm, fully layered and i18n-keyed.
</success_criteria>

<output>
Create `.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-15-SUMMARY.md` when done.
</output>
