---
phase: 92-theme-b-leave-management-kp-grade-time-tracking
plan: 06
type: execute
wave: 2
depends_on: ["92-02", "92-03", "92-04"]
files_modified:
  - packages/db/prisma/generated/**
autonomous: false
requirements: [LEAVE-01, LEAVE-02, LEAVE-03, TIME-EMP-01, TIME-EMP-03]

must_haves:
  truths:
    - "The generated Prisma client carries the new leave/employee-time/ewidencja/public-holiday models + LEAVE_REQUEST/EMPLOYEE_TIME_RECORD enum members (downstream typecheck resolves against the client, not the live DB)"
    - "The dev DB has the additive models pushed AND the ewidencja append-only trigger applied (immutability provable locally)"
    - "The live per-region (EU/ME) apply is authored and HELD at a blocking human gate (LOCAL-ONLY posture — recorded, not hard-blocking)"
  artifacts:
    - path: "packages/db/prisma/generated"
      provides: "regenerated client with all Phase-92 models + enums"
  key_links:
    - from: "prisma migrate/db push"
      to: "ewidencja append-only trigger migration"
      via: "the trigger ships as a raw-SQL migration, not db push alone"
      pattern: "ewidencja_append_only"
---

<objective>
[BLOCKING] Apply the Phase-92 schema to the dev DB and regenerate the Prisma client so every downstream service/router/test typechecks and the ewidencja immutability is provable locally. The additive models go via `prisma db push` (or `migrate dev`); the ewidencja append-only trigger + RLS MUST ship as the raw-SQL migration authored in Plan 04 (triggers live in migration.sql, not the schema DSL). The live per-region (EU/ME) production apply is authored and held at a blocking human gate per the P82–91 posture (LOCAL-ONLY; recorded post-merge, not hard-blocking).

This task is mandatory: build/typecheck pass without the push because types come from the generated client, not the live DB — skipping it creates a false-positive verification state.

**EXECUTION BLOCKED UNTIL PHASE 90 COMPLETES** — the leave/time models reference `Worker`/`EmployeeProfile`; do not apply before P90's EmployeeProfile schema is merged.

Purpose: Turn the schema (Plans 02/03/04) into a usable, immutability-enforced client + dev DB.
Output: Regenerated client + applied dev migration; authored un-applied per-region migration at a human gate.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-RESEARCH.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply additive schema to dev DB + ewidencja trigger migration + regenerate client</name>
  <files>packages/db/prisma/generated/**</files>
  <read_first>
    - packages/db/prisma/schema/migrations/20260701000000_ewidencja_append_only/migration.sql (the trigger to apply — Plan 04)
    - .planning/STATE.md § Blockers/Concerns (86-02 / 88-02 / 89-03 [BLOCKING] apply precedent: db push fallback when migrate dev is drift-blocked; per-region apply held at human gate)
    - packages/db/package.json (db:generate / db push scripts)
  </read_first>
  <action>Apply the additive Phase-92 models to the dev DB: run `prisma migrate dev` for the ewidencja append-only trigger (raw SQL requires a migration, not db push); if migrate dev is blocked by pre-existing migration-history drift (the P82–89 condition), fall back to `prisma db push` for the additive models AND apply the trigger migration SQL directly to the dev DB via `prisma db execute --file` so the trigger + RLS land. Then run `pnpm db:generate` so the client exposes `leaveLedgerEntry`, `leaveRequest`, `leaveType`, `leaveBalance`, `blackoutPeriod`, `employeeTimeRecord`, `ewidencjaSnapshot`, `publicHoliday` model delegates + the LEAVE_REQUEST/EMPLOYEE_TIME_RECORD enum members. Commit the regenerated client (tracked in-repo per the P85 convention). Do NOT touch live EU/ME/US DBs in this task.</action>
  <verify>
    <automated>pnpm db:generate 2>&1 | grep -qiE "Generated|generated" && pnpm typecheck --filter @contractor-ops/db 2>&1 | grep -qiE "error" && echo HAS-ERR || echo CLIENT-OK</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:generate` succeeds; the generated client exposes all 8 new model delegates + both new enum members.
    - The dev DB has the ewidencja `ewidencja_no_update` trigger applied (a manual `UPDATE "EwidencjaSnapshot"` in a psql/db-execute smoke raises `restrict_violation`).
    - `pnpm typecheck --filter @contractor-ops/db` passes against the regenerated client.
  </acceptance_criteria>
  <done>Client regenerated + dev DB carries additive models + ewidencja trigger.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Live per-region (EU/ME) migration apply — human gate</name>
  <files>packages/db/prisma/schema/migrations/20260701000000_ewidencja_append_only/migration.sql</files>
  <what-built>The additive Phase-92 models + the ewidencja append-only trigger migration are authored and applied to the dev DB + client. The live per-region EU/ME apply (which mutates production regional Postgres) is prepared but NOT executed.</what-built>
  <action>Human-gated live apply. Do NOT auto-run. Present the additive migration + the ewidencja trigger migration (+ down.sql) for review, then apply per region (EU then ME) only after P90's EmployeeProfile schema is confirmed merged in that region, running the ewidencja UPDATE-reject smoke after each apply. Per the LOCAL-ONLY posture, the operator MAY instead record this as a deferred post-merge migration_apply item in STATE.md `## Deferred Items` (like 89-03) — it must NOT hard-block local exec/CI.</action>
  <how-to-verify>
    1. Confirm P90 EmployeeProfile schema is merged + applied in the target region (leave/time FK targets exist).
    2. Review the additive migration + `20260701000000_ewidencja_append_only/migration.sql` (+ down.sql) for reversibility.
    3. Per region (EU then ME): apply the additive migration, then the ewidencja trigger migration; run a smoke `UPDATE "EwidencjaSnapshot"` and confirm it raises `restrict_violation`.
    4. Per LOCAL-ONLY posture this apply MAY be deferred as a recorded post-merge item (like 89-03) — it must NOT hard-block local exec/CI.
  </how-to-verify>
  <read_first>
    - .planning/STATE.md § Blockers/Concerns (89-03 per-region apply sequence precedent)
    - packages/db/prisma/schema/migrations/20260701000000_ewidencja_append_only/down.sql (rollback path)
  </read_first>
  <verify>
    <human-check>EU+ME apply completed with per-region ewidencja UPDATE-reject smoke passing, OR the apply recorded as a deferred post-merge item in STATE.md.</human-check>
  </verify>
  <acceptance_criteria>
    - Either: EU+ME apply completed and the ewidencja UPDATE-reject smoke passes per region; OR the apply is recorded in STATE.md `## Deferred Items` as a post-merge migration_apply item (LOCAL-ONLY), not hard-blocking.
  </acceptance_criteria>
  <done>Live per-region apply resolved — applied (with per-region smoke) OR recorded as a deferred post-merge item.</done>
  <resume-signal>Type "applied" (with region confirmations) or "deferred" (recorded as post-merge item) to resume.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| migration → live regional Postgres | schema apply mutates every region's DB; must be additive + reversible |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-06-01 | Tampering | ewidencja shipped without the trigger (db push alone) → mutable evidentiary record | mitigate | Trigger ships as a raw-SQL migration + a smoke UPDATE-reject check per apply |
| T-92-06-02 | Denial of Service | destructive/irreversible migration on live regions | mitigate | Additive-only + paired down.sql; blocking human gate before any live apply |
| T-92-06-03 | Repudiation | false-positive verification (client types pass without a push) | mitigate | Mandatory db:generate + dev-DB apply before downstream verification |
| T-92-06-SC | Tampering | dependency installs | mitigate | Zero new deps |
</threat_model>

<verification>
- `pnpm db:generate` + `pnpm typecheck --filter @contractor-ops/db` pass; dev-DB ewidencja UPDATE raises restrict_violation.
- Live per-region apply completed OR recorded as a deferred post-merge item.
</verification>

<success_criteria>
Client regenerated with all Phase-92 models/enums; dev DB carries the immutability trigger; live per-region apply resolved (applied or deferred at the human gate).
</success_criteria>

<output>
Create `.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-06-SUMMARY.md` (record apply-vs-deferred status per region).
</output>
