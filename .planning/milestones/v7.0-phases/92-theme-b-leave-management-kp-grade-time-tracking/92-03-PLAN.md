---
phase: 92-theme-b-leave-management-kp-grade-time-tracking
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema/approval.prisma
  - packages/db/prisma/schema/contract.prisma
  - packages/validators/src/approval.ts
  - packages/validators/src/leave.ts
  - packages/validators/src/employee-time.ts
  - packages/validators/src/index.ts
  - packages/api/src/services/notification-service.ts
autonomous: true
requirements: [LEAVE-02, TIME-EMP-01, TIME-EMP-02]

must_haves:
  truths:
    - "LEAVE_REQUEST exists in all four enum surfaces (ApprovalResourceType, EntityType, approvalResourceTypeEnum, ENTITY_ROUTES) so a leave flow routes, audits, and produces a CTA"
    - "EMPLOYEE_TIME_RECORD exists in EntityType + ENTITY_ROUTES so a WT-limit alert resolves an entity CTA"
    - "Leave + employee-time Zod input schemas exist in packages/validators with no Prisma dependency"
  artifacts:
    - path: "packages/validators/src/leave.ts"
      provides: "submitLeaveRequest / recordSickAbsence / leaveType / blackout Zod inputs + leaveKind/status enum mirrors"
    - path: "packages/validators/src/employee-time.ts"
      provides: "employee time-record Zod input (day-grain, minutes bounds, absenceKind mirror)"
  key_links:
    - from: "packages/api/src/services/notification-service.ts"
      to: "ENTITY_ROUTES"
      via: "LEAVE_REQUEST → /leave, EMPLOYEE_TIME_RECORD → /employee-time"
      pattern: "ENTITY_ROUTES"
---

<objective>
Perform the Pitfall-1 four-place enum extension atomically and add the leave + employee-time validator surface. `LEAVE_REQUEST` must land in `ApprovalResourceType` (used by `ApprovalChainConfig.resourceType`), `EntityType` (used by `ApprovalFlow.resourceType`, `AuditLog.resourceType`, `NotificationEvent.entityType`), `approvalResourceTypeEnum` (validators), and `ENTITY_ROUTES` (CTA route). `EMPLOYEE_TIME_RECORD` lands in `EntityType` + `ENTITY_ROUTES` for time-alert entity refs (P89 backfill had to fall back to ORGANIZATION because EntityType has no worker member — this closes that for time alerts).

Purpose: The enum + validator groundwork every leave/time router and notification depends on. Miss one surface → runtime enum error or a 404 CTA.
Output: 2 Prisma enum edits + 3 validator files + 1 ENTITY_ROUTES edit.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-PATTERNS.md

<interfaces>
From packages/db/prisma/schema/approval.prisma:106 — enum ApprovalResourceType { INVOICE DOCUMENT CONTRACT }
From packages/db/prisma/schema/contract.prisma:280 — enum EntityType { INVOICE CONTRACT DOCUMENT ORGANIZATION ... }
From packages/validators/src/approval.ts:23 — export const approvalResourceTypeEnum = z.enum(['INVOICE','DOCUMENT','CONTRACT'])
From packages/api/src/services/notification-service.ts:52 — const ENTITY_ROUTES: Record<string,string> = { INVOICE:'/invoices', CONTRACT:'/contracts', DOCUMENT:'/documents', ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Four-place LEAVE_REQUEST + EMPLOYEE_TIME_RECORD enum extension</name>
  <files>packages/db/prisma/schema/approval.prisma, packages/db/prisma/schema/contract.prisma, packages/validators/src/approval.ts, packages/api/src/services/notification-service.ts</files>
  <read_first>
    - packages/db/prisma/schema/approval.prisma:106 (ApprovalResourceType — add LEAVE_REQUEST)
    - packages/db/prisma/schema/contract.prisma:280 (EntityType — add LEAVE_REQUEST + EMPLOYEE_TIME_RECORD)
    - packages/validators/src/approval.ts:23 (approvalResourceTypeEnum — add 'LEAVE_REQUEST')
    - packages/api/src/services/notification-service.ts:52 (ENTITY_ROUTES — add both routes) + :64 (buildEntityUrl consumer)
    - 92-PATTERNS.md § Two-enum extension for LEAVE_REQUEST (Pitfall 1 — all four)
  </read_first>
  <action>Add `LEAVE_REQUEST` to `ApprovalResourceType` (approval.prisma:106) and to `EntityType` (contract.prisma:280); add `EMPLOYEE_TIME_RECORD` to `EntityType` as well. Add `'LEAVE_REQUEST'` to `approvalResourceTypeEnum` in validators/src/approval.ts:23. Add `LEAVE_REQUEST: '/leave'` and `EMPLOYEE_TIME_RECORD: '/employee-time'` to `ENTITY_ROUTES` (notification-service.ts:52) so `buildEntityUrl` emits a CTA. All enum values UPPER_SNAKE. Do not touch the Prisma migration/apply here — that is Plan 06. No breadcrumb comments.</action>
  <verify>
    <automated>grep -q "LEAVE_REQUEST" packages/db/prisma/schema/approval.prisma && grep -q "LEAVE_REQUEST" packages/db/prisma/schema/contract.prisma && grep -q "EMPLOYEE_TIME_RECORD" packages/db/prisma/schema/contract.prisma && grep -q "LEAVE_REQUEST" packages/validators/src/approval.ts && grep -q "'/employee-time'" packages/api/src/services/notification-service.ts && echo FOUR-PLACE-OK</automated>
  </verify>
  <acceptance_criteria>
    - `LEAVE_REQUEST` present in ApprovalResourceType, EntityType, approvalResourceTypeEnum, and ENTITY_ROUTES (all four surfaces).
    - `EMPLOYEE_TIME_RECORD` present in EntityType and ENTITY_ROUTES.
    - `prisma validate` passes; `pnpm db:audit-enum-casing` clean.
    - `pnpm typecheck --filter @contractor-ops/validators` passes.
  </acceptance_criteria>
  <done>All four LEAVE_REQUEST surfaces + both EMPLOYEE_TIME_RECORD surfaces carry the new members.</done>
</task>

<task type="auto">
  <name>Task 2: Leave + employee-time Zod validators</name>
  <files>packages/validators/src/leave.ts, packages/validators/src/employee-time.ts, packages/validators/src/index.ts</files>
  <read_first>
    - packages/validators/src/approval.ts (enum-mirror + zod schema convention; no Prisma import)
    - packages/validators/src/index.ts (barrel export convention)
    - 92-RESEARCH.md § Pattern 4 (leave ledger fields) + § Pattern 3 (employee-time fields for the input bounds)
  </read_first>
  <action>Create `leave.ts`: string-union enum mirrors `leaveKindEnum`, `leaveRequestStatusEnum`, `leaveLedgerTypeEnum` (mirror the Prisma enums, no Prisma dep); Zod inputs `submitLeaveRequestInput` (workerId, leaveTypeId, startDate/endDate ISO, requestedMinutes positive int, optional teamId, optional note), `recordSickAbsenceInput` (workerId, startDate/endDate, minutes, optional note), `leaveTypeUpsertInput`, `blackoutPeriodUpsertInput` (start<=end refine), `leaveAdjustmentInput` (signed minutes + mandatory reason min length for audit). Create `employee-time.ts`: `absenceKindEnum` mirror + `upsertEmployeeTimeRecordInput` (workerId, workDate ISO, workedMinutes/nightMinutes/overtimeMinutes50/overtimeMinutes100/weekendHolidayMinutes/onCallMinutes as non-negative ints, optional onCallLocation, optional absenceKind, wtOptOut boolean, source mirror). Export both from `index.ts`. `.strict()` on object schemas; bound minutes to a sane daily ceiling. Keep the packages/validators no-Prisma-dependency invariant.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/validators test 2>&1 | grep -qiE "pass|✓" ; pnpm typecheck --filter @contractor-ops/validators 2>&1 | grep -qiE "error" && echo HAS-ERR || echo VALIDATORS-OK</automated>
  </verify>
  <acceptance_criteria>
    - `leave.ts` + `employee-time.ts` export the named schemas + enum mirrors; both re-exported from `index.ts`.
    - No `@contractor-ops/db`/`@prisma` import in either file (no-Prisma-dep invariant holds).
    - Object schemas use `.strict()`; `leaveAdjustmentInput` requires a non-empty reason.
    - `pnpm typecheck --filter @contractor-ops/validators` passes.
  </acceptance_criteria>
  <done>Leave + employee-time validators exist, typed, exported, Prisma-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → tRPC procedure | leave/time inputs cross here; Zod validates before any DB write |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-03-01 | Tampering | LEAVE_REQUEST added to only one enum → runtime enum error / 404 CTA | mitigate | Four-place edit in one task; verify greps all four surfaces |
| T-92-03-02 | Elevation of Privilege | unvalidated leave/time payload (negative minutes, mass-assign status) | mitigate | `.strict()` Zod inputs; status never client-supplied on submit; requestedMinutes positive-int bound |
| T-92-03-03 | Repudiation | ledger ADJUSTMENT without a reason | mitigate | `leaveAdjustmentInput` requires a non-empty reason (audited downstream) |
| T-92-03-SC | Tampering | dependency installs | mitigate | Zero new deps |
</threat_model>

<verification>
- Four-place grep passes; `prisma validate` + `db:audit-enum-casing` clean.
- `pnpm typecheck --filter @contractor-ops/validators` passes.
</verification>

<success_criteria>
LEAVE_REQUEST + EMPLOYEE_TIME_RECORD land atomically across all enum/route surfaces; leave + employee-time Zod validators exist Prisma-free with strict, audited inputs.
</success_criteria>

<output>
Create `.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-03-SUMMARY.md` when done.
</output>
