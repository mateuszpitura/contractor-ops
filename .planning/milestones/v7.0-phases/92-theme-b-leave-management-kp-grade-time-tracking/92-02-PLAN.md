---
phase: 92-theme-b-leave-management-kp-grade-time-tracking
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema/leave.prisma
  - packages/db/prisma/schema/employee-time.prisma
  - packages/db/prisma/schema/reference.prisma
  - packages/db/src/tenant.ts
autonomous: true
requirements: [LEAVE-01, LEAVE-02, LEAVE-03, TIME-EMP-01]

must_haves:
  truths:
    - "The leave-balance ledger, leave-request, leave-type, blackout, employee-time, and public-holiday models exist as tenant-owning Prisma models (except PublicHoliday which is global)"
    - "LeaveLedgerEntry is app-layer append-only; every new tenant model inherits withTenantScope (absent from globalModels)"
    - "EmployeeTimeRecord is distinct from the contractor TimeEntry (keyed on workerId, no contractorId/contractId)"
  artifacts:
    - path: "packages/db/prisma/schema/leave.prisma"
      provides: "LeaveType, LeaveRequest, LeaveLedgerEntry, LeaveBalance, BlackoutPeriod (+ enums)"
      contains: "model LeaveLedgerEntry"
    - path: "packages/db/prisma/schema/employee-time.prisma"
      provides: "EmployeeTimeRecord (+ EmployeeTimeSource, AbsenceKind)"
      contains: "model EmployeeTimeRecord"
    - path: "packages/db/prisma/schema/reference.prisma"
      provides: "PublicHoliday global reference (no organizationId)"
      contains: "model PublicHoliday"
  key_links:
    - from: "packages/db/src/tenant.ts"
      to: "LeaveLedgerEntry"
      via: "APPEND_ONLY_MODELS set"
      pattern: "APPEND_ONLY_MODELS"
    - from: "packages/db/src/tenant.ts"
      to: "PublicHoliday"
      via: "globalModels set (only global addition)"
      pattern: "globalModels"
---

<objective>
Add the leave-domain + employee-statutory-time + public-holiday Prisma models and wire tenancy: the append-only leave ledger, the org-config leave types + blackout periods, the leave request, the balance cache, the distinct `EmployeeTimeRecord` (D-04 collision-forced new model), and the global `PublicHoliday` reference table. Register `LeaveLedgerEntry` as app-layer append-only and `PublicHoliday` as the ONLY new global model.

**EXECUTION BLOCKED UNTIL PHASE 90 COMPLETES** — these models reference `Worker(EMPLOYEE)` and are consumed alongside P90 `EmployeeProfile.etat`. Schema files may be authored now; the [BLOCKING] push (Plan 06) runs after P90's EmployeeProfile schema is merged.

Purpose: The record-of-record layer for LEAVE-01/02/03 + TIME-EMP-01. No app logic here — pure schema DSL.
Output: 3 new schema files + 2 narrow tenant.ts edits.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-RESEARCH.md
@.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-PATTERNS.md

<interfaces>
From packages/db/prisma/schema/employee.prisma (P90 — the attach point):
- model EmployeeProfile { workerId String @unique; etat Decimal? @db.Decimal(3,2); employmentStatus EmploymentStatus? }
- enum EmploymentStatus { ACTIVE ON_LEAVE ... }  // may gain no new member this phase

From packages/db/prisma/schema/worker.prisma:17 (tenant-owning header to copy):
- model Worker { id String @id @default(cuid()); organizationId String; ... organization Organization @relation(...); @@index([organizationId]) }

From packages/db/prisma/schema/organization.prisma:212 (existing Team — reuse; do NOT invent a new team model):
- model Team { id String @id; organizationId String; managerUserId String?; ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Leave-domain schema (types, request, ledger, balance cache, blackout)</name>
  <files>packages/db/prisma/schema/leave.prisma</files>
  <read_first>
    - packages/db/prisma/schema/worker.prisma:17 (tenant-owning header + @@index([organizationId]) convention)
    - packages/db/prisma/schema/approval.prisma:3 (ApprovalChainConfig — org-config shape for LeaveType/BlackoutPeriod)
    - packages/db/prisma/schema/time-tracking.prisma:49 (@@unique / @@index convention)
    - 92-RESEARCH.md § Pattern 4 (LeaveLedgerEntry exact shape: signed minutes, entryType LeaveLedgerType, effectiveDate @db.Date, sourceRef, reason, createdByUserId)
    - packages/db/prisma/schema/organization.prisma:212 (Team — LeaveRequest.teamId FK target)
  </read_first>
  <action>Create `leave.prisma` with five tenant-owning models, each carrying `organizationId` + `organization Organization @relation` + `@@index([organizationId, …])`: (1) `LeaveType` org-config (name, code, `kind LeaveKind`, `requiresApproval Boolean`, `paid Boolean`, active flag); (2) `BlackoutPeriod` org-config (`startDate`/`endDate @db.Date`, optional `teamId`, reason); (3) `LeaveRequest` (`workerId`, `leaveTypeId`, `startDate`/`endDate @db.Date`, `requestedMinutes Int`, `status LeaveRequestStatus` default PENDING, `teamId String?` → Team for same-team calendar grouping per OQ4, `approvalFlowId String?`); (4) `LeaveLedgerEntry` per 92-RESEARCH Pattern 4 (signed `minutes Int`, `entryType LeaveLedgerType` ACCRUAL|DEDUCTION|CARRYOVER|ADJUSTMENT, `leaveTypeId`, `effectiveDate @db.Date`, `sourceRef String?`, `reason String?`, `createdByUserId String?`, `@@index([organizationId, workerId, leaveTypeId, effectiveDate])`); (5) `LeaveBalance` cache (`@@unique([organizationId, workerId, leaveTypeId, year])`, `entitledMinutes`, `usedMinutes`, `carryoverMinutes`, `recomputedAt`). Add enums `LeaveKind`, `LeaveRequestStatus` (PENDING/APPROVED/REJECTED/CANCELLED), `LeaveLedgerType` — all UPPER_SNAKE per db:audit-enum-casing. `LeaveRequest.teamId` reuses the existing `Team` model (organization.prisma:212); do NOT create a new team model. Record in the summary the OQ4 decision: same-team grouping keys on `LeaveRequest.teamId` (existing Team), null → "Unassigned" bucket, since EmployeeProfile has no direct teamId. No breadcrumb comments (no phase/D-ID); domain IDs only.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db exec prisma validate 2>&1 | grep -qiE "valid|Prisma schema loaded" && pnpm db:audit-enum-casing 2>&1 | grep -qiE "pass|ok|0 " && echo SCHEMA-OK</automated>
  </verify>
  <acceptance_criteria>
    - `prisma validate` passes with all five models present.
    - Every leave model has `organizationId` + `@@index([organizationId, …])`; none is a global model.
    - All new enum values are UPPER_SNAKE_CASE (`pnpm db:audit-enum-casing` clean).
    - `LeaveRequest.teamId` references the existing `Team`; no new team-like model introduced.
  </acceptance_criteria>
  <done>leave.prisma validates with LeaveType/BlackoutPeriod/LeaveRequest/LeaveLedgerEntry/LeaveBalance + enums.</done>
</task>

<task type="auto">
  <name>Task 2: EmployeeTimeRecord + PublicHoliday schema + tenant.ts tenancy wiring</name>
  <files>packages/db/prisma/schema/employee-time.prisma, packages/db/prisma/schema/reference.prisma, packages/db/src/tenant.ts</files>
  <read_first>
    - packages/db/prisma/schema/time-tracking.prisma:29 (TimeEntry — copy shape, DROP timesheetId/contractorId/contractId; :63 TimeEntrySource name is TAKEN)
    - 92-RESEARCH.md § Pattern 3 (EmployeeTimeRecord full column list: workedMinutes, nightMinutes, overtimeMinutes50/100, weekendHolidayMinutes, onCallMinutes/onCallLocation, absenceKind, wtOptOut, source)
    - packages/db/src/tenant.ts:34 (APPEND_ONLY_MODELS Set — currently {'ClassificationDocument'}), :42 (globalModels — ExchangeRate/BoEBaseRateHistory precedent), :117 (applyTenantScope enforcement)
  </read_first>
  <action>Create `employee-time.prisma` `EmployeeTimeRecord` daily-grain model on `workerId` (NOT contractorId) per 92-RESEARCH Pattern 3, with `@@unique([organizationId, workerId, workDate])` + `@@index([organizationId, workerId, workDate])`; enums `EmployeeTimeSource { MANUAL IMPORTED }` (NOT `TimeEntrySource`) and `AbsenceKind { VACATION SICK PARENTAL BEREAVEMENT STUDY UNPAID OTHER_JUSTIFIED UNJUSTIFIED }`. Create `reference.prisma` `PublicHoliday` global reference — NO `organizationId` (like ExchangeRate): `countryCode`, `holidayDate @db.Date`, `name`, optional `region` (DE Bundesland), `@@unique([countryCode, holidayDate, region])`. Edit `tenant.ts`: add `'LeaveLedgerEntry'` to `APPEND_ONLY_MODELS` (blocks update/updateMany/upsert at the app layer — the ClassificationDocument precedent); add ONLY `'PublicHoliday'` to `globalModels`. Every other new leave/time/ewidencja model MUST stay OUT of globalModels so it inherits `withTenantScope`.</action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db exec prisma validate 2>&1 | grep -qiE "valid|schema loaded" && grep -q "LeaveLedgerEntry" packages/db/src/tenant.ts && grep -q "PublicHoliday" packages/db/src/tenant.ts && echo TENANCY-OK</automated>
  </verify>
  <acceptance_criteria>
    - `EmployeeTimeRecord` has `workerId`, no `contractorId`/`contractId`; uses `EmployeeTimeSource` (not `TimeEntrySource`).
    - `PublicHoliday` has no `organizationId` and IS added to `globalModels`.
    - `LeaveLedgerEntry` is in `APPEND_ONLY_MODELS`; no leave/time/ewidencja tenant model appears in `globalModels`.
    - `prisma validate` passes; `pnpm db:audit-enum-casing` clean.
  </acceptance_criteria>
  <done>employee-time + reference schema validate; tenant.ts append-only + global sets updated narrowly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| any tenant → Prisma extension | tenant-owning rows must auto-scope by organizationId; global rows must not |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-02-01 | Information Disclosure | new leave/time/ewidencja models added to globalModels by mistake | mitigate | Only PublicHoliday added; verify greps assert it; cross-org leak test (Plan 07/01) is the runtime proof |
| T-92-02-02 | Tampering/Repudiation | ledger row updated to inflate balance | mitigate | LeaveLedgerEntry in APPEND_ONLY_MODELS (blocks update/updateMany/upsert); corrections = reversing ADJUSTMENT inserts |
| T-92-02-03 | Tampering | PublicHoliday given an organizationId and treated as tenant data | accept | Global reference by design (local-only seeded); no PII, no tenant coupling |
| T-92-02-SC | Tampering | dependency installs | mitigate | Zero new deps (schema-only) |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/db exec prisma validate` passes.
- `pnpm db:audit-enum-casing` clean for all new enums.
- `grep APPEND_ONLY_MODELS packages/db/src/tenant.ts` shows LeaveLedgerEntry; `globalModels` shows only PublicHoliday added.
- NOTE: no `db push` in this plan — the generated-client + live apply is the [BLOCKING] Plan 06.
</verification>

<success_criteria>
Three schema files validate; tenancy wired so LeaveLedgerEntry is append-only and PublicHoliday is the only new global; every other new model inherits withTenantScope.
</success_criteria>

<output>
Create `.planning/phases/92-theme-b-leave-management-kp-grade-time-tracking/92-02-SUMMARY.md` (record the OQ4 same-team-grouping decision).
</output>
