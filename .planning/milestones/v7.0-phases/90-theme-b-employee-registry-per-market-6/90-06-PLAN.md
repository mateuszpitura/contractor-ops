---
phase: 90-theme-b-employee-registry-per-market-6
plan: 06
type: execute
wave: 5
depends_on:
  - 90-05
files_modified:
  - apps/web-vite/src/components/employees/compliance/hooks/use-employee-compliance.ts
  - apps/web-vite/src/components/employees/compliance/hooks/use-reveal-employee-pii.ts
  - apps/web-vite/src/components/employees/compliance/employee-compliance-section.tsx
  - apps/web-vite/src/components/employees/compliance/employee-pii-masked-reveal.tsx
  - apps/web-vite/src/components/employees/compliance/reference-list-picker.tsx
  - apps/web-vite/src/components/employees/compliance/pl-employee-fields.tsx
  - apps/web-vite/src/components/employees/compliance/de-employee-fields.tsx
  - apps/web-vite/src/components/employees/compliance/uk-employee-fields.tsx
  - apps/web-vite/src/components/employees/compliance/us-employee-fields.tsx
  - apps/web-vite/src/components/employees/compliance/ae-employee-fields.tsx
  - apps/web-vite/src/components/employees/compliance/sa-employee-fields.tsx
  - apps/web-vite/src/components/employees/employee-registration-page.tsx
  - apps/web-vite/src/i18n/locales/en/employees.json
  - apps/web-vite/src/i18n/locales/de/employees.json
  - apps/web-vite/src/i18n/locales/pl/employees.json
  - apps/web-vite/src/i18n/locales/ar/employees.json
autonomous: false
requirements:
  - EMP-REG-PL-01
  - EMP-REG-DE-01
  - EMP-REG-UK-01
  - EMP-REG-US-01
  - EMP-REG-AE-01
  - EMP-REG-SA-01

must_haves:
  truths:
    - "The per-market registration form dispatches by countryCode (PL/DE/UK/US/AE/SA) through EmployeeFieldsDispatch; the page is a thin composer gated by useFlag('module.workforce-employees') with render-tree removal when OFF (D-06, D-07)"
    - "Data flows page → wired section (loading/empty/error) → hook (SOLE tRPC boundary) → presentational; NO new *-container.tsx; check:web-vite-data-layer passes (D-06)"
    - "Encrypted PII uses EmployeePiiMaskedReveal (last-4 mono, gated reveal ABSENT without employeePii:read, audit-logged, never cached); Emirates-ID surfaces an amber advisory pill that never blocks save; three-class feedback (hard red / advisory amber / adviser-verify note); i18n parity en/en-US/de/pl/ar (D-04, D-06, Emirates-ID advisory)"
  artifacts:
    - path: "apps/web-vite/src/components/employees/compliance/hooks/use-employee-compliance.ts"
      provides: "the SOLE tRPC boundary for the employee compliance surface"
      min_lines: 25
    - path: "apps/web-vite/src/components/employees/compliance/employee-compliance-section.tsx"
      provides: "EmployeeFieldsDispatch + wired section (loading/empty/error)"
      min_lines: 60
    - path: "apps/web-vite/src/components/employees/employee-registration-page.tsx"
      provides: "thin flag-gated composer page"
      min_lines: 20
  key_links:
    - from: "apps/web-vite/src/components/employees/employee-registration-page.tsx"
      to: "module.workforce-employees"
      via: "useFlag render-tree removal"
      pattern: "useFlag\\('module.workforce-employees'\\)"
    - from: "apps/web-vite/src/components/employees/compliance/hooks/use-reveal-employee-pii.ts"
      to: "trpc.employee.revealPii"
      via: "reveal mutation held in local state, never cached"
      pattern: "revealPii"
---

<objective>
[HOLD until P89] Build the per-market employee registration UI: a thin flag-gated page, a wired compliance section that dispatches by countryCode, the two data hooks (the sole tRPC boundary), the masked-PII reveal, the reference-list picker, the six market field components, and full i18n parity.

Purpose: The HR-facing surface that registers an employee with validated statutory fields + masked PII. P89-GATED: it calls `trpc.employee.*` (Plan 05) and gates on `module.workforce-employees` (P89). EXECUTION HOLDS until P89 + Plan 05 land. UI work follows the APPROVED 90-UI-SPEC contract.
Output: hooks + wired section + masked-reveal + reference picker + 6 field components + page + 4 locale files.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/90-theme-b-employee-registry-per-market-6/90-UI-SPEC.md
@.planning/phases/90-theme-b-employee-registry-per-market-6/90-PATTERNS.md
@apps/web-vite/ARCHITECTURE.md

<p89_dependency>
This surface calls `trpc.employee.register` / `trpc.employee.revealPii` / `trpc.employee.listReferenceLists` (Plan 05) and gates on `module.workforce-employees` (P89). If P89's flag plumbing / `employeeRouter` or Plan 05's procedures are absent → STOP and report "Phase 90 Plan 06 HELD: trpc.employee.* procedures or module.workforce-employees not present". Do NOT add a new `*-container.tsx` — the contractor `-container` survivors are grandfathered; the employee surface follows the current layering (page → wired section → hook → presentational), enforced by check:web-vite-data-layer.
</p89_dependency>

<interfaces>
<!-- Analog files to mirror — extracted this session. -->

From apps/web-vite/src/components/contractors/country-compliance-section.tsx:
  CountryFieldsDispatch switch(countryCode) → per-market component, default: return null   (:246-288)
  Card/CardHeader/CardTitle(text-base font-semibold) + amber incomplete Badge(border-warning/20 bg-warning/5 text-warning) (:139-187)
  CountryComplianceLoadingCard (:74-82) — Loader2 animate-spin centered
  UaeFields(:290) / SaudiFields(:331) — the inline per-market field idiom (useId, space-y-2, Label text-sm font-medium, Input)

From apps/web-vite/src/components/contractors/compliance/ssn-masked-reveal.tsx (MIRROR):
  mono •••-••-{last4} span role="img" aria-label; reveal Button ABSENT (not disabled) without permission;
  aria-pressed={isRevealed}; Eye/EyeOff/Loader2; error <p role="alert" aria-live="polite" text-xs text-destructive>

From apps/web-vite/src/components/contractors/compliance/hooks/use-reveal-ssn.ts (MIRROR):
  useState revealed value (NEVER query cache); useMutation(trpc.x.reveal.mutationOptions({onSuccess: r=>setRevealed(r.id)}));
  returns { reveal, revealedX, isPending, isError, error, reset }

UI-SPEC Component Inventory: EntityTypeSelect<T> for enums (Lohnsteuerklasse/W-4/student-loan/visa/NitaqatBand/10-state);
  reui/combobox for ReferenceListPicker; Badge variant="warning" for the Emirates-ID advisory; AnimateIn entrance.
</interfaces>

<three_class_feedback>
<!-- UI-SPEC load-bearing semantic mapping. -->
HARD error (bad PESEL/NI/SteuerIdNr) → text-destructive inline FieldError, role="alert" aria-live="polite", surfaces only after a complete-looking value (never mid-keystroke).
ADVISORY (Emirates-ID Luhn fails, GOSI/WPS lenient) → amber Badge variant="warning" / amber box; copy "could not verify — save allowed"; NEVER blocks Save.
ADVISER-VERIFY (seeded reference lists, statutory rules) → muted dashed info box (border-dashed bg-muted/30 text-muted-foreground text-xs).
</three_class_feedback>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data hooks + wired section + masked-reveal + reference picker (layering-first)</name>
  <read_first>
    - apps/web-vite/ARCHITECTURE.md (the page → wired section → hook → presentational layering + the check:web-vite-data-layer rule; NO new -container)
    - apps/web-vite/src/components/contractors/compliance/hooks/use-reveal-ssn.ts (the reveal-hook idiom to mirror verbatim)
    - apps/web-vite/src/components/contractors/compliance/hooks/use-country-compliance.ts (the section read/save hook idiom)
    - apps/web-vite/src/components/contractors/country-compliance-section.tsx:74-288 (Card shell + loading + dispatch switch)
    - apps/web-vite/src/components/contractors/compliance/ssn-masked-reveal.tsx (the masked-reveal idiom to mirror)
    - 90-UI-SPEC.md State Coverage + Accessibility Contract + Component Inventory
  </read_first>
  <action>
    Confirm P89 + Plan 05 landed (see <p89_dependency>); HOLD if not. Build the data layer FIRST (the layering guard demands hook + wired section before presentational). Create `hooks/use-employee-compliance.ts` — the SOLE tRPC boundary for the section: wraps `trpc.employee.register` / config / `trpc.employee.listReferenceLists` queries + the save mutation; returns typed fields/config/save/loading/empty/error. Create `hooks/use-reveal-employee-pii.ts` mirroring `use-reveal-ssn.ts`: `useMutation(trpc.employee.revealPii.mutationOptions({ onSuccess }))`, revealed value in `useState` (NEVER cache, Pitfall 4), `reveal`/`reset`, parameterized by `field`. Create `employee-compliance-section.tsx` with `EmployeeFieldsDispatch` (switch countryCode PL/DE/UK/US/AE/SA → the six field components, `default: return null`) inside the Card shell (CardTitle text-base font-semibold, amber incomplete Badge, `EmployeeComplianceLoadingCard` from the Loader2 analog); the wired section branches loading/empty/error and reads ONLY from the hook — NO direct `useTRPC`/`useQuery`/`useMutation` in the section. Create `employee-pii-masked-reveal.tsx` mirroring `ssn-masked-reveal.tsx`, parameterized by `field` (PESEL/SSN/Iqama/Emirates-ID → mask shape + i18n namespace), `canReveal = can('employeePii', ['read'])` (button ABSENT when false), `aria-pressed`, value from the reveal hook (local state). Create `reference-list-picker.tsx` wrapping `reui/combobox` for ZUS/NFZ/urząd/Krankenkasse (keyboard-navigable, announces results). All copy = i18n keys; logical properties (me-*/ms-*) for RTL. Run `pnpm check:web-vite-data-layer`.
  </action>
  <verify>
    <automated>pnpm check:web-vite-data-layer 2>&1 | tail -5 && test -z "$(find apps/web-vite/src/components/employees -name '*-container.tsx')" && echo "no-container-ok"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check:web-vite-data-layer` passes: the section has NO direct tRPC; the two hooks are the only `useTRPC`/`useQuery`/`useMutation` sites for this surface.
    - NO `*-container.tsx` file exists under `apps/web-vite/src/components/employees` (the find returns empty → `no-container-ok`).
    - `EmployeeFieldsDispatch` switches all six market codes with `default: return null`.
    - `EmployeePiiMaskedReveal`: reveal button ABSENT (not disabled) without `employeePii:read`; revealed value held in local state (no query cache); `aria-pressed` + mono last-4 span with `role="img"`.
    - `pnpm typecheck --filter=@contractor-ops/web-vite` clean; no hardcoded user-facing strings (i18n keys only).
  </acceptance_criteria>
  <done>Data hooks (sole tRPC boundary) + wired dispatch section + masked-reveal + reference picker built; check:web-vite-data-layer passes; no container.</done>
</task>

<task type="auto">
  <name>Task 2: Six per-market field components + flag-gated page + i18n parity</name>
  <read_first>
    - apps/web-vite/src/components/contractors/country-compliance-section.tsx:290-394 (UaeFields/SaudiFields presentational idiom: useId, space-y-2, RequiredLabel, Input, EntityTypeSelect, useCallback handlers)
    - apps/web-vite/src/components/dashboard/dashboard-home.tsx (the useFlag render-tree-removal page idiom)
    - 90-UI-SPEC.md Copywriting Contract (the i18n key table — Employees.compliance.* / Employees.registry.*) + the three-class feedback mapping
    - An existing apps/web-vite/src/i18n/locales/en/*.json (the namespace shape + the en-US fallback-aware pattern)
    - The <three_class_feedback> block above
  </read_first>
  <action>
    Create the six presentational `{pl,de,uk,us,ae,sa}-employee-fields.tsx` (props in / JSX out, no tRPC) mirroring the `UaeFields`/`SaudiFields` idiom: `useId()`, `space-y-2` field blocks, `RequiredLabel` + `Input` (`inputMode="numeric"`, `maxLength`, `aria-invalid`/`aria-describedby`), `EntityTypeSelect<T>` for enums (PL NFZ; DE Lohnsteuerklasse I–VI + ELStAM note; UK student-loan + tax-code; US W-4 step-1c + 10-state withholding + free-text fallback; AE visa + WPS + the Emirates-ID `EmployeePiiMaskedReveal` and the amber advisory pill; SA Saudization NitaqatBand + GOSI), `ReferenceListPicker` for ZUS/NFZ/urząd/Krankenkasse, `EmployeePiiMaskedReveal` for PESEL(PL)/SSN(US)/Iqama(SA)/Emirates-ID(AE). Apply the <three_class_feedback> mapping: hard errors red inline FieldError; Emirates-ID + GOSI/WPS advisory amber Badge variant="warning" that NEVER blocks save; seeded-list adviser-verify muted dashed note. Create `employee-registration-page.tsx` as a thin composer: `useFlag('module.workforce-employees')` → render-tree removal (no skeleton/disabled stub) when OFF; `Suspense`; permission check; `AnimateIn` entrance; NO tRPC. Add `employees.json` to en/de/pl/ar locales with every key from the UI-SPEC Copywriting Contract (and the thin en-US override file if the project's en-US pattern requires it); ar is RTL. Run `pnpm i18n:parity`.
  </action>
  <verify>
    <automated>pnpm i18n:parity 2>&1 | tail -5 && pnpm typecheck --filter=@contractor-ops/web-vite 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Six per-market field components exist; each is presentational (no `useTRPC`/`useQuery`/`useMutation`).
    - `employee-registration-page.tsx` gates on `useFlag('module.workforce-employees')` and removes the surface from the render tree when OFF (no skeleton, no disabled stub).
    - The AE component surfaces the Emirates-ID advisory as amber `Badge variant="warning"` and the Save control is NOT blocked by a failing checksum.
    - `pnpm i18n:parity` passes for en/de/pl/ar (+ en-US fallback-aware); no hardcoded user-facing strings.
    - RTL uses logical properties only (no `ml/mr/pl/pr`); `pnpm typecheck --filter=@contractor-ops/web-vite` clean; `pnpm lint:no-breadcrumbs` clean.
  </acceptance_criteria>
  <done>Six market field components + flag-gated thin page + 4-locale i18n parity built; Emirates-ID advisory non-blocking; flag-off render-removal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full per-market employee registration UI: a flag-gated page, a wired compliance section that dispatches PL/DE/UK/US/AE/SA, masked-PII reveal (PESEL/SSN/Iqama/Emirates-ID), reference-list pickers (ZUS/NFZ/urząd/Krankenkasse), the three-class feedback (hard red / advisory amber / adviser-verify note), and i18n parity en/en-US/de/pl/ar. check:web-vite-data-layer + i18n:parity passed autonomously.
  </what-built>
  <how-to-verify>
    1. With `module.workforce-employees` enabled locally, open the employee registration page. Switch the market between PL/DE/UK/US/AE/SA — confirm the field set changes per market and matches the RESEARCH per-market table.
    2. Enter a bad PESEL / NI / Steuer-IdNr → confirm a RED inline error appears only after a complete-looking value (not mid-keystroke) and the form blocks save on it.
    3. Enter a format-valid Emirates ID with a wrong check digit → confirm an AMBER advisory pill appears and Save is STILL allowed (never blocked).
    4. With `employeePii:read`, reveal a masked PESEL/SSN → confirm the full value appears only after the click, is not in the network cache, and an audit row is written. Without the permission, confirm the reveal button is ABSENT (not just disabled).
    5. Toggle `module.workforce-employees` OFF → confirm the entire `/employee/*` surface is removed from the render tree (no skeleton, no disabled stub).
    6. Switch the locale to ar (RTL) → confirm layout mirrors correctly (logical properties) and all strings are translated (no raw keys).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. "Emirates-ID advisory is blocking save" / "reveal button disabled instead of absent").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → tRPC | All employee data via the two hooks; the section/components never call tRPC directly |
| rendered DOM → PII | Only last-4 in the DOM by default; full value enters only after an audited reveal |
| flag-off → surface | `module.workforce-employees` OFF removes the surface entirely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-06-01 | Information Disclosure | full PII in DOM/cache | mitigate | Masked last-4 default; reveal value in local state never query cache; reveal control ABSENT without employeePii:read |
| T-90-06-02 | Elevation of Privilege | flag bypass | mitigate | useFlag('module.workforce-employees') render-tree removal; server still enforces (Plan 05) |
| T-90-06-03 | Tampering | Emirates-ID advisory blocks save | accept | Amber advisory pill is non-blocking by contract (Pitfall 1 / UI-SPEC) |
| T-90-06-04 | Spoofing | layering bypass (direct tRPC in component) | mitigate | check:web-vite-data-layer enforces hook-only boundary; no new *-container.tsx |
| T-90-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs; UI-SPEC Registry Safety = first-party components only |
</threat_model>

<verification>
- `pnpm check:web-vite-data-layer` passes (hook-only tRPC boundary, no container).
- `pnpm i18n:parity` passes en/de/pl/ar (+ en-US fallback).
- `pnpm typecheck --filter=@contractor-ops/web-vite` + `pnpm lint:no-breadcrumbs` clean.
- Human-verify checkpoint: market dispatch, three-class feedback, masked reveal, flag-off removal, RTL.
</verification>

<success_criteria>
- Per-market dispatch UI with layering (page → wired section → hook → presentational), no container.
- Masked PII reveal (absent without permission, audited, uncached); Emirates-ID advisory non-blocking.
- Flag-off render-tree removal; i18n parity en/en-US/de/pl/ar; WCAG.
</success_criteria>

<output>
Create `.planning/phases/90-theme-b-employee-registry-per-market-6/90-06-SUMMARY.md` when done
</output>
