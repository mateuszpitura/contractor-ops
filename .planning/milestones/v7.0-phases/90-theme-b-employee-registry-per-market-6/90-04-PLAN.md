---
phase: 90-theme-b-employee-registry-per-market-6
plan: 04
type: execute
wave: 3
depends_on:
  - 90-03
files_modified:
  - packages/db/prisma/schema/employee.prisma
  - packages/db/src/tenant.ts
  - packages/auth/src/permissions.ts
  - packages/auth/src/roles.ts
  - packages/api/src/__tests__/employee-cross-org-leak.test.ts
autonomous: false
requirements:
  - EMP-REG-PL-01
  - EMP-REG-DE-01
  - EMP-REG-UK-01
  - EMP-REG-US-01
  - EMP-REG-AE-01
  - EMP-REG-SA-01

user_setup: []

must_haves:
  truths:
    - "EmployeeProfile is a 1:1-per-Employee tenant-owning model (countryCode + countryFields Json? + dedicated AES-256-GCM-encrypted PII columns + the three mandatory promoted typed columns: saudizationCategory/etat/employmentStatus) (D-01)"
    - "EmployeeProfile is NEVER in globalModels and a cross-org leak test proves org A cannot read org B's EmployeeProfile (D-01, security cross-tenant leak)"
    - "PESEL/SSN/Iqama/Emirates-ID are dedicated *Encrypted + *Last4 columns, never in countryFields JSON; the employeePii:read Better Auth permission is added and granted least-privilege to HR_ADMIN + owner + admin (D-04)"
    - "The additive multi-region (EU/ME/US) migration is applied behind a [BLOCKING] human gate, explicitly gated on P89's Employee table existing (D-08)"
  artifacts:
    - path: "packages/db/prisma/schema/employee.prisma"
      provides: "EmployeeProfile model + EmploymentStatus enum + FK to P89 Employee"
      contains: "model EmployeeProfile"
    - path: "packages/auth/src/permissions.ts"
      provides: "employeePii: ['read'] in accessControlStatement"
      contains: "employeePii"
    - path: "packages/api/src/__tests__/employee-cross-org-leak.test.ts"
      provides: "GREEN tenant-isolation proof for EmployeeProfile"
      min_lines: 30
  key_links:
    - from: "packages/db/prisma/schema/employee.prisma"
      to: "Employee (P89)"
      via: "employeeId String @unique FK relation"
      pattern: "employeeId\\s+String\\s+@unique"
    - from: "packages/db/src/tenant.ts"
      to: "globalModels"
      via: "EmployeeProfile MUST NOT appear in the allowlist"
      pattern: "globalModels"
---

<objective>
[HOLD until P89] Define the `EmployeeProfile` Prisma model (hybrid storage), add the `employeePii:read` Better Auth permission with a least-privilege grant set, confirm tenant-owning isolation with a cross-org leak test, and apply the additive multi-region migration behind a [BLOCKING] human gate.

Purpose: This is the storage + access-control foundation the registration router (Plan 05) writes through. It is P89-GATED: `EmployeeProfile` FKs to P89's `Employee`, and the `employeePii:read` grant references P89's 4 HR roles. EXECUTION HOLDS until P89's Employee table + HR roles + `module.workforce-employees` land.
Output: `employee.prisma` (EmployeeProfile + enum), `permissions.ts`/`roles.ts` (employeePii), a GREEN cross-org leak test, an applied [BLOCKING] migration.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/90-theme-b-employee-registry-per-market-6/90-RESEARCH.md
@.planning/phases/90-theme-b-employee-registry-per-market-6/90-PATTERNS.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-CONTEXT.md

<p89_dependency>
<!-- HARD GATE — verified absent this session (grep returns zero hits for Employee/employeeRouter/employeePii/module.workforce-employees). -->
This plan assumes P89 has delivered: the `Employee` Prisma model (links to `Worker`, workerType='EMPLOYEE'); the `employee` Better Auth resource + 4 HR roles HR_ADMIN/HR_MANAGER/PAYROLL_OFFICER/LEAVE_APPROVER (permissions.ts/roles.ts); the `module.workforce-employees` flag plumbing. If P89 has NOT executed (no `Employee` model in packages/db/prisma/schema, no HR roles in roles.ts) → STOP and report "Phase 90 Plan 04 HELD: Phase 89 not executed (no Employee model / HR roles)". Do not stub a fake Employee model.
</p89_dependency>

<interfaces>
<!-- Analog contracts extracted this session. -->

From packages/db/src/tenant.ts:42 (the allowlist — EmployeeProfile MUST NOT be added):
  const globalModels = new Set(['User','Session',...,'BoEBaseRateHistory', ...]);

From packages/db/prisma/schema/contractor.prisma (hybrid-storage + 1:1 analog):
  countryFields  Json?     // :38 — bulk non-PII per-country fields
  ssnEncrypted   String?   // :43 — iv:authTag:ciphertext dedicated column
  ssnLast4       String?   // :44
  // ContractorBillingProfile :132 — 1:1 with @unique parent FK + @@unique([organizationId, parentId]) + @@index([organizationId])

From packages/db/prisma/schema/gulf.prisma:
  enum NitaqatBand { PLATINUM HIGH_GREEN MID_GREEN LOW_GREEN YELLOW RED }   // :118 — REUSE for saudizationCategory
  model UaeFreeZone { ... }   // :100 — reference-lookup model template (no organizationId) IF a Prisma seed model is added

From packages/auth/src/permissions.ts:41-44 (MIRROR — add employeePii):
  contractorPii: ['read'],   // granted ONLY owner/admin/finance_admin
From packages/auth/src/roles.ts: the contractorPii grant set (mirror for employeePii)
</interfaces>

<promoted_columns>
<!-- D-01 mandatory three + RESEARCH storage table. -->
saudizationCategory  NitaqatBand?            (REUSE gulf.prisma:118 enum — P97 HR-dashboard filter)
etat                 Decimal? @db.Decimal(3,2)   (0.10–1.00 employment fraction — P94 payroll filter)
employmentStatus     EmploymentStatus?       (NEW enum: ACTIVE/ON_LEAVE/SUSPENDED/TERMINATED — UPPER_SNAKE per db:audit-enum-casing)
Optional 4th (planner call): lohnsteuerklasse  Lohnsteuerklasse?  (DE payroll filter)
Encrypted PII columns (dedicated, never JSON): peselEncrypted/peselLast4, ssnEncrypted/ssnLast4, iqamaEncrypted/iqamaLast4, emiratesIdEncrypted/emiratesIdLast4. Optional typed saudiIdType (1|2). NO AE/SA sub-table (RESEARCH discretion — visa/WPS/GOSI stay in JSON).
</promoted_columns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EmployeeProfile model + employeePii permission + cross-org leak test</name>
  <read_first>
    - packages/db/prisma/schema/contractor.prisma:9-60, 132-173 (Contractor hybrid columns + ContractorBillingProfile 1:1 + @@unique + @@index idiom)
    - packages/db/prisma/schema/gulf.prisma:1-10, 100-125 (region note + UaeFreeZone reference model + NitaqatBand enum to reuse)
    - packages/db/src/tenant.ts:38-55 (globalModels — confirm EmployeeProfile is NOT added)
    - packages/auth/src/permissions.ts:40-48 (contractorPii statement to mirror) + packages/auth/src/roles.ts (the contractorPii grant set + the P89 HR roles, if present)
    - packages/api/src/__tests__/tenant-isolation.test.ts (the cross-org leak idiom) + the Plan 01 skipped employee-cross-org-leak.test.ts
    - The <promoted_columns> block above
  </read_first>
  <action>
    First confirm P89 landed (see <p89_dependency>); HOLD if not. Create `packages/db/prisma/schema/employee.prisma` with `model EmployeeProfile`: `id`, `organizationId String` (tenant-owning), `employeeId String @unique` FK → P89 `Employee` (1:1), `countryCode String`, `countryFields Json?` (validated by employeeCountryFieldsSchemaMap — comment WHY, no breadcrumb IDs), the four `*Encrypted`/`*Last4` PII column pairs, the promoted typed columns `saudizationCategory NitaqatBand?` / `etat Decimal? @db.Decimal(3,2)` / `employmentStatus EmploymentStatus?` (+ optional `lohnsteuerklasse`/`saudiIdType`), timestamps, `@@unique([organizationId, employeeId])`, `@@index([organizationId])`, `@@index([organizationId, employmentStatus])`. Add `enum EmploymentStatus { ACTIVE ON_LEAVE SUSPENDED TERMINATED }` (UPPER_SNAKE). Do NOT add `EmployeeProfile` to `globalModels` in `tenant.ts` (verify it is absent; the file may not need editing — the guard is that it stays out). Add `employeePii: ['read']` to `accessControlStatement` in `permissions.ts` (mirror :44, comment drops breadcrumbs) and grant it in `roles.ts` to `HR_ADMIN` + `owner` + `admin` ONLY (exclude HR_MANAGER/PAYROLL_OFFICER/LEAVE_APPROVER unless a payroll need is shown — least-privilege, mirrors the contractor external_accountant exclusion). Un-skip `employee-cross-org-leak.test.ts` (Plan 01) and implement it against `tenant-isolation.test.ts`: seed an EmployeeProfile in org A, prove a client scoped to org B cannot read it (returns null / empty). Run `pnpm --filter @contractor-ops/db generate`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db generate && pnpm lint:schema 2>&1 | tail -3 && pnpm --filter @contractor-ops/api test -- employee-cross-org-leak</automated>
  </verify>
  <acceptance_criteria>
    - `model EmployeeProfile` exists with the four encrypted PII column pairs, the three promoted typed columns, `employeeId String @unique` FK, `@@index([organizationId])`.
    - `EmployeeProfile` is NOT in `globalModels` (`grep -c "'EmployeeProfile'" packages/db/src/tenant.ts` returns 0); `pnpm lint:schema` (globalModels guard) passes.
    - `employee-cross-org-leak.test.ts` is un-skipped and GREEN (org B cannot read org A's profile).
    - `employeePii: ['read']` in `accessControlStatement`; granted to exactly HR_ADMIN + owner + admin in `roles.ts`.
    - Prisma client regenerates clean; `pnpm db:audit-enum-casing` passes (UPPER_SNAKE); `pnpm lint:no-breadcrumbs` clean.
  </acceptance_criteria>
  <done>EmployeeProfile model + EmploymentStatus enum + employeePii permission (least-privilege) committed; cross-org leak test GREEN; not in globalModels.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    The additive `EmployeeProfile` table + `EmploymentStatus` enum (+ any reference-lookup table) in `employee.prisma`, the regenerated Prisma client, and the `employeePii:read` permission. The schema edits + client generation + cross-org leak test all completed autonomously in Task 1. This checkpoint applies the migration to the live regional databases — the one step that mutates external state.
  </what-built>
  <how-to-verify>
    1. CONFIRM Phase 89's Worker/Employee migration has already been applied to all regions — the `EmployeeProfile.employeeId` FK requires the `Employee` table to exist (P83–88 multi-region convention; if P89's migration is still held, this one HOLDS behind it).
    2. Review the additive migration SQL: it must be additive-only (new `EmployeeProfile` table + new enum + indexes + FK to `Employee`), NO existing-row mutation, NO backfill (unlike P89), with a down/rollback path.
    3. Apply per region in order: EU, then ME (AE/SA orgs live in the ME database per gulf.prisma), then US. Use the established `pnpm db:migrate:all` (or the migration-history-drift `db push` fallback documented in P82–88 if `migrate dev` is drift-blocked).
    4. Verify: each regional DB has the `EmployeeProfile` table + `EmploymentStatus` enum; the FK to `Employee` resolves; no existing table altered.
    5. Run `pnpm --filter @contractor-ops/api test -- employee-cross-org-leak` against a migrated DB — still GREEN.
  </how-to-verify>
  <resume-signal>Type "applied" once the migration is applied to all three regions and the cross-org leak test is green, or describe the blocker (e.g. "P89 migration not yet applied — holding").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → EmployeeProfile reads | Every read must carry organizationId (withTenantScope); cross-org reads are an IDOR risk |
| stored PII at rest → DB | National IDs encrypted in dedicated columns; the DB never holds plaintext PESEL/SSN/Iqama/EID |
| migration → live regional DBs | External-state mutation; additive-only, reversible, human-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-04-01 | Information Disclosure | cross-org EmployeeProfile read (IDOR) | mitigate | Tenant-owning, NOT in globalModels, withTenantScope inherited, dedicated cross-org leak test GREEN |
| T-90-04-02 | Information Disclosure | PII in countryFields JSON | mitigate | National IDs are dedicated encrypted columns only; the JSON schema (Plan 03) rejects national-ID keys |
| T-90-04-03 | Elevation of Privilege | employeePii:read over-grant | mitigate | Least-privilege: HR_ADMIN + owner + admin only; HR_MANAGER/PAYROLL_OFFICER/LEAVE_APPROVER excluded |
| T-90-04-04 | Tampering | additive migration to live DBs | mitigate | Additive-only (no backfill), reversible down path, [BLOCKING] human gate, per-region ordered EU→ME→US |
| T-90-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/db generate` clean; `pnpm lint:schema` (globalModels guard) passes; `pnpm db:audit-enum-casing` passes.
- `pnpm --filter @contractor-ops/api test -- employee-cross-org-leak` GREEN.
- `grep -c "'EmployeeProfile'" packages/db/src/tenant.ts` returns 0.
- Migration applied EU→ME→US behind the [BLOCKING] gate; FK to Employee resolves.
- `pnpm lint:no-breadcrumbs` clean.
</verification>

<success_criteria>
- EmployeeProfile hybrid model (JSON + encrypted PII columns + promoted typed columns) attached 1:1 to P89 Employee.
- Tenant-owning, not in globalModels, cross-org leak GREEN.
- employeePii:read added + granted least-privilege.
- Additive multi-region migration applied behind the human gate, P89-gated.
</success_criteria>

<output>
Create `.planning/phases/90-theme-b-employee-registry-per-market-6/90-04-SUMMARY.md` when done
</output>
