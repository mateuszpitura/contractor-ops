---
phase: 90-theme-b-employee-registry-per-market-6
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/validators/src/__tests__/employee-validators.test.ts
  - packages/validators/src/__tests__/employee-country-fields.test.ts
  - packages/api/src/__tests__/employee-registry.test.ts
  - packages/api/src/__tests__/employee-cross-org-leak.test.ts
  - packages/api/src/__tests__/employee-pii-crypto.test.ts
autonomous: true
requirements:
  - EMP-REG-PL-01
  - EMP-REG-DE-01
  - EMP-REG-UK-01
  - EMP-REG-US-01
  - EMP-REG-AE-01
  - EMP-REG-SA-01

must_haves:
  truths:
    - "Every greenfield statutory validator has a RED test pinning its canonical valid + invalid vectors before any implementation exists (D-03 validators-are-checksum-of-record)"
    - "The EmployeeProfile cross-org leak + encrypted-PII-no-leak + reveal-RBAC behaviors have RED scaffolds that the P89-gated waves turn GREEN (D-01, D-04)"
    - "The P89-independent validator + crypto scaffolds compile and run RED now; the P89-gated router/schema scaffolds are isolated so they can be skipped until P89 lands (D-08)"
  artifacts:
    - path: "packages/validators/src/__tests__/employee-validators.test.ts"
      provides: "RED unit vectors for PESEL/SteuerIdNr/NI/taxCode/SaudiId/EmiratesId/GOSI/WPS"
      min_lines: 60
    - path: "packages/validators/src/__tests__/employee-country-fields.test.ts"
      provides: "RED dispatch + required-vs-optional vectors"
      min_lines: 25
    - path: "packages/api/src/__tests__/employee-cross-org-leak.test.ts"
      provides: "RED tenant-isolation scaffold for EmployeeProfile"
      min_lines: 20
    - path: "packages/api/src/__tests__/employee-pii-crypto.test.ts"
      provides: "RED round-trip + last4 scaffold for employee-pii-crypto"
      min_lines: 20
  key_links:
    - from: "packages/validators/src/__tests__/employee-validators.test.ts"
      to: "packages/validators/src/employee-validators.ts"
      via: "import of the not-yet-existing validators"
      pattern: "from.*employee-validators"
---

<objective>
Lay the Wave-0 RED scaffolds for Phase 90. Validators are the registry's checksum-of-record, so every greenfield statutory validator gets a failing unit test with the canonical test vectors BEFORE it is implemented (Nyquist). The P89-gated integration scaffolds (cross-org leak, PII reveal RBAC) are stubbed RED so the later P89-gated waves only have to flip them GREEN.

Purpose: Lock the validator contracts + the PII/tenant invariants with executable tests so subsequent waves implement against pinned expectations, not prose.
Output: Five RED test files. Two (validators, country-fields) are P89-INDEPENDENT and run now. Three (registry, cross-org-leak, pii-crypto) — the crypto one is P89-INDEPENDENT; the registry + cross-org leak ones reference P89-gated surfaces and are written `describe.skip` with a `[HOLD until P89]` comment until the schema/router land.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-theme-b-employee-registry-per-market-6/90-RESEARCH.md
@.planning/phases/90-theme-b-employee-registry-per-market-6/90-VALIDATION.md
@.planning/phases/90-theme-b-employee-registry-per-market-6/90-PATTERNS.md

<canonical_test_vectors>
<!-- From RESEARCH.md Patterns 1-5 + Validation Architecture — computed/verified this session. -->
PESEL valid: 44051401359 (check digit 9). Invalid: bad-checksum 44051401358; impossible-DOB 44131401359 (month 13).
Steuer-IdNr valid: 36574261809 (ISO 7064 MOD 11,10 via mod11_10CheckDigit). Invalid: 36554266806 (fails the "exactly one digit appears 2-or-3× in positions 1-10" uniqueness rule).
NI valid: AB123456C. Invalid: DA123456C (first letter D), AO123456C (second letter O), BG123456C (disallowed prefix), QQ123456C (Q disallowed).
UK tax code valid: 1257L, K1257, BR, 0T, NT, S1257L, 1257L W1. Invalid: ZZZ, 12345L (>4 digits).
Saudi ID: classifySaudiId returns 1 for a valid leading-1 citizen ID (passes Luhn), 2 for a valid leading-2 resident ID, false for bad Luhn or leading digit ∉{1,2}. (Pick Luhn-valid 10-digit samples at implementation; assert the 1|2|false contract.)
Emirates ID format valid: 784-1990-1234567-1. Format invalid: 784-90-1234567-1. CHECKSUM IS ADVISORY: the validator returns format-valid even when the Luhn advisory fails — the test must assert format-pass independent of checksum-pass, never a hard fail on checksum.
GOSI: lenient ^\d{9}$ accept 123456789, reject 12345 (adviser-verify, MEDIUM confidence).
WPS Establishment ID: lenient ^\d{1,13}$ accept 0000000012345, reject non-digit.
</canonical_test_vectors>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED validator + country-fields scaffolds (P89-INDEPENDENT)</name>
  <read_first>
    - packages/validators/src/__tests__/de-validators.test.ts (the *-validators.test.ts table-driven idiom — describe/it.each shape)
    - packages/validators/src/de-validators.ts:29-37 (mod11_10CheckDigit — the reuse the SteuerIdNr test depends on; confirm exported)
    - packages/validators/src/country-fields.ts:279-298 (countryFieldsSchemaMap + validateCountryFields — the dispatch the country-fields test mirrors)
    - The <canonical_test_vectors> block above (authoritative valid/invalid vectors)
  </read_first>
  <action>
    Create `packages/validators/src/__tests__/employee-validators.test.ts` importing from `../employee-validators.js` (file does not exist yet → RED). Write table-driven `describe`/`it.each` blocks asserting: `isValidPesel` accepts 44051401359, rejects 44051401358 (checksum) + 44131401359 (DOB); `isValidSteuerIdNr` accepts 36574261809, rejects 36554266806 (uniqueness) + non-11-digit; `isValidNiNumber` accepts AB123456C, rejects DA123456C/AO123456C/BG123456C/QQ123456C; `isValidUkTaxCode` accepts 1257L/K1257/BR/0T/NT/S1257L/"1257L W1", rejects ZZZ/12345L; `classifySaudiId` returns 1 for a leading-1 Luhn-valid ID, 2 for leading-2, false for bad Luhn — assert the `1|2|false` union contract; `isValidEmiratesId` returns format-valid for 784-1990-1234567-1 and format-invalid for 784-90-1234567-1, and a SEPARATE assertion that the checksum result NEVER turns a format-valid ID into a hard reject (advisory-only, KEY FLAG 2 / Pitfall 1); `isValidGosi` accepts 9-digit rejects short; `isValidWpsEstablishmentId` accepts up to 13 digits rejects non-digit. Create `packages/validators/src/__tests__/employee-country-fields.test.ts` importing `validateEmployeeCountryFields` + `employeeCountryFieldsSchemaMap` from `../employee-country-fields.js` (RED): assert dispatch returns `{}` for an unknown country code, parses a minimal valid PL/DE/GB/US/AE/SA field object, rejects a missing required field, and — CRITICAL — assert that NO national-ID key (pesel/ssn/iqama/emiratesId) is accepted into the schema (PII never in countryFields JSON, Pitfall 4). Do NOT implement the validators — only the tests. No breadcrumb IDs in comments.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/validators test -- employee-validators employee-country-fields 2>&1 | grep -E "Cannot find module|employee-validators|failed" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and FAIL at import resolution (the validator + country-fields modules do not exist yet) — RED is terminal for this wave.
    - Every <canonical_test_vector> appears as an explicit assertion (valid AND invalid cases).
    - The Emirates-ID test asserts checksum is advisory: a format-valid ID with a failing Luhn is NOT hard-rejected.
    - The country-fields test asserts no national-ID key is accepted into the JSON schema.
    - No `Phase 90` / `EMP-REG-*` / `D-0*` breadcrumb strings in source comments (`pnpm lint:no-breadcrumbs` clean for these files).
  </acceptance_criteria>
  <done>Two RED validator/country-fields test files committed; vectors pinned; modules intentionally absent.</done>
</task>

<task type="auto">
  <name>Task 2: RED crypto round-trip + P89-gated integration scaffolds</name>
  <read_first>
    - packages/api/src/services/ssn-crypto.ts (the encrypt/decrypt/maskLast4 trio + iv:authTag:ciphertext format the employee-pii-crypto test mirrors)
    - packages/api/src/__tests__/tenant-isolation.test.ts (the cross-org leak idiom the EmployeeProfile leak test mirrors)
    - packages/api/src/routers/core/contractor-tax.ts:80-103 (the revealSsn RBAC + writeAuditLog shape the registry test mirrors)
  </read_first>
  <action>
    Create `packages/api/src/__tests__/employee-pii-crypto.test.ts` importing `encryptPii`/`decryptPii`/`maskLast4` from `../services/employee-pii-crypto.js` (RED): assert round-trip (`decryptPii(encryptPii(v)) === v`), `iv:authTag:ciphertext` three-part hex format, `maskLast4` strips non-digits and returns the last 4, and that two encryptions of the same value differ (random IV). This file is P89-INDEPENDENT — it turns GREEN in Plan 03. Create `packages/api/src/__tests__/employee-cross-org-leak.test.ts` and `packages/api/src/__tests__/employee-registry.test.ts` as `describe.skip(...)` blocks with a leading `// HOLD until P89: EmployeeProfile model + employeeRouter + employeePii permission land in Plan 04/05` comment. The cross-org leak file documents (in skipped its) that an EmployeeProfile read scoped to org A must not return org B's row. The registry file documents (skipped) that `register` omits all `*Encrypted` columns on its return, and `revealPii` requires `employeePii:read` + writes a `employee.<field>.revealed` audit row. Skipped suites keep CI green now and become the GREEN target when P89 lands. No breadcrumb IDs (the `HOLD until P89` note is an operational marker, allowed; it is not a requirement/decision ID).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- employee-pii-crypto 2>&1 | grep -E "Cannot find module|employee-pii-crypto|failed" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `employee-pii-crypto.test.ts` exists and FAILS at import resolution (employee-pii-crypto service absent) — RED, P89-INDEPENDENT.
    - `employee-cross-org-leak.test.ts` + `employee-registry.test.ts` exist as `describe.skip` with the `HOLD until P89` marker — they do NOT fail CI, they are the GREEN target for Plan 04/05.
    - The crypto test asserts random-IV (two encrypts of the same plaintext differ) — proves the GCM IV handling, not a static cipher.
    - No requirement/decision breadcrumb IDs in source comments.
  </acceptance_criteria>
  <done>Crypto RED scaffold (P89-independent) + two P89-gated skipped integration scaffolds committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → validators | Pure functions; no I/O, no PII at rest — scaffolds only assert behavior |
| (future) client → employeeRouter | Untrusted registration input; the skipped scaffolds pin the `.strict()` + omit + reveal-RBAC expectations the gated waves must satisfy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01-01 | Information Disclosure | employee-country-fields test fixtures | mitigate | Test asserts NO national-ID key is accepted into countryFields JSON — locks Pitfall 4 before any code |
| T-90-01-02 | Tampering | Emirates-ID advisory expectation | mitigate | Test pins checksum-advisory contract so no later wave can hard-block a format-valid EID |
| T-90-01-03 | Repudiation | revealPii audit expectation | mitigate | Skipped registry scaffold pins the `employee.<field>.revealed` writeAuditLog contract for Plan 05 |
| T-90-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/validators test -- employee-validators employee-country-fields` → RED at module resolution.
- `pnpm --filter @contractor-ops/api test -- employee-pii-crypto` → RED at module resolution.
- The two P89-gated scaffolds are `describe.skip` → full `pnpm --filter @contractor-ops/api test` stays green (no new failures from skipped suites).
- `pnpm lint:no-breadcrumbs` clean for the five new files.
</verification>

<success_criteria>
- Five RED/skip scaffolds committed; canonical vectors pinned as assertions.
- The P89-INDEPENDENT scaffolds (validators, country-fields, crypto) are RED-now; the P89-GATED scaffolds (cross-org-leak, registry) are skipped with a HOLD marker.
- No subsequent wave needs to invent test expectations — they implement against these.
</success_criteria>

<output>
Create `.planning/phases/90-theme-b-employee-registry-per-market-6/90-01-SUMMARY.md` when done
</output>
