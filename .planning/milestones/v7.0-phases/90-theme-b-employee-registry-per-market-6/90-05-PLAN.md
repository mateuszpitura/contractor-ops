---
phase: 90-theme-b-employee-registry-per-market-6
plan: 05
type: execute
wave: 4
depends_on:
  - 90-04
files_modified:
  - packages/api/src/routers/employee/employee-registry-router.ts
  - packages/api/src/services/elstam-stub.ts
  - packages/logger/src/pii-mask.ts
  - packages/api/src/__tests__/employee-registry.test.ts
autonomous: true
requirements:
  - EMP-REG-PL-01
  - EMP-REG-DE-01
  - EMP-REG-UK-01
  - EMP-REG-US-01
  - EMP-REG-AE-01
  - EMP-REG-SA-01

must_haves:
  truths:
    - "employeeRegistryRouter.register (Zod .strict()) validates per-market fields, encrypts the four national IDs into dedicated columns, splits promoted typed columns + JSON, and OMITS all *Encrypted columns on the return (D-09, Pitfall 4)"
    - "revealPii is field-parameterized, requires employeePii:read, decrypts via the right key (SSN→SSN_ENCRYPTION_KEY, others→EMPLOYEE_PII_ENCRYPTION_KEY), and writes an employee.<field>.revealed audit row; it is staff-router-only, never on portalAppRouter (D-04)"
    - "Emirates-ID register validation is format-blocking but checksum-advisory (returns a soft warning, never throws); PII is never logged (PII_MASK_PATHS extended) (D-03, Emirates-ID advisory)"
  artifacts:
    - path: "packages/api/src/routers/employee/employee-registry-router.ts"
      provides: "register + revealPii + listReferenceLists procedures on the P89 employeeRouter"
      exports: ["employeeRegistryRouter"]
    - path: "packages/api/src/services/elstam-stub.ts"
      provides: "documented ELStAM stub-hook seam (no network)"
      min_lines: 15
    - path: "packages/api/src/__tests__/employee-registry.test.ts"
      provides: "GREEN register-omits-encrypted + revealPii-RBAC+audit proof"
      min_lines: 40
  key_links:
    - from: "packages/api/src/routers/employee/employee-registry-router.ts"
      to: "employeePii permission"
      via: "requirePermission({ employeePii: ['read'] }) on revealPii"
      pattern: "employeePii"
    - from: "packages/api/src/routers/employee/employee-registry-router.ts"
      to: "writeAuditLog"
      via: "audit on register + revealPii"
      pattern: "writeAuditLog"
---

<objective>
[HOLD until P89] Populate the P89 `employeeRouter` with the registration + PII-reveal + reference-list procedures, add the ELStAM stub-hook seam, and extend the logger PII-mask paths. This wires the pre-tested Plan 02/03 validators + crypto + the Plan 04 model into a tenant-scoped, audit-logged, RBAC-gated registry API.

Purpose: The registry's write + reveal surface. P89-GATED: it mounts on P89's `employeeRouter`, gates on `module.workforce-employees`, and uses the Plan 04 `employeePii:read` permission. EXECUTION HOLDS until P89 + Plan 04 land.
Output: `employee-registry-router.ts` (register/revealPii/listReferenceLists), `elstam-stub.ts`, extended `pii-mask.ts`, GREEN registry test.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/90-theme-b-employee-registry-per-market-6/90-RESEARCH.md
@.planning/phases/90-theme-b-employee-registry-per-market-6/90-PATTERNS.md

<p89_dependency>
This plan mounts on P89's `employeeRouter` (which P89 ships as a flag-gated skeleton on `appRouter`, conditionally spread under `module.workforce-employees`), uses the Plan 04 `employeePii:read` permission and `EmployeeProfile` model. If P89's `employeeRouter` / `module.workforce-employees` conditional-spread is absent, or Plan 04 has not landed → STOP and report "Phase 90 Plan 05 HELD: P89 employeeRouter or Plan 04 EmployeeProfile not present". NEVER mount revealPii on `portalAppRouter` (staff-only — P84 lesson).
</p89_dependency>

<interfaces>
<!-- Analog contracts extracted this session. -->

From packages/api/src/routers/core/contractor-tax.ts:23-103 (MIRROR):
  updateUsProfile (:23): validate ID at boundary → findUnique with organizationId in where →
    encrypt into dedicated columns → ctx.db.contractor.update({ where:{id, organizationId}, data, omit:{ ssnEncrypted: true } })
  revealSsn (:80): .use(requirePermission({ contractorPii: ['read'] })) → findUnique org-scoped select encrypted col →
    NOT_FOUND if absent → decryptSsn → writeAuditLog({ action:'contractor.ssn.revealed', resourceType:'CONTRACTOR', metadata:{ field } }) → return plain
  imports: tenantProcedure, requirePermission, writeAuditLog, TRPCError, z, * as E from '../../errors'

From Plan 03 (packages/validators):
  validateEmployeeCountryFields(countryCode, fields), the greenfield validators, employee-reference-lists
From Plan 03 (packages/api): encryptPii/decryptPii/maskLast4 (EMPLOYEE_PII_ENCRYPTION_KEY)
From packages/api/src/services/ssn-crypto.ts: encryptSsn/decryptSsn/maskSsnLast4 (SSN_ENCRYPTION_KEY — reuse for the SSN column)
From Plan 04: employeePii: ['read'] permission; EmployeeProfile model (peselEncrypted/ssnEncrypted/iqamaEncrypted/emiratesIdEncrypted + *Last4 + promoted columns)

From packages/logger/src/pii-mask.ts: PII_MASK_PATHS (existing *.ssn entry to mirror)
</interfaces>

<reveal_key_routing>
<!-- revealPii is field-parameterized over 4 ID types / 2 crypto keys. -->
field 'ssn'        → decryptSsn (SSN_ENCRYPTION_KEY, reuse ssn-crypto)
field 'pesel'      → decryptPii (EMPLOYEE_PII_ENCRYPTION_KEY)
field 'iqama'      → decryptPii (EMPLOYEE_PII_ENCRYPTION_KEY)
field 'emiratesId' → decryptPii (EMPLOYEE_PII_ENCRYPTION_KEY)
audit action: employee.<field>.revealed; resourceType: EMPLOYEE; metadata: { field }
</reveal_key_routing>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: employeeRegistryRouter (register + revealPii + listReferenceLists)</name>
  <behavior>
    - register (Zod .strict()) creates EmployeeProfile; validates per-market fields; rejects an invalid PESEL/SteuerIdNr/NI; encrypts the four national IDs; the response OMITS every *Encrypted column
    - register with a format-valid Emirates ID whose Luhn fails SUCCEEDS and returns a soft advisory (never throws)
    - revealPii('ssn') requires employeePii:read → 403 without it; with it, returns the decrypted value AND writes an employee.ssn.revealed audit row
    - revealPii routes the decrypt key by field (ssn→SSN key, pesel/iqama/emiratesId→EMPLOYEE key)
    - register derives organizationId + workerType + encrypted columns server-side; client-set *Last4 / encrypted values are rejected (mass-assignment)
  </behavior>
  <read_first>
    - packages/api/src/routers/core/contractor-tax.ts:1-103 (the whole router: imports, updateUsProfile encrypt+omit, revealSsn RBAC+audit — the exact idiom to mirror)
    - packages/api/src/__tests__/employee-registry.test.ts (Plan 01 skipped scaffold — the register-omit + revealPii-RBAC+audit asserts to satisfy; un-skip it)
    - packages/api/src/errors.ts (the error-constant idiom for invalid-ID messages)
    - 90-PATTERNS.md lines 141-169 (employee-registry-router analog + the five "what differs" rules)
    - The <reveal_key_routing> + <interfaces> blocks above
  </read_first>
  <action>
    Confirm P89 + Plan 04 landed (see <p89_dependency>); HOLD if not. Create `packages/api/src/routers/employee/employee-registry-router.ts` exporting `employeeRegistryRouter`, mounted on P89's `employeeRouter` (NOT contractorRouter, NOT portal). `register`: input is `.strict()` Zod (employeeId, countryCode, the per-market non-PII fields, and the plaintext national IDs as separate input keys — NOT inside countryFields); run `validateEmployeeCountryFields` + the relevant greenfield validators (throw `BAD_REQUEST` with an `errors.ts` constant on a hard-invalid PESEL/SteuerIdNr/NI; for Emirates ID, block only on format-invalid and return a `checksumAdvisory` field in the response when the Luhn advisory fails — never throw, KEY FLAG 2); encrypt SSN via `encryptSsn` (+ `ssnLast4`) and PESEL/Iqama/Emirates-ID via `encryptPii` (+ `*Last4` via `maskLast4`); split the promoted typed columns (saudizationCategory/etat/employmentStatus) out of the input and the rest into `countryFields`; create the `EmployeeProfile` org-scoped; `omit` every `*Encrypted` column on the return (Pitfall 4); `writeAuditLog({ action:'employee.registered', resourceType:'EMPLOYEE', metadata })`. `revealPii`: `.use(requirePermission({ employeePii: ['read'] }))`, input `{ employeeId, field }`, org-scoped `findUnique` selecting only the one encrypted column, `NOT_FOUND` if absent, decrypt via the <reveal_key_routing> key, `writeAuditLog({ action:'employee.'+field+'.revealed', resourceType:'EMPLOYEE', metadata:{ field } })`, return the plain value. `listReferenceLists`: read the seeded reference lists (org not required — non-PII). Un-skip + complete `employee-registry.test.ts`. NO breadcrumb IDs; tenant from session; never log full PII.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- employee-registry && pnpm typecheck --filter=@contractor-ops/api 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test -- employee-registry` PASSES (Plan 01 skipped → GREEN): register omits all `*Encrypted` columns; revealPii is 403 without employeePii:read; with it returns the value + writes the audit row.
    - `register` input schema is `.strict()` and rejects client-set `*Last4`/encrypted/organizationId/workerType keys (mass-assignment).
    - A format-valid Emirates ID with a failing Luhn registers successfully with a `checksumAdvisory` (no throw).
    - `revealPii` routes the decrypt key by field; never appears on `portalAppRouter` (`grep -c revealPii packages/api/src/portal-root.ts` returns 0).
    - `pnpm lint:audit-log` passes (register + reveal write audit); `pnpm lint:no-breadcrumbs` clean; `pnpm typecheck --filter=@contractor-ops/api` clean.
  </acceptance_criteria>
  <done>employeeRegistryRouter with register/revealPii/listReferenceLists mounted on the P89 employeeRouter; encrypted-column omit + RBAC + audit GREEN; Emirates-ID advisory; staff-only.</done>
</task>

<task type="auto">
  <name>Task 2: ELStAM stub-hook seam + logger PII-mask extension</name>
  <read_first>
    - packages/logger/src/pii-mask.ts (PII_MASK_PATHS — the existing `*.ssn` entry + casing variants to mirror)
    - 90-RESEARCH.md Security V7 (the exact paths to add) + 90-PATTERNS.md lines 299-302 (elstam-stub no-analog guidance)
  </read_first>
  <action>
    Create `packages/api/src/services/elstam-stub.ts`: a documented stub-hook seam with a typed interface (e.g. `lookupElstam(input): ElstamStubResult`) that returns a typed STUB result and performs NO network call — header comment states this is a deliberate local-only seam (D-05, no live German gov API), wired for a later real integration. Extend `packages/logger/src/pii-mask.ts` `PII_MASK_PATHS` with `*.pesel`, `*.iqama`, `*.emiratesId`, `*.nationalId` (plus `*.ssn` already present), including the `countryFields.*` and casing variants mirroring the existing `*.ssn` entry, so no national ID is ever logged in full. NO breadcrumb IDs; the local-only/no-live-gov-API note is a real caveat (allowed).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/logger test 2>&1 | tail -3 && grep -c "pesel\|iqama\|emiratesId\|nationalId" packages/logger/src/pii-mask.ts</automated>
  </verify>
  <acceptance_criteria>
    - `elstam-stub.ts` exports a typed stub lookup that makes NO network call (no fetch/http import).
    - `PII_MASK_PATHS` includes pesel/iqama/emiratesId/nationalId (+ casing/countryFields variants) — grep returns ≥ 4.
    - `pnpm --filter @contractor-ops/logger test` passes; `pnpm typecheck --filter=@contractor-ops/api --filter=@contractor-ops/logger` clean; `pnpm lint:no-breadcrumbs` clean.
  </acceptance_criteria>
  <done>ELStAM stub seam (no network) + PII-mask paths extended so national IDs never log in full.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → register | Untrusted registration input; `.strict()` + server-derived org/encrypted columns |
| client → revealPii | Privileged decrypt path; RBAC + audit gate |
| app → logs | PII must never reach logs in full |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-05-01 | Information Disclosure | encrypted blob round-trips to client | mitigate | `omit: { *Encrypted: true }` on register/update return (contractor-tax.ts:76) |
| T-90-05-02 | Elevation / Repudiation | reveal without authz/audit | mitigate | `requirePermission(employeePii:read)` + `writeAuditLog('employee.<field>.revealed')` |
| T-90-05-03 | Spoofing | mass-assignment of org/encrypted/last4 | mitigate | `.strict()` input; organizationId/workerType/encrypted columns server-derived; client *Last4 rejected |
| T-90-05-04 | Information Disclosure | reveal exposed on portal | mitigate | Staff-router only; revealPii never on portalAppRouter (P84 lesson) |
| T-90-05-05 | Information Disclosure | PII in application logs | mitigate | PII_MASK_PATHS extended with pesel/iqama/emiratesId/nationalId + countryFields variants |
| T-90-05-06 | Tampering | Emirates-ID hard-block on advisory | accept | Checksum advisory-only returns a soft warning; never blocks register (Pitfall 1) |
| T-90-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- employee-registry` GREEN (register omit + revealPii RBAC + audit).
- `pnpm --filter @contractor-ops/logger test` GREEN; PII_MASK_PATHS extended.
- `pnpm lint:audit-log` + `pnpm lint:no-breadcrumbs` clean; `pnpm typecheck --filter=@contractor-ops/api --filter=@contractor-ops/logger` clean.
- `grep -c revealPii packages/api/src/portal-root.ts` returns 0 (staff-only).
</verification>

<success_criteria>
- register: validated, encrypted, omitting, audited, `.strict()`; Emirates-ID advisory.
- revealPii: field-parameterized, RBAC-gated, audit-logged, staff-only, correct key routing.
- ELStAM stub seam (no network); national IDs never log in full.
</success_criteria>

<output>
Create `.planning/phases/90-theme-b-employee-registry-per-market-6/90-05-SUMMARY.md` when done
</output>
