---
phase: 90-theme-b-employee-registry-per-market-6
plan: 07
type: execute
wave: 6
depends_on:
  - 90-02
  - 90-03
  - 90-05
  - 90-06
files_modified:
  - .planning/brain/wiki/domains/employee-registry.md
  - .planning/brain/wiki/structure/prisma-schema-areas.md
  - .planning/brain/wiki/structure/packages.md
  - .planning/brain/wiki/structure/web-vite-domains.md
  - .planning/brain/wiki/patterns/_index.md
  - .planning/brain/wiki/log.md
  - .planning/brain/wiki/hot.md
  - .planning/MEMORY.md
autonomous: true
requirements:
  - EMP-REG-PL-01
  - EMP-REG-DE-01
  - EMP-REG-UK-01
  - EMP-REG-US-01
  - EMP-REG-AE-01
  - EMP-REG-SA-01

must_haves:
  truths:
    - "The employee-registry domain + the per-market validator/PII-boundary/reference-list patterns are documented in the wiki in the same milestone as the code (documentation-follows-code)"
    - "MEMORY.md records the two new invariants: the employee national-ID PII-encryption boundary (PESEL/SSN/Iqama/Emirates-ID encrypted, dedicated key for the 3 non-SSN; tax/social IDs plain-RBAC-gated) and reference-lists-are-seeded-not-live-gov (D-04, D-05)"
    - "check:wiki-brain passes (no NEW doc drift) and the knowledge graph is rebuilt from the new code"
  artifacts:
    - path: ".planning/brain/wiki/domains/employee-registry.md"
      provides: "Purpose / Flow / Entry points / UI surface / Agent mistakes for the employee registry"
      min_lines: 30
    - path: ".planning/MEMORY.md"
      provides: "the two new Phase 90 invariants appended"
      contains: "EMPLOYEE_PII_ENCRYPTION_KEY"
  key_links:
    - from: ".planning/brain/wiki/domains/employee-registry.md"
      to: "packages/api/src/routers/employee/employee-registry-router.ts"
      via: "verify_with frontmatter path"
      pattern: "verify_with"
---

<objective>
Update the codebase wiki + MEMORY + knowledge graph to track the Phase 90 employee registry, in the same milestone as the code. The wiki is a living compass — it must track the code, not lag behind it. This closes the documentation-follows-code gate (CLAUDE.md) for the registry surface, PII boundary, and reference-list decisions.

Purpose: The next agent must find the employee-registry domain, the PII-encryption boundary, and the seeded-not-live-gov reference-list invariant from the wiki + MEMORY — not re-derive them.
Output: an employee-registry domain page, updated structure/pattern pages, log/hot refresh, two MEMORY invariants, a rebuilt graph.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/90-theme-b-employee-registry-per-market-6/90-CONTEXT.md
@.planning/phases/90-theme-b-employee-registry-per-market-6/90-RESEARCH.md
@CLAUDE.md

<doc_targets>
<!-- From 90-CONTEXT.md canonical_refs documentation-follows-code + CLAUDE.md trigger matrix. -->
- wiki/domains/employee-registry.md (NEW): Purpose, Flow (register → validate → encrypt/split → store; revealPii audited), Entry points (employeeRegistryRouter, EmployeeProfile, employee-validators), UI surface (employees/compliance), Agent mistakes (national IDs never in countryFields JSON; Emirates-ID checksum advisory; reference lists are local-only seeds not live gov; EmployeeProfile never in globalModels).
- wiki/structure/prisma-schema-areas.md: add EmployeeProfile (hybrid JSON + encrypted PII columns + promoted typed columns; tenant-owning; FK → P89 Employee).
- wiki/structure/packages.md: add employee-validators + employee-country-fields + employee-pii-crypto + reference-data to the validators/api package entries.
- wiki/structure/web-vite-domains.md: add the employees/compliance domain (page → wired section → hook → presentational; no container).
- wiki/patterns/_index.md (+ a pattern page if a new convention): the country-fields-registry-reuse + national-ID PII-encryption-boundary + seeded-reference-list (no live gov) patterns.
- wiki/log.md + hot.md: append/overwrite per the wiki-edit trigger.
- .planning/MEMORY.md: two invariants (PII boundary + reference-lists-not-live-gov).
</doc_targets>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Employee-registry domain page + structure/pattern wiki updates</name>
  <read_first>
    - .planning/brain/wiki/index.md + .planning/brain/wiki/structure/prisma-schema-areas.md + packages.md + web-vite-domains.md (the existing page shape + frontmatter + verify_with convention to mirror)
    - .planning/brain/wiki/domains/contractors-engagements.md (the closest domain-page analog — Purpose/Flow/Entry points/UI surface/Agent mistakes structure)
    - .planning/brain/wiki/patterns/_index.md + money-rounding.md (the pattern-page shape)
    - The Plan 02/03/04/05/06 SUMMARYs (the actual files + decisions to document)
    - The <doc_targets> block above
  </read_first>
  <action>
    Create `.planning/brain/wiki/domains/employee-registry.md` with the standard frontmatter (including `verify_with` pointing at `employee-registry-router.ts`, `employee.prisma`, `employee-validators.ts`, `employee-compliance-section.tsx`, and a `source_commit`) and the sections: Purpose, Flow (register → validateEmployeeCountryFields + greenfield validators → encrypt 4 national IDs into dedicated columns → split promoted typed columns + JSON → omit encrypted on return; revealPii → employeePii:read + audit), Entry points, UI surface (employees/compliance), Agent mistakes (the four listed in <doc_targets>). Update `prisma-schema-areas.md` (EmployeeProfile hybrid model), `packages.md` (the new validators/api modules), `web-vite-domains.md` (employees/compliance layering, no container). Append the country-fields-reuse + PII-boundary + seeded-reference-list patterns to `patterns/_index.md` (a dedicated pattern page only if the convention is genuinely new). Append to `log.md` and overwrite `hot.md` per the wiki-edit trigger. Bump `source_commit` on any touched page whose verify_with files changed. NO operational text in the wiki (no "phase in progress" — only the resulting code/domain facts, per MEMORY guidance). NO breadcrumb planning IDs.
  </action>
  <verify>
    <automated>test -f .planning/brain/wiki/domains/employee-registry.md && grep -c "verify_with\|Agent mistakes\|EmployeeProfile" .planning/brain/wiki/domains/employee-registry.md</automated>
  </verify>
  <acceptance_criteria>
    - `wiki/domains/employee-registry.md` exists with frontmatter `verify_with` + the five sections (Purpose/Flow/Entry points/UI surface/Agent mistakes).
    - `prisma-schema-areas.md`, `packages.md`, `web-vite-domains.md` reference the new EmployeeProfile model + the new validator/api modules + the employees/compliance domain.
    - `patterns/_index.md` lists the country-fields-reuse + PII-boundary + seeded-reference-list patterns.
    - `log.md` appended + `hot.md` overwritten; no operational/status text in any wiki page; no breadcrumb planning IDs.
  </acceptance_criteria>
  <done>Employee-registry domain page + structure/pattern wiki pages updated; log/hot refreshed.</done>
</task>

<task type="auto">
  <name>Task 2: MEMORY invariants + graph rebuild + wiki-brain gate</name>
  <read_first>
    - .planning/MEMORY.md (the append format — one-line title + a linked detail file, the existing project_* entries)
    - CLAUDE.md "Documentation follows code" section (the check:wiki-brain + graph rebuild commands)
  </read_first>
  <action>
    Append two invariants to `.planning/MEMORY.md`: (1) the employee national-ID PII-encryption boundary — PESEL/SSN/Iqama/Emirates-ID encrypted at rest + last4 + masked + employeePii:read reveal + audit; the 3 non-SSN IDs use a dedicated `EMPLOYEE_PII_ENCRYPTION_KEY` while SSN reuses `SSN_ENCRYPTION_KEY`; tax/social IDs (Steuer-IdNr/SV-Nummer/NI/PAYE) plain-but-RBAC-gated; national IDs NEVER in countryFields JSON; (2) employee reference lists (ZUS/NFZ/urząd/Krankenkasse) + ELStAM are seeded versioned adviser-verify data + a stub hook — NO live government API (local-only), and EmployeeProfile is tenant-owning (never globalModels, cross-org leak test). Then rebuild the knowledge graph and the BM25 index per the CLAUDE.md commands (rm the graph first for a deterministic rebuild, run graphify update, copy to .planning/graphs/graph.json; rebuild the brain contextual-prefix + bm25 index). Run `pnpm check:wiki-brain` and resolve any NEW drift it reports (only new drift from this change set, not pre-existing).
  </action>
  <verify>
    <automated>grep -c "EMPLOYEE_PII_ENCRYPTION_KEY\|reference list" .planning/MEMORY.md && pnpm check:wiki-brain 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/MEMORY.md` contains the two new invariants (PII boundary mentioning EMPLOYEE_PII_ENCRYPTION_KEY; reference-lists-not-live-gov).
    - `pnpm check:wiki-brain` passes (no NEW doc drift introduced by this change set).
    - The graph + BM25 index are rebuilt (`.planning/graphs/graph.json` regenerated).
    - No operational text in MEMORY (durable invariants only).
  </acceptance_criteria>
  <done>Two MEMORY invariants appended; graph + BM25 rebuilt; check:wiki-brain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → next agent | Stale/missing docs cause the next agent to hallucinate the PII boundary or re-call live gov APIs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-07-01 | Information Disclosure | wiki leaking PII or secrets | mitigate | Wiki documents the boundary + the env-var NAME only — never a real key value or a real national ID |
| T-90-07-02 | Repudiation | doc drift unrecorded | mitigate | check:wiki-brain gate + log.md/hot.md refresh in the same change set |
| T-90-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `wiki/domains/employee-registry.md` exists with verify_with + the five sections.
- `.planning/MEMORY.md` has the two invariants.
- `pnpm check:wiki-brain` passes (no new drift); graph + BM25 rebuilt.
- No operational text / breadcrumb planning IDs in any wiki page.
</verification>

<success_criteria>
- Employee-registry domain + structure + pattern pages document the code.
- The PII-boundary + seeded-reference-list invariants are in MEMORY.
- check:wiki-brain green; graph rebuilt — documentation tracks the code in the same milestone.
</success_criteria>

<output>
Create `.planning/phases/90-theme-b-employee-registry-per-market-6/90-07-SUMMARY.md` when done
</output>
