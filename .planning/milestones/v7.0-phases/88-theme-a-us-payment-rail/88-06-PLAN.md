---
phase: 88-theme-a-us-payment-rail
plan: 06
type: execute
wave: 6
depends_on: ["88-01", "88-02", "88-03", "88-04", "88-05"]
files_modified:
  - packages/integrations/src/adapters/payout/payout-initiation-adapter.ts
  - packages/integrations/src/adapters/payout/mock-modern-treasury-adapter.ts
  - packages/integrations/src/adapters/payout/modern-treasury-adapter.ts
  - packages/integrations/src/adapters/payout/stripe-treasury-adapter.ts
  - packages/integrations/src/adapters/plaid/plaid-identity-client.ts
  - packages/integrations/src/adapters/plaid/mock-plaid-identity-client.ts
  - packages/integrations/src/adapters/plaid/plaid-identity-client-live.ts
  - packages/feature-flags/src/flags-core.ts
  - packages/integrations/.env.example
  - packages/api/src/routers/finance/payment-core.ts
autonomous: false
requirements: [US-PAY-03, US-PAY-05]
user_setup: []
must_haves:
  truths:
    - "Implements D-04 (PayoutInitiationAdapter seam: Modern Treasury first concrete as a deterministic mock, Stripe Treasury a stub), D-05 (file export is the always-available default; programmatic ACH is opt-in per-org behind the EXISTING payments.ach-payouts PENDING flag — no new flag minted for ACH), D-08 (Plaid advisory fail-open verification status), D-09 (Plaid Identity adapter mock-behind-seam, live flag-dark), D-11 (US payout surface gated on module.us-expansion + US region), D-12 (AES-256-GCM creds), D-13 (idempotency + audit on payout init)"
    - "An opt-in org can initiate a programmatic ACH payout via the Modern Treasury mock; idempotent init (no double-pay) + writeAuditLog; the live client is dark until the flag flips and creds land"
    - "Stripe Treasury is a stub concrete implementing the same PayoutInitiationAdapter interface"
    - "A US contractor bank account is verifiable via the Plaid Identity mock; an unverified status (read per-item via PaymentRunItem.billingProfile → ContractorBillingProfile.plaidVerificationStatus) WARNS (advisory) and never blocks the payout (fail-open, mirrors P84 USPS)"
    - "Each [ASSUMED] external SDK (modern-treasury, plaid) install is gated behind a blocking human-verify checkpoint (name/source-repo/release-age/typosquat); the GA mock-behind-seam path installs ZERO external packages"
  artifacts:
    - path: "packages/integrations/src/adapters/payout/payout-initiation-adapter.ts"
      provides: "PayoutInitiationAdapter interface (initiatePayout, getPayoutStatus, optional handleWebhook)"
      contains: "initiatePayout"
    - path: "packages/integrations/src/adapters/payout/mock-modern-treasury-adapter.ts"
      provides: "deterministic Modern Treasury mock (default)"
      contains: "payment_order"
    - path: "packages/integrations/src/adapters/plaid/plaid-identity-client.ts"
      provides: "PlaidIdentityClient interface + mock (advisory fail-open)"
      contains: "PlaidIdentityClient"
    - path: "packages/api/src/routers/finance/payment-core.ts"
      provides: "opt-in programmatic-ACH payout-init procedure (idempotent, audited, flag+region gated, per-item Plaid advisory)"
      contains: "initiatePayout"
  key_links:
    - from: "payment-core.ts payout-init procedure"
      to: "lib/idempotency.ts reserve/complete/clear"
      via: "no-double-pay reservation"
      pattern: "reserve|complete|clear"
    - from: "payment-core.ts payout-init procedure"
      to: "require-us-expansion-flag assertUsExpansionEnabled + payments.ach-payouts flag"
      via: "gating"
      pattern: "assertUsExpansionEnabled|ach-payouts"
    - from: "payment-core.ts initiatePayout per-item Plaid advisory"
      to: "PaymentRunItem.billingProfile → ContractorBillingProfile.plaidVerificationStatus"
      via: "tenant-scoped Prisma include (run → items → billingProfile); plaidVerificationStatus !== 'VERIFIED' → advisory warning, never block"
      pattern: "plaidVerificationStatus"
    - from: "payment-core.ts initiatePayout Zod input (settlementCurrency override)"
      to: "payment-settlement.ts resolveSettlementCurrency"
      via: "per-run settlement-currency override threaded into resolveSettlementCurrency before the payout amount is sent to the adapter"
      pattern: "resolveSettlementCurrency|settlementCurrency"
---

<objective>
Build the two flag-dark adapter seams: the Modern Treasury programmatic-ACH PayoutInitiationAdapter (deterministic mock default + Stripe stub) and the Plaid Identity verification client (mock default), then wire the opt-in payout-init tRPC procedure (idempotent, audited, region+flag gated) — including the per-run settlement-currency resolution (88-05) and the per-item Plaid advisory read. The GA mock-behind-seam path installs zero external packages; any live SDK install is gated behind a blocking human-verify checkpoint.

Purpose: US-PAY-03 (opt-in programmatic ACH) + US-PAY-05 (Plaid bank-account verification). File export (88-04) remains the always-available default; programmatic init is opt-in (D-05).
Output: The payout + plaid adapter seams (interface + mock + dark live concrete), the existing-flag reuse, the env-schema additions, and the gated payout-init procedure. The 88-01 adapter scaffolds turn green.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/88-theme-a-us-payment-rail/88-CONTEXT.md
@.planning/phases/88-theme-a-us-payment-rail/88-RESEARCH.md
@.planning/phases/88-theme-a-us-payment-rail/88-PATTERNS.md
@.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-CONTEXT.md

<interfaces>
<!-- Exact signatures. Extracted from codebase. -->

From packages/integrations/src/adapters/tin-match/tin-match-client.ts (the seam pattern to mirror — NOT BaseAdapter):
- `export interface TinMatchClient { match(input: TinMatchInput): Promise<TinMatchResult>; }` — interface; `MockTinMatchClient` deterministic mock default; `EservicesTinMatchClient` live-dark.

From packages/integrations/src/adapters/tin-match/index.ts (the seam barrel pattern to mirror):
- Re-exports the interface types + the mock + the live concrete from the seam directory. The package barrel `packages/integrations/src/index.ts:22-29` then re-exports the tin-match seam from `./adapters/tin-match/index.js`. NOTE: the tin-match seam is NOT registered in `adapters/register-all.ts` — `register-all.ts` only wires `BaseAdapter`-shaped provider adapters (OAuth/webhook/health) into the provider registry. The payout + plaid seams mirror tin-match (focused client interfaces), so they are EXPORTED from a seam index + the package barrel and constructed directly by their consuming service — NOT registered in register-all.ts.

From packages/integrations/src/services/credential-service.ts:
- `getProviderEncryptionKey(providerSlug)` (22) → `${SLUG_UPPER}_ENCRYPTION_KEY` buffer.
- `encryptCredentials(blob, providerSlug): string` (42) — AES-256-GCM `iv:authTag:ciphertext`.
- `decryptCredentials(encrypted, providerSlug): CredentialBlob` (65).

From packages/feature-flags/src/flags-core.ts:
- `'payments.ach-payouts'` ALREADY registered PENDING (304) + in `V7_FLAG_KEYS` (410) — REUSE for programmatic ACH; do NOT re-add. `'module.us-expansion'` (211) gates the surface. Mint a NEW PENDING entry only for a distinct Plaid live path if one is genuinely needed.

From packages/api/src/middleware/require-us-expansion-flag.ts:
- `assertUsExpansionEnabled(organizationId, region): void` (29) — throws FORBIDDEN/US_EXPANSION_DISABLED.

From packages/api/src/lib/idempotency.ts (reserve/complete/clear, Upstash 24h):
- `reserve<T>(key, ttlSeconds)` → HIT|PENDING|RESERVED; `complete(key, result, ttl)`; `clear(key)`. `IDEMPOTENCY_TTL_SECONDS` exported from payment-shared.ts (32).

From packages/api/src/services/audit-writer.ts:
- `writeAuditLog({ tx?, organizationId, actorType, actorId?, action, resourceType, resourceId, metadata? })`.

From packages/api/src/services/payment-settlement.ts (88-05):
- `resolveSettlementCurrency({ contractorCurrency, perRunOverride? }): string` + `convertForSettlement(db, amountMinor, fromCurrency, settlementCurrency, paymentDate): Promise<{ amountMinor; rate; rateDate } | null>` — the per-run settlement seam the initiatePayout Zod input override feeds.

From the Prisma schema (the exact Plaid advisory join path — 88-02 adds plaidVerificationStatus to ContractorBillingProfile):
- `PaymentRunItem` (packages/db/prisma/schema/payment.prisma:36) — `organizationId` (tenant scope), `billingProfileId String?` (:42), relation `billingProfile ContractorBillingProfile? @relation(fields: [billingProfileId], references: [id])` (:62). This is the per-item profile the run pays out to — the precise join (do NOT navigate contractor.billingProfiles[] which could pick the wrong/cross-profile).
- `ContractorBillingProfile` (packages/db/prisma/schema/contractor.prisma:132) — `organizationId` (:134), `plaidVerificationStatus` (added by 88-02: VERIFIED/PENDING/FAILED), `billingProfile` is nullable on the item (handle null → advisory, never throw).

From packages/api/src/routers/finance/payment-core.ts:
- tenantProcedure router; `requirePermission({ payment: ['export'] })` pattern (from payment-export-router.ts:193-195); the atomic `updateMany` status-guard (payment-export-router.ts:115-140) prevents double-pay across pods.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Pre-install gate for the [ASSUMED] external SDKs (modern-treasury, plaid)</name>
  <action>
    Before any install of modern-treasury or plaid, PAUSE for the human to verify each package (name/source-repo/release-age/typosquat) per the steps below. The recommended GA path installs NEITHER SDK (ship the mock-behind-seam concretes from Tasks 2-4); only proceed to an install if the human explicitly approves after verification. The executor must not install either package without this approval.
  </action>
  <what-built>
    Pre-install gate for the two [ASSUMED] external SDKs. slopcheck was offline at research time, so modern-treasury@4.18.0 and plaid@42.2.0 are UNVERIFIED. The GA mock-behind-seam path (Tasks 2-4) installs ZERO external packages — these SDKs are needed ONLY inside the flag-dark live concretes, which this phase ships dark.
  </what-built>
  <how-to-verify>
    [BLOCKING — never auto-approved] For EACH SDK the executor intends to install (only if the live concrete is wired this phase; otherwise SKIP install entirely and ship the mock):
    1. Verify the package on its registry: https://www.npmjs.com/package/modern-treasury and https://www.npmjs.com/package/plaid — confirm the exact name (typosquat check), the official source repo (Modern-Treasury/modern-treasury-node; official Plaid), and a publish date older than the 7-day release-age gate.
    2. Confirm download counts roughly match RESEARCH (modern-treasury ~13k/wk, plaid ~770k/wk).
    3. After install: `pnpm audit` + `pnpm security:scan` must be clean.
    4. RECOMMENDED for GA: do NOT install either SDK — ship the mock-behind-seam concretes only; the live concrete imports the SDK lazily inside the flag-dark branch and the install is deferred to live activation.
    @midlandsbank/node-nacha is FORBIDDEN (no exposed source repo) — NACHA is hand-rolled in 88-04.
  </how-to-verify>
  <resume-signal>Type "approved — mock only (no SDK installed)" (recommended) or "approved — install modern-treasury and/or plaid after verification" with the verified name/repo/date, or describe a concern.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PayoutInitiationAdapter seam — interface + deterministic Modern Treasury mock + Stripe stub + dark live concrete</name>
  <behavior>
    - initiatePayout(input) on the mock returns a deterministic payment_order shape with id + a status in the documented enum pending→processing→completed→reconciled (same input → same output, no network/randomness).
    - getPayoutStatus(id) on the mock returns a terminal status deterministically.
    - The live Modern Treasury concrete refuses (throws / returns disabled) while the flag is dark / creds absent.
    - Stripe Treasury stub implements the same interface and throws a NOT_IMPLEMENTED/disabled result.
  </behavior>
  <read_first>
    - packages/integrations/src/adapters/tin-match/{tin-match-client.ts,mock-tin-match-client.ts,eservices-tin-match-client.ts} (seam + mock + dark-live trio to mirror)
    - packages/integrations/src/adapters/tin-match/index.ts (the seam barrel to mirror) + packages/integrations/src/index.ts:22-29 (how the package barrel re-exports the seam)
    - packages/integrations/src/adapters/register-all.ts (CONFIRM: BaseAdapter provider-registry only — the tin-match seam is NOT here, so the payout seam is NOT registered here either)
    - packages/integrations/src/services/credential-service.ts:22-83 (AES-256-GCM cred store)
    - packages/integrations/src/__tests__/modern-treasury-adapter.test.ts (88-01 scaffold to turn green)
    - 88-RESEARCH.md Pattern 4 + Pitfall 4 (mock must match the documented payment_order + webhook status enum) + 88-PATTERNS.md adapters section
  </read_first>
  <action>
    Create packages/integrations/src/adapters/payout/payout-initiation-adapter.ts with `export interface PayoutInitiationAdapter { initiatePayout(input): Promise<PayoutResult>; getPayoutStatus(id): Promise<PayoutStatus>; handleWebhook?(payload): Promise<...>; }` plus the PayoutInput/PayoutResult/PayoutStatus types shaped to the documented Modern Treasury payment_order ({ type:'ach', direction:'credit', amount, currency, originating_account_id, receiving_account_id }) and webhook status enum (pending→processing→completed→reconciled). Create mock-modern-treasury-adapter.ts (deterministic, fixture-mapped, no network — the shipped default), modern-treasury-adapter.ts (live concrete: HTTP Basic Org-ID/API-key via decryptCredentials; the SDK call lazily imported INSIDE the flag-dark branch only — refuses while dark), and stripe-treasury-adapter.ts (stub implementing the interface). Export the interface + types + mock + Stripe stub + dark live concrete from a seam barrel (packages/integrations/src/adapters/payout/index.ts) and re-export that barrel from the package barrel packages/integrations/src/index.ts — exactly as the tin-match seam is exported. Do NOT register these in adapters/register-all.ts (that file is the BaseAdapter provider-registry; the tin-match seam is not there, and neither is this). Turn the 88-01 modern-treasury scaffold green. NO SDK import at module top-level (so the package builds without the dep); webhook payloads `safeParse`d (no unsafe `as`). NO phase/D-ID breadcrumbs.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/integrations test -- modern-treasury 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/integrations test -- modern-treasury` GREEN.
    - PayoutInitiationAdapter interface + mock (default) + live concrete (dark) + Stripe stub all exist, are exported from the payout seam barrel + the package barrel (mirroring tin-match), and compile WITHOUT modern-treasury installed (no top-level SDK import).
    - The payout seam is NOT added to adapters/register-all.ts (mirrors tin-match; that file stays BaseAdapter-only).
    - Mock returns the documented payment_order shape + status enum; live concrete refuses while dark.
    - `pnpm typecheck --filter=@contractor-ops/integrations` passes.
  </acceptance_criteria>
  <done>Payout seam shipped mock-default + Stripe stub + dark live; exported via seam + package barrel (not register-all); builds with zero new deps; scaffold green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Plaid Identity client seam — interface + deterministic mock (advisory fail-open) + dark live concrete</name>
  <behavior>
    - verifyAccount(input) on the mock returns a deterministic status: VERIFIED / PENDING / FAILED + plaidAccountId.
    - An unverified (PENDING/FAILED) status produces an advisory warning, NEVER a thrown PRECONDITION_FAILED (fail-open, D-08; mirrors P84 USPS).
    - The live Plaid concrete refuses while the flag is dark / creds absent.
  </behavior>
  <read_first>
    - packages/integrations/src/adapters/tin-match/{tin-match-client.ts,mock-tin-match-client.ts} (seam + mock to mirror)
    - packages/integrations/src/adapters/tin-match/index.ts + packages/integrations/src/index.ts:22-29 (seam + package barrel export to mirror; the plaid seam is exported the same way, NOT registered in register-all.ts)
    - packages/integrations/src/services/credential-service.ts:22-83
    - packages/integrations/src/__tests__/plaid-adapter.test.ts (88-01 scaffold to turn green)
    - 88-RESEARCH.md US-PAY-05 + Pitfall 5 (advisory must not hard-block) + 88-PATTERNS.md adapters section
  </read_first>
  <action>
    Create packages/integrations/src/adapters/plaid/plaid-identity-client.ts with `export interface PlaidIdentityClient { verifyAccount(input): Promise<PlaidVerificationResult>; }` (result: { status: 'VERIFIED'|'PENDING'|'FAILED'; plaidAccountId: string | null; nameMatchScore?: number }), mock-plaid-identity-client.ts (deterministic, fixture-mapped default), and plaid-identity-client-live.ts (Link-token → public_token exchange → /auth/get + /identity/match; SDK lazily imported inside the dark branch only; refuses while dark). Export the interface + types + mock + dark live concrete from a seam barrel (packages/integrations/src/adapters/plaid/index.ts) and re-export from the package barrel packages/integrations/src/index.ts — mirroring tin-match; do NOT register in adapters/register-all.ts. The advisory mapping (status → warning, never block) lives in the consuming service/router, not the client. If a distinct Plaid live PENDING flag is needed (separate from payments.ach-payouts), register ONE new PENDING entry in flags-core.ts + V7_FLAG_KEYS (jurisdiction ANY, category payments) and add it to the signoff registry; otherwise reuse module.us-expansion gating only. Turn the 88-01 plaid scaffold green. NO top-level SDK import; safeParse external payloads. NO phase/D-ID breadcrumbs.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/integrations test -- plaid 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/integrations test -- plaid` GREEN.
    - PlaidIdentityClient interface + mock (default) + dark live concrete exist, exported via the plaid seam barrel + the package barrel (mirroring tin-match; not in register-all.ts); build WITHOUT plaid installed.
    - Mock returns VERIFIED/PENDING/FAILED; an unverified status NEVER throws (fail-open asserted in the test).
    - Any new Plaid flag (if added) is PENDING + in V7_FLAG_KEYS + the signoff registry; payments.ach-payouts NOT re-added.
    - `pnpm typecheck --filter=@contractor-ops/integrations` passes.
  </acceptance_criteria>
  <done>Plaid seam shipped mock-default + dark live; fail-open advisory; exported via seam + package barrel; builds with zero new deps; scaffold green.</done>
</task>

<task type="auto">
  <name>Task 4: Opt-in payout-init tRPC procedure (idempotent, audited, region+flag gated) + env-schema + per-item Plaid advisory at payout</name>
  <read_first>
    - packages/api/src/routers/finance/payment-export-router.ts:105-149 (runExportTransaction atomic updateMany no-double-pay) + :68-84 (writeAuditLog) + :192-195 (tenantProcedure + requirePermission)
    - packages/api/src/lib/idempotency.ts:90-160 (reserve/complete/clear) + packages/api/src/routers/finance/payment-shared.ts:32 (IDEMPOTENCY_TTL_SECONDS)
    - packages/api/src/middleware/require-us-expansion-flag.ts:29-58
    - packages/db/prisma/schema/payment.prisma:36-63 (PaymentRunItem — organizationId tenant scope + billingProfileId + billingProfile relation, the per-item join) and packages/db/prisma/schema/contractor.prisma:132-172 (ContractorBillingProfile — organizationId + the 88-02 plaidVerificationStatus field; relation is nullable on the item)
    - packages/api/src/services/payment-settlement.ts (88-05 resolveSettlementCurrency + convertForSettlement — the per-run settlement seam the input override feeds)
    - packages/integrations/.env.example + packages/integrations/src/env.ts (the env schema to extend) + CLAUDE.md env rule
    - 88-PATTERNS.md "Payout-init tRPC procedure" + "Idempotency on payout init" + "Feature flags (reuse)" sections
  </read_first>
  <action>
    Add an `initiatePayout` tenantProcedure to payment-core.ts gated by `requirePermission({ payment: ['export'] })`, `assertUsExpansionEnabled(organizationId, region)` (D-11), and the EXISTING `payments.ach-payouts` flag via `@contractor-ops/feature-flags` evaluate (opt-in per-org; dark by default — D-05). Zod `.strict()` input (server-derives amount/currency/withholding from the locked payment run — client supplies only the run id + idempotency key + provider choice + an optional per-run `settlementCurrency` override). Thread the `settlementCurrency` override into `resolveSettlementCurrency({ contractorCurrency, perRunOverride })` (88-05) and run `convertForSettlement` so the payout amount sent to the adapter is the settled amount (missing rate → surfaced error, never zeroed). Wrap the call with idempotency: `reserve` → on HIT return cached, on PENDING throw CONFLICT, else `initiatePayout` via the PayoutInitiationAdapter (mock default) then `complete`; `clear` on error (no double-pay, D-13). `writeAuditLog({ action:'payment_run.payout_initiated', resourceType:'PAYMENT_RUN', metadata: masked-only — last-4 account, never full routing/account })`.

    Per-item Plaid advisory (D-08, fail-open): load the run's items tenant-scoped (`where: { paymentRunId, organizationId }`) with the exact include `{ billingProfile: { select: { plaidVerificationStatus: true } } }` — read the status from `PaymentRunItem.billingProfile` (the per-item profile the run pays out to, via billingProfileId), NEVER from `contractor.billingProfiles[]`. For each item: when `item.billingProfile == null` OR `item.billingProfile.plaidVerificationStatus !== 'VERIFIED'`, push a per-item advisory warning into the result (and into the audit/compliance metadata) — it WARNS, it NEVER throws and NEVER blocks the payout (mirror P84 USPS). The query is tenant-scoped by `organizationId` on PaymentRunItem so no cross-tenant profile is ever read.

    Add `MODERN_TREASURY_ENCRYPTION_KEY` and `PLAID_ENCRYPTION_KEY` (+ optional provider API keys) to packages/integrations/.env.example and the package env.ts schema (optional / flag-dark — none required for GA); run `pnpm check:no-process-env`. NO console.* (logger); NO phase/D-ID breadcrumbs.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- payment 2>&1 | tail -20 && pnpm check:no-process-env 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `initiatePayout` procedure exists, gated by permission + assertUsExpansionEnabled + payments.ach-payouts flag (dark default).
    - Idempotent (reserve/complete/clear) — a duplicate retry returns the cached result / CONFLICT, never a second payout (test asserts no double-pay).
    - The per-run settlementCurrency override threads into resolveSettlementCurrency and the adapter receives the settled amount (88-05 wired here too).
    - writeAuditLog on payout init; metadata carries last-4 only, never full routing/account.
    - Plaid advisory is PER-ITEM, read via the tenant-scoped `PaymentRunItem.billingProfile.plaidVerificationStatus` include (NOT contractor.billingProfiles[]); an unverified or null-profile item surfaces an advisory warning in the result, NEVER blocks the payout (fail-open test asserts the payout still proceeds), and the query is organizationId-scoped (a tenant-isolation test asserts no cross-tenant profile read).
    - New env vars in .env.example + env.ts (optional); `pnpm check:no-process-env` + `pnpm typecheck --filter=@contractor-ops/api` + `pnpm lint:audit-log` pass.
  </acceptance_criteria>
  <done>Gated, idempotent, audited opt-in payout-init procedure wired; per-run settlement applied; per-item Plaid advisory via the precise tenant-scoped billingProfile join, non-blocking; env declared; flag reused not re-minted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → payout-init procedure | Untrusted run-id + idempotency key + settlementCurrency override cross here; amount/currency/withholding are server-derived from the locked run. |
| API → Modern Treasury / Plaid (live, dark) | Outbound provider calls (flag-dark); creds AES-256-GCM at rest. |
| npm install | [ASSUMED] SDKs cross the supply-chain boundary at install time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-06-01 | Tampering / Repudiation | double-pay on retry / multi-pod | mitigate | lib/idempotency.ts reserve/complete/clear (Upstash, 24h) on initiatePayout + the atomic updateMany status guard; writeAuditLog on init (D-13). |
| T-88-06-02 | Spoofing | unverified-account payout (fraud) | accept (advisory) | Plaid status surfaced as a per-item warning + audited (D-08, mirrors P84 USPS); hard-gate deferred to live Plaid creds — fail-open is the deliberate posture. |
| T-88-06-03 | Information Disclosure | bank PII in logs/audit | mitigate | Last-4-only in audit metadata; full routing/account AES-256-GCM at rest, never logged (D-12); mirror the tin-match metadata discipline. |
| T-88-06-04 | Spoofing | forged provider webhook (live path) | mitigate | safeParse the payload + verify the Modern Treasury webhook signature before acting; flag-dark until live; no unsafe `as`. |
| T-88-06-05 | Tampering | mass-assignment of amount/currency | mitigate | `.strict()` Zod DTO; server derives amount/currency/withholding/verification status from the locked run — client supplies only the run id + idempotency key + provider choice + settlementCurrency override. |
| T-88-06-06 | Information Disclosure / Elevation | cross-tenant payout / Plaid-status read | mitigate | tenantProcedure + organizationId on every query; the Plaid advisory reads the per-item `PaymentRunItem.billingProfile.plaidVerificationStatus` via a `where: { paymentRunId, organizationId }` include (never contractor.billingProfiles[]); add a *.security.test.ts tenant-isolation case (mirror tax-filing-tenant-isolation.security.test.ts). |
| T-88-06-SC | Tampering | npm install of modern-treasury / plaid | mitigate | Each [ASSUMED] SDK install gated behind the Task-1 blocking-human checkpoint (name/source-repo/release-age/typosquat; pnpm audit + security:scan); GA mock path installs none; SDKs lazily imported inside the dark branch only. @midlandsbank/node-nacha forbidden. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/integrations test -- modern-treasury plaid` GREEN; packages build WITHOUT the SDKs installed.
- `pnpm --filter @contractor-ops/api test -- payment` GREEN incl. no-double-pay + per-item Plaid-fail-open + tenant-isolation cases.
- `pnpm check:no-process-env`, `pnpm typecheck --filter=@contractor-ops/api`, `pnpm typecheck --filter=@contractor-ops/integrations`, `pnpm lint:audit-log` pass.
- `git diff -- '**/package.json'` shows no SDK dependency unless the Task-1 checkpoint explicitly approved an install (then `pnpm audit` + `pnpm security:scan` clean).
</verification>

<success_criteria>
- Opt-in programmatic ACH via the Modern Treasury mock (idempotent, audited, gated); Stripe stub on the same interface; live dark (US-PAY-03).
- Plaid Identity mock verification; an unverified per-item status warns via the precise tenant-scoped billingProfile join, never blocks (US-PAY-05).
- payments.ach-payouts reused (not re-minted); [ASSUMED] SDK installs gated behind a blocking human-verify; GA path zero new deps.
</success_criteria>

<output>
Create `.planning/phases/88-theme-a-us-payment-rail/88-06-SUMMARY.md` when done.
</output>
