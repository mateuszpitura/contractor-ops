---
phase: 88-theme-a-us-payment-rail
plan: 02
type: execute
wave: 2
depends_on: ["88-01"]
files_modified:
  - packages/db/prisma/schema/contractor.prisma
  - packages/db/prisma/schema/payment.prisma
autonomous: false
requirements: [US-PAY-01, US-PAY-04, US-PAY-05]
user_setup: []
must_haves:
  truths:
    - "Implements D-03 (Contractor.backupWithholdingFlagged column), D-08 (ContractorBillingProfile Plaid status fields), D-10 (ACH_NACHA + FEDWIRE on the Prisma PaymentExportFormat enum), D-12 (US routing/account encrypted+masked pairs)"
    - "Contractor.backupWithholdingFlagged is a queryable boolean column the payment-run seeding (88-03) reads at payout time"
    - "ContractorBillingProfile carries US routing/account encrypted+masked pairs mirroring the UK BACS pair, plus Plaid verification status fields"
    - "The PaymentExportFormat Prisma enum gains ACH_NACHA and FEDWIRE (UPPER_SNAKE_CASE, passes db:audit-enum-casing)"
    - "Migration applied across all regions (EU/ME/US) at a human gate before any consumer plan runs"
  artifacts:
    - path: "packages/db/prisma/schema/contractor.prisma"
      provides: "backupWithholdingFlagged + US bank fields + Plaid status fields"
      contains: "backupWithholdingFlagged"
    - path: "packages/db/prisma/schema/payment.prisma"
      provides: "ACH_NACHA + FEDWIRE enum members"
      contains: "ACH_NACHA"
  key_links:
    - from: "Contractor.backupWithholdingFlagged"
      to: "payment-shared.ts applyWithholding (88-03)"
      via: "Prisma select at item seeding"
      pattern: "backupWithholdingFlagged"
    - from: "ContractorBillingProfile.usRoutingNumberEncrypted"
      to: "generateNachaFile (88-04)"
      via: "decrypt-then-generate in the router"
      pattern: "usRoutingNumber|usAccountNumber"
---

<objective>
Add the additive Prisma schema for the US payment rail: the queryable backup-withholding flag (closes the P86 loose end), US routing/account encrypted+masked pairs, Plaid verification status fields, and the two new export-format enum members. Then apply the migration across all regions at a human gate.

Purpose: Every downstream plan (withholding deduction, NACHA generation, Plaid adapter) reads these columns/enums. They must exist and be migrated first.
Output: Updated schema files, regenerated Prisma client, and a multi-region migration applied at a human-verify gate.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/88-theme-a-us-payment-rail/88-CONTEXT.md
@.planning/phases/88-theme-a-us-payment-rail/88-RESEARCH.md

<interfaces>
<!-- Existing schema shapes to mirror. Extracted from codebase. -->

From packages/db/prisma/schema/contractor.prisma:
- `model Contractor` (line 9): `currency String @db.Char(3)` (line 19), `countryCode String @db.Char(2)` (line 18), advisory-flag analog `uspsVerified Boolean?` (line 46) + `uspsValidatedAt DateTime?` (line 47).
- `model ContractorBillingProfile` (line 132): `bankAccountMasked String?` / `bankAccountEncrypted String?` (143-144), `swiftBic String? @db.VarChar(11)` (146), and the UK encrypted+masked pair to mirror exactly: `ukSortCodeEncrypted/ukSortCodeMasked/ukAccountNumberEncrypted/ukAccountNumberMasked String?` (161-164).

From packages/db/prisma/schema/payment.prisma:
- `enum PaymentExportFormat { CSV BANK_FILE SEPA_XML SWIFT_XML MT940 XML API_PUSH BACS_STD18 }` (line 159).
- `model PaymentRunItem` WHT fields already exist (49-54): whtAmountMinor/whtRate/whtTreatyApplied/whtTreatyReference/whtServiceType/grossAmountMinor — NO migration needed for the deduction substrate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backup-withholding flag, US bank fields, and Plaid status fields to contractor.prisma</name>
  <read_first>
    - packages/db/prisma/schema/contractor.prisma:9-60 (Contractor model — uspsVerified/uspsValidatedAt advisory-flag analog)
    - packages/db/prisma/schema/contractor.prisma:132-170 (ContractorBillingProfile — bankAccount + UK encrypted/masked pair)
    - 88-PATTERNS.md "contractor.prisma" section (the exact field analogs to mirror)
  </read_first>
  <action>
    On model Contractor add `backupWithholdingFlagged Boolean?` (additive-nullable; stored against Contractor per D-03, re-pointed to Worker after Phase 89 — do NOT FK to Worker). On model ContractorBillingProfile add the US encrypted+masked bank pairs mirroring the UK pair exactly: `usRoutingNumberEncrypted String?`, `usRoutingNumberMasked String?`, `usAccountNumberEncrypted String?`, `usAccountNumberMasked String?` (AES-256-GCM at rest, masked for display — D-12; full values never logged). Also add the Plaid advisory fields: `plaidVerificationStatus String?` (values VERIFIED/PENDING/FAILED — keep as String to avoid a global-enum churn, mirroring the uspsVerified String/Boolean advisory posture), `plaidVerifiedAt DateTime?`, `plaidAccountId String?`. Comments: keep only real-domain WHY (AES-256-GCM); NO phase/D-ID breadcrumbs.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db exec prisma validate 2>&1 | grep -qiE "valid|schema is valid" && grep -q "backupWithholdingFlagged" packages/db/prisma/schema/contractor.prisma && grep -q "usRoutingNumberEncrypted" packages/db/prisma/schema/contractor.prisma && grep -q "plaidVerificationStatus" packages/db/prisma/schema/contractor.prisma && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `prisma validate` passes.
    - Contractor has `backupWithholdingFlagged Boolean?` (nullable, no Worker FK).
    - ContractorBillingProfile has usRoutingNumberEncrypted/Masked + usAccountNumberEncrypted/Masked + plaidVerificationStatus/plaidVerifiedAt/plaidAccountId, all nullable.
    - No new model added (so no globalModels / cross-org-leak-test obligation triggered).
  </acceptance_criteria>
  <done>contractor.prisma carries the flag, US bank pairs, and Plaid fields; schema validates.</done>
</task>

<task type="auto">
  <name>Task 2: Add ACH_NACHA and FEDWIRE to the PaymentExportFormat enum + regenerate client</name>
  <read_first>
    - packages/db/prisma/schema/payment.prisma:159-168 (enum PaymentExportFormat)
    - 88-PATTERNS.md "payment.prisma + contractor.prisma" section (enum is SEPARATE from the ExportFormat string union in 88-04)
  </read_first>
  <action>
    Add `ACH_NACHA` and `FEDWIRE` members to enum PaymentExportFormat (UPPER_SNAKE_CASE). Run `pnpm --filter @contractor-ops/db db:generate` to regenerate the prisma-client (the generated client is tracked in-repo per the P85 convention — commit it with the schema). Run `pnpm db:audit-enum-casing` for the new enum members.
  </action>
  <verify>
    <automated>grep -q "ACH_NACHA" packages/db/prisma/schema/payment.prisma && grep -q "FEDWIRE" packages/db/prisma/schema/payment.prisma && pnpm db:audit-enum-casing 2>&1 | grep -qiE "pass|ok|no .*issues|0 " && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - PaymentExportFormat enum contains ACH_NACHA and FEDWIRE.
    - `pnpm db:audit-enum-casing` passes (both members UPPER_SNAKE_CASE).
    - Prisma client regenerated and committed.
  </acceptance_criteria>
  <done>Enum carries the two US formats; casing audit green; client regenerated.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Apply the additive multi-region migration (EU/ME/US)</name>
  <action>
    Generate the additive migration for the Task-1 + Task-2 schema changes, then PAUSE for the human to review and apply it per region. The executor must NOT auto-apply to live regional databases — present the migration SQL and the per-region apply steps below, then wait for approval.
  </action>
  <what-built>
    Additive Prisma schema: Contractor.backupWithholdingFlagged, ContractorBillingProfile US routing/account encrypted+masked pairs + Plaid status fields, and ACH_NACHA + FEDWIRE on the PaymentExportFormat enum. All additive (nullable columns + enum additions) — no destructive change.
  </what-built>
  <how-to-verify>
    [BLOCKING] This migration mutates the live regional databases (EU/ME/US) and follows the P83–87 multi-region convention. Apply per region:
    1. Review the generated migration SQL — confirm only ADD COLUMN (nullable) + ALTER TYPE ... ADD VALUE statements (no DROP / NOT NULL without default / data loss).
    2. Apply to EU: `pnpm db:migrate:all` (or the project's per-region apply for DATABASE_URL_EU), then ME (DATABASE_URL_ME), then US (DATABASE_URL_US). The migration-history-drift fallback (`prisma db push` against each regional URL) is available as in P82–87 if `migrate dev` is drift-blocked.
    3. After each region: confirm the columns + enum values exist (`\d "ContractorBillingProfile"`, `\dT+ "PaymentExportFormat"`).
    4. Note any region intentionally deferred (e.g. US prod) consistent with prior-phase posture.
  </how-to-verify>
  <resume-signal>Type "approved" once the additive migration is applied (or explicitly deferred per region per the documented multi-region posture), or describe any issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → regional DB | New columns store bank PII (encrypted) and a withholding-decision flag; the migration touches live regional databases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-02-01 | Information Disclosure | usRoutingNumberEncrypted / usAccountNumberEncrypted | mitigate | Encrypted-at-rest columns only; the masked column is the only display surface; full values are AES-256-GCM encrypted by the writer (88-06) and never logged (D-12). |
| T-88-02-02 | Tampering | multi-region migration | mitigate | Additive-only SQL (nullable columns + enum ADD VALUE); reviewed at the blocking human gate before any regional apply; no NOT NULL / DROP. |
| T-88-02-03 | Information Disclosure / Elevation | backupWithholdingFlagged read | accept | Flag is a per-contractor boolean within the tenant; read at seeding is already organizationId-scoped by the existing payment-run query — no new cross-tenant surface. |
| T-88-SC | Tampering | npm install | mitigate | No package installed in this plan. |
</threat_model>

<verification>
- `prisma validate` passes.
- `pnpm db:audit-enum-casing` passes.
- Migration SQL is additive-only and applied (or explicitly deferred) per region at the human gate.
- `pnpm typecheck --filter=@contractor-ops/db` passes after client regeneration.
</verification>

<success_criteria>
- backupWithholdingFlagged, US bank pairs, and Plaid fields exist on the right models.
- ACH_NACHA + FEDWIRE on the Prisma enum, casing-audit green.
- Multi-region migration applied at a reviewed human gate.
</success_criteria>

<output>
Create `.planning/phases/88-theme-a-us-payment-rail/88-02-SUMMARY.md` when done.
</output>
