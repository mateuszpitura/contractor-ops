---
phase: 88-theme-a-us-payment-rail
plan: 07
type: execute
wave: 7
depends_on: ["88-02", "88-03", "88-04", "88-05", "88-06"]
files_modified:
  - .planning/brain/wiki/domains/us-payment-rail.md
  - .planning/brain/wiki/structure/api-routers-catalog.md
  - .planning/brain/wiki/structure/prisma-schema-areas.md
  - .planning/brain/wiki/integrations/modern-treasury.md
  - .planning/brain/wiki/integrations/plaid.md
  - .planning/brain/wiki/patterns/money-rounding.md
  - .planning/brain/wiki/patterns/feature-flags.md
  - .planning/brain/wiki/log.md
  - .planning/brain/wiki/hot.md
  - .planning/MEMORY.md
autonomous: true
requirements: [US-PAY-01, US-PAY-02, US-PAY-03, US-PAY-04, US-PAY-05]
must_haves:
  truths:
    - "Implements the documentation-follows-code mandate (D-14 + CLAUDE.md): every Phase-88 product change has a matching wiki + index + MEMORY entry in the same milestone, and check:wiki-brain passes"
    - "A US-payment-rail domain page documents the NACHA/Fedwire formats, the generalized withholding deduction (the payment run as the single source of truth — D-02), USD/settlement currency, and the programmatic-ACH + Plaid seams"
    - ".planning/MEMORY.md records the 'payment run is the withholding source of truth' invariant"
    - "Real domain IDs only in docs (NACHA/PPD/CCD/CTX/§3406/pacs.008); no planning-ID breadcrumbs in source"
  artifacts:
    - path: ".planning/brain/wiki/domains/us-payment-rail.md"
      provides: "US payout-rail domain page (Purpose/Flow/Entry points/UI surface/Agent mistakes)"
      contains: "withholding source of truth"
    - path: ".planning/MEMORY.md"
      provides: "payment-run-is-withholding-source-of-truth invariant"
      contains: "source of truth"
  key_links:
    - from: "wiki/domains/us-payment-rail.md"
      to: "payment-export.ts / payment-shared.ts / the adapters (verify_with)"
      via: "verify_with frontmatter paths"
      pattern: "verify_with"
---

<objective>
Update the wiki + indexes + MEMORY in the same milestone as the Phase-88 code, per the documentation-follows-code mandate. Stale docs cause the next agent to hallucinate; this closes the loop.

Purpose: D-14 + CLAUDE.md "documentation follows code" — the codebase wiki tracks the code. Capture the US payment rail, the generalized withholding deduction (payment run = source of truth), the new formats, and the adapter seams.
Output: A US-payment-rail domain page, catalog/schema/integration/pattern updates, log/hot refresh, the MEMORY invariant, and a green `pnpm check:wiki-brain`.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/88-theme-a-us-payment-rail/88-CONTEXT.md
@.planning/phases/88-theme-a-us-payment-rail/88-RESEARCH.md
@.planning/phases/88-theme-a-us-payment-rail/88-PATTERNS.md
@.planning/brain/wiki/index.md
@.planning/brain/wiki/meta/refresh-triggers.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author/extend the wiki pages for the US payment rail</name>
  <read_first>
    - .planning/brain/wiki/index.md (page conventions + frontmatter shape)
    - .planning/brain/wiki/meta/refresh-triggers.md (the trigger matrix for which pages each change type touches)
    - an existing domain page under .planning/brain/wiki/domains/ (e.g. a payments or contractors-engagements page) for the Purpose/Flow/Entry-points/UI-surface/Agent-mistakes shape + verify_with frontmatter
    - an existing integrations page under .planning/brain/wiki/integrations/ for the provider-page shape
    - the actual Phase-88 source files produced by 88-02..88-06 (payment-export.ts, payment-format-detection.ts, payment-shared.ts, payment-settlement.ts, tin-match.service.ts, the payout + plaid adapters, contractor.prisma, payment.prisma) — document what shipped, not the plan
  </read_first>
  <action>
    Create .planning/brain/wiki/domains/us-payment-rail.md (Purpose, Flow, Entry points, UI surface, Agent mistakes) covering: the ACH_NACHA hand-rolled generator + Fedwire pacs.008 in the payment-export factory; detectFormat USD+US-bank→ACH_NACHA / above-Same-Day-ACH-ceiling→FEDWIRE (ceiling is config not constant); the generalized withholding deduction (SA + US 24% §3406 + 1042-S treaty) at item seeding with the PAYMENT RUN AS THE SINGLE SOURCE OF TRUTH that 1099 box-4 / 1042-S box-2 aggregate (D-02); USD first-class + settlement-currency choice; the Modern Treasury PayoutInitiationAdapter + Plaid Identity seams (mock-default, flag-dark, Plaid fail-open advisory). Set verify_with frontmatter to the real shipped source paths. Update: structure/api-routers-catalog.md (new payout-init procedure + any namespace), structure/prisma-schema-areas.md (Contractor.backupWithholdingFlagged + ContractorBillingProfile US bank/Plaid fields + PaymentExportFormat ACH_NACHA/FEDWIRE), integrations/modern-treasury.md + integrations/plaid.md (provider pages: mock-behind-seam, flag-dark, env keys), patterns/money-rounding.md (the single-HALF-UP withholding + settlement-FX round), patterns/feature-flags.md (payments.ach-payouts REUSED for programmatic ACH — not re-minted; any new Plaid PENDING flag). Real domain IDs only (NACHA/PPD/CCD/CTX/§3406/pacs.008/Fedwire); no operational/round-status text (durable facts only).
  </action>
  <verify>
    <automated>test -f .planning/brain/wiki/domains/us-payment-rail.md && grep -qi "source of truth" .planning/brain/wiki/domains/us-payment-rail.md && grep -qi "ACH_NACHA\|NACHA" .planning/brain/wiki/structure/prisma-schema-areas.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - us-payment-rail.md exists with Purpose/Flow/Entry-points/UI-surface/Agent-mistakes + verify_with frontmatter pointing at the real shipped source.
    - api-routers-catalog.md, prisma-schema-areas.md, modern-treasury.md, plaid.md, money-rounding.md, feature-flags.md all reflect the Phase-88 changes.
    - Pages contain durable facts only (no "round in progress"/status text); real domain IDs only.
  </acceptance_criteria>
  <done>Wiki pages created/extended to track the Phase-88 code; verify_with paths set.</done>
</task>

<task type="auto">
  <name>Task 2: Record the MEMORY invariant, refresh log/hot, and run the wiki-brain pipeline</name>
  <read_first>
    - .planning/MEMORY.md (invariant entry shape)
    - .planning/brain/wiki/log.md + .planning/brain/wiki/hot.md (how entries/overwrites are recorded)
    - CLAUDE.md "Documentation follows code" section (the exact pipeline: bump source_commit, rebuild BM25, check:wiki-brain)
  </read_first>
  <action>
    Append to .planning/MEMORY.md the invariant: "Payment run is the single source of truth for withheld amount — the recorded PaymentRunItem.whtAmountMinor is authoritative; 1099 box-4 / 1042-S box-2 aggregate the year's actual payment-run withholding (never recompute in the forms); one jurisdiction-agnostic withholding path (SA + US 24% §3406 + 1042-S treaty); NACHA hand-rolled (zero deps); programmatic ACH + Plaid are mock-behind-seam, flag-dark; Plaid advisory fail-open." Add a wiki/log.md entry for the Phase-88 documentation pass and overwrite wiki/hot.md per the pipeline. Bump source_commit on the wiki frontmatter touched. Then run the documentation pipeline from CLAUDE.md: rebuild the BM25 index + the contextual-prefix pass under .planning/brain, and run `pnpm check:wiki-brain` until green (no NEW doc drift vs the branch base for the Phase-88 source files).
  </action>
  <verify>
    <automated>grep -qi "source of truth" .planning/MEMORY.md && pnpm check:wiki-brain 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - .planning/MEMORY.md contains the payment-run-source-of-truth invariant.
    - wiki/log.md has a Phase-88 entry; wiki/hot.md overwritten; source_commit bumped on touched pages.
    - `pnpm check:wiki-brain` passes (no NEW drift for the Phase-88 source files).
  </acceptance_criteria>
  <done>MEMORY invariant recorded, log/hot refreshed, BM25 rebuilt, check:wiki-brain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only plan; no runtime trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-07-01 | Information Disclosure | wiki examples | mitigate | Use only synthetic/masked routing/account examples in docs; never a real or full bank number; document the masked-only / AES-256-GCM discipline. |
| T-88-SC | Tampering | npm install | mitigate | No package installed in this plan. |
</threat_model>

<verification>
- `pnpm check:wiki-brain` passes.
- us-payment-rail.md + the catalog/schema/integration/pattern pages reflect shipped code; MEMORY invariant present.
</verification>

<success_criteria>
- The wiki tracks the Phase-88 code (domain/structure/integrations/money-rounding/feature-flags + log/hot).
- MEMORY records the payment-run-source-of-truth invariant.
- check:wiki-brain green.
</success_criteria>

<output>
Create `.planning/phases/88-theme-a-us-payment-rail/88-07-SUMMARY.md` when done.
</output>
