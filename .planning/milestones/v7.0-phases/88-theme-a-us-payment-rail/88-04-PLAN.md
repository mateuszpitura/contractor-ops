---
phase: 88-theme-a-us-payment-rail
plan: 04
type: execute
wave: 5
depends_on: ["88-01", "88-02", "88-03", "88-05"]
files_modified:
  - packages/api/src/services/payment-export.ts
  - packages/api/src/services/payment-format-detection.ts
  - packages/api/src/routers/finance/payment-shared.ts
autonomous: true
requirements: [US-PAY-01, US-PAY-04]
must_haves:
  truths:
    - "Implements D-10 (new ACH_NACHA + FEDWIRE formats in the payment-export factory: hand-rolled NACHA fixed-width + Fedwire ISO 20022 pacs.008; both added to the ExportFormat string union; detectFormat routes USD + US bank → ACH_NACHA and high-value above the Same-Day ACH ceiling → FEDWIRE with the ceiling as a config value not a constant)"
    - "A payment run exports a NACHA file: 94-char records, record-type order 1/5/6/8/9, correct entry hash, balanced control totals, 10-line block padding, SEC=PPD + service class 220 + txn code 22 defaults"
    - "A Fedwire pacs.008.001.xx XML exports for amounts above the Same-Day ACH ceiling (config: $1M now, $10M from 2027-09-17 — NOT a constant)"
    - "NACHA is hand-rolled with ZERO new dependencies (mirrors generateBacsStandard18)"
    - "The settlement conversion (D-07) runs on the file-export path: before the NACHA/Fedwire generator emits a record, resolveSettlementCurrency + convertForSettlement (88-05) have set each item's export amount/currency — the export file carries the settled amount, not the raw run amount"
  artifacts:
    - path: "packages/api/src/services/payment-export.ts"
      provides: "generateNachaFile + generateFedwirePacs008"
      contains: "generateNachaFile"
    - path: "packages/api/src/services/payment-format-detection.ts"
      provides: "ACH_NACHA + FEDWIRE on the ExportFormat union + detectUsFormat routing"
      contains: "ACH_NACHA"
    - path: "packages/api/src/routers/finance/payment-shared.ts"
      provides: "_generateExportFileForFormat ACH_NACHA + FEDWIRE branches + _buildExportItems settlement wiring (resolveSettlementCurrency + convertForSettlement on the export path)"
      contains: "convertForSettlement"
  key_links:
    - from: "payment-shared.ts _generateExportFileForFormat"
      to: "payment-export.ts generateNachaFile / generateFedwirePacs008"
      via: "format dispatch branch"
      pattern: "generateNachaFile|generateFedwirePacs008"
    - from: "payment-format-detection.ts detectUsFormat"
      to: "the Same-Day ACH ceiling config value"
      via: "amountMinor > sameDayCeilingMinor ? FEDWIRE : ACH_NACHA"
      pattern: "sameDayCeiling|FEDWIRE"
    - from: "payment-shared.ts _buildExportItems (export/seeding call site)"
      to: "payment-settlement.ts resolveSettlementCurrency + convertForSettlement"
      via: "settle each item amount/currency before the export buffer is generated"
      pattern: "resolveSettlementCurrency|convertForSettlement"
---

<objective>
Add the two US export-format generators to the payment-export factory — a hand-rolled NACHA fixed-width file (mirroring `generateBacsStandard18`, zero deps) and a Fedwire ISO 20022 pacs.008 XML (mirroring `generateSwiftXml`) — plus the `detectFormat` routing that selects them, the dispatch branch that emits them, and the wiring that runs the 88-05 settlement conversion on the export path so the file carries the settled amount.

Purpose: US-PAY-01 (NACHA file as a factory format) + US-PAY-04 (Fedwire for high-value above the Same-Day ACH ceiling); plus closing the D-07 settlement seam on the file-export path (the 88-05 service is consumed here, not left as dead code). The file export is the always-available GA default (D-05) — works with no provider creds.
Output: `generateNachaFile` + `generateFedwirePacs008`, the extended `ExportFormat` union + `detectUsFormat` rule, the `_generateExportFileForFormat` branches, and the `_buildExportItems` settlement call. The 88-01 NACHA + Fedwire scaffolds turn green.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/88-theme-a-us-payment-rail/88-CONTEXT.md
@.planning/phases/88-theme-a-us-payment-rail/88-RESEARCH.md
@.planning/phases/88-theme-a-us-payment-rail/88-PATTERNS.md

<interfaces>
<!-- Exact analog signatures. Extracted from codebase. -->

From packages/api/src/services/payment-export.ts:
- `generateBacsStandard18(items: BacsExportItem[], orgBank: BacsOrgBankInfo, _runRef: string, processingDate: Date): BacsGenerateResult` (513) — the fixed-width template. Reuse helpers `padField(value,len)` (471), `padZero(value,len)` (476). Pattern to copy: per-item amount-overflow throw (549), `totalAmount` accumulator, `detail.length !== RECORD_LEN` hard guard (591), `Buffer.from(allLines.join('\r\n'), 'ascii')` (608), `BacsGenerateResult = { fileBuffer; ext:'txt'; transliterationWarnings; modulusWarnings }`.
- `generateSwiftXml(items: ExportItem[], org: OrgBankInfo, runNumber: string): Buffer` (305) — pain.001.001.09 ISO 20022. Reuse `escapeXml` (105), `minorToDecimal(minor,currency)` (98), `items.reduce((s,i)=>s+i.amountMinor,0)`, the GrpHdr MsgId/CreDtTm/NbOfTxs/CtrlSum + per-item CdtTrfTxInf shape.
- `ExportItem` and `OrgBankInfo` exported from this module.

From packages/api/src/services/payment-format-detection.ts:
- `export type ExportFormat = 'SEPA_XML' | 'SWIFT_XML' | 'BANK_FILE' | 'CSV' | 'BACS_STD18'` (66) — add 'ACH_NACHA' | 'FEDWIRE'.
- `detectFormatForDestination(currency, destination)` (125) — the "check the specific rail first" ordering to mirror.

From packages/api/src/routers/finance/payment-shared.ts:
- `_generateExportFileForFormat(format, exportItems, orgBank, runRef)` (187-203) — dispatch: CSV / BANK_FILE / SWIFT_XML / (default) SEPA. Add ACH_NACHA + FEDWIRE branches. (Note: BACS routes through its own dedicated bacs.ts router because of the richer warnings result; NACHA's warnings can be aggregated the same way — return a sibling result and surface warnings, do not silently drop them.)
- `_buildExportItems(items, transferTitleTemplate): ExportItem[]` (248-277) — maps each run item's `amountMinor`/`currency` into the `ExportItem` the export buffer carries. THIS is the export/seeding call site where settlement must be applied. Called from payment-export-router.ts:303 immediately before `_generateExportFileForFormat` (:304).

From packages/api/src/services/payment-settlement.ts (created by 88-05 — this plan depends_on 88-05):
- `resolveSettlementCurrency({ contractorCurrency, perRunOverride? }): string` — returns the per-run override when set, else the contractor's currency (D-07 default).
- `convertForSettlement(db, amountMinor, fromCurrency, settlementCurrency, paymentDate): Promise<{ amountMinor: number; rate: number; rateDate: Date } | null>` — same-currency → unchanged (rate 1); else delegates to convertAmount at the payment-date ECB rate; null on missing rate (caller surfaces, never silently zeros).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hand-roll generateNachaFile (mirror generateBacsStandard18, zero deps)</name>
  <behavior>
    - Output is ASCII, CRLF-joined, every record exactly 94 chars.
    - Record order: 1 (file header) → 5 (batch header) → 6 (entry detail, one per item) → 8 (batch control) → 9 (file control), then all-9 padding records to make total line count a multiple of 10.
    - Batch-control entry hash = Σ(first 8 digits of each RDFI routing number) mod 10^10.
    - Total credit = Σ amountMinor (cents, no decimal); batch + file control totals agree.
    - Defaults: service class 220 (credits-only), SEC code PPD, transaction code 22 (checking credit); amounts right-justified zero-filled; alphanumeric left-justified space-filled.
    - Amount overflow (>= the cents field max) throws, like BACS.
  </behavior>
  <read_first>
    - packages/api/src/services/payment-export.ts:389-679 (generateBacsStandard18 + buildDetailRecord — the exact scaffold to mirror)
    - 88-RESEARCH.md Pattern 2 (record-field layout, entry hash, blocking, SEC/txn codes) + Pitfall 1 (balance/hash/blocking rejects)
    - packages/api/src/services/__tests__/payment-export-nacha.test.ts (the 88-01 golden-file scaffold to turn green)
  </read_first>
  <action>
    Add `generateNachaFile(items, orgBank, processingDate)` to payment-export.ts. Define `NACHA_RECORD_LEN = 94` and a `NachaExportItem` (contractor name, RDFI routing number, DFI account number, amountMinor) + `NachaOrgBankInfo` (immediate destination ODFI routing, immediate origin company ID, company name, ODFI identification — hand-set fields). Build record types 1/5/6/8/9 with per-record `padField`/`padZero` builders, each guarded `record.length !== NACHA_RECORD_LEN` → throw (mirror the BACS hard guard). Compute the entry hash as Σ first-8-digits of each RDFI routing, mod 10^10. Accumulate total credit in cents. After the 9 file-control record, append all-`9` filler records until the total line count is a multiple of 10 (blocking factor 10). Default service class 220, SEC=PPD, transaction code 22; expose SEC/txn as parameters defaulting to those values (Claude's-discretion per CONTEXT — PPD individual / CCD business / CTX). Return a `NachaGenerateResult` sibling of BacsGenerateResult: `{ fileBuffer: Buffer; ext: 'txt'; warnings: [...] }`, aggregating any transliteration/field-truncation warnings. Default unbalanced credit-only (RESEARCH A3 — adviser/ODFI verify; annotate "balanced-vs-unbalanced is ODFI-dependent — verify against the org bank ACH spec before live use"). Real domain IDs in comments (PPD/CCD/CTX, R01, service class 220, txn 22/32) — NO phase/D-ID breadcrumbs. Install NO dependency.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- payment-export-nacha 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test -- payment-export-nacha` GREEN (the 88-01 golden-file scaffold passes).
    - Every record is 94 chars; record-type order 1/5/6/8/9 + 9-record block padding to a multiple of 10.
    - Entry hash matches Σ first-8-digit RDFI routing mod 10^10; control totals agree.
    - SEC=PPD, service class 220, txn 22 defaults; SEC/txn parameterizable.
    - `git diff -- '**/package.json'` shows NO new dependency.
    - `pnpm typecheck --filter=@contractor-ops/api` passes.
  </acceptance_criteria>
  <done>Hand-rolled NACHA generator passes the golden-file scaffold; zero new deps; balancing posture annotated adviser-verify.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generateFedwirePacs008 + extend the ExportFormat union, detectFormat routing, the dispatch branch, and wire the 88-05 settlement conversion on the export path</name>
  <behavior>
    - generateFedwirePacs008 emits a pacs.008.001.xx Document XML with GrpHdr (MsgId/CreDtTm/NbOfTxs/CtrlSum) + one CdtTrfTxInf per item; control sum = Σ amountMinor.
    - detectUsFormat(currency, isUsBank, amountMinor, sameDayCeilingMinor): returns null unless currency==='USD' && isUsBank; else amountMinor > sameDayCeilingMinor ? 'FEDWIRE' : 'ACH_NACHA'.
    - The ceiling is a config value (default 1_000_000_00 cents now; 10_000_000_00 from 2027-09-17) — NOT a hardcoded constant inside detect.
    - _generateExportFileForFormat returns the NACHA buffer (ext 'txt') for ACH_NACHA and the Fedwire XML (ext 'xml') for FEDWIRE.
    - _buildExportItems applies settlement BEFORE the export buffer is built: for each run item it resolves the settlement currency (resolveSettlementCurrency — default contractor currency, per-run override honored) and runs convertForSettlement at the payment-date rate, so the ExportItem carries the settled amount/currency (not the raw run amount). A missing rate (convertForSettlement → null) surfaces an error / warning, never settles a zeroed amount.
  </behavior>
  <read_first>
    - packages/api/src/services/payment-export.ts:300-366 (generateSwiftXml — the pacs.008 mirror)
    - packages/api/src/services/payment-format-detection.ts:66 (ExportFormat union) + :125-144 (detectFormatForDestination ordering)
    - packages/api/src/routers/finance/payment-shared.ts:187-203 (_generateExportFileForFormat) + :248-277 (_buildExportItems — the export/seeding call site to wire settlement into) + payment-export-router.ts:303-305 (call order: _buildExportItems then _generateExportFileForFormat)
    - packages/api/src/services/payment-settlement.ts (88-05 output: resolveSettlementCurrency + convertForSettlement — the functions to call; do NOT reimplement FX/settlement)
    - 88-RESEARCH.md Pattern 3 + Code Examples (detectUsFormat, ceiling as config) + 88-PATTERNS.md "generateFedwirePacs008" + "ACH_NACHA / FEDWIRE routing" + "exchange-rate.ts → USD settlement" sections
    - packages/api/src/services/__tests__/payment-export-fedwire.test.ts (88-01 scaffold to turn green) + packages/api/src/services/__tests__/payment-format-detection.test.ts (extend)
  </read_first>
  <action>
    Add `generateFedwirePacs008(items, org, runNumber)` to payment-export.ts mirroring generateSwiftXml but with the pacs.008.001.xx message type + the ISO 20022 Business Application Header envelope; annotate "adviser-verify — live FedLine transmission is a bank channel (deferred); this export is the pacs.008 the operator hands to their bank; FAIM retired 2025-07-14". Add 'ACH_NACHA' | 'FEDWIRE' to the `ExportFormat` string union in payment-format-detection.ts and add `detectUsFormat(currency, isUsBank, amountMinor, sameDayCeilingMinor)` returning null / 'FEDWIRE' / 'ACH_NACHA' per the behavior; take the ceiling as a parameter sourced from a config value (define `SAME_DAY_ACH_CEILING_MINOR` config with the current $1M and a documented $10M-from-2027-09-17 note — NOT inlined). Add ACH_NACHA + FEDWIRE branches to `_generateExportFileForFormat` (ext 'txt' / 'xml'); for ACH_NACHA, surface the NachaGenerateResult warnings to the caller (do not drop). Wire settlement into `_buildExportItems` (payment-shared.ts): for each run item, call `resolveSettlementCurrency({ contractorCurrency, perRunOverride })` (the override threads from the export-router input / the locked run; default contractor currency) then `convertForSettlement(tx, item.amountMinor, item.currency, settlementCurrency, paymentDate)` — use the returned `amountMinor` + the settlement currency for the emitted ExportItem so the NACHA/Fedwire/SEPA buffer carries the settled value. On `convertForSettlement` → null, throw/surface a TRPCError (mirror the missing-rate-not-zero discipline, Pitfall 3) — never emit a zeroed amount. Reuse 88-05's functions verbatim; do NOT reimplement FX. NO phase/D-ID breadcrumbs; pacs.008 / Fedwire are real domain IDs.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- payment-export-fedwire payment-format-detection payment-export 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test -- payment-export-fedwire payment-format-detection` GREEN.
    - ExportFormat union includes 'ACH_NACHA' and 'FEDWIRE'.
    - detectUsFormat routes USD+US bank → ACH_NACHA and above-ceiling → FEDWIRE, with the ceiling supplied as config (test asserts the threshold flips at the boundary).
    - _generateExportFileForFormat emits the NACHA buffer for ACH_NACHA and the pacs.008 XML for FEDWIRE.
    - The settlement conversion is INVOKED on the export path: `grep -nE 'resolveSettlementCurrency|convertForSettlement' packages/api/src/routers/finance/payment-shared.ts` returns a match inside `_buildExportItems`; a behavior test asserts an item with a settlement currency different from the run currency exports the converted amount (not the raw amount), and that a missing rate surfaces an error rather than a zeroed amount.
    - `pnpm typecheck --filter=@contractor-ops/api` passes.
  </acceptance_criteria>
  <done>Fedwire pacs.008 generator + US detect routing + dispatch branches in place; the 88-05 settlement conversion is wired into the export call site (no longer dead code); both 88-01 scaffolds green; ceiling is config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payment-export factory → downloadable bank file | The NACHA file is the only artifact carrying full account/routing numbers; it is generated server-side from decrypted-in-router values. |
| settlement input → export amount | The settlement currency choice + FX rate determine the money the export file carries; both are server-derived via 88-05's convertForSettlement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-04-01 | Information Disclosure | NACHA file with full account/routing | mitigate | Generators receive already-decrypted values from the router; the generator never logs full account/routing; only the masked column is a display surface (D-12). File download is gated + audited by the existing lockAndExport path (88-06 wiring). |
| T-88-04-02 | Tampering | NACHA balance/hash/blocking | mitigate | Hard length guard per record (94-char), entry-hash + control-total formulas unit-tested against a golden fixture (Pitfall 1); operator-reject conditions covered. |
| T-88-04-03 | Tampering | Same-Day ACH routing threshold drift | mitigate | Ceiling is a config value (not a constant) with the $10M-from-2027-09-17 change documented; detect routing flips on the config boundary. |
| T-88-04-04 | Tampering | settlement amount / missing-rate silent zero on the export path | mitigate | _buildExportItems calls convertForSettlement (88-05); a missing rate returns null → surfaced as an error, never a zeroed export amount (Pitfall 3); single HALF-UP round delegated to convertAmount, never chained. |
| T-88-SC | Tampering | npm install (NACHA dep) | mitigate | NACHA hand-rolled — ZERO new dependency. `@midlandsbank/node-nacha` explicitly NOT installed (no exposed source repo). |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- payment-export-nacha payment-export-fedwire payment-format-detection payment-export` all GREEN.
- The settlement conversion is invoked on the export path (`grep -nE 'resolveSettlementCurrency|convertForSettlement' packages/api/src/routers/finance/payment-shared.ts` matches inside `_buildExportItems`).
- `git diff -- '**/package.json'` shows no new dependency.
- `pnpm typecheck --filter=@contractor-ops/api` passes.
</verification>

<success_criteria>
- NACHA file generates with valid records/hash/totals/blocking (US-PAY-01); zero new deps.
- Fedwire pacs.008 exports above the config ceiling (US-PAY-04).
- detectFormat routes USD+US bank → ACH_NACHA / above-ceiling → FEDWIRE; dispatch emits both.
- The export file carries the settled amount/currency (D-07): the 88-05 settlement service is invoked in _buildExportItems, not left as dead code.
</success_criteria>

<output>
Create `.planning/phases/88-theme-a-us-payment-rail/88-04-SUMMARY.md` when done.
</output>
