---
phase: 88-theme-a-us-payment-rail
plan: 12
type: execute
wave: 12
depends_on: ["88-11"]
gap_closure: true
autonomous: true
requirements: [US-PAY-05]
files_modified:
  - packages/api/src/routers/finance/payment-core.ts
  - packages/api/src/routers/finance/__tests__/payment-plaid-onboarding.test.ts
  - packages/api/src/errors.ts
  - .planning/brain/wiki/integrations/plaid.md
  - .planning/brain/wiki/domains/us-payment-rail.md
  - .planning/MEMORY.md
  - .planning/phases/88-theme-a-us-payment-rail/deferred-items.md

must_haves:
  truths:
    - "A US contractor billing profile can be verified via Plaid Identity through a reachable, mock-triggerable tRPC procedure that writes ContractorBillingProfile.plaidVerificationStatus (+ plaidVerifiedAt, plaidAccountId)."
    - "The verification is advisory + fail-open: it writes the status and never throws/blocks, and the payout advisory read now has a real non-null status to differentiate."
    - "The tin-match backup-withholding writer's non-wiring is an explicitly RECORDED defer to Phase 86 (the year-end batch owner), with the seam documented — not a silent omission."
  artifacts:
    - path: "packages/api/src/routers/finance/payment-core.ts"
      provides: "verifyBillingProfilePlaid tRPC mutation — the onboarding verification trigger that persists plaidVerificationStatus."
      contains: "plaidVerificationStatus"
    - path: ".planning/phases/88-theme-a-us-payment-rail/deferred-items.md"
      provides: "Recorded defer of the tin-match backupWithholdingFlagged writer to Phase 86."
      contains: "tin-match"
  key_links:
    - from: "paymentCore.verifyBillingProfilePlaid"
      to: "MockPlaidIdentityClient.verify → ContractorBillingProfile.plaidVerificationStatus"
      via: "verify + tenant-scoped update + audit"
      pattern: "MockPlaidIdentityClient"
    - from: "ContractorBillingProfile.plaidVerificationStatus"
      to: "_initiatePayoutForRun per-item advisory"
      via: "persisted status the payout path already reads"
      pattern: "plaidVerificationStatus"
---

<objective>
Close the two secondary verifier WARNINGs with explicit, recorded decisions:
1. WIRE a minimal, mock-triggerable Plaid onboarding verification procedure that writes ContractorBillingProfile.plaidVerificationStatus — so the "verifiable at onboarding" half of ROADMAP SC#5 (US-PAY-05) is real and the payout advisory read stops always taking the unverified branch.
2. RECORD an explicit defer of the tin-match backup-withholding writer (createBackupWithholdingFlagWriter / createDbTinMatchPersistence) to Phase 86 — the phase that owns the year-end batch trigger — with the seam documented, not omitted.

Purpose: The advisory-read/fail-open half of Plaid is already solid; only the write/trigger half was missing. Wiring a small mock-triggerable verification closes the gap properly rather than deferring it. The tin-match writer belongs to P86's trigger, so a documented defer (not a wire) is the correct, honest disposition.
Output: verifyBillingProfilePlaid procedure + reachability test; recorded tin-match defer; Plaid + gap-closure documentation sweep.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/88-theme-a-us-payment-rail/88-CONTEXT.md
@.planning/phases/88-theme-a-us-payment-rail/88-06-SUMMARY.md
@.planning/phases/88-theme-a-us-payment-rail/88-VERIFICATION.md
@.planning/phases/88-theme-a-us-payment-rail/deferred-items.md
@CLAUDE.md

@packages/api/src/routers/finance/payment-core.ts
@packages/integrations/src/adapters/plaid/plaid-identity-client.ts
@packages/integrations/src/adapters/plaid/mock-plaid-identity-client.ts

<interfaces>
<!-- What the onboarding trigger wires. -->

packages/integrations/src/adapters/plaid/ (from 88-06, exported via @contractor-ops/integrations barrel):
  class MockPlaidIdentityClient implements PlaidIdentityClient { verify(input: PlaidVerifyInput): Promise<PlaidVerificationResult> }
  PlaidVerifyInput { accountId: string; legalName: string; routingNumber: string; accountNumber: string }
  PlaidVerificationResult { status: 'VERIFIED'|'PENDING'|'FAILED'; advisoryWarning?: string; plaidAccountId?: string | null; nameMatchScore?: number }

packages/db ContractorBillingProfile (from 88-02): plaidVerificationStatus String?, plaidVerifiedAt DateTime?, plaidAccountId String?,
  usRoutingNumberMasked String?, usAccountNumberMasked String?, contractor.legalName

Gate pattern to mirror (payment-core.ts initiatePayout): assertUsExpansionEnabled(ctx.organizationId, ctx.region) + requirePermission({payment:['export']}) + tenant scope + writeAuditLog.

Consumer that already reads the status (do NOT change): _initiatePayoutForRun (payment-shared.ts:740) reads billingProfile.plaidVerificationStatus per item as a fail-open advisory.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire a mock-triggerable Plaid onboarding verification (US-PAY-05 write half)</name>
  <read_first>
    packages/api/src/routers/finance/payment-core.ts (initiatePayout gating + audit shape :364-406; imports + resolvePayoutAdapter pattern :1-44),
    packages/integrations/src/adapters/plaid/mock-plaid-identity-client.ts (deterministic verify),
    packages/integrations/src/adapters/plaid/plaid-identity-client.ts (PlaidVerifyInput / PlaidVerificationResult),
    packages/api/src/routers/finance/__tests__/payment-payout-init.test.ts (fake-db + caller harness to mirror),
    packages/db/prisma/schema/contractor.prisma (ContractorBillingProfile Plaid + US-masked fields)
  </read_first>
  <behavior>
    - verifyBillingProfilePlaid(billingProfileId) loads the tenant-scoped ContractorBillingProfile (+ contractor.legalName), calls MockPlaidIdentityClient.verify with the profile's masked US routing/account + legalName + an accountId (existing plaidAccountId or a derived deterministic id), and persists plaidVerificationStatus + plaidVerifiedAt (now) + plaidAccountId from the result.
    - Fail-open: whatever status the verify returns (VERIFIED/PENDING/FAILED) is written; the procedure NEVER throws on a non-VERIFIED result and never blocks — it returns { status, advisoryWarning? }.
    - Gated: assertUsExpansionEnabled(ctx.organizationId, ctx.region) + requirePermission (the payment:export gate, mirroring the sibling US-payment-rail mutations on this router) + .strict() Zod input.
    - Tenant-scoped: a billing profile in another org is NOT found (NOT_FOUND), never verified.
    - Writes one masked writeAuditLog on the status change (metadata: billingProfileId + resulting status only — never routing/account).
    - After running against a PENDING/VERIFIED mock, the profile's plaidVerificationStatus is non-null (the payout advisory read now differentiates).
  </behavior>
  <files>packages/api/src/routers/finance/payment-core.ts, packages/api/src/errors.ts, packages/api/src/routers/finance/__tests__/payment-plaid-onboarding.test.ts</files>
  <action>
    Add a `verifyBillingProfilePlaid` mutation to paymentCoreRouter mirroring initiatePayout's gating: tenantProcedure → requirePermission({payment:['export']}) → .strict() Zod input { billingProfileId: z.cuid() } → mutation that (1) calls assertUsExpansionEnabled(ctx.organizationId, ctx.region); (2) loads the ContractorBillingProfile scoped by { id, organizationId } including contractor.legalName + usRoutingNumberMasked + usAccountNumberMasked + plaidAccountId (throw NOT_FOUND via a new/existing E key when absent); (3) instantiates the MockPlaidIdentityClient (the GA default — the live client stays flag-dark per 88-06; do NOT install any SDK) and calls verify({ accountId: profile.plaidAccountId ?? <deterministic id from billingProfileId>, legalName, routingNumber: usRoutingNumberMasked ?? '', accountNumber: usAccountNumberMasked ?? '' }); (4) updates the profile with plaidVerificationStatus = result.status, plaidVerifiedAt = new Date(), plaidAccountId = result.plaidAccountId ?? existing; (5) writeAuditLog (action 'contractor_billing_profile.plaid_verified', masked metadata: billingProfileId + status only); (6) returns { status, advisoryWarning }. Fail-open: never throw on a non-VERIFIED status. Add a header comment: this is the mock-triggerable onboarding verification that makes plaidVerificationStatus writable; the live Plaid Link → public_token → verify flow stays behind the payments.plaid-verification flag + deferred live SDK (88-06). No breadcrumb IDs. Add E.BILLING_PROFILE_NOT_FOUND if no suitable key exists.

    Author payment-plaid-onboarding.test.ts (mirror the payout-init harness): verifying a profile writes a non-null plaidVerificationStatus + plaidVerifiedAt + audit; a mock that returns non-VERIFIED does NOT throw (fail-open) and still persists the status; a cross-org billingProfileId → NOT_FOUND (nothing written); the US-expansion gate throws when the surface is off. Assert the persisted status is later readable (the payout advisory now differentiates verified vs unverified).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api exec vitest run payment-plaid-onboarding payment && pnpm typecheck --filter=@contractor-ops/api && pnpm lint:audit-log && pnpm lint:no-breadcrumbs</automated>
  </verify>
  <done>verifyBillingProfilePlaid is a reachable, gated, tenant-scoped, fail-open mutation that persists plaidVerificationStatus via the mock; the test proves the write half of US-PAY-05 and fail-open behavior; no SDK installed; lints + typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Record tin-match defer + Plaid/gap-closure documentation sweep</name>
  <read_first>
    .planning/phases/88-theme-a-us-payment-rail/deferred-items.md (append format),
    .planning/phases/88-theme-a-us-payment-rail/88-03-SUMMARY.md (the tin-match writer disclosure) or 88-VERIFICATION.md Supplementary Finding,
    .planning/brain/wiki/integrations/plaid.md (existing Plaid integration page + verify_with),
    .planning/brain/wiki/domains/us-payment-rail.md (updated by 88-11 — add the onboarding-verification note),
    .planning/MEMORY.md (invariant format),
    CLAUDE.md (§ Documentation follows code — refresh pipeline + check:wiki-brain)
  </read_first>
  <files>.planning/phases/88-theme-a-us-payment-rail/deferred-items.md, .planning/brain/wiki/integrations/plaid.md, .planning/brain/wiki/domains/us-payment-rail.md, .planning/MEMORY.md</files>
  <action>
    Record the tin-match decision as an explicit DEFER, not an omission: append an entry to deferred-items.md titled "88 — tin-match backup-withholding writer deferred to Phase 86" explaining that createBackupWithholdingFlagWriter / createDbTinMatchPersistence (tin-match.service.ts) exist and are correct but have zero production callers because the P86 year-end tin-match batch/staff router that owns the trigger has not landed; the writer is the documented seam P86 will call to set Contractor.backupWithholdingFlagged (which applyWithholding already reads). State the disposition: DEFER to Phase 86 (LOCAL-ONLY posture — no live year-end batch yet), not a phase-88 gap. Add a matching one-line invariant to MEMORY.md.

    Update the Plaid integration wiki page (integrations/plaid.md): document the new onboarding-verification trigger (paymentCore.verifyBillingProfilePlaid → MockPlaidIdentityClient.verify → ContractorBillingProfile.plaidVerificationStatus; advisory fail-open; live client flag-dark) and add packages/api/src/routers/finance/payment-core.ts to its verify_with; bump source_commit. Add a one-line note to the us-payment-rail domain page cross-referencing the onboarding verification. Then run the wiki refresh pipeline (contextual-prefix + bm25 rebuild), overwrite hot.md, and append a gap-closure entry to log.md per CLAUDE.md. Durable facts only — no round-status/process text.
  </action>
  <verify>
    <automated>pnpm check:wiki-brain && grep -qi "tin-match" .planning/phases/88-theme-a-us-payment-rail/deferred-items.md && echo OK</automated>
  </verify>
  <done>deferred-items.md records the tin-match → P86 defer with the seam documented; integrations/plaid.md documents the onboarding trigger with updated verify_with; MEMORY.md + log.md + hot.md updated; `pnpm check:wiki-brain` reports 0 new drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → verifyBillingProfilePlaid | Client supplies only a billingProfileId; all account data is server-loaded from the tenant-scoped profile. |
| profile bank data → Plaid mock | Only masked routing/account are passed to the mock; full values stay AES-256-GCM at rest. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-12-01 | Elevation of privilege | who can trigger verification | mitigate | assertUsExpansionEnabled + requirePermission({payment:['export']}) + tenant-scoped load (foreign-org profile → NOT_FOUND). |
| T-88-12-02 | Information disclosure | bank data to Plaid / audit | mitigate | Only masked routing/account passed to the mock; audit metadata carries billingProfileId + status only; no full values logged. |
| T-88-12-03 | Denial of service / availability | verification blocking payout | accept/mitigate | Fail-open by design (D-08): non-VERIFIED never throws or blocks; the status is advisory only. |
| T-88-12-04 | Tampering | supply chain (Plaid SDK) | mitigate | GA path uses the deterministic mock only — zero SDK installed; the live client stays flag-dark (88-06). |
| T-88-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api exec vitest run payment-plaid-onboarding payment` → green (new trigger + no payment regression).
- `pnpm typecheck --filter=@contractor-ops/api` → clean; `git diff -- '**/package.json'` → no new dependency (mock only).
- `pnpm lint:audit-log && pnpm lint:no-breadcrumbs && pnpm check:wiki-brain` → clean / 0 new drift.
- deferred-items.md contains the recorded tin-match → P86 defer.
</verification>

<success_criteria>
Both secondary WARNINGs are closed with recorded decisions: the Plaid onboarding-verification write half of US-PAY-05 is wired and reachable (mock-triggerable, fail-open, audited), and the tin-match backup-withholding writer is explicitly deferred to Phase 86 with its seam documented. The gap-closure documentation-follows-code sweep leaves check:wiki-brain green.
</success_criteria>

<output>
Create `.planning/phases/88-theme-a-us-payment-rail/88-12-SUMMARY.md` when done.
</output>
