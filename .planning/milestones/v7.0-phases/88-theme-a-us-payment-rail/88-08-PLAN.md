---
phase: 88-theme-a-us-payment-rail
plan: 08
type: execute
wave: 8
depends_on: []
gap_closure: true
autonomous: true
requirements: [US-PAY-01, US-PAY-04]
files_modified:
  - packages/api/src/services/__tests__/payment-export-format-parity.test.ts
  - packages/api/src/services/__tests__/payment-format-detection.test.ts
  - packages/api/src/routers/finance/__tests__/payment-us-export.e2e.test.ts
  - packages/api/src/services/ach-return.service.ts
  - packages/api/src/services/__tests__/ach-return.service.test.ts

must_haves:
  truths:
    - "A parity test asserts paymentExportFormatEnum is a subset of the Prisma PaymentExportFormat enum AND contains ACH_NACHA + FEDWIRE — and it FAILS today (Zod enum omits both)."
    - "A detection test asserts detectFormatForDestination routes USD + US bank ≤ ceiling → ACH_NACHA, > ceiling → FEDWIRE, and groupItemsByFormat splits a mixed run — and it FAILS today (detectUsFormat has no caller)."
    - "An end-to-end test drives lockAndExport with a USD-to-US-bank run and expects a NACHA .txt file (≤ ceiling) and a Fedwire pacs.008 .xml (> ceiling) — and it FAILS today (Zod rejects the format + US fields never populated)."
    - "A pure test asserts parseNachaReturnFile + mapReturnCodeToStatus behavior (R01/R02/R03 → FAILED; NOC/COR → advisory) — and it FAILS today (ach-return.service is a throwing stub)."
  artifacts:
    - path: "packages/api/src/services/ach-return.service.ts"
      provides: "ACH return-code contract stub (types + parseNachaReturnFile + mapReturnCodeToStatus + applyAchReturns signatures) — throwing placeholders the RED tests import and 88-10 implements."
      contains: "parseNachaReturnFile"
    - path: "packages/api/src/services/__tests__/payment-export-format-parity.test.ts"
      provides: "Zod↔Prisma export-format parity gate (RED)."
    - path: "packages/api/src/services/__tests__/payment-format-detection.test.ts"
      provides: "US routing + grouping assertions (RED)."
    - path: "packages/api/src/routers/finance/__tests__/payment-us-export.e2e.test.ts"
      provides: "lockAndExport US export end-to-end assertion (RED)."
    - path: "packages/api/src/services/__tests__/ach-return.service.test.ts"
      provides: "return-code parse + map assertions (RED)."
  key_links:
    - from: "payment-export-format-parity.test.ts"
      to: "@contractor-ops/validators paymentExportFormatEnum + @contractor-ops/db PaymentExportFormat"
      via: "cross-package import"
      pattern: "paymentExportFormatEnum"
    - from: "payment-us-export.e2e.test.ts"
      to: "paymentExportRouter.lockAndExport"
      via: "tRPC caller with exportFormat ACH_NACHA / FEDWIRE"
      pattern: "lockAndExport"
    - from: "ach-return.service.test.ts"
      to: "ach-return.service"
      via: "parseNachaReturnFile + mapReturnCodeToStatus"
      pattern: "mapReturnCodeToStatus"
---

<objective>
Lay the Nyquist RED scaffolds for the phase-88 gap closure. Every test written here MUST fail before the Gap A/B/C fixes land — they encode the exact observable behavior the re-verification will check. This plan also creates the `ach-return.service.ts` interface stub (throwing placeholders) so the return-code RED tests compile and fail cleanly rather than error on a missing module, and so 88-10 has a locked contract to implement against.

Purpose: Prove the gaps exist as failing tests, and pin the contracts (enum parity, US routing precedence, end-to-end export reachability, return-code mapping) before any implementation.
Output: Four test files (one new, one extended, two new) + one throwing service stub.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/88-theme-a-us-payment-rail/88-GAP-SCOPE.md
@.planning/phases/88-theme-a-us-payment-rail/88-VERIFICATION.md
@.planning/phases/88-theme-a-us-payment-rail/88-PATTERNS.md
@CLAUDE.md

# Source under test (read to mirror fixture shapes exactly — do NOT modify source here)
@packages/validators/src/payment.ts
@packages/api/src/services/payment-format-detection.ts
@packages/api/src/routers/finance/payment-export-router.ts
@packages/api/src/routers/finance/payment-shared.ts
@packages/api/src/services/payment-export.ts

<interfaces>
<!-- Contracts the tests assert against. Extracted from the codebase — use directly. -->

From packages/validators/src/payment.ts (Gap A target — CURRENTLY missing the US members):
  export const paymentExportFormatEnum = z.enum(['CSV', 'BANK_FILE', 'SEPA_XML', 'SWIFT_XML']);

From packages/db (Prisma) enum PaymentExportFormat (payment.prisma:159-170):
  CSV BANK_FILE SEPA_XML SWIFT_XML MT940 XML API_PUSH BACS_STD18 ACH_NACHA FEDWIRE
  (import member set via: import { PaymentExportFormat } from '@contractor-ops/db')

From packages/api/src/services/payment-format-detection.ts:
  export function detectFormatForDestination(currency: string, destination: Destination): ExportFormat
  export function detectUsFormat(currency, isUsBank, amountMinor, sameDayCeilingMinor): ExportFormat | null   // ZERO production callers today
  export function sameDayAchCeilingMinor(asOf?: Date): number                                                  // 1_000_000_00 now, 10_000_000_00 from 2027-09-17
  export function groupItemsByFormat(items: ExportItem[]): Map<ExportFormat, ExportItem[]>
  export interface Destination { iban: string | null; ukSortCodeEncrypted: string | null; ukAccountNumberEncrypted: string | null }
  export type ExportFormat = 'SEPA_XML'|'SWIFT_XML'|'BANK_FILE'|'CSV'|'BACS_STD18'|'ACH_NACHA'|'FEDWIRE'

From packages/api/src/routers/finance/payment-export-router.ts:
  paymentExportRouter.lockAndExport — tenantProcedure, input paymentRunLockSchema { runId, exportFormat },
  returns { run, fileBase64, fileName }  (fileName ext = 'txt' NACHA / 'xml' Fedwire once wired)

From packages/api/src/services/payment-export.ts (ALREADY COMPLETE — reference only, do not rebuild):
  export function generateNachaFile(items: NachaExportItem[], orgBank: NachaOrgBankInfo): { fileBuffer: Buffer; ext: string; warnings: ... }
  export function generateFedwirePacs008(items: ExportItem[], orgBank: OrgBankInfo, runRef: string): Buffer
  ExportItem carries optional usRoutingNumber? / usAccountNumber? (payment-export.ts:36-37)

From packages/api/src/services/bank-account-crypto.ts (for e2e fixture encryption):
  export function encryptBankAccount(accountNumber: string): string
  export function decryptBankAccount(encrypted: string): string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — enum-parity + US routing/grouping scaffolds</name>
  <read_first>
    packages/validators/src/payment.ts (paymentExportFormatEnum :50),
    packages/api/src/services/payment-format-detection.ts (detectFormatForDestination :160, detectUsFormat :193, sameDayAchCeilingMinor :97, groupItemsByFormat :211),
    packages/api/src/services/__tests__/payment-format-detection.test.ts (mirror existing describe/fixture style),
    packages/api/src/services/payment-export.ts (ExportItem shape :19-38)
  </read_first>
  <files>packages/api/src/services/__tests__/payment-export-format-parity.test.ts, packages/api/src/services/__tests__/payment-format-detection.test.ts</files>
  <action>
    Create the cross-package parity test payment-export-format-parity.test.ts (in the API package, which depends on BOTH @contractor-ops/db and @contractor-ops/validators). Import `PaymentExportFormat` from @contractor-ops/db and `paymentExportFormatEnum` from @contractor-ops/validators. Assert two invariants: (1) every value in `paymentExportFormatEnum.options` is a member of the Prisma `PaymentExportFormat` enum (the Zod input enum must be a strict subset — it may never name a format the DB does not know); (2) `paymentExportFormatEnum.options` includes both `ACH_NACHA` and `FEDWIRE` (the file-export-factory US formats that must be selectable through lockAndExport). Invariant (2) FAILS today — that is the RED. Add a comment naming packages/db/prisma/schema/payment.prisma as the source of truth (per D-10: the string union and the Prisma enum are separate and both must carry the members). No breadcrumb IDs in the test body.

    Extend payment-format-detection.test.ts with a new describe block for US routing precedence. Assert: detectFormatForDestination on a USD payout to a US-bank destination (a destination signalling a US bank — see the Gap B contract: US routing/account present, no IBAN) returns 'ACH_NACHA' when amountMinor ≤ sameDayAchCeilingMinor() and 'FEDWIRE' when amountMinor > the ceiling; and that the precedence is BACS (GBP) → US (USD + US bank) → IBAN/SEPA/SWIFT (a GBP UK destination still returns BACS_STD18; a EUR EU-IBAN still returns SEPA_XML — no regression). Also assert groupItemsByFormat splits a mixed list (a USD US-bank item ≤ ceiling, a USD US-bank item > ceiling, and a EUR EU-IBAN item) into ACH_NACHA + FEDWIRE + SEPA_XML buckets. These assertions FAIL today because detectFormatForDestination never calls detectUsFormat and groupItemsByFormat uses the currency+IBAN detectFormat. Write the assertions against the Gap B API shape documented in 88-09's interfaces (detectFormatForDestination gaining a US-bank signal + amount/ceiling options, groupItemsByFormat gaining a paymentDate/ceiling-aware US branch) — if the current signature does not yet accept those, the test references the intended call form so it fails to compile/asserts wrong (RED); 88-09 makes it GREEN. Keep fixtures minimal and deterministic.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api exec vitest run payment-export-format-parity payment-format-detection 2>&1 | grep -E "FAIL|failed|✗|×" ; test ${PIPESTATUS[0]} -eq 0 || pnpm --filter @contractor-ops/api exec vitest run payment-export-format-parity payment-format-detection; echo "RED gate: the new assertions MUST report failing"</automated>
  </verify>
  <done>payment-export-format-parity.test.ts exists and its "contains ACH_NACHA + FEDWIRE" assertion fails; payment-format-detection.test.ts has the US-routing + grouping block and those cases fail. Pre-existing detection cases (BACS/SEPA/SWIFT) still pass (no regression authored).</done>
</task>

<task type="auto">
  <name>Task 2: RED — end-to-end lockAndExport US-export scaffold</name>
  <read_first>
    packages/api/src/routers/finance/payment-export-router.ts (lockAndExport :192-362, the run/items include shape :203-229),
    packages/api/src/routers/finance/payment-shared.ts (_buildExportItems :297, _generateExportFileForFormat :232, toNachaItem :206),
    packages/api/src/routers/__tests__/payment.test.ts (mirror the existing lockAndExport fixture + tRPC caller + db mock harness),
    packages/api/src/services/bank-account-crypto.ts (encryptBankAccount for the fixture US routing/account),
    packages/db/prisma/schema/contractor.prisma (ContractorBillingProfile US fields: usRoutingNumberEncrypted/Masked, usAccountNumberEncrypted/Masked)
  </read_first>
  <files>packages/api/src/routers/finance/__tests__/payment-us-export.e2e.test.ts</files>
  <action>
    Create payment-us-export.e2e.test.ts mirroring the existing payment.test.ts lockAndExport harness (same db-mock/tRPC-caller style, do not invent a new harness). Build a fixture payment run: organization with dataRegion 'US' (US source), currency 'USD', one PaymentRunItem whose contractor.currency is 'USD' and whose billingProfile carries encrypted US routing + account (via encryptBankAccount) plus the masked pair, no IBAN. Assert two end-to-end expectations that FAIL today:
      (a) calling lockAndExport with exportFormat 'ACH_NACHA' for an amount ≤ sameDayAchCeilingMinor() returns fileName ending '.txt' and a fileBase64 that decodes to NACHA output whose detail records are 94 chars and whose entry carries the item's routing/account (proving _buildExportItems decrypted + populated ExportItem.usRoutingNumber/usAccountNumber, not the masked/empty fallback);
      (b) calling lockAndExport with exportFormat 'FEDWIRE' for an amount > the ceiling returns fileName ending '.xml' and a pacs.008 body (contains 'pacs.008').
    Both fail today: (a)/(b) are rejected at Zod input validation (paymentExportFormatEnum omits ACH_NACHA/FEDWIRE), and even past that the US routing/account are never populated by _buildExportItems. This is the primary SC#1 + SC#4 reachability RED. Keep the org bank ACH origination fields (achImmediateDestination/Origin/companyId/odfiRoutingPrefix via org settings metadata) present so the NACHA header is structurally complete. No breadcrumb IDs.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api exec vitest run payment-us-export.e2e; echo "RED gate: both lockAndExport US-format cases MUST fail"</automated>
  </verify>
  <done>payment-us-export.e2e.test.ts exists; the ACH_NACHA and FEDWIRE cases fail (Zod rejection and/or missing US routing/account in the emitted file). The harness matches the existing payment.test.ts style.</done>
</task>

<task type="auto">
  <name>Task 3: RED — ACH return-code contract stub + parse/map scaffolds</name>
  <read_first>
    packages/api/src/routers/finance/payment-shared.ts (writeAuditLog usage :563, _initiatePayoutForRun tenant-scoped item load :717 as the DB-apply analog for 88-10),
    packages/api/src/services/payment-export.ts (generateNachaFile field layout :911 region — mirror its record structure so parseNachaReturnFile expectations match a real NACHA file the generator could emit),
    packages/db/prisma/schema/payment.prisma (PaymentRunItem status/failureReason :45-48; PaymentRunItemStatus enum :151)
  </read_first>
  <files>packages/api/src/services/ach-return.service.ts, packages/api/src/services/__tests__/ach-return.service.test.ts</files>
  <action>
    Create the ach-return.service.ts contract stub (interface-first — 88-10 implements the bodies). Export these types + signatures with throwing placeholders (each body: throw new Error('not implemented — 88-10')):
      - type AchReturnDisposition = 'FAILED' | 'ADVISORY'
      - interface AchReturnCodeMapping { code: string; disposition: AchReturnDisposition; reason: string }
      - interface AchReturnEntry { traceNumber: string; individualId: string; amountMinor: number; returnCode: string; addendaInfo: string }
      - function mapReturnCodeToStatus(code: string): AchReturnCodeMapping
      - function parseNachaReturnFile(fileText: string): AchReturnEntry[]
      - interface ApplyAchReturnsArgs { organizationId: string; paymentRunId: string; actorId: string; entries: AchReturnEntry[] }
      - interface ApplyAchReturnsResult { failed: number; advisory: number; skipped: number }
      - function applyAchReturns(db: DbClient, args: ApplyAchReturnsArgs): Promise<ApplyAchReturnsResult>   (import DbClient type from ../services/types)
    Document in the file header that R01/R02/R03 (and the R-code return family) are hard failures (payout returned by the RDFI) while COR/NOC C-codes are corrections (advisory, never a failure). Keep real domain IDs (R01, R02, R03, NOC, COR) in comments — those document behavior; no phase/plan/decision breadcrumb IDs.

    Create ach-return.service.test.ts with pure RED assertions against the stub: mapReturnCodeToStatus('R01'/'R02'/'R03') → disposition 'FAILED' with a reason mentioning insufficient funds / account closed / no account (unable to locate) respectively; mapReturnCodeToStatus for a COR/NOC code (e.g. 'C01' or 'NOC') → disposition 'ADVISORY'; parseNachaReturnFile(<a small fixture return file with one returned entry + an addenda 99 record carrying an R-code + addenda info>) → one AchReturnEntry with the expected traceNumber/individualId/amountMinor/returnCode/addendaInfo. All fail today (stub throws). Add a note in the test file header that the DB-apply idempotency RED (applyAchReturns flips a live PaymentRunItem and a re-delivered return is a no-op) is authored inside 88-10 as a TDD RED→GREEN pair because it requires the DB harness.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api exec vitest run ach-return.service; echo "RED gate: parse + map assertions MUST fail against the throwing stub"</automated>
  </verify>
  <done>ach-return.service.ts exports the full contract (types + three throwing functions); ach-return.service.test.ts exists and every case fails against the stub. The module imports cleanly (no missing-module error), so the suite runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → crypto helpers | Fixtures encrypt US routing/account via encryptBankAccount; test data only, never real PII. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-08-01 | Information disclosure | e2e fixture US routing/account | mitigate | Use synthetic routing/account numbers in fixtures; never log decoded file contents beyond length/format assertions. |
| T-88-08-02 | Tampering | RED test integrity | mitigate | Assertions encode exact expected behavior (94-char records, R-code→FAILED) so 88-09/88-10 cannot "pass" by weakening them. |
| T-88-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (test-only + a throwing stub) — zero new dependency surface. |
</threat_model>

<verification>
- All four RED files exist and their new assertions FAIL (that is success for a RED plan).
- No production source under packages/api/src (outside the new stub) is modified.
- Pre-existing payment-format-detection cases still pass (no regression authored).
- `pnpm lint:no-breadcrumbs` clean on the new files.
</verification>

<success_criteria>
The gap is now expressed as failing tests: enum parity (missing ACH_NACHA/FEDWIRE), US routing (detectUsFormat uncalled), end-to-end lockAndExport US export (Zod rejection + unpopulated US fields), and return-code parse/map (throwing stub). The ach-return.service contract is locked for 88-10.
</success_criteria>

<output>
Create `.planning/phases/88-theme-a-us-payment-rail/88-08-SUMMARY.md` when done.
</output>
