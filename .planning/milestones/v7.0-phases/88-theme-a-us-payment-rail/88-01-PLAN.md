---
phase: 88-theme-a-us-payment-rail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/services/__tests__/payment-export-nacha.test.ts
  - packages/api/src/services/__tests__/payment-export-fedwire.test.ts
  - packages/api/src/services/__tests__/payment-withholding.test.ts
  - packages/api/src/services/__tests__/payment-currency.test.ts
  - packages/integrations/src/__tests__/modern-treasury-adapter.test.ts
  - packages/integrations/src/__tests__/plaid-adapter.test.ts
autonomous: true
requirements: [US-PAY-01, US-PAY-02, US-PAY-03, US-PAY-04, US-PAY-05]
must_haves:
  truths:
    - "Implements the Wave-0 RED scaffolds from 88-VALIDATION.md: one failing test per US-PAY-01..05 + the generalized withholding deduction + the SA-WHT regression guard"
    - "Every Wave-0 test fails for the right reason (missing production symbol), not from a typo or import error"
    - "No external NACHA/Modern-Treasury/Plaid dependency is installed (GA floor needs zero new deps)"
  artifacts:
    - path: "packages/api/src/services/__tests__/payment-export-nacha.test.ts"
      provides: "RED golden-file scaffold for generateNachaFile (94-char records, entry hash, blocking)"
      contains: "generateNachaFile"
    - path: "packages/api/src/services/__tests__/payment-export-fedwire.test.ts"
      provides: "RED scaffold for generateFedwirePacs008"
      contains: "generateFedwirePacs008"
    - path: "packages/api/src/services/__tests__/payment-withholding.test.ts"
      provides: "RED scaffold for the generalized withholding deduction + SA regression guard"
      contains: "backupWithholdingFlagged"
    - path: "packages/integrations/src/__tests__/modern-treasury-adapter.test.ts"
      provides: "RED scaffold for the Modern Treasury PayoutInitiationAdapter mock"
      contains: "initiatePayout"
    - path: "packages/integrations/src/__tests__/plaid-adapter.test.ts"
      provides: "RED scaffold for the Plaid Identity mock (advisory fail-open)"
      contains: "PlaidIdentityClient"
  key_links:
    - from: "payment-withholding.test.ts"
      to: "payment-shared.ts applyWithholding (to be created in 88-03)"
      via: "import of the generalized function"
      pattern: "applyWithholding|_applyWhtIfSaudi"
---

<objective>
Lay the Wave-0 RED test scaffolds for Phase 88 per 88-VALIDATION.md. Each scaffold is a failing test that pins the expected behavior of a production symbol that does NOT yet exist (or is not yet generalized). This is the Nyquist anchor set — every downstream task turns one of these RED.

Purpose: Lock the contracts (function names, return shapes, golden-file assertions, the SA-WHT regression guard) before any implementation, so executors build against tests rather than exploring.
Output: Six new test files, all RED (failing because the production symbol is missing), zero new dependencies installed.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-theme-a-us-payment-rail/88-VALIDATION.md
@.planning/phases/88-theme-a-us-payment-rail/88-RESEARCH.md

<interfaces>
<!-- Existing symbols the scaffolds assert against or mirror. Extracted from codebase — do not re-explore. -->

From packages/api/src/services/payment-export.ts (analogs to mirror — DO NOT modify here):
- `export function generateBacsStandard18(items: BacsExportItem[], orgBank: BacsOrgBankInfo, _runRef: string, processingDate: Date): BacsGenerateResult` — the fixed-width 94/106-char, control-total, hard-length-guard template the NACHA scaffold mirrors.
- `export function generateSwiftXml(items: ExportItem[], org: OrgBankInfo, runNumber: string): Buffer` — the ISO 20022 pain.001.001.09 builder the Fedwire pacs.008 scaffold mirrors.
- `BacsGenerateResult = { fileBuffer: Buffer; ext: 'txt'; transliterationWarnings: [...]; modulusWarnings: [...] }`.
- Existing test analogs to mirror: `payment-export-swift.test.ts`, `payment-export.test.ts`, `exchange-rate.test.ts`, `form-1099-nec.service.test.ts`.

From packages/api/src/routers/finance/payment-shared.ts (to be generalized in 88-03):
- `export async function _applyWhtIfSaudi(tx, organizationId, paymentRunId): Promise<void>` — currently early-returns when `org.countryCode !== 'SA'`. 88-03 renames/widens to `applyWithholding`.

From packages/integrations/src/adapters/tin-match/tin-match-client.ts (seam pattern to mirror for adapters):
- `export interface TinMatchClient { match(input: TinMatchInput): Promise<TinMatchResult>; }`

From packages/api/src/services/exchange-rate.ts:
- `export async function convertAmount(db, amountMinor, fromCurrency, toCurrency, date?): Promise<{ amountMinor; rate; rateDate } | null>` — same-currency short-circuits rate 1; missing rate returns null.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED scaffolds for the two file generators (NACHA golden-file + Fedwire pacs.008)</name>
  <read_first>
    - packages/api/src/services/__tests__/payment-export-swift.test.ts (test-structure analog — describe/it/import shape)
    - packages/api/src/services/payment-export.ts:389-679 (generateBacsStandard18 + buildDetailRecord — the 94/106-char control-total template; 88-PATTERNS.md "generateNachaFile" section)
    - packages/api/src/services/payment-export.ts:300-366 (generateSwiftXml — the pacs.008 mirror; 88-PATTERNS.md "generateFedwirePacs008" section)
    - 88-RESEARCH.md Pattern 2 (NACHA record layout, entry hash = Σ first-8-digit RDFI routing mod 10^10, 10-line blocking) + Pattern 3 (Fedwire pacs.008.001.xx)
  </read_first>
  <action>
    Create payment-export-nacha.test.ts and payment-export-fedwire.test.ts under packages/api/src/services/__tests__/. Import `generateNachaFile` and `generateFedwirePacs008` from '../payment-export' (these symbols do NOT exist yet — that is the RED). For NACHA, assert against a small known-good fixture (2-3 credit entries): every record is exactly 94 chars; record-type prefixes appear in order 1 (file header), 5 (batch header), 6 (entry detail), 8 (batch control), 9 (file control); the batch-control entry hash equals the sum of the first-8-digits of each RDFI routing number truncated to the rightmost 10 digits; total credit equals Σ amountMinor in cents; total line count is a multiple of 10 (9-record block padding); service class 220 and SEC code PPD by default; transaction code 22 for checking. For Fedwire, assert the output is a pacs.008.001.xx Document XML containing GrpHdr MsgId/CreDtTm/NbOfTxs/CtrlSum and one CdtTrfTxInf per item, control sum equals Σ amountMinor. Mark both with a top-of-file comment that these are terminal-RED Wave-0 scaffolds. Use real domain IDs (PPD, CCD, CTX, service class 220, txn code 22/32, pacs.008) in test names — NO phase/requirement-ID breadcrumbs.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- payment-export-nacha payment-export-fedwire 2>&1 | grep -qiE "Cannot find name 'generateNachaFile'|generateNachaFile is not|is not a function|export.*generateNachaFile" && echo RED-OK || echo "verify fail reason"</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist and import generateNachaFile / generateFedwirePacs008 from '../payment-export'.
    - `pnpm --filter @contractor-ops/api test -- payment-export-nacha payment-export-fedwire` FAILS because the production symbols are absent (RED for the right reason — not a typo/import-path error).
    - NACHA test asserts: 94-char record length, record-type ordering 1/5/6/8/9, entry-hash formula, credit-total, 10-line block padding, SEC=PPD + service class 220 + txn code 22 defaults.
    - Fedwire test asserts pacs.008.001.xx envelope + per-item CdtTrfTxInf + control sum.
    - No NACHA dependency added to any package.json.
  </acceptance_criteria>
  <done>Two RED generator scaffolds committed; both fail on missing production symbols; zero new deps.</done>
</task>

<task type="auto">
  <name>Task 2: RED scaffolds for withholding generalization (+ SA regression guard), currency, and the two mock adapters</name>
  <read_first>
    - packages/api/src/routers/finance/payment-shared.ts:282-325 (_applyWhtIfSaudi — the SA branch that MUST stay regression-green; 88-PATTERNS.md "HIGHEST REGRESSION RISK" section)
    - packages/api/src/services/__tests__/exchange-rate.test.ts (currency-test analog)
    - packages/integrations/src/adapters/tin-match/{tin-match-client.ts,mock-tin-match-client.ts} (seam + deterministic-mock analog)
    - packages/integrations/src/__tests__/credential-service.test.ts (integrations-package test-structure analog)
    - 88-VALIDATION.md Per-Task Verification Map (the anchor rows for each scaffold)
  </read_first>
  <action>
    Create payment-withholding.test.ts and payment-currency.test.ts under packages/api/src/services/__tests__/, and modern-treasury-adapter.test.ts and plaid-adapter.test.ts under packages/integrations/src/__tests__/. payment-withholding.test.ts: assert the (not-yet-existing) generalized `applyWithholding` path computes amountMinor = grossAmountMinor − whtAmountMinor with whtAmountMinor = HALF-UP round(gross * rate / 100) for (a) a US contractor with backupWithholdingFlagged=true at 24% (IRC §3406) and (b) a foreign-recipient 1042-S treaty rate; AND a dedicated SA-regression `it` asserting an SA org cross-border item keeps byte-identical behavior (calculateWht path, whtServiceType 'technical_services', invoice.withholdingMinor write). payment-currency.test.ts: assert convertAmount(USD→USD) returns rate 1, USD↔EUR uses the stored rate, and a missing rate returns null (F-1 guard — NO USD=1.0 special-case). modern-treasury-adapter.test.ts: assert the mock `initiatePayout` returns a deterministic payment_order shape with a status in the pending→…→reconciled enum and the live client refuses while flag-dark. plaid-adapter.test.ts: assert the mock `PlaidIdentityClient` verify returns a status (VERIFIED/PENDING/FAILED) and that an unverified status produces an advisory warning, never a thrown PRECONDITION_FAILED (fail-open, D-08). Top-of-file comment marks each as terminal-RED Wave-0 scaffold.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- payment-withholding payment-currency 2>&1 | grep -qiE "applyWithholding|Cannot find|is not a function|backupWithholdingFlagged" && pnpm --filter @contractor-ops/integrations test -- modern-treasury plaid 2>&1 | grep -qiE "initiatePayout|PlaidIdentityClient|Cannot find" && echo RED-OK || echo "verify fail reason"</automated>
  </verify>
  <acceptance_criteria>
    - Four test files exist at the paths in files_modified.
    - The withholding scaffold contains an explicit SA-regression `it(...)` referencing the SA/calculateWht path AND the US 24% + 1042-S treaty branches.
    - The currency scaffold asserts convertAmount(USD→USD)=rate 1, USD↔EUR via stored rate, missing-rate→null (no USD=1.0 special-case per F-1).
    - The adapter scaffolds import a `PayoutInitiationAdapter`/mock and a `PlaidIdentityClient`/mock that do not yet exist.
    - All four FAIL on missing production symbols (RED for the right reason); no modern-treasury/plaid dependency installed.
  </acceptance_criteria>
  <done>Four RED scaffolds committed across api + integrations; all fail on missing symbols; SA-regression guard present; no new deps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only adds test files; no runtime trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01-01 | Information Disclosure | test fixtures containing routing/account numbers | mitigate | Use obviously-synthetic routing/account values in fixtures (e.g. 021000021 / 000123456789); never embed a real masked production account; full numbers never logged. |
| T-88-SC | Tampering | npm install (supply chain) | mitigate | This plan installs ZERO packages. `[ASSUMED]` SDK installs (modern-treasury, plaid) are deferred to 88-06 behind a blocking-human checkpoint; NACHA is hand-rolled (no dep). |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- payment-export-nacha payment-export-fedwire payment-withholding payment-currency` → all RED on missing symbols.
- `pnpm --filter @contractor-ops/integrations test -- modern-treasury plaid` → RED on missing symbols.
- `git diff --stat -- '**/package.json'` → no new dependency lines.
</verification>

<success_criteria>
- Six RED scaffolds exist, one per US-PAY-01..05 plus the generalized withholding deduction with an explicit SA-regression guard.
- Each fails because the production symbol is missing, not from a typo/import error.
- No external dependency installed.
</success_criteria>

<output>
Create `.planning/phases/88-theme-a-us-payment-rail/88-01-SUMMARY.md` when done.
</output>
