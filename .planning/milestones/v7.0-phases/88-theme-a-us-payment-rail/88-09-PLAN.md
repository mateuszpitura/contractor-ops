---
phase: 88-theme-a-us-payment-rail
plan: 09
type: execute
wave: 9
depends_on: ["88-08"]
gap_closure: true
autonomous: true
requirements: [US-PAY-01, US-PAY-04]
files_modified:
  - packages/validators/src/payment.ts
  - packages/api/src/services/payment-format-detection.ts
  - packages/api/src/routers/finance/payment-export-router.ts
  - packages/api/src/routers/finance/payment-shared.ts

must_haves:
  truths:
    - "RESTORED end-to-end reachability (SC#1/SC#4) runs through this wired chain: lockAndExport → paymentRunLockSchema accepts ACH_NACHA/FEDWIRE (Gap A) → _buildExportItems surfaces the item's decrypted US routing/account (Gap B) → _generateExportFileForFormat's pre-existing string dispatch → generateNachaFile / generateFedwirePacs008. This chain does NOT pass through groupItemsByFormat."
    - "lockAndExport accepts exportFormat ACH_NACHA and FEDWIRE (Zod no longer rejects them)."
    - "detectUsFormat gains ≥1 reachable production caller via the getFormatDetection advisory query (through detectFormatForDestination's US branch): a USD payout to a US bank is advised as ACH_NACHA at/below the Same-Day ACH ceiling and FEDWIRE above it."
    - "lockAndExport produces a balanced NACHA .txt (and a Fedwire pacs.008 .xml) carrying the item's decrypted US routing/account — end-to-end, from a real payment run."
    - "INTERNAL-CONSISTENCY guarantee (NOT user-observable reachability): groupItemsByFormat is made US-aware so a mixed USD/EUR list splits into ACH_NACHA/FEDWIRE/SEPA/SWIFT buckets. groupItemsByFormat has zero production callers before AND after this plan (only its own definition + tests) — this is a correctness guarantee for the helper, deliberately not claimed as a reachability truth."
  artifacts:
    - path: "packages/validators/src/payment.ts"
      provides: "paymentExportFormatEnum extended with ACH_NACHA + FEDWIRE (Prisma-mirror comment updated)."
      contains: "ACH_NACHA"
    - path: "packages/api/src/services/payment-format-detection.ts"
      provides: "detectFormatForDestination calls detectUsFormat before the IBAN fallback; groupItemsByFormat US-aware (internal-consistency, no production caller)."
      contains: "detectUsFormat"
    - path: "packages/api/src/routers/finance/payment-shared.ts"
      provides: "_buildExportItems decrypts + populates ExportItem.usRoutingNumber/usAccountNumber."
      contains: "usRoutingNumber"
    - path: "packages/api/src/routers/finance/payment-export-router.ts"
      provides: "lockAndExport billingProfile select surfaces the US encrypted fields; format passed through unchanged."
  key_links:
    - from: "paymentExportRouter.lockAndExport"
      to: "_generateExportFileForFormat generateNachaFile/generateFedwirePacs008"
      via: "paymentRunLockSchema.exportFormat = ACH_NACHA | FEDWIRE"
      pattern: "_generateExportFileForFormat"
    - from: "getFormatDetection → detectFormatForDestination"
      to: "detectUsFormat → sameDayAchCeilingMinor"
      via: "USD + US-bank precedence branch (the reachable production caller of detectUsFormat)"
      pattern: "detectUsFormat"
    - from: "_buildExportItems"
      to: "ExportItem.usRoutingNumber / usAccountNumber"
      via: "decryptBankAccount(usRoutingNumberEncrypted/usAccountNumberEncrypted)"
      pattern: "decryptBankAccount"
---

<objective>
Close Gap A (enum mirror) and Gap B (thread detectUsFormat + US-aware grouping + surface the decrypted US routing/account) so the already-complete NACHA/Fedwire generators become reachable end-to-end from a real payment run. This turns the 88-08 RED scaffolds GREEN and restores ROADMAP Success Criteria #1 and #4.

Purpose: The generators, dispatch, Prisma enum, detectUsFormat, and ExportItem US fields all exist and are unit-tested (88-04) — they are only unwired. This plan supplies the two missing wires + the US-field mapping.
Output: Four modified source files; the enum-parity, US-routing/grouping, and end-to-end lockAndExport tests pass.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/88-theme-a-us-payment-rail/88-GAP-SCOPE.md
@.planning/phases/88-theme-a-us-payment-rail/88-04-SUMMARY.md
@.planning/phases/88-theme-a-us-payment-rail/88-PATTERNS.md
@CLAUDE.md

@packages/validators/src/payment.ts
@packages/api/src/services/payment-format-detection.ts
@packages/api/src/routers/finance/payment-shared.ts
@packages/api/src/routers/finance/payment-export-router.ts

<interfaces>
<!-- Contracts to satisfy. The 88-08 RED tests already assert these. -->

Gap A — packages/validators/src/payment.ts:50 (extend, keep it a strict subset of the Prisma enum):
  paymentExportFormatEnum = z.enum(['CSV','BANK_FILE','SEPA_XML','SWIFT_XML','ACH_NACHA','FEDWIRE'])

Gap B — packages/api/src/services/payment-format-detection.ts:
  detectUsFormat(currency, isUsBank, amountMinor, sameDayCeilingMinor): ExportFormat | null   // ALREADY EXISTS — just call it
  sameDayAchCeilingMinor(asOf?: Date): number                                                  // ALREADY EXISTS
  Destination { iban; ukSortCodeEncrypted; ukAccountNumberEncrypted }                          // EXTEND with US signal
  ExportItem carries usRoutingNumber? / usAccountNumber? (payment-export.ts:36-37)

Gap B wiring — packages/api/src/routers/finance/payment-shared.ts:
  _buildExportItems(db, items, transferTitleTemplate, settlement) → ExportItem[]               // populate US fields
  toNachaItem(item) already reads item.usRoutingNumber ?? '' and item.usAccountNumber ?? item.iban (:206)
  _generateExportFileForFormat already dispatches ACH_NACHA + FEDWIRE by string (:247-260)

From packages/api/src/services/bank-account-crypto.ts:
  export function decryptBankAccount(encrypted: string): string   // server-side only, plaintext never leaves the fn (see bacs.ts precedent :133)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gap A — mirror the US export formats into the Zod input enum</name>
  <read_first>
    packages/validators/src/payment.ts (header comment :4-6 "Prisma enum mirrors"; paymentExportFormatEnum :50; paymentRunLockSchema :75),
    packages/db/prisma/schema/payment.prisma (PaymentExportFormat :159-170 — the source of truth)
  </read_first>
  <behavior>
    - paymentExportFormatEnum.options includes ACH_NACHA and FEDWIRE (per D-10).
    - Every paymentExportFormatEnum value remains a member of the Prisma PaymentExportFormat enum (strict subset — the 88-08 parity test).
    - paymentRunLockSchema still parses successfully for the existing formats (no regression).
  </behavior>
  <files>packages/validators/src/payment.ts</files>
  <action>
    Add 'ACH_NACHA' and 'FEDWIRE' to paymentExportFormatEnum (packages/validators/src/payment.ts:50) so it reads z.enum(['CSV','BANK_FILE','SEPA_XML','SWIFT_XML','ACH_NACHA','FEDWIRE']) — mirroring the Prisma PaymentExportFormat members that are selectable through the file-export factory (per D-10; BACS_STD18/MT940/XML/API_PUSH stay out — BACS has its own router, the others are not file-factory formats). Update the "Prisma enum mirrors" comment block near line 4-6 to note that this enum mirrors the *selectable file-export* subset of PaymentExportFormat and points at packages/db/prisma/schema/payment.prisma as the source of truth. This is the single change that stops lockAndExport's Zod input from rejecting the US formats. Do not touch any other schema. No breadcrumb IDs in the comment.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api exec vitest run payment-export-format-parity && pnpm typecheck --filter=@contractor-ops/validators</automated>
  </verify>
  <done>payment-export-format-parity test passes (subset + contains ACH_NACHA/FEDWIRE); validators typecheck clean; paymentRunLockSchema accepts 'ACH_NACHA' and 'FEDWIRE'.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gap B — thread detectUsFormat into the routing + grouping path</name>
  <read_first>
    packages/api/src/services/payment-format-detection.ts (detectFormatForDestination :160-179 precedence, detectUsFormat :193-201, sameDayAchCeilingMinor :97, groupItemsByFormat :211-222, Destination :108-112),
    packages/api/src/routers/finance/payment-export-router.ts (getFormatDetection query :364-414 — the reachable advisory that will now surface US formats)
  </read_first>
  <behavior>
    - detectFormatForDestination on USD + a US-bank destination returns ACH_NACHA when amountMinor ≤ ceiling, FEDWIRE when > ceiling.
    - Precedence preserved: GBP + UK sort/account → BACS_STD18; EUR + EU IBAN → SEPA_XML; PLN + PL IBAN → BANK_FILE; else → SWIFT_XML.
    - A caller that omits amountMinor never enters the US branch — it falls straight through to the IBAN fallback (unambiguous chosen behavior; no silent ACH default on a missing amount).
    - groupItemsByFormat splits a mixed USD-US-bank / EUR-IBAN list into ACH_NACHA/FEDWIRE/SEPA buckets (internal-consistency helper — no production caller).
    - detectUsFormat now has ≥1 reachable production call site (the getFormatDetection advisory → detectFormatForDestination US branch — the SC#4 reachability requirement).
  </behavior>
  <files>packages/api/src/services/payment-format-detection.ts, packages/api/src/routers/finance/payment-export-router.ts</files>
  <action>
    Extend the Destination interface with a US-bank signal (usRoutingNumberEncrypted: string | null and usAccountNumberEncrypted: string | null, mirroring the UK pair) and give detectFormatForDestination an optional options arg carrying amountMinor and an asOf/ceiling (default sameDayAchCeilingMinor()). Insert the US branch BETWEEN the BACS check and the IBAN fallback: compute isUsBank = Boolean(destination.usRoutingNumberEncrypted && destination.usAccountNumberEncrypted). Guard the US branch on a DEFINED amount — enter it only when options?.amountMinor !== undefined: then const usFmt = detectUsFormat(currency, isUsBank, options.amountMinor, ceiling); if usFmt !== null return usFmt. Do NOT default a missing amount to 0 (a `?? 0` default would silently route an amount-less US destination to ACH_NACHA, contradicting the fall-through contract); when amountMinor is undefined the US branch is skipped and routing falls straight through to the IBAN fallback. This is the single unambiguous chosen behavior — document it in the function's doc comment. Keep the BACS-first and IBAN-fallback behavior byte-identical for every non-US case (the 88-08 no-regression assertions).

    Switch groupItemsByFormat to the US-aware path: for each ExportItem compute isUsBank from item.usRoutingNumber/item.usAccountNumber presence, resolve the ceiling via sameDayAchCeilingMinor() (optionally accept an asOf param defaulting to now), call detectUsFormat(item.currency, isUsBank, item.amountMinor, ceiling) first and use it when non-null, else fall back to the existing detectFormat(item.currency, item.iban). This makes a mixed run split ACH_NACHA vs FEDWIRE vs SEPA/SWIFT correctly. (groupItemsByFormat has no production caller before or after this plan — this US-awareness is an internal-consistency guarantee for the helper, not the reachability path.)

    Point the getFormatDetection tRPC query (payment-export-router.ts:364-414) at detectFormatForDestination (US-aware) instead of the currency+IBAN detectFormat, passing the item amount + the destination's US encrypted-field presence (extend its billingProfiles select to include usRoutingNumberEncrypted/usAccountNumberEncrypted presence — you may select the columns and pass Boolean(...) without decrypting, since only presence is needed for the advisory). This gives detectUsFormat a genuine, reachable production call site (SC#4) and makes the advisory display the true US format. Never log or return the encrypted values from this query — presence only.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api exec vitest run payment-format-detection && pnpm typecheck --filter=@contractor-ops/api</automated>
  </verify>
  <done>US-routing + grouping tests pass; BACS/SEPA/SWIFT/Elixir cases unchanged; an amount-less call falls through to the IBAN fallback (no US match); detectUsFormat has a reachable production caller (getFormatDetection → detectFormatForDestination); api typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gap B wiring — surface decrypted US routing/account into the export items</name>
  <read_first>
    packages/api/src/routers/finance/payment-shared.ts (_buildExportItems :297-369 — where ExportItem is assembled; the billingProfile param type :309-313; toNachaItem :206-214),
    packages/api/src/routers/finance/__tests__/payment-export-settlement.test.ts (the EXISTING _buildExportItems consumer — derives its fixture type via `Parameters<typeof _buildExportItems>[1][number]` and builds `billingProfile` WITHOUT the US fields; must keep typechecking untouched),
    packages/api/src/routers/finance/payment-export-router.ts (lockAndExport run/items include :203-231; _buildExportItems call :309-314; format passthrough :315-320),
    packages/api/src/routers/finance/bacs.ts (decrypt-into-export-item precedent :218-235 — decryptBankAccount usage),
    packages/api/src/services/bank-account-crypto.ts (decryptBankAccount :38)
  </read_first>
  <behavior>
    - When a run item's billingProfile has usRoutingNumberEncrypted/usAccountNumberEncrypted, the emitted ExportItem carries the decrypted usRoutingNumber/usAccountNumber.
    - lockAndExport with exportFormat 'ACH_NACHA' (amount ≤ ceiling) returns a .txt NACHA file with 94-char detail records carrying that routing/account.
    - lockAndExport with exportFormat 'FEDWIRE' (amount > ceiling) returns a .xml pacs.008.
    - Non-US runs are byte-identical to today (US fields left undefined → dispatch/generators unchanged).
    - The two new billingProfile properties are OPTIONAL, so the existing settlement-test fixture (built without them) still typechecks.
  </behavior>
  <files>packages/api/src/routers/finance/payment-shared.ts, packages/api/src/routers/finance/payment-export-router.ts</files>
  <action>
    In lockAndExport (payment-export-router.ts), extend the items.include.billingProfile select to add usRoutingNumberEncrypted and usAccountNumberEncrypted (alongside the existing bankAccountMasked/swiftBic/bankName). In _buildExportItems (payment-shared.ts), widen the item billingProfile parameter type to include those two encrypted columns as OPTIONAL nullable properties — `usRoutingNumberEncrypted?: string | null` and `usAccountNumberEncrypted?: string | null` (use `?:`, matching the existing ExportItem.usRoutingNumber?/usAccountNumber? optional convention). Making them optional is load-bearing: the existing consumer packages/api/src/routers/finance/__tests__/payment-export-settlement.test.ts derives its fixture type from `Parameters<typeof _buildExportItems>[1][number]` and constructs `billingProfile` without the US fields; optional properties keep that untouched fixture typechecking (do NOT add them as required). When both are present decrypt them with decryptBankAccount (mirroring bacs.ts:230-231 — server-side only, plaintext never leaves the function) and set ExportItem.usRoutingNumber / usAccountNumber on the pushed item. When absent or undefined, leave them undefined (the existing toNachaItem fallback and the non-US generators are unaffected). Do not change the settlement conversion, the transfer-title logic, or any non-US field. Confirm the format passthrough in lockAndExport is unchanged — input.exportFormat flows straight into _generateExportFileForFormat, which already branches ACH_NACHA/FEDWIRE by string (88-04). Never log the decrypted routing/account; they exist only inside the file buffer.

    CLAUDE.md: this is a sensitive-bank-data path — no console.*, use @contractor-ops/logger only for warnings (counts, never values); the audit trail for the export already lives in writeExportAndComplianceRows and must never carry full routing/account.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api exec vitest run payment-us-export.e2e payment payment-export-nacha payment-export-fedwire payment-export-settlement && pnpm typecheck --filter=@contractor-ops/api</automated>
  </verify>
  <done>The end-to-end payment-us-export.e2e test passes (ACH_NACHA → .txt with routing/account; FEDWIRE → .xml pacs.008); the full payment regression suite (payment.test.ts) plus the existing payment-export-settlement.test.ts stay green (proving the optional-widening did not break the untouched consumer); api typecheck clean. detectUsFormat, generateNachaFile, and generateFedwirePacs008 are now all reachable from lockAndExport.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → lockAndExport | The client selects exportFormat (now including US formats); Zod validates it, tenant scope loads the run. |
| DB (encrypted) → export buffer | US routing/account are AES-256-GCM at rest; decrypted only server-side inside _buildExportItems, emitted only into the file buffer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-09-01 | Information disclosure | decrypted US routing/account in _buildExportItems | mitigate | decryptBankAccount server-side only (bacs.ts precedent); never logged; export audit metadata carries no routing/account; the tRPC response returns the file, not the plaintext fields. |
| T-88-09-02 | Tampering | export-format selection | mitigate | paymentRunLockSchema Zod enum constrains exportFormat to the known subset; format flows unchanged into the string dispatch; no client-controlled path bypasses it. |
| T-88-09-03 | Elevation of privilege | reaching the US export | mitigate | lockAndExport keeps requirePermission({payment:['export']}) + tenant scope; the US surface remains behind module.us-expansion where getFormatDetection/the run are created (no new ungated entry point added here). |
| T-88-09-04 | Information disclosure | getFormatDetection advisory | mitigate | The query selects only presence of the US encrypted columns (Boolean), never decrypts or returns them. |
| T-88-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs — NACHA/Fedwire are hand-rolled, zero new dependency. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api exec vitest run payment-export-format-parity payment-format-detection payment-us-export.e2e payment payment-export-nacha payment-export-fedwire payment-export-settlement` → all green.
- `pnpm typecheck --filter=@contractor-ops/api --filter=@contractor-ops/validators` → clean.
- `pnpm lint:no-breadcrumbs && pnpm lint:logs` → clean (no breadcrumb IDs, no console.*).
- Grep confirms a production call site: `grep -rn detectUsFormat packages/api/src --include=*.ts | grep -v __tests__` → ≥1 hit outside the definition (detectFormatForDestination / getFormatDetection).
</verification>

<success_criteria>
ROADMAP SC#1 (NACHA export reachable end-to-end) and SC#4 (Fedwire reachable + detectUsFormat applied) are restored: a USD payment run to a US bank exports a balanced NACHA file (≤ ceiling) or a Fedwire pacs.008 (> ceiling) through lockAndExport, and the detection advisory assigns the US format via a real reachable production call (getFormatDetection → detectFormatForDestination → detectUsFormat). (SC#1's return-code clause is closed by 88-10/88-11.)
</success_criteria>

<output>
Create `.planning/phases/88-theme-a-us-payment-rail/88-09-SUMMARY.md` when done.
</output>
</content>
</invoke>
