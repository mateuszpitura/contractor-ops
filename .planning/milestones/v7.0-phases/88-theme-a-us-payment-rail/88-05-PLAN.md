---
phase: 88-theme-a-us-payment-rail
plan: 05
type: execute
wave: 4
depends_on: ["88-01", "88-02"]
files_modified:
  - packages/api/src/services/exchange-rate.ts
  - packages/api/src/services/payment-settlement.ts
  - packages/api/src/services/__tests__/payment-settlement.test.ts
autonomous: true
requirements: [US-PAY-02]
must_haves:
  truths:
    - "Implements D-06 (USD is first-class — uses Organization.defaultCurrency; per RESEARCH F-1 USD is ALREADY in the ECB feed so NO USD=1.0 short-circuit is added; a guard test proves the existing FX path handles USD source/target) and D-07 (per-payout settlement-currency choice defaulting Contractor.currency, FX at the payment-date ECB rate via convertAmount)"
    - "convertAmount(USD→USD) returns rate 1; USD↔EUR uses the real stored rate; a missing rate returns null (not 1.0)"
    - "A cross-border payout can settle in USD (contractor receives USD) or convert to the contractor's local currency at the payment-date ECB rate, defaulting to Contractor.currency, overridable per run"
    - "resolveSettlementCurrency + convertForSettlement are consumed downstream (not dead code): the export path (88-04 _buildExportItems) and the programmatic initiatePayout (88-06) call them, and the per-run settlement-currency override threads from the 88-06 initiatePayout Zod input into resolveSettlementCurrency"
  artifacts:
    - path: "packages/api/src/services/payment-settlement.ts"
      provides: "resolveSettlementCurrency + convertForSettlement (reuses convertAmount); consumed by 88-04 export path + 88-06 initiatePayout"
      contains: "resolveSettlementCurrency"
    - path: "packages/api/src/services/__tests__/payment-settlement.test.ts"
      provides: "USD-first-class + settlement-currency tests"
      contains: "USD"
  key_links:
    - from: "payment-settlement.ts convertForSettlement"
      to: "exchange-rate.ts convertAmount"
      via: "payment-date ECB rate conversion"
      pattern: "convertAmount"
    - from: "payment-settlement.ts resolveSettlementCurrency"
      to: "payment-shared.ts _buildExportItems (88-04 export/seeding call site)"
      via: "settle each export item before the bank-file buffer is generated"
      pattern: "resolveSettlementCurrency|convertForSettlement"
    - from: "payment-settlement.ts resolveSettlementCurrency"
      to: "the per-run settlement-currency override input on the 88-06 initiatePayout Zod schema"
      via: "perRunOverride threaded from the tRPC input into resolveSettlementCurrency({ contractorCurrency, perRunOverride })"
      pattern: "settlementCurrency|perRunOverride|resolveSettlementCurrency"
---

<objective>
Make USD a first-class settlement currency and add the per-payout settlement-currency choice, reusing the existing EUR-based FX service. Per RESEARCH F-1, USD is already in the ECB feed — so this is a guard test that the existing path handles USD correctly plus a thin settlement-resolution service, NOT a USD=1.0 short-circuit. The service is the seam consumed by the export path (88-04) and the programmatic payout (88-06) — it is wired downstream, not shipped as dead code.

Purpose: US-PAY-02 / D-06 / D-07 — USD selectable with a per-org default, settlement-currency choice on cross-border payouts at the payment-date ECB rate (consistent with the P86 1099 box-1 conversion).
Output: A USD guard test on exchange-rate, plus `payment-settlement.ts` (`resolveSettlementCurrency` + `convertForSettlement`) with tests, designed so 88-04's `_buildExportItems` and 88-06's `initiatePayout` (with a per-run override input) call it. The 88-01 currency scaffold turns green.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/88-theme-a-us-payment-rail/88-CONTEXT.md
@.planning/phases/88-theme-a-us-payment-rail/88-RESEARCH.md
@.planning/phases/88-theme-a-us-payment-rail/88-PATTERNS.md

<interfaces>
<!-- Exact signatures. Extracted from codebase. -->

From packages/api/src/services/exchange-rate.ts:
- `export async function convertAmount(db: DbClient, amountMinor: number, fromCurrency: string, toCurrency: string, date?: Date): Promise<{ amountMinor: number; rate: number; rateDate: Date } | null>` (268). Same-currency → rate 1 (279). Cross-rates via EUR; returns null when a needed EUR→X rate is missing (293/300) or non-finite (311). ONE HALF-UP round (314). USD is ALREADY supported via the stored EUR→USD ECB rate — do NOT special-case USD.
- `export async function getRate(db, base, quote, date)` (234).

From packages/db:
- `Organization.defaultCurrency String? @db.Char(3)`, `Organization.dataRegion`, `Organization.countryCode`.
- `Contractor.currency String @db.Char(3)`.

<!-- Downstream consumers this service is designed for (do not implement them here — they live in 88-04 / 88-06): -->
- 88-04 `payment-shared.ts _buildExportItems` calls `resolveSettlementCurrency` + `convertForSettlement` per item before generating the export buffer.
- 88-06 `payment-core.ts initiatePayout` Zod input carries the per-run `settlementCurrency` override → passed as `perRunOverride` into `resolveSettlementCurrency`.
- Keep the signatures stable so both call sites can consume the service unchanged: `resolveSettlementCurrency({ contractorCurrency: string; perRunOverride?: string }): string` and `convertForSettlement(db, amountMinor, fromCurrency, settlementCurrency, paymentDate): Promise<{ amountMinor; rate; rateDate } | null>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: USD guard test on exchange-rate (F-1 — prove the existing path, no USD=1.0 short-circuit)</name>
  <read_first>
    - packages/api/src/services/exchange-rate.ts:234-321 (getRate + convertAmount)
    - packages/api/src/services/__tests__/exchange-rate.test.ts (test analog)
    - packages/api/src/services/__tests__/payment-currency.test.ts (the 88-01 currency scaffold to turn green)
    - 88-RESEARCH.md Pitfall 3 + Open-Q2 (F-1) + 88-PATTERNS.md "exchange-rate.ts → USD settlement" section
  </read_first>
  <action>
    Do NOT add a USD=1.0 short-circuit (F-1: the premise that USD is absent from ECB is wrong and the short-circuit would mask a genuine missing-rate-on-holiday null). Treat USD as a normal ECB currency. Extend the exchange-rate test (and turn the 88-01 payment-currency.test.ts scaffold green) to assert: convertAmount(amount, 'USD', 'USD') → rate 1; convertAmount(amount, 'USD', 'EUR') and ('EUR','USD') use the real stored EUR↔USD rate (seed a fixture rate); convertAmount with a missing rate for the date → null (NOT 1.0). If a defensive guard is genuinely wanted, it must NOT bypass the real getRate lookup for USD↔other conversions — document why none is added. NO production short-circuit edit unless a test proves a real gap; if exchange-rate.ts needs no code change, leave it unchanged and note that in the summary.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- payment-currency exchange-rate 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test -- payment-currency exchange-rate` GREEN.
    - Tests assert USD→USD rate 1, USD↔EUR via stored rate, missing-rate→null.
    - NO USD=1.0 short-circuit was added to convertAmount; the rationale is recorded.
  </acceptance_criteria>
  <done>USD proven first-class through the existing FX path; no special-casing; currency scaffold green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: payment-settlement.ts — resolveSettlementCurrency + convertForSettlement</name>
  <behavior>
    - resolveSettlementCurrency({ contractorCurrency, perRunOverride? }): returns the override when set, else the contractor's currency (D-07 default). On a USD-receiving choice returns 'USD'. The perRunOverride is the value threaded from the 88-06 initiatePayout Zod input (and reused by the 88-04 export path).
    - convertForSettlement(db, amountMinor, fromCurrency, settlementCurrency, paymentDate): when from === settlement returns the amount unchanged (rate 1); else delegates to convertAmount at the payment-date ECB rate; returns null when the rate is missing (caller surfaces, does not silently zero).
    - The settlement amount is what the export file carries (USD or the converted local-currency amount).
  </behavior>
  <read_first>
    - packages/api/src/services/exchange-rate.ts:268-321 (convertAmount — the function to reuse, do NOT reimplement FX)
    - packages/api/src/services/form-1099-nec.service.ts (how convertAmount is consumed for box-1 payment-date conversion — mirror the call shape)
    - 88-RESEARCH.md D-07 + Code Examples + 88-PATTERNS.md "exchange-rate.ts → USD settlement"
  </read_first>
  <action>
    Create payment-settlement.ts with `resolveSettlementCurrency` and `convertForSettlement` per the behavior. Reuse `convertAmount` verbatim for FX (Don't-Hand-Roll: never new rate math). Default the settlement currency to Contractor.currency, overridable per run via the `perRunOverride` parameter (Claude's-discretion per CONTEXT — surface as a per-run choice; document the per-run vs per-org-default decision in the summary, defaulting to per-run-with-contractor-currency-fallback). Keep `resolveSettlementCurrency({ contractorCurrency, perRunOverride? })` and `convertForSettlement(db, amountMinor, fromCurrency, settlementCurrency, paymentDate)` signatures stable — they are the seam the 88-04 export path (`_buildExportItems`) and the 88-06 `initiatePayout` Zod-input override both consume. Annotate amounts as integer minor units, single HALF-UP round delegated to convertAmount (never chain). Create payment-settlement.test.ts asserting: override wins; default = contractor currency; same-currency rate 1; cross-currency uses the payment-date rate; missing rate → null. NO console.* (use @contractor-ops/logger if logging); NO phase/D-ID breadcrumbs.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- payment-settlement 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - payment-settlement.ts + its test exist; `pnpm --filter @contractor-ops/api test -- payment-settlement` GREEN.
    - convertForSettlement delegates to convertAmount (no reimplemented FX math).
    - Default settlement currency = Contractor.currency; per-run override (perRunOverride) honored; missing-rate → null (not silent zero).
    - Signatures match what 88-04 (_buildExportItems) and 88-06 (initiatePayout) consume — `resolveSettlementCurrency({ contractorCurrency, perRunOverride? })` + `convertForSettlement(db, amountMinor, fromCurrency, settlementCurrency, paymentDate)`.
    - `pnpm typecheck --filter=@contractor-ops/api` + `pnpm lint:logs` pass.
  </acceptance_criteria>
  <done>Settlement-currency resolution + conversion in place, reusing convertAmount; stable seam for the 88-04 export path + 88-06 override; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settlement input → export amount | The settlement currency choice + FX rate determine the money amount the export file carries; both are server-derived. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-05-01 | Tampering | settlement amount / FX rate | mitigate | Currency + rate are server-derived (Contractor.currency default + payment-date ECB rate via convertAmount); per-run override is a constrained enum (USD or contractor currency), not a free client amount. |
| T-88-05-02 | Tampering | missing-rate silent zero | mitigate | convertForSettlement returns null on a missing rate; the caller surfaces an error rather than settling a zeroed amount (Pitfall 3). |
| T-88-SC | Tampering | npm install | mitigate | No package installed in this plan. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- payment-currency exchange-rate payment-settlement` all GREEN.
- No USD=1.0 short-circuit added (F-1 honored).
- The service is the stable seam consumed by 88-04 (`_buildExportItems`) and 88-06 (`initiatePayout` override) — signatures unchanged.
- `pnpm typecheck --filter=@contractor-ops/api` + `pnpm lint:logs` pass.
</verification>

<success_criteria>
- USD proven first-class through the existing ECB FX path (no special-casing).
- Settlement-currency choice (USD or contractor local) at the payment-date rate, defaulting Contractor.currency.
- resolveSettlementCurrency + convertForSettlement are a wired seam (consumed by 88-04 export path + 88-06 override), not dead code.
</success_criteria>

<output>
Create `.planning/phases/88-theme-a-us-payment-rail/88-05-SUMMARY.md` when done.
</output>
