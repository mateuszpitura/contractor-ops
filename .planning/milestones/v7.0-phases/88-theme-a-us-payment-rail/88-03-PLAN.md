---
phase: 88-theme-a-us-payment-rail
plan: 03
type: execute
wave: 3
depends_on: ["88-01", "88-02"]
files_modified:
  - packages/api/src/routers/finance/payment-shared.ts
  - packages/api/src/services/tin-match.service.ts
autonomous: true
requirements: [US-PAY-01]
must_haves:
  truths:
    - "Implements D-01 (generalize the Saudi WHT path to one jurisdiction-agnostic withholding deduction at item seeding), D-02 (the recorded whtAmountMinor on PaymentRunItem is the single source of truth the 1099 box-4 / 1042-S box-2 aggregate), D-03 (wire the P86 tin-match setBackupWithholdingFlag port to the new column), D-13 (writeAuditLog on withholding application)"
    - "A US contractor with backupWithholdingFlagged=true has payout reduced by 24% (IRC §3406): amountMinor = grossAmountMinor − round(gross*24/100)"
    - "A foreign-recipient 1042-S payout is reduced by the applyTreaty rate (30% statutory when no treaty); a single HALF-UP round, gross/net invariant holds"
    - "The existing Saudi WHT path stays byte-identical (regression-green) — SA REGRESSION is the highest risk"
    - "tin-match setBackupWithholdingFlag writes Contractor.backupWithholdingFlagged (idempotent), last-4-only in audit metadata, never a full TIN"
  artifacts:
    - path: "packages/api/src/routers/finance/payment-shared.ts"
      provides: "applyWithholding — jurisdiction-agnostic deduction generalizing _applyWhtIfSaudi"
      contains: "backupWithholdingFlagged"
    - path: "packages/api/src/services/tin-match.service.ts"
      provides: "wired setBackupWithholdingFlag writing the new column"
      contains: "backupWithholdingFlagged"
  key_links:
    - from: "payment-shared.ts applyWithholding"
      to: "treaty-rate.service applyTreaty + tax-rate.service calculateWht"
      via: "per-item rate resolution by jurisdiction"
      pattern: "applyTreaty|calculateWht"
    - from: "payment-shared.ts applyWithholding"
      to: "PaymentRunItem.whtAmountMinor (source of truth, D-02)"
      via: "tx.paymentRunItem.update"
      pattern: "whtAmountMinor"
---

<objective>
Generalize the single-jurisdiction Saudi withholding path into one jurisdiction-agnostic withholding deduction applied at payment-run item seeding, and wire the P86 tin-match backup-withholding writer to the new queryable column. This is where reporting becomes real money — the payout actually shrinks.

Purpose: D-01/D-02 — the recorded withholding figure on PaymentRunItem becomes the single source of truth the 1099 box-4 / 1042-S box-2 aggregate (no recompute in the forms). D-03 closes the P86 loose end.
Output: `applyWithholding` (generalizing `_applyWhtIfSaudi`) with US 24% backup-withholding + 1042-S treaty branches added AROUND an unchanged SA path, plus a wired tin-match writer.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/88-theme-a-us-payment-rail/88-CONTEXT.md
@.planning/phases/88-theme-a-us-payment-rail/88-RESEARCH.md
@.planning/phases/88-theme-a-us-payment-rail/88-PATTERNS.md
@.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-CONTEXT.md
@.planning/phases/87-theme-a-1042-s-us-classification-determination-letter/87-CONTEXT.md

<interfaces>
<!-- Exact signatures the executor builds against. Extracted from codebase — no exploration needed. -->

From packages/api/src/routers/finance/payment-shared.ts (the function to generalize):
- `export async function _applyWhtIfSaudi(tx: TxClient, organizationId: string, paymentRunId: string): Promise<void>` (282-325). Early-returns when `org.countryCode !== 'SA'`. SA branch: for each item calls `calculateWht(org.countryCode, item.contractor.countryCode, 'technical_services', item.amountMinor)`, then `tx.paymentRunItem.update` writing grossAmountMinor=amountMinor, amountMinor=netAmountMinor, whtAmountMinor, whtRate, whtTreatyApplied, whtTreatyReference, whtServiceType='technical_services', and `tx.invoice.update({ withholdingMinor })`.
- Called from `seedRunItems` (line 155) after `createMany`, before the `IN_RUN` invoice flip.

From packages/api/src/services/tax-rate.service.ts:
- `calculateWht(orgCountry, contractorCountry, serviceType, amountMinor)` → `{ whtAmountMinor, netAmountMinor, whtRate, treatyApplied, treatyReference, rateSource } | null`. Already HALF-UP `Math.round((gross*rate)/100)`.

From packages/api/src/services/treaty-rate.service.ts:
- `applyTreaty({ contractorResidency })` → `TreatyDecision` with `.rate` (percent), `.article`, `.source` (30% statutory fallback). Returns a RATE, not an amount — bridge with `Math.round((gross*rate)/100)`.

From packages/api/src/services/tin-match.service.ts:
- `interface TinMatchPersistence { setBackupWithholdingFlag(input: { organizationId; recipientId; tinLast4 }): Promise<void>; ... }` (79).
- `interface DbBackedPersistenceWriters { setBackupWithholdingFlag(...); createEscalation(...); }` (268) — caller-supplied; currently unwired to a column.
- `createDbTinMatchPersistence(writers, tx?)` (290) — builds the production persistence; writes audit via writeAuditLog with `metadata: { responseIndicator, tinLast4 }` (last-4 only).

From packages/db (88-02): `Contractor.backupWithholdingFlagged Boolean?`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize _applyWhtIfSaudi into a jurisdiction-agnostic applyWithholding (SA path UNCHANGED)</name>
  <behavior>
    - US contractor, backupWithholdingFlagged=true: rate=24 (IRC §3406); whtAmountMinor = round(gross*24/100); amountMinor = gross − wht; whtRate=24, whtTreatyApplied=false.
    - US, foreign 1042-S recipient (not backup-flagged): rate from applyTreaty({contractorResidency}).rate (30% statutory if no treaty); whtTreatyApplied = (source==='treaty'); whtTreatyReference = article.
    - SA org cross-border item: IDENTICAL to today — calculateWht path, whtServiceType 'technical_services', invoice.withholdingMinor written. (regression-green)
    - No applicable rate (e.g. US contractor not flagged + no 1042-S basis, or domestic): item untouched, no write.
    - Single HALF-UP round at rate application; never chain rounds (gross/net integer invariant).
  </behavior>
  <read_first>
    - packages/api/src/routers/finance/payment-shared.ts:282-325 (the SA branch to preserve EXACTLY) + :133-161 (seedRunItems call site)
    - packages/api/src/services/tax-rate.service.ts:104-146 (calculateWht)
    - packages/api/src/services/treaty-rate.service.ts:144-175 (applyTreaty)
    - 88-PATTERNS.md "HIGHEST REGRESSION RISK" section + 88-RESEARCH.md Pattern 1
    - packages/api/src/services/__tests__/payment-withholding.test.ts (the RED scaffold from 88-01 to turn green)
  </read_first>
  <action>
    Rename `_applyWhtIfSaudi` to `applyWithholding` (keep `_applyWhtIfSaudi` as a thin re-export alias if any other caller imports it — grep first; update the seedRunItems call to `applyWithholding`). Widen the `org` select to also read `dataRegion`; widen the item `include` to select `contractor.{countryCode, backupWithholdingFlagged}`. Replace the `if (org.countryCode !== 'SA') return;` early-return with a per-item jurisdiction dispatch that KEEPS the SA branch verbatim (same calculateWht call, same write fields incl. whtServiceType 'technical_services' and the invoice.withholdingMinor update) and ADDS two branches AROUND it: (a) US source + contractor.backupWithholdingFlagged → rate=24 (IRC §3406; annotate "verify rate with adviser before production — local-only/legal-deferred"); (b) US source + foreign recipient (1042-S basis) → rate=applyTreaty({contractorResidency: item.contractor.countryCode}).rate. For the US branches, compute whtAmountMinor = `Math.round((grossAmountMinor * rate) / 100)` (ONE HALF-UP round — Pitfall 2), set grossAmountMinor=amountMinor, amountMinor = gross − wht, whtRate=rate, whtTreatyApplied/Reference per branch; when rate resolves null, leave the item untouched. Wrap a `writeAuditLog({ tx, organizationId, actorType:'SYSTEM', action:'payment_run.withholding_applied', resourceType:'PAYMENT_RUN_ITEM', resourceId:item.id, metadata:{ whtRate, whtAmountMinor, basis } })` on each applied item (D-13; no full TIN). Determine US-source via org dataRegion/countryCode === US — mirror the SA gate shape. NO fenced phase/D-ID comments; real domain IDs (§3406, 1042-S) allowed.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- payment-withholding tax-rate.service 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test -- payment-withholding` is GREEN (the 88-01 scaffold turns green).
    - The SA-regression `it` in payment-withholding.test.ts AND the existing tax-rate.service tests are GREEN (SA path byte-identical).
    - US 24% backup-withholding and 1042-S treaty branches each produce amountMinor = gross − HALF-UP(gross*rate/100).
    - A `payment_run.withholding_applied` audit row is written per applied item (assert via spy or the integration test).
    - `pnpm typecheck --filter=@contractor-ops/api` passes.
  </acceptance_criteria>
  <done>One withholding path covers SA + US-backup + 1042-S; SA regression green; gross/net invariant + single round verified; audit on apply.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the P86 tin-match setBackupWithholdingFlag port to Contractor.backupWithholdingFlagged</name>
  <read_first>
    - packages/api/src/services/tin-match.service.ts:75-113 (TinMatchPersistence) + :260-309 (DbBackedPersistenceWriters + createDbTinMatchPersistence)
    - packages/api/src/services/__tests__/tin-match.service.test.ts:30-100 (how setBackupWithholdingFlag is asserted today)
    - 88-PATTERNS.md "tin-match.service.ts" section (D-03; idempotent set; last-4-only PII discipline)
  </read_first>
  <action>
    Provide a concrete `setBackupWithholdingFlag` writer that the DB-backed caller (the P86 year-end batch / staff router) wires in: `setBackupWithholdingFlag: async ({ organizationId, recipientId }) => { await tx.contractor.update({ where: { id: recipientId }, data: { backupWithholdingFlagged: true } }); }` — an idempotent set scoped through the caller's tx. Keep the advisory never-block posture and the existing audit path (`metadata: { responseIndicator, tinLast4 }`, last-4 only — never a full TIN/SSN). Decide and document the backfill posture per 88-RESEARCH Open-Q1: forward-only (recommended) OR a one-time migration reading the latest TaxFormSubmission.snapshotJson per contractor — implement forward-only and add an adviser-verify note that pre-existing snapshot-flagged contractors read false until re-run through tin-match. Do not change the deterministic-mock default; only the DB-backed wiring gains a real column write.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- tin-match 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The DB-backed setBackupWithholdingFlag writes Contractor.backupWithholdingFlagged=true via the caller's tx (idempotent).
    - `pnpm --filter @contractor-ops/api test -- tin-match` GREEN (mismatch path still sets the flag; the writer is exercised).
    - No full TIN/SSN appears in any audit metadata (last-4 only).
    - Backfill posture documented (forward-only) with an adviser-verify note.
    - `pnpm typecheck --filter=@contractor-ops/api` passes.
  </acceptance_criteria>
  <done>tin-match writer persists the new column; advisory posture + PII discipline preserved; backfill documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payment-run seeding tx → PaymentRunItem | Withholding is computed server-side and written inside the seeding transaction; the recorded figure is authoritative (D-02). |
| tin-match → Contractor flag | The backup-withholding decision flips a column read at payout time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-03-01 | Tampering | applyWithholding amount | mitigate | Rate + amount derived server-side from org region + Contractor.backupWithholdingFlagged + applyTreaty; never from client input; written in the seeding tx with writeAuditLog (D-13). |
| T-88-03-02 | Tampering | SA WHT regression | mitigate | SA branch kept byte-identical; explicit SA-regression test asserts unchanged calculateWht path + whtServiceType + invoice.withholdingMinor write. |
| T-88-03-03 | Information Disclosure | TIN in audit metadata | mitigate | Reuse the tin-match last-4-only metadata discipline; full TIN/SSN never logged or audited. |
| T-88-03-04 | Repudiation | withholding application | mitigate | `payment_run.withholding_applied` audit row per applied item, committed atomically via the seeding tx. |
| T-88-SC | Tampering | npm install | mitigate | No package installed in this plan. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- payment-withholding tax-rate.service tin-match` all GREEN.
- SA-regression test green; gross/net integer invariant verified; single HALF-UP round.
- `pnpm typecheck --filter=@contractor-ops/api` + `pnpm lint:audit-log` pass.
</verification>

<success_criteria>
- One jurisdiction-agnostic withholding deduction (SA + US-backup 24% + 1042-S treaty), SA regression green.
- Recorded whtAmountMinor is the single source of truth (D-02), audited on apply.
- tin-match writer persists Contractor.backupWithholdingFlagged.
</success_criteria>

<output>
Create `.planning/phases/88-theme-a-us-payment-rail/88-03-SUMMARY.md` when done.
</output>
