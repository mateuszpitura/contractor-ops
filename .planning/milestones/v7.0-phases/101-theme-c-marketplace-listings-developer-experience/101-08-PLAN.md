---
phase: 101-theme-c-marketplace-listings-developer-experience
plan: 08
type: execute
wave: 3
depends_on: ["101-04"]
files_modified:
  - apps/public-api/src/app.ts
  - apps/public-api/src/routes/docs.ts
  - apps/public-api/src/lib/portal-content.ts
  - apps/public-api/CHANGELOG.md
  - apps/public-api/src/__tests__/developer-portal.test.ts
  - .env.example
autonomous: true
requirements: [INTEG-DX-01]

must_haves:
  truths:
    - "The developer portal EXTENDS the shipped 98-06 Scalar `/docs` mount (it does NOT replace it): the OpenAPI reference stays served by `Scalar({url:'/v1/openapi.json'})`, and new sibling pages hang off the same host behind a `module.developer-portal` gate — `/docs/webhooks` (the 16-event catalog + the X-CO-Signature contract), `/docs/sdks` (the @contractor-ops/sdk + contractor-ops-sdk install guides), `/docs/recipes` (Zapier/n8n/Make + the TS/Python/Go/PHP verifier snippets), `/docs/changelog`, `/docs/deprecations`"
    - "The webhook event-catalog page renders from `packages/validators/src/webhooks` (100) — the same source the marketplace triggers generate from — so the catalog cannot drift from the events the API actually emits"
    - "The generated collections (101-04) are downloadable from the portal at `/collections/{postman,insomnia}.json`; the SDK page cites the 98-11 targets; the deprecations page surfaces the RFC-8594 `Sunset` headers from `version-headers.ts` (98-05)"
    - "The whole portal (the new pages + the collection routes) 404s when `module.developer-portal` is off (ship-dark); the OpenAPI reference `/docs` behavior is unchanged for existing consumers (greening `developer-portal.test.ts`)"
    - "The public `developers.contractor-ops.{tld}` hostname is a deploy-time DNS/Render step (deferred, recorded in 101-10) — the portal runs behind its flag locally now; no `console.*`"
  artifacts:
    - path: "apps/public-api/src/routes/docs.ts"
      provides: "The portal routes (webhooks/sdks/recipes/changelog/deprecations + collections), flag-gated"
      contains: "module.developer-portal"
    - path: "apps/public-api/src/lib/portal-content.ts"
      provides: "The content builders — the webhook catalog from packages/validators/src/webhooks; the SDK/recipe/deprecation content"
      contains: "webhooks"
    - path: "apps/public-api/CHANGELOG.md"
      provides: "The API changelog surfaced by /docs/changelog"
      contains: "v1"
---

<objective>
Build the developer portal (INTEG-DX-01) by extending the shipped Scalar `/docs` mount with the webhook event
catalog, SDK install guides, sample recipes, a changelog, and RFC-8594 deprecation notices — all behind a
default-off `module.developer-portal` flag, all sourced from the same artifacts the API generates so nothing
drifts. This greens `developer-portal.test.ts`.

Output: the portal pages + the collection download routes + the changelog + the flag gate.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-CONTEXT.md
@.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-RESEARCH.md

<interfaces>
Extend, do not replace (101-RESEARCH § A3 + Pattern 5): the 98-06 `app.get('/docs', Scalar({url:'/v1/
openapi.json'}))` stays. Add a `requireFlag('module.developer-portal')` guard on `/docs/*` sibling pages +
the `/collections/*` routes (404 when off — a plain 404, not a redirect, so the surface is dark). The
webhook catalog page renders `packages/validators/src/webhooks/index.ts` (100 — the 16-event union +
metadata) so it matches the API's real events. The SDK page cites the `.speakeasy/workflow.yaml` targets
(`@contractor-ops/sdk` npm / `contractor-ops-sdk` PyPI, 98-11). The recipes page embeds the marketplace
recipes + the `apps/public-api/docs/webhooks/verifiers/{ts,py,go,php}` snippets (100). The deprecations page
reads the `Sunset` headers surfaced by `apps/public-api/src/lib/version-headers.ts` (98-05). Serve the
101-04 collections as static files. The pages can be server-rendered HTML/Markdown (no heavy client build) —
keep them self-contained. Add `module.developer-portal` to `.env.example` docs if a local override is
useful. No `console.*`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The portal routes (webhooks/sdks/recipes/changelog/deprecations) behind the flag</name>
  <files>apps/public-api/src/app.ts, apps/public-api/src/routes/docs.ts, apps/public-api/src/lib/portal-content.ts, apps/public-api/CHANGELOG.md</files>
  <read_first>
    - 101-RESEARCH.md § A3 + Pattern 5 + Pitfall 8
    - apps/public-api/src/app.ts (the 98-06 Scalar /docs mount + the middleware chain)
    - packages/validators/src/webhooks/index.ts (the catalog to render) + apps/public-api/src/lib/version-headers.ts (Sunset)
    - .speakeasy/workflow.yaml (the SDK targets to document) + apps/public-api/docs/webhooks/verifiers/* (the recipe snippets)
  </read_first>
  <action>
    Author `portal-content.ts` (build the webhook-catalog view from the events union; the SDK-install content
    from the Speakeasy targets; the recipe content from the marketplace defs + verifier snippets; the
    deprecation list from the Sunset headers). Author `docs.ts`: `/docs/webhooks`, `/docs/sdks`,
    `/docs/recipes`, `/docs/changelog`, `/docs/deprecations` + `/collections/{postman,insomnia}.json`, all
    behind `requireFlag('module.developer-portal')` (404 off). Mount in `app.ts` alongside the existing
    Scalar `/docs`. Author `CHANGELOG.md` (the v1 surface). No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/public-api test developer-portal 2>&1 | tail -16; pnpm typecheck --filter @contractor-ops/public-api 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - The five portal pages + the two collection routes serve when the flag is on and 404 when off.
    - The Scalar `/docs` OpenAPI reference is unchanged; the webhook page renders the catalog events.
    - `developer-portal.test.ts` is GREEN.
  </acceptance_criteria>
  <done>The portal pages + collection downloads built behind the flag.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the deprecations + SDK + recipe content to real sources; env docs</name>
  <files>apps/public-api/src/lib/portal-content.ts, apps/public-api/src/__tests__/developer-portal.test.ts, .env.example</files>
  <read_first>
    - apps/public-api/src/lib/version-headers.ts (the Sunset header source) + 98-05 pagination/versioning
    - the 101-04 collection serve paths + the 101-06/101-07 recipe sources
  </read_first>
  <action>
    Ensure the deprecations page reflects the actual `Sunset`/`Deprecation` headers (RFC 8594), the SDK page
    the real npm/PyPI package names, and the recipes the real verifier snippets + marketplace defs — assert
    each in `developer-portal.test.ts` (the content is sourced, not hardcoded prose). Document
    `module.developer-portal` + the portal hostname note in `.env.example`. No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/public-api test developer-portal 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - The deprecation/SDK/recipe content is sourced from the real headers/targets/snippets (asserted).
    - `.env.example` documents the portal flag + the deferred hostname.
  </acceptance_criteria>
  <done>Portal content wired to real sources; env documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public internet -> portal | The portal is public content; it must expose only documentation, never a tenant surface or a live key |
| API artifacts -> portal content | The catalog/SDK/deprecation content must track the real API; drift misleads integrators |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-08-01 | Security Misconfiguration | The portal reachable when it should be dark | mitigate | `module.developer-portal` gate -> 404 on every portal route |
| T-101-08-02 | Information Disclosure | A tenant/key value in a portal page | mitigate | Content is built from public artifacts only; no tenant/key input |
| T-101-08-03 | Repudiation | A deprecation not surfaced, an integrator surprised | mitigate | The deprecations page reads the real `Sunset` headers (RFC 8594), asserted |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/public-api test developer-portal` — GREEN (pages serve on; 404 off; content sourced).
- `pnpm typecheck --filter @contractor-ops/public-api` — clean.
- The Scalar `/docs` reference is unchanged for existing consumers.
</verification>

<success_criteria>
A developer visits the portal and finds the OpenAPI reference, the webhook event catalog, SDK install guides,
sample recipes, a changelog, and deprecation notices — all sourced from the same artifacts the API generates,
behind a default-off flag, with the public hostname the only deferred step.
</success_criteria>

<output>
Create `.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-08-SUMMARY.md` when done.
Record the portal routes + the hostname note for the 101-10 EXTERNAL-ENABLEMENT row.
</output>
