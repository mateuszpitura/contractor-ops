---
phase: 101-theme-c-marketplace-listings-developer-experience
plan: 05
type: execute
wave: 1
depends_on: ["101-01"]
files_modified:
  - packages/db/prisma/schema/incident.prisma
  - packages/api/src/services/status-aggregator.ts
  - packages/api/src/routers/core/incident.ts
  - packages/api/src/root.ts
  - apps/public-api/src/routes/status.ts
  - apps/public-api/src/app.ts
  - packages/api/src/services/audit-writer.ts
autonomous: true
requirements: [INTEG-DX-03]

must_haves:
  truths:
    - "A public, UNAUTHENTICATED, short-cached `/status.json` endpoint (mounted OUTSIDE `apiKeyTenantProcedure`, before the flag/key gates) reports three coarse component states — `api`, `webhooks-dispatcher`, `background-jobs` — each `operational|degraded|down`, mapped from the SHIPPED health sources (no new monitoring stack)"
    - "The aggregator reuses the existing signals: the `apps/api/src/routes/health.ts` probes (api), the `job-health.ts` pending/failure counts + `FAILURE_ALERT_THRESHOLD` + the 100 delivery gauges (webhooks-dispatcher), and the `cron-worker/health.ts` last-success-per-job + `cron-monitor.ts` heartbeats (background-jobs)"
    - "The response exposes NO tenant data: no organizationId, no per-org metric, no raw probe body — only the three component states + `updatedAt` + open incidents (greening `status-page.test.ts`'s no-tenant-data assertion)"
    - "An `IncidentReport` model (title, status OPEN|MONITORING|RESOLVED, severity, startedAt, resolvedAt?, updates Json[], componentsAffected String[]) + a staff `incidentRouter` (create/update/resolve, audited `INCIDENT` entity type) provides incident history; open/recent incidents render in `/status.json`"
    - "The endpoint is gated behind `module.public-status-page` (404 when off — ship-dark), fail-safe (a probe error degrades that component, never 500s the whole page), and cached (short TTL) so it survives a status-check storm; no `console.*`"
  artifacts:
    - path: "apps/public-api/src/routes/status.ts"
      provides: "The public `/status.json` endpoint (unauthenticated, flag-gated, cached, no tenant data)"
      contains: "status.json"
    - path: "packages/api/src/services/status-aggregator.ts"
      provides: "`aggregateStatus()` — maps the shipped health sources into 3 coarse component states"
      contains: "operational"
    - path: "packages/db/prisma/schema/incident.prisma"
      provides: "The `IncidentReport` model — the status page's incident history"
      contains: "MONITORING"
---

<objective>
Build the backend for the public status page (INTEG-DX-03) by aggregating the SHIPPED health-monitoring
surface — not a new stack — into a public, unauthenticated `/status.json` that reports three coarse component
states plus incident history, exposing zero tenant data. This greens `status-page.test.ts` and feeds the
status page front-end (101-09).

Output: the `/status.json` aggregator endpoint + the `IncidentReport` model + staff router, flag-gated +
cached + tenant-data-free.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-CONTEXT.md
@.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-RESEARCH.md

<interfaces>
The health sources to WRAP (101-RESEARCH § A4 + Pattern 4): `apps/api/src/routes/health.ts` (public
probes; the aggregator maps all-ok->operational, any-degraded->degraded), `apps/cron-worker/src/jobs/handlers/
job-health.ts` (`FAILURE_ALERT_THRESHOLD` + pendingCount/recentFailureCount — the dispatcher-health rule),
`apps/cron-worker/src/health.ts` (last-success-per-job) + `packages/api/src/services/cron-monitor.ts`
(Cronitor heartbeats — the background-jobs component). The `/status.json` route mounts in `apps/public-api/
src/app.ts` BEFORE the `apiKeyTenantProcedure`/flag chain (it is public + unauthenticated) but behind a
`module.public-status-page` gate (404 when off). It MUST NOT return any tenant field — write a test-mirrored
deep-scan invariant (no `organizationId`, no per-org metric, no raw probe body). Fail-safe: wrap each
component probe in a try/catch so one failing probe degrades that component, never 500s the page. Cache with
a short TTL (reuse the existing cache util) so a status-check storm does not hammer the health checks. The
`IncidentReport` router mirrors `api-key.ts` (admin-gated, Zod, `writeAuditLog` with a new `INCIDENT`
entity type). One migration adds the model; mount `incidentRouter` in `root.ts`. No `console.*`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The status aggregator + the public /status.json endpoint</name>
  <files>packages/api/src/services/status-aggregator.ts, apps/public-api/src/routes/status.ts, apps/public-api/src/app.ts</files>
  <read_first>
    - 101-RESEARCH.md § A4 + Pattern 4 + Pitfall 5/8
    - apps/api/src/routes/health.ts (the probe shape + the body-trim precedent for network exposure)
    - apps/cron-worker/src/jobs/handlers/job-health.ts (FAILURE_ALERT_THRESHOLD + counts) + apps/cron-worker/src/health.ts
    - apps/public-api/src/app.ts (the middleware chain — mount /status.json BEFORE the key/flag chain, behind its own flag)
  </read_first>
  <action>
    Author `aggregateStatus()` -> `{ updatedAt, components: { api, dispatcher, jobs }, incidents }` where each
    component is `operational|degraded|down` from its source (api = health probes; dispatcher =
    `recentFailureCount <= FAILURE_ALERT_THRESHOLD && pending <= 100`; jobs = last-success within interval +
    heartbeats). Wrap each probe in try/catch (a failure degrades that component only). Mount `/status.json`
    in `app.ts` as a public, unauthenticated route gated by `module.public-status-page` (404 off), cached
    (short TTL). Assert (mirror the test) the payload carries NO tenant data. No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/public-api test status-page 2>&1 | tail -16; pnpm typecheck --filter @contractor-ops/public-api --filter @contractor-ops/api 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `/status.json` returns the three component states mapped from the shipped sources; a failing probe
      degrades only its component (no 500).
    - The payload has no organizationId/tenant field/raw probe body; 404 when the flag is off.
    - `status-page.test.ts`'s aggregation + no-tenant-data assertions are GREEN.
  </acceptance_criteria>
  <done>The public status aggregator + endpoint built.</done>
</task>

<task type="auto">
  <name>Task 2: The IncidentReport model + staff router + incident history in /status.json</name>
  <files>packages/db/prisma/schema/incident.prisma, packages/api/src/routers/core/incident.ts, packages/api/src/root.ts, packages/api/src/services/audit-writer.ts</files>
  <read_first>
    - 101-RESEARCH.md § A4 (IncidentReport shape)
    - packages/api/src/routers/core/api-key.ts (the staff-router + audit pattern)
    - packages/api/src/root.ts (the namespace mount + count) + services/audit-writer.ts (AuditEntityType)
  </read_first>
  <action>
    Add the `IncidentReport` model (one migration; `pnpm --filter @contractor-ops/db generate`). Author
    `incidentRouter` (admin-gated): `create`, `addUpdate`, `resolve`, `list` — each mutation `writeAuditLog`
    with a new `INCIDENT` entity type. Wire open/recent incidents into `aggregateStatus().incidents` (title,
    status, severity, componentsAffected, latest update — no tenant data). Mount in `root.ts`. No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/public-api test status-page 2>&1 | tail -8; pnpm --filter @contractor-ops/api test incident 2>&1 | tail -8; pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/db 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Incidents persist + audit; open incidents render in `/status.json` with no tenant data.
    - The router is mounted in `root.ts`; `AuditEntityType` has `INCIDENT`; typecheck clean.
  </acceptance_criteria>
  <done>Incident history model + router wired into the status page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| public internet -> /status.json | The endpoint is unauthenticated; it must expose only coarse component states + incidents, never tenant data or raw probe internals |
| health sources -> aggregator | A failing health probe must degrade a component, never crash the public page |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-05-01 | Information Disclosure | The status page leaking a tenant metric or raw probe body | mitigate | A deep-scan invariant asserts no organizationId/tenant/raw-probe field; only 3 coarse states + incidents |
| T-101-05-02 | Denial of Service | A status-check storm hammering the health checks | mitigate | Short-TTL cache in front of the aggregator; fail-safe per-component try/catch |
| T-101-05-03 | Security Misconfiguration | The status page reachable when it should be dark | mitigate | `module.public-status-page` gate -> 404 when off |
| T-101-05-SC | Tampering | No new deps | accept | Reuses the shipped health sources + cache util |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/public-api test status-page` — GREEN (aggregation + no-tenant-data + 404-when-off).
- `pnpm --filter @contractor-ops/api test incident` — GREEN.
- `pnpm typecheck --filter @contractor-ops/public-api --filter @contractor-ops/api --filter @contractor-ops/db` — clean; both routers mounted.
</verification>

<success_criteria>
The public status page has a truthful, tenant-data-free `/status.json` that reflects real API +
webhook-dispatcher + background-job health from the shipped monitoring, plus an operator-authored incident
history — behind a default-off flag, cached, and fail-safe.
</success_criteria>

<output>
Create `.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-05-SUMMARY.md` when done.
Record the `/status.json` shape for the 101-09 status page front-end.
</output>
