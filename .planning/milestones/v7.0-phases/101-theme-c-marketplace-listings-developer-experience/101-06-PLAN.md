---
phase: 101-theme-c-marketplace-listings-developer-experience
plan: 06
type: execute
wave: 2
depends_on: ["101-03"]
files_modified:
  - packages/n8n-nodes/package.json
  - packages/n8n-nodes/tsconfig.json
  - packages/n8n-nodes/vitest.config.ts
  - packages/n8n-nodes/src/nodes/ContractorOps/ContractorOps.node.ts
  - packages/n8n-nodes/src/nodes/ContractorOpsTrigger/ContractorOpsTrigger.node.ts
  - packages/n8n-nodes/src/credentials/ContractorOpsApi.credentials.ts
  - packages/n8n-nodes/src/__tests__/nodes.test.ts
  - packages/n8n-nodes/workflows/invoice-to-slack.json
  - packages/n8n-nodes/workflows/contractor-onboard-from-personio.json
  - packages/n8n-nodes/workflows/compliance-expiry-to-pagerduty.json
  - packages/n8n-nodes/README.md
  - .github/workflows/publish-n8n-nodes.yml
autonomous: true
requirements: [INTEG-N8N-01, INTEG-N8N-02]

must_haves:
  truths:
    - "A community node package `@contractor-ops/n8n-nodes` builds (`tsc`) with a regular node (the write actions), a trigger node (the webhook events), and an apiKey credential — all IMPORTING the 101-03 generated descriptions (not hand-authored), so it mirrors the Zapier trigger/action surface"
    - "The package `package.json` declares the n8n contract (`n8n.nodes` + `n8n.credentials` + `keywords:['n8n-community-node-package']`) so it is installable via the n8n community-nodes UI once published; the nodes + credential load without error (greening `packages/n8n-nodes` tests)"
    - "Three example workflows ship as valid n8n JSON exports — invoice->Slack, contractor-onboard-from-Personio, compliance-expiry->PagerDuty (INTEG-N8N-02) — plus a README docs page with install + auth + node-usage"
    - "The npm publish is a DARK CI job (`publish-n8n-nodes.yml`, `workflow_dispatch`/guarded, disabled by default) requiring `NPM_TOKEN` — mirror the 98-11 Speakeasy SDK publish posture; the package builds + tests now with NO publish on merge (INTEG-N8N-01 enable is an EXTERNAL-ENABLEMENT row)"
    - "Any new dev-dependency (`n8n-workflow` types, the node test harness) is pinned to a version >=7 days old (`.npmrc min-release-age=7`); `pnpm audit` + `pnpm security:scan` pass after the add; typosquat-checked; no `console.*`"
  artifacts:
    - path: "packages/n8n-nodes/package.json"
      provides: "The `@contractor-ops/n8n-nodes` community package manifest (n8n.nodes/credentials contract)"
      contains: "n8n-community-node-package"
    - path: "packages/n8n-nodes/src/nodes/ContractorOpsTrigger/ContractorOpsTrigger.node.ts"
      provides: "The trigger node — webhook-event subscriptions from the generated descriptions"
      contains: "ContractorOpsTrigger"
    - path: ".github/workflows/publish-n8n-nodes.yml"
      provides: "The DARK npm-publish CI job (disabled by default; NPM_TOKEN-gated) — mirror 98-11"
      contains: "workflow_dispatch"
---

<objective>
Build the `@contractor-ops/n8n-nodes` community node package (INTEG-N8N-01/-02) — the launch-day, self-serve
integration story — by importing the 101-03 generated node descriptions, shipping three example workflows +
docs, and wiring a DARK npm-publish job (no partner review needed; the publish token is the only external
dependency). This is the n8n mirror of the Zapier surface, built + tested now, publish-deferred.

Output: the n8n node package (regular + trigger node + credential) + three example workflows + README + the
dark publish job.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-CONTEXT.md
@.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-RESEARCH.md

<interfaces>
Import the generated descriptions from `@contractor-ops/marketplace-manifests` (101-03 `generate-n8n.ts`) —
do NOT hand-author the node's operation list. The n8n community-package contract (101-RESEARCH § B):
`package.json` needs `n8n: { nodes: [...compiled node paths...], credentials: [...] }` +
`keywords: ['n8n-community-node-package']`. The regular node exposes the write actions; the trigger node
subscribes to the webhook events (via the public `webhooks:manage` surface from 99/100) or polls; the
credential is the API key (`co_live_`/`co_test_`). Scaffold config by mirroring a leaf package
(`packages/validators`). The publish job MIRRORS 98-11's Speakeasy publish (dark: `workflow_dispatch` +
guarded on `NPM_TOKEN`; NOT triggered on push) — do context7 lookup for the current n8n community-package +
`n8n-workflow` conventions rather than guessing. New deps obey the 7-day floor: pin >=7 days old, `pnpm
audit` + `pnpm security:scan` + typosquat-check after add; if `n8n-workflow` cannot be pinned >=7 days old,
STOP and surface it (do not bypass). No `console.*`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The n8n node package (nodes + credential from the generated descriptions)</name>
  <files>packages/n8n-nodes/package.json, packages/n8n-nodes/tsconfig.json, packages/n8n-nodes/vitest.config.ts, packages/n8n-nodes/src/nodes/ContractorOps/ContractorOps.node.ts, packages/n8n-nodes/src/nodes/ContractorOpsTrigger/ContractorOpsTrigger.node.ts, packages/n8n-nodes/src/credentials/ContractorOpsApi.credentials.ts, packages/n8n-nodes/src/__tests__/nodes.test.ts</files>
  <read_first>
    - 101-RESEARCH.md § A6 (publishable-package posture) + § B (n8n shape)
    - packages/marketplace-manifests/src/generate-n8n.ts (the descriptions to import — 101-03)
    - packages/validators/package.json + tsconfig.json (the leaf-package scaffold)
    - context7: the current n8n community-node-package + credential conventions (do not guess the API)
  </read_first>
  <action>
    Scaffold `@contractor-ops/n8n-nodes`. Author the regular node (write actions), the trigger node
    (webhook events), and the apiKey credential — importing the 101-03 generated descriptions. Declare the
    `n8n` contract + the community keyword in `package.json`. Author `nodes.test.ts`: the node + trigger +
    credential descriptions load, every operation maps to a generated action/trigger, the credential injects
    the API-key header. Add any `n8n-workflow` dev-dep at >=7 days old; `pnpm audit` + `pnpm security:scan`.
    No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/n8n-nodes test 2>&1 | tail -16; pnpm --filter @contractor-ops/n8n-nodes build 2>&1 | tail -6; pnpm audit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The package builds; the nodes + credential load; operations map to the generated surface.
    - Any new dep is >=7 days old; `pnpm audit` clean.
    - `packages/n8n-nodes` tests GREEN.
  </acceptance_criteria>
  <done>The n8n node package built from the generated descriptions.</done>
</task>

<task type="auto">
  <name>Task 2: Example workflows + README + the DARK npm-publish job</name>
  <files>packages/n8n-nodes/workflows/invoice-to-slack.json, packages/n8n-nodes/workflows/contractor-onboard-from-personio.json, packages/n8n-nodes/workflows/compliance-expiry-to-pagerduty.json, packages/n8n-nodes/README.md, .github/workflows/publish-n8n-nodes.yml</files>
  <read_first>
    - 101-RESEARCH.md § B (the three named example workflows) + § A6/A7 (dark publish)
    - .github/workflows for the 98-11 Speakeasy publish job (the dark-publish template to mirror)
  </read_first>
  <action>
    Author three valid n8n workflow JSON exports (invoice->Slack, contractor-onboard-from-Personio,
    compliance-expiry->PagerDuty) using the package's nodes; a test parses them as valid n8n JSON. Author a
    README (install via community-nodes UI, auth with a `co_live_`/`co_test_` key, node usage, the three
    recipes). Author `.github/workflows/publish-n8n-nodes.yml` as a DARK job: `workflow_dispatch` only (never
    on push), guarded on `NPM_TOKEN` presence, `pnpm --filter @contractor-ops/n8n-nodes build` + `npm publish`
    — disabled by default (mirror 98-11). No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/n8n-nodes test 2>&1 | tail -8; node -e "['invoice-to-slack','contractor-onboard-from-personio','compliance-expiry-to-pagerduty'].forEach(w=>JSON.parse(require('fs').readFileSync('packages/n8n-nodes/workflows/'+w+'.json')));console.log('workflows-parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The three example workflows parse as valid n8n JSON; the README documents install + auth + recipes.
    - The publish job is dark (`workflow_dispatch`, `NPM_TOKEN`-gated, not on push).
  </acceptance_criteria>
  <done>Example workflows + docs + dark publish job shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| generated descriptions -> n8n nodes | The node surface must equal the generated surface; a hand-edited node drifts from the API |
| CI -> npm | Publishing must not happen without an explicit token + dispatch; a stray publish leaks a half-baked package |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-06-01 | Tampering | A supply-chain-compromised n8n dep | mitigate | 7-day release-age pin + `pnpm audit` + `pnpm security:scan` + typosquat-check |
| T-101-06-02 | Elevation of Privilege | An accidental npm publish on merge | mitigate | Dark `workflow_dispatch` job, `NPM_TOKEN`-gated, never on push (mirror 98-11) |
| T-101-06-03 | Information Disclosure | The node embedding a secret | mitigate | Auth is the apiKey credential (user-supplied); no secret in the package |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/n8n-nodes test` + `build` — GREEN.
- The three example workflows parse; the publish job is dark.
- `pnpm audit` + `pnpm security:scan` clean after any dep add.
</verification>

<success_criteria>
`@contractor-ops/n8n-nodes` is a build-verified, community-installable package whose nodes mirror the
generated Zapier surface, ships three real example workflows + docs, and publishes only via a deliberate,
token-gated CI dispatch — the launch-day self-serve integration, with the npm publish as the sole deferred step.
</success_criteria>

<output>
Create `.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-06-SUMMARY.md` when done.
Record the publish-enable steps (NPM_TOKEN + dispatch) for the 101-10 EXTERNAL-ENABLEMENT row.
</output>
