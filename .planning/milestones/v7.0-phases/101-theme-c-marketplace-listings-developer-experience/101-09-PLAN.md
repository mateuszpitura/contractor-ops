---
phase: 101-theme-c-marketplace-listings-developer-experience
plan: 09
type: execute
wave: 3
depends_on: ["101-03", "101-05"]
files_modified:
  - apps/web-vite/src/components/settings/marketplace-tab.tsx
  - apps/web-vite/src/components/settings/marketplace/listing-card.tsx
  - apps/web-vite/src/components/settings/hooks/use-marketplace-tab.ts
  - apps/web-vite/src/components/settings/api-keys-tab.tsx
  - apps/web-vite/src/components/settings/hooks/use-api-keys-tab.ts
  - apps/web-vite/src/i18n/messages/en/settings.json
  - apps/web-vite/src/components/settings/__tests__/marketplace-tab.test.tsx
  - apps/landing/app/status/page.tsx
  - apps/landing/app/status/status-view.tsx
  - apps/landing/app/status/__tests__/status.test.tsx
autonomous: false
requirements: [INTEG-MARKETPLACE-01, INTEG-DX-03, INTEG-DX-04]

must_haves:
  truths:
    - "Settings -> Developer gains a Marketplace surface (a sub-tab extending the 99-07 Developer page) that renders the three `MarketplaceListing` rows (101-03): per-platform status badge (DRAFT/SUBMITTED/IN_REVIEW/LIVE/REJECTED/NEEDS_CHANGES), version pin, last review feedback, and a state-advance control — through the container+hooks layers (the hook is the ONLY tRPC boundary), with loading/empty/error states (greening `marketplace-tab.test.tsx`)"
    - "The Developer page's API-keys surface gains a 'Create sandbox key' action that calls `api-key.createSandboxKey` (101-02) and shows the one-time `co_test_` key + the sandbox-org note + the 100/day limit (INTEG-DX-04 UI)"
    - "A public status page renders in `apps/landing` at `/status`: it fetches `/status.json` (101-05) and shows the three component states (operational/degraded/down) + the incident history — with loading/empty/error + accessible status semantics (color is not the only signal); the `status.contractor-ops.{tld}` hostname is a deferred DNS step"
    - "All UI follows `apps/web-vite/ARCHITECTURE.md` (page = thin composer, container = sections, hook = tRPC boundary, component = presentational) + WCAG (keyboard, focus, contrast, semantic HTML); i18next keys added for en (de/pl/ar parity, native review deferred); no direct `useTRPC`/`useQuery` in a page/component"
    - "The marketplace tab is Enterprise/admin-gated like the api-keys tab; no `console.*`; the status page exposes no tenant data (it only renders the public `/status.json`)"
  artifacts:
    - path: "apps/web-vite/src/components/settings/marketplace-tab.tsx"
      provides: "The Marketplace listing-status container (states + version pins + feedback + advance)"
      contains: "MarketplaceListing"
    - path: "apps/web-vite/src/components/settings/hooks/use-marketplace-tab.ts"
      provides: "The single tRPC boundary — list/update marketplace listings + createSandboxKey"
      contains: "useMarketplace"
    - path: "apps/landing/app/status/status-view.tsx"
      provides: "The public status page view — renders /status.json component states + incidents"
      contains: "operational"
---

<objective>
Build the two front-ends over the 101-03/101-05 backends: the internal marketplace listing-status dashboard +
the sandbox-key create action (Settings -> Developer, INTEG-MARKETPLACE-01 + INTEG-DX-04 UI), and the public
status page (`apps/landing`, INTEG-DX-03). This greens `marketplace-tab.test.tsx` + the landing `status`
test. Human-verify checkpoint on the visual pass.

Output: the Marketplace sub-tab + the sandbox-key action + the public status page, all container+hooks + WCAG
+ i18n.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-CONTEXT.md
@.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-RESEARCH.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
Follow the binding UI workflow (CLAUDE.md § UI skills): `frontend-design` + `semble search` before edits in
`apps/web-vite` / `apps/landing`, then impeccable for the product dashboard. Extend the 99-07 Developer page:
`apps/web-vite/src/components/settings/api-keys-tab.tsx` + `hooks/use-api-keys-tab.ts` are the CRUD pattern —
the Marketplace sub-tab mirrors it (container `marketplace-tab.tsx`, hook `use-marketplace-tab.ts` = the ONLY
tRPC boundary calling `marketplaceListing.list/update` + `api-key.createSandboxKey`, presentational
`listing-card.tsx`). Layer rules (`apps/web-vite/ARCHITECTURE.md`): page = thin composer, no `useTRPC` in a
page/component; loading/empty/error in the container. i18n: add en keys (`i18n/messages/en/settings.json`);
de/pl/ar parity (native review deferred — EXTERNAL-ENABLEMENT #9). The status page is a lean
`apps/landing/app/status/` route (Next) that fetches the public `/status.json` (101-05) — server-render the
initial state; the view is presentational + accessible (status conveyed by text + icon, not color alone).
Run `pnpm check:web-vite-data-layer` after the web-vite hook+container. NEVER run the full unscoped web-vite
suite — scope with `--filter` + a path. No `console.*`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The Marketplace listing-status sub-tab + the sandbox-key create action</name>
  <files>apps/web-vite/src/components/settings/marketplace-tab.tsx, apps/web-vite/src/components/settings/marketplace/listing-card.tsx, apps/web-vite/src/components/settings/hooks/use-marketplace-tab.ts, apps/web-vite/src/components/settings/api-keys-tab.tsx, apps/web-vite/src/components/settings/hooks/use-api-keys-tab.ts, apps/web-vite/src/i18n/messages/en/settings.json, apps/web-vite/src/components/settings/__tests__/marketplace-tab.test.tsx</files>
  <read_first>
    - apps/web-vite/ARCHITECTURE.md (the layer rules) + CLAUDE.md § web-vite UI layers
    - apps/web-vite/src/components/settings/api-keys-tab.tsx + hooks/use-api-keys-tab.ts (the 99-07 pattern to mirror + extend)
    - 101-RESEARCH.md § B (MarketplaceListing shape) + § A5 (createSandboxKey)
    - packages/api/src/routers/core/marketplace-listing.ts (101-03) + api-key.ts createSandboxKey (101-02)
  </read_first>
  <action>
    Author `use-marketplace-tab.ts` (the tRPC boundary: `marketplaceListing.list`/`update` +
    `api-key.createSandboxKey`), `marketplace-tab.tsx` (the container: loading/empty/error, the three
    listing cards, the advance control), `listing-card.tsx` (presentational: status badge, version pin,
    feedback). Extend `api-keys-tab.tsx` + its hook with a "Create sandbox key" action showing the one-time
    `co_test_` key + the 100/day + sandbox-org note. Add en i18n keys. Author `marketplace-tab.test.tsx`
    (states + advance + createSandboxKey). Run `pnpm check:web-vite-data-layer`. No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite test marketplace 2>&1 | tail -16; pnpm check:web-vite-data-layer 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - The Marketplace tab renders the three listings with states/version/feedback + advance; loading/empty/error present.
    - The sandbox-key action mints + reveals a `co_test_` key once; the hook is the only tRPC boundary.
    - `marketplace-tab.test.tsx` GREEN; `check:web-vite-data-layer` passes.
  </acceptance_criteria>
  <done>Marketplace dashboard + sandbox-key UI built on the container+hooks layers.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: The public status page (apps/landing) + visual pass</name>
  <files>apps/landing/app/status/page.tsx, apps/landing/app/status/status-view.tsx, apps/landing/app/status/__tests__/status.test.tsx</files>
  <read_first>
    - 101-RESEARCH.md § A4 (the /status.json shape) + § E (the landing status test)
    - apps/landing (the shell + the frontend-design/design-taste routing for a marketing surface)
    - packages/api/src/services/status-aggregator.ts (the /status.json contract from 101-05)
  </read_first>
  <action>
    Author `apps/landing/app/status/page.tsx` (server-fetch `/status.json`, pass to the view) +
    `status-view.tsx` (presentational: the three component states with text+icon+color, the incident
    history, an "all systems operational" empty state, a fetch-error state). Author `status.test.tsx`
    (renders operational/degraded/down + incidents). Accessible: status is conveyed by text + icon, not
    color alone; semantic landmarks; keyboard-navigable. HUMAN-VERIFY GATE: render the page (light/dark,
    mobile, RTL) + confirm the status colors + incident layout. No tenant data is rendered (only
    `/status.json`). No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/landing test status 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - The status page renders the three states + incidents from `/status.json` with loading/empty/error.
    - Status is not color-only; the page is keyboard-accessible; no tenant data is rendered.
    - Human-verify: the visual pass is confirmed (or deferred to the US/GA visual pass per EXTERNAL-ENABLEMENT #10).
  </acceptance_criteria>
  <done>Public status page built + visually verified (or visual pass explicitly deferred).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| staff -> marketplace state | Only an admin advances a listing; the UI enforces the same admin gate as the api-keys tab |
| public internet -> status page | The status page renders only the public `/status.json`; it must never fetch a tenant surface |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-09-01 | Elevation of Privilege | A non-admin advancing a listing | mitigate | The Marketplace tab is Enterprise/admin-gated (mirror the api-keys tab); the server router re-checks |
| T-101-09-02 | Information Disclosure | The status page rendering a tenant metric | mitigate | It renders only `/status.json` (no tenant data by construction, 101-05) |
| T-101-09-03 | Information Disclosure | The one-time `co_test_` key persisting in the UI | mitigate | Reveal-once (mirror the api-key create flow); never re-fetchable |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/web-vite test marketplace` — GREEN; `pnpm check:web-vite-data-layer` passes.
- `pnpm --filter @contractor-ops/landing test status` — GREEN.
- Human-verify: the status + marketplace visual pass (RTL/dark/mobile) confirmed or explicitly deferred.
</verification>

<success_criteria>
An operator tracks all three marketplace listings' review state from Settings -> Developer and mints a
sandbox key in one click; the public visits a truthful, accessible status page — both rendering the
101-03/101-05 backends through the container+hooks layers with no tenant leakage.
</success_criteria>

<output>
Create `.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-09-SUMMARY.md` when done.
Record any deferred visual-verify items for EXTERNAL-ENABLEMENT #10.
</output>
