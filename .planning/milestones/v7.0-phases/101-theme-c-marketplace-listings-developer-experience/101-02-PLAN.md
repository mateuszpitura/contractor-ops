---
phase: 101-theme-c-marketplace-listings-developer-experience
plan: 02
type: execute
wave: 1
depends_on: ["101-01"]
files_modified:
  - packages/db/prisma/schema/organization.prisma
  - packages/db/prisma/schema/api-key.prisma
  - packages/api/src/services/api-key-service.ts
  - packages/api/src/lib/demo.ts
  - packages/api/src/lib/api-tier-limits.ts
  - packages/api/src/services/api-quota-counter.ts
  - packages/api/src/middleware/api-tier-quota.ts
  - packages/api/src/services/sandbox-provisioning.ts
  - packages/api/src/routers/core/api-key.ts
  - packages/feature-flags/src/flags-core.ts
  - packages/feature-flags/src/signoff-registry-flags.ts
  - .env.example
autonomous: true
requirements: [INTEG-DX-04]

must_haves:
  truths:
    - "A sandbox tier exists as a NEW environment axis on the API key: `OrganizationApiKey.environment` (`LIVE`|`SANDBOX`, default LIVE) + `Organization.isSandbox` (default false) in ONE migration; `generateApiKey({environment})` mints `co_test_` for sandbox and `co_live_` for live (the existing 256-bit entropy + hash unchanged)"
    - "The sandbox is FAIL-CLOSED both directions: `resolveByPrefix` rejects a `co_test_` key whose org is not `isSandbox` AND a `co_live_` key against an `isSandbox` org — a sandbox key can NEVER resolve to a production org (the load-bearing INTEG-DX-04 control, greening `sandbox-isolation.security.test.ts`)"
    - "Sandbox isolation REUSES the demo read-only layer: `isDemoOrg` (and `isDemoContext`) is widened to return true for an `isSandbox` org, so every existing skip (outbox dispatch `outbox/index.ts:415`, email, ZATCA, payouts) and the `demoReadOnly` mutation guard cover sandbox orgs with NO new isolation code"
    - "A per-day quota caps sandbox keys at `SANDBOX_DAILY_REQUEST_QUOTA=100`: `incrementDailyRequestCount` (keyed `{orgId}:{YYYYMMDD}`, mirror the monthly counter) enforced in the tier-quota middleware when the key environment is SANDBOX; the 101st request/day is 429"
    - "`provisionSandboxOrg()` seeds a sandbox org (fixture contractor/invoice/workflow so reads return realistic shapes) + sets `isSandbox`; an `api-key.createSandboxKey` mutation issues a `co_test_` key against it (from the Developer page); three new flags are registered — `module.developer-portal`, `module.public-status-page`, `module.api-sandbox` (all default false, sign-off-gated) — and `module.api-sandbox` gates external self-serve issuance; `.env.example` documents the sandbox knobs"
  artifacts:
    - path: "packages/api/src/services/api-key-service.ts"
      provides: "The `environment` axis on generate/resolve with the fail-closed sandbox<->org check"
      contains: "co_test_"
    - path: "packages/api/src/lib/demo.ts"
      provides: "The widened `isDemoOrg` honoring `Organization.isSandbox` — the isolation reuse"
      contains: "isSandbox"
    - path: "packages/api/src/services/sandbox-provisioning.ts"
      provides: "`provisionSandboxOrg()` — seeds a fixture-populated sandbox org + sets isSandbox"
      contains: "provisionSandboxOrg"
---

<objective>
Build the free sandbox tier (INTEG-DX-04) by reusing the shipped demo read-only isolation rather than
inventing a parallel mechanism. Add a `LIVE|SANDBOX` environment axis to the API key (`co_test_` prefix),
widen the demo predicate to a persistent `Organization.isSandbox` marker (so every existing side-effect skip
covers sandbox orgs), fail closed on any prefix<->org mismatch, cap sandbox keys at 100 requests/day, and
provision a fixture-seeded sandbox org on demand. Register the three new phase flags. This greens
`sandbox-isolation.security.test.ts` — the phase's load-bearing security contract.

Output: the sandbox environment axis + widened demo predicate + 100/day counter + `provisionSandboxOrg` +
the sandbox-key mutation + three flags + one migration.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-CONTEXT.md
@.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-RESEARCH.md

<interfaces>
The demo predicate is the isolation control (101-RESEARCH § A1): `packages/api/src/lib/demo.ts` `isDemoOrg`
currently reads env only (`DEMO_MODE`/`DEMO_ORG_IDS`); add a persistent branch that returns true when the
org's `isSandbox` column is set. `isDemoOrg` becomes async if it must query — verify every caller (the
outbox drain, `app-email.ts`, `zatca-submission.ts`) awaits it or add a cached sync variant fed by the
tenant context; do NOT break the existing sync call sites without updating them. The API-key axis
(§ A5): `api-key-service.ts` `KEY_PREFIX` becomes a per-environment tag; `generateApiKey` takes
`{environment}`; `resolveByPrefix` looks up the key's env + the org's `isSandbox` and throws the existing
resolve-failure error on a mismatch (fail closed). The per-day counter mirrors
`services/api-quota-counter.ts` `incrementMonthlyRequestCount` (Upstash/Redis, keyed by day); the middleware
mirrors `middleware/api-tier-quota.ts` but branches on `ctx` key environment. Flags: mirror the existing
`module.public-api` entry shape in `flags-core.ts` (default false, category 'module', sign-off-gated) and add
the three module keys to `signoff-registry-flags.ts` if gated. One Prisma migration adds both columns; run
`pnpm --filter @contractor-ops/db generate` after. New env vars -> `.env.example` + the package env schema.
No `console.*`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The environment axis + the widened demo predicate + the migration</name>
  <files>packages/db/prisma/schema/organization.prisma, packages/db/prisma/schema/api-key.prisma, packages/api/src/services/api-key-service.ts, packages/api/src/lib/demo.ts</files>
  <read_first>
    - 101-RESEARCH.md § A1 + § A5 + Pattern 1 + Pattern 2
    - packages/api/src/services/api-key-service.ts (full — KEY_PREFIX, generateApiKey, resolveByPrefix)
    - packages/api/src/lib/demo.ts + packages/api/src/middleware/demo.ts (isDemoOrg/isDemoContext + every caller)
    - packages/api/src/services/outbox/index.ts:415 + packages/api/src/services/app-email.ts + zatca-submission.ts (the demo-skip call sites to keep working)
  </read_first>
  <action>
    Add `Organization.isSandbox Boolean @default(false)` + `OrganizationApiKey.environment` (an enum
    `ApiKeyEnvironment { LIVE SANDBOX }`, default LIVE) — one migration; `pnpm --filter @contractor-ops/db
    generate`. In `api-key-service.ts`: `generateApiKey({environment})` selects the `co_test_`/`co_live_`
    prefix tag; `resolveByPrefix` fetches the key's `environment` + the org's `isSandbox` and throws the
    resolve-failure error on either mismatch (`SANDBOX` key + non-sandbox org, or `LIVE` key + sandbox org).
    In `demo.ts`: widen `isDemoOrg` to also return true when the org is `isSandbox` (query + cache, or read
    from the tenant context if available); keep all existing callers working (update sync call sites to the
    async/cached form as needed). No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test sandbox-isolation 2>&1 | tail -20; pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/db 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The migration adds both columns; the Prisma client regenerates; typecheck is clean.
    - `generateApiKey` mints `co_test_`/`co_live_` by environment; `resolveByPrefix` fails closed both ways.
    - `isDemoOrg` returns true for an `isSandbox` org; every existing demo-skip call site still compiles + runs.
  </acceptance_criteria>
  <done>Environment axis + fail-closed resolve + widened demo predicate + migration landed.</done>
</task>

<task type="auto">
  <name>Task 2: The 100/day counter, provisionSandboxOrg, the sandbox-key mutation, and the three flags</name>
  <files>packages/api/src/lib/api-tier-limits.ts, packages/api/src/services/api-quota-counter.ts, packages/api/src/middleware/api-tier-quota.ts, packages/api/src/services/sandbox-provisioning.ts, packages/api/src/routers/core/api-key.ts, packages/feature-flags/src/flags-core.ts, packages/feature-flags/src/signoff-registry-flags.ts, .env.example</files>
  <read_first>
    - 101-RESEARCH.md § A5 (quota + provisioning) + § A6 (the flag gates)
    - packages/api/src/lib/api-tier-limits.ts + services/api-quota-counter.ts + middleware/api-tier-quota.ts (the monthly-quota idiom to mirror for the per-day cap)
    - packages/api/src/routers/core/api-key.ts (the key CRUD to extend with createSandboxKey)
    - packages/feature-flags/src/flags-core.ts:239-313 (module.public-api + the marketplace flags — the entry shape) + signoff-registry-flags.ts:73-77
  </read_first>
  <action>
    Add `SANDBOX_DAILY_REQUEST_QUOTA = 100` to `api-tier-limits.ts`; add `incrementDailyRequestCount`
    (keyed `{orgId}:{YYYYMMDD}`) to `api-quota-counter.ts`; branch the tier-quota middleware so a SANDBOX-env
    key enforces the per-day cap (429 over 100). Author `sandbox-provisioning.ts` `provisionSandboxOrg({user})`
    — create/attach a sandbox `Organization` (`isSandbox:true`), seed a small fixture set (a contractor +
    invoice + workflow so reads return realistic shapes; reuse existing demo-seed fixtures if present),
    idempotent per user. Add an `api-key.createSandboxKey` mutation (gated by `module.api-sandbox` for
    external self-serve; the Developer-page action calls it) that provisions the org if needed and mints a
    `co_test_` key. Register `module.developer-portal`, `module.public-status-page`, `module.api-sandbox` in
    `flags-core.ts` (default false, category 'module', sign-off-gated, mirror `module.public-api`) + add to
    `signoff-registry-flags.ts` if gated. Document the sandbox knobs in `.env.example` + the package env
    schema. No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test sandbox-isolation 2>&1 | tail -20; pnpm --filter @contractor-ops/feature-flags test 2>&1 | tail -8; pnpm check:no-process-env 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The 101st sandbox request in a day is 429; a LIVE key is unaffected by the daily cap.
    - `provisionSandboxOrg` seeds a fixture-populated sandbox org idempotently; `createSandboxKey` mints a
      `co_test_` key against it.
    - The three flags exist (default off, sign-off-gated); `.env.example` + env schema updated; no raw
      `process.env` regressions.
    - `sandbox-isolation.security.test.ts` is GREEN.
  </acceptance_criteria>
  <done>Sandbox tier fully built: 100/day cap + provisioning + sandbox-key mutation + flags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| external sandbox consumer -> production tenant | A `co_test_` key must resolve ONLY to a sandbox org; the fence is the org boundary + the fail-closed prefix<->org check |
| sandbox org -> real-world side-effects | A sandbox org must fire no real email/webhook/payout/ZATCA; the widened demo predicate is the control |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-02-01 | Elevation of Privilege | A `co_test_` key resolving to a production org | mitigate | `resolveByPrefix` fails closed on any prefix<->`isSandbox` mismatch, both directions (INTEG-DX-04 core control) |
| T-101-02-02 | Information Disclosure | A sandbox org firing a real webhook/email leaking to a third party | mitigate | `isDemoOrg` widened to `isSandbox`; every existing skip (outbox/email/ZATCA/payout) applies with no new code |
| T-101-02-03 | Denial of Service | An uncapped sandbox key as a free unlimited API | mitigate | `SANDBOX_DAILY_REQUEST_QUOTA=100` per-day counter; 101st request/day 429 |
| T-101-02-SC | Tampering | No new day-zero deps | accept | Reuses the existing Redis counter + Prisma; no new runtime dep |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test sandbox-isolation` — GREEN.
- `pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/db --filter @contractor-ops/feature-flags` — clean.
- `pnpm check:no-process-env` — no regression.
- The migration is generated + committed (applied at deploy — EXTERNAL-ENABLEMENT, recorded in 101-10).
</verification>

<success_criteria>
A developer can mint a `co_test_` key against a fixture-seeded sandbox org, call the read API and get
realistic shapes, and never touch production data or fire a real side-effect; the key is capped at 100
requests/day and fails closed on any attempt to cross the sandbox<->production boundary — all by reusing the
demo read-only layer, with the three phase flags registered default-off.
</success_criteria>

<output>
Create `.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-02-SUMMARY.md` when done.
Record the migration name + the `provisionSandboxOrg` fixture set for the 101-10 EXTERNAL-ENABLEMENT row.
</output>
