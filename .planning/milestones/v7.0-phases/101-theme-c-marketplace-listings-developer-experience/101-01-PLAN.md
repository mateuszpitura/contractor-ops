---
phase: 101-theme-c-marketplace-listings-developer-experience
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/api/src/__tests__/security/sandbox-isolation.security.test.ts
  - packages/marketplace-manifests/src/__tests__/listing-manifest.test.ts
  - packages/marketplace-manifests/src/__tests__/collection-generation.test.ts
  - apps/public-api/src/__tests__/status-page.test.ts
  - apps/public-api/src/__tests__/developer-portal.test.ts
  - packages/api/src/__tests__/marketplace-listing.test.ts
  - .planning/phases/101-theme-c-marketplace-listings-developer-experience/101-VALIDATION.md
autonomous: true
requirements: [INTEG-ZAPIER-01, INTEG-N8N-01, INTEG-MAKE-01, INTEG-MARKETPLACE-01, INTEG-DX-01, INTEG-DX-02, INTEG-DX-03, INTEG-DX-04]

must_haves:
  truths:
    - "A RED validation net exists BEFORE any Phase-101 production code: six NEW test files (sandbox-isolation, listing-manifest, collection-generation, status-page, developer-portal, marketplace-listing) each RED against the not-yet-built surface, each naming exactly which later plan turns it green (101-02 sandbox, 101-03 manifest+model, 101-04 collections, 101-05 status, 101-08 portal)"
    - "The sandbox-isolation net is the load-bearing security contract: a `co_test_` key resolves ONLY to a sandbox org and fails closed on a prefix<->`isSandbox` mismatch in BOTH directions; a sandbox org fires NO real side-effect (the widened demo predicate skips outbox/email); the 101st sandbox request in a day is 429 — RED until 101-02"
    - "The generation nets assert NON-DRIFT: every generated Zapier trigger maps to a real event-catalog event + every action to a real write operationId in `openapi.snapshot.json`; Postman + Insomnia cover every snapshot path; `generate --check` fails on any drift; the write-action count is asserted CONDITIONAL on the snapshot's write set (0 pre-100-09-flip, N after) so a writes-hidden snapshot is not a false failure — RED until 101-03/101-04"
    - "The status-page net asserts `/status.json` maps api/dispatcher/jobs component states from the shipped health sources AND exposes NO tenant data / no org id / no raw probe body; the developer-portal net asserts the portal 404s when `module.developer-portal` is off; the marketplace-listing net asserts the DRAFT->SUBMITTED->IN_REVIEW->LIVE/REJECTED/NEEDS_CHANGES state machine + audit — RED until 101-05/101-08/101-03"
    - "Every RED test is scoped-runnable (`--filter` + path), uses no watch mode, contacts NO real marketplace/external URL, and does not touch the full web-vite suite; `101-VALIDATION.md` frontmatter flips `nyquist_compliant: true` once the stubs land"
  artifacts:
    - path: "packages/api/src/__tests__/security/sandbox-isolation.security.test.ts"
      provides: "The executable INTEG-DX-04 sandbox-isolation contract (fail-closed prefix<->org + no-real-side-effect + 100/day) — RED until 101-02"
      contains: "co_test_"
    - path: "packages/marketplace-manifests/src/__tests__/listing-manifest.test.ts"
      provides: "The INTEG-ZAPIER-01/N8N-01/MAKE-01 generation-from-snapshot contract — RED until 101-03"
      contains: "operationId"
    - path: "apps/public-api/src/__tests__/status-page.test.ts"
      provides: "The INTEG-DX-03 status aggregator + no-tenant-data contract — RED until 101-05"
      contains: "status.json"
---

<objective>
Author the phase's RED validation net FIRST so every later plan is turned-green against an executable
contract, not prose. Six NEW test files cover the load-bearing sandbox-isolation security control plus the
generation-non-drift, status-aggregation, portal-gating, and marketplace-model contracts. Each RED file names
the plan that greens it (101-02 sandbox, 101-03 manifest generator + `MarketplaceListing` model, 101-04
collections, 101-05 status aggregator, 101-08 portal). This is the phase's Nyquist floor: no sandbox, no
generator, no status/portal code ships without a test that was RED before it.

Output: 6 new RED test files (no real external URL / no marketplace call) + the nyquist flip.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-CONTEXT.md
@.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-RESEARCH.md

<interfaces>
Mirror existing test seams: `packages/api/src/__tests__/security/*.security.test.ts` (the regression
pattern), the demo-org stubbing in `packages/api/src/lib/demo.ts` + `middleware/demo.ts`, and the
apiKey/flag-evaluator stubbing in `packages/api/src/routers/public-api/__tests__/*.test.ts`
(`vi.hoisted`/`vi.mock`). The six files assert CONTRACTS that later plans satisfy; where a symbol does not
yet exist (`generateApiKey({environment})`, `resolveByPrefix` sandbox branch, the widened `isDemoOrg`, the
`packages/marketplace-manifests` generator, `MarketplaceListing` model, `/status.json`, the portal routes),
the test references it so the file is RED — prefer a runtime assertion that fails over a `describe.skip`,
using `describe.skip('HOLD-until-101-0X')` ONLY where a hard type/import error blocks the whole file from
loading. Generation tests read a FIXTURE `openapi.snapshot.json` (a small hand-written spec fixture with a
couple of read + write operationIds) so the drift assertions do not depend on the live app build; the
write-count assertion counts write ops IN the fixture, never a hardcoded 6. NEVER hit a real marketplace,
npm, or external URL. No `console.*`; no watch mode.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the sandbox-isolation RED security suite + the marketplace-listing model RED suite</name>
  <files>packages/api/src/__tests__/security/sandbox-isolation.security.test.ts, packages/api/src/__tests__/marketplace-listing.test.ts</files>
  <read_first>
    - 101-RESEARCH.md § A1 (demo-predicate reuse), § A5 (sandbox axis + fail-closed), § B (MarketplaceListing shape), § E (requirement->test map)
    - packages/api/src/lib/demo.ts + packages/api/src/middleware/demo.ts (the predicate + guard to widen)
    - packages/api/src/services/api-key-service.ts:11,50,116 (KEY_PREFIX, generateApiKey, resolveByPrefix — the sandbox axis target)
    - packages/api/src/services/outbox/index.ts:415 (the demo-org skip a sandbox org must inherit)
  </read_first>
  <action>
    Author two RED suites against symbols 101-02/101-03 will create:
    - `sandbox-isolation.security.test.ts` (INTEG-DX-04): (a) `generateApiKey({environment:'SANDBOX'})`
      yields a `co_test_` prefix and `{environment:'LIVE'}` yields `co_live_`; (b) `resolveByPrefix` FAILS
      CLOSED — a `co_test_` key whose org is not `isSandbox` is rejected AND a `co_live_` key against an
      `isSandbox` org is rejected; (c) the widened `isDemoOrg`/`isDemoContext` returns true for an
      `isSandbox` org so a sandbox org's outbox dispatch is skipped (assert the `outbox/index.ts` demo-skip
      path is taken — no real dispatch) and a mutation is blocked by `demoReadOnly`; (d) the 101st sandbox
      request in a day is 429 (mock the per-day counter). RED until 101-02.
    - `marketplace-listing.test.ts` (INTEG-MARKETPLACE-01): the `MarketplaceListing` model persists one row
      per platform (ZAPIER/N8N/MAKE) with `status`, `versionPin`, `lastReviewFeedback`; the staff router
      lists + updates them; the state machine ACCEPTS DRAFT->SUBMITTED->IN_REVIEW->LIVE and
      IN_REVIEW->NEEDS_CHANGES/REJECTED but REJECTS an illegal jump (e.g. DRAFT->LIVE); every mutation writes
      an audit row. RED until 101-03.
    No `console.*`; scoped; no watch mode.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test sandbox-isolation marketplace-listing 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both RED files exist, each asserting the § A1/A5/B behavior (not `expect(true)`), each naming its green
      plan in a comment.
    - The sandbox file covers all three isolation facets (fail-closed both ways, no-real-side-effect, 100/day).
    - No real external/marketplace URL is contacted.
  </acceptance_criteria>
  <done>Sandbox-isolation + marketplace-listing RED contracts authored.</done>
</task>

<task type="auto">
  <name>Task 2: Author the generation, status-page, and developer-portal RED suites; flip nyquist</name>
  <files>packages/marketplace-manifests/src/__tests__/listing-manifest.test.ts, packages/marketplace-manifests/src/__tests__/collection-generation.test.ts, apps/public-api/src/__tests__/status-page.test.ts, apps/public-api/src/__tests__/developer-portal.test.ts, .planning/phases/101-theme-c-marketplace-listings-developer-experience/101-VALIDATION.md</files>
  <read_first>
    - 101-RESEARCH.md § A2 (generation + diff-check), § A3 (portal pages), § A4 (status aggregator), § B (artifact shapes), § D (pitfalls 3/4/5/8)
    - apps/public-api/scripts/build-openapi-snapshot.ts + apps/public-api/src/lib/build-openapi-doc.ts (98 — the snapshot source; may not exist if 98 unexecuted — use a fixture spec)
    - packages/validators/src/webhooks/index.ts (100 — the 16-event catalog; if absent use a fixture event list mirroring 100-RESEARCH § B)
    - apps/api/src/routes/health.ts + apps/cron-worker/src/jobs/handlers/job-health.ts (the status sources to map)
  </read_first>
  <action>
    Author four RED suites + flip nyquist:
    - `listing-manifest.test.ts` (INTEG-ZAPIER-01/N8N-01/MAKE-01): given a FIXTURE snapshot + event list,
      `generateManifests()` emits Zapier/n8n/Make defs where every trigger maps to a real event and every
      action maps to a real write operationId; the write-action count EQUALS the fixture's write-op count
      (0 when the fixture hides writes — assert it does not throw on an empty write set); the emitted defs
      pass each platform's shape validation. RED until 101-03.
    - `collection-generation.test.ts` (INTEG-DX-02): `generatePostman()`/`generateInsomnia()` emit one
      request per fixture-snapshot path with `{{baseUrl}}`/`{{apiKey}}` variables; `generate --check`
      (regenerate to a temp dir + diff the committed artifacts) FAILS when a committed artifact is mutated.
      RED until 101-04.
    - `status-page.test.ts` (INTEG-DX-03): `/status.json` returns `components:{api,dispatcher,jobs}` mapped
      from stubbed health sources (all-ok -> operational; a degraded probe -> degraded); the response body
      contains NO `organizationId`/tenant field/raw probe payload (assert a deep scan finds none); open
      `IncidentReport` rows render in `incidents`. RED until 101-05.
    - `developer-portal.test.ts` (INTEG-DX-01): with `module.developer-portal` OFF, `/docs/webhooks`,
      `/docs/sdks`, `/docs/recipes`, `/docs/changelog` all 404; with it ON they 200 and the webhooks page
      lists the catalog events. RED until 101-08.
    Then set `nyquist_compliant: true` in `101-VALIDATION.md`. All scoped; no `console.*`; no watch mode.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/marketplace-manifests test listing-manifest collection-generation 2>&1 | tail -16; pnpm --filter @contractor-ops/public-api test status-page developer-portal 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - Four new files exist and are RED, each naming its turn-green plan in a comment.
    - The generation tests use a FIXTURE snapshot (no live-app dependency) and assert the write-count
      conditionally (pitfall 4); the status test asserts the no-tenant-data invariant (pitfall 5).
    - `101-VALIDATION.md` `nyquist_compliant: true`.
  </acceptance_criteria>
  <done>Generation, status, and portal RED contracts authored; nyquist floor set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test suite -> phase code | The RED net is the executable spec; a missing/weak test lets a sandbox-isolation or artifact-drift gap ship silently |
| sandbox key -> production data | Modelled here as the sandbox-isolation test surface so the fail-closed prefix<->org check + the no-side-effect reuse are proven, not assumed |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01-01 | Elevation of Privilege | A `co_test_` key reaching a live org untested | mitigate | sandbox-isolation net asserts fail-closed both ways before 101-02 builds the axis |
| T-101-01-02 | Information Disclosure | A sandbox org firing a real side-effect / the status page leaking tenant data untested | mitigate | sandbox net asserts the widened demo skip; status net asserts no-tenant-data before 101-05 |
| T-101-01-03 | Tampering | A generated artifact drifting from the spec untested | mitigate | listing-manifest + collection-generation nets assert `generate --check` before 101-03/101-04 |
| T-101-01-SC | Tampering | npm installs | accept | No package installs (tests only; the packages/apps are scaffolded by later plans) |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test sandbox-isolation marketplace-listing` — present, all RED (expected pre-implementation).
- `pnpm --filter @contractor-ops/marketplace-manifests test listing-manifest collection-generation` — RED.
- `pnpm --filter @contractor-ops/public-api test status-page developer-portal` — RED.
- `101-VALIDATION.md` `nyquist_compliant: true`.
</verification>

<success_criteria>
Every Phase-101 load-bearing behavior (sandbox fail-closed isolation, no-real-side-effect reuse, per-day
cap, generation non-drift, status no-tenant-data, portal flag-gating, marketplace state machine) has a RED,
scoped, non-watch test that names the plan turning it green — the phase cannot ship a gap without a failing
test first, and no test ever contacts a real marketplace, npm, or external URL.
</success_criteria>

<output>
Create `.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-01-SUMMARY.md` when done.
Record the fixture-snapshot + fixture-event-list location so the generation plans (101-03/101-04) reuse it.
</output>
