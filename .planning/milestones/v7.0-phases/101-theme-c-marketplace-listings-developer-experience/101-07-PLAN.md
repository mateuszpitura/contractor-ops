---
phase: 101-theme-c-marketplace-listings-developer-experience
plan: 07
type: execute
wave: 2
depends_on: ["101-03"]
files_modified:
  - packages/zapier-app/package.json
  - packages/zapier-app/tsconfig.json
  - packages/zapier-app/vitest.config.ts
  - packages/zapier-app/src/index.ts
  - packages/zapier-app/src/authentication.ts
  - packages/zapier-app/src/triggers/index.ts
  - packages/zapier-app/src/creates/index.ts
  - packages/zapier-app/src/__tests__/app.test.ts
  - packages/marketplace-manifests/src/generate-make.ts
  - apps/public-api/marketplace/make/blueprint.json
  - packages/zapier-app/README.md
  - .planning/EXTERNAL-ENABLEMENT.md
autonomous: true
requirements: [INTEG-ZAPIER-01, INTEG-MAKE-01]

must_haves:
  truths:
    - "A Zapier CLI app (`packages/zapier-app`) exposes 8+ triggers (event-catalog-mapped) + 6+ actions (create contractor/invoice, approve invoice, mark payment paid, create workflow task, lookup contractor by tax id) — all IMPORTING the 101-03 generated Zapier definitions, not hand-authored; auth is a custom API-key field (`co_live_`/`co_test_`), with an OAuth 2.0 authorization-code variant scaffolded but optional"
    - "The app passes its Zapier sandbox/validate BUNDLE test locally (`zapier validate`-equivalent + a vitest that asserts every trigger maps to a real catalog event, every create to a real write operationId, and the auth wires the key header) — GREENing `packages/zapier-app` tests"
    - "The Make.com app blueprint (`apps/public-api/marketplace/make/blueprint.json`) is generated from the same source (modules = actions, instant triggers = webhook events, connection = apiKey) and validates against the Make blueprint shape (INTEG-MAKE-01)"
    - "The Zapier public-listing submission (`zapier push` + partner review, INTEG-ZAPIER-02) and the Make App Directory submission are DEFERRED external steps — recorded as `EXTERNAL-ENABLEMENT.md` rows (partner account + review timeline), tracked by the 101-03 `MarketplaceListing` dashboard; NO submission runs in this plan"
    - "New dev-deps (`zapier-platform-core`, the Zapier test harness) are pinned >=7 days old; `pnpm audit` + `pnpm security:scan` pass; the write-action count is conditional on the snapshot (0 pre-100-09-flip); no `console.*`"
  artifacts:
    - path: "packages/zapier-app/src/index.ts"
      provides: "The Zapier app definition (auth + triggers + creates) assembled from the generated defs"
      contains: "triggers"
    - path: "packages/zapier-app/src/authentication.ts"
      provides: "The custom API-key auth (co_live_/co_test_) + the optional OAuth 2.0 variant"
      contains: "apiKey"
    - path: "apps/public-api/marketplace/make/blueprint.json"
      provides: "The generated Make.com app blueprint (modules + instant triggers + apiKey connection)"
      contains: "modules"
---

<objective>
Build the Zapier CLI app + the Make.com blueprint (INTEG-ZAPIER-01, INTEG-MAKE-01) from the 101-03 generated
definitions, pass the Zapier sandbox bundle test locally, and record the marketplace SUBMISSIONS as deferred
external-enablement steps (partner accounts + review) tracked by the listing dashboard. Built + validated now,
submission-deferred.

Output: the Zapier app (auth + 8+ triggers + 6+ creates + bundle test) + the Make blueprint + the
EXTERNAL-ENABLEMENT submission rows.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-CONTEXT.md
@.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-RESEARCH.md

<interfaces>
Import the generated Zapier defs from `@contractor-ops/marketplace-manifests` (101-03 `generate-zapier.ts`)
+ have this plan finalize `generate-make.ts` -> the blueprint. The Zapier CLI app structure
(101-RESEARCH § B): `authentication` (custom API-key field sent as the key header; the OAuth 2.0
authorization-code variant scaffolded — INTEG-ZAPIER-01 allows either, ship the key auth now), `triggers`
(the 8+ webhook-event subscriptions), `creates` (the 6+ write operationIds). The bundle test mirrors
`zapier validate` + `zapier test` — a vitest that asserts the app definition is well-formed, every
trigger/create resolves to a real catalog event / write operationId, and auth wires the header (no live
Zapier account needed). Do context7 lookup for the current `zapier-platform-core` app shape rather than
guessing. `zapier push`/submission is NOT run here (deferred). New deps obey the 7-day floor (pin >=7 days
old, `pnpm audit` + `pnpm security:scan` + typosquat-check; if `zapier-platform-core` cannot be pinned >=7
days old, STOP + surface). Record the Zapier + Make submission rows in `EXTERNAL-ENABLEMENT.md` (mirror the
existing row style). No `console.*`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The Zapier CLI app (auth + triggers + creates) + the bundle test</name>
  <files>packages/zapier-app/package.json, packages/zapier-app/tsconfig.json, packages/zapier-app/vitest.config.ts, packages/zapier-app/src/index.ts, packages/zapier-app/src/authentication.ts, packages/zapier-app/src/triggers/index.ts, packages/zapier-app/src/creates/index.ts, packages/zapier-app/src/__tests__/app.test.ts</files>
  <read_first>
    - 101-RESEARCH.md § B (Zapier shape) + § A6/A7 (built-now, submit-deferred)
    - packages/marketplace-manifests/src/generate-zapier.ts (the triggers/creates to import — 101-03)
    - packages/validators/package.json + tsconfig.json (the leaf-package scaffold)
    - context7: the current zapier-platform-core app definition + authentication shape
  </read_first>
  <action>
    Scaffold `packages/zapier-app`. Assemble the app definition from the generated defs: `authentication`
    (custom API-key field -> the key header; scaffold the OAuth 2.0 authorization-code variant, disabled by
    default), `triggers` (8+ from the event catalog), `creates` (6+ from the write operationIds). Author
    `app.test.ts`: the definition validates, every trigger/create resolves to a real catalog event / write
    operationId, auth wires the header, the write-count assertion is conditional on the snapshot. Add
    `zapier-platform-core` at >=7 days old; `pnpm audit` + `pnpm security:scan`. No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/zapier-app test 2>&1 | tail -16; pnpm --filter @contractor-ops/zapier-app build 2>&1 | tail -6; pnpm audit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The app definition is valid + passes the bundle test; triggers/creates map to the generated surface.
    - Auth wires the API-key header; the OAuth variant is scaffolded but off.
    - Any new dep is >=7 days old; `pnpm audit` clean; `packages/zapier-app` tests GREEN.
  </acceptance_criteria>
  <done>The Zapier app built + bundle-tested from the generated defs.</done>
</task>

<task type="auto">
  <name>Task 2: The Make blueprint + the deferred-submission EXTERNAL-ENABLEMENT rows + README</name>
  <files>packages/marketplace-manifests/src/generate-make.ts, apps/public-api/marketplace/make/blueprint.json, packages/zapier-app/README.md, .planning/EXTERNAL-ENABLEMENT.md</files>
  <read_first>
    - 101-RESEARCH.md § B (Make blueprint shape) + § A7 (deferred submissions)
    - .planning/EXTERNAL-ENABLEMENT.md (the register + the row style; row #7 Speakeasy publish is the template)
    - packages/marketplace-manifests/src/generate-make.ts (the emitter stub from 101-03 to finalize)
  </read_first>
  <action>
    Finalize `generate-make.ts` -> `blueprint.json` (modules = the write actions, instant triggers = the
    webhook events, connection = the apiKey) + emit the committed blueprint; a test validates its shape.
    Author the Zapier README (install/auth/triggers/actions). Add three `EXTERNAL-ENABLEMENT.md` rows: (a)
    Zapier public-listing submission + review (partner account, 2-4wk, INTEG-ZAPIER-02) gated by
    `integration.marketplace-zapier`; (b) Make App Directory submission (partner account, 1-2wk) gated by
    `integration.marketplace-make`; (c) note the `MarketplaceListing` dashboard tracks both. Mirror the
    existing row columns (dependency / flag / default / dark-until / test / enablement steps). No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/marketplace-manifests test 2>&1 | tail -8; node -e "JSON.parse(require('fs').readFileSync('apps/public-api/marketplace/make/blueprint.json'));console.log('make-blueprint-ok')"; grep -qi "Zapier" .planning/EXTERNAL-ENABLEMENT.md && echo enablement-rows-ok</automated>
  </verify>
  <acceptance_criteria>
    - The Make blueprint validates + is generated from the same source (no hand-authoring).
    - The README documents the Zapier app; the EXTERNAL-ENABLEMENT rows record the deferred Zapier + Make
      submissions with partner-account + review timelines + the gating flags.
  </acceptance_criteria>
  <done>Make blueprint + deferred-submission register rows + Zapier docs shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| generated defs -> Zapier/Make apps | The app surface must equal the generated surface; a hand-edited trigger/action drifts from the API |
| founder -> marketplace submission | Submission needs a partner account the founder lacks; it must be a deferred register step, never a build gate |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-07-01 | Tampering | A supply-chain-compromised Zapier dep | mitigate | 7-day release-age pin + `pnpm audit` + `pnpm security:scan` + typosquat-check |
| T-101-07-02 | Spoofing | An app claiming actions the API does not have | mitigate | Every trigger/create resolves to a real catalog event / write operationId (bundle test) |
| T-101-07-03 | Information Disclosure | A secret embedded in the app/blueprint | mitigate | Auth is the user-supplied apiKey; no secret in the package/blueprint |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/zapier-app test` + `build` — GREEN (bundle test passes).
- The Make blueprint parses + validates; the EXTERNAL-ENABLEMENT rows exist.
- `pnpm audit` + `pnpm security:scan` clean after any dep add.
</verification>

<success_criteria>
The Zapier app (8+ triggers, 6+ actions) passes its sandbox bundle test and the Make blueprint validates —
both generated from the one OpenAPI source — with the marketplace submissions recorded as deferred,
dashboard-tracked external steps that never block the build.
</success_criteria>

<output>
Create `.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-07-SUMMARY.md` when done.
Record the Zapier `zapier push` + Make submission steps for the 101-10 close.
</output>
