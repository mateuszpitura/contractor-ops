---
phase: 101-theme-c-marketplace-listings-developer-experience
plan: 10
type: execute
wave: 4
depends_on: ["101-01", "101-02", "101-03", "101-04", "101-05", "101-06", "101-07", "101-08", "101-09"]
files_modified:
  - .planning/brain/wiki/domains/developer-experience.md
  - .planning/brain/wiki/domains/public-api-surface.md
  - .planning/brain/wiki/integrations/zapier.md
  - .planning/brain/wiki/integrations/n8n.md
  - .planning/brain/wiki/integrations/make.md
  - .planning/brain/wiki/integrations/_index.md
  - .planning/brain/wiki/structure/packages.md
  - .planning/brain/wiki/structure/api-routers-catalog.md
  - .planning/brain/wiki/structure/prisma-schema-areas.md
  - .planning/brain/wiki/patterns/feature-flags.md
  - .planning/brain/wiki/log.md
  - .planning/brain/wiki/hot.md
  - .planning/MEMORY.md
  - .planning/EXTERNAL-ENABLEMENT.md
  - .planning/milestones/v7.0-phases/101-theme-c-marketplace-listings-developer-experience/101-VALIDATION.md
  - .planning/milestones/v7.0-phases/101-theme-c-marketplace-listings-developer-experience/101-MILESTONE-HANDOFF.md
autonomous: false
requirements: [INTEG-ZAPIER-01, INTEG-ZAPIER-02, INTEG-N8N-01, INTEG-N8N-02, INTEG-MAKE-01, INTEG-MARKETPLACE-01, INTEG-DX-01, INTEG-DX-02, INTEG-DX-03, INTEG-DX-04]

must_haves:
  truths:
    - "The whole marketplace + DX surface hangs together end-to-end: the scoped suites are GREEN — `@contractor-ops/marketplace-manifests` (listing-manifest + collection-generation + `generate --check`), `@contractor-ops/api` (sandbox-isolation + marketplace-listing + incident), `@contractor-ops/public-api` (status-page + developer-portal), `@contractor-ops/n8n-nodes`, `@contractor-ops/zapier-app`, `@contractor-ops/web-vite` (marketplace), `@contractor-ops/landing` (status); the sandbox is fail-closed; every artifact is snapshot-derived + drift-checked; nothing external ships enabled"
    - "The documentation-follows-code obligation is discharged in the SAME change set: a NEW `wiki/domains/developer-experience.md` (portal, collections, status page, sandbox tier) + NEW `wiki/integrations/{zapier,n8n,make}.md` + updated `public-api-surface.md`, `integrations/_index.md`, `structure/{packages,api-routers-catalog,prisma-schema-areas}.md`, `patterns/feature-flags.md`; `pnpm check:wiki-brain` passes (no NEW drift); the graph + BM25 refresh is run; `source_commit` bumped"
    - "`.planning/MEMORY.md` records the durable Phase-101 invariants: (1) every marketplace + collection artifact GENERATES from `openapi.snapshot.json` + the webhook event catalog with a `generate --check` drift gate — never hand-authored; (2) the sandbox tier is a `co_test_` environment axis resolving ONLY to an `isSandbox` org (fail-closed both ways) that REUSES the demo read-only layer for no-real-side-effects + a 100/day cap; (3) the dev portal EXTENDS the 98 Scalar `/docs` behind `module.developer-portal`; (4) the status page is a public tenant-data-free aggregator over the shipped health monitoring behind `module.public-status-page`; (5) marketplace SUBMISSIONS are deferred external steps tracked by `MarketplaceListing`, never a build gate"
    - "`.planning/EXTERNAL-ENABLEMENT.md` gains rows for every deferred external step: Zapier public-listing submission (INTEG-ZAPIER-02, partner account, 2-4wk, `integration.marketplace-zapier`), Make App Directory submission (`integration.marketplace-make`), n8n npm publish (`NPM_TOKEN` + the dark CI dispatch), the `developers.`/`status.contractor-ops.{tld}` DNS steps, and the 101-02 sandbox/incident/marketplace migration as a deploy-time apply — each with default/dark-until/enablement columns"
    - "`101-VALIDATION.md` sign-off is complete + `wave_0_complete: true`; a `101-MILESTONE-HANDOFF.md` records that Phase 101 CLOSES Theme C (98-101) and the v7.0 milestone's integration platform — pointing to `/gsd:audit-milestone` for the v7.0 close; no `console.*`"
  artifacts:
    - path: ".planning/brain/wiki/domains/developer-experience.md"
      provides: "The DX domain page — portal, collections, status page, sandbox tier, marketplace generation"
      contains: "openapi.snapshot.json"
    - path: ".planning/EXTERNAL-ENABLEMENT.md"
      provides: "The deferred external-step register rows for the marketplace submissions + publish + DNS + migration"
      contains: "marketplace"
    - path: ".planning/milestones/v7.0-phases/101-theme-c-marketplace-listings-developer-experience/101-MILESTONE-HANDOFF.md"
      provides: "The Theme-C + v7.0 milestone-close handoff"
      contains: "Theme C"
---

<objective>
Close the phase AND Theme C: prove the marketplace + developer-experience surface hangs together end-to-end,
discharge the documentation-follows-code obligation (new + updated wiki + indexes + MEMORY in the same change
set), run the brain refresh, add the deferred external-step register rows, flip the validation sign-off, and
record the milestone-close handoff. Human-checkpoint on the wiki (durable facts only — no operational text).

Output: new/updated wiki + MEMORY invariants + EXTERNAL-ENABLEMENT rows + brain refresh + validation sign-off
+ the Theme-C/v7.0 milestone handoff.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-CONTEXT.md
@.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-RESEARCH.md

<interfaces>
Doc pipeline (CLAUDE.md § Documentation follows code): update the wiki pages, then `pnpm check:wiki-brain`
(must pass — no NEW drift for the changed source files), then the brain refresh: `rm -f
graphify-out/graph.json && graphify update . --no-cluster --force && cp graphify-out/graph.json
.planning/graphs/graph.json` and `cd .planning/brain && find wiki -name '*.md' -exec python3 scripts/
contextual-prefix.py {} --no-llm \; && python3 scripts/bm25-index.py build`. Bump `source_commit` on touched
wiki frontmatter. This is the LAST plan of the LAST Theme C phase — it verifies the whole phase, so run the
full scoped suites. NEVER run the full unscoped web-vite suite. EXTERNAL-ENABLEMENT row style: mirror the
existing rows (# | dependency | flag/gate | default | dark-until | auto-flip/test | enablement steps).
MEMORY bullet style: durable facts only, no round-status/operational text. The handoff notes this closes
Theme C + the v7.0 integration platform and routes to `/gsd:audit-milestone` for the milestone close.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end verification of the whole marketplace + DX surface</name>
  <files>.planning/milestones/v7.0-phases/101-theme-c-marketplace-listings-developer-experience/101-VALIDATION.md</files>
  <read_first>
    - 101-VALIDATION.md (the per-requirement map + sign-off checklist)
    - the Wave-0 suites + the generator `--check` gate + the web-vite/landing tests
  </read_first>
  <action>
    Run the full scoped verification: `pnpm --filter @contractor-ops/marketplace-manifests test`;
    `pnpm --filter @contractor-ops/api test sandbox-isolation marketplace-listing incident`;
    `pnpm --filter @contractor-ops/public-api test status-page developer-portal`;
    `pnpm --filter @contractor-ops/n8n-nodes test`; `pnpm --filter @contractor-ops/zapier-app test`;
    `pnpm --filter @contractor-ops/web-vite test marketplace`; `pnpm --filter @contractor-ops/landing test
    status`; the generator `generate --check`; `pnpm typecheck` across the touched packages;
    `node scripts/lint-audit-log.mjs`. Confirm every INTEG-* row in 101-VALIDATION is GREEN (INTEG-ZAPIER-02
    is the intentional deferred row); complete the sign-off + set `wave_0_complete: true`. Confirm the
    sandbox fails closed + every artifact is snapshot-derived + drift-checked + nothing external ships enabled.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/marketplace-manifests test 2>&1 | tail -12; pnpm --filter @contractor-ops/api test sandbox-isolation marketplace-listing 2>&1 | tail -10; pnpm --filter @contractor-ops/public-api test status-page developer-portal 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All marketplace/DX/sandbox/status/portal suites GREEN + the `generate --check` gate passes.
    - `pnpm typecheck` across touched packages clean; audit-log check passes.
    - 101-VALIDATION sign-off complete; `wave_0_complete: true`.
  </acceptance_criteria>
  <done>Whole marketplace + DX surface verified end-to-end; validation signed off.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Documentation-follows-code + brain refresh + EXTERNAL-ENABLEMENT + milestone handoff</name>
  <files>.planning/brain/wiki/domains/developer-experience.md, .planning/brain/wiki/domains/public-api-surface.md, .planning/brain/wiki/integrations/zapier.md, .planning/brain/wiki/integrations/n8n.md, .planning/brain/wiki/integrations/make.md, .planning/brain/wiki/integrations/_index.md, .planning/brain/wiki/structure/packages.md, .planning/brain/wiki/structure/api-routers-catalog.md, .planning/brain/wiki/structure/prisma-schema-areas.md, .planning/brain/wiki/patterns/feature-flags.md, .planning/brain/wiki/log.md, .planning/brain/wiki/hot.md, .planning/MEMORY.md, .planning/EXTERNAL-ENABLEMENT.md, .planning/milestones/v7.0-phases/101-theme-c-marketplace-listings-developer-experience/101-MILESTONE-HANDOFF.md</files>
  <read_first>
    - CLAUDE.md § Documentation follows code (the trigger matrix + the exact refresh pipeline commands)
    - .planning/brain/wiki/domains/public-api-surface.md + wiki/integrations/_index.md + wiki/structure/{packages,api-routers-catalog,prisma-schema-areas}.md + wiki/patterns/feature-flags.md (the pages to update)
    - .planning/MEMORY.md (append the 5 invariants; match the existing bullet style, no operational text)
    - .planning/EXTERNAL-ENABLEMENT.md (the register + the row style; row #7 Speakeasy publish is the template for the publish/submit rows)
  </read_first>
  <action>
    Author `wiki/domains/developer-experience.md` (the portal extending the 98 Scalar `/docs`; the
    snapshot-generated collections + marketplace defs + the `generate --check` gate; the public status
    aggregator; the sandbox tier reusing the demo layer; agent mistakes: "generate, never hand-author",
    "a `co_test_` key resolves ONLY to an `isSandbox` org — fail closed", "the status page carries no tenant
    data", "submission is deferred — the dashboard tracks it"). Author `wiki/integrations/{zapier,n8n,make}.md`
    (purpose, the generated surface, the credential, the submission/publish enablement). Update
    `public-api-surface.md` (the sandbox environment axis + the portal), `integrations/_index.md`,
    `structure/packages.md` (`marketplace-manifests`, `n8n-nodes`, `zapier-app`), `structure/
    api-routers-catalog.md` (`marketplaceListingRouter`, `incidentRouter`, `api-key.createSandboxKey`),
    `structure/prisma-schema-areas.md` (`MarketplaceListing`, `IncidentReport`, `Organization.isSandbox`,
    `OrganizationApiKey.environment`), `patterns/feature-flags.md` (the three new module flags + the
    marketplace flags). Append the 5 durable invariants to `.planning/MEMORY.md`. Add the deferred rows to
    `EXTERNAL-ENABLEMENT.md` (Zapier submission, Make submission, n8n npm publish, the two DNS hostnames, the
    101-02 migration apply). HUMAN-VERIFY GATE: confirm no operational/round-status text leaked into the wiki
    (durable facts only). Update `wiki/log.md`, overwrite `hot.md`, bump `source_commit`, then
    `pnpm check:wiki-brain` + the graph + BM25 refresh. Author `101-MILESTONE-HANDOFF.md`: Phase 101 closes
    Theme C (98-101) + the v7.0 integration platform; route to `/gsd:audit-milestone` for the v7.0 close;
    list the deferred external steps a human owns post-deploy. No `console.*`.
  </action>
  <verify>
    <automated>pnpm check:wiki-brain 2>&1 | tail -8; test -f .planning/milestones/v7.0-phases/101-theme-c-marketplace-listings-developer-experience/101-MILESTONE-HANDOFF.md && grep -qi "Theme C" .planning/milestones/v7.0-phases/101-theme-c-marketplace-listings-developer-experience/101-MILESTONE-HANDOFF.md && echo handoff-ok</automated>
  </verify>
  <acceptance_criteria>
    - The new DX + integration wiki pages + the updated public-api/structure/pattern pages reflect the
      generators, the sandbox tier, the portal, the status page, and the new models/flags; `pnpm
      check:wiki-brain` passes; graph + BM25 refreshed; `source_commit` bumped.
    - `.planning/MEMORY.md` has the 5 durable invariants (durable facts only — no operational text).
    - `EXTERNAL-ENABLEMENT.md` has the deferred submission/publish/DNS/migration rows.
    - `101-MILESTONE-HANDOFF.md` records the Theme-C + v7.0 close + the deferred human steps.
  </acceptance_criteria>
  <done>Docs-follows-code discharged; brain refreshed; external-enablement updated; Theme-C/v7.0 milestone handoff recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| code change -> wiki | A product change without a matching wiki update makes the next agent hallucinate |
| phase 101 -> v7.0 milestone close | The deferred external steps must be recorded so the milestone audit knows what a human still owns |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-10-01 | Repudiation | Stale docs hide the generator/sandbox/portal/status behavior | mitigate | Wiki + MEMORY updated in the same change set; `check:wiki-brain` gates |
| T-101-10-02 | Security Misconfiguration | An operator enabling a marketplace listing without knowing the submission/flag posture | mitigate | The integration pages + EXTERNAL-ENABLEMENT rows document the flags + submission steps |
| T-101-10-SC | Tampering | npm installs | accept | Docs only |
</threat_model>

<verification>
- The full scoped marketplace/DX/sandbox/status/portal suites GREEN (Task 1); `pnpm typecheck` across touched packages clean; audit-log check passes.
- `pnpm check:wiki-brain` passes; graph + BM25 refresh run per CLAUDE.md.
- `101-MILESTONE-HANDOFF.md` exists and records the Theme-C + v7.0 close + the deferred human steps.
</verification>

<success_criteria>
The phase is closed and coherent: Contractor Ops is reachable from three marketplaces (definitions generated
+ validated, submissions dashboard-tracked + deferred), backed by a portal, collections, a public status
page, and a free fail-closed sandbox tier — documented in the wiki + MEMORY + EXTERNAL-ENABLEMENT in the same
change set, closing Theme C and the v7.0 integration platform with a clean milestone handoff.
</success_criteria>

<output>
Create `.planning/phases/101-theme-c-marketplace-listings-developer-experience/101-10-SUMMARY.md` when done.
</output>
