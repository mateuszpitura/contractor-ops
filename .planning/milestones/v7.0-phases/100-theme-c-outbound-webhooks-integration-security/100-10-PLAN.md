---
phase: 100-theme-c-outbound-webhooks-integration-security
plan: 10
type: execute
wave: 6
depends_on: ["100-01", "100-02", "100-03", "100-04", "100-05", "100-06", "100-07", "100-08", "100-09"]
files_modified:
  - .planning/brain/wiki/domains/outbound-webhooks.md
  - .planning/brain/wiki/domains/public-api.md
  - .planning/brain/wiki/patterns/tenant-and-audit.md
  - .planning/brain/wiki/patterns/rate-limit.md
  - .planning/brain/wiki/structure/api-routers-catalog.md
  - .planning/brain/wiki/structure/key-services.md
  - .planning/brain/wiki/structure/prisma-schema-areas.md
  - .planning/brain/wiki/structure/cron-jobs.md
  - .planning/brain/wiki/log.md
  - .planning/brain/wiki/hot.md
  - .planning/MEMORY.md
  - .planning/EXTERNAL-ENABLEMENT.md
  - .planning/milestones/v7.0-phases/100-theme-c-outbound-webhooks-integration-security/100-VALIDATION.md
  - .planning/milestones/v7.0-phases/100-theme-c-outbound-webhooks-integration-security/100-101-HANDOFF.md
autonomous: false
requirements: [INTEG-WEBHOOK-01, INTEG-WEBHOOK-02, INTEG-WEBHOOK-03, INTEG-WEBHOOK-04, INTEG-WEBHOOK-05, INTEG-WEBHOOK-06, INTEG-WEBHOOK-07, INTEG-SEC-01, INTEG-SEC-02, INTEG-SEC-03, INTEG-SEC-04, INTEG-SEC-05]

must_haves:
  truths:
    - "The whole outbound-webhook + integration-security surface hangs together end-to-end: the scoped `packages/api` webhook + security suites (ssrf, hmac, redact, subscription, dispatch, rate-limit, owasp-api-gate) + `apps/public-api` (write-routes-dark, openapi-doc) + `apps/cron-worker` (api-key-leak-alarm) + `apps/web-vite` (webhooks) are GREEN; nothing dispatches without `module.outbound-webhooks`; the SSRF guard is enforced at subscribe + dispatch; the P99 writes are un-hidden behind the passed OWASP gate"
    - "The documentation-follows-code obligation is discharged in the SAME change set: a NEW `wiki/domains/outbound-webhooks.md` (purpose, flow, entry points, event catalog, SSRF/HMAC/DLQ/redaction, agent mistakes) + updated `public-api.md` (writes now live), `patterns/{tenant-and-audit,rate-limit}.md`, `structure/{api-routers-catalog,key-services,prisma-schema-areas,cron-jobs}.md`; `pnpm check:wiki-brain` passes (no NEW drift) and the graph + BM25 refresh is run per CLAUDE.md; `source_commit` bumped on touched pages"
    - "`.planning/MEMORY.md` records the durable Phase-100 invariants: (1) outbound webhooks reuse the OutboxEvent outbox for FAN-OUT only; per-delivery retry/DLQ is a dedicated drain with the [1m,5m,30m,2h,12h,24h] backoff; (2) customer webhook URLs are hostile — SSRF-guard at BOTH subscribe + dispatch with DNS-rebind re-resolve + IP-pin + redirects-off; (3) `X-CO-Signature t=…,v1=…` HMAC-SHA256 over `{t}.{body}` with a 5-min replay window + constant-time verify; (4) PII redaction is applied BEFORE the payload snapshot persists; (5) outbound models are NAME-DISTINCT from the inbound `WebhookDelivery`; (6) the P99 write flag-flip is GATED on the OWASP suite — un-hide in code, per-org `module.public-api` grant is a manual ops act"
    - "`.planning/EXTERNAL-ENABLEMENT.md` is updated: row #8 (public-API writes) reflects the un-hide; a NEW row for `module.outbound-webhooks` (dispatch gate, default OFF, what's dark until granted, the mock-receiver test that runs without it, the per-org enablement step) is added; the un-applied webhook migration (100-05) is recorded as a deploy-time step"
    - "`100-VALIDATION.md` sign-off is complete + `wave_0_complete: true`; a `100-101-HANDOFF.md` records the Phase-101 dependencies (the event catalog + SDK-with-writes + the verifier docs feed the marketplace apps + DX portal; the status page reuses the delivery gauges); no `console.*`"
  artifacts:
    - path: ".planning/brain/wiki/domains/outbound-webhooks.md"
      provides: "The outbound-webhooks domain page — dispatcher flow, event catalog, SSRF/HMAC/DLQ/redaction, agent mistakes"
      contains: "X-CO-Signature"
    - path: ".planning/milestones/v7.0-phases/100-theme-c-outbound-webhooks-integration-security/100-101-HANDOFF.md"
      provides: "The Phase-101 handoff — catalog/SDK/verifiers feed marketplace + DX portal; delivery gauges feed the status page"
      contains: "Phase 101"
---

<objective>
Close the phase: prove the outbound-webhook + integration-security surface hangs together end-to-end,
discharge the mandatory documentation-follows-code obligation (new + updated wiki + indexes + MEMORY in the
same change set), run the doc-drift + brain-refresh pipeline, update the external-enablement register, and
record the Phase-101 handoff. Human-checkpoint on the wiki (durable facts only — no operational text).

Output: new/updated wiki + MEMORY invariants + EXTERNAL-ENABLEMENT + brain refresh + validation sign-off + handoff.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-theme-c-outbound-webhooks-integration-security/100-CONTEXT.md
@.planning/phases/100-theme-c-outbound-webhooks-integration-security/100-RESEARCH.md

<interfaces>
Doc pipeline (CLAUDE.md § Documentation follows code): update the wiki pages, then `pnpm check:wiki-brain`
(must pass — no NEW drift for the changed source files), then the brain refresh: `rm -f
graphify-out/graph.json && graphify update . --no-cluster --force && cp graphify-out/graph.json
.planning/graphs/graph.json` and `cd .planning/brain && find wiki -name '*.md' -exec python3 scripts/
contextual-prefix.py {} --no-llm \; && python3 scripts/bm25-index.py build`. Bump `source_commit` on touched
wiki frontmatter. This is the LAST plan — it verifies the whole phase, so run the full scoped webhook/
security/public-api/cron/web-vite suites. NEVER run the full unscoped web-vite suite. EXTERNAL-ENABLEMENT
row style: mirror the existing rows (# | dependency | flag/gate | default | dark-until | auto-flip test |
enablement steps). MEMORY bullet style: durable facts only, no round-status/operational text.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end verification of the whole surface</name>
  <files>.planning/milestones/v7.0-phases/100-theme-c-outbound-webhooks-integration-security/100-VALIDATION.md</files>
  <read_first>
    - 100-VALIDATION.md (the per-requirement map + sign-off checklist)
    - the eight Wave-0 security/contract suites + write-routes-dark + the web-vite webhooks test
  </read_first>
  <action>
    Run the full scoped verification: `pnpm --filter @contractor-ops/api test webhook-ssrf webhook-hmac
    webhook-redact webhook-subscription webhook-dispatch webhook-rate-limit owasp-api-gate`; `pnpm --filter
    @contractor-ops/public-api test write-routes-dark openapi-doc`; `pnpm --filter @contractor-ops/cron-worker
    test api-key-leak-alarm`; `pnpm --filter @contractor-ops/web-vite test webhooks`; `pnpm typecheck
    --filter @contractor-ops/api --filter @contractor-ops/db --filter @contractor-ops/validators --filter
    @contractor-ops/public-api --filter @contractor-ops/cron-worker --filter @contractor-ops/web-vite`;
    `node scripts/lint-audit-log.mjs`; `node scripts/check-webhook-routes.mjs`. Confirm every INTEG-WEBHOOK /
    INTEG-SEC row in 100-VALIDATION is GREEN; complete the sign-off + set `wave_0_complete: true`. Confirm
    nothing dispatches without `module.outbound-webhooks` and the SSRF guard fires at both gates.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test webhook-ssrf webhook-hmac webhook-redact webhook-subscription webhook-dispatch webhook-rate-limit owasp-api-gate 2>&1 | tail -20; pnpm --filter @contractor-ops/public-api test write-routes-dark 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - All webhook + security suites + write-routes-dark + api-key-leak-alarm + web-vite webhooks GREEN.
    - `pnpm typecheck` across touched packages clean; audit-log + webhook-route checks pass.
    - 100-VALIDATION sign-off complete; `wave_0_complete: true`.
  </acceptance_criteria>
  <done>Whole outbound-webhook/integration-security surface verified end-to-end; validation signed off.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Documentation-follows-code + brain refresh + EXTERNAL-ENABLEMENT + Phase-101 handoff</name>
  <files>.planning/brain/wiki/domains/outbound-webhooks.md, .planning/brain/wiki/domains/public-api.md, .planning/brain/wiki/patterns/tenant-and-audit.md, .planning/brain/wiki/patterns/rate-limit.md, .planning/brain/wiki/structure/api-routers-catalog.md, .planning/brain/wiki/structure/key-services.md, .planning/brain/wiki/structure/prisma-schema-areas.md, .planning/brain/wiki/structure/cron-jobs.md, .planning/brain/wiki/log.md, .planning/brain/wiki/hot.md, .planning/MEMORY.md, .planning/EXTERNAL-ENABLEMENT.md, .planning/milestones/v7.0-phases/100-theme-c-outbound-webhooks-integration-security/100-101-HANDOFF.md</files>
  <read_first>
    - CLAUDE.md § Documentation follows code (the trigger matrix + the exact refresh pipeline commands)
    - .planning/brain/wiki/domains/public-api.md + wiki/patterns/{tenant-and-audit,rate-limit}.md + wiki/structure/{api-routers-catalog,key-services,prisma-schema-areas,cron-jobs}.md (the pages to update)
    - .planning/MEMORY.md (append the 6 invariants; match the existing bullet style, no operational text)
    - .planning/EXTERNAL-ENABLEMENT.md (row #8 to update + the new outbound-webhooks + migration rows)
  </read_first>
  <action>
    Author `wiki/domains/outbound-webhooks.md` (purpose; producer → outbox fan-out → deliver drain flow;
    entry points `enqueueWebhookEvent`/`/webhooks-outbound/_deliver`; the 16-event catalog; SSRF guard +
    DNS-rebind; `X-CO-Signature` + 5-min replay; DLQ + backoff; PII redaction; agent mistakes: "outbound ≠
    inbound `WebhookDelivery`", "SSRF-check at BOTH gates", "redact before persist", "nothing dispatches
    without `module.outbound-webhooks`"). Update `public-api.md` (writes are now live behind the OWASP gate;
    `webhooks:manage` scope), `patterns/tenant-and-audit.md` (`WEBHOOK_SUBSCRIPTION` audit + sourceIp/UA),
    `patterns/rate-limit.md` (the per-sub dispatch limit joins the pre-auth burst + post-auth quota),
    `structure/api-routers-catalog.md` (`webhookSubscriptionRouter`), `structure/key-services.md`
    (webhooks/{ssrf-guard,signer,redact,dispatcher}), `structure/prisma-schema-areas.md` (the three outbound
    models), `structure/cron-jobs.md` (`api-key-leak-alarm`). Append the 6 durable invariants to
    `.planning/MEMORY.md`. Update `EXTERNAL-ENABLEMENT.md`: row #8 (writes un-hidden) + a new
    `module.outbound-webhooks` row + the un-applied webhook migration as a deploy-time step. HUMAN-VERIFY
    GATE: confirm no operational/round-status text leaked into the wiki (durable facts only). Update
    `wiki/log.md`, overwrite `hot.md`, bump `source_commit`, then `pnpm check:wiki-brain` + the graph + BM25
    refresh. Author `100-101-HANDOFF.md`: the event catalog + SDK-with-writes + the TS/Python/Go/PHP verifier
    docs feed the Phase-101 marketplace apps (Zapier/n8n/Make) + DX portal; the delivery gauges feed the
    status page; the sandbox tier reuses the dark-flag posture. No `console.*`.
  </action>
  <verify>
    <automated>pnpm check:wiki-brain 2>&1 | tail -8; test -f .planning/milestones/v7.0-phases/100-theme-c-outbound-webhooks-integration-security/100-101-HANDOFF.md && grep -qi "Phase 101" .planning/milestones/v7.0-phases/100-theme-c-outbound-webhooks-integration-security/100-101-HANDOFF.md && echo handoff-ok</automated>
  </verify>
  <acceptance_criteria>
    - `wiki/domains/outbound-webhooks.md` (new) + the updated public-api/pattern/structure pages reflect the
      dispatcher, event catalog, SSRF/HMAC/DLQ/redaction, the per-sub dispatch limit, the outbound models,
      the leak-alarm cron, and the un-hidden writes; `pnpm check:wiki-brain` passes; graph + BM25 refreshed;
      `source_commit` bumped.
    - `.planning/MEMORY.md` has the 6 durable invariants (durable facts only — no operational text).
    - `EXTERNAL-ENABLEMENT.md` updates row #8 + adds the `module.outbound-webhooks` + migration rows.
    - `100-101-HANDOFF.md` records the catalog/SDK/verifiers → marketplace + DX portal + status-page deps.
  </acceptance_criteria>
  <done>Docs-follows-code discharged; brain refreshed; external-enablement updated; Phase-101 handoff recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| code change → wiki | A product change without a matching wiki update makes the next agent hallucinate |
| phase 100 → phase 101 | The event catalog + SDK + verifiers are the contract the marketplace/DX phase consumes |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-10-01 | Repudiation | Stale docs hide the new webhook/SSRF/HMAC/DLQ behavior | mitigate | Wiki + MEMORY updated in the same change set; `check:wiki-brain` gates |
| T-100-10-02 | Security Misconfiguration | An operator enables dispatch without knowing the SSRF/kill-switch posture | mitigate | The outbound-webhooks domain page + EXTERNAL-ENABLEMENT row document the gate + enablement steps |
| T-100-10-SC | Tampering | npm installs | accept | Docs only |
</threat_model>

<verification>
- The full scoped webhook/security/public-api/cron/web-vite suites GREEN (Task 1); `pnpm typecheck` across touched packages clean; audit-log + webhook-route checks pass.
- `pnpm check:wiki-brain` passes; graph + BM25 refresh run per CLAUDE.md.
- `100-101-HANDOFF.md` exists and names the catalog/SDK/verifier → marketplace + DX + status-page dependencies.
</verification>

<success_criteria>
The phase is closed and coherent: orgs subscribe to a typed event catalog, a greenfield dispatcher on the
existing outbox delivers signed, PII-safe, SSRF-guarded events with backoff + DLQ + alerts, keys raise a
leak alarm, the OWASP gate passed and the P99 writes are live — documented in the wiki + MEMORY +
EXTERNAL-ENABLEMENT in the same change set, with a clean handoff to Phase 101.
</success_criteria>

<output>
Create `.planning/phases/100-theme-c-outbound-webhooks-integration-security/100-10-SUMMARY.md` when done.
</output>