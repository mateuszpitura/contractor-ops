---
phase: 100-theme-c-outbound-webhooks-integration-security
plan: 04
type: execute
wave: 1
depends_on: ["100-01"]
files_modified:
  - packages/api/src/services/webhooks/redact.ts
  - packages/api/src/__tests__/security/webhook-redact.security.test.ts
autonomous: true
requirements: [INTEG-WEBHOOK-07]

must_haves:
  truths:
    - "`redactPii(payload, { includePii })` deep-strips (nested objects + arrays) every PII key when `includePii` is false (the default): PESEL, SSN, NI number, Steuer-IdNr / taxId, Emirates ID, Iqama, IBAN/bank account, `email`, `phone` — and retains them when `includePii` is true"
    - "The PII key-set is DERIVED from the real field inventory (`packages/validators/src/employee-country-fields.ts` + `legal/compliance-{ksa,uae}.ts` + `us-validators.ts`), not a hardcoded guess, so a field rename is caught; the redactor is exported for the fan-out handler (100-06) to apply BEFORE the payload snapshot is persisted"
    - "Redaction replaces values with a stable sentinel (e.g. `'[redacted]'`) rather than deleting the key, so downstream shape stays valid; the function is pure + synchronous"
    - "The `webhook-redact.security.test.ts` RED net from 100-01 is GREEN (asserting the redacted value is what would be PERSISTED, not only what is sent); no `console.*`"
  artifacts:
    - path: "packages/api/src/services/webhooks/redact.ts"
      provides: "redactPii + WEBHOOK_PII_KEYS — the RODO-defensible payload redactor applied before persist"
      contains: "redactPii"
---

<objective>
Build the PII redactor so outbound payloads are RODO-defensible by default. `include_pii:false` (the
per-subscription default) strips national identifiers and contact PII from the event payload BEFORE it is
snapshotted into the deliverable/DLQ row, so no PII persists in a row an operator (or a DLQ inspection)
could read. Consumed by the fan-out handler (100-06). Turns the `webhook-redact` RED net green.

Output: `webhooks/redact.ts` (`redactPii` + the derived `WEBHOOK_PII_KEYS`).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-theme-c-outbound-webhooks-integration-security/100-CONTEXT.md
@.planning/phases/100-theme-c-outbound-webhooks-integration-security/100-RESEARCH.md

<interfaces>
`redactPii<T>(payload: T, opts: { includePii: boolean }): T` — pure, deep, returns a redacted CLONE (never
mutates input). Build `WEBHOOK_PII_KEYS` (a Set of lowercased key names + a few regexes for `*IdNr`, `*ssn`)
from the validator field inventory rather than a literal list, so the set tracks the schema. Redaction is
key-name-based (defensive default) — a value under any PII key becomes `'[redacted]'`. The fan-out handler
(100-06) calls this on `payload.data` with the subscription's `includePii` before persisting the snapshot.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the redactor + derive the PII key-set</name>
  <files>packages/api/src/services/webhooks/redact.ts</files>
  <read_first>
    - 100-RESEARCH.md § A5 (redact-before-persist rationale + the field inventory sources)
    - packages/validators/src/employee-country-fields.ts (PESEL/Steuer-IdNr/Emirates/Iqama field names)
    - packages/validators/src/legal/compliance-ksa.ts + compliance-uae.ts (Iqama/Emirates ID fields)
    - packages/validators/src/us-validators.ts (SSN)
    - packages/api/src/__tests__/security/webhook-redact.security.test.ts (the RED contract — the exact keys asserted)
  </read_first>
  <action>
    Author `redact.ts`: assemble `WEBHOOK_PII_KEYS` from the validator inventory (import/reference the field
    names so a rename surfaces) plus `email`, `phone`, IBAN/bank-account keys; a small regex set for
    `*idnr`/`*ssn`/`*iqama` variants. `redactPii(payload,{includePii})`: if `includePii` return a structural
    clone unchanged; else recurse objects + arrays, replacing any value whose key matches with
    `'[redacted]'`. Pure, no mutation, no `console.*`. Export the key-set for the OWASP-gate test to assert
    against.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test webhook-redact 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - Every PII key in the RED contract is stripped when `includePii:false`, nested + in arrays; retained when true.
    - The key-set is derived from the validator inventory (a rename would be caught), not a hardcoded literal.
    - Input is never mutated; output shape stays valid; the `webhook-redact` net is GREEN.
  </acceptance_criteria>
  <done>redactPii + derived PII key-set authored (green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| event payload → persisted attempt/DLQ row | Redaction must happen before persist, or PII sits in a readable row |
| our platform → subscriber | Default-off PII is the RODO-defensible posture; opt-in is explicit per subscription |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-04-01 | Information Disclosure | PESEL/SSN/Iqama sent to an unauthorized subscriber | mitigate | `include_pii:false` default strips national IDs + contact PII |
| T-100-04-02 | Information Disclosure | PII persisted in a DLQ row inspected by an operator | mitigate | Redact BEFORE the snapshot persists (100-06 calls redact in fan-out) |
| T-100-04-03 | Tampering | A renamed PII field silently escapes redaction | mitigate | Key-set derived from the validator inventory, not a static list |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test webhook-redact` — GREEN.
- `pnpm typecheck --filter @contractor-ops/api` clean.
</verification>

<success_criteria>
Outbound payloads carry no national-identifier or contact PII unless a subscription explicitly opts in, and
the redaction is applied before the payload is persisted so no PII sits in a deliverable or dead-letter row.
</success_criteria>

<output>
Create `.planning/phases/100-theme-c-outbound-webhooks-integration-security/100-04-SUMMARY.md` when done.
</output>
