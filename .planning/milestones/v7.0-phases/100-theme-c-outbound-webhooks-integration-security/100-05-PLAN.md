---
phase: 100-theme-c-outbound-webhooks-integration-security
plan: 05
type: execute
wave: 2
depends_on: ["100-01", "100-02"]
files_modified:
  - packages/db/prisma/schema/webhook.prisma
  - packages/db/prisma/schema/migrations
  - packages/validators/src/webhooks/index.ts
  - packages/validators/src/index.ts
  - packages/api/src/services/audit-writer.ts
  - packages/api/src/__tests__/webhook-subscription.test.ts
autonomous: true
requirements: [INTEG-WEBHOOK-01, INTEG-WEBHOOK-02]

must_haves:
  truths:
    - "A new `webhook.prisma` adds THREE outbound models NAME-DISTINCT from the inbound `WebhookDelivery`: `WebhookSubscription` (org, url, `eventFilter String[]`, `secretEncrypted`, `includePii Boolean @default(false)`, `httpAllowed Boolean @default(false)`, `enabled`, `maxRetries Int @default(6)`, `lastSuccessAt?`, `lastFailureAt?`), `WebhookDeliveryAttempt` (subscriptionId, outboxEventId?, eventType, payloadJson snapshot, status, attempts, nextAttemptAt, responseStatus?, lastError?, deliveredAt?), and `WebhookDeadLetter` (`@@map(\"webhook_failures\")`); a new `OutboundWebhookStatus` enum (PENDING|SENDING|DELIVERED|FAILED|DEAD) — the inbound `WebhookDeliveryStatus` is NOT reused"
    - "All three models carry a required `organizationId` (RLS auto-scope, mirroring `OutboxEvent`), the attempt has `@@index([status, nextAttemptAt])` (the deliver-drain predicate), and the generated migration includes RLS policies mirroring the inbound `WebhookDelivery` policies (org_match + can_write_ops)"
    - "`packages/validators/src/webhooks/index.ts` defines the event catalog as a Zod discriminated union on `type` over the 16 events (contractor.*4, invoice.*5, payment_run.*2, workflow.*2, classification.outcome, compliance_doc.*2), each variant `.strict()`, plus the envelope `{ id, type, created_at, organization_id, data, include_pii }` and a `WebhookEventType` z.enum"
    - "`AuditEntityType` gains `WEBHOOK_SUBSCRIPTION` so subscription-lifecycle mutations audit; `writeAuditLog` accepts it"
    - "The `webhook-subscription.test.ts` RED net from 100-01 is GREEN for the model + catalog + audit-type portions (CRUD wiring lands in 100-08); `pnpm typecheck --filter @contractor-ops/db --filter @contractor-ops/validators --filter @contractor-ops/api` clean; no `console.*`"
  artifacts:
    - path: "packages/db/prisma/schema/webhook.prisma"
      provides: "WebhookSubscription + WebhookDeliveryAttempt + WebhookDeadLetter + OutboundWebhookStatus — the outbound persistence layer"
      contains: "WebhookSubscription"
    - path: "packages/validators/src/webhooks/index.ts"
      provides: "The event catalog discriminated union + envelope + WebhookEventType — the typed payload contract"
      contains: "compliance_doc.expiring_soon"
---

<objective>
Lay the outbound persistence + payload contract: the `WebhookSubscription`/`WebhookDeliveryAttempt`/
`WebhookDeadLetter` models (name-distinct from the inbound `WebhookDelivery`), the Zod discriminated-union
event catalog, and the `WEBHOOK_SUBSCRIPTION` audit type. This is the schema every later plan builds on —
the fan-out handler writes attempts, the deliver drain reads them, the DLQ captures exhaustion, and the
subscription service (100-08) does CRUD. Greens the model/catalog/audit portions of `webhook-subscription`.

Output: `webhook.prisma` + migration + `validators/webhooks` + the `WEBHOOK_SUBSCRIPTION` audit type.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-theme-c-outbound-webhooks-integration-security/100-CONTEXT.md
@.planning/phases/100-theme-c-outbound-webhooks-integration-security/100-RESEARCH.md

<interfaces>
Mirror `packages/db/prisma/schema/outbox.prisma` (`OutboxEvent` shape: attempts/nextAttemptAt/lastError/
status + `@@index([status, nextAttemptAt])` + `organization` relation) and the inbound
`packages/db/prisma/schema/integration.prisma` `WebhookDelivery` RLS policies
(`.../migrations/20260512000000_baseline/migration.sql:4722` — `webhookdelivery_select/write` policies) for
the new models' RLS. The secret is stored ENCRYPTED (100-08 handles gen/encrypt; here the column is
`secretEncrypted String`). Event catalog per 100-RESEARCH § B; envelope per § B. `AuditEntityType` +
`buildAuditLogRow` live in `packages/api/src/services/audit-writer.ts` (add the enum member alongside the
existing `EWIDENCJA_SNAPSHOT` etc.). The Prisma generator is `prisma-client` (Prisma 7); regional
`DATABASE_URL_*` — the migration is generated + committed, applied at deploy (EXTERNAL-ENABLEMENT posture).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the three outbound models + enum + migration + RLS</name>
  <files>packages/db/prisma/schema/webhook.prisma, packages/db/prisma/schema/migrations</files>
  <read_first>
    - 100-RESEARCH.md § A4 (the three models + why the inbound enum is not reused)
    - packages/db/prisma/schema/outbox.prisma (the shape to mirror: attempts/nextAttemptAt/status/index/relation)
    - packages/db/prisma/schema/integration.prisma (the inbound WebhookDelivery — the name-collision to avoid)
    - packages/db/prisma/schema/migrations/20260512000000_baseline/migration.sql:4717-4728 (the WebhookDelivery RLS policy shape to mirror)
  </read_first>
  <action>
    Create `webhook.prisma` with `WebhookSubscription`, `WebhookDeliveryAttempt`, `WebhookDeadLetter`
    (`@@map("webhook_failures")`), and `enum OutboundWebhookStatus { PENDING SENDING DELIVERED FAILED DEAD }`
    per § A4 (required `organizationId`, `organization` relations, the attempt's `@@index([status,
    nextAttemptAt])`, subscription `@@index([organizationId, enabled])`). Add the reverse relations on
    `Organization`. Generate the migration (Prisma migrate) and hand-add RLS policies to the migration SQL
    mirroring the inbound `WebhookDelivery` policies (`org_match` + `can_write_ops`). Do NOT apply to a live
    DB (deploy-time step — EXTERNAL-ENABLEMENT). No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db exec prisma generate 2>&1 | tail -6; pnpm typecheck --filter @contractor-ops/db 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Three outbound models + `OutboundWebhookStatus` exist; the inbound `WebhookDelivery`/
      `WebhookDeliveryStatus` are untouched.
    - Migration generated with RLS policies for all three; Prisma client compiles.
  </acceptance_criteria>
  <done>Outbound persistence + migration + RLS authored (client compiles).</done>
</task>

<task type="auto">
  <name>Task 2: Author the event catalog union + envelope + the WEBHOOK_SUBSCRIPTION audit type</name>
  <files>packages/validators/src/webhooks/index.ts, packages/validators/src/index.ts, packages/api/src/services/audit-writer.ts, packages/api/src/__tests__/webhook-subscription.test.ts</files>
  <read_first>
    - 100-RESEARCH.md § B (the 16-event catalog + envelope)
    - packages/api/src/services/audit-writer.ts (the AuditEntityType union + buildAuditLogRow to extend)
    - packages/validators/src/index.ts (the barrel export pattern)
    - packages/api/src/__tests__/webhook-subscription.test.ts (the RED contract — event enum + create + audit)
  </read_first>
  <action>
    Author `validators/src/webhooks/index.ts`: `WebhookEventType` (`z.enum` of the 16 events), a `.strict()`
    per-type payload variant, the discriminated union on `type`, and the envelope schema `{ id, type,
    created_at, organization_id, data, include_pii }`. Barrel-export from `validators/src/index.ts`. Add
    `WEBHOOK_SUBSCRIPTION` to `AuditEntityType` in `audit-writer.ts`. Un-skip / green the model+catalog+audit
    portions of `webhook-subscription.test.ts` (leave CRUD-wiring assertions RED for 100-08 with a comment).
    No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test webhook-subscription 2>&1 | tail -12; pnpm typecheck --filter @contractor-ops/validators --filter @contractor-ops/api 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The catalog union enumerates all 16 events, each variant `.strict()`; the envelope schema exists + is exported.
    - `AuditEntityType` includes `WEBHOOK_SUBSCRIPTION`; the model/catalog/audit portions of the RED net are GREEN.
  </acceptance_criteria>
  <done>Event catalog + envelope + audit type authored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| tenant data → subscription/attempt/DLQ rows | RLS must auto-scope per org, mirroring the outbox/inbound-webhook policies |
| inbound vs outbound webhook models | A shared model/enum would let the inbound reaper touch outbound rows |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-05-01 | Elevation of Privilege | Cross-tenant read of another org's subscriptions/deliveries | mitigate | Required `organizationId` + RLS policies on all three models |
| T-100-05-02 | Tampering | Inbound reaper mutates outbound rows (shared model) | mitigate | Name-distinct models + a separate `OutboundWebhookStatus` enum |
| T-100-05-03 | Repudiation | Subscription lifecycle unaudited | mitigate | `WEBHOOK_SUBSCRIPTION` audit type added so 100-08 mutations audit |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test webhook-subscription` — model/catalog/audit portions GREEN (CRUD RED for 100-08).
- `pnpm --filter @contractor-ops/db exec prisma generate` + `pnpm typecheck --filter @contractor-ops/db --filter @contractor-ops/validators --filter @contractor-ops/api` clean.
- Migration present with RLS policies; not applied to a live DB (deploy-time step).
</verification>

<success_criteria>
The outbound persistence layer + typed event catalog exist, are tenant-scoped by RLS, are name-distinct
from the inbound webhook models, and subscription lifecycle is auditable — the foundation the dispatcher and
subscription service build on.
</success_criteria>

<output>
Create `.planning/phases/100-theme-c-outbound-webhooks-integration-security/100-05-SUMMARY.md` when done.
Record the migration filename + confirm it is NOT applied (deploy-time — add to EXTERNAL-ENABLEMENT in 100-10).
</output>
