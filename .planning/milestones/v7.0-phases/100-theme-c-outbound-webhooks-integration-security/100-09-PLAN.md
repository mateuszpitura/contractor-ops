---
phase: 100-theme-c-outbound-webhooks-integration-security
plan: 09
type: execute
wave: 5
depends_on: ["100-01", "100-02", "100-03", "100-04", "100-05", "100-06", "100-07", "100-08"]
files_modified:
  - packages/api/src/__tests__/security/owasp-api-gate.security.test.ts
  - apps/public-api/src/routes/contractors.ts
  - apps/public-api/src/routes/invoices.ts
  - apps/public-api/src/routes/payment-runs.ts
  - apps/public-api/src/routes/payments.ts
  - apps/public-api/src/routes/workflows.ts
  - apps/public-api/src/routes/workflow-tasks.ts
  - apps/public-api/src/__tests__/write-routes-dark.test.ts
  - .planning/EXTERNAL-ENABLEMENT.md
autonomous: false
requirements: [INTEG-SEC-04]

must_haves:
  truths:
    - "INTEG-SEC-04 is an executable phase GATE, not prose: `owasp-api-gate.security.test.ts` asserts each OWASP API Top-10 class against the real surface — BOLA (a cross-org subscription/resource id → 404/forbidden), BFLA (a read-scope key cannot reach a write/manage endpoint — reuse the P99 403 matrix), SSRF (a private webhook url is rejected), mass-assignment (`.strict()` DTOs strip `organizationId`/`secretEncrypted`/money), security-misconfig (error responses leak no stack/secret/flag-internals), and injection (filter/sort params are parameterized) — all GREEN"
    - "ONLY AFTER the OWASP gate suite is GREEN does the write flag-flip happen: the P99 `hide:true` is removed from the six public WRITE routes (contractor create/update, invoice create/void, payment update, paymentRun create/transition/export, workflow create/execute, workflowTask transition), so the write-verb count in `buildOpenApiDocument` goes 0 → N and the writes appear in the derived spec / Scalar / SDK"
    - "`write-routes-dark.test.ts` (P99) is UPDATED to assert the writes are now PRESENT in the spec (the double-dark → live transition) while the per-org `module.public-api` grant remains a MANUAL Unleash/ops act (recorded in EXTERNAL-ENABLEMENT row #8, not scripted) — a write endpoint still 404s for an org whose flag is off (`assertPublicApiEnabled` unchanged)"
    - "The flip is a HUMAN-CHECKPOINT: a human confirms the OWASP suite is GREEN before the un-hide; `EXTERNAL-ENABLEMENT.md` row #8 is updated to record the un-hide is done + the per-org flag grant is the remaining manual step; `_initiatePayoutForRun` stays deferred (not un-hidden)"
    - "`pnpm --filter @contractor-ops/public-api test write-routes-dark openapi-doc` + `pnpm --filter @contractor-ops/api test owasp-api-gate` GREEN; no unscoped-mutating endpoint exists; no `console.*`"
  artifacts:
    - path: "packages/api/src/__tests__/security/owasp-api-gate.security.test.ts"
      provides: "The INTEG-SEC-04 OWASP API Top-10 automated review gate (BOLA/BFLA/SSRF/mass-assignment/misconfig/injection)"
      contains: "BOLA"
    - path: "apps/public-api/src/__tests__/write-routes-dark.test.ts"
      provides: "The double-dark → live assertion: writes present in the spec post-flip; still 404 per-org until the flag grant"
      contains: "hide"
---

<objective>
Close the phase's security gate and perform the inherited P99 write flag-flip — the deliberate, gated end of
Theme C's build-out. First prove the whole public surface against the OWASP API Top-10 as REAL tests; only
when that suite is GREEN, un-hide the P99 write routes so they enter the spec/SDK. The per-org
`module.public-api` grant stays a manual ops act. Human-checkpoint: confirm the gate is green before the flip.

Output: the OWASP gate suite (green) + un-hidden write routes + the updated dark→live test + EXTERNAL-ENABLEMENT.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-theme-c-outbound-webhooks-integration-security/100-CONTEXT.md
@.planning/phases/100-theme-c-outbound-webhooks-integration-security/100-RESEARCH.md

<interfaces>
OWASP gate: assert against the LIVE public surface — reuse the P99 `public-api-write-scope.security.test.ts`
403 matrix (BFLA), the tenant-isolation test (BOLA), the `strict-dto` tests (mass-assignment), the
`webhook-ssrf` guard (SSRF), and add misconfig (error-shape) + injection (filter/sort parameterization)
checks. The write routes carry `hide:true` on their `createRoute` definitions (`apps/public-api/src/routes/
*.ts`; see `apps/public-api/src/lib/build-openapi-doc.ts` — write routes are excluded from the derived doc
while hidden). Removing `hide:true` is the un-hide. The per-org dark gate `assertPublicApiEnabled`
(`require-public-api-flag.ts`) is UNCHANGED — the flip is granting `module.public-api` in Unleash per org, a
manual act recorded in `EXTERNAL-ENABLEMENT.md` row #8. `write-routes-dark.test.ts` currently asserts writes
are ABSENT from the spec; update it to assert PRESENT post-un-hide + still 404 for a flag-off org. Do NOT
un-hide `_initiatePayoutForRun` (P99 A7 — stays deferred). This is the LAST gate before docs (100-10).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the OWASP API Top-10 gate suite (green against the live surface)</name>
  <files>packages/api/src/__tests__/security/owasp-api-gate.security.test.ts</files>
  <read_first>
    - 100-RESEARCH.md § E (the OWASP gate row) + § D10 (do not un-hide before the gate)
    - the P99 security suites (public-api-write-scope BFLA matrix, tenant-isolation BOLA, strict-dto mass-assignment) — reuse their assertions (read if the P99 dir is present)
    - packages/api/src/services/webhooks/ssrf-guard.ts (the SSRF assertion source)
    - packages/api/src/__tests__/security/owasp-api-gate.security.test.ts (the RED stub from 100-01 to fill)
  </read_first>
  <action>
    Turn the 100-01 OWASP-gate stub into a real gate: one describe block per OWASP API class asserting the
    live behavior — BOLA (cross-org id → 404), BFLA (read-scope key → FORBIDDEN on write/manage; reuse the
    P99 matrix), SSRF (private webhook url rejected by `assertWebhookUrlSafe`), mass-assignment (`.strict()`
    strips `organizationId`/`secretEncrypted`/money), security-misconfig (an error response body contains no
    stack/secret/flag-internal), injection (a `${}`/`'; DROP` filter value is parameterized, not
    interpolated). Each assertion runs against the actual procedures/guards, not mocks-of-mocks. GREEN. No
    `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test owasp-api-gate 2>&1 | tail -16</automated>
  </verify>
  <acceptance_criteria>
    - Every OWASP class has a real assertion against the live surface; the suite is GREEN.
    - No class is a placeholder; BFLA reuses the P99 403 matrix; SSRF reuses the 100-02 guard.
  </acceptance_criteria>
  <done>OWASP API Top-10 gate is executable + green.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: GATED — un-hide the P99 write routes + update the dark→live test + EXTERNAL-ENABLEMENT</name>
  <files>apps/public-api/src/routes/contractors.ts, apps/public-api/src/routes/invoices.ts, apps/public-api/src/routes/payment-runs.ts, apps/public-api/src/routes/payments.ts, apps/public-api/src/routes/workflows.ts, apps/public-api/src/routes/workflow-tasks.ts, apps/public-api/src/__tests__/write-routes-dark.test.ts, .planning/EXTERNAL-ENABLEMENT.md</files>
  <read_first>
    - 100-RESEARCH.md § A8 (the flip is inherited + gated)
    - 99-CONTEXT.md D-03 (the double-dark contract — read if present) + 99-100-HANDOFF.md (the flip sequencing)
    - apps/public-api/src/lib/build-openapi-doc.ts (how hide:true excludes a route from the derived doc)
    - apps/public-api/src/routes/*.ts (the write createRoute definitions carrying hide:true)
    - .planning/EXTERNAL-ENABLEMENT.md row #8 (the register row to update)
  </read_first>
  <action>
    HUMAN-VERIFY GATE FIRST: confirm `owasp-api-gate` (Task 1) + all Wave-0 security nets are GREEN — if any
    is red, STOP (do not un-hide). Then remove `hide:true` from the six write route groups (contractor
    create/update, invoice create/void, payment update, paymentRun create/transition/export, workflow
    create/execute, workflowTask transition) so `buildOpenApiDocument` includes them — but NOT
    `_initiatePayoutForRun`. Update `write-routes-dark.test.ts` to assert the writes are now PRESENT in the
    derived spec AND still 404 for an org whose `module.public-api` is off (the per-org gate is unchanged).
    Update `EXTERNAL-ENABLEMENT.md` row #8: the un-hide is DONE in code; the remaining step is the manual
    per-org `module.public-api` Unleash grant (post-OWASP-gate, adviser sign-off where noted). No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/public-api test write-routes-dark openapi-doc 2>&1 | tail -14</automated>
  </verify>
  <acceptance_criteria>
    - Un-hide happens ONLY after the OWASP gate + Wave-0 nets are green (human-confirmed).
    - Write-verb count in the derived spec goes 0 → N (payout init NOT included); a flag-off org still 404s.
    - EXTERNAL-ENABLEMENT row #8 records the un-hide + the remaining manual per-org grant.
  </acceptance_criteria>
  <done>Write routes un-hidden behind the passed OWASP gate; dark→live test + register updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| OWASP gate → un-hide | The write surface must not become spec-visible until the gate proves the surface secure |
| code un-hide vs per-org flag | Un-hiding a route ≠ enabling it; the per-org `module.public-api` grant is the ops control |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-09-01 | Elevation of Privilege | Writes exposed before the security review | mitigate | Un-hide gated on the green OWASP suite + a human checkpoint |
| T-100-09-02 | Elevation of Privilege | A write reachable without a scope post-flip (BFLA) | mitigate | OWASP-gate BFLA class reuses the P99 403 matrix; every write keeps `requirePermission` |
| T-100-09-03 | Information Disclosure | An un-reviewed payout-init path un-hidden by accident | mitigate | `_initiatePayoutForRun` explicitly NOT un-hidden (stays deferred) |
| T-100-09-04 | Security Misconfiguration | Error responses leak internals post-exposure | mitigate | OWASP-gate misconfig class asserts no stack/secret/flag-internal in error bodies |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test owasp-api-gate` — GREEN (the gate).
- `pnpm --filter @contractor-ops/public-api test write-routes-dark openapi-doc` — GREEN (writes present post-flip; 404 per-org until granted).
- `pnpm typecheck --filter @contractor-ops/public-api --filter @contractor-ops/api` clean.
</verification>

<success_criteria>
The public API passes an executable OWASP API Top-10 gate, and only then are the P99 write endpoints
un-hidden into the spec/SDK — with the per-org enablement left as a deliberate manual grant, the payout-init
path still deferred, and no unscoped mutating endpoint anywhere.
</success_criteria>

<output>
Create `.planning/phases/100-theme-c-outbound-webhooks-integration-security/100-09-SUMMARY.md` when done.
Record the OWASP-gate green run + confirm the per-org flag grant remains a manual step for 100-10's handoff.
</output>
