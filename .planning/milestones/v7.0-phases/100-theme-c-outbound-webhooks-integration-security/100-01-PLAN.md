---
phase: 100-theme-c-outbound-webhooks-integration-security
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/api/src/__tests__/security/webhook-ssrf.security.test.ts
  - packages/api/src/__tests__/security/webhook-hmac.security.test.ts
  - packages/api/src/__tests__/security/webhook-redact.security.test.ts
  - packages/api/src/__tests__/webhook-subscription.test.ts
  - packages/api/src/__tests__/webhook-dispatch.test.ts
  - packages/api/src/__tests__/security/webhook-rate-limit.security.test.ts
  - packages/api/src/__tests__/security/owasp-api-gate.security.test.ts
  - apps/cron-worker/src/__tests__/api-key-leak-alarm.test.ts
  - .planning/phases/100-theme-c-outbound-webhooks-integration-security/100-VALIDATION.md
autonomous: true
requirements: [INTEG-WEBHOOK-01, INTEG-WEBHOOK-02, INTEG-WEBHOOK-03, INTEG-WEBHOOK-04, INTEG-WEBHOOK-05, INTEG-WEBHOOK-07, INTEG-SEC-01, INTEG-SEC-02, INTEG-SEC-03, INTEG-SEC-04, INTEG-SEC-05]

must_haves:
  truths:
    - "A RED security net exists BEFORE any Phase-100 production code: eight NEW test files (webhook-ssrf, webhook-hmac, webhook-redact, webhook-subscription, webhook-dispatch, webhook-rate-limit, owasp-api-gate, api-key-leak-alarm) each RED against the not-yet-built surface, each naming exactly which later plan turns it green"
    - "The SSRF net asserts BOTH gates: subscribe-time rejection of private/loopback/link-local/`169.254.169.254`/`0.0.0.0` AND dispatch-time DNS-rebind (a host that re-resolves public→private is blocked at connect) AND redirect-to-metadata is not followed AND HTTPS-only unless the per-org HTTP override — RED until 100-02"
    - "The HMAC net asserts `X-CO-Signature: t={unix_ms},v1={hex}` where `v1=HMAC_SHA256(secret,'{t}.{body}')`, a wrong secret fails constant-time compare, and a timestamp older than the 5-minute window is rejected BEFORE compare — RED until 100-03"
    - "The redaction net asserts PII (PESEL/SSN/NI/Steuer-IdNr/Emirates/Iqama/email/phone) is stripped from the PERSISTED `WebhookDeliveryAttempt.payloadJson` when `include_pii:false` (not only from the wire) and retained when true — RED until 100-04"
    - "The dispatch net asserts the backoff schedule (1m/5m/30m/2h/12h/24h, max 6), terminal → `webhook_failures` DLQ, DLQ-replay, poison-row isolation (a throwing row does not stall the batch), and the 5-failures/1h admin alert — RED until 100-06; the rate-limit net asserts 100/min per-sub throttle-not-drop — RED until 100-06; the leak-alarm net asserts >3 IPs/24h — RED until 100-07; the OWASP gate net asserts BOLA/BFLA/SSRF/mass-assignment/misconfig/injection as real checks — RED until 100-09"
    - "Every RED test is scoped-runnable (`--filter` + path), uses no watch mode, uses a local mock receiver (never a real external URL), and does not touch the full web-vite suite; `100-VALIDATION.md` frontmatter flips `nyquist_compliant: true` once the stubs land"
  artifacts:
    - path: "packages/api/src/__tests__/security/webhook-ssrf.security.test.ts"
      provides: "The executable INTEG-SEC-01/-02 SSRF + DNS-rebind + HTTPS-only contract — RED until 100-02"
      contains: "169.254.169.254"
    - path: "packages/api/src/__tests__/security/webhook-hmac.security.test.ts"
      provides: "The INTEG-WEBHOOK-04/-05 signature + 5-min replay contract — RED until 100-03"
      contains: "X-CO-Signature"
    - path: "packages/api/src/__tests__/webhook-dispatch.test.ts"
      provides: "The INTEG-WEBHOOK-03 backoff/DLQ/poison-isolation/alert contract — RED until 100-06"
      contains: "webhook_failures"
---

<objective>
Author the phase's RED validation net FIRST so every later plan is turned-green against an executable
contract, not prose. Eight NEW test files cover the security-critical controls (SSRF + DNS-rebind, HMAC +
replay, PII redaction, backoff/DLQ/poison, per-sub rate-limit, key-leak alarm, OWASP gate) plus the
subscription/catalog contract. Each RED file names the plan that greens it (100-02 SSRF, 100-03 HMAC,
100-04 redaction, 100-05 subscription/catalog, 100-06 dispatch/rate-limit, 100-07 leak-alarm, 100-09 OWASP
gate). This is the phase's Nyquist floor: no SSRF/HMAC/redaction/DLQ/rate-limit code ships without a test
that was RED before it.

Output: 8 new RED test files (against a local mock receiver) + the nyquist flip.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-theme-c-outbound-webhooks-integration-security/100-CONTEXT.md
@.planning/phases/100-theme-c-outbound-webhooks-integration-security/100-RESEARCH.md

<interfaces>
Mirror existing test seams: `packages/api/src/__tests__/security/*.security.test.ts` (the regression
pattern), `packages/test-utils/src/msw/handlers/qstash.ts` (MSW QStash publish handler — reuse to stub the
`webhook.deliver` enqueue), and the apiKey/flag-evaluator stubbing in
`packages/api/src/routers/public-api/__tests__/*.test.ts` (`vi.hoisted`/`vi.mock`). The eight files assert
CONTRACTS that later plans satisfy; where a symbol does not yet exist (e.g. `assertWebhookUrlSafe`,
`signWebhookPayload`, `redactPii`, `webhookSubscription` model, the deliver drain), the test references it
so the file is RED — prefer a runtime assertion that fails over a `describe.skip`, using `describe.skip
('HOLD-until-100-0X')` ONLY where a hard type/import error would block the whole file from loading.
Local mock receiver: spin an in-test `http.createServer` (or an MSW handler) that records method + headers
+ raw body; NEVER hit a real external URL. DNS-rebind: mock `dns.lookup` to return a public address on the
first call and a private one on the second (subscribe vs dispatch). No `console.*`; no watch mode.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the SSRF, HMAC, and redaction RED security suites</name>
  <files>packages/api/src/__tests__/security/webhook-ssrf.security.test.ts, packages/api/src/__tests__/security/webhook-hmac.security.test.ts, packages/api/src/__tests__/security/webhook-redact.security.test.ts</files>
  <read_first>
    - 100-RESEARCH.md § A1 (SSRF/DNS-rebind), § A2 (HMAC), § A5 (redaction), § E (requirement→test map)
    - packages/einvoice/src/asp/storecove/adapter.ts:348-367 (the HMAC hex + timingSafeEqual idiom to mirror)
    - packages/validators/src/employee-country-fields.ts + legal/compliance-{ksa,uae}.ts (PII field names to assert are stripped)
    - apps/api/src/lib/client-ip.ts (how IPs are parsed — informs the block-list test cases)
  </read_first>
  <action>
    Author three RED suites, each against symbols 100-02/03/04 will create:
    - `webhook-ssrf.security.test.ts` (INTEG-SEC-01/-02): a table of blocked hosts (`10.0.0.1`,
      `127.0.0.1`, `169.254.169.254`, `192.168.1.1`, `172.16.0.1`, `::1`, `0.0.0.0`, `[fe80::1]`,
      `[fc00::1]`, a DNS name that resolves to a private IP) each → `assertWebhookUrlSafe` throws; public
      hosts pass; `http://` throws unless `httpAllowed:true`; a DNS-rebind case (public on first lookup,
      private on second) is blocked at the dispatch gate (mock `dns.lookup`); a redirecting mock receiver
      (302 → metadata) is rejected (`redirect:'error'`). RED until 100-02.
    - `webhook-hmac.security.test.ts` (INTEG-WEBHOOK-04/-05): `signWebhookPayload(secret, t, body)` yields
      `t={t},v1={hex}`; a reference verifier recomputes + `timingSafeEqual`-passes; a wrong secret fails; a
      `t` older than 300_000ms is rejected by the verifier BEFORE compare. RED until 100-03.
    - `webhook-redact.security.test.ts` (INTEG-WEBHOOK-07): `redactPii(payload,{includePii:false})` strips
      every PII key (assert PESEL/SSN/NI/taxId/Emirates/Iqama/email/phone absent, nested + arrays);
      `includePii:true` retains them; assert the redaction is applied to the value that would be PERSISTED.
      RED until 100-04.
    No `console.*`; scoped; no watch mode.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test webhook-ssrf webhook-hmac webhook-redact 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Three RED files exist, each asserting the § E behavior (not `expect(true)`), each naming its green
      plan in a comment.
    - The SSRF file covers both gates (subscribe + dispatch/DNS-rebind + redirect + HTTPS-only).
    - No real external URL is contacted; DNS is mocked.
  </acceptance_criteria>
  <done>SSRF + HMAC + redaction RED contracts authored.</done>
</task>

<task type="auto">
  <name>Task 2: Author the dispatch/DLQ, rate-limit, subscription, OWASP-gate, and leak-alarm RED suites; flip nyquist</name>
  <files>packages/api/src/__tests__/webhook-subscription.test.ts, packages/api/src/__tests__/webhook-dispatch.test.ts, packages/api/src/__tests__/security/webhook-rate-limit.security.test.ts, packages/api/src/__tests__/security/owasp-api-gate.security.test.ts, apps/cron-worker/src/__tests__/api-key-leak-alarm.test.ts, .planning/phases/100-theme-c-outbound-webhooks-integration-security/100-VALIDATION.md</files>
  <read_first>
    - 100-RESEARCH.md § A3 (fan-out + deliver drain), § A4 (models), § A6 (rate-limit + leak alarm), § A7 (5/1h alert), § B (event catalog + retry/DLQ)
    - packages/api/src/services/outbox/index.ts (computeBackoffMs + dispatchAndFinalize — the poison-isolation + finalize idiom to mirror)
    - apps/cron-worker/src/jobs/handlers/job-health.ts:183-239 (the FAILURE_ALERT_THRESHOLD + captureMessage idiom the alert + leak-alarm tests assert)
    - apps/cron-worker/src/__tests__/job-health.test.ts (the cron-handler test harness shape)
  </read_first>
  <action>
    Author five RED suites + flip nyquist:
    - `webhook-subscription.test.ts` (INTEG-WEBHOOK-01/-02): the `WebhookEventType` union enumerates the 16
      catalog events; creating a subscription with a bad url is SSRF-rejected; a valid create persists +
      audits `WEBHOOK_SUBSCRIPTION`; `eventFilter` gates fan-out. RED until 100-05.
    - `webhook-dispatch.test.ts` (INTEG-WEBHOOK-03): a non-2xx delivery schedules the next attempt on the
      exact `[1m,5m,30m,2h,12h,24h]` schedule; attempt 7 moves the row to `webhook_failures` (DEAD) + bumps
      `lastFailureAt`; a DLQ row is replayable; a poison row (handler throws) does not stall sibling rows in
      the batch; 5 FAILED/DEAD in 1h fires the admin alert. RED until 100-06.
    - `webhook-rate-limit.security.test.ts` (INTEG-SEC-03): the 101st delivery in a minute for one
      subscription is REQUEUED (short delay), not dropped; a different sub is unaffected. RED until 100-06.
    - `owasp-api-gate.security.test.ts` (INTEG-SEC-04): explicit checks — a cross-org subscription id is
      404 (BOLA), a read-scope key cannot reach a write/manage endpoint (BFLA), a private webhook url is
      rejected (SSRF), a mass-assignment of `organizationId`/`secretEncrypted` is stripped, error responses
      leak no stack/secret (misconfig), a `${}`/`'; DROP` string in a filter is parameterized (injection).
      RED until 100-09.
    - `api-key-leak-alarm.test.ts` (cron-worker, INTEG-SEC-05): seeding the P99 `ApiKeyIpEvent` log with >3
      distinct IPs for one key in 24h fires the alarm; ≤3 does not. RED until 100-07.
    Then set `nyquist_compliant: true` in `100-VALIDATION.md`. All scoped; no `console.*`; no watch mode.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test webhook-subscription webhook-dispatch webhook-rate-limit owasp-api-gate 2>&1 | tail -20; pnpm --filter @contractor-ops/cron-worker test api-key-leak-alarm 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Five new files exist and are RED, each naming its turn-green plan in a comment.
    - Each asserts the behavior from 100-RESEARCH § E (not a placeholder).
    - `100-VALIDATION.md` `nyquist_compliant: true`.
  </acceptance_criteria>
  <done>Dispatch/DLQ, rate-limit, subscription, OWASP-gate, leak-alarm RED contracts authored; nyquist floor set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test suite → phase code | The RED net is the executable spec; a missing/weak test lets an SSRF/HMAC/redaction/DLQ gap ship silently |
| customer URL → dispatcher | Modelled here as the SSRF test surface so the guard is proven, not assumed |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01-01 | Information Disclosure | An SSRF/DNS-rebind path untested | mitigate | webhook-ssrf net RED before 100-02 greens the guard (both gates + rebind + redirect) |
| T-100-01-02 | Spoofing | Replayable/forgeable signature untested | mitigate | webhook-hmac net asserts replay window + constant-time compare before 100-03 |
| T-100-01-03 | Information Disclosure | PII persisted in a deliverable/DLQ row untested | mitigate | webhook-redact net asserts redaction at persist time before 100-04 |
| T-100-01-SC | Tampering | npm installs | accept | No package installs (tests only) |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test webhook-ssrf webhook-hmac webhook-redact webhook-subscription webhook-dispatch webhook-rate-limit owasp-api-gate` — all present, all RED (expected pre-implementation).
- `pnpm --filter @contractor-ops/cron-worker test api-key-leak-alarm` — RED.
- `100-VALIDATION.md` `nyquist_compliant: true`.
</verification>

<success_criteria>
Every Phase-100 security-critical behavior (SSRF + DNS-rebind, HMAC + replay, PII redaction, backoff/DLQ/
poison-isolation, per-sub rate-limit, key-leak alarm, OWASP gate) has a RED, scoped, non-watch test that
names the plan turning it green — the phase cannot ship a gap without a failing test first, and no test
ever contacts a real external URL.
</success_criteria>

<output>
Create `.planning/phases/100-theme-c-outbound-webhooks-integration-security/100-01-SUMMARY.md` when done.
Record the local-mock-receiver harness location so downstream delivery plans reuse it.
</output>
