---
phase: 99-theme-c-api-keys-scopes-rate-limiting
plan: 08
type: execute
wave: 5
depends_on: ["99-01", "99-02", "99-03", "99-04", "99-05", "99-06", "99-07"]
files_modified:
  - .planning/brain/wiki/domains/public-api.md
  - .planning/brain/wiki/patterns/rate-limit.md
  - .planning/brain/wiki/patterns/tenant-and-audit.md
  - .planning/brain/wiki/structure/api-routers-catalog.md
  - .planning/brain/wiki/structure/key-services.md
  - .planning/brain/wiki/log.md
  - .planning/brain/wiki/hot.md
  - .planning/MEMORY.md
  - .planning/milestones/v7.0-phases/99-theme-c-api-keys-scopes-rate-limiting/99-VALIDATION.md
  - .planning/milestones/v7.0-phases/99-theme-c-api-keys-scopes-rate-limiting/99-100-HANDOFF.md
autonomous: false
requirements: [INTEG-AUTH-01, INTEG-AUTH-02, INTEG-AUTH-03, INTEG-AUTH-04, INTEG-AUTH-05, INTEG-API-01]

must_haves:
  truths:
    - "The whole read+write+keys+scopes+rate-limit surface hangs together end-to-end: the full scoped `packages/api` public-api + security suites (write-scope BFLA, actor, rotation, tier-quota, mutation-audit, flag) + `apps/public-api` suites (strict-dto, openapi-doc, write-routes-dark, routes) + the web-vite api-keys tests are GREEN; NO write is reachable without a mandatory scope; the surface stays double-dark"
    - "The documentation-follows-code obligation is discharged in the SAME change set: `wiki/domains/public-api.md` + rate-limit/tenant-and-audit patterns + api-routers-catalog/key-services structure pages reflect the write procedures, the actor model, rotation-with-grace, the per-tier monthly quota, and the sourceIp/userAgent mutation audit; `pnpm check:wiki-brain` passes (no NEW drift) and the BM25/graph refresh is run per CLAUDE.md"
    - "`.planning/MEMORY.md` records the durable Phase-99 invariants: (1) API keys bind to a mutable `actingUserId` that is ATTRIBUTION-only (never authorization — scopes are the authority), (2) every public write carries a mandatory `requirePermission` scope (BFLA) + audits API_KEY+apiKeyId+sourceIp+userAgent, (3) rotation supersedes with a bounded grace window (grace-aware `resolveByPrefix`), (4) per-tier monthly quota (Starter 1k/Pro 10k/Ent unlimited) is post-auth + composes with the pre-auth burst limiter, (5) writes stay DOUBLE-DARK — the flag flip + un-hide is Phase 100"
    - "`99-VALIDATION.md` sign-off checklist is complete + `wave_0_complete: true`; every INTEG-AUTH requirement maps to a GREEN automated test"
    - "A `99-100-HANDOFF.md` records the Phase-100 dependency: the write flag flip + un-hide + SDK-write-inclusion happen in Phase 100 AFTER the INTEG-SEC-01 OWASP gate; the webhook-subscription tier caps (defined in `api-tier-limits.ts`) and the source-IP log (`ApiKeyIpEvent`) are consumed by Phase 100's webhook + leak-alarm work; `_initiatePayoutForRun` remains deferred"
  artifacts:
    - path: ".planning/brain/wiki/domains/public-api.md"
      provides: "Public REST API domain page updated with the write surface + keys/scopes/rate-limit + actor model + agent mistakes"
      contains: "actingUserId"
    - path: ".planning/milestones/v7.0-phases/99-theme-c-api-keys-scopes-rate-limiting/99-100-HANDOFF.md"
      provides: "The Phase-100 handoff — write flag flip sequencing, webhook-cap + source-IP-log consumption, deferred payout init"
      contains: "flag flip"
---

<objective>
Close the phase: prove the keys + scopes + rate-limit + inherited-write surface hangs together
end-to-end, discharge the mandatory documentation-follows-code obligation (wiki + indexes + MEMORY in the
same change set), run the doc-drift + brain-refresh pipeline, complete the validation sign-off, and record
the Phase-100 handoff (the write flag flip + un-hide + SDK-write-inclusion, the webhook-cap + source-IP-log
consumption, and the deferred payout init all live in 100).

Output: updated wiki/domain/pattern/structure pages + MEMORY invariants + brain refresh + the validation
sign-off + the Phase-100 handoff.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-CONTEXT.md
@.planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-RESEARCH.md

<interfaces>
Doc pipeline (CLAUDE.md § Documentation follows code): update the wiki pages listed, then
`pnpm check:wiki-brain` (must pass — no NEW drift for the changed source files), then the brain refresh:
`rm -f graphify-out/graph.json && graphify update . --no-cluster --force && cp graphify-out/graph.json
.planning/graphs/graph.json` and `cd .planning/brain && find wiki -name '*.md' -exec python3 scripts/
contextual-prefix.py {} --no-llm \; && python3 scripts/bm25-index.py build`. Bump `source_commit` on
touched wiki frontmatter. This is the LAST plan — it verifies the whole phase, so run the full scoped
public-api/api/web-vite api-keys suites. NEVER run the full unscoped web-vite suite. Do NOT flip
`module.public-api` or un-hide any route (Phase 100).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end verification of the whole surface</name>
  <files>.planning/milestones/v7.0-phases/99-theme-c-api-keys-scopes-rate-limiting/99-VALIDATION.md</files>
  <read_first>
    - 99-VALIDATION.md (the per-requirement map + sign-off checklist)
    - the six new security suites + strict-dto + write-routes-dark + openapi-doc + the api-keys web-vite test
  </read_first>
  <action>
    Run the full scoped verification: `pnpm --filter @contractor-ops/api test public-api public-api-write-scope
    api-key-actor api-key-rotation api-tier-quota public-api-mutation-audit public-api-flag api-key`; `pnpm
    --filter @contractor-ops/public-api test strict-dto openapi-doc write-routes-dark routes`; `pnpm --filter
    @contractor-ops/web-vite test api-keys`; `pnpm typecheck --filter @contractor-ops/api --filter
    @contractor-ops/validators --filter @contractor-ops/public-api --filter @contractor-ops/db --filter
    @contractor-ops/web-vite`; `node scripts/lint-audit-log.mjs`. Confirm every INTEG-AUTH requirement row in
    99-VALIDATION is GREEN; complete the sign-off checklist + set `wave_0_complete: true`. Confirm NO write is
    reachable without a scope and the surface is double-dark (writes absent from the spec; 404 when flag off).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test public-api public-api-write-scope api-key-actor api-key-rotation api-tier-quota public-api-mutation-audit public-api-flag 2>&1 | tail -20; pnpm --filter @contractor-ops/public-api test strict-dto openapi-doc write-routes-dark 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - All six new security suites + strict-dto + openapi-doc + write-routes-dark + api-keys web-vite test GREEN.
    - `pnpm typecheck` across the touched packages clean; audit-log lint passes.
    - 99-VALIDATION sign-off complete; `wave_0_complete: true`.
  </acceptance_criteria>
  <done>Whole keys/scopes/rate-limit/write surface verified end-to-end; validation signed off.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Documentation-follows-code + brain refresh + Phase-100 handoff</name>
  <files>.planning/brain/wiki/domains/public-api.md, .planning/brain/wiki/patterns/rate-limit.md, .planning/brain/wiki/patterns/tenant-and-audit.md, .planning/brain/wiki/structure/api-routers-catalog.md, .planning/brain/wiki/structure/key-services.md, .planning/brain/wiki/log.md, .planning/brain/wiki/hot.md, .planning/MEMORY.md, .planning/milestones/v7.0-phases/99-theme-c-api-keys-scopes-rate-limiting/99-100-HANDOFF.md</files>
  <read_first>
    - CLAUDE.md § Documentation follows code (the trigger matrix + the exact refresh pipeline commands)
    - .planning/brain/wiki/domains/public-api.md + wiki/patterns/{rate-limit,tenant-and-audit}.md + wiki/structure/{api-routers-catalog,key-services}.md (the pages to update, as authored by 98-12)
    - .planning/MEMORY.md (append the 5 invariants; match the existing bullet style, no operational text)
  </read_first>
  <action>
    Update the wiki: `domains/public-api.md` (the 6 write procedures, actor model, rotation-with-grace,
    per-tier quota, sourceIp/UA audit, agent-mistakes: "actingUserId is NOT authz", "writes are
    double-dark, do not flip the flag here"); `patterns/rate-limit.md` (the two-limiter model: pre-auth
    burst + post-auth monthly tier quota); `patterns/tenant-and-audit.md` (the API_KEY actor +
    actingUserId attribution + sourceIp/UA on every write); `structure/api-routers-catalog.md`
    (publicApiRouter write procedures + apiKeyRouter rotate/ipLog/usage); `structure/key-services.md`
    (api-key-service rotation grace + api-quota-counter). Append the 5 durable invariants to
    `.planning/MEMORY.md`. HUMAN-VERIFY GATE: confirm no operational/round-status text leaked into the wiki
    (durable facts only). Update `wiki/log.md`, overwrite `hot.md`, bump `source_commit` on touched pages,
    then `pnpm check:wiki-brain` + the graph + BM25 refresh (per § interfaces). Author `99-100-HANDOFF.md`:
    the write flag flip + un-hide + SDK-write-inclusion are Phase-100 acts after INTEG-SEC-01; Phase 100
    consumes `TIER_WEBHOOK_SUBSCRIPTION_CAP` + `ApiKeyIpEvent` (leak alarm); `_initiatePayoutForRun` stays
    deferred. No `console.*`.
  </action>
  <verify>
    <automated>pnpm check:wiki-brain 2>&1 | tail -8; test -f .planning/milestones/v7.0-phases/99-theme-c-api-keys-scopes-rate-limiting/99-100-HANDOFF.md && grep -qi "flag flip" .planning/milestones/v7.0-phases/99-theme-c-api-keys-scopes-rate-limiting/99-100-HANDOFF.md && echo handoff-ok</automated>
  </verify>
  <acceptance_criteria>
    - The wiki domain/pattern/structure pages reflect the write surface + actor model + rotation + per-tier
      quota + sourceIp/UA audit; `pnpm check:wiki-brain` passes; graph + BM25 refreshed; `source_commit` bumped.
    - `.planning/MEMORY.md` has the 5 durable invariants (durable facts only — no operational text).
    - `99-100-HANDOFF.md` records the write-flag-flip sequencing + webhook-cap/source-IP-log consumption +
      deferred payout init.
  </acceptance_criteria>
  <done>Docs-follows-code discharged; brain refreshed; Phase-100 handoff recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| code change → wiki | A product change without a matching wiki update makes the next agent hallucinate |
| phase 99 → phase 100 | The write-flag-flip must NOT happen in 99; the handoff must make that explicit |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-08-01 | Repudiation | Stale docs hide the new write/scope/rate-limit behavior | mitigate | Wiki + MEMORY updated in the same change set; `check:wiki-brain` gates |
| T-99-08-02 | Elevation of Privilege | A later phase flips the flag without the OWASP gate | mitigate | Handoff makes the flip a Phase-100 act after INTEG-SEC-01 |
| T-99-08-SC | Tampering | npm installs | accept | Docs only |
</threat_model>

<verification>
- `pnpm check:wiki-brain` passes; graph + BM25 refresh run per CLAUDE.md.
- The full scoped public-api/api/web-vite api-keys suites GREEN (Task 1); `pnpm typecheck` across touched packages clean.
- `99-100-HANDOFF.md` exists and names the flag flip + webhook-cap/source-IP-log consumption + deferred payout.
</verification>

<success_criteria>
The phase is closed and coherent: keys hash/rotate/revoke with audit, every write enforces a scope (BFLA),
the Developer page is complete, per-tier quotas apply, and every mutation audits apiKeyId+sourceIp+userAgent
— documented in the wiki + MEMORY in the same change set, with the write flag flip cleanly handed to Phase 100.
</success_criteria>

<output>
Create `.planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-08-SUMMARY.md` when done.
</output>