---
phase: 99-theme-c-api-keys-scopes-rate-limiting
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/api/src/__tests__/security/public-api-write-scope.security.test.ts
  - packages/api/src/__tests__/security/public-api-flag.security.test.ts
  - packages/api/src/__tests__/security/api-key-actor.security.test.ts
  - packages/api/src/__tests__/security/api-key-rotation.security.test.ts
  - packages/api/src/__tests__/security/api-tier-quota.security.test.ts
  - packages/api/src/__tests__/security/public-api-mutation-audit.security.test.ts
  - apps/public-api/src/__tests__/strict-dto.test.ts
  - .planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-VALIDATION.md
autonomous: true
requirements: [INTEG-AUTH-01, INTEG-AUTH-02, INTEG-AUTH-04, INTEG-AUTH-05, INTEG-API-01]

must_haves:
  truths:
    - "A RED validation net exists BEFORE any Phase-99 production code: the four NEW `*.security.test.ts` files (actor, rotation, tier-quota, mutation-audit) plus the repointed `strict-dto.test.ts` plus the un-skipped write-half rows in the two Phase-98 HOLD suites — every one RED against the not-yet-built surface, each naming exactly which later plan turns it green"
    - "The Phase-98 `public-api-write-scope.security.test.ts` matrix is SPLIT into DELIVERED (the 6 write entities Phase 99 builds) vs DEFERRED (compliance_document.create — see D-02) and its `describe.skip('HOLD-until-98-09')` live-403 block is un-skipped (RED until 99-04); the DEFERRED rows stay explicitly skipped with a comment, never silently dropped"
    - "`strict-dto.test.ts` money-rejection is repointed to the paymentRun create DTO (money server-derived) and the org/workerType-rejection to the contractor create DTO (per 98-09-HANDOFF), so it asserts the CORRECT mass-assignment contract"
    - "`api-key-actor.security.test.ts` asserts the D-01 contract: an FK-requiring create sets the FK to the key's actingUserId, the audit row records actorType:'API_KEY'+apiKeyId+metadata.actingUserId, and binding a key to a non-member/cross-org user is rejected"
    - "Every RED test is scoped-runnable (`--filter` + path), uses no watch mode, and does not touch the full web-vite suite; `99-VALIDATION.md` frontmatter flips `nyquist_compliant: true` once the stubs land"
  artifacts:
    - path: "packages/api/src/__tests__/security/api-key-actor.security.test.ts"
      provides: "The executable D-01 actor-model contract (FK-from-actingUserId + audit attribution + cross-org bind reject) — RED until 99-02/99-04"
      contains: "actingUserId"
    - path: "packages/api/src/__tests__/security/api-tier-quota.security.test.ts"
      provides: "The INTEG-AUTH-04 per-tier monthly quota 429 contract — RED until 99-03"
      contains: "TOO_MANY_REQUESTS"
    - path: "packages/api/src/__tests__/security/api-key-rotation.security.test.ts"
      provides: "The INTEG-AUTH-01 rotation-grace HMAC contract (usable within grace, 401 after) — RED until 99-05"
      contains: "graceExpiresAt"
---

<objective>
Author the phase's RED validation net FIRST so every later plan is turned-green against an executable
contract, not prose. Four NEW `*.security.test.ts` files (actor model, rotation grace, per-tier quota,
mutation audit) + a repointed `strict-dto.test.ts` + the un-skipped write-half rows of the two Phase-98
HOLD suites. Each RED file names the plan that greens it (99-02 actor, 99-03 quota, 99-04 writes/DTO/audit,
99-05 rotation, 99-06 dark routes). This is the phase's Nyquist floor: no scope/rotation/quota/audit code
ships without a test that was RED before it.

Output: 4 new RED security suites + 1 repointed DTO test + 2 un-skipped HOLD suites + the nyquist flip.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-CONTEXT.md
@.planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-RESEARCH.md

<interfaces>
Existing seams to mirror: `packages/api/src/__tests__/security/public-api-write-scope.security.test.ts`
(the `WRITE_SCOPE_MATRIX` + `describe.skip('HOLD-until-98-09')` block — split + un-skip), `public-api-flag
.security.test.ts` (write-half `describe.skip`), `apps/public-api/src/__tests__/strict-dto.test.ts`. Test
harness: build an apiKey ctx (`authMode:'apiKey'`, `apiKeyId`, `apiKeyScopes`, `organizationId`, region)
and invoke the public procedure directly — see `packages/api/src/routers/public-api/__tests__/*.test.ts`
for how the flag evaluator + apiKey ctx are stubbed (`vi.hoisted`/`vi.mock`). The four new files assert
CONTRACTS that later plans satisfy; where a symbol does not yet exist (e.g. `ctx.apiKeyActingUserId`, the
`rotate` procedure, `enforceApiTierQuota`), the test references it so the file is RED (compile/red), with
a `describe.skip('HOLD-until-99-0X')` guard ONLY where a hard type error would block the whole file from
loading — prefer an assertion that fails at runtime over a skip so the RED is visible.
scope strings: `PUBLIC_API_SCOPES` (`scope-utils.ts`) — the write scopes already exist; the matrix asserts
`permissionToScopes` ⊆ key scopes per row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split + un-skip the two Phase-98 HOLD suites; repoint strict-dto</name>
  <files>packages/api/src/__tests__/security/public-api-write-scope.security.test.ts, packages/api/src/__tests__/security/public-api-flag.security.test.ts, apps/public-api/src/__tests__/strict-dto.test.ts</files>
  <read_first>
    - packages/api/src/__tests__/security/public-api-write-scope.security.test.ts (the WRITE_SCOPE_MATRIX + the HOLD-until-98-09 describe.skip)
    - .planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-09-HANDOFF.md (DELIVERED vs DEFERRED split; strict-dto repoint guidance)
    - apps/public-api/src/__tests__/strict-dto.test.ts (the current org/workerType/money assertions to repoint)
  </read_first>
  <action>
    In `public-api-write-scope.security.test.ts`: split `WRITE_SCOPE_MATRIX` into DELIVERED (contractor
    create/update, invoice create/void, payment update, paymentRun create/transition/export, workflow
    create/execute, workflowTask transition — with their exact scope strings) and DEFERRED
    (complianceDocument create — keep an explicit `it.skip` with a `// D-02: compliance_document.create
    deferred — see 99-CONTEXT` comment). Un-skip the DELIVERED live-403 block (rename `HOLD-until-98-09`
    → the delivered describe) so it runs RED against the not-yet-built procedures (each row: a key WITHOUT
    the scope → FORBIDDEN, a key WITH it → not FORBIDDEN). In `public-api-flag.security.test.ts` un-skip
    the write-half 404 rows (RED until the write procedures exist). In `strict-dto.test.ts` repoint: money
    rejection → `publicApiPaymentRunCreateInputSchema` (server-derived money must be absent), org/workerType
    rejection → `publicApiContractorCreateInputSchema`. Leave them RED (DTOs not authored until 99-04). No
    `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test public-api-write-scope public-api-flag 2>&1 | tail -12; pnpm --filter @contractor-ops/public-api test strict-dto 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The write-scope matrix is DELIVERED/DEFERRED-split; DELIVERED live-403 block un-skipped (RED); the
      DEFERRED compliance row is an explicit commented skip.
    - `public-api-flag` write-half rows un-skipped (RED).
    - `strict-dto` money assertion targets paymentRun, org/workerType targets contractor (RED).
  </acceptance_criteria>
  <done>Phase-98 HOLD suites split + un-skipped (RED); strict-dto repointed (RED).</done>
</task>

<task type="auto">
  <name>Task 2: Author the four NEW RED security suites (actor, rotation, quota, mutation-audit)</name>
  <files>packages/api/src/__tests__/security/api-key-actor.security.test.ts, packages/api/src/__tests__/security/api-key-rotation.security.test.ts, packages/api/src/__tests__/security/api-tier-quota.security.test.ts, packages/api/src/__tests__/security/public-api-mutation-audit.security.test.ts, .planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-VALIDATION.md</files>
  <read_first>
    - 99-RESEARCH.md § A1 (actor), § A3 (rotation grace), § A4 (quota), § A5 (audit) + § E (the requirement→test map)
    - packages/api/src/routers/public-api/__tests__/*.test.ts (apiKey ctx + flag-evaluator stubbing pattern)
    - packages/api/src/services/audit-writer.ts (the AuditLog row shape the mutation-audit test asserts: actorType/actorId/ipAddress/userAgent/metadata)
    - packages/api/src/services/api-key-service.ts (resolveByPrefix — the grace where-clause the rotation test drives)
  </read_first>
  <action>
    Author four RED suites:
    - `api-key-actor.security.test.ts` (D-01): a paymentRun/workflow create invoked with an apiKey ctx sets
      the non-null FK (`createdByUserId`/`startedByUserId`) to the key's `actingUserId`; the emitted AuditLog
      records `actorType:'API_KEY'`, `actorId` = apiKeyId, `metadata.actingUserId`; binding/creating a key
      whose actingUserId is NOT an active member of the org is rejected. RED until 99-02/99-04.
    - `api-key-rotation.security.test.ts` (INTEG-AUTH-01): after `rotate`, the OLD key resolves within the
      grace window and 401s after `graceExpiresAt`; a revoked key 401s immediately; a superseded key cannot
      be rotated again. Use an injectable/mocked clock. RED until 99-05.
    - `api-tier-quota.security.test.ts` (INTEG-AUTH-04): with a STARTER/PRO tier stub and the monthly counter
      at limit, the next request throws `TOO_MANY_REQUESTS`; an ENTERPRISE tier never throws (unlimited).
      Stub `getSubscription` + the Upstash counter. RED until 99-03.
    - `public-api-mutation-audit.security.test.ts` (INTEG-AUTH-05): for a representative write per entity,
      exactly one AuditLog row is written carrying `apiKeyId`, non-null `ipAddress` (sourceIp), and
      `userAgent`. RED until 99-04.
    Then set `nyquist_compliant: true` in `99-VALIDATION.md` frontmatter. All scoped; no `console.*`; no
    watch mode.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test api-key-actor api-key-rotation api-tier-quota public-api-mutation-audit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Four new `*.security.test.ts` files exist and are RED (fail against the not-yet-built surface), each
      naming its turn-green plan in a comment.
    - Each asserts the behavior from 99-RESEARCH § E (not a placeholder `expect(true)`).
    - `99-VALIDATION.md` `nyquist_compliant: true`.
  </acceptance_criteria>
  <done>Four RED security contracts authored; nyquist floor set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test suite → phase code | The RED net is the executable spec; a missing/weak test lets a scope/quota/audit gap ship silently |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01-01 | Repudiation | A write without an audit contract | mitigate | `public-api-mutation-audit` asserts apiKeyId+sourceIp+userAgent on every write before 99-04 |
| T-99-01-02 | Elevation of Privilege | A write reachable without a scope (BFLA) untested | mitigate | Un-skipped live-403 matrix RED before 99-04 greens it |
| T-99-01-SC | Tampering | npm installs | accept | No package installs (tests only) |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test public-api-write-scope public-api-flag api-key-actor api-key-rotation api-tier-quota public-api-mutation-audit` — all present, all RED (expected pre-implementation).
- `pnpm --filter @contractor-ops/public-api test strict-dto` — RED (DTOs not yet authored).
- `99-VALIDATION.md` `nyquist_compliant: true`.
</verification>

<success_criteria>
Every Phase-99 secure behavior (actor FK, rotation grace, per-tier quota, mutation audit, BFLA 403 matrix,
mass-assignment) has a RED, scoped, non-watch test that names the plan turning it green — the phase cannot
ship a gap without a failing test first.
</success_criteria>

<output>
Create `.planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-01-SUMMARY.md` when done. Record which
rows are DEFERRED (compliance_document.create) so downstream plans keep them skipped.
</output>
