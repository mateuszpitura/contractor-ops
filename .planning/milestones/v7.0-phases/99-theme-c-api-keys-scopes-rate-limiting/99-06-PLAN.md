---
phase: 99-theme-c-api-keys-scopes-rate-limiting
plan: 06
type: execute
wave: 3
depends_on: ["99-04"]
files_modified:
  - apps/public-api/src/routes/contractors.ts
  - apps/public-api/src/routes/invoices.ts
  - apps/public-api/src/routes/payments.ts
  - apps/public-api/src/routes/payment-runs.ts
  - apps/public-api/src/routes/workflows.ts
  - apps/public-api/src/routes/workflow-tasks.ts
  - apps/public-api/src/__tests__/openapi-doc.test.ts
  - apps/public-api/src/__tests__/write-routes-dark.test.ts
  - packages/api/src/__tests__/security/public-api-flag.security.test.ts
autonomous: true
requirements: [INTEG-API-01, INTEG-API-02]

must_haves:
  truths:
    - "Every 99-04 write procedure gets a Hono `createRoute` (POST/PATCH) that delegates to the public tRPC caller, and each write `createRoute` carries `hide: true` so it is ABSENT from the derived OpenAPI 3.1 document (and therefore from the Scalar portal + Speakeasy SDK)"
    - "Writes are reachable ONLY through the flag-gated + scope-checked + quota-limited procedure (per-org dark until Phase 100); `hide:true` is the SECOND darkness layer (spec/SDK invisibility) — the two compose (double-dark). Phase 99 does NOT flip `module.public-api` and does NOT un-hide"
    - "The `openapi-doc.test.ts` writes-absent assertion holds: the 3.1 document contains all read paths and ZERO write paths/operations"
    - "`write-routes-dark.test.ts` asserts the double-darkness: a write route 404s when `module.public-api` is off AND the write path is absent from `buildOpenApiDocument(app)`; `public-api-flag.security.test.ts` write-half rows (un-skipped in 99-01) turn GREEN"
    - "compliance_documents has NO write route (D-02 deferred); classifications + audit_log have no write route (read-only)"
  artifacts:
    - path: "apps/public-api/src/routes/payment-runs.ts"
      provides: "hidden (hide:true) POST/PATCH write routes delegating to the dark payment_run write procedures"
      contains: "hide: true"
    - path: "apps/public-api/src/__tests__/write-routes-dark.test.ts"
      provides: "Double-dark contract — writes 404 when flag off AND absent from the derived spec/SDK"
      contains: "getOpenAPI31Document"
  key_links:
    - from: "apps/public-api/src/routes/payment-runs.ts"
      to: "packages/api/src/routers/public-api/payment-run.ts"
      via: "createRoute POST/PATCH → createPublicCaller(c).paymentRun.<verb> — hide:true keeps it out of the spec"
      pattern: "hide: true"
---

<objective>
Expose the 99-04 write procedures over HTTP as Hono `createRoute` POST/PATCH endpoints with `hide: true`
so they never enter the derived OpenAPI 3.1 document, the Scalar portal, or the Speakeasy SDK (absorbs the
deferred 98-10). This is the second darkness layer that composes with the inherited runtime per-org flag
gate + scope check + tier quota: a write endpoint is both 404-until-flag-granted AND invisible-to-consumers
until Phase 100. Phase 99 does NOT flip the flag or un-hide — it only builds the transport.

Output: hidden write routes for the 6 write entities + the writes-absent + double-dark tests GREEN.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-CONTEXT.md
@.planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-RESEARCH.md
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-10-PLAN.md

<interfaces>
`createRoute({ method:'post'|'patch', path:'/...', hide: true, request:{ body:{ content:{'application/
json':{ schema:<.strict() write DTO from 99-04> }}}}, responses:{ 200, 400, 401, 403, 404, 429 } })` +
`app.openapi(route, handler)` where the handler `const body = c.req.valid('json')` →
`createPublicCaller(c).<entity>.<verb>(body)`. `hide:true` excludes the route from `getOpenAPI31Document`.
Route→procedure map (mirrors 99-04): contractors POST(create)/PATCH(update); invoices POST(create)/
PATCH(void); payments PATCH(update); payment-runs POST(create)/PATCH(transition)/POST(export); workflows
POST(create)/POST(execute); workflow-tasks PATCH(transition). NO write route for compliance-documents (D-02),
classifications, or audit-log. The write DTOs are the SAME `.strict()` schemas 99-04 uses (defense-in-depth:
validate at the Hono boundary AND the tRPC input). `buildOpenApiDocument(app)` (`apps/public-api/src/lib/
build-openapi-doc.ts`) is the writes-hidden doc to assert against.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hidden write createRoutes to the 6 write-entity route modules</name>
  <files>apps/public-api/src/routes/contractors.ts, apps/public-api/src/routes/invoices.ts, apps/public-api/src/routes/payments.ts, apps/public-api/src/routes/payment-runs.ts, apps/public-api/src/routes/workflows.ts, apps/public-api/src/routes/workflow-tasks.ts</files>
  <read_first>
    - apps/public-api/src/routes/contractors.ts (the existing read createRoute module to extend with hidden writes)
    - 98-10-PLAN.md § interfaces (createRoute request.body + hide:true pattern) + 98-RESEARCH § Anti-Patterns (un-hiding writes forbidden pre-flip)
    - packages/api/src/routers/public-api/payment-run.ts (the 99-04 write procedures + their `.strict()` input DTOs to reuse)
  </read_first>
  <action>
    For each of the 6 write-entity route modules add the POST/PATCH `createRoute` definitions per the map,
    each with `hide: true`, a `request.body` bound to the SAME `.strict()` write DTO the tRPC procedure uses,
    and a handler that validates (`c.req.valid('json')`) and delegates to `createPublicCaller(c).<entity>.
    <verb>(...)`. Add `429` to the documented responses (tier quota). Do NOT add write routes to
    compliance-documents / classifications / audit-log. Keep the read routes unchanged. The runtime flag
    gate + scope check + quota live in the tRPC layer (inherited) — the Hono route is thin transport. No
    `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/public-api test routes 2>&1 | tail -10; grep -rl "hide: true" apps/public-api/src/routes | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The 6 write-entity route modules each have their POST/PATCH write routes with `hide: true`;
      compliance-documents/classifications/audit-log have NO write route.
    - Write handlers validate the `.strict()` body and delegate to the public caller (no business logic in Hono).
    - Route tests (reads still green) pass; write handlers wired to the 99-04 procedures.
  </acceptance_criteria>
  <done>Hidden write routes added for the 6 write entities.</done>
</task>

<task type="auto">
  <name>Task 2: writes-absent + double-dark + flag write-half GREEN</name>
  <files>apps/public-api/src/__tests__/openapi-doc.test.ts, apps/public-api/src/__tests__/write-routes-dark.test.ts, packages/api/src/__tests__/security/public-api-flag.security.test.ts</files>
  <read_first>
    - apps/public-api/src/lib/build-openapi-doc.ts (buildOpenApiDocument — the derived doc to assert against)
    - the existing openapi-doc.test.ts writes-absent assertion + apps/public-api/src/__tests__/rate-limit.security.test.ts (the Hono fetch-harness for the 404-when-flag-off assertion)
    - the 99-01 un-skipped public-api-flag write-half rows to green
  </read_first>
  <action>
    Confirm `openapi-doc.test.ts` asserts `buildOpenApiDocument(app)` contains every read path and ZERO
    write paths/operations (no POST/PATCH under any public path — writes `hide:true`). Author/finalize
    `write-routes-dark.test.ts` asserting the double-darkness: (a) a POST/PATCH to a write route with
    `module.public-api` OFF returns 404 (flag gate), and (b) the write path is absent from
    `buildOpenApiDocument(app)`. Turn GREEN the un-skipped write-half rows of `public-api-flag.security
    .test.ts` (a write procedure 404s when the flag is off). Keep all scoped. No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/public-api test openapi-doc write-routes-dark 2>&1 | tail -12; pnpm --filter @contractor-ops/api test public-api-flag 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `openapi-doc.test.ts` GREEN: reads present, writes absent, `openapi` 3.1.
    - `write-routes-dark.test.ts` GREEN: write 404 when flag off AND write path absent from the derived doc.
    - `public-api-flag.security.test.ts` write-half rows GREEN.
  </acceptance_criteria>
  <done>Writes-absent + double-dark + flag write-half contracts GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| consumer ← published spec/SDK | Writes must not appear in the doc/SDK/portal before the Phase-100 flip |
| consumer → write route | Reachable only when the flag is granted (Phase 100) + correct scope + within quota |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-06-01 | Information Disclosure | Write surface leaks into spec/SDK/portal pre-flip | mitigate | `hide:true` on every write createRoute; openapi-doc asserts zero write paths |
| T-99-06-02 | Elevation of Privilege | Write reachable before the flag is granted | mitigate | Inherited runtime flag gate 404s writes when off; write-routes-dark asserts it |
| T-99-06-03 | Tampering | Business logic drift in the Hono write handler | mitigate | Handler is thin transport → tRPC caller; same `.strict()` DTO (defense-in-depth) |
| T-99-06-SC | Tampering | npm installs | accept | No new deps |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/public-api test openapi-doc write-routes-dark routes` green.
- `pnpm --filter @contractor-ops/api test public-api-flag` green; `pnpm typecheck --filter @contractor-ops/public-api` clean.
- Grep: every write route carries `hide: true`; compliance-documents/classifications/audit-log have no write route.
</verification>

<success_criteria>
INTEG-API-01 (transport) + INTEG-API-02 (spec integrity): the 6 write entities are reachable over HTTP but
double-dark — 404 until the Phase-100 flag flip AND absent from the derived 3.1 spec / Scalar portal / SDK.
</success_criteria>

<output>
Create `.planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-06-SUMMARY.md` when done. Note that the
un-hide + flag flip is a Phase-100 act (INTEG-SEC-01 gate).
</output>
