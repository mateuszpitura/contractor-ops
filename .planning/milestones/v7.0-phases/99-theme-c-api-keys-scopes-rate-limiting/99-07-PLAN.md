---
phase: 99-theme-c-api-keys-scopes-rate-limiting
plan: 07
type: execute
wave: 4
depends_on: ["99-02", "99-03", "99-05"]
files_modified:
  - packages/api/src/routers/core/api-key.ts
  - apps/web-vite/src/components/settings/api-keys-tab.tsx
  - apps/web-vite/src/components/settings/api-keys/data-table.tsx
  - apps/web-vite/src/components/settings/api-keys/key-detail-drawer.tsx
  - apps/web-vite/src/components/settings/rotate-api-key-dialog.tsx
  - apps/web-vite/src/components/settings/hooks/use-api-keys-tab.ts
  - apps/web-vite/src/i18n/messages/en/settings.json
  - apps/web-vite/src/components/settings/__tests__/api-keys-tab.test.tsx
autonomous: true
requirements: [INTEG-AUTH-03]

must_haves:
  truths:
    - "The Settings → Developer (API Keys) surface gains, on top of the existing create/edit/revoke CRUD: last-used-at, a per-key source-IP log, a scope visualization (the granular scopes grouped into readable resource groups), the acting-user binding (view + rebind to an active member), and a rotation flow with a grace-period selector — all rendered through the container+hooks layers (the hook is the ONLY tRPC boundary), never calling useTRPC/useQuery from the page/component"
    - "Read-only backend queries feed the UI: `apiKeyRouter.ipLog(id)` (recent ApiKeyIpEvent rows, org-scoped) and `apiKeyRouter.usage()` (current-month request count + the tier quota from api-tier-limits) — both under `apiKeyAdminProcedure` (organization:update + ENTERPRISE); the rotate mutation (99-05) drives the rotation dialog and shows the new plaintext exactly once"
    - "Every section has loading + empty + error states; the surface is keyboard-accessible + WCAG-contrast + RTL-safe (ar) and localized via i18next in en/de/pl/ar (no hardcoded strings — `pnpm --filter @contractor-ops/web-vite check:i18n` / lint-hardcoded-strings clean)"
    - "The rotation dialog explains the grace window (old key keeps working for N hours), reveals the new key once with a copy affordance + an 'I saved it' confirmation, and never re-displays an old plaintext; the scope visualization shows least-privilege at a glance (read vs write per resource)"
    - "A component test (`api-keys-tab.test.tsx`) asserts the new surfaces render (rows with last-used + source-IP log + scope groups + rotate action) and their loading/empty/error states"
  artifacts:
    - path: "apps/web-vite/src/components/settings/rotate-api-key-dialog.tsx"
      provides: "Rotation flow — grace-period selector, one-time new-key reveal, grace explanation"
      contains: "graceHours"
    - path: "apps/web-vite/src/components/settings/hooks/use-api-keys-tab.ts"
      provides: "The single tRPC boundary — list/create/update/revoke/rotate + ipLog + usage queries feeding the Developer surface"
      contains: "rotate"
  key_links:
    - from: "apps/web-vite/src/components/settings/hooks/use-api-keys-tab.ts"
      to: "packages/api/src/routers/core/api-key.ts"
      via: "the hook consumes list/ipLog/usage/rotate; page + components stay presentational"
      pattern: "api-key"
---

<objective>
Complete the Settings → Developer surface (INTEG-AUTH-03): extend the existing API-keys CRUD with
last-used-at, a per-key source-IP log, a scope visualization, the acting-user binding (view + rebind), and
a rotation flow with a grace-period selector. Add the two read-only backend queries the UI needs (`ipLog`,
`usage`). Everything flows through web-vite's container+hooks architecture (the hook is the only tRPC
boundary), with mandatory loading/empty/error states, WCAG accessibility, RTL, and full i18n (en/de/pl/ar).

Output: the enriched Developer surface + rotation dialog + source-IP/scope/usage views + the backend
ipLog/usage queries + a component test.

**UI protocol (binding — see CLAUDE.md § UI skills):** invoke the `frontend-design` skill, then `impeccable`
for this product-dashboard surface, then `semble search` for the existing api-keys components BEFORE editing.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-CONTEXT.md
@.planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-RESEARCH.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
Existing surface: `apps/web-vite/src/components/settings/api-keys-tab.tsx` (Enterprise-gated via
`FeatureGate`) + `api-keys/data-table.tsx` + `create-api-key-dialog.tsx`/`edit-api-key-dialog.tsx`/
`revoke-api-key-dialog.tsx` + `hooks/use-api-keys-tab.ts` (the tRPC boundary). Backend: `apiKeyRouter`
(`packages/api/src/routers/core/api-key.ts`) `list` now returns `lastUsedAt` + `actingUserId`/`actingUser`
(99-02); `rotate` (99-05) returns the new plaintext once. ADD read-only queries: `ipLog(input:{id})` →
recent `ApiKeyIpEvent` rows (org-scoped, capped, ordered by seenAt desc) and `usage()` → `{ month, count,
quota }` (count from the monthly counter or an aggregate; quota from `TIER_MONTHLY_REQUEST_QUOTA[tier]` via
`getSubscription`) — both under `apiKeyAdminProcedure`. Layer rules (`apps/web-vite/ARCHITECTURE.md`): page
= thin composer (Suspense/permissions, no tRPC); container = section states; hook (`use-*.ts`) = the ONLY
useTRPC/useQuery/useMutation boundary; component = props-in/JSX-out. i18n: add keys to `en/settings.json`
(+ de/pl/ar parity — run the i18n parity/hardcoded-strings checks). Dialog convention: `DialogBody` scroll
+ sticky `DialogFooter` (see MEMORY web_vite_dialog_pattern). Entrance animation: `AnimateIn` (MEMORY).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend read-only queries — apiKeyRouter.ipLog + apiKeyRouter.usage</name>
  <files>packages/api/src/routers/core/api-key.ts</files>
  <read_first>
    - packages/api/src/routers/core/api-key.ts (apiKeyAdminProcedure + list select shape; actingUserId added by 99-02; rotate added by 99-05)
    - packages/api/src/services/api-quota-counter.ts (the current-month count source) + packages/api/src/lib/api-tier-limits.ts (the tier quota) + services/billing-service.ts getSubscription
    - packages/db/prisma/schema/api-key.prisma (ApiKeyIpEvent — org-scoped, indexed by [apiKeyId, seenAt])
  </read_first>
  <action>
    Add `ipLog: apiKeyAdminProcedure.input(entityIdSchema).query(...)` returning the recent `ApiKeyIpEvent`
    rows for the key (org-scoped `where:{apiKeyId: input.id, organizationId: ctx.organizationId}`, ordered
    seenAt desc, capped e.g. 50) with `{ipAddress, userAgent, seenAt}`. Add `usage: apiKeyAdminProcedure
    .query(...)` returning `{ month: 'YYYY-MM', count, quota }` where `count` = the current-month request
    count and `quota = TIER_MONTHLY_REQUEST_QUOTA[getSubscription(org).tier]` (Infinity → null/'unlimited').
    Both read-only, tenant-scoped, no audit needed. No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test api-key 2>&1 | tail -8; pnpm typecheck --filter @contractor-ops/api 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `apiKeyRouter.ipLog` + `apiKeyRouter.usage` exist, org-scoped, read-only, under `apiKeyAdminProcedure`.
    - `pnpm typecheck --filter @contractor-ops/api` clean.
  </acceptance_criteria>
  <done>Read-only ipLog + usage queries added for the Developer surface.</done>
</task>

<task type="auto">
  <name>Task 2: Developer surface — source-IP log, scope viz, acting-user, rotation flow, usage/quota</name>
  <files>apps/web-vite/src/components/settings/api-keys-tab.tsx, apps/web-vite/src/components/settings/api-keys/data-table.tsx, apps/web-vite/src/components/settings/api-keys/key-detail-drawer.tsx, apps/web-vite/src/components/settings/rotate-api-key-dialog.tsx, apps/web-vite/src/components/settings/hooks/use-api-keys-tab.ts, apps/web-vite/src/i18n/messages/en/settings.json, apps/web-vite/src/components/settings/__tests__/api-keys-tab.test.tsx</files>
  <read_first>
    - apps/web-vite/ARCHITECTURE.md (page/container/hook/component layering + the data-layer check)
    - apps/web-vite/src/components/settings/api-keys-tab.tsx + api-keys/data-table.tsx + hooks/use-api-keys-tab.ts (the surface to extend) + create/edit/revoke dialogs (the dialog + one-time-plaintext pattern to mirror for rotate)
    - a recent settings section with loading/empty/error + i18n as a reference (e.g. members.tsx / audit-log-tab.tsx)
    - the frontend-design + impeccable skills (binding for this surface)
  </read_first>
  <action>
    Extend the hook `use-api-keys-tab.ts` to expose `rotate` + `ipLog` + `usage` (the ONLY tRPC boundary).
    In `data-table.tsx` show `lastUsedAt` + a `Rotate` action + a compact scope-group summary (read/write per
    resource). Add `key-detail-drawer.tsx` (a drawer/expandable row) showing: the full scope visualization
    grouped by resource (read vs write), the acting-user binding (with a rebind control that lists active
    members — reuse an existing member-picker), the source-IP log (`ipLog`), and the monthly usage vs quota
    (`usage`) with a progress affordance. Add `rotate-api-key-dialog.tsx`: a grace-period selector (default
    24h, max per 99-05), an explanation that the old key keeps working for the grace window, a one-time
    reveal of the new plaintext with copy + an "I saved it" confirmation. Every section: loading + empty +
    error states; keyboard + focus + WCAG contrast; RTL-safe. Localize all strings in `en/settings.json` and
    mirror de/pl/ar (parity check). Add `api-keys-tab.test.tsx` asserting the new surfaces + their
    loading/empty/error render. Run `pnpm --filter @contractor-ops/web-vite check:web-vite-data-layer`. No
    `console.*`; no direct tRPC outside the hook.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite test api-keys 2>&1 | tail -10; pnpm --filter @contractor-ops/web-vite check:web-vite-data-layer 2>&1 | tail -4; pnpm --filter @contractor-ops/web-vite check:web-vite-dialog-pattern 2>&1 | tail -3 || true</automated>
  </verify>
  <acceptance_criteria>
    - The Developer surface shows last-used-at, source-IP log, scope visualization, acting-user binding
      (view + rebind), rotation-with-grace, and monthly usage vs quota — each with loading/empty/error.
    - The hook is the ONLY tRPC boundary (`check:web-vite-data-layer` clean); dialogs follow the
      DialogBody/DialogFooter convention; i18n parity (en/de/pl/ar) + no hardcoded strings.
    - `api-keys-tab.test.tsx` GREEN (new surfaces + their states render); WCAG keyboard/focus/contrast + RTL.
  </acceptance_criteria>
  <done>Developer page enriched with source-IP log, scope viz, acting-user, rotation flow, usage/quota.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Developer UI → key management | Only ENTERPRISE org admins reach the surface (apiKeyAdminProcedure + FeatureGate) |
| new plaintext reveal → clipboard | The rotated key is shown once; the UI must not persist or re-show it |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-07-01 | Information Disclosure | ipLog/usage leak across orgs | mitigate | Both queries org-scoped under `apiKeyAdminProcedure` (organization:update + ENTERPRISE) |
| T-99-07-02 | Information Disclosure | New plaintext re-shown / logged | mitigate | One-time reveal + "I saved it" confirm; never re-fetched; no console.* |
| T-99-07-03 | Elevation of Privilege | Rebind acting-user to a non-member via the UI | mitigate | The rebind mutation re-validates active membership server-side (99-02); UI only lists active members |
| T-99-07-SC | Tampering | npm installs | accept | No new deps (reuse packages/ui) |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/web-vite test api-keys` green; `check:web-vite-data-layer` + `check:web-vite-dialog-pattern` clean.
- `pnpm --filter @contractor-ops/api test api-key` green (ipLog/usage); `pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/web-vite` clean.
- i18n parity + hardcoded-strings checks clean for the new keys.
</verification>

<success_criteria>
INTEG-AUTH-03: the Settings → Developer page provides key CRUD, last-used-at, a source-IP log, scope
visualization, the acting-user binding, and a rotation flow with a grace period — accessible, localized
(en/de/pl/ar), RTL-safe, with loading/empty/error states, and the hook as the single tRPC boundary.
</success_criteria>

<output>
Create `.planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-07-SUMMARY.md` when done.
</output>