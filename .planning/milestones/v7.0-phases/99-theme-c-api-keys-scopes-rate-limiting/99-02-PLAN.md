---
phase: 99-theme-c-api-keys-scopes-rate-limiting
plan: 02
type: execute
wave: 1
depends_on: ["99-01"]
files_modified:
  - packages/db/prisma/schema/api-key.prisma
  - packages/api/src/routers/core/api-key.ts
  - packages/api/src/middleware/api-key-auth.ts
  - packages/api/src/services/api-key-service.ts
  - apps/public-api/src/lib/create-caller.ts
  - packages/api/src/errors.ts
  - packages/api/src/__tests__/security/api-key-actor.security.test.ts
autonomous: true
requirements: [INTEG-AUTH-01, INTEG-AUTH-05]

must_haves:
  truths:
    - "`OrganizationApiKey` gains a mutable non-null `actingUserId` (FK → User, named relation distinct from `createdBy`), plus the rotation columns (`supersededAt`, `supersededByKeyId`, `graceExpiresAt`) and a bounded per-key source-IP log (`ApiKeyIpEvent` child model), in ONE reversible migration; existing rows backfill `actingUserId = createdByUserId`"
    - "`actingUserId` is an ATTRIBUTION FK, never an authorization source — the key's authority stays its `scopes[]` enforced by `requirePermission`; nothing reads actingUserId to decide access. It defaults to `createdByUserId` at creation and is rebindable via `apiKeyRouter.update`, ALWAYS validated to be an ACTIVE member of the key's org (cross-org / removed / inactive → rejected)"
    - "`apiKeyAuthMiddleware` enriches `ctx.apiKeyActingUserId` (from the resolved key) so downstream FK-requiring creates have a real user id; the empty-string FK bug (`core/api-key.ts` `ctx.user?.id ?? ''`) is removed — creation requires a real member id"
    - "The public tRPC ctx carries `ctx.sourceIp` + `ctx.userAgent` captured at the Hono boundary (`x-forwarded-for` left-most / `x-real-ip`, `user-agent`) via `createPublicCaller`, so every later write can pass them to `writeAuditLog`; a per-key source-IP event is appended (capped) on each authenticated request"
    - "The `api-key-actor.security.test.ts` rows that assert (a) ctx exposes actingUserId and (b) binding a key to a non-member/cross-org user is rejected turn GREEN; the FK-on-create rows stay RED until 99-04"
  artifacts:
    - path: "packages/db/prisma/schema/api-key.prisma"
      provides: "OrganizationApiKey.actingUserId (named relation) + rotation columns + ApiKeyIpEvent child model — the actor + rotation + source-IP-log schema"
      contains: "actingUserId"
    - path: "packages/api/src/middleware/api-key-auth.ts"
      provides: "ctx enrichment: apiKeyActingUserId + (sourceIp/userAgent threaded from create-caller) so writes have a real actor + audit fields"
      contains: "apiKeyActingUserId"
  key_links:
    - from: "packages/api/src/routers/core/api-key.ts"
      to: "packages/db/prisma/schema/api-key.prisma"
      via: "create/update set + rebind actingUserId, guarded by an active-org-membership check"
      pattern: "actingUserId"
    - from: "apps/public-api/src/lib/create-caller.ts"
      to: "packages/api/src/services/audit-writer.ts"
      via: "sourceIp/userAgent captured at the Hono boundary → ctx → writeAuditLog ipAddress/userAgent"
      pattern: "x-forwarded-for"
---

<objective>
Land the API-key → user-identity actor model (D-01) — the money-affecting decision Phase 98 refused to
hack — plus the schema + ctx foundation the whole write half and the Developer page depend on. Add a
mutable `actingUserId` (attribution FK, NOT authorization) that defaults to the creator, is rebindable to
any active org member (guarded), and is surfaced as `ctx.apiKeyActingUserId`. In the same reversible
migration add the rotation columns (consumed by 99-05) and a bounded per-key source-IP log (consumed by
99-07). Thread `sourceIp` + `userAgent` from the Hono boundary into the public tRPC ctx so every later
write audits with `apiKeyId` + `sourceIp` + `userAgent` (INTEG-AUTH-05). Remove the empty-string
`createdByUserId` FK bug so the actor invariant holds from key birth.

Output: one reversible migration (actor + rotation + IP-log) + actingUserId create/rebind with a
membership guard + actingUserId/sourceIp/userAgent ctx + the actor test's bind-reject rows GREEN.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-CONTEXT.md
@.planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-RESEARCH.md

<interfaces>
Schema (`packages/db/prisma/schema/api-key.prisma`): `OrganizationApiKey` currently has `createdByUserId
String` + `createdBy User @relation(...)`. Add `actingUserId String` + `actingUser User @relation("ApiKey
ActingUser", fields:[actingUserId], references:[id])` (the second User relation MUST be named to avoid an
ambiguous-relation error; also add the back-reference on `User`). Add `supersededAt DateTime?`,
`supersededByKeyId String?` (self-relation optional), `graceExpiresAt DateTime?`. Add a child model
`ApiKeyIpEvent { id, apiKeyId, organizationId, ipAddress, userAgent?, seenAt @default(now()),
@@index([apiKeyId, seenAt]) }`. Migration discipline (CLAUDE.md): reversible — add `actingUserId`
NULLABLE, `UPDATE "OrganizationApiKey" SET "actingUserId" = "createdByUserId"` backfill, then `ALTER ...
SET NOT NULL` in the same migration. Run `pnpm --filter @contractor-ops/db exec prisma migrate dev`
(regional multi-DB — follow the repo's migration convention; do NOT hand-edit generated SQL beyond the
backfill statement).

Auth: `api-key-auth.ts:66-79` builds the enriched ctx (`apiKeyId`, `apiKeyScopes`). Add
`apiKeyActingUserId: keyRecord.actingUserId`. `resolveApiKey`/`resolveByPrefix` (`api-key-service.ts:107/
116`) must `select`/return `actingUserId`. Membership check: the org-membership relation Better Auth uses
(`packages/auth` — a `Member`/organization-membership record with `status`/`role`); reuse the existing
membership query used elsewhere (semble search "organization member active") rather than inventing one.
sourceIp/UA: `apps/public-api/src/lib/create-caller.ts:11` builds the tRPC ctx from the Hono `Context` —
add `sourceIp` (first hop of `c.req.header('x-forwarded-for')`, fallback `x-real-ip`) + `userAgent`
(`c.req.header('user-agent')`) to that ctx; extend the public ctx type. Append an `ApiKeyIpEvent` (capped:
skip if the same ip was logged for the key within a short debounce, and prune to last N) in the
authenticated path — reuse the `touchLastUsed` fire-and-forget style (`api-key-service.ts:152`).
Errors: add `INVALID_ACTING_USER` to `errors.ts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration (actingUserId + rotation cols + ApiKeyIpEvent) + actingUserId create/rebind guard</name>
  <files>packages/db/prisma/schema/api-key.prisma, packages/api/src/routers/core/api-key.ts, packages/api/src/errors.ts</files>
  <read_first>
    - packages/db/prisma/schema/api-key.prisma (the model to extend) + one recent reversible migration under packages/db/prisma/migrations/ (the backfill-then-NOT-NULL pattern)
    - packages/api/src/routers/core/api-key.ts (createInput/updateInput + create/update mutations + the createdByUserId: ctx.user?.id ?? '' bug at :68)
    - packages/auth membership: semble search "active organization member" — the query that confirms a userId belongs to an org (status ACTIVE) to guard the bind
  </read_first>
  <action>
    Extend `OrganizationApiKey`: add non-null `actingUserId` + named relation `actingUser` (+ User
    back-reference), `supersededAt`/`supersededByKeyId`/`graceExpiresAt` (nullable), and the
    `ApiKeyIpEvent` child model. Author the reversible migration (nullable → backfill `= createdByUserId`
    → NOT NULL). In `apiKeyRouter.create`: require a real creating-user id (remove `?? ''`); set
    `actingUserId` = an optional `input.actingUserId` (defaulting to `createdByUserId`), REJECTED with
    `INVALID_ACTING_USER` (BAD_REQUEST) if it is not an ACTIVE member of `ctx.organizationId`. In
    `apiKeyRouter.update`: allow rebinding `actingUserId` with the SAME membership guard; audit the rebind
    (`API_KEY_UPDATE` oldValues/newValues include actingUserId). Add `actingUserId` (+ actingUser name) to
    the `list` select for the Developer page. Add `INVALID_ACTING_USER` to `errors.ts`. No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db exec prisma validate 2>&1 | tail -3; pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/db 2>&1 | tail -6; grep -n "actingUserId" packages/db/prisma/schema/api-key.prisma packages/api/src/routers/core/api-key.ts</automated>
  </verify>
  <acceptance_criteria>
    - `api-key.prisma` has non-null `actingUserId` (named relation) + rotation columns + `ApiKeyIpEvent`;
      the migration is reversible (nullable→backfill→NOT NULL) and `prisma validate` passes.
    - create defaults actingUserId to the creator and rejects a non-active-member bind; update rebinds under
      the same guard, audited; no empty-string FK remains.
    - `pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/db` clean.
  </acceptance_criteria>
  <done>Actor + rotation + source-IP-log schema landed; actingUserId create/rebind guarded + audited.</done>
</task>

<task type="auto">
  <name>Task 2: Surface actingUserId + sourceIp + userAgent into ctx; append source-IP events; green the bind/ctx rows</name>
  <files>packages/api/src/middleware/api-key-auth.ts, packages/api/src/services/api-key-service.ts, apps/public-api/src/lib/create-caller.ts, packages/api/src/__tests__/security/api-key-actor.security.test.ts</files>
  <read_first>
    - packages/api/src/middleware/api-key-auth.ts:27-80 (the resolved-key ctx enrichment to extend)
    - packages/api/src/services/api-key-service.ts:107-138 (resolveApiKey/resolveByPrefix — add actingUserId to the select/return) + :152 (touchLastUsed fire-and-forget style to mirror for the ip-log append)
    - apps/public-api/src/lib/create-caller.ts (how the Hono Context builds the tRPC ctx — add sourceIp/userAgent)
    - the 99-01 api-key-actor.security.test.ts (the ctx-surface + bind-reject rows to green)
  </read_first>
  <action>
    Make `resolveByPrefix` include `actingUserId` in its `select`/return. In `apiKeyAuthMiddleware` add
    `apiKeyActingUserId: keyRecord.actingUserId` to the enriched ctx (+ the ctx type). In
    `create-caller.ts` capture `sourceIp` (parse `x-forwarded-for` left-most hop; fallback `x-real-ip`) +
    `userAgent` from the Hono `Context` and put them on the public tRPC ctx (+ type). Add an
    `appendApiKeyIpEvent(keyId, orgId, ip, ua)` helper (fire-and-forget, debounced + pruned to last N per
    key, mirroring `touchLastUsed`) and call it in the authenticated path. Turn GREEN the
    `api-key-actor.security.test.ts` rows asserting ctx exposes `apiKeyActingUserId` and that a non-member
    bind is rejected (the FK-on-create rows remain RED until 99-04). No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test api-key-actor 2>&1 | tail -12; pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/public-api 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `ctx.apiKeyActingUserId`, `ctx.sourceIp`, `ctx.userAgent` are available on the public tRPC ctx.
    - Each authenticated request appends a bounded `ApiKeyIpEvent` (fire-and-forget; debounced + pruned).
    - `api-key-actor.security.test.ts` ctx-surface + bind-reject rows GREEN; FK-on-create rows still RED.
  </acceptance_criteria>
  <done>Actor + audit-field ctx wired; source-IP events captured; actor bind contract GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| bind request → actingUserId FK | An org admin could try to bind a key to a user outside the org (IDOR / privilege ghost) |
| Hono header → audit sourceIp | `x-forwarded-for` is client-influenced; only the proxy-appended left hop is trusted |
| API key → mutation actor | The key must resolve to a real, active member for FK-requiring creates |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-02-01 | Elevation of Privilege | actingUserId bound to a cross-org/removed user | mitigate | Active-org-membership guard on create + rebind; reject with INVALID_ACTING_USER |
| T-99-02-02 | Elevation of Privilege | actingUserId mistaken for an authz source | mitigate | Scopes remain the only authority (requirePermission); actingUserId is attribution-only, documented + never read for access |
| T-99-02-03 | Repudiation | Missing sourceIp/userAgent on audits | mitigate | Captured at the Hono boundary into ctx; passed to writeAuditLog by every write (99-04) |
| T-99-02-04 | Tampering | Non-reversible / data-losing migration on a money-adjacent table | mitigate | Nullable→backfill→NOT NULL in one reversible migration; prisma validate + typecheck gate |
| T-99-02-SC | Tampering | npm installs | accept | No new deps |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test api-key-actor` — ctx-surface + bind-reject rows GREEN.
- `pnpm --filter @contractor-ops/db exec prisma validate` clean; migration reversible (backfill present).
- `pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/db --filter @contractor-ops/public-api` clean.
- Grep: no `?? ''` createdByUserId fallback remains in `core/api-key.ts`.
</verification>

<success_criteria>
The actor model is real and safe: every key resolves to a mutable, membership-guarded `actingUserId`
(attribution only) surfaced as `ctx.apiKeyActingUserId`; the public ctx carries `sourceIp` + `userAgent`;
the schema for rotation + the source-IP log exists. The four FK-requiring creates (99-04/99-05) now have a
non-null user to attribute to, without a synthetic-user refactor.
</success_criteria>

<output>
Create `.planning/phases/99-theme-c-api-keys-scopes-rate-limiting/99-02-SUMMARY.md` when done. Record the
migration name + that actingUserId is attribution-only (not authz) for downstream plans.
</output>
