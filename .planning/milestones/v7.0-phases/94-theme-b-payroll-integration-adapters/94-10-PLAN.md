---
phase: 94-theme-b-payroll-integration-adapters
plan: 10
type: execute
wave: 5
depends_on: [94-09]
files_modified:
  - .planning/brain/wiki/structure/packages.md
  - .planning/brain/wiki/structure/key-services.md
  - .planning/brain/wiki/structure/api-routers-catalog.md
  - .planning/brain/wiki/patterns/feature-flags.md
  - .planning/brain/wiki/domains/payroll-export.md
  - .planning/brain/wiki/log.md
  - .planning/brain/wiki/hot.md
  - .planning/EXTERNAL-ENABLEMENT.md
  - .planning/MEMORY.md
autonomous: true
requirements: [PAYROLL-PL-01, PAYROLL-PL-02, PAYROLL-PL-03, PAYROLL-DE-01, PAYROLL-DE-02, PAYROLL-UK-01, PAYROLL-US-01]

must_haves:
  truths:
    - "The new packages/payroll package, feed-builder, and payrollExport router are documented in the wiki (packages/key-services/api-routers-catalog)"
    - "A new domains/payroll-export.md wiki page captures purpose, flow, entry points, UI surface, and agent mistakes"
    - "EXTERNAL-ENABLEMENT.md has register rows for each live-API / partner gate (Gusto, QuickBooks, ADP-native, DATEVconnect, RTI XSD bundle)"
    - "MEMORY.md records the phase invariants; the graphify graph + BM25 index are rebuilt"
  artifacts:
    - path: ".planning/brain/wiki/domains/payroll-export.md"
      provides: "domain page for payroll export adapters"
      contains: "PayrollExportProfile"
    - path: ".planning/EXTERNAL-ENABLEMENT.md"
      provides: "register rows for the 5 payroll external/partner gates"
      contains: "payroll.gusto"
    - path: ".planning/MEMORY.md"
      provides: "phase-94 invariants (payroll package, feed source, flag-defer)"
      contains: "packages/payroll"
  key_links:
    - from: "domains/payroll-export.md"
      to: "packages/payroll + payroll-feed + payroll-export-router"
      via: "verify_with paths point at the real profile/registry/engine/feed/router files"
      pattern: "verify_with"
---

<objective>
Close the documentation-follows-code loop for Phase 94 (mandatory, never deferred): document the new `packages/payroll` package, feed-builder, and export router in the wiki; author a new `domains/payroll-export.md`; add EXTERNAL-ENABLEMENT register rows for every live-API/partner gate; record the invariants in MEMORY; and rebuild the graph + BM25 index. This is a required part of the same change set as the code.

Purpose: keep the wiki a living compass so the next agent doesn't hallucinate the payroll surface.
Output: updated wiki structure/pattern/domain pages + indexes, the external-enablement register, MEMORY invariants, and a refreshed graph/BM25.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/EXTERNAL-ENABLEMENT.md
@.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-RESEARCH.md
@.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-CONTEXT.md

<interfaces>
- `.planning/brain/wiki/structure/packages.md` — add the `@contractor-ops/payroll` entry (purpose, profiles, verify_with).
- `.planning/brain/wiki/structure/key-services.md` — add `payroll-feed.ts` + `register-payroll-profiles.ts`.
- `.planning/brain/wiki/structure/api-routers-catalog.md` — add the `payrollExport` namespace (dark behind module.workforce-employees).
- `.planning/brain/wiki/patterns/feature-flags.md` — add the 8+1 `payroll.*` flags (incl. new `payroll.sage-de`) + the flag-defer posture.
- `.planning/brain/wiki/domains/{employee-registry,worker-foundation}.md` — the sibling domain pages to model the new payroll-export page on.
- `.planning/EXTERNAL-ENABLEMENT.md` — the register table to append rows to.
- `CLAUDE.md` § "Documentation follows code" — the refresh pipeline (contextual-prefix + bm25-index + check:wiki-brain).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wiki structure + pattern pages + new domain page</name>
  <files>.planning/brain/wiki/structure/packages.md, .planning/brain/wiki/structure/key-services.md, .planning/brain/wiki/structure/api-routers-catalog.md, .planning/brain/wiki/patterns/feature-flags.md, .planning/brain/wiki/domains/payroll-export.md</files>
  <read_first>
    - `.planning/brain/wiki/structure/packages.md` (einvoice entry, to mirror for payroll)
    - `.planning/brain/wiki/domains/employee-registry.md` (domain-page shape + frontmatter + verify_with)
    - the actual files shipped in 94-01..94-09 (cite real paths in verify_with)
  </read_first>
  <action>
    Add `@contractor-ops/payroll` to `packages.md` (purpose: per-market payroll export profiles cloning the einvoice registry; list the profiles; verify_with the registry/engine/profile paths). Add `payroll-feed.ts` + `register-payroll-profiles.ts` to `key-services.md`. Add the `payrollExport` namespace to `api-routers-catalog.md` (note: dark behind `module.workforce-employees`; listTargets/export/connectNative). Add the `payroll.*` flags (including the new `payroll.sage-de`) + flag-defer posture to `patterns/feature-flags.md`. Author `domains/payroll-export.md` (frontmatter + Purpose, Flow, Entry points, UI surface, per-target/flag table, "Agent mistakes" — e.g. terminatedAt is on PersonnelFile not EmployeeProfile; don't overload payment-export; profiles are pure over PayrollFeed; native paths are flag-deferred). Bump `source_commit` on any touched page frontmatter.
  </action>
  <verify>
    <automated>test -f .planning/brain/wiki/domains/payroll-export.md && grep -q "PayrollExportProfile" .planning/brain/wiki/domains/payroll-export.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `domains/payroll-export.md` exists with Purpose/Flow/Entry points/UI surface/Agent mistakes + `verify_with` real paths.
    - packages/key-services/api-routers-catalog/feature-flags pages updated.
  </acceptance_criteria>
  <done>Wiki tracks the payroll surface.</done>
</task>

<task type="auto">
  <name>Task 2: EXTERNAL-ENABLEMENT register rows + MEMORY invariants</name>
  <files>.planning/EXTERNAL-ENABLEMENT.md, .planning/MEMORY.md</files>
  <read_first>
    - `.planning/EXTERNAL-ENABLEMENT.md` (existing row format — # / dependency / flag / default / dark-until / tests / enablement steps)
    - `.planning/MEMORY.md` (invariant bullet format)
  </read_first>
  <action>
    Append register rows to `EXTERNAL-ENABLEMENT.md`:
      (a) **Gusto OAuth app + client secret** — flag `payroll.gusto` — OFF — dark: live push; **CSV export is the shipping path** — tests: `gusto-adapter` live case `skipIf(!GUSTO_CLIENT_ID)` — enablement: create Gusto partner app → set `GUSTO_CLIENT_ID/SECRET` → flip `payroll.gusto` APPROVED.
      (b) **QuickBooks (Intuit) OAuth app + client secret** — flag `payroll.quickbooks` — OFF — same shape (CSV shipping path; `skipIf(!QUICKBOOKS_CLIENT_ID)`).
      (c) **ADP Marketplace partner approval + mTLS** — flag `payroll.adp` — OFF — dark: ADP **native** push (v7.1); **ADP CSV export ships now** — tests: none live this phase — enablement: ADP Marketplace approval + mTLS cert → v7.1 native seam.
      (d) **DATEVconnect REST subscription** — flag `payroll.datev` (native seam) — OFF — dark: live REST push; **DATEV ASCII file-export ships** — enablement: org DATEVconnect subscription + endpoint config.
      (e) **HMRC RTI year XSD bundle** — validation seam — not-bundled — dark: strict RTI XSD validation; **RTI export ships (non-throwing validate)** — enablement: download the year's HMRC XSD into the RTI schema-bundle dir.
    Append MEMORY bullets: `packages/payroll` = payroll export profile-registry (clone of einvoice, NOT the payment-export factory); the feed source = Worker+EmployeeProfile+PersonnelFile (terminatedAt/hireDate on PersonnelFile); Gusto/QuickBooks native flag-deferred with CSV fallback, ADP native v7.1.
  </action>
  <verify>
    <automated>grep -q "payroll.gusto" .planning/EXTERNAL-ENABLEMENT.md && grep -q "packages/payroll" .planning/MEMORY.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Five register rows present (Gusto, QuickBooks, ADP-native, DATEVconnect, RTI XSD), each with a buildable-now fallback noted.
    - MEMORY records the payroll-package + feed-source + flag-defer invariants.
  </acceptance_criteria>
  <done>External deps registered; invariants recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh pipeline (log/hot + BM25 + graph + check)</name>
  <files>.planning/brain/wiki/log.md, .planning/brain/wiki/hot.md</files>
  <read_first>
    - `CLAUDE.md` § "Documentation follows code" (the exact refresh commands)
  </read_first>
  <action>
    Append the change to `wiki/log.md`, overwrite `wiki/hot.md`, rebuild the BM25 index and contextual prefixes (`cd .planning/brain && find wiki -name '*.md' -exec python3 scripts/contextual-prefix.py {} --no-llm \; && python3 scripts/bm25-index.py build`), and rebuild the graphify graph (`rm -f graphify-out/graph.json && graphify update . --no-cluster --force && cp graphify-out/graph.json .planning/graphs/graph.json`). Then run `pnpm check:wiki-brain` and resolve any drift it reports for the files this phase touched.
  </action>
  <verify>
    <automated>pnpm check:wiki-brain 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check:wiki-brain` green for the phase's touched surfaces (no NEW drift).
    - `wiki/log.md` records the phase; `wiki/hot.md` overwritten; BM25 + graph rebuilt.
  </acceptance_criteria>
  <done>Refresh pipeline run; wiki-brain check green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| docs → code | Wiki claims must match shipped code; `verify_with` paths must be real (check:wiki-brain enforces). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-10-01 | Tampering | stale wiki misleads next agent | mitigate | `check:wiki-brain` gates NEW drift; verify_with points at real files. |
| T-94-10-02 | Repudiation | operational text leaks into wiki | mitigate | Wiki records durable code/domain facts only — no "phase in progress"/round-status text (MEMORY: wiki_no_operational_text). |
| T-94-10-SC | Tampering | npm installs | accept | Docs-only; no installs. |
</threat_model>

<verification>
- `pnpm check:wiki-brain` green (no NEW drift for the phase's surfaces).
- `domains/payroll-export.md` exists; EXTERNAL-ENABLEMENT + MEMORY updated; graph.json + BM25 rebuilt.
</verification>

<success_criteria>
The wiki tracks the payroll surface (package, feed-builder, router, flags, domain page), every live-API/partner gate has an EXTERNAL-ENABLEMENT row with a buildable-now fallback, MEMORY records the invariants, and the graph/BM25/wiki-brain check are refreshed — all in the same change set as the code.
</success_criteria>

<output>
Create `.planning/phases/94-theme-b-payroll-integration-adapters/94-10-SUMMARY.md` when done.
</output>
