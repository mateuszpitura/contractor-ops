---
phase: 94-theme-b-payroll-integration-adapters
plan: 06
type: execute
wave: 3
depends_on: [94-01, 94-02]
files_modified:
  - packages/payroll/src/profiles/us-shared/mapper.ts
  - packages/payroll/src/profiles/adp/index.ts
  - packages/payroll/src/profiles/adp/generator.ts
  - packages/payroll/src/profiles/gusto-csv/index.ts
  - packages/payroll/src/profiles/gusto-csv/generator.ts
  - packages/payroll/src/profiles/quickbooks-csv/index.ts
  - packages/payroll/src/profiles/quickbooks-csv/generator.ts
  - packages/payroll/src/index.ts
autonomous: true
requirements: [PAYROLL-US-01]

must_haves:
  truths:
    - "ADP CSV export matches its golden fixture (ADP native is deferred to v7.1 — CSV is the v7.0 ADP path)"
    - "Gusto and QuickBooks CSV fallback exports match their golden fixtures"
    - "A shared US feed→row mapper is the single source for the three US CSV column contracts"
    - "ADP registers behind payroll.adp; gusto-csv/quickbooks-csv are the fallbacks the native adapters delegate to when dark"
  artifacts:
    - path: "packages/payroll/src/profiles/us-shared/mapper.ts"
      provides: "mapUsEmployeeToRow — shared US feed→row projection (filingStatus/stateWithholding + ssnLast4)"
      contains: "filingStatus"
    - path: "packages/payroll/src/profiles/adp/index.ts"
      provides: "AdpProfile registered with flagKey payroll.adp (CSV export; native seam deferred v7.1)"
      contains: "payroll.adp"
    - path: "packages/payroll/src/profiles/gusto-csv/index.ts"
      provides: "GustoCsvProfile — the CSV fallback the native gusto profile uses when dark"
      contains: "payroll.gusto"
  key_links:
    - from: "adp/generator.ts"
      to: "us-shared/mapper.ts"
      via: "ADP CSV rows are built from mapUsEmployeeToRow projected onto ADP's column contract"
      pattern: "mapUsEmployeeToRow"
---

<objective>
Build the US CSV file-export floor — ADP CSV (the v7.0 ADP path; native ADP deferred to v7.1), plus Gusto CSV and QuickBooks CSV fallbacks — turning the US CSV golden-fixture RED tests (94-02) GREEN. A shared US mapper is the single source for the three column contracts. The native Gusto/QuickBooks OAuth adapters (Wave 4) delegate to these CSV profiles when their flag is dark.

Purpose: deliver the US file-export half of PAYROLL-US-01 with zero external dependency; establish the fallback the native tails reuse.
Output: registered `AdpProfile`/`GustoCsvProfile`/`QuickbooksCsvProfile` + a shared `us-shared/mapper.ts`.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-RESEARCH.md
@.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-VALIDATION.md

<interfaces>
- `packages/payroll/src/__tests__/{adp,gusto-csv,quickbooks-csv}.test.ts` + fixtures — the RED proofs (from 94-02).
- `packages/api/src/services/payment-export.ts:154-194` — CSV+BOM idiom.
- `packages/validators/src/employee-country-fields.ts:129-141` — US countryFields (filingStatus/stateWithholding/stateOther).
- `packages/feature-flags/src/flags-core.ts` — `payroll.adp`/`payroll.gusto`/`payroll.quickbooks` (already defined).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared US mapper</name>
  <files>packages/payroll/src/profiles/us-shared/mapper.ts</files>
  <read_first>
    - `packages/validators/src/employee-country-fields.ts:129-141` (US fields)
    - `94-RESEARCH.md` § Pattern 2 (US required matrix)
  </read_first>
  <action>
    Create `mapUsEmployeeToRow(employee: PayrollFeedEmployee)` returning a normalized US row object (name, hireDate, terminatedAt, filingStatus, stateWithholding, stateOther, ssnLast4). The full SSN is NOT in this row — where an incumbent import legally requires it, the feed-builder reveals it upstream (audited). Each target CSV generator projects this normalized row onto its own column contract.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/payroll typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `mapUsEmployeeToRow` exists and carries `ssnLast4` (not full SSN).
  </acceptance_criteria>
  <done>Shared US projection in place.</done>
</task>

<task type="auto">
  <name>Task 2: ADP + Gusto-CSV + QuickBooks-CSV generators</name>
  <files>packages/payroll/src/profiles/adp/{index,generator}.ts, packages/payroll/src/profiles/gusto-csv/{index,generator}.ts, packages/payroll/src/profiles/quickbooks-csv/{index,generator}.ts</files>
  <read_first>
    - `packages/payroll/src/__tests__/{adp,gusto-csv,quickbooks-csv}.test.ts` + goldens
    - `payment-export.ts:154-194` (CSV+BOM)
  </read_first>
  <action>
    Create three CSV generators (`generateAdpCsv`/`generateGustoCsv`/`generateQuickbooksCsv`), each using the exceljs+BOM idiom, each projecting `mapUsEmployeeToRow` onto the target's pinned column contract (ADP "PayForce"/"Workforce Now" import columns; Gusto employee-import columns; QuickBooks Payroll import columns) with an adviser-verify note pinning each layout version. Create the three `index.ts` files: `AdpProfile` (`profileId:'adp'`, `flagKey:'payroll.adp'`, comment: native ADP deferred v7.1), `GustoCsvProfile` (`profileId:'gusto-csv'`, `flagKey:'payroll.gusto'`), `QuickbooksCsvProfile` (`profileId:'quickbooks-csv'`, `flagKey:'payroll.quickbooks'`), each `country:'US'`, plus register fns. Note in the gusto-csv/quickbooks-csv comments that these are the fallbacks the native adapters (Wave 4) delegate to when the flag is dark.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/payroll test adp gusto-csv quickbooks-csv 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All three tests GREEN against goldens; each CSV starts with the UTF-8 BOM.
    - `AdpProfile.flagKey === 'payroll.adp'`; gusto-csv/quickbooks-csv carry `payroll.gusto`/`payroll.quickbooks`.
  </acceptance_criteria>
  <done>US CSV floor GREEN (PAYROLL-US-01 file-export half).</done>
</task>

<task type="auto">
  <name>Task 3: Export the US profiles from the package surface</name>
  <files>packages/payroll/src/index.ts</files>
  <read_first>
    - `packages/einvoice/src/index.ts` (export + register-convenience pattern)
  </read_first>
  <action>
    Add `AdpProfile`/`GustoCsvProfile`/`QuickbooksCsvProfile` + register fns + `mapUsEmployeeToRow` to `packages/payroll/src/index.ts`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/payroll test adp gusto-csv quickbooks-csv 2>&1 | tail -10 && pnpm -F @contractor-ops/payroll typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All three US CSV profiles + the mapper importable from `@contractor-ops/payroll`; typecheck green.
  </acceptance_criteria>
  <done>US CSV profiles exported from the package surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| PayrollFeed → US CSV | SSN masked to last-4 in the shared mapper; full SSN never a mapper field. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-06-01 | Information Disclosure | full SSN in CSV | mitigate | `mapUsEmployeeToRow` carries `ssnLast4`; full SSN only via the feed-builder's audited reveal when a format requires it. |
| T-94-06-02 | Tampering | ADP native attempted this phase | mitigate | ADP native is deferred v7.1; `AdpProfile` emits CSV only, native seam stays dark behind `payroll.adp`. |
| T-94-06-SC | Tampering | npm installs | accept | Reuses in-tree exceljs; no installs. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/payroll test adp gusto-csv quickbooks-csv` GREEN.
- `pnpm -F @contractor-ops/payroll typecheck` + `lint` green.
</verification>

<success_criteria>
The US CSV file-export floor is GREEN: ADP CSV (v7.0 ADP path), Gusto CSV, QuickBooks CSV — proven against goldens, sharing one US mapper, and set up as the fallback for the native tails.
</success_criteria>

<output>
Create `.planning/phases/94-theme-b-payroll-integration-adapters/94-06-SUMMARY.md` when done.
</output>
