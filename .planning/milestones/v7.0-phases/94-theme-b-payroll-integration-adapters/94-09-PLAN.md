---
phase: 94-theme-b-payroll-integration-adapters
plan: 09
type: execute
wave: 5
depends_on: [94-03, 94-04, 94-05, 94-06, 94-07, 94-08]
files_modified:
  - packages/api/src/services/payroll-feed.ts
  - packages/api/src/services/register-payroll-profiles.ts
  - packages/api/src/routers/workforce/payroll-export-router.ts
  - packages/api/src/root.ts
  - apps/web-vite/src/components/payroll/hooks/use-payroll-export.ts
  - apps/web-vite/src/components/payroll/payroll-export-container.tsx
  - apps/web-vite/src/components/payroll/payroll-export-panel.tsx
  - apps/web-vite/src/pages/payroll-export-page.tsx
  - apps/web-vite/src/messages/en.json
  - apps/web-vite/src/messages/de.json
  - apps/web-vite/src/messages/pl.json
  - apps/web-vite/src/messages/ar.json
autonomous: true
requirements: [PAYROLL-PL-01, PAYROLL-PL-02, PAYROLL-PL-03, PAYROLL-DE-01, PAYROLL-DE-02, PAYROLL-UK-01, PAYROLL-US-01]

must_haves:
  truths:
    - "buildPayrollFeed joins Worker+EmployeeProfile+PersonnelFile, masks PII to last-4, and is org-scoped"
    - "A gated tRPC payrollExport router lists targets, generates an export, and downloads it — with audit + flag gate"
    - "The payroll surface is absent from appRouter when module.workforce-employees is OFF (conditional spread)"
    - "A thin web-vite export surface (Page→Container→Hook→Component) has loading/empty/error states + i18n parity"
  artifacts:
    - path: "packages/api/src/services/payroll-feed.ts"
      provides: "buildPayrollFeed(db, orgId, employeeIds) → PayrollFeed (PII-masked)"
      contains: "Last4"
    - path: "packages/api/src/routers/workforce/payroll-export-router.ts"
      provides: "gated payrollExport router (listTargets/export/connectNative) with writeAuditLog"
      contains: "assertWorkforceEnabled"
    - path: "packages/api/src/services/register-payroll-profiles.ts"
      provides: "registerAllPayrollProfiles() — registers every file + bridge profile on boot"
      contains: "registerAllPayrollProfiles"
  key_links:
    - from: "payroll-export-router.ts"
      to: "PayrollExportEngine + buildPayrollFeed + evaluate(flagKey)"
      via: "export() builds the feed, gates on the target flag, engine.generate(targetId, feed), writeAuditLog, returns a signed download"
      pattern: "engine.generate"
---

<objective>
Wire the payroll package into the app: a PII-masking feed-builder, a flag-gated tRPC export router mounted dark behind `module.workforce-employees`, the register-all-profiles hook, and a thin web-vite export surface. This turns the feed-builder / export-procedure / cross-org RED tests (94-02) GREEN and delivers the end-to-end flow for all 7 requirements.

Purpose: make the export usable — HR selects a target + employees and downloads the export file (or triggers a native push), org-scoped and audited.
Output: `buildPayrollFeed`, `registerAllPayrollProfiles`, the `payrollExport` router, and the export UI with loading/empty/error + i18n parity.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-RESEARCH.md
@.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-VALIDATION.md

<interfaces>
- `packages/api/src/services/__tests__/payroll-feed.test.ts` + `routers/__tests__/{payroll-export,payroll-cross-org}.test.ts` — the RED proofs (from 94-02).
- `packages/db/prisma/schema/{worker,employee,personnel}.prisma` — the join the feed-builder performs (hireDate/terminatedAt on PersonnelFile).
- `packages/api/src/middleware/require-workforce-flag.ts:25-42` — assertWorkforceEnabled.
- `packages/api/src/root.ts:185-196` — `conditionalWorkforceRouters` (mount payrollExport here).
- `packages/api/src/lib/integration-procedure.ts:16-38` — tRPC procedure factory (permission/tier).
- `packages/api/src/services/audit-writer.ts` — writeAuditLog.
- `packages/api/src/routers/finance/payment-shared.ts:233-263` — the download-buffer plumbing to reuse for a file result.
- `packages/feature-flags/src/index.ts` — `evaluate` for per-target `payroll.*` gating.
- `apps/web-vite/ARCHITECTURE.md` — Page→Container→Hook→Component layering; `check:web-vite-*` guards.
- `packages/api/src/services/employee-pii-*.ts` (P90 reveal path) — the audited `employeePii:read` reveal for a full national ID when a format legally requires it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: buildPayrollFeed (PII-masked, org-scoped)</name>
  <files>packages/api/src/services/payroll-feed.ts</files>
  <read_first>
    - `packages/api/src/services/__tests__/payroll-feed.test.ts` (the RED proof)
    - `packages/db/prisma/schema/{worker,employee,personnel}.prisma`
    - the P90 `employeePii:read` reveal service
  </read_first>
  <action>
    Create `buildPayrollFeed(db, organizationId, employeeIds)` — query `Worker` (workerType EMPLOYEE) joined to `EmployeeProfile` (countryCode, countryFields, etat, employmentStatus, `*Last4`) and `PersonnelFile` (hireDate, terminatedAt), filtered by `organizationId` (session) and `employeeIds`. Build a `PayrollFeed` with `nationalIdLast4` from the appropriate `*Last4` and `countryFields` passed through. Where a specific target legally requires the full national ID (DATEV SV-Nr, RTI NINO), expose an opt-in `revealPii` path that goes through the audited `employeePii:read` reveal — never a bulk decrypt, never logged. Validate the output with `payrollFeedSchema`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test payroll-feed 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `payroll-feed.test.ts` GREEN: join is correct, PII masked to last-4, reveal only via the audited path.
    - Feed is org-scoped by session `organizationId`.
  </acceptance_criteria>
  <done>Feed-builder GREEN (the source for every profile).</done>
</task>

<task type="auto">
  <name>Task 2: registerAllPayrollProfiles + gated payrollExport router + dark mount</name>
  <files>packages/api/src/services/register-payroll-profiles.ts, packages/api/src/routers/workforce/payroll-export-router.ts, packages/api/src/root.ts</files>
  <read_first>
    - `packages/api/src/routers/__tests__/{payroll-export,payroll-cross-org}.test.ts` (RED proofs)
    - `root.ts:185-196` (conditionalWorkforceRouters), `require-workforce-flag.ts:25-42`, `payment-shared.ts:233-263` (download)
    - `packages/payroll/src/index.ts` (the register-convenience fns + engine)
  </read_first>
  <action>
    Create `register-payroll-profiles.ts` — `registerAllPayrollProfiles()` calling every `register*Profile()` (file + bridge), injecting the bridge callbacks (`resolveConnection`/`evaluateFlag`/`httpPost`) for gusto/quickbooks; call it once at API boot (mirror the einvoice profile-registration boot hook). Create `payroll-export-router.ts` with `listTargets` (engine.listTargets, filtered to flags the org may see), `export` (Zod `.strict()` input `{ targetId, employeeIds, format? }`; `assertWorkforceEnabled`; `evaluate(profile.flagKey)` → FORBIDDEN if dark; `buildPayrollFeed`; `engine.generate`; `writeAuditLog(PAYROLL_EXPORT, { targetId, employeeCount })`; return a signed download via the payment-shared download plumbing), and `connectNative` (OAuth connect handoff for gusto/quickbooks). Mount `payrollExport` inside `conditionalWorkforceRouters` in `root.ts`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test payroll-export payroll-cross-org 2>&1 | tail -20 && pnpm typecheck --filter=@contractor-ops/api 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `payroll-export.test.ts` + `payroll-cross-org.test.ts` GREEN: flag gate (workforce + per-target), audit written, org-scoped, `.strict()` input.
    - `payrollExport` absent from `appRouter` when `module.workforce-employees` OFF (METHOD_NOT_FOUND).
    - `lint:audit-log` green (export mutation audited).
  </acceptance_criteria>
  <done>Export router GREEN, mounted dark, all profiles registered.</done>
</task>

<task type="auto">
  <name>Task 3: web-vite export surface (Page→Container→Hook→Component) + i18n parity</name>
  <files>apps/web-vite/src/components/payroll/hooks/use-payroll-export.ts, apps/web-vite/src/components/payroll/payroll-export-container.tsx, apps/web-vite/src/components/payroll/payroll-export-panel.tsx, apps/web-vite/src/pages/payroll-export-page.tsx, apps/web-vite/src/messages/{en,de,pl,ar}.json</files>
  <read_first>
    - `apps/web-vite/ARCHITECTURE.md` (layering + guards)
    - an existing workforce container/hook pair (e.g. employee-time or leave) for style
    - `frontend-design` skill (read SKILL.md) + `semble search` before editing UI
  </read_first>
  <action>
    Create the hook `use-payroll-export.ts` (sole tRPC boundary — `payrollExport.listTargets` query + `payrollExport.export` mutation with download handling). Create `payroll-export-container.tsx` (calls the hook; renders section loading/empty/error). Create `payroll-export-panel.tsx` (presentational: target picker, employee multi-select, download button, dark-flag empty state). Create the thin `payroll-export-page.tsx` composer (Suspense + permission gate; no tRPC). Add every user-facing string as a key across `en/de/pl/ar` (+ en-US if the surface is US-facing) with parity. Follow the frontend-design + impeccable UI workflow (this is product dashboard UI — NOT design-taste-frontend).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite test payroll 2>&1 | tail -15 && pnpm check:web-vite-data-layer && pnpm i18n:parity 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Page has no tRPC; Container has no direct tRPC; the hook is the sole boundary (`check:web-vite-data-layer`/`check:web-vite-page-shells` green).
    - Loading/empty/error states present; dark-flag targets render an empty/disabled state, not a crash.
    - `i18n:parity` green across en/de/pl/ar.
  </acceptance_criteria>
  <done>Export surface shipped with mandatory states + i18n parity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → export procedure | `organizationId`/region from session; `employeeIds` filtered by org; `.strict()` input rejects injected tenant fields. |
| export → download | Buffer streamed via signed download (payment-shared plumbing); no PII in URL/logs. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-09-01 | Information Disclosure | cross-org employee export (IDOR) | mitigate | `withTenantScope` + `employeeIds` filtered by session org + the two-org regression test. |
| T-94-09-02 | Elevation | export with a dark/PENDING target flag | mitigate | Per-request `assertWorkforceEnabled` + per-target `evaluate(flagKey)` FORBIDDEN. |
| T-94-09-03 | Repudiation | untraceable export of PII | mitigate | `writeAuditLog(PAYROLL_EXPORT, { targetId, employeeCount, actor })` on every export. |
| T-94-09-SC | Tampering | npm installs | accept | No installs. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test payroll-feed payroll-export payroll-cross-org` GREEN.
- `pnpm typecheck --filter=@contractor-ops/api` + `pnpm check:web-vite-data-layer` + `pnpm i18n:parity` + `pnpm lint:audit-log` green.
- `pnpm --filter @contractor-ops/web-vite test payroll` GREEN (scoped — never unscoped).
</verification>

<success_criteria>
End-to-end payroll export works: HR selects a target + employees, the feed is built org-scoped + PII-masked, the engine generates the export, it downloads (or native-pushes when enabled), and every export is audited and flag-gated. All 7 requirements are reachable through the surface; the whole surface is dark behind `module.workforce-employees`.
</success_criteria>

<output>
Create `.planning/phases/94-theme-b-payroll-integration-adapters/94-09-SUMMARY.md` when done.
</output>
