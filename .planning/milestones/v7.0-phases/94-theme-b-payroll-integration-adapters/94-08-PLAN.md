---
phase: 94-theme-b-payroll-integration-adapters
plan: 08
type: execute
wave: 4
depends_on: [94-01, 94-02, 94-06]
files_modified:
  - packages/integrations/src/adapters/quickbooks-adapter.ts
  - packages/integrations/src/adapters/register-all.ts
  - packages/integrations/src/env.ts
  - packages/payroll/src/profiles/quickbooks/index.ts
  - packages/payroll/src/profiles/quickbooks/bridge.ts
  - packages/payroll/src/index.ts
  - .env.example
autonomous: true
requirements: [PAYROLL-US-01]

must_haves:
  truths:
    - "A QuickBooksAdapter (IntegrationProviderAdapter) implements OAuth 2.0 connect on the integrations framework"
    - "A bridge quickbooks payroll profile pushes via the adapter when payroll.quickbooks is APPROVED + connected, else falls back to quickbooks-csv"
    - "Live token-exchange/push tests conditionally-skip when QUICKBOOKS_CLIENT_ID is absent"
    - "QUICKBOOKS_CLIENT_ID/QUICKBOOKS_CLIENT_SECRET are in .env.example + the integrations env schema; no raw process.env"
  artifacts:
    - path: "packages/integrations/src/adapters/quickbooks-adapter.ts"
      provides: "QuickBooksAdapter with getOAuthConfig/exchangeCodeForTokens/refreshToken/getHealthStatus"
      contains: "getOAuthConfig"
    - path: "packages/payroll/src/profiles/quickbooks/bridge.ts"
      provides: "flag-gated bridge: native push when enabled+connected, else quickbooks-csv fallback"
      contains: "payroll.quickbooks"
  key_links:
    - from: "quickbooks/bridge.ts"
      to: "QuickBooksAdapter + QuickbooksCsvProfile"
      via: "when payroll.quickbooks APPROVED and an IntegrationConnection exists → POST mapped payload; else delegate to QuickbooksCsvProfile.generate"
      pattern: "fallback"
---

<objective>
Add the QuickBooks Payroll native OAuth adapter on the `packages/integrations` framework and a bridge payroll profile that pushes to QuickBooks' REST API when `payroll.quickbooks` is APPROVED and the org has connected — otherwise falling back to the `quickbooks-csv` file export (94-06). Turn the QuickBooks native RED tests (94-02) GREEN, with the live path conditionally-skipped until partner OAuth credentials land.

Purpose: deliver the QuickBooks native half of PAYROLL-US-01, buildable dark now, live when creds arrive.
Output: `QuickBooksAdapter`, the bridge `quickbooks` profile, env wiring, and the register-all hook. Mirrors 94-07 (Gusto).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/EXTERNAL-ENABLEMENT.md
@.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-RESEARCH.md
@.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-VALIDATION.md

<interfaces>
- `packages/integrations/src/adapters/__tests__/quickbooks-adapter.test.ts` — the RED proof (from 94-02).
- `packages/integrations/src/types/provider.ts:17-77` — IntegrationProviderAdapter + OAuthConfig.
- `packages/integrations/src/adapters/clockify-adapter.ts` — pure-fetch OAuth adapter to mirror.
- `packages/integrations/src/adapters/register-all.ts:69-140` — HEAVY/lazy registration.
- `packages/payroll/src/profiles/quickbooks-csv/index.ts` — the CSV fallback (from 94-06).
- Plan `94-07` — the Gusto twin; copy its shape.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: QuickBooksAdapter (OAuth 2.0 on the integrations framework)</name>
  <files>packages/integrations/src/adapters/quickbooks-adapter.ts, packages/integrations/src/adapters/register-all.ts, packages/integrations/src/env.ts, .env.example</files>
  <read_first>
    - `packages/integrations/src/adapters/clockify-adapter.ts` (pure-fetch OAuth shape)
    - `packages/integrations/src/adapters/gusto-adapter.ts` (the twin from 94-07)
    - `register-all.ts:69-140`
  </read_first>
  <action>
    Create `quickbooks-adapter.ts` — `QuickBooksAdapter implements IntegrationProviderAdapter` (`slug:'quickbooks'`, `supportsOAuth:true`) with `getOAuthConfig()` (env-var-named `QUICKBOOKS_CLIENT_ID`/`QUICKBOOKS_CLIENT_SECRET`, Intuit authorize/token URLs, scopes, `redirectPath:'/api/oauth/quickbooks/callback'`), `exchangeCodeForTokens`, `refreshToken`, `getHealthStatus`, and a pure `mapFeedToQuickbooksPayload(feed)`. `fetch`-based, no SDK. Register in the HEAVY/lazy tier of `register-all.ts`. Declare `QUICKBOOKS_CLIENT_ID`/`QUICKBOOKS_CLIENT_SECRET` in the integrations env schema + `.env.example`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/integrations test quickbooks-adapter 2>&1 | tail -15 && pnpm -F @contractor-ops/integrations typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `quickbooks-adapter.test.ts` GREEN for OAuthConfig + payload map; live case skipped without `QUICKBOOKS_CLIENT_ID`.
    - Env vars in the schema + `.env.example`; `check:no-process-env` unaffected.
    - QuickBooksAdapter registered in `register-all.ts`.
  </acceptance_criteria>
  <done>QuickBooks OAuth adapter on the framework; live path conditional-skip.</done>
</task>

<task type="auto">
  <name>Task 2: Bridge quickbooks payroll profile (native push or CSV fallback)</name>
  <files>packages/payroll/src/profiles/quickbooks/{index,bridge}.ts, packages/payroll/src/index.ts</files>
  <read_first>
    - `packages/payroll/src/profiles/quickbooks-csv/index.ts` (fallback)
    - `packages/payroll/src/profiles/gusto/bridge.ts` (the twin bridge from 94-07)
  </read_first>
  <action>
    Create `quickbooks/bridge.ts` mirroring the Gusto bridge (dependency-injected `resolveConnection`/`evaluateFlag`/`httpPost`): native push when `payroll.quickbooks` enabled + connected, else `QuickbooksCsvProfile.generate(feed)`. Create `quickbooks/index.ts` — `QuickBooksProfile` (`profileId:'quickbooks'`, `country:'US'`, `flagKey:'payroll.quickbooks'`). Keep `packages/payroll` free of api/db imports. Export `QuickBooksProfile` + `registerQuickBooksProfile()`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/payroll test quickbooks 2>&1 | tail -15 && pnpm -F @contractor-ops/payroll typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Bridge returns CSV fallback when dark/unconnected (no network) and native payload when enabled+connected (mocked).
    - `packages/payroll` imports nothing from `packages/api`/`packages/db`.
    - `QuickBooksProfile.flagKey === 'payroll.quickbooks'`.
  </acceptance_criteria>
  <done>Bridge quickbooks profile GREEN; native-or-fallback resolved by flag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| org → QuickBooks OAuth | Tokens AES-256-GCM in `credentialsRef` keyed on org+provider. |
| external QuickBooks payload | Parsed with `safeParse`; no unsafe `as`. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-08-01 | Elevation | live push before signoff | mitigate | Bridge gates on `payroll.quickbooks` APPROVED + connection; dark → CSV fallback, no network. |
| T-94-08-02 | Information Disclosure | token/PII in logs | mitigate | No token/SSN logged; response `safeParse`d. |
| T-94-08-SC | Tampering | vendor SDK install | accept | Pure `fetch`; no new package. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/integrations test quickbooks-adapter` + `pnpm -F @contractor-ops/payroll test quickbooks` GREEN (live skipped without creds).
- `pnpm -F @contractor-ops/integrations typecheck` + `pnpm -F @contractor-ops/payroll typecheck` + `pnpm check:no-process-env` green.
</verification>

<success_criteria>
QuickBooks native OAuth adapter + bridge profile delivered dark: buildable + tested now, live-push behind `payroll.quickbooks` with conditional-skip tests that auto-flip GREEN when partner credentials land. CSV fallback (94-06) covers the dark path.
</success_criteria>

<output>
Create `.planning/phases/94-theme-b-payroll-integration-adapters/94-08-SUMMARY.md` when done.
</output>
