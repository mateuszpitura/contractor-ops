---
phase: 94-theme-b-payroll-integration-adapters
plan: 04
type: execute
wave: 3
depends_on: [94-01, 94-02]
files_modified:
  - packages/payroll/src/profiles/datev/index.ts
  - packages/payroll/src/profiles/datev/generator.ts
  - packages/payroll/src/profiles/datev/constants.ts
  - packages/payroll/src/profiles/datev/datevconnect-seam.ts
  - packages/payroll/src/profiles/sage-de/index.ts
  - packages/payroll/src/profiles/sage-de/generator.ts
  - packages/payroll/src/index.ts
  - packages/feature-flags/src/flags-core.ts
  - packages/feature-flags/src/signoff-registry-flags.json
autonomous: true
requirements: [PAYROLL-DE-01, PAYROLL-DE-02]

must_haves:
  truths:
    - "DATEV Lohn und Gehalt ASCII export matches its golden fixture with an exact record-length hard-guard"
    - "The DATEVconnect REST live-push path is a wired-but-dark seam, not a v7.0 deliverable"
    - "Sage DE Personalwirtschaft CSV export matches its golden fixture"
    - "A new payroll.sage-de flag key is added so Sage DE is not conflated with DATEV"
  artifacts:
    - path: "packages/payroll/src/profiles/datev/generator.ts"
      provides: "DATEV Lohn ASCII generator (fixed-field, length-guarded)"
      contains: "length"
    - path: "packages/payroll/src/profiles/datev/datevconnect-seam.ts"
      provides: "dark DATEVconnect REST seam (throws NOT_ENABLED / returns unavailable when unsubscribed)"
      contains: "DATEVconnect"
    - path: "packages/feature-flags/src/flags-core.ts"
      provides: "payroll.sage-de flag key (category payroll, ship-dark)"
      contains: "payroll.sage-de"
  key_links:
    - from: "datev/generator.ts"
      to: "DE countryFields"
      via: "maps lohnsteuerklasse/svNummer/krankenkasse/steuerIdNr + hireDate/terminatedAt to the DATEV Lohn record"
      pattern: "lohnsteuerklasse"
---

<objective>
Build the two German payroll-export profiles — DATEV Lohn und Gehalt (ASCII import, the shipping path) with a dark DATEVconnect REST seam, and Sage HR/Personalwirtschaft (CSV) — turning the DE golden-fixture RED tests (94-02) GREEN, and add the discretionary `payroll.sage-de` flag so Sage DE has its own gate (Open Q1).

Purpose: deliver PAYROLL-DE-01/02 as the deterministic DE file-export floor; keep DATEVconnect REST wired-but-dark per D-02.
Output: registered `DatevProfile` + `SageDeProfile`, the DATEVconnect seam, and the new `payroll.sage-de` flag key.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-RESEARCH.md
@.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-VALIDATION.md

<interfaces>
- `packages/payroll/src/__tests__/{datev,sage-de}.test.ts` + fixtures — the RED proofs (from 94-02).
- `packages/api/src/services/payment-export.ts:545-552` (`padField`/`padZero`) + `:879-888` (`assertNachaLen` length-guard) — the fixed-width idiom for DATEV Lohn ASCII.
- `packages/api/src/services/payment-export.ts:154-194` — CSV idiom for Sage DE.
- `packages/validators/src/employee-country-fields.ts:75-93` — DE countryFields (lohnsteuerklasse/kirchensteuer/steuerIdNr/svNummer/krankenkasse/kinderfreibetrag).
- `packages/feature-flags/src/flags-core.ts:363-370` (`payroll.datev` entry — copy the shape for `payroll.sage-de`) + `signoff-registry-flags.json:154-161`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DATEV Lohn und Gehalt ASCII export + dark DATEVconnect seam</name>
  <files>packages/payroll/src/profiles/datev/{index,generator,constants,datevconnect-seam}.ts</files>
  <read_first>
    - `packages/payroll/src/__tests__/datev.test.ts` + `fixtures/datev.golden.txt` (exact record layout + length)
    - `payment-export.ts:545-552,879-888` (padField/padZero + length hard-guard)
    - `94-RESEARCH.md` § Pattern 3 (DATEV header-record convention)
  </read_first>
  <action>
    Create `constants.ts` pinning the DATEV Lohn/LODAS layout version (field order, widths, `DTVF`/`EXTF` header record) with an adviser-verify note. Create `generator.ts` — a pure `generateDatevAscii(feed)` building the header record + one fixed-field detail record per employee (mapping `lohnsteuerklasse`/`kirchensteuer`/`steuerIdNr`/`svNummer`/`krankenkasse`/`kinderfreibetrag` + `displayName`/`hireDate`/`terminatedAt`), with a hard record-length guard (mirror `assertNachaLen`) that throws on any width mismatch. Create `datevconnect-seam.ts` — a typed `pushViaDatevConnect(feed, connection)` that, until subscribed, returns `{ source:'DATEVCONNECT', available:false, note }` (mirror the gov-stub idiom) and makes no network call; documented as the "where subscribed" live seam (D-02). Create `index.ts` — `DatevProfile` (`profileId:'datev'`, `country:'DE'`, `flagKey:'payroll.datev'`, `generate` → ASCII buffer) + `registerDatevProfile()`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/payroll test datev 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `datev.test.ts` GREEN; the exact-record-length assertion passes.
    - `datevconnect-seam.ts` makes no network call and returns an `available:false` result until subscribed.
    - `DatevProfile.flagKey === 'payroll.datev'`.
  </acceptance_criteria>
  <done>DATEV Lohn ASCII GREEN; DATEVconnect wired-but-dark (PAYROLL-DE-01).</done>
</task>

<task type="auto">
  <name>Task 2: Add payroll.sage-de flag + Sage DE Personalwirtschaft profile</name>
  <files>packages/feature-flags/src/flags-core.ts, packages/feature-flags/src/signoff-registry-flags.json, packages/payroll/src/profiles/sage-de/{index,generator}.ts</files>
  <read_first>
    - `packages/feature-flags/src/flags-core.ts:363-370` (`payroll.datev` entry shape) + `:158-161` in signoff json
    - `packages/payroll/src/__tests__/sage-de.test.ts` + fixture
  </read_first>
  <action>
    Add a `payroll.sage-de` key to `flags-core.ts` (category `payroll`, owner `payroll-platform`, ship-dark description) and a matching PENDING entry to `signoff-registry-flags.json` — copy the `payroll.datev` shape exactly. Create `sage-de/generator.ts` + `sage-de/index.ts` (`SageDeProfile`, `profileId:'sage-de'`, `country:'DE'`, `flagKey:'payroll.sage-de'`) emitting the Sage DE Personalwirtschaft import CSV from the DE feed. Add `registerSageDeProfile()`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/feature-flags test 2>&1 | tail -10 && pnpm -F @contractor-ops/payroll test sage-de 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `payroll.sage-de` present in BOTH `flags-core.ts` and `signoff-registry-flags.json` (PENDING); feature-flags tests green.
    - `sage-de.test.ts` GREEN; `SageDeProfile.flagKey === 'payroll.sage-de'`.
  </acceptance_criteria>
  <done>Sage DE export GREEN behind its own new flag (PAYROLL-DE-02).</done>
</task>

<task type="auto">
  <name>Task 3: Export the DE profiles from the package surface</name>
  <files>packages/payroll/src/index.ts</files>
  <read_first>
    - `packages/einvoice/src/index.ts` (export + register-convenience pattern)
  </read_first>
  <action>
    Add `DatevProfile` + `SageDeProfile` + their `register*Profile()` fns to `packages/payroll/src/index.ts`, following the einvoice convention (explicit registration, test-isolation-safe).
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/payroll test datev sage-de 2>&1 | tail -10 && pnpm -F @contractor-ops/payroll typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Both DE profiles importable from `@contractor-ops/payroll`; typecheck green.
  </acceptance_criteria>
  <done>DE profiles exported from the package surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| PayrollFeed → DATEV/Sage export | SV-Nr / Steuer-IdNr are plain-but-RBAC-gated (not encrypted) fields already validated at the P90 boundary; the generator maps them from `countryFields`. |
| flag registry edit | Adding a key must keep `pnpm standards:check` + feature-flags tests green. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-04-01 | Tampering | malformed fixed-width DATEV record | mitigate | Exact-length hard-guard (mirror assertNachaLen) throws on any width mismatch. |
| T-94-04-02 | Elevation | DATEVconnect live push before subscription | mitigate | Seam returns `available:false` + no network call until the org subscribes; gated behind `payroll.datev`. |
| T-94-04-SC | Tampering | npm installs | accept | No installs. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/payroll test datev sage-de` GREEN; `pnpm -F @contractor-ops/feature-flags test` GREEN.
- `pnpm -F @contractor-ops/payroll typecheck` + `pnpm standards:check` green.
</verification>

<success_criteria>
PAYROLL-DE-01/02 delivered: DATEV Lohn ASCII (shipping) + DATEVconnect dark seam + Sage DE CSV, each proven against golden fixtures; Sage DE gains its own `payroll.sage-de` flag.
</success_criteria>

<output>
Create `.planning/phases/94-theme-b-payroll-integration-adapters/94-04-SUMMARY.md` when done.
</output>
