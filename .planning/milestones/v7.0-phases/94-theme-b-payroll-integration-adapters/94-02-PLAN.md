---
phase: 94-theme-b-payroll-integration-adapters
plan: 02
type: execute
wave: 2
depends_on: [94-01]
files_modified:
  - packages/payroll/src/__tests__/fixtures/README.md
  - packages/payroll/src/__tests__/symfonia.test.ts
  - packages/payroll/src/__tests__/comarch.test.ts
  - packages/payroll/src/__tests__/enova.test.ts
  - packages/payroll/src/__tests__/datev.test.ts
  - packages/payroll/src/__tests__/sage-de.test.ts
  - packages/payroll/src/__tests__/rti-fps.test.ts
  - packages/payroll/src/__tests__/rti-eps.test.ts
  - packages/payroll/src/__tests__/adp.test.ts
  - packages/payroll/src/__tests__/gusto-csv.test.ts
  - packages/payroll/src/__tests__/quickbooks-csv.test.ts
  - packages/integrations/src/adapters/__tests__/gusto-adapter.test.ts
  - packages/integrations/src/adapters/__tests__/quickbooks-adapter.test.ts
  - packages/api/src/services/__tests__/payroll-feed.test.ts
  - packages/api/src/routers/__tests__/payroll-export.test.ts
  - packages/api/src/routers/__tests__/payroll-cross-org.test.ts
autonomous: true
requirements: [PAYROLL-PL-01, PAYROLL-PL-02, PAYROLL-PL-03, PAYROLL-DE-01, PAYROLL-DE-02, PAYROLL-UK-01, PAYROLL-US-01]

must_haves:
  truths:
    - "Every file-export target has a failing (RED) golden-fixture round-trip test that pins generate(feed) == golden output"
    - "Gusto/QuickBooks native adapters have RED tests pinning OAuthConfig + payload map, with live calls conditionally-skipped when creds are absent"
    - "buildPayrollFeed has a RED test pinning the Worker+EmployeeProfile+PersonnelFile join and last-4 PII masking"
    - "The export procedure + a two-org IDOR regression are RED-scaffolded"
  artifacts:
    - path: "packages/payroll/src/__tests__/datev.test.ts"
      provides: "RED golden-fixture proof for DATEV Lohn ASCII + exact record length"
      contains: "generate"
    - path: "packages/payroll/src/__tests__/rti-fps.test.ts"
      provides: "RED golden-fixture proof for RTI FPS XML + non-throwing XSD seam"
      contains: "generate"
    - path: "packages/integrations/src/adapters/__tests__/gusto-adapter.test.ts"
      provides: "RED OAuthConfig + payload-map proof; live call skipIf(no creds)"
      contains: "skipIf"
    - path: "packages/api/src/services/__tests__/payroll-feed.test.ts"
      provides: "RED feed-builder join + PII-mask proof"
      contains: "Last4"
    - path: "packages/api/src/routers/__tests__/payroll-cross-org.test.ts"
      provides: "RED two-org IDOR regression for the payroll export"
      contains: "organizationId"
  key_links:
    - from: "datev.test.ts"
      to: "packages/payroll DatevProfile.generate"
      via: "asserts generate(feed).buffer.toString() equals fixtures/datev.golden.txt and buffer length is record-aligned"
      pattern: "golden"
---

<objective>
Seed the RED validation net enumerated in `94-VALIDATION.md` § Wave 1 Requirements so every downstream impl plan has a failing proof to turn GREEN. This is the Nyquist floor: one golden-fixture round-trip per file-export target, native-adapter OAuth/payload proofs (with conditional-skip live calls), the feed-builder join + PII-mask, the flag-gated export procedure, and a two-org IDOR regression.

Purpose: lock the observable output of each adapter (byte-for-byte against a committed golden file) BEFORE implementation, so executors build to a proof, not a description.
Output: RED test files (fail via missing profile/service/router module) + hand-authored golden fixtures. RED is the accepted terminal state for this plan.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-RESEARCH.md
@.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-VALIDATION.md

<interfaces>
<!-- Harness/style to mirror; the golden-fixture idiom to copy. -->
- `packages/einvoice/src/__tests__/{ksef,zatca}.test.ts` — generator round-trip test style (build canonical input → generate → assert output).
- `packages/iris/src/__tests__/fixtures/golden-1099-nec.json` + `xsd-bundle-present.ts` — the golden-fixture + non-throwing-when-bundle-absent idiom to copy for RTI.
- `packages/integrations/src/adapters/__tests__/` existing adapter tests — OAuthConfig assertion + mock-fetch style; `it.skipIf(...)` for live-cred-gated cases.
- `packages/api/src/routers/__tests__/` existing cross-org tests (e.g. worker/employee cross-org) — two-org IDOR harness.
- `94-RESEARCH.md` § Pattern 2 (the per-target field matrix each golden fixture encodes).

Current-state facts the RED scaffolds encode as the TARGET (not yet true):
- No `packages/payroll/src/profiles/*` modules exist yet (94-01 shipped only the contract).
- No `packages/integrations/src/adapters/{gusto,quickbooks}-adapter.ts`.
- No `packages/api/src/services/payroll-feed.ts`, no `packages/api/src/routers/workforce/payroll-export-router.ts`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Golden fixtures + RED round-trip tests for the file-export targets</name>
  <files>packages/payroll/src/__tests__/fixtures/README.md, packages/payroll/src/__tests__/{symfonia,comarch,enova,datev,sage-de,rti-fps,rti-eps,adp,gusto-csv,quickbooks-csv}.test.ts</files>
  <read_first>
    - `packages/payroll/src/types/{profile,feed}.ts` (the contract + DTO to import — from 94-01)
    - `94-RESEARCH.md` § Pattern 2 per-target matrix + § Pattern 3 format idioms
    - `packages/api/src/services/payment-export.ts:154-194` (CSV+BOM), `:276-301` (XML string-builder), `:656-727`/`:911-1071` (fixed-width length-guard) — the shapes the goldens must reflect
  </read_first>
  <action>
    Author one small, hand-built `PayrollFeed` fixture (2-3 employees per market with the required + a couple optional country fields) inline in each test. For each target, write a RED test importing the not-yet-built profile from `@contractor-ops/payroll` (e.g. `import { SymfoniaProfile } from '../profiles/symfonia'`) that asserts `profile.generate(feed)` returns `{ ext, mime }` as specified and `buffer.toString(...)` **equals a committed golden fixture** under `fixtures/`. Author the golden fixtures BY HAND from the target's real column/record contract (Symfonia CSV+XML, Comarch Optima, Enova, DATEV Lohn ASCII with an exact record-length assertion, Sage DE CSV, RTI FPS + EPS XML envelopes, ADP/Gusto/QuickBooks CSV). For RTI, additionally assert the XSD validate seam is **non-throwing** when the bundle is absent (mirror IRIS). Add `fixtures/README.md` documenting each fixture's source spec + adviser-verify note. All tests fail terminal-RED via Cannot-find-module for the profile — the expected state.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/payroll test symfonia comarch enova datev sage-de rti-fps rti-eps adp gusto-csv quickbooks-csv 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - All 10 test files exist and are collected by vitest (not skipped-empty).
    - Run exits non-zero (RED) via Cannot-find-module for `../profiles/*` — NOT a harness/syntax error.
    - Each golden fixture is committed and referenced; DATEV test asserts an exact record length; RTI test asserts the non-throwing XSD seam.
    - `grep -q "golden" packages/payroll/src/__tests__/datev.test.ts` succeeds.
    - `pnpm -F @contractor-ops/payroll typecheck` stays green (tsconfig excludes `__tests__`).
  </acceptance_criteria>
  <done>Every file-export target pinned RED against a hand-built golden fixture.</done>
</task>

<task type="auto">
  <name>Task 2: RED native-adapter proofs (Gusto + QuickBooks) with conditional-skip live calls</name>
  <files>packages/integrations/src/adapters/__tests__/gusto-adapter.test.ts, packages/integrations/src/adapters/__tests__/quickbooks-adapter.test.ts</files>
  <read_first>
    - `packages/integrations/src/types/provider.ts:17-77` (IntegrationProviderAdapter + OAuthConfig shape)
    - `packages/integrations/src/adapters/__tests__/` an existing OAuth adapter test (mock-fetch + OAuthConfig assertion)
    - `.planning/EXTERNAL-ENABLEMENT.md` (the flag-defer + conditional-skip invariant)
  </read_first>
  <action>
    Create `gusto-adapter.test.ts` + `quickbooks-adapter.test.ts` importing the not-yet-built adapters. Assert (RED via Cannot-find-module): `slug`/`displayName`/`supportsOAuth === true`; `getOAuthConfig()` returns the correct `clientIdEnvVar`/`clientSecretEnvVar`/`authorizationUrl`/`tokenUrl`/`redirectPath`/`scopes`; a pure `mapFeedToGustoPayload(feed)`/`mapFeedToQuickbooksPayload(feed)` produces the expected JSON shape (no PII beyond what the API requires); and a **live** token-exchange/push case wrapped in `it.skipIf(!process.env.GUSTO_CLIENT_ID)` / `it.skipIf(!process.env.QUICKBOOKS_CLIENT_ID)` so it auto-runs (and flips GREEN) only when creds land. Confirm the integrations tsconfig already excludes `src/**/__tests__/**` (Phase 88) so the RED does not brick the composite build.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/integrations test gusto-adapter quickbooks-adapter 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist, collected by vitest, exit RED via Cannot-find-module for the adapters.
    - `grep -q "skipIf" packages/integrations/src/adapters/__tests__/gusto-adapter.test.ts` succeeds (live call is conditionally skipped).
    - `pnpm -F @contractor-ops/integrations typecheck` stays green.
  </acceptance_criteria>
  <done>Gusto/QuickBooks native contract pinned RED; live path conditional-skip per the external-deps invariant.</done>
</task>

<task type="auto">
  <name>Task 3: RED feed-builder + export-procedure + two-org IDOR scaffolds</name>
  <files>packages/api/src/services/__tests__/payroll-feed.test.ts, packages/api/src/routers/__tests__/payroll-export.test.ts, packages/api/src/routers/__tests__/payroll-cross-org.test.ts</files>
  <read_first>
    - `packages/db/prisma/schema/{worker,employee,personnel}.prisma` (the join the feed-builder performs)
    - `packages/api/src/middleware/require-workforce-flag.ts:25-42` (assertWorkforceEnabled)
    - `packages/api/src/root.ts:185-196` (conditionalWorkforceRouters — the dark-mount the router joins)
    - existing cross-org test under `packages/api/src/routers/__tests__/` for harness/mock style
  </read_first>
  <action>
    Create `payroll-feed.test.ts` asserting (RED via missing `../../services/payroll-feed`): `buildPayrollFeed(db, orgId, employeeIds)` joins `Worker`→`EmployeeProfile`→`PersonnelFile`, carries `hireDate`/`terminatedAt` from PersonnelFile, projects promoted `etat`/`employmentStatus`, and sets `nationalIdLast4` from `*Last4` — NEVER a full national ID. Create `payroll-export.test.ts` asserting the export procedure (missing router) throws when `module.workforce-employees` is OFF, throws `FORBIDDEN` when the per-target `payroll.<target>` flag is PENDING/off, calls `writeAuditLog` with the target + employee count on success, and rejects a non-`.strict()` input. Create `payroll-cross-org.test.ts` asserting an ORG_A client can never export ORG_B employees (feed filtered by session org). All fail terminal-RED via missing modules.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test payroll-feed payroll-export payroll-cross-org 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist, collected, exit RED via Cannot-find-module for `payroll-feed`/`payroll-export-router` — NOT a harness error.
    - `grep -q "Last4" packages/api/src/services/__tests__/payroll-feed.test.ts` and `grep -q "organizationId" packages/api/src/routers/__tests__/payroll-cross-org.test.ts` succeed.
    - `pnpm typecheck --filter=@contractor-ops/api` stays green (tsconfig excludes `__tests__`).
  </acceptance_criteria>
  <done>Feed-builder, flag-gated export, and IDOR regression scaffolded RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → not-yet-built modules | Scaffolds reference missing profiles/services; must not brick typecheck (all three package tsconfigs exclude `__tests__`). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-02-01 | Tampering | false-green RED scaffold | mitigate | Each scaffold must fail via Cannot-find-module (the right reason), verified in acceptance; not a silent skip. |
| T-94-02-02 | Information Disclosure | golden fixture embeds real PII | mitigate | Fixtures use synthetic last-4-only identifiers; no real PESEL/SSN/SV-Nr in committed goldens. |
| T-94-02-SC | Tampering | npm installs | accept | No package installs; slopcheck N/A. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/payroll test` + `pnpm -F @contractor-ops/integrations test gusto-adapter quickbooks-adapter` + `pnpm -F @contractor-ops/api test payroll-feed payroll-export payroll-cross-org` collect all files and exit RED for the documented reasons.
- `pnpm -F @contractor-ops/payroll typecheck` + `pnpm typecheck --filter=@contractor-ops/api` + `pnpm -F @contractor-ops/integrations typecheck` stay green.
</verification>

<success_criteria>
Every Wave 1 test from `94-VALIDATION.md` exists, is collected, and is terminal-RED for the correct (missing-implementation) reason — giving Waves 3-5 a failing proof to satisfy. Golden fixtures are committed and hand-built from each target's real format.
</success_criteria>

<output>
Create `.planning/phases/94-theme-b-payroll-integration-adapters/94-02-SUMMARY.md` when done.
</output>
