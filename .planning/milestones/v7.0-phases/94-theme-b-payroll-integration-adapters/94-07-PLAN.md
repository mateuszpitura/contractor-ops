---
phase: 94-theme-b-payroll-integration-adapters
plan: 07
type: execute
wave: 4
depends_on: [94-01, 94-02, 94-06]
files_modified:
  - packages/integrations/src/adapters/gusto-adapter.ts
  - packages/integrations/src/adapters/register-all.ts
  - packages/integrations/src/env.ts
  - packages/payroll/src/profiles/gusto/index.ts
  - packages/payroll/src/profiles/gusto/bridge.ts
  - packages/payroll/src/index.ts
  - .env.example
autonomous: true
requirements: [PAYROLL-US-01]

must_haves:
  truths:
    - "A GustoAdapter (IntegrationProviderAdapter) implements OAuth 2.0 connect on the integrations framework"
    - "A bridge gusto payroll profile pushes via the adapter when payroll.gusto is APPROVED + connected, else falls back to gusto-csv"
    - "Live token-exchange/push tests conditionally-skip when GUSTO_CLIENT_ID is absent (auto-flip GREEN when creds land)"
    - "GUSTO_CLIENT_ID/GUSTO_CLIENT_SECRET are in .env.example + the integrations env schema; no raw process.env"
  artifacts:
    - path: "packages/integrations/src/adapters/gusto-adapter.ts"
      provides: "GustoAdapter with getOAuthConfig/exchangeCodeForTokens/refreshToken/getHealthStatus"
      contains: "getOAuthConfig"
    - path: "packages/payroll/src/profiles/gusto/bridge.ts"
      provides: "flag-gated bridge: native push when enabled+connected, else gusto-csv fallback"
      contains: "payroll.gusto"
  key_links:
    - from: "gusto/bridge.ts"
      to: "GustoAdapter + GustoCsvProfile"
      via: "when payroll.gusto APPROVED and an IntegrationConnection exists → POST mapped payload; else delegate to GustoCsvProfile.generate"
      pattern: "fallback"
---

<objective>
Add the Gusto native OAuth adapter on the `packages/integrations` framework and a bridge payroll profile that pushes to Gusto's REST API when `payroll.gusto` is APPROVED and the org has connected — otherwise falling back to the `gusto-csv` file export (94-06). Turn the Gusto native RED tests (94-02) GREEN, with the live token-exchange/push path conditionally-skipped until partner OAuth credentials land (the external-deps invariant).

Purpose: deliver the Gusto native half of PAYROLL-US-01, buildable dark now, live when creds arrive.
Output: `GustoAdapter`, the bridge `gusto` profile, env wiring, and the register-all hook.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/EXTERNAL-ENABLEMENT.md
@.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-RESEARCH.md
@.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-VALIDATION.md

<interfaces>
- `packages/integrations/src/adapters/__tests__/gusto-adapter.test.ts` — the RED proof (from 94-02).
- `packages/integrations/src/types/provider.ts:17-77` — IntegrationProviderAdapter + OAuthConfig contract.
- `packages/integrations/src/adapters/clockify-adapter.ts` — a pure-fetch OAuth adapter to mirror (no vendor SDK).
- `packages/integrations/src/adapters/register-all.ts:69-140` — HEAVY/lazy registration (add Gusto there).
- `packages/integrations/src/env.ts` (or the package env schema) — where env vars are declared.
- `packages/payroll/src/profiles/gusto-csv/index.ts` — the CSV fallback the bridge delegates to (from 94-06).
- `.planning/EXTERNAL-ENABLEMENT.md` — the register the Gusto gate row is recorded in (added in 94-10).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GustoAdapter (OAuth 2.0 on the integrations framework)</name>
  <files>packages/integrations/src/adapters/gusto-adapter.ts, packages/integrations/src/adapters/register-all.ts, packages/integrations/src/env.ts, .env.example</files>
  <read_first>
    - `packages/integrations/src/adapters/clockify-adapter.ts` (pure-fetch OAuth adapter shape)
    - `packages/integrations/src/types/provider.ts:17-77`
    - `register-all.ts:69-140` (lazy registration)
  </read_first>
  <action>
    Create `gusto-adapter.ts` — `GustoAdapter implements IntegrationProviderAdapter` (`slug:'gusto'`, `supportsOAuth:true`, `supportsWebhooks:false`) with `getOAuthConfig()` (env-var-named `GUSTO_CLIENT_ID`/`GUSTO_CLIENT_SECRET`, Gusto authorize/token URLs, scopes, `redirectPath:'/api/oauth/gusto/callback'`), `exchangeCodeForTokens`, `refreshToken`, `getHealthStatus`, and a pure `mapFeedToGustoPayload(feed)`. Use `fetch` (no vendor SDK). Register it in the HEAVY/lazy tier of `register-all.ts` (mirror Clockify). Declare `GUSTO_CLIENT_ID`/`GUSTO_CLIENT_SECRET` in the integrations env schema (no raw `process.env`) and add them to `.env.example`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/integrations test gusto-adapter 2>&1 | tail -15 && pnpm -F @contractor-ops/integrations typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `gusto-adapter.test.ts` GREEN for the OAuthConfig + payload-map assertions; the live case remains skipped without `GUSTO_CLIENT_ID`.
    - Env vars declared in the schema + `.env.example`; `pnpm check:no-process-env` unaffected.
    - GustoAdapter registered in `register-all.ts`.
  </acceptance_criteria>
  <done>Gusto OAuth adapter on the framework; live path conditional-skip.</done>
</task>

<task type="auto">
  <name>Task 2: Bridge gusto payroll profile (native push or CSV fallback)</name>
  <files>packages/payroll/src/profiles/gusto/{index,bridge}.ts, packages/payroll/src/index.ts</files>
  <read_first>
    - `packages/payroll/src/profiles/gusto-csv/index.ts` (the fallback)
    - `packages/feature-flags/src/flags-core.ts` (`payroll.gusto`)
    - `94-RESEARCH.md` § Pattern 4 (bridge posture — Open Q2 RESOLVED)
  </read_first>
  <action>
    Create `gusto/bridge.ts` — a function that, given the feed + an org's `IntegrationConnection` lookup + a flag evaluator, POSTs `mapFeedToGustoPayload(feed)` to Gusto when `payroll.gusto` is enabled AND a connection exists, otherwise delegates to `GustoCsvProfile.generate(feed)` and returns the CSV result. Create `gusto/index.ts` — `GustoProfile` (`profileId:'gusto'`, `country:'US'`, `flagKey:'payroll.gusto'`, `generate` → bridge). Because the connection/flag context lives in `packages/api`, keep the bridge dependency-injected (pass a `resolveConnection`/`evaluateFlag`/`httpPost` callback set) so `packages/payroll` stays free of api/db imports. Export `GustoProfile` + `registerGustoProfile()` from `index.ts`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/payroll test gusto 2>&1 | tail -15 && pnpm -F @contractor-ops/payroll typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The bridge returns the CSV fallback when the flag is dark/unconnected (no network) and the native payload when enabled+connected (mocked in the test).
    - `packages/payroll` imports nothing from `packages/api`/`packages/db` (dependency-injected callbacks only).
    - `GustoProfile.flagKey === 'payroll.gusto'`.
  </acceptance_criteria>
  <done>Bridge gusto profile GREEN; native-or-fallback resolved by flag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| org → Gusto OAuth | Tokens stored AES-256-GCM in `IntegrationConnection.credentialsRef` keyed on org+provider; never client-supplied. |
| external Gusto payload | Response parsed with `safeParse`; no unsafe `as`. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-07-01 | Elevation | live push before signoff | mitigate | Bridge gates on `payroll.gusto` APPROVED + a connection; dark → CSV fallback, no network. |
| T-94-07-02 | Information Disclosure | token/PII in logs | mitigate | No token or SSN logged; structured logger only; response `safeParse`d. |
| T-94-07-SC | Tampering | vendor SDK install | accept | Pure `fetch`; no new package. If an SDK is later chosen, 7-day age + checkpoint:human-verify. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/integrations test gusto-adapter` + `pnpm -F @contractor-ops/payroll test gusto` GREEN (live case skipped without creds).
- `pnpm -F @contractor-ops/integrations typecheck` + `pnpm -F @contractor-ops/payroll typecheck` + `pnpm check:no-process-env` green.
</verification>

<success_criteria>
Gusto native OAuth adapter + bridge profile delivered dark: buildable + tested now, live-push behind `payroll.gusto` with conditional-skip tests that auto-flip GREEN when partner credentials land. CSV fallback (94-06) covers the dark path.
</success_criteria>

<output>
Create `.planning/phases/94-theme-b-payroll-integration-adapters/94-07-SUMMARY.md` when done.
</output>
