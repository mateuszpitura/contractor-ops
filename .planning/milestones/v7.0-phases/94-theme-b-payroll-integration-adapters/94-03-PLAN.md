---
phase: 94-theme-b-payroll-integration-adapters
plan: 03
type: execute
wave: 3
depends_on: [94-01, 94-02]
files_modified:
  - packages/payroll/src/profiles/symfonia/index.ts
  - packages/payroll/src/profiles/symfonia/generator.ts
  - packages/payroll/src/profiles/symfonia/constants.ts
  - packages/payroll/src/profiles/comarch/index.ts
  - packages/payroll/src/profiles/comarch/generator.ts
  - packages/payroll/src/profiles/enova/index.ts
  - packages/payroll/src/profiles/enova/generator.ts
  - packages/payroll/src/index.ts
autonomous: true
requirements: [PAYROLL-PL-01, PAYROLL-PL-02, PAYROLL-PL-03]

must_haves:
  truths:
    - "Symfonia profile emits both a CSV and an XML export matching its golden fixtures"
    - "Comarch (Optima) and Enova365 profiles emit their import layouts matching golden fixtures"
    - "Each PL profile registers with flagKey payroll.symfonia / payroll.comarch / payroll.enova"
  artifacts:
    - path: "packages/payroll/src/profiles/symfonia/generator.ts"
      provides: "generateSymfoniaCsv + generateSymfoniaXml (pure PayrollFeed → Buffer)"
      contains: "0xef, 0xbb, 0xbf"
    - path: "packages/payroll/src/profiles/comarch/index.ts"
      provides: "ComarchProfile registered with flagKey payroll.comarch"
      contains: "payroll.comarch"
    - path: "packages/payroll/src/profiles/enova/index.ts"
      provides: "EnovaProfile registered with flagKey payroll.enova"
      contains: "payroll.enova"
  key_links:
    - from: "symfonia/generator.ts"
      to: "PayrollFeed employees + PL countryFields"
      via: "maps stanowisko/urzadSkarbowyCode/zusTitleCode/nfzOddzial/stawkaBrutto + peselLast4 to the Symfonia column contract"
      pattern: "stanowisko"
---

<objective>
Build the three Polish payroll-export profiles (Symfonia Kadry i Płace CSV+XML, Comarch ERP XL/Optima, Enova365) as pure `PayrollFeed → PayrollExportResult` generators registered in the payroll engine, turning the PL golden-fixture RED tests (94-02) GREEN.

Purpose: deliver PAYROLL-PL-01/02/03 — the deterministic PL file-export floor (no external dependency, no credentials).
Output: registered `SymfoniaProfile`/`ComarchProfile`/`EnovaProfile`, each behind its existing `payroll.*` flag, each proven against its committed golden fixture.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-RESEARCH.md
@.planning/milestones/v7.0-phases/94-theme-b-payroll-integration-adapters/94-VALIDATION.md

<interfaces>
- `packages/payroll/src/types/{profile,feed}.ts` — the contract to implement (from 94-01).
- `packages/payroll/src/__tests__/{symfonia,comarch,enova}.test.ts` + `fixtures/` — the RED proofs to satisfy (from 94-02).
- `packages/api/src/services/payment-export.ts:154-194` — `generateCsv` (exceljs + UTF-8 BOM) idiom to mirror for CSV.
- `packages/api/src/services/payment-export.ts:115-122,276-301` — `escapeXml` + XML string-builder idiom for Symfonia XML.
- `packages/api/src/services/payment-export.ts:59-102` — `stripDiacritics`/`formatMultiline` (Polish diacritic handling for bank/vendor ASCII).
- `packages/validators/src/employee-country-fields.ts:42-63` — PL countryFields keys (stanowisko/etat/urzadSkarbowyCode/zusTitleCode/nfzOddzial/stawkaBrutto).
- `packages/einvoice/src/index.ts:251-254` — register*Profile() convenience-fn idiom.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Symfonia Kadry i Płace (CSV + XML)</name>
  <files>packages/payroll/src/profiles/symfonia/{index,generator,constants}.ts</files>
  <read_first>
    - `packages/payroll/src/__tests__/symfonia.test.ts` + `fixtures/symfonia.golden.{csv,xml}` (the exact target output)
    - `payment-export.ts:154-194` (generateCsv) + `:276-301` (XML builder)
  </read_first>
  <action>
    Create `constants.ts` pinning the Symfonia column contract (CSV header order + XML element names) with an adviser-verify note citing the Symfonia import-format version. Create `generator.ts` with `generateSymfoniaCsv(feed)` (exceljs workbook, pinned columns, UTF-8 BOM prepend) and `generateSymfoniaXml(feed)` (escapeXml string-builder), each mapping PL `countryFields` (`stanowisko`/`urzadSkarbowyCode`/`zusTitleCode`/`nfzOddzial`/`stawkaBrutto`) + `peselLast4` + `displayName`/`hireDate`/`terminatedAt`/`etat`. Create `index.ts` exporting `SymfoniaProfile` (`profileId:'symfonia'`, `country:'PL'`, `displayName`, `flagKey:'payroll.symfonia'`, `generate` selecting CSV or XML by `opts.format`) + `registerSymfoniaProfile()`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/payroll test symfonia 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `symfonia.test.ts` GREEN for both CSV and XML.
    - CSV buffer starts with the UTF-8 BOM; XML is `escapeXml`-safe.
    - `SymfoniaProfile.flagKey === 'payroll.symfonia'`.
  </acceptance_criteria>
  <done>Symfonia CSV+XML export GREEN (PAYROLL-PL-01).</done>
</task>

<task type="auto">
  <name>Task 2: Comarch (ERP XL / Optima) + Enova365</name>
  <files>packages/payroll/src/profiles/comarch/{index,generator}.ts, packages/payroll/src/profiles/enova/{index,generator}.ts</files>
  <read_first>
    - `packages/payroll/src/__tests__/{comarch,enova}.test.ts` + fixtures
    - `payment-export.ts:154-194` (CSV idiom)
  </read_first>
  <action>
    Create `comarch/generator.ts` + `comarch/index.ts` (`ComarchProfile`, `profileId:'comarch'`, `country:'PL'`, `flagKey:'payroll.comarch'`) emitting the Comarch Optima "Płace" import layout (CSV/XML per its spec) from the PL feed. Create `enova/generator.ts` + `enova/index.ts` (`EnovaProfile`, `profileId:'enova'`, `flagKey:'payroll.enova'`) emitting the enova365 kadry-płace import layout. Each maps the same PL `countryFields` set; pin the layout version in a comment with an adviser-verify note. Add convenience `registerComarchProfile()`/`registerEnovaProfile()`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/payroll test comarch enova 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `comarch.test.ts` + `enova.test.ts` GREEN against their goldens.
    - `ComarchProfile.flagKey === 'payroll.comarch'`; `EnovaProfile.flagKey === 'payroll.enova'`.
  </acceptance_criteria>
  <done>Comarch + Enova export GREEN (PAYROLL-PL-02/03).</done>
</task>

<task type="auto">
  <name>Task 3: Export the PL profiles from the package surface</name>
  <files>packages/payroll/src/index.ts</files>
  <read_first>
    - `packages/einvoice/src/index.ts` (export + register-convenience pattern)
  </read_first>
  <action>
    Add the three PL profiles + their `register*Profile()` convenience functions to `packages/payroll/src/index.ts` so `packages/api`'s `registerAllPayrollProfiles()` (Wave 5) can register them. Do NOT auto-register at import time in a way that breaks `clearProfiles()` test isolation — follow the einvoice convention (convenience fns, called explicitly).
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/payroll test symfonia comarch enova 2>&1 | tail -10 && pnpm -F @contractor-ops/payroll typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All three PL profiles importable from `@contractor-ops/payroll`.
    - `pnpm -F @contractor-ops/payroll typecheck` green.
  </acceptance_criteria>
  <done>PL profiles exported from the package surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| PayrollFeed → PL export file | Generators are pure over the DTO; no PII beyond last-4 unless the format legally needs the full PESEL (revealed upstream in the feed-builder, never here). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-03-01 | Information Disclosure | full PESEL in export | mitigate | Generator reads `nationalIdLast4` only; full-PESEL formats get the value from the feed-builder's audited reveal, not hard-coded here. |
| T-94-03-02 | Tampering | diacritic corruption in vendor import | mitigate | Reuse `stripDiacritics`/`formatMultiline` where the vendor demands ASCII; golden fixture locks the encoding. |
| T-94-03-SC | Tampering | npm installs | accept | Reuses in-tree exceljs; no installs. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/payroll test symfonia comarch enova` GREEN.
- `pnpm -F @contractor-ops/payroll typecheck` + `pnpm -F @contractor-ops/payroll lint` green.
</verification>

<success_criteria>
PAYROLL-PL-01/02/03 delivered: Symfonia (CSV+XML), Comarch, and Enova profiles registered behind their flags and proven against golden fixtures. Deterministic, no external dependency.
</success_criteria>

<output>
Create `.planning/phases/94-theme-b-payroll-integration-adapters/94-03-SUMMARY.md` when done.
</output>
