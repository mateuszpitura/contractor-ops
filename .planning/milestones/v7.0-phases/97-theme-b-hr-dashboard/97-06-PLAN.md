---
phase: 97-theme-b-hr-dashboard
plan: 06
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - apps/web-vite/src/router/dashboard-routes.tsx
  - apps/web-vite/src/pages/dashboard/hr.tsx
  - apps/web-vite/src/lib/nav-items.tsx
  - apps/web-vite/src/lib/navigation.ts
  - apps/web-vite/src/components/hr-dashboard/hooks/use-hr-summary.ts
  - apps/web-vite/src/components/hr-dashboard/hooks/use-hr-headcount.ts
  - apps/web-vite/src/components/hr-dashboard/hooks/use-hr-utilization.ts
  - apps/web-vite/src/components/hr-dashboard/hr-dashboard-header.tsx
  - apps/web-vite/src/components/hr-dashboard/hr-headcount-section.tsx
  - apps/web-vite/src/components/hr-dashboard/hr-utilization-section.tsx
autonomous: true
requirements: [HR-DASH-01, HR-DASH-02]

must_haves:
  truths:
    - "A new staff route `dashboard/hr` renders `HrDashboardPage` (thin `Suspense` -> `HrDashboardPageContent`); a 'People / HR' nav entry is gated on `module.hr-dashboard` (useFlag) + `usePermissions().can('employee', ['read'])` so non-HR roles never see it (RESEARCH § Route + Namespace Inventory)"
    - "The tRPC boundary is ONLY the `components/hr-dashboard/hooks/use-*.ts` hooks; the page is a thin composer; the sections are wired (branch loading/empty/error/forbidden) over presentational `*View`s — `check:web-vite-data-layer` + `check:web-vite-page-shells` + `check:web-vite-presentational` pass (ARCHITECTURE.md)"
    - "The KPI header (getSummary) + headcount breakdown (HR-DASH-01) + vacation-utilization (HR-DASH-02) sections each render loading / empty / degraded states — a dark source surface (e.g. no leave data) renders a real empty card, not a crash (D-04 Widget Gating Map)"
    - "All copy is i18n keys under `HrDashboard.*` (no hardcoded strings); the KPI cards reuse the existing `KpiCard`/`HeadcountStat` idiom; entrance animation + WCAG (keyboard/focus/contrast/semantic) followed"
  artifacts:
    - path: "apps/web-vite/src/pages/dashboard/hr.tsx"
      provides: "HrDashboardPage + HrDashboardPageContent composing the sections"
      contains: "HrDashboardPageContent"
    - path: "apps/web-vite/src/components/hr-dashboard/hooks/use-hr-headcount.ts"
      provides: "headcount hook (sole tRPC boundary for HR-DASH-01)"
      contains: "useTRPC"
  key_links:
    - from: "hr-headcount-section.tsx"
      to: "hooks/use-hr-headcount.ts"
      via: "wired section consumes the hook's props bag + flags; renders the breakdown views"
      pattern: "use-hr-headcount"
    - from: "dashboard-routes.tsx"
      to: "pages/dashboard/hr.tsx"
      via: "lazy route dashboard/hr inside the staff shell, flag-gated in nav"
      pattern: "dashboard/hr"
---

<objective>
Build the staff HR dashboard shell + the headcount (HR-DASH-01) and vacation-utilization (HR-DASH-02) UI on the `hrDashboard` procedures (97-03). Mirror the reports.tsx page/widget/hook idiom and the `KpiCard`/`HeadcountStat` primitives; follow the web-vite layering (page → wired section → hook = sole tRPC boundary → presentational view) exactly per ARCHITECTURE.md. Wire the route into `dashboard-routes.tsx` and a flag+RBAC-gated nav entry so only HR roles see it. Every section renders loading/empty/degraded states (a dark source surface → real empty card). No doc-expiry/probation/Gulf widgets (97-07), no i18n-parity backfill (97-08 does the full 5-locale sweep — but every string is a key from the start).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-CONTEXT.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-RESEARCH.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-VALIDATION.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
- `apps/web-vite/src/pages/dashboard/reports.tsx:22-106` — the thin-page + widget-wrapper + `use-*-report` hook idiom to mirror (a `*PageContent` composing wired sections).
- `apps/web-vite/src/components/dashboard/kpi-cards.tsx` + `apps/web-vite/src/components/compliance/dashboard/compliance-kpi-cards.tsx` — the `KpiCard` primitive for the header.
- `apps/web-vite/src/components/saudization/saudization-dashboard.tsx` (`HeadcountStat`) — the headcount-stat presentation to reuse.
- `apps/web-vite/src/router/dashboard-routes.tsx:14-125` — where to register the lazy `dashboard/hr` route.
- `apps/web-vite/src/lib/nav-items.tsx` + `apps/web-vite/src/lib/navigation.ts` + `apps/web-vite/src/i18n/navigation.tsx` — the nav entry + label (flag+RBAC-gated).
- `apps/web-vite/src/hooks/use-permissions.ts` (`can('employee', ['read'])`) + the `useFlag` hook (`@contractor-ops/feature-flags`) — the nav + page gate.
- `97-RESEARCH.md` § Widget Gating Map, § Reuse Patterns 7; `ARCHITECTURE.md` (layering + `check:*` gates).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: route + page shell + flag/RBAC-gated nav</name>
  <files>apps/web-vite/src/router/dashboard-routes.tsx, apps/web-vite/src/pages/dashboard/hr.tsx, apps/web-vite/src/lib/nav-items.tsx, apps/web-vite/src/lib/navigation.ts</files>
  <read_first>
    - `apps/web-vite/src/pages/dashboard/reports.tsx:100-106` + `apps/web-vite/src/router/dashboard-routes.tsx` + `apps/web-vite/src/lib/nav-items.tsx`
    - `apps/web-vite/ARCHITECTURE.md` (Page rules)
  </read_first>
  <action>
    Add a lazy `dashboard/hr` route → `HrDashboardPage`. Create `pages/dashboard/hr.tsx`: `HrDashboardPage` = `Suspense` + `HrDashboardPageContent`; `HrDashboardPageContent` reads `useFlag('module.hr-dashboard')` + `usePermissions().can('employee', ['read'])` and `<Navigate to="/${locale}/unauthorized" replace />` when denied, otherwise composes `<HrDashboardHeader/>` + `<HrHeadcountSection/>` + `<HrUtilizationSection/>` (97-07 adds the rest). The page uses `useTranslations`/`usePermissions`/`useFlag`/`useParams` ONLY — no `useTRPC`/`useQuery`. Add the nav entry (label key `Navigation.hr`) gated on the same flag + `employee:read` so non-HR roles never render it.
  </action>
  <verify>
    <automated>pnpm check:web-vite-page-shells 2>&1 | tail -5 && pnpm --filter @contractor-ops/web-vite test src/lib/__tests__/navigation.test.ts 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/hr` route + page render inside the staff shell; the nav entry is flag+`employee:read`-gated.
    - The page is a thin composer (no tRPC); `check:web-vite-page-shells` passes.
  </acceptance_criteria>
  <done>The HR dashboard has a gated route + nav entry; only HR roles reach it.</done>
</task>

<task type="auto">
  <name>Task 2: KPI header + headcount section (HR-DASH-01)</name>
  <files>apps/web-vite/src/components/hr-dashboard/hooks/use-hr-summary.ts, apps/web-vite/src/components/hr-dashboard/hooks/use-hr-headcount.ts, apps/web-vite/src/components/hr-dashboard/hr-dashboard-header.tsx, apps/web-vite/src/components/hr-dashboard/hr-headcount-section.tsx</files>
  <read_first>
    - `apps/web-vite/src/components/dashboard/kpi-cards.tsx` + `apps/web-vite/src/components/saudization/saudization-dashboard.tsx` (`HeadcountStat`) + `apps/web-vite/src/components/reports/hooks/use-expiring-contracts-report.ts` (hook shape)
    - `apps/web-vite/ARCHITECTURE.md` (Hook return shape)
  </read_first>
  <action>
    `use-hr-summary.ts` (sole tRPC boundary): `useTRPC` → `hrDashboard.getSummary` → returns `{ isLoading, isEmpty, kpiProps }`. `hr-dashboard-header.tsx`: wired `HrDashboardHeader` branches loading/empty over a presentational `HrDashboardHeaderView` rendering four `KpiCard`s (total headcount, under-utilized, probation-due, expiring-docs). `use-hr-headcount.ts`: `hrDashboard.getHeadcount` → props bag with the breakdown arrays. `hr-headcount-section.tsx`: wired `HrHeadcountSection` branches loading/empty (no employees → real empty card) over `HrHeadcountView` rendering the by-department / by-jurisdiction / by-employment-type / by-contract-end breakdowns (reuse `HeadcountStat` / a small bar/table). All copy = `HrDashboard.*` keys; WCAG semantics (headings, `aria-label`s); entrance animation via `AnimateIn`.
  </action>
  <verify>
    <automated>pnpm check:web-vite-data-layer 2>&1 | tail -5 && pnpm check:web-vite-presentational 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Hooks are the only tRPC boundary; views are props-only; sections own loading/empty (`check:web-vite-data-layer` + `check:web-vite-presentational` pass).
    - The empty state renders when there are no employees (D-04); no hardcoded strings.
  </acceptance_criteria>
  <done>HR-DASH-01 headcount + the KPI header are on screen, layered per ARCHITECTURE.md.</done>
</task>

<task type="auto">
  <name>Task 3: vacation-utilization section (HR-DASH-02)</name>
  <files>apps/web-vite/src/components/hr-dashboard/hooks/use-hr-utilization.ts, apps/web-vite/src/components/hr-dashboard/hr-utilization-section.tsx</files>
  <read_first>
    - `apps/web-vite/src/components/leave/leave-balance-card.tsx` (minutes→days formatting) + ARCHITECTURE.md (canonical `DataTable`)
  </read_first>
  <action>
    `use-hr-utilization.ts`: `hrDashboard.getVacationUtilization` → props bag (per-worker taken/entitled/unused + the under-utilized subset). `hr-utilization-section.tsx`: wired `HrUtilizationSection` branches loading / empty ("Leave tracking not enabled / no balances yet" degraded card when the source is dark — D-04) over `HrUtilizationView`. Render the under-utilized workers prominently (>10 unused near year-end) + a per-worker table via the canonical `DataTable` (client pagination — full set loaded). Days formatting reuses the leave minutes→days helper. All copy `HrDashboard.*`; WCAG.
  </action>
  <verify>
    <automated>pnpm check:web-vite-data-layer && pnpm check:web-vite-presentational 2>&1 | tail -5 && pnpm --filter @contractor-ops/web-vite test src/components/hr-dashboard 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - The section renders loading/empty/degraded; the under-utilized subset is surfaced; the table uses the canonical `DataTable` (no raw shadcn table).
    - Layering checks pass; no hardcoded strings.
  </acceptance_criteria>
  <done>HR-DASH-02 vacation utilization + under-utilization is on screen with a real degraded state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Nav/page render → RBAC | The nav entry + page gate on `employee:read` + `module.hr-dashboard`; a non-HR user neither sees nor reaches the route (defense-in-depth over the server gate). |
| Hook → tRPC | The single hook boundary keeps the surface auditable; the server remains the authority (the UI gate is UX, not the security boundary). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-06-01 | Information Disclosure | a non-HR user reaching `dashboard/hr` directly | mitigate | The page `<Navigate>`s on `!can('employee',['read'])`; the server procedures independently return FORBIDDEN (97-03). |
| T-97-06-02 | Information Disclosure | a widget crashing on a dark source surface and leaking a stack | mitigate | Every section renders a real empty/degraded state (D-04); no unguarded render. |
</threat_model>

<verification>
- `pnpm check:web-vite-data-layer && pnpm check:web-vite-page-shells && pnpm check:web-vite-presentational` all pass.
- `pnpm --filter @contractor-ops/web-vite test src/components/hr-dashboard` (SCOPED — never full suite) green.
- `pnpm typecheck --filter=@contractor-ops/web-vite` green; `pnpm lint:no-breadcrumbs` + `pnpm standards:check` green.
- Wiki: `structure/web-vite-domains.md` (hr-dashboard domain) + the HR-dashboard domain page (UI surface / entry points) updated in this change set.
</verification>

<success_criteria>
`dashboard/hr` renders the KPI header + headcount (HR-DASH-01) + vacation-utilization (HR-DASH-02) on the `hrDashboard` procedures, layered page→section→hook→view per ARCHITECTURE.md, flag+RBAC-gated in nav, with loading/empty/degraded states and i18n keys throughout. `check:web-vite-*` all pass. Doc-expiry/probation/Gulf widgets are 97-07.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-06-SUMMARY.md` when done.
</output>
