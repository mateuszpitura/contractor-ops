---
phase: 97-theme-b-hr-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - packages/api/src/routers/workforce/__tests__/hr-dashboard-fixtures.ts
  - packages/api/src/routers/workforce/__tests__/hr-dashboard-rbac.test.ts
  - packages/api/src/routers/workforce/__tests__/hr-dashboard-org-scope.test.ts
  - packages/api/src/routers/workforce/__tests__/hr-dashboard-section-grain.test.ts
  - packages/api/src/routers/workforce/__tests__/hr-dashboard-gating.test.ts
  - packages/api/src/routers/workforce/__tests__/hr-dashboard-headcount.test.ts
  - packages/api/src/routers/workforce/__tests__/hr-dashboard-utilization.test.ts
  - packages/api/src/routers/workforce/__tests__/hr-dashboard-probation.test.ts
  - packages/api/src/services/__tests__/hr-dashboard-doc-expiry.test.ts
autonomous: true
requirements: [HR-DASH-01, HR-DASH-02, HR-DASH-03, HR-DASH-04, HR-DASH-05]

must_haves:
  truths:
    - "The HR-role RBAC fence exists and is RED: a `finance_admin`/`ops_manager`/`readonly` caller gets `FORBIDDEN` on every `hrDashboard.*`; `hr_admin`/`hr_manager`/`payroll_officer`/`leave_approver`/`owner` pass the gate (the load-bearing test — RESEARCH § The Load-Bearing Decision Option A)"
    - "The cross-org fence exists and is RED: an org-B session's headcount/utilization/expiry/probation aggregates never include an org-A row; no client-supplied org id is trusted"
    - "The per-section doc-expiry grain test exists and is RED: `getDocumentExpiry` returns only the sections the caller can `employeeFile{A..D}:read` — `payroll_officer` sees C only, `leave_approver` A only, `hr_admin` all four (RESEARCH C7)"
    - "The dark-flag gating, headcount-sums-to-total, utilization-from-cache, probation-window, and doc-expiry-band tests exist and are RED (they reference the not-yet-built `hrDashboard` router / helpers)"
    - "The RED scaffolds do not brick `tsc` — `packages/api` already excludes `src/**/__tests__/**`; a shared fixture builds a two-org, multi-role, multi-department, mixed-section-document, probation-window, leave-balance world"
  artifacts:
    - path: "packages/api/src/routers/workforce/__tests__/hr-dashboard-rbac.test.ts"
      provides: "the load-bearing HR-role fence (RED)"
      contains: "FORBIDDEN"
    - path: "packages/api/src/routers/workforce/__tests__/hr-dashboard-fixtures.ts"
      provides: "two-org, multi-role, multi-department, mixed-section, probation, leave-balance fixture"
      contains: "makeHrDashboardFixture"
  key_links:
    - from: "hr-dashboard-rbac.test.ts"
      to: "hrDashboard router (97-03/04/05)"
      via: "constructs callers under each role and asserts pass/FORBIDDEN; fails until the router lands"
      pattern: "hrDashboard"
---

<objective>
Seed the RED net — the security-first failing tests Wave 3 flips GREEN — BEFORE any `hrDashboard` procedure exists. The load-bearing test is the **HR-role RBAC fence**: only `employee:read` (owner + the four snake_case HR roles) reaches the dashboard; `finance_admin`/`ops_manager`/`readonly` get `FORBIDDEN` (RESEARCH § The Load-Bearing Decision, C5). Alongside it: the cross-org aggregation fence, the per-section doc-expiry grain (C7), dark-flag gating, and the aggregation-correctness proofs (headcount sums to total; utilization taken/entitled matches the `LeaveBalance` cache; probation 14/7/0 window boundaries; doc-expiry bands via `daysUntilExpiryInTz`).

Because the widgets are aggregates, **aggregation correctness is a security property** — a wrong `where` is both a bug and a cross-tenant leak, so the correctness tests sit in the same RED net as the RBAC fence.

Purpose: make the invariants executable and RED so an executor cannot mark Wave 3 done without them going GREEN. These tests reference the not-yet-built `hrDashboard` router + the `deriveEmployeeDocExpiry`/`deriveProbationWatchlist` helpers (RED via missing code); `packages/api` excludes `__tests__` from `tsc`, so they do not brick the package typecheck. No product code in this plan.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-CONTEXT.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-RESEARCH.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-VALIDATION.md

<interfaces>
- `packages/api/src/routers/__tests__/dashboard.test.ts` — the existing tRPC caller-factory + DB fixture harness for a rate-limited dashboard read (mirror the caller construction).
- `packages/api/src/middleware/rbac.ts` (`requirePermission`) + `packages/auth/src/roles.ts:198-243` — the role→permission grants the RBAC fence asserts (owner + hr_admin/hr_manager/payroll_officer/leave_approver hold `employee:read`; no other role does).
- `packages/api/src/routers/core/personnel-file/section-access.ts` — the `employeeFile{A..D}` grant lookup the section-grain test asserts against.
- `packages/db/prisma/schema/leave.prisma:105-123` — `LeaveBalance` shape the utilization test seeds + asserts against.
- `packages/compliance-policy/src/expiry.ts:42-50` — `daysUntilExpiryInTz` the doc-expiry band test uses as the oracle.
- `97-RESEARCH.md` § Validation Architecture, § The Load-Bearing Decision, § Corrections C5/C7.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared HR-dashboard test fixture</name>
  <files>packages/api/src/routers/workforce/__tests__/hr-dashboard-fixtures.ts</files>
  <read_first>
    - `packages/api/src/routers/__tests__/dashboard.test.ts` (caller + seed harness) + `hr-dashboard-fixtures` analog in the portal RED net (96-03 `portal-fixtures.ts`) for the fixture-factory shape
  </read_first>
  <action>
    Create `makeHrDashboardFixture()` seeding TWO orgs (A + B). In org A: employees across ≥2 departments, ≥2 `employmentType`s, ≥2 jurisdictions (`countryCode`), a mix of `employmentStatus` (incl. one TERMINATED that must be excluded from active headcount), workers with `probationEndsAt` at today / today+5 / today+12 / today+30 (window boundaries), `LeaveBalance` rows with known `entitledMinutes`/`usedMinutes`/`carryoverMinutes` (one under-utilized >10 days), and `PersonnelFileDocument` rows with `expiresAt` in each band (expired/30/60/90/none) across sections A/B/C/D. In org B: a disjoint employee + document set (for the cross-org fence). Export helpers to build a tRPC caller for each role (`owner`, `hr_admin`, `hr_manager`, `payroll_officer`, `leave_approver`, `finance_admin`, `ops_manager`, `readonly`) scoped to a given org. Provide an injectable `now` so the probation/expiry windows are deterministic.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api exec tsc --noEmit -p tsconfig.json 2>&1 | grep -c "hr-dashboard-fixtures" || echo "fixture excluded from tsc (expected)"</automated>
  </verify>
  <acceptance_criteria>
    - Two-org, multi-role, multi-department, mixed-section-document, probation-window, leave-balance world with a deterministic `now`.
    - Per-role caller factories exist; the fixture is pure setup (no assertions).
  </acceptance_criteria>
  <done>Every RED test shares one realistic, deterministic world.</done>
</task>

<task type="auto">
  <name>Task 2: RBAC fence + dark-flag gating (RED)</name>
  <files>packages/api/src/routers/workforce/__tests__/hr-dashboard-rbac.test.ts, packages/api/src/routers/workforce/__tests__/hr-dashboard-gating.test.ts</files>
  <read_first>
    - `packages/auth/src/roles.ts:198-243` + `97-RESEARCH.md` § The Load-Bearing Decision (Option A), § Corrections C5
  </read_first>
  <action>
    `hr-dashboard-rbac.test.ts`: for EACH `hrDashboard.*` procedure, assert `owner`/`hr_admin`/`hr_manager`/`payroll_officer`/`leave_approver` are allowed (no `FORBIDDEN`) and `finance_admin`/`ops_manager`/`readonly` throw `FORBIDDEN`. This is the load-bearing test — it references the not-yet-built router (RED).
    `hr-dashboard-gating.test.ts`: assert the `hrDashboard` namespace is absent from the router (per-procedure `METHOD_NOT_FOUND`) when `module.hr-dashboard` is OFF, and also when `module.workforce-employees` is OFF (the data prerequisite) — mirror the workforce dark-mount regression style.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hr-dashboard-rbac hr-dashboard-gating 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Both files RED (reference the missing router).
    - The role allow/deny matrix matches RESEARCH C5 exactly (no non-HR role passes).
  </acceptance_criteria>
  <done>The HR-role fence + dark-flag gating are executable and RED.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-org fence + per-section doc-expiry grain (RED)</name>
  <files>packages/api/src/routers/workforce/__tests__/hr-dashboard-org-scope.test.ts, packages/api/src/routers/workforce/__tests__/hr-dashboard-section-grain.test.ts</files>
  <read_first>
    - `packages/api/src/routers/core/personnel-file/section-access.ts` + `97-RESEARCH.md` § Corrections C7
  </read_first>
  <action>
    `hr-dashboard-org-scope.test.ts`: an org-B caller's `getHeadcount`/`getVacationUtilization`/`getDocumentExpiry`/`getProbationWatchlist`/`getNationalisationRollup` return ONLY org-B data — no org-A row appears in any total/bucket/window; a `.strict()` input rejects any injected org id.
    `hr-dashboard-section-grain.test.ts`: `getDocumentExpiry` under `payroll_officer` returns only section-C expiry rows (the section-B/A/D rows are absent); under `leave_approver` only section-A; under `hr_admin` all four; under `hr_manager` all four (C read-only does not hide C reads). Assert the filter is by the caller's `employeeFile{A..D}:read` grant, not by a hard-coded list.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hr-dashboard-org-scope hr-dashboard-section-grain 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Both RED; the org-scope test covers all five procedures; the section-grain test covers all four roles' distinct section visibility.
  </acceptance_criteria>
  <done>The cross-org leak fence + the personnel-section authz reuse are executable and RED.</done>
</task>

<task type="auto">
  <name>Task 4: Aggregation-correctness proofs (RED)</name>
  <files>packages/api/src/routers/workforce/__tests__/hr-dashboard-headcount.test.ts, packages/api/src/routers/workforce/__tests__/hr-dashboard-utilization.test.ts, packages/api/src/routers/workforce/__tests__/hr-dashboard-probation.test.ts, packages/api/src/services/__tests__/hr-dashboard-doc-expiry.test.ts</files>
  <read_first>
    - `packages/db/prisma/schema/leave.prisma:105-123` + `packages/compliance-policy/src/expiry.ts:42-50` + `97-RESEARCH.md` § Reuse Patterns (2,3,5)
  </read_first>
  <action>
    `hr-dashboard-headcount.test.ts`: each breakdown (by department / jurisdiction / employment-type) sums to the total ACTIVE count; a TERMINATED worker is excluded; a null `department` lands in an "unspecified" bucket (not dropped).
    `hr-dashboard-utilization.test.ts`: `taken` = `usedMinutes`, `entitled` = `entitledMinutes + carryoverMinutes` per worker-year from the `LeaveBalance` cache; the >10-unused-days flag fires for the seeded under-utilized worker ONLY inside the year-end window (assert with `now` set inside vs outside the window).
    `hr-dashboard-probation.test.ts`: the 14/7/0 buckets partition the window at the TZ start-of-day boundary — the today / +5 / +12 workers land in 0 / ≤7 / ≤14 respectively, the +30 worker is absent; TERMINATED excluded.
    `hr-dashboard-doc-expiry.test.ts`: `deriveEmployeeDocExpiry(rows, now)` buckets 90/60/30/expired using `daysUntilExpiryInTz` as the oracle (assert band boundaries); a null `expiresAt` row is excluded.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hr-dashboard-headcount hr-dashboard-utilization hr-dashboard-probation hr-dashboard-doc-expiry 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All four RED (reference the missing router/helpers).
    - Boundary cases (TERMINATED exclusion, null bucket, year-end window, TZ start-of-day, null-expiry exclusion) are each asserted.
  </acceptance_criteria>
  <done>Aggregation correctness — a security property here — is executable and RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Session role → dashboard access | The RBAC fence is the guarantee only owner + HR roles reach the surface; RED-tested per procedure. |
| Session org → aggregate rows | The cross-org fence is the guarantee no other org's row enters a total/bucket; RED-tested across all five procedures. |
| Caller section grants → document rows | The section grain is the guarantee a role never sees expiry for a section it cannot read; RED-tested across all four roles. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-02-01 | Elevation of Privilege | a non-HR role reading the HR dashboard | mitigate | `hr-dashboard-rbac.test.ts` proves `FORBIDDEN` for finance/ops/readonly; enforced by the `employee:read` gate in 97-03. |
| T-97-02-02 | Information Disclosure | a cross-org aggregate leak | mitigate | `hr-dashboard-org-scope.test.ts` proves org isolation across all five procedures; enforced by explicit `organizationId` in every `where` (97-03/04/05). |
| T-97-02-03 | Information Disclosure | a payroll/leave role seeing discipline (section B) expiry | mitigate | `hr-dashboard-section-grain.test.ts` proves section filtering; enforced by the `employeeFile{A..D}:read` filter in 97-04. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test hr-dashboard` — all nine RED files present and failing for the RIGHT reason (missing router/helpers, not a fixture bug).
- `pnpm typecheck --filter=@contractor-ops/api` green (the `__tests__` exclusion holds; RED scaffolds do not brick `tsc`).
- `pnpm lint:no-breadcrumbs` green (no phase/req IDs in test names or comments).
- No wiki change (test-only files are wiki-exempt); no `check:wiki-brain` obligation this plan.
</verification>

<success_criteria>
Nine RED test files exist: the HR-role RBAC fence (the load-bearing one), the cross-org fence, the per-section doc-expiry grain, dark-flag gating, and the four aggregation-correctness proofs — all failing because the `hrDashboard` router + helpers do not exist yet, none failing on a fixture bug. `packages/api` typecheck stays green. Wave 3 cannot be marked done until these go GREEN.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-02-SUMMARY.md` when done.
</output>
