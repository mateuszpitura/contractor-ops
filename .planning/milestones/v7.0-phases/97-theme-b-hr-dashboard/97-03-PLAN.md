---
phase: 97-theme-b-hr-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - packages/api/src/routers/workforce/hr-dashboard.ts
  - packages/api/src/services/hr-dashboard-utilization.ts
  - packages/api/src/root.ts
  - packages/validators/src/hr-dashboard.ts
autonomous: true
requirements: [HR-DASH-01, HR-DASH-02]

must_haves:
  truths:
    - "A new `hrDashboard` namespace is mounted inside `workforceRouters` (dark under `module.workforce-employees`) and every procedure runs through `hrDashboardProcedure = tenantProcedure.use(requirePermission({ employee: ['read'] })).use(assertHrDashboardEnabled).use(reportRateLimitMiddleware)` — the RBAC gate resolves to owner + the four HR roles ONLY (RESEARCH § The Load-Bearing Decision Option A)"
    - "`getHeadcount` returns { total, byDepartment, byJurisdiction, byEmploymentType, byContractEndBucket } from `employeeProfile.groupBy` scoped to `ctx.organizationId`, excluding TERMINATED from the active total, with null columns bucketed as 'unspecified' — the RED headcount + org-scope + RBAC + gating tests go GREEN"
    - "`getVacationUtilization` reads the `LeaveBalance` cache (taken = usedMinutes; entitled = entitledMinutes + carryoverMinutes) per worker-year and flags >10 unused days inside the year-end window — the RED utilization test goes GREEN; no ledger re-sum"
    - "Every procedure carries `report-rate-limit`; every `where` spells out `organizationId`; no client-supplied id is trusted; inputs are Zod `.strict()`"
    - "The utilization threshold logic lives in a PURE `hr-dashboard-utilization.ts` service (params in, structured out, no DB, no throws) mirroring `saudization-dashboard.ts`"
  artifacts:
    - path: "packages/api/src/routers/workforce/hr-dashboard.ts"
      provides: "hrDashboard router: getHeadcount + getVacationUtilization (+ getSummary composite)"
      contains: "hrDashboardProcedure"
    - path: "packages/api/src/services/hr-dashboard-utilization.ts"
      provides: "pure deriveVacationUtilization(balances, now) + under-utilization flag"
      contains: "deriveVacationUtilization"
  key_links:
    - from: "hr-dashboard.ts getHeadcount"
      to: "employeeProfile.groupBy (Prisma)"
      via: "by department/countryCode/employmentType/contractEndDate bucket, where organizationId, _count"
      pattern: "groupBy"
    - from: "root.ts workforceRouters"
      to: "hr-dashboard.ts hrDashboardRouter"
      via: "spread into the conditional workforce mount so it dark-mounts under module.workforce-employees"
      pattern: "hrDashboard"
---

<objective>
Build the `hrDashboard` router's headcount (HR-DASH-01) + vacation-utilization (HR-DASH-02) aggregate reads, flipping the RBAC / org-scope / dark-flag / headcount / utilization RED tests (97-02) GREEN. Mirror the staff dashboard aggregation idiom (`rateLimitedDashboardProcedure` + `groupBy`) but swap the gate `report:read → employee:read` (RESEARCH C5) so only owner + the four HR roles reach it, and mount inside `workforceRouters` so it dark-mounts under `module.workforce-employees` + re-asserts `module.hr-dashboard`.

Headcount is `employeeProfile.groupBy` over the columns 97-01 promoted; utilization reads the `LeaveBalance` materialized cache directly (no ledger re-sum) with the under-utilization threshold in a pure service mirroring `saudization-dashboard.ts`. No UI, no doc-expiry/probation (those are 97-04), no Emiratisation (97-05).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-CONTEXT.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-RESEARCH.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-VALIDATION.md

<interfaces>
- `packages/api/src/routers/core/dashboard.ts:34-90,327-339` — the `rateLimitedDashboardProcedure` shape + `$queryRaw`/`cachedSingleflight`/`readReplica` idiom to mirror (swap `report:read` → `employee:read`).
- `packages/api/src/routers/finance/invoice-crud.ts:509-533` — the `db.X.groupBy({ by, where: { organizationId }, _count })` idiom.
- `packages/api/src/middleware/report-rate-limit.ts:123` — `reportRateLimitMiddleware`.
- `packages/api/src/middleware/require-workforce-flag.ts` — `assertWorkforceEnabled`/`isWorkforceRegistered` (mirror for an `assertHrDashboardEnabled` re-assert of `module.hr-dashboard`).
- `packages/db/prisma/schema/leave.prisma:105-123` — `LeaveBalance` (`entitledMinutes`/`usedMinutes`/`carryoverMinutes`/`year`/`workerId`).
- `packages/api/src/services/saudization-dashboard.ts:1-19` — the pure-derivation service shape to mirror for `hr-dashboard-utilization.ts`.
- `packages/api/src/root.ts:186-198,264` — the `workforceRouters` object + `conditionalWorkforceRouters` mount.
- `97-RESEARCH.md` § Reuse Patterns 1/2, § Route + Namespace Inventory.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: hrDashboardProcedure + assertHrDashboardEnabled + module mount</name>
  <files>packages/api/src/routers/workforce/hr-dashboard.ts, packages/api/src/root.ts, packages/api/src/middleware/require-workforce-flag.ts</files>
  <read_first>
    - `packages/api/src/middleware/require-workforce-flag.ts` + `packages/api/src/routers/core/dashboard.ts:34-44` + `packages/api/src/root.ts:186-198`
    - `97-RESEARCH.md` § The Load-Bearing Decision (Option A), § Reuse Patterns 1/6
  </read_first>
  <action>
    Add `assertHrDashboardEnabled` (mirror `assertWorkforceEnabled`) that re-asserts `module.hr-dashboard` per request. In `hr-dashboard.ts`, define `const hrDashboardProcedure = tenantProcedure.use(requirePermission({ employee: ['read'] })).use(assertHrDashboardEnabled).use(reportRateLimitMiddleware);` and `export const hrDashboardRouter = router({ ... })`. Mount it inside the `workforceRouters` object in `root.ts` (so it dark-mounts under `module.workforce-employees` via the existing `conditionalWorkforceRouters` spread — no new conditional block) as `hrDashboard: hrDashboardRouter`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hr-dashboard-rbac hr-dashboard-gating 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - The RBAC fence + dark-flag gating RED tests go GREEN (finance/ops FORBIDDEN; namespace absent when either flag OFF).
    - The gate is `employee:read` (not `report:read`); the mount is inside `workforceRouters`.
  </acceptance_criteria>
  <done>The hrDashboard surface exists, HR-gated, dark-mounted, rate-limited.</done>
</task>

<task type="auto">
  <name>Task 2: getHeadcount (HR-DASH-01)</name>
  <files>packages/api/src/routers/workforce/hr-dashboard.ts, packages/validators/src/hr-dashboard.ts</files>
  <read_first>
    - `packages/api/src/routers/finance/invoice-crud.ts:509-533` + `97-RESEARCH.md` § Route + Namespace Inventory, § Reuse Patterns 1
  </read_first>
  <action>
    Add `getHeadcount: hrDashboardProcedure.input(z.object({}).strict()).query(...)` returning `{ total, byDepartment, byJurisdiction, byEmploymentType, byContractEndBucket }`. `total` = active count (`employmentStatus` in ACTIVE/ON_LEAVE/SUSPENDED, TERMINATED excluded). Each breakdown is an `employeeProfile.groupBy({ by: [col], where: { organizationId: ctx.organizationId, employmentStatus: { not: 'TERMINATED' } }, _count: { _all: true } })`; a null grouping key maps to an `'unspecified'` bucket (do not drop). `byContractEndBucket` buckets `contractEndDate` into {expired-or-past, ≤30d, ≤90d, later, none} via date arithmetic (TZ per org default). Define the `.strict()` input + result Zod types in `validators/src/hr-dashboard.ts`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hr-dashboard-headcount hr-dashboard-org-scope 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - Headcount buckets sum to the active total; TERMINATED excluded; null → 'unspecified'; org-scoped (RED headcount + org-scope tests GREEN).
    - No client-supplied id; `.strict()` input.
  </acceptance_criteria>
  <done>HR-DASH-01 headcount breakdown is live and correct.</done>
</task>

<task type="auto">
  <name>Task 3: getVacationUtilization (HR-DASH-02) + pure utilization service</name>
  <files>packages/api/src/services/hr-dashboard-utilization.ts, packages/api/src/routers/workforce/hr-dashboard.ts, packages/validators/src/hr-dashboard.ts</files>
  <read_first>
    - `packages/db/prisma/schema/leave.prisma:105-123` + `packages/api/src/services/saudization-dashboard.ts:1-19` + `97-RESEARCH.md` § Reuse Patterns 2
  </read_first>
  <action>
    Create pure `hr-dashboard-utilization.ts`: `deriveVacationUtilization(balances, now)` mapping each `{ workerId, year, entitledMinutes, usedMinutes, carryoverMinutes }` → `{ workerId, takenDays, entitledDays, unusedDays, underUtilized }` where `available = entitledMinutes + carryoverMinutes`, `taken = usedMinutes`, `unusedDays = (available - taken) / MINUTES_PER_WORK_DAY`, and `underUtilized = unusedDays > 10 && isWithinYearEndWindow(now)` (define the year-end window — e.g. the final N weeks of the leave year). No DB, no throws (mirror `saudization-dashboard.ts`). Add `getVacationUtilization: hrDashboardProcedure.input(z.object({ year: z.number().int().optional() }).strict()).query(...)` that reads `ctx.db.leaveBalance.findMany({ where: { organizationId: ctx.organizationId, year } })` and passes to the service. Return the per-worker list + a summary count of under-utilized workers.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hr-dashboard-utilization 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - taken/entitled equals the `LeaveBalance` cache; the >10-day flag fires only inside the year-end window (RED utilization test GREEN).
    - The threshold logic is pure + unit-covered; no ledger re-sum.
  </acceptance_criteria>
  <done>HR-DASH-02 vacation utilization + under-utilization flag is live and correct.</done>
</task>

<task type="auto">
  <name>Task 4: getSummary composite (optional KPI headline)</name>
  <files>packages/api/src/routers/workforce/hr-dashboard.ts, packages/validators/src/hr-dashboard.ts</files>
  <read_first>
    - `97-RESEARCH.md` § Route + Namespace Inventory (getSummary)
  </read_first>
  <action>
    Add `getSummary: hrDashboardProcedure.input(z.object({}).strict()).query(...)` returning the KPI-card headline numbers in one rate-limit-friendly call: `{ totalHeadcount, underUtilizedCount, probationDueCount, expiringDocCount }`. probationDueCount + expiringDocCount call into the 97-04 helpers (import; if 97-04 lands after, stub returns 0 and 97-04 wires them — note the ordering in the SUMMARY). Cache via `cachedSingleflight` (mirror `dashboard.kpis`) with a short burst TTL.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hr-dashboard 2>&1 | tail -15 && pnpm typecheck --filter=@contractor-ops/api 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `getSummary` returns the headline counts; org-scoped + HR-gated + rate-limited like the rest.
    - `packages/api` typecheck green.
  </acceptance_criteria>
  <done>The dashboard has a single-call KPI header source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Session → aggregate scope | `hrDashboardProcedure` (tenant + `employee:read` + flag + rate-limit) is the single gate; every `where` restates `organizationId`. |
| Client input → query shape | `.strict()` inputs reject any injected org/worker id; only optional bucket params accepted. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-03-01 | Information Disclosure | a groupBy returning cross-org counts | mitigate | Explicit `organizationId` in every `where`; RED org-scope test proves isolation (97-02). |
| T-97-03-02 | Elevation of Privilege | a non-HR role reading headcount | mitigate | `employee:read` gate (Task 1); RED RBAC test proves FORBIDDEN for non-HR. |
| T-97-03-03 | Denial of Service | aggregate flood | mitigate | `reportRateLimitMiddleware` shares the `report:${orgId}` budget (30/min); indexed columns (97-01). |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test hr-dashboard-rbac hr-dashboard-gating hr-dashboard-org-scope hr-dashboard-headcount hr-dashboard-utilization` all GREEN.
- `pnpm typecheck --filter=@contractor-ops/api --filter=@contractor-ops/validators` green.
- `pnpm lint:logs` (no `console.*`) + `pnpm lint:no-breadcrumbs` + `pnpm standards:check` green.
- Wiki: `structure/api-routers-catalog.md` (hrDashboard namespace) + `structure/key-services.md` (hr-dashboard-utilization) + the HR-dashboard domain page updated in this change set; verify `root.ts` mount reflected.
</verification>

<success_criteria>
`hrDashboard.getHeadcount` + `getVacationUtilization` (+ `getSummary`) are live inside `workforceRouters`, HR-gated (owner + 4 HR roles only), org-scoped, rate-limited, and correct (buckets sum to total; utilization matches the cache; >10-day flag windowed). The RBAC / dark-flag / org-scope / headcount / utilization RED tests are GREEN. No UI, no doc-expiry/probation, no Emiratisation.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-03-SUMMARY.md` when done.
</output>
