---
phase: 97-theme-b-hr-dashboard
plan: 04
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - packages/api/src/services/hr-dashboard-doc-expiry.ts
  - packages/api/src/services/hr-dashboard-probation.ts
  - packages/api/src/routers/workforce/hr-dashboard.ts
  - packages/validators/src/hr-dashboard.ts
autonomous: true
requirements: [HR-DASH-03, HR-DASH-04]

must_haves:
  truths:
    - "`getDocumentExpiry` composes the v6.0 F1 engine by running the PURE `@contractor-ops/compliance-policy` expiry math (`daysUntilExpiryInTz`, TZ from `PersonnelFile.countryCode`) over `PersonnelFileDocument.expiresAt` rows — it does NOT fork the reminder/TZ/digest logic (RESEARCH C4/D-02)"
    - "`getDocumentExpiry` filters each per-document row by the caller's `employeeFile{A|B|C|D}:read` grant so a `payroll_officer` sees only section-C expiry and a `leave_approver` only section-A — the RED section-grain test goes GREEN (RESEARCH C7)"
    - "`getProbationWatchlist` is a date-window query over the indexed `EmployeeProfile.probationEndsAt` (97-01), bucketed 14/7/0 at the TZ start-of-day boundary, TERMINATED excluded — the RED probation test goes GREEN (read-only widget, D-03; no reminder cron in v7.0)"
    - "Both procedures run through `hrDashboardProcedure` (97-03): `employee:read` gate + `assertHrDashboardEnabled` + `report-rate-limit`; every `where` spells out `organizationId`; `.strict()` inputs"
    - "The expiry-band + probation-window logic lives in PURE services (`hr-dashboard-doc-expiry.ts`, `hr-dashboard-probation.ts`) — params in, structured out, no DB, no throws"
  artifacts:
    - path: "packages/api/src/services/hr-dashboard-doc-expiry.ts"
      provides: "pure deriveEmployeeDocExpiry(docs, now) reusing compliance-policy + section-grain filter input"
      contains: "daysUntilExpiryInTz"
    - path: "packages/api/src/services/hr-dashboard-probation.ts"
      provides: "pure deriveProbationWatchlist(rows, now) 14/7/0 buckets"
      contains: "deriveProbationWatchlist"
  key_links:
    - from: "hr-dashboard-doc-expiry.ts"
      to: "@contractor-ops/compliance-policy daysUntilExpiryInTz"
      via: "TZ-correct expiry band over PersonnelFileDocument.expiresAt; no reminder-cron reuse"
      pattern: "daysUntilExpiryInTz"
    - from: "hr-dashboard.ts getDocumentExpiry"
      to: "employeeFile{A..D}:read grants"
      via: "row filtered out when the caller cannot read that document's section"
      pattern: "employeeFile"
---

<objective>
Build the `hrDashboard` document-expiry (HR-DASH-03) + probation-watchlist (HR-DASH-04) reads, flipping the section-grain + probation + doc-expiry RED tests (97-02) GREEN. HR-DASH-03 composes the v6.0 F1 engine the honest way (RESEARCH C4): the reusable F1 asset is the PURE `compliance-policy` date math (`daysUntilExpiryInTz`), not the contractor-only `compliance-reminder-scan` — so this runs that math over the `PersonnelFileDocument.expiresAt`/`docCategory` columns 97-01 added, with the TZ resolved from `PersonnelFile.countryCode`, and DROPS any document row whose `section` the caller cannot `employeeFile{A..D}:read` (the load-bearing per-section grain, C7). HR-DASH-04 is a read-only date-window over `probationEndsAt` bucketed 14/7/0 (D-03 — no reminder cron in v7.0). Both procedures reuse `hrDashboardProcedure` (97-03). No UI (97-07), no Emiratisation (97-05).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-CONTEXT.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-RESEARCH.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-VALIDATION.md

<interfaces>
- `packages/compliance-policy/src/expiry.ts:27-58` — `isExpired`/`daysUntilExpiryInTz`/`jurisdictionDate` (the pure expiry math to reuse); `policies/{de,ksa,pl,uae,uk,us}.ts` (per-jurisdiction TZ).
- `packages/db/prisma/schema/personnel.prisma:34-94` — `PersonnelFile.countryCode` (TZ source) + `PersonnelFileDocument.{expiresAt,docCategory,section}` (97-01).
- `packages/api/src/routers/core/personnel-file/section-access.ts` — the `employeeFile{A..D}` grant check to reuse for the section filter (do NOT reimplement the mapping).
- `packages/db/prisma/schema/employee.prisma` — `EmployeeProfile.probationEndsAt`/`employmentStatus` (97-01).
- `packages/api/src/routers/workforce/hr-dashboard.ts` (97-03) — `hrDashboardProcedure` to reuse.
- `97-RESEARCH.md` § Reuse Patterns 3/5, § Corrections C4/C7.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pure employee-doc-expiry service (compose F1 math)</name>
  <files>packages/api/src/services/hr-dashboard-doc-expiry.ts</files>
  <read_first>
    - `packages/compliance-policy/src/expiry.ts:27-58` + `97-RESEARCH.md` § Reuse Patterns 3, § Corrections C4
  </read_first>
  <action>
    Create pure `hr-dashboard-doc-expiry.ts`: `deriveEmployeeDocExpiry(docs, now)` where each doc = `{ documentId, expiresAt, docCategory, section, countryCode, workerDisplayName }`. For each row with a non-null `expiresAt`, compute `daysUntil = daysUntilExpiryInTz(expiresAt, tzForCountry(countryCode), now)` and bucket into `{ expired (<0), soon30 (0..30), soon60 (31..60), soon90 (61..90), later (>90) }`; drop null-`expiresAt` rows. Resolve `tzForCountry` from the compliance-policy per-jurisdiction TZ (reuse the policy TZ, do not hard-code). Return `{ byCategory, byBand, items }`. No DB, no throws. (The caller applies the section filter BEFORE calling this — see Task 2 — so this service is grant-agnostic and pure.)
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hr-dashboard-doc-expiry 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - Bands match `daysUntilExpiryInTz` boundaries (RED doc-expiry test GREEN); null `expiresAt` excluded; TZ from countryCode.
    - Pure (no Prisma/I/O); reuses compliance-policy, not compliance-reminder-scan.
  </acceptance_criteria>
  <done>Employee doc-expiry composes the F1 date math with zero fork of the reminder engine.</done>
</task>

<task type="auto">
  <name>Task 2: getDocumentExpiry procedure + section-grain filter (HR-DASH-03)</name>
  <files>packages/api/src/routers/workforce/hr-dashboard.ts, packages/validators/src/hr-dashboard.ts</files>
  <read_first>
    - `packages/api/src/routers/core/personnel-file/section-access.ts` + `97-RESEARCH.md` § Corrections C7, § Widget Gating Map
  </read_first>
  <action>
    Add `getDocumentExpiry: hrDashboardProcedure.input(z.object({}).strict()).query(...)`. Read `PersonnelFileDocument` rows for the org (`where: { organizationId: ctx.organizationId, expiresAt: { not: null }, deletedAt: null }`) joined to `PersonnelFile.countryCode` + the worker display name. BEFORE deriving bands, FILTER OUT any row whose `section` the caller cannot read — resolve the caller's `employeeFile{A..D}:read` grants via the existing section-access helper (reuse `hasSectionPermission`/the grant map; do not hard-code role→section). Pass the surviving rows to `deriveEmployeeDocExpiry`. Return its result. Define the `.strict()` input + result Zod types.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hr-dashboard-section-grain hr-dashboard-org-scope 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - payroll_officer sees only section-C expiry; leave_approver only A; hr_admin all four (RED section-grain test GREEN).
    - Org-scoped; section filter uses the existing grant machinery (C7), not a hard-coded list.
  </acceptance_criteria>
  <done>HR-DASH-03 document-expiry is live, composing F1 and respecting the per-section grain.</done>
</task>

<task type="auto">
  <name>Task 3: pure probation-watchlist service + getProbationWatchlist (HR-DASH-04)</name>
  <files>packages/api/src/services/hr-dashboard-probation.ts, packages/api/src/routers/workforce/hr-dashboard.ts, packages/validators/src/hr-dashboard.ts</files>
  <read_first>
    - `packages/db/prisma/schema/employee.prisma` (`probationEndsAt`/`employmentStatus`) + `97-RESEARCH.md` § Reuse Patterns 5, § Deferred Ideas (no cron)
  </read_first>
  <action>
    Create pure `hr-dashboard-probation.ts`: `deriveProbationWatchlist(rows, now)` bucketing each `{ workerId, probationEndsAt, displayName }` by `daysUntilExpiryInTz(probationEndsAt, tz, now)` into `{ dueToday (<=0), dueWithin7 (1..7), dueWithin14 (8..14) }`; rows past 14 days out are not in the watchlist. Add `getProbationWatchlist: hrDashboardProcedure.input(z.object({}).strict()).query(...)` that reads `employeeProfile.findMany({ where: { organizationId: ctx.organizationId, employmentStatus: { not: 'TERMINATED' }, probationEndsAt: { gte: startOfTodayTz, lte: addDays(startOfTodayTz, 14) } }, select: { workerId, probationEndsAt, worker: { select: { displayName } } } })` and passes to the service. Read-only — no notification/cron (D-03; Deferred Ideas).
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hr-dashboard-probation 2>&1 | tail -12 && pnpm typecheck --filter=@contractor-ops/api 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 14/7/0 buckets partition at the TZ start-of-day boundary; today → dueToday; +30 absent; TERMINATED excluded (RED probation test GREEN).
    - Pure service; read-only; no cron/notification; `packages/api` typecheck green.
  </acceptance_criteria>
  <done>HR-DASH-04 probation watchlist auto-surfaces 14/7/0-day workers, read-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Caller section grants → document rows | The `employeeFile{A..D}:read` filter is the section boundary; a section the caller cannot read never enters the expiry response. |
| Session org → doc/probation rows | Every `where` restates `organizationId`; no cross-org row surfaces. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-04-01 | Information Disclosure | a payroll/leave role seeing section-B (discipline) doc expiry | mitigate | The section filter (Task 2) drops rows outside the caller's grants; RED section-grain test proves it. |
| T-97-04-02 | Information Disclosure | probation/expiry rows leaking cross-org | mitigate | Explicit `organizationId` in every `where`; RED org-scope test proves isolation. |
| T-97-04-03 | Tampering | a forked expiry TZ diverging from F1 | mitigate | Reuse `compliance-policy` `daysUntilExpiryInTz` verbatim (Task 1) — one TZ source of truth. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test hr-dashboard-section-grain hr-dashboard-probation hr-dashboard-doc-expiry hr-dashboard-org-scope` all GREEN.
- `pnpm typecheck --filter=@contractor-ops/api --filter=@contractor-ops/validators` green.
- `pnpm lint:logs` + `pnpm lint:no-breadcrumbs` + `pnpm standards:check` green.
- Wiki: `structure/api-routers-catalog.md` (getDocumentExpiry/getProbationWatchlist) + `structure/key-services.md` (hr-dashboard-doc-expiry / hr-dashboard-probation) + the HR-dashboard + compliance-document domain pages updated; `verify_with` on the compliance-policy reuse noted.
</verification>

<success_criteria>
`hrDashboard.getDocumentExpiry` composes the F1 `compliance-policy` expiry math over employee akta documents and filters by the caller's per-section grants; `getProbationWatchlist` surfaces 14/7/0-day workers read-only. Both HR-gated, org-scoped, rate-limited. The section-grain / probation / doc-expiry RED tests are GREEN. No UI, no reminder cron, no Emiratisation.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-04-SUMMARY.md` when done.
</output>
