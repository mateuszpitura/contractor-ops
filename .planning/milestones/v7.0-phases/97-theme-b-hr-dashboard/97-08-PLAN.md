---
phase: 97-theme-b-hr-dashboard
plan: 08
type: execute
wave: 5
depends_on: ["06", "07"]
files_modified:
  - apps/web-vite/src/i18n/messages/en/HrDashboard.json
  - apps/web-vite/src/i18n/messages/en-US/HrDashboard.json
  - apps/web-vite/src/i18n/messages/de/HrDashboard.json
  - apps/web-vite/src/i18n/messages/pl/HrDashboard.json
  - apps/web-vite/src/i18n/messages/ar/HrDashboard.json
  - .planning/brain/wiki/domains/hr-dashboard.md
  - .planning/brain/wiki/structure/api-routers-catalog.md
  - .planning/MEMORY.md
autonomous: true
requirements: [HR-DASH-01, HR-DASH-02, HR-DASH-03, HR-DASH-04, HR-DASH-05]

must_haves:
  truths:
    - "Every `HrDashboard.*` (and the new `Navigation.hr`) message key exists in all five locales — en, en-US, de, pl, ar — with the SAME key set; `pnpm i18n:parity` is GREEN and no hardcoded strings remain"
    - "Arabic renders RTL and German uses the formal-Sie register; de/pl/ar are machine-assisted and FLAGGED for native review (EXTERNAL-ENABLEMENT #9) — en/en-US are canonical"
    - "Documentation-follows-code is complete in THIS change set: a new `wiki/domains/hr-dashboard.md` domain page + updates to `structure/{api-routers-catalog,key-services,web-vite-domains,prisma-schema-areas}.md` + `patterns/feature-flags.md` + `wiki/{log.md,hot.md}` + `MEMORY.md`, with `source_commit` bumped on touched pages"
    - "The graphify graph + BM25 index are rebuilt; `pnpm check:wiki-brain` is GREEN (no NEW drift for the phase's source files)"
  artifacts:
    - path: "apps/web-vite/src/i18n/messages/ar/HrDashboard.json"
      provides: "Arabic (RTL) HR-dashboard copy — machine-assisted, native-review-flagged"
      contains: "headcount"
    - path: ".planning/brain/wiki/domains/hr-dashboard.md"
      provides: "the HR-dashboard domain page (purpose / flow / entry points / UI surface / agent mistakes)"
      contains: "hrDashboard"
  key_links:
    - from: "wiki/domains/hr-dashboard.md"
      to: "packages/api/src/routers/workforce/hr-dashboard.ts + apps/web-vite/src/pages/dashboard/hr.tsx"
      via: "verify_with paths anchoring the domain page to the router + page"
      pattern: "verify_with"
---

<objective>
Close the phase with 5-locale i18n parity and documentation-follows-code — the mandatory same-change-set wiki/index/graph update for every product change (CLAUDE.md § Documentation follows code). Fill `HrDashboard.*` (+ `Navigation.hr`) for en / en-US / de / pl / ar at full key parity (RTL for ar, formal-Sie for de; de/pl/ar flagged for native review). Author the HR-dashboard domain wiki page and update every structure/pattern index the phase touched (routers catalog, key services, web-vite domains, prisma schema areas, feature flags), append the new invariants to MEMORY, refresh `log.md`/`hot.md`, and rebuild the graphify graph + BM25 index so `pnpm check:wiki-brain` is GREEN.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-CONTEXT.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-RESEARCH.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-VALIDATION.md

<interfaces>
- `apps/web-vite/src/i18n/messages/{en,en-US,de,pl,ar}/` — the per-locale message dirs; add `HrDashboard.json` (mirror a sibling namespace's key structure) + the `Navigation.hr` key in the existing nav namespace.
- `apps/web-vite/src/i18n/navigation.tsx` — the nav label wiring (97-06 added the entry; ensure the label key resolves in all locales).
- `.planning/brain/wiki/domains/_index.md` + a sibling domain page (e.g. the leave/personnel domain) — the domain-page template + index to extend.
- `.planning/brain/wiki/structure/{api-routers-catalog,key-services,web-vite-domains,prisma-schema-areas}.md` + `patterns/feature-flags.md` — the indexes to update.
- CLAUDE.md § Documentation follows code (the trigger matrix + the refresh pipeline commands) + `.planning/brain/wiki/meta/refresh-triggers.md`.
- `97-RESEARCH.md` § full (the corrections + RBAC + gap findings to record in MEMORY + the domain page's "agent mistakes").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5-locale i18n parity (en / en-US / de / pl / ar)</name>
  <files>apps/web-vite/src/i18n/messages/en/HrDashboard.json, apps/web-vite/src/i18n/messages/en-US/HrDashboard.json, apps/web-vite/src/i18n/messages/de/HrDashboard.json, apps/web-vite/src/i18n/messages/pl/HrDashboard.json, apps/web-vite/src/i18n/messages/ar/HrDashboard.json</files>
  <read_first>
    - a sibling namespace JSON in each locale dir (for structure) + `apps/web-vite/src/i18n/navigation.tsx`
    - EXTERNAL-ENABLEMENT #9 (native review deferral)
  </read_first>
  <action>
    Author `HrDashboard.json` in `en` (canonical) covering every string the 97-06/07 UI uses: title, KPI labels, headcount breakdown labels, utilization + under-utilization copy, doc-expiry band/category labels, probation severity labels, nationalisation (KSA/UAE) labels + the "record manual headcount" prompt, and all loading/empty/degraded/error copy. Copy the key set to `en-US` (US spelling/terms where they differ), `de` (formal Sie), `pl`, and `ar` (RTL; keep interpolation placeholders intact). Add the `Navigation.hr` label in each locale's nav namespace. Machine-assist de/pl/ar; add a native-review flag note in the phase SUMMARY (EXTERNAL-ENABLEMENT #9). Ensure identical key sets across all five (parity is a gate).
  </action>
  <verify>
    <automated>pnpm i18n:parity 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm i18n:parity` GREEN; identical key sets across en/en-US/de/pl/ar; no hardcoded strings in the 97-06/07 components.
    - ar interpolation placeholders intact (RTL); de uses Sie.
  </acceptance_criteria>
  <done>The HR dashboard is fully localized at 5-locale parity; de/pl/ar flagged for native review.</done>
</task>

<task type="auto">
  <name>Task 2: HR-dashboard domain wiki page + structure/pattern indexes</name>
  <files>.planning/brain/wiki/domains/hr-dashboard.md, .planning/brain/wiki/domains/_index.md, .planning/brain/wiki/structure/api-routers-catalog.md, .planning/brain/wiki/structure/key-services.md, .planning/brain/wiki/structure/web-vite-domains.md, .planning/brain/wiki/structure/prisma-schema-areas.md, .planning/brain/wiki/patterns/feature-flags.md</files>
  <read_first>
    - a sibling `wiki/domains/*.md` (frontmatter + section shape) + `wiki/domains/_index.md`
    - CLAUDE.md § Documentation follows code (trigger matrix)
  </read_first>
  <action>
    Create `wiki/domains/hr-dashboard.md`: Purpose (STAFF read-only HR command surface), Flow (hrDashboard procedures → web-vite dashboard/hr), Entry points (`packages/api/src/routers/workforce/hr-dashboard.ts`, `apps/web-vite/src/pages/dashboard/hr.tsx`), UI surface (the five widget groups), and **Agent mistakes** (record the corrections: P90 promoted only saudizationCategory/etat/employmentStatus — dept/type/contract-end/probation/doc-expiry were ADDED in 97-01; the gate is `employee:read` NOT `report:read`; the doc-expiry composes compliance-policy math, NOT compliance-reminder-scan; the Emiratisation rate is manual-input only). Frontmatter with `verify_with` anchoring the router + page + services + schema. Add it to `_index.md`. Update `api-routers-catalog.md` (the `hrDashboard` namespace + procedures), `key-services.md` (hr-dashboard-utilization / doc-expiry / probation + saudization per-country), `web-vite-domains.md` (the hr-dashboard component tree), `prisma-schema-areas.md` (EmployeeProfile promoted columns + PersonnelFileDocument expiry), `patterns/feature-flags.md` (module.hr-dashboard).
  </action>
  <verify>
    <automated>test -f .planning/brain/wiki/domains/hr-dashboard.md && grep -c "hrDashboard\|hr-dashboard" .planning/brain/wiki/structure/api-routers-catalog.md .planning/brain/wiki/structure/web-vite-domains.md</automated>
  </verify>
  <acceptance_criteria>
    - The domain page exists with `verify_with` anchors + the "agent mistakes" corrections; `_index.md` links it.
    - All five structure/pattern indexes reflect the new router/services/columns/flag.
  </acceptance_criteria>
  <done>The wiki tracks the HR-dashboard code; the next agent won't re-hallucinate the promoted-column or RBAC facts.</done>
</task>

<task type="auto">
  <name>Task 3: MEMORY + log/hot + graph/BM25 refresh</name>
  <files>.planning/MEMORY.md, .planning/brain/wiki/log.md, .planning/brain/wiki/hot.md</files>
  <read_first>
    - `.planning/MEMORY.md` (append format) + CLAUDE.md § Documentation follows code (refresh pipeline commands) + `.planning/brain/wiki/meta/refresh-triggers.md`
  </read_first>
  <action>
    Append to `MEMORY.md` the phase invariants: (1) HR-dashboard RBAC gate = `requirePermission({ employee: ['read'] })` (owner + the four snake_case HR roles; NOT `report:read`); (2) HR-DASH aggregation columns (dept/employmentType/contractEndDate/probationEndsAt/doc expiresAt+docCategory) were ADDED in 97-01 — they were NOT P90-promoted; (3) HR-DASH-03 composes `compliance-policy` pure math, not the contractor `compliance-reminder-scan`; (4) Emiratisation rate is manual-input only (F3 anti-feature). Update `wiki/log.md` (the change entry) + overwrite `wiki/hot.md`. Bump `source_commit` on touched wiki frontmatter. Then run the refresh pipeline: rebuild the graphify graph (`rm -f graphify-out/graph.json && graphify update . --no-cluster --force && cp graphify-out/graph.json .planning/graphs/graph.json`) and the BM25 index (`cd .planning/brain && find wiki -name '*.md' -exec python3 scripts/contextual-prefix.py {} --no-llm \; && python3 scripts/bm25-index.py build`).
  </action>
  <verify>
    <automated>pnpm check:wiki-brain 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check:wiki-brain` GREEN (no NEW drift for the phase's source files vs branch base).
    - MEMORY carries the four invariants; log/hot refreshed; graph + BM25 rebuilt.
  </acceptance_criteria>
  <done>The knowledge base + graph + BM25 track the phase; the doc-drift gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Machine-translated copy → production | de/pl/ar are machine-assisted; parity (not quality) is the gate; native review is a flagged post-deploy step (EXTERNAL-ENABLEMENT #9) — no legal/quality claim ships on the unreviewed copy. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-08-01 | Information Disclosure | a mistranslated RTL string clipping/overflowing a sensitive number | mitigate | The manual RTL/Sie visual pass (97-VALIDATION Manual-Only) confirms rendering before EU GA; parity guarantees the keys exist. |
</threat_model>

<verification>
- `pnpm i18n:parity` GREEN; `pnpm check:wiki-brain` GREEN.
- `pnpm typecheck --filter=@contractor-ops/web-vite` green; `pnpm lint:no-breadcrumbs` + `pnpm standards:check` green.
- The graphify graph (`.planning/graphs/graph.json`) + BM25 index rebuilt; `wiki/domains/hr-dashboard.md` present in `_index.md`.
- No source-code change in this plan beyond i18n JSON (wiki-exempt gate applies to the docs; the i18n JSON is the product change the wiki tracks).
</verification>

<success_criteria>
`HrDashboard.*` + `Navigation.hr` are at full 5-locale parity (RTL ar, Sie de; de/pl/ar native-review-flagged) with no hardcoded strings; the HR-dashboard domain wiki page + all touched structure/pattern indexes + MEMORY + log/hot are updated in this change set; the graph + BM25 are rebuilt and `pnpm check:wiki-brain` is GREEN. Phase 97 is documentation-complete.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-08-SUMMARY.md` when done.
</output>
