---
phase: 97-theme-b-hr-dashboard
plan: 07
type: execute
wave: 4
depends_on: ["04", "05"]
files_modified:
  - apps/web-vite/src/pages/dashboard/hr.tsx
  - apps/web-vite/src/components/hr-dashboard/hooks/use-hr-doc-expiry.ts
  - apps/web-vite/src/components/hr-dashboard/hooks/use-hr-probation.ts
  - apps/web-vite/src/components/hr-dashboard/hooks/use-hr-nationalisation.ts
  - apps/web-vite/src/components/hr-dashboard/hr-doc-expiry-section.tsx
  - apps/web-vite/src/components/hr-dashboard/hr-probation-section.tsx
  - apps/web-vite/src/components/hr-dashboard/hr-nationalisation-section.tsx
autonomous: true
requirements: [HR-DASH-03, HR-DASH-04, HR-DASH-05]

must_haves:
  truths:
    - "The document-expiry section (HR-DASH-03) renders the 90/60/30/expired bands by category, showing ONLY the sections the caller can read (the server already filters — the UI never assumes a full set); a caller with no readable expiry docs sees a real empty card"
    - "The probation section (HR-DASH-04) renders the 14/7/0-day watchlist with clear severity (0 = due today) and an empty card when nothing is due"
    - "The Gulf nationalisation section (HR-DASH-05) renders the KSA + UAE rollups side-by-side reusing the `HeadcountStat` idiom; when a country has no manual headcount it shows the 'record manual headcount' prompt (never a platform-derived rate — RESEARCH C6)"
    - "Each section is a wired section over a `use-*.ts` hook (sole tRPC boundary) + a presentational `*View`; the page composes them after the 97-06 sections; `check:web-vite-*` pass; all copy is `HrDashboard.*` keys; WCAG + loading/empty/error"
  artifacts:
    - path: "apps/web-vite/src/components/hr-dashboard/hr-doc-expiry-section.tsx"
      provides: "wired HrDocExpirySection + HrDocExpiryView (bands by category)"
      contains: "HrDocExpirySection"
    - path: "apps/web-vite/src/components/hr-dashboard/hooks/use-hr-nationalisation.ts"
      provides: "KSA + UAE rollup hook (sole tRPC boundary)"
      contains: "getNationalisationRollup"
  key_links:
    - from: "hr-probation-section.tsx"
      to: "hooks/use-hr-probation.ts"
      via: "wired section renders 14/7/0 buckets from the hook props bag"
      pattern: "use-hr-probation"
    - from: "hr.tsx HrDashboardPageContent"
      to: "the three new sections"
      via: "composed after header/headcount/utilization on the one HR screen"
      pattern: "HrDocExpirySection"
---

<objective>
Complete the HR dashboard UI with the document-expiry (HR-DASH-03), probation-watchlist (HR-DASH-04), and Saudization/Emiratisation rollup (HR-DASH-05) sections on the 97-04 + 97-05 procedures. Same layering as 97-06 (page composes wired sections; each section over a single `use-*` hook boundary; presentational views; loading/empty/degraded states). The Gulf section must honor the F3 posture — show the "record manual headcount" prompt when a country has no manual number, NEVER a platform-derived rate (RESEARCH C6). The doc-expiry section renders whatever the server returns (already section-filtered — the UI must not assume it sees every section). Compose all three after the 97-06 sections on the single `dashboard/hr` screen.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-CONTEXT.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-RESEARCH.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-VALIDATION.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
- `apps/web-vite/src/pages/dashboard/hr.tsx` (97-06) — `HrDashboardPageContent` to compose the three new sections into.
- `apps/web-vite/src/components/saudization/saudization-dashboard.tsx` — the `HeadcountStat` + nationalisation-rate presentation to reuse for HR-DASH-05.
- `apps/web-vite/src/components/reports/expiring-contracts-report.tsx` — the expiry-band presentation precedent (F1 UI) to mirror for the employee doc-expiry bands.
- `apps/web-vite/src/components/hr-dashboard/hooks/*` (97-06) — the hook shape to mirror; canonical `DataTable` for the watchlists.
- `97-RESEARCH.md` § Widget Gating Map, § Corrections C6/C7.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: document-expiry section (HR-DASH-03)</name>
  <files>apps/web-vite/src/components/hr-dashboard/hooks/use-hr-doc-expiry.ts, apps/web-vite/src/components/hr-dashboard/hr-doc-expiry-section.tsx</files>
  <read_first>
    - `apps/web-vite/src/components/reports/expiring-contracts-report.tsx` + `97-RESEARCH.md` § Corrections C7, § Widget Gating Map
  </read_first>
  <action>
    `use-hr-doc-expiry.ts` (sole tRPC boundary): `hrDashboard.getDocumentExpiry` → props bag `{ byBand, byCategory, items, isEmpty }`. `hr-doc-expiry-section.tsx`: wired `HrDocExpirySection` branches loading / empty ("No documents with expiry dates") over `HrDocExpiryView` rendering the expired/30/60/90 bands grouped by category (visa/work-permit/contract-renewal/medical/training) with severity color + a `DataTable` of items. The UI renders ONLY what the server returns — it must NOT assume every section is present (the server section-filters per caller, C7); no client-side section logic.
  </action>
  <verify>
    <automated>pnpm check:web-vite-data-layer && pnpm check:web-vite-presentational 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Bands by category render with severity; empty card when no readable expiry docs; the UI trusts the server's (section-filtered) set.
    - Layering checks pass; no hardcoded strings.
  </acceptance_criteria>
  <done>HR-DASH-03 document-expiry is on screen, composing F1 and honoring the section grain.</done>
</task>

<task type="auto">
  <name>Task 2: probation-watchlist section (HR-DASH-04)</name>
  <files>apps/web-vite/src/components/hr-dashboard/hooks/use-hr-probation.ts, apps/web-vite/src/components/hr-dashboard/hr-probation-section.tsx</files>
  <read_first>
    - `apps/web-vite/src/components/hr-dashboard/hr-utilization-section.tsx` (97-06 wired-section shape) + ARCHITECTURE.md (canonical `DataTable`)
  </read_first>
  <action>
    `use-hr-probation.ts`: `hrDashboard.getProbationWatchlist` → `{ dueToday, dueWithin7, dueWithin14, isEmpty }`. `hr-probation-section.tsx`: wired `HrProbationSection` branches loading / empty ("No probation periods ending soon") over `HrProbationView` rendering the 14/7/0 buckets with severity (0 = due today, most urgent) and a per-worker `DataTable` (name + probation-end date + days-remaining). All copy `HrDashboard.*`; WCAG; `AnimateIn`.
  </action>
  <verify>
    <automated>pnpm check:web-vite-data-layer && pnpm check:web-vite-presentational 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 14/7/0 buckets with severity + empty card; canonical `DataTable`; layering checks pass; no hardcoded strings.
  </acceptance_criteria>
  <done>HR-DASH-04 probation watchlist is on screen with clear severity.</done>
</task>

<task type="auto">
  <name>Task 3: Gulf nationalisation section (HR-DASH-05) + page composition</name>
  <files>apps/web-vite/src/components/hr-dashboard/hooks/use-hr-nationalisation.ts, apps/web-vite/src/components/hr-dashboard/hr-nationalisation-section.tsx, apps/web-vite/src/pages/dashboard/hr.tsx</files>
  <read_first>
    - `apps/web-vite/src/components/saudization/saudization-dashboard.tsx` (`HeadcountStat` + rate presentation) + `97-RESEARCH.md` § Corrections C6, § Widget Gating Map
  </read_first>
  <action>
    `use-hr-nationalisation.ts`: `hrDashboard.getNationalisationRollup` → `{ ksa?, uae?, isEmpty }`. `hr-nationalisation-section.tsx`: wired `HrNationalisationSection` branches loading / empty over `HrNationalisationView` rendering the KSA + UAE rollups side-by-side (reuse `HeadcountStat`: total/national headcount, nationalisation rate, read-through band, iqama/visa rollup). When a country's rollup is absent (no manual headcount), render the "record manual headcount to see the nationalisation rate" prompt — NEVER a platform-derived rate (C6). Compose all three sections (this + doc-expiry + probation) into `HrDashboardPageContent` after the 97-06 sections.
  </action>
  <verify>
    <automated>pnpm check:web-vite-data-layer && pnpm check:web-vite-page-shells && pnpm check:web-vite-presentational 2>&1 | tail -5 && pnpm --filter @contractor-ops/web-vite test src/components/hr-dashboard 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - KSA + UAE side-by-side; the "record manual headcount" prompt shows when a country has no manual number (no fabricated rate — C6).
    - The full HR screen composes all five widget groups; `check:web-vite-*` pass.
  </acceptance_criteria>
  <done>HR-DASH-05 Gulf rollup is on screen; the whole HR dashboard is composed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Server-filtered doc set → UI | The UI renders only what the server returns (already section-filtered); it never reconstructs a full section set client-side (no bypass of C7). |
| Manual headcount absence → nationalisation UI | A missing manual number renders a prompt, never a computed rate — the F3 anti-feature is upheld at the presentation layer too. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-07-01 | Information Disclosure | the UI inferring hidden sections from gaps | mitigate | The view is a dumb renderer of the server's filtered set; no client section logic (Task 1). |
| T-97-07-02 | Tampering | the UI displaying a platform-derived Emiratisation rate | mitigate | The view shows the manual-input prompt when the rollup is absent; the rate only ever comes from the server's manual-input result (Task 3). |
</threat_model>

<verification>
- `pnpm check:web-vite-data-layer && pnpm check:web-vite-page-shells && pnpm check:web-vite-presentational` all pass.
- `pnpm --filter @contractor-ops/web-vite test src/components/hr-dashboard` (SCOPED) green.
- `pnpm typecheck --filter=@contractor-ops/web-vite` green; `pnpm lint:no-breadcrumbs` + `pnpm standards:check` green.
- Wiki: `structure/web-vite-domains.md` + the HR-dashboard domain page (all five widget surfaces + entry points) updated in this change set.
</verification>

<success_criteria>
The `dashboard/hr` screen renders all five widget groups — KPI header + headcount + utilization (97-06) and doc-expiry + probation + Gulf rollup (this plan) — layered per ARCHITECTURE.md, with loading/empty/degraded states, the section grain honored, the F3 manual-input posture upheld, and i18n keys throughout. `check:web-vite-*` all pass.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-07-SUMMARY.md` when done.
</output>
