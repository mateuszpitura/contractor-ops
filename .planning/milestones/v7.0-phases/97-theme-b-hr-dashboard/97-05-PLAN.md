---
phase: 97-theme-b-hr-dashboard
plan: 05
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - packages/api/src/services/saudization-dashboard.ts
  - packages/api/src/routers/workforce/hr-dashboard.ts
  - packages/validators/src/hr-dashboard.ts
  - packages/api/src/services/__tests__/saudization-dashboard.test.ts
autonomous: true
requirements: [HR-DASH-05]

must_haves:
  truths:
    - "`saudization-dashboard.ts` gains a per-country (KSA + UAE) nationalisation rollup that PRESERVES the locked anti-features: the rate is derived from the MANUAL headcount input ONLY (never an EmployeeProfile-derived count) and the band is read-through, never computed (RESEARCH C6)"
    - "`hrDashboard.getNationalisationRollup` composes the F3 service for both countries side-by-side, returning the same result shape per country, gated by `hrDashboardProcedure` (employee:read + module.hr-dashboard + report-rate-limit) and org-scoped"
    - "The Emiratisation extension reuses the existing headcount + iqama/visa-rollup shape — no new rate math, no auto-band; the UAE branch mirrors the KSA branch structurally"
    - "The existing `saudization-dashboard` unit test is extended to cover the UAE branch (manual-input rate, read-through band, no platform-derived rate) — the RED nationalisation-rollup expectation goes GREEN"
  artifacts:
    - path: "packages/api/src/services/saudization-dashboard.ts"
      provides: "per-country nationalisation rollup preserving manual-input + read-through-band posture"
      contains: "UAE"
    - path: "packages/api/src/routers/workforce/hr-dashboard.ts"
      provides: "getNationalisationRollup composing KSA + UAE"
      contains: "getNationalisationRollup"
  key_links:
    - from: "hr-dashboard.ts getNationalisationRollup"
      to: "computeSaudizationDashboard (per country)"
      via: "manual headcount + config per KSA/UAE; no EmployeeProfile-derived rate"
      pattern: "getNationalisationRollup"
---

<objective>
Deliver HR-DASH-05 by extending the v6.0 F3 Saudization service for Emiratisation, and exposing a per-country rollup on `hrDashboard`. RESEARCH C6 is the guardrail: `computeSaudizationDashboard` derives the nationalisation rate from the MANUAL `SaudiHeadcountInput` ONLY (Nitaqat/UAE quotas count the whole workforce; the platform sees only contractors) and reads the band read-through from config — "NEVER recomputed or inferred from the rate. No band-derivation code path exists here by design." The Emiratisation extension MUST preserve exactly that posture — a manual UAE headcount input and a read-through band, never an `EmployeeProfile`-derived Emiratisation %. This is a structural anti-feature, not a preference.

Reuse the existing headcount + iqama-rollup shape for the UAE branch (visa/Emirates-ID rollup mirrors the iqama rollup). Expose `getNationalisationRollup` composing both countries. No UI (97-07).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-CONTEXT.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-RESEARCH.md
@.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-VALIDATION.md

<interfaces>
- `packages/api/src/services/saudization-dashboard.ts:1-161` — `computeSaudizationDashboard` (the manual-input rate + read-through band, the locked anti-features at :1-19,105-114 to preserve); `SaudiHeadcountInput`/`SaudizationConfigInput`/`SaudizationDashboardResult` (the shapes to generalize per-country).
- `packages/api/src/services/saudization-dashboard.ts:43-47,132-147` — the `IqamaItemInput` + `iqamaRollup` reduce (mirror for a UAE visa/Emirates-ID rollup).
- `packages/api/src/routers/workforce/hr-dashboard.ts` (97-03) — `hrDashboardProcedure` to reuse.
- The rows the router feeds: manual per-country headcount + config (the KSA source is `SaudiHeadcount`/`SaudizationConfig`; the UAE source is its per-country analog — confirm the manual-headcount row shape at HEAD and reuse; do NOT derive from EmployeeProfile).
- `97-RESEARCH.md` § Reuse Patterns 4, § Corrections C6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: generalize the F3 service per-country (preserve the anti-features)</name>
  <files>packages/api/src/services/saudization-dashboard.ts, packages/api/src/services/__tests__/saudization-dashboard.test.ts</files>
  <read_first>
    - `packages/api/src/services/saudization-dashboard.ts:1-161` (read the WHOLE file — the anti-feature comments at :1-19,105-114 are binding) + `97-RESEARCH.md` § Corrections C6
  </read_first>
  <action>
    Add a per-country parameter (`country: 'KSA' | 'UAE'`) to a generalized rollup so the UAE branch reuses the KSA derivation: rate = manual `headcount.national / headcount.total` ONLY (rename the inputs generically — e.g. `nationalHeadcount` — keeping KSA back-compat), band read-through from config, quarterly-reentry window preserved, and a permit rollup mirroring `iqamaRollup` (UAE: visa/Emirates-ID expiry). Do NOT add any code path that computes the rate or band from an `EmployeeProfile` groupBy — the platform-derived breakdown stays subordinate and never drives the rate (keep the existing structural guarantee + its comments; update the comments to cover UAE without adding phase/req IDs). Extend the unit test with a UAE case: manual-input rate, read-through band, subordinate platform breakdown, no auto-band.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test saudization-dashboard 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The UAE branch derives the rate from manual headcount only + reads the band read-through; no `EmployeeProfile`-derived rate path exists (the anti-feature is preserved structurally).
    - The existing KSA behavior is byte-for-byte unchanged (back-compat); the unit test covers both countries.
  </acceptance_criteria>
  <done>The F3 service computes both KSA Saudization + UAE Emiratisation without violating the manual-input/read-through-band invariant.</done>
</task>

<task type="auto">
  <name>Task 2: getNationalisationRollup on hrDashboard (HR-DASH-05)</name>
  <files>packages/api/src/routers/workforce/hr-dashboard.ts, packages/validators/src/hr-dashboard.ts</files>
  <read_first>
    - `packages/api/src/routers/workforce/hr-dashboard.ts` (97-03 `hrDashboardProcedure`) + `97-RESEARCH.md` § Route + Namespace Inventory
  </read_first>
  <action>
    Add `getNationalisationRollup: hrDashboardProcedure.input(z.object({}).strict()).query(...)` that, per country the org operates in (KSA/UAE — detect from the presence of a manual headcount/config row, org-scoped), reads the manual headcount + config + the iqama/visa compliance-item rollup and calls the generalized service, returning `{ ksa?: Result, uae?: Result }` (a country absent when no manual headcount is recorded → the UI shows the "record manual headcount" prompt, per the Widget Gating Map). Org-scoped; no client id; `.strict()` input; the result Zod type in `validators/src/hr-dashboard.ts`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hr-dashboard-org-scope saudization-dashboard 2>&1 | tail -12 && pnpm typecheck --filter=@contractor-ops/api 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Returns KSA + UAE rollups side-by-side (each present only when its manual headcount exists); org-scoped + HR-gated + rate-limited.
    - No platform-derived rate; `packages/api` typecheck green.
  </acceptance_criteria>
  <done>HR-DASH-05 nationalisation rollup composes F3 for both Gulf countries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Manual headcount → nationalisation rate | The manual input is the ONLY rate source; the platform-derived breakdown is subordinate and never authoritative — the structural anti-feature preserved from F3. |
| Session org → rollup rows | Every read restates `organizationId`; no cross-org headcount/config surfaces. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-05-01 | Tampering | an Emiratisation rate silently computed from EmployeeProfile (false compliance signal) | mitigate | The service has no EmployeeProfile-derived rate path (Task 1); the unit test asserts the rate comes from manual input only. |
| T-97-05-02 | Information Disclosure | cross-org headcount/config leak | mitigate | Explicit `organizationId`; RED org-scope test covers `getNationalisationRollup`. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test saudization-dashboard hr-dashboard-org-scope` GREEN.
- `pnpm typecheck --filter=@contractor-ops/api --filter=@contractor-ops/validators` green.
- `pnpm lint:no-breadcrumbs` + `pnpm standards:check` green.
- Wiki: `structure/key-services.md` (saudization-dashboard per-country) + the Gulf/nationalisation domain page + `structure/api-routers-catalog.md` (getNationalisationRollup) updated; the F3 anti-feature (manual-input rate, read-through band) restated for UAE.
</verification>

<success_criteria>
`saudization-dashboard.ts` computes both KSA Saudization + UAE Emiratisation from MANUAL headcount only with a read-through band (the F3 anti-feature preserved), the KSA path unchanged, and `hrDashboard.getNationalisationRollup` composes both side-by-side, HR-gated + org-scoped. HR-DASH-05 is delivered. No UI.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/97-theme-b-hr-dashboard/97-05-SUMMARY.md` when done.
</output>
