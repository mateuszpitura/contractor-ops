---
phase: 82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/middleware/__tests__/add-on.test.ts
  - packages/db/src/__tests__/region-lockstep.test.ts
  - packages/feature-flags/src/__tests__/v7-flags-registered.test.ts
  - packages/feature-flags/src/__tests__/boot-gate.test.ts
  - apps/public-api/src/lib/__tests__/error-handler.test.ts
  - .planning/milestones/v7.0-phases/82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl/IRIS-TCC-ENROLLMENT.md
autonomous: true
requirements: [FOUND7-01, FOUND7-02, FOUND7-03]
user_setup:
  - service: IRS IRIS (Information Returns Intake System)
    why: "TCC (Transmitter Control Code) enrollment for IRIS XML A2A e-file is a real-world IRS ops action the founder must start now (~45-day lead) so Phase 86 (US-FORM-05) can transmit. No app code — this plan only writes the ops doc that records the started calendar dependency."
    dashboard_config:
      - task: "Begin IRIS A2A TCC application via IRS e-Services (ID.me identity verification + Responsible Official + application)"
        location: "https://www.irs.gov/e-file-providers/information-returns-intake-system-iris — IRIS Application for TCC"

must_haves:
  truths:
    - "Every Wave 0 test file from 82-VALIDATION.md exists and is runnable (RED is acceptable; later plans turn them GREEN)."
    - "IRIS-TCC-ENROLLMENT.md exists, records the ~45-day TCC lead time and a concrete start date, and cross-links Phase 86 (US-FORM-05). (D-08)"
  artifacts:
    - path: "packages/api/src/middleware/__tests__/add-on.test.ts"
      provides: "SC#1 allow/deny + chain-order test scaffold for requireAddOn"
    - path: "packages/db/src/__tests__/region-lockstep.test.ts"
      provides: "SC#3 five-way region lockstep assertion scaffold"
    - path: "packages/feature-flags/src/__tests__/v7-flags-registered.test.ts"
      provides: "SC#2 all-keys-present (FLAGS ∧ signoff registry) scaffold"
    - path: "apps/public-api/src/lib/__tests__/error-handler.test.ts"
      provides: "SC#1 REST ADD_ON_REQUIRED → 403 mapping scaffold"
    - path: ".planning/milestones/v7.0-phases/82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl/IRIS-TCC-ENROLLMENT.md"
      provides: "SC#4 IRIS TCC enrollment ops doc"
      contains: "45"
  key_links:
    - from: "later plans (82-02/03/04)"
      to: "these test files"
      via: "scoped vitest run as <automated> verify"
      pattern: "pnpm --filter @contractor-ops/(api|db|feature-flags|public-api) test"
---

<objective>
Create the Wave 0 test scaffolding for all four success criteria (so every downstream task has an automated verify to sample against per the Nyquist contract) and write the IRIS TCC enrollment ops doc (SC#4 — the only non-code success criterion).

Purpose: 82-VALIDATION.md enumerates six test gaps that later plans turn GREEN. They must exist as RED stubs first so 82-02/03/04 can run `--filter … test` as their `<automated>` verify rather than declaring missing tests. SC#4 is a pure doc artifact (D-08) with no analog and no app code, so it lands here in the foundation wave.
Output: 5 test files (RED) + 1 ops doc.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl/82-CONTEXT.md
@.planning/milestones/v7.0-phases/82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl/82-RESEARCH.md
@.planning/milestones/v7.0-phases/82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl/82-PATTERNS.md
@.planning/milestones/v7.0-phases/82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl/82-VALIDATION.md

<interfaces>
<!-- Contracts the downstream plans implement against. Test stubs assert these shapes. -->
<!-- requireAddOn (82-04 will implement): throws TRPCError FORBIDDEN, message=JSON
     { type: 'ADD_ON_REQUIRED', requiredAddOn: AddOnKey, currentAddOns: string[] }.
     ADD_ON_KEYS = ['workforce','us-cross-border'] as const. -->
<!-- region.ts (82-02 will implement): SUPPORTED_REGIONS readonly tuple; DataRegion union;
     REGION_ENV_MAP / REPLICA_ENV_MAP are Record<DataRegion,string>. After 82-02 all contain EU/ME/US. -->
<!-- feature-flags schemas.ts (82-02): regionSchema = z.enum([...]); .options must match SUPPORTED_REGIONS. -->
<!-- V7_FLAG_KEYS (82-03): exported readonly string[] of the 19 dot-namespaced keys;
     getFlagSignoff(key) and FLAGS[key] must both resolve for every cohort key. -->
<!-- error-handler (82-04): extractErrorDetails(raw) parses JSON message; ADD_ON_REQUIRED → { code:'ADD_ON_REQUIRED', message } and rides FORBIDDEN→403. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the IRIS TCC enrollment ops doc (SC#4)</name>
  <read_first>
    - .planning/milestones/v7.0-phases/82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl/82-CONTEXT.md (D-08 — content requirements: ~45-day lead, start date as a started calendar dependency, cross-link Phase 86)
    - 82-RESEARCH.md § State of the Art (FIRE decommissions 2026-12-31 → IRIS-PRIMARY; TY2026 returns filed early 2027 MUST use IRIS XML A2A; new IRIS TCC required, FIRE TCC does not carry over)
    - 82-PATTERNS.md final section "IRIS-TCC-ENROLLMENT.md (CREATE) — NO ANALOG (by design)" — confirms pure ops doc, use Write tool
    - .planning/REQUIREMENTS.md (US-FORM-05 verbatim — the Phase 86 requirement this doc cross-links)
  </read_first>
  <action>
    Write `.planning/milestones/v7.0-phases/82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl/IRIS-TCC-ENROLLMENT.md` (use the Write tool — no analog, no app code per D-08). Record: (1) why a NEW IRIS A2A TCC is required (IRS FIRE decommissions 2026-12-31; FIRE TCC does NOT carry over to IRIS; TY2026 information returns filed early 2027 MUST go via IRIS XML A2A); (2) the ~45-day enrollment lead time stated explicitly as the number "45"; (3) a concrete "Started:" calendar date (today, 2026-06-07) and an "Earliest ready" date = start + ~45 days, framed as a started calendar dependency; (4) the enrollment steps at a real-world level (IRS e-Services ID.me identity verification, Responsible Official designation, IRIS Application for TCC); (5) an explicit cross-link to "Phase 86 (US-FORM-05, IRIS XML A2A e-file)" — that phase consumes the TCC. Add the Standing-Constraint annotation that this is a real-world IRS ops action the founder takes (LOCAL-ONLY: no app code files anything until Phase 86). Do NOT seed any in-app onboarding task (no product theater per D-08).
  </action>
  <verify>
    <automated>test -f .planning/milestones/v7.0-phases/82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl/IRIS-TCC-ENROLLMENT.md &amp;&amp; grep -q '45' .planning/milestones/v7.0-phases/82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl/IRIS-TCC-ENROLLMENT.md &amp;&amp; grep -qi 'US-FORM-05\|Phase 86' .planning/milestones/v7.0-phases/82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl/IRIS-TCC-ENROLLMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the real (non-symlink) phase path.
    - Contains the literal "45" (lead time) — maps to VALIDATION.md SC#4 row (`grep -q '45'`).
    - Contains a "Started:" date and a Phase 86 / US-FORM-05 cross-link.
    - No application code, no Prisma/UI/seed changes (D-08).
  </acceptance_criteria>
  <done>SC#4 satisfied: IRIS-TCC-ENROLLMENT.md exists, records the ~45-day lead + start date as a started calendar dependency, cross-links Phase 86.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold the add-on + error-handler test files (SC#1 RED stubs)</name>
  <read_first>
    - packages/api/src/middleware/tier.ts (the clone target requireAddOn mirrors — TIER_REQUIRED FORBIDDEN shape, proProcedure composition)
    - packages/api/src/middleware/__tests__ (existing middleware test conventions — read a sibling test to mirror imports/mocks for getSubscription)
    - apps/public-api/src/lib/error-handler.ts (extractErrorDetails, TIER_REQUIRED branch lines 51-57 — the shape ADD_ON_REQUIRED mirrors)
    - 82-PATTERNS.md § add-on.ts and § error-handler.ts (exact ADD_ON_REQUIRED JSON shape + STARTER base-tier chain per D-11)
    - 82-VALIDATION.md rows SC#1 (three rows: middleware allow/deny+chain, REST mapping, grant mutation)
  </read_first>
  <behavior>
    - add-on.test.ts: requireAddOn('workforce') on an org whose Subscription.addOns lacks 'workforce' throws TRPCError code FORBIDDEN with message parseable to { type:'ADD_ON_REQUIRED', requiredAddOn:'workforce', currentAddOns:[] }.
    - add-on.test.ts: an org whose addOns includes 'workforce' passes through (next() called).
    - add-on.test.ts: workforceProcedure composes requireTier('STARTER') BEFORE requireAddOn (chain-order assertion per D-11).
    - error-handler.test.ts: extractErrorDetails(JSON.stringify({type:'ADD_ON_REQUIRED', requiredAddOn:'workforce'})) returns { code:'ADD_ON_REQUIRED', ... } and the HTTP status maps to 403 (FORBIDDEN→403).
  </behavior>
  <action>
    Create `packages/api/src/middleware/__tests__/add-on.test.ts` and `apps/public-api/src/lib/__tests__/error-handler.test.ts` as RED stubs that import from the not-yet-existing/extended modules (`../add-on`, the error-handler ADD_ON_REQUIRED branch) and assert the behaviors above. Mock `getSubscription` to return controllable `addOns` arrays (mirror how the sibling middleware tests mock it). The add-on key const is `ADD_ON_KEYS = ['workforce','us-cross-border']` and the base tier is STARTER (D-11) — do NOT write PRO (RESEARCH A2 was illustrative; PATTERNS corrects to STARTER). These MUST fail now (modules absent) — RED is correct; 82-04 turns them GREEN. Do NOT implement requireAddOn or the error-handler branch here.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test add-on 2>&amp;1 | tail -5; pnpm --filter @contractor-ops/public-api test error-handler 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and vitest collects them (suite runs, does not error on parse).
    - Tests are RED (the implementation modules do not exist yet) — this is the expected Wave 0 state, not a failure of this task.
    - Assertions reference the exact ADD_ON_REQUIRED JSON shape and STARTER base tier (D-02 + D-11), so 82-04 has a precise contract to satisfy.
    - Maps to VALIDATION.md SC#1 rows 1 and 2.
  </acceptance_criteria>
  <done>add-on.test.ts and error-handler.test.ts exist as RED stubs asserting the ADD_ON_REQUIRED contract; collectible by vitest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold the region-lockstep, v7-flags, and boot-gate test files (SC#2 + SC#3 RED stubs)</name>
  <read_first>
    - packages/db/src/region.ts (SUPPORTED_REGIONS, DataRegion, REGION_ENV_MAP, getRegionalClient lazy-throw)
    - packages/db/src/replica.ts (REPLICA_ENV_MAP Record<DataRegion,string>, lines 72-75)
    - packages/feature-flags/src/schemas.ts (regionSchema line 23, flagCategorySchema lines 6-15)
    - packages/feature-flags/src/__tests__/boot-gate.test.ts (existing — extend it; confirms gate is explicit-call, no gated FLAGS currently fire)
    - packages/feature-flags/src/registry.ts (assertFlagSignoffsOrExit lines 60-85; getFlagSignoff; FLAG_KEYS import) and packages/feature-flags/src/index.ts (re-exports: assertFlagSignoffsOrExit, FLAG_KEYS)
    - 82-PATTERNS.md § flags-core.ts (canonical 19-key V7_FLAG_KEYS mapping) + § region.ts + § schemas.ts + Shared Patterns "Record<DataRegion,string> as a compile-time lockstep"
    - 82-VALIDATION.md rows SC#2 (boot-gate exit-on-missing, all-keys-present) + SC#3 (five-way lockstep, buildLazyBag US, getRegionalClient US)
  </read_first>
  <behavior>
    - region-lockstep.test.ts: the region sets from SUPPORTED_REGIONS, regionSchema.options, Object.keys(REGION_ENV_MAP), and Object.keys(REPLICA_ENV_MAP) are all identical and all include 'US' (after 82-02). Assert getRegionalClient('US') does NOT throw "Unsupported data region" (it throws only on missing env, lazily).
    - v7-flags-registered.test.ts: for every key in V7_FLAG_KEYS (19 keys), FLAGS[key] is defined AND getFlagSignoff(key) !== undefined; assert the count is 19 and every key is dot-namespaced (matches /^[a-z0-9]+(\.[a-z0-9-]+)+$/).
    - boot-gate.test.ts (extend): with a v7.0 cohort key present in FLAGS+GATED prefixes but MISSING from the signoff registry, assertFlagSignoffsOrExit() triggers process.exit(1) (mock process.exit); with all present it returns true; with FLAG_SIGNOFF_BYPASS=local it warns instead of exiting.
  </behavior>
  <action>
    Create `packages/db/src/__tests__/region-lockstep.test.ts` and `packages/feature-flags/src/__tests__/v7-flags-registered.test.ts`, and EXTEND `packages/feature-flags/src/__tests__/boot-gate.test.ts` with a v7.0-cohort exit-on-missing case. Import `V7_FLAG_KEYS` (82-03 will export it from flags-core), `getFlagSignoff`/`assertFlagSignoffsOrExit` (from the package index), region symbols from `@contractor-ops/db`, and `regionSchema` from feature-flags schemas. Assert the behaviors above. Mock `process.exit` in the boot-gate case. These MUST be RED now (US not yet in regions, V7_FLAG_KEYS not yet exported, v7 keys not registered) — 82-02 turns the region/regionSchema parts GREEN, 82-03 turns the flag/boot-gate parts GREEN. Do NOT add US to regions or register flags here.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db test region-lockstep 2>&amp;1 | tail -5; pnpm --filter @contractor-ops/feature-flags test v7-flags 2>&amp;1 | tail -5; pnpm --filter @contractor-ops/feature-flags test boot-gate 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist and vitest collects them.
    - region-lockstep asserts the four region sources hold identical sets incl. 'US' and getRegionalClient('US') is not an "Unsupported data region" throw — maps VALIDATION.md SC#3 rows.
    - v7-flags-registered asserts exactly 19 keys, all dot-namespaced, present in FLAGS ∧ signoff registry — maps VALIDATION.md SC#2 all-keys-present row.
    - boot-gate extension asserts process.exit(1) on a missing cohort key + bypass downgrade — maps VALIDATION.md SC#2 boot-gate row.
    - RED is the expected Wave 0 state.
  </acceptance_criteria>
  <done>region-lockstep, v7-flags-registered, and the extended boot-gate test exist as RED stubs asserting SC#2/SC#3 contracts; collectible by vitest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ops-doc → founder | IRIS-TCC-ENROLLMENT.md is internal planning text; no untrusted input, no runtime surface. |
| test files → CI | Test scaffolds run in CI only; no production code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01-01 | Information Disclosure | IRIS-TCC-ENROLLMENT.md | accept | Doc records process + lead time only; no TCC, no SSN, no IRS credentials committed (those are real-world secrets the founder holds, never in repo). |
| T-82-01-02 | Tampering | test scaffolds | accept | RED stubs assert contracts; they cannot weaken production behavior — they only fail until 82-02/03/04 implement. No production code shipped in this plan. |
| T-82-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (no new deps — RESEARCH § Package Legitimacy Audit: Not applicable). slopcheck N/A. |
</threat_model>

<verification>
- `test -f` + `grep` on IRIS-TCC-ENROLLMENT.md (SC#4 doc-presence).
- All five test files collected by vitest (RED acceptable — they encode the contracts the next wave satisfies).
- No production source modified — `git diff --stat` shows only test files + the ops doc.
</verification>

<success_criteria>
- IRIS-TCC-ENROLLMENT.md exists with the "45" lead time and Phase 86 cross-link (SC#4 GREEN).
- The six VALIDATION.md Wave 0 test gaps are scaffolded (one combined boot-gate extension + five files), each collectible by vitest, each asserting the precise downstream contract.
- No 3-consecutive-task gap without an automated verify (all three tasks have `<automated>`).
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl/82-01-SUMMARY.md` when done.
</output>
