---
phase: 82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl
plan: 02
type: execute
wave: 2
depends_on: ["82-01"]
files_modified:
  - packages/db/src/region.ts
  - packages/db/src/replica.ts
  - packages/feature-flags/src/schemas.ts
  - packages/api/src/middleware/feature-flag.ts
  - packages/validators/src/env.ts
  - packages/db/scripts/migrate-all-regions.ts
  - .env.example
autonomous: true
requirements: [FOUND7-03]

must_haves:
  truths:
    - "A request carrying region=US resolves through SUPPORTED_REGIONS, the DataRegion union, DATABASE_URL_US, and buildLazyBag flag coercion without a runtime throw or silent EU-coercion."
    - "SUPPORTED_REGIONS, regionSchema.options, REGION_ENV_MAP keys, and REPLICA_ENV_MAP keys all hold identical region sets including US (five-way lockstep); migrate-all-regions REGION_ENV_VARS + replica map carry US. (D-05, D-07)"
    - "DATABASE_URL_US is OPTIONAL — the app boots clean locally with no US DB; getRegionalClient('US') throws only on actual access. (D-06)"
    - "flagCategorySchema gains 'payroll' (owned here so 82-03's payroll.* flags register without a second writer to schemas.ts)."
  artifacts:
    - path: "packages/db/src/region.ts"
      provides: "SUPPORTED_REGIONS incl. US + REGION_ENV_MAP['US']='DATABASE_URL_US'"
      contains: "US"
    - path: "packages/db/src/replica.ts"
      provides: "REPLICA_ENV_MAP['US']='DATABASE_URL_US_RO' (Record<DataRegion> forces it)"
      contains: "US"
    - path: "packages/feature-flags/src/schemas.ts"
      provides: "regionSchema = z.enum(['EU','ME','US']) (hidden 5th place) + 'payroll' in flagCategorySchema"
      contains: "'US'"
    - path: "packages/api/src/middleware/feature-flag.ts"
      provides: "buildLazyBag US branch — no EU coercion"
    - path: "packages/validators/src/env.ts"
      provides: "DATABASE_URL_US optional"
      contains: "DATABASE_URL_US"
  key_links:
    - from: "packages/db/src/region.ts SUPPORTED_REGIONS"
      to: "REGION_ENV_MAP / REPLICA_ENV_MAP"
      via: "Record<DataRegion,string> compile-time lockstep"
      pattern: "Record<DataRegion"
    - from: "packages/api/src/middleware/feature-flag.ts buildLazyBag"
      to: "packages/feature-flags/src/schemas.ts regionSchema"
      via: "US bag passes downstream Zod validation"
      pattern: "region === 'US'"
---

<objective>
Enable `us-east-1` as the third supported region across the five lockstep sites so a `region=US` request resolves without a runtime throw or silent EU-coercion (FOUND7-03 / SC#3). Also make the single additive `'payroll'` category edit in `flagCategorySchema` here, since this plan already owns `schemas.ts` — that lets 82-03 register the payroll flags without a second writer to the same file (file-ownership for parallel waves).

Purpose: Every US-data phase (83+) depends on region routing accepting US. The `Record<DataRegion,string>` types make this a compile-time lockstep — adding `'US'` to the union force-fails tsc until both env maps gain a `US` entry. `DATABASE_URL_US` stays OPTIONAL (D-06) so the app boots clean locally with no US DB and the lockstep test needs no live US database.
Output: the 5-place atomic change (region.ts, replica.ts, feature-flags schemas.ts regionSchema, feature-flag.ts buildLazyBag, validators env.ts) + the D-07 adjacent (migrate-all-regions.ts) + the 'payroll' category + .env.example.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl/82-CONTEXT.md
@.planning/milestones/v7.0-phases/82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl/82-RESEARCH.md
@.planning/milestones/v7.0-phases/82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl/82-PATTERNS.md

<interfaces>
<!-- The RED test from 82-01 this plan turns GREEN:
     packages/db/src/__tests__/region-lockstep.test.ts asserts:
       new Set(SUPPORTED_REGIONS) === new Set(regionSchema.options)
         === new Set(Object.keys(REGION_ENV_MAP)) === new Set(Object.keys(REPLICA_ENV_MAP)) === {EU,ME,US}
       getRegionalClient('US') is NOT an "Unsupported data region" throw. -->
<!-- DataRegion = (typeof SUPPORTED_REGIONS)[number] — a TS union, NOT a Prisma enum (Pitfall 5). -->
<!-- regionSchema feeds evalContextSchema.region (schemas.ts:53); buildLazyBag's US bag fails Zod
     validation downstream unless regionSchema gains 'US' (Pitfall 4). -->
<!-- 'payroll' category in flagCategorySchema is consumed by 82-03 (8 payroll.* flags). 82-03
     depends_on this plan precisely so the category exists before those flags register. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add US to the DB region lockstep — region.ts + replica.ts</name>
  <read_first>
    - packages/db/src/region.ts (analog: SUPPORTED_REGIONS + DataRegion + REGION_ENV_MAP lines 8-18; getRegionalClient lazy-throw 42-63; preWarmRegionalClients skip-on-missing 69-77)
    - packages/db/src/replica.ts (analog: REPLICA_ENV_MAP Record<DataRegion,string> lines 72-75; getReplicaClient/readReplica writer-fallback 234-237, 293-296)
    - 82-PATTERNS.md § region.ts + § replica.ts (exact edits) + Shared Patterns "Record<DataRegion,string> as a compile-time lockstep"
    - 82-RESEARCH.md Pitfall 5 (DataRegion is a TS union, not a Prisma enum — do NOT look in schema/) + Pitfall 6 (replica forced by type; env stays optional/unset; deferred US replica)
  </read_first>
  <action>
    In `packages/db/src/region.ts`: add `'US'` to the `SUPPORTED_REGIONS` const tuple (`['EU','ME'] → ['EU','ME','US'] as const`) and add `US: 'DATABASE_URL_US'` to `REGION_ENV_MAP` (the `Record<DataRegion,string>` type force-fails tsc until this entry exists). Do NOT modify `getRegionalClient` or `preWarmRegionalClients` — their existing lazy-throw-on-missing-env and skip-on-missing behavior IS the D-06 desired behavior. In `packages/db/src/replica.ts`: add `US: 'DATABASE_URL_US_RO'` to `REPLICA_ENV_MAP` (forced by the same `Record<DataRegion,string>` type). The US replica env stays optional/unset (US read-replica deferred per CONTEXT Deferred Ideas) — `getReplicaClient`/`readReplica` already fall back to the writer when the `_RO` var is absent, so this is harmless. No `console.*` (repo uses createLogger).
  </action>
  <verify>
    <automated>pnpm typecheck --filter=@contractor-ops/db 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `SUPPORTED_REGIONS` includes `'US'`; `DataRegion` union auto-propagates (no manual type edit).
    - `REGION_ENV_MAP['US'] === 'DATABASE_URL_US'` and `REPLICA_ENV_MAP['US'] === 'DATABASE_URL_US_RO'`.
    - `pnpm typecheck --filter=@contractor-ops/db` passes (the `Record<DataRegion>` compile-time lockstep is satisfied) — this IS the SC#3 lockstep enforcer.
    - getRegionalClient/preWarmRegionalClients unmodified.
  </acceptance_criteria>
  <done>region.ts + replica.ts carry US in both Record<DataRegion> maps; package typechecks; US env stays optional.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: feature-flags schemas.ts (regionSchema 5th place + 'payroll' category), buildLazyBag US branch, DATABASE_URL_US optional env</name>
  <read_first>
    - packages/feature-flags/src/schemas.ts (analog: regionSchema line 23; flagCategorySchema enum lines 6-15; evalContextSchema.region line 53 derives from regionSchema; leave jurisdictionSchema line 3 as EU/ME/ANY)
    - packages/api/src/middleware/feature-flag.ts (analog: buildLazyBag region branch lines 28-43 inside fn 22-50 — the `let region: 'EU'|'ME'` decl line 28, unknown→EU else 33-43)
    - packages/validators/src/env.ts (analog: optional DATABASE_URL_*_RO vars lines 32-33; getServerEnv boot validator 408-416)
    - 82-PATTERNS.md § schemas.ts (BOTH edits: (1) flagCategorySchema 'payroll' + (2) regionSchema US) + § feature-flag.ts + § validators/src/env.ts (exact edits, corrected line ranges) + Shared Patterns "Optional regional env var"
    - 82-RESEARCH.md Pitfall 4 (regionSchema is the hidden 5th place; US flags use jurisdiction:'ANY', do NOT add US to jurisdictionSchema) + the "buildLazyBag — accept US" + "Region 4(+1)-place change" code examples + D-09 (the 'payroll' category is required by 82-03's 8 payroll.* flags)
  </read_first>
  <action>
    This plan OWNS `packages/feature-flags/src/schemas.ts` for the whole phase — it makes BOTH additive enum edits in one place so 82-03 can register the payroll flags without touching this file. (1a) `schemas.ts` — widen `regionSchema` to `z.enum(['EU','ME','US'])` (the hidden 5th place of the US lockstep — `evalContextSchema.region` derives from it, so this is what lets the US bag pass downstream Zod validation). Leave `jurisdictionSchema` as `['EU','ME','ANY']` — do NOT add `'US'` there (US flags use `jurisdiction:'ANY'`; the jurisdiction field partitions flag-visibility, distinct from the eval-context region). (1b) `schemas.ts` — add `'payroll'` to `flagCategorySchema` (one-line additive enum change, D-09; the 8 `payroll.*` flags in 82-03 need this category to exist). (2) `packages/api/src/middleware/feature-flag.ts`: widen the local `region` type in `buildLazyBag` to `'EU'|'ME'|'US'` and add an explicit `else if (ctx.region === 'US') region = 'US';` branch BEFORE the genuinely-unknown `else` that warn-coerces to EU (D-05 — the explicit SC#3 failure mode is US silently coercing to EU; the unknown `else` stays fail-closed EU). Use the existing `log.warn` (createLogger), no `console.*`. (3) `packages/validators/src/env.ts`: add `DATABASE_URL_US: z.string().min(1).optional()` to `databaseSchema` (D-06 — OPTIONAL, mirror the `_RO` optional pattern, NOT the required `_EU`/`_ME`); optionally add `DATABASE_URL_US_RO` optional too for replica consistency. (4) Add `DATABASE_URL_US` (and `DATABASE_URL_US_RO`) to `.env.example` per the CLAUDE.md env-var rule, commented as optional/local-skip.
  </action>
  <verify>
    <automated>pnpm typecheck --filter=@contractor-ops/feature-flags --filter=@contractor-ops/api --filter=@contractor-ops/validators 2>&amp;1 | tail -15; pnpm --filter @contractor-ops/api test feature-flag 2>&amp;1 | tail -8; pnpm check:no-process-env 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `regionSchema.options` === `['EU','ME','US']`; `flagCategorySchema` includes `'payroll'`; `jurisdictionSchema` unchanged (EU/ME/ANY).
    - buildLazyBag with `ctx.region==='US'` returns a US bag (no EU coercion, no throw); genuinely-unknown still fails closed to EU.
    - `DATABASE_URL_US` is optional in env.ts and present in .env.example; `getServerEnv()` does not require it.
    - `pnpm --filter @contractor-ops/api test feature-flag` GREEN for the US-bag case (turns the 82-01 SC#3 buildLazyBag stub GREEN).
    - `check:no-process-env` clean (env accessed via schema, not raw process.env in app code).
  </acceptance_criteria>
  <done>schemas.ts carries regionSchema US + 'payroll' category; buildLazyBag and env.ts accept US; the US bag passes Zod; DATABASE_URL_US optional; .env.example updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-07 adjacent (migrate-all-regions) + green the region-lockstep test</name>
  <read_first>
    - packages/db/scripts/migrate-all-regions.ts (analog: REGION_ENV_VARS plain array line 40; migrateRegion skip-on-missing lines 49-54)
    - packages/db/src/__tests__/region-lockstep.test.ts (the 82-01 RED stub this task turns GREEN — read to confirm the exact assertions)
    - 82-PATTERNS.md § migrate-all-regions.ts (PLAIN array — tsc will NOT force it, add manually) + § seed-dev.ts NOTE (US seed-org path DEFERRED for Phase 82 per D-06/A5 — do NOT widen the seed-dev region unions here)
    - 82-RESEARCH.md Pitfall 6 (replica forced by type; migrate is a plain array needing manual add; seed-dev US org optional/deferred)
  </read_first>
  <action>
    In `packages/db/scripts/migrate-all-regions.ts`: add `'DATABASE_URL_US'` to the `REGION_ENV_VARS` plain array (it is NOT `Record<DataRegion>`-typed so tsc will NOT force it — add manually for D-07 consistency). Safe because `migrateRegion` already skips-on-missing URL (lines 49-54): an unset US DB locally is a no-op. Do NOT widen the `seed-dev.ts` `'EU'|'ME'` region-loop unions to seed a US org — that US-seed-org path is DEFERRED to Phase 83 (US-INFRA) per D-06/A5; the SC#3 lockstep test needs no live US DB or US org. (Note: `seed-dev.ts` is owned by 82-04 for the add-on seed grant, which is region-independent — this plan does not touch seed-dev.) After the edit, run the 82-01 region-lockstep test — with region.ts/replica.ts (Task 1) and regionSchema (Task 2) already carrying US, all four region sources now hold identical sets and the test goes GREEN.
  </action>
  <verify>
    <automated>pnpm typecheck --filter=@contractor-ops/db 2>&amp;1 | tail -8; pnpm --filter @contractor-ops/db test region-lockstep 2>&amp;1 | tail -10; pnpm --filter @contractor-ops/db test region 2>&amp;1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `REGION_ENV_VARS` includes `'DATABASE_URL_US'`; `migrateRegion` skips it when the URL is unset (no-op locally).
    - `seed-dev.ts` region unions UNCHANGED (US seed-org deferred per D-06; this plan does not touch seed-dev).
    - `pnpm --filter @contractor-ops/db test region-lockstep` GREEN — the five-way lockstep (SUPPORTED_REGIONS / regionSchema.options / REGION_ENV_MAP / REPLICA_ENV_MAP all = {EU,ME,US}) holds. Maps VALIDATION.md SC#3 lockstep row.
    - `pnpm --filter @contractor-ops/db test region` GREEN — getRegionalClient('US') is not an "Unsupported data region" throw (lazy-throws only on missing env). Maps VALIDATION.md SC#3 getRegionalClient row.
  </acceptance_criteria>
  <done>FOUND7-03 complete: US accepted across all five lockstep sites + the migrate adjacent; region-lockstep + region tests GREEN; no silent EU-coercion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request region → DB client | `ctx.region` (derived from session/org, never raw client input) selects the regional Prisma client. A US org's data must NOT land in the EU DB (data-residency). |
| env → region resolution | `DATABASE_URL_US` is the only thing that routes US data; absent ⇒ lazy-throw on access (fail-loud), never a silent EU fallback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-02-01 | Information Disclosure (data-residency) | buildLazyBag region coercion | mitigate | Explicit `US` branch BEFORE the unknown→EU `else` (D-05): `region=US` must NOT silently coerce to EU. region-lockstep + feature-flag tests assert no EU-coercion. This is the security-load-bearing edit of the plan. |
| T-82-02-02 | Tampering / fail-open | getRegionalClient('US') with missing DATABASE_URL_US | mitigate | Existing lazy-throw is preserved (unmodified): on actual US access with no env, it throws loudly rather than routing to EU. D-06 keeps the env optional but access fail-loud. |
| T-82-02-03 | Spoofing | ctx.region source | accept | `ctx.region` is derived from session/org (per CLAUDE.md tenant-from-session), not client-supplied; no new client-trust surface added here. |
| T-82-02-04 | Tampering | npm/pip/cargo installs | mitigate | No package installs (RESEARCH § Package Legitimacy Audit: Not applicable). slopcheck N/A. |
</threat_model>

<verification>
- `pnpm typecheck --filter=@contractor-ops/db` — the `Record<DataRegion,string>` compile-time lockstep (no US entry ⇒ tsc fails). CI-canonical.
- `pnpm --filter @contractor-ops/db test region-lockstep` + `test region` GREEN (SC#3).
- `pnpm --filter @contractor-ops/api test feature-flag` GREEN for the US bag (SC#3).
- `pnpm check:no-process-env` clean.
- App boots locally with no `DATABASE_URL_US` set (D-06) — no boot regression introduced.
</verification>

<success_criteria>
- A `region=US` request resolves through SUPPORTED_REGIONS → DataRegion → DATABASE_URL_US → buildLazyBag without a runtime throw or silent EU-coercion (SC#3).
- Five-way lockstep asserted GREEN: SUPPORTED_REGIONS, regionSchema.options, REGION_ENV_MAP keys, REPLICA_ENV_MAP keys all = {EU, ME, US}.
- `DATABASE_URL_US` optional; app boots clean locally with no US DB.
- `flagCategorySchema` carries `'payroll'` (so 82-03 registers payroll flags without re-touching schemas.ts).
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/82-v7-0-foundation-add-on-billing-flag-registry-us-region-enabl/82-02-SUMMARY.md` when done.
</output>
