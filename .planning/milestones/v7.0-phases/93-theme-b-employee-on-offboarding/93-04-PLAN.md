---
phase: 93-theme-b-employee-on-offboarding
plan: 04
type: execute
wave: 2
depends_on: [93-02]
files_modified:
  - packages/api/src/routers/integrations/deprovisioning.ts
autonomous: true
requirements: [EMP-OFF-02]

must_haves:
  truths:
    - "startDeprovisioningRun accepts a worker subject (workerId) as well as a contractor assignment"
    - "The worker branch sources externalUserId from Worker.email and country/TZ from EmployeeProfile.countryCode"
    - "The 14-day cooldown gate reads EmployeeProfile.terminatedAt and blocks pre-cooldown"
    - "COUNTRY_TZ has a US entry (America/New_York)"
    - "A worker run writes DeprovisioningRun{workerId, contractorId:null, assignmentId:null}; step fan-out + idempotency + QStash body unchanged"
  artifacts:
    - path: "packages/api/src/routers/integrations/deprovisioning.ts"
      provides: "Worker branch in startDeprovisioningRun + US TZ + worker resolver"
      contains: "terminatedAt"
  key_links:
    - from: "startDeprovisioningRun worker branch"
      to: "canStartDeprovisioning({ endedAt: terminatedAt })"
      via: "reused pure cooldown gate"
      pattern: "canStartDeprovisioning"
    - from: "DeprovisioningRun.create (worker)"
      to: "QStash step fan-out (externalUserId only)"
      via: "unchanged worker-agnostic execution half"
      pattern: "externalUserId"
---

<objective>
Worker-key the F2 IdP `DeprovisioningRun` trigger — the phase spine (EMP-OFF-02 criterion-3). The saga's execution half (QStash step runner, token resolver, `canStartDeprovisioning`, `recomputeRunStatus`, adapters) is ALREADY worker-agnostic; contractor coupling lives in exactly the trigger. Add a `workerId` branch that reads `Worker.email` + `EmployeeProfile.countryCode`/`terminatedAt`, reuses the pure cooldown gate, and writes a `DeprovisioningRun` with null contractor FKs. Turns `worker-deprovisioning.test.ts` (Plan 01) GREEN.

Purpose: Get this right or employee offboarding IdP cannot compose. Do NOT touch the step runner, token resolver, cooldown, or adapters.
Output: worker branch + `resolveWorkerForDeprovisioning` + `COUNTRY_TZ['US']` in `deprovisioning.ts`.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md
@.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-RESEARCH.md

<interfaces>
- `startDeprovisioningRun` (deprovisioning.ts:202-345): `integrationProcedure({ permission:{ idp:['start_run'] } })` :202; contractor read via ENDED assignment :210-236; cooldown decision :225-234; `externalUserId = assignment.contractor.email` :236; run create with contractorId/assignmentId :263-286; step fan-out :294-316; P2002 idempotency :328-343.
- `resolveAssignmentForContractor` (:177-190) — contractor-only; add a worker sibling OR bypass (worker supplies workerId directly, assignmentId stays null).
- `COUNTRY_TZ` (:103-110) has DE/GB/PL/SA/AE — NO US; `DEFAULT_JURISDICTION_TZ='Europe/Berlin'` :110.
- `canStartDeprovisioning` (`packages/idp-saga/src/cooldown.ts`) — pure, keys on `{status, endedAt, jurisdictionTz}` — REUSE unchanged.
- `EmployeeProfile` (employee.prisma): `workerId`, `countryCode`, `terminatedAt` (added Plan 02), `worker { email }`.
- UNCHANGED (worker-agnostic): `apps/api/src/routes/idp-deprovisioning.ts`, `idp-deprovisioning-step-runner.ts` (`stepRunnerBodySchema` — externalUserId only), `idp-token-resolver.ts`, `recomputeRunStatus`, adapters.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Worker branch in startDeprovisioningRun + US TZ + worker resolver</name>
  <files>packages/api/src/routers/integrations/deprovisioning.ts</files>
  <read_first>
    - `packages/api/src/routers/integrations/deprovisioning.ts:103-110` (COUNTRY_TZ), `:177-190` (resolveAssignmentForContractor), `:202-345` (startDeprovisioningRun)
    - `packages/idp-saga/src/cooldown.ts` (canStartDeprovisioning signature)
    - `packages/api/src/routers/integrations/__tests__/worker-deprovisioning.test.ts` (Plan 01 RED contract)
    - `.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md` § deprovisioning.ts (THE MAIN NEW BUILD) + § Pitfall 7
    - `.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-RESEARCH.md` § Code Examples (worker cooldown reuse)
  </read_first>
  <behavior>
    - Input is a discriminated union: `{subjectType:'CONTRACTOR', assignmentId}` (existing) OR `{subjectType:'EMPLOYEE', workerId}`.
    - EMPLOYEE branch: `employeeProfile.findFirst({ where:{ workerId, organizationId }, select:{ countryCode, terminatedAt, worker:{ select:{ email } } } })`; not-found → typed error.
    - Cooldown: `canStartDeprovisioning({ endedAt: emp.terminatedAt ?? null, status: emp.terminatedAt ? 'ENDED' : 'ACTIVE', jurisdictionTz: COUNTRY_TZ[emp.countryCode] ?? DEFAULT_JURISDICTION_TZ })`; not-allowed → `FORBIDDEN`/`DEPROVISIONING_COOLDOWN_ACTIVE`.
    - A run created before terminatedAt+14d is BLOCKED; on/after is ALLOWED (test asserts both).
    - `externalUserId = emp.worker.email`; run created with `{ workerId, contractorId:null, assignmentId:null }`.
    - `COUNTRY_TZ['US'] === 'America/New_York'`.
    - Contractor path unchanged (regression); step fan-out + P2002 idempotency identical.
  </behavior>
  <action>
    Add `US: 'America/New_York'` to `COUNTRY_TZ` (Pitfall 7). Convert the mutation input to a `z.discriminatedUnion('subjectType', …)` with the existing contractor-assignment variant and a new `{subjectType:'EMPLOYEE', workerId: z.string().min(1)}` variant (`.strict()`). Branch the handler: for EMPLOYEE, read `EmployeeProfile` (org-scoped) for `countryCode`/`terminatedAt`/`worker.email`, feed the reused `canStartDeprovisioning` with a synthesized `ENDED` status when `terminatedAt` is set, resolve `externalUserId` from `emp.worker.email`, and write `DeprovisioningRun` with `{ workerId: input.workerId, contractorId: null, assignmentId: null }`. Add a `resolveWorkerForDeprovisioning(workerId)` helper OR bypass the assignment resolver entirely for the worker path (assignmentId stays null). Keep the step fan-out (`deriveProvidersForRun`, QStash body with `externalUserId` only), the P2002 idempotency handler, `writeAuditLog` (`auditEvent:'deprovision_run_started'`, resourceType now `EMPLOYEE`), and `integrationProcedure({ permission:{ idp:['start_run'] } })` unchanged. Do NOT edit the step runner, token resolver, cooldown, or adapters.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test worker-deprovisioning idp-deprovisioning-step-runner 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `worker-deprovisioning.test.ts` GREEN: worker run blocks pre-cooldown, allows post-cooldown, writes `{workerId, contractorId:null, assignmentId:null}`, and `COUNTRY_TZ['US']==='America/New_York'`.
    - `idp-deprovisioning-step-runner` regression GREEN (asserts no change to the step runner).
    - `grep -q "terminatedAt" packages/api/src/routers/integrations/deprovisioning.ts` and `grep -q "America/New_York" packages/api/src/routers/integrations/deprovisioning.ts`.
    - `git diff --exit-code apps/api/src/routes/idp-deprovisioning.ts packages/api/src/services/idp-deprovisioning-step-runner.ts packages/idp-saga/src/cooldown.ts` shows NO changes.
    - `pnpm typecheck --filter=@contractor-ops/api` green.
  </acceptance_criteria>
  <done>Employee deprovisioning triggers through the reused saga with a dated-termination cooldown; execution half untouched; Plan 01 worker-deprovisioning contract GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → startDeprovisioningRun | Untrusted `workerId`; server must re-check tenancy + cooldown, never trust UI gate state. |
| trigger → live IdP providers | A run suspends/revokes real accounts; must not fire before cooldown. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-04-01 | Elevation / Tampering | Cooldown bypass to deprovision early | mitigate | Server re-runs `canStartDeprovisioning` from `EmployeeProfile.terminatedAt`; UI cannot bypass; no admin override — earlier deprovision requires editing `terminatedAt` (audited, Plan 08). |
| T-93-04-02 | Tampering | Subject spoofing (worker + assignment both set) | mitigate | Discriminated-union input (exactly one subject) + DB CHECK (Plan 02). |
| T-93-04-03 | Information Disclosure | Cross-org EmployeeProfile/worker read | mitigate | `employeeProfile.findFirst` filtered `organizationId: ctx.organizationId`; tenant-owning model inherits `withTenantScope`. |
| T-93-04-04 | Repudiation | Deprovision start not audited | mitigate | Reused `writeAuditLog`/`auditEvent:'deprovision_run_started'` with `resourceType:'EMPLOYEE'`. |
| T-93-04-SC | Tampering | npm installs | accept | No package installs; slopcheck N/A. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test worker-deprovisioning idp-deprovisioning-step-runner` GREEN.
- `git diff --exit-code` clean on the four UNCHANGED execution-half files.
- `pnpm typecheck --filter=@contractor-ops/api` + `pnpm lint:audit-log` green.
</verification>

<success_criteria>
An employee can be deprovisioned through the existing F2 saga (extends, not duplicates): the trigger reads the employee subject + dated `terminatedAt` cooldown, writes a worker-keyed run with null contractor FKs, and the worker-agnostic step execution proceeds unchanged.
</success_criteria>

<output>
Create `.planning/phases/93-theme-b-employee-on-offboarding/93-04-SUMMARY.md` when done.
</output>
