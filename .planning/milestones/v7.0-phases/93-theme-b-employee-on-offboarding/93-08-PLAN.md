---
phase: 93-theme-b-employee-on-offboarding
plan: 08
type: execute
wave: 3
depends_on: [93-03, 93-05, 93-06, 93-07]
files_modified:
  - packages/api/src/routers/employee/employee-lifecycle-router.ts
  - packages/api/src/routers/employee/__tests__/employee-lifecycle-router.test.ts
  - packages/api/src/root.ts
autonomous: true
requirements: [EMP-ON-01, EMP-OFF-01, EMP-OFF-02]

must_haves:
  truths:
    - "HR can start an onboarding or offboarding run for an employee via a jurisdiction-resolving procedure"
    - "The lifecycle router resolves the correct per-market template by the employee's countryCode -> jurisdiction (via the shared mapCountryCodeToJurisdiction resolver)"
    - "A generateCert procedure produces a DRAFT statutory cert via statutory-cert-pdf; recordTermination writes terminatedAt"
    - "Every lifecycle mutation is flag-gated (assertWorkforceEnabled), tenant-scoped, HR-RBAC-gated, and audit-logged"
    - "The router mounts through the existing conditionalWorkforceRouters spread (flag-off -> METHOD_NOT_FOUND)"
    - "The router calls the single startWorkflowRun helper (Plan 03) — no duplicate tx.workflowRun.create — and reuses mapCountryCodeToJurisdiction (no ad-hoc jurisdiction map)"
    - "The composition seam has a behavioral unit test (jurisdiction->template, flag-off METHOD_NOT_FOUND, audit emission, HR-RBAC)"
  artifacts:
    - path: "packages/api/src/routers/employee/employee-lifecycle-router.ts"
      provides: "start on/offboard + generateCert + recordTermination procedures"
      contains: "assertWorkforceEnabled"
    - path: "packages/api/src/routers/employee/__tests__/employee-lifecycle-router.test.ts"
      provides: "Behavioral proof of the composition seam"
      contains: "METHOD_NOT_FOUND"
    - path: "packages/api/src/root.ts"
      provides: "employeeLifecycle mounted in workforceRouters"
      contains: "employeeLifecycle"
  key_links:
    - from: "employeeLifecycle.startOnboarding/startOffboarding"
      to: "startWorkflowRun helper (Plan 03, workflow-execution-runs.ts)"
      via: "resolve template via mapCountryCodeToJurisdiction then call the single start helper"
      pattern: "startWorkflowRun"
    - from: "employeeLifecycle.generateCert"
      to: "renderStatutoryCert (Plan 06)"
      via: "creates StatutoryCertificate row + renders"
      pattern: "renderStatutoryCert"
---

<objective>
Add the employee-facing entry point that ties the Wave 2 pieces together (EMP-ON-01, EMP-OFF-01, EMP-OFF-02): an `employee-lifecycle-router.ts` exposing start-onboarding / start-offboarding (resolving the correct per-market template by the employee's jurisdiction and starting a worker `WorkflowRun` via the shared `startWorkflowRun` helper), generate-cert (creating + rendering a DRAFT `StatutoryCertificate`), and record-termination (writing `EmployeeProfile.terminatedAt`, audited, feeding the IdP cooldown). Mount it through the existing `conditionalWorkforceRouters` spread and prove it with a behavioral unit test.

Purpose: This is the composition seam — it consumes Plan 03 (worker `startWorkflowRun` helper), Plan 05 (per-market templates), Plan 06 (cert service), Plan 07 (gov stubs). It adds NO new flag/mount block — it extends the existing `workforceRouters` const. Because this is the most load-bearing new file, it ships with a behavioral test (not just typecheck + grep).
Output: lifecycle router + behavioral test + root.ts mount entry.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md
@.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-RESEARCH.md

<interfaces>
- Router shape: mirror `deprovisioningRouter` (`router({ … integrationProcedure … })`) + `startRun` (`tenantProcedure.use(requirePermission(...)).input(zodSchema).mutation(...)`, workflow-execution-runs.ts:38-41).
- `assertWorkforceEnabled(orgId, region)` — `packages/api/src/middleware/require-workforce-flag.ts:25`; call first line of every procedure.
- Mount: `root.ts:175-182` — `const workforceRouters = { worker, employee } as const; const conditionalWorkforceRouters = isWorkforceRegistered() ? workforceRouters : ({} as typeof workforceRouters)`. Add `employeeLifecycle: employeeLifecycleRouter` to the const (do NOT add a new conditional block).
- Worker startRun (SINGLE path — Plan 03): import and call the exported `startWorkflowRun(tx, { subjectType:'EMPLOYEE', workerId, templateId }, { organizationId, actorUserId })` helper from `workflow-execution-runs.ts`. This is the ONLY start path — do NOT replicate `tx.workflowRun.create`. Wrap the resolve+start in the router's own `ctx.db.$transaction` and pass `tx` through.
- Template resolution: derive `jurisdiction` from `EmployeeProfile.countryCode` via `mapCountryCodeToJurisdiction` (`packages/compliance-policy/src/jurisdiction-resolver.ts:36` — REUSE, no ad-hoc PL/DE/GB->UK/US mapping), then read the org's `WorkflowTemplate` where `organizationId` + `jurisdiction` + `type` (ONBOARDING|OFFBOARDING).
- Cert: `renderStatutoryCert` (Plan 06) — create a `StatutoryCertificate` row (snapshotJson from the employee + run) then render.
- HR RBAC (P89): `requirePermission({ employee: ['manage'] })` (confirm the exact employee permission key in `roles.ts` gated to hr_admin/hr_manager/payroll_officer).
- Gov stubs (Plan 07): invoked from the MANUAL task-completion action path; referenced here if the router exposes a complete-gov-task action.
- `writeAuditLog`: on startOnboarding, startOffboarding, generateCert, recordTermination.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: employee-lifecycle-router — start on/offboard + jurisdiction resolution + recordTermination</name>
  <files>packages/api/src/routers/employee/employee-lifecycle-router.ts</files>
  <read_first>
    - `packages/api/src/routers/integrations/deprovisioning.ts:112` (router structure) + `:202` (integrationProcedure mutation shape)
    - `packages/api/src/routers/workflow/workflow-execution-runs.ts:38-140` (the exported `startWorkflowRun` helper this router calls — Plan 03)
    - `packages/compliance-policy/src/jurisdiction-resolver.ts:36` (`mapCountryCodeToJurisdiction` — REUSE; already used by classification-read/submit)
    - `packages/api/src/routers/employee/employee-registry-router.ts` (existing employee router conventions: tenantProcedure, requirePermission, assertWorkforceEnabled)
    - `packages/api/src/middleware/require-workforce-flag.ts:25` (assertWorkforceEnabled)
    - `.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md` § employee-lifecycle-router
  </read_first>
  <action>
    Create `employee-lifecycle-router.ts` exporting `employeeLifecycleRouter = router({ … })`. Each procedure: `assertWorkforceEnabled(ctx.organizationId, ctx.region)` first line, Zod `.strict()` input rejecting injected `organizationId`/`workerType`, HR-role `requirePermission`, `writeAuditLog` on success, all reads/writes scoped by `ctx.organizationId`. Procedures: (1) `startOnboarding({ workerId })` — derive `jurisdiction` from the employee's `countryCode` via `mapCountryCodeToJurisdiction` (compliance-policy resolver — REUSE, no ad-hoc PL/DE/GB->UK/US map), resolve the org's ONBOARDING `WorkflowTemplate` for that jurisdiction (throw a typed NOT_FOUND if unseeded), then start a worker run by calling the exported `startWorkflowRun` helper (Plan 03) with `subjectType:'EMPLOYEE'` inside `ctx.db.$transaction` — NEVER replicate `tx.workflowRun.create`; (2) `startOffboarding({ workerId })` — same, OFFBOARDING template; (3) `recordTermination({ workerId, terminatedAt })` — write `EmployeeProfile.terminatedAt` + transition employmentStatus to TERMINATED, audited, so the IdP cooldown (Plan 04) has its dated signal. Return the created run id(s).
  </action>
  <verify>
    <automated>pnpm typecheck --filter=@contractor-ops/api 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "assertWorkforceEnabled" packages/api/src/routers/employee/employee-lifecycle-router.ts` and `grep -q "terminatedAt" packages/api/src/routers/employee/employee-lifecycle-router.ts`.
    - `startOnboarding`/`startOffboarding` resolve the template via the shared resolver (`grep -q "mapCountryCodeToJurisdiction" employee-lifecycle-router.ts`) and start a `subjectType:'EMPLOYEE'` run through the helper (`grep -q "startWorkflowRun" employee-lifecycle-router.ts`).
    - No duplicate run-create: `! grep -q "workflowRun.create" packages/api/src/routers/employee/employee-lifecycle-router.ts` (the create stays solely in `workflow-execution-runs.ts`).
    - Each mutation calls `writeAuditLog` and `requirePermission` (HR role).
    - `pnpm typecheck --filter=@contractor-ops/api` green; `pnpm lint:audit-log` green.
  </acceptance_criteria>
  <done>Lifecycle router starts jurisdiction-correct employee on/offboarding runs (via the shared resolver + shared start helper) and records the dated termination signal, all flag-gated + RBAC-gated + audited.</done>
</task>

<task type="auto">
  <name>Task 2: generateCert procedure + root.ts mount</name>
  <files>packages/api/src/routers/employee/employee-lifecycle-router.ts, packages/api/src/root.ts</files>
  <read_first>
    - `packages/api/src/services/statutory-cert-pdf.ts` (renderStatutoryCert — Plan 06)
    - `packages/api/src/root.ts:175-182` (workforceRouters const + conditionalWorkforceRouters spread + `:248` `...conditionalWorkforceRouters`)
    - `.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md` § root.ts mount
  </read_first>
  <action>
    Add a `generateCert({ workflowRunId, workerId, certType })` mutation to `employeeLifecycleRouter`: build the immutable `snapshotJson` from the employee + run (via the Plan 06 PII-strip so only `*Last4` persists), create a `StatutoryCertificate` row (status DRAFT, jurisdiction from the run), then call `renderStatutoryCert({ certId })`; return the signed download URL. Flag-gate + RBAC-gate + audit as in Task 1. In `root.ts`, import `employeeLifecycleRouter` and add `employeeLifecycle: employeeLifecycleRouter` to the existing `workforceRouters` const (keep the `as const` + `as typeof` so flag-off keeps client typing constant and returns METHOD_NOT_FOUND). Do NOT add a new conditional block or a new flag.
  </action>
  <verify>
    <automated>pnpm typecheck --filter=@contractor-ops/api 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "renderStatutoryCert" packages/api/src/routers/employee/employee-lifecycle-router.ts`.
    - `grep -q "employeeLifecycle" packages/api/src/root.ts` and the entry sits inside the existing `workforceRouters` const (no new conditional block added).
    - `pnpm typecheck --filter=@contractor-ops/api` green.
    - With `module.workforce-employees` off, `employeeLifecycle.*` is absent from the router (METHOD_NOT_FOUND) — asserted by the Task 3 behavioral test.
  </acceptance_criteria>
  <done>generateCert produces a DRAFT cert via the Plan 06 pipeline; the router mounts through the existing flag-gated spread.</done>
</task>

<task type="auto">
  <name>Task 3: employee-lifecycle-router behavioral unit test</name>
  <files>packages/api/src/routers/employee/__tests__/employee-lifecycle-router.test.ts</files>
  <read_first>
    - `packages/api/src/routers/employee/employee-lifecycle-router.ts` (the router under test — Tasks 1-2)
    - an existing router test under `packages/api/src/routers/employee/__tests__/` (or `integrations/__tests__/`) for harness/mocking + flag-off + createCaller style
    - `packages/api/src/middleware/require-workforce-flag.ts:25` (assertWorkforceEnabled — flag-off path)
    - `.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md` § employee-lifecycle-router
  </read_first>
  <behavior>
    - jurisdiction→template: an EMPLOYEE with `countryCode:'PL'` resolves the org's PL ONBOARDING WorkflowTemplate (via `mapCountryCodeToJurisdiction`) and starts the run against it; an unseeded jurisdiction throws typed NOT_FOUND.
    - flag-off: with `module.workforce-employees` disabled, calling any `employeeLifecycle.*` procedure yields METHOD_NOT_FOUND / `assertWorkforceEnabled` rejection (router absent from the flag-off caller).
    - audit: startOnboarding / startOffboarding / generateCert / recordTermination each emit `writeAuditLog` (resourceType `EMPLOYEE`).
    - HR-RBAC: a non-HR role (lacking the employee HR permission) is rejected; hr_admin/hr_manager/payroll_officer pass.
  </behavior>
  <action>
    Create `employee-lifecycle-router.test.ts` exercising the router via the project's caller/harness (mock tRPC ctx + spy on `writeAuditLog`; stub the Plan 03 `startWorkflowRun` helper + Plan 06 `renderStatutoryCert` as needed). Assert the four behaviors above. Mirror the flag-off assertion pattern used by existing workforce/deprovisioning tests. This is the behavioral proof for the composition seam (complements the typecheck + grep gates in Tasks 1-2).
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test employee-lifecycle-router 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `employee-lifecycle-router.test.ts` is GREEN and collected by vitest (not skipped-empty).
    - All four contract points assert (jurisdiction→template, flag-off METHOD_NOT_FOUND, audit emission, HR-RBAC).
    - `grep -qE "METHOD_NOT_FOUND|assertWorkforceEnabled" packages/api/src/routers/employee/__tests__/employee-lifecycle-router.test.ts` and `grep -q "writeAuditLog" packages/api/src/routers/employee/__tests__/employee-lifecycle-router.test.ts` succeed.
  </acceptance_criteria>
  <done>The load-bearing composition seam has a passing behavioral unit test covering jurisdiction resolution, flag-off, audit, and HR-RBAC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client -> lifecycle mutation | Untrusted `{workerId, terminatedAt, certType}`; server enforces tenancy, flag, RBAC, and audits. |
| flag-off surface | Router must be unreachable when `module.workforce-employees` is off. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-08-01 | Elevation | Flag-off surface still reachable | mitigate | Mounted via `conditionalWorkforceRouters` spread (METHOD_NOT_FOUND) + per-request `assertWorkforceEnabled`; Task 3 asserts flag-off. |
| T-93-08-02 | Elevation / BFLA | Non-HR role runs lifecycle mutation | mitigate | `requirePermission` on the employee HR permission (hr_admin/hr_manager/payroll_officer); owner BFLA fence unchanged (P89); Task 3 asserts HR-RBAC. |
| T-93-08-03 | Tampering | Cooldown bypass via early terminatedAt edit | mitigate | `recordTermination` write is audited; the cooldown is re-computed server-side in Plan 04 (no UI override). |
| T-93-08-04 | Information Disclosure | Cross-org run/template/cert access | mitigate | All reads/writes scoped by `ctx.organizationId`; template resolve filters org + jurisdiction. |
| T-93-08-05 | Repudiation | Lifecycle mutation not audited | mitigate | `writeAuditLog` on start/generateCert/recordTermination with `resourceType:'EMPLOYEE'`; Task 3 asserts emission. |
| T-93-08-SC | Tampering | npm installs | accept | No package installs; slopcheck N/A. |
</threat_model>

<verification>
- `pnpm typecheck --filter=@contractor-ops/api` green.
- `pnpm lint:audit-log` green (all lifecycle mutations audited).
- `pnpm -F @contractor-ops/api test employee-lifecycle-router` GREEN (jurisdiction→template, flag-off METHOD_NOT_FOUND, audit, HR-RBAC).
- Flag-off: `employeeLifecycle.*` absent from the router when `module.workforce-employees` is off.
- Single-create invariant: only `workflow-execution-runs.ts` contains `workflowRun.create` (the router calls the Plan 03 `startWorkflowRun` helper, never a local create).
</verification>

<success_criteria>
HR can start a jurisdiction-correct employee onboarding/offboarding run (via the shared resolver + shared start helper — no duplication), generate a DRAFT statutory cert, and record a dated termination — all flag-gated, tenant-scoped, HR-RBAC-gated, and audited — composing Plans 03/05/06/07 through the existing workforce mount without a new flag or conditional block, proven by a behavioral unit test.
</success_criteria>

<output>
Create `.planning/phases/93-theme-b-employee-on-offboarding/93-08-SUMMARY.md` when done.
</output>
