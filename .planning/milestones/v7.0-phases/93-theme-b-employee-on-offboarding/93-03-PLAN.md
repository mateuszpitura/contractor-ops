---
phase: 93-theme-b-employee-on-offboarding
plan: 03
type: execute
wave: 2
depends_on: [93-02]
files_modified:
  - packages/validators/src/workflow.ts
  - packages/api/src/routers/workflow/workflow-execution-runs.ts
  - packages/api/src/routers/workflow/workflow-shared.ts
autonomous: true
requirements: [EMP-ON-01, EMP-OFF-01, EMP-OFF-02]

must_haves:
  truths:
    - "startRun accepts a discriminated-union subject: exactly one of contractorId | workerId"
    - "The worker branch creates WorkflowRun(entityType=EMPLOYEE, entityId=worker.id, workerId=worker.id, contractorId=null)"
    - "A single exported startWorkflowRun(tx, …) helper owns the WorkflowRun create; both workflow.startRun and employeeLifecycleRouter (Plan 08) call it — no duplicate tx.workflowRun.create"
    - "assertRunCompletable, instantiateTaskRuns, and calculateProgress are reused UNCHANGED"
    - "resolveAssignee supports employee runs via ROLE_BASED/FIXED_USER (never CONTRACTOR_OWNER for employees)"
  artifacts:
    - path: "packages/validators/src/workflow.ts"
      provides: "Discriminated-union startRunSchema (CONTRACTOR | EMPLOYEE)"
      contains: "discriminatedUnion"
    - path: "packages/api/src/routers/workflow/workflow-execution-runs.ts"
      provides: "Worker branch + exported startWorkflowRun helper owning the single WorkflowRun create"
      contains: "startWorkflowRun"
    - path: "packages/api/src/routers/workflow/workflow-shared.ts"
      provides: "Subject-agnostic assignee + notification"
      contains: "ROLE_BASED"
  key_links:
    - from: "startRun worker branch"
      to: "tx.worker.findFirst({ workerType: 'EMPLOYEE' })"
      via: "subject resolution"
      pattern: "workerType.*EMPLOYEE"
    - from: "workflow.startRun (tRPC procedure)"
      to: "startWorkflowRun helper (same module)"
      via: "procedure delegates to the extracted helper (behavior-preserving)"
      pattern: "startWorkflowRun"
    - from: "workflowRun.create"
      to: "instantiateTaskRuns (unchanged)"
      via: "employee subject bag satisfies structural type"
      pattern: "instantiateTaskRuns"
---

<objective>
Add the worker subject path to the v1.0 WorkflowRun start flow so an employee on/offboarding run instantiates through the SAME engine + gate as contractors — the cheap half of the F4 composition (EMP-OFF-02) and the engine both onboarding (EMP-ON-01) and offboarding (EMP-OFF-01) templates run on. Turns `worker-start-run.test.ts` (Plan 01) GREEN.

Purpose: The engine, `assertRunCompletable` gate, `instantiateTaskRuns`, and progress calc are already subject-agnostic. The only work is a discriminated-union input, a worker branch that reads a `Worker(EMPLOYEE)` instead of a contractor, generalizing the assignee/notification so employees (who lack `internalOwnerUserId`) are supported, and extracting a single reusable `startWorkflowRun` helper that Plan 08 will call (so the run-create is never duplicated).
Output: discriminated-union `startRunSchema`, exported `startWorkflowRun` helper + worker branch in `startRun`, subject-agnostic `resolveAssignee` + notification.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md
@.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-RESEARCH.md

<interfaces>
- `startRunSchema` current (validators/workflow.ts:133): flat `z.object({ templateId, contractorId, contractId })`; `StartRunInput = z.infer<...>` exported at :139.
- `startRun` (workflow-execution-runs.ts:38-107): `requirePermission({ workflow: ['execute'] })` :39; contractor lookup `tx.contractor.findFirst` :56-61; `workflowRun.create` with `entityType:'CONTRACTOR'`, `entityId:contractor.id`, `contractorId:contractor.id`; `instantiateTaskRuns(tx, org, runId, template.tasks, contractor, contract, now)`; `writeAuditLog` on start; returns `contractorName` :138.
- Extraction (Plan 08 consumes this): the WorkflowRun create currently lives INLINE in the `startRun` `.mutation()` callback — no exported helper exists (VERIFIED: `workflow-execution-runs.ts` is the ONLY non-test source file containing `workflowRun.create`). Extract a NON-tRPC `startWorkflowRun(tx, input, { organizationId, actorUserId })` in `workflow-execution-runs.ts` that OWNS the single `tx.workflowRun.create` for BOTH subjects; the tRPC `startRun` becomes a thin delegate (behavior-preserving). `employeeLifecycleRouter` (Plan 08) imports and calls this helper — it MUST NOT replicate the create.
- `instantiateTaskRuns` (workflow-execution-shared.ts:196): signature is structurally typed `contractor: { id: string; [k: string]: unknown }` — an employee bag satisfies it; NO change.
- `resolveAssignee` (workflow-shared.ts:134-168): `ROLE_BASED` resolves via `tx.member.findFirst` :150-157; `CONTRACTOR_OWNER`/`CONTRACT_OWNER` read `internalOwnerUserId` :159-162 (employees lack this).
- `assertRunCompletable` (workflow-shared.ts:314-367): keys on `workflowRunId`+org — reuse UNCHANGED.
- HR roles available (P89): `hr_admin`, `hr_manager`, `payroll_officer`, `leave_approver`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Discriminated-union startRunSchema</name>
  <files>packages/validators/src/workflow.ts</files>
  <read_first>
    - `packages/validators/src/workflow.ts:133-139` (startRunSchema + StartRunInput export)
    - `packages/api/src/routers/workflow/__tests__/worker-start-run.test.ts` (Plan 01 RED contract)
    - `.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md` § validators/workflow.ts
  </read_first>
  <behavior>
    - A `{subjectType:'CONTRACTOR', templateId, contractorId, contractId?}` input parses (regression — existing callers unaffected).
    - A `{subjectType:'EMPLOYEE', templateId, workerId}` input parses.
    - An input with both contractorId and workerId, or neither, is rejected by the union.
    - The EMPLOYEE variant is `.strict()` — an injected `organizationId`/`workerType` is rejected (V5).
  </behavior>
  <action>
    Convert `startRunSchema` to `z.discriminatedUnion('subjectType', [...])` with a `CONTRACTOR` variant (`subjectType` literal, `templateId`, `contractorId` min 1, `contractId: optionalFk`) and an `EMPLOYEE` variant (`subjectType` literal, `templateId`, `workerId` min 1), the EMPLOYEE variant `.strict()`. Keep `StartRunInput = z.infer<typeof startRunSchema>` exported. Update any existing `startRun` callers that pass a bare object to include `subjectType:'CONTRACTOR'` (grep callers first).
  </action>
  <verify>
    <automated>pnpm typecheck --filter=@contractor-ops/validators && pnpm -F @contractor-ops/api test worker-start-run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "discriminatedUnion" packages/validators/src/workflow.ts` and `grep -q "subjectType" packages/validators/src/workflow.ts`.
    - `pnpm typecheck --filter=@contractor-ops/validators` exits 0.
    - No caller of `startRunSchema` fails typecheck (all pass `subjectType`).
  </acceptance_criteria>
  <done>startRunSchema is a discriminated union enforcing exactly-one subject; contractor path unchanged; EMPLOYEE variant strict.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Worker branch + extract startWorkflowRun helper</name>
  <files>packages/api/src/routers/workflow/workflow-execution-runs.ts</files>
  <read_first>
    - `packages/api/src/routers/workflow/workflow-execution-runs.ts:38-140` (whole startRun — the inline create to extract)
    - `packages/api/src/routers/workflow/workflow-execution-shared.ts:196` (instantiateTaskRuns structural type)
    - `packages/db/prisma/schema/worker.prisma:12-24` (Worker, workerType, email)
    - `packages/api/src/routers/workflow/__tests__/worker-start-run.test.ts` (Plan 01 RED contract)
    - `.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md` § workflow-execution-runs.ts
  </read_first>
  <behavior>
    - EMPLOYEE input: `tx.worker.findFirst({ where:{ id: input.workerId, organizationId, workerType:'EMPLOYEE', deletedAt:null } })`; not-found → typed error.
    - Creates `workflowRun` with `entityType:'EMPLOYEE'`, `entityId:worker.id`, `workerId:worker.id`, `contractorId:null`.
    - Passes the worker bag to `instantiateTaskRuns` (no signature change).
    - `writeAuditLog` fires on the employee start (resourceType now `EMPLOYEE` — the enum member added in Plan 02).
    - CONTRACTOR path byte-identical behavior (regression assertion in the test).
    - The `tx.workflowRun.create` lives in exactly ONE place: the exported `startWorkflowRun` helper in `workflow-execution-runs.ts`; the tRPC `startRun` delegates to it with no duplicate create anywhere.
  </behavior>
  <action>
    First, extract the run-instantiation body of `startRun` into an exported NON-tRPC helper `startWorkflowRun(tx, input, { organizationId, actorUserId })` in `workflow-execution-runs.ts` that OWNS the single `tx.workflowRun.create`; refactor the existing `startRun` `.mutation()` to delegate to it (behavior-preserving — the CONTRACTOR path stays byte-identical). Then branch inside the helper on `input.subjectType`: for `EMPLOYEE`, replace the `tx.contractor.findFirst` subject lookup with `tx.worker.findFirst` gated on `workerType:'EMPLOYEE'` + `organizationId`; build the create payload with `entityType:'EMPLOYEE'`, `entityId:worker.id`, `workerId:worker.id`, `contractorId:null`, `contractId:null`; pass the worker record as the subject bag to `instantiateTaskRuns` (unchanged). Keep `requirePermission({ workflow:['execute'] })` on the tRPC procedure. Emit `writeAuditLog` with `resourceType:'EMPLOYEE'` + `entityId:worker.id` for the employee start (keep the contractor branch's audit as-is). Do NOT modify `assertRunCompletable`, `calculateProgress`, `unblockDependentsAndRecomputeRun`, or `instantiateTaskRuns`. `employeeLifecycleRouter` (Plan 08) will import and call `startWorkflowRun` — do NOT duplicate the create anywhere else.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test worker-start-run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `worker-start-run.test.ts` is GREEN (worker run created with entityType EMPLOYEE + workerId + null contractorId; contractor regression passes).
    - `grep -q "workerType" packages/api/src/routers/workflow/workflow-execution-runs.ts` and `grep -q "export.*startWorkflowRun" packages/api/src/routers/workflow/workflow-execution-runs.ts`.
    - Single-create invariant: `grep -rl "workflowRun.create" packages/api/src | grep -v "__tests__"` returns EXACTLY `packages/api/src/routers/workflow/workflow-execution-runs.ts` (no other source file creates a WorkflowRun).
    - `git diff --stat packages/api/src/routers/workflow/workflow-execution-shared.ts` shows NO change to `instantiateTaskRuns` signature.
    - `pnpm typecheck --filter=@contractor-ops/api` green.
  </acceptance_criteria>
  <done>startRun instantiates an employee run through the reused engine via a single extracted startWorkflowRun helper; the Plan 01 worker-start-run contract is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Subject-agnostic assignee + notification</name>
  <files>packages/api/src/routers/workflow/workflow-shared.ts</files>
  <read_first>
    - `packages/api/src/routers/workflow/workflow-shared.ts:134-168` (resolveAssignee; owner modes :159-162; ROLE_BASED member lookup :150-157)
    - `packages/api/src/routers/workflow/workflow-execution-runs.ts:138` (returns `contractorName` — generalize to a subject display name)
    - `.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md` § workflow-shared.ts
  </read_first>
  <action>
    Make `resolveAssignee` employee-safe: keep `ROLE_BASED` (resolves HR roles `hr_admin`/`hr_manager`/`payroll_officer`/`leave_approver` via `tx.member.findFirst`) and `FIXED_USER` working for employee runs; for the owner modes (`CONTRACTOR_OWNER`/`CONTRACT_OWNER`) return null gracefully when the subject is an employee (no `internalOwnerUserId`) rather than throwing. Optionally add an `EMPLOYEE_HR_OWNER` mode that resolves the org's `hr_admin`. Generalize the "for {contractorName}" notification/display name at workflow-execution-runs.ts:138 to a subject-agnostic display name (worker full name for employee runs, contractor name for contractor runs). Employee seed templates (Plan 05) will use `ROLE_BASED` assignees.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test workflow-shared worker-start-run 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "ROLE_BASED" packages/api/src/routers/workflow/workflow-shared.ts` and the owner-mode branches no longer throw for a null `internalOwnerUserId`.
    - `workflow-shared` regression test GREEN (contractor assignee resolution unchanged).
    - `pnpm typecheck --filter=@contractor-ops/api` green.
  </acceptance_criteria>
  <done>resolveAssignee + notification are subject-agnostic; employee runs resolve HR-role assignees without owner-mode crashes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → startRun | Untrusted `{templateId, subjectId}`; must not let a caller inject `organizationId`/`workerType` or set both subjects. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-03-01 | Tampering | Subject spoofing (both/neither subject) | mitigate | `z.discriminatedUnion` on `subjectType` (exactly one) + `.strict()` on EMPLOYEE variant + DB CHECK (Plan 02). |
| T-93-03-02 | Information Disclosure | Cross-org worker read in startRun | mitigate | `tx.worker.findFirst` filtered `organizationId: ctx.organizationId` (never client input) + `workerType:'EMPLOYEE'`. |
| T-93-03-03 | Repudiation | Employee run start not audited | mitigate | `writeAuditLog` on employee start with `resourceType:'EMPLOYEE'` + `entityId`. |
| T-93-03-SC | Tampering | npm installs | accept | No package installs; slopcheck N/A. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test worker-start-run workflow-shared` GREEN.
- `pnpm typecheck --filter=@contractor-ops/api --filter=@contractor-ops/validators` green.
- `pnpm lint:audit-log` green (employee start audited).
- Single-create invariant: only `workflow-execution-runs.ts` holds `workflowRun.create` (the extracted `startWorkflowRun` helper is the sole creation path Plan 08 reuses).
</verification>

<success_criteria>
An employee on/offboarding WorkflowRun starts through the reused v1.0 engine + `assertRunCompletable` gate with zero engine changes; the run-create is extracted into one shared `startWorkflowRun` helper (no duplication); the subject invariant is enforced at the type layer; the Plan 01 worker-start-run contract is GREEN.
</success_criteria>

<output>
Create `.planning/phases/93-theme-b-employee-on-offboarding/93-03-SUMMARY.md` when done.
</output>
