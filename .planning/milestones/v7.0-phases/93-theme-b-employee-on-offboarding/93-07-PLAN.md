---
phase: 93-theme-b-employee-on-offboarding
plan: 07
type: execute
wave: 2
depends_on: [93-02]
files_modified:
  - packages/api/src/services/i9-everify-stub.ts
  - packages/api/src/services/zus-zwua-stub.ts
  - packages/api/src/services/abmeldung-sv-stub.ts
  - packages/api/src/services/hmrc-rti-stub.ts
  - packages/api/src/services/pit-filing-stub.ts
autonomous: true
requirements: [EMP-ON-01, EMP-OFF-01]

must_haves:
  truths:
    - "Each gov interaction has a typed, network-free stub seam returning {source:'STUB', available:false, note}"
    - "Stub notes mask PII to last-2 characters"
    - "Steuer-ID lookup reuses the existing lookupElstam (not re-stubbed)"
    - "No stub makes any network call"
  artifacts:
    - path: "packages/api/src/services/zus-zwua-stub.ts"
      provides: "ZUS ZWUA submission seam"
      contains: "STUB"
    - path: "packages/api/src/services/i9-everify-stub.ts"
      provides: "I-9 + E-Verify seam"
      contains: "available: false"
  key_links:
    - from: "gov stub"
      to: "manual WorkflowTaskTemplate"
      via: "seam completed by hand by HR (no live channel)"
      pattern: "source: 'STUB'"
---

<objective>
Ship the D-05 government-integration seams: typed, network-free stub functions for I-9+E-Verify, ZUS ZWUA, Abmeldung SV, HMRC RTI, and PIT-2/PIT-11 filing — each mirroring `elstam-stub.ts`, returning `{source:'STUB', available:false, note}` with PII masked. No live gov calls this phase (local-only). Turns `gov-stubs.test.ts` (Plan 01) GREEN.

Purpose: Each gov interaction becomes a documented seam a later live integration slots into, backed by a MANUAL/NOTIFICATION workflow task the HR user completes by hand (task content lives in Plan 05 templates; the router wires them in Plan 08). Steuer-ID lookup reuses the existing `lookupElstam` — do NOT re-stub it.
Output: 5 gov stub service modules.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md
@.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-RESEARCH.md

<interfaces>
- `elstam-stub.ts` (the idiom): `:18` input interface, `:33-41` STUB result shape (`GovStubResult { source:'STUB'; available:false; note:string }`), `:55-62` network-free body; existing `lookupElstam` for Steuer-ID (reuse — do NOT re-stub).
- Gov-stub test contract: `packages/api/src/services/__tests__/gov-stubs.test.ts` (Plan 01 RED) asserts `{source:'STUB', available:false}`, no network, last-2 PII mask.
- Coverage: I-9 + E-Verify (US onboarding), ZUS ZWUA (PL offboarding), Abmeldung SV (DE offboarding), HMRC RTI (UK on+offboarding), PIT-2 (PL onboarding) / PIT-11 (PL offboarding) filing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Five gov stub seams (mirror elstam-stub)</name>
  <files>packages/api/src/services/i9-everify-stub.ts, packages/api/src/services/zus-zwua-stub.ts, packages/api/src/services/abmeldung-sv-stub.ts, packages/api/src/services/hmrc-rti-stub.ts, packages/api/src/services/pit-filing-stub.ts</files>
  <read_first>
    - `packages/api/src/services/elstam-stub.ts:18-62` (input iface, STUB result shape, network-free body, lookupElstam)
    - `packages/api/src/services/__tests__/gov-stubs.test.ts` (Plan 01 RED contract)
    - `.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md` § gov stubs
  </read_first>
  <behavior>
    - `submitI9EVerify`, `submitZusZwua`, `submitAbmeldungSv`, `submitHmrcRti`, `submitPitFiling` each take a typed input and return `{source:'STUB', available:false, note}` synchronously with no network I/O.
    - `note` masks the subject identifier to last-2 characters (e.g. PESEL/SV-Nr/NINO ending XX).
    - No fetch/undici/HTTP client import in any stub module.
  </behavior>
  <action>
    Define a shared `GovStubResult { source:'STUB'; available:false; note:string }` type (co-locate or import from a small shared module; mirror the elstam-stub shape). Create the five stub modules: `i9-everify-stub.ts` (`I9EVerifyInput { ssnLast4, caseId? }` → note masks E-Verify status pending, no live SSA/DHS channel), `zus-zwua-stub.ts` (`ZwuaSubmitInput { pesel, terminationDate }` → note masks PESEL last-2, no live PUE ZUS channel), `abmeldung-sv-stub.ts` (`AbmeldungSvInput { svNumber, terminationDate }` → note masks SV-Nr last-2, no live DEUEV channel), `hmrc-rti-stub.ts` (`HmrcRtiInput { niNumber, payrollId, eventType }` → note masks NINO last-2, no live HMRC RTI channel), `pit-filing-stub.ts` (`PitFilingInput { pesel, formType: 'PIT-2'|'PIT-11' }` → note masks PESEL last-2, no live e-Deklaracje channel). Each is a pure synchronous function; use `@contractor-ops/logger` only if logging (no `console.*`). Do NOT re-stub Steuer-ID — the Plan 05 DE onboarding template's Steuer-ID step reuses `lookupElstam`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test gov-stubs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `gov-stubs.test.ts` GREEN for all five stubs (STUB/available:false shape + last-2 mask).
    - Network-free proof: `grep -RlE "fetch\(|undici|axios|node:https|node:http" packages/api/src/services/i9-everify-stub.ts packages/api/src/services/zus-zwua-stub.ts packages/api/src/services/abmeldung-sv-stub.ts packages/api/src/services/hmrc-rti-stub.ts packages/api/src/services/pit-filing-stub.ts` returns nothing.
    - `grep -q "STUB" packages/api/src/services/zus-zwua-stub.ts` and `grep -q "available: false" packages/api/src/services/i9-everify-stub.ts`.
    - `pnpm typecheck --filter=@contractor-ops/api` green.
  </acceptance_criteria>
  <done>Five typed, network-free gov stub seams exist and satisfy the Plan 01 gov-stub contract; Steuer-ID reuses lookupElstam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| stub → (future) gov network | This phase makes NO live gov call; the seam must be inert (no network) until a live adapter replaces it. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-07-01 | Information Disclosure | PII (PESEL/SSN/NINO) in stub note/logs | mitigate | Notes mask to last-2 only; no full identifier logged. |
| T-93-07-02 | Spoofing / SSRF | Accidental live outbound call | mitigate | Stubs are pure synchronous functions with no HTTP client import (grep-verified). |
| T-93-07-SC | Tampering | npm installs | accept | No package installs; slopcheck N/A. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test gov-stubs` GREEN.
- No HTTP client import in any stub (grep).
- `pnpm typecheck --filter=@contractor-ops/api` + `lint:logs` green (no `console.*`).
</verification>

<success_criteria>
Every in-scope gov interaction (I-9+E-Verify, ZUS ZWUA, Abmeldung SV, HMRC RTI, PIT-2/PIT-11) has a typed, network-free stub seam returning the STUB/unavailable shape with PII masked, ready to back a MANUAL workflow task; Steuer-ID reuses `lookupElstam`.
</success_criteria>

<output>
Create `.planning/phases/93-theme-b-employee-on-offboarding/93-07-SUMMARY.md` when done.
</output>
