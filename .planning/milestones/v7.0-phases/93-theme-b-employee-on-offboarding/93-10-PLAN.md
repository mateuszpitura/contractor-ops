---
phase: 93-theme-b-employee-on-offboarding
plan: 10
type: execute
wave: 5
depends_on: [93-03, 93-04, 93-05, 93-06, 93-07, 93-08, 93-09]
files_modified:
  - .planning/brain/wiki/domains/worker-onboarding-offboarding.md
  - .planning/brain/wiki/structure/prisma-schema-areas.md
  - .planning/brain/wiki/structure/key-services.md
  - .planning/brain/wiki/structure/api-routers-catalog.md
  - .planning/brain/wiki/patterns/workflow-engine.md
  - .planning/brain/wiki/patterns/offboarding.md
  - .planning/brain/wiki/log.md
  - .planning/brain/wiki/hot.md
  - .planning/MEMORY.md
autonomous: true
requirements: [EMP-ON-01, EMP-OFF-01, EMP-OFF-02]

must_haves:
  truths:
    - "A worker-onboarding-offboarding domain wiki page documents the per-market templates, worker-keyed run/saga, and cert PDFs"
    - "structure pages reflect WorkflowRun.workerId, DeprovisioningRun worker-keying, EmployeeProfile.terminatedAt, StatutoryCertificate, and the new routers/services"
    - "MEMORY.md records the phase invariants (worker-keying, adviser-verify certs, terminatedAt cooldown signal)"
    - "check:wiki-brain passes (no NEW doc drift for the source files this phase touched)"
  artifacts:
    - path: ".planning/brain/wiki/domains/worker-onboarding-offboarding.md"
      provides: "Domain page for employee on/offboarding"
      contains: "worker"
    - path: ".planning/MEMORY.md"
      provides: "Phase 93 invariants"
      contains: "terminatedAt"
  key_links:
    - from: "wiki domain page"
      to: "source files (verify_with)"
      via: "documentation-follows-code"
      pattern: "verify_with"
---

<objective>
Close the documentation-follows-code loop for Phase 93 (CLAUDE.md mandate): a new `worker-onboarding-offboarding` domain page, refreshed structure/pattern pages for the schema + services + routers this phase added, MEMORY invariants, and `check:wiki-brain` green. Doc drift is GATED, not advisory — this is not optional.

Purpose: The next agent must find the worker-keyed run/saga, per-market templates, and adviser-verify cert pipeline in the wiki, not by re-reading source. Update happens in the SAME milestone change set as the code.
Output: 1 new domain page + refreshed structure/pattern pages + log/hot + MEMORY + green wiki gate.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/brain/wiki/hot.md
@.planning/brain/wiki/index.md
@.planning/MEMORY.md
@.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-CONTEXT.md

<interfaces>
- Wiki update matrix (CLAUDE.md §Documentation follows code): new/changed feature -> domain page; new tRPC namespace -> api-routers-catalog + domain; new services -> key-services; new Prisma models/fields -> prisma-schema-areas; new pattern -> patterns/*; any wiki edit -> log.md + overwrite hot.md + rebuild BM25.
- Analog domain page from Phase 89: `.planning/brain/wiki/domains/worker-foundation.md` (structure/frontmatter/`verify_with` shape to mirror).
- Wiki gate: `pnpm check:wiki-brain` fails on NEW drift when a `verify_with` source changed without its page updated in the same change set.
- Refresh pipeline (CLAUDE.md): after edits, run the contextual-prefix + bm25 rebuild commands (graph.json/BM25 are local, gitignored — WARN-only).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain page + structure/pattern refresh</name>
  <files>.planning/brain/wiki/domains/worker-onboarding-offboarding.md, .planning/brain/wiki/structure/prisma-schema-areas.md, .planning/brain/wiki/structure/key-services.md, .planning/brain/wiki/structure/api-routers-catalog.md, .planning/brain/wiki/patterns/workflow-engine.md, .planning/brain/wiki/patterns/offboarding.md</files>
  <read_first>
    - `.planning/brain/wiki/domains/worker-foundation.md` (Phase 89 analog — frontmatter + verify_with shape)
    - `.planning/brain/wiki/structure/{prisma-schema-areas.md,key-services.md,api-routers-catalog.md}` (sections to extend)
    - `.planning/brain/wiki/patterns/{workflow-engine.md,offboarding.md}` (existing pattern pages to extend)
    - the SUMMARY files from Plans 02-09 for the as-built facts
  </read_first>
  <action>
    Create `.planning/brain/wiki/domains/worker-onboarding-offboarding.md` (Purpose, Flow, Entry points, UI surface, Agent mistakes) documenting: worker-keyed `WorkflowRun`/`DeprovisioningRun` (extends v6.0 F4, never duplicates — 4 coupling points), per-market PL/DE/UK/US boot-upserted `WorkflowTemplate`s, adviser-verify-watermarked statutory cert PDFs (v7.0 subset + v7.5 deferrals), gov stub seams, `employeeLifecycleRouter`. Set `verify_with` to the real source paths (deprovisioning.ts, workflow-execution-runs.ts, statutory-cert-pdf.ts, employee-lifecycle-router.ts, employee-templates/*). Extend `structure/prisma-schema-areas.md` (WorkflowRun.workerId, DeprovisioningRun nullable-FK + workerId, EmployeeProfile.terminatedAt, WorkflowTemplate.jurisdiction/seedKey, StatutoryCertificate), `structure/key-services.md` (statutory-cert-pdf + gov stubs + employee-templates), `structure/api-routers-catalog.md` (employeeLifecycle namespace under workforceRouters), and `patterns/{workflow-engine.md,offboarding.md}` (subject-agnostic run + worker-keyed saga). Bump `source_commit` on touched pages if HEAD advanced.
  </action>
  <verify>
    <automated>test -f .planning/brain/wiki/domains/worker-onboarding-offboarding.md && grep -q "verify_with" .planning/brain/wiki/domains/worker-onboarding-offboarding.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `worker-onboarding-offboarding.md` exists with Purpose/Flow/Entry-points/Agent-mistakes sections + `verify_with` listing the real source paths.
    - `prisma-schema-areas.md` mentions `WorkflowRun.workerId`, `DeprovisioningRun` worker-keying, `EmployeeProfile.terminatedAt`, and `StatutoryCertificate`.
    - `api-routers-catalog.md` lists the `employeeLifecycle` namespace.
    - `key-services.md` lists `statutory-cert-pdf` + the gov stubs + `employee-templates`.
  </acceptance_criteria>
  <done>Domain + structure + pattern pages reflect the as-built Phase 93 code.</done>
</task>

<task type="auto">
  <name>Task 2: MEMORY invariants + log/hot + wiki gate</name>
  <files>.planning/MEMORY.md, .planning/brain/wiki/log.md, .planning/brain/wiki/hot.md</files>
  <read_first>
    - `.planning/MEMORY.md` (invariant list format)
    - `.planning/brain/wiki/log.md` + `hot.md` (append/overwrite conventions)
    - CLAUDE.md §Documentation follows code (refresh pipeline commands + `check:wiki-brain`)
  </read_first>
  <action>
    Append Phase 93 invariants to `.planning/MEMORY.md`: (1) `WorkflowRun`/`DeprovisioningRun` are worker-keyed (nullable contractor FKs + `workerId`); the engine, `assertRunCompletable` gate, step runner, token resolver, and `canStartDeprovisioning` cooldown are REUSED UNCHANGED — coupling lives in exactly 4 places; (2) statutory cert PDFs are DRAFT + LOCKED adviser-verify-watermarked, snapshot carries `*Last4` only, absent from `messages/*.json`; (3) `EmployeeProfile.terminatedAt` is the dated termination signal feeding the 14-day IdP cooldown (mirrors `ContractorAssignment.endedAt`); (4) per-market templates seed `WorkflowTemplate` (NOT `WorkflowRoleTemplate`) on the compound `@@unique([organizationId, jurisdiction, type, seedKey])`. Append a `log.md` entry and overwrite `hot.md`. Run the wiki refresh pipeline (contextual-prefix + bm25 rebuild) and `pnpm check:wiki-brain`.
  </action>
  <verify>
    <automated>pnpm check:wiki-brain 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "terminatedAt" .planning/MEMORY.md` and `grep -qi "adviser-verify" .planning/MEMORY.md` and `grep -qi "worker-key" .planning/MEMORY.md`.
    - `.planning/brain/wiki/log.md` has a new Phase 93 entry; `hot.md` overwritten.
    - `pnpm check:wiki-brain` exits 0 (no NEW doc drift for the source files this phase touched).
  </acceptance_criteria>
  <done>MEMORY records the four Phase 93 invariants; log/hot updated; wiki-brain gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| docs vs code | Stale docs mislead the next agent; the wiki must track the code in the same change set. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-10-01 | Repudiation | Doc drift (code changed, wiki stale) | mitigate | `check:wiki-brain` gate green for all touched `verify_with` sources; domain page authored this change set. |
| T-93-10-SC | Tampering | npm installs | accept | No package installs; slopcheck N/A. |
</threat_model>

<verification>
- `pnpm check:wiki-brain` green.
- Domain page + structure/pattern pages exist and reference the real source paths.
- MEMORY invariants present.
</verification>

<success_criteria>
The wiki + MEMORY reflect the as-built Phase 93 worker-keyed run/saga, per-market templates, adviser-verify cert pipeline, gov stubs, and lifecycle router; `check:wiki-brain` passes — documentation follows code in the same change set.
</success_criteria>

<output>
Create `.planning/phases/93-theme-b-employee-on-offboarding/93-10-SUMMARY.md` when done.
</output>
