---
phase: 93-theme-b-employee-on-offboarding
plan: 09
type: execute
wave: 4
depends_on: [93-04, 93-08]
files_modified:
  - apps/web-vite/src/components/idp/hooks/use-start-deprovisioning.ts
  - apps/web-vite/src/components/idp/deprovisioning-trigger.tsx
  - apps/web-vite/src/components/employees/hooks/use-employee-lifecycle.ts
  - apps/web-vite/src/components/employees/employee-lifecycle-container.tsx
  - apps/web-vite/src/components/employees/employee-lifecycle-panel.tsx
  - apps/web-vite/src/pages/dashboard/employees/lifecycle.tsx
  - apps/web-vite/messages/en.json
  - apps/web-vite/messages/de.json
  - apps/web-vite/messages/pl.json
  - apps/web-vite/messages/ar.json
  - apps/web-vite/messages/en-US.json
autonomous: true
requirements: [EMP-ON-01, EMP-OFF-01, EMP-OFF-02]

must_haves:
  truths:
    - "HR can trigger IdP deprovisioning for an employee via a worker-keyed hook variant (workerId passed straight through)"
    - "HR can start on/offboarding and generate a cert from an employee lifecycle surface"
    - "HR can record a termination date (recordTermination); the employee IdP deprovisioning trigger is actionable ONLY after a termination date is set"
    - "The run view reuses workflows/workflow-run/* by runId (subject-agnostic)"
    - "Every surface has loading + empty + error states and WCAG (keyboard, focus, semantic HTML, contrast)"
    - "i18n parity across en/de/pl/ar (+ en-US); no hardcoded user-facing strings"
    - "web-vite layering holds: Page thin -> Container -> Hook (sole tRPC boundary) -> Component"
  artifacts:
    - path: "apps/web-vite/src/components/employees/hooks/use-employee-lifecycle.ts"
      provides: "Sole tRPC boundary for employee lifecycle (start on/offboard, generateCert, recordTermination)"
      contains: "recordTermination"
    - path: "apps/web-vite/src/components/employees/employee-lifecycle-panel.tsx"
      provides: "Lifecycle actions + termination-gated IdP deprovisioning trigger"
      contains: "terminatedAt"
    - path: "apps/web-vite/src/components/idp/hooks/use-start-deprovisioning.ts"
      provides: "Worker-keyed deprovisioning trigger variant"
      contains: "workerId"
  key_links:
    - from: "use-employee-lifecycle.ts"
      to: "trpc.employeeLifecycle.startOnboarding/startOffboarding/generateCert/recordTermination"
      via: "sole tRPC boundary"
      pattern: "employeeLifecycle"
    - from: "employee-lifecycle-panel.tsx IdP deprovisioning trigger"
      to: "terminatedAt gate (worker-keyed DeprovisioningTriggerWired)"
      via: "trigger disabled / 'not eligible' until terminatedAt set; server re-runs canStartDeprovisioning"
      pattern: "terminatedAt"
    - from: "use-start-deprovisioning.ts worker path"
      to: "trpc.deprovisioning.startDeprovisioningRun (subjectType EMPLOYEE)"
      via: "workerId passed straight through"
      pattern: "workerId"
---

<objective>
Ship the web-vite UI for employee on/offboarding (EMP-ON-01, EMP-OFF-01, EMP-OFF-02): a worker-keyed variant of the IdP deprovisioning trigger hook + a new employee lifecycle surface (start on/offboarding, generate cert, record termination — the gate that makes the IdP trigger eligible — and view run) that REUSES the subject-agnostic `workflows/workflow-run/*` components by `runId`. Full loading/empty/error states, WCAG, i18n parity (en/de/pl/ar/en-US), and strict Page→Container→Hook→Component layering.

Purpose: The run-view and IdP-run-view components are already subject-agnostic — the new client work is a worker-keyed trigger path, the record-termination action, and a thin lifecycle surface. Without a UI path to record a termination date, `EmployeeProfile.terminatedAt` stays null and the worker IdP deprovisioning trigger (which the server hard-gates on the dated cooldown) can never functionally succeed — so recording termination is the reachability spine, not an extra. Keep the hook as the sole tRPC boundary so `check:web-vite-data-layer` stays green.
Output: worker deprovisioning hook variant + trigger prop; employee lifecycle Page/Container/Hook/Component with record-termination + termination-gated IdP trigger; i18n across all 5 locales.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web-vite/ARCHITECTURE.md
@.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md

<interfaces>
- `use-start-deprovisioning.ts:44-125` — dual-path today (`assignmentId` direct OR `contractorId` -> server resolver `resolveAssignmentForContractor` :55-62). Add a `workerId?` path that passes `workerId` straight through (no server resolve; `assignmentId` stays null) into `startDeprovisioningRun` with `subjectType:'EMPLOYEE'`.
- `deprovisioning-trigger.tsx:142-169` — `DeprovisioningTriggerWired` gates on `permissions.can('idp', ['start_run'])`, renders loading/error/unresolved states; extend props with `workerId?` + a `disabledReason?` (for the termination gate).
- Reuse UNCHANGED by `runId`: `workflows/workflow-run/*` (`task-checklist.tsx:12`, `task-card-run.tsx:108`, `run-header.tsx`, `task-attachments.tsx`, `task-comments.tsx`), `idp/deprovisioning-run-view.tsx:124`.
- Layering (Pitfall 5): Page = thin composer (Suspense/permissions, NO tRPC); Container = calls the domain hook, section loading/empty/error; Hook = SOLE tRPC boundary; Component = presentational.
- Project UI memory: AnimateIn entrance convention; Dialog body/footer (DialogBody scroll + DialogFooter sticky) — enforced by `check:web-vite-dialog-pattern`.
- i18n: `useTranslations`; parity en/de/pl/ar + en-US; NO hardcoded strings; the cert adviser-verify disclaimer is a LOCKED const (Plan 06) and must NOT be added to messages.
- Employee lifecycle tRPC: `trpc.employeeLifecycle.startOnboarding/startOffboarding/generateCert/recordTermination` (Plan 08), gated behind `useFlag('module.workforce-employees')`.
- recordTermination gate (BLOCKER spine): the lifecycle panel exposes a "Record Termination" action (termination-date input) calling `employeeLifecycle.recordTermination({ workerId, terminatedAt })`, gated by the SAME HR-RBAC + flag as the other mutations (server-enforced; client gate is UX only). The hook surfaces the employee's current `terminatedAt` (read via the existing Phase 90 employee query, now carrying the Plan 02 field) so the panel disables / marks "not eligible" the worker-keyed IdP deprovisioning trigger until a termination date exists. The 14-day cooldown stays server-side (Plan 04 re-runs `canStartDeprovisioning`) — the client MUST NOT bypass it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Worker-keyed deprovisioning trigger variant</name>
  <files>apps/web-vite/src/components/idp/hooks/use-start-deprovisioning.ts, apps/web-vite/src/components/idp/deprovisioning-trigger.tsx</files>
  <read_first>
    - `apps/web-vite/src/components/idp/hooks/use-start-deprovisioning.ts:44-125` (dual-path hook)
    - `apps/web-vite/src/components/idp/deprovisioning-trigger.tsx:140-169` (DeprovisioningTriggerWired)
    - `.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md` § use-start-deprovisioning
  </read_first>
  <action>
    Add a `workerId?` input path to `use-start-deprovisioning.ts`: when `workerId` is provided (and no `assignmentId`/`contractorId`), skip the server resolver query and call `startDeprovisioningRun` with `{ subjectType:'EMPLOYEE', workerId }`; `assignmentId` stays null. Keep the existing contractor/assignment paths byte-identical. Extend `DeprovisioningTriggerWired` props with `workerId?` and an optional `disabledReason?` (so the lifecycle panel can render a "not eligible until termination recorded" state); keep the `permissions.can('idp', ['start_run'])` gate and the loading/error/unresolved render states. Keep the hook as the sole tRPC boundary (no tRPC in the component).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "workerId" apps/web-vite/src/components/idp/hooks/use-start-deprovisioning.ts` and the worker path passes `subjectType:'EMPLOYEE'`.
    - `grep -q "workerId" apps/web-vite/src/components/idp/deprovisioning-trigger.tsx`.
    - `pnpm check:web-vite-data-layer` green (no tRPC leaked into the component).
    - web-vite typecheck green.
  </acceptance_criteria>
  <done>The IdP trigger deprovisions an employee by workerId through the existing hook; contractor paths unchanged; data-layer guard green.</done>
</task>

<task type="auto">
  <name>Task 2: Employee lifecycle surface (Page -> Container -> Hook -> Component) with record-termination + termination-gated IdP trigger</name>
  <files>apps/web-vite/src/pages/dashboard/employees/lifecycle.tsx, apps/web-vite/src/components/employees/employee-lifecycle-container.tsx, apps/web-vite/src/components/employees/hooks/use-employee-lifecycle.ts, apps/web-vite/src/components/employees/employee-lifecycle-panel.tsx</files>
  <read_first>
    - `apps/web-vite/ARCHITECTURE.md` (Page/Container/Hook/Component contract)
    - `apps/web-vite/src/components/workflows/workflow-run/task-checklist.tsx` + `pages/dashboard/workflows/detail.tsx:72` (reuse by runId)
    - existing employee registry container/hook from Phase 90 (`apps/web-vite/src/components/employees/`) for conventions + the employee read that now carries `terminatedAt`
    - project memory: AnimateIn entrance + Dialog body/footer pattern
  </read_first>
  <action>
    Create the lifecycle surface: `pages/dashboard/employees/lifecycle.tsx` (thin composer — Suspense + `useFlag('module.workforce-employees')` gate + `permissions`, NO tRPC); `employee-lifecycle-container.tsx` (calls `use-employee-lifecycle`, renders section loading/empty/error states); `use-employee-lifecycle.ts` (SOLE tRPC boundary — `startOnboarding`, `startOffboarding`, `generateCert`, `recordTermination` mutations + a read that surfaces the employee's current `terminatedAt`/employmentStatus via the existing Phase 90 employee query); `employee-lifecycle-panel.tsx` (presentational — start on/offboard actions, cert download, a "Record Termination" action with a termination-date input calling `recordTermination`, embeds the reused `workflow-run/*` run view by `runId`, and the worker-keyed IdP deprovisioning trigger from Task 1). GATE the IdP trigger: render it disabled with a "not eligible until termination recorded" affordance (`disabledReason`) while `terminatedAt` is null, enabled once set — the 14-day cooldown remains server-enforced (Plan 04 re-runs `canStartDeprovisioning`; the client never bypasses). All lifecycle actions are HR-only in the UI (mirror the server P89 HR-RBAC gate; server is authoritative). Use AnimateIn entrance; any dialog uses DialogBody (scroll) + DialogFooter (sticky). All user-facing text via `useTranslations` under a new `EmployeeLifecycle` namespace — no hardcoded strings. Include loading, empty (no runs yet), and error states on every data section; WCAG keyboard/focus/semantic-HTML/contrast.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Page contains NO `useTRPC`/`useQuery`/`useMutation` (thin composer); Container has no direct tRPC; the hook is the only tRPC caller.
    - `pnpm check:web-vite-data-layer` and `pnpm check:web-vite-dialog-pattern` green.
    - `grep -q "employeeLifecycle" apps/web-vite/src/components/employees/hooks/use-employee-lifecycle.ts` and `grep -q "recordTermination" apps/web-vite/src/components/employees/hooks/use-employee-lifecycle.ts`.
    - `grep -q "recordTermination" apps/web-vite/src/components/employees/employee-lifecycle-panel.tsx`; the IdP deprovisioning trigger is rendered disabled/"not eligible" while `terminatedAt` is null and enabled once set (assert the conditional in the panel).
    - Loading/empty/error states present in the container; no hardcoded user-facing strings (i18n hardcoded-string guard green).
    - web-vite typecheck green.
  </acceptance_criteria>
  <done>Employee lifecycle surface works with correct layering, all three section states, WCAG, records terminations, gates the IdP trigger on terminatedAt, and reuses the run view by runId.</done>
</task>

<task type="auto">
  <name>Task 3: i18n parity across en/de/pl/ar/en-US</name>
  <files>apps/web-vite/messages/en.json, apps/web-vite/messages/de.json, apps/web-vite/messages/pl.json, apps/web-vite/messages/ar.json, apps/web-vite/messages/en-US.json</files>
  <read_first>
    - `apps/web-vite/messages/en.json` (existing key structure + a recent v7.0 namespace for shape)
    - the i18n:parity check script (root `package.json`)
  </read_first>
  <action>
    Add the `EmployeeLifecycle` namespace keys (actions incl. record-termination + termination-date label, run-status labels, cert-download, the IdP "not eligible until termination recorded" affordance, section empty/error copy) to `en.json` (base American-neutral), then achieve full key parity in `de.json`, `pl.json`, `ar.json` (RTL), and thin `en-US.json` overrides. Do NOT add any cert adviser-verify disclaimer text to messages (it is a LOCKED const — locked-phrases-guard enforces absence). Provide real translations (not English placeholders) for de/pl/ar; ar strings are machine-acceptable pending native review (recorded post-merge item).
  </action>
  <verify>
    <automated>pnpm i18n:parity 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm i18n:parity` green — `EmployeeLifecycle` keys present at parity in en/de/pl/ar (+ en-US fallback-aware).
    - No cert disclaimer literal appears in any `messages/*.json` (locked-phrases-guard green).
    - No hardcoded-string guard violations for the new surface.
  </acceptance_criteria>
  <done>Employee lifecycle UI is fully localized at parity across all five locales with disclaimers kept out of i18n.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| SPA -> tRPC | The hook is the only boundary; server re-enforces tenancy/flag/RBAC (client gate is UX only). |
| flag-off render | Surface must not render employee lifecycle when `module.workforce-employees` is off. |
| termination gate | UI disables the IdP trigger until `terminatedAt` is set; the server cooldown gate is authoritative. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-09-01 | Elevation | Flag-off surface rendered client-side | mitigate | Page gates on `useFlag('module.workforce-employees')`; server returns METHOD_NOT_FOUND regardless (Plan 08). |
| T-93-09-02 | Information Disclosure | tRPC in Page/Component leaks data-layer | mitigate | Hook is sole tRPC boundary; `check:web-vite-data-layer` enforces. |
| T-93-09-03 | Repudiation / liability | Cert disclaimer diverges via i18n edit | mitigate | Disclaimer stays LOCKED const (Plan 06), absent from messages; locked-phrases-guard enforces. |
| T-93-09-04 | Tampering / Elevation | Client bypasses the termination gate to trigger early IdP deprovisioning | mitigate | The `terminatedAt` gate is UX only; the server (Plan 04) re-runs `canStartDeprovisioning` from `EmployeeProfile.terminatedAt` on every `startDeprovisioningRun` — no client override; `recordTermination` is HR-RBAC-gated + audited (Plan 08). |
| T-93-09-SC | Tampering | npm installs | accept | No package installs; slopcheck N/A. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/web-vite exec tsc --noEmit` green.
- `pnpm check:web-vite-data-layer` + `pnpm check:web-vite-dialog-pattern` green.
- `pnpm i18n:parity` green; locked-phrases-guard green.
- IdP deprovisioning trigger is not actionable until a termination date is recorded (disabled/"not eligible"), and actionable after — the cooldown stays server-enforced.
- MEMORY: never run the full web-vite suite unscoped — scope any web-vite test by path.
</verification>

<success_criteria>
HR can, from web-vite, start jurisdiction-correct employee on/offboarding, generate a DRAFT cert, record a termination date (the gate that makes the worker IdP deprovisioning trigger eligible), view the reused run, and trigger employee IdP deprovisioning by workerId — with correct layering, loading/empty/error states, WCAG, and full i18n parity across en/de/pl/ar/en-US.
</success_criteria>

<output>
Create `.planning/phases/93-theme-b-employee-on-offboarding/93-09-SUMMARY.md` when done.
</output>
