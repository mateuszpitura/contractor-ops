---
phase: 93-theme-b-employee-on-offboarding
plan: 06
type: execute
wave: 2
depends_on: [93-02]
files_modified:
  - packages/validators/src/legal/disclaimers.ts
  - packages/api/src/services/statutory-cert-pdf.ts
  - packages/api/src/pdf-templates/swiadectwo-pracy.tsx
  - packages/api/src/pdf-templates/pit-11.tsx
  - packages/api/src/pdf-templates/arbeitszeugnis-simple.tsx
  - packages/api/src/pdf-templates/lohnsteuerbescheinigung.tsx
  - packages/api/src/pdf-templates/p45.tsx
  - packages/api/src/pdf-templates/w2.tsx
autonomous: true
requirements: [EMP-OFF-01]

must_haves:
  truths:
    - "renderStatutoryCert builds an immutable snapshot carrying only *Last4 fields (never a full national ID)"
    - "A CAS double-render guard skips a second render when pdfArchiveKey is already set"
    - "Every cert renders a prominent LOCKED adviser-verify disclaimer + DRAFT status"
    - "The R2 archive key is org-scoped (emp-cert/<orgId>/<id>.pdf)"
    - "v7.0 cert subset shipped: swiadectwo pracy, PIT-11, simple Arbeitszeugnis, Lohnsteuerbescheinigung, P45, W-2; qualified Arbeitszeugnis/P11D/COBRA/401k deferred v7.5"
    - "Cert disclaimer strings are LOCKED const in disclaimers.ts and ABSENT from messages/*.json"
  artifacts:
    - path: "packages/api/src/services/statutory-cert-pdf.ts"
      provides: "render → immutable snapshot → R2 CAS archive"
      contains: "pdfArchiveKey"
    - path: "packages/validators/src/legal/disclaimers.ts"
      provides: "CERT_ADVISER_VERIFY_EN/DE/PL locked const + registry entries"
      contains: "CERT_ADVISER_VERIFY"
    - path: "packages/api/src/pdf-templates/swiadectwo-pracy.tsx"
      provides: "PL swiadectwo pracy react-pdf template"
      contains: "CERT_ADVISER_VERIFY"
  key_links:
    - from: "statutory-cert-pdf.ts"
      to: "StatutoryCertificate.pdfArchiveKey (CAS guard)"
      via: "updateMany where pdfArchiveKey null"
      pattern: "pdfArchiveKey: null"
    - from: "cert template"
      to: "disclaimers.CERT_ADVISER_VERIFY_*"
      via: "prominent watermark/footer"
      pattern: "CERT_ADVISER_VERIFY"
---

<objective>
Generate DRAFT statutory termination-paperwork PDFs on the EXISTING render→immutable-snapshot→R2-CAS infra (D-04, EMP-OFF-01): a `statutory-cert-pdf.ts` service mirroring `form-1099-nec-pdf.ts` + NEW `@react-pdf/renderer` templates for the v7.0 cert subset, each PROMINENTLY watermarked with a LOCKED adviser-verify disclaimer. Turns `statutory-cert-pdf.test.ts` + the cert half of the cross-org leak test (Plan 01) GREEN.

Purpose: Solo-founder liability + legal DEFERRED — certs are DRAFT + adviser-verify-watermarked, never presented as adviser-approved. Snapshots carry `*Last4` only, never a full national ID.
Output: locked disclaimer const + registry, cert service, 6 react-pdf templates.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md
@.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-RESEARCH.md

<interfaces>
- `form-1099-nec-pdf.ts`: snapshot type :24, CAS guard `updateMany({ where:{ id, pdfArchiveKey:null } })` :115-122 (count 0 → short-circuit skipped), lazy `const { renderToBuffer } = await import('@react-pdf/renderer')` :40, org-scoped key :57, `putObjectAndSignDownload({ key, body, contentType, ttlSeconds:60 })` :127-134, `createLogger` :21, TIN last-4 only :28.
- `ir35-sds.tsx`: `import { Document, Page, StyleSheet, Text, View } from '@react-pdf/renderer'` :16; `TEMPLATE_VERSION`/`RENDERER_SLUG` const :18-19; `StyleSheet.create` :65 — the legal-doc precedent.
- `disclaimers.ts`: `SDS_DISCLAIMER_EN` :28 style; `RESERVED_DISCLAIMER_KEYS` :111-124 + `LOCKED_DISCLAIMERS` :126-139 (register a new key in BOTH); locked strings must be ABSENT from `messages/*.json` (locked-phrases-guard).
- `StatutoryCertificate` model (Plan 02): `snapshotJson`, `pdfArchiveKey`, `workflowRunId`, `workerId`, `certType`, `jurisdiction`, `status`.
- `buildFormSnapshot` recursive strip precedent: tax-form.service.ts:14 (strip full-SSN/TIN keys, keep *Last4).
- `putObjectAndSignDownload`: `packages/api/src/services/r2.ts` (R2 CAS; EU bucket for PL/DE/UK, US bucket lazy-throws if `R2_BUCKET_NAME_US` unset).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locked adviser-verify disclaimer const + registry</name>
  <files>packages/validators/src/legal/disclaimers.ts</files>
  <read_first>
    - `packages/validators/src/legal/disclaimers.ts:28-33` (SDS_DISCLAIMER_EN style), `:111-124` (RESERVED_DISCLAIMER_KEYS), `:126-141` (LOCKED_DISCLAIMERS + LockedDisclaimerKey)
    - the locked-phrases-guard test (search `packages/validators` / `apps/web-vite` for `locked-phrases`)
  </read_first>
  <action>
    Add `CERT_ADVISER_VERIFY_EN`, `CERT_ADVISER_VERIFY_DE`, `CERT_ADVISER_VERIFY_PL` locked const strings (each stating the document is a DRAFT and needs verification by a jurisdiction-specific legal/tax adviser before use, and does not constitute legal advice). Register all three keys in BOTH `RESERVED_DISCLAIMER_KEYS` and `LOCKED_DISCLAIMERS` so the locked-phrases-guard automatically enforces their absence from `messages/*.json`. Export them from the validators package barrel so `@contractor-ops/api` pdf-templates can import them.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/validators typecheck 2>&1 | tail -8 && grep -v '^//' packages/validators/src/legal/disclaimers.ts | grep -c "CERT_ADVISER_VERIFY"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' packages/validators/src/legal/disclaimers.ts | grep -c "CERT_ADVISER_VERIFY"` ≥ 6 (3 defs + 3 registry refs).
    - All three keys appear in `RESERVED_DISCLAIMER_KEYS` and `LOCKED_DISCLAIMERS`.
    - Absence proof: the literal disclaimer text and keys are absent from every `apps/web-vite/messages/*.json` (locked-phrases-guard test GREEN).
    - `pnpm --filter @contractor-ops/validators typecheck` green.
  </acceptance_criteria>
  <done>Adviser-verify disclaimers are locked const, registered, and CI-guarded absent from i18n.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: statutory-cert-pdf service (snapshot + CAS + R2) — turns cert tests GREEN</name>
  <files>packages/api/src/services/statutory-cert-pdf.ts</files>
  <read_first>
    - `packages/api/src/services/form-1099-nec-pdf.ts` (whole file — render→CAS→R2)
    - `packages/api/src/services/tax-form.service.ts:14` (buildFormSnapshot recursive strip)
    - `packages/api/src/services/r2.ts` (putObjectAndSignDownload)
    - `packages/api/src/services/__tests__/statutory-cert-pdf.test.ts` + `packages/api/src/routers/__tests__/employee-lifecycle-cross-org.test.ts` (Plan 01 RED contracts)
    - `.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md` § statutory-cert-pdf.ts
  </read_first>
  <behavior>
    - `renderStatutoryCert({ certId })` builds an immutable snapshot from the row's `snapshotJson` (values-as-of-generation), recursively stripping any full-national-ID key (pesel/ssn/nino/steuerId/iqama/emiratesId), keeping only `*Last4`.
    - CAS: `updateMany({ where:{ id, pdfArchiveKey:null }, data:{ pdfArchiveKey } })` — count 0 → return skipped with no render/upload.
    - The rendered buffer embeds `CERT_ADVISER_VERIFY_*` + a DRAFT status marker.
    - R2 key is `emp-cert/<orgId>/<certId>.pdf` (org-scoped).
    - Cross-org: a cert row for another org is unreadable/unwritable (withTenantScope) — cross-leak cert assertions GREEN.
  </behavior>
  <action>
    Create `statutory-cert-pdf.ts` mirroring `form-1099-nec-pdf.ts`: a `CertRenderSnapshot` type (only `*Last4` + display fields), a recursive PII strip reusing/mirroring `buildFormSnapshot`, the CAS `updateMany` guard on `StatutoryCertificate.pdfArchiveKey`, a lazy `await import('@react-pdf/renderer')` render dispatching on `certType`/`jurisdiction` to the correct template component (Task 3), org-scoped key `emp-cert/${orgId}/${certId}.pdf`, `putObjectAndSignDownload` with `ttlSeconds:60`, and `createLogger({ service:'statutory-cert-pdf' })`. All Prisma reads/writes scoped by `organizationId`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test statutory-cert-pdf employee-lifecycle-cross-org 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `statutory-cert-pdf.test.ts` GREEN (snapshot *Last4-only, CAS skip on second call, disclaimer present).
    - The cert-row assertions in `employee-lifecycle-cross-org.test.ts` GREEN (org-scoped, `emp-cert/<orgId>/` key).
    - `grep -q "pdfArchiveKey: null" packages/api/src/services/statutory-cert-pdf.ts` and `grep -q "emp-cert/" packages/api/src/services/statutory-cert-pdf.ts`.
    - `pnpm typecheck --filter=@contractor-ops/api` green.
  </acceptance_criteria>
  <done>Cert render+snapshot+CAS+R2 pipeline works, PII-safe and idempotent; Plan 01 cert contracts GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: v7.0 react-pdf cert templates (subset) with adviser-verify watermark</name>
  <files>packages/api/src/pdf-templates/swiadectwo-pracy.tsx, packages/api/src/pdf-templates/pit-11.tsx, packages/api/src/pdf-templates/arbeitszeugnis-simple.tsx, packages/api/src/pdf-templates/lohnsteuerbescheinigung.tsx, packages/api/src/pdf-templates/p45.tsx, packages/api/src/pdf-templates/w2.tsx</files>
  <read_first>
    - `packages/api/src/pdf-templates/ir35-sds.tsx:1-70` (Document/Page/StyleSheet legal-doc precedent)
    - `packages/api/src/pdf-templates/form-1099-nec-copy-b.tsx` (structured-form precedent)
    - `.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md` § pdf-templates (v7.0 subset A3/Open-Q2)
  </read_first>
  <action>
    Create the v7.0 cert subset templates: `swiadectwo-pracy.tsx`, `pit-11.tsx` (PL), `arbeitszeugnis-simple.tsx`, `lohnsteuerbescheinigung.tsx` (DE), `p45.tsx` (UK), `w2.tsx` (US). Each mirrors `ir35-sds.tsx`: `Document`/`Page`/`Text`/`View` + `StyleSheet.create`, `TEMPLATE_VERSION`/`RENDERER_SLUG` const, reads ONLY from the immutable snapshot (never live constants), a STABLE `renderedAt` for byte stability, and a PROMINENT fixed watermark/footer rendering the jurisdiction-appropriate `CERT_ADVISER_VERIFY_*` disclaimer + a `DRAFT` status band on every page. Record in the plan summary that qualified (free-text) Arbeitszeugnis, P11D, COBRA, and 401k packets are DEFERRED to v7.5 (per D-04 / research A3).
  </action>
  <verify>
    <automated>pnpm typecheck --filter=@contractor-ops/api 2>&1 | tail -8 && for f in swiadectwo-pracy pit-11 arbeitszeugnis-simple lohnsteuerbescheinigung p45 w2; do grep -q "CERT_ADVISER_VERIFY" packages/api/src/pdf-templates/$f.tsx && echo "$f OK" || echo "$f MISSING"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 6 template files exist and each greps positive for `CERT_ADVISER_VERIFY` (adviser-verify watermark present).
    - Each template reads only from the snapshot prop (no import of live domain constants).
    - `pnpm typecheck --filter=@contractor-ops/api` green.
    - Summary records the v7.5 deferral list (qualified Arbeitszeugnis, P11D, COBRA, 401k).
  </acceptance_criteria>
  <done>The v7.0 cert subset renders as DRAFT with a prominent adviser-verify watermark; deferrals recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| snapshot → PDF/R2 | A cert embeds employee identity data; must not carry full national IDs and must be org-scoped in storage. |
| cert artifact → reader | A DRAFT cert must never read as adviser-approved (liability). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-06-01 | Information Disclosure | Full national ID in snapshot/PDF | mitigate | Recursive strip (mirror `buildFormSnapshot`) — keep only `*Last4`; asserted in test. |
| T-93-06-02 | Information Disclosure | Cross-org cert read via R2 key guess | mitigate | Org-scoped key `emp-cert/<orgId>/<id>.pdf` + tenant-scoped row read + two-org leak test. |
| T-93-06-03 | Repudiation / liability | Cert presented as legal advice | mitigate | Prominent LOCKED `CERT_ADVISER_VERIFY_*` watermark + DRAFT band; signoff-registry PENDING. |
| T-93-06-04 | Tampering | Double-render produces divergent archives | mitigate | CAS `updateMany where pdfArchiveKey null` claims the slot; second render skipped. |
| T-93-06-SC | Tampering | npm installs | accept | No package installs (`@react-pdf/renderer` already in-tree ^4.5.1); slopcheck N/A. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test statutory-cert-pdf employee-lifecycle-cross-org` GREEN.
- `pnpm --filter @contractor-ops/validators typecheck` + locked-phrases-guard GREEN (disclaimers absent from messages).
- `pnpm typecheck --filter=@contractor-ops/api` green.
</verification>

<success_criteria>
The v7.0 statutory cert subset generates as immutable, PII-safe (*Last4-only), org-scoped, CAS-guarded DRAFT PDFs carrying a prominent LOCKED adviser-verify watermark; deferrals to v7.5 are recorded; Plan 01 cert + cross-leak contracts are GREEN.
</success_criteria>

<output>
Create `.planning/phases/93-theme-b-employee-on-offboarding/93-06-SUMMARY.md` when done.
</output>
