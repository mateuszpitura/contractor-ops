---
phase: 93-theme-b-employee-on-offboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/routers/workflow/__tests__/worker-start-run.test.ts
  - packages/api/src/routers/integrations/__tests__/worker-deprovisioning.test.ts
  - packages/api/src/services/__tests__/statutory-cert-pdf.test.ts
  - packages/api/src/services/__tests__/gov-stubs.test.ts
  - packages/api/src/routers/__tests__/employee-lifecycle-cross-org.test.ts
autonomous: true
requirements: [EMP-ON-01, EMP-OFF-01, EMP-OFF-02]

must_haves:
  truths:
    - "Failing (RED) test files exist that pin the worker-keyed startRun subject contract"
    - "Failing (RED) test files exist that pin the worker-keyed DeprovisioningRun cooldown + nullable-FK contract"
    - "Failing (RED) test files exist that pin the statutory-cert snapshot+CAS+disclaimer contract and the gov-stub network-free shape"
    - "A two-org cross-leak regression scaffold exists for StatutoryCertificate + per-market template rows"
  artifacts:
    - path: "packages/api/src/routers/workflow/__tests__/worker-start-run.test.ts"
      provides: "RED contract for EMP-OFF-02 worker startRun branch"
      contains: "entityType"
    - path: "packages/api/src/routers/integrations/__tests__/worker-deprovisioning.test.ts"
      provides: "RED contract for worker deprovisioning cooldown + nullable FKs"
      contains: "terminatedAt"
    - path: "packages/api/src/services/__tests__/statutory-cert-pdf.test.ts"
      provides: "RED contract for cert snapshot/CAS/disclaimer"
      contains: "pdfArchiveKey"
    - path: "packages/api/src/services/__tests__/gov-stubs.test.ts"
      provides: "RED contract for gov stub STUB/unavailable shape"
      contains: "STUB"
    - path: "packages/api/src/routers/__tests__/employee-lifecycle-cross-org.test.ts"
      provides: "RED two-org IDOR regression for new tenant-owning models"
      contains: "organizationId"
  key_links:
    - from: "worker-start-run.test.ts"
      to: "workflow-execution-runs.startRun worker branch"
      via: "asserts WorkflowRun(entityType=EMPLOYEE, workerId, contractorId=null)"
      pattern: "entityType.*EMPLOYEE"
---

<objective>
Seed the Wave 0 RED test scaffolds enumerated in `93-VALIDATION.md` § Wave 0 Requirements so every downstream impl plan has a failing contract to turn GREEN. This is the Nyquist floor for the phase: worker-keyed startRun, worker-keyed deprovisioning cooldown, statutory-cert render/CAS/disclaimer, gov-stub shape, and a two-org cross-leak regression.

Purpose: Lock the observable behavior of each seam BEFORE implementation so executors implement to a proof, not a description.
Output: 5 new `*.test.ts` files that are terminal-RED (fail via not-yet-existing branch / Cannot-find-module / missing prisma-client property). RED is the accepted terminal state for this plan.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-VALIDATION.md
@.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-RESEARCH.md

<interfaces>
<!-- Existing analog tests the executor should mirror for style/harness. Read them once, copy the harness shape. -->
- Mirror harness style from `packages/api/src/services/__tests__/` existing service tests and `packages/api/src/routers/integrations/__tests__/` existing deprovisioning tests.
- ELStAM stub test shape (the gov-stub analog): search `packages/api/src/services/__tests__/` for the elstam-stub test.
- Cross-org leak test shape: prior art in Phase 89 (`Worker cross-org leak`, per STATE.md 89-05) — search `packages/api` for existing `cross-org` / `cross-leak` tests.

Current-state facts the scaffolds encode as the TARGET (not yet true):
- `WorkflowRun` has no `workerId` (workflow.prisma:105-147); `startRun` hardcodes `entityType:'CONTRACTOR'` (workflow-execution-runs.ts:38+).
- `DeprovisioningRun.contractorId`/`assignmentId` are NON-NULL (idp-deprovisioning.prisma:11-12); no `workerId`.
- `EmployeeProfile` has no `terminatedAt` (employee.prisma:12-55).
- No `statutory-cert-pdf.ts`, no `*-stub.ts` gov seams beyond `elstam-stub.ts`, no `StatutoryCertificate` model.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED scaffolds for worker-keyed startRun + worker deprovisioning</name>
  <files>packages/api/src/routers/workflow/__tests__/worker-start-run.test.ts, packages/api/src/routers/integrations/__tests__/worker-deprovisioning.test.ts</files>
  <read_first>
    - `packages/api/src/routers/workflow/workflow-execution-runs.ts` (startRun :38-107 — contractor branch to fork)
    - `packages/api/src/routers/integrations/deprovisioning.ts` (startDeprovisioningRun :202-345, COUNTRY_TZ :103-110, cooldown reuse :225-234)
    - `packages/idp-saga/src/cooldown.ts` (canStartDeprovisioning — the pure gate the worker path reuses)
    - `.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-RESEARCH.md` § Code Examples (worker cooldown reuse block)
    - an existing router test under `packages/api/src/routers/integrations/__tests__/` for harness/mocking style
  </read_first>
  <action>
    Create `packages/api/src/routers/workflow/__tests__/worker-start-run.test.ts` asserting that a worker-subject `startRun` input (`subjectType:'EMPLOYEE'`, `workerId`) creates a `WorkflowRun` with `entityType='EMPLOYEE'`, `entityId=worker.id`, `workerId=worker.id`, `contractorId=null`, and that `instantiateTaskRuns` is invoked with the employee subject bag. Assert the CONTRACTOR path is unchanged (regression). Create `packages/api/src/routers/integrations/__tests__/worker-deprovisioning.test.ts` asserting: (1) the worker branch reads `EmployeeProfile.terminatedAt` + `EmployeeProfile.countryCode` + `Worker.email`, feeds `canStartDeprovisioning({endedAt:terminatedAt, status:'ENDED', jurisdictionTz})`, and blocks pre-cooldown with `FORBIDDEN`/`DEPROVISIONING_COOLDOWN_ACTIVE`; (2) on allow, creates `DeprovisioningRun{workerId, contractorId:null, assignmentId:null}`; (3) `COUNTRY_TZ['US']` resolves to `America/New_York` (Pitfall 7). These fail terminal-RED because the worker branch and schema do not yet exist — that is the expected state.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test worker-start-run worker-deprovisioning 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and are collected by vitest (not skipped-empty).
    - The run exits non-zero (RED) with failures pointing at the missing worker branch / missing `workerId` / missing `terminatedAt` — NOT at a syntax/harness error.
    - Source assertion: `grep -q "entityType" packages/api/src/routers/workflow/__tests__/worker-start-run.test.ts` and `grep -q "America/New_York" packages/api/src/routers/integrations/__tests__/worker-deprovisioning.test.ts` both succeed.
  </acceptance_criteria>
  <done>worker-start-run + worker-deprovisioning tests exist, RED for the right reason, encoding the EMP-OFF-02 subject + cooldown contract.</done>
</task>

<task type="auto">
  <name>Task 2: RED scaffolds for statutory-cert PDF + gov stubs</name>
  <files>packages/api/src/services/__tests__/statutory-cert-pdf.test.ts, packages/api/src/services/__tests__/gov-stubs.test.ts</files>
  <read_first>
    - `packages/api/src/services/form-1099-nec-pdf.ts` (snapshot :24, CAS guard :115-122, render :40, R2 key :57, sign :127-134)
    - `packages/api/src/services/elstam-stub.ts` (:18 input iface, :33-41 STUB result shape, :55-62 body)
    - `packages/api/src/services/__tests__/` existing elstam-stub test (mirror the network-free assertion)
    - `.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md` § statutory-cert-pdf + § gov stub sections
  </read_first>
  <action>
    Create `packages/api/src/services/__tests__/statutory-cert-pdf.test.ts` asserting: (1) `renderStatutoryCert` builds an immutable snapshot carrying only `*Last4` fields (never a full national ID key such as `pesel`/`ssn`/`nino`); (2) the CAS double-render guard — a second call with `pdfArchiveKey` already set returns `{skipped:true}` and does NOT re-render/re-upload; (3) the rendered document contains the locked adviser-verify disclaimer text (`CERT_ADVISER_VERIFY_EN`). Create `packages/api/src/services/__tests__/gov-stubs.test.ts` asserting each gov stub (`submitZusZwua`, `submitAbmeldungSv`, `submitHmrcRti`, `submitI9EVerify`, `submitPitFiling`) returns `{source:'STUB', available:false, note:<string>}`, makes no network call (no fetch/undici invocation), and masks PII to last-2 in `note`. Both files fail terminal-RED via Cannot-find-module (services do not exist yet).
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test statutory-cert-pdf gov-stubs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and are collected by vitest.
    - Run exits non-zero (RED) via Cannot-find-module for `statutory-cert-pdf`/`*-stub` — NOT a harness error.
    - Source assertion: `grep -q "skipped" packages/api/src/services/__tests__/statutory-cert-pdf.test.ts` and `grep -q "STUB" packages/api/src/services/__tests__/gov-stubs.test.ts` both succeed.
  </acceptance_criteria>
  <done>Cert-PDF + gov-stub contracts scaffolded RED, encoding the EMP-OFF-01 immutable-snapshot/CAS/disclaimer + D-05 stub-seam shape.</done>
</task>

<task type="auto">
  <name>Task 3: RED two-org cross-leak regression for new tenant-owning models</name>
  <files>packages/api/src/routers/__tests__/employee-lifecycle-cross-org.test.ts</files>
  <read_first>
    - `packages/db/prisma/schema/worker.prisma:8-10` + `employee.prisma:7-10` (tenant-owning header comments — models absent from `globalModels`, inherit `withTenantScope`)
    - STATE.md 89-05 note (`Worker cross-org leak test` prior art) and the v7.0 standing decision "two-org cross-leak test per new model"
    - `.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-RESEARCH.md` § Security Domain (IDOR row)
  </read_first>
  <action>
    Create `packages/api/src/routers/__tests__/employee-lifecycle-cross-org.test.ts` asserting: an ORG_A-scoped client reading `db.statutoryCertificate` and the per-market `db.workflowTemplate` (jurisdiction-seeded) rows NEVER returns ORG_B rows, and a write attempting to attach a StatutoryCertificate to another org's `workflowRunId` is rejected under `withTenantScope`. Assert the R2 archive key is org-scoped (`emp-cert/<orgId>/…`). Fails terminal-RED because `StatutoryCertificate` (prisma-client property) and the jurisdiction-seeded templates do not yet exist.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test employee-lifecycle-cross-org 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists, collected by vitest, exits non-zero (RED) via missing `statutoryCertificate` property / missing seed — NOT a harness error.
    - Source assertion: `grep -q "emp-cert/" packages/api/src/routers/__tests__/employee-lifecycle-cross-org.test.ts` succeeds.
  </acceptance_criteria>
  <done>Cross-org IDOR regression scaffolded RED for StatutoryCertificate + per-market template rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → prisma-client | Scaffolds reference not-yet-existing models; must not brick typecheck (api tsconfig excludes `__tests__`). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01-01 | Tampering | RED scaffold false-green | mitigate | Each scaffold must fail for the RIGHT reason (missing branch/module/property), verified in acceptance; not a silent skip. |
| T-93-01-SC | Tampering | npm installs | accept | No package installs in this plan; slopcheck N/A. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test worker-start-run worker-deprovisioning statutory-cert-pdf gov-stubs employee-lifecycle-cross-org` collects all 5 files and exits RED for the documented reasons.
- `pnpm typecheck --filter=@contractor-ops/api` stays green (tsconfig excludes `__tests__`).
</verification>

<success_criteria>
All 5 Wave 0 test files from `93-VALIDATION.md` exist, are collected, and are terminal-RED for the correct (missing-implementation) reason — giving every Wave 2 impl plan a failing contract to satisfy.
</success_criteria>

<output>
Create `.planning/phases/93-theme-b-employee-on-offboarding/93-01-SUMMARY.md` when done.
</output>
