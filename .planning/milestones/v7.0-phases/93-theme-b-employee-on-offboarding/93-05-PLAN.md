---
phase: 93-theme-b-employee-on-offboarding
plan: 05
type: execute
wave: 2
depends_on: [93-02]
files_modified:
  - packages/employee-templates/package.json
  - packages/employee-templates/src/index.ts
  - packages/employee-templates/src/types.ts
  - packages/employee-templates/src/content/pl.ts
  - packages/employee-templates/src/content/de.ts
  - packages/employee-templates/src/content/uk.ts
  - packages/employee-templates/src/content/us.ts
  - packages/employee-templates/src/upsert-on-boot.ts
  - packages/employee-templates/src/__tests__/upsert-on-boot.test.ts
  - packages/api/src/services/post-org-create-hook.ts
autonomous: true
requirements: [EMP-ON-01, EMP-OFF-01]

must_haves:
  truths:
    - "PL/DE/UK/US × {ONBOARDING, OFFBOARDING} = 8 WorkflowTemplate seeds exist as typed-const content keyed by Jurisdiction"
    - "Boot upsert materializes the 8 templates per org idempotently on @@unique([organizationId, jurisdiction, type, seedKey])"
    - "Re-running the boot hook produces NO duplicate rows (idempotent)"
    - "Seed rows are status DRAFT + appliesToEntityType EMPLOYEE so orgs review/activate/edit"
    - "Per-market step lists cover the ROADMAP criteria 1+2 statutory steps (with adviser-verify annotations + gov-stub/cert task markers)"
  artifacts:
    - path: "packages/employee-templates/src/upsert-on-boot.ts"
      provides: "Idempotent WorkflowTemplate + WorkflowTaskTemplate upsert"
      contains: "workflowTemplate.upsert"
    - path: "packages/employee-templates/src/content/pl.ts"
      provides: "PL onboarding + offboarding step lists"
      contains: "badania"
    - path: "packages/api/src/services/post-org-create-hook.ts"
      provides: "Boot-hook wiring for employee market templates"
      contains: "upsertEmployeeMarketTemplates"
  key_links:
    - from: "runPostOrganizationCreateHooks"
      to: "upsertEmployeeMarketTemplates"
      via: "fail-soft boot hook"
      pattern: "upsertEmployeeMarketTemplates"
    - from: "upsert-on-boot"
      to: "@@unique([organizationId, jurisdiction, type, seedKey])"
      via: "idempotent compound-unique upsert"
      pattern: "organizationId_jurisdiction_type_seedKey"
---

<objective>
Ship PL/DE/UK/US onboarding + offboarding step templates as boot-upserted `WorkflowTemplate`/`WorkflowTaskTemplate` rows (EMP-ON-01, EMP-OFF-01, D-03). Reuse the `offboarding-templates` boot-upsert PATTERN but retarget it at `WorkflowTemplate` (what `startRun` instantiates) — NOT `WorkflowRoleTemplate` (the KT registry). Content is authored as `Jurisdiction`-keyed typed-const, materialized DRAFT + org-editable on boot.

Purpose: These are the per-market templates that `startRun` (Plan 03) instantiates and the lifecycle router (Plan 08) resolves by jurisdiction. Idempotency is enforced by the new compound unique from Plan 02.
Output: new `packages/employee-templates` package (content + upsert), boot-hook wiring, idempotency test GREEN.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md
@.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-RESEARCH.md

<interfaces>
- Retarget the boot-upsert idiom from `packages/offboarding-templates/src/upsert-on-boot.ts` (upsert-on-compound-unique) and `seeds.ts` (typed-const seed shape) — but write `prisma.workflowTemplate.upsert`/`workflowTaskTemplate`, keyed on `organizationId_jurisdiction_type_seedKey`.
- `runPostOrganizationCreateHooks(prisma, organizationId)` (post-org-create-hook.ts:14-28) — fail-soft (logged, NOT re-thrown); add `await upsertEmployeeMarketTemplates(prisma, organizationId)` next to the existing `upsertSeedTemplates` call.
- `Jurisdiction = 'UK'|'DE'|'PL'|'US'|'KSA'|'UAE'` from `@contractor-ops/compliance-policy` (types.ts:6) — import the TYPE only (no edit to compliance-policy).
- `WorkflowTemplateType` already has `ONBOARDING`/`OFFBOARDING`; seed 8 rows = 4 jurisdictions × 2 types, `status:'DRAFT'`, `appliesToEntityType:'EMPLOYEE'`.
- Task assignees use `ROLE_BASED` (HR roles) per Plan 03 — never CONTRACTOR_OWNER.
- Idempotency test analog: `packages/offboarding-templates/src/__tests__/upsert-on-boot.test.ts`.

Per-market step lists (ROADMAP criteria 1+2 — author as task step-lists; gov-backed steps carry a `govStub` marker, cert-emitting offboarding steps carry a `certType` marker for Plan 06/08):
- PL ON: badania wstępne (MANUAL), PIT-2 (DOCUMENT_COLLECTION), PPK auto-zapis (MANUAL), IKE/IKZE (NOTIFICATION). PL OFF: świadectwo pracy (cert), ekwiwalent za urlop (reads P92 leave balance — MANUAL for now), ZUS ZWUA (govStub), PIT-11 (cert).
- DE ON: Personalfragebogen (DOCUMENT_COLLECTION), Steuer-ID lookup (reuse existing lookupElstam), SV-Ausweis (MANUAL), bAV (NOTIFICATION). DE OFF: Arbeitszeugnis simple (cert), Abmeldung SV (govStub), Lohnsteuerbescheinigung (cert).
- UK ON: P45/P46 (DOCUMENT_COLLECTION), RTI flag (govStub), pension auto-enrol (NOTIFICATION). UK OFF: P45 (cert), final RTI (govStub), pension (MANUAL), P11D (MANUAL — cert deferred v7.5).
- US ON: W-4 (DOCUMENT_COLLECTION), I-9 + E-Verify (govStub), state W-4 (DOCUMENT_COLLECTION), direct-deposit (MANUAL). US OFF: final paycheck per state (MANUAL — feeds P94), COBRA (MANUAL — packet deferred v7.5), W-2 (cert), 401k (MANUAL — deferred v7.5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold employee-templates package + Jurisdiction-keyed content</name>
  <files>packages/employee-templates/package.json, packages/employee-templates/src/index.ts, packages/employee-templates/src/types.ts, packages/employee-templates/src/content/pl.ts, packages/employee-templates/src/content/de.ts, packages/employee-templates/src/content/uk.ts, packages/employee-templates/src/content/us.ts</files>
  <read_first>
    - `packages/offboarding-templates/src/{seeds.ts,types.ts,index.ts,package.json}` (package shape + typed-const seed idiom)
    - `packages/compliance-policy/src/types.ts:6` (Jurisdiction type)
    - `packages/db/prisma/schema/workflow.prisma:4-24` (WorkflowTemplate + WorkflowTaskTemplate fields) and `:255-257` (WorkflowTemplateType ONBOARDING/OFFBOARDING)
    - `.planning/milestones/v7.0-phases/93-theme-b-employee-on-offboarding/93-PATTERNS.md` § post-org-create-hook + employee-templates
  </read_first>
  <action>
    Create the `@contractor-ops/employee-templates` workspace package (mirror `offboarding-templates/package.json` deps + build config; add `@contractor-ops/compliance-policy` for the `Jurisdiction` type and `@contractor-ops/db` for Prisma types). In `src/types.ts` define a `MarketTemplateSeed` type (jurisdiction, type ONBOARDING|OFFBOARDING, seedKey, name, tasks[] where each task carries `title`, `taskType` (MANUAL|DOCUMENT_COLLECTION|NOTIFICATION|…), `assigneeMode:'ROLE_BASED'`, `assigneeRole`, optional `govStub` marker, optional `certType` marker, optional `adviserVerify:boolean` annotation, `sortOrder`). In `src/content/{pl,de,uk,us}.ts` author the ON+OFF step lists per the interfaces block above (statutory steps flagged `adviserVerify:true`), and register them into a `Jurisdiction`-keyed const map via register-on-import (mirror compliance-policy `index.ts` side-effect registration). Export the aggregated `ALL_MARKET_TEMPLATE_SEEDS` from `src/index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/employee-templates typecheck 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `test -f packages/employee-templates/src/content/pl.ts` … `us.ts` (4 files).
    - `grep -q "badania" packages/employee-templates/src/content/pl.ts`, `grep -q "Personalfragebogen" packages/employee-templates/src/content/de.ts`, `grep -qi "P45" packages/employee-templates/src/content/uk.ts`, `grep -qi "I-9\|E-Verify" packages/employee-templates/src/content/us.ts`.
    - Exactly 8 seeds (4 jurisdictions × ONBOARDING+OFFBOARDING) in `ALL_MARKET_TEMPLATE_SEEDS`.
    - `pnpm --filter @contractor-ops/employee-templates typecheck` exits 0.
  </acceptance_criteria>
  <done>employee-templates package + 8 Jurisdiction-keyed typed-const seeds with statutory steps + adviser-verify annotations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Idempotent boot-upsert + idempotency test</name>
  <files>packages/employee-templates/src/upsert-on-boot.ts, packages/employee-templates/src/__tests__/upsert-on-boot.test.ts</files>
  <read_first>
    - `packages/offboarding-templates/src/upsert-on-boot.ts` (upsert-on-compound-unique idiom)
    - `packages/offboarding-templates/src/__tests__/upsert-on-boot.test.ts` (idempotency test analog)
    - `packages/db/prisma/schema/workflow.prisma` (WorkflowTemplate `@@unique([organizationId, jurisdiction, type, seedKey])` added Plan 02)
  </read_first>
  <behavior>
    - First upsert for an org creates 8 WorkflowTemplate rows (DRAFT, appliesToEntityType EMPLOYEE) + their WorkflowTaskTemplate children.
    - Second upsert (same org) creates ZERO new rows and ZERO duplicate task templates (idempotent on the compound unique).
    - An org that already hand-edited a seeded template keeps its edits on re-run of the seed content fields that are `update`-guarded (do not clobber name/status if org-modified — mirror the seed package's update posture).
  </behavior>
  <action>
    In `src/upsert-on-boot.ts` implement `upsertEmployeeMarketTemplates(prisma, organizationId)` iterating `ALL_MARKET_TEMPLATE_SEEDS`, calling `prisma.workflowTemplate.upsert({ where:{ organizationId_jurisdiction_type_seedKey:{ organizationId, jurisdiction, type, seedKey } }, create:{ …, status:'DRAFT', appliesToEntityType:'EMPLOYEE' }, update:{ … } })` then upserting each child `WorkflowTaskTemplate` keyed by a stable per-task key. Write `src/__tests__/upsert-on-boot.test.ts` (mirror the offboarding-templates test) asserting first-run creates 8 templates and a second run creates zero duplicates.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/employee-templates test upsert-on-boot 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `upsert-on-boot.test.ts` GREEN: first run = 8 templates; second run = no dup templates/tasks.
    - `grep -q "workflowTemplate.upsert" packages/employee-templates/src/upsert-on-boot.ts`.
    - `pnpm --filter @contractor-ops/employee-templates typecheck` green.
  </acceptance_criteria>
  <done>Boot upsert is idempotent on the compound unique; the idempotency contract is GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Wire into runPostOrganizationCreateHooks</name>
  <files>packages/api/src/services/post-org-create-hook.ts</files>
  <read_first>
    - `packages/api/src/services/post-org-create-hook.ts:14-28` (fail-soft hook; existing upsertSeedTemplates call)
  </read_first>
  <action>
    Add `@contractor-ops/employee-templates` as a dependency of `@contractor-ops/api`, import `upsertEmployeeMarketTemplates`, and call it inside `runPostOrganizationCreateHooks` alongside `upsertSeedTemplates` — inside the existing try/catch so a seed failure is LOGGED (`createLogger`), NOT re-thrown (boot must not fail on seed error). Keep the existing offboarding KT seed call untouched.
  </action>
  <verify>
    <automated>pnpm typecheck --filter=@contractor-ops/api 2>&1 | tail -10 && grep -q "upsertEmployeeMarketTemplates" packages/api/src/services/post-org-create-hook.ts && echo WIRED</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "upsertEmployeeMarketTemplates" packages/api/src/services/post-org-create-hook.ts`.
    - The call is inside the existing try/catch (fail-soft) — a thrown seed error does not propagate.
    - `pnpm typecheck --filter=@contractor-ops/api` green.
  </acceptance_criteria>
  <done>Every new org boots with the 8 per-market employee templates upserted fail-soft.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| boot hook → per-org template rows | Seed runs per org at creation; must be org-scoped + idempotent, must not fail org creation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-05-01 | Information Disclosure | Template rows leak cross-org | mitigate | Upsert always scoped by `organizationId`; compound unique includes org; two-org leak test (Plan 01 → Plan 06 covers template rows). |
| T-93-05-02 | Denial of Service | Seed error aborts org creation | mitigate | Fail-soft try/catch in `runPostOrganizationCreateHooks` (logged, not re-thrown). |
| T-93-05-03 | Repudiation / liability | Statutory step presented as vetted | mitigate | Seeds ship `status:'DRAFT'` + `adviserVerify:true` annotations; orgs must review/activate. |
| T-93-05-SC | Tampering | new workspace package | accept | `employee-templates` is a first-party workspace package (no external install); slopcheck N/A. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/employee-templates test upsert-on-boot` GREEN + typecheck green.
- `pnpm typecheck --filter=@contractor-ops/api` green (hook wired).
- Manual: 8 seeds = 4 jurisdictions × 2 types confirmed by grep count.
</verification>

<success_criteria>
Per-market PL/DE/UK/US onboarding + offboarding templates exist for every org on boot (idempotent, DRAFT, org-editable), covering the ROADMAP criteria 1+2 statutory steps with adviser-verify annotations and gov-stub/cert task markers — ready for `startRun` (Plan 03) and the lifecycle router (Plan 08).
</success_criteria>

<output>
Create `.planning/phases/93-theme-b-employee-on-offboarding/93-05-SUMMARY.md` when done.
</output>
