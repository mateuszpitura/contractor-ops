---
phase: 91-theme-b-akta-osobowe-personnel-file
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/db/src/__tests__/personnel-retention.test.ts
  - packages/compliance-policy/src/__tests__/personnel-registry.test.ts
  - packages/auth/src/__tests__/personnel-file-rbac.test.ts
  - packages/api/src/__tests__/personnel-file-tenant-isolation.test.ts
  - packages/api/src/__tests__/personnel-file-rbac-router.test.ts
  - packages/api/src/__tests__/personnel-erasure.test.ts
  - packages/api/src/__tests__/personnel-classifier.test.ts
autonomous: true
requirements: [AKTA-01, AKTA-02, AKTA-03, AKTA-04]

must_haves:
  truths:
    - "A failing (RED) automated test exists for every one of AKTA-01..04 plus the cross-org leak invariant before any implementation lands"
    - "Each test encodes the locked behavior (per-section RBAC + owner/contractor fence, retention anchor math incl. US I-9 max() and indefinite-while-active, never-claim-full-erasure, taxonomy/AI/killswitch/low-confidence routing, cross-org isolation)"
    - "Each RED test is terminal-RED via missing module/export or assertion failure and does NOT brick its package typecheck"
  artifacts:
    - path: "packages/db/src/__tests__/personnel-retention.test.ts"
      provides: "AKTA-02 resolver math (4 jurisdictions + max() + indefinite)"
      contains: "getPersonnelRetentionCutoff"
    - path: "packages/compliance-policy/src/__tests__/personnel-registry.test.ts"
      provides: "registry register-on-import + no duplicate-id throw + every recordType exists in RETENTION_YEARS"
    - path: "packages/auth/src/__tests__/personnel-file-rbac.test.ts"
      provides: "AKTA-01 structural: employeeFileA..D in statement, absent from owner allPermissions, wired into 4 HR roles"
    - path: "packages/api/src/__tests__/personnel-file-tenant-isolation.test.ts"
      provides: "PersonnelFile absent from globalModels + ORG_A never sees ORG_B file"
    - path: "packages/api/src/__tests__/personnel-file-rbac-router.test.ts"
      provides: "AKTA-01 behavioral: payroll_officer gets locked section B over the wire"
    - path: "packages/api/src/__tests__/personnel-erasure.test.ts"
      provides: "AKTA-03 per-section disposition + fullErasureClaimed===false under any hold"
    - path: "packages/api/src/__tests__/personnel-classifier.test.ts"
      provides: "AKTA-04 taxonomy-hit/AI-fallback/killswitch-off→admin/low-confidence→admin"
  key_links:
    - from: "packages/api/src/__tests__/personnel-file-tenant-isolation.test.ts"
      to: "packages/db/src/tenant.ts globalModels"
      via: "structural source read assertion"
      pattern: "globalModels"
---

<objective>
Lay the Wave-0 RED test scaffolds that pin every locked behavior of Phase 91 BEFORE any
implementation exists. These are the Nyquist contract: each of AKTA-01..04 plus the cross-org
leak invariant gets a failing automated test that later plans turn GREEN.

Purpose: prevent false-positive verification — impl plans must satisfy a pre-written test, not a
test written to match whatever shipped.
Output: 7 RED test files across packages/db, packages/compliance-policy, packages/auth, packages/api.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-RESEARCH.md
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-VALIDATION.md

Cross-org leak harness to clone (mock-Prisma): packages/api/src/__tests__/worker-tenant-isolation.test.ts (verified :294-353).
RBAC source-of-truth to assert against: packages/auth/src/permissions.ts (accessControlStatement) + packages/auth/src/roles.ts (allPermissions :20-45, 4 HR roles :191-218).
Retention shape the resolver must satisfy: packages/db/src/retention-policy.ts (getRetentionCutoff returns null = "no rule / never purge").
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED scaffolds — retention resolver (db) + registry (compliance-policy)</name>
  <files>packages/db/src/__tests__/personnel-retention.test.ts, packages/compliance-policy/src/__tests__/personnel-registry.test.ts</files>
  <read_first>
    - packages/db/src/retention-policy.ts (current getRetentionCutoff null semantics)
    - packages/db/tsconfig.json (confirm src/**/__tests__ excluded so a missing-module import is terminal-RED without bricking tsc)
    - packages/compliance-policy/src/doc-registry.ts (register-on-import idiom + duplicate-id throw)
    - packages/compliance-policy/tsconfig.json (excludes src/**/__tests__ — confirmed)
    - packages/compliance-policy/src/jurisdiction-resolver.ts + types.ts (Jurisdiction = 'UK'|'DE'|'PL'|'US'|'KSA'|'UAE')
  </read_first>
  <action>
    Author personnel-retention.test.ts importing the not-yet-existing getPersonnelRetentionCutoff from '@contractor-ops/db' (terminal-RED via missing export). Encode these cases against the locked D-04 math: (a) PL post-2019 — TERMINATION_DATE anchor + 10y → retainUntil = terminationDate + 10y, erasable only when now >= retainUntil; (b) DE de-personalakte-tax 10y and de-accident-records 30y; (c) UK uk-personnel-general 6y / uk-personnel-financial 7y; (d) US I-9 max(HIRE_DATE+3y, TERMINATION_DATE+1y) returns the LATER cutoff; (e) active employee (terminationDate=null) where a rule needs TERMINATION_DATE → retainUntil null = retain indefinitely, erasable false; (f) if ANY rule across a section yields indefinite, the section is indefinite. Use the documented signature getPersonnelRetentionCutoff(rules, { hireDate, terminationDate, documentDate, now }) → { erasable, retainUntil, citation }.
    Author personnel-registry.test.ts importing the not-yet-existing getPersonnelSections, resolveSectionForDocumentType, getPersonnelRetentionRules from '@contractor-ops/compliance-policy' (terminal-RED via missing export). Assert: PL/DE/UK/US each register 4 sections SECTION_A..D on import; re-registering a duplicate id throws (mirror doc-registry); resolveSectionForDocumentType returns a SECTION_A..D for a known DocumentType and null for an unknown one; US SECTION_A carries TWO retention rules (us-i9-post-hire + us-i9-post-termination); and a cross-package consistency assertion that EVERY rule.recordType returned by getPersonnelRetentionRules across all jurisdictions/sections is a key present in the db RETENTION_YEARS export (single-source-of-years guard, D-03).
    Do NOT implement any source — only the tests. No watch-mode flags.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db test personnel-retention.test.ts; pnpm --filter @contractor-ops/compliance-policy test personnel-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; running them FAILS (RED) with a missing-export/module error or unmet assertion, NOT a syntax error.
    - `pnpm --filter @contractor-ops/db typecheck` and `pnpm --filter @contractor-ops/compliance-policy typecheck` still pass (tests excluded from tsc).
    - personnel-retention.test.ts contains a case named for active-employee→indefinite (terminationDate null) and a case for US I-9 max().
    - personnel-registry.test.ts asserts every recordType ∈ RETENTION_YEARS keys.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: RED scaffolds — RBAC structural (auth) + tenant isolation + RBAC router (api)</name>
  <files>packages/auth/src/__tests__/personnel-file-rbac.test.ts, packages/api/src/__tests__/personnel-file-tenant-isolation.test.ts, packages/api/src/__tests__/personnel-file-rbac-router.test.ts</files>
  <read_first>
    - packages/auth/src/permissions.ts (accessControlStatement — assert employeeFileA..D will be added)
    - packages/auth/src/roles.ts (allPermissions owner-duplicate :20-45; 4 HR roles :191-218; the existing `employee` exclusion from allPermissions is the fence to mirror)
    - packages/api/src/__tests__/worker-tenant-isolation.test.ts (mock-Prisma cross-org harness :294-353 — clone its shape)
    - packages/db/src/tenant.ts (globalModels set — assert PersonnelFile is NOT a member)
  </read_first>
  <action>
    personnel-file-rbac.test.ts (auth): import accessControlStatement + roles from the package source. Assert (assertion-RED until 91-04): accessControlStatement has resources employeeFileA, employeeFileB, employeeFileC, employeeFileD each granting ['read','write']; the owner allPermissions object does NOT contain any employeeFile* key (BFLA fence, mirroring how `employee` is absent); hr_admin holds read+write on all four; hr_manager holds A/B/D read+write and C read only; payroll_officer holds C read only; leave_approver holds A read only. (These are the A9 ADVISER-VERIFY matrix values — encode them so a drift is caught.)
    personnel-file-tenant-isolation.test.ts (api): clone worker-tenant-isolation.test.ts. Add a structural test asserting 'PersonnelFile' and 'PersonnelFileDocument' are NOT listed in the globalModels set in tenant.ts (source read). Add a behavioral test: ORG_A caller's personnelFile read for an ORG_B workerId returns nothing / NOT_FOUND and never an ORG_B row (terminal-RED via the not-yet-existing personnelFile router on appRouter).
    personnel-file-rbac-router.test.ts (api): build a caller as payroll_officer and assert the personnelFile getFile response renders section B as { status: 'locked' } with NO documents array; build a caller as hr_admin and assert section B is { status: 'unlocked' } with a documents array; assert a contractor/owner-without-grants caller gets all four sections locked. Terminal-RED via the missing personnelFile router.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/auth test personnel-file-rbac.test.ts; pnpm --filter @contractor-ops/api test personnel-file-tenant-isolation.test.ts personnel-file-rbac-router.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist and FAIL (RED); auth test fails on assertions, api tests fail on missing router export.
    - tenant-isolation test contains a source-read assertion on the globalModels token and a cross-org behavioral assertion.
    - rbac-router test contains an explicit payroll_officer→section-B-locked assertion.
    - `pnpm --filter @contractor-ops/api typecheck` still passes (api tsconfig excludes __tests__).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: RED scaffolds — per-section erasure + classifier routing (api)</name>
  <files>packages/api/src/__tests__/personnel-erasure.test.ts, packages/api/src/__tests__/personnel-classifier.test.ts</files>
  <read_first>
    - packages/api/src/routers/compliance/gdpr.ts (requestErasure :89; recordRetention/retainedUnderStatute :110-116/:318-332; erasure_retained_under_statute audit — the never-over-claim idiom to mirror at per-section grain)
    - packages/api/src/services/ocr-extraction.ts (:120-146 killswitch-gated AI; SKIPPED-not-FAILED on !enabled; createPresignedDownloadUrl :149)
    - packages/feature-flags/src/flags-core.ts (:76-85 killswitch.ai-invoice-parser shape — the classifier flag clones it)
  </read_first>
  <action>
    personnel-erasure.test.ts: terminal-RED via the not-yet-existing personnelFile.requestErasure procedure. Encode criterion #3 verbatim behavior: given a workerId whose section C is still inside its statutory window and section A is past it, the response is { fullErasureClaimed: false, sections: [ {section:'A', disposition:'erased'}, {section:'C', disposition:'retained', citation: <non-empty>, retainUntil: <date>} ] }; assert fullErasureClaimed is false whenever ANY section is retained; assert a writeAuditLog call with action 'personnel_file.erasure_retained_under_statute' fires when a hold is active; assert the all-erased case returns fullErasureClaimed true.
    personnel-classifier.test.ts: terminal-RED via the not-yet-existing classifyPersonnelDocument service. Encode the D-07 routing: (a) a DocumentType with a deterministic taxonomy hit → classificationMethod 'DETERMINISTIC', section assigned, NO AI/Claude call made (assert the Claude adapter is not invoked); (b) taxonomy miss + killswitch ON + AI confidence 92 with margin ≥15 → classificationMethod 'AI', section = top guess; (c) taxonomy miss + killswitch evaluate returns enabled:false (Unleash-unreachable / off) → routed to PENDING_REVIEW (classificationMethod 'PENDING'), upload NOT blocked, NO Claude call; (d) taxonomy miss + AI top confidence 70 (below 85) → PENDING_REVIEW. Mock the killswitch evaluate + the Claude adapter as injected seams.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test personnel-erasure.test.ts personnel-classifier.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and FAIL (RED) via missing procedure/service export.
    - personnel-erasure.test.ts asserts fullErasureClaimed===false under any hold AND the audit action string 'personnel_file.erasure_retained_under_statute'.
    - personnel-classifier.test.ts has all four routing cases (deterministic / AI-assign / killswitch-off→admin / low-confidence→admin) and asserts no Claude call on the deterministic and killswitch-off paths.
    - api typecheck still passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| n/a (test authoring) | This plan adds only test files; no runtime trust boundary is crossed. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-01-01 | Repudiation | Test coverage gaps | mitigate | Every AKTA-01..04 + cross-org invariant gets a RED test here; impl plans cannot be marked done without turning the specific test GREEN. |
| T-91-01-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH Package Legitimacy Audit = N/A); vitest already present in every target package. |
</threat_model>

<verification>
All 7 files exist and FAIL (RED). Package typechecks for db/compliance-policy/auth/api remain green
(tests excluded from tsc). No watch-mode flags anywhere.
</verification>

<success_criteria>
7 RED scaffolds authored; each maps to a locked behavior; each is terminal-RED; no package tsc broken.
</success_criteria>

<output>
Create `.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-01-SUMMARY.md` when done.
</output>
