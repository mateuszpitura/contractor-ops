---
phase: 91-theme-b-akta-osobowe-personnel-file
plan: 06
type: execute
wave: 2
depends_on: ["91-02", "91-03"]
files_modified:
  - packages/feature-flags/src/flags-core.ts
  - packages/api/src/services/personnel-classifier.ts
requirements: [AKTA-04]
autonomous: true

must_haves:
  truths:
    - "Document→section classification is hybrid: deterministic taxonomy first (no AI), then a killswitch-gated Claude-Vision fallback, then admin (PENDING_REVIEW) — never blocking the upload (D-07)"
    - "killswitch.ai-personnel-classifier (default-on, killWhenUnknown) forces the AI off on an Unleash outage → routes to admin, never auto-misclassifies"
    - "A new tenant-owning personnel doc's content is never sent cross-tenant; AI runs only on the row's own org context"
  artifacts:
    - path: "packages/feature-flags/src/flags-core.ts"
      provides: "killswitch.ai-personnel-classifier declaration (NOT signoff-gated)"
      contains: "killswitch.ai-personnel-classifier"
    - path: "packages/api/src/services/personnel-classifier.ts"
      provides: "classifyPersonnelDocument — taxonomy→AI→admin routing + confidence threshold"
      exports: ["classifyPersonnelDocument"]
  key_links:
    - from: "packages/api/src/services/personnel-classifier.ts"
      to: "evaluate('killswitch.ai-personnel-classifier')"
      via: "kill-switch gate before any Claude call"
      pattern: "killswitch.ai-personnel-classifier"
---

<objective>
Build the document→section classifier (genuine new build #3, D-07): a deterministic-first, Claude-
Vision-fallback, admin-last service behind a new killswitch, plus the killswitch declaration. Turns
91-01's classifier test GREEN.

Purpose: most documents classify deterministically from their doc-type; the AI only resolves the
ambiguous tail, and it must fail safe (off/unreachable → admin, never block the upload).
Output: flags-core.ts (+killswitch.ai-personnel-classifier), personnel-classifier.ts (the service).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-PATTERNS.md
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-RESEARCH.md

<interfaces>
killswitch shape (packages/feature-flags/src/flags-core.ts:76-85): 'killswitch.ai-invoice-parser' =
  { key, default:true, category:'kill-switch', jurisdiction:'ANY', owner:'ops', killWhenUnknown:true,
  description }. Killswitches are NOT signoff-gated (verified is-gated-flag.test.ts:45) — do NOT add a
  signoff-registry PENDING entry for this key (unlike module.*/gulf.* flags).
killswitch-gated AI idiom (packages/api/src/services/ocr-extraction.ts:120-146): resolveOrgRegion →
  evaluate(key, { organizationId, region }) → if (!parser.enabled) mark SKIPPED (not FAILED), return,
  never block; createPresignedDownloadUrl (:149) for the doc bytes.
Claude Vision adapter (packages/integrations/src/adapters/claude-ocr-adapter.ts:232+ messages.create +
  tool_use structured JSON); ocr.ts:22 confidence is a 0–100 scale.
Deterministic taxonomy + jurisdiction: resolveSectionForDocumentType(jurisdiction, documentType) from
  @contractor-ops/compliance-policy (91-03); jurisdiction via mapCountryCodeToJurisdiction(countryCode).
Threshold (A10): auto-assign only when topConfidence >= 85 AND (top - second) >= 15; else PENDING_REVIEW.
PersonnelFileDocument fields to set (91-02): section, classificationMethod (DETERMINISTIC|AI|MANUAL|
  PENDING), aiSectionGuess, aiConfidence.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: declare killswitch.ai-personnel-classifier</name>
  <files>packages/feature-flags/src/flags-core.ts</files>
  <read_first>
    - packages/feature-flags/src/flags-core.ts (:76-85 the invoice-parser killswitch to clone; confirm the kill-switch category is not in the gated-prefix set)
  </read_first>
  <action>
    Add 'killswitch.ai-personnel-classifier' to the flag registry with the identical shape to killswitch.ai-invoice-parser: key:'killswitch.ai-personnel-classifier', default:true, category:'kill-switch', jurisdiction:'ANY', owner:'ops', killWhenUnknown:true, and a description explaining it is the emergency disable for the AI document→section classifier and that killWhenUnknown forces it OFF on an Unleash outage so an incident routes uploads to the admin classify-step rather than auto-misclassifying personnel PII. Do NOT add a signoff-registry PENDING entry (killswitches are not signoff-gated).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/feature-flags typecheck && pnpm --filter @contractor-ops/feature-flags test</automated>
  </verify>
  <acceptance_criteria>
    - flags-core.ts registers killswitch.ai-personnel-classifier with default:true, category:'kill-switch', killWhenUnknown:true.
    - No new entry in signoff-registry.ts (the is-gated-flag test, if it enumerates, still passes with this key non-gated).
    - feature-flags typecheck + tests green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: classifyPersonnelDocument service (taxonomy → AI → admin)</name>
  <files>packages/api/src/services/personnel-classifier.ts</files>
  <read_first>
    - packages/api/src/services/ocr-extraction.ts (:100-149 — the killswitch gate, SKIPPED-not-FAILED, presign reuse, QStash-async shape)
    - packages/integrations/src/adapters/claude-ocr-adapter.ts (:232+ messages.create/tool_use) + packages/integrations/src/types/ocr.ts (:22 confidence scale)
    - packages/api/src/__tests__/personnel-classifier.test.ts (91-01 — the four routing cases + the injected seams it mocks)
    - packages/compliance-policy/src/personnel-sections.ts (resolveSectionForDocumentType) + jurisdiction-resolver (mapCountryCodeToJurisdiction)
  </read_first>
  <action>
    Create classifyPersonnelDocument(params: { organizationId; personnelFileDocumentId; documentId; countryCode; documentType: string; storageKey }) returning a discriminated result { method: 'DETERMINISTIC'|'AI'|'PENDING'; section?: PersonnelFileSection; aiSectionGuess?: PersonnelFileSection; aiConfidence?: number }. Export the threshold constants PERSONNEL_CLASSIFY_MIN_CONFIDENCE = 85 and PERSONNEL_CLASSIFY_MIN_MARGIN = 15. Flow: (a) jurisdiction = mapCountryCodeToJurisdiction(countryCode); section = resolveSectionForDocumentType(jurisdiction, documentType); if non-null → return { method:'DETERMINISTIC', section } and make NO Claude call. (b) on null → region = resolveOrgRegion(organizationId); parser = evaluate('killswitch.ai-personnel-classifier', { organizationId, region }); if (!parser.enabled) → return { method:'PENDING' } (route to admin; never throw/FAIL/block — Pitfall 5); log info with parser.reason via @contractor-ops/logger. (c) enabled → createPresignedDownloadUrl(storageKey) → call the Claude Vision section classifier (a new prompt + a structured tool_use result giving per-section confidence on the 0–100 scale; inject the adapter as a seam so the test mocks it). Pick top + second; if top >= PERSONNEL_CLASSIFY_MIN_CONFIDENCE AND (top - second) >= PERSONNEL_CLASSIFY_MIN_MARGIN → { method:'AI', section: top, aiSectionGuess: top, aiConfidence: top }; else → { method:'PENDING', aiSectionGuess: top, aiConfidence: top }. The Claude call uses ONLY this row's org context (no cross-tenant batching). No console.*; structured logging; never log document bytes/PII. Persisting the result onto PersonnelFileDocument + flipping Document.status is the router's job (91-08) — this service is pure routing + the model call.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test personnel-classifier.test.ts && pnpm --filter @contractor-ops/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - personnel-classifier.test.ts (91-01) is GREEN: deterministic hit makes no Claude call; AI 92/margin≥15 → method 'AI'; killswitch evaluate enabled:false → method 'PENDING' with no Claude call and no throw; AI top 70 → method 'PENDING'.
    - The service never throws or returns a FAILED/blocking state when the killswitch is off or Unleash is unreachable.
    - Threshold constants 85 / 15 are exported and used.
    - No console.* (uses @contractor-ops/logger); the Claude adapter + killswitch evaluate are injected seams.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| personnel doc bytes → Claude Vision (external) | Special-category PII (RODO Art. 9) leaves the system to the model provider; must be org-scoped and gated. |
| Unleash → classifier | Flag-eval outcome decides whether AI runs on PII. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-06-01 | Information Disclosure | classifier leaking one org's doc into another's classification | mitigate | Service takes a single organizationId + presigned URL for that row only; no batching; region resolved per-org. |
| T-91-06-02 | Denial of Service / Availability | Unleash outage leaving AI running on PII or blocking uploads | mitigate | killWhenUnknown forces OFF → method 'PENDING' (admin step); upload already persisted, never blocked. |
| T-91-06-03 | Tampering | malicious upload reaching the model | mitigate | Classification runs after the existing virus-scan/size gate on the document upload path (enforced in 91-08 wiring); the service receives a scanned doc's storageKey. |
| T-91-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (Anthropic SDK already present in packages/integrations). |
</threat_model>

<verification>
personnel-classifier.test.ts GREEN; feature-flags + api typecheck green; killswitch fails safe to
admin; no signoff-registry entry added for the killswitch.
</verification>

<success_criteria>
Killswitch declared (non-gated) and the hybrid classifier routes taxonomy→AI→admin with the 85/15
threshold, never blocking the upload; 91-01 classifier test GREEN.
</success_criteria>

<output>
Create `.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-06-SUMMARY.md` when done.
</output>
