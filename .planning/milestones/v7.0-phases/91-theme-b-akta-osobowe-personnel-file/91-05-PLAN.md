---
phase: 91-theme-b-akta-osobowe-personnel-file
plan: 05
type: execute
wave: 2
depends_on: ["91-02", "91-03"]
files_modified:
  - packages/db/src/retention-policy.ts
  - packages/db/src/personnel-retention.ts
  - packages/db/src/index.ts
  - packages/db/src/soft-delete.ts
  - apps/cron-worker/src/jobs/handlers/data-purge.ts
requirements: [AKTA-02]
autonomous: true

must_haves:
  truths:
    - "Statutory akta years register on the SAME RETENTION_YEARS map — no parallel engine (D-03)"
    - "A new resolver expresses what the flat map cannot: per-rule event anchor, US I-9 max() combinator, and indefinite-while-active (null cutoff)"
    - "Both deletion chokepoints route PersonnelFile/PersonnelFileDocument: a delete inside the window soft-deletes; the purge cron excludes rows whose cutoff is null or in the future (D-05)"
  artifacts:
    - path: "packages/db/src/personnel-retention.ts"
      provides: "getPersonnelRetentionCutoff(rules, dates) → {erasable, retainUntil, citation}"
      exports: ["getPersonnelRetentionCutoff"]
    - path: "packages/db/src/retention-policy.ts"
      provides: "8 akta year tokens on RETENTION_YEARS"
      contains: "pl-akta-post2019"
  key_links:
    - from: "apps/cron-worker/src/jobs/handlers/data-purge.ts"
      to: "getPersonnelRetentionCutoff"
      via: "per-row anchor-driven exclusion from deleteMany"
      pattern: "getPersonnelRetentionCutoff"
    - from: "packages/db/src/personnel-retention.ts"
      to: "RETENTION_YEARS"
      via: "reads years by rule.recordType (single source, D-03)"
      pattern: "RETENTION_YEARS\\["
---

<objective>
Build the AKTA-02 retention engine: register the akta years on the shared RETENTION_YEARS map (D-03,
no parallel engine), add the event-anchor resolver the flat map cannot express (D-04 — anchor + max()
+ indefinite-while-active), and route PersonnelFile rows through both existing deletion chokepoints
(D-05 — soft-delete + scheduled archive). Turns 91-01's retention test GREEN.

Purpose: the codebase already solves "retain past a statutory window across 3 deletion paths"; the
ONLY missing piece is the rule layer — a thin resolver over the existing years map.
Output: retention-policy.ts (+tokens), personnel-retention.ts (resolver), index.ts (export),
soft-delete.ts + data-purge.ts (chokepoints).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-PATTERNS.md
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-RESEARCH.md

<interfaces>
RETENTION_YEARS (packages/db/src/retention-policy.ts:13): flat {recordType: years}; the source comment
  (:4-5/:16-17) explicitly invites Phase 91 to register here. getRetentionCutoff (:48-58) returns null =
  "no rule / never purge". DO NOT map PersonnelFile into MODEL_RETENTION_TYPE (that flat path anchors on
  deletedAt — Pitfall 1).
Rule type lives in compliance-policy (db → compliance-policy is allowed): import type {
  PersonnelRetentionRule, RetentionAnchor } from '@contractor-ops/compliance-policy' (91-03).
soft-delete chokepoint (packages/db/src/soft-delete.ts:26 softDeleteModels Set; :72-75 isRetentionGuarded
  / requiresSoftDelete). A non-null cutoff forces soft-delete.
data-purge chokepoint (apps/cron-worker/src/jobs/handlers/data-purge.ts:24 imports getRetentionCutoff
  from @contractor-ops/db; :43-45 cutoffFor; runs on BASE prisma = TRUE hard delete; children→parent
  order; R2-before-DB cleanup :90-110 for storageKey rows).
db public exports (packages/db/src/index.ts:33-41 retention block).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: akta year tokens + getPersonnelRetentionCutoff resolver + export</name>
  <files>packages/db/src/retention-policy.ts, packages/db/src/personnel-retention.ts, packages/db/src/index.ts</files>
  <read_first>
    - packages/db/src/retention-policy.ts (full — RETENTION_YEARS shape + adviser-verify comment style + getRetentionCutoff null semantics)
    - packages/db/src/index.ts (:33-41 retention export block)
    - packages/compliance-policy/src/personnel-types.ts (the PersonnelRetentionRule + RetentionAnchor types to import)
    - packages/db/src/__tests__/personnel-retention.test.ts (91-01 — the exact math/cases to satisfy)
  </read_first>
  <action>
    retention-policy.ts: add the 8 akta tokens to RETENTION_YEARS — 'pl-akta-post2019':10, 'pl-akta-legacy':50, 'de-personalakte-tax':10, 'de-accident-records':30, 'uk-personnel-general':6, 'uk-personnel-financial':7, 'us-i9-post-hire':3, 'us-i9-post-termination':1 — each with an adviser-verify inline comment carrying the real statutory ID (KP art. 94⁵, §147 AO/§257 HGB, Limitation Act 1980, 8 CFR 274a.2). Do NOT add PersonnelFile to MODEL_RETENTION_TYPE.
    personnel-retention.ts (NEW): export function getPersonnelRetentionCutoff(rules: PersonnelRetentionRule[], dates: { hireDate: Date | null; terminationDate: Date | null; documentDate: Date | null; now: Date }): { erasable: boolean; retainUntil: Date | null; citation: string | null }. For each rule: resolve the anchor date (HIRE_DATE→hireDate, TERMINATION_DATE→terminationDate, DOCUMENT_DATE→documentDate); if the required anchor date is null → that rule yields indefinite. retainUntil for a rule = anchorDate + RETENTION_YEARS[rule.recordType] (years; mirror getRetentionCutoff's setFullYear). Combine rules by max(): if ANY rule is indefinite → overall retainUntil null (= never purge); otherwise overall retainUntil = the LATEST rule retainUntil (US I-9 max(HIRE+3y, TERM+1y)). citation = the citation of the binding (latest) rule, or the first indefinite rule's citation. erasable = retainUntil !== null && now >= retainUntil. Read years ONLY from RETENTION_YEARS (single source). Empty rules array → { erasable: true, retainUntil: null, citation: null } (no rule = not retained — but document this branch explicitly so a missing-rule does not accidentally protect or expose data; the caller for personnel rows always supplies rules).
    index.ts: export getPersonnelRetentionCutoff + re-export the PersonnelRetentionRule/RetentionAnchor types (re-export from compliance-policy or declare a local alias) so consumers import retention from one place.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db test personnel-retention.test.ts && pnpm --filter @contractor-ops/db typecheck</automated>
  </verify>
  <acceptance_criteria>
    - personnel-retention.test.ts (91-01) is GREEN — all four jurisdictions, US I-9 max(), and active-employee→indefinite (null cutoff) cases pass.
    - RETENTION_YEARS contains all 8 akta tokens with adviser-verify comments; PersonnelFile is NOT in MODEL_RETENTION_TYPE.
    - getPersonnelRetentionCutoff returns retainUntil null when a required anchor is null and erasable false in that case.
    - db typecheck green; getPersonnelRetentionCutoff exported from packages/db/src/index.ts.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: route PersonnelFile rows through both deletion chokepoints (soft-delete + data-purge)</name>
  <files>packages/db/src/soft-delete.ts, apps/cron-worker/src/jobs/handlers/data-purge.ts</files>
  <read_first>
    - packages/db/src/soft-delete.ts (full — softDeleteModels Set :26, requiresSoftDelete :74-75)
    - apps/cron-worker/src/jobs/handlers/data-purge.ts (full — cutoffFor :43, the per-model deleteMany loop :122-160, children→parent order, R2-before-DB :90-110)
    - packages/db/src/personnel-retention.ts (the resolver from Task 1)
    - packages/compliance-policy/src (getPersonnelRetentionRules, resolveSectionForDocumentType — to fetch a row's rules by jurisdiction+section)
  </read_first>
  <action>
    soft-delete.ts: add 'PersonnelFile' and 'PersonnelFileDocument' to the softDeleteModels Set so a delete at the ORM chokepoint ALWAYS converts to a soft delete (set deletedAt) — never a hard delete (D-05). The windowed hard-delete is the cron's job, not the ORM's, so this unconditional soft-delete is correct and minimal here.
    data-purge.ts: add a PersonnelFileDocument sweep BEFORE a PersonnelFile sweep (children→parent). For each soft-deleted PersonnelFileDocument, resolve its jurisdiction (parent PersonnelFile.countryCode → mapCountryCodeToJurisdiction) + section, fetch getPersonnelRetentionRules(jurisdiction, section), and call getPersonnelRetentionCutoff(rules, { hireDate: parent.hireDate, terminationDate: parent.terminatedAt, documentDate: doc.documentDate ?? doc.document.createdAt, now }). EXCLUDE from the hard deleteMany any row whose retainUntil is null (indefinite) or whose retainUntil > now (still inside the window) — only purge rows where erasable is true. Mirror for the PersonnelFile parent (use its own hireDate/terminatedAt; a PersonnelFile is purgeable only once all its documents are purged AND its own rules — if any — are past the window). Because retention is per-row (anchor-driven), do this as an explicit collect-ids-then-deleteMany({ where: { id: { in: erasableIds } } }) rather than the flat cutoffFor() path (Pitfall 1). Reuse the existing R2-before-DB cleanup ordering for any storageKey the personnel docs reference via Document. Log with createCronLogger; no console.*.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db test && pnpm --filter @contractor-ops/cron-worker typecheck</automated>
  </verify>
  <acceptance_criteria>
    - softDeleteModels includes 'PersonnelFile' and 'PersonnelFileDocument'; a delete on either soft-deletes.
    - data-purge collects erasable ids via getPersonnelRetentionCutoff and never includes a row with retainUntil null or in the future.
    - The sweep deletes children (PersonnelFileDocument) before parents (PersonnelFile) and reuses R2-before-DB cleanup for doc bytes.
    - cron-worker typecheck green; no console.* (uses createCronLogger).
    - Existing db soft-delete + data-purge tests still pass (no regression to the flat path for other models).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| cron worker → tenant data | The purge cron runs on BASE prisma (no tenant scope) and can hard-delete; the resolver is the only thing standing between a held file and destruction. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-05-01 | Tampering | early/erroneous purge of a statutorily-held file | mitigate | Resolver returns null (never purge) while any required anchor is absent or any rule still in-window; data-purge excludes those ids; soft-delete forces soft-delete at the ORM layer. Tests cover indefinite + max() + per-jurisdiction. |
| T-91-05-02 | Repudiation | parallel/divergent retention engine drifting from the source years | mitigate | Years live ONLY in RETENTION_YEARS; resolver reads from it; D-03 invariant; registry test asserts recordType ⊆ RETENTION_YEARS keys. |
| T-91-05-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. |
</threat_model>

<verification>
personnel-retention.test.ts GREEN; db + cron-worker typecheck green; existing retention/purge tests
unbroken; PersonnelFile rows soft-delete and only purge past their per-row window.
</verification>

<success_criteria>
Akta years registered on the shared map, the event-anchor resolver lands and passes the locked math,
and both chokepoints route PersonnelFile rows correctly (indefinite-while-active honored).
</success_criteria>

<output>
Create `.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-05-SUMMARY.md` when done.
</output>
