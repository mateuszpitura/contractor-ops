---
phase: 91-theme-b-akta-osobowe-personnel-file
plan: 04
type: execute
wave: 1
depends_on: ["91-01"]
files_modified:
  - packages/auth/src/permissions.ts
  - packages/auth/src/roles.ts
requirements: [AKTA-01]
autonomous: true

must_haves:
  truths:
    - "Per-section RBAC is a real permission-layer grain: employeeFileA..D resources each grant ['read','write']"
    - "The 4 P89 HR roles carry a per-section matrix (payroll sees C/pay, not B/discipline)"
    - "owner is NEVER auto-granted any employeeFile* (the allPermissions BFLA fence holds, mirroring how `employee` is excluded); no contractor role gains a section grant"
  artifacts:
    - path: "packages/auth/src/permissions.ts"
      provides: "employeeFileA..D resources on accessControlStatement"
      contains: "employeeFileA"
    - path: "packages/auth/src/roles.ts"
      provides: "section→role wiring on the 4 HR roles; allPermissions untouched for sections"
      contains: "employeeFileA"
  key_links:
    - from: "packages/auth/src/roles.ts allPermissions"
      to: "employeeFileA..D"
      via: "DELIBERATE ABSENCE (BFLA fence)"
      pattern: "allPermissions"
---

<objective>
Add the per-section RBAC grain (genuine new build #1, D-02): resource-per-section employeeFileA..D
on the Better-Auth accessControlStatement, wired into the 4 HR roles WITHOUT weakening the P89 BFLA
fence (owner never auto-granted; no contractor mutation). This is enforced at the permission layer;
the router (91-07) consumes it via requirePermission and a per-section programmatic check.

Purpose: a payroll role can see section C (pay) without section B (discipline) — the core AKTA-01
access-control promise.
Output: permissions.ts (+4 resources), roles.ts (section→role matrix; allPermissions fence preserved).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-PATTERNS.md

<interfaces>
accessControlStatement (packages/auth/src/permissions.ts:12-56): resource→actions map. Finest grain
  today is resource-split (contractorPii :44, employee :49, employeePii :54). Add employeeFileA..D here.
allPermissions (packages/auth/src/roles.ts:20-45): the owner-only DUPLICATE of the statement. It
  deliberately omits `employee`/`employeePii`-style HR-only grants in spirit — confirmed it does NOT
  list `employee`. employeeFileA..D MUST be equally absent (adding them re-opens the P89 BFLA hole).
4 HR roles (packages/auth/src/roles.ts:191-218): hr_admin / hr_manager / payroll_officer /
  leave_approver — each grants `employee` (+narrow contractor:read) and NEVER a contractor mutation.
A9 recommended matrix (ADVISER-VERIFY): hr_admin A/B/C/D r+w; hr_manager A/B/D r+w + C read;
  payroll_officer C read; leave_approver A read; owner NONE.
The Permission type + requirePermission flow through unchanged with resource-per-section (research-
  verified: attribute-layer would also force changes to Permission, requirePermission, the AC
  statement, AND permissionToScopes — resource-per-section drops into all four for free).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: add employeeFileA..D resources to the access control statement</name>
  <files>packages/auth/src/permissions.ts</files>
  <read_first>
    - packages/auth/src/permissions.ts (full — the statement, the contractorPii/employee/employeePii precedents, the Permission/Resource types at :60-74 that derive from the statement)
  </read_first>
  <action>
    Add four resources to accessControlStatement: employeeFileA, employeeFileB, employeeFileC, employeeFileD, each granting ['read', 'write'] (per the RESEARCH-recommended resource-per-section shape). Place them adjacent to the existing employee/employeePii block with a comment explaining the per-section akta grain (no plan/decision-ID breadcrumbs in the comment — pnpm lint:no-breadcrumbs; explain the WHY: per-section access so a payroll role reaches pay without discipline). Do NOT touch the Permission/Resource derived types — they pick up the new resources automatically.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/auth typecheck</automated>
  </verify>
  <acceptance_criteria>
    - accessControlStatement contains employeeFileA, employeeFileB, employeeFileC, employeeFileD each = ['read','write'].
    - auth typecheck green; the Permission type now accepts { employeeFileC: ['read'] } (no manual type edit needed).
    - No breadcrumb IDs in any added comment.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: wire the section matrix into the 4 HR roles; preserve the owner BFLA fence</name>
  <files>packages/auth/src/roles.ts</files>
  <read_first>
    - packages/auth/src/roles.ts (full — allPermissions :20-45, owner role :53, the 4 HR roles :191-218)
    - packages/auth/src/__tests__/personnel-file-rbac.test.ts (91-01 — the exact matrix it asserts)
  </read_first>
  <action>
    Wire the A9 matrix into the 4 HR roles: hr_admin → employeeFileA/B/C/D all ['read','write']; hr_manager → employeeFileA/B/D ['read','write'] + employeeFileC ['read']; payroll_officer → employeeFileC ['read'] only; leave_approver → employeeFileA ['read'] only. Leave every other role (owner, admin, finance_admin, ops_manager, team_manager, legal_compliance_viewer, it_admin, external_accountant, readonly, platform_operator) WITHOUT any employeeFile* grant. CRITICAL: do NOT add employeeFileA..D to the allPermissions const (:20-45) — owner must NOT auto-gain them; this preserves the P89 BFLA fence exactly as `employee` is excluded today. Confirm the role-permission matrix snapshot test (if present) is updated to include the four new resources for the HR roles and absent for the rest.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/auth test personnel-file-rbac.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - personnel-file-rbac.test.ts (91-01) is GREEN.
    - allPermissions (owner) contains NO employeeFile* key; owner therefore holds none of the four.
    - hr_admin has all four r+w; hr_manager A/B/D r+w + C read; payroll_officer C read only; leave_approver A read only.
    - No contractor role gained a contractor mutation; the existing 14-role matrix test (if any) still passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| caller role → section data | The role→section grant is the gate that decides which section bytes a caller may read. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-04-01 | Elevation (BFLA) | owner silently auto-granted sections | mitigate | employeeFileA..D kept OUT of allPermissions; structural test asserts owner holds none. |
| T-91-04-02 | Elevation (BFLA) | a contractor/HR role reaching a section it shouldn't | mitigate | Least-privilege matrix (payroll_officer C-read only); no contractor role gets any section; enforced at the permission layer, asserted by 91-01. |
| T-91-04-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. |
</threat_model>

<verification>
auth typecheck green; personnel-file-rbac.test.ts GREEN; owner/contractor section-fence asserted.
</verification>

<success_criteria>
Resource-per-section grain added and wired into the 4 HR roles with the owner/contractor fence intact;
91-01 structural RBAC test GREEN.
</success_criteria>

<output>
Create `.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-04-SUMMARY.md` when done.
</output>
