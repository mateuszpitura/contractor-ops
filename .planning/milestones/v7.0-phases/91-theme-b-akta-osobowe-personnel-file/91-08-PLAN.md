---
phase: 91-theme-b-akta-osobowe-personnel-file
plan: 08
type: execute
wave: 4
depends_on: ["91-06", "91-07"]
files_modified:
  - packages/api/src/routers/core/personnel-file/classify.ts
requirements: [AKTA-04]
autonomous: true

must_haves:
  truths:
    - "Attaching a document to a personnel file runs the hybrid classifier: deterministic hit assigns the section immediately; ambiguous/low-confidence/killswitch-off routes to PENDING_REVIEW — the upload is never blocked"
    - "An admin classify-step approves (assigns SECTION_A..D + flips Document.status ACTIVE) or rejects (ARCHIVED) a PENDING_REVIEW personnel document, with an audit log on each"
    - "The pending-review queue lists only the caller-org's awaiting documents"
  artifacts:
    - path: "packages/api/src/routers/core/personnel-file/classify.ts"
      provides: "attachDocument + classifyApprove + classifyReject + pendingReviewQueue"
      contains: "PENDING_REVIEW"
  key_links:
    - from: "packages/api/src/routers/core/personnel-file/classify.ts"
      to: "classifyPersonnelDocument"
      via: "attachDocument invokes the hybrid classifier service"
      pattern: "classifyPersonnelDocument"
    - from: "classifyApprove"
      to: "writeAuditLog / auditedMutation"
      via: "audited section assignment in-tx"
      pattern: "audit"
---

<objective>
Fill the classify sub-router (AKTA-04): wire document attach → the hybrid classifier (91-06), and add
the admin classify-step (approve assigns a section + ACTIVE; reject ARCHIVES) plus the pending-review
queue. Mirrors the compliance-admin PENDING_REVIEW approve/reject flow. Reuses the existing
virus-scan-gated upload — the document is already persisted before classification runs.

Purpose: most docs file themselves; the ambiguous tail lands in a queue an admin clears — never a
blocked upload.
Output: personnel-file/classify.ts (attachDocument, classifyApprove, classifyReject, pendingReviewQueue).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-PATTERNS.md

<interfaces>
classifier (packages/api/src/services/personnel-classifier.ts, 91-06): classifyPersonnelDocument(params)
  → { method:'DETERMINISTIC'|'AI'|'PENDING'; section?; aiSectionGuess?; aiConfidence? }.
Admin approve/reject precedent (packages/api/src/routers/compliance/compliance-admin.ts:259-336):
  findFirst doc by {id, organizationId}; assert status === 'PENDING_REVIEW' else PRECONDITION_FAILED;
  verify owning link; tx.document.update status ACTIVE (approve) / ARCHIVED (reject); auditedMutation
  in-tx; best-effort post-tx notification OUTSIDE the $transaction.
Document stack: Document.status DocumentStatus {ACTIVE, PENDING_REVIEW, ...}; the existing upload path
  (packages/api/src/routers/core/document.ts virus gate :359, presign, pending-upload) persists +
  virus-scans the doc BEFORE this runs.
PersonnelFileDocument fields (91-02): section, classificationMethod (DETERMINISTIC|AI|MANUAL|PENDING),
  aiSectionGuess, aiConfidence; documentId @unique → Document.
section-access helper (91-07): for admin actions use requirePermission({ compliance: ['override'] })
  (the existing admin-override grain) on approve/reject — the admin clearing the queue is an HR/compliance admin.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: attachDocument → hybrid classifier; PENDING on ambiguous</name>
  <files>packages/api/src/routers/core/personnel-file/classify.ts</files>
  <read_first>
    - packages/api/src/routers/core/personnel-file/classify.ts (the empty stub from 91-07 to replace)
    - packages/api/src/services/personnel-classifier.ts (classifyPersonnelDocument result shape, 91-06)
    - packages/api/src/routers/core/document.ts (the existing upload/virus-scan/presign flow the attach reuses)
    - packages/api/src/__tests__/personnel-classifier.test.ts (91-01 — extend with the attach→persist assertion if the test covers the router seam)
  </read_first>
  <action>
    Replace the classify stub's empty router with classifyRouter exposing attachDocument: tenantProcedure.use(requirePermission({ employee: ['update'] })).input(z.object({ workerId: z.string(), documentId: z.string(), documentType: z.string(), documentDate: z.coerce.date().optional() }).strict()). Flow: assertWorkforceEnabled; load the PersonnelFile by workerId (tenant-scoped); verify the Document belongs to the org and has passed the virus-scan gate (status not blocked) — reuse the document router's ownership/scan check, do NOT re-implement upload. Create the PersonnelFileDocument row (classificationMethod PENDING, section null). Call classifyPersonnelDocument({ organizationId, personnelFileDocumentId, documentId, countryCode: file.countryCode, documentType, storageKey }). On method DETERMINISTIC or AI → set PersonnelFileDocument.section + classificationMethod + (aiSectionGuess/aiConfidence for AI) and leave Document.status ACTIVE (visible, filed). On method PENDING → set classificationMethod PENDING (+ aiSectionGuess/aiConfidence if present) and set Document.status PENDING_REVIEW so it surfaces in the admin queue. NEVER throw/block on the PENDING path; the doc is already persisted. writeAuditLog on the attach mutation. Dispatch the AI path async via QStash where the classifier requires it (mirror ocr-extraction's async shape) — the synchronous deterministic path returns immediately.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test personnel-classifier.test.ts && pnpm --filter @contractor-ops/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - attachDocument creates a PersonnelFileDocument and routes per the classifier result; deterministic/AI → section set + Document ACTIVE; PENDING → Document.status PENDING_REVIEW.
    - The upload is never blocked or FAILED when classification is PENDING (killswitch-off / low-confidence).
    - Document ownership + virus-scan status are verified (reusing the existing gate); no re-implementation of upload/presign.
    - writeAuditLog fires on attach; api typecheck green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: admin classify-step (approve/reject) + pending-review queue</name>
  <files>packages/api/src/routers/core/personnel-file/classify.ts</files>
  <read_first>
    - packages/api/src/routers/compliance/compliance-admin.ts (:259-336 — PENDING_REVIEW verify + flip + auditedMutation + post-tx notification)
    - packages/api/src/services/audit-writer.ts (auditedMutation / writeAuditLog, pass tx)
  </read_first>
  <action>
    Add to classifyRouter: classifyApprove: tenantProcedure.use(requirePermission({ compliance: ['override'] })).input(z.object({ personnelFileDocumentId: z.string(), section: z.enum(['SECTION_A','SECTION_B','SECTION_C','SECTION_D']) }).strict()) — in a $transaction: load the PersonnelFileDocument + its Document {id,status} scoped to org; assert Document.status === 'PENDING_REVIEW' else PRECONDITION_FAILED; set PersonnelFileDocument.section = input.section + classificationMethod MANUAL; flip Document.status ACTIVE; auditedMutation (action 'personnel_file.document.classify_approved', pass tx). classifyReject: same gate, input { personnelFileDocumentId, reason: z.enum([...closed reasons: 'wrong_employee','not_personnel_doc','duplicate','illegible']), note?: string } — flip Document.status ARCHIVED; auditedMutation (action 'personnel_file.document.classify_rejected'). Best-effort uploader notification OUTSIDE the $transaction (mirror compliance-admin). pendingReviewQueue: tenantProcedure.use(requirePermission({ compliance: ['override'] })).query — list the org's PersonnelFileDocument rows whose Document.status === 'PENDING_REVIEW' (tenant-scoped) with employee/jurisdiction/filename/uploaded/aiSectionGuess/aiConfidence projected for the admin UI.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api typecheck && pnpm --filter @contractor-ops/api test personnel-classifier.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - classifyApprove only succeeds on a PENDING_REVIEW personnel doc owned by the caller org; sets the chosen section (MANUAL) + Document ACTIVE; writes an audit row in-tx.
    - classifyReject flips Document.status ARCHIVED with an audit row; the file is not deleted.
    - pendingReviewQueue returns only the caller-org's PENDING_REVIEW personnel docs.
    - Both mutations are gated by compliance:override; api typecheck green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| uploader → classifier → admin | Uploaded bytes + uploader-chosen documentType cross here; the admin step is the human authority on ambiguous classification. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-08-01 | Tampering | mass-assignment of section/status/organizationId on attach/approve | mitigate | Zod .strict() DTOs; section from a closed enum; status server-set; org from session; document ownership verified. |
| T-91-08-02 | Elevation | non-admin clearing the review queue | mitigate | approve/reject/queue gated by compliance:override. |
| T-91-08-03 | Availability | classifier failure blocking the upload | mitigate | PENDING path persists the doc + routes to admin; attach never throws on a classifier non-result (Pitfall 5). |
| T-91-08-04 | Repudiation | unaudited section assignment | mitigate | auditedMutation in-tx on approve/reject + writeAuditLog on attach. |
| T-91-08-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. |
</threat_model>

<verification>
api typecheck green; classifier routing + attach/approve/reject behavior verified; PENDING never
blocks; admin actions audited + org-scoped.
</verification>

<success_criteria>
attachDocument runs the hybrid classifier and routes deterministic/AI→filed, ambiguous→PENDING_REVIEW;
the admin classify-step approves/rejects with audit; the queue is org-scoped.
</success_criteria>

<output>
Create `.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-08-SUMMARY.md` when done.
</output>
