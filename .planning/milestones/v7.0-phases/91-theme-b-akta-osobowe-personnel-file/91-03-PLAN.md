---
phase: 91-theme-b-akta-osobowe-personnel-file
plan: 03
type: execute
wave: 1
depends_on: ["91-01"]
files_modified:
  - packages/compliance-policy/src/personnel-types.ts
  - packages/compliance-policy/src/personnel-sections.ts
  - packages/compliance-policy/src/personnel-retention-rules.ts
  - packages/compliance-policy/src/index.ts
requirements: [AKTA-01, AKTA-02]
autonomous: true

must_haves:
  truths:
    - "A register-on-import per-jurisdiction registry maps (jurisdiction, DocumentType) → SECTION_A..D and (jurisdiction, section) → retention rules for PL/DE/UK/US"
    - "Each retention rule declares an event-typed anchor (HIRE_DATE|TERMINATION_DATE|DOCUMENT_DATE) and a statutory citation; US SECTION_A carries two rules for the max() combinator"
    - "Every section label + retention citation string carries the adviser-verify annotation (Standing Constraint)"
    - "Every rule.recordType is one of the akta tokens that 91-05 adds to RETENTION_YEARS (single source of years, D-03)"
  artifacts:
    - path: "packages/compliance-policy/src/personnel-sections.ts"
      provides: "section taxonomy + doc-type→section resolver, register-on-import"
      exports: ["getPersonnelSections", "resolveSectionForDocumentType"]
    - path: "packages/compliance-policy/src/personnel-retention-rules.ts"
      provides: "(jurisdiction, section) → PersonnelRetentionRule[]"
      exports: ["getPersonnelRetentionRules"]
    - path: "packages/compliance-policy/src/personnel-types.ts"
      provides: "PersonnelFileSection, RetentionAnchor, PersonnelRetentionRule, PersonnelRetentionRecordType types"
  key_links:
    - from: "packages/compliance-policy/src/index.ts"
      to: "personnel-sections + personnel-retention-rules"
      via: "side-effect import (register-on-import) + re-export"
      pattern: "import './personnel-sections'"
---

<objective>
Build the per-jurisdiction section taxonomy + retention-rule registry as a register-on-import module
in compliance-policy (D-01/D-03). It owns the rule SHAPE (anchor + citation + recordType) and the
doc-type→section map; db's resolver (91-05) imports the rule type and reads years from RETENTION_YEARS.

Purpose: jurisdiction nuance lives in seed data (mirrors the v6.0-F1 doc-registry idiom), keeping
RBAC, retention, and classification uniform across PL/DE/UK/US.
Output: personnel-types.ts, personnel-sections.ts, personnel-retention-rules.ts, index.ts wiring.

Dependency note: db depends on compliance-policy (one-way) — so this package must NOT import from
@contractor-ops/db. The recordType union is declared HERE; 91-05 makes RETENTION_YEARS keys match,
and 91-01's registry test asserts the cross-package consistency.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-PATTERNS.md
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-RESEARCH.md

<interfaces>
Register-on-import idiom (packages/compliance-policy/src/doc-registry.ts:17-61): a module-level Map +
  registerX() that throws on duplicate id + a BASELINE array looped at import time.
Side-effect wiring (packages/compliance-policy/src/index.ts:7-12): `import './policies/uk'; ...`.
Adviser-verify annotation idiom (packages/compliance-policy/src/policies/pl.ts registerPolicyRule
  draftLegalText: '... (PENDING legal review)').
Jurisdiction type (packages/compliance-policy/src/types.ts:6): 'UK'|'DE'|'PL'|'US'|'KSA'|'UAE' — use
  this enum (KSA/UAE form), NOT doc-registry's 'AE'|'SA'. Only PL/DE/UK/US in scope.
DocumentType enum lives in the prisma client (packages/db/prisma/schema/contract.prisma:233) — to
  avoid a db import, the taxonomy keys DocumentType by its string literal values (a local string-keyed
  map), not by importing the db enum.
Assumptions to seed (RESEARCH A1-A8): PL cz. A/B/C/D (KP §94 + 2018 rozporządzenie); DE Stammdaten/
  Vertrag-Entgelt/Bescheinigungen/Abmahnungen (no statutory section basis — A6); UK personnel-file
  groupings; US I-9(SECTION_A, separate-storage note A7)/employment/certs/disciplinary. Years tokens
  (A1-A4): pl-akta-post2019(10)/pl-akta-legacy(50); de-personalakte-tax(10)/de-accident-records(30);
  uk-personnel-general(6)/uk-personnel-financial(7); us-i9-post-hire(3)/us-i9-post-termination(1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: personnel-types.ts + section taxonomy registry (register-on-import)</name>
  <files>packages/compliance-policy/src/personnel-types.ts, packages/compliance-policy/src/personnel-sections.ts</files>
  <read_first>
    - packages/compliance-policy/src/doc-registry.ts (register-on-import + duplicate-id throw to mirror)
    - packages/compliance-policy/src/types.ts (Jurisdiction)
    - packages/compliance-policy/src/policies/pl.ts (adviser-verify draftLegalText idiom)
    - packages/db/prisma/schema/contract.prisma (DocumentType enum literal values to key the taxonomy)
  </read_first>
  <action>
    personnel-types.ts: export type PersonnelFileSection = 'SECTION_A' | 'SECTION_B' | 'SECTION_C' | 'SECTION_D' (literal values mirror the prisma enum from 91-02); export type RetentionAnchor = 'HIRE_DATE' | 'TERMINATION_DATE' | 'DOCUMENT_DATE'; export type PersonnelRetentionRecordType = a union of the 8 akta token literals ('pl-akta-post2019' | 'pl-akta-legacy' | 'de-personalakte-tax' | 'de-accident-records' | 'uk-personnel-general' | 'uk-personnel-financial' | 'us-i9-post-hire' | 'us-i9-post-termination'); export interface PersonnelRetentionRule { recordType: PersonnelRetentionRecordType; anchor: RetentionAnchor; citation: string }; export interface PersonnelSectionDef { jurisdiction: Jurisdiction; section: PersonnelFileSection; labelKey: string; statutory: boolean; citationKey?: string }.
    personnel-sections.ts: a module-level Map keyed by jurisdiction holding the 4 PersonnelSectionDef rows + a second Map keyed by `${jurisdiction}:${documentType}` → PersonnelFileSection (the deterministic taxonomy, D-07a). registerPersonnelSection() + registerSectionForDocumentType() that throw on duplicate id (mirror doc-registry). A BASELINE array seeds PL/DE/UK/US (statutory=true for PL+US sections, false for DE+UK groupings — A6). Export getPersonnelSections(jurisdiction): PersonnelSectionDef[] and resolveSectionForDocumentType(jurisdiction, documentType: string): PersonnelFileSection | null (returns null on no deterministic mapping → caller falls through to AI/admin). Each labelKey is an i18n key (PersonnelFile.sections.<JUR>.<X>.label) and the adviser-verify note is carried as a sibling key; statutory citations live in the retention rules (Task 2), not here. US SECTION_A's def notes the I-9 separate-storage expectation (A7) in its adviser-verify copy.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/compliance-policy typecheck</automated>
  </verify>
  <acceptance_criteria>
    - personnel-types.ts exports PersonnelFileSection, RetentionAnchor, PersonnelRetentionRecordType, PersonnelRetentionRule, PersonnelSectionDef.
    - getPersonnelSections('PL'|'DE'|'UK'|'US') each return exactly 4 section defs (SECTION_A..D).
    - resolveSectionForDocumentType returns a SECTION_A..D for at least one seeded DocumentType per jurisdiction and null for an unknown DocumentType.
    - DE/UK section defs have statutory=false; PL/US have statutory=true.
    - No import from '@contractor-ops/db' anywhere in the file (cycle guard).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: retention-rule registry + index wiring (register-on-import)</name>
  <files>packages/compliance-policy/src/personnel-retention-rules.ts, packages/compliance-policy/src/index.ts</files>
  <read_first>
    - packages/compliance-policy/src/index.ts (:7-12 side-effect imports + :14-61 export block to extend)
    - packages/compliance-policy/src/policies/pl.ts (PENDING-legal-review citation idiom)
    - packages/compliance-policy/src/personnel-types.ts (the rule type from Task 1)
  </read_first>
  <action>
    personnel-retention-rules.ts: a Map keyed `${jurisdiction}:${section}` → PersonnelRetentionRule[]. registerPersonnelRetentionRule() throws on duplicate key (mirror doc-registry). Seed BASELINE for PL/DE/UK/US: PL sections → pl-akta-post2019 (TERMINATION_DATE anchor) with the pl-akta-legacy variant noted; DE → de-personalakte-tax (TERMINATION_DATE) on pay/cert sections + de-accident-records (DOCUMENT_DATE, 30y) where occupational records apply; UK → uk-personnel-general (TERMINATION_DATE) general + uk-personnel-financial (TERMINATION_DATE) on pay; US SECTION_A → TWO rules [ {recordType:'us-i9-post-hire', anchor:'HIRE_DATE'}, {recordType:'us-i9-post-termination', anchor:'TERMINATION_DATE'} ] so 91-05's resolver max()-combines them (8 CFR 274a.2). Every citation string ends with the adviser-verify annotation (e.g. '… (PENDING jurisdiction legal/tax adviser verification)') — real statutory IDs kept (KP §94/art. 94⁵, §147 AO/§257 HGB, Limitation Act 1980, 8 CFR 274a.2). Export getPersonnelRetentionRules(jurisdiction, section): PersonnelRetentionRule[].
    index.ts: add `import './personnel-sections';` and `import './personnel-retention-rules';` to the side-effect block, and re-export getPersonnelSections, resolveSectionForDocumentType, getPersonnelRetentionRules, and the personnel-types types in the existing export block.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/compliance-policy test personnel-registry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - personnel-registry.test.ts (91-01) is now GREEN: register-on-import works, duplicate id throws, US SECTION_A has two rules, every recordType is a PersonnelRetentionRecordType literal.
    - getPersonnelRetentionRules('US','SECTION_A') returns two rules with anchors HIRE_DATE and TERMINATION_DATE.
    - Every citation string contains a real statutory ID AND the adviser-verify annotation substring.
    - index.ts side-effect-imports both new modules and re-exports all three getters + the types.
    - No '@contractor-ops/db' import (cycle guard).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| seed config → runtime | Pure in-process config; no external/untrusted input. The risk is legal correctness, not injection. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-03-01 | Repudiation / Compliance | unverified statutory windows/sections | mitigate | Every window/section/citation ships with the adviser-verify annotation (Standing Constraint, REQUIREMENTS line 26); LOCAL-ONLY; recorded as a post-merge legal item. |
| T-91-03-02 | Tampering | duplicate/overlapping registry entries silently overriding | mitigate | registerX() throws on duplicate id (registry test enforces). |
| T-91-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. |
</threat_model>

<verification>
compliance-policy typecheck green; personnel-registry.test.ts GREEN; no db import (cycle-safe);
adviser-verify annotation present on every statutory string.
</verification>

<success_criteria>
Section taxonomy + retention-rule registries register on import for PL/DE/UK/US, US I-9 carries the
two-rule max() shape, every recordType matches the akta token union, and 91-01's registry test is GREEN.
</success_criteria>

<output>
Create `.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-03-SUMMARY.md` when done.
</output>
