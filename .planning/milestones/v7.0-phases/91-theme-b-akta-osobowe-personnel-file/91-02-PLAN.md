---
phase: 91-theme-b-akta-osobowe-personnel-file
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema/personnel.prisma
  - packages/db/prisma/schema/worker.prisma
  - packages/db/prisma/schema/migrations/__personnel_file_additive/migration.sql
  - packages/db/prisma/schema/migrations/__personnel_file_additive/down.sql
autonomous: false
requirements: [AKTA-01]

must_haves:
  truths:
    - "A PersonnelFile (1:1 → Worker.id via workerId @unique) exists, is tenant-owning, and is NOT in globalModels"
    - "A PersonnelFileDocument join carries a canonical SECTION_A..D enum and links 1:1 to the existing Document stack"
    - "PersonnelFile carries the hireDate + terminatedAt read-seam columns (P90 has no hire date; P93 populates terminatedAt; null termination → retain indefinitely)"
    - "The generated Prisma client exposes PersonnelFile + PersonnelFileDocument types and the DB schema is pushed so verification is not a false positive"
  artifacts:
    - path: "packages/db/prisma/schema/personnel.prisma"
      provides: "PersonnelFile + PersonnelFileDocument models + PersonnelFileSection + PersonnelDocClassificationMethod enums"
      contains: "model PersonnelFile"
    - path: "packages/db/prisma/schema/worker.prisma"
      provides: "personnelFile back-relation on Worker"
      contains: "personnelFile PersonnelFile?"
  key_links:
    - from: "packages/db/prisma/schema/personnel.prisma"
      to: "packages/db/prisma/schema/worker.prisma Worker.id"
      via: "workerId @unique sidecar FK"
      pattern: "workerId\\s+String\\s+@unique"
    - from: "packages/db/prisma/schema/personnel.prisma PersonnelFileDocument"
      to: "Document"
      via: "documentId @unique 1:1 FK to the existing Document model"
      pattern: "documentId"
---

<objective>
Add the persistence substrate for the personnel file: a tenant-owning PersonnelFile (1:1 → Worker),
a PersonnelFileDocument join carrying the canonical SECTION_A..D enum and 1:1-linked to the existing
Document stack, plus the hireDate/terminatedAt retention read-seam. Author the additive migration,
regenerate the client, and push the schema.

Purpose: every later wave (retention engine, router, classifier, erasure, UI) is typed and queried
against these models. D-01 locks a 4-section view; this realizes it as an enum-on-link.
Output: personnel.prisma, worker.prisma relation, additive migration, regenerated client, pushed DB.

Hard dependency: Phase 90 (EmployeeProfile/Worker) must be COMPLETE before execution — the FK targets
Worker.id and reads EmployeeProfile.countryCode. Do not start until Phase 90 has landed.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-PATTERNS.md

<interfaces>
Sidecar-FK precedent (packages/db/prisma/schema/contractor.prisma:64-72):
  Contractor { workerId String @unique; worker Worker @relation(fields:[workerId], references:[id]) }
Worker side (packages/db/prisma/schema/worker.prisma): already has `contractor Contractor?` and
  `employeeProfile EmployeeProfile?`; add `personnelFile PersonnelFile?` alongside.
EmployeeProfile (packages/db/prisma/schema/employee.prisma): carries countryCode + employmentStatus
  enum {ACTIVE,ON_LEAVE,SUSPENDED,TERMINATED} but NO hireDate/startDate column — confirmed. The
  HIRE_DATE anchor therefore lives on PersonnelFile.hireDate (this phase).
Document stack (packages/db/prisma/schema/contract.prisma:130 Document, :161 DocumentLink, :233
  DocumentType enum, :250 DocumentStatus {ACTIVE,PENDING_REVIEW,SUPERSEDED,EXPIRED,ARCHIVED}) — reuse;
  PersonnelFileDocument 1:1-links a Document, it does NOT fork it.
globalModels invariant (packages/db/src/tenant.ts) — PersonnelFile/PersonnelFileDocument MUST stay out.
Additive-migration precedent: packages/db/prisma/schema/migrations/__employee_profile_additive/.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PersonnelFile + PersonnelFileDocument models + enums + Worker relation + migration + generate</name>
  <files>packages/db/prisma/schema/personnel.prisma, packages/db/prisma/schema/worker.prisma, packages/db/prisma/schema/migrations/__personnel_file_additive/migration.sql, packages/db/prisma/schema/migrations/__personnel_file_additive/down.sql</files>
  <read_first>
    - packages/db/prisma/schema/worker.prisma (full — the relation stub + tenant-owning comment)
    - packages/db/prisma/schema/employee.prisma (1:1 sidecar precedent + countryCode + EmploymentStatus)
    - packages/db/prisma/schema/contractor.prisma (around :64-72 — workerId @unique FK)
    - packages/db/prisma/schema/contract.prisma (Document :130, DocumentStatus :250, DocumentType :233)
    - packages/db/prisma/schema/migrations/__employee_profile_additive/migration.sql + down.sql (additive/reversible pattern, NO destructive ops)
  </read_first>
  <action>
    Create personnel.prisma defining: enum PersonnelFileSection { SECTION_A SECTION_B SECTION_C SECTION_D } (UPPER_SNAKE per db:audit-enum-casing); enum PersonnelDocClassificationMethod { DETERMINISTIC AI MANUAL PENDING }. model PersonnelFile { id String @id @default(cuid()); organizationId String; workerId String @unique; countryCode String (set at file creation from the worker's EmployeeProfile.countryCode — the jurisdiction key for retention/sections); hireDate DateTime? @db.Date (HIRE_DATE anchor — nullable read-seam); terminatedAt DateTime? (TERMINATION_DATE anchor — nullable read-seam P93 populates; null → active → retain indefinitely); createdAt; updatedAt; deletedAt DateTime?; organization Organization @relation(fields:[organizationId], references:[id]); worker Worker @relation(fields:[workerId], references:[id]); documents PersonnelFileDocument[]; @@unique([organizationId, workerId]); @@index([organizationId]) }. model PersonnelFileDocument { id String @id @default(cuid()); organizationId String; personnelFileId String; documentId String @unique (1:1 to Document); section PersonnelFileSection? (null until classified); documentDate DateTime? @db.Date (DOCUMENT_DATE anchor; fallback Document.createdAt); classificationMethod PersonnelDocClassificationMethod @default(PENDING); aiSectionGuess PersonnelFileSection?; aiConfidence Int?; createdAt; updatedAt; deletedAt DateTime?; personnelFile PersonnelFile @relation(fields:[personnelFileId], references:[id]); document Document @relation(fields:[documentId], references:[id]); organization Organization @relation(fields:[organizationId], references:[id]); @@index([organizationId]); @@index([personnelFileId]) }. Add the reciprocal relation fields required by Prisma on Worker (`personnelFile PersonnelFile?` in worker.prisma), Organization, and Document (a back-relation `personnelFileDocument PersonnelFileDocument?`). Do NOT add PersonnelFile/PersonnelFileDocument to globalModels (tenant.ts) — leave it untouched so both inherit withTenantScope. Author migrations/__personnel_file_additive/{migration.sql,down.sql}: additive CREATE TYPE + CREATE TABLE + indexes + FKs only (mirror __employee_profile_additive; no NOT NULL backfill, no drops in the up). Run `pnpm --filter @contractor-ops/db db:generate` (or the repo's prisma generate script) so the committed client exposes the new model types.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db db:generate && pnpm --filter @contractor-ops/db typecheck && pnpm --filter @contractor-ops/db exec node -e "const{PrismaClient}=require('./generated/client'); new PrismaClient(); console.log('client ok')" 2>/dev/null || echo "verify generate output for PersonnelFile model"</automated>
  </verify>
  <acceptance_criteria>
    - personnel.prisma defines PersonnelFile, PersonnelFileDocument, PersonnelFileSection (SECTION_A..D), PersonnelDocClassificationMethod.
    - PersonnelFile.workerId is `@unique` and FKs Worker.id; @@unique([organizationId, workerId]) present.
    - PersonnelFileDocument.documentId is `@unique` and FKs Document.id; section is the optional PersonnelFileSection enum (enum-on-link, D-01).
    - PersonnelFile has hireDate (@db.Date, nullable) and terminatedAt (nullable).
    - worker.prisma has `personnelFile PersonnelFile?`.
    - `db:audit-enum-casing` passes (UPPER_SNAKE); no entry added to globalModels in tenant.ts.
    - The additive migration is reversible (down.sql drops what up.sql creates) and contains no destructive op on existing tables.
    - `prisma generate` succeeds and `pnpm --filter @contractor-ops/db typecheck` is green.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    PersonnelFile + PersonnelFileDocument models, the additive migration files, and a regenerated
    Prisma client. The schema is NOT yet applied to a live database — every downstream typecheck/test
    passes against the generated client even though no table exists (the false-positive verification
    state called out in the phase brief).
  </what-built>
  <how-to-verify>
    Apply the additive schema so later waves run against a real DB. From repo root:
    1. Run the repo's push/apply flow for the local dev DB — `pnpm --filter @contractor-ops/db db:push`
       (or `prisma db push --schema packages/db/prisma/schema`). This repo's migrate-dev path has known
       history drift (see STATE.md 84-03/85-01/89-02); `db push` to the local dev DB is the established
       fallback. Confirm the PersonnelFile and PersonnelFileDocument tables now exist
       (`\dt "PersonnelFile*"` in psql, or a trivial `prisma.personnelFile.count()`).
    2. Per the LOCAL-ONLY Standing Constraint, the per-region production apply (EU/ME) is a recorded
       post-merge item — do NOT apply to regional production here; note it as deferred in the SUMMARY.
    3. Reply "approved" once the local dev push succeeds (or "deferred — local push blocked" with the
       blocker), so execute-phase can release Wave 2.
  </how-to-verify>
  <resume-signal>Type "approved" (local push done) or "deferred — &lt;reason&gt;"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → tenant DB | Any personnel-file query crosses the org-tenant boundary; the model must be tenant-scoped, never client-trusted for organizationId. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-02-01 | Information Disclosure (IDOR) | PersonnelFile / PersonnelFileDocument cross-org read | mitigate | Tenant-owning models deliberately ABSENT from globalModels → inherit withTenantScope; organizationId carried on both; cross-org leak test (91-01) enforces ORG_A never sees ORG_B file. |
| T-91-02-02 | Tampering | additive migration corrupting existing tables | mitigate | Up migration is additive-only (CREATE TYPE/TABLE/INDEX/FK); reversible down.sql; no NOT NULL/backfill/drop on existing tables. |
| T-91-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. |
</threat_model>

<verification>
prisma generate succeeds; db typecheck green; PersonnelFile* absent from globalModels; the
tenant-isolation structural assertion in 91-01 (globalModels source read) will pass once the model
lands; local dev DB push applied (or deferral recorded).
</verification>

<success_criteria>
Models + enums + relation + additive migration authored; client regenerated; schema pushed to local
dev DB (or deferral recorded); nothing added to globalModels.
</success_criteria>

<output>
Create `.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-02-SUMMARY.md` when done.
</output>
