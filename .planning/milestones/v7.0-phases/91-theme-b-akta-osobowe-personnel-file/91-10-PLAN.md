---
phase: 91-theme-b-akta-osobowe-personnel-file
plan: 10
type: execute
wave: 4
depends_on: ["91-07"]
files_modified:
  - apps/web-vite/src/components/employees/personnel-file/hooks/use-personnel-file.ts
  - apps/web-vite/src/components/employees/personnel-file/personnel-file-shell.tsx
  - apps/web-vite/src/components/employees/personnel-file/personnel-file-section-card.tsx
  - apps/web-vite/src/components/employees/personnel-file/personnel-file-section-status-badge.tsx
  - apps/web-vite/src/components/employees/personnel-file/personnel-retention-panel.tsx
  - apps/web-vite/src/routes/dashboard/employees/personnel-file-route.tsx
  - apps/web-vite/messages/en.json
  - apps/web-vite/messages/de.json
  - apps/web-vite/messages/pl.json
  - apps/web-vite/messages/ar.json
requirements: [AKTA-01, AKTA-02]
autonomous: true

must_haves:
  truths:
    - "A staff-side 4-section personnel-file shell renders each section in one of five distinguishable states: loading, locked, empty, error, populated"
    - "Locked sections render a Locked card (title visible, NO document body/count) driven by the server's per-section {status:'locked'} — never client-side hiding"
    - "Each section surfaces its retention posture (retained-while-employed vs retained-until-date+citation) with the adviser-verify note; the page-level retention panel lists all four"
    - "The hook is the sole tRPC boundary; i18n parity holds across en/de/pl/ar (ar RTL)"
  artifacts:
    - path: "apps/web-vite/src/components/employees/personnel-file/hooks/use-personnel-file.ts"
      provides: "sole tRPC boundary: personnelFile.getFile → per-section props"
      contains: "useTRPC"
    - path: "apps/web-vite/src/components/employees/personnel-file/personnel-file-section-card.tsx"
      provides: "loading|locked|empty|error|populated section card"
      contains: "locked"
  key_links:
    - from: "personnel-file-section-card.tsx"
      to: "components/documents/*"
      via: "reuse DocumentList/DocumentCard unmodified for populated sections"
      pattern: "document-list"
    - from: "use-personnel-file.ts"
      to: "personnelFile.getFile"
      via: "useTRPC + useQuery (sole boundary)"
      pattern: "personnelFile"
---

<objective>
Build the staff-side 4-section personnel-file shell (AKTA-01) with the AKTA-02 retention display, per
91-UI-SPEC. Reuse components/documents/* unmodified for populated sections; build only the shell,
section card (5 states), status badge, and retention panel. The hook is the sole tRPC boundary.

Purpose: the per-section RBAC that 91-07 enforces server-side becomes visibly, conspicuously gated in
the UI (four locked cards when a caller has no grants).
Output: use-personnel-file hook, shell, section card, status badge, retention panel, route, i18n keys.

UI workflow (CLAUDE.md, binding): route through the frontend-design skill + semble search BEFORE
edits; then impeccable for this product surface. Reuse the documents family; do not fork it.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-UI-SPEC.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
Router (91-07): personnelFile.getFile({ workerId }) → { workerId, jurisdiction, employmentActive,
  terminatedAt, sections: [{ section, status:'locked' } | { section, status:'unlocked', documents,
  retention: { retainUntil, citation, indefinite } }] }.
Reuse unmodified: components/documents/{document-list,document-card,version-history,pdf-preview,
  drop-zone}.tsx + hooks/use-document-list.ts (hook = sole tRPC boundary precedent).
Section states + copy: 91-UI-SPEC Screen 1 (loading = document-list skeleton :20-34; locked = title +
  Lock icon + --status-blocked badge, NO body mount; empty = document-list empty :36-44 + inline upload
  CTA; error = section-scoped AlertTriangle + Retry; populated = DocumentList/DocumentCard).
Status tokens (packages/ui/src/styles/status.css): --status-blocked (locked), --status-warning
  (retained near expiry), --status-success. Amber adviser-verify note pattern from
  drv-clearance-panel.tsx:138-142 (role="note", border-amber-300 bg-amber-50 text-amber-800 + dark).
Section labels: 91-UI-SPEC Screen 1 table (PL/DE/UK/US A..D). PL/US carry a citation chip (statutory);
  DE/UK do NOT (A6 — organizational groupings). i18n namespace: PersonnelFile (new top-level, mirrors
  Documents/Compliance). RTL: logical Tailwind utilities only (ms-/me-/ps-/pe-/start-/end-).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: use-personnel-file hook + 4-section shell + section card (5 states) + status badge + route</name>
  <files>apps/web-vite/src/components/employees/personnel-file/hooks/use-personnel-file.ts, apps/web-vite/src/components/employees/personnel-file/personnel-file-shell.tsx, apps/web-vite/src/components/employees/personnel-file/personnel-file-section-card.tsx, apps/web-vite/src/components/employees/personnel-file/personnel-file-section-status-badge.tsx, apps/web-vite/src/routes/dashboard/employees/personnel-file-route.tsx</files>
  <read_first>
    - apps/web-vite/src/components/documents/hooks/use-document-list.ts (hook-as-sole-boundary shape to mirror)
    - apps/web-vite/src/components/documents/{document-list,document-card}.tsx (loading :20-34 + empty :36-44 patterns to reuse verbatim)
    - apps/web-vite/ARCHITECTURE.md (page thin composer / container / hook / component layering)
    - apps/web-vite/src/routes/dashboard/ (existing employees route + how a [workerId] detail route is registered)
    - 91-UI-SPEC.md Screen 1 (states table + RBAC visual contract + section labels)
  </read_first>
  <action>
    Run the frontend-design skill + semble search first (binding). use-personnel-file.ts: the SOLE tRPC boundary — useTRPC + useQuery(trpc.personnelFile.getFile.queryOptions({ workerId })) mapping each server section to presentational props { section, state: 'loading'|'locked'|'empty'|'error'|'populated', documents?, retention? }; no useTRPC/useQuery anywhere else in this component tree. personnel-file-shell.tsx: PersonnelFileView (thin composer with Suspense/error boundary) + a data-wired PersonnelFile section rendering the header (employee name + jurisdiction chip + Active/Terminated{date} chip), the collapsible retention summary strip, and four personnel-file-section-card instances. personnel-file-section-card.tsx: render the five states exactly per UI-SPEC — locked renders the title + Lock icon + status-blocked badge with NO CollapsibleContent body/count mounted (aria-disabled, still Tab-reachable, inert); empty reuses the documents empty-state + inline upload CTA; error is SECTION-SCOPED (AlertTriangle + Retry re-triggering only that section); populated composes DocumentList/DocumentCard unmodified. personnel-file-section-status-badge.tsx: locked/empty/populated badge via the STATUS_CLASS_MAP + tDynLoose pattern (mirror compliance-status-badge). Register the detail route (e.g. /dashboard/employees/:workerId/personnel-file) as a thin page composer per the existing routing convention. Use logical Tailwind utilities only (RTL). All copy via useTranslations('PersonnelFile') — no hardcoded strings.
  </action>
  <verify>
    <automated>pnpm check:web-vite-data-layer && pnpm --filter @contractor-ops/web-vite test src/components/employees/personnel-file && pnpm --filter @contractor-ops/web-vite typecheck</automated>
  </verify>
  <acceptance_criteria>
    - use-personnel-file.ts is the only file importing useTRPC/useQuery in the tree (check:web-vite-data-layer passes).
    - The section card renders all five states; a locked section mounts NO document body/count.
    - A section-level error does not take down the whole page (other sections still interactive).
    - The route renders as a thin page composer (no useTRPC/useQuery in the page); web-vite typecheck green.
    - No hardcoded user-facing strings; logical (RTL-safe) Tailwind utilities only (check:rtl-logical-props passes for the new files).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: retention panel + PersonnelFile i18n keys (shell/sections/retention) × 4 locales</name>
  <files>apps/web-vite/src/components/employees/personnel-file/personnel-retention-panel.tsx, apps/web-vite/messages/en.json, apps/web-vite/messages/de.json, apps/web-vite/messages/pl.json, apps/web-vite/messages/ar.json</files>
  <read_first>
    - apps/web-vite/src/components/contractors/.../drv-clearance-panel.tsx (:138-142 amber role="note" adviser-verify banner to reuse)
    - apps/web-vite/messages/en.json (existing Documents/Compliance namespace shape to mirror for PersonnelFile)
    - 91-UI-SPEC.md Screen 3 (retention summary) + i18n Contract (namespace + key list)
  </read_first>
  <action>
    personnel-retention-panel.tsx: the page-level retention summary (collapsed by default) listing all four sections' posture — "Retained while employed" (active, neutral) or "Retained until {date} — {citation}" (--status-warning near expiry). Clicking a section expands the citation row (PL/US real citation; DE/UK omit the citation row when the grouping is not inherently statutory — never fabricate). Render the amber adviser-verify note (role="note") once at the panel foot. Add the PersonnelFile namespace to ALL FOUR locale files in the SAME change set: PersonnelFile.shell.{header,retentionSummaryToggle,uploadCta,notFoundHeading,notFoundBody,backToEmployees}; PersonnelFile.sections.{empty.*,locked.*,error.*}; PersonnelFile.sections.<PL|DE|UK|US>.<A|B|C|D>.{label,adviserVerifyNote}; PersonnelFile.retention.{whileEmployed,retainedUntil,citationLabel,adviserVerifyNote}. de/pl/ar are real translations (not English placeholders); ar is RTL-safe.
  </action>
  <verify>
    <automated>pnpm i18n:parity && pnpm --filter @contractor-ops/web-vite typecheck</automated>
  </verify>
  <acceptance_criteria>
    - i18n:parity passes: PersonnelFile namespace present with identical key sets in en/de/pl/ar.
    - The retention panel shows "retained while employed" for an active employee and a dated+citation row once terminated; DE/UK section citation rows are omitted (not fabricated).
    - The adviser-verify amber note renders once via the shared role="note" pattern.
    - No hardcoded strings; web-vite typecheck green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| server per-section payload → UI | The UI must render only what the server returns; it must not receive-all-then-hide (BFLA hole). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-10-01 | Information Disclosure (BFLA) | client-side section hiding leaking bytes over the wire | mitigate | The hook branches on the server's {status:'locked'} (no document payload for locked sections); the UI never fetches all sections then filters. |
| T-91-10-02 | Repudiation / Compliance | retention copy read as verified legal fact | mitigate | Every retention/section label carries the adviser-verify note; DE/UK citations omitted rather than fabricated. |
| T-91-10-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (shadcn primitives already present per UI-SPEC). |
</threat_model>

<verification>
check:web-vite-data-layer + i18n:parity + check:rtl-logical-props pass; scoped web-vite tests + typecheck
green; five section states + retention posture render per UI-SPEC.
</verification>

<success_criteria>
The 4-section shell renders all five states with server-driven per-section locking and retention
posture; PersonnelFile i18n namespace parity across all four locales.
</success_criteria>

<output>
Create `.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-10-SUMMARY.md` when done.
</output>
