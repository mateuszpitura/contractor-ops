---
phase: 91-theme-b-akta-osobowe-personnel-file
plan: 07
type: execute
wave: 3
depends_on: ["91-02", "91-03", "91-04", "91-05"]
files_modified:
  - packages/api/src/routers/core/personnel-file/index.ts
  - packages/api/src/routers/core/personnel-file/read.ts
  - packages/api/src/routers/core/personnel-file/section-access.ts
  - packages/api/src/routers/core/personnel-file/classify.ts
  - packages/api/src/routers/core/personnel-file/erasure.ts
  - packages/api/src/root.ts
requirements: [AKTA-01, AKTA-02]
autonomous: true

must_haves:
  truths:
    - "A flag-gated personnelFile router is mounted in workforceRouters and reachable only under module.workforce-employees"
    - "getFile returns per-section {status:'locked'} (no document payload) or {status:'unlocked', documents, retention}; the lock is decided by a permission-layer check, never an app-side filter of an already-fetched payload"
    - "A payroll_officer sees section C unlocked and section B locked; an owner-without-grants sees all four locked; ORG_A can never read ORG_B's file (NOT_FOUND)"
    - "Each section surfaces its retention posture (indefinite-while-active vs retainUntil+citation) via the shared resolver, resolving a real representative documentDate for DOCUMENT_DATE-anchored sections (never a hardcoded null) so the display matches 91-09’s erasure math"
  artifacts:
    - path: "packages/api/src/routers/core/personnel-file/read.ts"
      provides: "getFile per-section read + retention display"
      contains: "employeeFile"
    - path: "packages/api/src/routers/core/personnel-file/section-access.ts"
      provides: "hasSectionPermission(ctx, section) — session + apiKey branches"
      exports: ["hasSectionPermission"]
    - path: "packages/api/src/routers/core/personnel-file/index.ts"
      provides: "personnelFileRouter merging read + classify(stub) + erasure(stub)"
      exports: ["personnelFileRouter"]
  key_links:
    - from: "packages/api/src/root.ts"
      to: "personnelFileRouter"
      via: "workforceRouters spread (flag-gated)"
      pattern: "personnelFile:"
    - from: "packages/api/src/routers/core/personnel-file/read.ts"
      to: "hasSectionPermission"
      via: "per-section gate decides which sections to query"
      pattern: "hasSectionPermission"
---

<objective>
Build the personnel-file router foundation: a flag-gated router mounted in workforceRouters with a
per-section read (getFile) that enforces AKTA-01 at the permission layer and surfaces AKTA-02
retention posture per section. Create empty classify/erasure sub-router stubs so 91-08/91-09 can fill
their own files without touching index.ts. Turns 91-01's tenant-isolation + rbac-router tests GREEN.

Purpose: the difference between "return all sections then hide some" (a BFLA hole) and "only query
the sections the caller may read" — this realizes the latter.
Output: personnel-file/{index,read,section-access,classify(stub),erasure(stub)}.ts + root.ts mount.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-PATTERNS.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
Workforce mount (packages/api/src/root.ts:175-182): const workforceRouters = { worker, employee } as
  const; conditionalWorkforceRouters = isWorkforceRegistered() ? workforceRouters : {} — add
  `personnelFile: personnelFileRouter`.
Per-request guard (packages/api/src/routers/core/employee.ts:34): assertWorkforceEnabled(ctx.organizationId, ctx.region).
RBAC: requirePermission (middleware) + the programmatic per-section check mirrors rbac.ts:
  session → authApi.hasPermission({ headers: ctx.headers, body: { permissions: { employeeFileC: ['read'] } } });
  apiKey → permissionToScopes({ employeeFileC: ['read'] }) tested against ctx.apiKeyScopes.
Tenant scope: ctx.db is the tenant client (withTenantScope) — a findUnique by workerId for another org
  returns null → NOT_FOUND (no app-side org filter needed; never trust client organizationId).
Retention display: getPersonnelRetentionRules(jurisdiction, section) (compliance-policy) +
  getPersonnelRetentionCutoff(rules, { hireDate, terminationDate, documentDate, now }) (@contractor-ops/db),
  jurisdiction = mapCountryCodeToJurisdiction(personnelFile.countryCode).
Section enum: PersonnelFileSection SECTION_A..D (prisma) ↔ employeeFileA..D resources (A→A … D→D).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: section-access helper + sub-router stubs + index + root mount</name>
  <files>packages/api/src/routers/core/personnel-file/section-access.ts, packages/api/src/routers/core/personnel-file/classify.ts, packages/api/src/routers/core/personnel-file/erasure.ts, packages/api/src/routers/core/personnel-file/index.ts, packages/api/src/root.ts</files>
  <read_first>
    - packages/api/src/middleware/rbac.ts (the two-branch session/apiKey permission check to mirror programmatically)
    - packages/api/src/lib/scope-utils.ts (permissionToScopes)
    - packages/api/src/root.ts (:160-198 — the workforceRouters const + conditional spread)
    - packages/api/src/routers/core/index.ts (how core routers are exported/merged)
  </read_first>
  <action>
    section-access.ts: export an async hasSectionPermission(ctx, section: PersonnelFileSection): Promise<boolean> that maps SECTION_A..D → employeeFileA..D and runs the SAME check requirePermission uses — apiKey branch tests permissionToScopes({ [resource]: ['read'] }) against ctx.apiKeyScopes; session branch calls authApi.hasPermission({ headers: ctx.headers, body: { permissions: { [resource]: ['read'] } } }) and returns the boolean (does NOT throw). Also export a sectionToResource(section) map for reuse.
    classify.ts + erasure.ts: export classifyRouter and erasureRouter as EMPTY routers (router({})) with a // filled by a later plan note (no breadcrumb IDs). These stubs let index.ts compile and mount now; 91-08 fills classify, 91-09 fills erasure.
    index.ts: export const personnelFileRouter = router({ ...readRouter procedures, ...classifyRouter, ...erasureRouter }) (or the codebase's merge idiom — mergeRouters / nested namespaces, matching the contractor.ts split-router precedent). Re-export personnelFileRouter.
    root.ts: import personnelFileRouter; add `personnelFile: personnelFileRouter` to the workforceRouters const so it inherits the isWorkforceRegistered() conditional spread + the typeof workforceRouters cast (preserves client typing across the flag branch).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - hasSectionPermission returns a boolean (never throws) and covers both authMode branches.
    - classifyRouter + erasureRouter exist as empty routers; index.ts merges read+classify+erasure into personnelFileRouter.
    - root.ts mounts personnelFile in workforceRouters; appRouter typechecks; the router is absent when isWorkforceRegistered() is false.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: getFile per-section read + retention display</name>
  <files>packages/api/src/routers/core/personnel-file/read.ts</files>
  <read_first>
    - packages/api/src/routers/core/employee.ts (tenantProcedure + requirePermission + assertWorkforceEnabled + .strict() input shape to mirror)
    - packages/api/src/routers/core/personnel-file/section-access.ts (hasSectionPermission from Task 1)
    - packages/api/src/__tests__/personnel-file-rbac-router.test.ts + personnel-file-tenant-isolation.test.ts (91-01 — the exact response shape they assert)
    - packages/db/src/personnel-retention.ts (getPersonnelRetentionCutoff) + packages/compliance-policy/src (getPersonnelRetentionRules, mapCountryCodeToJurisdiction)
  </read_first>
  <action>
    read.ts: export a readRouter with getFile: tenantProcedure.use(requirePermission({ employee: ['read'] })).input(z.object({ workerId: z.string() }).strict()).query: call assertWorkforceEnabled(ctx.organizationId, ctx.region); load the PersonnelFile by workerId via ctx.db (tenant-scoped — NOT_FOUND if absent/another org). Resolve jurisdiction = mapCountryCodeToJurisdiction(file.countryCode). For each section of [SECTION_A, SECTION_B, SECTION_C, SECTION_D]: if (await hasSectionPermission(ctx, section)) is false → push { section, status: 'locked' } with NO documents/count; else → query that section's PersonnelFileDocument rows (tenant-scoped), resolve a representative documentDate for the section from its own rows (the BINDING one = the latest of doc.documentDate ?? doc.document.createdAt across the section so a DOCUMENT_DATE-anchored rule yields the most-conservative cutoff, matching 91-09's per-doc resolution; null ONLY when the section has no documents), compute retention via getPersonnelRetentionCutoff(getPersonnelRetentionRules(jurisdiction, section), { hireDate: file.hireDate, terminationDate: file.terminatedAt, documentDate: sectionRepresentativeDocumentDate, now: new Date() }) and push { section, status: 'unlocked', documents, retention: { retainUntil, citation, indefinite: retainUntil === null && file.terminatedAt === null } }. Do NOT hardcode documentDate: null — a DE de-accident-records section (DOCUMENT_DATE anchor, 30yr from 91-03) would otherwise ALWAYS display 'retained indefinitely' regardless of real document age, diverging from 91-09's erasure computation (which resolves documentDate from the actual PersonnelFileDocument row). Return { workerId, jurisdiction, employmentActive: file.terminatedAt === null, terminatedAt: file.terminatedAt, sections }. NEVER fetch all sections then filter — only query the unlocked ones (Pitfall 3). Add a getRetentionSummary query (or include it in getFile) covering all 4 sections' cutoffs for the page-level retention panel; locked sections still return their retention posture WITHOUT documents (retention dates are non-sensitive; document presence is) — for a locked DOCUMENT_DATE-anchored section, resolve the representative documentDate server-side (read the rows to compute the cutoff) without returning the documents array, so the summary stays accurate for locked sections without leaking document presence.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test personnel-file-rbac-router.test.ts personnel-file-tenant-isolation.test.ts && pnpm --filter @contractor-ops/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - personnel-file-rbac-router.test.ts GREEN: payroll_officer → section B { status:'locked' } with no documents array; hr_admin → section B { status:'unlocked' } with documents; owner-without-grants → all four locked.
    - personnel-file-tenant-isolation.test.ts GREEN: ORG_A getFile for an ORG_B workerId returns NOT_FOUND, never an ORG_B row.
    - Locked sections carry NO documents/count in the response (no leak of "N documents exist").
    - Each unlocked section's retention reflects indefinite-while-active (terminatedAt null) vs a concrete retainUntil+citation.
    - DOCUMENT_DATE-anchored sections (e.g. DE de-accident-records) use a representative documentDate resolved from the section's rows, NOT a hardcoded null — the summary does not show 'indefinite' for a section whose real documents are aged (consistent with 91-09).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| web-vite client → personnelFile router | Untrusted workerId + session/apiKey cross here; org + section access must be server-decided. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-07-01 | Information Disclosure (BFLA) | a role reading a section it lacks | mitigate | hasSectionPermission gates BEFORE querying; locked sections carry no document payload; negative test (payroll→B locked) enforces it at the permission layer, not app-filter. |
| T-91-07-02 | Information Disclosure (IDOR) | cross-org file read | mitigate | ctx.db tenant scope; findUnique by workerId for another org → NOT_FOUND; client organizationId never trusted; leak test enforces. |
| T-91-07-03 | Tampering | client injecting organizationId/section | mitigate | Zod .strict() input ({ workerId }); section list is server-fixed; assertWorkforceEnabled per request. |
| T-91-07-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. |
</threat_model>

<verification>
api typecheck green; rbac-router + tenant-isolation tests GREEN; locked sections leak nothing; router
absent when the workforce flag is off.
</verification>

<success_criteria>
Flag-gated personnelFile router mounted; per-section read enforced at the permission layer with
retention posture; cross-org + per-section negative tests GREEN; classify/erasure stubs ready for fill.
</success_criteria>

<output>
Create `.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-07-SUMMARY.md` when done.
</output>
