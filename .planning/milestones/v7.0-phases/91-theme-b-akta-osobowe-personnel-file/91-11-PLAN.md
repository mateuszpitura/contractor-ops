---
phase: 91-theme-b-akta-osobowe-personnel-file
plan: 11
type: execute
wave: 5
depends_on: ["91-08", "91-09", "91-10"]
files_modified:
  - apps/web-vite/src/components/employees/personnel-file/hooks/use-personnel-classify-queue.ts
  - apps/web-vite/src/components/employees/personnel-file/hooks/use-personnel-erasure.ts
  - apps/web-vite/src/components/employees/personnel-file/personnel-classify-queue/data-table.tsx
  - apps/web-vite/src/components/employees/personnel-file/personnel-classify-queue/personnel-classify-review-dialog.tsx
  - apps/web-vite/src/components/employees/personnel-file/personnel-erasure-dialog.tsx
  - apps/web-vite/src/components/employees/personnel-file/erasure-disposition-badge.tsx
  - packages/validators/src/index.ts
  - packages/validators/src/__tests__/locked-phrases-guard.test.ts
  - apps/web-vite/messages/en.json
  - apps/web-vite/messages/de.json
  - apps/web-vite/messages/pl.json
  - apps/web-vite/messages/ar.json
requirements: [AKTA-03, AKTA-04]
autonomous: false

must_haves:
  truths:
    - "An admin classify review queue lists PENDING_REVIEW personnel docs with an Approve (assign section)/Reject dialog"
    - "The erasure flow renders a per-section disposition list AND a page-level banner that shows the partial-erasure copy whenever fullErasureClaimed is false — it NEVER shows a full-erasure success while any section is retained (criterion #3)"
    - "The adviser-verify legal note is a locked-phrase constant (test-guarded), not a freely-editable i18n string"
    - "Classify/erasure i18n keys ship across all four locales"
  artifacts:
    - path: "apps/web-vite/src/components/employees/personnel-file/personnel-erasure-dialog.tsx"
      provides: "erasure confirm + per-section disposition result + criterion-#3 banner"
      contains: "fullErasureClaimed"
    - path: "packages/validators/src/index.ts"
      provides: "PERSONNEL_FILE_RETENTION_ADVISER_VERIFY locked-phrase constant(s)"
      contains: "PERSONNEL_FILE_RETENTION_ADVISER_VERIFY"
  key_links:
    - from: "personnel-classify-review-dialog.tsx"
      to: "personnelFile.classifyApprove/classifyReject"
      via: "use-personnel-classify-queue hook (sole boundary)"
      pattern: "classifyApprove"
    - from: "personnel-erasure-dialog.tsx"
      to: "personnelFile.requestErasure"
      via: "use-personnel-erasure hook (sole boundary)"
      pattern: "requestErasure"
---

<objective>
Build the admin classify review queue (AKTA-04) and the RODO erasure flow (AKTA-03) staff UI per
91-UI-SPEC Screens 2–3, plus the locked-phrase constant for the adviser-verify legal note. The erasure
result view carries the single most legally load-bearing pixel in the phase: the criterion-#3 banner
that never claims full erasure while any hold is active.

Purpose: an admin clears the ambiguous-doc queue; a staff member issues an honest, per-section erasure.
Output: classify-queue table + review dialog, erasure dialog + disposition view + badge, two hooks,
validators locked-phrase + guard test, classify/erasure i18n keys × 4 locales.

UI workflow (CLAUDE.md, binding): frontend-design skill + semble search BEFORE edits, then impeccable.
Reuse the approval-queue table shell + upload-review dialog shell; do not fork them.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-UI-SPEC.md

<interfaces>
Router (91-08/91-09): personnelFile.pendingReviewQueue() → rows (employee, jurisdiction, filename,
  documentType, uploaded, aiSectionGuess, aiConfidence, personnelFileDocumentId);
  personnelFile.classifyApprove({ personnelFileDocumentId, section }); classifyReject({
  personnelFileDocumentId, reason, note? }); personnelFile.requestErasure({ workerId }) → { workerId,
  fullErasureClaimed, sections: [{ section, disposition:'erased'|'retained', citation?, retainUntil? }] }.
Reuse shells: components/time/approval-queue/data-table.tsx (WorkbenchDataTable + row Approve/Reject);
  components/contractors/compliance/upload-review-dialog.tsx (Tabs approve/reject, DialogBody+DialogFooter
  — the dialog body/footer convention is enforced by check:web-vite-dialog-pattern);
  components/ocr/confidence-badge.tsx (WRAP for i18n — do not reuse its hardcoded English tooltip verbatim).
Erasure view (UI-SPEC Screen 3): AlertDialog confirm (document-card.tsx:203-222 pattern); disposition
  rows — erased = CheckCircle2/--status-success; retained = ShieldAlert (NOT Lock)/--status-warning +
  citation + retainUntil; page-level banner: fullErasureClaimed true → success "All sections erased";
  false → warning partial-erasure copy (renders even if only 1 of 4 retained).
Locked-phrase idiom (packages/validators/src/index.ts): SOFTWARE_NOT_LEGAL_ADVICE_EN/DE +
  __tests__/locked-phrases-guard.test.ts assert exact wording. Add PERSONNEL_FILE_RETENTION_ADVISER_VERIFY_*.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: classify review queue (table + approve/reject dialog + hook)</name>
  <files>apps/web-vite/src/components/employees/personnel-file/hooks/use-personnel-classify-queue.ts, apps/web-vite/src/components/employees/personnel-file/personnel-classify-queue/data-table.tsx, apps/web-vite/src/components/employees/personnel-file/personnel-classify-queue/personnel-classify-review-dialog.tsx</files>
  <read_first>
    - apps/web-vite/src/components/time/approval-queue/data-table.tsx (WorkbenchDataTable + RowActions :118-146 to mirror)
    - apps/web-vite/src/components/contractors/compliance/upload-review-dialog.tsx (Tabs + DialogBody/DialogFooter shell to reuse; REJECT_REASON_CATEGORIES)
    - apps/web-vite/src/components/ocr/confidence-badge.tsx (getConfidenceConfig — wrap for i18n)
    - 91-UI-SPEC.md Screen 2 (queue columns + approve tab section Select + reject reasons)
  </read_first>
  <action>
    Run frontend-design + semble first. use-personnel-classify-queue.ts: sole tRPC boundary — useQuery(pendingReviewQueue) + useMutation(classifyApprove) + useMutation(classifyReject) with query invalidation; returns table rows + action handlers + loading/empty/error. personnel-classify-queue/data-table.tsx: mirror ApprovalQueueTable (WorkbenchDataTable) with columns Employee / Jurisdiction / Document (getFileIcon reuse) / Uploaded / AI guess (section + i18n-wrapped confidence badge or "—") / Actions (Approve/Reject rows); Loading/Empty ("Nothing pending review")/Error(Retry) states. personnel-classify-review-dialog.tsx: reuse upload-review-dialog's Tabs + DialogBody/DialogFooter shell — Approve tab = a Select of the 4 jurisdiction-resolved section labels pre-selected to aiSectionGuess when present, confirm "Approve & assign section" (disabled until chosen, variant default/accent); Reject tab = closed-enum reason Select ('wrong_employee'|'not_personnel_doc'|'duplicate'|'illegible') + optional Textarea, confirm "Reject document" (variant destructive). Row-level actions only (no bulk bar, per UI-SPEC v1).
  </action>
  <verify>
    <automated>pnpm check:web-vite-data-layer && pnpm check:web-vite-dialog-pattern && pnpm --filter @contractor-ops/web-vite test src/components/employees/personnel-file && pnpm --filter @contractor-ops/web-vite typecheck</automated>
  </verify>
  <acceptance_criteria>
    - use-personnel-classify-queue.ts is the sole tRPC boundary for the queue (check:web-vite-data-layer passes).
    - The review dialog uses DialogBody + DialogFooter (check:web-vite-dialog-pattern passes) with Approve(section Select)/Reject(reason) tabs.
    - The confidence badge is i18n-wrapped (no second hardcoded-English copy).
    - Loading/empty/error states present; web-vite typecheck green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: erasure dialog + disposition view + badge + hook + locked-phrase + classify/erasure i18n</name>
  <files>apps/web-vite/src/components/employees/personnel-file/hooks/use-personnel-erasure.ts, apps/web-vite/src/components/employees/personnel-file/personnel-erasure-dialog.tsx, apps/web-vite/src/components/employees/personnel-file/erasure-disposition-badge.tsx, packages/validators/src/index.ts, packages/validators/src/__tests__/locked-phrases-guard.test.ts, apps/web-vite/messages/en.json, apps/web-vite/messages/de.json, apps/web-vite/messages/pl.json, apps/web-vite/messages/ar.json</files>
  <read_first>
    - apps/web-vite/src/components/documents/document-card.tsx (:203-222 AlertDialog confirm pattern)
    - packages/validators/src/index.ts (SOFTWARE_NOT_LEGAL_ADVICE_* locked-phrase idiom) + __tests__/locked-phrases-guard.test.ts
    - 91-UI-SPEC.md Screen 3 (erasure flow + criterion-#3 banner rules + Copywriting Contract)
    - apps/web-vite/messages/en.json (PersonnelFile namespace added in 91-10 — extend, do not duplicate)
  </read_first>
  <action>
    use-personnel-erasure.ts: sole tRPC boundary — useMutation(requestErasure) returning the disposition result + status. personnel-erasure-dialog.tsx: "Request erasure" primary-accent entry (page-level, targets the whole workerId); an AlertDialog confirm (document-card pattern) explaining partial-erasure is possible, confirm variant destructive "Request erasure"; then a result view = per-section disposition rows (erased = CheckCircle2/--status-success; retained = ShieldAlert/--status-warning + citation + retainUntil) UNDER a page-level banner. HARD RULE: when result.fullErasureClaimed === false, render the partial-erasure warning banner (the exact Copywriting-Contract phrasing) and NEVER a full-erasure success — the banner renders even if only one of four sections is retained; only fullErasureClaimed === true renders the "All sections erased" success banner. Foot the view with the amber role="note" adviser-verify banner. erasure-disposition-badge.tsx: erased/retained badge (STATUS_CLASS_MAP + tDynLoose). Add PERSONNEL_FILE_RETENTION_ADVISER_VERIFY_EN/DE/PL/AR locked-phrase constant(s) to packages/validators/src/index.ts and wire them into locked-phrases-guard.test.ts (assert exact wording per locale, mirroring SOFTWARE_NOT_LEGAL_ADVICE_*). Add the classify + erasure i18n keys (PersonnelFile.classify.*, PersonnelFile.classifyReview.*, PersonnelFile.erasure.*) to ALL FOUR locale files with real de/pl/ar translations; the erasure success/partial copy must be the locked phrasing from the Copywriting Contract.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/validators test locked-phrases-guard.test.ts && pnpm i18n:parity && pnpm check:web-vite-data-layer && pnpm --filter @contractor-ops/web-vite typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The erasure result banner shows the partial-erasure copy whenever fullErasureClaimed is false (a test/story asserts the 1-of-4-retained case renders the warning, never a success).
    - PERSONNEL_FILE_RETENTION_ADVISER_VERIFY_* exists in validators and locked-phrases-guard.test.ts asserts its exact per-locale wording (GREEN).
    - i18n:parity passes for the added classify/erasure keys across en/de/pl/ar.
    - Retained rows use ShieldAlert (distinct from the RBAC Lock icon); check:web-vite-data-layer + web-vite typecheck green.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full staff-side personnel-file UI: the 4-section shell (five states incl. conspicuous locked
    cards), the admin classify review queue + approve/reject dialog, and the RODO erasure flow with the
    criterion-#3 partial-erasure banner. All wired to the live router behind module.workforce-employees.
  </what-built>
  <how-to-verify>
    Start web-vite (`pnpm --filter @contractor-ops/web-vite dev`), sign in as an org with the workforce
    add-on, open an employee's personnel file (/dashboard/employees/&lt;workerId&gt;/personnel-file):
    1. As a payroll_officer, confirm section B (discipline) shows the LOCKED card (title visible, no
       documents/count) and section C (pay) is readable. As a role with no section grants, confirm all
       four cards render Locked (the deliberately conspicuous state).
    2. Upload a document; confirm it is never blocked — it either files into a section or shows
       "Pending classification review". In the classify queue, Approve one (assign a section) and Reject
       one; confirm the doc moves out of the queue.
    3. Click "Request erasure" on an employee with at least one section still inside its retention
       window. CRITICAL: confirm the page banner reads the partial-erasure copy (NOT "All sections
       erased"/any full-erasure success) and each retained row shows its citation + retain-until.
    4. Toggle the `ar` locale; confirm RTL layout is correct (no left/right bleed) and copy is translated.
    Reply "approved" or describe issues (especially any wording on the erasure banner that could read as
    full erasure while a hold is active — treat that as a contract violation, not a nitpick).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| erasure result → staff eyes | The banner is a legal representation of what was/wasn't erased; wording is load-bearing. |
| admin queue → section assignment | The admin's Approve writes a section grant-relevant classification. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-11-01 | Repudiation | UI implying full erasure during a hold | mitigate | Banner branches on fullErasureClaimed; partial copy is the locked phrasing; human-verify checkpoint gates it; locked-phrase test guards the legal note. |
| T-91-11-02 | Information Disclosure (BFLA) | classify queue exposing another org's docs | mitigate | Queue driven by the org-scoped pendingReviewQueue; hook is the sole boundary. |
| T-91-11-03 | Tampering | drift of the adviser-verify legal wording per locale | mitigate | Locked-phrase constant + locked-phrases-guard test. |
| T-91-11-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (shadcn primitives already present). |
</threat_model>

<verification>
locked-phrases-guard + i18n:parity + check:web-vite-data-layer + check:web-vite-dialog-pattern pass;
scoped web-vite tests + typecheck green; human-verify confirms the criterion-#3 banner + locked sections.
</verification>

<success_criteria>
Classify queue + review dialog and the erasure flow ship with the criterion-#3 banner and a test-guarded
adviser-verify locked phrase; classify/erasure i18n parity across four locales; visual checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-11-SUMMARY.md` when done.
</output>
