---
phase: 91-theme-b-akta-osobowe-personnel-file
plan: 09
type: execute
wave: 4
depends_on: ["91-03", "91-05", "91-07"]
files_modified:
  - packages/api/src/routers/core/personnel-file/erasure.ts
requirements: [AKTA-03]
autonomous: true

must_haves:
  truths:
    - "A per-employee erasure request returns a per-section disposition list: each section erased only past its retention window, or retained with a statutory citation + retainUntil"
    - "fullErasureClaimed is false whenever ANY section is under a hold — the response never claims full erasure during a hold (criterion #3, verbatim-locked)"
    - "An audit row (personnel_file.erasure_retained_under_statute) is written when any hold is active; the GDPR path is the only one that sets allowAuditPurge"
  artifacts:
    - path: "packages/api/src/routers/core/personnel-file/erasure.ts"
      provides: "requestErasure({ workerId }) → { workerId, fullErasureClaimed, sections[] }"
      contains: "fullErasureClaimed"
  key_links:
    - from: "requestErasure"
      to: "getPersonnelRetentionCutoff + getPersonnelRetentionRules"
      via: "per-section disposition decision"
      pattern: "getPersonnelRetentionCutoff"
    - from: "requestErasure"
      to: "writeAuditLog personnel_file.erasure_retained_under_statute"
      via: "never-over-claim audit when a hold is active"
      pattern: "erasure_retained_under_statute"
---

<objective>
Fill the erasure sub-router (genuine new build #2, AKTA-03 / D-06): a per-employee requestErasure that
extends the gdpr statutory-hold mechanism from whole-org/whole-model grain to per-employee + per-section
+ per-jurisdiction. Erases sections past their window, retains held sections WITH a citation, and NEVER
claims full erasure while any hold is active. Turns 91-01's erasure test GREEN.

Purpose: a legally-honest, RODO-defensible erasure — the single most legally load-bearing behavior in
the phase (criterion #3 is verbatim-locked).
Output: personnel-file/erasure.ts (requestErasure).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-PATTERNS.md
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-RESEARCH.md

<interfaces>
gdpr statutory-hold idiom (packages/api/src/routers/compliance/gdpr.ts): requestErasure :89;
  recordRetention/retainedUnderStatute :110-116 (model→citation, never over-claim); the
  organization.erasure_retained_under_statute audit :318-332 — mirror at per-EMPLOYEE grain with action
  'personnel_file.erasure_retained_under_statute'.
allowAuditPurge (packages/db/src/rls.ts:32): AuditLog is append-only EXCEPT the GDPR erasure path may set
  allowAuditPurge — set it ONLY here, never elsewhere.
resolver (91-05): getPersonnelRetentionCutoff(rules, { hireDate, terminationDate, documentDate, now }) →
  { erasable, retainUntil, citation }. rules from getPersonnelRetentionRules(jurisdiction, section) (91-03).
soft-delete (91-05): deleting a PersonnelFileDocument soft-deletes (deletedAt) — erasing a section means
  soft-deleting its docs (the windowed hard-purge is the cron's job); the section data is removed from view.
Response shape (RESEARCH): { workerId, fullErasureClaimed: retained.length === 0, sections: [{ section,
  disposition: 'erased'|'retained', citation?, retainUntil? }] }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: requestErasure — per-section dispositions + never-over-claim audit</name>
  <files>packages/api/src/routers/core/personnel-file/erasure.ts</files>
  <read_first>
    - packages/api/src/routers/core/personnel-file/erasure.ts (the empty stub from 91-07 to replace)
    - packages/api/src/routers/compliance/gdpr.ts (requestErasure :74-346 — citation summary + retainedUnderStatute + erasure_retained_under_statute audit + allowAuditPurge usage)
    - packages/db/src/rls.ts (:32 allowAuditPurge gate)
    - packages/api/src/__tests__/personnel-erasure.test.ts (91-01 — the exact disposition + fullErasureClaimed + audit assertions)
    - packages/db/src/personnel-retention.ts + packages/compliance-policy/src (getPersonnelRetentionRules, mapCountryCodeToJurisdiction)
  </read_first>
  <action>
    Replace the erasure stub with erasureRouter exposing requestErasure: tenantProcedure.use(requirePermission({ employee: ['delete'] })).input(z.object({ workerId: z.string() }).strict()).mutation. Flow: assertWorkforceEnabled; load the PersonnelFile by workerId (tenant-scoped; NOT_FOUND if absent/other org); jurisdiction = mapCountryCodeToJurisdiction(file.countryCode). For each section SECTION_A..D: rules = getPersonnelRetentionRules(jurisdiction, section); for the section's documents resolve the binding cutoff via getPersonnelRetentionCutoff(rules, { hireDate: file.hireDate, terminationDate: file.terminatedAt, documentDate: <doc.documentDate ?? doc.document.createdAt for DOCUMENT_DATE rules>, now }); if erasable (retainUntil non-null AND now >= retainUntil for ALL the section's docs) → soft-delete the section's PersonnelFileDocument rows and record disposition 'erased'; else → record disposition 'retained' with citation (the resolver's citation) + retainUntil. Build sections[] and fullErasureClaimed = retained.length === 0 (NEVER true while any section retained — Pitfall 4). When any section is retained, writeAuditLog action 'personnel_file.erasure_retained_under_statute' resourceType matching the employee/worker grain with metadata.retainedUnderStatute = { section → citation } (mirror gdpr.ts:318-332). The GDPR path is the ONLY place allowAuditPurge is set, if a purge of the audit trail were ever required — do not set it elsewhere. Return { workerId, fullErasureClaimed, sections }.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test personnel-erasure.test.ts && pnpm --filter @contractor-ops/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - personnel-erasure.test.ts (91-01) is GREEN: section past window → 'erased'; section in window → 'retained' with a non-empty citation + retainUntil; fullErasureClaimed === false whenever any section retained; all-erased → fullErasureClaimed true.
    - An audit row with action 'personnel_file.erasure_retained_under_statute' is written exactly when a hold is active.
    - Erasing a section soft-deletes its PersonnelFileDocument rows (does not hard-delete); a held section's docs are untouched.
    - No path sets allowAuditPurge except this GDPR erasure procedure.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| erasure request → statutory hold | A RODO erasure request crosses into legal-retention territory; the response is a compliance artifact. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-09-01 | Repudiation | erasure response over-claiming full erasure during a hold | mitigate | fullErasureClaimed = retained.length===0; per-section dispositions with citation; audit row when held — criterion #3 verbatim, asserted by 91-01. |
| T-91-09-02 | Tampering | early erasure of a held section | mitigate | resolver gates erasable per-section; held sections soft-deleted only past the window; the cron never purges in-window rows. |
| T-91-09-03 | Tampering | tampering with the append-only audit trail | mitigate | allowAuditPurge set ONLY on this GDPR path; AuditLog otherwise append-only (rls.ts). |
| T-91-09-04 | Elevation (IDOR) | erasing another org's file | mitigate | tenant-scoped lookup by workerId → NOT_FOUND for another org; employee:delete required. |
| T-91-09-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. |
</threat_model>

<verification>
api typecheck green; personnel-erasure.test.ts GREEN; never-over-claim invariant + audit verified;
held sections retained with citations.
</verification>

<success_criteria>
Per-employee requestErasure returns honest per-section dispositions, never claims full erasure during a
hold, and audits held sections; 91-01 erasure test GREEN.
</success_criteria>

<output>
Create `.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-09-SUMMARY.md` when done.
</output>
