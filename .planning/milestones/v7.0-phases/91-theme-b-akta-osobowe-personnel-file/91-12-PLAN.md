---
phase: 91-theme-b-akta-osobowe-personnel-file
plan: 12
type: execute
wave: 6
depends_on: ["91-02", "91-03", "91-04", "91-05", "91-06", "91-07", "91-08", "91-09", "91-10", "91-11"]
files_modified:
  - .planning/brain/wiki/domains/personnel-file.md
  - .planning/brain/wiki/structure/prisma-schema-areas.md
  - .planning/brain/wiki/structure/key-services.md
  - .planning/brain/wiki/structure/api-routers-catalog.md
  - .planning/brain/wiki/patterns/rbac-permissions.md
  - .planning/brain/wiki/patterns/audit-log.md
  - .planning/brain/wiki/patterns/feature-flags.md
  - .planning/brain/wiki/log.md
  - .planning/brain/wiki/hot.md
  - .planning/MEMORY.md
requirements: [AKTA-01, AKTA-02, AKTA-03, AKTA-04]
autonomous: true

must_haves:
  truths:
    - "The wiki tracks the code in the SAME change set: a new personnel-file domain page + refreshed structure/pattern pages for the new model, services, router, per-section RBAC, audit, and killswitch"
    - "Three invariants are recorded in MEMORY.md: per-section RBAC grain (resource-per-section, never to owner), shared-retention-map + event-anchor resolver (no parallel engine), per-section statutory-hold erasure never over-claims"
    - "pnpm check:wiki-brain is GREEN and the full phase scoped test suites + typecheck are green"
  artifacts:
    - path: ".planning/brain/wiki/domains/personnel-file.md"
      provides: "Purpose / Flow / Entry points / UI surface / Agent mistakes for the akta domain"
      contains: "personnel"
    - path: ".planning/MEMORY.md"
      provides: "three Phase-91 invariants"
      contains: "per-section"
  key_links:
    - from: ".planning/brain/wiki/structure/api-routers-catalog.md"
      to: "personnelFile router in root.ts workforceRouters"
      via: "catalog entry"
      pattern: "personnelFile"
---

<objective>
Close the documentation-follows-code loop (CLAUDE.md gated) in the same milestone change set: author the
personnel-file domain wiki page, refresh the structure/pattern pages the phase touched, record the three
new invariants in MEMORY.md, and run the wiki-brain + full scoped test/typecheck gates as the phase seal.

Purpose: the next agent must not hallucinate — the wiki is the living compass and must track the akta
model, retention resolver, per-section RBAC, erasure grain, and classifier killswitch.
Output: wiki domain + structure + pattern pages, log/hot refresh, MEMORY invariants, green gates.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-CONTEXT.md
@.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-RESEARCH.md

<interfaces>
Documentation-follows-code targets (CONTEXT canonical_refs + RESEARCH Documentation-Follows-Code):
  domains/personnel-file.md (new); structure/prisma-schema-areas.md (PersonnelFile/PersonnelFileDocument/
  sections); structure/key-services.md (personnel-retention resolver + personnel-classifier); structure/
  api-routers-catalog.md (personnelFile router; verify root.ts workforce spread); patterns/rbac-permissions.md
  (per-section grain); patterns/audit-log.md (erasure + classify audits); patterns/feature-flags.md
  (killswitch.ai-personnel-classifier). Then wiki/log.md + overwrite wiki/hot.md; bump source_commit on
  touched pages.
Refresh pipeline (CLAUDE.md): pnpm check:wiki-brain; then in .planning/brain — contextual-prefix.py
  --no-llm on touched pages + bm25-index.py build. Graph auto-rebuilds via .husky/post-commit.
Wiki hygiene (MEMORY): durable domain/code facts only — NO operational/round-status text. No plan/
  decision-ID breadcrumbs in source; wiki prose may reference the domain, not planning IDs.
Test gate (91-VALIDATION): scope web-vite by path (NEVER the full unscoped suite — RAM). All seven
  Wave-0 tests must be GREEN before the phase is done.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: personnel-file domain page + structure/pattern refresh + MEMORY invariants</name>
  <files>.planning/brain/wiki/domains/personnel-file.md, .planning/brain/wiki/structure/prisma-schema-areas.md, .planning/brain/wiki/structure/key-services.md, .planning/brain/wiki/structure/api-routers-catalog.md, .planning/brain/wiki/patterns/rbac-permissions.md, .planning/brain/wiki/patterns/audit-log.md, .planning/brain/wiki/patterns/feature-flags.md, .planning/MEMORY.md, .planning/brain/wiki/log.md, .planning/brain/wiki/hot.md</files>
  <read_first>
    - .planning/brain/wiki/domains/worker-foundation.md (the P89 domain page shape to mirror for a new domain page)
    - .planning/brain/wiki/structure/prisma-schema-areas.md + key-services.md + api-routers-catalog.md (existing sections to extend)
    - .planning/brain/wiki/patterns/rbac-permissions.md + audit-log.md + feature-flags.md (existing pattern pages to extend)
    - .planning/MEMORY.md (invariant format to append)
  </read_first>
  <action>
    domains/personnel-file.md (NEW): Purpose (jurisdiction-correct akta on the Worker abstraction), Flow (upload → hybrid classifier → section or PENDING_REVIEW admin step; retention resolver over the shared map; per-employee per-section erasure), Entry points (personnelFile router under workforceRouters; use-personnel-file hook), UI surface (4-section shell + classify queue + erasure view), Agent mistakes (do NOT app-filter sections; do NOT build a parallel retention engine; do NOT claim full erasure during a hold; do NOT add PersonnelFile to globalModels; do NOT add employeeFileA..D to owner allPermissions). Include verify_with source paths. Extend prisma-schema-areas.md (PersonnelFile + PersonnelFileDocument + PersonnelFileSection enum + hireDate/terminatedAt read-seam), key-services.md (getPersonnelRetentionCutoff event-anchor resolver + classifyPersonnelDocument), api-routers-catalog.md (personnelFile: getFile/attachDocument/classifyApprove/classifyReject/pendingReviewQueue/requestErasure — flag-gated in root.ts), patterns/rbac-permissions.md (resource-per-section employeeFileA..D, never to owner), patterns/audit-log.md (personnel_file.erasure_retained_under_statute + classify audits + allowAuditPurge only on the GDPR path), patterns/feature-flags.md (killswitch.ai-personnel-classifier, not signoff-gated, killWhenUnknown → admin step). Append THREE invariants to MEMORY.md: (1) per-section RBAC grain = resource-per-section employeeFileA..D, never in owner allPermissions; (2) akta retention = years on the shared RETENTION_YEARS map + a new event-anchor resolver (HIRE/TERMINATION/DOCUMENT + max() + indefinite-while-active), no parallel engine; (3) per-employee/per-section statutory-hold erasure returns dispositions and never claims full erasure while any hold is active. Update wiki/log.md; overwrite wiki/hot.md; bump source_commit on touched pages; then run the brain refresh pipeline (contextual-prefix --no-llm on touched pages + bm25 build).
  </action>
  <verify>
    <automated>pnpm check:wiki-brain</automated>
  </verify>
  <acceptance_criteria>
    - domains/personnel-file.md exists with Purpose/Flow/Entry points/UI surface/Agent mistakes + verify_with paths.
    - prisma-schema-areas / key-services / api-routers-catalog / rbac-permissions / audit-log / feature-flags pages each carry the new personnel-file facts.
    - MEMORY.md has the three invariants.
    - wiki/log.md updated, wiki/hot.md overwritten, source_commit bumped on touched pages.
    - pnpm check:wiki-brain is GREEN (no doc-drift for the phase's source files).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: phase verification — full scoped suites + typecheck + guards green</name>
  <files>.planning/brain/wiki/log.md</files>
  <read_first>
    - .planning/phases/91-theme-b-akta-osobowe-personnel-file/91-VALIDATION.md (the per-wave sampling + phase-gate commands)
  </read_first>
  <action>
    Run the phase gate: the scoped suites for every touched backend package (api, db, compliance-policy, auth, feature-flags, validators) + the scoped web-vite personnel-file tests (NEVER the full unscoped web-vite suite — RAM, MEMORY). Confirm all seven Wave-0 tests are GREEN (retention resolver, registry, auth RBAC structural, tenant isolation, rbac-router, erasure, classifier). Run pnpm typecheck (tsc, CI-canonical) and the relevant guards (i18n:parity, check:web-vite-data-layer, check:web-vite-dialog-pattern, check:rtl-logical-props, lint:audit-log, lint:no-breadcrumbs, db:audit-enum-casing). Record the green results (and any deferred [BLOCKING] regional-apply item from 91-02) in a short durable note in wiki/log.md. Do not mark the phase done if any AKTA-01..04 test is red.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db test && pnpm --filter @contractor-ops/compliance-policy test && pnpm --filter @contractor-ops/auth test && pnpm --filter @contractor-ops/api test personnel-file-rbac-router.test.ts personnel-file-tenant-isolation.test.ts personnel-erasure.test.ts personnel-classifier.test.ts && pnpm --filter @contractor-ops/web-vite test src/components/employees/personnel-file && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Every scoped backend suite (db, compliance-policy, auth, feature-flags, validators, api) is green.
    - All seven Wave-0 tests are GREEN; no AKTA-01..04 test is red.
    - pnpm typecheck (tsc) passes; i18n:parity + check:web-vite-data-layer + check:web-vite-dialog-pattern + check:rtl-logical-props + lint:no-breadcrumbs + db:audit-enum-casing pass.
    - wiki/log.md records the green gate + any deferred regional-apply item.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| docs vs code | Stale docs → the next agent hallucinates; the wiki-brain gate is the boundary that keeps them synced. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-12-01 | Repudiation | undocumented invariants re-broken by a future phase | mitigate | Three MEMORY invariants + a domain page with an Agent-mistakes section; check:wiki-brain gate. |
| T-91-12-02 | Repudiation | phase marked done with a red AKTA test (false verification) | mitigate | Task 2 runs all seven Wave-0 tests + typecheck; phase not done on any red. |
| T-91-12-SC | Tampering | npm/pip/cargo installs | accept | No packages installed. |
</threat_model>

<verification>
check:wiki-brain GREEN; all scoped backend suites + scoped web-vite personnel-file tests + tsc green;
seven Wave-0 tests GREEN; MEMORY invariants recorded.
</verification>

<success_criteria>
Wiki tracks the code (new domain page + refreshed structure/pattern pages), three invariants recorded,
and the full phase gate (tests + typecheck + guards + wiki-brain) is green.
</success_criteria>

<output>
Create `.planning/phases/91-theme-b-akta-osobowe-personnel-file/91-12-SUMMARY.md` when done.
</output>
