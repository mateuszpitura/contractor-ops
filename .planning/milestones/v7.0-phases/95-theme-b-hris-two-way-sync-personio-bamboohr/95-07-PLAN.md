---
phase: 95-theme-b-hris-two-way-sync-personio-bamboohr
plan: 07
type: execute
wave: 4
depends_on: [95-01, 95-02, 95-03, 95-04, 95-05]
files_modified:
  - packages/api/src/services/outbox/handlers.ts
  - packages/api/src/services/outbox/hris-push.ts
  - packages/api/src/services/outbox/index.ts
  - packages/api/src/routers/finance/[invoice-paid-producer].ts
  - packages/api/src/routers/finance/[payment-status-producer].ts
  - packages/api/src/routers/[classification-outcome-producer].ts
autonomous: true
requirements: [HRIS-SYNC-03]

must_haves:
  truths:
    - "THREE new outbox event types exist (invoice.paid was NOT already one — correction C1): hris.invoice-paid.push, hris.payment-status.push, hris.classification-outcome.push — each a literal + OutboxEventPayloadMap entry + handler"
    - "Each handler resolves the org's connected HRIS via loadOrgIntegrationConnection (optional), no-ops when there is no connection or the integration.*-sync flag is dark, calls assertNotHrisOwnedField (change-origin guard), and dispatches the adapter push threading outboxEventId as the idempotency key"
    - "The three producer mutations enqueue the event inside their existing $transaction (never call the adapter inline) with a dedupKey = `${workerId}:${eventType}:${businessEventId}`"
    - "Push is transactional (commits iff the business write commits), idempotent (outboxEventId + dedupKey), retriable (existing outbox backoff)"
    - "Loop-prevention holds: the push carries only CO-owned business events; a pull emits no push"
  artifacts:
    - path: "packages/api/src/services/outbox/hris-push.ts"
      provides: "the three HRIS push handlers + hrisProviderFor(org) resolver + change-origin guard call"
      contains: "hris.invoice-paid.push"
  key_links:
    - from: "producer mutation ($transaction)"
      to: "enqueueOutboxEvent"
      via: "the mutation enqueues an hris.*.push event; the drain dispatches it to the adapter"
      pattern: "enqueueOutboxEvent"
---

<objective>
Build the **outbound push** (HRIS-SYNC-03): add the three HRIS push outbox event types + handlers and wire the three producer mutations to enqueue them. **Correction C1: `invoice.paid` is NOT already an outbox event type** — all three are new. Turns the `hris-outbox` + `hris-push-loop` RED tests (95-02) GREEN.

Purpose: on invoice-paid / payment-status / classification-outcome, durably push to the connected HRIS via the transactional outbox — transactional, idempotent (`outboxEventId`), retriable — with the mutation only enqueueing (never calling the adapter inline), and the disjoint-partition loop-break preserved.
Output: three event types + handlers in `outbox/handlers.ts` + `outbox/hris-push.ts`, and one `enqueueOutboxEvent` call inside each producer's existing `$transaction`.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-CONTEXT.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-RESEARCH.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-VALIDATION.md

<interfaces>
- `packages/api/src/services/outbox/handlers.ts:28-84` — `OutboxEventType` + `OutboxEventPayloadMap` + `outboxHandlerRegistry` (extend all three).
- `packages/api/src/services/outbox/index.ts:201-289` — `enqueueOutboxEvent` (producer API) + the `enqueueNotificationOutboxEvent` typed wrapper to mirror.
- `packages/api/src/services/outbox/__tests__/outbox.test.ts` — harness.
- `packages/api/src/lib/integration-connection.ts:74-127` — `loadOrgIntegrationConnection(..., { optional:true, status:'CONNECTED' })`.
- `packages/api/src/services/hris-sync/field-partition.ts` (95-01) — `assertNotHrisOwnedField`.
- `packages/api/src/services/audit-writer.ts:66-121` — `writeAuditLog`.
- `95-RESEARCH.md` § Pattern 3 + Correction C1 + Pitfall 1/2 + Assumptions A5 (locating the producers).
- **Producer discovery (A5):** use `mcp__semble__search` for "invoice paid mutation" / "payment status update mutation" / "classification outcome decided mutation" — the file names in `files_modified` are placeholders the executor MUST replace with the real committing-transaction seams (search `invoice.paidAt` / payment status transitions / classification result writes).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three HRIS push event types + payload map (handlers.ts)</name>
  <files>packages/api/src/services/outbox/handlers.ts</files>
  <read_first>
    - `packages/api/src/services/outbox/handlers.ts:28-52` (the exact literal + map + registry-type shape)
    - `95-RESEARCH.md` § Pattern 3
  </read_first>
  <action>
    Extend `OutboxEventType` with the three literals (`'hris.invoice-paid.push' | 'hris.payment-status.push' | 'hris.classification-outcome.push'`), add the three `OutboxEventPayloadMap` entries (each carrying `workerId` + the business id + minimal denormalized fields per Pattern 3), and register the three handlers in `outboxHandlerRegistry` (delegating to the handlers authored in Task 2). Keep the tsc-tied map/registry alignment so a typo is a compile error.
  </action>
  <verify>
    <automated>grep -E "hris.invoice-paid.push|hris.payment-status.push|hris.classification-outcome.push" packages/api/src/services/outbox/handlers.ts && pnpm typecheck --filter=@contractor-ops/api 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Three literals + three payload-map entries + three registry handlers; tsc green.
  </acceptance_criteria>
  <done>The outbox knows the three HRIS push event types.</done>
</task>

<task type="auto">
  <name>Task 2: Push handlers (hris-push.ts) — resolve connection, guard, dispatch, audit</name>
  <files>packages/api/src/services/outbox/hris-push.ts</files>
  <read_first>
    - `packages/api/src/lib/integration-connection.ts:74-127`
    - `packages/api/src/services/hris-sync/field-partition.ts` (`assertNotHrisOwnedField`)
    - the adapters (95-04/95-05) `pushEmployeeEvent`
    - `95-RESEARCH.md` § Pattern 3
  </read_first>
  <action>
    Create `hris-push.ts` with `hrisProviderFor(tenantDb, organizationId)` (the org's connected HRIS provider via `loadOrgIntegrationConnection(..., { optional:true, status:'CONNECTED', provider: 'PERSONIO'|'BAMBOOHR' })` — try both; return null if none) and the three handlers. Each handler: resolve the connection (no connection → **no-op return**, not an error); `evaluate('integration.<provider>-sync', { organizationId, region })` dark → no-op return; `assertNotHrisOwnedField(payload)` (change-origin guard); `await loadHeavyAdapters()` + `getAdapter(connection.provider).pushEmployeeEvent(creds, { ...payload, idempotencyKey: ctx.outboxEventId })`; `writeAuditLog({ actorType:'INTEGRATION', action:'hris.push.<event>', resourceType:'WORKER', resourceId: payload.workerId })`. Throwing = transient → the outbox retries (backoff). No `console.*`; `safeParse` any adapter response.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hris-outbox hris-push-loop 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - No connection / dark flag → handler no-ops (not an error).
    - `assertNotHrisOwnedField` invoked before every push (loop guard).
    - `outboxEventId` threaded as the idempotency key; `writeAuditLog(INTEGRATION)` on push.
    - `hris-outbox` + `hris-push-loop` RED tests GREEN.
  </acceptance_criteria>
  <done>The three push handlers dispatch to the connected HRIS idempotently, guarded, and audited.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the three producer mutations to enqueue (never inline)</name>
  <files>packages/api/src/routers/finance/[invoice-paid-producer].ts, packages/api/src/routers/finance/[payment-status-producer].ts, packages/api/src/routers/[classification-outcome-producer].ts</files>
  <read_first>
    - locate the real producers via `mcp__semble__search` (invoice paid / payment status transition / classification outcome) — replace the placeholder paths in `files_modified`
    - `packages/api/src/services/outbox/index.ts:201-289` (`enqueueOutboxEvent` inside `$transaction`)
  </read_first>
  <action>
    In each producer mutation, inside its existing committing `$transaction`, add one `enqueueOutboxEvent({ tx, organizationId, eventType:'hris.<event>.push', payload:{ workerId, ...businessFields }, dedupKey:`${workerId}:hris.<event>.push:${businessEventId}` })`. The mutation must NOT call the adapter directly (D-03) — it only enqueues, so the push commits iff the business write commits and is deduped on retry. If a business event lacks a single obvious mutation seam, enqueue at the nearest committing `$transaction` for that state change (A5). Guard: only enqueue when the worker is an EMPLOYEE (contractor invoices don't push to an HRIS) — resolve `workerType`/worker from the business row.
  </action>
  <verify>
    <automated>grep -rn "hris.invoice-paid.push\|hris.payment-status.push\|hris.classification-outcome.push" packages/api/src/routers/ | grep -v __tests__ | head && pnpm -F @contractor-ops/api test hris-outbox 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Each producer enqueues inside its `$transaction` with a stable `dedupKey`; no inline adapter call.
    - Only EMPLOYEE workers enqueue an HRIS push.
    - `pnpm typecheck --filter=@contractor-ops/api` green.
  </acceptance_criteria>
  <done>The three business events durably enqueue an HRIS push; the mutation never touches the adapter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| business mutation → HRIS push | Only via the transactional outbox; never inline; deduped + idempotent. |
| push payload → HRIS | The change-origin guard ensures it carries no HRIS-owned field (loop-break). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-07-01 | Tampering | write-back loop (push echoes into a pull) | mitigate | Disjoint partition + `assertNotHrisOwnedField` guard (Task 2); pull emits no push (`hris-push-loop` GREEN). |
| T-95-07-02 | Repudiation/dup | double-push on retry | mitigate | `outboxEventId` idempotency key + `dedupKey` enqueue dedup (Task 2/3). |
| T-95-07-03 | Elevation | push fires with the flag dark / no connection | mitigate | Handler no-ops when dark or unconnected (Task 2). |
| T-95-07-04 | Information Disclosure | push a contractor (non-employee) to an HRIS | mitigate | Only EMPLOYEE workers enqueue (Task 3). |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test hris-outbox hris-push-loop` — GREEN.
- `pnpm typecheck --filter=@contractor-ops/api` green.
- `pnpm lint:audit-log` + `pnpm lint:logs` + `pnpm standards:check` green.
</verification>

<success_criteria>
Three new HRIS push outbox event types + handlers dispatch to the connected HRIS transactionally, idempotently (`outboxEventId`), retriably, and only when connected + flag-enabled; the three producer mutations enqueue inside their `$transaction` (never inline); the change-origin guard + disjoint partition keep the two-way sync loop-free. HRIS-SYNC-03 delivered.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-07-SUMMARY.md` when done.
</output>
