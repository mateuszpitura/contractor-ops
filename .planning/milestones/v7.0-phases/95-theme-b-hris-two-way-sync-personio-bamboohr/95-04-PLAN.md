---
phase: 95-theme-b-hris-two-way-sync-personio-bamboohr
plan: 04
type: execute
wave: 3
depends_on: [95-01, 95-02, 95-03]
files_modified:
  - packages/integrations/src/adapters/personio-adapter.ts
  - packages/integrations/src/adapters/hris-rate-limiter.ts
  - packages/integrations/src/env.ts
  - packages/integrations/src/index.ts
  - .env.example
autonomous: true
requirements: [HRIS-SYNC-01]

must_haves:
  truths:
    - "PersonioAdapter is a KsefAdapter-shaped adapter (supportsOAuth=false) — a proprietary client-credentials bearer, NOT an RFC-6749 OAuth2 client"
    - "listEmployees pulls Personio API v2 /persons with offset/limit≤200 pagination under a ≤200 req/min token-bucket limiter, and safeParses every page (no unsafe `as`)"
    - "The delta path passes `updated_since` from the stored cursor; attribute-scoped credentials silently omit unpermitted fields (missing ≠ error)"
    - "pushEmployeeEvent maps a CO business event to a Personio attribute PATCH, threading the outbox idempotency key"
    - "The live token-exchange/pull/push path is behind `integration.personio-sync` + creds; the adapter is fully testable against the recorded fixture with zero credentials"
  artifacts:
    - path: "packages/integrations/src/adapters/personio-adapter.ts"
      provides: "PersonioAdapter (bearer + listEmployees + pushEmployeeEvent + getHealthStatus)"
      contains: "class PersonioAdapter"
    - path: "packages/integrations/src/adapters/hris-rate-limiter.ts"
      provides: "token-bucket limiter (≤200 req/min) shared by HRIS adapters"
      contains: "RateLimiter"
  key_links:
    - from: "personio-adapter.ts listEmployees"
      to: "hris-rate-limiter.ts"
      via: "each page fetch acquires a limiter token before the request"
      pattern: "acquire"
---

<objective>
Build the **Personio adapter** (HRIS-SYNC-01) on the `packages/integrations` framework: a KsefAdapter-shaped adapter (proprietary client-credentials bearer, NOT RFC-6749), an API-v2 `/persons` pull with offset/limit≤200 pagination under a conservative ≤200 req/min limiter, and an attribute-PATCH push. Turns the Personio adapter RED tests (95-02) GREEN, with the live token-exchange/pull/push path conditionally-skipped until partner credentials land.

Purpose: give the pull orchestrator (95-06) and push handlers (95-07) a concrete `listEmployees`/`pushEmployeeEvent` client for Personio that is fully testable NOW against the recorded fixture.
Output: `PersonioAdapter` + a shared HRIS rate-limiter + env vars, all behind the dark `integration.personio-sync` flag. Registration in `register-all` is Wave 5 (95-08).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-CONTEXT.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-RESEARCH.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-VALIDATION.md

<interfaces>
- `packages/integrations/src/adapters/ksef-adapter.ts:10-18` — the non-OAuth adapter shape to mirror (`supportsOAuth=false`, token/credential auth, extends `BaseAdapter`).
- `packages/integrations/src/types/provider.ts:39-77` — the `IntegrationProviderAdapter` contract; `CredentialBlob` / `ProviderHealthStatus` imports.
- `packages/integrations/src/types/credentials.ts` — `CredentialBlob` shape for the bearer/token.
- `packages/api/src/services/hris-sync/types.ts` (95-01) — `HrisEmployeeRecord` / `HrisPushPayload` the adapter returns/consumes.
- an existing pure-fetch adapter (e.g. `dataport-company-registry-adapter.ts` or `ksef-adapter.ts`) for the `fetch` + `safeParse` idiom.
- `95-RESEARCH.md` § Code Examples (Personio skeleton) + Pitfall 6 (rate limit + attribute-scoped).
- `.planning/research/SUMMARY.md:33,220` — Personio v2, proprietary bearer, 200/min, offset≤200, attribute-scoped, v1 deprecates 2026-07-31.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared HRIS rate-limiter (≤200 req/min token bucket)</name>
  <files>packages/integrations/src/adapters/hris-rate-limiter.ts</files>
  <read_first>
    - `95-RESEARCH.md` § Pitfall 6 (conservative limiter)
    - any existing resilience/backoff util in `packages/integrations/src/services/` for style
  </read_first>
  <action>
    Create a small token-bucket `RateLimiter` (`acquire(): Promise<void>` that resolves when a token is available, refilling at a configurable rate; default 200 tokens / 60_000 ms). Pure, no I/O beyond timers. Used by both HRIS adapters so a burst pull never exceeds the per-credential budget. Keep it conservative (the 200/min figure is MEDIUM-confidence community data — a tighter real limit must still pass).
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/integrations test hris-rate-limiter 2>&1 | tail -15 || echo "limiter created (tested via adapter)"</automated>
  </verify>
  <acceptance_criteria>
    - `RateLimiter.acquire()` caps throughput to the configured rate (a burst of N>limit is serialized).
    - No external dep.
  </acceptance_criteria>
  <done>A conservative token-bucket limiter exists for the HRIS pull.</done>
</task>

<task type="auto">
  <name>Task 2: PersonioAdapter (bearer + listEmployees + pushEmployeeEvent + health)</name>
  <files>packages/integrations/src/adapters/personio-adapter.ts, packages/integrations/src/index.ts</files>
  <read_first>
    - `packages/integrations/src/adapters/ksef-adapter.ts` (non-OAuth shape)
    - `packages/integrations/src/types/provider.ts:39-77` + `types/credentials.ts`
    - the recorded fixture `__tests__/fixtures/personio/employees.json` (95-02)
    - `95-RESEARCH.md` § Code Examples (Personio skeleton) + Pitfall 6
  </read_first>
  <action>
    Create `PersonioAdapter extends BaseAdapter` — `slug='personio'`, `displayName='Personio'`, `supportsOAuth=false`, `supportsWebhooks=false`. Implement: a private `bearer(creds)` that mints/caches a short-lived bearer from the client-credentials pair (POST the Personio auth endpoint; cache to `tokenExpiresAt`); `listEmployees(creds, { updatedSince? }): Promise<HrisEmployeeRecord[]>` — GET `/v2/persons` with `offset`/`limit` (≤200) pagination, `updated_since` when a cursor is passed, each page fetch gated by the rate-limiter, each payload `safeParse`d into `HrisEmployeeRecord` (unpermitted/omitted attributes are normal — never throw); `pushEmployeeEvent(creds, payload)` — PATCH the mapped Personio attribute for the worker, threading `payload.idempotencyKey` (the outboxEventId) as an idempotency header; `getHealthStatus(connectionId)` (or inherit the BaseAdapter default). Export `PersonioAdapter` + a `fetchPersonioEmployees` convenience if the orchestrator prefers a free function (mirror `fetchJiraProjects`). No `console.*`; `safeParse` only, no unsafe `as`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/integrations test personio 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `supportsOAuth === false`; the adapter never constructs an RFC-6749 authorization-code flow.
    - `listEmployees` parses the recorded fixture, paginates offset/limit≤200, and is rate-limited.
    - Every external payload goes through `safeParse` (grep: no `as HrisEmployeeRecord`/unsafe cast on the fetch result).
    - The Personio adapter RED tests (95-02) turn GREEN; live cases SKIP without creds.
  </acceptance_criteria>
  <done>PersonioAdapter pulls + pushes against the fixture; live path dark.</done>
</task>

<task type="auto">
  <name>Task 3: Env vars (PERSONIO_CLIENT_ID/SECRET) + .env.example</name>
  <files>packages/integrations/src/env.ts, .env.example</files>
  <read_first>
    - `packages/integrations/src/env.ts` (the package env schema; how other adapter secrets are declared optional)
    - the `check:no-process-env` rule
  </read_first>
  <action>
    Add `PERSONIO_CLIENT_ID` + `PERSONIO_CLIENT_SECRET` to the integrations env schema as **optional** (live path dark until set; absence must not break boot). Add both to `.env.example` with a comment that the live Personio path is gated behind `integration.personio-sync`. Do not read `process.env` directly in the adapter — resolve via the package env accessor.
  </action>
  <verify>
    <automated>grep -E "PERSONIO_CLIENT_ID|PERSONIO_CLIENT_SECRET" packages/integrations/src/env.ts .env.example && pnpm check:no-process-env 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Both env vars optional in the schema + present in `.env.example`.
    - `pnpm check:no-process-env` green; adapter uses the env accessor, not raw `process.env`.
  </acceptance_criteria>
  <done>Personio credentials are declared; boot works with them absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Personio REST → adapter | External payloads are untrusted → `safeParse`, never unsafe `as`. |
| client-credentials → bearer | The bearer is minted server-side from encrypted creds; never logged. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-04-01 | Tampering | malformed Personio payload coerced via `as` | mitigate | `safeParse` on every page (Task 2). |
| T-95-04-02 | DoS | pull bursts past 200 req/min | mitigate | token-bucket limiter (Task 1). |
| T-95-04-03 | Information Disclosure | bearer/secret logged | mitigate | no `console.*`; secrets never in log fields (D-07). |
| T-95-04-SC | Supply chain | new dep | accept | No new package — pure `fetch` (Personio ships no Node SDK). |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/integrations test personio hris-rate-limiter` — GREEN; live cases SKIPPED without creds.
- `pnpm typecheck --filter=@contractor-ops/integrations` green.
- `pnpm check:no-process-env` + `pnpm lint:logs` + `pnpm standards:check` green.
</verification>

<success_criteria>
`PersonioAdapter` (client-credentials bearer, v2 offset≤200 pull under a ≤200/min limiter, attribute-PATCH push) is GREEN against the recorded fixture, with the live path dark behind `integration.personio-sync` + conditional-skip creds. HRIS-SYNC-01 satisfied at the adapter tier.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-04-SUMMARY.md` when done.
</output>
