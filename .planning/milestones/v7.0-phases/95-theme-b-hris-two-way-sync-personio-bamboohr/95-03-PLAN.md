---
phase: 95-theme-b-hris-two-way-sync-personio-bamboohr
plan: 03
type: execute
wave: 2
depends_on: [95-01, 95-02]
files_modified:
  - packages/db/prisma/schema/integration.prisma
  - packages/db/prisma/schema/migrations/__20260705120000_hris_two_way_sync/migration.sql
  - packages/api/src/services/hris-sync/mapping.ts
  - packages/api/src/services/hris-sync/__tests__/mapping.test.ts
autonomous: true
requirements: [HRIS-SYNC-06]

must_haves:
  truths:
    - "`IntegrationProvider` gains `PERSONIO` and `BAMBOOHR` (UPPER_SNAKE_CASE) — the adapters can now own IntegrationConnection rows"
    - "One HRIS per org is a DB-enforced raw-SQL PARTIAL unique index (`WHERE provider IN ('PERSONIO','BAMBOOHR')`) — NOT a Prisma @@unique, which cannot express a filtered index (correction C2)"
    - "The `configJson` HRIS mapping + sync-state schema is a Zod `.strict()` contract with a public projection (Teams precedent) that never exposes credentialsRef/raw sync-state"
    - "The migration is `__`-prefixed / unapplied (drift-blocked posture) and Prisma client regenerates cleanly"
  artifacts:
    - path: "packages/db/prisma/schema/migrations/__20260705120000_hris_two_way_sync/migration.sql"
      provides: "enum add + the one-HRIS-per-org partial unique index (raw SQL)"
      contains: "CREATE UNIQUE INDEX"
    - path: "packages/api/src/services/hris-sync/mapping.ts"
      provides: "hrisFieldMappingSchema (.strict) + resolveMapping + publicHrisConfig projection + HrisSyncState read/write helpers"
      contains: "hrisFieldMappingSchema"
  key_links:
    - from: "migration.sql partial index"
      to: "IntegrationProvider PERSONIO/BAMBOOHR"
      via: "the WHERE clause references the two new enum values"
      pattern: "provider IN ('PERSONIO','BAMBOOHR')"
---

<objective>
Lay the connection + model layer for HRIS sync (HRIS-SYNC-06): add the `PERSONIO`/`BAMBOOHR` provider enum values (correction C3), author the **one-HRIS-per-org** constraint as a raw-SQL **partial** unique index (correction C2 — Prisma `@@unique` cannot filter), and define the `configJson` field-mapping + sync-state schema with a public projection (Teams precedent). Turns the `hris-one-per-org` RED test (95-02) GREEN and gives the pull/push a typed mapping contract to read.

Purpose: make Personio-XOR-BambooHR a structural DB invariant (prevents three-way sync hell) and give every connection a place to store its field mapping + snapshot-diff state without a new table.
Output: an updated `integration.prisma` + an `__`-prefixed migration (enum + partial index) + a GREEN `mapping.ts`. No adapters/orchestrator yet.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-CONTEXT.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-RESEARCH.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-VALIDATION.md

<interfaces>
- `packages/db/prisma/schema/integration.prisma:31,124-160` — `IntegrationConnection @@unique([organizationId, provider, userId])` + the `IntegrationProvider` enum ending at `ZATCA` + the documented "Prisma @@unique can't do partial indexes" note (WebhookDelivery:124-128). This is exactly why the one-HRIS constraint is raw SQL.
- `packages/db/prisma/schema/migrations/` — the migration directory. Find a recent `__`-prefixed migration to mirror the header/format + how enum `ALTER TYPE ... ADD VALUE` is written.
- `packages/api/src/routers/integrations/teams.ts:16,32-36,54-63,132-153` — the `configJson` mapping schema + `publicTeamsConfig` projection to mirror.
- `95-RESEARCH.md` § Pattern 4 (the mapping schema) + Corrections C2/C3 + Pitfall 3/8.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PERSONIO + BAMBOOHR to the IntegrationProvider enum</name>
  <files>packages/db/prisma/schema/integration.prisma</files>
  <read_first>
    - `packages/db/prisma/schema/integration.prisma:138-160` (the enum block)
    - the `db:audit-enum-casing` rule (UPPER_SNAKE_CASE)
  </read_first>
  <action>
    Append `PERSONIO` and `BAMBOOHR` to `enum IntegrationProvider` (after `ZATCA`), UPPER_SNAKE_CASE. No other schema change here — the one-HRIS-per-org index is authored as raw SQL in the migration (Task 2), NOT as a Prisma `@@unique` (it can't express the `WHERE` filter).
  </action>
  <verify>
    <automated>grep -E "PERSONIO|BAMBOOHR" packages/db/prisma/schema/integration.prisma && pnpm db:generate 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `PERSONIO` + `BAMBOOHR` present in the enum, UPPER_SNAKE_CASE.
    - `pnpm db:generate` succeeds; the generated client compiles.
    - `pnpm db:audit-enum-casing` green.
  </acceptance_criteria>
  <done>The two HRIS providers exist in the enum; adapters can own connection rows.</done>
</task>

<task type="auto">
  <name>Task 2: Migration — enum values + one-HRIS-per-org partial unique index (raw SQL)</name>
  <files>packages/db/prisma/schema/migrations/__20260705120000_hris_two_way_sync/migration.sql</files>
  <read_first>
    - a recent `__`-prefixed migration under `packages/db/prisma/schema/migrations/` (header + `ALTER TYPE ... ADD VALUE` idiom)
    - `95-RESEARCH.md` Correction C2 (the exact index DDL)
    - the `lint:raw-sql` annotation convention (a `-- safe-raw-sql:` comment explaining the DDL)
  </read_first>
  <action>
    Author the migration body: `ALTER TYPE "IntegrationProvider" ADD VALUE IF NOT EXISTS 'PERSONIO';` + `... 'BAMBOOHR';` then the partial unique index:
    `CREATE UNIQUE INDEX IF NOT EXISTS "integration_connection_one_hris_per_org" ON "IntegrationConnection" ("organizationId") WHERE "provider" IN ('PERSONIO','BAMBOOHR');`
    Annotate the raw DDL so `lint:raw-sql` passes (this is schema DDL, not a tenant-scoped query). Keep the migration `__`-prefixed (drift-blocked / unapplied — applied at deploy per the Standing Constraint). Note in a header comment WHY this is raw SQL (Prisma `@@unique` can't filter) — no phase/req IDs in the comment (traceability lives in the commit + `.planning/`).
  </action>
  <verify>
    <automated>grep -E "CREATE UNIQUE INDEX.*integration_connection_one_hris_per_org" packages/db/prisma/schema/migrations/__20260705120000_hris_two_way_sync/migration.sql && grep -E "provider.*IN.*PERSONIO.*BAMBOOHR" packages/db/prisma/schema/migrations/__20260705120000_hris_two_way_sync/migration.sql && pnpm lint:raw-sql 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The partial unique index DDL is present with the `WHERE provider IN ('PERSONIO','BAMBOOHR')` filter.
    - Both `ADD VALUE` statements present (idempotent `IF NOT EXISTS`).
    - Migration `__`-prefixed; `lint:raw-sql` green.
  </acceptance_criteria>
  <done>One HRIS per org is a DB-enforced partial unique index; the enum add ships in the same migration.</done>
</task>

<task type="auto">
  <name>Task 3: configJson mapping + sync-state schema + public projection</name>
  <files>packages/api/src/services/hris-sync/mapping.ts, packages/api/src/services/hris-sync/__tests__/mapping.test.ts</files>
  <read_first>
    - `packages/api/src/routers/integrations/teams.ts:32-36,54-63,132-153` (the mapping schema + `publicTeamsConfig` projection)
    - `95-RESEARCH.md` § Pattern 4
    - `95-01` `types.ts` (`HrisFieldMapping`, `HrisSyncState`)
  </read_first>
  <action>
    Create `mapping.ts`: `hrisFieldMappingSchema` (Zod `.strict()` — `standard` object of optional HRIS-attr-key strings for displayName/email/position/department/employmentStatus/hireDate/terminatedAt + optional `customAttributes: Record<string,string>`); `resolveMapping(configJson): HrisFieldMapping` (safe-parse with a sensible default map); `readSyncState(configJson): HrisSyncState` + `writeSyncState(configJson, patch)` helpers over the `configJson` blob (lastSuccessfulSyncAt + hashes); and `publicHrisConfig(configJson)` that projects to `{ provider, mapping }` only — NEVER `credentialsRef` or raw hashes (mirror `publicTeamsConfig`). Write `mapping.test.ts` (GREEN): `.strict()` rejects an unknown top-level key; `resolveMapping` round-trips; `publicHrisConfig` omits sync-state + credentials; `readSyncState`/`writeSyncState` round-trip a hash map.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test mapping 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `hrisFieldMappingSchema` is `.strict()`; unknown keys rejected.
    - `publicHrisConfig` never exposes `credentialsRef`/`hashes` (test asserts absence).
    - Tests GREEN.
  </acceptance_criteria>
  <done>The connection carries a typed, safely-projected field mapping + snapshot-diff state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| org → integration connection | One HRIS per org must be structurally impossible to violate (DB-enforced). |
| connection.configJson → API response | The public projection must not leak credentials or raw sync-state. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-03-01 | Tampering | two HRIS connected to one org (three-way sync) | mitigate | Partial unique index → P2002 on the second connect (Task 2); the `hris-one-per-org` test (95-02) turns GREEN. |
| T-95-03-02 | Information Disclosure | `credentialsRef`/sync-state hashes leak via configJson read | mitigate | `publicHrisConfig` projects to `{ provider, mapping }` only (Task 3). |
| T-95-03-03 | Tampering | migration auto-applied / enum lowercased | mitigate | `__`-prefixed unapplied migration; UPPER_SNAKE_CASE enum; `db:audit-enum-casing` (Task 1). |
</threat_model>

<verification>
- `pnpm db:generate` + `pnpm typecheck --filter=@contractor-ops/api` green.
- `pnpm -F @contractor-ops/api test mapping` GREEN; `pnpm -F @contractor-ops/api test hris-one-per-org` turns GREEN (constraint now exists — run against a test DB with the migration applied).
- `pnpm lint:raw-sql` + `pnpm db:audit-enum-casing` + `pnpm standards:check` green.
</verification>

<success_criteria>
`IntegrationProvider` has `PERSONIO`/`BAMBOOHR`; one HRIS per org is a DB-enforced raw-SQL partial unique index (`hris-one-per-org` GREEN); and `mapping.ts` gives a `.strict()` field-mapping + sync-state contract with a credential-safe public projection. No adapters/orchestrator yet.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-03-SUMMARY.md` when done.
</output>
