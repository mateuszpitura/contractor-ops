---
phase: 95-theme-b-hris-two-way-sync-personio-bamboohr
plan: 09
type: execute
wave: 6
depends_on: [95-08]
files_modified:
  - .planning/brain/wiki/integrations/personio.md
  - .planning/brain/wiki/integrations/bamboohr.md
  - .planning/brain/wiki/integrations/_index.md
  - .planning/brain/wiki/domains/hris-sync.md
  - .planning/brain/wiki/structure/cron-jobs.md
  - .planning/brain/wiki/structure/api-routers-catalog.md
  - .planning/brain/wiki/structure/prisma-schema-areas.md
  - .planning/brain/wiki/patterns/feature-flags.md
  - .planning/EXTERNAL-ENABLEMENT.md
  - .planning/MEMORY.md
autonomous: true
requirements: [HRIS-SYNC-01, HRIS-SYNC-02, HRIS-SYNC-03, HRIS-SYNC-04, HRIS-SYNC-05, HRIS-SYNC-06]

must_haves:
  truths:
    - "Documentation follows code IN THE SAME CHANGE SET (mandatory, never deferred): the two new adapters, the pull orchestrator, the three push outbox types, the field partition, and the sync flow are all documented"
    - "New wiki pages integrations/personio.md + integrations/bamboohr.md + domains/hris-sync.md; updated cron-jobs, api-routers-catalog, prisma-schema-areas, feature-flags, integrations/_index"
    - "EXTERNAL-ENABLEMENT.md gains three register rows: Personio credential gate, BambooHR OAuth credential gate, BambooHR custom-attribute contract gate"
    - "MEMORY.md records the HRIS two-way-sync invariants (disjoint-partition loop-break; invoice.paid is NOT an outbox type; one-HRIS-per-org is a raw-SQL partial index)"
    - "Graph + BM25 rebuilt; check:wiki-brain green"
  artifacts:
    - path: ".planning/brain/wiki/domains/hris-sync.md"
      provides: "the HRIS two-way sync domain page (purpose, flow, field partition, loop-break, entry points, agent mistakes)"
      contains: "field partition"
    - path: ".planning/EXTERNAL-ENABLEMENT.md"
      provides: "three new register rows for the Personio/BambooHR credential + custom-attr gates"
      contains: "integration.personio-sync"
  key_links:
    - from: "wiki domain page verify_with"
      to: "packages/api/src/services/hris-sync/*"
      via: "the domain page's verify_with lists the sync source files so check:wiki-brain gates drift"
      pattern: "hris-sync"
---

<objective>
Close the documentation-follows-code loop for Phase 95 (mandatory, never deferred): document the two new adapters, the pull orchestrator, the three push outbox types, the source-of-truth field partition + loop-break, and the sync flow in the wiki; add the EXTERNAL-ENABLEMENT register rows for every credential/contract gate; record the invariants in MEMORY; and rebuild the graph + BM25 index. This is a required part of the same change set as the code (`pnpm check:wiki-brain` gates it).

Purpose: keep the codebase wiki a living compass so the next agent doesn't hallucinate the field partition, the loop-break, or the "invoice.paid is not an outbox type" fact.
Output: new + updated wiki pages, three EXTERNAL-ENABLEMENT rows, a MEMORY invariant, and a rebuilt graph/BM25 — `check:wiki-brain` green.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-CONTEXT.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-RESEARCH.md
@.planning/EXTERNAL-ENABLEMENT.md

<interfaces>
- `.planning/brain/wiki/integrations/_index.md` + an existing provider page (e.g. `integrations/jira.md` or the KSeF/peppol page) — the integration-page frontmatter + section shape to mirror.
- `.planning/brain/wiki/structure/cron-jobs.md` — add the `hris-sync` hourly job row.
- `.planning/brain/wiki/structure/api-routers-catalog.md` — add the `hrisSync` namespace (conditional workforce).
- `.planning/brain/wiki/structure/prisma-schema-areas.md` — note the `PERSONIO`/`BAMBOOHR` enum + the partial unique index.
- `.planning/brain/wiki/patterns/feature-flags.md` — note `integration.personio-sync` / `integration.bamboohr-sync` wiring.
- `.planning/EXTERNAL-ENABLEMENT.md` — the register table (rows #11–#15 are the Phase-94 precedent to mirror).
- CLAUDE.md § "Documentation follows code" + the refresh pipeline commands.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New + updated wiki pages</name>
  <files>.planning/brain/wiki/integrations/personio.md, .planning/brain/wiki/integrations/bamboohr.md, .planning/brain/wiki/domains/hris-sync.md, .planning/brain/wiki/integrations/_index.md, .planning/brain/wiki/structure/cron-jobs.md, .planning/brain/wiki/structure/api-routers-catalog.md, .planning/brain/wiki/structure/prisma-schema-areas.md, .planning/brain/wiki/patterns/feature-flags.md</files>
  <read_first>
    - an existing `integrations/*.md` page (frontmatter + `verify_with` + sections)
    - the four structure pages to update + `patterns/feature-flags.md`
  </read_first>
  <action>
    Author `integrations/personio.md` (client-credentials bearer, API v2, 200/min, offset≤200, attribute-scoped; pull/push surface; `integration.personio-sync` gate; `verify_with: packages/integrations/src/adapters/personio-adapter.ts`) and `integrations/bamboohr.md` (OAuth 2.0, un-paginated directory, standard-field sync, custom-attr gate; `verify_with: bamboohr-adapter.ts`). Author `domains/hris-sync.md` — Purpose, Flow (pull orchestrator + push outbox), the **field partition table** (field → owner → direction), the **loop-break** (disjoint partition + syncHash + change-origin guard), Entry points (`hrisSyncRouter`, `runHrisPull`, the three outbox types), Agent mistakes (invoice.paid is NOT an outbox type; one-HRIS-per-org is a raw-SQL partial index; national IDs never HRIS-writable); `verify_with` listing `packages/api/src/services/hris-sync/*` + `outbox/hris-push.ts`. Update `integrations/_index.md` (+2 providers), `structure/cron-jobs.md` (+`hris-sync` hourly), `structure/api-routers-catalog.md` (+`hrisSync` conditional-workforce namespace), `structure/prisma-schema-areas.md` (+enum + partial index), `patterns/feature-flags.md` (+the two `integration.*-sync` flags wired). Bump `source_commit` on touched frontmatter. NO operational text (no "audit in progress") — durable facts only.
  </action>
  <verify>
    <automated>ls .planning/brain/wiki/integrations/personio.md .planning/brain/wiki/integrations/bamboohr.md .planning/brain/wiki/domains/hris-sync.md && grep -l "field partition" .planning/brain/wiki/domains/hris-sync.md</automated>
  </verify>
  <acceptance_criteria>
    - Three new pages + five updated pages exist; the domain page carries the field-partition table + loop-break + the three "agent mistakes".
    - `verify_with` paths point at the real source files.
  </acceptance_criteria>
  <done>The wiki tracks the HRIS sync code.</done>
</task>

<task type="auto">
  <name>Task 2: EXTERNAL-ENABLEMENT rows + MEMORY invariant</name>
  <files>.planning/EXTERNAL-ENABLEMENT.md, .planning/MEMORY.md</files>
  <read_first>
    - `.planning/EXTERNAL-ENABLEMENT.md` rows #11–#15 (Phase-94 precedent format)
    - `.planning/MEMORY.md` (invariant style)
  </read_first>
  <action>
    Append three register rows to `.planning/EXTERNAL-ENABLEMENT.md` (next free numbers, mirror the column format):
    - **Personio partner client-credentials app** — Flag `integration.personio-sync` — Default OFF — Dark: live Personio pull/push (the recorded-fixture engine ships) — Tests: `packages/integrations` `personio-adapter` live case `it.skipIf(!PERSONIO_CLIENT_ID)` — Steps: create a Personio API v2 client-credentials app → set `PERSONIO_CLIENT_ID`/`PERSONIO_CLIENT_SECRET` → flip `integration.personio-sync` APPROVED per org.
    - **BambooHR partner OAuth app** — Flag `integration.bamboohr-sync` — Default OFF — Dark: live BambooHR OAuth connect + pull/push (standard-field engine + fixtures ship) — Tests: `bamboohr-adapter` live case `it.skipIf(!BAMBOOHR_CLIENT_ID)` — Steps: create a BambooHR OAuth 2.0 app → set `BAMBOOHR_CLIENT_ID`/`BAMBOOHR_CLIENT_SECRET` → flip `integration.bamboohr-sync` APPROVED per org.
    - **BambooHR custom-attribute contract** — Gate `BAMBOOHR_CUSTOM_ATTR_VERIFIED` env (not a flag) — Default unset — Dark: BambooHR custom-attribute → countryFields mapping (standard-field sync ships) — Tests: `bamboohr-adapter` custom-attr case `it.skipIf(!BAMBOOHR_CUSTOM_ATTR_VERIFIED)` — Steps: verify the tenant's custom-attribute contract → set `BAMBOOHR_CUSTOM_ATTR_VERIFIED` → the custom-attr path activates.
    Add a MEMORY.md bullet: the three HRIS invariants (disjoint-partition loop-break makes two-way sync acyclic; `invoice.paid` is NOT an outbox event type — three `hris.*.push` types were added; one-HRIS-per-org is a raw-SQL partial unique index, not a Prisma `@@unique`).
  </action>
  <verify>
    <automated>grep -E "integration.personio-sync|integration.bamboohr-sync|BAMBOOHR_CUSTOM_ATTR_VERIFIED" .planning/EXTERNAL-ENABLEMENT.md | head && grep -iE "hris|one-HRIS-per-org|disjoint" .planning/MEMORY.md | head</automated>
  </verify>
  <acceptance_criteria>
    - Three EXTERNAL-ENABLEMENT rows present (Personio, BambooHR OAuth, BambooHR custom-attr).
    - MEMORY invariant recorded.
  </acceptance_criteria>
  <done>Every credential/contract gate + the durable invariants are registered.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild graph + BM25 + check:wiki-brain</name>
  <files>.planning/graphs/graph.json</files>
  <read_first>
    - CLAUDE.md § "Documentation follows code" refresh-pipeline commands
  </read_first>
  <action>
    Run the refresh pipeline: `rm -f graphify-out/graph.json && graphify update . --no-cluster --force && cp graphify-out/graph.json .planning/graphs/graph.json`; then `cd .planning/brain && find wiki -name '*.md' -exec python3 scripts/contextual-prefix.py {} --no-llm \; && python3 scripts/bm25-index.py build`. Finally `pnpm check:wiki-brain`. (Graph/BM25 are local gitignored artifacts — WARN-only in CI; run them so the local index is fresh.)
  </action>
  <verify>
    <automated>pnpm check:wiki-brain 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check:wiki-brain` green (no NEW drift on the touched source files).
    - Graph + BM25 rebuilt.
  </acceptance_criteria>
  <done>The knowledge base + graph are fresh; the drift gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| code change → wiki | Doc drift is GATED (`check:wiki-brain`) — the wiki must ship in the same change set. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-09-01 | Repudiation | stale wiki → next agent hallucinates the field partition / outbox facts | mitigate | New/updated pages + `verify_with` + `check:wiki-brain` (Tasks 1/3). |
| T-95-09-02 | — | credential gate undocumented → someone hard-blocks planning on a partner account | mitigate | EXTERNAL-ENABLEMENT rows (Task 2). |
</threat_model>

<verification>
- `pnpm check:wiki-brain` green.
- Three new wiki pages + five updated pages present; three EXTERNAL-ENABLEMENT rows; MEMORY invariant.
- `pnpm standards:check` green.
</verification>

<success_criteria>
Documentation follows code in the same change set: the adapters, pull orchestrator, three push outbox types, field partition, and loop-break are documented (new `integrations/personio.md` + `integrations/bamboohr.md` + `domains/hris-sync.md` + five updated structure/pattern pages); EXTERNAL-ENABLEMENT has the Personio + BambooHR credential gates + the BambooHR custom-attr gate; MEMORY records the invariants; graph + BM25 rebuilt; `check:wiki-brain` green.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-09-SUMMARY.md` when done.
</output>
