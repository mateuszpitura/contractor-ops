---
phase: 95-theme-b-hris-two-way-sync-personio-bamboohr
plan: 02
type: execute
wave: 1
depends_on: [95-01]
files_modified:
  - packages/api/src/services/hris-sync/__tests__/hris-pull-orchestrator.test.ts
  - packages/api/src/services/hris-sync/__tests__/hris-cross-org.test.ts
  - packages/api/src/services/hris-sync/__tests__/hris-one-per-org.test.ts
  - packages/api/src/services/outbox/__tests__/hris-outbox.test.ts
  - packages/api/src/services/outbox/__tests__/hris-push-loop.test.ts
  - packages/integrations/src/adapters/__tests__/personio-adapter.test.ts
  - packages/integrations/src/adapters/__tests__/bamboohr-adapter.test.ts
  - packages/api/src/routers/workforce/__tests__/hris-sync-router.test.ts
  - packages/integrations/src/adapters/__tests__/fixtures/personio/employees.json
  - packages/integrations/src/adapters/__tests__/fixtures/bamboohr/employees.json
autonomous: true
requirements: [HRIS-SYNC-01, HRIS-SYNC-02, HRIS-SYNC-03, HRIS-SYNC-04, HRIS-SYNC-05, HRIS-SYNC-06]

must_haves:
  truths:
    - "Every downstream impl plan has a failing (RED) proof to turn GREEN — the Nyquist floor for the phase"
    - "The RED net covers: field-partition-survives-a-conflicting-pull, two-org IDOR, one-HRIS-per-org DB constraint, pull idempotency + orchestration, push loop-prevention + three outbox event types, both adapters (with conditional-skip live cases), and the flag-gated router"
    - "Recorded provider fixtures (the local mock HRIS) exist so the engine is testable NOW with zero partner credentials"
    - "Live-API cases are `it.skipIf(!creds)` so they auto-run + flip GREEN when credentials land; the BambooHR custom-attr case is `it.skipIf(!BAMBOOHR_CUSTOM_ATTR_VERIFIED)`"
  artifacts:
    - path: "packages/integrations/src/adapters/__tests__/fixtures/personio/employees.json"
      provides: "recorded Personio /v2/persons page (the mock HRIS input)"
      contains: "externalId"
    - path: "packages/api/src/services/hris-sync/__tests__/hris-one-per-org.test.ts"
      provides: "the one-HRIS-per-org partial-index regression (RED until 95-03)"
      contains: "one-per-org"
    - path: "packages/api/src/services/outbox/__tests__/hris-push-loop.test.ts"
      provides: "loop-prevention proof — a pull emits no push + change-origin guard"
      contains: "assertNotHrisOwnedField"
  key_links:
    - from: "hris tests"
      to: "95-01 field-partition + sync-hash"
      via: "tests import the GREEN foundation and assert the not-yet-built orchestrator/adapters/router against it"
      pattern: "projectToWritablePatch"
---

<objective>
Seed the RED validation net enumerated in `95-VALIDATION.md` § Wave 1 so every downstream impl plan has a failing proof to turn GREEN. This is the phase's Nyquist floor and the **RED-net-first** mandate: the five security/correctness invariants (field-partition conflict-resolution, one-HRIS-per-org, pull-idempotency, push-loop-prevention, two-org IDOR) get their tests **before** any orchestrator/adapter/router exists, plus adapter contract proofs (with conditional-skip live cases) and the flag-gated router.

Purpose: lock the behavior contract so Waves 3–5 are turn-RED-GREEN, and provide the recorded provider fixtures (the local mock HRIS) that make the engine testable with zero partner credentials.
Output: RED test files (failing via missing modules) + committed recorded fixtures. No product code — the `field-partition`/`sync-hash` foundation from 95-01 is the only GREEN dependency these import.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-CONTEXT.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-RESEARCH.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-VALIDATION.md

<interfaces>
<!-- Test-shape anchors to mirror. Read once; clone the harness idiom. -->
- `packages/api/src/services/outbox/__tests__/outbox.test.ts` — the outbox producer/drain test harness (mock `$executeRawUnsafe`, assert eventType/dedup) to mirror for the HRIS push tests.
- `packages/api/src/services/google-workspace-sync-orchestrator.ts:278-364` — the orchestrator shape the `hris-pull-orchestrator.test.ts` asserts against (SyncLog STARTED→SUCCESS, advisory-lock skip, configJson snapshot).
- `packages/integrations/src/adapters/jira-adapter.ts` + `ksef-adapter.ts` — the adapter shapes the adapter tests assert (getOAuthConfig for BambooHR; bearer for Personio).
- `packages/api/src/lib/integration-connection.ts:74-127` — `loadOrgIntegrationConnection` used by the one-per-org + cross-org tests.
- `95-RESEARCH.md` § Validation Architecture (the full test map) + `95-VALIDATION.md` § Wave 1 Requirements.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recorded provider fixtures (the local mock HRIS)</name>
  <files>packages/integrations/src/adapters/__tests__/fixtures/personio/employees.json, packages/integrations/src/adapters/__tests__/fixtures/bamboohr/employees.json</files>
  <read_first>
    - `95-RESEARCH.md` § Pattern 2 (Personio `/v2/persons` delta + offset≤200; BambooHR `/v1/employees/directory` un-paginated) + Standard Stack (attribute-scoped Personio)
  </read_first>
  <action>
    Create two recorded-response fixtures shaped like the real provider payloads: `personio/employees.json` (a `/v2/persons` page — array of persons with `id`, standard attributes name/email/position/department/status/hire-date + a couple custom attributes + an `updated_at`), and `bamboohr/employees.json` (a `/v1/employees/directory` un-paginated list — `employees[]` with `id`, `displayName`, `workEmail`, `jobTitle`, `department`, `status`, `hireDate`). Include at least one record with a value that (via a hostile mapping) would map to a protected key, so the field-partition test can prove it is dropped. These are the golden inputs the mock HRIS replays — no live call.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('packages/integrations/src/adapters/__tests__/fixtures/personio/employees.json','utf8')); JSON.parse(require('fs').readFileSync('packages/integrations/src/adapters/__tests__/fixtures/bamboohr/employees.json','utf8')); console.log('fixtures parse OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Both fixtures parse as JSON and carry ≥2 employee records each, including standard + custom attributes.
    - At least one record includes a value usable to prove protected-key dropping.
  </acceptance_criteria>
  <done>The mock HRIS input exists; the engine is testable with zero credentials.</done>
</task>

<task type="auto">
  <name>Task 2: Field-partition-survives + cross-org IDOR + one-HRIS-per-org RED tests</name>
  <files>packages/api/src/services/hris-sync/__tests__/hris-cross-org.test.ts, packages/api/src/services/hris-sync/__tests__/hris-one-per-org.test.ts, packages/api/src/services/hris-sync/__tests__/hris-pull-orchestrator.test.ts</files>
  <read_first>
    - `packages/api/src/services/google-workspace-sync-orchestrator.ts:278-364` (orchestrator shape)
    - `packages/api/src/lib/integration-connection.ts:74-127` (connection load)
    - `95-VALIDATION.md` § Per-Task Verification Map (the exact behaviors)
  </read_first>
  <action>
    Author three RED test files importing the (not-yet-built) `runHrisPull` from `../pull-orchestrator` and the GREEN `projectToWritablePatch`/`syncHash` from 95-01:
    - `hris-pull-orchestrator.test.ts` — asserts a run writes `IntegrationSyncLog(direction:'INBOUND', syncType:'hris_employee_sync', status STARTED→SUCCESS)`, takes the `sync` advisory-lock (a concurrent run returns skipped:already-running), passes the stored delta cursor, applies only the allowlist patch, and is per-record best-effort (one malformed record doesn't abort). RED via the missing orchestrator module.
    - `hris-cross-org.test.ts` — a Personio record whose `externalId` resolves (via `ExternalLink`) to org B's worker is NOT written when the run executes under `tenantStore.run({ organizationId: orgA })`. RED.
    - `hris-one-per-org.test.ts` — connecting a second HRIS (BambooHR) to an org already connected to Personio throws (P2002 → typed `CONFLICT`). Assert against the intended partial-index constraint; RED until 95-03 adds the enum + migration.
    Use the recorded fixtures as the adapter's `listEmployees` return (mock the adapter).
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hris-pull-orchestrator hris-cross-org hris-one-per-org 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All three files present and FAILING for the right reason (missing orchestrator/enum/migration — not a syntax/import error against 95-01).
    - The cross-org + one-per-org tests are explicit two-org / two-connection scenarios.
  </acceptance_criteria>
  <done>The field-partition-survives + IDOR + one-HRIS-per-org RED net is seeded.</done>
</task>

<task type="auto">
  <name>Task 3: Push loop-prevention + three-event outbox RED tests</name>
  <files>packages/api/src/services/outbox/__tests__/hris-outbox.test.ts, packages/api/src/services/outbox/__tests__/hris-push-loop.test.ts</files>
  <read_first>
    - `packages/api/src/services/outbox/__tests__/outbox.test.ts` (harness to mirror)
    - `packages/api/src/services/outbox/handlers.ts:28-84` (the registry to extend)
    - `95-RESEARCH.md` § Pattern 3 + Pitfall 1/2
  </read_first>
  <action>
    Author two RED test files:
    - `hris-outbox.test.ts` — the three event types (`hris.invoice-paid.push`, `hris.payment-status.push`, `hris.classification-outcome.push`) dispatch through the handler registry to the connected adapter's push; when no HRIS connection exists OR the `integration.*-sync` flag is dark, the handler is a **no-op** (not an error); `outboxEventId` is threaded as the downstream idempotency key. RED via missing event types/handlers.
    - `hris-push-loop.test.ts` — the loop-prevention proof: running the pull orchestrator (mocked) enqueues NO push event (disjoint partition), and `assertNotHrisOwnedField` (GREEN from 95-01) throws if a push payload is constructed with an HRIS-owned key. RED for the pull-no-push half (missing orchestrator) and GREEN for the guard half.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hris-outbox hris-push-loop 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both files present; the three event-type literals are asserted by name.
    - The no-op-when-dark and idempotency contracts are explicit assertions.
  </acceptance_criteria>
  <done>Push loop-prevention + three-event outbox RED net is seeded.</done>
</task>

<task type="auto">
  <name>Task 4: Adapter contract RED tests (conditional-skip live) + router RED test</name>
  <files>packages/integrations/src/adapters/__tests__/personio-adapter.test.ts, packages/integrations/src/adapters/__tests__/bamboohr-adapter.test.ts, packages/api/src/routers/workforce/__tests__/hris-sync-router.test.ts</files>
  <read_first>
    - `packages/integrations/src/adapters/jira-adapter.ts` + `ksef-adapter.ts` (adapter shapes)
    - `packages/integrations/src/types/provider.ts:17-77` (OAuthConfig + adapter contract)
    - `95-RESEARCH.md` § Pattern 4/5 + § Environment Availability (conditional-skip posture)
  </read_first>
  <action>
    Author three RED test files:
    - `personio-adapter.test.ts` — `PersonioAdapter` has `supportsOAuth === false`; `listEmployees` parses the recorded fixture, respects offset/limit≤200 pagination, and stays under the ≤200 req/min limiter (assert the limiter caps a burst); `safeParse` rejects a malformed payload without an unsafe `as`. A **live** token-exchange/pull case wrapped in `it.skipIf(!process.env.PERSONIO_CLIENT_ID)`. RED via missing adapter.
    - `bamboohr-adapter.test.ts` — `BambooHrAdapter` has `supportsOAuth === true` and a `getOAuthConfig()` naming `BAMBOOHR_CLIENT_ID`/`SECRET` + a `redirectPath`; `listEmployees` parses the un-paginated directory fixture; the **standard-field** map works; the **custom-attribute** mapping case is `it.skipIf(!process.env.BAMBOOHR_CUSTOM_ATTR_VERIFIED)`; a **live** case is `it.skipIf(!process.env.BAMBOOHR_CLIENT_ID)`. RED.
    - `hris-sync-router.test.ts` — the `hrisSync` router enforces `assertWorkforceEnabled` + `evaluate('integration.*-sync')`, connect is XOR (one-HRIS-per-org), `syncNow` calls the orchestrator, get/set mapping round-trips via `configJson`, and `writeAuditLog` fires on connect/disconnect. RED via missing router.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/integrations test personio bamboohr 2>&1 | tail -15; pnpm -F @contractor-ops/api test hris-sync-router 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All three files present + FAILING via missing modules (not harness errors).
    - Live cases are `it.skipIf(!creds)`; the BambooHR custom-attr case is `it.skipIf(!verified)` (they SKIP, not fail, with no creds).
  </acceptance_criteria>
  <done>Adapter + router RED net is seeded; live paths conditional-skip cleanly with zero credentials.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → not-yet-built modules | RED scaffolds must fail via missing modules, not brick `tsc` (tests are excluded from the package typecheck — Phase 88). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-02-01 | Tampering | RED tests brick `tsc --noEmit` | mitigate | `packages/api` + `packages/integrations` tsconfig already exclude `src/**/__tests__/**`; confirm, don't re-add a broad include. |
| T-95-02-02 | — | live-API tests fail CI without creds | mitigate | Every live case `it.skipIf(!creds)`; the BambooHR custom-attr case `it.skipIf(!verified)`. |
| T-95-02-SC | Tampering | fixture data carries real PII | accept | Fixtures are synthetic (fake ids/names); no real national IDs. |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test hris` + `pnpm -F @contractor-ops/integrations test personio bamboohr` — all RED for the right reason (missing modules), live cases SKIPPED.
- `pnpm typecheck --filter=@contractor-ops/api` + `--filter=@contractor-ops/integrations` green (tests excluded from tsc).
- Fixtures parse; `pnpm standards:check` green.
</verification>

<success_criteria>
The full Wave 1 RED net from `95-VALIDATION.md` is seeded: field-partition-survives, cross-org IDOR, one-HRIS-per-org, pull orchestration/idempotency, push loop-prevention + three outbox event types, both adapters (conditional-skip live), and the flag-gated router — all failing via missing modules — plus recorded provider fixtures. The GREEN 95-01 foundation is the only implemented dependency.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-02-SUMMARY.md` when done.
</output>
