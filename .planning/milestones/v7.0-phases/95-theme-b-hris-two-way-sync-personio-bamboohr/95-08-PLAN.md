---
phase: 95-theme-b-hris-two-way-sync-personio-bamboohr
plan: 08
type: execute
wave: 5
depends_on: [95-03, 95-04, 95-05, 95-06, 95-07]
files_modified:
  - packages/api/src/routers/workforce/hris-sync-router.ts
  - packages/api/src/root.ts
  - packages/integrations/src/adapters/register-all.ts
  - apps/cron-worker/src/jobs/handlers/hris-sync.ts
  - apps/cron-worker/src/jobs/registry.ts
  - apps/cron-worker/src/env.ts
  - apps/web-vite/src/components/hris-sync/hooks/use-hris-sync.ts
  - apps/web-vite/src/components/hris-sync/hris-sync-settings-container.tsx
  - apps/web-vite/src/components/hris-sync/hris-sync-connect-card.tsx
  - apps/web-vite/src/components/hris-sync/hris-sync-mapping-table.tsx
  - apps/web-vite/src/pages/settings/hris-sync-page.tsx
  - apps/web-vite/src/i18n/locales/en/hris-sync.json
autonomous: true
requirements: [HRIS-SYNC-01, HRIS-SYNC-02, HRIS-SYNC-03, HRIS-SYNC-04, HRIS-SYNC-06]

must_haves:
  truths:
    - "A flag-gated tRPC hrisSyncRouter (connect XOR, disconnect, syncNow, get/set mapping) is mounted dark inside conditionalWorkforceRouters (METHOD_NOT_FOUND when module.workforce-employees OFF)"
    - "Both adapters are registered in the HEAVY lazy tier of register-all; the pull/push paths await loadHeavyAdapters()"
    - "An hourly cron handler (hris-sync) runs runScheduledHrisSync, registered with CRON_HRIS_SYNC_SCHEDULE (default '0 * * * *')"
    - "Connect fires a fire-and-forget first pull so the user sees registry fields populate; connect/disconnect writeAuditLog; connect is XOR (one-HRIS-per-org P2002 → CONFLICT)"
    - "A web-vite settings surface (Page→Container→Hook→Component) with mandatory loading/empty/error + i18n parity (en/de/pl/ar + en-US), the hook the sole tRPC boundary"
  artifacts:
    - path: "packages/api/src/routers/workforce/hris-sync-router.ts"
      provides: "connect/disconnect/syncNow/getMapping/setMapping procedures, flag-gated + audited"
      contains: "hrisSyncRouter"
    - path: "apps/cron-worker/src/jobs/handlers/hris-sync.ts"
      provides: "hourly hris-sync cron handler → runScheduledHrisSync"
      contains: "runScheduledHrisSync"
  key_links:
    - from: "root.ts workforceRouters"
      to: "hrisSyncRouter"
      via: "hrisSync mounted in the const so it is absent when module.workforce-employees OFF"
      pattern: "hrisSync"
---

<objective>
Wire the HRIS sync engine into the app: a flag-gated tRPC router (connect/disconnect/syncNow/mapping) mounted dark, both adapters registered in `register-all`, an hourly cron handler, a fire-and-forget on-connect pull, and a thin web-vite settings surface. This turns the `hris-sync-router` RED test (95-02) GREEN and delivers the end-to-end connect→pull→push flow behind the dark flags.

Purpose: expose the engine to HR admins (connect an HRIS, map fields, Sync now) and the scheduler (hourly pull), while keeping the whole surface dark behind `module.workforce-employees` + the per-provider `integration.*-sync` flags.
Output: `hrisSyncRouter` + root mount + `register-all` entries + cron handler/registry/env + a Page→Container→Hook→Component settings surface with i18n parity.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-CONTEXT.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-RESEARCH.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-VALIDATION.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
- `packages/api/src/routers/integrations/teams.ts:65-153` — the integration router shape (get status, get/set mapping) + `integrationSettingsProcedure`.
- `packages/api/src/routers/integrations/jira.ts:144` — the OAuth complete-connection mutation firing a fire-and-forget first sync (the on-connect pull pattern).
- `packages/api/src/lib/integration-procedure.ts:16-38` — `integrationProcedure`/`integrationSettingsProcedure` (RBAC + tenant).
- `packages/api/src/middleware/require-workforce-flag.ts:25-42` — `assertWorkforceEnabled`.
- `packages/api/src/root.ts:185-196,262` — `workforceRouters` + `conditionalWorkforceRouters` (mount `hrisSync` here).
- `packages/integrations/src/adapters/register-all.ts:69-140` — the HEAVY lazy tier (add both adapters).
- `apps/cron-worker/src/jobs/{registry.ts:64-67, handlers/org-definition-sync.ts}` + `env.ts:42-46` — the cron registration + `CRON_*_SCHEDULE` env idiom.
- an existing web-vite integration-settings surface (e.g. the Teams/Jira settings page) for the Page→Container→Hook→Component pattern + loading/empty/error.
- `95-RESEARCH.md` § Pattern 5 + Pitfall 9.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: hrisSyncRouter (connect XOR + disconnect + syncNow + mapping) + dark mount</name>
  <files>packages/api/src/routers/workforce/hris-sync-router.ts, packages/api/src/root.ts</files>
  <read_first>
    - `packages/api/src/routers/integrations/teams.ts:65-153` + `jira.ts:144`
    - `packages/api/src/middleware/require-workforce-flag.ts:25-42`
    - `packages/api/src/root.ts:185-196`
    - `packages/api/src/services/hris-sync/mapping.ts` (95-03) + `pull-orchestrator.ts` (95-06)
  </read_first>
  <action>
    Create `hrisSyncRouter` (`integrationSettingsProcedure`-based). Procedures: `getStatus` (the org's HRIS connection status + `publicHrisConfig`); `connect` (Zod `.strict()` input `{ provider: 'PERSONIO'|'BAMBOOHR', ...creds/oauth-start }`; per-request `assertWorkforceEnabled` + `evaluate('integration.<provider>-sync')`; create the `IntegrationConnection` — catch P2002 from the partial index → `TRPCError CONFLICT` "another HRIS is already connected"; `writeAuditLog`; **fire-and-forget** `runHrisPull(...).catch(log)` so registry fields populate); `disconnect` (`writeAuditLog`); `syncNow` (calls `runHrisPull` for the org's connection); `getMapping`/`setMapping` (round-trip `configJson` via `hrisFieldMappingSchema` `.strict()`). Mount `hrisSync: hrisSyncRouter` inside `workforceRouters` in `root.ts` so it is absent when `module.workforce-employees` is OFF.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hris-sync-router root-router-gating 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `connect` is XOR (second HRIS → CONFLICT); `writeAuditLog` on connect/disconnect; on-connect fire-and-forget pull.
    - Per-request `assertWorkforceEnabled` + `evaluate('integration.*-sync')`; `.strict()` inputs.
    - Mounted in `workforceRouters`; absent when the flag is OFF (`root-router-gating` GREEN).
    - `hris-sync-router` RED test GREEN.
  </acceptance_criteria>
  <done>The flag-gated HRIS router is live-dark and audited.</done>
</task>

<task type="auto">
  <name>Task 2: Register both adapters (HEAVY tier) + cron handler/registry/env</name>
  <files>packages/integrations/src/adapters/register-all.ts, apps/cron-worker/src/jobs/handlers/hris-sync.ts, apps/cron-worker/src/jobs/registry.ts, apps/cron-worker/src/env.ts</files>
  <read_first>
    - `packages/integrations/src/adapters/register-all.ts:69-140` (HEAVY tier)
    - `apps/cron-worker/src/jobs/handlers/org-definition-sync.ts` (handler shape)
    - `apps/cron-worker/src/jobs/registry.ts:64-67` + `env.ts:42-46`
  </read_first>
  <action>
    Add `PersonioAdapter` + `BambooHrAdapter` to the HEAVY lazy tier `Promise.all` import + `registerAdapter(...)` block in `register-all.ts` (both pull vendor REST clients). Create `apps/cron-worker/src/jobs/handlers/hris-sync.ts` mirroring `org-definition-sync.ts` — call `runScheduledHrisSync({ prisma, getRegionalClient, createTenantClientFrom, tenantStore })`, `createCronLogger`, Sentry-capture on failure, return `{ ok, durationMs, details }`. Register `{ meta:{ name:'hris-sync', schedule: env.CRON_HRIS_SYNC_SCHEDULE }, handler: hrisSyncHandler }` in the cron registry. Add `CRON_HRIS_SYNC_SCHEDULE: z.string().default('0 * * * *')` to `apps/cron-worker/src/env.ts` (hourly). No `console.*`.
  </action>
  <verify>
    <automated>grep -E "PersonioAdapter|BambooHrAdapter" packages/integrations/src/adapters/register-all.ts && grep -E "hris-sync|CRON_HRIS_SYNC_SCHEDULE" apps/cron-worker/src/jobs/registry.ts apps/cron-worker/src/env.ts && pnpm typecheck --filter=@contractor-ops/cron-worker 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Both adapters registered in the HEAVY tier; `getAdapter('personio'|'bamboohr')` resolves after `loadHeavyAdapters()`.
    - `hris-sync` cron handler registered with `CRON_HRIS_SYNC_SCHEDULE` (hourly default).
    - cron-worker typecheck green.
  </acceptance_criteria>
  <done>Adapters are registered; the hourly pull is scheduled.</done>
</task>

<task type="auto">
  <name>Task 3: web-vite HRIS settings surface (Page→Container→Hook→Component + i18n)</name>
  <files>apps/web-vite/src/components/hris-sync/hooks/use-hris-sync.ts, apps/web-vite/src/components/hris-sync/hris-sync-settings-container.tsx, apps/web-vite/src/components/hris-sync/hris-sync-connect-card.tsx, apps/web-vite/src/components/hris-sync/hris-sync-mapping-table.tsx, apps/web-vite/src/pages/settings/hris-sync-page.tsx, apps/web-vite/src/i18n/locales/en/hris-sync.json</files>
  <read_first>
    - `apps/web-vite/ARCHITECTURE.md` (layer rules)
    - an existing integration-settings page (Teams/Jira) for the pattern + loading/empty/error
    - the `check:web-vite-data-layer` / `check:web-vite-page-shells` rules
  </read_first>
  <action>
    Build the surface: `use-hris-sync.ts` (the SOLE tRPC boundary — `getStatus`/`connect`/`disconnect`/`syncNow`/`getMapping`/`setMapping`); `hris-sync-settings-container.tsx` (section loading/empty/error); `hris-sync-connect-card.tsx` (provider picker Personio|BambooHR + connect/disconnect + Sync-now + last-sync status); `hris-sync-mapping-table.tsx` (standard-field mapping editor); `hris-sync-page.tsx` (thin composer — `Suspense` + permission gate, NO tRPC). All user-facing strings via `useTranslations` from `hris-sync.json`; add en + stub de/pl/ar/en-US to keep `i18n:parity` green (native review deferred per EXTERNAL-ENABLEMENT). Mandatory loading/empty (no HRIS connected)/error states.
  </action>
  <verify>
    <automated>pnpm check:web-vite-data-layer 2>&1 | tail -5 && pnpm check:web-vite-page-shells 2>&1 | tail -5 && pnpm i18n:parity 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Page has no tRPC; Container has no direct tRPC; the hook is the only tRPC seam (`check:web-vite-data-layer` green).
    - Mandatory loading/empty/error states present.
    - `i18n:parity` green (en/de/pl/ar + en-US keys present); no hardcoded user-facing strings.
  </acceptance_criteria>
  <done>HR admins can connect an HRIS, map fields, and Sync now — behind the dark flags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → hrisSyncRouter | Flag-gated + RBAC + `.strict()` inputs; org from session. |
| flag OFF → surface | Router absent from appRouter (METHOD_NOT_FOUND). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-08-01 | Elevation | reach the router with `module.workforce-employees` OFF | mitigate | Conditional root spread + per-request `assertWorkforceEnabled` (Task 1). |
| T-95-08-02 | Tampering | connect a second HRIS (three-way sync) | mitigate | P2002 from the partial index → CONFLICT (Task 1). |
| T-95-08-03 | Information Disclosure | mapping/status leaks credentials | mitigate | `publicHrisConfig` projection (95-03). |
| T-95-08-04 | — | i18n / data-layer guard regressions | mitigate | `i18n:parity` + `check:web-vite-*` (Task 3). |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test hris-sync-router root-router-gating` — GREEN.
- `pnpm typecheck --filter=@contractor-ops/api` + `--filter=@contractor-ops/cron-worker` + `--filter=@contractor-ops/web-vite` green.
- `pnpm check:web-vite-data-layer` + `check:web-vite-page-shells` + `i18n:parity` + `lint:audit-log` + `standards:check` green.
</verification>

<success_criteria>
The end-to-end HRIS flow is wired and dark: a flag-gated, audited `hrisSyncRouter` (connect XOR / disconnect / syncNow / mapping) mounted in `conditionalWorkforceRouters`, both adapters in the HEAVY tier, an hourly `hris-sync` cron, a fire-and-forget on-connect pull, and a Page→Container→Hook→Component settings surface with i18n parity. HRIS-SYNC-01/02/03/04/06 exposed end-to-end.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-08-SUMMARY.md` when done.
</output>
