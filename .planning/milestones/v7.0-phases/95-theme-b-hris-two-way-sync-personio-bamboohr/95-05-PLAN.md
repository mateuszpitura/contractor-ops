---
phase: 95-theme-b-hris-two-way-sync-personio-bamboohr
plan: 05
type: execute
wave: 3
depends_on: [95-01, 95-02, 95-03, 95-04]
files_modified:
  - packages/integrations/src/adapters/bamboohr-adapter.ts
  - packages/integrations/src/env.ts
  - packages/integrations/src/index.ts
  - .env.example
autonomous: true
requirements: [HRIS-SYNC-04]

must_haves:
  truths:
    - "BambooHrAdapter is a JiraAdapter-shaped OAuth 2.0 adapter (supportsOAuth=true + getOAuthConfig naming BAMBOOHR_CLIENT_ID/SECRET) — the Basic-auth API key is deprecated for B2B and NOT used"
    - "listEmployees pulls the un-paginated /v1/employees/directory and returns HrisEmployeeRecord[] for snapshot-diff; same pull/push surface as Personio"
    - "The STANDARD-field mapping ships; the CUSTOM-attribute mapping path is gated behind a verification flag with conditional-skip tests (D-06)"
    - "The live OAuth round-trip + pull/push is behind `integration.bamboohr-sync` + creds; the adapter is fully testable against the recorded fixture with zero credentials"
  artifacts:
    - path: "packages/integrations/src/adapters/bamboohr-adapter.ts"
      provides: "BambooHrAdapter (getOAuthConfig + exchange/refresh + listEmployees + pushEmployeeEvent)"
      contains: "class BambooHrAdapter"
  key_links:
    - from: "bamboohr-adapter.ts getOAuthConfig"
      to: "OAuthConfig (provider.ts)"
      via: "returns clientIdEnvVar/clientSecretEnvVar/authorizationUrl/tokenUrl/scopes/redirectPath"
      pattern: "getOAuthConfig"
---

<objective>
Build the **BambooHR adapter** (HRIS-SYNC-04) on the framework — a JiraAdapter-shaped OAuth 2.0 adapter with the **same pull/push surface** as Personio. Ship the verified **standard-field** sync; **gate the custom-attribute mapping** path behind a verification flag with conditional-skip tests (D-06 — the BambooHR custom-attr contract is unverified). Turns the BambooHR adapter RED tests (95-02) GREEN, with the live path conditionally-skipped until partner OAuth credentials land.

Purpose: give the pull orchestrator (95-06) and push handlers (95-07) a BambooHR `listEmployees`/`pushEmployeeEvent` client, reusing the shared rate-limiter (95-04) and the `HrisEmployeeRecord` contract (95-01).
Output: `BambooHrAdapter` + env vars, behind the dark `integration.bamboohr-sync` flag, with the custom-attr path conditional-skip. Registration in `register-all` is Wave 5 (95-08).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-CONTEXT.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-RESEARCH.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-VALIDATION.md

<interfaces>
- `packages/integrations/src/adapters/jira-adapter.ts:92-101` — the OAuth adapter shape (`slug`, `supportsOAuth=true`, `override getOAuthConfig`).
- `packages/integrations/src/types/provider.ts:17-32` — `OAuthConfig` (clientIdEnvVar/clientSecretEnvVar/authorizationUrl/tokenUrl/scopes/redirectPath).
- `packages/integrations/src/adapters/personio-adapter.ts` (95-04) — `listEmployees`/`pushEmployeeEvent` signatures to match + the shared `hris-rate-limiter`.
- the recorded fixture `__tests__/fixtures/bamboohr/employees.json` (95-02).
- `95-RESEARCH.md` § Code Examples (BambooHR skeleton) + Pitfall 7 (custom-attr gate).
- `.planning/research/SUMMARY.md:34,39,221` — BambooHR OAuth 2.0 mandatory, un-paginated list, no Node SDK, Basic-auth deprecated.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BambooHrAdapter (OAuth config + exchange/refresh + list/push)</name>
  <files>packages/integrations/src/adapters/bamboohr-adapter.ts, packages/integrations/src/index.ts</files>
  <read_first>
    - `packages/integrations/src/adapters/jira-adapter.ts` (OAuth adapter shape)
    - `packages/integrations/src/types/provider.ts:17-77`
    - `packages/integrations/src/adapters/personio-adapter.ts` (match the pull/push signatures)
    - the recorded fixture `__tests__/fixtures/bamboohr/employees.json`
  </read_first>
  <action>
    Create `BambooHrAdapter implements IntegrationProviderAdapter` — `slug='bamboohr'`, `displayName='BambooHR'`, `supportsOAuth=true`, `supportsWebhooks=false`. Implement `getOAuthConfig()` (clientIdEnvVar `BAMBOOHR_CLIENT_ID`, clientSecretEnvVar `BAMBOOHR_CLIENT_SECRET`, authorization/token URLs, `scopes`, `redirectPath: '/api/oauth/bamboohr/callback'`); `exchangeCodeForTokens(code, redirectUri)` + `refreshToken(creds)` (mirror Jira; return `CredentialBlob`); `listEmployees(creds, _opts): Promise<HrisEmployeeRecord[]>` — GET `/v1/employees/directory` (un-paginated), rate-limited via the shared limiter, `safeParse` into `HrisEmployeeRecord[]` (opts.updatedSince unused → snapshot-diff happens in the orchestrator); `pushEmployeeEvent(creds, payload)` — POST the mapped BambooHR field for the worker, threading `payload.idempotencyKey`. The **standard-field** mapping is fully implemented. `getHealthStatus` inherits or is implemented. No `console.*`; `safeParse` only.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/integrations test bamboohr 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `supportsOAuth === true`; `getOAuthConfig()` names `BAMBOOHR_CLIENT_ID`/`SECRET` + a redirectPath.
    - `listEmployees` parses the un-paginated fixture; `safeParse` on every payload (no unsafe `as`).
    - Standard-field BambooHR adapter RED tests (95-02) turn GREEN; live cases SKIP without creds.
  </acceptance_criteria>
  <done>BambooHR OAuth adapter pulls + pushes standard fields against the fixture; live path dark.</done>
</task>

<task type="auto">
  <name>Task 2: Gate the custom-attribute mapping path (D-06)</name>
  <files>packages/integrations/src/adapters/bamboohr-adapter.ts</files>
  <read_first>
    - `95-RESEARCH.md` § Pitfall 7 + § Open Questions Q5
    - `95-VALIDATION.md` § Manual-Only Verifications (BambooHR custom-attribute)
  </read_first>
  <action>
    Add a `mapCustomAttributes(record, mapping.customAttributes)` path that is **only exercised when the contract is verified** — read a `BAMBOOHR_CUSTOM_ATTR_VERIFIED` env flag (optional; default unset). When unset, the adapter returns standard fields only and the custom-attr mapping is a documented no-op (never throws). The corresponding test case is `it.skipIf(!process.env.BAMBOOHR_CUSTOM_ATTR_VERIFIED)` so it auto-runs + flips GREEN when the contract is confirmed. Add a code comment explaining WHY the path is gated (contract unverified) — no phase/req IDs (traceability → commit + `.planning/`).
  </action>
  <verify>
    <automated>grep -E "BAMBOOHR_CUSTOM_ATTR_VERIFIED|customAttributes" packages/integrations/src/adapters/bamboohr-adapter.ts && pnpm -F @contractor-ops/integrations test bamboohr 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - The custom-attr path is a no-op when `BAMBOOHR_CUSTOM_ATTR_VERIFIED` is unset (never throws).
    - The custom-attr test is `it.skipIf(!verified)` (SKIPS, not fails, without the flag).
    - Standard-field sync works regardless.
  </acceptance_criteria>
  <done>Custom-attribute mapping is flag-deferred; standard-field sync ships; the test auto-flips when the contract lands.</done>
</task>

<task type="auto">
  <name>Task 3: Env vars (BAMBOOHR_CLIENT_ID/SECRET) + .env.example</name>
  <files>packages/integrations/src/env.ts, .env.example</files>
  <read_first>
    - `packages/integrations/src/env.ts` (95-04 added PERSONIO_* — mirror)
  </read_first>
  <action>
    Add `BAMBOOHR_CLIENT_ID` + `BAMBOOHR_CLIENT_SECRET` (+ optional `BAMBOOHR_CUSTOM_ATTR_VERIFIED`) to the integrations env schema as **optional**, and to `.env.example` with a note that the live BambooHR path is gated behind `integration.bamboohr-sync` and the custom-attr path behind `BAMBOOHR_CUSTOM_ATTR_VERIFIED`. Resolve via the package env accessor (no raw `process.env` in the adapter).
  </action>
  <verify>
    <automated>grep -E "BAMBOOHR_CLIENT_ID|BAMBOOHR_CLIENT_SECRET" packages/integrations/src/env.ts .env.example && pnpm check:no-process-env 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Both OAuth env vars optional in the schema + present in `.env.example`.
    - `pnpm check:no-process-env` green.
  </acceptance_criteria>
  <done>BambooHR credentials declared; boot works with them absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| BambooHR REST/OAuth → adapter | External payloads + token exchange are untrusted → `safeParse`, encrypted creds, no unsafe `as`. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-05-01 | Spoofing | OAuth token theft / cross-org reuse | mitigate | Tokens in AES-256-GCM `credentialsRef` keyed org+provider; refresh-lock; live path dark until APPROVED. |
| T-95-05-02 | Tampering | unverified custom-attr contract corrupts mapping | mitigate | Custom-attr path gated behind `BAMBOOHR_CUSTOM_ATTR_VERIFIED` (Task 2). |
| T-95-05-03 | Tampering | malformed directory payload via `as` | mitigate | `safeParse` on every payload (Task 1). |
| T-95-05-SC | Supply chain | new dep | accept | No new package — pure `fetch` (BambooHR ships no Node SDK). |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/integrations test bamboohr` — standard-field GREEN; custom-attr + live cases SKIPPED without flags/creds.
- `pnpm typecheck --filter=@contractor-ops/integrations` green.
- `pnpm check:no-process-env` + `pnpm lint:logs` + `pnpm standards:check` green.
</verification>

<success_criteria>
`BambooHrAdapter` (OAuth 2.0, un-paginated directory pull, standard-field mapping, push) is GREEN against the recorded fixture; the custom-attribute path is flag-deferred with a conditional-skip test; the live path is dark behind `integration.bamboohr-sync` + conditional-skip creds. HRIS-SYNC-04 satisfied at the adapter tier.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-05-SUMMARY.md` when done.
</output>
