---
phase: 95-theme-b-hris-two-way-sync-personio-bamboohr
plan: 06
type: execute
wave: 4
depends_on: [95-01, 95-02, 95-03, 95-04, 95-05]
files_modified:
  - packages/api/src/services/hris-sync/pull-orchestrator.ts
  - packages/api/src/services/hris-sync/apply-patch.ts
  - packages/api/src/services/hris-sync/index.ts
autonomous: true
requirements: [HRIS-SYNC-02, HRIS-SYNC-05]

must_haves:
  truths:
    - "runHrisPull clones the directory-sync orchestrator: IntegrationSyncLog(INBOUND) STARTED→SUCCESS/FAILED, `sync` advisory-lock per connection (released in finally), token/bearer refresh, failure recorded on both syncLog + connection"
    - "The pull applies ONLY the allowlist patch (projectToWritablePatch) — the write payload physically cannot carry financial/compliance/PII columns (HRIS-SYNC-05 enforced end-to-end)"
    - "Idempotency: syncHash gates each record — an unchanged snapshot is a no-op write; delta cursor via updated-since (Personio) else snapshot-diff (BambooHR)"
    - "Per-record best-effort: one malformed/failed record doesn't abort the run (mirror org-definition-sync); writeAuditLog(INTEGRATION) on each applied write; tenant-scoped so a record never writes another org's worker"
    - "The scheduled fan-out (runScheduledHrisSync) iterates CONNECTED Personio/BambooHR connections, region-aware, throttled by lastSyncAt (hourly cadence)"
  artifacts:
    - path: "packages/api/src/services/hris-sync/pull-orchestrator.ts"
      provides: "runHrisPull(single connection) + runScheduledHrisSync(cron fan-out)"
      contains: "runHrisPull"
    - path: "packages/api/src/services/hris-sync/apply-patch.ts"
      provides: "applyPatchToWorker — resolves ExternalLink→worker, writes Worker/EmployeeProfile/PersonnelFile from the allowlist patch, merges countryFields, audits"
      contains: "applyPatchToWorker"
  key_links:
    - from: "pull-orchestrator.ts"
      to: "field-partition.projectToWritablePatch + sync-hash.syncHash"
      via: "per record: project → hash → skip-if-equal → applyPatchToWorker"
      pattern: "projectToWritablePatch"
---

<objective>
Build the **inbound pull orchestrator** (HRIS-SYNC-02) as a clone of `google-workspace-sync-orchestrator.ts`, applying the allowlist patch so the source-of-truth split (HRIS-SYNC-05) is enforced end-to-end. Turns the `hris-pull-orchestrator`, `hris-cross-org`, and pull-idempotency RED tests (95-02) GREEN.

Purpose: pull people/contracts/departments/(gated custom-attrs) from the connected HRIS on hourly cron + on-demand, write ONLY the HRIS-owned registry fields via `projectToWritablePatch`, skip unchanged records via `syncHash`, and never touch a financial/compliance/PII column or another org's worker.
Output: `runHrisPull` (single connection) + `runScheduledHrisSync` (cron fan-out) + `applyPatchToWorker`. The cron handler + router wiring is Wave 5 (95-08).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-CONTEXT.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-RESEARCH.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-VALIDATION.md

<interfaces>
- `packages/api/src/services/google-workspace-sync-orchestrator.ts:278-364` — clone target: syncLog create, advisory-lock acquire/skip/release-in-finally, `ensureFreshCredentials` token refresh, `persistSyncSuccess`/`recordSyncFailure`.
- `packages/api/src/services/org-definition-sync.ts:396-497` — the cron fan-out (`runScheduled*`): iterate CONNECTED connections, `lastSyncAt` throttle, `getRegionalClient` + `createTenantClientFrom` + `tenantStore.run`, per-connection try/catch.
- `packages/api/src/lib/advisory-lock.ts:78-104` — `tryAcquireAdvisoryLock(prisma,'sync',key)` / `releaseAdvisoryLock`.
- `packages/api/src/lib/integration-connection.ts:74-127` — `loadOrgIntegrationConnection`.
- `packages/db/prisma/schema/integration.prisma:38-57` — `ExternalLink` (externalId → entityId/workerId) for resolving a remote record to a local worker.
- `packages/api/src/services/hris-sync/{field-partition,sync-hash,mapping,types}.ts` (95-01/95-03) — projection, hash, mapping resolver.
- `packages/api/src/services/audit-writer.ts:66-121` — `writeAuditLog` (pass `tx`).
- the adapters (95-04/95-05) — `getAdapter(connection.provider).listEmployees(...)`.
- `95-RESEARCH.md` § Pattern 2 + Pitfall 4/5/6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: applyPatchToWorker — allowlist write + countryFields merge + audit</name>
  <files>packages/api/src/services/hris-sync/apply-patch.ts</files>
  <read_first>
    - `packages/db/prisma/schema/{worker,employee,personnel}.prisma` (the write targets)
    - `packages/db/prisma/schema/integration.prisma:38-57` (ExternalLink resolution)
    - `packages/api/src/services/audit-writer.ts:66-121`
    - `95-RESEARCH.md` § Pattern 1 (patch shape) + Pitfall 4/5
  </read_first>
  <action>
    Create `applyPatchToWorker(tenantDb, organizationId, externalId, patch: HrisWritableEmployeePatch, opts: { origin: 'HRIS_PULL' })`: resolve the local `workerId` via `ExternalLink` filtered by `organizationId` (skip + log if no link — a new remote employee is surfaced, not auto-created, mirroring the directory-sync "notify, don't auto-provision" posture unless the phase later decides otherwise); in one `$transaction` write only the allowlist fields — `Worker.displayName/email`, `EmployeeProfile.employmentStatus/etat` and a **merge** of `countryFieldsPatch` into existing `EmployeeProfile.countryFields` (never a wholesale replace — CO-owned countryFields keys survive), `PersonnelFile.hireDate/terminatedAt` — and `writeAuditLog({ actorType:'INTEGRATION', action:'hris.pull.apply', resourceType:'WORKER', resourceId: workerId, tx })`. The function accepts only `HrisWritableEmployeePatch`, so no protected column can be written. Tenant-scoped `tenantDb` guarantees org isolation.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test field-partition-survives hris-cross-org 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Writes only allowlist columns; `countryFields` is merged, not replaced (CO keys survive).
    - Resolves the worker via `ExternalLink` filtered by org (cross-org test GREEN).
    - `writeAuditLog(INTEGRATION)` on each applied write (`lint:audit-log` green).
  </acceptance_criteria>
  <done>The allowlist write path is tenant-safe, audited, and structurally unable to touch protected columns.</done>
</task>

<task type="auto">
  <name>Task 2: runHrisPull — single-connection orchestrator (clone GWS)</name>
  <files>packages/api/src/services/hris-sync/pull-orchestrator.ts, packages/api/src/services/hris-sync/index.ts</files>
  <read_first>
    - `packages/api/src/services/google-workspace-sync-orchestrator.ts:278-364` (clone)
    - `packages/api/src/lib/advisory-lock.ts:78-104`
    - `packages/api/src/services/hris-sync/{field-partition,sync-hash,mapping}.ts`
  </read_first>
  <action>
    Create `runHrisPull({ organizationId, connectionId, actorUserId })`: create `IntegrationSyncLog(direction:'INBOUND', syncType:'hris_employee_sync', status:'STARTED')`; acquire `tryAcquireAdvisoryLock(prisma,'sync',`hris:${connectionId}`)` — on miss, mark the log SUCCESS with `{ skipped:true, reason:'already-running' }` and return; in try: `loadAndValidateConnection` (org match + status CONNECTED), `getAdapter(connection.provider)` (await `loadHeavyAdapters()` first), `ensureFreshCredentials` (mirror GWS token refresh; for Personio, mint/refresh the bearer), read the delta cursor + hashes from `resolveMapping`/`readSyncState(connection.configJson)`, `adapter.listEmployees(creds, { updatedSince })`, then per record: `projectToWritablePatch` → `syncHash` → skip if equal to the stored hash → `applyPatchToWorker` (per-record try/catch, best-effort like org-definition-sync:306-323, count errors); `writeSyncState(configJson, { lastSuccessfulSyncAt, hashes })` + update `syncLog` SUCCESS + connection `lastSyncAt/lastSuccessAt`; catch → `recordSyncFailure(syncLog, connection, msg)` + rethrow; finally → release the lock (best-effort). `createCronLogger`/`createLogger` — no `console.*`.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hris-pull-orchestrator 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SyncLog(INBOUND) STARTED→SUCCESS/FAILED; advisory-lock acquired + released; concurrent run skips.
    - Delta cursor passed; unchanged records skipped (idempotent — asserted by a re-run producing zero writes).
    - Per-record best-effort (one bad record → `errors++`, run continues).
    - `hris-pull-orchestrator` RED test GREEN.
  </acceptance_criteria>
  <done>Single-connection pull mirrors the proven orchestrator and enforces the allowlist + idempotency.</done>
</task>

<task type="auto">
  <name>Task 3: runScheduledHrisSync — cron fan-out</name>
  <files>packages/api/src/services/hris-sync/pull-orchestrator.ts</files>
  <read_first>
    - `packages/api/src/services/org-definition-sync.ts:396-497` (the fan-out to clone)
  </read_first>
  <action>
    Add `runScheduledHrisSync(deps)` mirroring `runScheduledOrgDefinitionSync`: `prisma.integrationConnection.findMany({ where: { status:'CONNECTED', provider: { in: ['PERSONIO','BAMBOOHR'] } }, select: { ..., organization: { select: { dataRegion:true } } } })`; throttle by `lastSyncAt` against an hourly cutoff (`SYNC_THROTTLE_MS = 60*60*1000`); per connection resolve the regional tenant client + run `runHrisPull` inside `tenantStore.run({ organizationId, region }, ...)`; per-connection try/catch → summary `{ evaluated, ran, skipped, runs[] }`. Return the summary for the cron handler to log.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test hris-pull-orchestrator 2>&1 | tail -10 && pnpm typecheck --filter=@contractor-ops/api 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Iterates only CONNECTED PERSONIO/BAMBOOHR connections; region-aware; hourly throttle.
    - Runs each inside `tenantStore.run` (org isolation preserved).
    - Returns an `{ evaluated, ran, skipped, runs }` summary.
  </acceptance_criteria>
  <done>The hourly cron fan-out is implemented (handler registration is 95-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| HRIS record → CO DB write | Only the allowlist patch crosses; tenant-scoped; audited. |
| concurrent syncs | Serialized per connection via the `sync` advisory-lock. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-06-01 | Tampering | pull overwrites a financial/compliance/PII column | mitigate | `applyPatchToWorker` accepts only `HrisWritableEmployeePatch` (Task 1). |
| T-95-06-02 | Information Disclosure | record for org A writes org B's worker (IDOR) | mitigate | `tenantStore.run` + tenant-scoped db + `ExternalLink` filtered by org (Task 1/3); cross-org test GREEN. |
| T-95-06-03 | DoS | overlapping syncs / redundant writes | mitigate | advisory-lock + `syncHash` skip (Task 2). |
| T-95-06-04 | Availability | one bad record aborts the whole run | mitigate | per-record best-effort (Task 2). |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test hris-pull-orchestrator hris-cross-org field-partition-survives` — GREEN.
- `pnpm typecheck --filter=@contractor-ops/api` green.
- `pnpm lint:audit-log` + `pnpm lint:logs` + `pnpm standards:check` green.
</verification>

<success_criteria>
`runHrisPull` + `runScheduledHrisSync` clone the proven orchestrator: SyncLog(INBOUND) + advisory-lock + token refresh + delta/snapshot-diff + `syncHash` idempotency + per-record best-effort, applying ONLY the allowlist patch tenant-safely with audit. HRIS-SYNC-02 delivered; HRIS-SYNC-05 enforced end-to-end. Cross-org + idempotency + orchestrator RED tests GREEN.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-06-SUMMARY.md` when done.
</output>
