---
phase: 95-theme-b-hris-two-way-sync-personio-bamboohr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/services/hris-sync/field-partition.ts
  - packages/api/src/services/hris-sync/sync-hash.ts
  - packages/api/src/services/hris-sync/types.ts
  - packages/api/src/services/hris-sync/__tests__/field-partition.test.ts
  - packages/api/src/services/hris-sync/__tests__/sync-hash.test.ts
autonomous: true
requirements: [HRIS-SYNC-05, HRIS-SYNC-02]

must_haves:
  truths:
    - "A typed HRIS-writable allowlist (`HrisWritableEmployeePatch`) exists whose keys are ONLY the HRIS-owned registry fields — invoice/payment/classification/compliance and *Encrypted/*Last4 national-ID keys are physically absent from the type"
    - "`projectToWritablePatch(raw, mapping)` returns only allowlisted keys and DROPS (never throws on) any HRIS attribute mapped to a CO-owned or unknown key"
    - "`syncHash(patch)` is deterministic and key-order-independent so an unchanged snapshot hashes equal and the pull can skip the write (idempotency primitive)"
    - "The field-owner map + change-origin tag are exported for the pull orchestrator (95-06) and push guard (95-07) to import"
    - "Both modules are GREEN-tested and pure (no Prisma, no I/O) so they golden-test without a DB"
  artifacts:
    - path: "packages/api/src/services/hris-sync/field-partition.ts"
      provides: "FieldOwner map + HrisWritableEmployeePatch type + projectToWritablePatch() + assertNotHrisOwnedField()"
      contains: "HrisWritableEmployeePatch"
    - path: "packages/api/src/services/hris-sync/sync-hash.ts"
      provides: "deterministic syncHash(patch) + ChangeOrigin tag"
      contains: "syncHash"
    - path: "packages/api/src/services/hris-sync/types.ts"
      provides: "HrisEmployeeRecord (raw provider DTO) + HrisFieldMapping + HrisSyncState + HrisPushPayload shared contracts"
      contains: "HrisEmployeeRecord"
  key_links:
    - from: "field-partition.ts projectToWritablePatch"
      to: "types.ts HrisFieldMapping"
      via: "projection reads the org mapping to resolve HRIS attr keys → allowlisted target keys"
      pattern: "HrisFieldMapping"
---

<objective>
Stand up the pure foundation of the HRIS sync engine — the **source-of-truth field partition** (D-01, HRIS-SYNC-05) and the **loop-prevention primitives** (the write-back-loop mechanism, HRIS-SYNC-02) — as I/O-free, GREEN-tested modules every downstream plan imports.

Purpose: make "financial/compliance fields are physically un-writable by the pull" true at the **type level** (the protected keys are absent from `HrisWritableEmployeePatch`, so they can never enter the Prisma update payload the mapper emits), and give the pull a deterministic idempotency hash so an unchanged hourly snapshot is a no-op. This is the crux the whole phase rests on; it ships GREEN (a foundation gate, not a RED plan).
Output: a compiling, GREEN `packages/api/src/services/hris-sync/` foundation with `field-partition.ts`, `sync-hash.ts`, and the shared `types.ts` contract the adapters (Wave 3), orchestrator (95-06), and push handlers (95-07) build against. No Prisma, no adapters, no router yet.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-CONTEXT.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-RESEARCH.md
@.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-VALIDATION.md

<interfaces>
<!-- The pull-target columns that define the allowlist. Read once; the allowlist keys map 1:1 to these. -->
- `packages/db/prisma/schema/employee.prisma:20-63` — `EmployeeProfile`: `countryCode`, `countryFields` JSON, promoted `etat` (Decimal(3,2)), `employmentStatus` (enum ACTIVE/ON_LEAVE/SUSPENDED/TERMINATED), and the encrypted `*Encrypted`/`*Last4` national-ID columns. The encrypted/Last4 columns are **CO-owned, never in the allowlist**.
- `packages/db/prisma/schema/personnel.prisma:49-50` — `PersonnelFile.hireDate` (@db.Date) + `terminatedAt`: the hire/termination anchors (HRIS-owned).
- `packages/db/prisma/schema/worker.prisma` — `Worker.displayName` + `email`: the registry name/contact (HRIS-owned).
- `packages/validators/src/employee-country-fields.ts:189-196` — `employeeCountryFieldsSchemaMap`: the per-market `.strict()` schemas that reject national identifiers from `countryFields`. The `countryFieldsPatch` allowlist must only carry keys these schemas accept.
- `95-RESEARCH.md` § Pattern 1 (the exact `HrisWritableEmployeePatch` shape) + Pattern 4 (`HrisFieldMapping`/`HrisSyncState`) + Code Examples (`syncHash`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared contract types (types.ts)</name>
  <files>packages/api/src/services/hris-sync/types.ts</files>
  <read_first>
    - `95-RESEARCH.md` § Pattern 1, 2, 3, 4 (the DTO/mapping/state/push shapes)
    - `packages/api/src/routers/integrations/teams.ts:16` (the `configJson` mapping precedent)
  </read_first>
  <action>
    Create `types.ts` with the shared, I/O-free contracts: `HrisEmployeeRecord` (the normalized raw provider record — `externalId`, a `Record<string, unknown>` of provider attributes, `provider: 'PERSONIO'|'BAMBOOHR'`); `HrisFieldMapping` (`{ standard: {...optional keys}; customAttributes?: Record<string,string> }`); `HrisSyncState` (`{ lastSuccessfulSyncAt?: string; hashes?: Record<string,string> }`); `HrisPushPayload` (the three push shapes union — invoice-paid/payment-status/classification-outcome, each carrying `workerId` + business id). Export `HRIS_PROVIDERS = ['PERSONIO','BAMBOOHR'] as const`. No Prisma imports — these are wire/DTO types.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api exec tsc --noEmit -p tsconfig.json 2>&1 | grep -i "hris-sync/types" | head -5 || echo "types.ts typechecks"</automated>
  </verify>
  <acceptance_criteria>
    - `HrisEmployeeRecord`, `HrisFieldMapping`, `HrisSyncState`, `HrisPushPayload`, `HRIS_PROVIDERS` all exported.
    - No `@contractor-ops/db` / Prisma import (pure DTO module).
  </acceptance_criteria>
  <done>Shared HRIS-sync contracts defined and typechecking.</done>
</task>

<task type="auto">
  <name>Task 2: Field partition — owner map + allowlist projection (the SYNC-05 crux)</name>
  <files>packages/api/src/services/hris-sync/field-partition.ts, packages/api/src/services/hris-sync/__tests__/field-partition.test.ts</files>
  <read_first>
    - `95-RESEARCH.md` § Pattern 1 + Pitfall 4 (PII/financial columns absent from the patch)
    - `packages/db/prisma/schema/employee.prisma:20-63` (the exact columns; encrypted/Last4 are CO-owned)
    - `packages/validators/src/employee-country-fields.ts:189-196` (countryFields key vocabulary the patch may carry)
  </read_first>
  <action>
    Create `field-partition.ts`:
    (a) `export type FieldOwner = 'HRIS' | 'CONTRACTOR_OPS';`
    (b) `HrisWritableEmployeePatch` — the allowlist type with EXACTLY: `displayName?`, `email?`, `employmentStatus?`, `etat?`, `hireDate?`, `terminatedAt?`, `countryFieldsPatch?: Record<string, unknown>`. **No** invoice/payment/classification/compliance keys; **no** `*Encrypted`/`*Last4`; **no** `organizationId`/`workerId`.
    (c) `FIELD_OWNER: Record<string, FieldOwner>` — the documented partition table (registry keys → HRIS; invoice/payment/classification/compliance + national-ID keys → CONTRACTOR_OPS).
    (d) `projectToWritablePatch(raw: HrisEmployeeRecord, mapping: HrisFieldMapping): HrisWritableEmployeePatch` — resolves each mapped HRIS attribute to its target allowlist key; any attribute mapped to a CO-owned or unknown key is **dropped** (never throws — attribute-scoped Personio omits fields silently); `countryFieldsPatch` is filtered to the configured HRIS-owned key set and never carries a national-ID key.
    (e) `assertNotHrisOwnedField(payload: HrisPushPayload): void` — the change-origin guard the push handler calls; throws if a push payload carries any key present in the HRIS-owned set (defense-in-depth against a future co-owned field re-introducing a loop).
    Then write `field-partition.test.ts` (GREEN): projection returns only allowlisted keys; a raw record with an invoice amount / payment status / a PESEL value mapped to a protected key → those keys ABSENT from the returned patch; `countryFieldsPatch` excludes an unmapped/national-ID key; `assertNotHrisOwnedField` throws for an HRIS-owned key and passes for a CO-owned business payload; a "protected field survives" case: given a prior DB-shaped row object, applying the patch (spread) leaves financial/`*Encrypted` fields untouched.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test field-partition 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `HrisWritableEmployeePatch` has NO financial/compliance/national-ID keys (grep the file: no `invoice`, `payment`, `Encrypted`, `Last4`, `classification`).
    - `projectToWritablePatch` drops protected/unknown-mapped keys without throwing.
    - `assertNotHrisOwnedField` throws on an HRIS-owned key.
    - Tests GREEN.
  </acceptance_criteria>
  <done>The source-of-truth partition is enforced at the type + projection boundary and proven GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: sync-hash — deterministic idempotency + change-origin tag</name>
  <files>packages/api/src/services/hris-sync/sync-hash.ts, packages/api/src/services/hris-sync/__tests__/sync-hash.test.ts</files>
  <read_first>
    - `95-RESEARCH.md` § Code Examples (`syncHash`) + Pitfall 1 (loop-break)
  </read_first>
  <action>
    Create `sync-hash.ts`: `export type ChangeOrigin = 'HRIS_PULL' | 'CONTRACTOR_OPS';` and `syncHash(patch: HrisWritableEmployeePatch): string` using `node:crypto` `createHash('sha256')` over `JSON.stringify(patch, Object.keys(patch).sort())` (stable key order so equal patches hash equal regardless of provider field order). Write `sync-hash.test.ts` (GREEN): identical patches with different key insertion order hash equal; a changed value changes the hash; an empty patch hashes stably; assert the "skip when equal" contract by hashing a stored vs incoming patch.
  </action>
  <verify>
    <automated>pnpm -F @contractor-ops/api test sync-hash 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `syncHash` deterministic + key-order-independent (test proves equal hash for reordered keys).
    - Uses `node:crypto` (no new dep).
    - Tests GREEN.
  </acceptance_criteria>
  <done>The pull idempotency primitive is GREEN; unchanged snapshots will hash equal and skip the write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| HRIS record → CO write payload | The allowlist projection is the boundary — CO-owned fields must be structurally unable to cross it. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01-01 | Tampering | HRIS value overwrites a financial/compliance field | mitigate | `HrisWritableEmployeePatch` excludes those keys at the type level (Task 2); acceptance greps for absence. |
| T-95-01-02 | Information Disclosure | national ID (`*Encrypted`/`*Last4`) written or leaked by the pull | mitigate | National-ID keys absent from the allowlist; `countryFieldsPatch` filtered; `.strict()` per-market schema rejects them downstream (Pitfall 4). |
| T-95-01-03 | Tampering | write-back loop from a future co-owned field | mitigate | `assertNotHrisOwnedField` fails loudly rather than looping (Task 2e). |
</threat_model>

<verification>
- `pnpm -F @contractor-ops/api test field-partition sync-hash` — all GREEN.
- `pnpm typecheck --filter=@contractor-ops/api` green (pure modules, no Prisma).
- `pnpm standards:check` green; `pnpm lint:no-breadcrumbs` green (no phase/req IDs in source comments).
</verification>

<success_criteria>
`packages/api/src/services/hris-sync/{field-partition,sync-hash,types}.ts` exist, are pure (no I/O), GREEN-tested, and enforce the source-of-truth partition at the type boundary. Downstream plans import `HrisWritableEmployeePatch`, `projectToWritablePatch`, `assertNotHrisOwnedField`, `syncHash`, and the shared contracts. No adapters/orchestrator/router yet.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/95-theme-b-hris-two-way-sync-personio-bamboohr/95-01-SUMMARY.md` when done.
</output>
