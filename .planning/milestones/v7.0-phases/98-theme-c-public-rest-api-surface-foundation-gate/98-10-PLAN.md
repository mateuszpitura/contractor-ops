---
phase: 98-theme-c-public-rest-api-surface-foundation-gate
plan: 10
type: execute
wave: 6
depends_on: ["98-06", "98-09"]
files_modified:
  - apps/public-api/src/routes/contractors.ts
  - apps/public-api/src/routes/invoices.ts
  - apps/public-api/src/routes/payments.ts
  - apps/public-api/src/routes/payment-runs.ts
  - apps/public-api/src/routes/workflows.ts
  - apps/public-api/src/routes/workflow-tasks.ts
  - apps/public-api/src/routes/compliance-documents.ts
  - apps/public-api/src/__tests__/openapi-doc.test.ts
  - apps/public-api/src/__tests__/write-routes-dark.test.ts
autonomous: true
requirements: [INTEG-API-01, INTEG-API-02]

must_haves:
  truths:
    - "Every write procedure from 98-09 gets a Hono `createRoute` (POST/PATCH) that delegates to the public tRPC caller — but each write `createRoute` carries `hide: true` so it is ABSENT from the derived OpenAPI 3.1 document (and therefore from the Speakeasy SDK + Scalar portal)"
    - "Writes are reachable ONLY through the flag-gated + scope-checked procedure (dark per-org until Phase 99); the `hide:true` is the SECOND darkness layer (spec/SDK invisibility), independent of the runtime flag gate"
    - "The `openapi-doc.test.ts` writes-absent assertion is finalized: the 3.1 document contains all read paths and ZERO write paths"
    - "A `write-routes-dark.test.ts` asserts the double-darkness: a write route 404s when `module.public-api` is off, and the write path is absent from `buildOpenApiDocument(app)`"
  artifacts:
    - path: "apps/public-api/src/routes/payment-runs.ts"
      provides: "hidden (hide:true) POST/PATCH write routes delegating to the dark payment_run write procedures"
      contains: "hide: true"
    - path: "apps/public-api/src/__tests__/write-routes-dark.test.ts"
      provides: "Double-dark contract — writes 404 when flag off AND absent from the derived spec/SDK"
      contains: "getOpenAPI31Document"
  key_links:
    - from: "apps/public-api/src/routes/payment-runs.ts"
      to: "packages/api/src/routers/public-api/payment-run.ts"
      via: "createRoute POST/PATCH → createPublicCaller(c).paymentRun.<verb> — hide:true keeps it out of the spec"
      pattern: "hide: true"
---

<objective>
Expose the 98-09 write procedures over HTTP as Hono `createRoute` POST/PATCH endpoints, but with `hide: true` so they never enter the derived OpenAPI 3.1 document, the Scalar portal, or the Speakeasy-generated SDK. This is the second darkness layer (spec/SDK invisibility) that composes with the runtime per-org flag gate (98-04): a write endpoint is both 404-until-flag-granted AND invisible-to-consumers until Phase 99. This satisfies INTEG-API-01's write half at the transport layer while guaranteeing the write surface does not leak into published artifacts before Phase 99 lands key management + scope UI.

Output: hidden write routes for the 7 write entities + the finalized "writes absent from spec" + double-dark tests.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-RESEARCH.md
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-SPIKE-FINDINGS.md

<interfaces>
`createRoute({ method:'post'|'patch', path:'/...', hide: true, request:{ body:{ content:{'application/json':{ schema:<.strict() write DTO> }}}}, responses:{ 200, 400, 401, 403, 404 } })` + `app.openapi(route, handler)` where the handler `const body = c.req.valid('json')` → `createPublicCaller(c).<entity>.<verb>(body)`. `hide:true` excludes the route from `getOpenAPI31Document` (RESEARCH § Anti-Patterns: "un-hiding writes" is forbidden in P98). Route→procedure map mirrors 98-09: contractors POST(create)/PATCH(update), invoices PATCH(void)[+POST create], payments POST(create)/PATCH(update), payment-runs POST(create)/PATCH(transition)/POST(export), workflows POST(create)/POST(execute), workflow-tasks PATCH(transition), compliance-documents POST(create)/PATCH(link). No write route for classifications or audit-log (read-only).
The write DTOs are the same `.strict()` schemas 98-09 uses for the tRPC `.input()` (defense-in-depth: validate at the Hono boundary AND the tRPC input).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hidden write createRoutes to the 7 write-entity route modules</name>
  <files>apps/public-api/src/routes/contractors.ts, apps/public-api/src/routes/invoices.ts, apps/public-api/src/routes/payments.ts, apps/public-api/src/routes/payment-runs.ts, apps/public-api/src/routes/workflows.ts, apps/public-api/src/routes/workflow-tasks.ts, apps/public-api/src/routes/compliance-documents.ts</files>
  <read_first>
    - apps/public-api/src/routes/contractors.ts (post-98-07 createRoute read module to extend with hidden writes)
    - 98-RESEARCH.md § Pattern 1 (createRoute request.body) + § Anti-Patterns (hide:true on writes; every spec route is an OpenAPIHono instance)
    - packages/api/src/routers/public-api/payment-run.ts (the 98-09 write procedures + their `.strict()` input DTOs to reuse)
  </read_first>
  <action>
    For each of the 7 write-entity route modules, add the POST/PATCH `createRoute` definitions per the map above, each with `hide: true`, a `request.body` bound to the same `.strict()` write DTO the tRPC procedure uses, and a handler that validates (`c.req.valid('json')`) and delegates to `createPublicCaller(c).<entity>.<verb>(...)`. Do NOT add write routes to classifications/audit-log. Keep the read routes unchanged. The runtime flag gate + scope check live in the tRPC layer (inherited) — the Hono route is thin transport. No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/public-api test routes 2>&1 | tail -10; grep -rl "hide: true" apps/public-api/src/routes | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The 7 write-entity route modules each have their POST/PATCH write routes with `hide: true`; classifications/audit-log have NO write route.
    - Write handlers validate the `.strict()` body and delegate to the public caller (thin transport; no business logic in Hono).
    - Route tests (reads still green) pass; write handlers wired to the 98-09 procedures.
  </acceptance_criteria>
  <done>Hidden write routes added for the 7 write entities.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize openapi-doc writes-absent + double-dark test</name>
  <files>apps/public-api/src/__tests__/openapi-doc.test.ts, apps/public-api/src/__tests__/write-routes-dark.test.ts</files>
  <read_first>
    - apps/public-api/src/lib/build-openapi-doc.ts (buildOpenApiDocument — the derived doc to assert against)
    - the Wave-0 openapi-doc.test.ts (98-02) writes-absent assertion to finalize
    - apps/public-api/src/__tests__/rate-limit.security.test.ts (the *.security-style Hono fetch-harness for the 404-when-flag-off assertion)
  </read_first>
  <action>
    Finalize `openapi-doc.test.ts`: assert `buildOpenApiDocument(app)` contains every read path (9 entities) and ZERO write paths/operations (no POST/PATCH under any public path — writes are `hide:true`). Add `write-routes-dark.test.ts` asserting the double-darkness: (a) a POST/PATCH to a write route with `module.public-api` OFF returns 404 (flag gate), and (b) the write path is absent from `buildOpenApiDocument(app)` (spec/SDK invisibility). Keep both scoped.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/public-api test openapi-doc write-routes-dark 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `openapi-doc.test.ts` GREEN: reads present, writes absent, `openapi` 3.1.
    - `write-routes-dark.test.ts` GREEN: write 404 when flag off AND write path absent from the derived doc.
  </acceptance_criteria>
  <done>Writes-absent + double-dark contracts GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| consumer ← published spec/SDK | Writes must not appear in the doc/SDK/portal before Phase 99 |
| consumer → write route | Reachable only when flag granted (Phase 99) + correct scope |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-10-01 | Information Disclosure | Write surface leaks into spec/SDK/portal pre-99 | mitigate | `hide:true` on every write createRoute; openapi-doc test asserts zero write paths |
| T-98-10-02 | Elevation of Privilege | Write reachable before flag granted | mitigate | Runtime flag gate (98-04) 404s writes when off; write-routes-dark test asserts it |
| T-98-10-03 | Tampering | Business logic drift in the Hono write handler | mitigate | Handler is thin transport → tRPC caller; validation via the same `.strict()` DTO (defense-in-depth) |
| T-98-10-SC | Tampering | npm installs | accept | No new deps |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/public-api test openapi-doc write-routes-dark routes` green.
- `pnpm typecheck --filter @contractor-ops/public-api` clean.
- Grep: every write route carries `hide: true`; classifications/audit-log have no write route.
</verification>

<success_criteria>
INTEG-API-01 (transport) + INTEG-API-02 (spec integrity): the 7 write entities are reachable over HTTP but double-dark — 404 until the per-org flag is granted AND absent from the derived 3.1 spec / Scalar portal / SDK until Phase 99.
</success_criteria>

<output>
Create `.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-10-SUMMARY.md` when done.
</output>
