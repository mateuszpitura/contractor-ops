---
phase: 98-theme-c-public-rest-api-surface-foundation-gate
plan: 07
type: execute
wave: 3
depends_on: ["98-06"]
files_modified:
  - apps/public-api/src/routes/contractors.ts
  - apps/public-api/src/routes/invoices.ts
  - apps/public-api/src/routes/contracts.ts
  - apps/public-api/src/routes/documents.ts
  - apps/public-api/src/routes/feature-flags.ts
  - packages/api/src/routers/public-api/contractor.ts
  - packages/api/src/routers/public-api/invoice.ts
  - packages/api/src/routers/public-api/contract.ts
  - packages/api/src/routers/public-api/document.ts
  - packages/validators/src/public-api/index.ts
  - apps/public-api/src/routes/__tests__/contractors.test.ts
  - apps/public-api/src/routes/__tests__/invoices.test.ts
  - apps/public-api/src/routes/__tests__/contracts.test.ts
  - apps/public-api/src/routes/__tests__/documents.test.ts
autonomous: true
requirements: [INTEG-API-04]

must_haves:
  truths:
    - "The 5 existing read routes (contractors, invoices, contracts, documents, feature-flags) are migrated to `OpenAPIHono` `createRoute` so they appear in the derived 3.1 spec with `.strict()` DTOs + typed responses"
    - "All list procedures move from offset (page/pageSize/skip/take/count) to opaque-cursor pagination (`cursorClause` + `paginateByLastKeptUndefined` + encode/decode) with the `{data,meta:{nextCursor,hasMore}}` envelope — `total` is dropped (single convention, no offset/cursor split)"
    - "`filter[field]=`/`sort=` conventions replace the ad-hoc `status`/`lifecycleStage`/`sortBy`/`sortOrder` query params; each entity's `.strict()` filter allowlist rejects unknown fields"
    - "The existing route tests are updated to the new envelope (no `meta.total`) and cursor assertions; no offset param survives publicly"
  artifacts:
    - path: "packages/api/src/routers/public-api/contractor.ts"
      provides: "contractor.list migrated to cursor + filter/sort; getById unchanged shape; read scope preserved"
      contains: "cursorClause"
    - path: "apps/public-api/src/routes/contractors.ts"
      provides: "createRoute list + getById with .strict() query DTO + {data,meta:{nextCursor,hasMore}} envelope"
      contains: "createRoute"
  key_links:
    - from: "packages/api/src/routers/public-api/contractor.ts"
      to: "packages/api/src/lib/pagination.ts + apps/public-api/src/lib/openapi-cursor.ts"
      via: "decodeCursor(input.cursor) → cursorClause → paginateByLastKeptUndefined → encodeCursor(nextCursor)"
      pattern: "paginateByLastKeptUndefined"
---

<objective>
Migrate the 5 shipped read routes to the new single convention: `createRoute` (so they enter the derived 3.1 spec), opaque-cursor pagination, `filter[field]`/`sort` query grammar, and the `{data,meta:{nextCursor,hasMore}}` envelope. This is D-03's "migrate the existing reads too — no offset/cursor split" work. It runs AFTER 98-06 (the host is `OpenAPIHono`) and is sequenced separately from the net-new reads (98-08) to avoid concurrent edits to `publicApiRouter/index.ts` + `app.ts` route mounts.

Output: 5 Hono route modules on `createRoute`, 4 tRPC read routers on cursor (feature-flags read is a non-paginated evaluation — migrate to createRoute for spec presence only), the offset DTOs replaced, and the route tests updated to the cursor envelope.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-RESEARCH.md
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-SPIKE-FINDINGS.md

<interfaces>
Read pattern to preserve: each list procedure is `apiKeyTenantProcedure.use(requirePermission({<entity>:['read']})).input(<listDto>).query(...)` with `where: { organizationId: ctx.organizationId, deletedAt: null }` + explicit `select`. Swap offset (`skip/take/count`, return `{items,total,page,pageSize}`) for cursor: `const rows = await ctx.db.X.findMany({ where, orderBy, select, ...cursorClause({cursor: decodeCursor(input.cursor), limit: input.limit}) }); const { items, nextCursor } = paginateByLastKeptUndefined(rows, {cursor: decodeCursor(input.cursor), limit: input.limit}); return { items, nextCursor: nextCursor ? encodeCursor(nextCursor) : undefined }`. Hono handler wraps to `{ data: items, meta: { nextCursor: nextCursor ?? null, hasMore: nextCursor != null } }`.
DTO location per 98-SPIKE-FINDINGS VERDICT A/B. Replace `publicApiContractorListInputSchema` etc. (offset) with cursor+filter/sort `.strict()` versions extending `publicListBaseSchema` (98-05); update BOTH the tRPC `.input()` and the Hono `createRoute.request.query`.
feature-flags read (routes/feature-flags.ts + routers/public-api/feature-flags.ts) is an evaluation lookup, not a paginated entity — migrate to `createRoute` for spec presence but keep its response shape (no cursor). Its scope stays as-is.
Existing route tests: apps/public-api/src/routes/__tests__/{contractors,invoices,contracts,documents}.test.ts assert the old `meta.total` envelope — update to `{nextCursor,hasMore}` + a cursor round-trip case (RESEARCH Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate contractor + invoice + contract + document tRPC read routers to cursor</name>
  <files>packages/api/src/routers/public-api/contractor.ts, packages/api/src/routers/public-api/invoice.ts, packages/api/src/routers/public-api/contract.ts, packages/api/src/routers/public-api/document.ts, packages/validators/src/public-api/index.ts</files>
  <read_first>
    - packages/api/src/routers/public-api/contractor.ts (the offset list → cursor target; keep select + read scope + tenant/deletedAt filter)
    - packages/api/src/lib/pagination.ts:64-105 (cursorClause + paginateByLastKeptUndefined) + apps/public-api/src/lib/openapi-cursor.ts (encode/decode from 98-05)
    - packages/validators/src/public-api/index.ts:39-72 (the offset list schemas to replace with cursor+filter/sort) + publicListBaseSchema (98-05)
    - 98-RESEARCH.md § Code Examples (cursor + filter/sort DTO) + Pitfall 5 (drop total)
  </read_first>
  <action>
    For contractor/invoice/contract/document `.list`: replace the offset input schema with a `.strict()` cursor+filter/sort DTO extending `publicListBaseSchema` — each entity's `filter` allowlist maps its former query params (contractor: status, lifecycleStage; invoice: status, contractorId; contract: status, contractorId; document: entityType, entityId) and `sort` is the leading-`-`=desc enum over the former `sortBy` fields. Rewrite the query body to cursor pagination (decodeCursor → cursorClause → paginateByLastKeptUndefined → encodeCursor), returning `{ items, nextCursor }` (drop `total/page/pageSize`). Keep `requirePermission({<entity>:['read']})`, the explicit `select`, and `where:{organizationId, deletedAt:null}` untouched. `getById` procedures are unchanged. Update the validators file: remove/replace the offset schemas with the cursor versions (grep for downstream importers first — only the public-api routers + Hono routes should import them).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test public-api 2>&1 | tail -12; pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/validators 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - 4 list procedures use cursor (no `skip`/`count`/`total`); return `{items,nextCursor}`.
    - Each cursor+filter/sort DTO is `.strict()`; filter allowlist matches the former params; read scope + tenant/deletedAt filter preserved.
    - `pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/validators` clean; the public-api router unit tests updated + green.
  </acceptance_criteria>
  <done>4 read routers on cursor + filter/sort; offset DTOs replaced.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the 5 Hono routes to createRoute + envelope; update route tests</name>
  <files>apps/public-api/src/routes/contractors.ts, apps/public-api/src/routes/invoices.ts, apps/public-api/src/routes/contracts.ts, apps/public-api/src/routes/documents.ts, apps/public-api/src/routes/feature-flags.ts, apps/public-api/src/routes/__tests__/contractors.test.ts, apps/public-api/src/routes/__tests__/invoices.test.ts, apps/public-api/src/routes/__tests__/contracts.test.ts, apps/public-api/src/routes/__tests__/documents.test.ts</files>
  <read_first>
    - apps/public-api/src/routes/contractors.ts (the plain-Hono route to convert; keep createPublicCaller delegation)
    - 98-RESEARCH.md § Pattern 1 (createRoute + app.openapi(route,handler) + c.req.valid('query')) + Anti-Patterns (every spec route must be an OpenAPIHono instance)
    - apps/public-api/src/lib/parse-list-query.ts (parseBracketedQuery from 98-05 for `filter[...]`) 
    - the existing route tests (the `meta.total` envelope to replace)
  </read_first>
  <action>
    Convert each route module to an `OpenAPIHono` instance using `createRoute({ method:'get', path:'/', request:{ query:<cursor DTO> }, responses:{ 200:{...envelope schema...}, 401, 403 } })` + `app.openapi(route, handler)`. In the handler, parse `filter[...]` via `parseBracketedQuery(c.req.query())` then hand the validated input to `createPublicCaller(c)`; wrap the caller result as `{ data: items, meta: { nextCursor: nextCursor ?? null, hasMore: nextCursor != null } }`. Keep the `getById` routes (convert to createRoute with a path param). Migrate feature-flags to createRoute (spec presence; keep its non-cursor response). Update the 4 route tests to assert the `{data,meta:{nextCursor,hasMore}}` envelope + a cursor round-trip (page 1 nextCursor → page 2), and REMOVE any `meta.total` assertion. Ensure the openapi-doc test now sees these 5 as spec paths.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/public-api test routes openapi-doc 2>&1 | tail -14</automated>
  </verify>
  <acceptance_criteria>
    - All 5 route modules are `OpenAPIHono` `createRoute` instances (grep: `createRoute` present in each); they appear in the derived spec.
    - Handlers parse `filter[...]` before validation and emit the `{data,meta:{nextCursor,hasMore}}` envelope; no `meta.total` remains.
    - The 4 route tests assert the cursor envelope + round-trip and are green; `openapi-doc.test.ts` lists the 5 read paths.
  </acceptance_criteria>
  <done>5 Hono reads on createRoute + cursor envelope; route tests updated; reads present in the 3.1 spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client query → read procedure | Cursor + filter + sort untrusted; org-scoped keyset + `.strict()` allowlist bound it |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-07-01 | Information Disclosure | Cross-tenant read via cursor/filter | mitigate | `where:{organizationId, deletedAt:null}` unchanged; keyset scoped by org; cursor opaque |
| T-98-07-02 | Denial of Service | Deep offset scans on unbounded tables | mitigate | Offset removed; cursor + limit≤100; no COUNT/total |
| T-98-07-SC | Tampering | npm installs | accept | No new deps |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test public-api` + `pnpm --filter @contractor-ops/public-api test routes openapi-doc` green.
- `pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/validators --filter @contractor-ops/public-api` clean.
- Grep: no `page`/`pageSize`/`skip`/`meta.total` in the public read path.
</verification>

<success_criteria>
INTEG-API-04 for the existing surface: the 5 shipped reads are on cursor pagination + `filter[field]`/`sort`, `createRoute`-derived into the 3.1 spec, with the single `{data,meta:{nextCursor,hasMore}}` envelope. No offset/cursor split remains.
</success_criteria>

<output>
Create `.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-07-SUMMARY.md` when done.
</output>
