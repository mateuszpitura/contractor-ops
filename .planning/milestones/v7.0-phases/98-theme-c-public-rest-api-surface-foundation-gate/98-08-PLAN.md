---
phase: 98-theme-c-public-rest-api-surface-foundation-gate
plan: 08
type: execute
wave: 4
depends_on: ["98-03", "98-04", "98-07"]
files_modified:
  - packages/api/src/routers/public-api/payment.ts
  - packages/api/src/routers/public-api/payment-run.ts
  - packages/api/src/routers/public-api/workflow.ts
  - packages/api/src/routers/public-api/workflow-task.ts
  - packages/api/src/routers/public-api/classification.ts
  - packages/api/src/routers/public-api/compliance-document.ts
  - packages/api/src/routers/public-api/audit.ts
  - packages/api/src/routers/public-api/index.ts
  - packages/validators/src/public-api/index.ts
  - apps/public-api/src/routes/payments.ts
  - apps/public-api/src/routes/payment-runs.ts
  - apps/public-api/src/routes/workflows.ts
  - apps/public-api/src/routes/workflow-tasks.ts
  - apps/public-api/src/routes/classifications.ts
  - apps/public-api/src/routes/compliance-documents.ts
  - apps/public-api/src/routes/audit-log.ts
  - apps/public-api/src/app.ts
  - packages/api/src/routers/public-api/__tests__/tenant-isolation.security.test.ts
autonomous: true
requirements: [INTEG-API-01, INTEG-API-04]

must_haves:
  truths:
    - "Seven net-new READ surfaces exist — payments, payment_runs, workflows, workflow_tasks, classifications, compliance_documents, audit_log — each a cursor-paginated tRPC `.query` under `apiKeyTenantProcedure` + `requirePermission({<resource>:['read']})`, mounted as an `OpenAPIHono` `createRoute`"
    - "Read scopes: payment:read (payments + payment_runs), workflow:read (workflows + workflow_tasks), classification:read, document:read (compliance_documents), auditLog:read (audit_log)"
    - "audit_log read mirrors the internal `core/audit.ts` cursor pattern (opaque cursor, `total:null`); every query filters `organizationId: ctx.organizationId` (+ `deletedAt:null` where the model soft-deletes)"
    - "The public-api tRPC router index composes the 7 new sub-routers; the Hono host mounts the 7 new routes; all appear in the derived 3.1 spec as reads"
    - "The Wave-0 tenant-isolation.security.test cross-org assertions for the new model families turn GREEN"
  artifacts:
    - path: "packages/api/src/routers/public-api/audit.ts"
      provides: "audit_log cursor read mirroring core/audit.ts (total:null); auditLog:read scope"
      contains: "cursorClause"
    - path: "packages/api/src/routers/public-api/index.ts"
      provides: "publicApiRouter composed with payment/paymentRun/workflow/workflowTask/classification/complianceDocument/audit sub-routers"
      contains: "audit"
  key_links:
    - from: "packages/api/src/routers/public-api/audit.ts"
      to: "packages/api/src/routers/core/audit.ts:153"
      via: "same cursorClause + paginateByLastKeptUndefined + total:null keyset pattern, re-scoped to the API key's org"
      pattern: "paginateByLastKeptUndefined"
---

<objective>
Add the read side of the 4 net-new entities plus the 3 read-only surfaces so all 9 entities are readable over the public API: payments, payment_runs, workflows, workflow_tasks, classifications, compliance_documents, audit_log. Each is a cursor-paginated tRPC read under the flag-gated, scope-checked `apiKeyTenantProcedure`, delegated to by a `createRoute` Hono route. This completes the READ surface; writes (7 entities) follow in 98-09/98-10. Sequenced after 98-07 to serialize edits to `publicApiRouter/index.ts` + `app.ts`.

Output: 7 new tRPC read sub-routers + 7 new Hono routes + validators DTOs + index/host wiring + the tenant-isolation test GREEN for the new families.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-RESEARCH.md
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-SPIKE-FINDINGS.md

<interfaces>
Copy the 98-07 cursor read shape verbatim: `apiKeyTenantProcedure.use(requirePermission({<resource>:['read']})).input(<cursor DTO>).query(...)` → `where:{organizationId: ctx.organizationId, deletedAt:null}` (+ entity filter allowlist) → cursorClause/paginateByLastKeptUndefined/encodeCursor → `{items,nextCursor}`; Hono `createRoute` wraps to `{data,meta:{nextCursor,hasMore}}`.
Resource→scope mapping (98-03): payments + payment_runs → `payment:read`; workflows + workflow_tasks → `workflow:read`; classifications → `classification:read`; compliance_documents → `document:read`; audit_log → `auditLog:read`.
audit_log: mirror packages/api/src/routers/core/audit.ts:153-165 (cursorClause + paginateByLastKeptUndefined + `total:null`); re-scope to `ctx.organizationId`. Do NOT expose the COUNT_CAP total publicly (cursor mode). Choose a conservative, PII-aware `select` for the audit read (id, action, actorType, resourceType, resourceId, createdAt, metadata as appropriate) — never leak another tenant's actor identity.
Prisma model names + soft-delete: confirm via semble which of PaymentRun/WorkflowTask/Classification/document models have `deletedAt` before adding the filter (some logs are append-only, not soft-deleted — audit_log has no deletedAt).
Mount order: add sub-routers to publicApiRouter/index.ts and `.route(...)` mounts to app.ts; keep them reads only (no write procedures here).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: payment + payment-run + workflow + workflow-task read sub-routers + Hono routes</name>
  <files>packages/api/src/routers/public-api/payment.ts, packages/api/src/routers/public-api/payment-run.ts, packages/api/src/routers/public-api/workflow.ts, packages/api/src/routers/public-api/workflow-task.ts, packages/validators/src/public-api/index.ts, apps/public-api/src/routes/payments.ts, apps/public-api/src/routes/payment-runs.ts, apps/public-api/src/routes/workflows.ts, apps/public-api/src/routes/workflow-tasks.ts</files>
  <read_first>
    - packages/api/src/routers/public-api/contractor.ts (post-98-07 cursor read template to copy)
    - semble search: the Payment / PaymentRun / WorkflowRun / WorkflowTask Prisma models (fields, deletedAt presence, org scoping) for the `select` + filter allowlist
    - apps/public-api/src/routes/contractors.ts (post-98-07 createRoute template) + parse-list-query.ts (parseBracketedQuery)
    - 98-03-SUMMARY (the read scope strings: payment:read, workflow:read)
  </read_first>
  <action>
    Create 4 tRPC read sub-routers (`list` + `getById`) copying the 98-07 cursor template, each with its `requirePermission({payment:['read']})` or `{workflow:['read']}` scope, an org+deletedAt (where applicable) filter, a conservative `select`, and a `.strict()` cursor+filter/sort DTO (payments: status, contractorId; payment_runs: status; workflows: status; workflow_tasks: status/workflowRunId — confirm real fields via semble). Add the DTOs to validators (or app layer per VERDICT). Create the 4 Hono `createRoute` route modules delegating to the callers with the `{data,meta}` envelope. Do NOT add any write/mutation here. No `console.*`.
  </action>
  <verify>
    <automated>pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/validators --filter @contractor-ops/public-api 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 4 read sub-routers exist with the correct read scope + org-scoped keyset + cursor; 4 Hono createRoute modules delegate with the envelope.
    - `select`/filter fields are grounded in the real Prisma models (semble-verified), no money/PII over-exposure.
    - Typecheck clean across the three packages.
  </acceptance_criteria>
  <done>payments/payment_runs/workflows/workflow_tasks reads built (tRPC + Hono).</done>
</task>

<task type="auto">
  <name>Task 2: classification + compliance-document + audit-log reads; wire index + host; tenant-isolation GREEN</name>
  <files>packages/api/src/routers/public-api/classification.ts, packages/api/src/routers/public-api/compliance-document.ts, packages/api/src/routers/public-api/audit.ts, packages/api/src/routers/public-api/index.ts, apps/public-api/src/routes/classifications.ts, apps/public-api/src/routes/compliance-documents.ts, apps/public-api/src/routes/audit-log.ts, apps/public-api/src/app.ts, packages/api/src/routers/public-api/__tests__/tenant-isolation.security.test.ts</files>
  <read_first>
    - packages/api/src/routers/core/audit.ts:103-181 (the cursor + total:null pattern to mirror publicly; the AuditLog select + org scoping)
    - semble search: Classification model + the compliance/personnel document model (doc-link-service) for the read select/filter
    - packages/api/src/routers/public-api/index.ts (compose the 7 new sub-routers) + apps/public-api/src/app.ts (add 7 `.route(...)` mounts)
    - the Wave-0 tenant-isolation.security.test (98-02) — the cross-org assertions to turn GREEN for the new families
    - MEMORY: project_gtm_classification_liability (classification is READ-only here; no verdict write surface)
  </read_first>
  <action>
    Create classification.list/getById (`requirePermission({classification:['read']})`), compliance-document.list/getById (`requirePermission({document:['read']})`), and audit.list (`requirePermission({auditLog:['read']})`) mirroring core/audit.ts cursor + `total:null` + a PII-aware `select`. Compose all 7 new sub-routers into `publicApiRouter` (index.ts) as `payment`, `paymentRun`, `workflow`, `workflowTask`, `classification`, `complianceDocument`, `audit`. Add the 7 `.route('/payments', ...)` … `.route('/audit-log', ...)` mounts to app.ts. classification is READ-only (no write surface — aligns INTEG-AUTH-02 + GTM posture). Turn the tenant-isolation.security.test cross-org cases GREEN for payment/paymentRun/workflow/workflowTask/classification/document/auditLog (rowB under an ORG_A key → null / injected org-A where).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test public-api tenant-isolation 2>&1 | tail -14; pnpm --filter @contractor-ops/public-api test openapi-doc 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - 3 more read sub-routers + routes exist; `publicApiRouter` composes all 7; app.ts mounts all 7; the 9 entities are all readable + present in the derived spec.
    - audit_log read mirrors core/audit (cursor + total:null) and is org-scoped with a PII-aware select.
    - `tenant-isolation.security.test` GREEN for every new model family; classification exposes NO write procedure.
  </acceptance_criteria>
  <done>classifications/compliance_documents/audit_log reads built + wired; cross-org isolation GREEN; full read surface (9 entities) live in the spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API key → net-new reads | Each new read is a fresh IDOR/BOLA surface; org-scoped keyset + read scope + flag gate bound it |
| audit_log read → response | Audit rows may carry actor identity + metadata; select must not leak cross-tenant or over-expose |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-08-01 | Information Disclosure | Cross-tenant read of payment/workflow/audit rows | mitigate | `where:{organizationId: ctx.organizationId}` on every query; tenant-isolation test GREEN for all new families |
| T-98-08-02 | Information Disclosure | audit_log over-exposure (actor PII, other-tenant refs) | mitigate | PII-aware `select`; org-scoped; no COUNT/total; mirror core/audit read shape |
| T-98-08-03 | Elevation of Privilege | Classification write leaking in as a "read" surface | mitigate | classification is READ-only; no mutation procedure defined (write deferred per INTEG-AUTH-02 + GTM) |
| T-98-08-SC | Tampering | npm installs | accept | No new deps |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test public-api tenant-isolation` green; `pnpm --filter @contractor-ops/public-api test openapi-doc` shows all 9 reads.
- `pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/validators --filter @contractor-ops/public-api` clean.
</verification>

<success_criteria>
All 9 entities are readable over the versioned, cursor-paginated, scope-checked, flag-gated public API and present in the derived 3.1 spec. Cross-tenant isolation is proven for every new model family; classification + audit_log are read-only.
</success_criteria>

<output>
Create `.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-08-SUMMARY.md` when done.
</output>
