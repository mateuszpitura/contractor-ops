---
phase: 98-theme-c-public-rest-api-surface-foundation-gate
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/public-api/src/__tests__/strict-dto.test.ts
  - apps/public-api/src/__tests__/openapi-doc.test.ts
  - apps/public-api/src/__tests__/cursor-filter.test.ts
  - apps/public-api/src/__tests__/version-headers.test.ts
  - packages/api/src/__tests__/security/public-api-flag.security.test.ts
  - packages/api/src/__tests__/security/public-api-write-scope.security.test.ts
  - packages/api/src/routers/public-api/__tests__/tenant-isolation.security.test.ts
autonomous: true
requirements: [INTEG-API-01, INTEG-API-02, INTEG-API-03, INTEG-API-04]

must_haves:
  truths:
    - "Every Phase-98 requirement + the D-05 dark gate has at least one RED (terminal Cannot-find-module) or describe.skip HOLD test registered BEFORE any implementation lands (Nyquist: nyquist_validation=true)"
    - "The BFLA contract is RED-first: a correctly-authenticated Enterprise key WITHOUT a write scope must be asserted to 403 on every write procedure (public-api-write-scope.security.test.ts) — plan 98-09 turns it GREEN"
    - "The D-05 double-dark contract is RED-first: module.public-api OFF → 404 for reads AND writes (public-api-flag.security.test.ts) — plan 98-04 turns it GREEN"
    - "Scaffolds are terminal-RED via missing-module import; they never brick tsc (public-api / api tsconfig already excludes __tests__)"
  artifacts:
    - path: "packages/api/src/__tests__/security/public-api-write-scope.security.test.ts"
      provides: "BFLA RED contract — every write procedure 403s an authenticated key that lacks the mandatory scope (INTEG-API-01 / D-02)"
    - path: "packages/api/src/__tests__/security/public-api-flag.security.test.ts"
      provides: "D-05 RED contract — per-org module.public-api OFF returns 404 for reads AND writes"
    - path: "apps/public-api/src/__tests__/strict-dto.test.ts"
      provides: "Mass-assignment RED contract — .strict() write DTOs reject organizationId / workerType / money keys"
    - path: "apps/public-api/src/__tests__/openapi-doc.test.ts"
      provides: "INTEG-API-02 RED contract — getOpenAPI31Document emits 3.1 with all reads; writes ABSENT (hide:true)"
    - path: "apps/public-api/src/__tests__/cursor-filter.test.ts"
      provides: "INTEG-API-04 RED contract — opaque cursor round-trip + tamper→BAD_REQUEST; filter[x]/sort parsed; .strict() rejects unknown filter"
  key_links:
    - from: "packages/api/src/routers/public-api/__tests__/tenant-isolation.security.test.ts"
      to: "packages/api/src/__tests__/security/tenant-isolation-extra.security.test.ts"
      via: "same tenantStore.run + injected-where + cross-org-null idiom, extended to the net-new public read/write model families"
      pattern: "tenantStore\\.run|organizationId"
---

<objective>
Lay the complete Wave-0 RED regression net from `98-VALIDATION.md` so every INTEG-API-01..05 behavior + the D-05 dark gate has an automated feedback contract BEFORE implementation. Nyquist validation is enabled — the implementation waves (98-03..98-11) turn these RED scaffolds GREEN. This is the double-dark public API's safety harness: the BFLA (write-without-scope → 403) and the flag-off (module disabled → 404) contracts are asserted RED here so they can never silently regress to a live hole.

Output: 6 net-new test files + 1 tenant-isolation extension, all RED (terminal Cannot-find-module) or `describe.skip` HOLD.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-VALIDATION.md
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-RESEARCH.md

<interfaces>
Test framework: Vitest 4.x, per-package config, Hono fetch-harness (no supertest). NEVER run the unscoped web-vite suite (RAM). Existing seams to mirror:
  - apps/public-api/src/__tests__/app.test.ts (real app assembly; createPublicCaller stubbed via vi.hoisted)
  - apps/public-api/src/__tests__/rate-limit.security.test.ts (*.security.test.ts regression pattern at the Hono boundary)
  - packages/api/src/__tests__/security/tenant-isolation-extra.security.test.ts (cross-org null + injected-where idiom)
Terminal-RED is the accepted Wave-0 state: import a not-yet-existing module/symbol → Cannot-find-module. Do NOT stub the missing modules.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hono-boundary RED scaffolds (strict-dto, openapi-doc, cursor-filter, version-headers)</name>
  <files>apps/public-api/src/__tests__/strict-dto.test.ts, apps/public-api/src/__tests__/openapi-doc.test.ts, apps/public-api/src/__tests__/cursor-filter.test.ts, apps/public-api/src/__tests__/version-headers.test.ts</files>
  <read_first>
    - apps/public-api/src/__tests__/app.test.ts (assembly + stub idiom)
    - 98-RESEARCH.md § Code Examples (opaque cursor encode/decode; parseBracketedQuery; versionHeaders middleware — the exact shapes these tests assert)
    - 98-VALIDATION.md § Per-Task Verification Map (the behavior each file must encode)
  </read_first>
  <action>
    Create 4 RED unit scaffolds importing not-yet-existing modules:
    - `strict-dto.test.ts`: imports the write DTOs (e.g. from `@contractor-ops/validators/public-api` — a create/update schema not yet added) and asserts `.strict()` REJECTS a body carrying `organizationId`, `workerType`, and money/minor keys (e.g. `totalMinor`), and ACCEPTS a clean body. Terminal-RED on the missing write-DTO export.
    - `openapi-doc.test.ts`: imports the app (`../app`) and asserts `app.getOpenAPI31Document(...)` returns `openapi` starting `3.1`, contains the read paths (`/contractors` etc.), and DOES NOT contain any write path (writes are `hide:true`). RED until 98-06 rewrites app to OpenAPIHono.
    - `cursor-filter.test.ts`: imports `encodeCursor`/`decodeCursor` from the not-yet-existing `../lib/openapi-cursor` and `parseBracketedQuery` from the `../lib/parse-list-query` extension; asserts round-trip `decode(encode(id))===id`, a tampered token → BAD_REQUEST, `filter[status]=X` parses to `{filter:{status:'X'}}`, `sort=-createdAt` passes through, and a `.strict()` list schema rejects an unknown `filter[bogus]`. Terminal-RED on the missing lib.
    - `version-headers.test.ts`: imports `versionHeaders` from the not-yet-existing `../lib/version-headers`; asserts NO `Sunset` header is emitted for current v1 (policy has no sunset) and that when a policy sets `sunset`/`deprecation` it emits RFC 8594 `Sunset` + `Link; rel="sunset"`. Terminal-RED on the missing middleware.
    No `console.*`; no watch flags (scoped `vitest run` only).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/public-api test strict-dto openapi-doc cursor-filter version-headers 2>&1 | grep -qiE "Cannot find module|FAIL|openapi-cursor|version-headers" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist and are collected by the scoped public-api run (visible, not skipped-by-typo).
    - Each fails RED on a missing module/symbol against its future target (98-05/98-06/98-10 turn them GREEN).
    - `openapi-doc.test.ts` encodes BOTH "reads present" and "writes absent" assertions.
  </acceptance_criteria>
  <done>4 Hono-boundary RED scaffolds registered.</done>
</task>

<task type="auto">
  <name>Task 2: tRPC security RED scaffolds (write-scope BFLA, flag dark gate, tenant isolation)</name>
  <files>packages/api/src/__tests__/security/public-api-write-scope.security.test.ts, packages/api/src/__tests__/security/public-api-flag.security.test.ts, packages/api/src/routers/public-api/__tests__/tenant-isolation.security.test.ts</files>
  <read_first>
    - packages/api/src/__tests__/security/tenant-isolation-extra.security.test.ts (the cross-org null + injected-where idiom to mirror)
    - packages/api/src/routers/public-api/__tests__/contractor.test.ts (how public-api procedures are called with an apiKey ctx in tests — the harness to reuse)
    - packages/api/src/middleware/rbac.ts:19-42 (apiKey-mode scope check the write-scope test exercises) + packages/api/src/lib/scope-utils.ts:33-38 (PUBLIC_API_SCOPES)
    - 98-RESEARCH.md § Security Domain (STRIDE: BFLA / dark-surface-leakage / cross-tenant IDOR)
  </read_first>
  <action>
    Create 3 RED tRPC scaffolds:
    - `public-api-write-scope.security.test.ts`: for EACH intended write procedure (contractor.create/update, invoice.create/update, payment.create/update/export, paymentRun.create/transition, workflow.create/execute, workflowTask.transition, complianceDocument.create/link — the 98-09 surface), assert that a key whose `apiKeyScopes` OMITS the required scope throws FORBIDDEN, and a key WITH the scope passes the gate. Drive it from a data-table of `{procedurePath, requiredScope}` so adding a write later without a scope entry FAILS the test (the BFLA tripwire). Terminal-RED: the write procedures do not exist yet — import failure is the accepted RED state; structure the table so 98-09 fills it GREEN.
    - `public-api-flag.security.test.ts`: assert that with `module.public-api` evaluated OFF for the caller's org, a public read (contractor.list) AND a public write both throw NOT_FOUND (404 dark), and with the flag ON they pass the gate. Terminal-RED on the missing `assertPublicApiEnabled` / flag-gated procedure (98-04).
    - `tenant-isolation.security.test.ts` (under routers/public-api/__tests__): extend the existing tenant-isolation idiom to the net-new public read/write model families (payment, paymentRun, workflow, workflowTask, classification, auditLog, document/compliance) — seed rowA(ORG_A)+rowB(ORG_B), read/mutate rowB under an ORG_A key, assert null / P2025 and that the injected `where` carries `organizationId: ORG_A`. Terminal-RED on the missing net-new procedures.
    Use `describe.skip('… HOLD-until-<plan>')` only where a scaffold would otherwise brick collection; prefer terminal Cannot-find-module. No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test public-api-write-scope public-api-flag tenant-isolation 2>&1 | grep -qiE "Cannot find module|FORBIDDEN|NOT_FOUND|FAIL|skip" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist and are collected by the scoped api run.
    - The write-scope test is table-driven so a future unscoped write is a hard FAIL (BFLA tripwire), and encodes the with-scope-passes / without-scope-403 pair.
    - The flag test encodes read-404 AND write-404 when off (double-dark).
    - The tenant-isolation extension structurally mirrors `tenant-isolation-extra.security.test.ts`.
  </acceptance_criteria>
  <done>3 tRPC security RED scaffolds registered; BFLA + dark-gate + cross-org contracts pinned before implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness → future public procedures | Scaffolds must register the BFLA + dark-gate contracts RED so a live hole cannot ship silently |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-02-01 | Elevation of Privilege | Write endpoint reachable without a scope | mitigate | Table-driven write-scope test FAILS if any write lacks a scope entry (turned GREEN by 98-09) |
| T-98-02-02 | Information Disclosure | Public surface reachable when module dark | mitigate | flag test asserts read+write 404 when module.public-api off (GREEN by 98-04) |
| T-98-02-03 | Tampering | RED scaffold silently green (false pass) | mitigate | Each asserts a concrete vector or Cannot-find-module; verify greps the RED/skip signal |
| T-98-02-SC | Tampering | npm installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/public-api test` COLLECTS the 4 boundary scaffolds (RED).
- `pnpm --filter @contractor-ops/api test public-api` COLLECTS the 3 tRPC security scaffolds (RED/skip).
- `pnpm typecheck --filter @contractor-ops/public-api --filter @contractor-ops/api` is NOT bricked (tsconfig excludes __tests__).
</verification>

<success_criteria>
All 7 test files exist, are collected, and are RED or HOLD-skipped; the BFLA, dark-gate, mass-assignment, cursor, versioning, and 3.1-doc contracts are pinned before any implementation. No tsc regression.
</success_criteria>

<output>
Create `.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-02-SUMMARY.md` when done.
</output>
