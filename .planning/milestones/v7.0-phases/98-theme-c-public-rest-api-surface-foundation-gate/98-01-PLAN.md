---
phase: 98-theme-c-public-rest-api-surface-foundation-gate
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/public-api/package.json
  - pnpm-lock.yaml
  - apps/public-api/src/__tests__/openapi-metadata-spike.test.ts
  - .planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-SPIKE-FINDINGS.md
autonomous: false
requirements: [INTEG-API-02]

must_haves:
  truths:
    - "`@hono/zod-openapi` and `@scalar/hono-api-reference` are installed in apps/public-api pinned to exact versions published ≥7 days ago; `pnpm audit` + `pnpm security:scan` pass after the add"
    - "A one-shot supply-chain human-verify checkpoint gates the single `pnpm add` (slopcheck was unavailable at research time — packages tagged [ASSUMED])"
    - "The spike PROVES whether a `.strict()` schema authored in @contractor-ops/validators registers its component name + parameters into `app.getOpenAPI31Document()`; the boolean verdict is recorded in 98-SPIKE-FINDINGS.md and decides the DTO authoring location for every later route plan"
    - "openapi: '3.1.x' is emitted by getOpenAPI31Document (confirms the 3.1 requirement is reachable before any route migration)"
  artifacts:
    - path: "apps/public-api/src/__tests__/openapi-metadata-spike.test.ts"
      provides: "Executable proof that a validators-authored .strict() Zod-4 schema does (or does not) surface into the derived 3.1 document — resolves RESEARCH seam A2 / Pitfall 1"
      contains: "getOpenAPI31Document"
    - path: ".planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-SPIKE-FINDINGS.md"
      provides: "The locked verdict (validators-authored schemas register YES/NO) that plans 98-05..98-10 consume to decide where DTOs live"
---

<objective>
Install the two new public-api dependencies behind a one-time supply-chain checkpoint, and run the load-bearing Wave-0 SPIKE that resolves the single highest-risk unknown in the phase: **do `@contractor-ops/validators` `.strict()` Zod-4 schemas register their component names + query parameters into the `@hono/zod-openapi` OpenAPI 3.1 document?** (RESEARCH seam A2 / Pitfall 1.)

Purpose: every later plan that authors DTOs (98-05 shared DTOs, 98-07 read migration, 98-08 net-new reads, 98-09/10 writes) needs a locked answer to "author route DTOs in `@contractor-ops/validators` with plain `zod`, OR author them in the app layer with the `z` re-exported from `@hono/zod-openapi`". Shipping the whole phase on the wrong assumption would mean re-authoring every DTO. This spike buys that certainty for the cost of one throwaway test.

Output: two pinned deps (audit-clean) + one spike test + a written verdict (`98-SPIKE-FINDINGS.md`).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-CONTEXT.md
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-RESEARCH.md

<interfaces>
Repo is on Zod 4 (`z.iso.datetime()` in packages/validators/src/contract.ts). `@hono/zod-openapi@1.x` peers `zod ^4.0.0` + `hono >=4.10.0` (apps/public-api has hono ^4.12.18). Do NOT install `@hono/zod-openapi@0.x` (Zod 3). Scalar version moves fast — re-pick the newest release ≥7 days old at execution time (research picked 0.11.5; 0.11.6/0.11.7 were <7 days and MUST NOT be pinned).

The `.npmrc` `min-release-age` unit bug breaks bare `npm view` from the repo root — run version checks from /tmp or with `--config-dir` (see MEMORY: project_npmrc_npm_release_age_unit_bug).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Supply-chain verify + pin + install the two deps</name>
  <files>apps/public-api/package.json, pnpm-lock.yaml</files>
  <read_first>
    - 98-RESEARCH.md § Package Legitimacy Audit (both packages tagged [ASSUMED] — slopcheck was unavailable; official Hono/Scalar monorepos, 6-figure weekly downloads, no postinstall)
    - 98-RESEARCH.md § Standard Stack (exact peer ranges + the version-verification commands)
    - CLAUDE.md § Monorepo & dependencies (7-day release age; `pnpm audit` + `pnpm security:scan` after any dep change; typosquat check)
  </read_first>
  <action>
    HUMAN-VERIFY GATE (one-time, per VALIDATION § Manual-Only): before the single `pnpm add`, confirm for BOTH `@hono/zod-openapi` and `@scalar/hono-api-reference`: (a) exact package name + publisher is the official honojs / scalar org (no typosquat), (b) the pinned version was published ≥7 days ago, (c) no install scripts. Re-run the version pick fresh (from /tmp to dodge the .npmrc unit bug):
      `npm view @hono/zod-openapi version time --json`
      `npm view @scalar/hono-api-reference version time --json`
    Pin `@hono/zod-openapi@1.4.0` (or newest ≥7-day 1.x) and `@scalar/hono-api-reference@<newest ≥7-day 0.11.x>` — EXACT versions (no `^` past the floor). Install scoped:
      `pnpm --filter @contractor-ops/public-api add @hono/zod-openapi@<v> @scalar/hono-api-reference@<v>`
    Then `pnpm audit && pnpm security:scan`. Do NOT install Speakeasy as an npm dep (it is a CI-only standalone binary — plan 98-11).
  </action>
  <verify>
    <automated>cd apps/public-api && node -e "const p=require('./package.json');if(!p.dependencies['@hono/zod-openapi']||!p.dependencies['@scalar/hono-api-reference'])process.exit(1);console.log('deps-present')"</automated>
  </verify>
  <acceptance_criteria>
    - Both deps present in apps/public-api/package.json at exact ≥7-day-old versions; lockfile updated.
    - `pnpm audit` + `pnpm security:scan` clean (no new advisories introduced by the add).
    - Speakeasy is NOT added to any package.json.
  </acceptance_criteria>
  <done>Two deps pinned + installed + audit-clean behind the one-time human-verify.</done>
</task>

<task type="auto">
  <name>Task 2: Zod-4 ↔ zod-openapi metadata spike + written verdict</name>
  <files>apps/public-api/src/__tests__/openapi-metadata-spike.test.ts, .planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-SPIKE-FINDINGS.md</files>
  <read_first>
    - 98-RESEARCH.md § Pitfall 1 + Assumptions A2 (the exact seam: plain-validators schemas vs the package's own `z`)
    - 98-RESEARCH.md § Pattern 1 (createRoute + app.getOpenAPI31Document shape)
    - packages/validators/src/public-api/index.ts (publicApiContractorListInputSchema — a real validators schema to feed the spike)
    - github.com/honojs/middleware/tree/main/packages/zod-openapi README (`.openapi()` on the package `z` vs Zod 4 native `.meta()`) via context7 if needed
  </read_first>
  <action>
    Write ONE throwaway test that builds a minimal `OpenAPIHono`, registers a `createRoute` whose `request.query` is a `.strict()` schema authored in `@contractor-ops/validators` (reuse `publicApiContractorListInputSchema` or a tiny new `.strict()` object exported for the spike), calls `app.getOpenAPI31Document({ openapi: '3.1.0', info: { title: 't', version: '1.0.0' } })`, and asserts on the emitted document: (a) `openapi` starts with `3.1`; (b) the route's query parameters appear under `paths[...].get.parameters`; (c) whether a named `components.schemas` entry / `$ref` is produced for the validators schema (this is the crux — assert BOTH branches so the test documents the actual behavior rather than a guess). Run it. Then write `98-SPIKE-FINDINGS.md` recording the boolean verdict:
      - **VERDICT A (registers):** later plans author route DTOs in `@contractor-ops/validators` (plain `zod`) and pass them straight to `createRoute` — validators stays the single DTO source.
      - **VERDICT B (does NOT register):** later plans author the route `request`/`responses` schemas in the app layer using `z` from `@hono/zod-openapi`, importing only enums/primitives from validators (the tRPC `.input()` still uses the validators schema for defense-in-depth).
    Keep the spike test in the tree as executable documentation (it is cheap and guards regressions). No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/public-api test openapi-metadata-spike 2>&1 | grep -qiE "PASS|✓|openapi" && echo SPIKE-RAN</automated>
  </verify>
  <acceptance_criteria>
    - The spike test runs green (it asserts the ACTUAL behavior, whichever branch is true).
    - `98-SPIKE-FINDINGS.md` states VERDICT A or VERDICT B explicitly and names the consequence for DTO authoring location.
    - The document emits `openapi: 3.1.x` (proves the 3.1 target — INTEG-API-02 — is reachable).
  </acceptance_criteria>
  <done>Spike executed; DTO-authoring verdict locked in 98-SPIKE-FINDINGS.md for downstream plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| npm registry → repo | Two new [ASSUMED] deps enter the build; a malicious/typosquatted package would run in the API host |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01-SC | Tampering | `pnpm add @hono/zod-openapi @scalar/hono-api-reference` | mitigate | One-time human-verify (name/publisher/age/no-install-scripts) + `pnpm audit` + `pnpm security:scan`; exact-pin ≥7-day versions; no Speakeasy npm dep |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/public-api test openapi-metadata-spike` green.
- `pnpm audit` + `pnpm security:scan` clean.
- `98-SPIKE-FINDINGS.md` exists with an explicit VERDICT A/B.
</verification>

<success_criteria>
Deps installed audit-clean behind a supply-chain checkpoint; the Zod-4↔zod-openapi metadata seam is resolved to a written verdict that every DTO-authoring plan downstream consumes. INTEG-API-02's 3.1 target proven reachable.
</success_criteria>

<output>
Create `.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-01-SUMMARY.md` when done. Record the spike VERDICT (A/B) in the SUMMARY so the orchestrator can pass it to plans 98-05..98-10.
</output>
