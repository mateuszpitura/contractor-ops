---
phase: 98-theme-c-public-rest-api-surface-foundation-gate
plan: 12
type: execute
wave: 8
depends_on: ["98-07", "98-08", "98-10", "98-11"]
files_modified:
  - .planning/brain/wiki/domains/public-api.md
  - .planning/brain/wiki/structure/api-routers-catalog.md
  - .planning/brain/wiki/structure/packages.md
  - .planning/brain/wiki/structure/key-services.md
  - .planning/brain/wiki/integrations/developer-portal.md
  - .planning/brain/wiki/patterns/feature-flags.md
  - .planning/brain/wiki/patterns/tenant-and-audit.md
  - .planning/brain/wiki/patterns/rate-limit.md
  - .planning/brain/wiki/log.md
  - .planning/brain/wiki/hot.md
  - .planning/MEMORY.md
  - .planning/milestones/v7.0-phases/98-theme-c-public-rest-api-surface-foundation-gate/98-VALIDATION.md
autonomous: false
requirements: [INTEG-API-01, INTEG-API-02, INTEG-API-03, INTEG-API-04, INTEG-API-05]

must_haves:
  truths:
    - "The documentation-follows-code obligation is discharged in the SAME milestone: a public-api domain wiki page + the structure/pattern/integration pages reflect the new read+write surface, the derived OpenAPI spec, cursor pagination, the API_KEY audit actor, the per-org dark gate, and the Speakeasy SDK pipeline"
    - "`.planning/MEMORY.md` records the durable invariants: (1) NO public write endpoint without a mandatory enforced scope (BFLA), (2) external mutations audit as actorType:'API_KEY'+apiKeyId, (3) public lists are opaque-cursor only (no offset/total), (4) the whole public surface is per-org dark via module.public-api, (5) writes are double-dark (flag + hide:true) until Phase 99"
    - "`pnpm check:wiki-brain` passes (no NEW doc drift for the changed source files); the BM25/graph refresh is run per the CLAUDE.md pipeline"
    - "The full scoped public-api + api public-api test suites are green + all Wave-0 security tests (write-scope BFLA, flag dark gate, tenant isolation, strict-dto, cursor, version-headers, openapi-doc, write-routes-dark) green; 98-VALIDATION.md nyquist_compliant flipped true with the sign-off checklist complete"
    - "A cross-phase HANDOFF to Phase 99 is recorded: the granular-on-key vs plural-display scope reconciliation (A1), that write endpoints wait on the flag flip + key-management/scope-picker UI, and that SDK 1.0 promotion is post-99"
  artifacts:
    - path: ".planning/brain/wiki/domains/public-api.md"
      provides: "Public REST API domain page — purpose, flow, entry points, the 9-entity read+write surface, dark-gating, agent mistakes"
      contains: "module.public-api"
    - path: ".planning/MEMORY.md"
      provides: "The 5 durable Phase-98 public-API invariants appended"
      contains: "API_KEY"
---

<objective>
Close the phase: prove the whole read+write+spec+SDK surface hangs together end-to-end, discharge the mandatory documentation-follows-code obligation (wiki + indexes + MEMORY in the same change set), run the doc-drift + brain-refresh pipeline, flip the validation sign-off, and record the Phase-99 handoff (the write flag flip, the scope-taxonomy reconciliation, and SDK 1.0 promotion all live in 99).

Output: the public-api wiki domain + structure/pattern/integration updates, MEMORY invariants, a green full scoped suite, `check:wiki-brain` passing, and `98-VALIDATION.md` signed off.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-CONTEXT.md
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-VALIDATION.md
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-RESEARCH.md

<interfaces>
Doc-follows-code triggers touched by this phase (CLAUDE.md § Documentation follows code): new feature/flow → `wiki/domains/public-api.md`; new tRPC namespace(s) → `wiki/structure/api-routers-catalog.md` (publicApiRouter grew 7 sub-routers); new/moved services reuse → `wiki/structure/key-services.md`; new packages export/dep → `wiki/structure/packages.md` (public-api deps); new integration/portal → `wiki/integrations/developer-portal.md`; flag enforcement → `wiki/patterns/feature-flags.md` (module.public-api per-org gate); audit actor → `wiki/patterns/tenant-and-audit.md` (API_KEY actor); rate-limit note stays (per-tier is Phase 99). Any wiki edit → append `wiki/log.md`, overwrite `wiki/hot.md`, rebuild BM25 + graph, `pnpm check:wiki-brain`.
Wiki = durable facts only (MEMORY: feedback_wiki_no_operational_text) — record the resulting code/domain facts, NOT round status. No source-comment breadcrumbs (that is code; this is .planning + wiki).
Verify the router count claim in CLAUDE.md/wiki against packages/api/src/root.ts + public-root if it changed (publicApiRouter is a separate router; confirm the count statement).
Full scoped suites (NEVER unscoped web-vite): `pnpm --filter @contractor-ops/public-api test` + `pnpm --filter @contractor-ops/api test public-api` + the security tests. `pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/validators --filter @contractor-ops/public-api --filter @contractor-ops/auth`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Public-API wiki domain + structure/pattern/integration pages + MEMORY invariants</name>
  <files>.planning/brain/wiki/domains/public-api.md, .planning/brain/wiki/structure/api-routers-catalog.md, .planning/brain/wiki/structure/packages.md, .planning/brain/wiki/structure/key-services.md, .planning/brain/wiki/integrations/developer-portal.md, .planning/brain/wiki/patterns/feature-flags.md, .planning/brain/wiki/patterns/tenant-and-audit.md, .planning/brain/wiki/patterns/rate-limit.md, .planning/MEMORY.md</files>
  <read_first>
    - .planning/brain/wiki/index.md + hot.md (the wiki structure + the domain/pattern page conventions to match: Purpose, Flow, Entry points, UI surface, Agent mistakes, verify_with frontmatter)
    - the existing wiki/structure/api-routers-catalog.md (how routers are catalogued — add the 7 publicApiRouter sub-routers + the read/write split + dark-gating)
    - .planning/MEMORY.md (append format for durable invariants)
    - the 98-0x SUMMARY files (the as-built facts to record: base path, scopes, flag gate, API_KEY audit, cursor envelope, hide:true, SDK 0.x)
  </read_first>
  <action>
    Author `wiki/domains/public-api.md`: Purpose (versioned external REST for 9 entities), Flow (Hono OpenAPIHono → publicApiRouter tRPC caller → org-scoped Prisma), Entry points (apps/public-api/src/app.ts, packages/api/src/routers/public-api/*), the read (9)/write (7) split + classification/audit read-only, the per-org `module.public-api` dark gate + writes double-dark (flag + hide:true) until Phase 99, cursor pagination + filter/sort + `/v1` + RFC 8594, the API_KEY audit actor, and an Agent-mistakes section (never add a write without a mandatory scope; never reintroduce a hand-written spec; never expose writes in the SDK pre-99). Update: api-routers-catalog (publicApiRouter sub-routers), packages (public-api deps `@hono/zod-openapi`+`@scalar/hono-api-reference`; new SDK packages @contractor-ops/sdk + contractor-ops-sdk 0.x), key-services (the reused invariant helpers under public writes + the API_KEY audit path), integrations/developer-portal (Scalar portal + Speakeasy SDKs), patterns/feature-flags (module.public-api per-org enforcement), patterns/tenant-and-audit (API_KEY actor on external mutations), patterns/rate-limit (flat 100/min; per-tier is Phase 99). Set/refresh `verify_with` frontmatter to the real source paths. Append to `.planning/MEMORY.md` the 5 durable invariants (BFLA scope-mandatory; API_KEY audit actor; opaque-cursor-only public lists; per-org dark gate; writes double-dark until 99). Durable facts only — no round/status text.
  </action>
  <verify>
    <automated>pnpm check:wiki-brain 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `wiki/domains/public-api.md` exists with Purpose/Flow/Entry points/Agent-mistakes + `verify_with` pointing at real source.
    - api-routers-catalog / packages / key-services / integrations / patterns pages reflect the as-built surface; MEMORY has the 5 invariants.
    - `pnpm check:wiki-brain` passes (no NEW drift for the phase's changed source files).
  </acceptance_criteria>
  <done>Documentation-follows-code discharged: public-api domain + structure/pattern/integration pages + MEMORY invariants; wiki-brain green.</done>
</task>

<task type="auto">
  <name>Task 2: Brain refresh pipeline + full scoped suite + validation sign-off + Phase-99 handoff</name>
  <files>.planning/brain/wiki/log.md, .planning/brain/wiki/hot.md, .planning/milestones/v7.0-phases/98-theme-c-public-rest-api-surface-foundation-gate/98-VALIDATION.md</files>
  <read_first>
    - CLAUDE.md § Documentation follows code (the exact refresh pipeline commands: contextual-prefix.py, bm25-index.py build; graphify update)
    - 98-VALIDATION.md § Validation Sign-Off (the checklist + nyquist_compliant frontmatter to flip)
    - the 98-03/98-09 SUMMARYs (the scope-taxonomy reconciliation to hand to Phase 99)
  </read_first>
  <action>
    Run the brain refresh pipeline: append the wiki `log.md` entry, overwrite `hot.md`, rebuild the BM25 index + the graphify graph per the CLAUDE.md commands (graph.json is a gitignored artifact — WARN-only; the husky post-commit also rebuilds it). Run the FULL scoped suites: `pnpm --filter @contractor-ops/public-api test`, `pnpm --filter @contractor-ops/api test public-api`, plus the security tests (write-scope, public-api-flag, tenant-isolation, strict-dto, cursor-filter, version-headers, openapi-doc, write-routes-dark) — all green — and `pnpm typecheck` across the 4 touched packages. Flip `98-VALIDATION.md` `nyquist_compliant: true` + `wave_0_complete: true` and tick the Validation Sign-Off checklist. Record the Phase-99 HANDOFF in the SUMMARY: (a) granular `resource:action` on the key vs plural `entity:read|write` display bundles (A1) — Phase 99 owns the scope-picker taxonomy; (b) the write surface goes live only when Phase 99 flips `module.public-api` per-org + ships key-management/scope UI + per-tier rate limits; (c) SDK 1.0 promotion is post-99; (d) classification-write + payout-initiation were deferred (A7).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/public-api test 2>&1 | tail -6; pnpm --filter @contractor-ops/api test public-api 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Brain refresh run (log.md appended, hot.md overwritten, BM25 rebuilt); `pnpm check:wiki-brain` green.
    - Full scoped public-api + api public-api suites + all Wave-0 security tests green; typecheck clean across public-api/api/validators/auth.
    - `98-VALIDATION.md` has `nyquist_compliant: true` + `wave_0_complete: true` and a completed sign-off checklist.
    - The Phase-99 handoff (scope taxonomy, write flag flip, SDK 1.0 promotion, deferred classification-write/payout) is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Brain refreshed; full suite + security tests green; validation signed off; Phase-99 handoff recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| code → wiki/knowledge base | Docs must track the as-built surface or the next agent hallucinates the API contract/gating |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-12-01 | (integrity) | Stale wiki misleads on the dark-gating / BFLA invariants | mitigate | Domain + pattern pages record the flag gate + mandatory-scope + double-dark facts; MEMORY invariants; check:wiki-brain gate |
| T-98-12-02 | Elevation of Privilege | Phase 99 flips the flag without the scope taxonomy reconciled → BFLA/lockout | mitigate | Handoff records granular-on-key vs plural-display (A1) + the write-live preconditions |
| T-98-12-SC | Tampering | npm installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `pnpm check:wiki-brain` green (no NEW drift for the phase's changed source).
- `pnpm --filter @contractor-ops/public-api test` + `pnpm --filter @contractor-ops/api test public-api` + the security tests green; `pnpm typecheck` across public-api/api/validators/auth clean.
- `98-VALIDATION.md` signed off (`nyquist_compliant: true`).
</verification>

<success_criteria>
The phase is coherent end-to-end and self-documenting: all 5 INTEG-API requirements delivered (reads live; writes built + scope-enforced + double-dark; 3.1 spec + Scalar; /v1 + versioning; cursor + filter/sort; 0.x SDK pipeline), the wiki + MEMORY track the as-built surface, the validation contract is signed off, and Phase 99 has an explicit handoff.
</success_criteria>

<output>
Create `.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-12-SUMMARY.md` when done. Record the Phase-99 handoff (scope taxonomy A1, write flag-flip preconditions, SDK 1.0 promotion, deferred classification-write + payout-initiation A7).
</output>
