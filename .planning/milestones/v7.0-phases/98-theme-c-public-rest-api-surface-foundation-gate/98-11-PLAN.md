---
phase: 98-theme-c-public-rest-api-surface-foundation-gate
plan: 11
type: execute
wave: 7
depends_on: ["98-06", "98-10"]
files_modified:
  - apps/public-api/scripts/build-openapi-snapshot.ts
  - apps/public-api/openapi.snapshot.json
  - apps/public-api/package.json
  - .speakeasy/workflow.yaml
  - .github/workflows/sdk-generate.yml
  - .github/workflows/sdk-publish.yml
  - .env.example
autonomous: false
requirements: [INTEG-API-05]

must_haves:
  truths:
    - "A build-time script (`build-openapi-snapshot.ts`) imports the app and writes `openapi.snapshot.json` from `buildOpenApiDocument(app)` — writes are absent (hide:true) so the snapshot is READ-ONLY; CI diff-checks the committed snapshot so SDK regen is deterministic"
    - "`.speakeasy/workflow.yaml` defines one source (the snapshot) + a TypeScript target (npm `@contractor-ops/sdk`) + a Python target (PyPI `contractor-ops-sdk`), consuming the standalone Speakeasy binary — NOT an npm runtime dependency"
    - "CI generates SDKs via `speakeasy-api/sdk-generation-action` and publishes 0.x PRERELEASE (dark) to npm + PyPI; promotion to 1.0 is deferred to post-Phase-99 (surface-stable)"
    - "Required CI secrets are documented (`SPEAKEASY_API_KEY`, `NPM_TOKEN`, and PyPI OIDC trusted-publishing or `PYPI_TOKEN`); the published SDK contains NO write endpoints (the snapshot has writes hidden)"
  artifacts:
    - path: "apps/public-api/scripts/build-openapi-snapshot.ts"
      provides: "Deterministic spec snapshot emitter (reuses buildOpenApiDocument; writes hidden) for Speakeasy + CI diff-check"
      contains: "buildOpenApiDocument"
    - path: ".speakeasy/workflow.yaml"
      provides: "Speakeasy source(snapshot) + TS(npm) + Python(PyPI) targets at 0.x prerelease"
      contains: "typescript"
    - path: ".github/workflows/sdk-publish.yml"
      provides: "CI publish of 0.x prerelease SDKs to npm + PyPI (dark)"
      contains: "sdk-generation-action"
---

<objective>
Stand up the Speakeasy codegen + publish pipeline (D-04): a deterministic OpenAPI snapshot (writes hidden), a `.speakeasy/workflow.yaml` with TS + Python targets, and GitHub Actions that generate + publish **0.x prerelease** SDKs to npm (`@contractor-ops/sdk`) + PyPI (`contractor-ops-sdk`). Publishing 0.x (not public 1.0) avoids churning early adopters with a breaking 2.0 when Phase 99 finalizes the write surface + scopes. Speakeasy is a standalone binary — no day-zero npm runtime dependency (satisfies the 7-day floor by construction).

Output: snapshot script + committed snapshot + Speakeasy workflow + generate/publish CI + documented secrets. The actual first publish (live registry credentials) is a manual/CI-triggered op per VALIDATION § Manual-Only.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-RESEARCH.md

<interfaces>
Spec builder: apps/public-api/src/lib/build-openapi-doc.ts `buildOpenApiDocument(app)` (98-06) already emits the writes-hidden 3.1 doc. The snapshot script imports the app + that builder and writes `openapi.snapshot.json`. Add an `openapi:snapshot` script to apps/public-api/package.json. CI diff-checks the committed snapshot (regen on PR → fail on drift, mirroring the doc-drift discipline).
Speakeasy (RESEARCH § Speakeasy section + A5): `.speakeasy/workflow.yaml` `workflowVersion: 1.0.0`, `sources.contractor-ops-api.inputs[0].location: ./apps/public-api/openapi.snapshot.json`, `targets.typescript-sdk{target:typescript, source, publish.npm.token:$NPM_TOKEN}`, `targets.python-sdk{target:python, source, publish.pypi...}`. Confirm the EXACT key schema against `speakeasy quickstart` output at execution time (A5 — wrong keys fail CI, not prod). Pin `speakeasy-api/sdk-generation-action@<major>` (research: @v15 — confirm current major, ≥7-day). 0.x prerelease version set via the target's version config.
Existing CI: .github/workflows/{ci.yml, security-scan.yml, integration-smoke.yml}. Add two NEW workflows (generate on PR/direct, publish on tag/dispatch) — do NOT fold into ci.yml. New CI secrets go via GitHub repo settings (manual); document them in .env.example comments + the SUMMARY. Prefer PyPI OIDC trusted publishing over a long-lived PYPI_TOKEN (RESEARCH Open Q#3).
Package names to RESERVE: npm `@contractor-ops/sdk`, PyPI `contractor-ops-sdk` (first publish reserves them; note in SUMMARY).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic OpenAPI snapshot script + committed snapshot + npm script</name>
  <files>apps/public-api/scripts/build-openapi-snapshot.ts, apps/public-api/openapi.snapshot.json, apps/public-api/package.json</files>
  <read_first>
    - apps/public-api/src/lib/build-openapi-doc.ts (buildOpenApiDocument — the single builder to reuse)
    - apps/public-api/src/app.ts (how to import the assembled app without booting the server/Sentry — export the app so the script imports it clean)
    - 98-RESEARCH.md § Open Questions #4 (build-time snapshot vs live boot — recommend the build-time importer) + § Speakeasy section
  </read_first>
  <action>
    Write `build-openapi-snapshot.ts` that imports the assembled `app` + `buildOpenApiDocument`, produces the writes-hidden 3.1 document, and writes it to `apps/public-api/openapi.snapshot.json` with stable key ordering (deterministic diff). Add `"openapi:snapshot": "tsx scripts/build-openapi-snapshot.ts"` to apps/public-api/package.json. Run it once and commit the snapshot. The snapshot MUST contain the 9 read paths and ZERO write operations (writes are `hide:true`). No `console.*` — use `@contractor-ops/logger` or a plain fs write.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/public-api run openapi:snapshot; grep -Eic '"(post|patch|put|delete)"' apps/public-api/openapi.snapshot.json</automated>
  </verify>
  <acceptance_criteria>
    - `openapi.snapshot.json` exists, is committed, and its `grep` for write verbs returns 0 (reads-only; writes hidden).
    - The `openapi:snapshot` script regenerates it deterministically (re-run → no diff).
    - The snapshot reuses `buildOpenApiDocument` (single source of truth with the runtime `/openapi.json`).
  </acceptance_criteria>
  <done>Deterministic writes-hidden spec snapshot script + committed snapshot.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Speakeasy workflow + generate/publish CI (0.x prerelease) + documented secrets</name>
  <files>.speakeasy/workflow.yaml, .github/workflows/sdk-generate.yml, .github/workflows/sdk-publish.yml, .env.example</files>
  <read_first>
    - 98-RESEARCH.md § Speakeasy `.speakeasy/workflow.yaml` example + § Runtime State Inventory (SPEAKEASY_API_KEY / NPM_TOKEN / PYPI_TOKEN|OIDC secrets) + A5 (confirm exact keys via `speakeasy quickstart`)
    - .github/workflows/ci.yml (repo Actions conventions: pnpm setup, Node version, caching to mirror)
    - the committed apps/public-api/openapi.snapshot.json (the source input)
  </read_first>
  <action>
    HUMAN-VERIFY GATE: confirm the exact `.speakeasy/workflow.yaml` key schema against current `speakeasy quickstart`/docs and pin the current `speakeasy-api/sdk-generation-action` major (≥7-day) before wiring CI (A5 — wrong keys fail CI silently). Then author `.speakeasy/workflow.yaml`: one source `contractor-ops-api` → `./apps/public-api/openapi.snapshot.json`; a `typescript-sdk` target (npm `@contractor-ops/sdk`, version 0.x prerelease) and a `python-sdk` target (PyPI `contractor-ops-sdk`, 0.x prerelease). Add two workflows: `sdk-generate.yml` (on PR/push touching the snapshot → `speakeasy run` / generation-action in PR mode + a snapshot-drift check that fails if the committed snapshot differs from a fresh regen) and `sdk-publish.yml` (on tag/dispatch → generate + publish 0.x prerelease to npm + PyPI). Prefer PyPI OIDC trusted publishing over `PYPI_TOKEN`. Document the required repo secrets (`SPEAKEASY_API_KEY`, `NPM_TOKEN`, PyPI OIDC/`PYPI_TOKEN`) in `.env.example` comments + the SUMMARY, and note the npm/PyPI name reservation is done by the first publish. Do NOT add Speakeasy to any package.json (binary only). Do NOT promote to 1.0 (post-Phase-99).
  </action>
  <verify>
    <automated>test -f .speakeasy/workflow.yaml && test -f .github/workflows/sdk-generate.yml && test -f .github/workflows/sdk-publish.yml && grep -l "sdk-generation-action" .github/workflows/sdk-*.yml && grep -qi "typescript" .speakeasy/workflow.yaml && grep -qi "python" .speakeasy/workflow.yaml && echo pipeline-wired</automated>
  </verify>
  <acceptance_criteria>
    - `.speakeasy/workflow.yaml` has one snapshot source + TS(npm @contractor-ops/sdk) + Python(PyPI contractor-ops-sdk) targets at 0.x prerelease.
    - Two CI workflows exist: generate (with snapshot-drift check) + publish (0.x prerelease, dark); the action major is pinned ≥7-day.
    - Secrets documented; Speakeasy is NOT an npm dependency; no 1.0 promotion.
  </acceptance_criteria>
  <done>Speakeasy pipeline + generate/publish CI (0.x prerelease) wired behind the schema human-verify; secrets + name reservation documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| snapshot → SDK/registry | Only the writes-hidden read surface reaches the SDK; drift check keeps it deterministic |
| CI secrets → npm/PyPI publish | Publish credentials (least-privilege; OIDC preferred) gate the registry push |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-11-01 | Information Disclosure | Write endpoints leak into the published SDK | mitigate | SDK generated from the writes-hidden snapshot; snapshot write-verb grep = 0; drift check in CI |
| T-98-11-02 | Spoofing / Tampering | Long-lived PyPI token exfiltration | mitigate | Prefer PyPI OIDC trusted publishing; least-privilege NPM_TOKEN; secrets in repo settings not code |
| T-98-11-03 | Tampering | Premature public 1.0 SDK breaks adopters at Phase 99 | mitigate | Publish 0.x prerelease only; 1.0 promotion deferred post-99 |
| T-98-11-SC | Tampering | Speakeasy action supply chain | mitigate | Pin the action major (≥7-day); standalone binary — no npm runtime dep; human-verify the schema/action before wiring |
</threat_model>

<verification>
- `apps/public-api/openapi.snapshot.json` committed; write-verb grep = 0; `openapi:snapshot` re-run produces no diff.
- `.speakeasy/workflow.yaml` + `sdk-generate.yml` + `sdk-publish.yml` present and well-formed; action major pinned.
- Speakeasy absent from all package.json; secrets documented in `.env.example` + SUMMARY.
</verification>

<success_criteria>
INTEG-API-05: a Speakeasy codegen + publish pipeline exists — TS + Python SDKs generated from the writes-hidden 3.1 snapshot and published as 0.x prerelease (dark) to npm + PyPI, with a deterministic snapshot + drift check and no npm runtime dependency. 1.0 promotion deferred to post-Phase-99.
</success_criteria>

<output>
Create `.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-11-SUMMARY.md` when done. Record the confirmed Speakeasy workflow keys + action major + the required repo secrets + name-reservation status.
</output>
