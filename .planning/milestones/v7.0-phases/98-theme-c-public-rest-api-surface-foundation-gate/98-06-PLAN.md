---
phase: 98-theme-c-public-rest-api-surface-foundation-gate
plan: 06
type: execute
wave: 2
depends_on: ["98-01", "98-05"]
files_modified:
  - apps/public-api/src/app.ts
  - apps/public-api/src/openapi.ts
  - apps/public-api/src/lib/build-openapi-doc.ts
  - render.yaml
  - .env.example
  - apps/public-api/src/__tests__/app-sri.test.ts
  - apps/public-api/src/__tests__/openapi-doc.test.ts
autonomous: true
requirements: [INTEG-API-02, INTEG-API-03]

must_haves:
  truths:
    - "`app` is an `OpenAPIHono` instance; the hand-written `openapi.ts` literal is DELETED; the spec is derived via `app.getOpenAPI31Document(...)` and served at `/openapi.json` (kills the drift the literal caused)"
    - "The interactive docs are served by `@scalar/hono-api-reference` `Scalar({url})` — the CDN `<script>`+SRI HTML, the `SCALAR_SRI_HASH`/`SCALAR_SRI_PLACEHOLDER` boot-throw, and the `ENABLE_API_DOCS` gate are removed"
    - "Base path is `/v1` (INTEG-API-03); `render.yaml healthCheckPath` is updated to `/v1/health`; the doc `servers` + Scalar `url` agree with the base path; a repo-wide grep confirms no first-party caller depends on `/api/v1` (else a redirect is added)"
    - "The `versionHeaders` middleware is mounted in the chain; the existing middleware order (requestId → observability → secureHeaders → cors → rateLimit) is preserved"
    - "The 5 existing routes still SERVE through the migrated host (compatibility) even before 98-07 converts them to `createRoute` — the openapi-doc test asserts 3.1 emission (route bodies land in 98-07/08)"
  artifacts:
    - path: "apps/public-api/src/app.ts"
      provides: "OpenAPIHono host: basePath('/v1'), middleware chain + versionHeaders, doc31 endpoint, Scalar mount"
      contains: "OpenAPIHono"
    - path: "apps/public-api/src/lib/build-openapi-doc.ts"
      provides: "getOpenAPI31Document(...) builder (writes hidden via hide:true) reused by the runtime /openapi.json AND the 98-11 spec-snapshot script"
      contains: "getOpenAPI31Document"
  key_links:
    - from: "apps/public-api/src/app.ts"
      to: "@scalar/hono-api-reference (Scalar)"
      via: "Scalar({url:'/v1/openapi.json'}) replaces the CDN+SRI HTML"
      pattern: "Scalar\\("
---

<objective>
Migrate the Hono host to `OpenAPIHono` so the OpenAPI 3.1 spec is derived from route definitions instead of the drift-prone hand-written `openapi.ts` literal, and replace the CDN+SRI Scalar HTML with the `@scalar/hono-api-reference` dependency. Move the base path to `/v1` (INTEG-API-03) and reconcile the render healthcheck + spec `servers` + Scalar `url`. Mount the `versionHeaders` middleware. This is the transport shell; the per-route `createRoute` conversions land in 98-07 (existing reads) and 98-08 (net-new reads) — this plan keeps the 5 existing routes serving through the migrated host and asserts a valid 3.1 document is emitted.

Output: OpenAPIHono `app.ts` + a shared `build-openapi-doc.ts` builder + Scalar dep mount + `/v1` base + render/env cleanup + the openapi-doc test emitting 3.1.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-RESEARCH.md
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-SPIKE-FINDINGS.md

<interfaces>
Current host: apps/public-api/src/app.ts — `new Hono().basePath('/api/v1')`, middleware chain (:79-106), route mounts (:109-113), health (:116), hand-written spec `openapi.ts` served at :119, Scalar CDN+SRI HTML behind ENABLE_API_DOCS (:127-180). `OpenAPIHono extends Hono` — basePath/use/route/get all inherit; plain `Hono` sub-apps still SERVE when `.route()`-mounted but do NOT contribute to the derived spec (that is fine until 98-07 converts them).
Spec builder: `app.getOpenAPI31Document({ openapi:'3.1.0', info:{title:'Contractor Ops API', version:'1.0.0'}, servers:[{url:'/v1'}] })`. Extract this into `lib/build-openapi-doc.ts` so 98-11's snapshot script reuses the identical builder (writes hidden via `hide:true` on the write routes).
Scalar: `import { Scalar } from '@scalar/hono-api-reference'`; `app.get('/docs', Scalar({ url:'/v1/openapi.json' }))`.
Removals: delete openapi.ts; delete the SCALAR_VERSION/SCALAR_SRI/SCALAR_SRI_PLACEHOLDER/ENABLE_API_DOCS logic + the boot-throw; remove `SCALAR_SRI_HASH`/`ENABLE_API_DOCS` from .env.example (and note the Render dashboard vars are now unused). Update app-sri.test.ts (it tests the removed SRI throw) — delete or repoint to assert the Scalar dep mount + no SRI env.
render.yaml:410 `healthCheckPath: /api/v1/health` → `/v1/health`. Confirm no first-party caller of `/api/v1` (grep apps/ + packages/); the app is local-only (MEMORY) so blast radius is low, but if a caller exists add a `/api/v1/*`→`/v1/*` redirect or keep both.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OpenAPIHono host + shared doc builder + Scalar dep + versionHeaders + /v1 base</name>
  <files>apps/public-api/src/app.ts, apps/public-api/src/openapi.ts, apps/public-api/src/lib/build-openapi-doc.ts, apps/public-api/src/__tests__/app-sri.test.ts, apps/public-api/src/__tests__/openapi-doc.test.ts</files>
  <read_first>
    - apps/public-api/src/app.ts (full — the chain, mounts, Scalar HTML, SRI throw to remove)
    - 98-RESEARCH.md § Pattern 1 + § Scalar mount + Pitfall 4 (basePath vs /v1 vs servers agreement) + Anti-Patterns (two OpenAPI sources; mixing OpenAPIHono + plain Hono)
    - 98-SPIKE-FINDINGS.md (VERDICT — affects how routes will register; the host is verdict-agnostic)
    - apps/public-api/src/__tests__/app.test.ts + app-sri.test.ts (assembly + the SRI test to retire)
  </read_first>
  <action>
    Rewrite `app.ts` to `const app = new OpenAPIHono().basePath('/v1')`. Preserve the middleware chain order (requestId → observability → secureHeaders → cors(allowlist) → rateLimit) and ADD `versionHeaders` (from 98-05) after rateLimit. Keep `.route('/contractors', contractors)` etc. mounting the existing 5 route modules (they still serve; spec contribution comes in 98-07). Extract `build-openapi-doc.ts` exporting `buildOpenApiDocument(app)` = `app.getOpenAPI31Document({ openapi:'3.1.0', info:{title:'Contractor Ops API', version:'1.0.0'}, servers:[{url:'/v1'}] })`; serve it at `app.get('/openapi.json', c => c.json(buildOpenApiDocument(app)))`. DELETE `openapi.ts`. Mount `app.get('/docs', Scalar({ url:'/v1/openapi.json' }))` — remove the SCALAR_VERSION/SRI/placeholder/boot-throw and the ENABLE_API_DOCS gate entirely. Keep `/v1/health`, `onError(handleError)`, and the `notFound` JSON. Retire `app-sri.test.ts` (delete, or repoint to assert `/docs` mounts via the Scalar dep + no SRI env). Turn `openapi-doc.test.ts` GREEN for the 3.1 emission + reads-present assertions it can satisfy now (the writes-absent assertion is finalized in 98-10).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/public-api test app openapi-doc 2>&1 | tail -10; test ! -f apps/public-api/src/openapi.ts && echo openapi-literal-deleted</automated>
  </verify>
  <acceptance_criteria>
    - `app` is `OpenAPIHono`, base `/v1`; `openapi.ts` literal deleted; `/openapi.json` derived via `buildOpenApiDocument`; `/docs` served by the Scalar dep (no CDN/SRI/ENABLE_API_DOCS).
    - `build-openapi-doc.ts` is the single builder (reused by 98-11's snapshot).
    - `versionHeaders` mounted; chain order preserved; `openapi-doc.test.ts` asserts `openapi` starts `3.1`.
    - The old app-sri SRI-throw test is retired (no dangling reference to the removed env).
  </acceptance_criteria>
  <done>OpenAPIHono host + derived 3.1 spec + Scalar dep + /v1 + versionHeaders; hand-written spec + SRI plumbing removed.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile deploy + env for /v1 (render healthcheck, dead env vars, caller grep)</name>
  <files>render.yaml, .env.example</files>
  <read_first>
    - render.yaml:401-410 (public-api service block; healthCheckPath) + the custom-domain routing note (RESEARCH § Runtime State Inventory)
    - .env.example (SCALAR_SRI_HASH + ENABLE_API_DOCS entries to remove)
  </read_first>
  <action>
    Update `render.yaml` `healthCheckPath` from `/api/v1/health` to `/v1/health`. Remove `SCALAR_SRI_HASH` + `ENABLE_API_DOCS` from `.env.example` (now unused) and add a short comment noting the matching Render dashboard vars (`sync:false`) are obsolete and should be dropped there (dashboard change is a manual op, not code). Grep the repo for first-party callers of `/api/v1` (`grep -rn "/api/v1" apps packages --include="*.ts" --include="*.tsx"`); if any first-party caller exists, record it in the SUMMARY and add a `/api/v1/*`→`/v1/*` redirect in app.ts (or keep both bases) rather than breaking it — the app is local-only so the default expectation is zero external callers.
  </action>
  <verify>
    <automated>grep -n "healthCheckPath" render.yaml; grep -rn "/api/v1" apps packages --include="*.ts" --include="*.tsx" | grep -v "__tests__" | head</automated>
  </verify>
  <acceptance_criteria>
    - `render.yaml healthCheckPath` is `/v1/health`.
    - `SCALAR_SRI_HASH` + `ENABLE_API_DOCS` removed from `.env.example` with the Render-dashboard note.
    - Any `/api/v1` first-party caller is either redirected/kept (documented) or confirmed absent in the SUMMARY.
  </acceptance_criteria>
  <done>Deploy + env reconciled to /v1; dead SRI/docs env removed; caller grep resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| browser → /docs script | Docs script provenance (was CDN+SRI, now a vendored npm dep) |
| deploy config → live service | healthCheckPath + base path must agree or the service is marked unhealthy |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-06-01 | Tampering | 3rd-party docs script via CDN + fragile SRI pin | mitigate | Replace CDN load with the `@scalar/hono-api-reference` npm dep (removes SRI-pin boot fragility) |
| T-98-06-02 | Information Disclosure | Two OpenAPI sources drift (literal + derived) | mitigate | DELETE openapi.ts; single derived doc via build-openapi-doc |
| T-98-06-03 | Denial of Service | Base-path/healthcheck mismatch marks service down | mitigate | render healthCheckPath → /v1/health in the same change; servers/Scalar url agree |
| T-98-06-SC | Tampering | npm installs | accept | Deps already added + audited (98-01); none added here |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/public-api test app openapi-doc app-sri` green (app-sri retired/repointed).
- `pnpm typecheck --filter @contractor-ops/public-api` clean.
- `apps/public-api/src/openapi.ts` no longer exists; `/openapi.json` derived; `/docs` via Scalar dep.
</verification>

<success_criteria>
INTEG-API-02: OpenAPI 3.1 auto-derived (no hand-written literal) + Scalar portal via the npm dep. INTEG-API-03: `/v1` base + versionHeaders mounted + render/env reconciled. The 5 existing routes still serve; the derived doc emits 3.1.
</success_criteria>

<output>
Create `.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-06-SUMMARY.md` when done. Record the `/api/v1` caller-grep result (redirect added vs none found).
</output>
