---
phase: 98-theme-c-public-rest-api-surface-foundation-gate
plan: 09
type: execute
wave: 5
depends_on: ["98-03", "98-04", "98-08"]
files_modified:
  - packages/api/src/routers/public-api/contractor.ts
  - packages/api/src/routers/public-api/invoice.ts
  - packages/api/src/routers/public-api/payment.ts
  - packages/api/src/routers/public-api/payment-run.ts
  - packages/api/src/routers/public-api/workflow.ts
  - packages/api/src/routers/public-api/workflow-task.ts
  - packages/api/src/routers/public-api/compliance-document.ts
  - packages/validators/src/public-api/index.ts
  - packages/api/src/__tests__/security/public-api-write-scope.security.test.ts
  - apps/public-api/src/__tests__/strict-dto.test.ts
autonomous: true
requirements: [INTEG-API-01]

must_haves:
  truths:
    - "The 7 write entities gain mutation procedures in `publicApiRouter` under `apiKeyTenantProcedure`, REUSING invariant helpers + plain services (never reimplementing business rules): payment-shared (VALID_TRANSITIONS, autoCompleteRunIfTerminal, loadEligibleInvoices, validateInvoicesForRun, seedRunItems, allocateRunNumber, applyWithholdingToRun, _generateExportFileForFormat), workflow-shared (validateTransition, unblockDependentsAndRecomputeRun), invoice-shared, doc-link-service, payment-export/payment-settlement"
    - "EVERY write procedure carries a MANDATORY `requirePermission({<resource>:['create'|'update'|'execute'|'export']})` from day one — the BFLA hard constraint; the only session-coupling swapped is the audit actor (`ctx.user.id`/'USER' → `ctx.apiKeyId`/'API_KEY')"
    - "Every write DTO is `.strict()` and OMITS `organizationId`, `workerType`, and money/minor fields entirely (mass-assignment blocked); tenant is `ctx.organizationId` from the key, never client input; mutations run in `ctx.db.$transaction` and call `writeAuditLog({actorType:'API_KEY', actorId: ctx.apiKeyId, ...})`"
    - "The whole write surface stays DARK — it is only reachable when `module.public-api` is granted (98-04 gate) which does not happen until Phase 99; no Hono route + no SDK exposure yet (98-10 adds hide:true routes)"
    - "The Wave-0 `public-api-write-scope.security.test.ts` (BFLA matrix) + `strict-dto.test.ts` (mass-assignment) turn GREEN"
  artifacts:
    - path: "packages/api/src/routers/public-api/payment-run.ts"
      provides: "payment_run create/transition/export reusing payment-shared invariants; payment:create/update/export scopes; API_KEY audit"
      contains: "VALID_TRANSITIONS"
    - path: "packages/api/src/routers/public-api/workflow-task.ts"
      provides: "workflow_task transition reusing workflow-shared validateTransition + unblockDependentsAndRecomputeRun; workflow:update/execute scope"
      contains: "validateTransition"
  key_links:
    - from: "packages/api/src/routers/public-api/payment-run.ts"
      to: "packages/api/src/routers/finance/payment-shared.ts"
      via: "import + reuse VALID_TRANSITIONS / autoCompleteRunIfTerminal — never reimplement the FSM"
      pattern: "payment-shared"
    - from: "packages/api/src/services/audit-writer.ts"
      to: "packages/api/src/routers/public-api/*.ts"
      via: "writeAuditLog({actorType:'API_KEY', actorId: ctx.apiKeyId}) on every write — lint-enforced"
      pattern: "actorType: 'API_KEY'"
---

<objective>
Re-implement the 7 write entities' mutations in the public tRPC layer (D-01), reusing the invariant helpers + plain services so no business rule is reimplemented — the only session-coupling replaced is the audit actor (`API_KEY`/`apiKeyId`). Every write carries a mandatory enforced scope from day one (D-02 BFLA hard constraint) and a `.strict()` DTO that makes `organizationId`/`workerType`/money mass-assignment impossible. The surface is dark by construction (shared flag gate; no Hono routes/SDK yet). This is the write half of INTEG-API-01 with the write-before-99 BFLA risk eliminated.

**Locked write-verb-per-entity (RESEARCH Open Q#1 default, reconciled with the 98-03 scope set):**
| Entity | Verbs | Scope(s) | Reuse |
|--------|-------|----------|-------|
| contractors | create, update | contractor:create/update | tenant-scoped create/update + `.strict()` allowlist (invoice-shared-style validation where present) |
| invoices | void (+create) | invoice:update (create) | invoice-shared (validateInvoiceAmounts, recomputeDuplicateHash, coerceInvoiceDateFields) |
| payments | create, update | payment:create/update | payment-settlement / payment-shared plain helpers |
| payment_runs | create, transition, export | payment:create/update/export | payment-shared (loadEligibleInvoices, validateInvoicesForRun, seedRunItems, allocateRunNumber, applyWithholdingToRun, VALID_TRANSITIONS, autoCompleteRunIfTerminal, _generateExportFileForFormat) + payment-export/payment-settlement |
| workflows | create, execute | workflow:create/execute | workflow-shared |
| workflow_tasks | transition | workflow:update/execute | workflow-shared (validateTransition, unblockDependentsAndRecomputeRun) |
| compliance_documents | create, link | document:create/update | doc-link-service |

NOTE: `payment_runs` payout-initiation (`_initiatePayoutForRun`, takes userId) is NOT exposed in Phase 98 (RESEARCH A7) — its actor coupling needs a dedicated API_KEY variant; defer.

Output: write procedures on the 7 sub-routers + `.strict()` write DTOs + the BFLA + strict-dto tests GREEN.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-CONTEXT.md
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-RESEARCH.md
@.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-SPIKE-FINDINGS.md

<interfaces>
Write pattern (RESEARCH § Pattern 2): `apiKeyTenantProcedure.use(requirePermission({<resource>:['<action>']})).input(<.strict() write DTO>).mutation(async ({ctx,input}) => ctx.db.$transaction(async tx => { const row = await tx.X.findFirstOrThrow({where:{id: input.id, organizationId: ctx.organizationId}}); <reuse invariant helper>; await tx.X.update({where:{id: row.id}, data:<explicit allowlist>}); await writeAuditLog({tx, organizationId: ctx.organizationId, actorType:'API_KEY', actorId: ctx.apiKeyId, action:'<x.verb>', resourceType:'<X>', resourceId: row.id, metadata:{...}}); return {...}; }))`.
Audit: audit-writer.ts:25 already includes `'API_KEY'` in AuditActorType; writeAuditLog is lint-enforced (scripts/lint-audit-log.mjs) — every mutation MUST call it. Reuse helpers are plain, tx-taking, org-scoped (payment-shared.ts:64/89/151/187/233/268/522/645; workflow-shared.ts:219/233; invoice-shared.ts:46/63/107). Do NOT touch the internal session mutation closures.
`.strict()` write DTOs live in validators (VERDICT A) or app layer for the route + validators for the tRPC input (VERDICT B); either way the DTO must omit organizationId/workerType/money and be `.strict()`.
Scopes are already in PUBLIC_API_SCOPES (98-03); the resources (contractor/invoice/payment/workflow/document) all have the needed actions in the auth statement — no auth change here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: contractor + invoice + compliance_document writes (create/update/void/link) + strict-dto GREEN</name>
  <files>packages/api/src/routers/public-api/contractor.ts, packages/api/src/routers/public-api/invoice.ts, packages/api/src/routers/public-api/compliance-document.ts, packages/validators/src/public-api/index.ts, apps/public-api/src/__tests__/strict-dto.test.ts</files>
  <read_first>
    - packages/api/src/routers/finance/invoice-shared.ts:46/63/107/140 (validateServicePeriod, validateInvoiceAmounts, recomputeDuplicateHash, revalidateStaleVatIfNeeded — reuse; do NOT reimplement) + invoice-actions.ts:19 (void inline — mirror the invariant, reuse helpers)
    - packages/api/src/services/doc-link-service.ts (plain link/create service to reuse for compliance_documents)
    - packages/api/src/services/audit-writer.ts:25/118 (API_KEY actor + writeAuditLog signature)
    - semble search: the Contractor create/update internal mutation to identify the invariant helpers + the exact writable field allowlist (exclude organizationId/workerType/type-locked fields)
    - the Wave-0 strict-dto.test.ts (98-02) — the mass-assignment assertions to satisfy
  </read_first>
  <action>
    Add write procedures: contractor.create + contractor.update (`requirePermission({contractor:['create']})`/`['update']`), invoice.void (`invoice:['update']`) + optional invoice.create (`invoice:['create']`), complianceDocument.create + complianceDocument.link (`document:['create']`/`['update']` via doc-link-service). Each: `.strict()` write DTO omitting organizationId/workerType/money; tenant from `ctx.organizationId`; explicit Prisma `data` allowlist; reuse the invoice-shared / doc-link-service helpers for validation/side-effects; `writeAuditLog({actorType:'API_KEY', actorId: ctx.apiKeyId, ...})` inside the transaction. Add the write DTOs to validators (per VERDICT). Turn `strict-dto.test.ts` GREEN (extra org/workerType/money keys rejected; clean body accepted). No Hono route yet (98-10). No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/public-api test strict-dto 2>&1 | tail -6; pnpm --filter @contractor-ops/api test public-api 2>&1 | tail -8; node scripts/lint-audit-log.mjs 2>&1 | tail -3 || true</automated>
  </verify>
  <acceptance_criteria>
    - contractor/invoice/compliance_document writes exist, each with a mandatory scope, `.strict()` DTO, org-from-key, explicit data allowlist, API_KEY audit inside a transaction.
    - `strict-dto.test.ts` GREEN; the audit-log lint passes (every mutation audits).
    - No business rule reimplemented — invoice-shared / doc-link-service helpers are imported and used.
  </acceptance_criteria>
  <done>contractor/invoice/compliance_document writes built (dark); mass-assignment blocked; API_KEY audit in place.</done>
</task>

<task type="auto">
  <name>Task 2: payment + payment_run + workflow + workflow_task writes (FSM/money) + BFLA matrix GREEN</name>
  <files>packages/api/src/routers/public-api/payment.ts, packages/api/src/routers/public-api/payment-run.ts, packages/api/src/routers/public-api/workflow.ts, packages/api/src/routers/public-api/workflow-task.ts, packages/validators/src/public-api/index.ts, packages/api/src/__tests__/security/public-api-write-scope.security.test.ts</files>
  <read_first>
    - packages/api/src/routers/finance/payment-shared.ts (VALID_TRANSITIONS:645, autoCompleteRunIfTerminal:268, loadEligibleInvoices:64, validateInvoicesForRun:89, seedRunItems:151, allocateRunNumber:187, applyWithholdingToRun:522, _generateExportFileForFormat:233) + payment-run-ops.ts:16 (inline FSM to mirror, reuse helpers) + services payment-export.ts / payment-settlement.ts
    - packages/api/src/routers/workflow/workflow-shared.ts:219/233 (validateTransition, unblockDependentsAndRecomputeRun) + workflow-execution-tasks.ts:42 (inline mutation to mirror)
    - RESEARCH § Pattern 2 (the payment_run.transition example) + A7 (do NOT expose _initiatePayoutForRun)
    - the Wave-0 public-api-write-scope.security.test.ts table (98-03 populated it) — the with/without-scope 403 matrix to satisfy
  </read_first>
  <action>
    Add write procedures reusing the invariant helpers ONLY (never reimplementing FSM/money logic):
    - payment.create/update (`payment:create/update`) via payment-settlement/payment-shared plain helpers.
    - paymentRun.create (loadEligibleInvoices→validateInvoicesForRun→groupByCurrency→allocateRunNumber→seedRunItems→applyWithholdingToRun), paymentRun.transition (VALID_TRANSITIONS guard → update → autoCompleteRunIfTerminal), paymentRun.export (_generateExportFileForFormat + payment-export/payment-settlement) — scopes `payment:create`/`payment:update`/`payment:export`. Do NOT expose payout initiation.
    - workflow.create/execute (`workflow:create`/`workflow:execute`).
    - workflowTask.transition (validateTransition guard → update → unblockDependentsAndRecomputeRun) — `workflow:update`/`workflow:execute`.
    Each: mandatory scope, `.strict()` DTO (no organizationId/workerType/money — for payment_run the money is server-derived from eligible invoices, never client-supplied), org-from-key, `ctx.db.$transaction`, `writeAuditLog(API_KEY)`. Turn `public-api-write-scope.security.test.ts` GREEN: every write 403s a key without its scope and passes with it (the BFLA matrix). No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test public-api-write-scope public-api 2>&1 | tail -14; pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/validators 2>&1 | tail -4; node scripts/lint-audit-log.mjs 2>&1 | tail -3 || true</automated>
  </verify>
  <acceptance_criteria>
    - payment/payment_run/workflow/workflow_task writes reuse the named invariant helpers (grep confirms imports from payment-shared / workflow-shared); no FSM/money logic reimplemented.
    - Every write has a mandatory scope + `.strict()` DTO + org-from-key + API_KEY audit; money is server-derived (never client body).
    - `public-api-write-scope.security.test.ts` GREEN across the full write matrix (403-without-scope, pass-with-scope); typecheck + audit-log lint clean.
  </acceptance_criteria>
  <done>payment/payment_run/workflow/workflow_task writes built (dark) reusing invariants; BFLA matrix GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| external API key → mutation | Untrusted body; tenant from key; scope mandatory; invariants reused not reimplemented |
| request body → Prisma data | `.strict()` DTO + explicit `data` allowlist prevent org/workerType/money mass-assignment |
| mutation → audit trail | Every external mutation audited as API_KEY (non-repudiation) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-09-01 | Elevation of Privilege | Write reachable without a scope (BFLA) | mitigate | Mandatory `requirePermission` on EVERY write; table-driven BFLA test asserts 403-without-scope for all |
| T-98-09-02 | Tampering | Mass-assignment of organizationId/workerType/money | mitigate | `.strict()` DTOs omit them; explicit Prisma `data` allowlist; money server-derived; tenant from ctx |
| T-98-09-03 | Tampering | Reimplemented FSM/money diverging from internal invariants (double-spend) | mitigate | Import + reuse payment-shared / workflow-shared helpers; never reimplement VALID_TRANSITIONS/export |
| T-98-09-04 | Repudiation | External mutation without audit | mitigate | writeAuditLog(API_KEY, apiKeyId) inside the tx; lint-enforced |
| T-98-09-05 | Elevation of Privilege | Payout initiation exposed with wrong actor coupling | mitigate | `_initiatePayoutForRun` NOT exposed in P98 (deferred; needs API_KEY variant) |
| T-98-09-SC | Tampering | npm installs | accept | No new deps |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test public-api-write-scope public-api` green; `pnpm --filter @contractor-ops/public-api test strict-dto` green.
- `pnpm typecheck --filter @contractor-ops/api --filter @contractor-ops/validators` clean; `node scripts/lint-audit-log.mjs` passes.
- Grep: every write procedure has a `requirePermission(` line and a `writeAuditLog(` with `actorType: 'API_KEY'`.
</verification>

<success_criteria>
INTEG-API-01 (write half): the 7 write entities have scope-mandatory, `.strict()`, org-scoped, invariant-reusing, API_KEY-audited mutations — dark by construction until Phase 99. No unscoped mutating procedure exists; no business rule reimplemented; no mass-assignment path.
</success_criteria>

<output>
Create `.planning/phases/98-theme-c-public-rest-api-surface-foundation-gate/98-09-SUMMARY.md` when done.
</output>
