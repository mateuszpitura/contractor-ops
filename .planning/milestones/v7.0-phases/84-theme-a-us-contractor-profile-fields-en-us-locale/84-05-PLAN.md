---
phase: 84-theme-a-us-contractor-profile-fields-en-us-locale
plan: 05
type: execute
wave: 3
depends_on: [84-01, 84-03, 84-04]
autonomous: true
requirements: [US-FIELD-01, US-FIELD-02, US-FIELD-03, US-FIELD-04]
files_modified:
  - packages/api/src/routers/core/contractor.ts
must_haves:
  truths:
    - "getCountryFieldsConfig returns hasCountryFields=true + the US field list for a US org; US is registered server-side (3-place edit, place 1+2) (US-FIELD-04, D-06)"
    - "updateUsProfile stores EIN plain (validated) and SSN via encryptSsn → ssnEncrypted + ssnLast4 (dedicated columns, NEVER countryFields JSONB); USPS normalize runs advisory/non-blocking on save (US-FIELD-01/02/03, D-01/D-03)"
    - "revealSsn is a STAFF-router-only procedure gated by requirePermission({contractorPii:['read']}), tenant-scoped, decrypts, and writeAuditLog(action='contractor.ssn.revealed') with NO SSN in the audit row (US-FIELD-02, D-02/D-09)"
    - "The normal contractor read path exposes ssnLast4 only — ssnEncrypted is never selected outside revealSsn (Pitfall 3)"
    - "revealSsn appears on NO portal router (Pitfall 6)"
  artifacts:
    - path: "packages/api/src/routers/core/contractor.ts"
      provides: "US config + updateUsProfile + revealSsn"
      contains: "revealSsn"
  key_links:
    - from: "packages/api/src/routers/core/contractor.ts (revealSsn)"
      to: "requirePermission({contractorPii:['read']}) + writeAuditLog + decryptSsn"
      via: "staff tenantProcedure with reveal gate"
      pattern: "contractorPii"
    - from: "packages/api/src/routers/core/contractor.ts (updateUsProfile)"
      to: "encryptSsn + UspsAddressClient.validateAddress"
      via: "SSN → ssnEncrypted/ssnLast4 columns; USPS advisory normalize"
      pattern: "encryptSsn"
---

<objective>
Wire the US server surface into the staff `contractor` router (US-FIELD-01/02/03/04): register
`'US'` in `getCountryFieldsConfig` (place 1 of the 3-place US registration; place 2 is
`countryFieldsSchemaMap` from Plan 01; place 3 is the React switch in Plan 06); add
`updateUsProfile` (EIN plain via usCountryFieldsSchema, SSN → `encryptSsn` into the dedicated
`ssnEncrypted`/`ssnLast4` columns NOT the JSONB, USPS advisory normalize on save writing
`uspsVerified`/`uspsValidatedAt`); and add the audit-logged `revealSsn` reveal procedure
gated by `contractorPii:['read']` — staff router ONLY, never portal.

Purpose: The encrypted-column write path + the least-privilege audit-logged reveal are the
server half of the SSN contract; the UI (Plan 06) consumes these procedures.
Output: contractor.ts edits (config + updateUsProfile + revealSsn).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v7.0-phases/84-theme-a-us-contractor-profile-fields-en-us-locale/84-RESEARCH.md
@packages/api/src/routers/core/contractor.ts
@packages/api/src/middleware/rbac.ts
@packages/api/src/services/audit-writer.ts

<interfaces>
From packages/api/src/routers/core/contractor.ts (the procedures to extend/mirror):
  getCountryFieldsConfig: tenantProcedure.query — returns { hasCountryFields, countryCode, fields }  (register US fields list here)
  updateCountryFields: tenantProcedure.use(requirePermission({contractor:['update']})) — Zod via countryFieldsSchemaMap (mirror for updateUsProfile)
  encryptBankAccount precedent at :270, :723 ; writeAuditLog usage in this file

From Plan 01: usCountryFieldsSchema, isValidEin, isValidSsn (@contractor-ops/validators); SSN_ENCRYPTION_KEY/USPS creds in env
From Plan 03: encryptSsn/decryptSsn (../../services/ssn-crypto) ; contractorPii:['read'] (Better Auth) ; Contractor.ssnEncrypted/ssnLast4/uspsVerified/uspsValidatedAt columns
From Plan 04: UspsAddressClient.validateAddress(input) → { verified, normalized?, status } (@contractor-ops/gov-api)

Reveal contract (84-RESEARCH.md:374-392, staff-router-only Pitfall 6):
  revealSsn: tenantProcedure.use(requirePermission({contractorPii:['read']})).input({contractorId}).mutation → { ssn }
    - select only { id, ssnEncrypted } scoped by organizationId ; NOT_FOUND if absent
    - writeAuditLog action='contractor.ssn.revealed' resourceType='CONTRACTOR' metadata={field:'ssn'}  (NEVER the value)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: register US config + updateUsProfile (EIN plain, SSN encrypted, USPS advisory)</name>
  <read_first>
    - packages/api/src/routers/core/contractor.ts:1311-1381 (getCountryFieldsConfig + updateCountryFields — the exact patterns to extend/mirror) and :260-275, :720-725 (encryptBankAccount + masked/last4 precedent)
    - packages/api/src/services/ssn-crypto.ts (Plan 03 — encryptSsn)
    - 84-RESEARCH.md:126-147 (architecture: updateUsProfile EIN plain + SSN→ssn-crypto + dedicated columns + USPS advisory), :300-303 (Pitfall 3 — never select ssnEncrypted in normal reads), :356-371 (usCountryFieldsSchema)
  </read_first>
  <behavior>
    - getCountryFieldsConfig for a US org → { hasCountryFields:true, countryCode:'US', fields:[entityType, ein, addressLine1, city, state, zipCode] }
    - updateUsProfile parses input via usCountryFieldsSchema (EIN validated, state.length(2)); rejects invalid EIN with BAD_REQUEST
    - SSN input (separate from the JSONB) → encryptSsn → ssnEncrypted, ssnLast4 = cleaned.slice(-4); SSN never written to countryFields
    - USPS normalize runs on save (advisory): writes uspsVerified + uspsValidatedAt; a USPS failure does NOT fail the mutation (save succeeds unverified)
    - the normal getCountryFields / contractor read path returns ssnLast4 only, never ssnEncrypted
  </behavior>
  <action>
    In `getCountryFieldsConfig`, add a `US` branch returning the US `fields` list (mirror the AE/SA branch; the entity-type + EIN + address fields, NOT ssn — SSN is rendered by the dedicated control, not the generic field list). Add an `updateUsProfile` procedure (gate `requirePermission({contractor:['update']})`) taking `{ contractorId, fields }` validated by `usCountryFieldsSchema` for the JSONB part (EIN stored plain in countryFields) PLUS an optional `ssn` input validated by `isValidSsn`; on a provided SSN, set `ssnEncrypted = encryptSsn(cleaned)` and `ssnLast4 = cleaned.slice(-4)` (mirror the bankAccount precedent at :270) — write these to the dedicated columns, NEVER into countryFields. Run `UspsAddressClient.validateAddress` on the address fields advisory/non-blocking: wrap in try/catch (fail-open), write `uspsVerified`/`uspsValidatedAt`; a USPS error leaves the save successful + unverified. Tenant-scope every query/update by `ctx.organizationId`. Ensure no read path selects `ssnEncrypted` (Pitfall 3).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    US registered in getCountryFieldsConfig; updateUsProfile validates EIN/SSN, encrypts SSN to dedicated columns (never JSONB), runs USPS advisory (non-blocking). ssnEncrypted never selected in a normal read. api typecheck clean.
  </acceptance_criteria>
  <done>US config + updateUsProfile shipped; SSN encrypted to dedicated columns; USPS advisory; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: revealSsn (staff-router-only, RBAC-gated, audit-logged)</name>
  <read_first>
    - packages/api/src/routers/core/contractor.ts (writeAuditLog call sites in this file; the staff contractor router export — revealSsn registers HERE, not in any routers/portal/*)
    - packages/api/src/middleware/rbac.ts:19-61 (requirePermission → FORBIDDEN)
    - packages/api/src/services/ssn-crypto.ts (decryptSsn) ; packages/api/src/services/audit-writer.ts (writeAuditLog signature)
    - packages/api/src/routers/core/__tests__/contractor-reveal-ssn.test.ts (Plan 00 RED — turn GREEN)
    - 84-RESEARCH.md:374-392 (reveal procedure reference), :316-319 (Pitfall 6 — never portal)
  </read_first>
  <behavior>
    - caller without contractorPii:['read'] → FORBIDDEN (no decrypt, no audit)
    - caller with the permission → { ssn } (decrypted) AND a writeAuditLog row action='contractor.ssn.revealed', metadata={field:'ssn'} with NO raw SSN value
    - cross-tenant contractorId (different org) → NOT_FOUND (organizationId-scoped select)
    - contractor without an ssnEncrypted → NOT_FOUND
    - the procedure is registered on the staff contractor router and on NO portal router
  </behavior>
  <action>
    Add `revealSsn: tenantProcedure.use(requirePermission({ contractorPii: ['read'] })).input(z.object({ contractorId: z.string() })).mutation(...)` to the staff `contractor` router. Inside: `findUnique` selecting ONLY `{ id, ssnEncrypted }` scoped by `organizationId: ctx.organizationId` → NOT_FOUND if missing or no ssnEncrypted; `decryptSsn(c.ssnEncrypted)`; `writeAuditLog({ organizationId, actorType:'USER', actorId: ctx.session.user.id, action:'contractor.ssn.revealed', resourceType:'CONTRACTOR', resourceId: c.id, metadata:{ field:'ssn' } })` — NEVER place the SSN in the audit row; return `{ ssn }`. Confirm by grep that no `routers/portal/*` file gains a revealSsn (Pitfall 6).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test src/routers/core/__tests__/contractor-reveal-ssn.test.ts && ! grep -rl "revealSsn" packages/api/src/routers/portal 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    contractor-reveal-ssn.test.ts (Plan 00) green: FORBIDDEN without perm, decrypt+audit (no SSN in row) with perm, cross-tenant NOT_FOUND, staff-router-only. grep confirms no portal revealSsn. `pnpm lint:audit-log` clean.
  </acceptance_criteria>
  <done>revealSsn shipped staff-only + RBAC + audit (no SSN in row); Plan 00 reveal test green; no portal exposure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client → updateUsProfile / revealSsn | untrusted input at the staff tRPC boundary; tenant from session only |
| reveal decision | contractorPii:read gate + audit before any plaintext SSN leaves the server |
| portal vs staff router | SSN reveal must exist on staff only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-05-01 | Information Disclosure | wide SSN read path | mitigate | ssnEncrypted NEVER selected outside revealSsn; normal reads expose ssnLast4 only (Pitfall 3) |
| T-84-05-02 | Elevation of Privilege | reveal without permission | mitigate | requirePermission({contractorPii:['read']}) → FORBIDDEN; deny-by-default (D-09); requirePermission re-reads role live |
| T-84-05-03 | Repudiation | unaudited SSN access | mitigate | writeAuditLog on every reveal (append-only); metadata field:'ssn', value NEVER logged |
| T-84-05-04 | Spoofing/IDOR | cross-tenant reveal/update | mitigate | organizationId: ctx.organizationId scoping → cross-tenant = NOT_FOUND |
| T-84-05-05 | Tampering (mass-assignment) | SSN via JSONB update path | mitigate | SSN is a separate validated input → dedicated columns; usCountryFieldsSchema excludes ssn so the JSONB path cannot set it (D-01) |
| T-84-05-06 | Information Disclosure | reveal on portal | mitigate | revealSsn on staff router only; grep gate asserts no routers/portal/* revealSsn (Pitfall 6) |
| T-84-05-07 | Denial of Service | USPS coupling to save | mitigate | USPS validate wrapped fail-open; save succeeds unverified on USPS error (D-03) |
| T-84-SC | Tampering | npm installs | accept | No new deps |
</threat_model>

<verification>
`pnpm --filter @contractor-ops/api test src/routers/core/__tests__/contractor-reveal-ssn.test.ts` green; `pnpm --filter @contractor-ops/api typecheck` clean; `pnpm lint:audit-log` + `pnpm lint:logs` clean; grep confirms no portal revealSsn.
</verification>

<success_criteria>
US config registered; updateUsProfile encrypts SSN to dedicated columns + USPS advisory non-blocking; revealSsn staff-only + contractorPii:read + audit (no SSN in row) + tenant-scoped; no wide ssnEncrypted read.
</success_criteria>

<output>
Create `.planning/phases/84-theme-a-us-contractor-profile-fields-en-us-locale/84-05-SUMMARY.md` when done.
</output>
