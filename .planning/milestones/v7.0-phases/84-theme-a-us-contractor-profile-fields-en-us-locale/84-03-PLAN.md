---
phase: 84-theme-a-us-contractor-profile-fields-en-us-locale
plan: 03
type: execute
wave: 2
depends_on: [84-01]
autonomous: true
requirements: [US-FIELD-02]
files_modified:
  - packages/db/prisma/schema/contractor.prisma
  - packages/api/src/services/ssn-crypto.ts
  - packages/auth/src/permissions.ts
  - packages/auth/src/roles.ts
  - packages/auth/src/__tests__/permissions.test.ts
  - packages/logger/src/pii-mask.ts
  - packages/logger/src/__tests__/index.test.ts
must_haves:
  truths:
    - "[BLOCKING] Contractor gains additive nullable columns ssnEncrypted, ssnLast4, uspsVerified, uspsValidatedAt applied to the live dev DB (db push / direct ALTER) and the Prisma client is regenerated, BEFORE any code reads them (D-01 crypto correction)"
    - "encryptSsn/decryptSsn round-trip via AES-256-GCM with a dedicated SSN_ENCRYPTION_KEY (separate from the bank key); ssnLast4 = cleaned.slice(-4) (D-01)"
    - "contractorPii:['read'] is a new Better Auth access-control statement granted to owner + admin + finance_admin ONLY; the other 7 roles (incl. external_accountant) deny (D-02/D-09)"
    - "owner gets contractorPii:read via the DUPLICATED allPermissions const in roles.ts AND the accessControlStatement (both edited) (Pitfall 2)"
    - "*.ssn, *.ein, and their *.countryFields.* variants are redacted by pino (D-08)"
  artifacts:
    - path: "packages/db/prisma/schema/contractor.prisma"
      provides: "ssnEncrypted/ssnLast4/uspsVerified/uspsValidatedAt on Contractor"
      contains: "ssnEncrypted"
    - path: "packages/api/src/services/ssn-crypto.ts"
      provides: "encryptSsn/decryptSsn (mirror bank-account-crypto)"
      exports: ["encryptSsn", "decryptSsn"]
    - path: "packages/auth/src/permissions.ts"
      provides: "contractorPii:['read'] statement"
      contains: "contractorPii"
    - path: "packages/auth/src/roles.ts"
      provides: "contractorPii grant to owner/admin/finance_admin"
      contains: "contractorPii"
    - path: "packages/logger/src/pii-mask.ts"
      provides: "ssn/ein PII_MASK_PATHS"
      contains: ".ssn"
  key_links:
    - from: "packages/api/src/services/ssn-crypto.ts"
      to: "getServerEnv().SSN_ENCRYPTION_KEY"
      via: "getEncryptionKey()"
      pattern: "SSN_ENCRYPTION_KEY"
    - from: "packages/auth/src/roles.ts"
      to: "allPermissions (owner duplicate) + per-role grants"
      via: "contractorPii: ['read']"
      pattern: "contractorPii"
---

<objective>
Build the SSN security foundation: the additive Prisma columns (applied to the live dev DB
ahead of any reader — [BLOCKING]), the `ssn-crypto.ts` AES-256-GCM helper with a dedicated
`SSN_ENCRYPTION_KEY`, the new `contractorPii:['read']` Better Auth permission granted to
owner/admin/finance_admin ONLY (D-09), and the pino PII-mask entries for ssn/ein (D-08).
This is the load-bearing security surface; Plan 04 (USPS) and Plan 05 (reveal procedure +
UI) build on it.

Purpose: Encrypt-at-rest SSN + least-privilege reveal gate + log redaction must exist before
the reveal procedure (Plan 05) and any column read.
Output: prisma columns (pushed), ssn-crypto.ts, permissions/roles edits, pii-mask edits, three test extensions.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/84-theme-a-us-contractor-profile-fields-en-us-locale/84-RESEARCH.md
@packages/api/src/services/bank-account-crypto.ts
@packages/auth/src/permissions.ts
@packages/auth/src/roles.ts
@packages/logger/src/pii-mask.ts

<interfaces>
From packages/db/prisma/schema/contractor.prisma (model Contractor — additive nullable columns beside countryFields + latestVatValidation*):
  ssnEncrypted    String?
  ssnLast4        String?
  uspsVerified    Boolean?
  uspsValidatedAt DateTime?

From packages/api/src/services/bank-account-crypto.ts (exact mirror, NEW key):
  const ALGORITHM='aes-256-gcm'; const IV_LENGTH=12; getEncryptionKey()=Buffer.from(getServerEnv().SSN_ENCRYPTION_KEY,'hex')
  encryptSsn(ssn)→'iv:authTag:ciphertext' ; decryptSsn(enc)→ssn ; ssnLast4 = cleaned.slice(-4)

From packages/auth/src/permissions.ts: accessControlStatement += contractorPii: ['read']
From packages/auth/src/roles.ts: allPermissions (owner dup, lines 18-38) += contractorPii:['read'] AND owner/admin/finance_admin role objects += contractorPii:['read']. DENY: ops_manager, team_manager, legal_compliance_viewer, it_admin, external_accountant, readonly, platform_operator.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] additive Contractor columns — push to dev DB + regenerate client</name>
  <read_first>
    - packages/db/prisma/schema/contractor.prisma:9-60 (model Contractor field block + countryFields + latestVatValidation* — add the new columns here, all nullable)
    - 84-RESEARCH.md:174-175 (column list: ssnEncrypted/ssnLast4/uspsVerified/uspsValidatedAt) and :256, :532 (db push / direct ALTER — prisma migrate dev is drift-blocked; per-region prod apply deferred)
    - .planning/STATE.md Accumulated Context (Phase 82/83 db-push fallback posture)
  </read_first>
  <files>packages/db/prisma/schema/contractor.prisma</files>
  <action>
    Add to `model Contractor` (additive, all nullable so existing rows default NULL — zero data migration): `ssnEncrypted String?`, `ssnLast4 String?`, `uspsVerified Boolean?`, `uspsValidatedAt DateTime?`, placed beside `countryFields` / the VAT-validation denormalized columns. Apply to the live dev DB with `pnpm --filter @contractor-ops/db db:push` (or a direct additive `ALTER TABLE "Contractor" ADD COLUMN IF NOT EXISTS ...` if push is drift-blocked, matching the Phase 82/83 fallback) and regenerate the Prisma client (`pnpm --filter @contractor-ops/db generate` / the repo's generate script). DO NOT run `prisma migrate dev` (pre-existing migration-history drift). Record in the SUMMARY: "per-region production column apply deferred (Phase 82/83 posture)". This task MUST land before any plan reads ssnEncrypted/ssnLast4 (Plan 05).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db generate && pnpm --filter @contractor-ops/db typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    Four nullable columns present in schema + applied to dev DB + client regenerated (the generated Prisma types expose ssnEncrypted/ssnLast4/uspsVerified/uspsValidatedAt on Contractor). No migrate-dev run. Prod apply deferred note recorded.
  </acceptance_criteria>
  <done>Columns pushed to dev DB, client regenerated, prod-apply deferral noted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ssn-crypto.ts (AES-256-GCM, dedicated key)</name>
  <read_first>
    - packages/api/src/services/bank-account-crypto.ts:1-56 (the exact mirror — copy structure, swap the env key)
    - packages/api/src/services/__tests__/ssn-crypto.test.ts (Plan 00 RED scaffold — turn GREEN)
    - 84-RESEARCH.md:195-212 (Pattern 1 — single string, iv:authTag:ciphertext, NEW SSN_ENCRYPTION_KEY) ; env key added in Plan 01
  </read_first>
  <behavior>
    - encryptSsn('123456789') then decryptSsn(...) → '123456789'
    - output is exactly 3 colon-separated hex parts (iv:authTag:ciphertext)
    - two encrypts of the same SSN differ (random 12-byte IV)
    - decryptSsn rejects a malformed (non-3-part) input
    - ssnLast4 derivation: '123456789'.slice(-4) === '6789'
  </behavior>
  <action>
    Create ssn-crypto.ts mirroring bank-account-crypto.ts verbatim except `getEncryptionKey()` reads `getServerEnv().SSN_ENCRYPTION_KEY` (the new key from Plan 01, NEVER reuse BANK_ACCOUNT_ENCRYPTION_KEY — blast-radius separation D-01). Export `encryptSsn`/`decryptSsn`. The last4 column is derived by the caller (Plan 05) as `cleaned.slice(-4)`; if a `maskSsnLast4` helper is convenient, export it here. The crypto file performs no logging of the SSN value.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test src/services/__tests__/ssn-crypto.test.ts</automated>
  </verify>
  <acceptance_criteria>
    ssn-crypto.test.ts (Plan 00) green: round-trip, 3-part format, random-IV, malformed-reject. Uses SSN_ENCRYPTION_KEY, not the bank key.
  </acceptance_criteria>
  <done>encryptSsn/decryptSsn shipped with dedicated key; Plan 00 crypto test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: contractorPii:read permission + grants + pii-mask</name>
  <read_first>
    - packages/auth/src/permissions.ts:12-44 (accessControlStatement — add contractorPii)
    - packages/auth/src/roles.ts:18-38 (allPermissions owner DUPLICATE — Pitfall 2) and the owner/admin/finance_admin role objects (:46, :48, :70) + the 7 deny roles
    - packages/auth/src/__tests__/permissions.test.ts and roles.test.ts (existing access-control matrix tests — EXTEND with the 10-role contractorPii assertion)
    - packages/logger/src/pii-mask.ts:14-67 (PII_MASK_PATHS — add ssn/ein) and packages/logger/src/__tests__/index.test.ts (extend redaction test)
    - 84-RESEARCH.md:214-225 (Pattern 2 RBAC), :288-298 (Pitfalls 1+2), :40-42 (D-08 mask paths)
  </read_first>
  <behavior>
    - hasPermission({contractorPii:['read']}) → true for owner, admin, finance_admin
    - → false (deny) for ops_manager, team_manager, legal_compliance_viewer, it_admin, external_accountant, readonly, platform_operator (all 7)
    - accessControlStatement and roles.ts allPermissions both contain contractorPii (no drift)
    - a log object with `{ contractor: { ssn:'123-45-6789', ein:'12-3456789' } }` and `{ countryFields: { ssn, ein } }` redacts both
  </behavior>
  <action>
    permissions.ts: add `contractorPii: ['read'],` to `accessControlStatement`. roles.ts: add `contractorPii: ['read'],` to the local `allPermissions` const (owner — the duplicate, Pitfall 2) AND to the `admin` and `finance_admin` role objects. Do NOT add it to ops_manager/team_manager/legal_compliance_viewer/it_admin/external_accountant/readonly/platform_operator (deny-by-default; external_accountant explicitly excluded per D-09). pii-mask.ts: add `'*.ssn'`, `'*.ein'`, `'*.SSN'`, `'*.EIN'`, `'*.countryFields.ssn'`, `'*.countryFields.ein'` (+ casing variants) to `PII_MASK_PATHS`; add `'ssn'`/`'ein'` to `PII_MASK_KEYWORDS`. Extend permissions/roles test with the full 10-role contractorPii matrix and the logger index test with the ssn/ein redaction assertion.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/auth test && pnpm --filter @contractor-ops/logger test src/__tests__/index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Auth test: contractorPii granted to owner/admin/finance_admin, denied to the other 7 (D-09). owner allPermissions duplicate edited (Pitfall 2 — owner passes). Logger test: ssn/ein + countryFields variants redacted. `pnpm lint:logs` clean.
  </acceptance_criteria>
  <done>contractorPii:read added (statement + owner dup + 3 grants, 7 denies); ssn/ein masked; auth + logger tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| stored SSN at rest | DB column — encrypted, decryptable only server-side with SSN_ENCRYPTION_KEY |
| role → contractorPii:read | RBAC decision boundary; deny-by-default for 7 of 10 roles |
| log sink | SSN/EIN must never reach the log sink in cleartext |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-03-01 | Information Disclosure | SSN at rest | mitigate | AES-256-GCM via ssn-crypto.ts; dedicated SSN_ENCRYPTION_KEY (separate blast radius from bank key); per-encrypt random IV; authTag verified on decrypt (mirror bank-account-crypto, never hand-rolled) |
| T-84-03-02 | Elevation of Privilege | contractorPii:read grant | mitigate | Deny-by-default: granted to owner/admin/finance_admin only; external_accountant + 6 others denied (D-09); 10-role matrix test asserts it; owner allPermissions duplicate edited (Pitfall 2) |
| T-84-03-03 | Information Disclosure | SSN/EIN in logs | mitigate | PII_MASK_PATHS += *.ssn/*.ein + countryFields variants (D-08); lint:logs gate |
| T-84-03-04 | Tampering | drift-blocked migrate | mitigate | Additive nullable columns via db push / direct ALTER (Phase 82/83 posture); no migrate-dev; per-region prod apply deferred + recorded |
| T-84-SC | Tampering | npm installs | accept | No new deps — node:crypto stdlib, better-auth + pino already in tree |
</threat_model>

<verification>
`pnpm --filter @contractor-ops/api test src/services/__tests__/ssn-crypto.test.ts`, `pnpm --filter @contractor-ops/auth test`, `pnpm --filter @contractor-ops/logger test src/__tests__/index.test.ts`, `pnpm --filter @contractor-ops/db generate` all green; `pnpm lint:logs` clean.
</verification>

<success_criteria>
Columns pushed + client regenerated (before any reader); SSN encrypt/decrypt with dedicated key; contractorPii:read = owner/admin/finance_admin only (Pitfall 2 owner-dup handled); ssn/ein log-masked.
</success_criteria>

<output>
Create `.planning/phases/84-theme-a-us-contractor-profile-fields-en-us-locale/84-03-SUMMARY.md` when done.
</output>
