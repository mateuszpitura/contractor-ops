---
phase: 84-theme-a-us-contractor-profile-fields-en-us-locale
plan: 02
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [US-LOC-01]
files_modified:
  - apps/web-vite/src/i18n/messages.ts
  - apps/web-vite/src/i18n/index.ts
  - apps/web-vite/messages/en-US.json
  - scripts/i18n-parity.mjs
  - packages/lint-guards/src/i18n-parity/run-guard.ts
  - packages/lint-guards/src/__tests__/i18n-parity.test.ts
  - apps/web-vite/src/i18n/__tests__/messages.test.ts
must_haves:
  truths:
    - "en-US is a selectable locale registered in SUPPORTED_LOCALES + localeMeta + the on-demand loader (US-LOC-01, D-04)"
    - "Switching to en-US inherits every unchanged key from en via the fallbackLng map en-US → en → pl (effective full parity) (D-04)"
    - "The i18n:parity gate treats a key present in en as covered for en-US (fallback-aware peer mode) — en-US is NOT a strict peer and a thin-override en-US.json passes (D-04)"
    - "A key genuinely missing from a strict peer (de/pl/ar) still fails parity"
    - "Intl en-US renders MM/DD/YYYY dates and $ currency"
  artifacts:
    - path: "apps/web-vite/messages/en-US.json"
      provides: "thin-override en-US bundle (divergent keys only)"
    - path: "apps/web-vite/src/i18n/messages.ts"
      provides: "en-US in SUPPORTED_LOCALES/localeMeta/localeLoaders"
      contains: "en-US"
    - path: "packages/lint-guards/src/i18n-parity/run-guard.ts"
      provides: "fallback-aware peer mode"
      contains: "fallbackBase"
  key_links:
    - from: "apps/web-vite/src/i18n/index.ts"
      to: "en fallback bundle for en-US"
      via: "registerLocaleBundle('en') when locale==='en-US'"
      pattern: "fallback"
    - from: "scripts/i18n-parity.mjs"
      to: "run-guard fallback-aware mode"
      via: "en-US peer with fallbackBase en"
      pattern: "en-US"
---

<objective>
Register `en-US` as a thin-override locale (US-LOC-01, D-04): add it to web-vite
`SUPPORTED_LOCALES` + `localeMeta` + the on-demand loader, wire the `fallbackLng` chain so
`en-US → en → pl` inherits every unchanged key, create the (initially near-empty)
`en-US.json` thin override, and teach the `i18n:parity` gate a fallback-aware peer mode so a
key present in `en` counts as covered for `en-US` (en-US is NOT added to the strict peers
array — that would red-CI on every not-yet-overridden key).

Purpose: en-US must be selectable + at effective full parity via fallback before the US UI
copy lands; the parity gate must not break on a deliberately-thin override.
Output: messages.ts/index.ts edits, en-US.json, parity-guard fallback mode, two test extensions.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v7.0-phases/84-theme-a-us-contractor-profile-fields-en-us-locale/84-RESEARCH.md
@.planning/milestones/v7.0-phases/84-theme-a-us-contractor-profile-fields-en-us-locale/84-UI-SPEC.md
@apps/web-vite/src/i18n/messages.ts
@apps/web-vite/src/i18n/index.ts
@packages/lint-guards/src/i18n-parity/run-guard.ts

<interfaces>
From apps/web-vite/src/i18n/messages.ts:
  export const SUPPORTED_LOCALES = ['en', 'pl', 'ar', 'de'] as const   // add 'en-US'
  export const DEFAULT_LOCALE: Locale = 'pl'
  export const localeMeta: Record<Locale, LocaleMeta>                  // add en-US entry
  const localeLoaders: Record<Locale, () => Promise<...>>             // add en-US loader
  export function pickBestLocale(preferences): Locale                  // currently normalizes en-US → en; preserve direct en-US match if exact

From apps/web-vite/src/i18n/index.ts:
  i18next.init({ fallbackLng: DEFAULT_LOCALE, supportedLngs: SUPPORTED_LOCALES, ... })  // fallbackLng must become a map en-US → en (then default pl)
  async function applyLocale(locale): registers the locale bundle + DEFAULT_LOCALE fallback bundle  // also register 'en' when locale === 'en-US'

From packages/lint-guards/src/i18n-parity/run-guard.ts:
  export interface I18nParityOptions { messagesDir; base; peers; baseline? }   // add fallbackBase?: Set<string> or a fallbackPeers map
  export async function runI18nParity(opts): Promise<I18nParityOffence[]>

From scripts/i18n-parity.mjs: peers: ['de','pl','ar'] — DO NOT add 'en-US' to this strict array; add it via the new fallback-aware mode.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fallback-aware i18n:parity guard</name>
  <read_first>
    - packages/lint-guards/src/i18n-parity/run-guard.ts:11-80 (I18nParityOptions + runI18nParity flatten/missing logic — extend, do not rewrite)
    - packages/lint-guards/src/__tests__/i18n-parity.test.ts (existing guard test — EXTEND with fallback cases)
    - scripts/i18n-parity.mjs (the CLI call sites — base:'en', peers:['de','pl','ar']; --update-baseline branch)
    - 84-RESEARCH.md:241-248, :311-314 (fallback-aware peer mode design + Pitfall 5)
  </read_first>
  <behavior>
    - a key present only in en-US (divergent override) → covered for en-US (pass)
    - a key present only in en (not in en-US) → covered for en-US via fallback (pass)
    - a key in en but missing from a STRICT peer (de) → still an offence (fail) — fallback mode must NOT relax strict peers
    - a key missing from BOTH en-US and en → offence for en-US (true gap)
  </behavior>
  <action>
    Add an optional `fallbackBase?: Set<string>` (or a `fallbackPeers?: Record<string, string>` mapping a peer to its fallback locale) to `I18nParityOptions`. In `runI18nParity`, when a peer is the fallback-aware locale (en-US), treat the covered set as `peerKeys ∪ baseKeys` (a key in en counts as covered for en-US). Strict peers (de/pl/ar) keep exact `peerKeys` semantics unchanged. Update scripts/i18n-parity.mjs to run en-US as a fallback-aware peer with fallbackBase = en's flattened keys, in BOTH the --update-baseline branch and the main branch (so en-US never adds spurious baseline offences). Keep `peers: ['de','pl','ar']` as the strict array. Extend i18n-parity.test.ts with the four behavior cases above.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/lint-guards test src/__tests__/i18n-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Guard test green: en-US divergent-key pass, en-only-key covered-for-en-US pass, strict-peer-missing still fails, both-missing fails. Strict peer semantics for de/pl/ar unchanged.
  </acceptance_criteria>
  <done>run-guard fallback-aware mode + i18n-parity.mjs wiring + tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: register en-US locale + fallback chain + thin override</name>
  <read_first>
    - apps/web-vite/src/i18n/messages.ts:9-68 (SUPPORTED_LOCALES, localeMeta, localeLoaders, pickBestLocale)
    - apps/web-vite/src/i18n/index.ts:81-141 (init fallbackLng/supportedLngs + applyLocale fallback-bundle registration + html lang/dir)
    - apps/web-vite/src/i18n/__tests__/messages.test.ts (existing — EXTEND) and pick-best-locale.test.ts
    - 84-UI-SPEC.md §E (locale switcher entry "English (US)", MM/DD/YYYY, $, RTL invariant) ; 84-RESEARCH.md:184-187, :472-473
  </read_first>
  <behavior>
    - SUPPORTED_LOCALES includes 'en-US'; isSupportedLocale('en-US') → true
    - localeMeta['en-US'] = { nativeName 'English (US)', englishName 'English (US)', dir 'ltr' }
    - localeLoaders['en-US'] dynamic-imports messages/en-US.json
    - applyLocale('en-US') registers en-US AND en (fallback) bundles; html dir stays ltr; ar still rtl (no regression)
    - Intl.DateTimeFormat('en-US') → MM/DD/YYYY order; Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}) → leading $
  </behavior>
  <action>
    Add `'en-US'` to `SUPPORTED_LOCALES`, a `localeMeta['en-US']` entry (nativeName/englishName "English (US)", dir 'ltr'), and an `en-US` `localeLoaders` entry importing `../../messages/en-US.json`. Preserve `pickBestLocale` behavior: an exact `en-US` preference should resolve to `en-US` (add a direct exact-match check before the 2-letter normalization, so the en-US choice sticks). In index.ts, change `fallbackLng` from the bare DEFAULT_LOCALE to a map `{ 'en-US': ['en', DEFAULT_LOCALE], default: [DEFAULT_LOCALE] }`; in `applyLocale`, when `locale === 'en-US'` also `registerLocaleBundle('en')` (so fallback keys resolve) in addition to the existing DEFAULT_LOCALE fallback registration; keep `document.documentElement.dir` ltr for en-US (the `locale === 'ar' ? 'rtl' : 'ltr'` ternary already yields ltr — verify, do not regress ar). Create `apps/web-vite/messages/en-US.json` as a near-empty thin override `{}` (US copy keys are added by Plan 05; this plan only registers the file + a single proof-of-divergence key is optional). Extend messages.test.ts with the registration + Intl en-US formatting assertions.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite test src/i18n/__tests__/messages.test.ts && pnpm i18n:parity</automated>
  </verify>
  <acceptance_criteria>
    messages.test.ts green (en-US registered, Intl MM/DD/YYYY + $). `pnpm i18n:parity` green (en-US fallback-aware, thin en-US.json passes; de/pl/ar strict unchanged). ar RTL unregressed. en-US.json exists.
  </acceptance_criteria>
  <done>en-US selectable + fallback chain + thin override; messages test + i18n:parity green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Locale registration + a build-time parity gate; no runtime input boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-02-01 | Tampering | i18n:parity gate relaxation | mitigate | Fallback-aware mode applies ONLY to en-US; de/pl/ar keep exact-parity semantics (test asserts a strict-peer gap still fails) — the gate is not globally weakened |
| T-84-02-02 | Denial of Service (UX regression) | ar RTL | accept | en-US is ltr; applyLocale dir ternary unchanged for ar — messages test + manual RTL smoke (84-VALIDATION manual row) guard non-regression |
| T-84-SC | Tampering | npm installs | accept | No new deps — i18next/Intl already in tree |
</threat_model>

<verification>
`pnpm --filter @contractor-ops/lint-guards test src/__tests__/i18n-parity.test.ts`, `pnpm --filter @contractor-ops/web-vite test src/i18n/__tests__/messages.test.ts`, `pnpm i18n:parity` all green. Manual: locale switcher shows "English (US)"; ar still RTL (84-VALIDATION manual row).
</verification>

<success_criteria>
en-US registered + selectable + effective full parity via fallback; i18n:parity fallback-aware (thin override passes, strict peers unchanged); Intl en-US date/currency correct; no ar RTL regression.
</success_criteria>

<output>
Create `.planning/phases/84-theme-a-us-contractor-profile-fields-en-us-locale/84-02-SUMMARY.md` when done.
</output>
