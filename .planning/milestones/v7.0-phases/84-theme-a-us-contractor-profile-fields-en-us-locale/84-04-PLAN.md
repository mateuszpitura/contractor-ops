---
phase: 84-theme-a-us-contractor-profile-fields-en-us-locale
plan: 04
type: execute
wave: 2
depends_on: [84-01]
autonomous: true
requirements: [US-FIELD-03]
files_modified:
  - packages/gov-api/src/clients/usps-client.ts
  - packages/gov-api/src/schemas/usps-address.schema.ts
  - packages/gov-api/src/index.ts
must_haves:
  truths:
    - "A US address is normalized/validated via the USPS Addresses 3.0 API (OAuth2 client-credentials + CASS) and returns a verified/unverified flag + the normalized form (US-FIELD-03, D-03)"
    - "The USPS rate-limiter is keyed on a FIXED GLOBAL id (per-credential 60/hr), NOT organizationId (load-bearing pitfall, D-03 correction)"
    - "USPS down / 60-req-hr throttle hit / Redis down → returns unverified WITHOUT throwing to the save path (fail-open, advisory, never blocks save) (D-03)"
    - "The USPS response is parsed through a Zod safeParse boundary; malformed → unverified, no throw (Assumption A2 mitigation)"
  artifacts:
    - path: "packages/gov-api/src/clients/usps-client.ts"
      provides: "UspsAddressClient (mirror hmrc-vat-client)"
      contains: "UspsAddressClient"
    - path: "packages/gov-api/src/schemas/usps-address.schema.ts"
      provides: "Zod token + address response schemas"
  key_links:
    - from: "packages/gov-api/src/clients/usps-client.ts"
      to: "GovApiRateLimiter"
      via: "fixed global identifier, config 60/3_600_000"
      pattern: "usps-global|60"
    - from: "packages/gov-api/src/clients/usps-client.ts"
      to: "usps-address.schema.ts"
      via: "safeParse on the USPS response"
      pattern: "safeParse"
---

<objective>
Build the USPS Addresses 3.0 adapter as a new `UspsAddressClient` subclass of `GovApiClient`
(US-FIELD-03, D-03), mirroring `hmrc-vat-client.ts`: OAuth2 client-credentials + in-memory
token cache + single-flight refresh, a `GovApiRateLimiter` sliding window of 60/hr keyed on a
FIXED GLOBAL id (the cap is per-credential global — per-org bucketing would blow the real
cap), an address-result Redis cache, a Zod `safeParse` boundary, and fail-open advisory
behavior (throttle / Redis-down / USPS-down → `unverified`, never throws to the save path).
No live creds locally (LOCAL-ONLY) → the optional-env + fail-open path yields `unverified`.

Purpose: External gov-API adapter belongs in gov-api (mirrors HMRC/VIES); the orchestration
on-save is wired in Plan 05.
Output: usps-client.ts, usps-address.schema.ts, gov-api index export.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v7.0-phases/84-theme-a-us-contractor-profile-fields-en-us-locale/84-RESEARCH.md
@packages/gov-api/src/clients/hmrc-vat-client.ts
@packages/gov-api/src/rate-limiter.ts

<interfaces>
From packages/gov-api (the template + primitives to reuse):
  class GovApiClient (base — OAuth2 token cache, audit-logger, schemas dir)
  class GovApiRateLimiter (Upstash sliding-window, fail-open when Upstash env unset)
  hmrc-vat-client.ts: OAuth2 client-credentials → token cache (TTL−5min) → single-flight refresh → GovApiRateLimiter → Zod safeParse → typed errors → fail-open

USPS specifics (84-RESEARCH.md:227-239, :399-404, Open Question 1 :420-423, Assumptions A2/A3):
  token:    POST https://apis.usps.com/oauth2/v3/token   (grant_type=client_credentials, ~8h token)
  validate: GET  https://apis.usps.com/addresses/v3/address?streetAddress=&secondaryAddress=&city=&state=&ZIPCode=&ZIPPlus4=
  rate-limit identifier: a FIXED key e.g. 'usps-global' (NOT orgId) ; config { maxRequests:60, windowMs:3_600_000 }
  response additionalInfo: DPVConfirmation/business/vacant/centralDeliveryPoint (field-name spelling [ASSUMED] — fetch developers.usps.com/api/81 OpenAPI at impl time to pin; safeParse makes drift fail-loud in the adapter test)
  optional env: USPS_CLIENT_ID / USPS_CLIENT_SECRET (Plan 01; absent locally → fail-open unverified)

Return contract consumed by Plan 05:
  validateAddress(input) → { verified: boolean, normalized?: { streetAddress, city, state, ZIPCode, ZIPPlus4 }, status: 'verified'|'unverified'|'unavailable' }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: USPS Zod schemas + token/validate client</name>
  <read_first>
    - packages/gov-api/src/clients/hmrc-vat-client.ts (full — OAuth2 client-credentials, token cache TTL−5min, single-flight refresh, safeParse boundary, typed errors, fail-open; the exact template)
    - packages/gov-api/src/rate-limiter.ts (GovApiRateLimiter API — identifier + config; fail-open semantics)
    - packages/gov-api/src/index.ts (export block to extend)
    - 84-RESEARCH.md:227-239 (Pattern 3 USPS deltas), :306-309 (Pitfall 4 global limiter), :420-423 (OpenAPI field-pinning), Environment Availability :437-447 (fail-open when creds/Upstash unset)
  </read_first>
  <behavior>
    - token fetched once + cached across two validateAddress calls (single-flight)
    - validateAddress returns { verified:true, normalized } on a CASS-confirmed response
    - 60/hr GLOBAL self-throttle → { verified:false, status:'unavailable' } WITHOUT throwing
    - Redis/limiter failure → fail-open (allows) and returns unverified, no throw
    - second validate of the same raw address hits the Redis cache (no second fetch)
    - malformed USPS JSON → safeParse fails → { verified:false }, no throw
    - missing USPS creds (LOCAL-ONLY) → returns unverified, never crashes
  </behavior>
  <action>
    Create usps-address.schema.ts: Zod schemas for the OAuth token response and the `/addresses/v3/address` response (address + `additionalInfo`); at implementation time fetch the official OpenAPI at developers.usps.com/api/81 (JSON spec link) to pin exact field-name casing — the `safeParse` boundary makes a wrong guess fail-loud in the adapter test, not in prod (Assumption A2). Create usps-client.ts: `UspsAddressClient extends GovApiClient` mirroring hmrc-vat-client — OAuth2 client-credentials token cache (TTL−5min, single-flight refresh), a `GovApiRateLimiter` with a FIXED GLOBAL identifier (e.g. 'usps-global') and config `{ maxRequests: 60, windowMs: 3_600_000 }` (NEVER orgId — Pitfall 4), an Upstash-Redis address-result cache keyed by `sha256(raw normalized-input string)`, a `validateAddress(input)` returning `{ verified, normalized?, status }`. Fail-open everywhere: self-throttle / 5xx / network error / Redis-down / missing creds → return `{ verified:false, status:'unavailable'|'unverified' }`, never throw to the caller (D-03 advisory). Use `@contractor-ops/logger`, not console. Export `UspsAddressClient` (+ types) from gov-api index.ts. The Plan 00 RED usps-client.test.ts turns GREEN here.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/gov-api test src/clients/__tests__/usps-client.test.ts && pnpm --filter @contractor-ops/gov-api typecheck</automated>
  </verify>
  <acceptance_criteria>
    usps-client.test.ts (Plan 00) green: token cache, GLOBAL 60/hr self-throttle → unverified (no throw), Redis-down fail-open, cache hit, safeParse-malformed → unverified. Rate-limiter keyed on a fixed global id, not orgId. gov-api typecheck clean. Exported.
  </acceptance_criteria>
  <done>UspsAddressClient + schemas shipped, fail-open + global limiter; Plan 00 USPS test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| app → USPS API | outbound to a third-party gov API over OAuth2; response is untrusted until safeParse |
| shared 60/hr USPS quota | a per-credential GLOBAL cap shared across all orgs |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-04-01 | Denial of Service | USPS 60/hr cap blowout | mitigate | GovApiRateLimiter keyed on a FIXED GLOBAL id (not orgId) so N orgs cannot each claim 60/hr (Pitfall 4); address-result cache amortizes; 429/throttle → advisory unverified |
| T-84-04-02 | Tampering | malformed USPS response | mitigate | Zod safeParse boundary; field-names pinned from the official OpenAPI; parse failure → unverified, never an unsafe `as` |
| T-84-04-03 | Denial of Service | USPS down / save-path coupling | mitigate | Fail-open advisory: USPS-down/Redis-down/throttle → unverified, never throws to save (D-03 — USPS NEVER blocks save) |
| T-84-04-04 | Information Disclosure | address sent to USPS | accept | Only the address itself crosses (no SSN/EIN/PII beyond the address); LOCAL-ONLY has no live creds (fail-open) |
| T-84-SC | Tampering | npm installs | accept | No new deps — @upstash/ratelimit + @upstash/redis already gov-api deps (84-RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
`pnpm --filter @contractor-ops/gov-api test src/clients/__tests__/usps-client.test.ts` green; `pnpm --filter @contractor-ops/gov-api typecheck` clean. Real USPS round-trip is manual-UAT post-deploy (84-VALIDATION manual row).
</verification>

<success_criteria>
UspsAddressClient validates/normalizes via USPS 3.0, GLOBAL-keyed 60/hr limiter, fail-open advisory (never blocks save), safeParse boundary; works unverified with no creds locally.
</success_criteria>

<output>
Create `.planning/phases/84-theme-a-us-contractor-profile-fields-en-us-locale/84-04-SUMMARY.md` when done.
</output>
