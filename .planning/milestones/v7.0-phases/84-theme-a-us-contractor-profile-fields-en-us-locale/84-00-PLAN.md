---
phase: 84-theme-a-us-contractor-profile-fields-en-us-locale
plan: 00
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [US-FIELD-01, US-FIELD-02, US-FIELD-03, US-FIELD-04, US-LOC-01]
files_modified:
  - packages/validators/src/__tests__/us-validators.test.ts
  - packages/api/src/services/__tests__/ssn-crypto.test.ts
  - packages/api/src/routers/core/__tests__/contractor-reveal-ssn.test.ts
  - packages/gov-api/src/clients/__tests__/usps-client.test.ts
  - apps/web-vite/src/components/contractors/__tests__/country-compliance-us.test.tsx
  - apps/web-vite/src/components/contractors/compliance/__tests__/ssn-masked-reveal.test.tsx
must_haves:
  truths:
    - "Every Wave 0 RED test file from 84-VALIDATION.md exists and runs (red until its implementation lands)"
    - "No three consecutive downstream tasks lack an automated verify — the scaffolds give every later task a target"
  artifacts:
    - path: "packages/validators/src/__tests__/us-validators.test.ts"
      provides: "EIN/SSN validator vectors (US-FIELD-01/02)"
    - path: "packages/api/src/services/__tests__/ssn-crypto.test.ts"
      provides: "SSN encrypt/decrypt round-trip + last4 (US-FIELD-02)"
    - path: "packages/api/src/routers/core/__tests__/contractor-reveal-ssn.test.ts"
      provides: "reveal RBAC + audit + staff-router-only (US-FIELD-02, D-02/D-09)"
    - path: "packages/gov-api/src/clients/__tests__/usps-client.test.ts"
      provides: "USPS adapter throttle/fail-open/cache/safeParse (US-FIELD-03, D-03)"
    - path: "apps/web-vite/src/components/contractors/__tests__/country-compliance-us.test.tsx"
      provides: "US dispatch render (US-FIELD-04, D-06)"
    - path: "apps/web-vite/src/components/contractors/compliance/__tests__/ssn-masked-reveal.test.tsx"
      provides: "SSN masked/reveal/absent/loading/error states (US-FIELD-02/04, UI-SPEC §B)"
  key_links: []
---

<objective>
Lay the Wave 0 RED test scaffolds enumerated in 84-VALIDATION.md so every downstream
implementation task has a concrete automated target. This satisfies the Nyquist contract
(no three consecutive tasks without an automated verify) and locks the behavioral
contracts for the EIN/SSN validators, SSN crypto, the reveal RBAC procedure, the USPS
adapter, and the two web-vite component surfaces.

Purpose: Tests describe expected behavior before implementation; later plans turn them GREEN.
Output: Six new test files (RED — assertions reference not-yet-existing exports/components).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/84-theme-a-us-contractor-profile-fields-en-us-locale/84-VALIDATION.md
@.planning/milestones/v7.0-phases/84-theme-a-us-contractor-profile-fields-en-us-locale/84-RESEARCH.md
@.planning/milestones/v7.0-phases/84-theme-a-us-contractor-profile-fields-en-us-locale/84-UI-SPEC.md

<interfaces>
<!-- Contracts the scaffolds assert against. Executor writes tests to these shapes;
     implementation plans (01-05) make them real. -->

From packages/validators/src/us-validators.ts (Plan 01 — to be created):
  export function isValidEin(raw: string): boolean   // XX-XXXXXXX + IRS prefix table
  export function isValidSsn(raw: string): boolean   // format + invalid-range exclusion

From packages/api/src/services/ssn-crypto.ts (Plan 02 — to be created):
  export function encryptSsn(ssn: string): string    // iv:authTag:ciphertext, SSN_ENCRYPTION_KEY
  export function decryptSsn(encrypted: string): string

From packages/api/src/routers/core/contractor.ts (Plan 03 — revealSsn procedure):
  contractor.revealSsn({ contractorId }) → { ssn } ; requires contractorPii:['read'] else FORBIDDEN ; writes audit row action='contractor.ssn.revealed'

From packages/gov-api/src/clients/usps-client.ts (Plan 04 — to be created):
  class UspsAddressClient { validateAddress(input) → { verified: boolean, normalized?, status } }  // fail-open

From apps/web-vite/src/components/contractors/compliance/us-compliance-fields.tsx (Plan 05):
  <UsComplianceFields values onChange contractorId />  ; <SsnMaskedReveal contractorId last4 canReveal />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold validators + crypto RED tests</name>
  <read_first>
    - packages/validators/src/__tests__/uk-validators.test.ts (table-driven vector pattern to mirror)
    - packages/validators/src/__tests__/country-fields.test.ts (existing — DO NOT recreate; Plan 01 extends it, this task only adds the US-validator file)
    - packages/api/src/services/bank-account-crypto.ts:1-56 (encrypt/decrypt round-trip shape ssn-crypto mirrors)
    - 84-RESEARCH.md:323-353 (isValidEin / isValidSsn reference vectors + invalid ranges) and :462-466 (test map rows)
  </read_first>
  <files>packages/validators/src/__tests__/us-validators.test.ts, packages/api/src/services/__tests__/ssn-crypto.test.ts</files>
  <action>
    Create us-validators.test.ts importing `isValidEin`/`isValidSsn` from '../us-validators.js' (file does not exist yet — RED). Table-driven vectors mirroring uk-validators.test.ts: EIN accepts `12-3456789` with a valid IRS prefix (e.g. 12), rejects bad format (`123456789` without proper grouping that the impl rejects), rejects a retired/invalid 2-digit prefix (use one NOT in the RESEARCH VALID_EIN_PREFIXES set, e.g. `07`), strips whitespace. SSN accepts a valid `078-05-1120`-style number, rejects area `000`/`666`/`900-999`, group `00`, serial `0000`, and bad format. Create ssn-crypto.test.ts importing `encryptSsn`/`decryptSsn` from '../../services/ssn-crypto.js' (RED): assert round-trip equality, that ciphertext is `iv:authTag:ciphertext` (3 colon-parts), that two encrypts of the same SSN differ (random IV), and a last4-derivation helper expectation (`cleaned.slice(-4)`). Set a dev `SSN_ENCRYPTION_KEY` in the test env shim the same way ssn-crypto's siblings do (mirror how bank-account-crypto tests provide BANK_ACCOUNT_ENCRYPTION_KEY). No fenced implementations in tests beyond the assertions. These files MUST be RED on run (missing imports).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/validators test src/__tests__/us-validators.test.ts 2>&1 | grep -qiE "cannot find|failed|error" && echo "RED-OK" ; pnpm --filter @contractor-ops/api test src/services/__tests__/ssn-crypto.test.ts 2>&1 | grep -qiE "cannot find|failed|error" && echo "RED-OK"</automated>
  </verify>
  <acceptance_criteria>
    Both test files exist; both fail to resolve their target import (RED). Vectors cover every US-FIELD-01/02 row in 84-VALIDATION.md (EIN format+prefix, SSN format+all invalid ranges, crypto round-trip + random-IV + last4).
  </acceptance_criteria>
  <done>us-validators.test.ts + ssn-crypto.test.ts committed, RED, vectors complete.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold reveal-RBAC + USPS adapter RED tests</name>
  <read_first>
    - packages/api/src/routers/core/__tests__ (an existing contractor router test for the mocked-ctx + tenantProcedure caller pattern; mirror it)
    - packages/api/src/routers/core/contractor.ts:1334-1381 (getCountryFields/updateCountryFields shape; revealSsn slots beside these)
    - packages/api/src/middleware/rbac.ts:19-61 (requirePermission → FORBIDDEN path the test asserts)
    - packages/api/src/services/audit-writer.ts (writeAuditLog signature the reveal asserts is called with action='contractor.ssn.revealed', no SSN in metadata)
    - packages/gov-api/src/clients/hmrc-vat-client.ts (token-cache + rate-limit + fail-open + safeParse shape the USPS test asserts) and 84-RESEARCH.md:227-239, :469
  </read_first>
  <files>packages/api/src/routers/core/__tests__/contractor-reveal-ssn.test.ts, packages/gov-api/src/clients/__tests__/usps-client.test.ts</files>
  <action>
    Create contractor-reveal-ssn.test.ts (RED): with a mocked ctx, assert `contractor.revealSsn` (1) throws FORBIDDEN when the caller lacks `contractorPii:['read']`, (2) returns `{ ssn }` and calls `writeAuditLog` with `action:'contractor.ssn.revealed'`, `resourceType:'CONTRACTOR'`, and metadata that NEVER contains the raw SSN value (assert the audit metadata has no field equal to the decrypted SSN), (3) scopes the contractor query by `organizationId` (cross-tenant → NOT_FOUND), (4) is registered on the staff `contractor` router, not any portal router (assert the procedure key exists on the staff router export). Create usps-client.test.ts (RED) with mocked `fetch` + mocked Redis/limiter: assert token cache reuse (single token fetch across two validate calls), 60/hr GLOBAL self-throttle returns `{ verified:false }` without throwing (D-03 fail-open), Redis-down/limiter-failure fails open (allows, returns unverified — never throws to the save path), an address-cache hit avoids a second fetch, and that a malformed USPS response is caught by `safeParse` (returns unverified, no throw). Reference the not-yet-existing `UspsAddressClient` import (RED).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test src/routers/core/__tests__/contractor-reveal-ssn.test.ts 2>&1 | grep -qiE "cannot find|failed|error" && echo "RED-OK" ; pnpm --filter @contractor-ops/gov-api test src/clients/__tests__/usps-client.test.ts 2>&1 | grep -qiE "cannot find|failed|error" && echo "RED-OK"</automated>
  </verify>
  <acceptance_criteria>
    Both test files exist and are RED. Reveal test covers D-02/D-09 RBAC (FORBIDDEN without perm), audit (no SSN in row), tenant scoping, staff-router-only. USPS test covers token cache, global throttle fail-open, Redis-down fail-open, cache hit, safeParse — every US-FIELD-03 row in 84-VALIDATION.md.
  </acceptance_criteria>
  <done>contractor-reveal-ssn.test.ts + usps-client.test.ts committed, RED, coverage complete.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold web-vite component RED tests (path-scoped)</name>
  <read_first>
    - apps/web-vite/src/components/contractors/__tests__ (existing country-compliance test if any; mirror the jsdom + mocked-tRPC-hook render pattern)
    - apps/web-vite/src/components/contractors/country-compliance-section.tsx:221-244 (CountryFieldsDispatch switch the US-dispatch test renders)
    - apps/web-vite/src/components/contractors/vat-validation-status-pill.tsx (pill render-state pattern)
    - 84-UI-SPEC.md §B (SSN masked/reveal-available/reveal-absent/revealed/loading/error states) and §A (dispatch case 'US') ; 84-VALIDATION.md:51-52
  </read_first>
  <files>apps/web-vite/src/components/contractors/__tests__/country-compliance-us.test.tsx, apps/web-vite/src/components/contractors/compliance/__tests__/ssn-masked-reveal.test.tsx</files>
  <action>
    Create country-compliance-us.test.tsx (RED): render `CountryFieldsDispatch` (or the view) with `countryCode='US'` and assert `UsComplianceFields` (not-yet-existing) renders — EIN input, SSN field, US address block present. Create ssn-masked-reveal.test.tsx (RED) for the `SsnMaskedReveal` component (not-yet-existing) asserting the five states from UI-SPEC §B: masked default shows `•••-••-1234` with the last-4 aria-label and NO full value in the DOM; with `canReveal=false` the reveal control is ABSENT (not disabled); with `canReveal=true` a "Reveal SSN" button renders; clicking it (mocked reveal mutation) shows loading (Loader2) then the full value + "Hide SSN"; a reveal error renders an inline `role="alert"` FieldError and falls back to masked. Mock the tRPC reveal hook. These run path-scoped only.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite test src/components/contractors/__tests__/country-compliance-us.test.tsx 2>&1 | grep -qiE "cannot find|failed|error" && echo "RED-OK" ; pnpm --filter @contractor-ops/web-vite test src/components/contractors/compliance/__tests__/ssn-masked-reveal.test.tsx 2>&1 | grep -qiE "cannot find|failed|error" && echo "RED-OK"</automated>
  </verify>
  <acceptance_criteria>
    Both component test files exist, RED, path-scoped (never the unscoped web-vite suite). They assert the US dispatch render and all five SSN states from UI-SPEC §B including the reveal-absent (control not rendered) and the no-full-value-in-DOM invariants.
  </acceptance_criteria>
  <done>country-compliance-us.test.tsx + ssn-masked-reveal.test.tsx committed, RED, UI-SPEC §B states covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Wave 0 adds only test files — no runtime surface, no input crosses a boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-00-01 | Information Disclosure | test fixtures | mitigate | Use synthetic EIN/SSN vectors only (never a real SSN); reveal test asserts the SSN value is absent from the audit-metadata assertion fixture |
| T-84-SC | Tampering | npm/pip installs | accept | No installs this phase — Vitest present in every target package (84-RESEARCH Package Legitimacy Audit: no new install surface) |
</threat_model>

<verification>
All six files exist and are RED. Per-file scoped commands above. web-vite path-scoped only.
</verification>

<success_criteria>
Six RED test files committed; each fails on a missing target import/component; vectors/assertions map 1:1 to the 84-VALIDATION.md per-task rows.
</success_criteria>

<output>
Create `.planning/phases/84-theme-a-us-contractor-profile-fields-en-us-locale/84-00-SUMMARY.md` when done.
</output>
