---
phase: 84-theme-a-us-contractor-profile-fields-en-us-locale
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [US-FIELD-01, US-FIELD-02]
files_modified:
  - packages/validators/src/us-validators.ts
  - packages/validators/src/country-fields.ts
  - packages/validators/src/index.ts
  - packages/validators/src/env.ts
  - packages/validators/src/minimal-server-env.ts
  - packages/validators/src/__tests__/country-fields.test.ts
  - .env.example
must_haves:
  truths:
    - "An EIN is accepted only in XX-XXXXXXX format with a valid IRS prefix; a retired/invalid prefix is rejected (D-05)"
    - "An SSN is accepted only in XXX-XX-XXXX format outside the invalid ranges (area 000/666/900-999, group 00, serial 0000) (D-05)"
    - "usCountryFieldsSchema parses US country-fields (entityType + EIN, NOT SSN) and is registered in countryFieldsSchemaMap (D-05)"
    - "SSN_ENCRYPTION_KEY (hex-32) is a required server-env var so the SSN write path never throws on an unset key (D-01)"
  artifacts:
    - path: "packages/validators/src/us-validators.ts"
      provides: "isValidEin + isValidSsn pure validators"
      exports: ["isValidEin", "isValidSsn"]
    - path: "packages/validators/src/country-fields.ts"
      provides: "usCountryFieldsSchema + US in countryFieldsSchemaMap"
      contains: "usCountryFieldsSchema"
    - path: "packages/validators/src/env.ts"
      provides: "SSN_ENCRYPTION_KEY (hex32) + optional USPS creds"
      contains: "SSN_ENCRYPTION_KEY"
  key_links:
    - from: "packages/validators/src/country-fields.ts"
      to: "packages/validators/src/us-validators.ts"
      via: "import isValidEin in usCountryFieldsSchema .refine"
      pattern: "isValidEin"
    - from: "packages/validators/src/country-fields.ts"
      to: "countryFieldsSchemaMap"
      via: "US: usCountryFieldsSchema entry"
      pattern: "US:\\s*usCountryFieldsSchema"
---

<objective>
Ship the US field validators and schema: `isValidEin` (XX-XXXXXXX + IRS prefix table) and
`isValidSsn` (format + invalid-range exclusion) per D-05, the `usCountryFieldsSchema`
registered in `countryFieldsSchemaMap`, and the new `SSN_ENCRYPTION_KEY` env var (hex-32,
separate from the bank key per the D-01 crypto correction) plus optional USPS creds. SSN
itself is NOT in the JSONB schema — it has dedicated encrypted columns (Plan 02/03); the
schema carries entityType + EIN + US address fields only.

Purpose: Pure validators run client + server (same as uk/de-validators); the env keys must
exist before any code reads `getServerEnv().SSN_ENCRYPTION_KEY`.
Output: us-validators.ts, country-fields.ts edit, env edits, .env.example, country-fields test extension.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v7.0-phases/84-theme-a-us-contractor-profile-fields-en-us-locale/84-RESEARCH.md
@.planning/milestones/v7.0-phases/84-theme-a-us-contractor-profile-fields-en-us-locale/84-UI-SPEC.md
@packages/validators/src/uk-validators.ts
@packages/validators/src/country-fields.ts

<interfaces>
From packages/validators/src/country-fields.ts (the map + validate fn to extend):
  export const countryFieldsSchemaMap: Record<string, z.ZodTypeAny> = { AE, SA, GB, DE }
  export function validateCountryFields(countryCode, fields): Record<string, unknown>

From packages/validators/src/env.ts:184 — pattern: `BANK_ACCOUNT_ENCRYPTION_KEY: hex32,`
From packages/validators/src/minimal-server-env.ts:55 — pattern: `BANK_ACCOUNT_ENCRYPTION_KEY: HEX32,`

IRS valid-prefix set + SSN invalid-range rules: 84-RESEARCH.md:327-353 (annotate legal-deferred).
US entity-type enum: derive from UI-SPEC §A field list + 84-RESEARCH.md:359 (SOLE_PROPRIETOR/LLC/C_CORP/S_CORP/PARTNERSHIP/INDIVIDUAL) — confirm against the UI-SPEC dropdown; UPPER_SNAKE_CASE (uk/de convention).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: us-validators.ts (isValidEin + isValidSsn)</name>
  <read_first>
    - packages/validators/src/uk-validators.ts:1-50 (isValidUtr structure: strip whitespace/hyphens, anchored regex, pure, no I/O — mirror exactly)
    - 84-RESEARCH.md:327-353 (VALID_EIN_PREFIXES set + SSN invalid-range rules, both CITED + legal-deferred)
    - packages/validators/src/__tests__/us-validators.test.ts (the RED scaffold from Plan 00 — this turns it GREEN)
  </read_first>
  <behavior>
    - isValidEin('12-3456789') → true (valid IRS prefix 12)
    - isValidEin('07-1234567') → false (07 not in VALID_EIN_PREFIXES per RESEARCH)
    - isValidEin('123456789') → true/false per the impl's accepted grouping; reject anything not matching ^(\d{2})-?(\d{7})$
    - isValidSsn('078-05-1120') → true ; area 000/666/900-999 → false ; group 00 → false ; serial 0000 → false ; bad format → false
    - both strip whitespace/hyphens before validating; anchored regex (no ReDoS)
  </behavior>
  <action>
    Create us-validators.ts mirroring uk-validators.ts. Export `isValidEin(raw)`: a `VALID_EIN_PREFIXES` Set from the RESEARCH list, match `^(\d{2})-?(\d{7})$` after stripping whitespace, return prefix ∈ set. Export `isValidSsn(raw)`: strip `[\s-]`, match `^(\d{3})(\d{2})(\d{4})$`, reject area `=== 0 || === 666 || >= 900`, group `=== '00'`, serial `=== '0000'`. Annotate the EIN-prefix table and SSN-range rules with a comment: "Table accuracy needs verification by a jurisdiction-specific legal/tax adviser before production deploy (LOCAL-ONLY)". No live lookup. Re-export from packages/validators/src/index.ts (mirror the uk-validators re-export block at index.ts:710).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/validators test src/__tests__/us-validators.test.ts</automated>
  </verify>
  <acceptance_criteria>
    us-validators.test.ts (Plan 00) passes GREEN. Both validators pure, anchored, whitespace/hyphen-tolerant, legal-deferred-annotated. Exported from index.ts.
  </acceptance_criteria>
  <done>isValidEin + isValidSsn shipped + re-exported; Plan 00 validator test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: usCountryFieldsSchema + env keys</name>
  <read_first>
    - packages/validators/src/country-fields.ts:44-94 (ukCountryFieldsSchema + superRefine conditional pattern) and :224-242 (countryFieldsSchemaMap + validateCountryFields)
    - packages/validators/src/env.ts:1-10, :180-190 (hex32 schema + BANK_ACCOUNT_ENCRYPTION_KEY) and minimal-server-env.ts:1-10, :50-60 (HEX32 + BANK_ACCOUNT_ENCRYPTION_KEY)
    - packages/validators/src/__tests__/country-fields.test.ts (existing — EXTEND, do not recreate)
    - 84-RESEARCH.md:356-371 (usCountryFieldsSchema shape — SSN NOT in JSONB) and Open Question 3 :430-433 (entity-type → EIN-vs-SSN conditional)
  </read_first>
  <behavior>
    - usCountryFieldsSchema.parse({ entityType:'LLC', ein:'12-3456789', addressLine1, city, state:'CA', zipCode }) → ok
    - invalid EIN → ZodError on `ein` path
    - state longer than 2 chars → ZodError (.length(2))
    - SSN is absent from the schema entirely (assert no `ssn` key in the parsed shape)
    - entity-type conditional EIN requirement mirrors the uk superRefine (LLC/C_CORP/S_CORP/PARTNERSHIP require EIN; INDIVIDUAL/SOLE_PROPRIETOR may omit) — confirm the matrix against the UI-SPEC
  </behavior>
  <action>
    Add `usEntityTypeEnum` (UPPER_SNAKE_CASE values confirmed against UI-SPEC §A; per 84-RESEARCH SOLE_PROPRIETOR/LLC/C_CORP/S_CORP/PARTNERSHIP/INDIVIDUAL) and `usCountryFieldsSchema` to country-fields.ts mirroring ukCountryFieldsSchema: `entityType`, `ein` (`.refine(v => !v || isValidEin(v), 'Invalid EIN').optional()`), `addressLine1`/`city`/`zipCode` optional strings, `state` `z.string().length(2).optional()`, `uspsVerified z.boolean().optional()`. A `superRefine` requiring EIN for entity types that need it (mirror the uk SOLE_TRADER→UTR conditional). Import isValidEin from us-validators.js. Register `US: usCountryFieldsSchema` in `countryFieldsSchemaMap`. Export `usCountryFieldsSchema` + `UsCountryFields` type + `usEntityTypeEnum` from index.ts. Add `SSN_ENCRYPTION_KEY: hex32` to env.ts (beside BANK_ACCOUNT_ENCRYPTION_KEY:184) + `SSN_ENCRYPTION_KEY: HEX32` to minimal-server-env.ts (:55); add optional `USPS_CLIENT_ID`/`USPS_CLIENT_SECRET` as `z.string().optional()` (LOCAL-ONLY no creds). Add `SSN_ENCRYPTION_KEY` (with a generated dev hex-32 value) + `USPS_CLIENT_ID`/`USPS_CLIENT_SECRET` placeholders to .env.example. Extend country-fields.test.ts with the US-parse vectors above.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/validators test src/__tests__/country-fields.test.ts && pnpm --filter @contractor-ops/validators typecheck</automated>
  </verify>
  <acceptance_criteria>
    country-fields.test.ts US vectors pass (valid parse, invalid EIN, state length, SSN absent, entity-type conditional). `US` in countryFieldsSchemaMap. SSN_ENCRYPTION_KEY required in both env schemas; USPS creds optional; .env.example updated. validators typecheck clean.
  </acceptance_criteria>
  <done>usCountryFieldsSchema registered, env keys added, country-fields test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client/server → usCountryFieldsSchema | untrusted US field input parsed at the tRPC boundary (Plan 03) + RHF resolver (Plan 05) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01-01 | Tampering | usCountryFieldsSchema | mitigate | Strict Zod: `state.length(2)`, EIN `.refine(isValidEin)`, entity-type enum; SSN deliberately excluded so it cannot be mass-assigned through the JSONB path (D-01) |
| T-84-01-02 | Information Disclosure | SSN_ENCRYPTION_KEY | mitigate | Separate hex-32 key from BANK_ACCOUNT_ENCRYPTION_KEY (blast-radius separation, D-01); required-in-schema so an unset key fails loud at boot, never silently stores plaintext |
| T-84-01-03 | Tampering | ReDoS on EIN/SSN regex | mitigate | Anchored `^...$` regex on whitespace-stripped input (mirror uk-validators T-56-03) |
| T-84-SC | Tampering | npm installs | accept | No new deps — zod already in tree |
</threat_model>

<verification>
`pnpm --filter @contractor-ops/validators test src/__tests__/us-validators.test.ts src/__tests__/country-fields.test.ts` green; `pnpm --filter @contractor-ops/validators typecheck` clean. `pnpm check:no-process-env` if env-access files touched.
</verification>

<success_criteria>
isValidEin/isValidSsn correct per D-05 vectors; usCountryFieldsSchema registered (US) with SSN excluded; SSN_ENCRYPTION_KEY required + USPS creds optional in both env schemas + .env.example; legal-deferred annotations present.
</success_criteria>

<output>
Create `.planning/phases/84-theme-a-us-contractor-profile-fields-en-us-locale/84-01-SUMMARY.md` when done.
</output>
