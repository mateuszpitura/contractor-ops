---
phase: 86-theme-a-tin-match-1099-nec-iris-e-file-state-filing
plan: 05
type: execute
wave: 2
depends_on: [86-02]
files_modified:
  - packages/api/src/services/form-1099-nec.service.ts
  - packages/api/src/services/form-1099-nec-pdf.ts
  - packages/api/src/pdf-templates/form-1099-nec-copy-b.tsx
  - packages/api/src/services/__tests__/form-1099-nec.service.test.ts
  - packages/api/src/pdf-templates/__tests__/form-1099-nec-copy-b.test.tsx
autonomous: true
requirements: [US-FORM-04]
must_haves:
  truths:
    - "Box-1 nonemployee comp is aggregated by payment/settlement date within the calendar tax year, FX-converted to USD at the payment-date rate, per recipient per payer-org"
    - "Generation is gated by the tax-year-keyed Tax1099Threshold table ($600 TY2025 / $2,000 TY2026), never a constant"
    - "Box-4 backup withholding is populated when the recipient's W-9 backup-withholding flag is set or a TIN mismatch exists (records what is recorded; the 24% reduction is Phase 88)"
    - "A CORRECTED 1099 inserts a new immutable Form1099Nec row that supersedes the prior ACTIVE row inside one $transaction — the original is never mutated"
    - "The recipient Copy-B PDF renders via @react-pdf/renderer renderToBuffer from the stored immutable snapshot, shows TIN last-4 only, carries the adviser-verify annotation, and is archived to the US R2 tax bucket"
    - "Batch generation uses idempotency reserve/complete/clear so a retried batch never double-files; sign/correct actions are audit-logged"
    - "Implements D-06 (aggregation basis + threshold table), D-07 (box-4), D-08 (CORRECTED=supersede), D-09 (Copy-B PDF + archive), D-18 (adviser-verify), D-19 (audit + idempotency)"
  artifacts:
    - path: "packages/api/src/services/form-1099-nec.service.ts"
      provides: "batch aggregation (payment-date, FX, per recipient/payer-org) + threshold gate + CORRECTED supersede"
      contains: "supersede"
    - path: "packages/api/src/pdf-templates/form-1099-nec-copy-b.tsx"
      provides: "react-pdf Copy-B substitute template (last-4 TIN, Pub 1179, adviser note)"
      contains: "Form1099NecCopyB"
    - path: "packages/api/src/services/form-1099-nec-pdf.ts"
      provides: "lazy renderToBuffer + R2 archive (US tax bucket)"
      contains: "renderToBuffer"
  key_links:
    - from: "packages/api/src/services/form-1099-nec.service.ts"
      to: "Tax1099Threshold"
      via: "tax-year-keyed threshold lookup gates generation"
      pattern: "tax1099Threshold|Tax1099Threshold"
    - from: "packages/api/src/services/form-1099-nec.service.ts"
      to: "exchange-rate.ts"
      via: "FX-convert non-USD payouts at payment-date rate"
      pattern: "convertAmount|getRate"
    - from: "packages/api/src/services/form-1099-nec-pdf.ts"
      to: "@react-pdf/renderer"
      via: "lazy renderToBuffer of the Copy-B template"
      pattern: "renderToBuffer"
---

<objective>
Build the 1099-NEC generation engine (US-FORM-04): batch aggregation of box-1 by payment/settlement
date (FX-converted to USD at the payment-date rate, per recipient per payer-org), the tax-year-keyed
threshold gate ($600 TY2025 / $2,000 TY2026), box-4 backup-withholding population, the CORRECTED =
supersede chain (mirror `tax-form.service.ts`), and the recipient Copy-B PDF (`@react-pdf/renderer@4.5.1`
`renderToBuffer`, Pub 1179 black-ink substitute, last-4 TIN, adviser-verify) archived to the US R2 tax
bucket. Batch generation is idempotent (reserve/complete/clear) and audit-logged.

Purpose: This is deterministic published-threshold computation (like reverse-charge / treaty lookup),
not a classification verdict. Turns the Plan 01 service + PDF RED scaffolds GREEN. The recorded
backup-withholding flag is read by Phase 88 to actually reduce payout 24%.
Output: the 1099 batch service + Copy-B template + the PDF render/archive service.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-PATTERNS.md
@.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-RESEARCH.md

<interfaces>
<!-- CORRECTED = supersede, verbatim from tax-form.service.ts. -->
From packages/api/src/services/tax-form.service.ts (supersedeAndInsert L181-223; buildFormSnapshot L108; sanitizeFields L76; FORBIDDEN_FIELD_KEYS L69):
  await tx.taxFormSubmission.updateMany({ where: { ...status:'ACTIVE' }, data:{ status:'SUPERSEDED' } });
  await tx.taxFormSubmission.create({ data:{ ...status:'ACTIVE', snapshotJson } });   // supersede BEFORE insert, one $transaction
  // sanitizeFields strips any ssn/fullTin key → snapshot keeps last-4 only
From packages/api/src/services/exchange-rate.ts: getRate(...) , convertAmount(...) — payment-date FX source (reuse).
From packages/api/src/services/late-payment-claim-pdf.ts (L85-109): const { renderToBuffer } = await import('@react-pdf/renderer'); ... R2 key `<feature>/<orgId>/<id>.pdf`; CAS guard updateMany where status='PENDING_RENDER'.
From packages/api/src/lib/idempotency.ts (L90-149): reserve<Result>(key,ttl) → MISS|PENDING|HIT; complete(key,result,ttl); clear(key).
From packages/validators/src/us-validators.ts: isValidEin, isValidSsn (reuse).
@react-pdf/renderer is 4.5.1 (NOT 3.4.5 — CONTEXT stale).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: form-1099-nec.service — aggregation + threshold gate + box-4 + CORRECTED supersede + idempotency</name>
  <files>packages/api/src/services/form-1099-nec.service.ts, packages/api/src/services/__tests__/form-1099-nec.service.test.ts</files>
  <read_first>
    - packages/api/src/services/__tests__/form-1099-nec.service.test.ts — the Plan 01 RED scaffold this turns GREEN (aggregation / threshold / supersede assertions).
    - packages/api/src/services/tax-form.service.ts — `supersedeAndInsert` (L181-223) supersede-before-insert in one `$transaction`; `buildFormSnapshot`/`sanitizeFields`/`FORBIDDEN_FIELD_KEYS` (L69-108) — the last-4-only snapshot sanitizer.
    - packages/api/src/services/exchange-rate.ts — `getRate`/`convertAmount` for payment-date FX (reuse; do NOT add a new rate fetcher).
    - packages/api/src/lib/idempotency.ts (L90-149) — `reserve`/`complete`/`clear` for the batch.
    - packages/api/src/services/audit-writer.ts (L61-137) — `writeAuditLog` (pass `tx`).
    - 86-RESEARCH.md D-06 (aggregation basis + threshold table), D-07 (box-4), D-08 (supersede), Pitfall 1 (TY-keyed threshold), Pattern 2.
  </read_first>
  <behavior>
    - `generateBatch({ organizationId, payerOrgId, taxYear })` sums box-1 nonemployee comp by payment/settlement date within the calendar tax year, FX-converts non-USD payouts at the payment-date rate, aggregated per recipient per payer-org.
    - A recipient at/above the tax-year threshold (read from `Tax1099Threshold`: $600 TY2025, $2,000 TY2026) yields a `Form1099Nec`; below threshold does not.
    - Box-4 is populated when the recipient's backup-withholding flag is set or a TIN mismatch exists (records the recorded amount; no 24% reduction here).
    - A CORRECTED generation flips the prior ACTIVE row to SUPERSEDED then inserts a new ACTIVE row inside one `$transaction`; the original row is never updated.
    - A retried batch (same idempotency key) does not create duplicate rows.
  </behavior>
  <action>
    Implement `form-1099-nec.service.ts`: `generateBatch` reads settled payments + `exchange-rate` `convertAmount` to compute box-1 minor (USD) by payment/settlement date per recipient per payer-org (D-06); look up the threshold from `Tax1099Threshold` by `taxYear` (NEVER a constant — Pitfall 1); for recipients at/above threshold, build an immutable `snapshotJson` via a sanitizer mirroring `buildFormSnapshot`/`sanitizeFields` (strip any full-TIN/SSN key → last-4 only) and create a `Form1099Nec` ACTIVE row; populate `box4BackupWithholdingMinor` when the recipient backup-withholding flag is set or a TIN mismatch exists (D-07). Wrap the batch in `idempotency.reserve/complete/clear` (D-19) so a retry never double-files. Implement `fileCorrection` mirroring `supersedeAndInsert`: in one `$transaction`, `updateMany` prior ACTIVE rows for `(organizationId, payerOrgId, recipientId, taxYear)` → SUPERSEDED, then `create` the new ACTIVE row with `corrected: true` (D-08 — never mutate the filed row), and `writeAuditLog({ action: 'form1099.correction', ... }, tx)`. `writeAuditLog` on generate + correct (D-19). Annotate computed figures adviser-verify (D-18) in the snapshot metadata. Update the Plan 01 scaffold GREEN. No `console.*`; no full TIN in any log/snapshot.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- form-1099-nec.service</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test -- form-1099-nec.service` passes (was RED).
    - Tests prove: aggregation by payment date + FX to USD per recipient/payer-org; threshold gate generates at $600 for TY2025 and at $2,000 for TY2026 (and suppresses below); CORRECTED supersedes (prior ACTIVE → SUPERSEDED, new ACTIVE created, original untouched); retried batch does not duplicate.
    - `grep -c "tax1099Threshold\|Tax1099Threshold" packages/api/src/services/form-1099-nec.service.ts` is ≥ 1 (table lookup, not a constant); `grep -Ec "= *60000\b|= *200000\b" packages/api/src/services/form-1099-nec.service.ts` is 0 (no hardcoded threshold constant).
    - `grep -c "convertAmount\|getRate" packages/api/src/services/form-1099-nec.service.ts` is ≥ 1 (reuses the FX service).
    - `grep -c "writeAuditLog" packages/api/src/services/form-1099-nec.service.ts` is ≥ 1; `grep -c "reserve\|complete\|clear" packages/api/src/services/form-1099-nec.service.ts` is ≥ 1 (idempotency).
  </acceptance_criteria>
  <done>The 1099 batch aggregates by payment-date + FX, gates on the tax-year threshold table, populates box-4, supersedes on CORRECTED in one transaction, and is idempotent + audited; Plan 01 scaffold GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Copy-B PDF template + lazy renderToBuffer + R2 archive</name>
  <files>packages/api/src/pdf-templates/form-1099-nec-copy-b.tsx, packages/api/src/services/form-1099-nec-pdf.ts, packages/api/src/pdf-templates/__tests__/form-1099-nec-copy-b.test.tsx</files>
  <read_first>
    - packages/api/src/pdf-templates/__tests__/form-1099-nec-copy-b.test.tsx — the Plan 01 RED render-smoke scaffold this turns GREEN.
    - packages/api/src/pdf-templates/late-payment-claim.tsx (+ ir35-sds.tsx, drv-defense-bundle.tsx) — the react-pdf template structure (Document/Page/View/Text, styles) to mirror.
    - packages/api/src/services/late-payment-claim-pdf.ts (L85-109) — lazy `import('@react-pdf/renderer')` `renderToBuffer`, the `<feature>/<orgId>/<id>.pdf` R2 key shape, and the CAS render guard.
    - 86-RESEARCH.md D-09 (Copy-B only, consent-gated furnishing, archive either way), Pitfall 6 (consent — but furnishing/consent is Plan 06's portal; this plan only renders + archives), D-18 (adviser-verify), Pub 1179 last-4 masking.
  </read_first>
  <behavior>
    - `Form1099NecCopyB(props)` produces a react-pdf Document showing payer/recipient, tax year, box-1 + box-4 amounts, TIN last-4 only, and the adviser-verify footnote.
    - The PDF service renders the template to a non-empty Buffer from the stored immutable snapshot (not a live recompute) and archives it to the US R2 tax bucket; a second render of the same row does not re-render (CAS guard).
  </behavior>
  <action>
    Implement `form-1099-nec-copy-b.tsx` as a react-pdf substitute Copy B (black ink, Pub 1179 §4.6) rendering from the stored snapshot: payer + recipient identity (recipient TIN last-4 ONLY — never full TIN, Pub 1179 masking + the P84/P85 SsnMaskedReveal invariant), tax year, box-1 nonemployee comp, box-4 federal backup withholding, and the D-18 adviser-verify footnote string. Implement `form-1099-nec-pdf.ts` mirroring `late-payment-claim-pdf.ts`: lazy `const { renderToBuffer } = await import('@react-pdf/renderer')` (4.5.1 — keep react-pdf out of cold paths), render from the immutable `Form1099Nec.snapshotJson` ("values AS FILED"), archive to the US R2 tax-archive bucket under `1099-nec/<orgId>/<id>.pdf`, set `pdfArchiveKey`, and use the CAS render guard (`updateMany where status/render-state`) so the render is not double-run. Render Copy B ONLY — never IRS Copy A (that goes via the IRIS XML, Plan 04). Update the Plan 01 render-smoke scaffold GREEN. No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- form-1099-nec-copy-b</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test -- form-1099-nec-copy-b` passes (render-smoke: non-empty Buffer).
    - `grep -c "renderToBuffer" packages/api/src/services/form-1099-nec-pdf.ts` is ≥ 1 and it is a lazy dynamic import.
    - `grep -c "1099-nec/" packages/api/src/services/form-1099-nec-pdf.ts` confirms the R2 archive key shape.
    - The template renders recipient TIN last-4 only (test asserts a full 9-digit TIN never appears in the rendered text) and includes the adviser-verify footnote.
  </acceptance_criteria>
  <done>The Copy-B PDF renders to a buffer from the immutable snapshot (last-4 TIN, adviser-verify), archives to the US R2 bucket, and is not double-rendered; Plan 01 scaffold GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settled payments → aggregation | Money + FX is server-derived domain logic; never client input |
| recipient TIN → snapshot / PDF / log | Full TIN must never enter the snapshot, the rendered PDF, or a log (last-4 only) |
| filed Form1099Nec → CORRECTED | A filed row is append-only; correction supersedes, never updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-05-01 | Tampering | mutating a filed 1099 | mitigate | `fileCorrection` supersedes (updateMany ACTIVE→SUPERSEDED then create) in one `$transaction`; the original row is never updated (D-08, test-asserted). |
| T-86-05-02 | Tampering | double-filing on a retried batch | mitigate | `idempotency.reserve/complete/clear` wraps `generateBatch` (D-19, test-asserted). |
| T-86-05-03 | Information Disclosure | full TIN in snapshot / PDF / log | mitigate | Snapshot sanitizer strips full-TIN keys (mirror `sanitizeFields`); PDF + logs show last-4 only (Pub 1179). |
| T-86-05-04 | Tampering | wrong-year threshold / hardcoded constant | mitigate | Threshold read from `Tax1099Threshold` by tax year; grep gate rejects a hardcoded 60000/200000 constant (Pitfall 1). |
| T-86-05-05 | Repudiation | un-audited generation / correction | mitigate | `writeAuditLog` on generate + correct, passing `tx` (D-19). |
| T-86-05-06 | Spoofing/Tampering | rendering IRS Copy A as a PDF | mitigate | Render Copy B ONLY; Copy A goes via the IRIS XML (Plan 04) — documented in the template. |
| T-86-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (RESEARCH § Package Legitimacy Audit: `@react-pdf/renderer@4.5.1` already installed; zero NEW external deps). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- form-1099-nec.service` + `form-1099-nec-copy-b` green (Plan 01 scaffolds flipped).
- Aggregation by payment-date + FX; threshold from the TY-keyed table; box-4 populated; CORRECTED supersedes; batch idempotent + audited.
- Copy-B PDF renders from the immutable snapshot (last-4 TIN, adviser-verify), archived to the US R2 bucket; never Copy A.
- No full TIN in any snapshot/PDF/log; no hardcoded threshold constant.
</verification>

<success_criteria>
- US-FORM-04 satisfied: tax-year-keyed 1099-NEC generation with CORRECTED support, recipient Copy-B PDF, and an audit-immutable archive.
</success_criteria>

<output>
Create `.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-05-SUMMARY.md` when done.
</output>
