---
phase: 86-theme-a-tin-match-1099-nec-iris-e-file-state-filing
plan: 06
type: execute
wave: 3
depends_on: [86-03, 86-04, 86-05]
files_modified:
  - packages/api/src/services/tax-filing-transmitter.ts
  - packages/api/src/services/state-filing-output.ts
  - packages/api/src/routers/finance/tax-1099-router.ts
  - packages/api/src/routers/portal/portal-tax-1099-router.ts
  - packages/api/src/root.ts
  - packages/api/src/portal-root.ts
  - packages/api/src/services/__tests__/tax-filing-transmitter.test.ts
  - packages/api/src/__tests__/security/tax-filing-tenant-isolation.security.test.ts
  - apps/cron-worker/src/jobs/handlers/year-end-1099-reminder.ts
  - apps/cron-worker/src/jobs/registry.ts
  - apps/cron-worker/src/env.ts
  - .env.example
autonomous: true
requirements: [US-FORM-04, US-FORM-05, US-FORM-07]
must_haves:
  truths:
    - "A TaxFilingTransmitter factory selects ManualDownload (default) | IrisA2A (dark, behind module.iris-efile) | Vendor (stub), with one generation pipeline and a swappable tail (D-03)"
    - "The manual default returns the XSD-validated XML buffer for admin download; the same ack parser consumes the manual-uploaded ack and the A2A poll result (D-04)"
    - "Staff tax-1099 router exposes batch-generate / build-XML / download / upload-ack / escalate / file-correction with assertUsExpansionEnabled first, Zod-strict, idempotent, audited; conditionally spread out of appRouter when the flag is OFF"
    - "Portal tax-1099 router exposes recordEdeliveryConsent / withdrawConsent / downloadCopyB scoped to ctx.contractorId (IDOR), consent server-derived (ip/actorId/timestamp), download gated on stored consent; no consent → paper/manual flag"
    - "Per-state filing emits the CFSF state code for participating states and a downloadable CSV/summary + manual guidance for non-CFSF / direct-file states (D-13/D-14)"
    - "A notify-only year-end cron reminds that the batch is due — it NEVER auto-generates or auto-transmits (D-05)"
    - "A cross-org leak test proves a second org cannot read another org's Form1099Nec/IrisSubmission (D-16)"
    - "Implements D-02 (live A2A built but dark behind module.iris-efile; FIRE doc-only), D-03/D-04/D-05/D-13/D-14/D-15/D-19/A1"
  artifacts:
    - path: "packages/api/src/services/tax-filing-transmitter.ts"
      provides: "factory: ManualDownload (default) | IrisA2A (dark) | Vendor (stub)"
      contains: "ManualDownload"
    - path: "packages/api/src/routers/finance/tax-1099-router.ts"
      provides: "staff batch/filing/TIN-match procedures, flag-gated + audited + idempotent"
      contains: "assertUsExpansionEnabled"
    - path: "packages/api/src/routers/portal/portal-tax-1099-router.ts"
      provides: "portal consent + Copy-B download scoped to ctx.contractorId"
      contains: "ctx.contractorId"
    - path: "apps/cron-worker/src/jobs/handlers/year-end-1099-reminder.ts"
      provides: "notify-only year-end reminder (never auto-files)"
      contains: "createCronLogger"
  key_links:
    - from: "packages/api/src/root.ts"
      to: "usExpansionRouters"
      via: "conditional spread of the staff 1099 router (METHOD_NOT_FOUND when flag OFF)"
      pattern: "usExpansionRouters"
    - from: "packages/api/src/services/tax-filing-transmitter.ts"
      to: "module.iris-efile"
      via: "IrisA2A branch gated on the existing flag (no new flag)"
      pattern: "iris-efile"
    - from: "apps/cron-worker/src/jobs/registry.ts"
      to: "year-end-1099-reminder"
      via: "getJobDefinitions entry + CRON_*_SCHEDULE env"
      pattern: "year-end-1099-reminder"
---

<objective>
Wire the year-end loop's transmit tail + control surface (US-FORM-04/05/07): the `TaxFilingTransmitter`
factory (ManualDownload default | IrisA2A dark | Vendor stub, D-03), the staff `tax-1099-router`
(batch-generate / build-XML / download / upload-ack / escalate / file-correction — flag-gated, Zod,
idempotent, audited) conditionally spread out of `appRouter`, the portal `portal-tax-1099-router`
(e-delivery consent + Copy-B download scoped to `ctx.contractorId`), per-state CFSF/non-CFSF output
(D-13/D-14), and the notify-only year-end cron (D-05). Closes the D-16 cross-org leak test.

Purpose: This connects Plans 03 (TIN-match), 04 (IRIS XML + ack parser), 05 (1099 service + PDF)
into a usable, flag-dark-safe surface — the manual-upload default ships with no TCC; live A2A stays
dark behind `module.iris-efile`. Turns the Plan 01 transmitter + security scaffolds GREEN.
Output: transmitter factory + state-filing output + staff/portal routers + root spread + cron + cross-org leak test.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-PATTERNS.md
@.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-RESEARCH.md

<interfaces>
<!-- The format-factory + conditional-spread + portal-IDOR idioms, verbatim. -->
From packages/api/src/services/payment-export.ts: generateElixir | generateSepaXml | generateSwiftXml | generateBacsStandard18 (format factory).
From packages/api/src/routers/finance/payment-export-router.ts: _generateExportFileForFormat + writeExportAndComplianceRows (export-lock + compliance-snapshot, atomic, audited).
From packages/api/src/root.ts (L157-163): const usExpansionRouters = { taxForm: taxFormRouter } as const; conditionalUsExpansionRouters = isUsExpansionRegistered() ? usExpansionRouters : ({} as typeof usExpansionRouters);
From packages/api/src/middleware/require-us-expansion-flag.ts (L29-58): assertUsExpansionEnabled(ctx.organizationId, ctx.region) — first line of every procedure; isUsExpansionRegistered() for the spread.
From packages/api/src/routers/portal/portal-tax-form-router.ts (L41,171-234): deriveClientIp(ctx.headers); every read/write scoped to ctx.contractorId (IDOR); ESIGN attestation ip/actorId/signedAt 100% server-derived (client schema omits them); writeAuditLog inside $transaction.
From packages/api/src/lib/idempotency.ts (L90-149): reserve/complete/clear.
From packages/feature-flags: evaluate('module.iris-efile', ...) gates the IrisA2A branch (A1 — reuse, no new flag).
From apps/cron-worker/src/jobs/registry.ts (L29-108): getJobDefinitions env param + { meta:{name,schedule}, handler } entries; createCronLogger in handlers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TaxFilingTransmitter factory + per-state output; cross-org leak test GREEN</name>
  <files>packages/api/src/services/tax-filing-transmitter.ts, packages/api/src/services/state-filing-output.ts, packages/api/src/services/__tests__/tax-filing-transmitter.test.ts, packages/api/src/__tests__/security/tax-filing-tenant-isolation.security.test.ts</files>
  <read_first>
    - packages/api/src/services/__tests__/tax-filing-transmitter.test.ts + packages/api/src/__tests__/security/tax-filing-tenant-isolation.security.test.ts — the Plan 01 RED scaffolds these turn GREEN.
    - packages/api/src/services/payment-export.ts + packages/api/src/routers/finance/payment-export-router.ts — `_generateExportFileForFormat` format-factory shape + `writeExportAndComplianceRows` (export-lock + compliance-snapshot, atomic, audited) to mirror for the transmit/lock record.
    - packages/iris/src/index.ts — `buildIrisXml` + `xsdValidate` (Plan 04) the ManualDownload branch invokes.
    - packages/api/src/services/iris-ack-parser.ts — the single ack parser (Plan 04) both branches feed.
    - packages/db generated client — `StateFilingConfig` (Plan 02) for CFSF vs non-CFSF routing.
    - 86-RESEARCH.md Pattern 3 (factory) + D-03/D-04 + D-13/D-14 (CFSF code + per-state CSV) + A1 (reuse `module.iris-efile`).
  </read_first>
  <behavior>
    - The factory returns `ManualDownload` by default; it returns `IrisA2A` only when `module.iris-efile` is enabled; `Vendor` is a stub that throws "not configured".
    - `ManualDownload.transmit` runs `buildIrisXml` → `xsdValidate`; on valid it returns the XML buffer for download and records an `IrisSubmission`; on invalid it returns the validation report without filing.
    - For a recipient in a CFSF-participating state, the B-record carries the CFSF state code (no separate file); for a non-CFSF / direct-file state, `state-filing-output` produces a downloadable CSV/summary keyed by state with manual-portal guidance.
    - A second org cannot read another org's `Form1099Nec`/`IrisSubmission` rows.
  </behavior>
  <action>
    Implement `tax-filing-transmitter.ts` as a factory mirroring `payment-export.ts`: `interface TaxFilingTransmitter { transmit(xml, ctx): Promise<TransmitResult>; }` and a selector — `manual` → `ManualDownload` (default; runs `buildIrisXml`→`xsdValidate`, returns the validated XML buffer for admin download, records an `IrisSubmission` with the schema VersionNum/VersionDt, D-01), `iris-a2a` → `IrisA2A` (DARK — gated on `evaluate('module.iris-efile')`; the SOAP/MTOM transport body is a documented stub/seam off the GA critical path per the No-Analog note — do NOT install a SOAP dep here), `vendor` → `Vendor` (stub throwing "not configured"). Copy the export-lock + compliance-snapshot atomic-write idiom from `writeExportAndComplianceRows` for the transmit/lock record + `writeAuditLog`. Implement `state-filing-output.ts`: read `StateFilingConfig`; for CFSF participants confirm the CFSF state code is set on the B-record (no separate file); for non-CFSF / direct-file states (incl. Maryland) emit a per-state CSV/summary (recipients, amounts, state income/withholding boxes) + the documented manual-portal guidance (D-14) — NO bespoke per-state e-file integration. Turn the transmitter scaffold GREEN (default Manual; IrisA2A only when flagged). Write the cross-org leak `.security.test.ts` proving a second org cannot read another org's `Form1099Nec`/`IrisSubmission` (D-16) and turn it GREEN. No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- tax-filing-transmitter tax-filing-tenant-isolation</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test -- tax-filing-transmitter` passes: factory defaults to ManualDownload; IrisA2A only when `module.iris-efile` enabled.
    - The cross-org leak `.security.test.ts` passes: org B reading org A's `Form1099Nec`/`IrisSubmission` returns nothing (D-16).
    - `grep -c "iris-efile" packages/api/src/services/tax-filing-transmitter.ts` is ≥ 1 (gated on the existing flag); `grep -rn "iris-a2a-transmit" packages/api/src` returns nothing.
    - `state-filing-output.ts` produces a per-state CSV for a non-CFSF state and sets the CFSF code for a participating state (test-asserted).
  </acceptance_criteria>
  <done>The transmitter factory ships Manual default + dark IrisA2A (existing flag) + Vendor stub; per-state CFSF/non-CFSF output works; cross-org leak test GREEN; Plan 01 transmitter + security scaffolds flipped.</done>
</task>

<task type="auto">
  <name>Task 2: Staff tax-1099 router + portal consent/download router + root/portal-root spread</name>
  <files>packages/api/src/routers/finance/tax-1099-router.ts, packages/api/src/routers/portal/portal-tax-1099-router.ts, packages/api/src/root.ts, packages/api/src/portal-root.ts</files>
  <read_first>
    - packages/api/src/routers/core/tax-form-router.ts (L26-58) — the staff router shape: `assertUsExpansionEnabled` first, `requirePermission({ contractor: ['read'] })` (reuse — NO new permission), Zod input on every procedure.
    - packages/api/src/routers/portal/portal-tax-form-router.ts (L41,171-234) — `deriveClientIp(ctx.headers)`, every read/write scoped to `ctx.contractorId` (IDOR), ESIGN attestation server-derives ip/actorId/signedAt (client schema omits them), `writeAuditLog` inside `$transaction`.
    - packages/api/src/root.ts (L157-163) — the `usExpansionRouters` const + conditional spread; ADD the staff 1099 router here (keep the const so the spread TYPE is constant).
    - packages/api/src/portal-root.ts — how `portalAppRouter` merges portal routers (flat merge — cannot be conditionally spread; the per-request `assertUsExpansionEnabled` is the load-bearing gate there).
    - packages/api/src/lib/idempotency.ts — wrap batch-generate + transmit (D-19).
    - 86-RESEARCH.md D-09 (consent gate), D-15 (flag gating), D-19 (audit + idempotency); 86-UI-SPEC.md "IRS Electronic-Delivery Consent Contract".
  </read_first>
  <action>
    Implement `tax-1099-router.ts` (staff) with `assertUsExpansionEnabled(ctx.organizationId, ctx.region)` as the FIRST line of every procedure, `requirePermission({ contractor: ['read'] })` (reuse — no new Better Auth permission), Zod `.strict()` input on every procedure (server-derives tax year / amounts / status — reject mass-assignment of org/money fields), wrapping the procedures: `generateBatch` (calls the Plan 05 service, idempotency reserve/complete/clear), `buildAndValidateXml` (Plan 04 generator + validator), `downloadValidatedXml` (ManualDownload — returns the buffer), `uploadAck` (`safeParse` the uploaded file, feed `iris-ack-parser`, update `IrisSubmission`/`IrisAck` status, `writeAuditLog`), `listTinMismatches` / `escalateMismatch` / `resolveMismatch` (Plan 03 service), `fileCorrection` (Plan 05 supersede), `getStateFilingOutput` (Plan 06 Task 1). Implement `portal-tax-1099-router.ts` on `portalAppRouter` with `assertUsExpansionEnabled` per request + every procedure scoped to `ctx.contractorId` (IDOR — never client-supplied): `recordEdeliveryConsent` (server-derives ip via `deriveClientIp(ctx.headers)` + `ctx.contractorId` + `new Date()`; client schema OMITS all three — mirror ESIGN attestation), `withdrawConsent`, `downloadCopyB` (gated on stored consent; no consent → return a paper/manual flag, never the PDF). Add the staff router to the `usExpansionRouters` const in `root.ts` (conditional spread → METHOD_NOT_FOUND when flag OFF, D-15); merge the portal router into `portalAppRouter` in `portal-root.ts` (per-request gate is load-bearing). `writeAuditLog` on every sign/transmit/correct/consent/escalate (D-19). No `console.*`.
  </action>
  <verify>
    <automated>pnpm typecheck --filter @contractor-ops/api</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "assertUsExpansionEnabled" packages/api/src/routers/finance/tax-1099-router.ts` equals the procedure count (first line of every procedure).
    - `grep -c "ctx.contractorId" packages/api/src/routers/portal/portal-tax-1099-router.ts` is ≥ 3 (consent/withdraw/download all scoped); the consent input Zod schema does NOT contain `ip`/`actorId`/`signedAt`/`timestamp` fields (server-derived).
    - `grep -c "tax1099\|tax-1099\|taxFiling" packages/api/src/root.ts` shows the staff router added to `usExpansionRouters`.
    - `grep -c "reserve\|complete\|clear" packages/api/src/routers/finance/tax-1099-router.ts` is ≥ 1 (idempotent batch/transmit).
    - `pnpm typecheck --filter @contractor-ops/api` is green.
  </acceptance_criteria>
  <done>Staff + portal 1099 routers exist (flag-gated, Zod-strict, IDOR-scoped, audited, idempotent); staff router conditionally spread in root.ts; portal router merged with the per-request gate; api typechecks.</done>
</task>

<task type="auto">
  <name>Task 3: Notify-only year-end 1099 reminder cron + env</name>
  <files>apps/cron-worker/src/jobs/handlers/year-end-1099-reminder.ts, apps/cron-worker/src/jobs/registry.ts, apps/cron-worker/src/env.ts, .env.example</files>
  <read_first>
    - apps/cron-worker/src/jobs/registry.ts (L29-108) — the `getJobDefinitions` env param type + the `{ meta:{name,schedule}, handler }` entry shape; add `CRON_YEAR_END_1099_REMINDER_SCHEDULE`.
    - apps/cron-worker/src/jobs/handlers/ — an existing notify-style handler (e.g. `trial-notifications`/`reminders`) using `createCronLogger`; copy its structure.
    - apps/cron-worker/src/env.ts — the cron-worker env schema; add the new `CRON_*_SCHEDULE` field.
    - 86-RESEARCH.md D-05 (notify-only — NEVER auto-generate/auto-transmit) + the anti-pattern "Auto-filing from the cron".
  </read_first>
  <action>
    Create `year-end-1099-reminder.ts` using `createCronLogger`: it ONLY notifies that the 1099-NEC batch is due for the current tax year (an in-app notification / reminder) — it MUST NOT call `generateBatch`, build XML, or transmit (D-05 — human reviews before any immutable archive + IRS file). Register a `{ meta: { name: 'year-end-1099-reminder', schedule: env.CRON_YEAR_END_1099_REMINDER_SCHEDULE }, handler }` entry in `getJobDefinitions` and add `CRON_YEAR_END_1099_REMINDER_SCHEDULE: string;` to its env param type. Add the env var to `apps/cron-worker/src/env.ts` schema + `.env.example` (with a January-window cron expression default note). No `console.*` (use `createCronLogger`).
  </action>
  <verify>
    <automated>grep -c "year-end-1099-reminder" apps/cron-worker/src/jobs/registry.ts && grep -c "CRON_YEAR_END_1099_REMINDER_SCHEDULE" apps/cron-worker/src/env.ts .env.example && pnpm typecheck --filter @contractor-ops/cron-worker</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "year-end-1099-reminder" apps/cron-worker/src/jobs/registry.ts` is ≥ 1 (registered).
    - `grep -c "CRON_YEAR_END_1099_REMINDER_SCHEDULE" apps/cron-worker/src/env.ts` is ≥ 1 and the same key is present in `.env.example`.
    - The handler contains NO call to `generateBatch`/`buildIrisXml`/transmit (notify-only — D-05); `grep -Ec "generateBatch|buildIrisXml|transmit\(" apps/cron-worker/src/jobs/handlers/year-end-1099-reminder.ts` is 0.
    - `grep -c "console\." apps/cron-worker/src/jobs/handlers/year-end-1099-reminder.ts` is 0 (uses `createCronLogger`).
    - `pnpm typecheck --filter @contractor-ops/cron-worker` is green.
  </acceptance_criteria>
  <done>A notify-only year-end reminder cron is registered with its env (in `.env.example` + the cron-worker env schema), uses `createCronLogger`, and never auto-generates or auto-transmits (D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff client → tax-1099 router | Untrusted input crosses here — Zod `.strict()`, tenant from session, flag-gated |
| portal recipient → consent/download router | Recipient input crosses here — every read/write scoped to `ctx.contractorId` (IDOR) |
| uploaded ack file → uploadAck procedure | An admin-uploaded ack file is untrusted — `safeParse`, never `as` |
| cron → year-end loop | The cron must only notify; it must never trigger generation/transmit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-06-01 | Information Disclosure | cross-tenant 1099 read (IDOR/BOLA) | mitigate | Portal procedures scoped to `ctx.contractorId`; staff scoped to session org; cross-org leak `.security.test.ts` GREEN (Task 1). |
| T-86-06-02 | Tampering / Elevation | mass-assignment of org/money/status via tRPC | mitigate | `.strict()` Zod DTOs; tax year / amounts / status / signedAt / ip / actorId server-derived; client schema omits them. |
| T-86-06-03 | Info-disclosure / Tampering | SSRF / XXE via the dark IrisA2A endpoint + uploaded ack | mitigate | IrisA2A URL pinned (peppol pattern, dark); `uploadAck` `safeParse`s via the XXE-safe ack parser (Plan 04). |
| T-86-06-04 | Repudiation | premature/unauthorized auto-file | mitigate | Cron is notify-only (D-05); generation + transmit are separate, deliberate, human-initiated staff actions (test-asserted no auto-file). |
| T-86-06-05 | Compliance / Repudiation | Copy-B furnished without consent | mitigate | `downloadCopyB` gated on stored consent (Pub 1179 §4.6); no consent → paper/manual flag, never the PDF (D-09). |
| T-86-06-06 | Tampering | double-filing on a retried batch/transmit | mitigate | `idempotency.reserve/complete/clear` wraps batch-generate + transmit (D-19). |
| T-86-06-07 | Elevation | surface reachable with `module.us-expansion` OFF | mitigate | Staff router conditionally spread out of `appRouter` (METHOD_NOT_FOUND) + per-request `assertUsExpansionEnabled` on both surfaces (D-15). |
| T-86-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (RESEARCH § Package Legitimacy Audit: zero new external deps; the dark SOAP/MTOM dep is explicitly deferred to the A2A-build task with its own `checkpoint:human-verify`). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- tax-filing-transmitter tax-filing-tenant-isolation` green (Plan 01 scaffolds flipped).
- Transmitter defaults to ManualDownload; IrisA2A dark behind `module.iris-efile` (no new flag); Vendor stub.
- Staff router flag-gated + conditionally spread; portal router IDOR-scoped + consent-gated; both Zod-strict + audited + idempotent.
- Per-state CFSF code emitted for participating states; non-CFSF → downloadable CSV + manual guidance.
- Year-end cron is notify-only; never auto-files; api + cron-worker typecheck green.
</verification>

<success_criteria>
- US-FORM-05 transmit tail complete: ManualDownload default (no TCC), dark IrisA2A, one ack parser both paths.
- US-FORM-07 per-state filing: CFSF auto-forward code + non-CFSF downloadable output.
- US-FORM-04 control surface: staff batch/correction + portal consent/download, human-in-the-loop (notify-only cron).
</success_criteria>

<output>
Create `.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-06-SUMMARY.md` when done.
</output>
