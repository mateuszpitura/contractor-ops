---
phase: 86-theme-a-tin-match-1099-nec-iris-e-file-state-filing
plan: 03
type: execute
wave: 2
depends_on: [86-02]
files_modified:
  - packages/integrations/src/adapters/tin-match/tin-match-client.ts
  - packages/integrations/src/adapters/tin-match/mock-tin-match-client.ts
  - packages/integrations/src/adapters/tin-match/eservices-tin-match-client.ts
  - packages/integrations/src/adapters/tin-match/index.ts
  - packages/api/src/services/tin-match.service.ts
  - packages/api/src/services/__tests__/tin-match.service.test.ts
autonomous: true
requirements: [US-FORM-03]
must_haves:
  truths:
    - "A TinMatchClient interface abstracts the IRS TIN-Matching call; a deterministic MockTinMatchClient is the default and returns the IRS numerical response indicator"
    - "The live EServicesTinMatchClient exists behind the seam, dark — credential-decrypt + pinned-literal base URL (SSRF-safe), never instantiated unless its flag/PAF gate clears"
    - "The TIN-match service applies a 24h cache and retries transient failures"
    - "A mismatch sets the recipient's backup-withholding flag AND creates an admin escalation, and NEVER hard-blocks (the 1099 still generates with the TIN as captured)"
    - "Sign/escalate actions write an audit log; full TIN/SSN is never logged"
    - "Implements D-10 (intake + year-end revalidate), D-11 (mock default, live flag-gated), D-12 (mismatch → flag + escalate, never block), D-19 (audit)"
  artifacts:
    - path: "packages/integrations/src/adapters/tin-match/tin-match-client.ts"
      provides: "TinMatchClient interface (name/TIN → numerical response indicator)"
      contains: "interface TinMatchClient"
    - path: "packages/integrations/src/adapters/tin-match/mock-tin-match-client.ts"
      provides: "deterministic mock returning matched/mismatched/invalid indicators"
      contains: "MockTinMatchClient"
    - path: "packages/integrations/src/adapters/tin-match/eservices-tin-match-client.ts"
      provides: "dark live client — pinned literal base URL, decryptCredentials"
      contains: "decryptCredentials"
    - path: "packages/api/src/services/tin-match.service.ts"
      provides: "24h cache + retry + escalation; mismatch sets backup-withholding flag, never blocks"
      contains: "escalat"
  key_links:
    - from: "packages/api/src/services/tin-match.service.ts"
      to: "TinMatchClient"
      via: "service calls the injected client (mock by default)"
      pattern: "TinMatchClient"
    - from: "packages/integrations/src/adapters/tin-match/eservices-tin-match-client.ts"
      to: "decryptCredentials"
      via: "credential-decrypt + pinned literal base URL"
      pattern: "decryptCredentials\\(.*tin"
---

<objective>
Build the IRS TIN-Matching capability (US-FORM-03) as a fully-shipped deterministic core behind
an adapter seam: a `TinMatchClient` interface, a default `MockTinMatchClient`, a dark live
`EServicesTinMatchClient` (SSRF-safe pinned URL), and a `tin-match.service.ts` that owns the 24h
cache, retry, and the mismatch → backup-withholding-flag + admin-escalation logic — never a hard
block (D-12). This is the earliest mismatch signal that feeds the 1099 batch's backup-withholding
decision (read by Plan 05).

Purpose: Ship and fully test all TIN-match logic with no live IRS credentials; keep the live
e-Services client dark until PAF enrollment clears (D-11). Turns the Plan 01 RED scaffold GREEN.
Output: tin-match adapter seam + mock + dark live client + the cache/retry/escalation service.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-PATTERNS.md
@.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-RESEARCH.md

<interfaces>
<!-- SSRF-safe pinned-URL credential factory — copy verbatim posture. -->
From packages/api/src/services/peppol-adapter-factory.ts (L35-64):
  const blob = decryptCredentials(connection.credentialsRef, 'peppol');
  const baseUrl = env === 'production' ? PRODUCTION_BASE_URL : SANDBOX_BASE_URL; // pinned literal, never user-influenced
From packages/integrations/src/adapters/base-adapter.ts (L42-65): abstract adapter interface idiom (OAuth/credential/retry defaults).
From packages/validators/src/us-validators.ts: isValidEin(raw), isValidSsn(raw) — reuse, do not duplicate.
From packages/api/src/services/audit-writer.ts: writeAuditLog({ tx?, organizationId, actorType, actorId, action, resourceType, resourceId, ipAddress?, metadata }).
The recipient backup-withholding flag lives on the Contractor / W-9 record captured in P85 (read it; this service SETS it on mismatch).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TinMatchClient interface + deterministic mock + dark live e-Services client</name>
  <files>packages/integrations/src/adapters/tin-match/tin-match-client.ts, packages/integrations/src/adapters/tin-match/mock-tin-match-client.ts, packages/integrations/src/adapters/tin-match/eservices-tin-match-client.ts, packages/integrations/src/adapters/tin-match/index.ts</files>
  <read_first>
    - packages/integrations/src/adapters/base-adapter.ts (L42-100) — the abstract-interface + default-shape idiom for the `TinMatchClient` interface.
    - packages/api/src/services/peppol-adapter-factory.ts (L35-64) — the `decryptCredentials(ref, '<provider>')` + pinned-literal-base-URL (SSRF-safe) pattern for the dark live client.
    - packages/integrations/src/adapters/ (sibling adapter dir layout + index.ts barrel export convention).
    - 86-RESEARCH.md US-FORM-03 row (interactive ≤25 / bulk ≤100k modes; numerical response indicator per request) + A4 (single interface now, live deferred).
  </read_first>
  <behavior>
    - `MockTinMatchClient.check({ name, tin, tinType })` returns a `{ responseIndicator }` — `0` (matched) for a known-good fixture, a non-zero IRS indicator for a known-mismatch fixture, and an invalid-format indicator for a malformed TIN.
    - The mock is deterministic (same input → same indicator) with no network or randomness.
    - `EServicesTinMatchClient` constructs with a pinned literal base URL chosen by the credential `environment` field — never from user input.
  </behavior>
  <action>
    Define `interface TinMatchClient { check(input: { name: string; tin: string; tinType: 'EIN' | 'SSN' }): Promise<{ responseIndicator: number; matched: boolean }>; }` in `tin-match-client.ts` (model the IRS numerical response indicator; `matched` derives from indicator `0`). Implement `MockTinMatchClient` as the deterministic default keyed on fixture TINs (matched / mismatched / invalid), reusing `isValidEin`/`isValidSsn` from `packages/validators` for format gating (do NOT duplicate validators). Implement `EServicesTinMatchClient` (DARK — built, not wired into the default path) mirroring `peppol-adapter-factory`: `decryptCredentials(connection.credentialsRef, 'irs-tin-match')` + a pinned-literal base URL selected by the credential `environment` (PRODUCTION vs SANDBOX literals), never user-influenced (SSRF-safe). Barrel-export from `index.ts`. NEVER log a full TIN/SSN. No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/integrations typecheck 2>/dev/null || pnpm typecheck --filter @contractor-ops/integrations</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "interface TinMatchClient" packages/integrations/src/adapters/tin-match/tin-match-client.ts` is 1.
    - `grep -c "decryptCredentials(.*irs-tin-match" packages/integrations/src/adapters/tin-match/eservices-tin-match-client.ts` is ≥ 1.
    - `grep -c "PRODUCTION\|SANDBOX" packages/integrations/src/adapters/tin-match/eservices-tin-match-client.ts` confirms two pinned literal URLs (no user-influenced URL).
    - `grep -rc "console\." packages/integrations/src/adapters/tin-match/` is 0.
    - `pnpm typecheck --filter @contractor-ops/integrations` is green.
  </acceptance_criteria>
  <done>The TinMatchClient seam exists: deterministic mock (default) + dark SSRF-safe live client; barrel-exported; typechecks; no full-TIN logging.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tin-match.service — 24h cache + retry + mismatch → flag + escalate (never block)</name>
  <files>packages/api/src/services/tin-match.service.ts, packages/api/src/services/__tests__/tin-match.service.test.ts</files>
  <read_first>
    - packages/api/src/services/__tests__/tin-match.service.test.ts — the Plan 01 RED scaffold; this turns it GREEN. Read its asserted behaviors first.
    - packages/api/src/services/tax-form.service.ts — the service shape (pure resolver + DB-writing function split) to mirror; the reverse-charge/P85 D-10 auto-record-and-escalate posture.
    - packages/api/src/services/audit-writer.ts (L61-137) — `writeAuditLog` signature; pass `tx` inside a `$transaction`.
    - packages/integrations/src/adapters/tin-match/index.ts — the `TinMatchClient` + `MockTinMatchClient` exported in Task 1.
    - 86-RESEARCH.md D-12 / Pitfall (never hard-block) + Pub 1281 B-notice/CP2100 mechanics (mismatch → backup-withholding flag).
  </read_first>
  <behavior>
    - A cache hit within 24h returns the cached indicator without re-calling the client.
    - A transient client failure is retried; a persistent failure surfaces as a non-blocking advisory result.
    - On a mismatch indicator: the recipient's backup-withholding flag is set, an admin escalation record is created, `writeAuditLog` is called, and the function RETURNS a result (never throws / never blocks) — the 1099 still generates downstream.
    - On a match: no flag set, no escalation, audit records the check.
  </behavior>
  <action>
    Implement `tin-match.service.ts` taking an injected `TinMatchClient` (defaults to `MockTinMatchClient`; the live client is supplied only when its flag/PAF gate clears — D-11). Owns: a 24h cache (keyed on org+recipient+name+tin-last4; reuse Redis/in-DB cache idiom present in the repo, or a TIN-match cache row — planner-discretion, but the cache key MUST NOT store a full TIN), bounded retry on transient failure, and the mismatch handler — on a non-zero/mismatch indicator, set the recipient backup-withholding flag + create an admin escalation + `writeAuditLog({ action: 'tin_match.mismatch', ... })` inside a `$transaction`, then RETURN the advisory result (D-12 — never throw, never block). Expose a single-recipient check (W-9 intake, D-10) and a batch revalidate (year-end, D-10) entry point. Update the Plan 01 test scaffold to GREEN. NEVER log a full TIN/SSN; the audit metadata stores last-4 only.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- tin-match.service</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test -- tin-match.service` passes (was RED in Plan 01).
    - Tests prove: 24h cache hit avoids re-call; transient retry; mismatch sets the backup-withholding flag + creates an escalation + writes an audit log and DOES NOT throw/block.
    - `grep -c "writeAuditLog" packages/api/src/services/tin-match.service.ts` is ≥ 1.
    - `grep -rIn "ssn\|fullTin\|fulltin" packages/api/src/services/tin-match.service.ts | grep -vi "last4\|Last4" ` shows no full-TIN logging path (manual confirm a full TIN never reaches a log or cache key).
  </acceptance_criteria>
  <done>The TIN-match service caches 24h, retries transients, and on mismatch sets the backup-withholding flag + escalates + audits WITHOUT blocking; Plan 01 scaffold GREEN; no full-TIN logging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → IRS e-Services (dark) | External credentialed call; URL must be pinned (SSRF), creds decrypted from the encrypted store |
| recipient TIN → cache/log/audit | Full TIN must never land in a cache key, log line, or audit row (last-4 only) |
| mismatch result → 1099 generation | A mismatch must remain advisory — it can set a flag + escalate but must not block generation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-03-01 | Information Disclosure | full TIN/SSN in logs / cache key | mitigate | Cache key uses org+recipient+name+TIN-last4; Pino never logs full TIN; audit metadata stores last-4 only. |
| T-86-03-02 | Tampering / Info-disclosure | SSRF via live e-Services URL | mitigate | `EServicesTinMatchClient` uses a pinned literal base URL by credential `environment` (peppol pattern); never user-influenced. |
| T-86-03-03 | Denial of Service | mismatch hard-blocks the year-end loop | mitigate | D-12: mismatch sets flag + escalates + returns an advisory result; the service never throws on mismatch (test-asserted). |
| T-86-03-04 | Repudiation | un-audited flag set / escalation | mitigate | `writeAuditLog` on every mismatch flag-set + escalation, inside the `$transaction` (D-19). |
| T-86-03-05 | Spoofing | live client used without enrollment | mitigate | `MockTinMatchClient` is the default; the live client is dark, supplied only when its PAF/flag gate clears (D-11). |
| T-86-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (RESEARCH § Package Legitimacy Audit: zero new external deps). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- tin-match.service` green (Plan 01 scaffold flipped).
- Mock is default; live e-Services client is dark + SSRF-safe (pinned literal URL).
- Mismatch sets backup-withholding flag + escalates + audits, never blocks.
- No full TIN/SSN in any log, cache key, or audit row.
</verification>

<success_criteria>
- US-FORM-03 satisfied: TIN check with 24h cache + retry + admin escalation; mismatch never a hard block; live client deferred behind the seam.
</success_criteria>

<output>
Create `.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-03-SUMMARY.md` when done.
</output>
