---
phase: 86-theme-a-tin-match-1099-nec-iris-e-file-state-filing
plan: 08
type: execute
wave: 4
depends_on: [86-02, 86-03, 86-04, 86-05, 86-06]
files_modified:
  - .planning/brain/wiki/domains/us-tax-year-end-filing.md
  - .planning/brain/wiki/structure/api-routers-catalog.md
  - .planning/brain/wiki/structure/prisma-schema-areas.md
  - .planning/brain/wiki/structure/cron-jobs.md
  - .planning/brain/wiki/structure/packages.md
  - .planning/brain/wiki/integrations/irs-iris.md
  - .planning/brain/wiki/integrations/irs-eservices-tin-matching.md
  - .planning/brain/wiki/integrations/_index.md
  - .planning/brain/wiki/patterns/feature-flags.md
  - .planning/brain/wiki/index.md
  - .planning/brain/wiki/log.md
  - .planning/brain/wiki/hot.md
  - .planning/MEMORY.md
autonomous: true
requirements: [US-FORM-03, US-FORM-04, US-FORM-05, US-FORM-07]
must_haves:
  truths:
    - "A US year-end-filing domain wiki page documents the full loop (TIN-match → 1099-NEC → IRIS → state filing): Purpose, Flow, Entry points, UI surface, Agent mistakes — with correct verify_with source paths"
    - "The new tRPC procedures (staff tax-1099 + portal consent/download), new Prisma models, the year-end cron, the new packages/iris package, and the IRS integrations are recorded in the corresponding structure/integrations wiki pages"
    - "feature-flags.md records that module.iris-efile is the dark IRIS A2A transmit gate (no redundant iris-a2a-transmit flag)"
    - "wiki/log.md + hot.md are updated and MEMORY.md records the phase's load-bearing invariants"
    - "pnpm check:wiki-brain passes (no NEW doc drift introduced by this phase)"
    - "Implements the documentation-follows-code mandate for every product change in Plans 02-06"
  artifacts:
    - path: ".planning/brain/wiki/domains/us-tax-year-end-filing.md"
      provides: "the year-end-loop domain page (Purpose/Flow/Entry/UI/Agent-mistakes + verify_with)"
      contains: "1099-NEC"
    - path: ".planning/brain/wiki/integrations/irs-iris.md"
      provides: "IRS IRIS integration page (manual-upload default, dark A2A, XSD bundle, ack statuses)"
      contains: "IRIS"
    - path: ".planning/MEMORY.md"
      provides: "phase invariants (tax-year-keyed threshold, CORRECTED=supersede, manual-upload default, one ack parser, never-hard-block TIN mismatch)"
      contains: "1099-NEC"
  key_links:
    - from: ".planning/brain/wiki/domains/us-tax-year-end-filing.md"
      to: "packages/api/src/services/form-1099-nec.service.ts"
      via: "verify_with source path"
      pattern: "form-1099-nec.service"
    - from: ".planning/brain/wiki/structure/api-routers-catalog.md"
      to: "tax-1099-router"
      via: "new staff + portal namespaces catalogued"
      pattern: "tax-1099|tax1099"
---

<objective>
Satisfy the documentation-follows-code mandate (CLAUDE.md, gated by `check:wiki-brain`) for every
product change in Plans 02-06: add a US year-end-filing domain wiki page, record the new tRPC
procedures / Prisma models / cron job / `packages/iris` package / IRS integrations / flag in the
structure + integrations + patterns wiki pages, update `log.md` + `hot.md` + `index.md`, and record
the phase's load-bearing invariants in `MEMORY.md`.

Purpose: Stale docs make the next agent hallucinate; the codebase wiki must track the code in the
SAME milestone. This plan is the gated doc closure for Phase 86.
Output: one domain page + structure/integrations/patterns updates + log/hot/index + MEMORY invariants.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-CONTEXT.md
@.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-RESEARCH.md

<interfaces>
<!-- Mirror the P85 us-tax-forms wiki page + the established wiki frontmatter (verify_with / source_commit). -->
The P85 phase added a US W-form domain page — extend it or add this sibling year-end-filing page consistently.
Required updates per 86-RESEARCH § Documentation-follows-code:
  wiki/domains/ (year-end-filing) · wiki/structure/api-routers-catalog.md · wiki/structure/prisma-schema-areas.md ·
  wiki/structure/cron-jobs.md · wiki/structure/packages.md (new packages/iris) · wiki/integrations/ (IRS IRIS + e-Services TIN-Matching) ·
  wiki/patterns/feature-flags.md (module.iris-efile) · wiki/log.md + hot.md ; .planning/MEMORY.md for new invariants.
Wiki frontmatter: every page carries verify_with (source paths) + source_commit; bump source_commit on touched pages.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: US year-end-filing domain page + structure + integrations + patterns wiki updates</name>
  <files>.planning/brain/wiki/domains/us-tax-year-end-filing.md, .planning/brain/wiki/structure/api-routers-catalog.md, .planning/brain/wiki/structure/prisma-schema-areas.md, .planning/brain/wiki/structure/cron-jobs.md, .planning/brain/wiki/structure/packages.md, .planning/brain/wiki/integrations/irs-iris.md, .planning/brain/wiki/integrations/irs-eservices-tin-matching.md, .planning/brain/wiki/integrations/_index.md, .planning/brain/wiki/patterns/feature-flags.md, .planning/brain/wiki/index.md</files>
  <read_first>
    - .planning/brain/wiki/index.md + .planning/brain/wiki/domains/ — find the P85 US W-form domain page to extend/sibling and copy the page frontmatter (verify_with / source_commit / tags) shape.
    - .planning/brain/wiki/structure/api-routers-catalog.md — the existing catalog format for tRPC namespaces (the P85 `taxForm:` entry) to mirror for the new staff + portal 1099 namespaces.
    - .planning/brain/wiki/structure/prisma-schema-areas.md — the tax-schema area entry to extend with the five new models.
    - .planning/brain/wiki/structure/cron-jobs.md + packages.md — the cron-job + package catalog formats (add the year-end reminder + the new `packages/iris`).
    - .planning/brain/wiki/integrations/_index.md + an existing integration page (e.g. peppol/jira) — the integration page shape for the two new IRS integration pages.
    - .planning/brain/wiki/patterns/feature-flags.md — where to note `module.iris-efile` = dark IRIS A2A gate (A1).
  </read_first>
  <action>
    Create `wiki/domains/us-tax-year-end-filing.md` (or extend the P85 us-tax page if that is the established sibling — match the index convention): document the loop — Purpose (year-end US information-return loop on P85 data), Flow (TIN-match at intake + year-end revalidate → batch aggregate box-1 by payment-date/FX/threshold → immutable Form1099Nec + Copy-B PDF → buildIrisXml→xsdValidate→ManualDownload default / dark IrisA2A → one ack parser → CFSF state code / non-CFSF CSV), Entry points (staff `tax-1099-router`, portal consent/download router, year-end cron), UI surface (the `tax-filing/` staff surface + portal consent step), Agent mistakes (threshold is a TY-keyed table not a constant; TIN mismatch never hard-blocks; cron is notify-only; render Copy B only; reuse `module.iris-efile`, do not mint a flag). Set `verify_with` to the real source paths (`form-1099-nec.service.ts`, `tin-match.service.ts`, `tax-filing-transmitter.ts`, `packages/iris/src/generator.ts`+`validator.ts`, `iris-ack-parser.ts`, the two routers, the cron handler). Catalog the new staff + portal namespaces in `api-routers-catalog.md`, the five models in `prisma-schema-areas.md`, the year-end reminder in `cron-jobs.md`, the new `packages/iris` in `packages.md`, the two IRS integrations in `integrations/irs-iris.md` + `integrations/irs-eservices-tin-matching.md` (+ link in `_index.md`), and the `module.iris-efile` note in `patterns/feature-flags.md`. Add the domain page to `wiki/index.md`. Bump `source_commit` on every touched page. Annotate adviser-verify on tax-figure claims (D-18).
  </action>
  <verify>
    <automated>test -f .planning/brain/wiki/domains/us-tax-year-end-filing.md && grep -lq "1099-NEC" .planning/brain/wiki/domains/us-tax-year-end-filing.md && grep -lq "iris-efile" .planning/brain/wiki/patterns/feature-flags.md && grep -liq "iris" .planning/brain/wiki/integrations/irs-iris.md</automated>
  </verify>
  <acceptance_criteria>
    - `wiki/domains/us-tax-year-end-filing.md` exists with Purpose/Flow/Entry/UI/Agent-mistakes sections and a `verify_with` listing the real service + router + iris-package source paths.
    - `grep -c "tax-1099\|tax1099" .planning/brain/wiki/structure/api-routers-catalog.md` is ≥ 1 (new namespaces catalogued).
    - `grep -c "Form1099Nec\|IrisSubmission\|Tax1099Threshold\|StateFilingConfig" .planning/brain/wiki/structure/prisma-schema-areas.md` is ≥ 1.
    - `grep -c "packages/iris\|@contractor-ops/iris" .planning/brain/wiki/structure/packages.md` is ≥ 1; `grep -c "year-end-1099-reminder" .planning/brain/wiki/structure/cron-jobs.md` is ≥ 1.
    - `grep -c "iris-efile" .planning/brain/wiki/patterns/feature-flags.md` is ≥ 1 (no `iris-a2a-transmit`).
  </acceptance_criteria>
  <done>The year-end-filing domain page + all structure/integrations/patterns wiki updates exist with real `verify_with` paths and bumped `source_commit`; new namespaces/models/cron/package/integrations/flag catalogued.</done>
</task>

<task type="auto">
  <name>Task 2: Update log.md + hot.md + MEMORY.md invariants; run check:wiki-brain</name>
  <files>.planning/brain/wiki/log.md, .planning/brain/wiki/hot.md, .planning/MEMORY.md</files>
  <read_first>
    - .planning/brain/wiki/log.md — the append-an-entry format (date + page + change).
    - .planning/brain/wiki/hot.md — this file is OVERWRITTEN each wiki edit (per CLAUDE.md refresh pipeline).
    - .planning/MEMORY.md — the invariant-bullet format (one line + link); record only DURABLE code/domain facts (no operational/round-status text — per the user memory rule).
    - CLAUDE.md § Documentation follows code — the refresh pipeline (`pnpm check:wiki-brain`; BM25 rebuild + contextual-prefix are local artifacts, WARN-only).
  </read_first>
  <action>
    Append a `log.md` entry for the Phase 86 wiki changes. Overwrite `hot.md` per the refresh-pipeline convention. Add to `MEMORY.md` the phase's load-bearing invariants as durable code/domain facts (NOT operational text): (1) 1099-NEC threshold is a tax-year-keyed `Tax1099Threshold` table ($600 TY2025 / $2,000 TY2026 OBBBA), never a constant; (2) CORRECTED 1099 = supersede (append-only `Form1099Nec`, mirror `TaxFormSubmission`), never mutate a filed row; (3) IRIS = manual-upload default (no TCC) + dark A2A behind `module.iris-efile` (no `iris-a2a-transmit` flag); FIRE is doc-only; (4) ONE `iris-ack-parser` consumes both the manual ack file and the A2A poll result (6 statuses); (5) TIN mismatch sets the backup-withholding flag + escalates but NEVER hard-blocks; (6) `Form1099Nec` registered in `MODEL_RETENTION_TYPE` (4yr) + `softDeleteModels` (closes the P83 wiring point). Run `pnpm check:wiki-brain` and confirm no NEW drift is introduced by this phase (rebuild BM25 / contextual-prefix per the pipeline if the local artifacts are present; their absence is WARN-only). No `console.*`; no planning/requirement IDs in any source comment (these are wiki/MEMORY docs — IDs are fine here, but never in `apps/`/`packages/` source).
  </action>
  <verify>
    <automated>grep -c "1099-NEC\|Form1099Nec" .planning/MEMORY.md && pnpm check:wiki-brain</automated>
  </verify>
  <acceptance_criteria>
    - `MEMORY.md` gains ≥ 4 of the six invariant bullets above as durable facts (no operational/round-status text).
    - `log.md` has a new Phase 86 entry; `hot.md` is overwritten.
    - `pnpm check:wiki-brain` passes (no NEW drift for the source files Plans 02-06 changed — every touched wiki page's `verify_with` covers them).
  </acceptance_criteria>
  <done>`log.md` + `hot.md` updated, `MEMORY.md` records the phase invariants as durable facts, and `pnpm check:wiki-brain` passes with no new drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code change → wiki | The wiki must track the code; stale `verify_with` paths cause the next agent to hallucinate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-08-01 | Repudiation | doc drift (wiki not updated with the code) | mitigate | This plan updates every page whose `verify_with` covers the Plans 02-06 source files; `check:wiki-brain` gates it. |
| T-86-08-02 | Tampering | operational/stale text written into durable wiki | mitigate | MEMORY/wiki record only durable code/domain facts (per the user-memory rule), never round-status/"in progress" text. |
| T-86-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (docs only; RESEARCH § Package Legitimacy Audit: zero new external deps). No legitimacy checkpoint required. |
</threat_model>

<verification>
- US year-end-filing domain page exists with real `verify_with` paths + Purpose/Flow/Entry/UI/Agent-mistakes.
- New namespaces / models / cron / `packages/iris` / IRS integrations / `module.iris-efile` catalogued in the structure/integrations/patterns pages.
- `log.md` + `hot.md` updated; `MEMORY.md` records the phase invariants as durable facts.
- `pnpm check:wiki-brain` passes (no NEW drift).
</verification>

<success_criteria>
- The documentation-follows-code mandate is satisfied for Phase 86 in the same milestone: the wiki tracks the new year-end-filing code, and the gate is green.
</success_criteria>

<output>
Create `.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-08-SUMMARY.md` when done.
</output>
