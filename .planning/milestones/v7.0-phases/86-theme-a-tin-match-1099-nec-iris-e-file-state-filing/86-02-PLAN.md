---
phase: 86-theme-a-tin-match-1099-nec-iris-e-file-state-filing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema/tax.prisma
  - packages/db/prisma/schema/organization.prisma
  - packages/db/prisma/schema/contractor.prisma
  - packages/db/prisma/seed/tax-1099-config.ts
  - packages/db/prisma/seed/index.ts
  - packages/db/src/retention-policy.ts
  - packages/db/src/soft-delete.ts
  - packages/feature-flags/src/flags-core.ts
autonomous: false
requirements: [US-FORM-04, US-FORM-05, US-FORM-07]
must_haves:
  truths:
    - "Form1099Nec is an immutable, supersede-able, tenant-owning model (organizationId + Organization relation) mirroring TaxFormSubmission"
    - "IrisSubmission and IrisAck records exist and are append-only, recording the schema VersionNum/VersionDt the file was built against and the parsed ack status"
    - "Tax1099Threshold is a tax-year-keyed config table seeded with TY2025=$600 and TY2026=$2,000 (OBBBA), never a constant"
    - "StateFilingConfig is a per-state config table (CFSF participation + filing threshold + state-withholding-box rules) seeded with adviser-verify annotation"
    - "Form1099Nec is registered in MODEL_RETENTION_TYPE → '1099-NEC' (4yr) AND joins softDeleteModels so the data-purge chokepoint enforces IRS retention (closes the P83 wiring point)"
    - "The new models + config tables exist in EU/ME/US regional databases (migration applied)"
    - "module.iris-efile is confirmed registered PENDING and reused for the dark A2A path — no redundant iris-a2a-transmit flag is minted (A1)"
    - "Implements D-06 (tax-year-keyed threshold), D-08/D-16 (immutable supersede), D-13 (per-state config), D-17 (retention registration), A1 (reuse module.iris-efile)"
  artifacts:
    - path: "packages/db/prisma/schema/tax.prisma"
      provides: "Form1099Nec + IrisSubmission + IrisAck + Tax1099Threshold + StateFilingConfig models + status enums"
      contains: "model Form1099Nec"
    - path: "packages/db/src/retention-policy.ts"
      provides: "Form1099Nec → '1099-NEC' entry in MODEL_RETENTION_TYPE"
      contains: "Form1099Nec"
    - path: "packages/db/src/soft-delete.ts"
      provides: "Form1099Nec added to softDeleteModels"
      contains: "Form1099Nec"
    - path: "packages/db/prisma/seed/tax-1099-config.ts"
      provides: "TY2025 $600 + TY2026 $2,000 threshold rows + per-state CFSF config rows"
      contains: "2000"
  key_links:
    - from: "packages/db/src/retention-policy.ts"
      to: "MODEL_RETENTION_TYPE"
      via: "Form1099Nec key → '1099-NEC' record type"
      pattern: "Form1099Nec:\\s*'1099-NEC'"
    - from: "packages/db/src/soft-delete.ts"
      to: "softDeleteModels Set"
      via: "Form1099Nec added so the chokepoint sees it"
      pattern: "Form1099Nec"
    - from: "Form1099Nec"
      to: "Organization"
      via: "tenant FK relation (never globalModels)"
      pattern: "organization\\s+Organization\\s+@relation"
---

<objective>
Lay the data substrate for the year-end loop: add the immutable, supersede-able tenant-owning
`Form1099Nec` model + the `IrisSubmission`/`IrisAck` append-only records + two config tables
(`Tax1099Threshold` tax-year-keyed, `StateFilingConfig` per-state CFSF), seed them, register
`Form1099Nec` retention (closing the P83 wiring point), confirm the `module.iris-efile` flag,
then run the `[BLOCKING]` multi-region migration so EU/ME/US carry the new schema.

Purpose: Every downstream plan (TIN-match, 1099 service, IRIS generator, transmitter, routers,
UI) typechecks against the regenerated Prisma client produced here. Build + typecheck pass
WITHOUT the live migration (types come from the generated client), so the migration is mandatory
to avoid false-positive verification (the schema-push requirement).
Output: extended `tax.prisma`, retention + soft-delete registration, seeded config, applied
multi-region migration, regenerated client.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-PATTERNS.md
@.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-RESEARCH.md

<interfaces>
<!-- The CORRECTED = supersede chain mirrors TaxFormSubmission verbatim. -->
From packages/db/prisma/schema/tax.prisma (TaxFormSubmission L86-113, WhtCertificate L45-68):
  enum TaxFormStatus { DRAFT ACTIVE SUPERSEDED }
  model TaxFormSubmission { id; organizationId; contractorId; status TaxFormStatus @default(DRAFT);
    snapshotJson Json; supersededById String? @unique; ...
    organization Organization @relation(...); supersededBy/supersedes @relation("Supersede");
    @@index([organizationId, contractorId, formType, status]) }
  model WhtCertificate { grossAmountMinor Int; whtAmountMinor Int; currency String @db.Char(3); paymentDate DateTime @db.Date; ... }
From packages/db/src/retention-policy.ts:
  RETENTION_YEARS = { '1099-NEC': 4, 'backup-withholding': 7 }   // already seeded
  MODEL_RETENTION_TYPE: Partial<Record<string, RetainedRecordType>> = {}   // ships EMPTY — register here
From packages/db/src/soft-delete.ts:
  const softDeleteModels = new Set(['Organization','Contractor','Contract','Invoice','Document']);  // add Form1099Nec
From packages/feature-flags/src/flags-core.ts: 'module.iris-efile' registered (L247) — confirm PENDING, do NOT add a new flag.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Form1099Nec + IRIS records + config tables to tax.prisma</name>
  <files>packages/db/prisma/schema/tax.prisma, packages/db/prisma/schema/organization.prisma, packages/db/prisma/schema/contractor.prisma</files>
  <read_first>
    - packages/db/prisma/schema/tax.prisma — read `TaxFormSubmission` (L86-113) for the supersede-chain shape, `WhtCertificate` (L45-68) for the money-minor + currency + paymentDate shape, and `TaxRate` (L4-21) for the `effectiveFrom`/`effectiveTo` config-table idiom.
    - packages/db/prisma/schema/organization.prisma — locate the Organization back-relation block (where `taxFormSubmissions TaxFormSubmission[]` was added in P85).
    - packages/db/prisma/schema/contractor.prisma — locate the Contractor back-relation block + confirm the existing `ssnLast4` column (the snapshot keeps last-4 only).
    - 86-PATTERNS.md § "tax.prisma — Form1099Nec + IRIS + config tables" — the What-to-copy / What-differs table (taxYear, box amounts, cfsfStateCode, payerOrgId axis).
  </read_first>
  <action>
    In `tax.prisma`, add UPPER_SNAKE enums and models (Prisma enum-casing gate `db:audit-enum-casing`): `enum Form1099Status { DRAFT ACTIVE SUPERSEDED }` and `enum IrisAckStatus { ACCEPTED REJECTED PROCESSING PARTIALLY_ACCEPTED ACCEPTED_WITH_ERRORS NOT_FOUND }` (the six IRIS statuses, D-04). `model Form1099Nec`: `id`, `organizationId` (payer-org tenant), `payerOrgId String` (D-06 aggregation axis — for self-org filings equals `organizationId`; carried explicitly so a future agent-org-on-behalf-of-payer split is non-breaking), `recipientId String` (FK Contractor), `taxYear Int`, `status Form1099Status @default(DRAFT)`, `box1AmountMinor Int`, `box4BackupWithholdingMinor Int @default(0)`, `currency String @db.Char(3) @default("USD")`, `cfsfStateCode String? @db.Char(2)`, `corrected Boolean @default(false)`, `snapshotJson Json` (immutable record-of-record; last-4 TIN only, never full SSN — mirror TaxFormSubmission), `pdfArchiveKey String?`, `supersededById String? @unique`, `deletedAt DateTime?` (REQUIRED for soft-delete chokepoint, Task 3), `createdAt DateTime @default(now())`; relations `organization Organization @relation(...)`, `recipient Contractor @relation(...)`, self-relation pair `supersededBy`/`supersedes` named `"Form1099Supersede"`; indexes `@@index([organizationId, payerOrgId, recipientId, taxYear, status])` and `@@index([organizationId, taxYear, status])`. `model IrisSubmission`: `id`, `organizationId`, `taxYear Int`, `schemaVersionNum String`, `schemaVersionDt String`, `transmitMethod String` (`manual`/`iris-a2a`/`vendor`), `originalReceiptId String?` (for replacements), `status IrisAckStatus?`, `xmlArchiveKey String?`, `deletedAt DateTime?`, `createdAt DateTime @default(now())`; `organization Organization @relation(...)`; `@@index([organizationId, taxYear])`. `model IrisAck`: `id`, `organizationId`, `submissionId String`, `status IrisAckStatus`, `errorInformationJson Json?` (the Error Information Group), `receiptId String?`, `parsedAt DateTime @default(now())`; relations to Organization + IrisSubmission; `@@index([organizationId, submissionId])`. Config tables (NOT constants — Pitfall 1/5): `model Tax1099Threshold` — `id`, `taxYear Int @unique`, `box1ThresholdMinor Int`, `currency String @db.Char(3) @default("USD")`, `note String?` (adviser-verify); `model StateFilingConfig` — `id`, `stateCode String @db.Char(2)`, `taxYear Int`, `cfsfParticipant Boolean`, `requiresDirectFiling Boolean`, `filingThresholdMinor Int?`, `stateWithholdingBoxRules Json?`, `note String?`, `@@unique([stateCode, taxYear])`. Add inverse back-relations `form1099Nec Form1099Nec[]`, `irisSubmissions IrisSubmission[]`, `irisAcks IrisAck[]` on Organization, and `form1099Nec Form1099Nec[]` on Contractor. NEVER add any new model to globalModels (tenant-owning). No app code here.
  </action>
  <verify>
    <automated>cd packages/db && pnpm exec prisma format --schema prisma/schema && pnpm exec prisma validate --schema prisma/schema</automated>
  </verify>
  <acceptance_criteria>
    - `prisma validate` exits 0 (all relations have inverse sides).
    - `grep -n "model Form1099Nec\|model IrisSubmission\|model IrisAck\|model Tax1099Threshold\|model StateFilingConfig" packages/db/prisma/schema/tax.prisma` returns all five.
    - `grep -c "supersededById\s*String?\s*@unique" packages/db/prisma/schema/tax.prisma` is ≥ 2 (TaxFormSubmission + Form1099Nec).
    - `grep -n "deletedAt" packages/db/prisma/schema/tax.prisma` shows `deletedAt` on `Form1099Nec` (required for the soft-delete chokepoint).
    - `grep -c "ACCEPTED_WITH_ERRORS\|PARTIALLY_ACCEPTED\|NOT_FOUND" packages/db/prisma/schema/tax.prisma` confirms the six-status enum is present.
  </acceptance_criteria>
  <done>Five new tenant-owning models + two status enums + back-relations exist; `prisma validate` green; `Form1099Nec` carries `deletedAt` + a supersede chain; config tables are tax-year/state-keyed (not constants).</done>
</task>

<task type="auto">
  <name>Task 2: Seed threshold + per-state config; register retention + soft-delete; confirm flag</name>
  <files>packages/db/prisma/seed/tax-1099-config.ts, packages/db/prisma/seed/index.ts, packages/db/src/retention-policy.ts, packages/db/src/soft-delete.ts, packages/feature-flags/src/flags-core.ts</files>
  <read_first>
    - packages/db/prisma/seed/index.ts — read `main()` to wire a new `seedTax1099Config(prisma)` call alongside the existing seed calls.
    - packages/db/prisma/seed/wht-rates.ts — copy the `upsert` idiom (idempotent re-seed on the unique key).
    - packages/db/src/retention-policy.ts — read the EMPTY `MODEL_RETENTION_TYPE` (L31) + the `RETENTION_YEARS` map (already has `'1099-NEC':4`, `'backup-withholding':7`).
    - packages/db/src/soft-delete.ts — read the `softDeleteModels` Set (L26).
    - packages/feature-flags/src/flags-core.ts — read the `module.iris-efile` entry (L247) to CONFIRM it is registered PENDING (do NOT add a new flag; A1).
    - 86-RESEARCH.md Pitfall 1 ($600 TY2025 / $2,000 TY2026) + Pitfall 5 (CFSF list drift, MD special case) + Open Q3 (per-state list — adviser-verify-annotate).
  </read_first>
  <action>
    Create `packages/db/prisma/seed/tax-1099-config.ts` exporting `seedTax1099Config(prisma)`: upsert `Tax1099Threshold` rows `{ taxYear: 2025, box1ThresholdMinor: 60000 }` and `{ taxYear: 2026, box1ThresholdMinor: 200000 }` (USD minor units — $600 and $2,000; D-06/Pitfall 1), each with a `note` carrying the D-18 adviser-verify annotation. Seed `StateFilingConfig` for TY2025/TY2026 from the current IRS CF/SF participant guidance: mark CFSF participants (`cfsfParticipant: true, requiresDirectFiling: false`), mark Maryland `cfsfParticipant: true, requiresDirectFiling: true` (participates in name but requires direct filing — Pitfall 5), and seed the ~18 non-CFSF / direct-file states (`cfsfParticipant: false, requiresDirectFiling: true`) per A3; every row carries an adviser-verify `note` ("re-verify per tax year against the IRS CF/SF coordinator FAQ"). Wire the call in `seed/index.ts` `main()`. In `retention-policy.ts`, add `Form1099Nec: '1099-NEC'` to `MODEL_RETENTION_TYPE` (the only production entry — closes the P83 wiring point, D-17). In `soft-delete.ts`, add `'Form1099Nec'` to the `softDeleteModels` Set so the data-purge chokepoint sees it. In `flags-core.ts`, add a one-line code comment on the `module.iris-efile` entry confirming it is the dark IRIS A2A transmit gate for Phase 86 (A1) — do NOT mint a new flag, do NOT change its PENDING signoff status. No `console.*` (seed uses the existing seed logger/print convention).
  </action>
  <verify>
    <automated>cd packages/db && grep -c "Form1099Nec: '1099-NEC'" src/retention-policy.ts && grep -c "'Form1099Nec'" src/soft-delete.ts && pnpm typecheck --filter @contractor-ops/db</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "200000" packages/db/prisma/seed/tax-1099-config.ts` ≥ 1 (TY2026 $2,000 minor) and `grep -c "60000" packages/db/prisma/seed/tax-1099-config.ts` ≥ 1 (TY2025 $600 minor).
    - `grep -c "Form1099Nec: '1099-NEC'" packages/db/src/retention-policy.ts` is 1.
    - `grep -c "'Form1099Nec'" packages/db/src/soft-delete.ts` is 1.
    - `grep -ci "requiresDirectFiling: true" packages/db/prisma/seed/tax-1099-config.ts` ≥ 1 (Maryland + non-CFSF states) and an adviser-verify `note` is present on threshold + state rows.
    - `grep -rn "iris-a2a-transmit" packages/feature-flags/src` returns nothing (no redundant flag minted — A1).
    - `pnpm typecheck --filter @contractor-ops/db` is green.
  </acceptance_criteria>
  <done>Threshold ($600/$2,000) + per-state CFSF config seeded with adviser-verify notes; `Form1099Nec` registered in both `MODEL_RETENTION_TYPE` and `softDeleteModels`; `module.iris-efile` confirmed (no new flag); db typechecks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Generate client + create + apply multi-region migration (EU/ME/US)</name>
  <files>packages/db/prisma/migrations/ (generated)</files>
  <read_first>
    - packages/db/scripts/migrate-all-regions.ts — read the `DATABASE_URL_US` entry in `REGION_ENV_VARS` + the EU/ME/US iterate-and-fail-fast loop. Do NOT edit this script (run, do not edit).
    - packages/db/package.json — confirm the `db:generate`, `db:migrate:dev`, `db:migrate:all`, `db:seed` scripts (repo uses `prisma migrate`, NOT `db push`).
    - .planning/STATE.md — prior-phase note: `migrate dev` has hit pre-existing migration-history drift; the additive `CREATE TABLE`/`ALTER` direct fallback used in P82-85 applies if drift-blocked; the canonical path is `migrate dev`.
  </read_first>
  <what-built>
    Schema edits (Task 1) + seed/retention/soft-delete registration (Task 2) are complete. This step
    regenerates the typed Prisma client (downstream plans cannot typecheck without `Form1099Nec`/
    `IrisSubmission`/`Tax1099Threshold`/`StateFilingConfig` in the client) and creates + applies the
    migration across EU/ME/US. Build + typecheck pass WITHOUT the live migration (types come from the
    generated client), so this step is mandatory to avoid a false-positive verification — US contractors
    would otherwise hit "table does not exist" at runtime (Pitfall 5 posture).
  </what-built>
  <action>
    Run autonomously in order:
    1. `pnpm --filter @contractor-ops/db db:generate` — regenerate the typed client; downstream plans depend on the five new models being present.
    2. `pnpm --filter @contractor-ops/db db:migrate:dev --name add_form_1099_nec_iris_and_tax_config` — create + apply the migration SQL locally. If blocked by the pre-existing migration-history drift (STATE.md / P82-85 posture), apply the additive `CREATE TABLE` for the five models + the two enum types + the `softDeleteModels` `deletedAt` columns via the documented direct-ALTER fallback and record the fallback in the SUMMARY.
    3. `pnpm --filter @contractor-ops/db db:migrate:all` — apply across EU/ME/US (`DATABASE_URL_US` already wired in `migrate-all-regions.ts`); a missing local `DATABASE_URL_US` is skipped (no-op) — confirm EU at minimum.
    4. `pnpm --filter @contractor-ops/db db:seed` — apply the threshold + per-state config rows from Task 2.
    Then pause for human confirmation that the migration landed (or the drift-fallback was taken), because a per-region production apply and any drift resolution are operational decisions.
  </action>
  <how-to-verify>
    - `src/generated/prisma/client` reflects `Form1099Nec` + `IrisSubmission` + `IrisAck` + `Tax1099Threshold` + `StateFilingConfig`.
    - The migration (or direct-ALTER fallback) applied to EU at minimum; `db:migrate:all` ran for available regions.
    - The threshold + per-state seed rows are present (query `Tax1099Threshold` shows 2025=60000, 2026=200000).
    - `pnpm typecheck --filter @contractor-ops/db` is green against the regenerated client.
  </how-to-verify>
  <verify>
    <automated>pnpm --filter @contractor-ops/db db:generate && pnpm typecheck --filter @contractor-ops/db</automated>
  </verify>
  <resume-signal>Type "approved" once the client is regenerated and the migration is applied (or describe the drift-fallback taken).</resume-signal>
  <done>Prisma client regenerated with the five new models; migration applied to available regions (or drift-fallback recorded); threshold + per-state config seeded; `typecheck --filter @contractor-ops/db` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → DB | Threshold + per-state values are developer-authored placeholders; adviser-verify before production reliance |
| migration runner → regional DBs | The five models must land identically across EU/ME/US or US payers hit "table does not exist" |
| tenant rows ↔ Organization | Form1099Nec/IrisSubmission/IrisAck must never become globalModels (cross-org leak) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-02-01 | Information Disclosure | new tenant-owning models added to globalModels | mitigate | All five carry `organizationId` + an `Organization` relation and are NEVER added to globalModels; the cross-org leak test scaffolded in Plan 01 turns GREEN in Plan 06's service wiring. |
| T-86-02-02 | Tampering | threshold as a constant / wrong-year value | mitigate | `Tax1099Threshold` is a tax-year-keyed table seeded $600 TY2025 / $2,000 TY2026 (Pitfall 1); grep gate asserts both values. |
| T-86-02-03 | Tampering | mutating a filed 1099 | mitigate | `Form1099Status` machine + `supersededById` chain at the schema level (D-08/D-16); append-only enforcement lands in Plan 05's service. |
| T-86-02-04 | Denial of Service | regional migration drift (US not migrated) | mitigate | `db:migrate:all` (EU/ME/US) at the BLOCKING checkpoint, not single-region. |
| T-86-02-05 | Repudiation | retention not enforced (early hard-delete of a 1099) | mitigate | `Form1099Nec` registered in `MODEL_RETENTION_TYPE` (4yr) AND `softDeleteModels` so the data-purge chokepoint blocks early purge (D-17, closes P83 wiring point). |
| T-86-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (RESEARCH § Package Legitimacy Audit: zero new external deps). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `prisma validate` green; five new tenant-owning models + six-status ack enum present.
- `Form1099Nec` registered in both `MODEL_RETENTION_TYPE` and `softDeleteModels`.
- Threshold seeded $600 TY2025 / $2,000 TY2026; per-state CFSF config seeded with adviser-verify notes + Maryland direct-filing case.
- No redundant `iris-a2a-transmit` flag; `module.iris-efile` confirmed PENDING.
- Multi-region migration applied (or drift-fallback documented); regenerated client typechecks.
</verification>

<success_criteria>
- US-FORM-04/05/07 data substrate exists: immutable 1099 + IRIS records + tax-year/per-state config tables.
- US-INFRA-03 retention wiring closed for `Form1099Nec` (4yr enforced at the purge chokepoint).
</success_criteria>

<output>
Create `.planning/phases/86-theme-a-tin-match-1099-nec-iris-e-file-state-filing/86-02-SUMMARY.md` when done.
</output>
