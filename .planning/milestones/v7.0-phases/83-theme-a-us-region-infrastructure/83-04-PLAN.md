---
phase: 83-theme-a-us-region-infrastructure
plan: 04
type: execute
wave: 2
depends_on: ["83-01"]
files_modified:
  - packages/db/src/retention-policy.ts
  - packages/db/src/index.ts
  - packages/db/src/__tests__/retention-policy.test.ts
  - packages/db/src/soft-delete.ts
  - packages/db/src/__tests__/soft-delete.test.ts
  - apps/cron-worker/src/jobs/handlers/data-purge.ts
  - apps/cron-worker/src/__tests__/data-purge.test.ts
  - packages/api/src/routers/compliance/gdpr.ts
  - packages/api/src/routers/__tests__/gdpr.test.ts
autonomous: true
requirements: [US-INFRA-03]
must_haves:
  truths:
    - "A single retention resolver (RETENTION_YEARS {'1099-NEC':4,'backup-withholding':7} + MODEL_RETENTION_TYPE ships EMPTY + getRetentionCutoff) lives in packages/db/src/retention-policy.ts and is consumed by all three deletion chokepoints (D-04/D-07)"
    - "No early hard-delete of a retained record: a retained model in-window cannot be hard-deleted at the soft-delete extension, the data-purge cron (THE load-bearing base-prisma hard-delete path), or gdpr erasure; non-retained models keep the flat 90-day sweep (D-05)"
    - "gdpr RODO erasure soft-deletes-with-exemption for retained records (statutory citation in summary + writeAuditLog), never deleteByOrgAndCount; verified against the Invoice fixture (D-05 #3, D-06)"
  artifacts:
    - path: "packages/db/src/retention-policy.ts"
      provides: "RETENTION_YEARS + MODEL_RETENTION_TYPE (empty) + resolveRetentionYears + getRetentionCutoff"
      contains: "getRetentionCutoff"
      min_lines: 25
    - path: "apps/cron-worker/src/jobs/handlers/data-purge.ts"
      provides: "per-model retention-aware cutoff (retained→4y/7y, else→90d) on the base-prisma hard-delete"
      contains: "getRetentionCutoff"
    - path: "packages/api/src/routers/compliance/gdpr.ts"
      provides: "retention-exemption branch — softDeleteByOrgAndCount + retainedUnderStatute citation for retained models"
      contains: "retain"
  key_links:
    - from: "apps/cron-worker/src/jobs/handlers/data-purge.ts"
      to: "getRetentionCutoff (@contractor-ops/db)"
      via: "per-model cutoff resolution before deleteMany"
      pattern: "getRetentionCutoff"
    - from: "packages/api/src/routers/compliance/gdpr.ts"
      to: "softDeleteByOrgAndCount + writeAuditLog"
      via: "retained-model branch with statutory citation"
      pattern: "softDeleteByOrgAndCount"
    - from: "packages/db/src/soft-delete.ts"
      to: "getRetentionCutoff"
      via: "retained-in-window guard in the $allModels delete hook"
      pattern: "getRetentionCutoff"
---

<objective>
US-INFRA-03 — IRS-mandated retention via soft-delete + scheduled archive, with NO
early hard-delete of a retained record.

Ship ONE light retention-policy primitive (`packages/db/src/retention-policy.ts`:
`RETENTION_YEARS = { '1099-NEC': 4, 'backup-withholding': 7 }`, a `MODEL_RETENTION_TYPE`
map that ships EMPTY per D-06, and `getRetentionCutoff(model, now)`), then wire all
THREE deletion chokepoints to consult it:
  1. `soft-delete.ts` extension — a retained model in-window cannot be hard-deleted.
  2. `data-purge.ts` cron — THE load-bearing path: it imports the BASE prisma (no
     soft-delete extension) so its `deleteMany` is a TRUE hard-delete past a flat
     `RETENTION_DAYS=90`. Retained models must consult the policy window; non-retained
     keep the 90-day sweep.
  3. `gdpr.ts` RODO erasure — retained records soft-delete-with-exemption (statutory
     citation in the summary + `writeAuditLog`), NOT hard-delete via `deleteByOrgAndCount`.

Purpose: US-INFRA-03's success criterion — retention enforced (4-yr 1099-NEC, 7-yr
backup-withholding) with no early hard-delete. Built reusable so Theme B AKTA-02
extends the SAME resolver (D-04). The map ships EMPTY (D-06 — no tax tables this
phase); the guard is proven against the existing `Invoice` soft-delete fixture, and
Phase 86 tax models opt in by registering their window + joining `softDeleteModels`.
Output: the resolver module, the three wired chokepoints, and the extended tests GREEN.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/83-theme-a-us-region-infrastructure/83-CONTEXT.md
@.planning/milestones/v7.0-phases/83-theme-a-us-region-infrastructure/83-RESEARCH.md

<interfaces>
<!-- From research (in-tree 2026-06-07). Use directly. -->

Resolver to build — packages/db/src/retention-policy.ts (NEW; RESEARCH Pattern 4, lines 228-269):
  export const RETENTION_YEARS = { '1099-NEC': 4, 'backup-withholding': 7 } as const;
  export type RetainedRecordType = keyof typeof RETENTION_YEARS;
  export const MODEL_RETENTION_TYPE: Partial<Record<string, RetainedRecordType>> = {};  // EMPTY (D-06; Phase 86 registers)
  export function resolveRetentionYears(recordType: RetainedRecordType): number;
  export function getRetentionCutoff(model: string, now: Date): Date | null;  // null when model has no retention rule
  // export all from packages/db/src/index.ts

From packages/db/src/index.ts (add the export line, mirror :29-30 region exports):
  // export { RETENTION_YEARS, MODEL_RETENTION_TYPE, resolveRetentionYears, getRetentionCutoff } from './retention-policy.js';
  // export type { RetainedRecordType } from './retention-policy.js';

From packages/db/src/soft-delete.ts (:24 softDeleteModels, :57 $allModels delete hook — RESEARCH lines 543, 315):
  const softDeleteModels = new Set(['Organization','Contractor','Contract','Invoice','Document']);
  // withSoftDelete $allModels.delete/deleteMany → update(deletedAt). Add: retained model in-window → block real hard-delete.

From apps/cron-worker/src/jobs/handlers/data-purge.ts (:24 base prisma import, :29 RETENTION_DAYS=90, :119-132 per-model deleteMany — RESEARCH Pattern 5, lines 271-287, Pitfall 2, lines 344-348):
  import { prisma } from '@contractor-ops/db';   // BASE client — NO soft-delete extension → deleteMany is a TRUE hard-delete
  const RETENTION_DAYS = 90;                      // default sweep for non-retained models (unchanged)
  // each model: tx.<model>.deleteMany({ where: { deletedAt: { not: null, lt: cutoff } } })
  // CHANGE: cutoffFor(model, now, flatCutoff) = getRetentionCutoff(model, now) ?? flatCutoff;
  // cron uses ctx.log (createCronLogger) — no console.*

From packages/api/src/routers/compliance/gdpr.ts (:17 deleteByOrgAndCount HARD-deletes ~40 models, :33 softDeleteByOrgAndCount, requestErasure mutation, :247 writeAuditLog — RESEARCH Pattern 6, lines 289-299, Pitfall 3, lines 350-353):
  // requestErasure runs on ctx.db (HAS soft-delete extension). For a model with a retention rule
  // (MODEL_RETENTION_TYPE), route through softDeleteByOrgAndCount + add a `retainedUnderStatute`
  // section to the summary with the citation (e.g. "IRS 1099-NEC: 4-year retention, 26 CFR 1.6001-1")
  // and writeAuditLog the retention-blocked erasure attempt; NEVER deleteByOrgAndCount for retained models.
  // existing erasure message hard-codes "90 days" — update for retained records held to their statutory window.

Test fixture (D-06): Invoice is the representative retained model. In tests, inject a
test-local MODEL_RETENTION_TYPE entry (e.g. Invoice → '1099-NEC') so behavior is proven
WITHOUT shipping a production map entry (production map stays EMPTY). Phase 86 adds the real entry.

Legal note (Standing Constraint): statutory-citation copy needs jurisdiction-specific
legal/tax-adviser verification before production deploy — ship annotated, not hard-blocked.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the retention-policy resolver module + export it; turn the Plan-01 RED scaffold GREEN</name>
  <files>packages/db/src/retention-policy.ts, packages/db/src/index.ts, packages/db/src/__tests__/retention-policy.test.ts</files>
  <read_first>
    - packages/db/src/__tests__/retention-policy.test.ts (the RED scaffold from Plan 01 — turn GREEN)
    - 83-RESEARCH.md Pattern 4 (lines 228-269) — the exact map + resolver shape
    - packages/api/src/services/reverse-charge.service.ts — the const-map style this mirrors (EU_MEMBER_STATES / DE13bServiceType)
    - packages/db/src/index.ts (:29-30 — region export pattern to mirror)
  </read_first>
  <behavior>
    - `resolveRetentionYears('1099-NEC')` === 4; `resolveRetentionYears('backup-withholding')` === 7.
    - `getRetentionCutoff(model, now)` returns `now` minus the retention years for a mapped model, `null` for an unmapped model.
    - `MODEL_RETENTION_TYPE` ships EMPTY (D-06); the test injects a fixture entry locally.
  </behavior>
  <action>
    Create `packages/db/src/retention-policy.ts` with `RETENTION_YEARS` (the two IRS values, annotated "needs jurisdiction legal/tax-adviser verification before production deploy" per Standing Constraint), `RetainedRecordType`, `MODEL_RETENTION_TYPE` shipped EMPTY (D-06; comment that Phase 86 + Theme B AKTA-02 register here), `resolveRetentionYears`, and `getRetentionCutoff(model, now)` (returns `null` when the model has no rule, else `now` minus the years). Export all from `packages/db/src/index.ts` mirroring the region export lines. Turn the Plan-01 RED scaffold GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db test retention-policy</automated>
  </verify>
  <done>retention-policy.ts exports the map + resolvers; index.ts re-exports them; MODEL_RETENTION_TYPE is EMPTY; retention-policy test GREEN (VALIDATION.md SC#3 resolver row).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make data-purge cron retention-aware (THE load-bearing D-05 path) + extend soft-delete guard</name>
  <files>apps/cron-worker/src/jobs/handlers/data-purge.ts, apps/cron-worker/src/__tests__/data-purge.test.ts, packages/db/src/soft-delete.ts, packages/db/src/__tests__/soft-delete.test.ts</files>
  <read_first>
    - apps/cron-worker/src/jobs/handlers/data-purge.ts (:24 base prisma import, :29 RETENTION_DAYS=90, :119-132 per-model deleteMany; uses ctx.log)
    - apps/cron-worker/src/__tests__/data-purge.test.ts (mocks base prisma — extend)
    - packages/db/src/soft-delete.ts (:24 softDeleteModels, :57 $allModels delete hook)
    - packages/db/src/__tests__/soft-delete.test.ts (uses Invoice fixture)
    - 83-RESEARCH.md Pattern 5 (lines 271-287) + Pitfall 2 (lines 344-348)
  </read_first>
  <behavior>
    data-purge:
    - CANNOT hard-delete in-window: a fixture-mapped row whose `deletedAt` is older than 90d but inside the 4yr/7yr window is NOT purged.
    - PURGES after window: a retained row past its window IS swept; a non-retained row past 90d IS swept (default preserved).
    soft-delete extension:
    - a retained model in-window cannot be hard-deleted (the explicit retained-window case is guarded); a non-retained model behaves exactly as today (Invoice fixture).
  </behavior>
  <action>
    In `data-purge.ts`, import `getRetentionCutoff` from `@contractor-ops/db` and compute a per-model cutoff: `cutoffFor(model, now, flatCutoff) = getRetentionCutoff(model, now) ?? flatCutoff` — retained models use the 4yr/7yr cutoff, all others keep the flat `RETENTION_DAYS=90` cutoff. Apply it to every per-model `deleteMany({ where: { deletedAt: { not: null, lt: cutoff } } })`. Keep `ctx.log` (createCronLogger) — no `console.*`. Because `MODEL_RETENTION_TYPE` is EMPTY (D-06), production behavior is identical to today for all current models — the test injects a fixture entry to prove the wiring. In `soft-delete.ts`, extend the `$allModels` delete hook so that for a model with a retention rule still inside its window, a real hard-delete is blocked (the explicit retained-window guard — the extension already converts delete→soft-delete; guard the in-window hard-delete case). Extend both test files: data-purge for cannot-delete-in-window + purges-after-window (fixture-mapped model), soft-delete for the retained-in-window guard (Invoice fixture).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/cron-worker test data-purge && pnpm --filter @contractor-ops/db test soft-delete</automated>
  </verify>
  <done>data-purge consults `getRetentionCutoff` per model (retained→window, else→90d) on the base-prisma hard-delete path; soft-delete extension blocks in-window hard-delete of a retained model; non-retained behavior unchanged; both scoped tests GREEN (VALIDATION.md SC#3 cannot-delete-in-window / purges-after-window / soft-delete rows).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: gdpr RODO erasure — retention-exemption branch (soft-delete-with-citation, audit-logged)</name>
  <files>packages/api/src/routers/compliance/gdpr.ts, packages/api/src/routers/__tests__/gdpr.test.ts</files>
  <read_first>
    - packages/api/src/routers/compliance/gdpr.ts (:17 deleteByOrgAndCount, :33 softDeleteByOrgAndCount, requestErasure mutation, :247 writeAuditLog, the "90 days" erasure message)
    - packages/api/src/routers/__tests__/gdpr.test.ts (extend)
    - packages/api/src/services/audit-writer.ts — writeAuditLog signature (sensitive-mutation audit pattern)
    - 83-RESEARCH.md Pattern 6 (lines 289-299) + Pitfall 3 (lines 350-353)
  </read_first>
  <behavior>
    - A retained (fixture-mapped) model is routed through `softDeleteByOrgAndCount` (NOT `deleteByOrgAndCount`) and appears in a `retainedUnderStatute` summary section with its statutory citation; `writeAuditLog` records the retention-blocked erasure attempt.
    - A non-retained model is erased exactly as today (no behavior change).
    - The erasure summary message no longer claims full deletion for records under an active statutory hold (RODO-honesty: never says "erased" for held records).
  </behavior>
  <action>
    In `gdpr.ts` `requestErasure`, branch on whether a model has a retention rule (look it up via `MODEL_RETENTION_TYPE` / `getRetentionCutoff` from `@contractor-ops/db`). For a retained model: route through the existing `softDeleteByOrgAndCount` (NOT `deleteByOrgAndCount`), add it to a `retainedUnderStatute` summary section carrying the statutory citation (e.g. "IRS 1099-NEC: 4-year retention, 26 CFR 1.6001-1" — annotate "needs legal/tax-adviser verification before production deploy"), and `writeAuditLog` the retention-blocked erasure attempt (sensitive mutation). Update the existing erasure summary copy so it does not claim full erasure for held records (the message currently hard-codes "90 days"). Non-retained models keep the current path. Phase 83 ships the BRANCH; the production model list is empty until Phase 86 — verify against a fixture model in `gdpr.test.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test gdpr</automated>
  </verify>
  <done>gdpr erasure soft-deletes-with-exemption for retained (fixture) models — `retainedUnderStatute` summary + statutory citation + `writeAuditLog`, never `deleteByOrgAndCount`; non-retained models unchanged; gdpr test GREEN (VALIDATION.md SC#3 gdpr row).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron base-prisma → Postgres DELETE | the only path that can permanently destroy a retained record (no soft-delete extension) |
| RODO erasure request → retained tax record | user-initiated deletion vs statutory retention obligation |
| retention resolver (packages/db) ↔ 3 chokepoints | one source of truth consumed by three physically-separate processes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-04-01 | Tampering / Repudiation | data-purge cron hard-delete | mitigate | per-model `getRetentionCutoff` cutoff — retained models use 4y/7y window, not flat 90d; THE load-bearing D-05 path; covered by cannot-delete-in-window test |
| T-83-04-02 | Tampering | soft-delete extension | mitigate | retained model in-window cannot be hard-deleted at the `$allModels` delete hook; non-retained unchanged (Invoice fixture) |
| T-83-04-03 | Repudiation | RODO over-claim during statutory hold | mitigate | gdpr summary surfaces `retainedUnderStatute` + citation; soft-delete-with-exemption (never deleteByOrgAndCount); `writeAuditLog` on the blocked erasure; summary never claims "erased" for held records |
| T-83-04-04 | Elevation of Privilege | globalModels (IDOR) | accept | no tenant-owning models added this phase; tax models stay OUT of globalModels (Phase 86 concern; reaffirmed) |
| T-83-04-05 | Information Disclosure | EU-pinned base-prisma purge misses US/ME rows | accept | PRE-EXISTING cross-region gap (Pitfall 6) — D-05 (no in-window hard-delete) is satisfied trivially for US rows the EU-pinned purge can't see; region fan-out recorded as deferred ops item, out of Phase 83 scope |
| T-83-04-SC | Tampering | npm installs | accept | zero packages installed (RESEARCH § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/db test retention-policy soft-delete` GREEN.
- `pnpm --filter @contractor-ops/cron-worker test data-purge` GREEN (cannot-delete-in-window + purges-after-window).
- `pnpm --filter @contractor-ops/api test gdpr` GREEN (retention-exemption branch).
- `pnpm lint:audit-log lint:logs` pass (writeAuditLog on the blocked erasure; createCronLogger in data-purge; no console.*).
- `pnpm typecheck` GREEN across db / cron-worker / api.
</verification>

<success_criteria>
- [ ] ONE retention resolver in `packages/db/src/retention-policy.ts` (RETENTION_YEARS + EMPTY MODEL_RETENTION_TYPE + getRetentionCutoff), reusable by Theme B AKTA-02 (D-04).
- [ ] All three chokepoints consult the same resolver: soft-delete extension, data-purge cron (base-prisma hard-delete), gdpr erasure (D-05).
- [ ] No early hard-delete of a retained record in-window; non-retained models keep the 90-day sweep.
- [ ] gdpr erasure soft-deletes-with-exemption + statutory citation + writeAuditLog for retained records (D-05 #3); verified against the Invoice fixture (D-06).
- [ ] Statutory-citation copy annotated for legal/tax-adviser verification; EU-pinned-purge region fan-out recorded as deferred (Pitfall 6).
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/83-theme-a-us-region-infrastructure/83-04-SUMMARY.md` when done
</output>
