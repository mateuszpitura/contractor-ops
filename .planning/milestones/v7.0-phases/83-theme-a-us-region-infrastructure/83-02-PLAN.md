---
phase: 83-theme-a-us-region-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["83-01"]
files_modified:
  - packages/auth/src/config.ts
  - packages/auth/src/__tests__/org-creation-region.test.ts
  - packages/api/src/services/org-cache.ts
  - packages/api/src/middleware/tenant.ts
  - packages/api/src/services/org-definition-sync.ts
  - apps/api/src/routes/oauth.ts
  - packages/api/src/routers/portal/portal-shared.ts
  - packages/api/src/middleware/portal-auth.ts
  - packages/api/src/services/ksef-sync-orchestrator.ts
  - apps/api/src/routes/idp-deprovisioning.ts
  - packages/db/scripts/seed-dev.ts
  - packages/api/src/__tests__/tenant-region.test.ts
autonomous: true
requirements: [US-INFRA-01]
must_haves:
  truths:
    - "A US-billing org create writes dataRegion='US' via the organizationCreation.beforeCreate hook; EU/ME default unchanged; dataRegion set ONLY at creation (D-01 immutable)"
    - "A US org's authenticated request resolves the us-east-1 Prisma client via getRegionalClient('US') at the tenant boundary; cross-region read replicas remain off by default (D-02)"
    - "Every dataRegion cast site widened from 'EU'|'ME' to the shared DataRegion type so 'US' is type-legal end-to-end (no silent US drop) (D-01 planner-resolved anchor)"
  artifacts:
    - path: "packages/auth/src/config.ts"
      provides: "organizationCreation.beforeCreate US-assignment hook + additionalFields billing-country (D-01)"
      contains: "organizationCreation"
    - path: "packages/api/src/services/org-cache.ts"
      provides: "OrgMeta.dataRegion typed DataRegion (widened from string)"
      contains: "DataRegion"
    - path: "packages/api/src/__tests__/tenant-region.test.ts"
      provides: "US org → US client routing assertion"
      contains: "US"
  key_links:
    - from: "packages/api/src/middleware/tenant.ts"
      to: "getRegionalClient(region)"
      via: "region = meta.dataRegion ?? 'EU' (DataRegion-typed)"
      pattern: "getRegionalClient"
    - from: "packages/auth/src/config.ts beforeCreate"
      to: "Organization.dataRegion"
      via: "billing-country additionalFields → dataRegion mapping"
      pattern: "dataRegion"
---

<objective>
US-INFRA-01 — durable per-org US DB routing (creation → resolution).

(1) Add the single origin of `dataRegion='US'`: a Better Auth
`organizationCreation.beforeCreate` hook (org-create no longer flows through a tRPC
mutation — those were removed; today every org defaults to EU). The hook maps a US
billing-country selection to `dataRegion: 'US'`; EU/ME (or absent) stays EU.
`dataRegion` is set ONLY here — it is immutable (D-01).
(2) Widen every scattered `as 'EU' | 'ME'` / `?? 'EU'` cast site to the shared
`DataRegion` type so `'US'` is type-legal end-to-end and never silently dropped on
the hot path that feeds `getRegionalClient`.

Purpose: This is US-INFRA-01's success criterion 1 — a US-billing org's requests
resolve a `us-east-1` Prisma client. `getRegionalClient('US')` already resolves
(Phase 82); this plan makes US assignable at creation and type-legal at every consumer.
Replicas stay off by default (D-02 — `REPLICA_ENV_MAP['US']` stays inert).
Output: the beforeCreate hook, widened cast sites, DataRegion-typed `OrgMeta`, and
the US-routing tenant test GREEN.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v7.0-phases/83-theme-a-us-region-infrastructure/83-CONTEXT.md
@.planning/milestones/v7.0-phases/83-theme-a-us-region-infrastructure/83-RESEARCH.md

<interfaces>
<!-- From research (in-tree 2026-06-07). Use directly. -->

From packages/db/src/region.ts:
  export type DataRegion = (typeof SUPPORTED_REGIONS)[number];  // 'EU' | 'ME' | 'US'
  export function getRegionalClient(region: string): PrismaClient;  // 'US' resolves (Phase 82); lazy-throw if DATABASE_URL_US unset

From packages/auth/src/config.ts:
  // :252 — existing databaseHooks.session.create.before (the pattern to mirror)
  // :457 — organization({ ac, roles, ... }) plugin block (NO organizationCreation hook today)
  // org-create runs through Better Auth authClient.organization.create — tRPC create/update were removed (organization.ts:16)

From packages/api/src/services/org-cache.ts:
  interface OrgMeta { dataRegion: string; /* ... */ }   // :38 — widen string → DataRegion
  export function getOrgMeta(orgId: string): Promise<OrgMeta | null>;  // 5-min TTL, invalidation on update

CAST SITES to widen 'EU'|'ME' → DataRegion (from RESEARCH Pitfall 4, lines 357-365):
  packages/api/src/middleware/tenant.ts:106          region = meta?.dataRegion ?? 'EU'   (hot path → getRegionalClient)
  packages/api/src/services/org-definition-sync.ts:398   (c.organization.dataRegion ?? 'EU') as 'EU' | 'ME'
  apps/api/src/routes/oauth.ts:377                   (org.dataRegion ?? 'EU') as 'EU' | 'ME'
  packages/api/src/routers/portal/portal-shared.ts:23    ?? 'EU'
  packages/api/src/middleware/portal-auth.ts:69          ?? 'EU'
  packages/api/src/services/ksef-sync-orchestrator.ts:507  ?? 'EU'
  apps/api/src/routes/idp-deprovisioning.ts:58           ?? 'EU'
  packages/db/scripts/seed-dev.ts:900,910            pickRegion/buildOrgs ('EU'|'ME')[] union + as 'EU'|'ME'
  // api-key-auth.ts:63 already DataRegion-typed (benign) — no change.

NOTE: regional-storage.ts:39 cast is owned by Plan 03 (do NOT touch it here).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the organizationCreation.beforeCreate US-assignment hook (D-01) + billing-country additionalFields</name>
  <files>packages/auth/src/config.ts, packages/auth/src/__tests__/org-creation-region.test.ts</files>
  <read_first>
    - packages/auth/src/config.ts (:252 databaseHooks.session.create.before pattern; :457 organization() plugin block)
    - packages/auth/src/__tests__/org-creation-region.test.ts (the RED scaffold from Plan 01 — turn it GREEN)
    - 83-RESEARCH.md Pattern 2 (lines 179-200) + Assumption A1 / Open Q1 (lines 419, 429-432) — confirm how the SPA passes billing country into authClient.organization.create
  </read_first>
  <behavior>
    - A create payload carrying a US billing-country selection → `dataRegion: 'US'`.
    - An EU/ME selection (or absent billing country) → `dataRegion: 'EU'` (schema @default(EU) intent preserved).
    - `dataRegion` is set ONLY in `beforeCreate` — no afterCreate/update path mutates it (D-01 immutable).
  </behavior>
  <action>
    Add an `organizationCreation: { beforeCreate }` block to the `organization({ ... })` plugin in `config.ts` (mirror the existing `databaseHooks.session.create.before` shape). Resolve `dataRegion` from a billing-country/region signal on the create payload and return `{ data: { ...organization, dataRegion } }`. Per Open Q1 (A1): confirm how the billing-country selection reaches `authClient.organization.create` — if it is not already carried, add a `billingCountry` (or `region`) entry to the org plugin `additionalFields` schema (Zod-validated per CLAUDE.md V5) so the create boundary validates it. Map a US billing selection → `'US'`; everything else → `'EU'`. Never set `dataRegion` in `afterCreate`/`update` (immutable, D-01). Turn the Plan-01 RED scaffold GREEN.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/auth test org-creation-region</automated>
  </verify>
  <done>beforeCreate hook maps US billing → dataRegion='US', EU/ME → 'EU'; billing-country input Zod-validated via additionalFields; org-creation-region test GREEN (VALIDATION.md SC#1 creation row).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen dataRegion cast sites to DataRegion + type OrgMeta.dataRegion; assert US routing in tenant test</name>
  <files>packages/api/src/services/org-cache.ts, packages/api/src/middleware/tenant.ts, packages/api/src/services/org-definition-sync.ts, apps/api/src/routes/oauth.ts, packages/api/src/routers/portal/portal-shared.ts, packages/api/src/middleware/portal-auth.ts, packages/api/src/services/ksef-sync-orchestrator.ts, apps/api/src/routes/idp-deprovisioning.ts, packages/db/scripts/seed-dev.ts, packages/api/src/__tests__/tenant-region.test.ts</files>
  <read_first>
    - 83-RESEARCH.md Pitfall 4 (lines 356-367) — the enumerated cast sites with exact line numbers
    - packages/api/src/middleware/tenant.ts (:106 region resolution; :137 ctx.db client build)
    - packages/api/src/services/org-cache.ts (:38 OrgMeta.dataRegion: string)
    - packages/api/src/__tests__/tenant-region.test.ts (extend with the US-routing assertion)
  </read_first>
  <behavior>
    - tenant middleware: a US org's `meta.dataRegion === 'US'` routes to `getRegionalClient('US')` (US client), not EU.
    - `OrgMeta.dataRegion` is typed `DataRegion` (not `string`) so US is type-legal through the cache.
    - `?? 'EU'` fallbacks preserved (US is only ever assigned at creation, never defaulted).
  </behavior>
  <action>
    Replace each narrow `as 'EU' | 'ME'` cast and narrow-typed `?? 'EU'` site listed in `<interfaces>` with the shared `DataRegion` import from `@contractor-ops/db`. Widen `OrgMeta.dataRegion: string` → `DataRegion` in `org-cache.ts:38`. Widen `seed-dev.ts` `pickRegion`/`buildOrgs` region union from `('EU'|'ME')[]` to `DataRegion[]` (seed still only picks EU/ME — no US seed org this phase, deferred from Phase 82). Keep every `?? 'EU'` fallback as-is (safe default; US must be explicitly assigned). Do NOT touch `regional-storage.ts:39` (Plan 03 owns it) and do NOT touch `api-key-auth.ts:63` (already DataRegion-typed). Extend `tenant-region.test.ts` with an assertion that a US `OrgMeta.dataRegion` routes the tenant client to `getRegionalClient('US')`.
  </action>
  <verify>
    <automated>pnpm typecheck --filter @contractor-ops/api && pnpm --filter @contractor-ops/api test tenant-region</automated>
  </verify>
  <done>No `as 'EU' | 'ME'` casts remain in the listed files; `OrgMeta.dataRegion` is `DataRegion`-typed; tenant-region test asserts US org → US client; `pnpm typecheck --filter @contractor-ops/api` and the scoped test are GREEN (VALIDATION.md SC#1 routing rows).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SPA → Better Auth organization.create | billing-country selection crosses here (untrusted client input) |
| session → tenant middleware | region resolved from `org.dataRegion` (server-side), never from client input |
| US org request → regional Prisma client | wrong-region client = cross-region data leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-02-01 | Tampering | dataRegion post-create | mitigate | `dataRegion` set ONLY in `beforeCreate` (D-01 immutable); no afterCreate/update path exposes it — mass-assignment / region-tamper blocked |
| T-83-02-02 | Information Disclosure | tenant client routing | mitigate | region resolved from `org.dataRegion` at the tenant boundary → `getRegionalClient(region)`; widened DataRegion typing prevents silent US→EU drop; `lint:region-leakage` gate run before done |
| T-83-02-03 | Spoofing/Tampering | billing-country create input | mitigate | billing-country Zod-validated via `additionalFields` at the Better Auth boundary (CLAUDE.md V5); never trusted raw |
| T-83-02-04 | Elevation of Privilege | globalModels | accept | no tenant-owning models added this phase; Organization stays in globalModels as before (no change) |
| T-83-02-SC | Tampering | npm installs | accept | zero packages installed (RESEARCH § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/auth test org-creation-region` GREEN.
- `pnpm typecheck --filter @contractor-ops/api` GREEN (no narrow-cast type errors).
- `pnpm --filter @contractor-ops/api test tenant-region` GREEN (US → US client).
- `pnpm lint:region-leakage` passes (no new base-prisma access to region-scoped models).
- grep confirms no `as 'EU' | 'ME'` remains in the listed files (except regional-storage.ts, owned by Plan 03).
</verification>

<success_criteria>
- [ ] beforeCreate hook assigns dataRegion='US' for US billing; EU/ME default unchanged; immutable after.
- [ ] All listed cast sites widened to DataRegion; OrgMeta.dataRegion is DataRegion-typed.
- [ ] A US org routes to getRegionalClient('US'); replicas remain off by default (D-02, REPLICA_ENV_MAP['US'] inert — no active replica path wired).
- [ ] Scoped auth + api tests + filtered typecheck GREEN.
</success_criteria>

<output>
Create `.planning/milestones/v7.0-phases/83-theme-a-us-region-infrastructure/83-02-SUMMARY.md` when done
</output>
