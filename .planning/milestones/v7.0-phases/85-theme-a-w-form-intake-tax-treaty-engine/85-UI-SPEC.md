---
phase: 85
slug: theme-a-w-form-intake-tax-treaty-engine
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-06-16
---

# Phase 85 — UI Design Contract

> Visual and interaction contract for the W-9 / W-8BEN / W-8BEN-E intake wizards (portal),
> the treaty-claim display, and the staff read/track surface. Generated by gsd-ui-researcher,
> verified by gsd-ui-checker.
>
> **This phase consumes an established design system — it does not define new tokens.**
> The "Precision Craft" / Workbench-Atelier token layer (`@contractor-ops/ui/styles/tokens.css`
> + `apps/web-vite/src/styles/globals.css`) is the source of truth. Every value below is
> pre-populated from that layer; the contract here REFERENCES tokens, never hardcodes hex.
> This is a PRODUCT surface: `frontend-design` then `impeccable` + `PRODUCT.md` register —
> NEVER `design-taste-frontend`.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | shadcn (initialized) | `packages/ui/components.json` |
| Preset | `base-nova` (style), `neutral` base color, CSS variables, `rtl: true` | `packages/ui/components.json` |
| Component library | shadcn primitives over Radix/Base-UI (`@/components/shadcn`) + reui registry (`@/components/reui`) | `components.json` aliases |
| Icon library | lucide (`lucide-react`) | `components.json` `iconLibrary` |
| Font — body/UI | **Outfit** (`--font-sans`), weights 300–900 | `globals.css` `--font-sans` |
| Font — display/headings | **Bricolage Grotesque** (`--font-display`) | `globals.css` `--font-display` |
| Font — mono/numeric (TIN, rates) | **JetBrains Mono** (`--font-mono`) | `globals.css` `--font-mono` |
| Font — RTL (Arabic) | **Noto Sans Arabic** (auto-swapped via `:root[dir="rtl"]`) | `globals.css` |

**Design tokens are consumed, not authored.** Use Tailwind semantic utilities
(`bg-background`, `bg-card`, `text-muted-foreground`, `text-primary`, `border-border`,
`bg-destructive`, `text-success`, `text-warning`) — never raw oklch/hex in this phase.

---

## Spacing Scale

The repo already ships an 8-point scale plus three semantic vertical-rhythm tokens. Use the
semantic tokens for layout rhythm and the numeric scale for component-internal spacing.

| Token | Value | Tailwind | Usage in this phase |
|-------|-------|----------|---------------------|
| xs | 4px | `gap-1` / `p-1` | Icon ↔ label gaps inside stepper items, pill icon gap |
| sm | 8px | `space-y-2` / `gap-2` | Label ↔ control (`--space-stack-tight`); field error spacing |
| md | 16px | `space-y-4` / `p-4` | Rows inside a wizard-step card (`--space-card-gap`); card padding |
| lg | 24px | `space-y-6` / `gap-6` | Between top-level page sections (`--space-section-gap`); stepper nav gap |
| xl | 32px | `p-8` | Wizard container outer padding on ≥sm |
| 2xl | 48px | `py-12` | Empty-state vertical breathing room |
| 3xl | 64px | `py-16` | Full-screen portal wizard top/bottom inset (mirrors `organization-onboarding`) |

Semantic tokens (preferred for rhythm): `space-y-stack-tight` (8px), `space-y-card-gap` (16px),
`space-y-section-gap` (24px) — defined in `tokens.css`, bridged in `globals.css` `@theme inline`.

Exceptions:
- **Touch targets:** interactive controls (stepper next/back, reveal toggle, checkboxes) must be
  ≥44×44px effective hit area on touch (portal is contractor-facing/mobile). Use `size-9`+ wrappers
  or `min-h-11` on buttons; visual size may stay smaller but the hit area must meet 44px.
- **Radius:** `--radius: 0.625rem` (10px) base; cards use `rounded-xl`, fields `rounded-md`.
  No new radius values.

---

## Typography

The repo uses a **fluid `clamp()` scale** (`globals.css` `@theme inline`). Body base is 14px at
1.5 line-height (`body` rule). Declared roles for this phase (4 sizes, 2 weights):

| Role | Size token | Tailwind | Weight | Line Height | Usage |
|------|-----------|----------|--------|-------------|-------|
| Display (wizard title, step heading) | `--text-2xl` → `--text-3xl` (clamp ~22–30px) | `text-2xl` / `text-3xl`, `font-display` | semibold (600) | 1.15 (`leading-tight`) | Wizard H1, success-receipt heading |
| Heading (step card title, section) | `--text-lg` (clamp ~16–18px) | `text-lg`, `font-display` | semibold (600) | 1.75 | Per-step card `<h2>`, staff card title |
| Body (field values, help, instructions) | `--text-sm` / `--text-base` (clamp 13–15px) | `text-sm` | regular (400) | 1.5 | Field help text, perjury-attestation paragraph, treaty explanation |
| Label / meta (field labels, status pill, last-4) | `--text-xs` → `--text-sm` (clamp 11–14px) | `text-xs` / `text-sm` | regular (400) | 1.25–1.5 | Form `<Label>`, status badges, "Article 7" caption |

**Two weights only: 400 and 600 — no exceptions.** `font-normal` (400) for ALL body, values, and
field labels; `font-semibold` (600) for headings and the primary CTA. **Field labels are
distinguished from body by SIZE (`text-xs` / `text-sm`), never by weight.** The shadcn `Label`
default (`font-medium`/500) MUST be overridden to `font-normal` via Tailwind (`className="font-normal"`)
wherever it is used in this phase. No `font-medium` (500) anywhere.

**Numeric/monospace:** TIN/SSN last-4, FTIN, treaty rate %, and "Article N" render in
`font-mono tabular-nums` (matches `SsnMaskedReveal` and `hero-metric` precedent) so digits align.

---

## Color

60/30/10 split maps onto the existing token layer. **No new colors** — the phase reuses the
teal-primary "Precision Craft" palette. Accent (teal `--primary`) is reserved, not sprayed.

| Role | Token | Tailwind | Usage |
|------|-------|----------|-------|
| Dominant (60%) | `--background` / `--surface-0` | `bg-background` | Page + wizard backdrop, form well |
| Secondary (30%) | `--card` / `--secondary` / `--muted` | `bg-card`, `bg-muted` | Wizard step cards, staff status cards, sidebar, read-only summary panels |
| Accent (10%) | `--primary` (Deep Teal `oklch(0.44 0.145 178)` light / `0.65` dark) | `bg-primary`, `text-primary` | See reserved list ↓ |
| Destructive | `--destructive` | `bg-destructive`, `text-destructive` | Validation errors, "override the auto-rate" warning copy, supersede confirmation |

**Focal point:** Primary focal point of every portal wizard screen is the **active wizard step
card**; specifically the **primary CTA at the bottom of each step card** anchors the screen (single
accent-filled button, largest interactive target, end of the reading path). Everything else —
stepper, fields, help text — supports getting the user to that one action.

**Accent (teal `--primary`) reserved EXCLUSIVELY for:**
1. The single primary CTA per step (`Continue to {next step}`, `Sign & submit`).
2. The active/current stepper indicator + the progress bar fill (`step-current-ring`, `[data-slot="progress-indicator"]`).
3. Focus-visible ring on inputs/buttons (`--ring` = teal; `focus-glow` / `[data-form-control]:focus-visible`).
4. The reveal toggle text/icon (`text-primary`, matching `SsnMaskedReveal`).
5. Inline links (e.g. "Why this rate?", treaty-article reference link).

Accent is **never** used for: every interactive element, body text, card borders, secondary
buttons (those use `variant="secondary"` / `variant="outline"` / `variant="ghost"`), or decoration
on the staff table rows.

**Semantic status colors (already defined — use, don't redefine):**
- `--success` (green) — form status `ACTIVE`, treaty `verified`, "submitted" receipt.
- `--warning` (amber) — form `expiring`/`expired`, treaty rate defaulted to 30% statutory (no treaty), override-pending.
- `--info` (blue) — `DRAFT` status, informational treaty notes.
- `--destructive` — errors, superseded, validation failures, the override-reason requirement.

Status pills MUST reuse the `Badge` variants (`success` / `warning` / `secondary` / `destructive`)
exactly as `UspsAddressStatusPill` does — same icon+label+tooltip idiom, same `data-status` attr.

**Adviser-deferred banner (D-04):** every seeded treaty rate/article shown to a user carries a
muted, non-alarming informational note ("Rates pending verification by a tax adviser") rendered in
`text-muted-foreground` with an `info`-toned icon — NOT destructive/warning styling (it is a posture
note, not an error).

---

## Copywriting Contract

All strings are i18n keys across **en / en-US / de / pl / ar(RTL)** (D-13). en-US inherits en via
fallback-parity (P84 D-04); de/pl/ar require exact-parity keys in the same change set or `i18n:parity`
red-CIs. Copy below is the **en source-of-truth**; verb+noun and tone are the contract.

| Element | Copy (en source) |
|---------|------------------|
| Wizard entry CTA (portal landing / staff "request") | **Start tax form** (contractor) / **Request tax form** (staff) |
| Determination step heading | **Confirm your tax form** |
| Determination body | *"Based on your profile we'll prepare your **{formName}**. If this isn't right, you can change it below."* (formName = W-9 / W-8BEN / W-8BEN-E) |
| Step primary CTA (intermediate) | **Continue to {next step}** (e.g. "Continue to certify", "Continue to treaty claim") — verb+noun for screen-reader clarity, never a bare "Continue" |
| Final step primary CTA | **Sign & submit** |
| Back / secondary | **Back** (ghost/outline secondary nav; never accent) |
| Save-and-finish-later | **Save draft** |
| Treaty auto-populate caption | *"Treaty rate applied automatically: **{rate}%** under **{article}** ({country})."* |
| Treaty no-match (30% default) | *"No tax treaty applies for **{country}** — the default **30%** statutory rate is used."* (warning tone) |
| Treaty override affordance (staff) | **Override rate** → requires a reason field labeled *"Reason for override (required)"* |
| Perjury attestation (required checkboxes) | Reproduce IRS "under penalties of perjury…" language verbatim per form (W-9 Part II / W-8BEN Part III / W-8BEN-E Part XXX); the **certification text is a domain string, not freely reworded copy** — adviser-deferred (D-04/D-11). |
| Typed-name field label | **Type your full legal name to sign** |
| Submission success heading | **Tax form submitted** |
| Submission success body | *"Your {formName} is on file. We've recorded your signature on {date}. You can view or update it anytime."* (links to read-only receipt) |
| Empty state heading (portal, no forms yet) | **No tax form on file** |
| Empty state body (portal) | *"Complete your tax status form so we can pay you correctly. It takes a few minutes."* + **Start tax form** CTA |
| Empty state heading (staff, no forms for contractor) | **No tax form submitted yet** |
| Empty state body (staff) | *"This contractor hasn't completed a W-form. Request one to start the process."* + **Request tax form** CTA |
| Loading state | Skeleton (`animate-shimmer`) for the wizard shell + per-step card; `Loader2` spinner inside the CTA during submit ("Submitting…"). Never a blank screen. |
| Error state (load failure) | **Couldn't load your tax form** — *"Something went wrong. Refresh to try again."* + **Reload tax form** action |
| Error state (submit failure) | Inline `role="alert"` under the CTA: *"We couldn't submit your form. Check your entries and try again."* — preserves entered data, never wipes the wizard |
| Field validation error | Per-field `role="alert"` `text-destructive` text below the input (mirrors `organization-onboarding`) |

**Destructive / high-stakes actions + confirmation approach:**

| Action | Confirmation |
|--------|--------------|
| **Sign & submit** (creates an immutable, legally-attested record) | Final-step explicit gate: all perjury checkboxes checked + typed legal name matches + an explicit "I understand this is a legal signature" affirmation before the CTA enables. This is intentional friction, not a destructive-delete dialog. |
| **Re-certify / replace an active form** (supersedes the prior signed record) | `AlertDialog` confirmation: *"Submitting a new form will replace your current {formName} on file. The previous version is kept for records but no longer active. Continue?"* — Confirm = `default`, Cancel = `outline`. (Append-only; never a hard delete.) |
| **Override the auto-resolved treaty rate** (staff only) | Inline gate: the **reason field is required** and the CTA stays disabled until a reason is entered; submit writes `writeAuditLog`. Warning-toned helper text explains the auto-detected value is preserved for audit. |
| **Reveal full SSN** (staff summary, W-9 records) | Reuse `SsnMaskedReveal` exactly — gated by `contractorPii:read`, audit-logged on the server, never revealed on hover/focus/mount, control ABSENT (not disabled) without permission. |

No literal record-deletion exists in this phase (records are immutable + supersede-chained), so no
"delete" destructive dialog is specified.

---

## Component Inventory (reuse contract — do not reinvent)

| Need | Reuse | Source |
|------|-------|--------|
| Multi-step wizard frame + RHF idiom | `reui` `Stepper / StepperNav / StepperItem / StepperIndicator / StepperTitle / StepperSeparator` + `react-hook-form` + `@hookform/resolvers` (zod) | `apps/web-vite/src/components/onboarding/organization-onboarding.tsx`; `packages/ui/src/components/reui/stepper.tsx` |
| Step indicator + progress | `OnboardingStepIndicator` pattern + `Progress` (`[data-slot="progress-indicator"]` gradient fill) | `organization-onboarding.tsx:55-83` |
| Closest tax-intake analog | `zatca/onboarding-wizard.tsx` + `tax-details-form.tsx` | `apps/web-vite/src/components/zatca/` |
| Dialog-hosted wizard (if modal, not full page) | `contractor-wizard/wizard-dialog.tsx`; DialogBody (scroll) + DialogFooter (sticky actions) convention | `contractor-wizard/`, `packages/ui/src/components/shadcn/dialog.tsx` |
| SSN masked + gated reveal (W-9 staff summary) | `SsnMaskedReveal` (verbatim — `contractorPii:read` gate, audit, no DOM leak) | `contractors/compliance/ssn-masked-reveal.tsx` |
| Advisory status pill (form status, treaty-verified, expiry) | `UspsAddressStatusPill` idiom (Badge variant map + tooltip + `data-status`) | `contractors/usps-address-status-pill.tsx` |
| Country / entity selects | shadcn `Select` (Controller-bound) | `organization-onboarding.tsx:134-160` |
| Inputs, labels, buttons, badges, cards, checkboxes, tooltip, alert-dialog | shadcn primitives `@contractor-ops/ui/components/shadcn/*` | `packages/ui/src/components/shadcn/` |
| Entrance animation | `AnimateIn` (fade+slide, staggered `delay`) | `components/shared/animate-in.tsx` |
| Translations | `useTranslations('<Namespace>')` hook | `i18n/useTranslations.ts` |

**Web-vite layering (enforced — `check:web-vite-data-layer`):** page (thin composer, Suspense +
permissions, no tRPC) → container (`*-container.tsx`, owns loading/empty/error, no direct tRPC) →
hook (`use-*.ts`, the sole tRPC/React-Query boundary) → component (presentational, props in/JSX out).
Portal wizard mutations hit `portalAppRouter` only (D-07); staff read/track hits the staff `taxRouter`.

---

## State & Accessibility Contract (mandatory per surface)

Every wizard step and the staff surface MUST specify all four states and meet WCAG:

| Surface | Loading | Empty | Error | A11y notes |
|---------|---------|-------|-------|-----------|
| Portal wizard shell | Skeleton card (`animate-shimmer`) while determination/profile loads | "No tax form on file" + Start CTA (pre-wizard) | "Couldn't load your tax form" + Reload tax form | Stepper `aria-label` (wizard progress), `aria-current="step"` on active item; focus moves to the new step's heading on Continue/Back; each field `aria-invalid` + `aria-describedby` error wiring (mirror `organization-onboarding`) |
| Per-step form | Disabled inputs + CTA spinner during async validation/submit | n/a (always has fields) | Inline `role="alert"` per field + a step-level submit error region | Semantic `<form>`, `<Label htmlFor>`, `noValidate`, required-checkbox group has a group label |
| Attestation step | CTA spinner "Submitting…" on submit | n/a | `role="alert" aria-live="polite"` submit-failure region, data preserved | Perjury checkboxes are real `<input type=checkbox>` with visible labels; typed-name field accessible name = visible label (Label-in-Name) |
| Treaty claim display | Inline skeleton on the rate/article line while resolving | n/a (auto-populated or 30% default) | Falls back to 30% statutory + warning note if lookup fails | Rate/article announced via `aria-live="polite"` when it auto-populates so SR users hear the change |
| Staff status surface | Table/card skeleton rows | "No tax form submitted yet" + Request CTA | Card-level error + Reload tax form | Status pills carry `aria-label`; full SSN absent (not just hidden) without `contractorPii:read` |

**RTL (ar):** the wizard MUST work right-to-left. Use logical properties already in the codebase
(`ms-*`/`me-*`, `ps-*`/`pe-*`, `text-start`/`text-end`), and directional icons flip via `rtl:rotate-180`
(see `organization-onboarding` `ArrowRight`). Stepper separators and progress direction follow `dir`.
Noto Sans Arabic auto-applies under `:root[dir="rtl"]`. No `ml-/mr-/pl-/pr-/text-left/right` literals.

**Contrast:** all text/status combinations use the token pairs (`*-foreground` on its surface) which
already meet WCAG AA in this design system; status badges keep their paired foregrounds. Do not place
`text-primary` body text on `bg-primary`.

---

## Registry Safety

`components.json` declares third-party registries (`@magic`, `@aceternity`, `@cult`, `@origin`,
`@tailark`, `@reui`, `@shadcnio`). **This phase pulls ZERO new blocks from any registry** — every
component it needs already exists in-tree (`@contractor-ops/ui` shadcn + the already-installed `@reui`
`stepper`). No `npx shadcn add` is in scope.

| Registry | Blocks used this phase | Safety Gate |
|----------|------------------------|-------------|
| shadcn official / `@contractor-ops/ui` (in-tree) | Stepper (reui, already vendored), Input, Label, Button, Badge, Card, Select, Checkbox, Tooltip, AlertDialog, Dialog, Progress | not required — already in-tree, no fetch |
| `@reui` / `@magic` / `@aceternity` / `@cult` / `@origin` / `@tailark` / `@shadcnio` | **none** | not invoked — no new third-party block enters the contract this phase |

If the planner/executor later decides a NEW third-party block is needed, the registry vetting gate
(`npx shadcn view {block} --registry {url}` + suspicious-pattern scan) MUST run before it enters the
contract — that is out of scope for this draft because no such block is declared.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
