---
phase: 85-theme-a-w-form-intake-tax-treaty-engine
plan: 03
type: execute
wave: 3
depends_on: ["85-01", "85-02"]
files_modified:
  - packages/api/src/services/tax-form.service.ts
  - packages/api/src/routers/portal/portal-tax-form-router.ts
  - packages/api/src/routers/portal/portal.ts
  - packages/api/src/routers/core/tax.ts
  - packages/api/src/root.ts
  - packages/api/src/routers/portal/__tests__/tax-form.test.ts
  - packages/api/src/routers/core/__tests__/tax-form-staff.test.ts
requirements: [US-FORM-01, US-FORM-02, US-LOC-02, US-LOC-03]
autonomous: true
must_haves:
  truths:
    - "A contractor can submit a W-9/W-8BEN/W-8BEN-E from the portal; it inserts an immutable record + audit"
    - "Re-certification inserts a NEW row and supersedes the prior ACTIVE one (never mutates a signed record)"
    - "Full SSN never appears in the snapshot or the portal response"
    - "ESIGN attestation captures typed name + server-derived timestamp + IP + contractorId into the snapshot"
    - "Staff can read/track form status without leaking full SSN; reveal stays behind contractorPii:read"
    - "All US form procedures are gated behind module.us-expansion"
    - "Implements locked decisions D-06, D-07, D-08, D-11 (defer official IRS PDF — structured signed snapshot + audit; portal-primary self-cert; staff read/track + request only, no on-behalf signing; lightweight ESIGN attestation + W-8BEN-E LOB category, no external e-sign)"
  artifacts:
    - path: "packages/api/src/services/tax-form.service.ts"
      provides: "snapshot builder + supersede logic + expiry calc + treaty resolution wiring"
      exports: ["buildFormSnapshot", "supersedeAndInsert"]
    - path: "packages/api/src/routers/portal/portal-tax-form-router.ts"
      provides: "getDetermination / saveDraft / submitTaxForm / getMyForms (portalProcedure)"
      exports: ["portalTaxFormRouter"]
    - path: "packages/api/src/routers/core/tax.ts"
      provides: "staff listFormSubmissions / requestTaxForm (tenantProcedure, PII-gated)"
      contains: "listFormSubmissions"
  key_links:
    - from: "packages/api/src/routers/portal/portal-tax-form-router.ts"
      to: "ctx.contractorId"
      via: "IDOR-safe scoping (never client contractorId)"
      pattern: "ctx\\.contractorId"
    - from: "packages/api/src/routers/portal/portal-tax-form-router.ts"
      to: "treaty-rate.service applyTreaty"
      via: "resolve treaty claim on submit"
      pattern: "applyTreaty"
    - from: "packages/api/src/root.ts"
      to: "module.us-expansion"
      via: "conditional-spread router gating"
      pattern: "us-expansion"
---

<objective>
Wire the portal-primary submission flow and the staff read/track surface on top of the data +
engine layers. The contractor self-certifies (portalProcedure → ctx.contractorId), the snapshot
is built server-side with the ESIGN attestation block (typed name + server IP/timestamp/userId),
re-cert supersedes atomically, and every sensitive action is audited. Staff get read/track only
(D-08). The whole US surface is gated behind `module.us-expansion`.

Purpose: This is the legally-load-bearing layer — beneficial-owner self-cert (D-07), immutable
record (D-05), PII discipline (D-09), reasoned override + audit (D-10). Integration tests prove
immutability, supersede, PII non-leakage, ESIGN capture, and RBAC.
Output: `tax-form.service.ts`, `portal-tax-form-router.ts`, staff procedures on `tax.ts`, flag
gating in `root.ts`, and the two integration test files.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/85-theme-a-w-form-intake-tax-treaty-engine/85-RESEARCH.md
@.planning/phases/85-theme-a-w-form-intake-tax-treaty-engine/85-PATTERNS.md
@.planning/phases/85-theme-a-w-form-intake-tax-treaty-engine/85-VALIDATION.md
@packages/api/src/services/treaty-rate.service.ts
@packages/api/src/services/tax-form-routing.ts
@packages/validators/src/w-form-validators.ts

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

From packages/api/src/routers/portal/portal-profile-router.ts (self-service + $transaction + audit, L383-470):
```typescript
submitUploadReplacement: portalProcedure
  .input(z.object({ /* Zod */ }))
  .mutation(async ({ ctx, input }) => {
    return await ctx.db.$transaction(async tx => {
      // scoped to ctx.contractorId + ctx.organizationId (never client id)
      await writeAuditLog({ tx, organizationId: ctx.organizationId,
        actorType: 'CONTRACTOR', actorId: ctx.contractorId,
        action: 'compliance.upload.submitted', resourceType: 'CONTRACTOR',
        resourceId: item.contractorId, metadata: { /* ... */ } });
    });
  }),
// getProfile (L56-71) NEVER selects ssnEncrypted/bankAccountEncrypted.
```

From packages/api/src/middleware/portal-auth.ts (L75-88): portalProcedure ctx = { contractorId, organizationId, contractor, region, db (regional tenant-scoped), headers }. Never trust client contractorId. Derive IP from ctx.headers server-side.

From packages/api/src/routers/portal/portal.ts (L14-20): mergeRouters FLAT portal.* namespace — add portalTaxFormRouter to that call.

From packages/api/src/routers/core/contractor-tax.ts (revealSsn — PII-gate analog, L80-103):
```typescript
revealSsn: tenantProcedure
  .use(requirePermission({ contractorPii: ['read'] }))
  .input(z.object({ contractorId: z.string().min(1) }))
  .mutation(async ({ ctx, input }) => {
    // decryptSsn(contractor.ssnEncrypted)
    await writeAuditLog({ organizationId: ctx.organizationId, actorType: 'USER',
      actorId: ctx.user?.id, action: 'contractor.ssn.revealed',
      resourceType: 'CONTRACTOR', resourceId: contractor.id, metadata: { field: 'ssn' } });
    return { ssn };
  }),
```

From packages/api/src/root.ts (CLASSIFICATION_ENABLED conditional-spread gating, L114-147):
```typescript
const CLASSIFICATION_ENABLED =
  ClassificationFlagBag.isEnabled('module.classification-engine') || Boolean(process.env.QA_DEFAULT_ORG_ID);
const conditionalClassificationRouters = CLASSIFICATION_ENABLED ? classificationRouters : ({} as typeof classificationRouters);
// ...spread into appRouter: ...conditionalClassificationRouters,
```
module.us-expansion is REGISTERED PENDING (flags-core.ts:211, default false). Dev bypass: FLAG_SIGNOFF_BYPASS=local.

From packages/api/src/services/wht-certificate.service.ts: immutable create-only discipline (L53-71) + generateCertificateNumber (L8-22) reuse candidate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tax-form.service.ts — snapshot builder + supersede + expiry (immutable, D-05/D-11)</name>
  <files>packages/api/src/services/tax-form.service.ts</files>
  <read_first>
    - packages/api/src/services/wht-certificate.service.ts — read the create-only immutability discipline (L53-71) + `generateCertificateNumber` (L8-22) for a human-readable ref if needed (85-PATTERNS.md § tax-form.service).
    - packages/api/src/services/treaty-rate.service.ts (from Plan 02) — `applyTreaty` + `resolveTreatyDecision` signatures.
    - packages/api/src/services/audit-writer.ts — `writeAuditLog` (L118-137), `actorType: 'CONTRACTOR'` (L25), `resourceType` subset (L28-44, use `'CONTRACTOR'`).
    - 85-RESEARCH.md § "W-Form Field → Structured Column Map" (the exact snapshot fields per form) + § Pitfall 4 (only DRAFT mutable).
  </read_first>
  <behavior>
    - Test: `buildFormSnapshot` embeds the ESIGN attestation block (perjuryAccepted, signerName, signedAt, ip, userId/contractorId) and the captured form fields, and stores last-4 only for any TIN (no full SSN).
    - Test: `supersedeAndInsert` within a tx inserts the new ACTIVE row and sets the prior ACTIVE row's status=SUPERSEDED + supersededById; a non-DRAFT row cannot be mutated in place.
    - Test: expiry is set ~3yr out for W-8BEN/W-8BEN-E; W-9 has no fixed expiry (or per IRS rule documented).
  </behavior>
  <action>
    Implement `buildFormSnapshot({ formType, fields, attestation, treatyClaim })` that returns the immutable JSON payload (the legal record-of-record — official IRS-form PDF generation is DEFERRED to Phase 86/87 per D-06) — embed the perjury attestation (`perjuryAccepted`, `signerName`, server-derived `signedAt`, `ip`, `actorId`), the captured form fields, and the resolved treaty claim (article + rate + residency). NEVER write a full SSN into the snapshot — store `ssnLast4` (reference the encrypted P84 column) only (Anti-pattern guard). Implement `supersedeAndInsert(tx, { contractorId, organizationId, formType, snapshot, treatyClaim, signerName, signedAt, expiresAt })` that (1) flips the prior ACTIVE row for this contractor+formType to SUPERSEDED + sets `supersededById`, (2) inserts the new row as ACTIVE — all within the passed `tx`. Reject mutation of non-DRAFT rows (service-level assertion, Pitfall 4). Compute `expiresAt` (~3yr for W-8 forms). No `console.*` (`@contractor-ops/logger`). Tests live in Task 3's integration file or a focused service test.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test src/services/__tests__/tax-form.service.test.ts 2>/dev/null || pnpm typecheck --filter @contractor-ops/api</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function buildFormSnapshot\|export function supersedeAndInsert\|export async function supersedeAndInsert" packages/api/src/services/tax-form.service.ts` present.
    - `grep -ci "ssnEncrypted\|fullSsn" packages/api/src/services/tax-form.service.ts` — no full-SSN value written into the snapshot payload (only last-4 reference).
    - Supersede sets prior `status: 'SUPERSEDED'` + `supersededById` and inserts a new row (assert in the integration test, Task 3).
    - `pnpm typecheck --filter @contractor-ops/api` passes.
  </acceptance_criteria>
  <done>Snapshot/supersede/expiry service exists, immutable + PII-safe, treaty claim embedded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: portal-tax-form-router.ts + staff procedures on tax.ts + flag gating in root.ts</name>
  <files>packages/api/src/routers/portal/portal-tax-form-router.ts, packages/api/src/routers/portal/portal.ts, packages/api/src/routers/core/tax.ts, packages/api/src/root.ts</files>
  <read_first>
    - packages/api/src/routers/portal/portal-profile-router.ts — `portalProcedure` + `$transaction` + `writeAuditLog(actorType:'CONTRACTOR')` (L383-470); `getProfile` (L56-71) NEVER selects encrypted PII; `complianceItems` scoping (L355-369). IDOR: scope to `ctx.contractorId`/`ctx.organizationId`, never client.
    - packages/api/src/routers/portal/portal.ts — `mergeRouters` flat namespace (L14-20).
    - packages/api/src/routers/core/tax.ts — `taxRouter` `tenantProcedure` queries (L13-55); mounted at `tax:` (root.ts:208).
    - packages/api/src/routers/core/contractor-tax.ts — `revealSsn` PII-gate (L80-103) — REUSE the existing procedure for full-SSN; do NOT re-expose SSN in the staff summary.
    - packages/api/src/root.ts — `CLASSIFICATION_ENABLED` conditional-spread (L114-147) + the `buildFlagBag` idiom.
    - packages/api/src/middleware/rbac.ts — `requirePermission` (L19-61); 85-PATTERNS.md § RBAC: form read/track reuses `contractor:read`; do NOT add a new permission (Pitfall 2).
  </read_first>
  <behavior>
    - Test: `getDetermination` returns the routed formType (via `determineFormType`) + auto-populated treaty article/rate from the contractor profile, scoped to `ctx.contractorId`.
    - Test: `submitTaxForm` resolves treaty (`applyTreaty`), builds snapshot, supersedes prior ACTIVE, inserts, and writes audit — all in one `$transaction`.
    - Test: a portal caller can never read/write another contractor's form (IDOR — ignores any client-supplied contractorId).
    - Test: staff `listFormSubmissions` returns status/track WITHOUT full SSN; reveal stays on the existing `revealSsn` (contractorPii:read).
    - Test: US procedures are absent from the router when `module.us-expansion` is off and the dev bypass is unset.
  </behavior>
  <action>
    Create `portal-tax-form-router.ts` with `portalProcedure` procedures: `getDetermination` (query — routes formType + auto-populates treaty claim), `saveDraft` (mutation — upserts a DRAFT row, only DRAFT mutable), `submitTaxForm` (mutation — validates with `taxFormSubmissionSchema`, derives IP server-side from `ctx.headers` [never client body], calls `applyTreaty` + `buildFormSnapshot` + `supersedeAndInsert` + `writeAuditLog(actorType:'CONTRACTOR', actorId: ctx.contractorId)` inside `ctx.db.$transaction`), and `getMyForms` (query — list own forms with status/expiry, never selects encrypted PII). All scoped to `ctx.contractorId`/`ctx.organizationId`. Mount via `mergeRouters` in `portal.ts`. Extend `taxRouter` in `tax.ts` with staff `listFormSubmissions` (tenantProcedure query — status/track summary, full SSN ABSENT) and `requestTaxForm` (mutation — flags status + audit; the email/notification delivery is deferred per the deferred list). Reuse the existing `revealSsn` for any full-SSN exposure (do NOT add a permission). In `root.ts`, gate the US tax procedures behind `module.us-expansion` using the `CLASSIFICATION_ENABLED` conditional-spread idiom (flag bag check OR a dev bypass), with defense-in-depth per-request guard at the procedure layer. No `console.*`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test src/routers/portal/__tests__/tax-form.test.ts src/routers/core/__tests__/tax-form-staff.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ctx.contractorId" packages/api/src/routers/portal/portal-tax-form-router.ts` shows every read/write scoped to the session contractor (IDOR guard); no `input.contractorId` is trusted for scoping.
    - `grep -n "applyTreaty\|buildFormSnapshot\|supersedeAndInsert\|writeAuditLog" packages/api/src/routers/portal/portal-tax-form-router.ts` shows the submit pipeline wired inside `$transaction`.
    - `grep -n "ctx.headers" packages/api/src/routers/portal/portal-tax-form-router.ts` shows IP derived server-side; `grep -ci "input.ip\|body.ip" ...` is 0 (no client-supplied IP).
    - `grep -n "module.us-expansion\|us-expansion" packages/api/src/root.ts` shows the gate.
    - `grep -c "permissions.ts\|accessControlStatement" packages/auth/src/roles.ts` unchanged vs base (no new permission added — Pitfall 2).
    - `pnpm --filter @contractor-ops/api test src/routers/portal/__tests__/tax-form.test.ts` GREEN.
    - `pnpm lint:audit-log` passes (sensitive mutations audited).
  </acceptance_criteria>
  <done>Portal submit/draft/determination/list + staff read/track wired, flag-gated, audited, IDOR + tenant scoped, server-derived attestation IP.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integration tests — immutability, supersede, PII non-leak, ESIGN capture, RBAC</name>
  <files>packages/api/src/routers/portal/__tests__/tax-form.test.ts, packages/api/src/routers/core/__tests__/tax-form-staff.test.ts</files>
  <read_first>
    - packages/api/src/routers/__tests__/portal-profile.test.ts AND packages/api/src/routers/__tests__/portal.test.ts — the portal-session test harness/mock to mirror (85-PATTERNS.md § No Analog: partial — reuse the session mock).
    - 85-VALIDATION.md § Per-Task Verification Map (the Wave-2 integration rows: immutable insert + supersede, full-SSN-never-in-output, W-8BEN-E LOB+article, ESIGN capture, staff RBAC).
    - packages/api/src/routers/core/__tests__/ — any existing tenantProcedure test harness for the staff side.
  </read_first>
  <action>
    Write the integration tests (these may have been authored RED in Tasks 1-2; finalize here). Portal test (`tax-form.test.ts`): W-9 submit inserts an immutable ACTIVE row + an audit row; re-cert inserts a NEW row and flips the prior to SUPERSEDED; the portal response + the stored `snapshotJson` contain NO full SSN (assert no full-SSN substring); W-8BEN-E captures `lobCategory` (line 14b) + `treatyArticle` (line 15); the attestation snapshot contains typed `signerName` + server `signedAt` + derived `ip` + `contractorId`; a cross-contractor read is rejected (IDOR). Staff test (`tax-form-staff.test.ts`): `listFormSubmissions` returns status without full SSN; the full-SSN reveal path requires `contractorPii:read` and is audited. Use the existing portal-session + tenant test harness; do NOT spin a new framework.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test src/routers/portal/__tests__/tax-form.test.ts src/routers/core/__tests__/tax-form-staff.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All five Wave-2 integration behaviors GREEN (85-VALIDATION.md US-FORM-01/02 integration rows).
    - The PII assertion explicitly checks the serialized response + `snapshotJson` for the absence of a full SSN (last-4 only).
    - The supersede test asserts prior `status === 'SUPERSEDED'` and a new ACTIVE row exists.
    - The RBAC test asserts a caller without `contractorPii:read` cannot reveal full SSN.
    - `pnpm --filter @contractor-ops/api test` (API package full) GREEN at wave merge.
  </acceptance_criteria>
  <done>Integration suite proves immutability + supersede + PII non-leak + ESIGN capture + RBAC for the W-form surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| portal client → portalProcedure | Untrusted contractor input; client must never set its own contractorId/IP/identity |
| staff client → tenantProcedure | Staff read/track; full SSN behind contractorPii:read only |
| feature flag → router assembly | US surface must not exist when module.us-expansion is off |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-03-01 | Information Disclosure | Full SSN/TIN via snapshot JSON or portal response | mitigate | Snapshot stores last-4 + encrypted reference only; `getProfile`/`getMyForms` never select encrypted PII; integration test asserts no full SSN in output (RESEARCH § Security Domain). |
| T-85-03-02 | Elevation of Privilege | Cross-contractor form access (IDOR) | mitigate | Every portal read/write scoped to `ctx.contractorId` (session), never a client-supplied id; test rejects cross-contractor access (mirror `portal-profile-router`). |
| T-85-03-03 | Elevation of Privilege | Cross-tenant form access | mitigate | `ctx.organizationId` from session + `ctx.db` regional tenant client auto-scopes; never trust client org. |
| T-85-03-04 | Tampering / Repudiation | Forged attestation IP/identity | mitigate | IP + actorId derived server-side from `ctx.headers`/session; client-supplied IP rejected (grep gate `input.ip` == 0). |
| T-85-03-05 | Tampering / Repudiation | Mutating a signed legal record | mitigate | Append-only `TaxFormSubmission`; only DRAFT mutable; re-cert inserts + supersedes; service rejects non-DRAFT mutation; tested. |
| T-85-03-06 | Tampering | Treaty-rate override (staff) | mitigate | Override requires a reason + `writeAuditLog`; auto-detected value persisted alongside (from Plan 02 `resolveTreatyDecision`). |
| T-85-03-07 | Repudiation | Staff on-behalf signing (weak cert) | accept | D-08 — staff cannot sign; only the portal beneficial-owner self-cert path exists. No staff mutation creates a signed record. |
| T-85-03-08 | Elevation of Privilege | US surface exposed before flag sign-off | mitigate | `module.us-expansion` conditional-spread gate in `root.ts` + per-request guard; absent when flag off (tested). |
| T-85-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (RESEARCH § Package Legitimacy Audit: N/A). No legitimacy checkpoint required. |
</threat_model>

<verification>
- Portal submit/draft/determination/list + staff list/request wired and tested.
- Immutability + supersede + PII non-leak + ESIGN + RBAC integration tests GREEN.
- Flag gating present; audit on sensitive mutations; `lint:audit-log` green.
- `pnpm typecheck --filter @contractor-ops/api` + API package test suite GREEN.
</verification>

<success_criteria>
- US-FORM-01/02: contractor self-cert submit creates an immutable, audited, PII-safe record; staff read/track without leaking SSN.
- US-LOC-02/03: treaty claim resolved + auto-populated on submit; override audited.
</success_criteria>

<output>
Create `.planning/phases/85-theme-a-w-form-intake-tax-treaty-engine/85-03-SUMMARY.md` when done.
</output>
