---
phase: 85-theme-a-w-form-intake-tax-treaty-engine
plan: 04
type: execute
wave: 4
depends_on: ["85-03"]
files_modified:
  - apps/web-vite/src/components/portal/tax-forms/tax-form-wizard.tsx
  - apps/web-vite/src/components/portal/tax-forms/hooks/use-tax-form-wizard.ts
  - apps/web-vite/src/components/portal/tax-forms/step-determination.tsx
  - apps/web-vite/src/components/portal/tax-forms/step-w9.tsx
  - apps/web-vite/src/components/portal/tax-forms/step-w8ben.tsx
  - apps/web-vite/src/components/portal/tax-forms/step-w8ben-e.tsx
  - apps/web-vite/src/components/portal/tax-forms/step-attest.tsx
  - apps/web-vite/src/components/portal/tax-forms/__tests__/tax-form-wizard.test.tsx
  - apps/web-vite/src/components/contractors/tax-forms/tax-form-status-card.tsx
  - apps/web-vite/src/pages/portal/tax-form-page.tsx
  - apps/web-vite/messages/en.json
  - apps/web-vite/messages/de.json
  - apps/web-vite/messages/pl.json
  - apps/web-vite/messages/ar.json
  - apps/web-vite/messages/en-US.json
  - .planning/brain/wiki/structure/prisma-schema-areas.md
  - .planning/brain/wiki/structure/api-routers-catalog.md
  - .planning/brain/wiki/structure/web-vite-domains.md
  - .planning/brain/wiki/domains/us-tax-forms.md
  - .planning/brain/wiki/log.md
  - .planning/brain/wiki/hot.md
  - .planning/MEMORY.md
requirements: [US-FORM-01, US-FORM-02, US-LOC-02, US-LOC-03]
autonomous: true
must_haves:
  truths:
    - "A contractor can complete the W-9/W-8BEN/W-8BEN-E wizard in the portal with a confirm/override determination step"
    - "The treaty article + rate auto-populate in the W-8BEN/W-8BEN-E step and announce via aria-live"
    - "The attestation step gates Sign & submit behind all perjury checkboxes + a matching typed legal name"
    - "Staff see a form status card with advisory pill; full SSN stays behind the gated reveal"
    - "Every wizard string is i18n'd across en/de/pl/ar with en-US fallback; the wizard works RTL"
    - "The wiki + log/hot + MEMORY are updated in the same change set and check:wiki-brain passes"
  artifacts:
    - path: "apps/web-vite/src/components/portal/tax-forms/tax-form-wizard.tsx"
      provides: "container: reui Stepper + step dispatch, loading/empty/error states"
      min_lines: 40
    - path: "apps/web-vite/src/components/portal/tax-forms/hooks/use-tax-form-wizard.ts"
      provides: "sole tRPC/RHF boundary (portal procedures)"
    - path: "apps/web-vite/src/components/portal/tax-forms/step-attest.tsx"
      provides: "perjury checkboxes + typed-name gate before Sign & submit"
    - path: ".planning/brain/wiki/domains/us-tax-forms.md"
      provides: "US W-form domain page"
  key_links:
    - from: "apps/web-vite/src/components/portal/tax-forms/hooks/use-tax-form-wizard.ts"
      to: "portal tax-form procedures"
      via: "useTRPC portal namespace"
      pattern: "tax(Form)?"
    - from: "apps/web-vite/src/components/portal/tax-forms/tax-form-wizard.tsx"
      to: "use-tax-form-wizard hook"
      via: "container consumes hook (no direct tRPC)"
      pattern: "useTaxFormWizard"
    - from: "apps/web-vite/src/components/contractors/tax-forms/tax-form-status-card.tsx"
      to: "SsnMaskedReveal"
      via: "gated reveal reuse"
      pattern: "SsnMaskedReveal"
---

<objective>
Build the portal-primary W-form wizard (determination → form → attestation → receipt) and the
staff read/track status card per 85-UI-SPEC, fully i18n'd (en/de/pl/ar + en-US fallback) and
RTL-correct, following the web-vite page→container→hook→component layering (D-13). Then update the wiki +
log/hot + MEMORY in the same change set (documentation-follows-code gate).

Purpose: This is the contractor-facing surface that makes US-FORM-01/02 + US-LOC-02/03 real for a
user — the beneficial owner self-certifies, the treaty claim auto-populates, and staff track
status without PII leakage. The wizard reuses the established `reui` Stepper + RHF idiom; no new
design tokens or registry blocks.
Output: wizard container + hook + 4 step components + staff status card + portal page + i18n keys
+ component tests + wiki/MEMORY updates.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/85-theme-a-w-form-intake-tax-treaty-engine/85-UI-SPEC.md
@.planning/phases/85-theme-a-w-form-intake-tax-treaty-engine/85-PATTERNS.md
@.planning/phases/85-theme-a-w-form-intake-tax-treaty-engine/85-VALIDATION.md
@apps/web-vite/ARCHITECTURE.md
@apps/web-vite/src/components/onboarding/organization-onboarding.tsx
@apps/web-vite/src/components/onboarding/hooks/use-organization-onboarding.ts

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

Web-vite layering (enforced by check:web-vite-data-layer): page (thin, Suspense + permissions, NO tRPC)
→ container (*-container.tsx / wizard.tsx — owns loading/empty/error, NO direct tRPC)
→ hook (use-*.ts — the SOLE tRPC/React-Query boundary)
→ component (presentational, props in / JSX out).

Analog A (canonical layering): onboarding/organization-onboarding.tsx + onboarding/hooks/use-organization-onboarding.ts
- reui Stepper imports (organization-onboarding.tsx L12-19); OnboardingStepIndicator (L55-83) with aria-current="step", Progress fill, RTL rtl:rotate-180 on the arrow (L256).
- RHF + zodResolver + per-field aria-invalid + aria-describedby + role="alert" (OrgDetailsStep L109-184); Controller-bound Select (L134-160).
- Hook: useForm + zodResolver, useId, useTranslations, multi-step useState, handleSubmit(onValid) returned as onSubmit (use-organization-onboarding.ts L68-163).
- Entrance: AnimateIn (fade+slide staggered delay) at L276/289/300 — components/shared/animate-in.tsx.

Analog B (step dispatch + skeleton): zatca/onboarding-wizard.tsx OnboardingWizardView (L50-78: {activeStep === N && <Step .../>}) + OnboardingWizardSkeleton (L19-42).

Staff surface reuse:
- SsnMaskedReveal (contractors/compliance/ssn-masked-reveal.tsx, L28-100): props { contractorId, last4, canReveal }; control ABSENT (not disabled) when !canReveal (L70); reveal via use-reveal-ssn hook; font-mono tabular-nums; role="alert" aria-live="polite" on error.
- UspsAddressStatusPill (contractors/usps-address-status-pill.tsx, L43-109): Badge VARIANT_MAP { labelKey, badgeVariant, icon, tooltipKey } + data-status. Reuse the shape for form status (ACTIVE/DRAFT/SUPERSEDED/expiring).

i18n: useTranslations('<Namespace>') from ../../i18n/useTranslations; en-US fallback-parity (P84 D-04 — do NOT add en-US to strict i18n:parity peers); logical RTL props only (ms-/me-, text-start/end, rtl:rotate-180).

Validators (Plan 02): taxFormSubmissionSchema (discriminated union) drives the RHF zodResolver per step.
Portal procedures (Plan 03): getDetermination / saveDraft / submitTaxForm / getMyForms.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wizard hook + container + step components (portal page→container→hook→component)</name>
  <files>apps/web-vite/src/components/portal/tax-forms/hooks/use-tax-form-wizard.ts, apps/web-vite/src/components/portal/tax-forms/tax-form-wizard.tsx, apps/web-vite/src/components/portal/tax-forms/step-determination.tsx, apps/web-vite/src/components/portal/tax-forms/step-w9.tsx, apps/web-vite/src/components/portal/tax-forms/step-w8ben.tsx, apps/web-vite/src/components/portal/tax-forms/step-w8ben-e.tsx, apps/web-vite/src/pages/portal/tax-form-page.tsx</files>
  <read_first>
    - FIRST run the `frontend-design` skill (read its SKILL.md) then `impeccable` per the UI-skills routing — MANDATORY before any web-vite UI edit (CLAUDE.md UI skills); then `semble search` for the wizard analogs.
    - apps/web-vite/src/components/onboarding/organization-onboarding.tsx — full read: Stepper imports (L12-19), OnboardingStepIndicator (L55-83), OrgDetailsStep field rendering + Controller Select (L109-184), AnimateIn usage (L276/289/300), RTL arrow (L256).
    - apps/web-vite/src/components/onboarding/hooks/use-organization-onboarding.ts — full read: the RHF+zod+useId+useTranslations+multi-step hook shape (L68-163) — this is the sole-tRPC-boundary pattern to mirror.
    - apps/web-vite/src/components/zatca/onboarding-wizard.tsx — step-dispatch (L50-78) + skeleton (L19-42).
    - apps/web-vite/ARCHITECTURE.md — the page→container→hook→component contract enforced by `check:web-vite-data-layer`.
    - 85-UI-SPEC.md — Copywriting Contract (exact en strings), Color (accent reserved for the single CTA + active stepper + focus ring), Typography (two weights 400/600; Label overridden to font-normal), Component Inventory (reuse contract), State & Accessibility Contract (4 states + RTL per surface).
  </read_first>
  <behavior>
    - Test: the wizard renders the determination step with the auto-routed formName and a confirm/override control.
    - Test: choosing W-8BEN renders the W-8BEN step with the treaty article/rate auto-populated (from getDetermination) and an aria-live announcement.
    - Test: loading shows the skeleton; a load error shows "Couldn't load your tax form" + Reload; pre-wizard empty shows "No tax form on file" + Start CTA.
    - Test (RTL): rendered under dir="rtl" the stepper/arrow flip (logical props) and Arabic strings resolve.
  </behavior>
  <action>
    Create `use-tax-form-wizard.ts` as the SOLE tRPC/React-Query boundary: `useForm` + `zodResolver(taxFormSubmissionSchema)`, `useTranslations('TaxFormWizard')`, multi-step `useState`, and the portal queries/mutations (`getDetermination`, `saveDraft`, `submitTaxForm`, `getMyForms`) — return form state, step controls, the resolved determination, and `onSubmit`. Create `tax-form-wizard.tsx` (container) that owns the `reui` Stepper + step indicator + AnimateIn + the loading/empty/error states (NO direct tRPC). Create the step components (`step-determination.tsx` confirm/override; `step-w9.tsx`; `step-w8ben.tsx` + `step-w8ben-e.tsx` with the auto-populated treaty article/rate line announced via `aria-live="polite"`, plus the LOB field on W-8BEN-E) as presentational components (props in / JSX out), Controller-bound shadcn Select for country/entity, per-field `aria-invalid`/`aria-describedby`/`role="alert"`. Override the shadcn `Label` to `font-normal` (UI-SPEC typography — two weights only). Accent (`bg-primary`) ONLY on the single per-step CTA + active stepper indicator + focus ring. Create the thin `tax-form-page.tsx` (Suspense + portal permission gate, no tRPC). Use semantic tokens (`bg-card`, `text-muted-foreground`), logical RTL props only — no `ml-/mr-/text-left/right`. All strings via `useTranslations` (added in Task 3). Add the component test under `__tests__` covering the four states + RTL.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite test src/components/portal/tax-forms && pnpm check:web-vite-data-layer</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm check:web-vite-data-layer` passes — only `use-tax-form-wizard.ts` touches tRPC; container + steps + page do NOT (layering enforced).
    - `pnpm --filter @contractor-ops/web-vite test src/components/portal/tax-forms` GREEN for determination render, W-8BEN auto-populate + aria-live, loading/empty/error, and RTL (85-VALIDATION.md Wave-3 row).
    - `grep -rn "ml-\|mr-\|text-left\|text-right" apps/web-vite/src/components/portal/tax-forms/` returns 0 (logical RTL props only).
    - `grep -rn "font-medium" apps/web-vite/src/components/portal/tax-forms/` returns 0 (two-weight rule; Label overridden to font-normal).
    - `grep -rn "bg-primary\|text-primary" apps/web-vite/src/components/portal/tax-forms/` confined to CTA / active stepper / focus / links (no accent on body or card borders).
    - `pnpm typecheck --filter @contractor-ops/web-vite` passes.
  </acceptance_criteria>
  <done>Portal wizard (determination → form steps) renders with layering enforced, treaty auto-populate announced, four states + RTL covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attestation step + submission receipt + staff status card</name>
  <files>apps/web-vite/src/components/portal/tax-forms/step-attest.tsx, apps/web-vite/src/components/portal/tax-forms/__tests__/tax-form-wizard.test.tsx, apps/web-vite/src/components/contractors/tax-forms/tax-form-status-card.tsx</files>
  <read_first>
    - 85-UI-SPEC.md — § Copywriting Contract (perjury attestation = verbatim IRS domain string per form; "Type your full legal name to sign"; success heading/body) + § Destructive/high-stakes actions (Sign & submit gate: all checkboxes + matching typed name + "I understand this is a legal signature"; re-cert AlertDialog) + § State & Accessibility Contract (attestation row: real `<input type=checkbox>`, Label-in-Name, role="alert" aria-live submit-failure region preserving data).
    - apps/web-vite/src/components/contractors/compliance/ssn-masked-reveal.tsx — full read (L28-100): reuse VERBATIM for the W-9 staff summary; control ABSENT when `!canReveal`.
    - apps/web-vite/src/components/contractors/usps-address-status-pill.tsx — VARIANT_MAP + tooltip + data-status idiom (L43-109) to mirror for form status.
    - 85-RESEARCH.md § "No Analog Found" — step-attest gate logic (all checkboxes + typed-name-match) is novel UX; build from the onboarding form + checkbox-group primitives.
  </read_first>
  <behavior>
    - Test: Sign & submit stays disabled until every perjury checkbox is checked AND the typed legal name matches the contractor's legal name AND the legal-signature affirmation is checked.
    - Test: a submit failure shows a `role="alert" aria-live="polite"` region and preserves entered data (does not wipe the wizard).
    - Test: on success the receipt heading "Tax form submitted" renders with the signed date.
    - Test (staff card): the form status pill maps ACTIVE→success, DRAFT→info, SUPERSEDED→destructive, expiring→warning; the full SSN control is ABSENT without `contractorPii:read`.
  </behavior>
  <action>
    Create `step-attest.tsx`: real `<input type="checkbox">` perjury items (labels are the verbatim IRS domain strings per form — W-9 Part II / W-8BEN Part III / W-8BEN-E Part XXX, sourced as i18n domain strings, adviser-deferred D-04/D-11), a typed-legal-name field whose accessible name equals the visible label (Label-in-Name), and an "I understand this is a legal signature" affirmation. The `Sign & submit` CTA is disabled until all checkboxes + name-match + affirmation are satisfied (intentional friction, not a delete dialog). Wire the re-cert AlertDialog confirmation (supersede warning copy) when an ACTIVE form already exists. On submit, show the `Loader2` "Submitting…" CTA spinner; on failure render the inline `role="alert"` region preserving data; on success render the lightweight human-readable receipt (the optional summary permitted by D-06; no pixel-accurate IRS PDF this phase). Create `tax-form-status-card.tsx` (staff surface): reuse the `UspsAddressStatusPill` VARIANT_MAP shape for form status, reuse `SsnMaskedReveal` VERBATIM for the W-9 last-4 + gated reveal, render the adviser-deferred treaty note in `text-muted-foreground` with an info icon (NOT destructive). Add/extend the component test (`__tests__/tax-form-wizard.test.tsx`) for the attestation gate + submit-failure-preserves-data + staff pill mapping. All strings i18n (Task 3).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite test src/components/portal/tax-forms</automated>
  </verify>
  <acceptance_criteria>
    - The attestation-gate test GREEN: CTA disabled until checkboxes + typed-name-match + affirmation; enabled once all true.
    - Submit-failure test GREEN: `role="alert"` region present, entered data preserved.
    - Staff-card test GREEN: status→variant mapping + SSN control ABSENT (not just disabled) without `contractorPii:read`.
    - `grep -n "SsnMaskedReveal" apps/web-vite/src/components/contractors/tax-forms/tax-form-status-card.tsx` shows the verbatim reuse.
    - `grep -rn "type=\"checkbox\"\|type='checkbox'" apps/web-vite/src/components/portal/tax-forms/step-attest.tsx` shows real checkboxes (not div toggles).
    - `pnpm typecheck --filter @contractor-ops/web-vite` passes.
  </acceptance_criteria>
  <done>Attestation gate + receipt + staff status card built per UI-SPEC, PII-safe reveal reused, all four states covered.</done>
</task>

<task type="auto">
  <name>Task 3: i18n keys (en/de/pl/ar + en-US) + documentation-follows-code (wiki + log/hot + MEMORY)</name>
  <files>apps/web-vite/messages/en.json, apps/web-vite/messages/de.json, apps/web-vite/messages/pl.json, apps/web-vite/messages/ar.json, apps/web-vite/messages/en-US.json, .planning/brain/wiki/structure/prisma-schema-areas.md, .planning/brain/wiki/structure/api-routers-catalog.md, .planning/brain/wiki/structure/web-vite-domains.md, .planning/brain/wiki/domains/us-tax-forms.md, .planning/brain/wiki/log.md, .planning/brain/wiki/hot.md, .planning/MEMORY.md</files>
  <read_first>
    - apps/web-vite/messages/en.json — read the existing namespace structure to add a `TaxFormWizard` namespace consistently; mirror to de/pl/ar; en-US gets only the en-US-specific overrides (fallback-parity, P84 D-04).
    - 85-UI-SPEC.md § Copywriting Contract — the en source-of-truth strings (entry CTA, determination, per-step CTAs, treaty captions, perjury, typed-name, success, empty/loading/error).
    - 85-RESEARCH.md § Pitfall 6 (i18n parity gate fails on new keys without de/pl/ar) + § Project Constraints (the exact wiki pages to update; `pnpm check:wiki-brain` before done).
    - .planning/brain/wiki/structure/web-vite-domains.md + api-routers-catalog.md + prisma-schema-areas.md — read each to append accurately (do not leave stale paths).
    - .planning/MEMORY.md — read the tail to append the new invariant in-format.
  </read_first>
  <action>
    Add the `TaxFormWizard` (and a `TaxFormStaff`) i18n namespace with ALL wizard/status/state strings from the UI-SPEC Copywriting Contract to `en.json` AND `de.json`, `pl.json`, `ar.json` in the SAME change set (the `i18n:parity` gate requires exact de/pl/ar parity; en-US inherits via fallback — add only en-US-specific overrides if any). Then update documentation-follows-code in the same change set: `wiki/structure/prisma-schema-areas.md` (the `WithholdingTaxRate.treatyArticle` extension + the new `TaxFormSubmission` model), `wiki/structure/api-routers-catalog.md` (the new portal `portal.*` tax-form procedures + the staff `tax.*` list/request), `wiki/structure/web-vite-domains.md` (the new portal tax-forms wizard surface + staff status card), a NEW `wiki/domains/us-tax-forms.md` domain page (Purpose, Flow, Entry points, UI surface, Agent mistakes — the W-9/W-8BEN/W-8BEN-E intake + treaty engine), `wiki/log.md` + overwrite `wiki/hot.md`, and append the new invariant to `.planning/MEMORY.md` (treaty rows in the shared WithholdingTaxRate table, immutable supersede-able TaxFormSubmission FK'd to Contractor, portal-primary self-cert, module.us-expansion gate). Bump `source_commit` on any touched wiki frontmatter. Run the wiki refresh pipeline (contextual-prefix + BM25 rebuild) per CLAUDE.md. No planning-ID breadcrumbs in any source comments (real domain IDs W-9/W-8BEN/Article 7 are fine).
  </action>
  <verify>
    <automated>pnpm i18n:parity && pnpm check:wiki-brain</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm i18n:parity` passes — `TaxFormWizard`/`TaxFormStaff` keys present in en/de/pl/ar at exact parity; en-US resolves via fallback (no strict en-US peer break).
    - `pnpm check:wiki-brain` passes — every `verify_with` source touched this change set has its wiki page updated in the same change set (no NEW drift).
    - `.planning/brain/wiki/domains/us-tax-forms.md` exists with Purpose/Flow/Entry points/UI surface/Agent mistakes sections.
    - `grep -n "TaxFormSubmission\|treatyArticle" .planning/brain/wiki/structure/prisma-schema-areas.md` present; `grep -n "tax-form\|TaxForm" .planning/brain/wiki/structure/api-routers-catalog.md` present.
    - `pnpm lint:no-breadcrumbs` passes (no phase/req-ID comments in source).
  </acceptance_criteria>
  <done>All wizard strings i18n'd at parity; wiki + log/hot + MEMORY updated in the same change set; `check:wiki-brain` + `i18n:parity` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → portal wizard | Contractor input; the wizard must not surface another contractor's data |
| staff browser → status card | Full SSN must stay behind the gated reveal, never rendered into the DOM without permission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-04-01 | Information Disclosure | Full SSN in the staff status card DOM | mitigate | Reuse `SsnMaskedReveal` VERBATIM — control ABSENT (not disabled) without `contractorPii:read`, reveal only on explicit click, never on hover/focus/mount; no full SSN in props. Tested. |
| T-85-04-02 | Tampering / Repudiation | Premature / unintended signature | mitigate | The attestation gate disables `Sign & submit` until all perjury checkboxes + a matching typed legal name + the legal-signature affirmation are satisfied (intentional friction). Server still re-derives IP/ts/identity (Plan 03). |
| T-85-04-03 | Tampering | Re-cert silently replacing a signed record | mitigate | `AlertDialog` supersede confirmation before re-cert; append-only on the server (Plan 03) — UI never hard-deletes. |
| T-85-04-04 | Information Disclosure | Client-trusted determination/treaty data | accept | The wizard only displays the server-resolved determination/treaty claim; the authoritative resolution + persistence happen server-side (Plan 03). Client display is advisory. |
| T-85-04-SC | Tampering | npm/pip/cargo + shadcn registry installs | accept | No package or registry installs this plan (UI-SPEC § Registry Safety: ZERO new blocks; RESEARCH § Package Legitimacy Audit: N/A). No legitimacy checkpoint required. |
</threat_model>

<verification>
- Portal wizard (determination → steps → attestation → receipt) + staff status card built per UI-SPEC.
- `check:web-vite-data-layer` + scoped web-vite component tests + `i18n:parity` + `check:wiki-brain` + `lint:no-breadcrumbs` GREEN.
- Treaty auto-populate announced; attestation gate enforced; SSN reveal gated.
- Wiki + log/hot + MEMORY updated in the same change set.
</verification>

<success_criteria>
- US-FORM-01/02: a contractor self-completes the correct wizard with confirm/override + ESIGN attestation; staff track status PII-safely.
- US-LOC-02/03: treaty article + rate auto-populate visibly in the W-8BEN/W-8BEN-E step.
- Documentation-follows-code satisfied.
</success_criteria>

<output>
Create `.planning/phases/85-theme-a-w-form-intake-tax-treaty-engine/85-04-SUMMARY.md` when done.
</output>
