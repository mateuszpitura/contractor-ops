---
phase: 85-theme-a-w-form-intake-tax-treaty-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema/tax.prisma
  - packages/db/prisma/schema/contractor.prisma
  - packages/db/prisma/schema/organization.prisma
  - packages/db/prisma/seed/wht-rates.ts
  - packages/db/prisma/seed/index.ts
autonomous: false
requirements: [US-LOC-02, US-LOC-03, US-FORM-01, US-FORM-02]
must_haves:
  truths:
    - "WithholdingTaxRate carries a structured treatyArticle alongside the existing free-text treatyReference"
    - "US treaty rows exist for PL/DE/UK/IE/NL (reduced) and AE/SA (30% statutory, no treaty) plus an XX fallback"
    - "A TaxFormSubmission table exists that is append-only with a supersede chain, FK'd to Contractor"
    - "The new table + columns exist in EU/ME/US regional databases (migration applied)"
    - "US rows store treatyRate as whole-number percent (30.0 / 0.0 / null), never fractions"
  artifacts:
    - path: "packages/db/prisma/schema/tax.prisma"
      provides: "treatyArticle column on WithholdingTaxRate + TaxFormSubmission model + TaxFormType/TaxFormStatus enums"
      contains: "model TaxFormSubmission"
    - path: "packages/db/prisma/seed/wht-rates.ts"
      provides: "US sourceCountry='US' business_profits treaty rows + XX fallback"
      contains: "sourceCountry: 'US'"
  key_links:
    - from: "packages/db/prisma/seed/wht-rates.ts"
      to: "WithholdingTaxRate"
      via: "upsert on composite unique key"
      pattern: "sourceCountry_contractorResidency_serviceType_effectiveFrom"
    - from: "TaxFormSubmission"
      to: "Contractor"
      via: "FK relation"
      pattern: "contractor\\s+Contractor\\s+@relation"
---

<objective>
Lay the data foundation for US W-form intake + the treaty engine: extend `WithholdingTaxRate`
with a structured `treatyArticle` column (US-LOC-03 auto-populate source), add the immutable
`TaxFormSubmission` model with a supersede chain (US-FORM-01/02 record-of-record), seed the
per-country US treaty rows (US-LOC-02), then run the multi-region migration so EU/ME/US all
carry the new schema.

Purpose: Every downstream plan (treaty-rate service, portal/staff routers, wizard UI) typechecks
against the regenerated Prisma client produced here. This plan is the hard Wave-1 prerequisite.
Output: extended `tax.prisma`, back-relations on Contractor + Organization, seeded US rows,
applied multi-region migration, regenerated Prisma client.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/85-theme-a-w-form-intake-tax-treaty-engine/85-CONTEXT.md
@.planning/phases/85-theme-a-w-form-intake-tax-treaty-engine/85-RESEARCH.md
@.planning/phases/85-theme-a-w-form-intake-tax-treaty-engine/85-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From packages/db/prisma/schema/tax.prisma (WithholdingTaxRate, L23 — EXTEND additively):
```prisma
model WithholdingTaxRate {
  id                  String    @id @default(cuid())
  sourceCountry       String    @db.Char(2)
  contractorResidency String    @db.Char(2)
  serviceType         String    @db.VarChar(50)   // reuse as income-type axis: 'business_profits' (D-02)
  standardRate        Decimal   @db.Decimal(5, 2)
  treatyRate          Decimal?  @db.Decimal(5, 2)
  treatyReference     String?   @db.VarChar(100)  // existing free-text — keep
  effectiveFrom       DateTime  @db.Date
  effectiveTo         DateTime? @db.Date
  createdAt           DateTime  @default(now())
  updatedAt           DateTime  @updatedAt
  @@unique([sourceCountry, contractorResidency, serviceType, effectiveFrom])  // UNCHANGED — do NOT add a 5th key field
  @@index([sourceCountry])
  @@index([sourceCountry, contractorResidency])
}
```

From packages/db/prisma/seed/wht-rates.ts (upsert idiom + SA percent units, L147-162):
```typescript
await prisma.withholdingTaxRate.upsert({
  where: { sourceCountry_contractorResidency_serviceType_effectiveFrom: {
      sourceCountry: rate.sourceCountry, contractorResidency: rate.contractorResidency,
      serviceType: rate.serviceType, effectiveFrom: rate.effectiveFrom } },
  update: { ...rate }, create: { ...rate },
});
// SA rows store percent units: 5.0 / 20.0 / null  → US rows MUST follow: 30.0 / 0.0 / null
```

From packages/db/prisma/schema/contractor.prisma — Contractor at L9; existing back-relation idiom at L86:
```prisma
taxIdValidations          TaxIdValidation[]
```
ContractorType enum at contractor.prisma:310 = SOLE_TRADER | COMPANY | INDIVIDUAL_FREELANCER | OTHER (form-routing axis).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend WithholdingTaxRate + add TaxFormSubmission model and enums + back-relations</name>
  <files>packages/db/prisma/schema/tax.prisma, packages/db/prisma/schema/contractor.prisma, packages/db/prisma/schema/organization.prisma</files>
  <read_first>
    - packages/db/prisma/schema/tax.prisma — read `WithholdingTaxRate` (L23-39) AND the `WhtCertificate` immutable-record analog (L41-64) AND `TaxIdValidation` (L83-104) for the FK-to-Contractor/Organization relation + multi-index idiom (85-PATTERNS.md § tax.prisma, Analog B).
    - packages/db/prisma/schema/contractor.prisma — read the Contractor model (L9-107) to locate where back-relations are declared (L86 `taxIdValidations TaxIdValidation[]`) and confirm `ContractorType` enum (L310).
    - packages/db/prisma/schema/organization.prisma — read the Organization model to locate its back-relation block.
    - 85-RESEARCH.md § "Extending WithholdingTaxRate" + § "TaxFormSubmission model (new)" code blocks (the exact field set is given there).
  </read_first>
  <action>
    Extend the EXISTING `WithholdingTaxRate` table rather than a new US table (D-01). In `tax.prisma`: add ONE nullable column to `WithholdingTaxRate`: `treatyArticle String? @db.VarChar(40)` (D-02). Do NOT touch the `@@unique([sourceCountry, contractorResidency, serviceType, effectiveFrom])` key — adding a 5th field is a breaking migration (Pitfall 1). Keep `treatyReference` free-text. Then add two enums `TaxFormType { W9 W8BEN W8BENE }` and `TaxFormStatus { DRAFT ACTIVE SUPERSEDED }` (UPPER_SNAKE per repo enum-casing gate), and the `TaxFormSubmission` model per the 85-RESEARCH field set: `id`, `organizationId`, `contractorId` (FK Contractor per D-12 — NOT Worker), `formType TaxFormType`, `status TaxFormStatus @default(DRAFT)`, `snapshotJson Json`, `treatyArticle String? @db.VarChar(40)`, `treatyRate Decimal? @db.Decimal(5,2)`, `contractorResidency String? @db.Char(2)`, `signerName String?`, `signedAt DateTime?`, `expiresAt DateTime?`, `supersededById String? @unique`, `createdAt DateTime @default(now())`. Add relations: `organization Organization @relation(...)`, `contractor Contractor @relation(...)`, and the self-relation pair `supersededBy`/`supersedes` named `"Supersede"`. Indexes: `@@index([organizationId, contractorId, formType, status])` and `@@index([organizationId, status, expiresAt])`. In `contractor.prisma` add the inverse `taxFormSubmissions TaxFormSubmission[]` back-relation in the Contractor relations block; in `organization.prisma` add the matching `taxFormSubmissions TaxFormSubmission[]` back-relation. No `console.*`; no app code here.
  </action>
  <verify>
    <automated>cd packages/db && pnpm exec prisma format --schema prisma/schema && pnpm exec prisma validate --schema prisma/schema</automated>
  </verify>
  <acceptance_criteria>
    - `prisma validate` exits 0 (schema is coherent, all relations have inverse sides).
    - `grep -n "treatyArticle" packages/db/prisma/schema/tax.prisma` shows the new column on `WithholdingTaxRate` AND on `TaxFormSubmission`.
    - `grep -n "model TaxFormSubmission\|enum TaxFormType\|enum TaxFormStatus" packages/db/prisma/schema/tax.prisma` returns all three.
    - `grep -c "@@unique(\[sourceCountry, contractorResidency, serviceType, effectiveFrom\])" packages/db/prisma/schema/tax.prisma` is 1 (key UNCHANGED).
    - `grep -n "taxFormSubmissions" packages/db/prisma/schema/contractor.prisma packages/db/prisma/schema/organization.prisma` shows the back-relation on both.
    - `pnpm db:audit-enum-casing` (or the repo enum-casing gate) passes for the two new enums.
  </acceptance_criteria>
  <done>Schema extended additively, `TaxFormSubmission` + enums + back-relations present, `@@unique` key untouched, `prisma validate` green.</done>
</task>

<task type="auto">
  <name>Task 2: Seed US treaty rows (PL/DE/UK/IE/NL reduced; AE/SA + XX statutory) in wht-rates.ts</name>
  <files>packages/db/prisma/seed/wht-rates.ts, packages/db/prisma/seed/index.ts</files>
  <read_first>
    - packages/db/prisma/seed/wht-rates.ts — read the existing SA rows + the `upsert` block (L147-162) + the `'XX'` fallback rows (L108-144) + the AE `treatyRate: null` row (L32-40). Copy units exactly (whole-number percent).
    - packages/db/prisma/seed/index.ts — read `main()` (L30-34) to confirm `seedWhtRates(prisma)` is already wired (extend in-place) or add a `seedUsTreatyRates` call.
    - 85-RESEARCH.md § "US treaty seed rows" code block + § "W-Form Field → Structured Column Map" (article assignments) + Assumptions A1/A2/A6.
    - 85-VALIDATION.md § Manual-Only Verifications (UAE/KSA = no US treaty → 30% statutory).
  </read_first>
  <action>
    Add `sourceCountry='US'` rows to the seed, all `serviceType: 'business_profits'`, `standardRate: 30.0`, `effectiveFrom` a sensible treaty-era date (D-03). Treaty countries PL/DE/GB/IE/NL: `treatyRate: 0.0`, `treatyArticle: 'Article 7'`, `treatyReference` naming the specific income-tax treaty (business profits, no US PE/fixed base). Non-treaty AE and SA (A6): `treatyRate: null`, `treatyArticle: null`, `treatyReference` annotating "no US income-tax treaty — 30% statutory". Add the `'XX'` fallback row: `treatyRate: null` (→ 30% statutory default). Annotate the whole US block with a comment marking the rates/articles as ADVISER-DEFERRED placeholders (D-04) — use a domain-meaningful comment, NOT a planning-ID breadcrumb. Reuse the existing `upsert` keyed on the composite unique. If `seedWhtRates` already runs in `index.ts main()`, extending it in-place needs no `index.ts` change; only touch `index.ts` if you split a new `seedUsTreatyRates` fn.
  </action>
  <verify>
    <automated>cd packages/db && grep -v '^\s*//' prisma/seed/wht-rates.ts | grep -c "sourceCountry: 'US'"</automated>
  </verify>
  <acceptance_criteria>
    - The grep above returns ≥ 8 (PL, DE, GB, IE, NL, AE, SA, XX).
    - `grep -v '^\s*//' packages/db/prisma/seed/wht-rates.ts | grep -c "treatyArticle: 'Article 7'"` returns ≥ 5 (the five treaty countries).
    - `grep -c "0\\.30\|0\\.0[0-9]" packages/db/prisma/seed/wht-rates.ts` on the new rows confirms NO fractional rate stored (Pitfall 3 — must be `30.0`/`0.0`/`null`, never `0.30`).
    - AE and SA rows have `treatyRate: null` (no US treaty, A6).
    - `pnpm typecheck --filter @contractor-ops/db` passes for the seed file.
  </acceptance_criteria>
  <done>US treaty rows seeded in percent units with structured `treatyArticle`; AE/SA + XX are statutory (null rate); adviser-deferred annotation present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Generate client + create + apply multi-region migration (EU/ME/US)</name>
  <files>packages/db/prisma/migrations/ (generated)</files>
  <read_first>
    - packages/db/scripts/migrate-all-regions.ts — read L43 (`DATABASE_URL_US` in `REGION_ENV_VARS`) + L51-92 (iterate EU/ME/US, fail fast). Do NOT edit this script (85-PATTERNS.md § migrate-all-regions — run, do not edit).
    - packages/db/package.json — confirm the `db:generate`, `db:migrate:dev`, `db:migrate:all` scripts (repo uses `prisma migrate`, NOT `db push`; 85-RESEARCH.md § Environment BLOCKING).
    - .planning/STATE.md — prior-phase note: `migrate dev` has hit pre-existing migration-history drift; if `db:migrate:dev` is drift-blocked locally, the additive `ALTER`/`CREATE TABLE` fallback used in Phases 82-84 applies, but the canonical path is `migrate dev`.
  </read_first>
  <what-built>
    Schema edits (Task 1) + seed (Task 2) are complete. This step regenerates the typed Prisma client
    (downstream plans cannot typecheck without `TaxFormSubmission`/`treatyArticle` in the client) and
    creates + applies the migration across EU/ME/US. Build + typecheck pass WITHOUT the live migration
    (types come from the generated client), so this step is mandatory to avoid a false-positive
    verification (Pitfall 5 — US region must not be left without the table).
  </what-built>
  <action>
    Run autonomously in order (each step is CLI-runnable):
    1. `pnpm --filter @contractor-ops/db db:generate` — regenerate the typed client (`src/generated/prisma/client`); downstream plans depend on `TaxFormSubmission`/`treatyArticle` being present.
    2. `pnpm --filter @contractor-ops/db db:migrate:dev --name add_tax_form_submission_and_treaty_article` — create + apply the migration SQL locally. If blocked by the pre-existing migration-history drift documented in STATE.md (Phase 82-84 posture), apply the additive `CREATE TABLE "TaxFormSubmission"` + the two enum types + `ALTER TABLE "WithholdingTaxRate" ADD COLUMN "treatyArticle"` via the documented direct-ALTER fallback and record the fallback in the SUMMARY.
    3. `pnpm --filter @contractor-ops/db db:migrate:all` — apply across EU/ME/US (`DATABASE_URL_US` already wired in `migrate-all-regions.ts:43`); a missing local `DATABASE_URL_US` is skipped (no-op) — confirm EU at minimum.
    4. `pnpm --filter @contractor-ops/db db:seed` (or the seed entrypoint) — apply the US rows from Task 2.
    Then pause for human confirmation that the migration landed (or that the drift-fallback was taken), because a per-region production apply and any drift resolution are operational decisions.
  </action>
  <how-to-verify>
    Confirm each command above succeeded (or the drift-fallback was applied + recorded):
    - `src/generated/prisma/client` reflects `TaxFormSubmission` + `treatyArticle`.
    - The migration (or direct-ALTER fallback) applied to EU at minimum; `db:migrate:all` ran for available regions.
    - The US seed rows are present.
    - `pnpm typecheck --filter @contractor-ops/db` is green against the regenerated client.
  </how-to-verify>
  <verify>
    <automated>pnpm --filter @contractor-ops/db db:generate && pnpm typecheck --filter @contractor-ops/db</automated>
  </verify>
  <resume-signal>Type "approved" once the client is regenerated and the migration is applied (or describe the drift-fallback taken).</resume-signal>
  <done>Prisma client regenerated with `TaxFormSubmission` + `treatyArticle`; migration applied to available regions (or drift-fallback recorded); US seed rows present; `typecheck --filter @contractor-ops/db` green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed script → DB | Seed values are developer-authored; adviser-deferred rates are placeholders, not authoritative legal data |
| migration runner → regional DBs | Schema must land identically across EU/ME/US or US contractors hit "table does not exist" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-85-01-01 | Tampering | WithholdingTaxRate `@@unique` key | mitigate | Add only the nullable `treatyArticle` column; never alter the 4-field unique key (a key change breaks the seed upsert + `calculateWht` lookup). Verified by grep gate. |
| T-85-01-02 | Information Disclosure | TaxFormSubmission `snapshotJson` | mitigate | Schema-level: `snapshotJson` is `Json`; the storage discipline (last-4 only, never full SSN) is enforced in Plan 03's service. This plan only provisions the column. |
| T-85-01-03 | Denial of Service | Regional migration drift (US not migrated) | mitigate | Run `db:migrate:all` (EU/ME/US), not single-region deploy (Pitfall 5). BLOCKING checkpoint. |
| T-85-01-04 | Tampering | Treaty rate units (fraction vs percent) | mitigate | Seed US rows in whole-number percent (`30.0`/`0.0`/`null`); grep gate rejects fractional values (Pitfall 3 — a `0.30` would under-withhold 100×). |
| T-85-01-05 | Repudiation | Mutating a signed legal record | mitigate | `TaxFormSubmission.status` machine (DRAFT/ACTIVE/SUPERSEDED) + `supersededById` chain at the schema level; append-only enforcement lands in Plan 03's service. |
| T-85-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (RESEARCH § Package Legitimacy Audit: N/A — zero new external deps). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `prisma validate` green; `treatyArticle` on both `WithholdingTaxRate` and `TaxFormSubmission`.
- `TaxFormSubmission` + `TaxFormType` + `TaxFormStatus` exist; back-relations on Contractor + Organization.
- ≥ 8 US seed rows in percent units; AE/SA/XX statutory (null rate); 5 treaty rows with `Article 7`.
- Multi-region migration applied (or drift-fallback documented); regenerated client typechecks.
</verification>

<success_criteria>
- US-LOC-02/03 data substrate exists: structured treaty column + seeded per-country rows.
- US-FORM-01/02 record substrate exists: immutable `TaxFormSubmission` with supersede chain FK'd to Contractor.
- EU/ME/US schemas carry the new table + column.
</success_criteria>

<output>
Create `.planning/phases/85-theme-a-w-form-intake-tax-treaty-engine/85-01-SUMMARY.md` when done.
</output>
