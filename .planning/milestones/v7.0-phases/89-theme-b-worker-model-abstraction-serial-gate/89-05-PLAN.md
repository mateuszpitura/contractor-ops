---
phase: 89-theme-b-worker-model-abstraction-serial-gate
plan: 05
type: execute
wave: 3
depends_on: ["89-01", "89-02"]
files_modified:
  - packages/auth/src/permissions.ts
  - packages/auth/src/roles.ts
  - packages/auth/src/__tests__/roles.test.ts
  - packages/api/src/__tests__/worker-tenant-isolation.test.ts
autonomous: true
requirements: [WORKER-03, WORKER-04]
must_haves:
  truths:
    - "A new `employee` resource is added to accessControlStatement with an HR-only action set, gating HR-only fields by per-type RBAC (D-08)"
    - "Four new roles (HR_ADMIN, HR_MANAGER, PAYROLL_OFFICER, LEAVE_APPROVER — reconciled to the existing snake_case RoleName convention) are added via ac.newRole, granting only `employee` (+ minimal read) actions and NOT contractor mutations (BFLA fence) (D-08)"
    - "The existing roles (owner/admin/finance_admin/ops_manager/team_manager/legal_compliance_viewer/it_admin/external_accountant/readonly/platform_operator) are byte-identical — a regression test asserts no existing grant changed; the duplicated owner allPermissions const is left untouched (D-08)"
    - "Worker is tenant-owning: a cross-org leak test proves an ORG_A caller cannot read ORG_B Worker rows; Worker inherits withTenantScope (organizationId), is NOT in globalModels (D-09/D-11)"
  artifacts:
    - path: "packages/auth/src/permissions.ts"
      provides: "employee resource in accessControlStatement"
      contains: "employee:"
    - path: "packages/auth/src/roles.ts"
      provides: "4 new HR roles via ac.newRole"
    - path: "packages/api/src/__tests__/worker-tenant-isolation.test.ts"
      provides: "Worker cross-org leak test"
  key_links:
    - from: "packages/auth/src/roles.ts"
      to: "packages/auth/src/permissions.ts"
      via: "ac.newRole references the employee resource action set"
      pattern: "employee:"
    - from: "packages/api/src/__tests__/worker-tenant-isolation.test.ts"
      to: "packages/db/src/tenant.ts"
      via: "withTenantScope org injection on Worker (Worker absent from globalModels)"
      pattern: "organizationId"
---

<objective>
WORKER-03 + WORKER-04: the per-type RBAC and the four new HR roles, plus the Worker cross-org leak test. Additive only — a new `employee` resource and four roles whose grants are narrow (employee actions, not contractor mutations), with the existing roles proven byte-identical. Worker's tenant invariant is proven by a cross-org leak test cloned from tenant-isolation.test.ts.

Purpose: HR-only fields are gated by per-type RBAC and the new roles cannot reach contractor mutations (BFLA fence); the new tenant-owning Worker table cannot leak across orgs.
Output: employee resource + 4 roles + existing-roles-byte-identical regression + Worker cross-org leak test.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-CONTEXT.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-RESEARCH.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-PATTERNS.md

<interfaces>
<!-- accessControlStatement resource arrays (permissions.ts:12-46) — add `employee` after contractorPii (line 44): -->
  contractor: ['create','read','update','delete','bulk'],  invoice: ['create','read','update','delete','approve'],
  contractorPii: ['read'],

<!-- ac.newRole narrow-grant analog (roles.ts:75-88 finance_admin): -->
  finance_admin: ac.newRole({ contractor: ['read'], invoice: ['create','read','update','delete','approve'], ... }),

<!-- DO NOT TOUCH (roles.ts:18-42) — the duplicated owner allPermissions const (out of scope, D-08). -->
<!-- RoleName union (roles.ts:183): existing roles are snake_case (finance_admin, it_admin) — reconcile new role keys to this convention. -->

<!-- Worker cross-org leak idiom (tenant-isolation.test.ts): createCallerFactory + ORG_A_ID/ORG_B_ID two-org seed + cross-org NOT_FOUND assertions. -->
<!-- Worker is tenant-owning: organizationId present, NOT in globalModels (tenant.ts:42-68) → withTenantScope injects org scope. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: employee resource + 4 HR roles + existing-roles-byte-identical regression</name>
  <read_first>
    - packages/auth/src/permissions.ts:12-48 (accessControlStatement resource arrays + createAccessControl + the Resource/ActionsFor types)
    - packages/auth/src/roles.ts (full — the duplicated owner allPermissions 18-42 DO NOT TOUCH; finance_admin narrow-grant 75-88; the RoleName union 183; the snake_case naming convention)
    - packages/auth/src/__tests__/roles.test.ts + permissions.test.ts + role-permission-matrix.test.ts (the existing role assertions to extend; the matrix is the byte-identical guard candidate)
    - PATTERNS.md "packages/auth/src/{permissions.ts,roles.ts}" section (action set discretion; naming reconciliation note; BFLA fence)
  </read_first>
  <action>
    Modify packages/auth/src/permissions.ts: add an `employee` resource to accessControlStatement after `contractorPii` (line 44) with an HR-only action set — e.g. `employee: ['create','read','update','delete']` (+ any HR-only field action the four roles need); this is the per-type RBAC surface for HR-only fields. Keep the Resource/ActionsFor types intact (they derive from the statement).
    Modify packages/auth/src/roles.ts: add four roles via ac.newRole, reconciling the CONTEXT UPPER_SNAKE names to the existing snake_case RoleName convention (hr_admin, hr_manager, payroll_officer, leave_approver) and documenting the mapping in the SUMMARY (CONTEXT names HR_ADMIN etc.; the codebase convention is snake_case — match it unless a deliberate deviation is recorded). Grant each role ONLY `employee` (+ minimal read where needed) actions — NOT contractor create/update/delete (BFLA fence): hr_admin = full employee CRUD; hr_manager = employee read/update; payroll_officer = employee read (+ payroll-relevant read only); leave_approver = employee read (+ leave-approval action if added). Extend the RoleName union accordingly. DO NOT touch the duplicated owner allPermissions const (18-42).
    Extend packages/auth/src/__tests__/roles.test.ts (and/or role-permission-matrix.test.ts): assert the 4 new roles exist, assert the `employee` resource is present in accessControlStatement, and add a byte-identical regression asserting every PRE-EXISTING role's grant map is unchanged (snapshot or explicit per-role assertion of the 10 existing roles incl. owner). No breadcrumb IDs in comments.
  </action>
  <acceptance_criteria>
    - `grep -q "employee:" packages/auth/src/permissions.ts` (employee resource in accessControlStatement).
    - `grep -Eq "hr_admin|hr_manager|payroll_officer|leave_approver" packages/auth/src/roles.ts` (4 new roles; snake_case reconciliation documented in SUMMARY).
    - None of the 4 new roles grant contractor create/update/delete/bulk (BFLA fence) — asserted in the role test.
    - `pnpm --filter @contractor-ops/auth test -- roles` GREEN, including the existing-roles-byte-identical regression.
    - The duplicated owner allPermissions const (roles.ts:18-42) is unchanged (diff shows no edit there).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/auth test -- roles && grep -q "employee:" packages/auth/src/permissions.ts</automated>
  </verify>
  <done>employee resource + 4 narrow HR roles added; existing roles byte-identical (regression GREEN); owner allPermissions untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Worker cross-org leak test</name>
  <read_first>
    - packages/api/src/__tests__/tenant-isolation.test.ts (full — createCallerFactory, ORG_A_ID/ORG_B_ID two-org seed, the cross-org NOT_FOUND/empty-result assertions to clone)
    - packages/db/src/tenant.ts:42-68 (globalModels — confirm Worker is NOT present so withTenantScope injects organizationId on Worker reads)
    - packages/db/prisma/schema/worker.prisma (the Worker model from Plan 02 — organizationId column)
    - PATTERNS.md "Wave-0 Test Pattern Assignments" worker-tenant-isolation row + "Tenant scoping" shared pattern
  </read_first>
  <action>
    Create packages/api/src/__tests__/worker-tenant-isolation.test.ts cloning the tenant-isolation.test.ts two-org idiom: seed Worker rows for ORG_A and ORG_B (mock Prisma or the same harness the existing test uses), build ORG_A and ORG_B callers via createCallerFactory through the workerRouter (cross-type read), and assert an ORG_A caller reading workers returns ONLY ORG_A rows and never an ORG_B row (cross-org read returns no foreign rows). Add an assertion that the Worker model is absent from the globalModels set (import the set or assert via a tenant-scope behavior) so the tenant invariant is structurally guarded. Name the describe block so it is selectable via `test -- worker-tenant-isolation` (or `worker-cross-org-leak`). No breadcrumb IDs.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test -- worker-tenant-isolation` GREEN: ORG_A caller never sees ORG_B Worker rows.
    - The test asserts Worker is NOT in globalModels (tenant-owning) — either by importing the set or by a behavioral cross-org assertion.
    - `pnpm lint:no-breadcrumbs` clean for the new test file.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- worker-tenant-isolation</automated>
  </verify>
  <done>Worker cross-org leak test GREEN; tenant invariant on Worker proven; Worker confirmed tenant-owning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| role grants → resource actions | RBAC statement is server-authoritative; new roles must not widen contractor access |
| ORG_A caller → ORG_B Worker rows | Cross-tenant boundary on the new tenant-owning table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-05-01 | Elevation of privilege | 4 new HR roles reaching contractor mutations | mitigate | Roles grant only `employee` (+minimal read); explicit test asserts no contractor create/update/delete/bulk (BFLA fence, D-08) |
| T-89-05-02 | Tampering | accidental change to existing role grants | mitigate | Byte-identical regression on all 10 existing roles incl. owner; owner allPermissions const untouched (D-08) |
| T-89-05-03 | Information disclosure | cross-org read of Worker rows | mitigate | Worker tenant-owning, NOT in globalModels → withTenantScope injects organizationId; cross-org leak test (D-09/D-11) |
| T-89-05-SC | Tampering | npm installs | accept | No external packages installed (all in-tree) |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/auth test -- roles` GREEN (4 new roles + employee resource + existing-roles-byte-identical).
- `pnpm --filter @contractor-ops/api test -- worker-tenant-isolation` GREEN.
- `pnpm --filter @contractor-ops/auth typecheck` passes (RoleName union extended); `pnpm lint:no-breadcrumbs` clean.
</verification>

<success_criteria>
- employee resource + 4 narrow HR roles added; existing roles byte-identical; owner allPermissions untouched.
- New roles cannot reach contractor mutations (BFLA fence proven by test).
- Worker cross-org leak test proves the tenant invariant; Worker is tenant-owning (not globalModels).
</success_criteria>

<output>
Create `.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-05-SUMMARY.md` when done
</output>
