---
phase: 89-theme-b-worker-model-abstraction-serial-gate
plan: 06
type: execute
wave: 4
depends_on: ["89-03", "89-04", "89-05"]
files_modified:
  - .planning/brain/wiki/domains/worker-foundation.md
  - .planning/brain/wiki/structure/prisma-schema-areas.md
  - .planning/brain/wiki/structure/api-routers-catalog.md
  - .planning/brain/wiki/structure/key-services.md
  - .planning/brain/wiki/patterns/_index.md
  - .planning/brain/wiki/log.md
  - .planning/brain/wiki/hot.md
  - .planning/MEMORY.md
autonomous: true
requirements: [WORKER-01, WORKER-02, WORKER-03, WORKER-04, WORKER-05]
must_haves:
  truths:
    - "A worker-foundation domain wiki page documents the Worker base table + workerType discriminator + the one-time additive backfill + the workerType-scoped contractor reads, so the next agent does not re-derive the model (documentation-follows-code, D-12/D-13)"
    - "prisma-schema-areas + api-routers-catalog + key-services wiki pages reflect the Worker base, the worker/employee router split (contractor shape preserved), and the withWorkerTypeDefault extension + check:contractor-rawsql-workertype guard"
    - "A patterns page documents the worker-type Prisma extension (explicit-where-wins) + per-type RBAC + the three-layer flag-off so the convention is reusable in Phases 90-97"
    - "MEMORY.md records the two new invariants: (1) Worker base table + one-time backfill (WORKER-01 amended); (2) contractor reads are workerType-scoped by a central extension + the 4 raw-SQL sites are contractor-only-by-table guarded by check:contractor-rawsql-workertype"
  artifacts:
    - path: ".planning/brain/wiki/domains/worker-foundation.md"
      provides: "Worker/employee-foundation domain page (Purpose, Flow, Entry points, Agent mistakes)"
    - path: ".planning/MEMORY.md"
      provides: "two new Worker-abstraction invariants"
  key_links:
    - from: ".planning/brain/wiki/domains/worker-foundation.md"
      to: "packages/db/src/worker-type.ts"
      via: "verify_with reference to the extension + Worker model"
      pattern: "worker-type"
---

<objective>
Documentation-follows-code closure for the Worker gate: the wiki pages + MEMORY invariants land in the SAME phase as the code so the next agent (Phases 90-97) inherits an accurate compass. Covers the Worker base table, the backfill, the workerType-scoped reads + raw-SQL guard, the router split, the per-type RBAC, and the flag-off pattern.

Purpose: stale docs make the next agent hallucinate the model. This is a GATED requirement (pnpm check:wiki-brain) — the phase is not done until the wiki tracks the code.
Output: the worker-foundation domain page + updated structure/patterns pages + log/hot + MEMORY invariants + graph refresh; check:wiki-brain GREEN.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-CONTEXT.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-RESEARCH.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-PATTERNS.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-01-SUMMARY.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-02-SUMMARY.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-03-SUMMARY.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-04-SUMMARY.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Worker-foundation domain page + structure/patterns wiki updates</name>
  <read_first>
    - .planning/brain/wiki/index.md + hot.md (wiki structure, frontmatter/verify_with convention, source_commit bumping)
    - .planning/brain/wiki/structure/prisma-schema-areas.md + api-routers-catalog.md + key-services.md (the pages to extend — match their format)
    - .planning/brain/wiki/patterns/_index.md (where to register the new worker-type-extension / per-type-RBAC / flag-off pattern)
    - .planning/brain/wiki/meta/refresh-triggers.md (the trigger matrix this phase satisfies)
    - the five 89-0X-SUMMARY.md files (the actual landed file paths + decisions to document — read AFTER prior plans complete)
    - .planning/brain/wiki/domains/contractors-engagements.md (an existing domain page to mirror for the new worker-foundation page)
  </read_first>
  <action>
    Create .planning/brain/wiki/domains/worker-foundation.md (mirror an existing domain page's frontmatter incl. verify_with pointing at packages/db/prisma/schema/worker.prisma, packages/db/src/worker-type.ts, packages/api/src/routers/core/worker.ts + employee.ts, packages/api/src/middleware/require-workforce-flag.ts): document Purpose (the Theme B gate — Worker base table that Contractor/Employee link to), Flow (Worker is the identity root; Contractor links via sidecar workerId @unique; workerType defaults CONTRACTOR; one-time additive idempotent reversible per-region backfill; contractor reads workerType-scoped by withWorkerTypeDefault), Entry points (workerRouter/employeeRouter behind module.workforce-employees; contractor.* always-on, shape-preserved), and Agent mistakes (do NOT re-key Contractor.id; the 4 raw-SQL sites are extension blind spots guarded by check:contractor-rawsql-workertype; do NOT add Worker to globalModels; do NOT re-register the flag).
    Update prisma-schema-areas.md (Worker base + workerType + the additive backfill ordering), api-routers-catalog.md (worker/employee split + contractor shape frozen by snapshot; verify the appRouter mount), key-services.md (the backfill script + the extension chain). Add a patterns entry (worker-type Prisma extension with explicit-where-wins + per-type RBAC + three-layer flag-off) under patterns/_index.md (and a dedicated pattern page if the format warrants). Bump source_commit on touched pages per the HEAD-advanced trigger. No operational text in wiki (durable facts only — no "phase in progress").
  </action>
  <acceptance_criteria>
    - .planning/brain/wiki/domains/worker-foundation.md exists with verify_with referencing worker.prisma + worker-type.ts + the new routers, and the four documented sections (Purpose/Flow/Entry points/Agent mistakes).
    - prisma-schema-areas.md, api-routers-catalog.md, key-services.md, patterns/_index.md each updated to reflect the landed code.
    - No operational/round-status text in any wiki page (durable facts only).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/brain/wiki/domains/worker-foundation.md && grep -q "worker-type" .planning/brain/wiki/domains/worker-foundation.md</automated>
  </verify>
  <done>worker-foundation domain page + structure/patterns pages reflect the landed Worker gate; verify_with paths correct; no operational text.</done>
</task>

<task type="auto">
  <name>Task 2: MEMORY invariants + log/hot + graph refresh + check:wiki-brain</name>
  <read_first>
    - .planning/MEMORY.md (the invariant-bullet format to append to)
    - .planning/brain/wiki/log.md + hot.md (the log-append + hot-overwrite convention)
    - CLAUDE.md "Documentation follows code" section (the exact refresh pipeline: check:wiki-brain, graphify update, bm25 rebuild)
  </read_first>
  <action>
    Append two invariants to .planning/MEMORY.md: (1) "Worker base table + one-time backfill (WORKER-01 amended) — Contractor links via sidecar workerId @unique FK; Contractor.id stays stable; backfill is idempotent (WHERE workerId IS NULL) + reversible + per-region + audit-logged; NOT NULL enforced LAST after staging-snapshot parity." (2) "Contractor reads are workerType-scoped by the central withWorkerTypeDefault extension (explicit-where-wins); the 4 $queryRaw FROM Contractor sites are the extension's blind spot — contractor-only-by-table and guarded by check:contractor-rawsql-workertype (wired into lint:ci)." Keep the WHY, no breadcrumb IDs in any source comment (these go in .planning/, which is allowed).
    Append a log.md entry for the wiki changes and overwrite hot.md per convention. Rebuild the graph and BM25 index per the CLAUDE.md pipeline: `rm -f graphify-out/graph.json && graphify update . --no-cluster --force && cp graphify-out/graph.json .planning/graphs/graph.json` then `cd .planning/brain && find wiki -name '*.md' -exec python3 scripts/contextual-prefix.py {} --no-llm \; && python3 scripts/bm25-index.py build`. Then run `pnpm check:wiki-brain` and resolve any NEW drift it reports for files this phase touched (graph.json/BM25 absence is WARN-only).
  </action>
  <acceptance_criteria>
    - .planning/MEMORY.md contains both new invariants (Worker base + backfill; workerType-scoped reads + raw-SQL guard).
    - log.md has a new entry; hot.md overwritten per convention.
    - `pnpm check:wiki-brain` passes (no NEW drift for files touched this phase; graph/BM25 absence WARN-only is acceptable).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "withWorkerTypeDefault\|workerType-scoped\|Worker base table" .planning/MEMORY.md && pnpm check:wiki-brain</automated>
  </verify>
  <done>Two MEMORY invariants recorded; log/hot updated; graph+BM25 rebuilt; check:wiki-brain GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| code state → wiki/MEMORY | Documentation must track code; stale docs mislead the next agent (no untrusted runtime input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-06-01 | Repudiation / Information integrity | wiki drift vs landed code | mitigate | Documentation-follows-code in the same phase; check:wiki-brain gates NEW drift; verify_with paths point at the real files |
| T-89-06-SC | Tampering | npm installs | accept | No external packages installed (doc-only) |
</threat_model>

<verification>
- worker-foundation domain page exists with correct verify_with; structure/patterns pages updated.
- MEMORY has the two invariants; log/hot updated; graph+BM25 rebuilt.
- `pnpm check:wiki-brain` GREEN.
</verification>

<success_criteria>
- The wiki tracks the landed Worker gate (domain + structure + patterns); the next agent inherits an accurate compass.
- The two new invariants are in MEMORY; the refresh pipeline ran; check:wiki-brain passes.
</success_criteria>

<output>
Create `.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-06-SUMMARY.md` when done
</output>
