---
phase: 89-theme-b-worker-model-abstraction-serial-gate
plan: 04
type: execute
wave: 3
depends_on: ["89-01", "89-02"]
files_modified:
  - packages/api/src/routers/core/worker.ts
  - packages/api/src/routers/core/employee.ts
  - packages/api/src/middleware/require-workforce-flag.ts
  - packages/api/src/errors.ts
  - packages/api/src/root.ts
  - packages/api/src/__tests__/workforce-flag.test.ts
  - apps/web-vite/src/components/dashboard/dashboard-home.tsx
autonomous: true
requirements: [WORKER-02, WORKER-05]
must_haves:
  truths:
    - "A shared workerRouter (cross-type ops) and a skeleton employeeRouter exist; both pass an explicit workerType in their where so withWorkerTypeDefault does NOT force-filter them (workerRouter cross-type, employeeRouter workerType='EMPLOYEE') (D-05/D-06)"
    - "The existing contractorRouter composition (core+country+tax+engagements+bulk) and its `contractor:` mount in root.ts are unchanged in shape — the route-shape snapshot from Plan 01 stays GREEN (D-06/D-07)"
    - "With module.workforce-employees OFF, worker.* and employee.* are absent from appRouter (METHOD_NOT_FOUND) via conditional-spread, AND the per-request requireWorkforceFlag guard throws FORBIDDEN (defense-in-depth, mirroring us-expansion) (D-10)"
    - "contractor.* is NOT flag-gated — it is the always-on existing surface (D-06)"
    - "web-vite removes /employee/* surfaces from the render tree via useFlag('module.workforce-employees') (D-10); a WORKFORCE_DISABLED error key has en/en-US/de/pl/ar parity (D-14)"
  artifacts:
    - path: "packages/api/src/routers/core/worker.ts"
      provides: "shared cross-type workerRouter (explicit workerType)"
      exports: ["workerRouter"]
    - path: "packages/api/src/routers/core/employee.ts"
      provides: "flag-gated skeleton employeeRouter (workerType='EMPLOYEE')"
      exports: ["employeeRouter"]
    - path: "packages/api/src/middleware/require-workforce-flag.ts"
      provides: "assertWorkforceEnabled + isWorkforceRegistered"
      exports: ["assertWorkforceEnabled", "isWorkforceRegistered"]
  key_links:
    - from: "packages/api/src/root.ts"
      to: "packages/api/src/middleware/require-workforce-flag.ts"
      via: "conditional-spread of workforceRouters gated by isWorkforceRegistered()"
      pattern: "conditionalWorkforceRouters"
    - from: "apps/web-vite/src/components/dashboard/dashboard-home.tsx"
      to: "module.workforce-employees"
      via: "useFlag render-tree removal"
      pattern: "useFlag\\('module.workforce-employees'\\)"
---

<objective>
The WORKER-02 router split and the WORKER-05 three-layer flag-off — both exact mirrors of in-tree precedents. A shared workerRouter (cross-type), a skeleton employeeRouter, a require-workforce-flag guard, the conditional-spread in root.ts, the WORKFORCE_DISABLED error, and the web-vite render-tree removal. The existing contractorRouter shape is untouched (the Plan-01 snapshot proves it).

Purpose: expose the cross-type + employment surface while keeping the entire Theme B surface dark behind module.workforce-employees (PENDING). contractor.* stays always-on and shape-frozen.
Output: worker.ts + employee.ts + require-workforce-flag.ts + WORKFORCE_DISABLED + root.ts conditional-spread + web-vite render removal + the flag-gate integration test. Plan-01's route-shape snapshot stays GREEN.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-CONTEXT.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-RESEARCH.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-PATTERNS.md

<interfaces>
<!-- Conditional-spread + guard mirror (root.ts 157-163, 228; require-us-expansion-flag.ts 29-58): -->
From packages/api/src/root.ts:
  const usExpansionRouters = { taxForm: taxFormRouter } as const;
  const conditionalUsExpansionRouters = isUsExpansionRegistered() ? usExpansionRouters : ({} as typeof usExpansionRouters);
  // appRouter: { ..., contractor: contractorRouter (line 179, UNCHANGED), ...conditionalUsExpansionRouters }
From packages/api/src/middleware/require-us-expansion-flag.ts:
  assertUsExpansionEnabled(orgId, region): evaluate('module.us-expansion', ...); throws TRPCError FORBIDDEN US_EXPANSION_DISABLED
  isUsExpansionRegistered(): evaluate('module.us-expansion', { organizationId: 'ROOT', region: 'EU' }).enabled || Boolean(process.env.QA_DEFAULT_ORG_ID)
From packages/api/src/errors.ts:339: export const US_EXPANSION_DISABLED = 'usExpansionDisabled';

<!-- contractorRouter shape (DO NOT change — frozen by Plan-01 snapshot): -->
From packages/api/src/routers/core/contractor.ts:12: mergeRouters(core, country, tax, engagements, bulk)

<!-- web-vite render-removal analog (dashboard-home.tsx:83): -->
  const classificationEnabled = useFlag('module.classification-engine');

<!-- The flag is already PENDING — DO NOT re-register (flags-core.ts:220): -->
  'module.workforce-employees'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: require-workforce-flag guard + WORKFORCE_DISABLED error + workerRouter + skeleton employeeRouter</name>
  <read_first>
    - packages/api/src/middleware/require-us-expansion-flag.ts (full — assertUsExpansionEnabled 29-47, isUsExpansionRegistered 55-58 — exact mirror, rename only)
    - packages/api/src/errors.ts:339 (US_EXPANSION_DISABLED — add the parallel WORKFORCE_DISABLED)
    - packages/api/src/routers/core/contractor.ts (composition + how a router is built with the tRPC init helpers) and contractor-core.ts (a procedure with Zod .strict() input + writeAuditLog on mutation)
    - packages/feature-flags/src/flags-core.ts:220 (module.workforce-employees already PENDING — do NOT re-register)
    - the i18n error files where usExpansionDisabled is keyed (en/en-US/de/pl/ar) — add the workforceDisabled key with parity
    - PATTERNS.md require-workforce-flag.ts + worker.ts/employee.ts sections
  </read_first>
  <action>
    Create packages/api/src/middleware/require-workforce-flag.ts as an exact mirror of require-us-expansion-flag.ts: export `assertWorkforceEnabled(organizationId, region)` (evaluate 'module.workforce-employees', region coerced ME→'ME' else 'EU', throw TRPCError FORBIDDEN with message WORKFORCE_DISABLED + cause {flag, reason}, logger service:'workforce-flag-guard') and `isWorkforceRegistered()` (evaluate with organizationId:'ROOT', region:'EU' → .enabled || Boolean(process.env.QA_DEFAULT_ORG_ID)). Do NOT re-register the flag.
    Modify packages/api/src/errors.ts: add `export const WORKFORCE_DISABLED = 'workforceDisabled';` next to US_EXPANSION_DISABLED. Add the `workforceDisabled` message key to each i18n error file that carries `usExpansionDisabled`, with en/en-US/de/pl/ar parity (i18n:parity).
    Create packages/api/src/routers/core/worker.ts exporting `workerRouter` — cross-type ops; every procedure calls assertWorkforceEnabled and passes an EXPLICIT workerType in its where (so withWorkerTypeDefault does not force-filter — explicit-where-wins). Zod `.strict()` DTOs that reject organizationId/workerType in the input (set server-side from session); writeAuditLog on any mutation. Keep it minimal (a cross-type list/read is sufficient for the gate).
    Create packages/api/src/routers/core/employee.ts exporting `employeeRouter` — a SKELETON: minimal/empty procedures that call assertWorkforceEnabled and query with explicit `workerType: 'EMPLOYEE'`. No employee profile fields (Phase 90 scope fence). Zod .strict().
    No breadcrumb IDs in comments.
  </action>
  <acceptance_criteria>
    - `grep -q "assertWorkforceEnabled" packages/api/src/middleware/require-workforce-flag.ts` and `grep -q "isWorkforceRegistered" packages/api/src/middleware/require-workforce-flag.ts`; flag key is `module.workforce-employees`.
    - `grep -q "WORKFORCE_DISABLED" packages/api/src/errors.ts` and `pnpm i18n:parity` passes for the new workforceDisabled key (en/en-US/de/pl/ar).
    - worker.ts + employee.ts export workerRouter / employeeRouter; every procedure passes an explicit workerType and uses Zod .strict(); employeeRouter has no profile fields.
    - `pnpm --filter @contractor-ops/api typecheck` passes; `pnpm lint:no-breadcrumbs` clean.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/api typecheck && grep -q "assertWorkforceEnabled" packages/api/src/middleware/require-workforce-flag.ts && grep -q "WORKFORCE_DISABLED" packages/api/src/errors.ts</automated>
  </verify>
  <done>Guard + WORKFORCE_DISABLED (i18n parity) + workerRouter + skeleton employeeRouter exist; explicit workerType + .strict() throughout; flag not re-registered.</done>
</task>

<task type="auto">
  <name>Task 2: root.ts conditional-spread + web-vite render removal + flag-gate integration test</name>
  <read_first>
    - packages/api/src/root.ts (lines 3 import, 147 classification spread, 157-163 us-expansion spread, 179 contractor mount UNCHANGED, 227-228 the spread site; the const-lifting note 131-135)
    - packages/api/src/middleware/require-workforce-flag.ts (the isWorkforceRegistered just created)
    - apps/web-vite/src/components/dashboard/dashboard-home.tsx:83 (useFlag('module.classification-engine') render-removal analog)
    - PATTERNS.md root.ts conditional-spread + web-vite render-removal sections; "Wave-0 Test Pattern Assignments" workforce-flag row
    - apps/web-vite/ARCHITECTURE.md (Page→Container→Hook→Component layers if any skeleton surface renders)
  </read_first>
  <action>
    Modify packages/api/src/root.ts: import workerRouter, employeeRouter, and isWorkforceRegistered. Add `const workforceRouters = { worker: workerRouter, employee: employeeRouter } as const;` and `const conditionalWorkforceRouters = isWorkforceRegistered() ? workforceRouters : ({} as typeof workforceRouters);` mirroring the us-expansion idiom (preserve the const-lifting cast so client typing stays stable). Spread `...conditionalWorkforceRouters` into appRouter next to `...conditionalUsExpansionRouters`. CRITICAL: do NOT touch the `contractor: contractorRouter` mount (line 179) — it stays always-on and shape-unchanged.
    Modify apps/web-vite/src/components/dashboard/dashboard-home.tsx (or the nav/route surface that would host /employee/*): add `const workforceEnabled = useFlag('module.workforce-employees');` and remove the /employee/* surface from the render tree when false, mirroring the classification render-removal. Minimal this phase (flag-dark skeleton); if a skeleton surface renders, follow web-vite Page→Container→Hook→Component + WCAG loading/empty/error. Run `pnpm check:web-vite-data-layer` if any web-vite file is touched.
    Create packages/api/src/__tests__/workforce-flag.test.ts: with the flag OFF (isWorkforceRegistered false / evaluate disabled), assert `worker` and `employee` are ABSENT from appRouter._def.procedures (METHOD_NOT_FOUND surface) AND assertWorkforceEnabled throws FORBIDDEN; with the flag ON, assert worker.* / employee.* are present. Assert `contractor.*` is present in BOTH states (never flag-gated). Mirror the us-expansion flag behavior.
  </action>
  <acceptance_criteria>
    - `grep -q "conditionalWorkforceRouters" packages/api/src/root.ts` and `...conditionalWorkforceRouters` is spread into appRouter; the `contractor: contractorRouter` mount is unchanged (the Plan-01 route-shape snapshot stays GREEN).
    - `grep -q "useFlag('module.workforce-employees')" apps/web-vite/src/components/dashboard/dashboard-home.tsx` (or the chosen nav surface) and `/employee/*` is removed from the tree when off.
    - `pnpm --filter @contractor-ops/api test -- workforce-flag` GREEN: flag-off → worker/employee absent + guard FORBIDDEN; flag-on → present; contractor.* present in both.
    - `pnpm --filter @contractor-ops/api test -- contractor-contract-snapshot` still GREEN (no shape drift).
    - `pnpm check:web-vite-data-layer` passes if a web-vite file changed.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- workforce-flag contractor-contract-snapshot && grep -q "conditionalWorkforceRouters" packages/api/src/root.ts</automated>
  </verify>
  <done>Conditional-spread wired (contractor mount untouched); web-vite render-removal in place; flag-gate test GREEN; route-shape snapshot still GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → tRPC (worker/employee namespaces) | Untrusted input; flag-gated + Zod .strict() at route entry |
| flag state → router availability | Defense-in-depth: load-time absent + per-request FORBIDDEN + render-tree removal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-04-01 | Elevation of privilege | dark employee/worker surface | mitigate | Three-layer flag-off: conditional-spread (METHOD_NOT_FOUND) + assertWorkforceEnabled per-request (FORBIDDEN) + web-vite render-tree removal (D-10) |
| T-89-04-02 | Tampering | mass-assignment of organizationId/workerType on worker/employee create | mitigate | Zod .strict() DTOs reject those keys; organizationId session-derived; workerType set server-side (RESEARCH Security V5) |
| T-89-04-03 | Tampering | contractor.* route-shape drift during split | mitigate | contractor mount unchanged; Plan-01 snapshot fails CI on any drift (D-06/D-07) |
| T-89-04-04 | Information disclosure | cross-type leak via explicit workerType bypass | mitigate | workerRouter/employeeRouter pass explicit workerType (explicit-where-wins is intended, not a leak); withWorkerTypeDefault defaults contractor reads |
| T-89-04-SC | Tampering | npm installs | accept | No external packages installed (all in-tree) |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api typecheck` + `test -- workforce-flag contractor-contract-snapshot` GREEN.
- `pnpm i18n:parity` passes for workforceDisabled (en/en-US/de/pl/ar).
- `pnpm check:web-vite-data-layer` passes if web-vite touched.
- `pnpm lint:no-breadcrumbs` + `pnpm lint:audit-log` clean for new mutation procedures.
</verification>

<success_criteria>
- Shared workerRouter + skeleton employeeRouter exist, both passing explicit workerType; contractorRouter shape frozen (snapshot GREEN).
- Three-layer flag-off proven: flag-off → worker/employee absent + FORBIDDEN; flag-on → present; contractor.* always present.
- web-vite removes /employee/* from the render tree when the flag is off; WORKFORCE_DISABLED has full i18n parity.
</success_criteria>

<output>
Create `.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-04-SUMMARY.md` when done
</output>
