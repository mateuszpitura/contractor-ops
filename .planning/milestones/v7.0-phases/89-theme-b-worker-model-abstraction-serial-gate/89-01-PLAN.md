---
phase: 89-theme-b-worker-model-abstraction-serial-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/__tests__/contractor-contract-snapshot.test.ts
  - packages/api/src/__tests__/worker-regression.test.ts
  - packages/db/src/__tests__/backfill-worker.test.ts
  - packages/db/src/__tests__/worker-type.test.ts
autonomous: true
requirements: [WORKER-01, WORKER-02]
must_haves:
  truths:
    - "The contractor.* tRPC procedure surface (names + input/output shapes) is captured as a GREEN snapshot against the CURRENT pre-Worker router, so any later split that drops/renames/reshapes a procedure fails CI (D-07)"
    - "The contractor-parity regression suite (list / dashboard / payment-run / classification-scan / export / portal reads, including the 4 raw-SQL sites) runs GREEN against the current pre-Worker schema — the baseline the post-backfill parity is proven against (D-03)"
    - "The withWorkerTypeDefault extension behavior is locked as a RED scaffold: injects workerType='CONTRACTOR' on find*/count/aggregate/groupBy on Worker reads and leaves an explicit caller-supplied workerType untouched (explicit-where-wins, D-04/D-05)"
    - "planWorkerBackfill idempotency is locked as a RED scaffold: a contractor already carrying a workerId is skipped, a re-run produces zero new inserts (D-02)"
  artifacts:
    - path: "packages/api/src/__tests__/contractor-contract-snapshot.test.ts"
      provides: "contractor.* route-shape snapshot lock (GREEN at capture, RED on drift)"
      contains: "appRouter._def.procedures"
    - path: "packages/api/src/__tests__/worker-regression.test.ts"
      provides: "contractor-parity BASELINE lock (GREEN pre-Worker)"
    - path: "packages/db/src/__tests__/backfill-worker.test.ts"
      provides: "planWorkerBackfill idempotency RED scaffold"
    - path: "packages/db/src/__tests__/worker-type.test.ts"
      provides: "withWorkerTypeDefault extension RED scaffold (all read ops + explicit-where-wins)"
  key_links:
    - from: "packages/api/src/__tests__/contractor-contract-snapshot.test.ts"
      to: "packages/api/src/root.ts"
      via: "appRouter._def.procedures introspection"
      pattern: "appRouter\\._def\\.procedures"
---

<objective>
Lock every contractor behavior the gate must not regress BEFORE any model change exists. This is the Wave-0 RED scaffold plan plus the two GREEN baselines: the contractor.* route-shape snapshot and the contractor-parity regression suite, both captured against the current pre-Worker router/schema. The extension and backfill scaffolds are RED (terminal until Waves 2-3 turn them GREEN).

Purpose: "Lock the contract before you move it." A snapshot or regression suite captured AFTER the split locks the wrong shape (Pitfall 6) — these must exist and pass on the unmodified codebase so drift is provable.
Output: Two GREEN baseline tests (route-shape snapshot, contractor-parity regression) + two RED scaffolds (backfill idempotency, withWorkerTypeDefault extension).
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-CONTEXT.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-RESEARCH.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-PATTERNS.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-VALIDATION.md

<interfaces>
<!-- tRPC v11 _def.procedures is a flat Record<string, AnyProcedure> keyed by dotted path. -->
<!-- contractorRouter composition (DO NOT change shape — snapshot freezes it): -->
From packages/api/src/routers/core/contractor.ts:
  export const contractorRouter = mergeRouters(
    contractorCoreRouter, contractorCountryRouter, contractorTaxRouter,
    contractorEngagementsRouter, contractorBulkRouter,
  );

<!-- Parity-regression + tenant-isolation idiom (mock Prisma + createCallerFactory): -->
From packages/api/src/__tests__/tenant-isolation.test.ts (lines 1-60): two-org seed (ORG_A/ORG_B), createCallerFactory, mocked ctx.db.

<!-- The 4 raw-SQL sites the parity suite must cover (extension blind spot): -->
  packages/api/src/routers/core/dashboard.ts:76   (fetchKpis activeContractors COUNT)
  packages/api/src/routers/core/search.ts:75       (command-palette contractor FTS)
  packages/api/src/routers/core/contractor-shared.ts:305  (billingModel JSONB facet id-set)
  packages/api/src/routers/core/contractor-shared.ts:324  (FTS facet id-set)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GREEN baselines — contractor.* route-shape snapshot + contractor-parity regression</name>
  <read_first>
    - packages/api/src/root.ts (appRouter export; contractor mount line 179; conditional-spread idiom 157-163)
    - packages/api/src/routers/core/contractor.ts (the mergeRouters composition that must be frozen)
    - packages/api/src/__tests__/tenant-isolation.test.ts (lines 1-60 — mock-Prisma + createCallerFactory idiom to clone)
    - packages/api/src/routers/core/dashboard.ts:58-90, search.ts:60-90, contractor-shared.ts:300-335 (the 4 raw-SQL sites the parity suite asserts)
    - PATTERNS.md "Pattern 3: tRPC contract snapshot" + "Wave-0 Test Pattern Assignments" rows for these two files
  </read_first>
  <action>
    Create packages/api/src/__tests__/contractor-contract-snapshot.test.ts: import appRouter from ../root, build a sorted array of Object.keys(appRouter._def.procedures) filtered to keys starting with 'contractor.', assert toMatchSnapshot(). Add a second assertion serializing each contractor.* procedure's input/output shape — first attempt zod v4 native z.toJSONSchema() on proc._def input/output (A1: verify the export exists in the pinned zod ^4.4.3 at run time); if z.toJSONSchema is unavailable, fall back to snapshotting the sorted procedure-name list ONLY and record the fallback in the SUMMARY (do NOT add zod-to-json-schema in this plan — that is a gated install). Capture the snapshot against the CURRENT router so it is GREEN now.
    Create packages/api/src/__tests__/worker-regression.test.ts as the contractor-parity BASELINE: clone the tenant-isolation.test.ts mock-Prisma + createCallerFactory setup; exercise the contractor read paths the gate must not regress — contractor list (contractor.list via buildContractorListWhere), dashboard fetchKpis activeContractors count, a payment-run contractor read, a classification-scan contractor read, an export read, and a portal contractor read — plus the 4 raw-SQL sites (dashboard.ts:76, search.ts:75, contractor-shared.ts:305 + :324). Assert each returns the expected contractor-only rows/counts against the current (pre-Worker) schema so the suite is GREEN now and becomes the provable baseline for post-backfill parity. Name the describe block so it is selectable via `test -- contractor-parity`. No breadcrumb IDs in source comments (lint:no-breadcrumbs).
  </action>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test -- contractor-contract-snapshot` is GREEN and writes a snapshot file containing the sorted contractor.* procedure names (and shapes if z.toJSONSchema available).
    - `pnpm --filter @contractor-ops/api test -- contractor-parity` is GREEN against the current schema and covers all six read paths + the 4 raw-SQL sites named above.
    - Source contains the literal `appRouter._def.procedures` and a snapshot assertion.
    - `pnpm lint:no-breadcrumbs` passes for the two new files (no Phase/WORKER/D-NN IDs in comments).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- contractor-contract-snapshot contractor-parity</automated>
  </verify>
  <done>Both baselines GREEN; snapshot file written; lint:no-breadcrumbs clean for the new files.</done>
</task>

<task type="auto">
  <name>Task 2: RED scaffolds — backfill idempotency + withWorkerTypeDefault extension</name>
  <read_first>
    - packages/db/scripts/backfill-free-zone-assignment.ts:85-124 (planFreeZoneBackfill — the pure-transform + idempotency-guard analog)
    - packages/db/src/soft-delete.ts:5-7, 45-53, 117-157 (PrismaExtensible type + injectDeletedAtNull + per-op $allModels injection, incl. findUnique)
    - packages/db/src/tenant.ts:18-20, 71-80 (PrismaExtensible export + READ_OPERATIONS set the extension's read-op set must match)
    - RESEARCH.md "Pattern 1: withWorkerTypeDefault" (the injectWorkerTypeDefault explicit-where-wins guard + WORKER_TYPE_READ_OPS) + "Code Examples → Backfill plan transform"
    - PATTERNS.md "Highest-Risk Items" (design A: model set is {'Worker'} only)
  </read_first>
  <action>
    Create packages/db/src/__tests__/backfill-worker.test.ts as a RED scaffold for a not-yet-existing pure export `planWorkerBackfill(rows)` (Plan 03 implements it; import is expected to fail until then). Assert: (1) a contractor with workerId=null yields one WorkerInsert carrying its organizationId + shared identity; (2) a contractor already carrying a workerId is skipped (idempotency); (3) running the plan over its own already-linked output yields zero new inserts (re-run safety); (4) the source array is never mutated. Mark RED-terminal-until-Plan-03 in the SUMMARY.
    Create packages/db/src/__tests__/worker-type.test.ts as a RED scaffold for the not-yet-existing `withWorkerTypeDefault` extension (Plan 02 implements it). Using a mock-query client in the soft-delete-test style, assert across all read ops (findMany, findFirst, findFirstOrThrow, findUnique, findUniqueOrThrow, count, aggregate, groupBy) on a `worker` model read: (a) when where has no workerType, the extension injects workerType='CONTRACTOR'; (b) when the caller passes an explicit workerType (e.g. 'EMPLOYEE'), the where is left untouched (explicit-where-wins); (c) a non-Worker model read is not modified (design A: model set is {'Worker'} only). Mark RED-terminal-until-Plan-02.
    No breadcrumb IDs in source comments.
  </action>
  <acceptance_criteria>
    - packages/db/src/__tests__/backfill-worker.test.ts exists and references a `planWorkerBackfill` import (RED — fails because the export does not exist yet).
    - packages/db/src/__tests__/worker-type.test.ts exists, references `withWorkerTypeDefault`, and asserts inject-default + explicit-where-wins across the 8 read ops (RED).
    - Both RED states are documented in the SUMMARY as terminal until Plans 02/03 turn them GREEN; the failures are import/undefined errors, NOT assertion-logic errors.
    - `pnpm lint:no-breadcrumbs` clean for the two new files.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/db test -- backfill-worker worker-type 2>&1 | grep -Eiq "fail|cannot find|is not (defined|a function)|RED" && echo "RED scaffolds present" || (echo "expected RED scaffolds" && exit 1)</automated>
  </verify>
  <done>Both RED scaffolds present and failing for the right reason (missing export, not bad logic); documented terminal-RED in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → router/extension under test | Tests assert the contractor surface; no live data or external input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01-01 | Tampering | contractor.* route-shape | mitigate | Snapshot captured GREEN on current router; CI fails on any name/shape drift after the split (D-07) |
| T-89-01-02 | Information disclosure | contractor read paths (incl. 4 raw-SQL sites) | mitigate | Parity baseline locks current contractor-only result sets; post-backfill suite must match (D-03) |
| T-89-01-SC | Tampering | npm installs | mitigate | No installs in this plan; if z.toJSONSchema is unavailable the fallback is names-only snapshot, NOT an unvetted dependency add |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- contractor-contract-snapshot contractor-parity` GREEN (baselines locked on current schema).
- `pnpm --filter @contractor-ops/db test -- backfill-worker worker-type` RED for the right reason (missing exports).
- `pnpm lint:no-breadcrumbs` clean for the four new files.
</verification>

<success_criteria>
- Route-shape snapshot + contractor-parity regression GREEN against the unmodified codebase (baselines that make "shapes preserved" and "zero regression" provable).
- Backfill-idempotency and extension RED scaffolds in place, terminal until Plans 02/03.
- No new dependency added; no breadcrumb IDs in comments.
</success_criteria>

<output>
Create `.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-01-SUMMARY.md` when done
</output>
