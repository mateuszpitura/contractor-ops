---
phase: 89-theme-b-worker-model-abstraction-serial-gate
plan: 03
type: execute
wave: 3
depends_on: ["89-02"]
files_modified:
  - packages/db/scripts/backfill-worker.ts
  - packages/db/prisma/schema/contractor.prisma
  - packages/db/prisma/schema/migrations/__worker_id_required/migration.sql
autonomous: false
requirements: [WORKER-01]
must_haves:
  truths:
    - "planWorkerBackfill is a pure, unit-tested transform that emits one WorkerInsert per contractor with workerId IS NULL and skips already-linked contractors, so a re-run produces zero new inserts (idempotent, D-02)"
    - "The backfill script creates one Worker per unlinked contractor and atomically sets Contractor.workerId in the same transaction, batched for the largest org, with a --dry-run that performs zero writes and masked-DB-URL pino logging (D-02)"
    - "A single system-actor writeAuditLog row records the one-time backfill per region (D-13)"
    - "A reversible down/rollback path nulls Contractor.workerId and drops orphaned Worker rows without ever destroying or relinking a Contractor row (D-02)"
    - "Migration B (workerId SET NOT NULL + FK REFERENCES Worker(id)) is authored to run LAST — only after largest-org staging-snapshot contractor parity is signed off (D-02/D-03)"
  artifacts:
    - path: "packages/db/scripts/backfill-worker.ts"
      provides: "idempotent reversible per-region Worker backfill + planWorkerBackfill pure export"
      exports: ["planWorkerBackfill"]
    - path: "packages/db/prisma/schema/migrations/__worker_id_required/migration.sql"
      provides: "Migration B — NOT NULL + FK enforcement (runs LAST, after parity)"
      contains: "SET NOT NULL"
  key_links:
    - from: "packages/db/scripts/backfill-worker.ts"
      to: "packages/db/scripts/backfill-free-zone-assignment.ts"
      via: "structural mirror (pure transform + $transaction + --dry-run + masked pino)"
      pattern: "createPrismaClientForUrl"
    - from: "packages/db/scripts/backfill-worker.ts"
      to: "packages/api/src/services/audit-writer.ts"
      via: "system-actor writeAuditLog row"
      pattern: "writeAuditLog"
---

<objective>
The milestone's highest-risk operation: the one-time, additive, idempotent, per-region, reversible data backfill that creates a Worker row per existing v1-v6 contractor and links it, then — only after largest-org staging-snapshot contractor parity is signed off — enforces workerId NOT NULL + FK (Migration B, LAST). This plan is NON-AUTONOMOUS: the per-region apply and the NOT-NULL enforcement are gated behind a [BLOCKING] human checkpoint (P83-88 convention), because they mutate live regional DBs.

Purpose: zero contractor-row loss is the gate's pass condition. The backfill is additive-only and reversible; parity is verified on a real largest-org snapshot before NOT NULL makes the link irreversible-in-practice.
Output: backfill-worker.ts (turns Plan-01's backfill RED scaffold GREEN) + Migration B + a [BLOCKING] apply/parity/enforce checkpoint.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-CONTEXT.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-RESEARCH.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-PATTERNS.md
@.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-VALIDATION.md

<interfaces>
<!-- Backfill script structural analog (copy verbatim, swap domain): -->
From packages/db/scripts/backfill-free-zone-assignment.ts:
  pino({ ...getBaseLoggerOptions(), name }); dbUrl.replace(/:[^:@/]+@/, ':***@');
  const { createPrismaClientForUrl } = await import('../src/client.js');  // NOT legacy @prisma/client
  prisma.$transaction([...]); finally { await prisma.$disconnect(); }
  export function planFreeZoneBackfill(rows): Insert[]  // pure, idempotency guard "if (c.hasAssignment) continue;"
  // usage block (29-33): DATABASE_URL=$DATABASE_URL_{EU,ME,US} tsx ... [--dry-run]

<!-- planWorkerBackfill contract (from Plan-01 RED scaffold + RESEARCH Code Examples): -->
  export function planWorkerBackfill(rows: readonly ContractorForWorker[]): Array<{ contractorId: string; worker: WorkerInsert }>
  // skip "if (c.workerId) continue;"  -> idempotency guard (WHERE workerId IS NULL)

<!-- Per-region deploy (do NOT hand-roll a bash loop): -->
  pnpm --filter @contractor-ops/db db:migrate:all  // migrate-all-regions.ts iterates DATABASE_URL_{EU,ME,US}, fails fast, skips missing US

<!-- Audit row: -->
From packages/api/src/services/audit-writer.ts: writeAuditLog(...) — system-actor row for the one-time backfill
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: backfill-worker.ts — pure planWorkerBackfill + idempotent reversible per-region script</name>
  <read_first>
    - packages/db/scripts/backfill-free-zone-assignment.ts (full — copy the structure: pure export 85-124, --dry-run 127/165, masked pino 43/134, lazy createPrismaClientForUrl 138-141, $transaction, $disconnect)
    - packages/db/scripts/migrate-all-regions.ts (per-region invocation contract; skips-on-missing-US)
    - packages/db/src/__tests__/backfill-worker.test.ts (the Plan-01 RED scaffold this turns GREEN — its 4 assertions define the contract)
    - packages/api/src/services/audit-writer.ts (writeAuditLog signature for the system-actor row)
    - RESEARCH.md "Code Examples → Backfill plan transform" + "Pattern 2" + PATTERNS.md backfill-worker.ts section (the 4 DIFFERS: two-writes-per-contractor-in-one-tx, writeAuditLog, batching, rollback)
  </read_first>
  <behavior>
    - planWorkerBackfill(rows): for each contractor with workerId == null, emit { contractorId, worker: { organizationId, displayName, email } }; skip any contractor already carrying a workerId.
    - Re-run over already-linked output → zero new inserts (idempotent).
    - Source array never mutated.
  </behavior>
  <action>
    Create packages/db/scripts/backfill-worker.ts mirroring backfill-free-zone-assignment.ts. Export the pure `planWorkerBackfill(rows)` exactly per the Plan-01 scaffold contract (idempotency guard `if (c.workerId) continue;`). The runnable main: lazy-import createPrismaClientForUrl from ../src/client.js (modern generator, never legacy @prisma/client); pino with getBaseLoggerOptions + masked DB URL; `--dry-run` flag that logs the plan and performs zero writes. For the apply path, select `contractor.findMany({ where: { workerId: null }, select: { id, organizationId, displayName, email } })`, compute the plan, then for each insert do create Worker + contractor.update({ where:{id}, data:{ workerId } }) — the create and the link MUST be atomic in the same $transaction step. BATCH the creates (chunk ~1k per $transaction) so the largest org does not lock/timeout (free-zone used a single tx; chunk here). After the writes, emit ONE system-actor writeAuditLog row recording the one-time backfill (count, region) via audit-writer. $disconnect in finally. Add a `--rollback` branch (or sibling exported function) that nulls Contractor.workerId for rows this backfill linked and deletes the now-orphaned Worker rows — Contractor rows are NEVER touched destructively. Add a usage comment block mirroring free-zone (per-region DATABASE_URL invocation). No console.* (pino only); no breadcrumb IDs.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/db test -- backfill-worker` GREEN (Plan-01 scaffold now passes): single insert per unlinked contractor, linked rows skipped, re-run = zero inserts, source not mutated.
    - The script contains `--dry-run` (zero-write path), batched `$transaction` chunks, a `--rollback`/down path, and exactly one `writeAuditLog` call for the one-time backfill.
    - `grep -q "createPrismaClientForUrl" packages/db/scripts/backfill-worker.ts` (lazy modern client) and NO import of the legacy `@prisma/client` default entry.
    - `pnpm lint:logs` passes (no console.*) for the new script.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/db test -- backfill-worker && grep -q "createPrismaClientForUrl" packages/db/scripts/backfill-worker.ts && grep -q "dry-run" packages/db/scripts/backfill-worker.ts</automated>
  </verify>
  <done>planWorkerBackfill GREEN + idempotent; script has dry-run, batched atomic create+link, audit row, rollback; modern lazy client; no console.*.</done>
</task>

<task type="auto">
  <name>Task 2: Migration B (workerId NOT NULL + FK) authored to run LAST</name>
  <read_first>
    - packages/db/prisma/schema/contractor.prisma (the workerId String? @unique added in Plan 02 — to be promoted to required + FK)
    - packages/db/prisma/schema/migrations/__worker_base_additive/migration.sql (Plan-02 Migration A — B is its strict successor)
    - RESEARCH.md "Pattern 2" step 3 + "Pitfall 4" (NOT NULL/FK must run only after backfill verified, never in the same migration that adds the column)
  </read_first>
  <action>
    Author Migration B by hand under packages/db/prisma/schema/migrations/<timestamp>_worker_id_required/migration.sql: `ALTER TABLE "Contractor" ALTER COLUMN "workerId" SET NOT NULL;` and `ALTER TABLE "Contractor" ADD CONSTRAINT "Contractor_workerId_fkey" FOREIGN KEY ("workerId") REFERENCES "Worker"("id");`. Commit a paired down SQL (drop the FK constraint, drop NOT NULL). Update packages/db/prisma/schema/contractor.prisma to promote `workerId String @unique` (drop the `?`) and the relation to required — but DO NOT run/apply this migration in this task: it is gated behind the Task 3 [BLOCKING] checkpoint and must only run after the backfill + parity sign-off (otherwise existing null rows fail — Pitfall 4). Note in the SUMMARY that the schema edit + Migration B are authored-but-not-applied until the checkpoint resolves.
  </action>
  <acceptance_criteria>
    - Migration B SQL exists and contains both `SET NOT NULL` and `FOREIGN KEY ("workerId") REFERENCES "Worker"`; a paired down SQL exists.
    - The contractor.prisma workerId is promoted to required (`workerId String @unique`) in the schema source, with a SUMMARY note that application is deferred to the Task-3 checkpoint.
    - Migration B is NOT applied to any DB in this autonomous task (verified by it being the gated artifact, not run).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "SET NOT NULL" packages/db/prisma/schema/migrations/*_worker_id_required/migration.sql && grep -q 'REFERENCES "Worker"' packages/db/prisma/schema/migrations/*_worker_id_required/migration.sql</automated>
  </verify>
  <done>Migration B authored (NOT NULL + FK + down) and schema promoted to required; application deferred to the [BLOCKING] checkpoint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] Per-region backfill apply + staging-snapshot parity + Migration B enforcement (human gate)</name>
  <action>
    HUMAN GATE — Claude pauses here; this checkpoint mutates live regional Postgres (the milestone's highest-risk operation). The human provisions the largest-org staging snapshot, runs the dry-run / apply / parity / idempotency / reversibility checks in <how-to-verify>, and only after staging parity sign-off applies Migration A → backfill → Migration B per region. Migration B (NOT NULL + FK) runs LAST. Claude does not apply any migration or backfill to a live DB autonomously — it resumes only on the approval signal.
  </action>
  <what-built>
    A complete, idempotent, reversible per-region Worker backfill (backfill-worker.ts with --dry-run + --rollback + system-actor audit row) and an authored-but-unapplied Migration B (workerId NOT NULL + FK). All unit + parity baselines are GREEN. What remains is the live-data application, which mutates regional Postgres and is the milestone's highest-risk operation — it requires human confirmation per region.
  </what-built>
  <how-to-verify>
    1. PROVISION the largest-org staging snapshot (no code fallback — this is the gate's pass condition per RESEARCH Environment Availability). Restore the largest org's prod-shaped snapshot to staging.
    2. DRY-RUN: `DATABASE_URL=$STAGING_URL tsx packages/db/scripts/backfill-worker.ts --dry-run` — confirm the plan count equals the unlinked-contractor count and ZERO writes occur.
    3. APPLY on staging: `DATABASE_URL=$STAGING_URL tsx packages/db/scripts/backfill-worker.ts` — confirm one Worker per contractor, every Contractor.workerId populated, exactly one system-actor audit row, and Worker rows == Contractor rows in the org.
    4. PARITY: run `pnpm --filter @contractor-ops/api test -- contractor-parity` against staging + spot-check contractor lists / dashboard KPIs / a payment-run / a classification-scan / an export / the portal — they MUST match the pre-backfill baseline (D-03).
    5. RE-RUN safety: run the backfill again on staging — confirm zero new inserts (idempotent).
    6. REVERSIBILITY: run `--rollback` on staging — confirm pre-backfill state restored (workerId nulled, orphaned Worker rows dropped, Contractor rows intact). Then re-apply.
    7. Only after staging parity sign-off: apply Migration A then the backfill then Migration B PER REGION via `pnpm --filter @contractor-ops/db db:migrate:all` for the schema migrations and the per-region `DATABASE_URL=$DATABASE_URL_{EU,ME,US} tsx ... backfill-worker.ts` for the data backfill (EU, then ME, then US if DATABASE_URL_US set). Migration B (NOT NULL + FK) runs LAST, only after each region's backfill completes and parity holds.
  </how-to-verify>
  <verify>
    <human-check>Staging-snapshot contractor parity signed off; dry-run zero-write confirmed; idempotency + reversibility confirmed; per-region apply (Migration A → backfill → Migration B) completed or explicitly deferred per LOCAL-ONLY with a recorded post-merge item.</human-check>
  </verify>
  <done>Per-region backfill applied (or staging-only + deferred prod-region item) with contractor parity signed off; Migration B enforced LAST.</done>
  <resume-signal>Type "approved" once staging parity is signed off and the per-region apply (Migration A → backfill → Migration B) has completed (or "staging-only approved" if prod-region apply is deferred per LOCAL-ONLY posture — record the deferral as a post-merge item, do NOT hard-block).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backfill script → live regional Postgres (EU/ME/US) | One-time data mutation over every existing contractor row — highest-risk boundary |
| backfill script → audit log | System-actor row records the one-time operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-03-01 | Tampering / DoS-to-business | backfill over contractor rows | mitigate | Additive-only (create Worker + set workerId); Contractor rows never destroyed/relinked; idempotent (WHERE workerId IS NULL); reversible --rollback; dry-run; staging-snapshot parity before NOT NULL (D-02/D-03) |
| T-89-03-02 | Repudiation | one-time backfill | mitigate | Single system-actor writeAuditLog row per region records count + region (D-13) |
| T-89-03-03 | DoS | largest-org single mega-transaction | mitigate | Batched ~1k creates/tx to avoid lock/timeout (Pitfall, RESEARCH Open-Q #3) |
| T-89-03-04 | Tampering | premature NOT NULL/FK | mitigate | Migration B authored-but-unapplied until backfill + parity verified; never in the same migration that added the column (Pitfall 4) |
| T-89-03-05 | Information disclosure | duplicate Worker on re-run | mitigate | Contractor.workerId @unique index makes a double-link a DB-enforced conflict; WHERE workerId IS NULL guard (Pitfall 3) |
| T-89-03-SC | Tampering | npm installs | accept | No external packages installed (all in-tree) |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/db test -- backfill-worker` GREEN (idempotency proven on the pure transform).
- Migration B SQL contains SET NOT NULL + FK + down; schema promoted to required (authored-not-applied until checkpoint).
- [BLOCKING] checkpoint: staging-snapshot parity signed off; per-region apply (A → backfill → B) completed or deferred per LOCAL-ONLY with a recorded post-merge item.
- `pnpm lint:logs` + `pnpm lint:no-breadcrumbs` clean for the new script.
</verification>

<success_criteria>
- Idempotent, reversible, per-region, audit-logged Worker backfill exists and passes its unit + idempotency tests.
- Migration B (NOT NULL + FK) authored to run LAST, gated behind the [BLOCKING] checkpoint.
- Largest-org staging-snapshot contractor parity is the explicit pass condition before NOT NULL; contractor rows never lost or relinked.
</success_criteria>

<output>
Create `.planning/phases/89-theme-b-worker-model-abstraction-serial-gate/89-03-SUMMARY.md` when done
</output>
