---
phase: 69-de-i18n-parity-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/messages/de.json
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - FOUND-03

must_haves:
  truths:
    - "All 32 EN-only keys identified by GAP-67-01-01 are present in apps/web/messages/de.json — 25 Payments.lateInterest.* + 1 Payments.skonto.previewLineEn + 6 Admin.ClassificationEngineFlag.*"
    - "R-06 de-locale parity test (apps/web/src/i18n/__tests__/de-locale.test.ts:55-72 'every key present in en.json also exists in de.json') exits 0 with missing-key array empty"
    - "R-14 informal-register grep `grep -cE '\\b(Du|Dir|Dein[a-z]*)\\b' apps/web/messages/de.json` returns exactly 0"
    - "Locked-phrases guard `pnpm --filter @contractor-ops/validators exec vitest run locked-phrases-guard` continues to pass 78/78 (no drift)"
    - "Every ICU placeholder name in the 32 new translations is preserved verbatim from EN ({date}, {percent}, {discountDays}, {netDays}, {amount}, {name}, {revokeLink}, {count})"
    - "REQUIREMENTS.md FOUND-03 status flips from `Pending` to `Complete`; Traceability table updated to match"
    - "JSON.parse(apps/web/messages/de.json) does not throw"
    - "FOUND-03 traceability flip and 32-key insertion land as a single atomic commit `fix(69-01): add 32 missing DE translations + flip FOUND-03 to Complete`"
  artifacts:
    - path: "apps/web/messages/de.json"
      provides: "DE locale message catalog with full parity against en.json"
      contains: "Payments.lateInterest.{sectionHeading,explanationTooltip,b2cBanner,principalOutstanding,daysOverdue,rateUsed,dailyAccrual,interestAccrued,fixedCompensation,totalStatutoryClaim,claimCta,claimSecondaryOption,claimDialogTitle,claimDialogBody,downloadClaimPdf,claimedBanner,waiveCta,waiveDialogTitle,waiveReasonPlaceholder,waiveTypeInterestOnly,waiveTypeCompensationOnly,waiveTypeBoth,waiveConfirm,waivedBanner,revokeWaiverCta,revokeReasonPlaceholder} + Payments.skonto.previewLineEn + Admin.ClassificationEngineFlag.{title,subtitle,appSideValue,signoffRegistry,pendingGate,pendingGateResolution}"
    - path: ".planning/REQUIREMENTS.md"
      provides: "FOUND-03 traceability complete"
      contains: "| FOUND-03 |.*| Complete |"
  key_links:
    - from: "apps/web/messages/de.json"
      to: "apps/web/messages/en.json"
      via: "next-intl message-key parity"
      pattern: "every leaf key in en.json appears in de.json (R-06 assertion in de-locale.test.ts:61-72)"
    - from: "apps/web/messages/de.json#Payments.skonto.previewLineEn"
      to: "packages/validators/src/legal/de.ts:91-93 (XRECHNUNG_SKONTO_DESCRIPTION_TEMPLATE)"
      via: "D-07 alignment — runtime UI string matches CI-locked compile-time constant"
      pattern: "literal '{percent}% Skonto bei Zahlung innerhalb von {discountDays} Tagen, sonst netto {netDays} Tage'"
    - from: ".planning/REQUIREMENTS.md FOUND-03"
      to: ".planning/v5.0-MILESTONE-AUDIT.md FOUND-03"
      via: "audit closure"
      pattern: "FOUND-03 row status field flips Pending → Complete on R-06 green (D-11)"
---

<objective>
Author the 32 missing German translations in `apps/web/messages/de.json` so the Phase 56 R-06 de-locale parity test passes with zero missing keys, closing GAP-67-01-01 (the parity gap introduced by Phases 63 and 64), and flip REQUIREMENTS.md FOUND-03 from `Pending` to `Complete` in the same atomic commit.

Purpose: Phase 69 D-11 — the R-06 parity test currently fails with 32 missing keys. Authoring the translations using the locked D-03 / D-07 / D-08 term mappings makes the test green. The traceability flip is part of the same commit so the audit trail closes cleanly (matches the Phase 67 single-commit pattern for verified-and-flipped requirements).

Strict scope (CONTEXT.md domain): ONLY `apps/web/messages/de.json` is modified for the i18n change. NO UI / TSX / call-site changes. NO EN copy edits. NO PL/AR translations. NO new keys beyond the 32 EN-only ones. NO refactoring of existing DE entries (the 3 DE-only stubs `overdueInterestColumn`, `overdueFilterChip`, `emptyOverdue` in `Payments.lateInterest` stay untouched — the parity test is one-directional `enKeys ⊂ deKeys` so DE-only keys are harmless). The REQUIREMENTS.md edit is the ONE other file modification, scoped to the FOUND-03 row + Traceability table.

Output: Two file edits (de.json + REQUIREMENTS.md), one atomic commit, three green verification commands (R-06, R-14, locked-phrases-guard).
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/69-de-i18n-parity-fix/69-CONTEXT.md
@.planning/phases/69-de-i18n-parity-fix/69-RESEARCH.md
@.planning/phases/69-de-i18n-parity-fix/69-VALIDATION.md
</context>

<threat_model>
- THREAT (T-69-01): A translator accidentally introduces "Du" / "Dir" / "Dein" forms into one of the 32 new keys, breaking the formal-Sie invariant Phase 56 D-04 established → MITIGATION: Task 1 acceptance criteria runs `grep -cE '\b(Du|Dir|Dein[a-z]*)\b' apps/web/messages/de.json` and asserts the result is exactly `0`. The task action embeds D-04 verb-form rules verbatim ("Erlassen" / "Widerrufen" / infinitive — never "Du erlässt"). The `waiveReasonPlaceholder` value uses zero second-person pronouns by design ("Begründung (erforderlich, mind. 10 Zeichen) — z. B. mit Kunden ausgehandelter Vergleich, Kulanz, Verwaltungsfehler").
- THREAT (T-69-02): A locked DE legal phrase from `packages/validators/src/legal/de.ts` is accidentally copy/pasted into one of the runtime UI keys, creating two sources of truth for the same legal text → MITIGATION: locked-phrases-guard is a hard gate in Task 1 acceptance criteria (`78/78 must remain green`). The 32 keys in this phase are LPCDA-flavored UK statute copy + super-admin operations copy — by design they DO NOT overlap with the locked DE-tax-locked phrases (which cover Scheinselbständigkeit / DRV / SGB territory).
- THREAT (T-69-03): A translator translates an ICU placeholder name (e.g., `{percent}` → `{prozent}`) breaking call-site interpolation at runtime in Phase 63 LateInterestSection / WaiveDialog / ClaimDialog and Phase 64 ClassificationEngineFlagPanel → MITIGATION: D-06 locks placeholder names. Task 1 acceptance criteria explicitly greps each placeholder name (`{date}`, `{percent}`, `{discountDays}`, `{netDays}`, `{amount}`, `{name}`, `{revokeLink}`, `{count}`) and asserts presence in the right values. The Task action embeds the placeholder-preserving D-03 mappings verbatim — the executor copy/pastes from the plan, not from EN.
- THREAT (T-69-04): A stray comma, unescaped quote, or unmatched brace in the 32-key insertion breaks `JSON.parse(de.json)` at next-intl startup, rendering every DE locale page un-loadable → MITIGATION: R-06's first sub-test (`de-locale.test.ts:56-58`) does `expect(() => JSON.parse(raw)).not.toThrow()` — Task 1 verification re-runs `vitest run de-locale` AFTER the JSON edit and BEFORE the commit, so a JSON-validity break aborts the commit. Task 1 also runs `node -e "JSON.parse(require('fs').readFileSync('apps/web/messages/de.json','utf8'))"` as a fast-fail pre-check.
- THREAT (T-69-05): A `£{amount}` placeholder is reformatted to `{amount} GBP` or `EUR{amount}` in translation, drifting from EN structure and breaking the LPCDA-GBP semantic that D-05 locks → MITIGATION: D-05 keeps `£{amount}` verbatim (LPCDA is intrinsically GBP-denominated). Task 1 action embeds the literal `£{amount}` string in the `claimedBanner` translation; acceptance criteria greps for the literal substring `£{amount}` in the DE value.
- THREAT (T-69-06): The REQUIREMENTS.md flip happens but R-06 is still red, creating a false `Complete` status → MITIGATION: Task 2 sequence is strict — verification commands run FIRST, REQUIREMENTS.md edit happens SECOND, atomic commit is THIRD. If any verification command fails, the REQUIREMENTS.md edit and commit do NOT happen.
- THREAT (T-69-07): Steuerberater + Plain-ops review concludes (post-deploy) that some translations need rework, but the work has already shipped to (eventually) production → MITIGATION: Per CONTEXT.md D-10 + Standing Project Constraints (STATE.md), the app is LOCAL-ONLY and legal/regulatory verification is DEFERRED — recorded as a post-deploy review item in 69-SUMMARY.md "Manual-Only Verifications". Reworks (if any) are handled in a follow-on phase before any production rollout. Does NOT block this phase or create a STATE.md blocker.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author 32 DE translations + run all three re-validation gates</name>
  <files>
    apps/web/messages/de.json
  </files>
  <read_first>
    apps/web/messages/de.json,
    apps/web/messages/en.json,
    apps/web/src/i18n/__tests__/de-locale.test.ts,
    packages/validators/src/legal/de.ts,
    .planning/phases/69-de-i18n-parity-fix/69-CONTEXT.md,
    .planning/phases/69-de-i18n-parity-fix/69-RESEARCH.md
  </read_first>
  <action>
Open `apps/web/messages/de.json`. Make THREE in-place edits. Use the EXACT translation strings below (copy/paste from this plan — they encode D-03 / D-07 / D-08 verbatim). Preserve every ICU placeholder name verbatim from EN per D-06. Keep `£{amount}` verbatim per D-05. Use formal-Sie register only per D-04.

---

### EDIT 1 — Replace the entire `Payments.lateInterest` block

Locate the existing `Payments.lateInterest` block (currently `de.json:2994-2999`):

```json
    "lateInterest": {
      "sectionHeading": "Statutory late-payment interest",
      "overdueInterestColumn": "Verzugszinsen",
      "overdueFilterChip": "Überfällig",
      "emptyOverdue": "Keine überfälligen UK-B2B-Rechnungen"
    },
```

Replace it with the following 28-leaf-key block (25 EN-aligned keys in EN-source-order per the discretion note in CONTEXT.md, then the 3 DE-only stubs preserved at the end). Use the literal strings below verbatim:

```json
    "lateInterest": {
      "sectionHeading": "Gesetzliche Verzugszinsen (LPCDA)",
      "explanationTooltip": "Berechnet nach dem Late Payment of Commercial Debts (Interest) Act 1998 (LPCDA). Zinssatz = Bank of England Leitzins + 8 %, festgesetzt am letzten Tag des vorangegangenen 6-monatigen gesetzlichen Bezugszeitraums (30. Juni oder 31. Dezember).",
      "b2cBanner": "Gesetzliche Verzugszinsen nicht anwendbar (B2C-Transaktion).",
      "principalOutstanding": "Ausstehender Hauptbetrag",
      "daysOverdue": "Tage in Verzug",
      "rateUsed": "Verwendeter Zinssatz",
      "dailyAccrual": "Täglicher Zinsanlauf",
      "interestAccrued": "Aufgelaufene Zinsen",
      "fixedCompensation": "Pauschale Entschädigung",
      "totalStatutoryClaim": "Gesetzlicher Gesamtanspruch",
      "claimCta": "Gesetzliche Verzugszinsen geltend machen",
      "claimSecondaryOption": "Anspruch als Nebenrechnung ausstellen",
      "claimDialogTitle": "Gesetzliche Verzugszinsen geltend machen?",
      "claimDialogBody": "Damit werden die aktuellen Beträge für Zinsen und Entschädigung als Anspruchs-Snapshot festgehalten, ein PDF-Anspruchsschreiben erzeugt und (optional) eine Nebenrechnung über den Anspruchsbetrag ausgestellt. Der Snapshot ist unveränderlich — weitere Zinsen werden danach nicht mehr aufgeschlagen.",
      "downloadClaimPdf": "Anspruchsschreiben herunterladen",
      "claimedBanner": "Anspruchs-Snapshot erstellt am {date} — £{amount}. Auf diesen Anspruch fallen keine weiteren Zinsen an.",
      "waiveCta": "Zinsen erlassen",
      "waiveDialogTitle": "Gesetzliche Verzugszinsen erlassen?",
      "waiveReasonPlaceholder": "Begründung (erforderlich, mind. 10 Zeichen) — z. B. mit Kunden ausgehandelter Vergleich, Kulanz, Verwaltungsfehler",
      "waiveTypeInterestOnly": "Nur Zinsen",
      "waiveTypeCompensationOnly": "Nur Entschädigung",
      "waiveTypeBoth": "Beides",
      "waiveConfirm": "Zinsen erlassen",
      "waivedBanner": "Zinsen am {date} von {name} erlassen. {revokeLink}",
      "revokeWaiverCta": "Verzicht widerrufen",
      "revokeReasonPlaceholder": "Begründung für den Widerruf (erforderlich) — z. B. Kunde hat Vergleich abgelehnt",
      "overdueInterestColumn": "Verzugszinsen",
      "overdueFilterChip": "Überfällig",
      "emptyOverdue": "Keine überfälligen UK-B2B-Rechnungen"
    },
```

Key notes encoding the locked decisions:
- `sectionHeading` was previously the EN literal "Statutory late-payment interest"; D-03 OVERWRITES it with the formal-Sie translation "Gesetzliche Verzugszinsen (LPCDA)".
- `explanationTooltip` follows D-01 — full LPCDA name spelled out on first occurrence with `(LPCDA)` parenthetical, then the term is unambiguous for the rest of this surface.
- `claimedBanner` keeps the literal `£{amount}` per D-05 (LPCDA is intrinsically GBP).
- `waiveReasonPlaceholder` and `revokeReasonPlaceholder` use zero second-person pronouns to keep R-14 at 0 per D-04.
- `waivedBanner` keeps `{date}`, `{name}`, `{revokeLink}` placeholders verbatim per D-06.
- The 3 DE-only stubs (`overdueInterestColumn`, `overdueFilterChip`, `emptyOverdue`) are PRESERVED at the end of the block — strict-scope rule "no refactoring of existing DE entries" applies; deleting them risks runtime `MISSING_MESSAGE` errors at unaudited call sites.

---

### EDIT 2 — Insert `previewLineEn` into the `Payments.skonto` block

Locate the existing `Payments.skonto` block (`de.json:3010-3035`). Find the `netPeriodLabel` line:

```json
      "netPeriodLabel": "Netto-Frist (Tage)",
      "saveTerm": "Skonto-Bedingung speichern",
```

Insert the new key BETWEEN `netPeriodLabel` and `saveTerm` (matching EN's relative ordering at `en.json:2896-2898`):

```json
      "netPeriodLabel": "Netto-Frist (Tage)",
      "previewLineEn": "{percent}% Skonto bei Zahlung innerhalb von {discountDays} Tagen, sonst netto {netDays} Tage",
      "saveTerm": "Skonto-Bedingung speichern",
```

The translated value MUST be the literal string `"{percent}% Skonto bei Zahlung innerhalb von {discountDays} Tagen, sonst netto {netDays} Tage"` — this is byte-identical to `XRECHNUNG_SKONTO_DESCRIPTION_TEMPLATE` at `packages/validators/src/legal/de.ts:91-93` per CONTEXT.md D-07. Both sources of truth must remain aligned; if the locked-phrase template ever changes, this key needs to be re-aligned (call out in 69-SUMMARY).

---

### EDIT 3 — Insert the `Admin.ClassificationEngineFlag` block as a sibling of `Admin.BoeRate`

Locate the existing `Admin` block (`de.json:5540-5559`):

```json
  "Admin": {
    "BoeRate": {
      "pageTitle": "Bank of England Leitzinshistorie",
      ...
      "pollerFailure": "Letzter BoE-API-Abruf am {date} fehlgeschlagen. Manuelle Erfassung weiterhin möglich; der Abruf wird beim nächsten geplanten Lauf erneut versucht."
    }
  }
```

Find the closing `}` of the `BoeRate` object (line 5558) and append the new sibling block AFTER the closing brace of `BoeRate` and BEFORE the closing brace of `Admin`. The result must be:

```json
  "Admin": {
    "BoeRate": {
      "pageTitle": "Bank of England Leitzinshistorie",
      ...
      "pollerFailure": "Letzter BoE-API-Abruf am {date} fehlgeschlagen. Manuelle Erfassung weiterhin möglich; der Abruf wird beim nächsten geplanten Lauf erneut versucht."
    },
    "ClassificationEngineFlag": {
      "title": "Klassifizierungs-Engine Flag-Status",
      "subtitle": "Status des Kill-Switch `module.classification-engine` und Disclaimer-Signoff-Registry.",
      "appSideValue": "App-seitiger Wert (was Nutzer sehen)",
      "signoffRegistry": "Signoff-Registry",
      "pendingGate": "Flag in Unleash AKTIV, aber app-seitig blockiert — {count} Disclaimer offen (PENDING).",
      "pendingGateResolution": "Auflösen durch PR-Update von `packages/validators/src/legal/signoff-registry.json`"
    }
  }
```

Critical commas:
- The closing `}` of `BoeRate` (was followed by `}` of `Admin`) now needs a `,` after it (since a sibling follows).
- The new `ClassificationEngineFlag` block has NO trailing comma after its closing `}` (it's the last child of `Admin`).

The 6 keys' values use D-08 verbatim:
- Backticks around `module.classification-engine` and `packages/validators/src/legal/signoff-registry.json` are preserved verbatim per D-08 — they're file-path / module identifiers, not natural-language terms.
- `pendingGate` keeps the `{count}` placeholder verbatim per D-06.

---

### After all 3 edits — Pre-commit verification (run all in this order)

Run from repo root `/Users/mateusz.pitura/Repos/projects/contractor-ops`:

1. **Fast-fail JSON validity check** (catches stray commas / unmatched braces before vitest spin-up):
   ```
   node -e "JSON.parse(require('fs').readFileSync('apps/web/messages/de.json','utf8')); console.log('JSON_VALID=true')"
   ```
   Must print `JSON_VALID=true`. If it throws, revert the edit (`git checkout -- apps/web/messages/de.json`) and re-attempt.

2. **R-06 de-locale parity test** (the test this phase exists to satisfy):
   ```
   pnpm --filter @contractor-ops/web exec vitest run de-locale
   ```
   Must exit 0. The "every key present in en.json also exists in de.json" assertion (de-locale.test.ts:61-72) MUST report `missing: []`. The other R-06 sub-tests (`de in routing.locales`, `localeSettings de entry exists`) MUST also pass — they were passing before this phase and must continue to pass after.

3. **R-14 informal-register check** (D-04 invariant):
   ```
   grep -cE '\b(Du|Dir|Dein[a-z]*)\b' apps/web/messages/de.json
   ```
   Must print exactly `0`. If it prints anything else, find the offending key and replace the second-person form with formal-Sie or infinitive per D-04 examples in CONTEXT.md.

4. **Locked-phrases guard** (defense-in-depth — proves no accidental drift of the 78 locked DE legal phrases):
   ```
   pnpm --filter @contractor-ops/validators exec vitest run locked-phrases-guard
   ```
   Must exit 0 with all 78 guards green. None of the 32 new keys should overlap with locked phrases by design (LPCDA UK statute terms vs locked DE-tax-locked phrases occupy disjoint vocabulary domains).

5. **Placeholder presence spot-checks** (D-06 invariant — catch a translated placeholder name):
   ```
   grep -F '{date}' apps/web/messages/de.json | grep -F 'lateInterest' || echo 'MISSING_DATE_PLACEHOLDER'
   grep -F '{amount}' apps/web/messages/de.json | grep -F 'claimedBanner' || echo 'MISSING_AMOUNT_PLACEHOLDER'
   grep -F '{name}' apps/web/messages/de.json | grep -F 'waivedBanner' || echo 'MISSING_NAME_PLACEHOLDER'
   grep -F '{revokeLink}' apps/web/messages/de.json | grep -F 'waivedBanner' || echo 'MISSING_REVOKELINK_PLACEHOLDER'
   grep -F '{count}' apps/web/messages/de.json | grep -F 'pendingGate' || echo 'MISSING_COUNT_PLACEHOLDER'
   grep -F '{percent}' apps/web/messages/de.json | grep -F 'previewLineEn' || echo 'MISSING_PERCENT_PLACEHOLDER'
   grep -F '{discountDays}' apps/web/messages/de.json | grep -F 'previewLineEn' || echo 'MISSING_DISCOUNTDAYS_PLACEHOLDER'
   grep -F '{netDays}' apps/web/messages/de.json | grep -F 'previewLineEn' || echo 'MISSING_NETDAYS_PLACEHOLDER'
   ```
   None of the `MISSING_*` echoes may appear.

If ALL of the above pass, proceed to Task 2. If any FAILS, do NOT commit; fix the offending key and re-run from step 1.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && node -e "JSON.parse(require('fs').readFileSync('apps/web/messages/de.json','utf8'))" && pnpm --filter @contractor-ops/web exec vitest run de-locale && [ "$(grep -cE '\b(Du|Dir|Dein[a-z]*)\b' apps/web/messages/de.json)" = "0" ] && pnpm --filter @contractor-ops/validators exec vitest run locked-phrases-guard</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('apps/web/messages/de.json','utf8'))"` exits 0
    - `pnpm --filter @contractor-ops/web exec vitest run de-locale` exits 0
    - `grep -cE '\b(Du|Dir|Dein[a-z]*)\b' apps/web/messages/de.json` returns exactly `0`
    - `pnpm --filter @contractor-ops/validators exec vitest run locked-phrases-guard` exits 0
    - `grep -c '"sectionHeading": "Gesetzliche Verzugszinsen (LPCDA)"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"claimDialogTitle": "Gesetzliche Verzugszinsen geltend machen?"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"waiveDialogTitle": "Gesetzliche Verzugszinsen erlassen?"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"b2cBanner": "Gesetzliche Verzugszinsen nicht anwendbar (B2C-Transaktion)."' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"principalOutstanding": "Ausstehender Hauptbetrag"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"daysOverdue": "Tage in Verzug"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"rateUsed": "Verwendeter Zinssatz"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"dailyAccrual": "Täglicher Zinsanlauf"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"interestAccrued": "Aufgelaufene Zinsen"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"fixedCompensation": "Pauschale Entschädigung"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"totalStatutoryClaim": "Gesetzlicher Gesamtanspruch"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"claimCta": "Gesetzliche Verzugszinsen geltend machen"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"claimSecondaryOption": "Anspruch als Nebenrechnung ausstellen"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"downloadClaimPdf": "Anspruchsschreiben herunterladen"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"waiveCta": "Zinsen erlassen"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"waiveTypeInterestOnly": "Nur Zinsen"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"waiveTypeCompensationOnly": "Nur Entschädigung"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"waiveTypeBoth": "Beides"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"waiveConfirm": "Zinsen erlassen"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"revokeWaiverCta": "Verzicht widerrufen"' apps/web/messages/de.json` returns exactly `1`
    - `grep -F '£{amount}' apps/web/messages/de.json` outputs at least one line containing `claimedBanner`
    - `grep -c '"previewLineEn": "{percent}% Skonto bei Zahlung innerhalb von {discountDays} Tagen, sonst netto {netDays} Tage"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"title": "Klassifizierungs-Engine Flag-Status"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"appSideValue": "App-seitiger Wert (was Nutzer sehen)"' apps/web/messages/de.json` returns exactly `1`
    - `grep -c '"signoffRegistry": "Signoff-Registry"' apps/web/messages/de.json` returns exactly `1`
    - `grep -F '`module.classification-engine`' apps/web/messages/de.json` outputs at least one line (backticked identifier preserved per D-08)
    - `grep -F '`packages/validators/src/legal/signoff-registry.json`' apps/web/messages/de.json` outputs at least one line (backticked path preserved per D-08)
    - `grep -F '{count}' apps/web/messages/de.json` outputs at least one line containing `pendingGate`
  </acceptance_criteria>
  <done>The 32 EN-only keys are now present in `de.json` with formal-Sie LPCDA-aware translations per D-03 / D-07 / D-08; R-06 parity test green; R-14 register check at 0; locked-phrases-guard 78/78 still green; all ICU placeholders preserved verbatim. Ready for the FOUND-03 traceability flip + atomic commit in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Flip FOUND-03 to Complete in REQUIREMENTS.md + atomic commit</name>
  <files>
    .planning/REQUIREMENTS.md
    apps/web/messages/de.json
  </files>
  <read_first>
    .planning/REQUIREMENTS.md,
    .planning/phases/69-de-i18n-parity-fix/69-CONTEXT.md
  </read_first>
  <action>
PRECONDITION: Task 1 must have left the working tree with `apps/web/messages/de.json` MODIFIED but NOT YET COMMITTED, and ALL three verification commands GREEN. If any of those is not true, STOP and re-run Task 1.

---

### Step 1 — Edit `.planning/REQUIREMENTS.md` to flip FOUND-03

Open `.planning/REQUIREMENTS.md`. There are two places to edit (matching the Phase 67 single-commit pattern for verified-and-flipped requirements):

**(a) The FOUND-03 row in the requirements table.** Locate the row where the leftmost cell is `FOUND-03` and the status cell currently reads `Pending`. Change the status cell from `Pending` to `Complete`. Do NOT modify any other column on that row (description / phase / acceptance criteria all stay as-is). The row must remain a valid Markdown table row.

**(b) The Traceability table line for FOUND-03.** Locate the Traceability table (typically a separate section / table in REQUIREMENTS.md that maps requirement IDs to phase IDs / status). Find the row for `FOUND-03` and change its status field from `Pending` to `Complete`. If the Traceability table includes a `Verified by` or `Phase` column, set the value to `Phase 69 (R-06 green)`.

If the Traceability table does NOT include a status column for FOUND-03 (some REQUIREMENTS.md formats only flip the main requirements table), edit (b) is a no-op and only edit (a) lands.

**Sanity-check after the edits**: run
```
grep -E '^\| FOUND-03 \|.*\| Complete \|' .planning/REQUIREMENTS.md
```
If it returns at least one match, the flip is recorded correctly. If it returns 0 matches, the markdown-table cell layout is different from the regex assumption — open REQUIREMENTS.md and locate the FOUND-03 row visually, then edit the status cell to `Complete` so the regex matches.

---

### Step 2 — Re-run all three verification gates one final time (defense-in-depth before commit)

```
cd /Users/mateusz.pitura/Repos/projects/contractor-ops
node -e "JSON.parse(require('fs').readFileSync('apps/web/messages/de.json','utf8'))"
pnpm --filter @contractor-ops/web exec vitest run de-locale
[ "$(grep -cE '\b(Du|Dir|Dein[a-z]*)\b' apps/web/messages/de.json)" = "0" ] && echo R14_PASS=true || echo R14_FAIL
pnpm --filter @contractor-ops/validators exec vitest run locked-phrases-guard
```

All four must be green. If anything fails: revert the REQUIREMENTS.md edit (`git checkout -- .planning/REQUIREMENTS.md`), do NOT commit, return to Task 1.

---

### Step 3 — Stage exactly two files and create one atomic commit

```
git add apps/web/messages/de.json .planning/REQUIREMENTS.md
git status
```

`git status` MUST show exactly these two files staged and zero other files. If anything else is staged, unstage it: `git reset HEAD <unintended-file>`.

Then create the commit with this exact message via HEREDOC:

```
git commit -m "$(cat <<'EOF'
fix(69-01): add 32 missing DE translations + flip FOUND-03 to Complete

Closes GAP-67-01-01 (32-key DE-locale parity gap introduced by
Phases 63 and 64 against the Phase 56 invariant). Authors:

- 25 Payments.lateInterest.* keys — LPCDA late-interest dialog,
  claim PDF copy, waiver flow. Translations follow Phase 69 D-01
  (German with parenthetical English on first occurrence per UI
  surface) and D-03 (authoritative term mapping). Formal-Sie
  register throughout per D-04 (R-14 grep returns 0).
- 1 Payments.skonto.previewLineEn key — aligned with the locked
  XRECHNUNG_SKONTO_DESCRIPTION_TEMPLATE at packages/validators/
  src/legal/de.ts:91-93 per D-07 (single source of truth for the
  German Skonto preview phrase).
- 6 Admin.ClassificationEngineFlag.* keys — super-admin classification-
  engine flag-status panel from Phase 64. Backticked code identifiers
  (module.classification-engine, packages/validators/src/legal/
  signoff-registry.json) preserved verbatim per D-08.

Verification gates (all green pre-commit):
- R-06 de-locale parity test (apps/web/src/i18n/__tests__/
  de-locale.test.ts) — exits 0, missing-key array empty.
- R-14 informal-register grep (Du/Dir/Dein) — returns 0.
- Locked-phrases guard (packages/validators/.../locked-phrases-
  guard.test.ts) — 78/78 green (no drift).

Traceability:
- REQUIREMENTS.md FOUND-03 status flips Pending → Complete (D-11).

Strict scope: only apps/web/messages/de.json + .planning/
REQUIREMENTS.md modified. No UI / TSX / call-site changes,
no EN copy edits, no PL/AR translations, no new keys.

Post-deploy item (per Standing Project Constraints — STATE.md
LOCAL-ONLY, legal sign-off DEFERRED): the 25 LPCDA late-interest
translations need post-deploy review by commissioned Steuerberater
(LPCDA-vs-BGB framing per D-02) and Plain operations (customer-
experience review). Recorded in 69-SUMMARY.md "Manual-Only
Verifications". This does NOT block ship and does NOT create a
STATE.md blocker (D-10).

Refs: FOUND-03, GAP-67-01-01 (.planning/phases/56-country-
foundations-german-i18n/56-VERIFICATION.md)
EOF
)"
```

Do NOT use `--no-verify`. If a pre-commit hook fails, fix the underlying issue and create a NEW commit (do NOT amend).

---

### Step 4 — Post-commit sanity

```
git log -1 --pretty=format:'%s'
git log -1 --stat
```

Confirm:
- Commit subject starts with `fix(69-01):`.
- Diff includes exactly `apps/web/messages/de.json` and `.planning/REQUIREMENTS.md` (no other files).
- Commit body references D-11 and GAP-67-01-01.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && git log -1 --pretty=format:'%s' | grep -E '^fix\(69-01\):' && grep -E '^\| FOUND-03 \|.*\| Complete \|' .planning/REQUIREMENTS.md && pnpm --filter @contractor-ops/web exec vitest run de-locale</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit subject matches regex `^fix\(69-01\): add 32 missing DE translations \+ flip FOUND-03 to Complete`
    - `git log -1 --stat` shows exactly two files: `apps/web/messages/de.json` and `.planning/REQUIREMENTS.md` (no other files)
    - Commit body references `D-11`, `GAP-67-01-01`, `FOUND-03`, `D-01`, `D-03`, `D-04`, `D-07`, `D-08`
    - `grep -E '^\| FOUND-03 \|.*\| Complete \|' .planning/REQUIREMENTS.md` returns at least one match
    - `pnpm --filter @contractor-ops/web exec vitest run de-locale` exits 0 (R-06 green at HEAD)
    - `[ "$(grep -cE '\b(Du|Dir|Dein[a-z]*)\b' apps/web/messages/de.json)" = "0" ]` (R-14 still 0 at HEAD)
    - `pnpm --filter @contractor-ops/validators exec vitest run locked-phrases-guard` exits 0 (locked-phrases 78/78 still green at HEAD)
  </acceptance_criteria>
  <done>FOUND-03 is flipped to `Complete` in REQUIREMENTS.md (and the Traceability table); the 32-key insertion + traceability flip ship as one atomic commit `fix(69-01): ...`; all three re-validation gates remain green at HEAD. Phase 69 is functionally complete pending the post-deploy Steuerberater + Plain operations review noted in 69-SUMMARY.md "Manual-Only Verifications" (D-10 — non-blocking per Standing Project Constraints).</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web exec vitest run de-locale` → exit 0 (R-06 GREEN, missing-key array empty)
2. `grep -cE '\b(Du|Dir|Dein[a-z]*)\b' apps/web/messages/de.json` → exactly `0` (R-14 invariant)
3. `pnpm --filter @contractor-ops/validators exec vitest run locked-phrases-guard` → exit 0 with 78/78 reported (no locked-phrase drift)
4. `git log -1 --pretty=format:'%s'` → matches `^fix\(69-01\):`
5. `git log -1 --stat` → shows exactly `apps/web/messages/de.json` and `.planning/REQUIREMENTS.md`, nothing else
6. `grep -E '^\| FOUND-03 \|.*\| Complete \|' .planning/REQUIREMENTS.md` → at least one match
7. `node -e "const en=JSON.parse(require('fs').readFileSync('apps/web/messages/en.json','utf8')); const de=JSON.parse(require('fs').readFileSync('apps/web/messages/de.json','utf8')); function f(o,p=''){return o&&typeof o==='object'?Object.entries(o).flatMap(([k,v])=>f(v,p?p+'.'+k:k)):[p];} const ek=new Set(f(en)),dk=new Set(f(de)); const m=[...ek].filter(k=>!dk.has(k)); console.log('MISSING_COUNT='+m.length); if(m.length){console.log(JSON.stringify(m,null,2)); process.exit(1);}"` → prints `MISSING_COUNT=0` and exits 0 (full parity proof, defense-in-depth check independent of the vitest assertion)
8. `grep -c "ClassificationEngineFlag" apps/web/messages/de.json` → at least `1` (Phase 64 namespace landed)
9. `grep -c "previewLineEn" apps/web/messages/de.json` → exactly `1` (Phase 63 Skonto key landed)

Post-deploy review checkpoint (D-10 — recorded in 69-SUMMARY.md "Manual-Only Verifications", does NOT block this phase):
- LPCDA late-interest copy reviewed by commissioned Steuerberater (LPCDA-vs-BGB framing per D-02).
- LPCDA late-interest copy reviewed by Plain operations team (customer-facing tone per D-01 / D-04).
- If reviewers flag changes, file a follow-on phase before any production rollout. Per Standing Project Constraints (STATE.md, LOCAL-ONLY deploy), this is NOT a STATE.md blocker.
</verification>
