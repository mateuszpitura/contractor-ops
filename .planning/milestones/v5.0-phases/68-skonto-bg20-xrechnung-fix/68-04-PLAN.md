---
phase: 68-skonto-bg20-xrechnung-fix
plan: 04
type: execute
wave: 2
depends_on: [68-01]
files_modified:
  - packages/einvoice/src/profiles/zugferd-de/profile.ts
  - packages/einvoice/src/profiles/zugferd-de/generator.ts
  - packages/einvoice/src/profiles/zugferd-de/__tests__/generator.test.ts
  - packages/einvoice/src/profiles/zugferd-de/__fixtures__/comfort-skonto.json
autonomous: true
requirements:
  - EINV-02
  - EINV-04
  - PAY-04

must_haves:
  truths:
    - "ZugferdDEProfile.generate signature accepts an optional opts arg with leitwegId and skontoTerm fields (symmetric with XRechnungDEProfile.generate per CONTEXT D-05)"
    - "GenerateZugferdInput has a new skontoTerm field alongside existing leitwegId, conformanceLevel, producedAt"
    - "generateZugferdPdf threads input.skontoTerm into generateXRechnungCii(input.invoice, leitwegId, skontoTerm) at the existing line 82 call site"
    - "ZugferdDEProfile.generate forwards opts.skontoTerm into generateZugferdPdf via the GenerateZugferdInput shape"
    - "An end-to-end test in zugferd-de/__tests__/generator.test.ts generates a ZUGFeRD PDF with a Skonto term, extracts the embedded factur-x.xml using the existing extractCiiXml helper, and asserts the extracted CII contains the substrings #SKONTO#TAGE=, #PROZENT=, #BASISBETRAG="
    - "The same test asserts the no-Skonto path does NOT contain #SKONTO# in extracted CII (no-regression branch)"
    - "tsc --noEmit clean across packages/einvoice and packages/api after the change"
    - "Existing zugferd-de generator.test.ts cases (Phase 62) still pass"
  artifacts:
    - path: "packages/einvoice/src/profiles/zugferd-de/profile.ts"
      provides: "ZugferdDEProfile.generate(invoice, opts?) symmetric DE profile contract"
      contains: "skontoTerm"
    - path: "packages/einvoice/src/profiles/zugferd-de/generator.ts"
      provides: "GenerateZugferdInput + generateZugferdPdf with Skonto threading"
      contains: "skontoTerm"
    - path: "packages/einvoice/src/profiles/zugferd-de/__tests__/generator.test.ts"
      provides: "End-to-end embedded-CII BG-20 emission test"
      contains: "SKONTO"
    - path: "packages/einvoice/src/profiles/zugferd-de/__fixtures__/comfort-skonto.json"
      provides: "EInvoice fixture for Skonto-bearing ZUGFeRD generation"
      contains: "SKONTO"
  key_links:
    - from: "packages/einvoice/src/profiles/zugferd-de/generator.ts"
      to: "packages/einvoice/src/profiles/xrechnung-de/generator.ts"
      via: "calls generateXRechnungCii(invoice, leitwegId, skontoTerm)"
      pattern: "generateXRechnungCii"
    - from: "packages/einvoice/src/profiles/zugferd-de/profile.ts"
      to: "packages/einvoice/src/profiles/zugferd-de/generator.ts"
      via: "calls generateZugferdPdf({ invoice, ..., skontoTerm })"
      pattern: "generateZugferdPdf"
---

<objective>
Plumb the Skonto term through `ZugferdDEProfile.generate` and `generateZugferdPdf` so that ZUGFeRD PDF/A-3 documents emitted with a Skonto opt contain BG-20 Payment Terms in their embedded factur-x.xml.

Purpose: Phase 68 D-05. ZUGFeRD reuses `generateXRechnungCii` for the embedded CII (Phase 62 pattern). The current `generator.ts:82` call drops the third positional argument, and `ZugferdDEProfile.generate(invoice)` takes no opts — so even a caller who knows about Skonto cannot get it through to the embedded CII. This plan widens both surfaces symmetrically with `XRechnungDEProfile.generate` (D-05 rationale: symmetric DE profile contracts) and lands an end-to-end test that PROVES the BG-20 string lands in the extracted CII bytes (the deeper end of D-08 Layer C — Plan 05 covers the router boundary).

Output:
- `GenerateZugferdInput.skontoTerm?: SkontoTermInput | null` field added.
- `generator.ts:82` 3-arg call: `generateXRechnungCii(input.invoice, leitwegId, input.skontoTerm ?? null)`.
- `ZugferdDEProfile.generate(invoice, opts?: { leitwegId?, skontoTerm? })` forwards `opts.skontoTerm` into `generateZugferdPdf`.
- New `__fixtures__/comfort-skonto.json` (structural copy of `comfort-minimal.json`).
- New tests in `__tests__/generator.test.ts` extracting CII from a Skonto-PDF and asserting `#SKONTO#…` substrings.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/68-skonto-bg20-xrechnung-fix/68-CONTEXT.md
@.planning/phases/68-skonto-bg20-xrechnung-fix/68-RESEARCH.md
@.planning/phases/62-zugferd-e-invoicing/62-CONTEXT.md
</context>

<threat_model>
- THREAT: `ZugferdDEProfile.generate` is widened with the new `opts` param but the EInvoiceProfile shared interface still demands `(invoice): Promise<string>` -> MITIGATION: Plan 01 already widened the shared `EInvoiceProfile.generate` to `(invoice, opts?: unknown)`. This plan declares `depends_on: [68-01]` so wave-2 execution waits on wave-1.
- THREAT: A future maintainer reverts `input.skontoTerm` to "always null" in generator.ts:82 and tests still pass because no test extracts the CII from a generated PDF -> MITIGATION: this plan's end-to-end test EXTRACTS the embedded factur-x.xml from a real generated PDF via the existing `extractCiiXml` helper at `__tests__/generator.test.ts:28-57` and greps for the `#SKONTO#` extension. Removing the threading at line 82 fails the assertion deterministically.
- THREAT: PDF generation is slow (pdf-lib + react-pdf) and the new test increases CI runtime -> MITIGATION: the existing zugferd-de generator.test.ts already takes the PDF-generation hit (Phase 62 tests). Adding ONE more `generateZugferdPdf` invocation for the Skonto branch + reusing the comfort-minimal fixture for the no-Skonto branch is a small marginal cost. The new test reuses the existing `extractCiiXml` helper.
- THREAT: The new fixture (`comfort-skonto.json`) drifts in shape from `comfort-minimal.json` and breaks the parser -> MITIGATION: the new fixture is a structural copy of `comfort-minimal.json` (same EInvoice envelope shape) — only the `id` and `paymentReference` fields differ. The Skonto term itself is supplied at CALL TIME (in the test code), NOT in the fixture, mirroring the Plan 02 fixture pattern.
- THREAT: The opts type for `ZugferdDEProfile.generate` is declared inline (anonymous object type), drifting from the explicit `XRechnungGenerateOptions` symbol -> MITIGATION: the per-profile opts shape is small (2 fields) and intentionally inline — extracting `ZugferdGenerateOptions` for two fields used in one place is premature DRY. If a future phase adds a third option, promote then.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add skontoTerm? to GenerateZugferdInput and thread it into generateXRechnungCii at line 82</name>
  <files>
    packages/einvoice/src/profiles/zugferd-de/generator.ts
  </files>
  <read_first>
    packages/einvoice/src/profiles/zugferd-de/generator.ts,
    packages/einvoice/src/profiles/xrechnung-de/generator.ts,
    packages/einvoice/src/profiles/xrechnung-de/index.ts,
    .planning/phases/68-skonto-bg20-xrechnung-fix/68-CONTEXT.md
  </read_first>
  <action>
Open `packages/einvoice/src/profiles/zugferd-de/generator.ts`. Make two edits.

Edit 1 — extend the imports (around line 21) to add `SkontoTermInput` as a type-only import:

Locate the existing import:

    import { generateXRechnungCii } from '../xrechnung-de/generator.js';

Add immediately after it:

    import type { SkontoTermInput } from '../xrechnung-de/generator.js';

Edit 2 — add `skontoTerm?` to `GenerateZugferdInput` (currently lines 32-48) and thread it through at line 82.

Update the interface to:

    export interface GenerateZugferdInput {
      invoice: EInvoice;
      /** Defaults to 'COMFORT' — the only supported outbound level. */
      conformanceLevel?: ZugferdConformanceLevel;
      /**
       * Byte-stable timestamp stamped into XMP + /Info. Callers passing
       * fixtures / running deterministic tests should set this explicitly.
       * Defaults to `new Date()`.
       */
      producedAt?: Date;
      /**
       * Optional Leitweg-ID for XRechnung BT-10 BuyerReference embedding. Not
       * strictly required for ZUGFeRD COMFORT but preserved for parity with
       * the XRechnung generator. Defaults to `null` (no Leitweg-ID).
       */
      leitwegId?: string | null;
      /**
       * Optional Skonto (early payment discount) term. When provided, the
       * embedded CII XML emits a BG-20 <ram:SpecifiedTradePaymentTerms>
       * block with the locked German phrase + the structured
       * #SKONTO#TAGE=…#PROZENT=…#BASISBETRAG=…# extension per XRechnung
       * 3.0.2 Anhang E.
       *
       * Per Phase 68 D-05 — symmetric with XRechnungGenerateOptions.skontoTerm
       * from xrechnung-de/index.ts. Resolution policy lives at the caller
       * (api-side: routers/einvoice.ts:generateZugferdPdf after Plan 05),
       * not here.
       */
      skontoTerm?: SkontoTermInput | null;
    }

Update the call at line 82. Currently:

    log.debug('building CII XML');
    const xml = generateXRechnungCii(input.invoice, leitwegId);

Replace with:

    log.debug('building CII XML');
    // Phase 68 D-05 — thread input.skontoTerm into the CII helper so the
    // embedded factur-x.xml carries the BG-20 Payment Terms block. Defaults
    // to null when omitted (no-Skonto path matches Phase 62 behaviour).
    const xml = generateXRechnungCii(input.invoice, leitwegId, input.skontoTerm ?? null);

Do NOT touch the level-gate check (lines 73-76), the `producedAt` resolution (line 78), the visual-PDF rendering (line 85), the PDF/A-3 wrapping (line 88-93), or the structural sanity check (line 96).
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops &amp;&amp; grep -nE "skontoTerm\?: SkontoTermInput \| null;|generateXRechnungCii\(input\.invoice, leitwegId, input\.skontoTerm \?\? null\)" packages/einvoice/src/profiles/zugferd-de/generator.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/einvoice/src/profiles/zugferd-de/generator.ts contains the literal `skontoTerm?: SkontoTermInput | null;`
    - packages/einvoice/src/profiles/zugferd-de/generator.ts contains the literal `generateXRechnungCii(input.invoice, leitwegId, input.skontoTerm ?? null)`
    - packages/einvoice/src/profiles/zugferd-de/generator.ts does NOT contain the obsolete substring `generateXRechnungCii(input.invoice, leitwegId);`
    - packages/einvoice/src/profiles/zugferd-de/generator.ts contains the comment substring `Phase 68 D-05`
    - packages/einvoice/src/profiles/zugferd-de/generator.ts contains the import substring `import type { SkontoTermInput }`
    - `cd packages/einvoice && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The ZUGFeRD generator now threads the Skonto term into the embedded CII. Direct callers passing `{ skontoTerm }` to `generateZugferdPdf` get correct BG-20 emission inside the PDF/A-3.</done>
</task>

<task type="auto">
  <name>Task 2: Widen ZugferdDEProfile.generate to accept opts and forward into generateZugferdPdf</name>
  <files>
    packages/einvoice/src/profiles/zugferd-de/profile.ts
  </files>
  <read_first>
    packages/einvoice/src/profiles/zugferd-de/profile.ts,
    packages/einvoice/src/profiles/zugferd-de/generator.ts,
    packages/einvoice/src/profiles/xrechnung-de/index.ts,
    packages/einvoice/src/types/profile.ts,
    .planning/phases/68-skonto-bg20-xrechnung-fix/68-CONTEXT.md
  </read_first>
  <action>
Open `packages/einvoice/src/profiles/zugferd-de/profile.ts`. Make two edits.

Edit 1 — extend the imports (around line 20) to bring in `SkontoTermInput`:

Locate the existing import:

    import { generateZugferdPdf } from './generator.js';

Add immediately after it:

    import type { SkontoTermInput } from '../xrechnung-de/generator.js';

Edit 2 — widen `ZugferdDEProfile.generate(invoice)` to accept opts and forward them into `generateZugferdPdf`.

Locate the current method (lines 40-49):

    /**
     * Generate a ZUGFeRD hybrid PDF/A-3 B document from the canonical
     * `EInvoice` envelope. Returns a base64-encoded PDF string to satisfy
     * the `EInvoiceProfile.generate(): Promise<string>` contract — callers
     * that need the raw bytes should call `generateZugferdPdf` directly.
     */
    async generate(invoice: EInvoice): Promise<string> {
      const bytes = await generateZugferdPdf({ invoice });
      return Buffer.from(bytes).toString('base64');
    }

Replace with:

    /**
     * Generate a ZUGFeRD hybrid PDF/A-3 B document from the canonical
     * `EInvoice` envelope. Returns a base64-encoded PDF string to satisfy
     * the `EInvoiceProfile.generate(): Promise<string>` contract — callers
     * that need the raw bytes should call `generateZugferdPdf` directly.
     *
     * Per Phase 68 D-05 — symmetric with `XRechnungDEProfile.generate(invoice, opts)`
     * so cross-DE callers can stay format-agnostic. Forwards `opts.leitwegId`
     * and `opts.skontoTerm` into the underlying `generateZugferdPdf` (which
     * threads them into the embedded CII XML).
     */
    async generate(
      invoice: EInvoice,
      opts?: { leitwegId?: string | null; skontoTerm?: SkontoTermInput | null },
    ): Promise<string> {
      const bytes = await generateZugferdPdf({
        invoice,
        leitwegId: opts?.leitwegId ?? null,
        skontoTerm: opts?.skontoTerm ?? null,
      });
      return Buffer.from(bytes).toString('base64');
    }

Notes:
- The opts type is inline (anonymous object) per CONTEXT D-05 + RESEARCH Pitfall — extracting a `ZugferdGenerateOptions` type for one call site is premature DRY.
- The shared `EInvoiceProfile.generate(invoice, opts?: unknown)` signature (widened by Plan 01) accepts this narrower per-profile signature as a structural subtype.
- Do NOT touch `parse`, `validate`, `getComplianceStatus`, the `sign`/`qrCode` properties, or the `profileId`/`country`/`displayName` fields.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops &amp;&amp; grep -nE "skontoTerm: opts\?\.skontoTerm \?\? null|opts\?: \{ leitwegId\?: string \| null; skontoTerm\?: SkontoTermInput \| null \}" packages/einvoice/src/profiles/zugferd-de/profile.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/einvoice/src/profiles/zugferd-de/profile.ts contains the literal `skontoTerm: opts?.skontoTerm ?? null,`
    - packages/einvoice/src/profiles/zugferd-de/profile.ts contains the literal `leitwegId: opts?.leitwegId ?? null,`
    - packages/einvoice/src/profiles/zugferd-de/profile.ts contains the import substring `import type { SkontoTermInput }`
    - packages/einvoice/src/profiles/zugferd-de/profile.ts contains the comment substring `Phase 68 D-05`
    - packages/einvoice/src/profiles/zugferd-de/profile.ts does NOT contain the obsolete substring `await generateZugferdPdf({ invoice });`
    - `cd packages/einvoice && npx tsc --noEmit` exits 0
    - `cd packages/api && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The ZUGFeRD profile wrapper now forwards leitwegId and skontoTerm symmetric with XRechnungDEProfile. The shared EInvoiceProfile.generate(invoice, opts?: unknown) contract continues to type-check.</done>
</task>

<task type="auto">
  <name>Task 3: Create comfort-skonto.json fixture (structural copy of comfort-minimal.json)</name>
  <files>
    packages/einvoice/src/profiles/zugferd-de/__fixtures__/comfort-skonto.json
  </files>
  <read_first>
    packages/einvoice/src/profiles/zugferd-de/__fixtures__/comfort-minimal.json,
    packages/einvoice/src/types/invoice.ts,
    .planning/phases/68-skonto-bg20-xrechnung-fix/68-CONTEXT.md
  </read_first>
  <action>
Read `packages/einvoice/src/profiles/zugferd-de/__fixtures__/comfort-minimal.json` to confirm the exact EInvoice payload shape.

Create a new file `packages/einvoice/src/profiles/zugferd-de/__fixtures__/comfort-skonto.json` that is a STRUCTURAL COPY of `comfort-minimal.json`, differing ONLY in:
- `id` field: change to `"INV-2026-SKONTO"`
- `paymentReference` field inside `paymentMeans`: change to `"INV-2026-SKONTO"`

All other fields (issueDate, dueDate, currencyCode, supplier, customer, lines, taxBreakdown, taxExclusiveAmount, taxInclusiveAmount, payableAmount, paymentMeans.{code,dueDate,bankAccount,bankName}) must be IDENTICAL to comfort-minimal.json. The Skonto term itself is NOT in the fixture — it is supplied at CALL TIME by the test code via the second arg to `generateZugferdPdf({ invoice, skontoTerm })`.

Result example:

    {
      "id": "INV-2026-SKONTO",
      "issueDate": "2026-01-15",
      "dueDate": "2026-02-14",
      "invoiceTypeCode": "380",
      "currencyCode": "EUR",
      "profileId": "zugferd-de",
      "supplier": { ...same as comfort-minimal... },
      "customer": { ...same as comfort-minimal... },
      "lines": [ ...same as comfort-minimal... ],
      "taxExclusiveAmount": 120000,
      "taxInclusiveAmount": 142800,
      "payableAmount": 142800,
      "taxBreakdown": [ ...same as comfort-minimal... ],
      "paymentMeans": {
        "code": "58",
        "dueDate": "2026-02-14",
        "bankAccount": "DE89370400440532013000",
        "bankName": "Commerzbank",
        "paymentReference": "INV-2026-SKONTO"
      }
    }

Verify the file parses as valid JSON. The structural identity to comfort-minimal lets the no-Skonto branch reuse comfort-minimal directly (no third fixture needed).
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops &amp;&amp; node -e "const a=JSON.parse(require('fs').readFileSync('packages/einvoice/src/profiles/zugferd-de/__fixtures__/comfort-skonto.json','utf8')); const b=JSON.parse(require('fs').readFileSync('packages/einvoice/src/profiles/zugferd-de/__fixtures__/comfort-minimal.json','utf8')); if (a.id !== 'INV-2026-SKONTO') process.exit(1); if (a.payableAmount !== b.payableAmount) process.exit(2); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/einvoice/src/profiles/zugferd-de/__fixtures__/comfort-skonto.json` exists
    - File parses as valid JSON
    - File top-level `id` field equals `"INV-2026-SKONTO"`
    - File `paymentMeans.paymentReference` field equals `"INV-2026-SKONTO"`
    - File `payableAmount` equals comfort-minimal.json's `payableAmount` (both 142800)
    - File does NOT contain a top-level `"skontoTerm"` key (Skonto opts come from the test code, NOT from the fixture)
  </acceptance_criteria>
  <done>The Skonto-bearing ZUGFeRD fixture is structurally identical to comfort-minimal so any KoSIT failure in the test is provably attributable to the Skonto path, not envelope drift.</done>
</task>

<task type="auto">
  <name>Task 4: Extend zugferd-de/__tests__/generator.test.ts with end-to-end embedded-CII BG-20 assertions</name>
  <files>
    packages/einvoice/src/profiles/zugferd-de/__tests__/generator.test.ts
  </files>
  <read_first>
    packages/einvoice/src/profiles/zugferd-de/__tests__/generator.test.ts,
    packages/einvoice/src/profiles/zugferd-de/generator.ts,
    packages/einvoice/src/profiles/zugferd-de/__fixtures__/comfort-skonto.json,
    packages/einvoice/src/profiles/zugferd-de/__fixtures__/comfort-minimal.json,
    .planning/phases/68-skonto-bg20-xrechnung-fix/68-CONTEXT.md,
    .planning/phases/68-skonto-bg20-xrechnung-fix/68-RESEARCH.md
  </read_first>
  <action>
Open `packages/einvoice/src/profiles/zugferd-de/__tests__/generator.test.ts`. The file already has:
- `loadFixture(name)` helper at line 23 that reads `../__fixtures__/${name}.json`
- `extractCiiXml(pdf)` helper at line 28 that decodes the embedded factur-x.xml from a generated PDF/A-3
- A `describe('generateZugferdPdf', () => { ... })` block starting at line 59 with `beforeAll` loading `comfortMinimal`, `reverseCharge`, `klein` fixtures

Make two edits.

Edit 1 — extend the `beforeAll` block to also load the new comfort-skonto fixture. Locate the block (around lines 64-68):

    beforeAll(async () => {
      comfortMinimal = await loadFixture('comfort-minimal');
      reverseCharge = await loadFixture('reverse-charge-leitweg');
      klein = await loadFixture('kleinunternehmer');
    });

Add a new `let` variable at the top of the describe block (alongside `comfortMinimal`, `reverseCharge`, `klein` declarations around lines 60-62):

    let comfortSkonto: EInvoice;

And add the load call to `beforeAll`:

    beforeAll(async () => {
      comfortMinimal = await loadFixture('comfort-minimal');
      reverseCharge = await loadFixture('reverse-charge-leitweg');
      klein = await loadFixture('kleinunternehmer');
      comfortSkonto = await loadFixture('comfort-skonto');
    });

Edit 2 — append two new `it()` blocks at the END of the existing `describe('generateZugferdPdf')` block (just before its closing `});`):

    // -----------------------------------------------------------------------
    // Phase 68 · Plan 04 — Skonto BG-20 in embedded CII (end-to-end)
    //
    // Closes the embedded-CII half of the v5.0 audit I-1 finding for the
    // ZUGFeRD path. The router half (call-shape assertion against the
    // mocked generator) is locked by Plan 05's
    // packages/api/src/routers/__tests__/einvoice.generate-zugferd.test.ts
    // extension. Together the two halves prove the Skonto term reaches the
    // embedded factur-x.xml when a Skonto-bearing DE invoice is finalized
    // through the ZUGFeRD path.
    // -----------------------------------------------------------------------

    it('Skonto path: extracted CII contains structured BG-20 #SKONTO# extension (Phase 68 D-05/D-08 Layer C)', async () => {
      const out = await generateZugferdPdf({
        invoice: comfortSkonto,
        producedAt: FIXED_TIME,
        skontoTerm: {
          discountPercent: 3,
          discountPeriodDays: 7,
          netPeriodDays: 30,
        },
      });
      const cii = await extractCiiXml(out);
      expect(cii).toContain('<ram:SpecifiedTradePaymentTerms>');
      expect(cii).toContain('#SKONTO#TAGE=7');
      expect(cii).toContain('#PROZENT=3.00');
      expect(cii).toContain('#BASISBETRAG=');
    });

    it('No-Skonto path: extracted CII does NOT contain #SKONTO# (no-regression on Phase 62 behaviour)', async () => {
      const out = await generateZugferdPdf({
        invoice: comfortMinimal,
        producedAt: FIXED_TIME,
      });
      const cii = await extractCiiXml(out);
      expect(cii).not.toContain('#SKONTO#');
    });

Notes:
- `FIXED_TIME` is already defined at line 21 of the test file.
- `extractCiiXml` is the existing helper at line 28; do NOT redefine it.
- The Skonto term values (3% / 7 days / 30 days) match Plan 02's profile.test.ts so the assertions read consistently across both DE profiles.
- Run the file: `pnpm --filter @contractor-ops/einvoice test -- zugferd-de/__tests__/generator.test.ts`. ALL tests must pass — both the new tests AND the Phase 62 tests already in the file.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops &amp;&amp; pnpm --filter @contractor-ops/einvoice test -- zugferd-de/__tests__/generator.test.ts 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - packages/einvoice/src/profiles/zugferd-de/__tests__/generator.test.ts contains the literal `comfortSkonto = await loadFixture('comfort-skonto');`
    - The file contains an `it(` block whose name includes the substring `Skonto path: extracted CII contains structured BG-20`
    - The file contains the literal `expect(cii).toContain('#SKONTO#TAGE=7')`
    - The file contains the literal `expect(cii).toContain('#PROZENT=3.00')`
    - The file contains the literal `expect(cii).toContain('#BASISBETRAG=')`
    - The file contains an `it(` block whose name includes the substring `No-Skonto path: extracted CII does NOT contain`
    - The file contains the literal `expect(cii).not.toContain('#SKONTO#')`
    - `pnpm --filter @contractor-ops/einvoice test -- zugferd-de/__tests__/generator.test.ts` exits 0 (all tests pass — including pre-existing Phase 62 cases and the 2 new Skonto cases)
  </acceptance_criteria>
  <done>End-to-end ZUGFeRD path is locked: a real PDF is generated, the embedded factur-x.xml is extracted, and the BG-20 #SKONTO# extension is asserted present. The audit I-1 ZUGFeRD-cascading finding is now provable at the embedded-CII byte level.</done>
</task>

<task type="auto">
  <name>Task 5: Atomic commit fix(68-04): wire Skonto through ZugferdDEProfile and generateZugferdPdf</name>
  <files>
    packages/einvoice/src/profiles/zugferd-de/profile.ts,
    packages/einvoice/src/profiles/zugferd-de/generator.ts,
    packages/einvoice/src/profiles/zugferd-de/__tests__/generator.test.ts,
    packages/einvoice/src/profiles/zugferd-de/__fixtures__/comfort-skonto.json
  </files>
  <read_first>
    .planning/phases/68-skonto-bg20-xrechnung-fix/68-CONTEXT.md
  </read_first>
  <action>
1. Confirm Tasks 1-4 staged: the four files above.
2. Confirm `cd packages/einvoice && npx tsc --noEmit` exits 0.
3. Confirm `cd packages/api && npx tsc --noEmit` exits 0 (api consumes ZugferdDEProfile via the public einvoice surface).
4. Confirm `pnpm --filter @contractor-ops/einvoice test` exits 0 (full einvoice suite — no regression on parser, validator, structural-check, xmp-template, pdf-wrapper, invoice-template).
5. Create one atomic commit:

    fix(68-04): wire Skonto through ZugferdDEProfile + generateZugferdPdf

    Adds skontoTerm?: SkontoTermInput | null to GenerateZugferdInput and
    threads it as the third positional arg to generateXRechnungCii at
    zugferd-de/generator.ts:82 (which builds the embedded factur-x.xml).

    Widens ZugferdDEProfile.generate to accept opts?: { leitwegId?,
    skontoTerm? } symmetric with XRechnungDEProfile.generate (CONTEXT
    D-05). The shared EInvoiceProfile.generate(invoice, opts?: unknown)
    contract from Plan 01 covers the per-profile narrowing.

    End-to-end regression test (zugferd-de/__tests__/generator.test.ts,
    two new cases) generates a real ZUGFeRD PDF/A-3 with a Skonto term,
    extracts the embedded factur-x.xml via the existing extractCiiXml
    helper, and asserts:
      - #SKONTO#TAGE=7 + #PROZENT=3.00 + #BASISBETRAG= present in the
        extracted CII (closes the embedded-CII half of audit I-1 for
        the ZUGFeRD path)
      - no #SKONTO# substring in the no-Skonto branch (no-regression
        on Phase 62 behaviour)

    Refs: Phase 68 D-05, D-08 (Layer C deeper end); v5.0 audit I-1.

Do NOT use `--no-verify`. If a pre-commit hook fails, fix and create a NEW commit.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops &amp;&amp; git log -1 --pretty=format:'%s' | grep -E '^fix\(68-04\)'</automated>
  </verify>
  <acceptance_criteria>
    - HEAD commit subject matches regex `^fix\(68-04\):`
    - `git log -1 --stat` shows exactly 4 files: zugferd-de/profile.ts, zugferd-de/generator.ts, zugferd-de/__tests__/generator.test.ts, zugferd-de/__fixtures__/comfort-skonto.json
    - Commit body references Phase 68 D-05, D-08, audit I-1
  </acceptance_criteria>
  <done>The ZUGFeRD profile + generator wiring fix lands atomically. The embedded-CII BG-20 emission is provable via byte-level test. Plan 05 can now wire the cascade in the router boundary.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `cd packages/einvoice && npx tsc --noEmit` -> exit 0
2. `cd packages/api && npx tsc --noEmit` -> exit 0
3. `pnpm --filter @contractor-ops/einvoice test` -> exit 0 (full suite green)
4. `git log -1 --pretty=format:'%s'` -> matches `^fix\(68-04\):`
5. `grep -c "skontoTerm" packages/einvoice/src/profiles/zugferd-de/generator.ts` -> ≥ 2 (interface field + threading at line 82)
6. `grep -c "skontoTerm" packages/einvoice/src/profiles/zugferd-de/profile.ts` -> ≥ 2 (opts type + forwarding)
7. `grep -c "comfortSkonto" packages/einvoice/src/profiles/zugferd-de/__tests__/generator.test.ts` -> ≥ 2 (let declaration + load + use)
8. `test -f packages/einvoice/src/profiles/zugferd-de/__fixtures__/comfort-skonto.json && echo OK` -> prints OK
</verification>
