---
phase: 60
plan: 02
type: execute
wave: 1
depends_on: [60-01]
files_modified:
  - packages/db/prisma/schema/classification.prisma
  - packages/db/prisma/schema/contractor.prisma
  - packages/db/prisma/schema/organization.prisma
  - packages/db/prisma/schema/auth.prisma
  - packages/db/prisma/schema/audit.prisma
  - packages/validators/src/notification.ts
  - packages/api/src/services/cron-monitor.ts
  - packages/api/src/schemas/reassessment-trigger-reason.ts
  - packages/api/src/schemas/__tests__/reassessment-trigger-reason.test.ts
  - packages/api/src/services/audit-writer.ts
  - packages/api/src/services/__tests__/audit-writer.test.ts
  - packages/api/src/routers/contractor.ts
  - packages/api/src/routers/contract.ts
  - packages/api/src/routers/__tests__/contractor.test.ts
  - packages/api/src/routers/__tests__/contract.test.ts
  - packages/api/src/services/reassessment-trigger-scan.ts
  - packages/api/src/services/__tests__/reassessment-trigger-scan.test.ts
  - packages/api/src/routers/reassessment-trigger.ts
  - packages/api/src/routers/__tests__/reassessment-trigger.test.ts
  - packages/api/src/routers/classification.ts
  - packages/api/src/routers/__tests__/classification.test.ts
  - packages/api/src/root.ts
  - apps/web/src/app/api/cron/classification-reassessment-triggers/route.ts
  - apps/web/src/app/api/cron/classification-reassessment-triggers/__tests__/route.test.ts
  - apps/web/messages/en.json
  - apps/web/messages/de.json
  - apps/web/messages/pl.json
  - apps/web/messages/ar.json
  - apps/web/src/components/contractors/classification/reassessment-trigger/trigger-chip.tsx
  - apps/web/src/components/contractors/classification/reassessment-trigger/trigger-cta.tsx
  - apps/web/src/components/contractors/classification/reassessment-trigger/dismiss-dialog.tsx
  - apps/web/src/components/contractors/classification/reassessment-trigger/__tests__/trigger-chip.test.tsx
  - apps/web/src/components/contractors/classification/reassessment-trigger/__tests__/dismiss-dialog.test.tsx
autonomous: true
requirements: [CLASS-08]
requirements_addressed: [CLASS-08]
tags: [classification, reassessment, cron, audit-log, ir35, uk, triggers]

must_haves:
  truths:
    - "Prisma model ReassessmentTrigger + enum ReassessmentTriggerStatus (OPEN|ACKNOWLEDGED|RESOLVED|DISMISSED) exist per D-08 with back-relations on Organization, ContractorAssignment, ClassificationAssessment (priorAssessment), ClassificationDocument (priorSdsDocument), User (acknowledgedByUser, dismissedByUser)."
    - "ContractorAssignment and Contract mutations in packages/api/src/routers/contractor.ts and packages/api/src/routers/contract.ts write AuditLog rows on create/update/delete via shared audit-writer service (resolves Open Question #1 — audit writes did NOT exist before this plan)."
    - "Daily cron /api/cron/classification-reassessment-triggers rejects requests without Bearer CRON_SECRET (401); authorised calls run runReassessmentTriggerScan inside withCronMonitor + Sentry."
    - "Reassessment scan filters: resourceType IN {CONTRACTOR, CONTRACT} on GB engagements with a completed IR35 ClassificationAssessment; material-change allowlist enforced (D-07)."
    - "CronScanState table persists lastScanCompletedAt per scan name; initial deploy seeded to deploy time so historical audits are NOT replayed."
    - "Dedup: multiple audit rows for (contractorAssignmentId, priorAssessmentId) within one scan APPEND to existing OPEN/ACKNOWLEDGED trigger's triggerReasons array (no new row)."
    - "Submitting a new IR35 ClassificationAssessment on an engagement auto-sets matching OPEN/ACKNOWLEDGED trigger.status → RESOLVED via classification.submit extension."
    - "triggerReasons JSONB is Zod-parsed on read AND write via triggerReasonsSchema."
    - "tRPC router reassessmentTrigger with list/listByEngagement (contractor:read) + acknowledge/dismiss (contractor:update); dismiss requires dismissedReason length ≥10."
    - "CSRF/RBAC test: cross-org ReassessmentTrigger dismiss is rejected."
    - "Notification type classification.reassessment_trigger registered in NOTIFICATION_TYPES."
  artifacts:
    - path: packages/db/prisma/schema/classification.prisma
      provides: "ReassessmentTrigger model + ReassessmentTriggerStatus enum per D-08"
      contains: "model ReassessmentTrigger"
    - path: packages/db/prisma/schema/classification.prisma
      provides: "CronScanState singleton model { name String @id, lastScanCompletedAt DateTime, updatedAt DateTime @updatedAt }"
      contains: "model CronScanState"
    - path: packages/api/src/schemas/reassessment-trigger-reason.ts
      provides: "triggerReasonSchema + triggerReasonsSchema Zod exports for JSONB parsing"
      contains: "triggerReasonsSchema"
    - path: packages/api/src/services/audit-writer.ts
      provides: "writeAuditLog({ organizationId, actorType, action, resourceType, resourceId, oldValues, newValues }) helper used by contractor + contract routers"
      contains: "export async function writeAuditLog"
    - path: packages/api/src/services/reassessment-trigger-scan.ts
      provides: "runReassessmentTriggerScan() orchestrator + isMaterialChange allowlist + dedupAndCreateTrigger"
      contains: "runReassessmentTriggerScan"
    - path: packages/api/src/routers/reassessment-trigger.ts
      provides: "tRPC router: list, listByEngagement (contractor:read), acknowledge, dismiss (contractor:update)"
      contains: "contractorUpdateProcedure"
    - path: apps/web/src/app/api/cron/classification-reassessment-triggers/route.ts
      provides: "Daily cron shell cloned from reminders — Bearer CRON_SECRET + withCronMonitor + Sentry"
      contains: "classification-reassessment-triggers"
    - path: apps/web/src/components/contractors/classification/reassessment-trigger/trigger-chip.tsx
      provides: "ReassessmentTriggerChip — shadcn Badge (warning variant) + RefreshCcw icon + label per UI-SPEC"
      contains: "Reassessment recommended"
  key_links:
    - from: packages/api/src/routers/contractor.ts
      to: packages/api/src/services/audit-writer.ts
      via: "writeAuditLog({ resourceType: 'CONTRACTOR', resourceId: assignmentId }) on assignment create/update/delete"
      pattern: "writeAuditLog"
    - from: packages/api/src/routers/contract.ts
      to: packages/api/src/services/audit-writer.ts
      via: "writeAuditLog({ resourceType: 'CONTRACT', resourceId: contractId }) on contract create/update"
      pattern: "writeAuditLog"
    - from: packages/api/src/services/reassessment-trigger-scan.ts
      to: packages/db/src/raw.ts
      via: "prismaRaw.auditLog.findMany since lastScanCompletedAt"
      pattern: "prismaRaw.auditLog"
    - from: packages/api/src/routers/classification.ts
      to: packages/api/src/services/reassessment-trigger-scan.ts (auto-resolve helper)
      via: "on submit: set matching OPEN/ACKNOWLEDGED ReassessmentTrigger.status = RESOLVED, resolvedAt = now"
      pattern: "status: 'RESOLVED'"
    - from: apps/web/src/app/api/cron/classification-reassessment-triggers/route.ts
      to: packages/api/src/services/reassessment-trigger-scan.ts
      via: "runReassessmentTriggerScan() call"
      pattern: "runReassessmentTriggerScan"

user_setup:
  - service: cron-scheduler
    why: "Register classification-reassessment-triggers daily cron"
    dashboard_config:
      - task: "Add 0 3 * * * UTC schedule hitting GET /api/cron/classification-reassessment-triggers with Bearer CRON_SECRET header"
        location: "Render/Railway cron panel"
---

<objective>
Deliver CLASS-08 reassessment triggers end-to-end: resolve Open Question #1 by wiring audit-log writes on ContractorAssignment + Contract mutations (prerequisite), add ReassessmentTrigger + CronScanState Prisma models, implement daily AuditLog scan service with material-change allowlist + dedup, daily cron route, tRPC router (acknowledge/dismiss), extend classification.submit to auto-resolve matching triggers, and ship UI chip + CTA + dismiss dialog per UI-SPEC.

Purpose: Automated detection of material changes (rate, scope, extension) on UK IR35 engagements that should trigger a reassessment of the Status Determination Statement.

Output: 2 new Prisma models, 1 new cron route, 1 new tRPC router, 1 new audit-writer helper service, 1 new reassessment-scan service, 1 new Zod schema module, 3 new React components, extensions to contractor/contract/classification routers, i18n strings in 4 locales.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-classification-polish/60-CONTEXT.md
@.planning/phases/60-classification-polish/60-RESEARCH.md
@.planning/phases/60-classification-polish/60-UI-SPEC.md
@.planning/phases/60-classification-polish/60-VALIDATION.md
@.planning/phases/60-classification-polish/60-01-PLAN.md
@.planning/STATE.md

# Upstream for IR35 assessment shape
@.planning/phases/58-classification-engine-rule-sets/58-CONTEXT.md
@.planning/phases/59-classification-documents-chain-tracking/59-CONTEXT.md

# Reference implementations
@packages/db/prisma/schema/audit.prisma
@packages/db/prisma/schema/classification.prisma
@packages/db/prisma/schema/contract.prisma
@packages/db/prisma/schema/contractor.prisma
@packages/api/src/routers/classification.ts
@packages/api/src/routers/contractor.ts
@packages/api/src/routers/contract.ts
@apps/web/src/app/api/cron/reminders/route.ts
@packages/api/src/services/notification-service/index.ts

<interfaces>
From packages/db/prisma/schema/audit.prisma:
```prisma
model AuditLog {
  id             String
  organizationId String
  actorType      ActorType
  action         String
  resourceType   EntityType   // includes CONTRACTOR + CONTRACT, no CONTRACTOR_ASSIGNMENT
  resourceId     String
  oldValuesJson  Json?
  newValuesJson  Json?
  createdAt      DateTime @default(now())
  // audit logs are immutable
}
```

From packages/db/prisma/schema/contract.prisma (EntityType enum — VERIFIED:251-268):
```prisma
enum EntityType {
  ORGANIZATION
  CONTRACTOR       // Phase 60 reuses this for ContractorAssignment audit rows
  CONTRACT
  DOCUMENT
  INVOICE
  // ... 11 more — NO CONTRACTOR_ASSIGNMENT
}
```

From .planning/phases/60-classification-polish/60-RESEARCH.md Example 2 (ReassessmentTrigger schema):
```prisma
enum ReassessmentTriggerStatus { OPEN ACKNOWLEDGED RESOLVED DISMISSED }
model ReassessmentTrigger {
  id String @id @default(cuid())
  organizationId String
  contractorAssignmentId String
  priorAssessmentId String
  priorSdsDocumentId String?
  triggeredAt DateTime @default(now())
  triggerReasons Json   // Zod-parsed: Array<{field, oldValue?, newValue?, auditLogId, resourceType}>
  status ReassessmentTriggerStatus @default(OPEN)
  acknowledgedByUserId String?
  acknowledgedAt DateTime?
  resolvedAt DateTime?
  dismissedByUserId String?
  dismissedAt DateTime?
  dismissedReason String? @db.VarChar(1000)
  // back-relations
  @@index([organizationId, contractorAssignmentId, status], map: "RT_org_assign_status_idx")
  @@index([organizationId, triggeredAt], map: "RT_org_triggeredAt_idx")
}
```

From packages/db/src/raw.ts (created in Plan 60-01):
```ts
export const prismaRaw: PrismaClient;  // non-tenant-scoped; cron uses this
```

From packages/validators/src/notification.ts (EXTEND — preserve Plan 60-01 additions):
```ts
export const NOTIFICATION_TYPES = [
  /* ... existing ... */
  'classification.economic_dependency_warning',
  'classification.economic_dependency_critical',
  'classification.reassessment_trigger',  // Plan 60-02 adds this
] as const;
```

From packages/api/src/routers/classification.ts (submit mutation — EXTEND to auto-resolve):
```ts
submit: tenantProcedure.use(requirePermission({ contractor: ['update'] })).input(submitInput).mutation(async ({ ctx, input }) => {
  // existing submit logic creates completed ClassificationAssessment
  // APPEND: if countryCode === 'GB', set matching ReassessmentTrigger.status = RESOLVED
});
```

Material-change allowlist per D-07 (VERIFIED):
- ContractorAssignment (resourceType='CONTRACTOR' with resourceId=assignmentId per pitfall 4): activeTo, projectId, teamId, status
- Contract: rateValueMinor, rateType, billingModel, startDate, endDate, scope, description
- IGNORED: tag links, owner reassignments, allocationPercent changes, notes, costCenterId
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Audit writer + ContractorAssignment/Contract mutation instrumentation + schema + scaffolds</name>
  <files>packages/db/prisma/schema/classification.prisma, packages/db/prisma/schema/contractor.prisma, packages/db/prisma/schema/organization.prisma, packages/db/prisma/schema/auth.prisma, packages/validators/src/notification.ts, packages/api/src/services/cron-monitor.ts, packages/api/src/schemas/reassessment-trigger-reason.ts, packages/api/src/schemas/__tests__/reassessment-trigger-reason.test.ts, packages/api/src/services/audit-writer.ts, packages/api/src/services/__tests__/audit-writer.test.ts, packages/api/src/routers/contractor.ts, packages/api/src/routers/contract.ts, packages/api/src/routers/__tests__/contractor.test.ts, packages/api/src/routers/__tests__/contract.test.ts, packages/api/src/services/__tests__/reassessment-trigger-scan.test.ts, packages/api/src/routers/__tests__/reassessment-trigger.test.ts, apps/web/src/app/api/cron/classification-reassessment-triggers/__tests__/route.test.ts</files>
  <read_first>
    - packages/db/prisma/schema/audit.prisma (AuditLog shape, EntityType enum)
    - packages/db/prisma/schema/contract.prisma (Contract fields — rateValueMinor, startDate, endDate, scope, description; lines 1-100)
    - packages/db/prisma/schema/contractor.prisma (ContractorAssignment shape; AssignmentStatus enum)
    - packages/api/src/routers/contractor.ts (existing mutations for contractorAssignment.* — find create/update/delete call sites)
    - packages/api/src/routers/contract.ts (existing Contract mutations)
    - packages/api/src/middleware/tenant.ts (tenant context shape — organizationId + userId + actor type)
    - .planning/phases/60-classification-polish/60-RESEARCH.md §Pitfall 1 + §Pitfall 2 + §Pitfall 4 + §Pitfall 12 + §Example 2
    - .planning/phases/60-classification-polish/60-01-PLAN.md (Plan 60-01 frontmatter files_modified — prismaRaw + NOTIFICATION_TYPES extensions must be in place)
  </read_first>
  <behavior>
    - Test (audit-writer happy path): writeAuditLog({ organizationId, actorType: 'USER', action: 'UPDATE', resourceType: 'CONTRACTOR', resourceId: 'asg_123', oldValues: { activeTo: '2026-01-01' }, newValues: { activeTo: '2027-01-01' } }) creates exactly one AuditLog row with those fields.
    - Test (audit-writer idempotent on failure): if prisma.auditLog.create throws, the mutation caller's transaction rolls back (no orphan rows).
    - Test (contractor router audit): calling contractorAssignment.update via tRPC creates an AuditLog row with resourceType='CONTRACTOR' + resourceId=assignmentId (resolves Open Question #1).
    - Test (contract router audit): calling contract.update via tRPC creates an AuditLog row with resourceType='CONTRACT' + resourceId=contractId.
    - Test (triggerReasonSchema): z.parse accepts { field: 'activeTo', oldValue: '...', newValue: '...', auditLogId: 'aud_1', resourceType: 'CONTRACTOR' }; rejects missing auditLogId.
    - Test (triggerReasonsSchema): parses an array of TriggerReason objects.
  </behavior>
  <action>
    1. Append to packages/db/prisma/schema/classification.prisma:
       ```prisma
       enum ReassessmentTriggerStatus { OPEN ACKNOWLEDGED RESOLVED DISMISSED }

       model ReassessmentTrigger {
         id                     String                    @id @default(cuid())
         organizationId         String
         contractorAssignmentId String
         priorAssessmentId      String
         priorSdsDocumentId     String?
         triggeredAt            DateTime                  @default(now())
         triggerReasons         Json
         status                 ReassessmentTriggerStatus @default(OPEN)
         acknowledgedByUserId   String?
         acknowledgedAt         DateTime?
         resolvedAt             DateTime?
         dismissedByUserId      String?
         dismissedAt            DateTime?
         dismissedReason        String?                   @db.VarChar(1000)
         createdAt              DateTime                  @default(now())
         updatedAt              DateTime                  @updatedAt

         organization         Organization             @relation(fields: [organizationId], references: [id])
         contractorAssignment ContractorAssignment     @relation(fields: [contractorAssignmentId], references: [id])
         priorAssessment      ClassificationAssessment @relation("ReassessmentTriggerPriorAssessment", fields: [priorAssessmentId], references: [id])
         priorSdsDocument     ClassificationDocument?  @relation(fields: [priorSdsDocumentId], references: [id])
         acknowledgedByUser   User?                    @relation("ReassessmentTriggerAck", fields: [acknowledgedByUserId], references: [id])
         dismissedByUser      User?                    @relation("ReassessmentTriggerDismiss", fields: [dismissedByUserId], references: [id])

         @@index([organizationId, contractorAssignmentId, status], map: "RT_org_assign_status_idx")
         @@index([organizationId, triggeredAt], map: "RT_org_triggeredAt_idx")
       }

       model CronScanState {
         name                 String   @id  @db.VarChar(100)
         lastScanCompletedAt  DateTime
         updatedAt            DateTime @updatedAt
       }
       ```
    2. Add back-relations:
       - packages/db/prisma/schema/contractor.prisma ContractorAssignment: `reassessmentTriggers ReassessmentTrigger[]`
       - packages/db/prisma/schema/organization.prisma Organization: `reassessmentTriggers ReassessmentTrigger[]`
       - packages/db/prisma/schema/classification.prisma ClassificationAssessment: `reassessmentTriggers ReassessmentTrigger[] @relation("ReassessmentTriggerPriorAssessment")`
       - packages/db/prisma/schema/classification.prisma ClassificationDocument: `reassessmentTriggers ReassessmentTrigger[]`
       - packages/db/prisma/schema/auth.prisma User: `acknowledgedReassessmentTriggers ReassessmentTrigger[] @relation("ReassessmentTriggerAck")` + `dismissedReassessmentTriggers ReassessmentTrigger[] @relation("ReassessmentTriggerDismiss")`
    3. Append to packages/validators/src/notification.ts NOTIFICATION_TYPES: `'classification.reassessment_trigger'` (after the two Plan 60-01 entries).
    4. Append to packages/api/src/services/cron-monitor.ts CronMonitors enum: `classificationReassessmentTriggers: 'classification-reassessment-triggers'`.
    5. Create packages/api/src/schemas/reassessment-trigger-reason.ts:
       ```ts
       import { z } from 'zod';
       export const triggerReasonSchema = z.object({
         field: z.string().min(1),
         oldValue: z.unknown().optional(),
         newValue: z.unknown().optional(),
         auditLogId: z.string().min(1),
         resourceType: z.enum(['CONTRACTOR', 'CONTRACT']),
       });
       export type TriggerReason = z.infer<typeof triggerReasonSchema>;
       export const triggerReasonsSchema = z.array(triggerReasonSchema);
       export type TriggerReasons = z.infer<typeof triggerReasonsSchema>;
       ```
    6. Create packages/api/src/schemas/__tests__/reassessment-trigger-reason.test.ts with 4 tests: happy path, missing auditLogId rejected, array parse, unknown resourceType rejected.
    7. Create packages/api/src/services/audit-writer.ts:
       ```ts
       import type { ActorType, EntityType } from '@contractor-ops/db';
       import { prisma } from '@contractor-ops/db';
       import { createLogger } from '@contractor-ops/logger';
       const log = createLogger('audit-writer');
       export async function writeAuditLog(input: {
         organizationId: string;
         actorType: ActorType;
         actorId?: string | null;
         action: 'CREATE' | 'UPDATE' | 'DELETE' | string;
         resourceType: EntityType;
         resourceId: string;
         oldValues?: Record<string, unknown> | null;
         newValues?: Record<string, unknown> | null;
         tx?: Parameters<Parameters<typeof prisma.$transaction>[0]>[0];
       }): Promise<void> {
         const client = input.tx ?? prisma;
         await client.auditLog.create({
           data: {
             organizationId: input.organizationId,
             actorType: input.actorType,
             actorId: input.actorId ?? null,
             action: input.action,
             resourceType: input.resourceType,
             resourceId: input.resourceId,
             oldValuesJson: input.oldValues ?? undefined,
             newValuesJson: input.newValues ?? undefined,
           },
         });
       }
       ```
       NO console.* — use createLogger.
    8. Create packages/api/src/services/__tests__/audit-writer.test.ts — 3 tests: happy path creates row; accepts transaction client; rejects missing organizationId.
    9. Modify packages/api/src/routers/contractor.ts:
       - Locate every ContractorAssignment mutation (create/update/delete/assign/unassign — grep `ctx.db.contractorAssignment.` or `contractorAssignment:` inside router mutations)
       - Wrap each mutation in a `ctx.db.$transaction(async tx => { ... })` block
       - Inside the transaction: capture `oldValues = await tx.contractorAssignment.findUnique({ where: { id } })` BEFORE update/delete
       - Perform the mutation
       - Call `await writeAuditLog({ organizationId: ctx.organizationId, actorType: 'USER', actorId: ctx.userId, action: 'UPDATE'|'CREATE'|'DELETE', resourceType: 'CONTRACTOR', resourceId: assignmentId, oldValues, newValues, tx })`
       - For create: oldValues=null; for delete: newValues=null
    10. Same pattern for packages/api/src/routers/contract.ts — instrument create/update/delete with resourceType='CONTRACT', resourceId=contractId.
    11. Append tests to packages/api/src/routers/__tests__/contractor.test.ts verifying AuditLog row written on assignment update (1 new test case) and on create + delete (2 more cases).
    12. Append tests to packages/api/src/routers/__tests__/contract.test.ts verifying AuditLog on contract update (1 test).
    13. Create packages/api/src/services/__tests__/reassessment-trigger-scan.test.ts with describe.todo scaffolds: `filter` (60-02-01), `material-fields` (60-02-02), `dedup` (60-02-03), `no-prior-assessment` (60-02-04).
    14. Create packages/api/src/routers/__tests__/reassessment-trigger.test.ts with describe.todo scaffolds: `auto-resolve` (60-02-05), `status-transitions` (60-02-06), `rbac` (60-02-07).
    15. Create apps/web/src/app/api/cron/classification-reassessment-triggers/__tests__/route.test.ts with describe.todo scaffolds: `rejects missing Bearer`, `authorised call runs scan`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db exec prisma format && pnpm --filter @contractor-ops/api test -- audit-writer reassessment-trigger-reason contractor.test contract.test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "model ReassessmentTrigger" packages/db/prisma/schema/classification.prisma` returns 1 match.
    - `grep -n "model CronScanState" packages/db/prisma/schema/classification.prisma` returns 1 match.
    - `grep -n "enum ReassessmentTriggerStatus" packages/db/prisma/schema/classification.prisma` returns 1 match.
    - `grep -n "reassessmentTriggers ReassessmentTrigger" packages/db/prisma/schema/contractor.prisma` returns 1 match.
    - `grep -n "reassessmentTriggers ReassessmentTrigger" packages/db/prisma/schema/organization.prisma` returns 1 match.
    - `grep -n "ReassessmentTriggerPriorAssessment\\|ReassessmentTriggerAck\\|ReassessmentTriggerDismiss" packages/db/prisma/schema` returns ≥3 matches.
    - `grep -n "classification.reassessment_trigger" packages/validators/src/notification.ts` returns 1 match.
    - `grep -n "classification-reassessment-triggers" packages/api/src/services/cron-monitor.ts` returns 1 match.
    - `grep -n "export const triggerReasonSchema\\|export const triggerReasonsSchema" packages/api/src/schemas/reassessment-trigger-reason.ts` returns 2 matches.
    - `grep -n "export async function writeAuditLog" packages/api/src/services/audit-writer.ts` returns 1 match.
    - `grep -c "writeAuditLog(" packages/api/src/routers/contractor.ts` returns ≥3 (create + update + delete/assign mutations).
    - `grep -c "writeAuditLog(" packages/api/src/routers/contract.ts` returns ≥2 (create + update minimum).
    - `grep -cE "console\\.(log|warn|error|info|debug)" packages/api/src/services/audit-writer.ts` returns 0.
    - `pnpm --filter @contractor-ops/api test -- audit-writer` green; `pnpm --filter @contractor-ops/api test -- reassessment-trigger-reason` green.
    - `pnpm --filter @contractor-ops/api test -- contractor.test` includes ≥3 new AuditLog-assertion tests all green; `contract.test` includes ≥1 new test green.
  </acceptance_criteria>
  <done>Schema pushed-ready; ContractorAssignment + Contract mutations emit AuditLog (Open Question #1 resolved); triggerReasonsSchema exists with tests; all Wave-0 scaffolds for reassessment scan + router + cron route present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 [BLOCKING]: Prisma push + scan service + cron route + router + classification auto-resolve + UI components</name>
  <files>packages/api/src/services/reassessment-trigger-scan.ts, packages/api/src/services/__tests__/reassessment-trigger-scan.test.ts, packages/api/src/routers/reassessment-trigger.ts, packages/api/src/routers/__tests__/reassessment-trigger.test.ts, packages/api/src/routers/classification.ts, packages/api/src/routers/__tests__/classification.test.ts, packages/api/src/root.ts, apps/web/src/app/api/cron/classification-reassessment-triggers/route.ts, apps/web/src/app/api/cron/classification-reassessment-triggers/__tests__/route.test.ts, apps/web/messages/en.json, apps/web/messages/de.json, apps/web/messages/pl.json, apps/web/messages/ar.json, apps/web/src/components/contractors/classification/reassessment-trigger/trigger-chip.tsx, apps/web/src/components/contractors/classification/reassessment-trigger/trigger-cta.tsx, apps/web/src/components/contractors/classification/reassessment-trigger/dismiss-dialog.tsx, apps/web/src/components/contractors/classification/reassessment-trigger/__tests__/trigger-chip.test.tsx, apps/web/src/components/contractors/classification/reassessment-trigger/__tests__/dismiss-dialog.test.tsx</files>
  <read_first>
    - apps/web/src/app/api/cron/reminders/route.ts (shell to clone verbatim)
    - apps/web/src/app/api/cron/classification-economic-dependency/route.ts (Plan 60-01 reference cron)
    - packages/api/src/routers/classification.ts (submit mutation — location for auto-resolve extension)
    - packages/db/prisma/schema/audit.prisma (AuditLog query fields)
    - .planning/phases/60-classification-polish/60-RESEARCH.md §Pattern 4 + §Pattern 5 + §Pattern 7 + §Pitfall 1 + §Pitfall 4 + §Pitfall 12
    - .planning/phases/60-classification-polish/60-UI-SPEC.md §"CLASS-08 — Reassessment Triggers" copy + §Component Inventory entries for trigger-chip/trigger-cta/dismiss-dialog
  </read_first>
  <behavior>
    - Test (filter): seed 10 AuditLog rows — 3 CONTRACTOR on GB engagements w/ prior IR35 assessment (material), 2 CONTRACTOR on DE engagements (skipped), 2 CONTRACT on GB engagements w/ prior IR35 (material), 3 with only non-material field changes (skipped). Scan creates exactly 5 triggers OR ≤2 triggers if dedup clusters 3 CONTRACTOR rows with same (assignmentId, priorAssessmentId) into one.
    - Test (since-last-run): CronScanState.lastScanCompletedAt set to t=T; audit rows created before T are ignored, after T are processed; after scan, lastScanCompletedAt updated to scan start time.
    - Test (material-fields allowlist): CONTRACTOR audit row with oldValues.activeTo != newValues.activeTo → triggers. Row with ONLY allocationPercent change → NO trigger. Row with ONLY notes change → NO trigger. Row with tag links only → NO trigger. Contract row with rateValueMinor change → triggers. Contract row with ONLY notes change → NO trigger.
    - Test (no-prior-assessment): GB engagement without any completed IR35 ClassificationAssessment → material change does NOT create trigger.
    - Test (dedup): two audit rows for same assignmentId in one scan → triggerReasons array has 2 entries, only 1 ReassessmentTrigger row created, status=OPEN. Subsequent scan with another audit row on same assignment → triggerReasons appended (3 entries), still 1 row.
    - Test (dedup across OPEN + ACKNOWLEDGED): existing ACKNOWLEDGED trigger + new material change → appends to ACKNOWLEDGED row, doesn't create OPEN row.
    - Test (auto-resolve): seed OPEN ReassessmentTrigger on assignment X. Call classification.submit for a new IR35 assessment on X → trigger.status=RESOLVED, resolvedAt set.
    - Test (status transitions): acknowledge → ACKNOWLEDGED + acknowledgedByUserId/At set; dismiss with reason → DISMISSED + dismissedByUserId/At/Reason set.
    - Test (dismiss requires reason): dismiss with reason.length < 10 → tRPC BAD_REQUEST.
    - Test (RBAC): dismiss gated on contractor:update; acknowledge gated on contractor:update; list gated on contractor:read; cross-org id → FORBIDDEN/NOT_FOUND.
    - Test (UI trigger-chip): renders shadcn Badge with warning variant + RefreshCcw icon + label "Reassessment recommended" (from i18n).
    - Test (UI dismiss-dialog): reason textarea < 10 chars → destructive button disabled; ≥10 chars → enabled; on confirm, invokes tRPC mutation.
  </behavior>
  <action>
    1. Run `pnpm --filter @contractor-ops/db prisma:generate && pnpm --filter @contractor-ops/db prisma:push` [BLOCKING]. Must succeed before continuing.
    2. Seed CronScanState row for reassessment scan at deploy time — add seed in Prisma seed file or in a one-shot migration helper at startup: `prismaRaw.cronScanState.upsert({ where: { name: 'classification-reassessment-triggers' }, update: {}, create: { name: 'classification-reassessment-triggers', lastScanCompletedAt: new Date() } })`. Document in plan — prevents replaying historical audits per RESEARCH Pattern 4 pagination note + Runtime State inventory.
    3. Implement packages/api/src/services/reassessment-trigger-scan.ts per RESEARCH Pattern 4 + 5:
       - `export async function runReassessmentTriggerScan(now?: Date): Promise<{ scanned: number; material: number; triggersCreated: number; triggersAppended: number }>`
       - Read CronScanState.lastScanCompletedAt for name='classification-reassessment-triggers'; process AuditLog rows where `createdAt > lastScan && createdAt <= now && resourceType IN ('CONTRACTOR', 'CONTRACT')`
       - For CONTRACTOR rows (assignmentId = resourceId): join to ContractorAssignment → Contractor.countryCode; skip if !== 'GB'
       - For CONTRACT rows (contractId = resourceId): join to Contract → ContractorAssignment → Contractor.countryCode; skip if !== 'GB'
       - Apply `isMaterialChange` allowlist from D-07:
         ```ts
         const CONTRACTOR_MATERIAL_FIELDS = new Set(['activeTo', 'projectId', 'teamId', 'status']);
         const CONTRACT_MATERIAL_FIELDS = new Set(['rateValueMinor', 'rateType', 'billingModel', 'startDate', 'endDate', 'scope', 'description']);
         const IGNORED_FIELDS = new Set(['allocationPercent', 'notes', 'costCenterId', 'tagLinks', 'ownerId']);
         ```
       - For each engagement (grouped by contractorAssignmentId): require a completed IR35 ClassificationAssessment — `ClassificationAssessment.findFirst({ where: { contractorAssignmentId, countryCode: 'GB', status: 'completed' }, orderBy: { completedAt: 'desc' }, include: { classificationDocuments: { where: { kind: 'SDS' }, orderBy: { generatedAt: 'desc' }, take: 1 } } })`; skip if none.
       - Call `dedupAndCreateTrigger` (RESEARCH Pattern 5) — append to existing OPEN/ACKNOWLEDGED row for same (contractorAssignmentId, priorAssessmentId), else create new row with status=OPEN + dispatch notification.
       - Use `triggerReasonsSchema.parse()` on EVERY read AND write of the JSONB column.
       - Update CronScanState.lastScanCompletedAt to scan end time at finish.
       - Log via createCronLogger('classification-reassessment-triggers'); emit metrics.gauge for `scanned`, `material`, `triggersCreated`, `triggersAppended`.
       - Notification dispatch uses resolveRbacRecipients (from Plan 60-01), entityType='CONTRACTOR' + entityId=contractorAssignmentId (per pitfall 4), type='classification.reassessment_trigger'.
       - Add `// PHASE-60-CROSS-ORG-AGGREGATE` sentinel above prismaRaw.auditLog.findMany (reason: scan iterates all orgs).
       - Add `take: 10000` safety limit on findMany with a metrics.gauge warn if hit (per RESEARCH Pattern 4).
    4. Fill packages/api/src/services/__tests__/reassessment-trigger-scan.test.ts with real tests replacing describe.todo stubs — minimum 8 tests covering filter, material-fields, dedup, no-prior-assessment, since-last-run + CronScanState update.
    5. Implement packages/api/src/routers/reassessment-trigger.ts:
       ```ts
       const contractorReadProcedure = tenantProcedure.use(requirePermission({ contractor: ['read'] }));
       const contractorUpdateProcedure = tenantProcedure.use(requirePermission({ contractor: ['update'] }));
       const dismissInput = z.object({
         id: z.string().min(1),
         reason: z.string().min(10).max(1000),
       });
       export const reassessmentTriggerRouter = router({
         list: contractorReadProcedure.input(z.object({ status: z.enum(['OPEN','ACKNOWLEDGED','RESOLVED','DISMISSED']).optional(), limit: z.number().min(1).max(100).default(50) })).query(...),
         listByEngagement: contractorReadProcedure.input(z.object({ contractorAssignmentId: z.string() })).query(...),
         acknowledge: contractorUpdateProcedure.input(z.object({ id: z.string().min(1) })).mutation(...),
         dismiss: contractorUpdateProcedure.input(dismissInput).mutation(...),
       });
       ```
       Transitions:
       - acknowledge: status OPEN → ACKNOWLEDGED; sets acknowledgedByUserId=ctx.userId, acknowledgedAt=now
       - dismiss: status OPEN|ACKNOWLEDGED → DISMISSED; sets dismissedByUserId, dismissedAt, dismissedReason
       - Invalid transitions (dismiss on RESOLVED) → TRPCError BAD_REQUEST
    6. Wire into packages/api/src/root.ts appRouter: `reassessmentTrigger: reassessmentTriggerRouter`.
    7. Extend packages/api/src/routers/classification.ts submit mutation:
       - After the completed assessment is persisted, if countryCode === 'GB': `await ctx.db.reassessmentTrigger.updateMany({ where: { contractorAssignmentId: input.contractorAssignmentId, status: { in: ['OPEN', 'ACKNOWLEDGED'] } }, data: { status: 'RESOLVED', resolvedAt: new Date() } })`
       - This uses the tenant-scoped client so cross-org rows cannot be mutated.
    8. Append tests to packages/api/src/routers/__tests__/classification.test.ts verifying the auto-resolve behaviour (row 60-02-05).
    9. Fill packages/api/src/routers/__tests__/reassessment-trigger.test.ts with real tests: list, listByEngagement org-scoping, acknowledge happy path, dismiss reason validation, dismiss RBAC (cross-org FORBIDDEN), status-transition invalid cases.
    10. Create apps/web/src/app/api/cron/classification-reassessment-triggers/route.ts cloning apps/web/src/app/api/cron/reminders/route.ts shell:
        - verifyCronSecret Bearer check
        - Sentry.withMonitor + withCronMonitor('classificationReassessmentTriggers', ...) wrapping `runReassessmentTriggerScan()`
        - createCronLogger('classification-reassessment-triggers')
        - Schedule: `{ schedule: { type: 'crontab', value: '0 3 * * *' }, timezone: 'UTC' }`
        - NO console.*
    11. Fill apps/web/src/app/api/cron/classification-reassessment-triggers/__tests__/route.test.ts with real tests.
    12. Add to apps/web/messages/en.json under `Classification.polish.reassessmentTrigger`:
        ```json
        "reassessmentTrigger": {
          "chipLabel": "Reassessment recommended",
          "chipTooltip": "Material changes were detected since the last IR35 Status Determination Statement.",
          "ctaPrimary": "Start new assessment",
          "ctaSecondary": "View prior SDS",
          "reasonLine": "{field} changed from {oldValue} to {newValue} on {date}",
          "notificationTitle": "Reassessment recommended: {engagementName}",
          "notificationBody": "{N} material change(s) detected. The prior SDS was completed on {priorDate}. Open the engagement to review.",
          "dismissAction": "Dismiss trigger",
          "dismissHeading": "Dismiss this reassessment trigger?",
          "dismissBody": "The system will not re-fire on these field changes. You can still start a new assessment manually.",
          "dismissReasonLabel": "Reason for dismissing (required)",
          "emptyHeading": "No reassessment triggers",
          "emptyBody": "All UK engagements with a Status Determination Statement are still aligned with their captured facts."
        }
        ```
        Mirror to de.json, pl.json, ar.json.
    13. Create React components per UI-SPEC §Component Inventory:
        - trigger-chip.tsx: shadcn Badge (variant="warning" — add variant to badge.tsx if absent) + lucide `RefreshCcw` (size=12) + label from i18n. Inline placement (not full-width banner).
        - trigger-cta.tsx: shadcn `button` + `dropdown-menu` with items (Start new assessment / Dismiss / View prior SDS). Routes to `/contractors/[id]/engagements/[engagementId]/classification/new` on Start. Calls dismiss dialog on Dismiss. Opens priorSdsDocument download URL on View prior SDS.
        - dismiss-dialog.tsx: shadcn `alert-dialog` + textarea (reason). Destructive button disabled until reason.length ≥ 10. `role="alert"` on validation error. On confirm → trpc.reassessmentTrigger.dismiss.mutate + toast via sonner.
    14. Create __tests__/trigger-chip.test.tsx (renders label + icon + aria) and __tests__/dismiss-dialog.test.tsx (reason length gating, submit flow, a11y).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db prisma:generate && pnpm --filter @contractor-ops/db prisma:push && pnpm --filter @contractor-ops/api test -- reassessment classification.test && pnpm --filter @contractor-ops/web test -- reassessment-trigger</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/db prisma:push` exits 0 (ReassessmentTrigger + CronScanState tables exist).
    - `grep -n "runReassessmentTriggerScan" packages/api/src/services/reassessment-trigger-scan.ts` returns ≥1 match.
    - `grep -n "PHASE-60-CROSS-ORG-AGGREGATE" packages/api/src/services/reassessment-trigger-scan.ts` returns ≥1 match.
    - `grep -n "triggerReasonsSchema.parse" packages/api/src/services/reassessment-trigger-scan.ts` returns ≥2 matches (read + write).
    - `grep -nE "activeTo|projectId|teamId|rateValueMinor|scope|description" packages/api/src/services/reassessment-trigger-scan.ts` returns ≥6 matches (allowlist).
    - `grep -nE "allocationPercent|notes|costCenterId" packages/api/src/services/reassessment-trigger-scan.ts` returns ≥2 matches in IGNORED_FIELDS set definition.
    - `grep -n "status: 'RESOLVED'" packages/api/src/routers/classification.ts` OR `grep -n "RESOLVED" packages/api/src/routers/classification.ts` returns ≥1 match (auto-resolve).
    - `grep -n "reassessmentTrigger" packages/api/src/root.ts` returns ≥1 match (wired).
    - `grep -n "verifyCronSecret" apps/web/src/app/api/cron/classification-reassessment-triggers/route.ts` returns ≥1 match.
    - `grep -n "createCronLogger('classification-reassessment-triggers')" apps/web/src/app/api/cron/classification-reassessment-triggers/route.ts` returns 1 match.
    - `grep -n "RefreshCcw" apps/web/src/components/contractors/classification/reassessment-trigger/trigger-chip.tsx` returns ≥1 match.
    - `grep -n "length.*10\\|min(10)" apps/web/src/components/contractors/classification/reassessment-trigger/dismiss-dialog.tsx packages/api/src/routers/reassessment-trigger.ts` returns ≥2 matches (dismiss reason length gating).
    - `grep -cE "console\\.(log|warn|error|info|debug)" packages/api/src/services/reassessment-trigger-scan.ts apps/web/src/app/api/cron/classification-reassessment-triggers/route.ts packages/api/src/routers/reassessment-trigger.ts` returns 0.
    - `grep -c "reassessmentTrigger\\|Reassessment" apps/web/messages/en.json apps/web/messages/de.json apps/web/messages/pl.json apps/web/messages/ar.json` returns ≥4 matches total.
    - VALIDATION.md rows 60-02-01..07 all map to real passing tests (no describe.todo remaining).
    - Full test suite: `pnpm --filter @contractor-ops/api test -- reassessment classification.test` and `pnpm --filter @contractor-ops/web test -- reassessment-trigger` — all green.
  </acceptance_criteria>
  <done>ReassessmentTrigger model + CronScanState pushed; AuditLog-based scan service with material-field allowlist + dedup shipping; cron route + tRPC router + UI components green; classification.submit auto-resolves matching triggers; VALIDATION.md rows 60-02-* green; Open Question #1 resolved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron scheduler → /api/cron/classification-reassessment-triggers | Bearer CRON_SECRET gate |
| tRPC client → reassessmentTrigger.{acknowledge,dismiss} | contractor:update gate + tenantProcedure isolation |
| prismaRaw.auditLog.findMany (cross-org) | Scan reads AuditLog rows across all orgs sequentially; per-row organizationId extracted for trigger creation |
| ClassificationAssessment.submit → updateMany on ReassessmentTrigger | tenant-scoped client ensures only same-org triggers resolved |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-07 | S (Spoofing) | /api/cron/classification-reassessment-triggers | HIGH | mitigate | verifyCronSecret Bearer check — 401 on missing/mismatched. Test in route.test.ts. |
| T-60-08 | E (Elevation of Privilege) | reassessmentTrigger.dismiss | HIGH | mitigate | contractor:update gate via requirePermission middleware; tenant isolation via tenantProcedure; cross-org id returns TRPC FORBIDDEN per integration test. |
| T-60-09 | T (Tampering) | triggerReasons JSONB | MEDIUM | mitigate | triggerReasonsSchema.parse() on BOTH read and write paths; malformed JSONB rejected at scan + router layer (research Pitfall 12). Unit test asserts malformed row rejected. |
| T-60-10 | R (Repudiation) | Dismiss action without record | MEDIUM | mitigate | dismiss mutation writes dismissedByUserId + dismissedAt + dismissedReason; all three non-nullable after dismiss transition. The router also calls writeAuditLog to record the ReassessmentTrigger dismissal itself (resourceType='CONTRACTOR'); this extends the audit trail beyond just the trigger row. |
| T-60-11 | I (Information Disclosure) | triggerReasons exposing old/new rate values in JSONB | MEDIUM | accept | Trigger row scoped to organizationId; only viewable via tRPC listByEngagement gated by contractor:read. Rate values ARE PII-adjacent; access already requires tenant + permission gate. |
| T-60-12 | D (Denial of Service) | AuditLog scan loading >10k rows | LOW | mitigate | `take: 10000` safety limit + metrics.gauge alert if hit. Pagination can be added in v5.1 if sustained high volume. |
| T-60-13 | S (Spoofing) | Notification target-org confusion | LOW | accept | notification-service.dispatch resolves IntegrationConnection scoped by organizationId (verified in research) — inherited mitigation. |
| T-60-14 | T (Tampering) | AuditLog row manipulation after scan | LOW | mitigate | AuditLog is append-only by project convention (audit.prisma comment). CronScanState.lastScanCompletedAt ensures exactly-once scan per row. New audit-writer helper does NOT allow updates or deletes — only create. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/db exec prisma format` — schema valid
- `pnpm --filter @contractor-ops/db prisma:push` — new models in DB
- `pnpm --filter @contractor-ops/api test -- reassessment classification.test contractor.test contract.test audit-writer` — all green (≥15 tests)
- `pnpm --filter @contractor-ops/web test -- reassessment-trigger` — chip + dismiss-dialog green
- Manual: create test CONTRACTOR engagement, complete IR35 assessment, update assignment.activeTo, run cron locally — one trigger row created; call classification.submit for new assessment → trigger.status=RESOLVED
- Manual: run cron locally twice in 5 min with no new audits between → second run creates zero triggers (CronScanState update works)
</verification>

<success_criteria>
- ReassessmentTrigger + CronScanState Prisma models + back-relations land
- ContractorAssignment + Contract mutations emit AuditLog via audit-writer helper (Open Question #1 resolved)
- triggerReasonsSchema Zod-parses JSONB on read + write
- runReassessmentTriggerScan applies material-field allowlist + dedup + since-last-run filter
- Daily cron /api/cron/classification-reassessment-triggers — Bearer auth + Sentry + metrics
- tRPC router: list + listByEngagement (read), acknowledge + dismiss (update); dismiss reason ≥10 chars
- classification.submit auto-sets matching OPEN/ACKNOWLEDGED triggers to RESOLVED
- 3 React components (trigger-chip, trigger-cta, dismiss-dialog) per UI-SPEC
- i18n Classification.polish.reassessmentTrigger.* in 4 locales
- Zero console.* in new source
- VALIDATION.md rows 60-02-01..07 all have real green tests
</success_criteria>

<output>
After completion, create `.planning/phases/60-classification-polish/60-02-SUMMARY.md`:
- Open Question #1 resolved: AuditLog writes wired onto ContractorAssignment + Contract mutations via shared audit-writer helper
- CronScanState seeded to deploy time — historical audits are NOT replayed
- Manual-Only Verifications: cron 0 3 * * * UTC schedule registration deferred per STATE.md; Steuerberater sign-off on any German-language copy unchanged (chrome only here)
- Test count green: ≥ 15 for this plan
- Next plan: 60-03 adds Statusfeststellungsverfahren model and piggybacks on existing reminders cron for 90/30/7-day expiry reminders
</output>
