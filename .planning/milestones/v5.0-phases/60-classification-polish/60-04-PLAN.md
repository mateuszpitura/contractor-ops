---
phase: 60
plan: 04
type: execute
wave: 3
depends_on: [60-01, 60-02, 60-03]
files_modified:
  - packages/api/src/lib/csv.ts
  - packages/api/src/lib/__tests__/csv.test.ts
  - packages/api/src/routers/classification-dashboard.ts
  - packages/api/src/routers/__tests__/classification-dashboard.test.ts
  - packages/api/src/root.ts
  - apps/web/messages/en.json
  - apps/web/messages/de.json
  - apps/web/messages/pl.json
  - apps/web/messages/ar.json
  - apps/web/src/app/[locale]/(dashboard)/classification/page.tsx
  - apps/web/src/app/[locale]/(dashboard)/classification/__tests__/page.test.tsx
  - apps/web/src/app/[locale]/(dashboard)/classification/__tests__/a11y.test.tsx
  - apps/web/src/components/contractors/classification/dashboard/market-card.tsx
  - apps/web/src/components/contractors/classification/dashboard/coverage-tile.tsx
  - apps/web/src/components/contractors/classification/dashboard/risk-distribution-tile.tsx
  - apps/web/src/components/contractors/classification/dashboard/overdue-reassessments-tile.tsx
  - apps/web/src/components/contractors/classification/dashboard/active-alerts-tile.tsx
  - apps/web/src/components/contractors/classification/dashboard/refresh-dashboard-button.tsx
  - apps/web/src/components/contractors/classification/dashboard/download-csv-button.tsx
  - apps/web/src/components/contractors/classification/dashboard/__tests__/risk-distribution-tile.test.tsx
  - apps/web/src/components/contractors/classification/dashboard/__tests__/market-card.test.tsx
autonomous: true
requirements: [CLASS-10]
requirements_addressed: [CLASS-10]
tags: [classification, dashboard, csv, reporting, analytics, security, xss]

must_haves:
  truths:
    - "escapeCsvField in packages/api/src/lib/csv.ts neutralises CSV formula-prefix characters (=, +, -, @) by prefixing with a single quote — closes the research-identified CSV-injection gap (A11)."
    - "tRPC router classificationDashboard exports 8 procedures covering 4 tiles × 2 markets + a coverageByMarket + riskDistributionByMarket + overdueByMarket + activeAlertsByMarket + exportMarketCsv (server-side streaming via signed R2 URL, 300s TTL per D-16)."
    - "Dashboard page /[locale]/(dashboard)/classification/page.tsx renders two market cards (GB IR35 + DE Scheinselbständigkeit) stacked vertically with 4 tiles each per UI-SPEC D-13."
    - "Risk-distribution tile uses native flex + --success/--warning/--destructive OKLCh tokens — NO chart library added (UI-SPEC Registry Safety PASS)."
    - "Stacked-bar tile has role='img' + aria-label + per-segment tooltip with counts and percentages."
    - "All dashboard tRPC queries filter ClassificationAssessment.status='completed' (pitfall 8 — drafts excluded)."
    - "All dashboard tRPC queries are multi-tenant scoped via tenantProcedure — cross-org assertion in integration tests."
    - "CSV export contains columns per D-16 plus UTF-8 BOM; engagement rows with user-entered names starting with =/+/-/@ are neutralised (test with contractor name '=cmd|/C calc'!A1')."
    - "Manual refresh button invalidates classificationDashboard.* React Query caches with aria-live='polite' announcement per UI-SPEC."
    - "i18n strings under Classification.polish.dashboard.* in 4 locales."
  artifacts:
    - path: packages/api/src/lib/csv.ts
      provides: "escapeCsvField extended to neutralise =, +, -, @ formula prefixes; encodeCsvUtf8Bom unchanged"
      contains: "escapeCsvField"
    - path: packages/api/src/routers/classification-dashboard.ts
      provides: "classificationDashboardRouter with 8 procedures covering coverage/riskDistribution/overdue/activeAlerts per market + exportMarketCsv"
      contains: "classificationDashboardRouter"
    - path: apps/web/src/app/[locale]/(dashboard)/classification/page.tsx
      provides: "Server component dashboard page — renders global header + 2 market cards with 4 tiles each per UI-SPEC"
      contains: "MarketCard"
    - path: apps/web/src/components/contractors/classification/dashboard/risk-distribution-tile.tsx
      provides: "Native-flex stacked bar — success/warning/destructive OKLCh tokens + per-segment tooltip + role='img' + aria-label"
      contains: "bg-[--success]"
    - path: apps/web/src/components/contractors/classification/dashboard/download-csv-button.tsx
      provides: "Signed-URL fetch trigger for exportMarketCsv mutation + disabled spinner state"
      contains: "exportMarketCsv"
  key_links:
    - from: apps/web/src/app/[locale]/(dashboard)/classification/page.tsx
      to: packages/api/src/routers/classification-dashboard.ts
      via: "RSC tRPC calls for all 4 tiles per market"
      pattern: "classificationDashboard\\."
    - from: packages/api/src/routers/classification-dashboard.ts
      to: packages/db/prisma/schema/classification.prisma (ClassificationAssessment + EconomicDependencyAlertState + ReassessmentTrigger + Statusfeststellungsverfahren)
      via: "Aggregated queries joining all 4 Phase-60 models + Phase-58 outcomes"
      pattern: "status: 'completed'"
    - from: packages/api/src/routers/classification-dashboard.ts
      to: packages/api/src/lib/csv.ts
      via: "encodeCsvUtf8Bom + escapeCsvField for exportMarketCsv"
      pattern: "encodeCsvUtf8Bom"
    - from: packages/api/src/routers/classification-dashboard.ts
      to: packages/api/src/services/r2.ts
      via: "putObjectAndSignDownload for signed 300s CSV URL"
      pattern: "putObjectAndSignDownload"

user_setup: []
---

<objective>
Deliver CLASS-10: per-market compliance health dashboard consuming Phase-58 ClassificationAssessment + Phase-60 EconomicDependencyAlertState + ReassessmentTrigger + Statusfeststellungsverfahren. Extend escapeCsvField to neutralise formula-prefix CSV injection (research gap A11), ship classificationDashboard tRPC router with 8 procedures + CSV export, build the dashboard page with 2 market cards × 4 tiles using native flex stacked bar (no chart library), and expose manual refresh + CSV download per UI-SPEC.

Purpose: Single-pane-of-glass compliance view across GB IR35 and DE Scheinselbständigkeit — coverage %, risk distribution, overdue reassessments, active alerts — plus a compliance-officer-friendly CSV export.

Output: 1 CSV library extension (with security test), 1 new tRPC router, 1 new page, 7 new React components, UI-SPEC copy in 4 locales.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/60-classification-polish/60-CONTEXT.md
@.planning/phases/60-classification-polish/60-RESEARCH.md
@.planning/phases/60-classification-polish/60-UI-SPEC.md
@.planning/phases/60-classification-polish/60-VALIDATION.md
@.planning/phases/60-classification-polish/60-01-PLAN.md
@.planning/phases/60-classification-polish/60-02-PLAN.md
@.planning/phases/60-classification-polish/60-03-PLAN.md

# Upstream
@.planning/phases/58-classification-engine-rule-sets/58-CONTEXT.md
@.planning/phases/59-classification-documents-chain-tracking/59-CONTEXT.md

# Reference implementations
@packages/api/src/lib/csv.ts
@packages/api/src/routers/classification.ts
@packages/api/src/routers/classification-document.tsx
@packages/api/src/services/r2.ts
@packages/api/src/middleware/rbac.ts
@packages/api/src/middleware/tenant.ts
@apps/web/src/app/globals.css
@apps/web/src/components/contractors/classification/outcome/verdict-banner.tsx

<interfaces>
From packages/api/src/lib/csv.ts (EXISTING — extend):
```ts
// Current implementation escapes , " \r \n only.
// EXTEND to neutralise leading =/+/-/@ per research gap A11 (OWASP CSV injection):
export function escapeCsvField(value: string | number | null | undefined): string;
```

From packages/db/prisma/schema (ALL relevant models):
```prisma
model ClassificationAssessment {
  status          String   // 'draft' | 'completed' — filter pitfall 8
  countryCode     String   // 'GB' | 'DE'
  outcome         Json?    // Phase 58 D-03 discriminated union
  completedAt     DateTime?
  contractorAssignmentId String
}
model EconomicDependencyAlertState {       // from Plan 60-01
  currentBand           EconomicDependencyBand
  lastBillingShare      Decimal
  contractorAssignmentId String @unique
}
model ReassessmentTrigger {                // from Plan 60-02
  status   ReassessmentTriggerStatus  // OPEN|ACKNOWLEDGED|RESOLVED|DISMISSED
  contractorAssignmentId String
}
model Statusfeststellungsverfahren {       // from Plan 60-03
  outcome  StatusfeststellungsverfahrenOutcome  // PENDING|SELBSTANDIG|ABHANGIG|WITHDRAWN
  validTo  DateTime?
  contractorAssignmentId String
}
```

From packages/api/src/services/r2.ts:
```ts
export async function putObjectAndSignDownload(input: {
  key: string;
  body: Buffer | Uint8Array;
  contentType: string;
  ttlSeconds: number;
  contentDisposition?: string;
}): Promise<{ signedUrl: string; expiresInSeconds: number }>;
```

UI-SPEC D-16 CSV columns:
- engagementId, contractorName, country, latestVerdict, latestCompletedAt,
  latestScore (DE only), economicBand (DE only), billingShare (DE only),
  openReassessmentTrigger (Y/N), drvOutcome (DE only), drvValidTo (DE only)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Extend escapeCsvField security + router + test scaffolds</name>
  <files>packages/api/src/lib/csv.ts, packages/api/src/lib/__tests__/csv.test.ts, packages/api/src/routers/classification-dashboard.ts, packages/api/src/routers/__tests__/classification-dashboard.test.ts, packages/api/src/root.ts, apps/web/src/app/[locale]/(dashboard)/classification/__tests__/page.test.tsx, apps/web/src/app/[locale]/(dashboard)/classification/__tests__/a11y.test.tsx, apps/web/src/components/contractors/classification/dashboard/__tests__/risk-distribution-tile.test.tsx, apps/web/src/components/contractors/classification/dashboard/__tests__/market-card.test.tsx</files>
  <read_first>
    - packages/api/src/lib/csv.ts (current escapeCsvField + encodeCsvUtf8Bom shape — lines 1-32 per research)
    - packages/api/src/lib/__tests__/csv.test.ts (current test coverage — EXTEND, do NOT replace)
    - packages/api/src/routers/classification.ts (tenantProcedure + requirePermission chain pattern)
    - packages/api/src/services/r2.ts (putObjectAndSignDownload signature)
    - .planning/phases/60-classification-polish/60-RESEARCH.md §Pattern 8 + §Pitfall 8 + §Security Domain (formula-injection gap)
    - .planning/phases/60-classification-polish/60-UI-SPEC.md §"CLASS-10 — Compliance Health Dashboard" copy + component inventory
  </read_first>
  <behavior>
    - Test (escapeCsvField leading =): escapeCsvField('=cmd|/C calc'!A1') returns "'=cmd|/C calc'!A1" or `"'=cmd|/C calc'!A1"` wrapped in quotes — the leading `=` MUST be prefixed with a single quote so Excel treats it as text.
    - Test (escapeCsvField leading +): `+1` → `'+1`.
    - Test (escapeCsvField leading -): `-1` → `'-1`.
    - Test (escapeCsvField leading @): `@SUM(A1)` → `'@SUM(A1)`.
    - Test (escapeCsvField leading tab): tab-prefixed already-neutralised values pass through unchanged (no double-prefixing).
    - Test (escapeCsvField no-change): "normal text" returned unchanged (modulo existing quote-wrapping for commas/newlines).
    - Test (router scaffolds): classificationDashboardRouter exports 8 procedures; each procedure imported from test file compiles (type-only).
  </behavior>
  <action>
    1. Extend packages/api/src/lib/csv.ts escapeCsvField. Read current implementation first. Add formula-prefix neutralisation per OWASP CSV injection guidance:
       ```ts
       const FORMULA_PREFIXES = new Set(['=', '+', '-', '@']);
       export function escapeCsvField(value: string | number | null | undefined): string {
         if (value === null || value === undefined) return '';
         let s = typeof value === 'string' ? value : String(value);
         // Formula-injection neutralisation (OWASP / research gap A11).
         // Prefix with single quote so spreadsheet apps treat the cell as text.
         if (s.length > 0 && FORMULA_PREFIXES.has(s[0]!)) {
           s = `'${s}`;
         }
         // Existing quote/comma/newline escape stays below (preserve verbatim).
         if (s.includes(',') || s.includes('"') || s.includes('\n') || s.includes('\r')) {
           s = `"${s.replace(/"/g, '""')}"`;
         }
         return s;
       }
       ```
       Preserve the encodeCsvUtf8Bom signature + export.
    2. Extend packages/api/src/lib/__tests__/csv.test.ts with ≥6 new test cases covering the 5 behaviour tests above + a combined "=cmd|'/C calc'!A1" test (leading `=` + embedded quote + comma).
    3. Create packages/api/src/routers/classification-dashboard.ts skeleton with all 8 procedures as `contractorReadProcedure.input(...).query(async ({ ctx, input }) => { throw new Error('TODO Task 2') })` stubs. Procedures:
       - `coverageByMarket` — input: `{ market: 'GB'|'DE' }`; output: `{ completed: number; total: number }`
       - `riskDistributionByMarket` — input: `{ market: 'GB'|'DE' }`; output: `{ counts: { safe: number; warning: number; critical: number }, totalCompleted: number }` (where for GB: safe=Outside, warning=Undetermined, critical=Inside per UI-SPEC D-14)
       - `overdueByMarket` — input: `{ market: 'GB'|'DE' }`; output: `{ count: number; items: Array<{ contractorAssignmentId: string; contractorName: string; reason: string }> }`
       - `activeAlertsByMarket` — input: `{ market: 'GB'|'DE' }`; output: for GB `{ openReassessmentTriggers: number }`; for DE `{ economicBands: { warning: number; critical: number }; drvExpiringWithin90d: number }`
       - `globalHeader` — input: `{}`; output: `{ totalContractors: number; totalActiveEngagements: number; lastScannedAt: Date | null }` (lastScannedAt = max across EconomicDependencyAlertState.lastScannedAt + CronScanState.lastScanCompletedAt for reassessment-triggers)
       - `exportMarketCsv` — input: `{ market: 'GB'|'DE' }`; mutation; output: `{ url: string; expiresInSeconds: number }` (300s TTL)
       All use `contractorReadProcedure = tenantProcedure.use(requirePermission({ contractor: ['read'] }))`; exportMarketCsv may additionally require contractor:read (users who can view the dashboard can export).
    4. Wire into packages/api/src/root.ts: `classificationDashboard: classificationDashboardRouter`.
    5. Create packages/api/src/routers/__tests__/classification-dashboard.test.ts with describe.todo scaffolds matching VALIDATION.md rows: `coverage` (60-04-01), `risk-distribution` (60-04-02), `overdue` (60-04-03), `active-alerts` (60-04-04), `csv-sanitization` (60-04-05), `csv-format` (60-04-06).
    6. Create apps/web/src/app/[locale]/(dashboard)/classification/__tests__/page.test.tsx + __tests__/a11y.test.tsx with describe.todo scaffolds: `page.test.tsx: renders two market cards + 4 tiles each`, `renders global header`, `refresh button updates cache`, `download CSV opens signed URL`; `a11y.test.tsx: stacked bar has role img + aria-label`, `refresh button aria-live polite`, `keyboard focus order logical`.
    7. Create __tests__/risk-distribution-tile.test.tsx + __tests__/market-card.test.tsx scaffolds — describe.todo for VALIDATION.md 60-04-07 + 60-04-08.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- csv classification-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "FORMULA_PREFIXES\\|formula" packages/api/src/lib/csv.ts` returns ≥1 match.
    - `grep -cE "'=|'\\+|'-|'@" packages/api/src/lib/__tests__/csv.test.ts` returns ≥4 matches (4 neutralisation test cases).
    - `pnpm --filter @contractor-ops/api test -- csv` green with ≥6 new neutralisation tests.
    - `grep -n "classificationDashboardRouter" packages/api/src/routers/classification-dashboard.ts packages/api/src/root.ts` returns ≥2 matches (definition + wiring).
    - `grep -nE "coverageByMarket|riskDistributionByMarket|overdueByMarket|activeAlertsByMarket|globalHeader|exportMarketCsv" packages/api/src/routers/classification-dashboard.ts` returns ≥6 matches (all 6 named procedures declared).
    - `grep -c "describe.todo\\|describe\\(" packages/api/src/routers/__tests__/classification-dashboard.test.ts` returns ≥6 (one per VALIDATION row).
    - `grep -c "describe.todo\\|describe\\(" apps/web/src/app/[locale]/(dashboard)/classification/__tests__/page.test.tsx apps/web/src/app/[locale]/(dashboard)/classification/__tests__/a11y.test.tsx apps/web/src/components/contractors/classification/dashboard/__tests__/risk-distribution-tile.test.tsx apps/web/src/components/contractors/classification/dashboard/__tests__/market-card.test.tsx` returns ≥6 total (scaffolds for 60-04-07 + 60-04-08).
  </acceptance_criteria>
  <done>CSV helper hardened against formula injection with passing tests; dashboard router skeleton wired; all Wave-0 test scaffolds present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dashboard router procedures + CSV export + UI page + 7 components + i18n</name>
  <files>packages/api/src/routers/classification-dashboard.ts, packages/api/src/routers/__tests__/classification-dashboard.test.ts, apps/web/messages/en.json, apps/web/messages/de.json, apps/web/messages/pl.json, apps/web/messages/ar.json, apps/web/src/app/[locale]/(dashboard)/classification/page.tsx, apps/web/src/app/[locale]/(dashboard)/classification/__tests__/page.test.tsx, apps/web/src/app/[locale]/(dashboard)/classification/__tests__/a11y.test.tsx, apps/web/src/components/contractors/classification/dashboard/market-card.tsx, apps/web/src/components/contractors/classification/dashboard/coverage-tile.tsx, apps/web/src/components/contractors/classification/dashboard/risk-distribution-tile.tsx, apps/web/src/components/contractors/classification/dashboard/overdue-reassessments-tile.tsx, apps/web/src/components/contractors/classification/dashboard/active-alerts-tile.tsx, apps/web/src/components/contractors/classification/dashboard/refresh-dashboard-button.tsx, apps/web/src/components/contractors/classification/dashboard/download-csv-button.tsx, apps/web/src/components/contractors/classification/dashboard/__tests__/risk-distribution-tile.test.tsx, apps/web/src/components/contractors/classification/dashboard/__tests__/market-card.test.tsx</files>
  <read_first>
    - packages/api/src/routers/classification.ts (full file — tRPC query patterns to mirror)
    - packages/api/src/routers/classification-document.tsx (R2-sign mutation pattern)
    - packages/api/src/services/r2.ts (putObjectAndSignDownload signature)
    - apps/web/src/app/globals.css lines 77-80 (OKLCh success/warning/destructive tokens)
    - apps/web/src/components/contractors/classification/outcome/verdict-banner.tsx (semantic triad pattern)
    - apps/web/src/components/ui/skeleton.tsx (shadcn skeleton for loading states)
    - .planning/phases/60-classification-polish/60-UI-SPEC.md §"CLASS-10 — Compliance Health Dashboard" + §Accessibility Contract + §Component Inventory
    - .planning/phases/60-classification-polish/60-RESEARCH.md §Pattern 8 + §Pattern 9 + §Pitfall 8 + §Pitfall 3
  </read_first>
  <behavior>
    - Test (coverage): seed 5 completed + 2 draft GB assessments on 7 active GB engagements → coverageByMarket({market:'GB'}) = `{ completed: 5, total: 7 }`. Drafts excluded per pitfall 8.
    - Test (risk-distribution GB): seed completed IR35 outcomes: 3 Outside, 2 Inside, 1 Undetermined → `{ counts: { safe:3, warning:1, critical:2 }, totalCompleted:6 }`.
    - Test (risk-distribution DE): seed completed DRV outcomes: 2 green, 3 amber, 1 red → `{ counts: { safe:2, warning:3, critical:1 }, totalCompleted:6 }`.
    - Test (overdue GB): only OPEN + ACKNOWLEDGED ReassessmentTriggers counted; RESOLVED + DISMISSED excluded.
    - Test (overdue DE): 12mo-old completed DE assessments counted.
    - Test (active alerts GB): count of OPEN + ACKNOWLEDGED triggers.
    - Test (active alerts DE): count of EconomicDependencyAlertState where currentBand IN {warning, critical} + count of Statusfeststellungsverfahren where validTo within next 90 days AND outcome IN (SELBSTANDIG, ABHANGIG).
    - Test (cross-org): user in Org A queries coverageByMarket → result contains only Org A data; Org B rows never appear.
    - Test (exportMarketCsv columns): CSV first row = `"Engagement ID","Contractor","Country",...` (header); data rows contain one per engagement with all 11 D-16 columns (DE-only columns empty-string for GB rows via market filter).
    - Test (exportMarketCsv formula neutralisation): contractor with name `=cmd|'/C calc'!A1` → CSV row contains `"'=cmd|'/C calc'!A1"` (leading `=` prefixed with `'` via escapeCsvField).
    - Test (exportMarketCsv TTL): returned `expiresInSeconds === 300`.
    - Test (exportMarketCsv UTF-8 BOM): first 3 bytes of signed URL content = `0xEF 0xBB 0xBF` (test via encodeCsvUtf8Bom output inspection).
    - Test (page render): renders GlobalHeader + 2 MarketCards with titles "UK — IR35" and "Germany — Scheinselbständigkeit".
    - Test (page render empty): zero active engagements → empty state per UI-SPEC copy.
    - Test (refresh button): click invalidates classificationDashboard queries; has aria-live='polite' region announcing "Dashboard data refreshed" on completion.
    - Test (download CSV): click triggers mutation, receives signed URL, initiates window.open or anchor click with the URL; button shows spinner during fetch (disabled state).
    - Test (risk-distribution tile): total=10 with counts {safe:5, warning:3, critical:2} → 3 divs with widths 50%/30%/20% (within ±0.1pp); role='img' + aria-label sums to 100%; each segment has Tooltip.
    - Test (a11y axe): dashboard page passes axe-core automated WCAG 2.2 AA checks; no colour-only indicators.
  </behavior>
  <action>
    1. Fill packages/api/src/routers/classification-dashboard.ts with full implementations of all 6 procedures + exportMarketCsv. Key implementation notes:
       - All queries use `ctx.db.*` (tenant-scoped — NOT prismaRaw — because dashboard runs in request context).
       - All ClassificationAssessment queries include `status: 'completed'` filter (pitfall 8).
       - For `coverageByMarket`: total = count of active ContractorAssignments where Contractor.countryCode === market; completed = count of distinct contractorAssignmentIds with at least one completed assessment for that countryCode.
       - For `riskDistributionByMarket`: fetch latest completed assessment per engagement (groupBy contractorAssignmentId + max completedAt); parse `outcome` JSONB via Zod type guards (IR35 outcome kind='IR35' verdict; DRV outcome kind='SCHEINSELBSTANDIGKEIT' riskLevel); map GB verdicts (outside→safe, undetermined→warning, inside→critical) and DE riskLevel (green→safe, amber→warning, red→critical).
       - For `overdueByMarket`:
         - GB: `ReassessmentTrigger.findMany({ where: { status: { in: ['OPEN','ACKNOWLEDGED'] } }, include: { contractorAssignment: { include: { contractor: { where: { countryCode: 'GB' } } } } } })`.
         - DE: ClassificationAssessments with countryCode='DE' + status='completed' where latest `completedAt < now - 12 months`.
       - For `activeAlertsByMarket`:
         - GB: count of overdueByMarket items (consistent).
         - DE: `EconomicDependencyAlertState.count({ where: { currentBand: { in: ['warning','critical'] } } })` + `Statusfeststellungsverfahren.count({ where: { validTo: { gte: now, lte: addDays(now, 90) }, outcome: { in: ['SELBSTANDIG','ABHANGIG'] } } })`.
       - For `globalHeader`: totalContractors = count of Contractors; totalActiveEngagements = count of ContractorAssignments where status=ACTIVE; lastScannedAt = max of (EconomicDependencyAlertState.lastScannedAt max) and (CronScanState.lastScanCompletedAt for 'classification-reassessment-triggers').
       - For `exportMarketCsv`:
         ```ts
         exportMarketCsv: contractorReadProcedure
           .input(z.object({ market: z.enum(['GB', 'DE']) }))
           .mutation(async ({ ctx, input }) => {
             const rows = await buildDashboardRows(ctx, input.market);
             const header = [
               'Engagement ID','Contractor','Country','Latest verdict','Assessment completed',
               'DRV score','Economic-dep band','Billing share',
               'Open reassessment trigger?','DRV outcome','DRV valid until',
             ];
             const csvText = [
               header.map(escapeCsvField).join(','),
               ...rows.map(r => [
                 r.engagementId, r.contractorName, r.country, r.latestVerdict ?? '',
                 r.latestCompletedAt ? r.latestCompletedAt.toISOString().slice(0,10) : '',
                 r.latestScore ?? '',
                 r.economicBand ?? '',
                 r.billingShare ?? '',
                 r.openTrigger ? 'Y' : 'N',
                 r.drvOutcome ?? '',
                 r.drvValidTo ? r.drvValidTo.toISOString().slice(0,10) : '',
               ].map(escapeCsvField).join(',')),
             ].join('\r\n');
             const buf = encodeCsvUtf8Bom(csvText);  // or whatever the current API is; match existing signature
             const key = `classification-dashboard-exports/${ctx.organizationId}/${input.market}-${new Date().toISOString().replace(/[:.]/g,'-')}.csv`;
             const { signedUrl, expiresInSeconds } = await putObjectAndSignDownload({
               key, body: buf, contentType: 'text/csv; charset=utf-8', ttlSeconds: 300,
               contentDisposition: `attachment; filename="classification-${input.market}-${new Date().toISOString().slice(0,10)}.csv"`,
             });
             return { url: signedUrl, expiresInSeconds };
           }),
         ```
       - `buildDashboardRows` is a helper that joins all 4 Phase-60 models for the given market; keep DE-only fields empty string for GB rows.
    2. Fill packages/api/src/routers/__tests__/classification-dashboard.test.ts with real tests replacing describe.todo stubs — minimum 14 tests covering every behaviour above.
    3. Add to apps/web/messages/en.json under `Classification.polish.dashboard`:
       ```json
       "dashboard": {
         "pageH1": "Classification health",
         "pageSubline": "Live view of IR35 and Scheinselbständigkeit posture across all engagements.",
         "refreshButton": "Refresh data",
         "refreshAnnouncement": "Dashboard data refreshed",
         "downloadCsv": "Download CSV",
         "lastScanned": "Last scanned {relativeTime} ago",
         "totalContractors": "{N} contractors",
         "totalActiveEngagements": "{N} active engagements",
         "gbCardTitle": "UK — IR35",
         "deCardTitle": "Germany — Scheinselbständigkeit",
         "coverageTitle": "Assessment coverage",
         "coverageBody": "{N} of {M} active engagements have a completed assessment.",
         "riskDistributionTitle": "Risk distribution",
         "riskDistributionTooltipHint": "Hover a segment for raw counts and percentages.",
         "overdueTitle": "Overdue reassessments",
         "alertsGbTitle": "Open reassessment triggers",
         "alertsDeTitle": "Active alerts",
         "emptyNoEngagementsHeading": "No active engagements",
         "emptyNoEngagementsBody": "Add a contractor and create an engagement to populate the compliance dashboard.",
         "emptyNoAssessmentsHeading": "No completed assessments yet",
         "emptyNoAssessmentsBody": "Run an IR35 or Scheinselbständigkeit assessment from any engagement to populate this view."
       }
       ```
       Mirror to de.json, pl.json, ar.json.
    4. Create the 7 React components per UI-SPEC §Component Inventory:
       - `market-card.tsx` — server component; accepts `{ market: 'GB'|'DE' }`; renders shadcn `Card` with title + 4 tile children.
       - `coverage-tile.tsx` — KPI numeral (text-3xl semibold, accent colour when ≥80% healthy per UI-SPEC) + body text + shadcn `progress` bar.
       - `risk-distribution-tile.tsx` — **native-flex stacked bar** exactly per RESEARCH §Pattern 9 code sample; `bg-[--success]` / `bg-[--warning]` / `bg-[--destructive]`; shadcn `Tooltip` on each segment; `role='img'` + computed aria-label with counts + percentages.
       - `overdue-reassessments-tile.tsx` — KPI numeral + inline list of up to 5 items; "Show more" link to filtered engagement listing.
       - `active-alerts-tile.tsx` — dispatches on market: GB renders trigger count; DE renders warning/critical band counts + DRV expiring ≤90d count; chip list.
       - `refresh-dashboard-button.tsx` — shadcn `button` + lucide `RotateCw`; calls `trpc.useUtils().classificationDashboard.invalidate()`; aria-live='polite' region for announcement; spinner for ≥500ms minimum to avoid jank per UI-SPEC.
       - `download-csv-button.tsx` — shadcn `button` + lucide `Download`; per-market prop; calls exportMarketCsv mutation; opens signed URL on success; disabled + spinner during mutation.
       All components use shadcn primitives + lucide icons + OKLCh tokens — NO new dependencies.
    5. Create apps/web/src/app/[locale]/(dashboard)/classification/page.tsx as a server component:
       - Fetches globalHeader via tRPC SSR helper.
       - Renders Breadcrumb → Page H1 (Bricolage Grotesque) → Subline → Global header (3 KPIs) → Refresh button (client island) → two MarketCards stacked vertically (32px gap per UI-SPEC).
       - Layout: `flex flex-col gap-8 max-w-5xl mx-auto py-12`.
       - No tabs (UI-SPEC D-13 — users see both markets at once).
    6. Fill apps/web/src/app/[locale]/(dashboard)/classification/__tests__/page.test.tsx + __tests__/a11y.test.tsx with real tests replacing describe.todo stubs. Include axe-core assertion for the page per VALIDATION.md 60-04-08.
    7. Fill apps/web/src/components/contractors/classification/dashboard/__tests__/risk-distribution-tile.test.tsx and __tests__/market-card.test.tsx with real tests.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- classification-dashboard && pnpm --filter @contractor-ops/web test -- classification-dashboard risk-distribution-tile market-card classification/page classification/a11y</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "coverageByMarket|riskDistributionByMarket|overdueByMarket|activeAlertsByMarket|globalHeader|exportMarketCsv" packages/api/src/routers/classification-dashboard.ts` returns ≥6 matches to fully-implemented procedures (not stubs — bodies contain Prisma calls).
    - `grep -cE "status: ['\"]completed['\"]" packages/api/src/routers/classification-dashboard.ts` returns ≥2 (pitfall 8 filter in coverage + riskDistribution).
    - `grep -n "ttlSeconds: 300" packages/api/src/routers/classification-dashboard.ts` returns 1 match.
    - `grep -n "encodeCsvUtf8Bom\\|escapeCsvField" packages/api/src/routers/classification-dashboard.ts` returns ≥2 matches.
    - `grep -n "putObjectAndSignDownload" packages/api/src/routers/classification-dashboard.ts` returns ≥1 match.
    - `grep -n "bg-\\[--success\\]\\|bg-\\[--warning\\]\\|bg-\\[--destructive\\]" apps/web/src/components/contractors/classification/dashboard/risk-distribution-tile.tsx` returns ≥3 matches.
    - `grep -n "role=['\"]img['\"]\\|aria-label" apps/web/src/components/contractors/classification/dashboard/risk-distribution-tile.tsx` returns ≥2 matches.
    - `grep -n "aria-live=['\"]polite['\"]" apps/web/src/components/contractors/classification/dashboard/refresh-dashboard-button.tsx` returns ≥1 match.
    - `grep -nE "MarketCard|CoverageTile|RiskDistributionTile|OverdueReassessmentsTile|ActiveAlertsTile|RefreshDashboardButton|DownloadCsvButton" apps/web/src/app/[locale]/(dashboard)/classification/page.tsx` returns ≥5 matches (page composes the components).
    - `grep -cE "console\\.(log|warn|error|info|debug)" packages/api/src/routers/classification-dashboard.ts apps/web/src/app/[locale]/(dashboard)/classification apps/web/src/components/contractors/classification/dashboard` returns 0.
    - `grep -cE "Recharts|Tremor|Victory|chart\\.js|d3" packages/api/src/routers/classification-dashboard.ts apps/web/src/components/contractors/classification/dashboard` returns 0 (UI-SPEC registry safety — no chart lib).
    - `grep -c "pageH1\\|coverageTitle\\|riskDistributionTitle\\|downloadCsv" apps/web/messages/en.json apps/web/messages/de.json apps/web/messages/pl.json apps/web/messages/ar.json` returns ≥16 matches (4 keys × 4 locales).
    - `pnpm --filter @contractor-ops/api test -- classification-dashboard` ≥14 tests green covering VALIDATION.md rows 60-04-01..06 (including csv-sanitization with `=cmd|'/C calc'!A1` case).
    - `pnpm --filter @contractor-ops/web test -- classification-dashboard risk-distribution-tile market-card` ≥6 tests green covering 60-04-07 + 60-04-08.
    - `pnpm --filter @contractor-ops/web test -- classification/a11y` includes axe-core assertion green.
  </acceptance_criteria>
  <done>All 8 router procedures implemented; CSV export neutralises formula prefixes (security gap A11 closed); dashboard page + 7 components shipped using native flex stacked bar; no chart library added; a11y automated (axe) green; VALIDATION.md rows 60-04-* green; nyquist_compliant can flip once all plans land.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tRPC client → classificationDashboard.* | contractor:read gate + tenantProcedure (multi-tenant isolation) |
| exportMarketCsv → R2 bucket via putObjectAndSignDownload | Signed URL TTL 300s; per-org key prefix enforces tenant isolation |
| CSV file → user spreadsheet app (Excel/Numbers/LibreOffice) | User-entered data (contractor names, DRV references) round-trips to spreadsheet — CSV injection surface |
| RSC page render → tRPC query | Server component fetches with ctx.organizationId from session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-15 | I (Information Disclosure) | CSV formula injection via contractor name | HIGH | mitigate | escapeCsvField extended to prefix `=`/`+`/`-`/`@` with single quote per OWASP (research gap A11). Unit test 60-04-05 asserts `=cmd|'/C calc'!A1` becomes `'=cmd|'/C calc'!A1`. Applies to ALL user-entered string fields in CSV (contractor name, DRV reference if we ever added it — not in D-16 column set but defence in depth). |
| T-60-16 | I (Information Disclosure) | Cross-tenant dashboard leak | HIGH | mitigate | All queries go through tenantProcedure + requirePermission({ contractor: ['read'] }); integration test 60-04 asserts Org A query yields only Org A rows. |
| T-60-17 | T (Tampering) | Signed-URL reuse / shared token | MEDIUM | accept | TTL=300s; signed URL is unique per request (timestamp in object key); attacker with a captured URL has ≤5min access to a tenant-specific file. Accept; rotate by extending TTL if needed. |
| T-60-18 | D (Denial of Service) | Expensive dashboard query on org with thousands of engagements | LOW | accept | CONTEXT.md D-15 accepts low-hundreds-per-org target; materialised-view option deferred. Add pagination via `take: 1000` on detail queries as defensive ceiling. |
| T-60-19 | S (Spoofing) | CSRF on exportMarketCsv | LOW | mitigate | Inherited — tRPC mutations require tRPC-next auth context; no bare POST endpoint. |
| T-60-20 | E (Elevation of Privilege) | Dashboard bypasses RBAC via direct tRPC call | HIGH | mitigate | requirePermission({ contractor: ['read'] }) on every procedure in classificationDashboard router. |
| T-60-21 | I (Information Disclosure) | draft assessments in dashboard | LOW | mitigate | pitfall 8 — all queries filter `status: 'completed'`. Drafts carry unfinished data that should not inform compliance aggregates; dedicated test asserts drafts excluded. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- csv classification-dashboard` ≥20 tests green (existing + new).
- `pnpm --filter @contractor-ops/web test -- classification-dashboard risk-distribution-tile market-card classification/page classification/a11y` ≥8 tests green including axe-core.
- Manual: seed 10 GB engagements (5 with completed IR35 assessments: 3 Outside, 1 Inside, 1 Undetermined) + 10 DE engagements (6 with completed DRV: 3 green, 2 amber, 1 red). Open `/en/classification` — verify GB card shows 50% coverage + stacked bar 60/20/20 + DE card shows 60% coverage + stacked bar 50/33/17.
- Manual: seed a contractor named `=HYPERLINK("http://evil.com","click")` and export CSV. Open in Excel/LibreOffice — cell renders as literal text, NO hyperlink execution.
- Manual: verify `Download CSV` button fetches signed URL + downloads file; second download within 5min still works; after 5+ min, URL returns 403 (R2 TTL expired).
- Manual (a11y): tab-navigate through page — focus order: global header → market-1 tiles → market-1 CTAs → market-2 tiles → market-2 CTAs. Refresh click announces via screen reader.
</verification>

<success_criteria>
- escapeCsvField neutralises `=`, `+`, `-`, `@` prefixes (security gap A11 closed with tests)
- classificationDashboard router: 6 read procedures + exportMarketCsv mutation, all contractor:read gated
- All queries filter status='completed' (pitfall 8)
- CSV export: 11 columns per D-16, UTF-8 BOM, 300s TTL, escapeCsvField applied
- Dashboard page: 2 market cards stacked, 4 tiles each, global header, refresh + CSV buttons
- Risk-distribution tile: native flex + OKLCh tokens + tooltip + role='img' + aria-label (no chart lib)
- aria-live='polite' on refresh button; axe-core WCAG 2.2 AA green
- i18n Classification.polish.dashboard.* in 4 locales
- Zero console.* in new source
- Zero new npm dependencies introduced
- VALIDATION.md rows 60-04-01..08 all green — `nyquist_compliant: true` can flip once all 4 plans land
</success_criteria>

<output>
After completion, create `.planning/phases/60-classification-polish/60-04-SUMMARY.md`:
- Security gap A11 (CSV formula injection) closed via escapeCsvField extension
- Needs verification by legal entity before production deploy: none new from this plan (dashboard chrome only; no legal text)
- Manual-Only Verifications:
  1. Excel/LibreOffice open-test for formula neutralisation
  2. Visual design check: stacked-bar colour match with Phase 58 outcome pill palette (UI-SPEC)
  3. Cron schedule slot coordination for `0 2 * * *` + `0 3 * * *` UTC (from Plans 60-01 + 60-02 — surface here as phase-level ops handoff item)
- Performance: dashboard queries cost bounded per CONTEXT.md D-15; materialised snapshot deferred per research.
- Phase 60 closure: all 4 plans land → mark all CLASS-07/08/09/10 requirements Complete in REQUIREMENTS.md traceability table; flip VALIDATION.md `nyquist_compliant: true` after `pnpm test` full suite green.
- Test count green: ≥ 22 for this plan (router + page + components + a11y).
</output>
