---
phase: 62-zugferd-e-invoicing
plan: 07
type: execute
wave: 6
depends_on: ["06"]
files_modified:
  - apps/web/e2e/functional/intake-upload-flow.spec.ts
  - apps/web/e2e/functional/zugferd-download-flow.spec.ts
  - apps/web/e2e/fixtures/intake/comfort-minimal.pdf.base64
  - apps/web/e2e/fixtures/intake/malformed.xml
  - apps/web/e2e/fixtures/intake/xrechnung-with-warnings.xml
  - apps/web/e2e/fixtures/intake/README.md
autonomous: true
requirements: [EINV-02, EINV-03]
user_setup: []

must_haves:
  truths:
    - "Playwright E2E covers the full inbound flow: upload PDF → list → detail → acknowledge validation → confirm match → convert → Invoice row exists"
    - "Playwright E2E covers the full outbound flow: open invoice → click Download ZUGFeRD PDF → file downloads → second click returns cached URL"
    - "Playwright E2E covers the flag-off path: /invoices/intake returns 404"
    - "All e2e specs run via `pnpm test:e2e --grep 'intake|zugferd'` in under 120 seconds"
  artifacts:
    - path: apps/web/e2e/functional/intake-upload-flow.spec.ts
      provides: "End-to-end inbound intake flow"
      min_lines: 100
    - path: apps/web/e2e/functional/zugferd-download-flow.spec.ts
      provides: "End-to-end outbound ZUGFeRD download flow"
      min_lines: 60
  key_links:
    - from: apps/web/e2e/functional/intake-upload-flow.spec.ts
      to: apps/web/src/app/[locale]/(dashboard)/invoices/intake/page.tsx
      via: "Navigates the intake list + detail UI"
      pattern: "/invoices/intake"
    - from: apps/web/e2e/functional/zugferd-download-flow.spec.ts
      to: apps/web/src/components/invoices/einvoice-tab/download-zugferd-pdf-button.tsx
      via: "Clicks the Download ZUGFeRD PDF button"
      pattern: "Download ZUGFeRD PDF"
---

<objective>
Ship end-to-end Playwright coverage for both Phase 62 requirements, using deterministic fixtures.

Purpose: Unit + integration tests in Plans 02–06 verify each layer. This plan verifies the *entire stack* behaves correctly as a user flow — the only way to catch integration seams (client ↔ tRPC ↔ service ↔ Prisma ↔ R2). It also locks in the user-observable contract against future regressions.

Output: Two deterministic e2e specs + 3 fixtures under `apps/web/e2e/fixtures/intake/`.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-zugferd-e-invoicing/62-CONTEXT.md
@.planning/phases/62-zugferd-e-invoicing/62-UI-SPEC.md
@apps/web/e2e/functional/billing-flow.spec.ts
@apps/web/e2e/functional/invoice-einvoice-flow.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare deterministic e2e fixtures</name>
  <files>apps/web/e2e/fixtures/intake/comfort-minimal.pdf.base64, apps/web/e2e/fixtures/intake/malformed.xml, apps/web/e2e/fixtures/intake/xrechnung-with-warnings.xml, apps/web/e2e/fixtures/intake/README.md</files>
  <read_first>packages/einvoice/scripts/generate-zugferd-fixtures.ts, packages/einvoice/src/profiles/zugferd-de/__fixtures__/comfort-minimal.json, apps/web/e2e/functional/invoice-einvoice-flow.spec.ts</read_first>
  <action>
Produce three fixtures under `apps/web/e2e/fixtures/intake/`:

1. **`comfort-minimal.pdf.base64`** — base64-encoded ZUGFeRD COMFORT PDF. Generate at plan-execution time by running `pnpm --filter @contractor-ops/einvoice exec tsx scripts/generate-zugferd-fixtures.ts --out-dir /tmp/zfe2e && base64 -w0 /tmp/zfe2e/comfort-minimal.pdf > apps/web/e2e/fixtures/intake/comfort-minimal.pdf.base64`. Commit the base64 text — NOT a binary file — to keep diffs tractable and ensure deterministic builds (the generator uses pinned dates + fixtures, so output is byte-stable).

2. **`malformed.xml`** — a literal file:
   ```xml
   <?xml version="1.0"?>
   <not-closed-properly>
   ```
   One line; no closing tag. Used to verify the CII_PARSE_FAILED path.

3. **`xrechnung-with-warnings.xml`** — a real CII XML that passes XSD but triggers a KoSIT schematron warning (e.g., the BT-10 missing case from `packages/einvoice/src/profiles/xrechnung-de/__tests__/fixtures/kosit-negative-missing-bt10.xml`). Copy that fixture here verbatim.

4. **`README.md`** — one-pager documenting how each fixture was generated, the sha256, and how to regenerate.

Add a line in `.gitattributes` or similar if the project excludes `*.base64` from text-lint — verify the file is committable as-is.
  </action>
  <verify>test -f apps/web/e2e/fixtures/intake/comfort-minimal.pdf.base64 && test -s apps/web/e2e/fixtures/intake/comfort-minimal.pdf.base64 && grep -q "not-closed-properly" apps/web/e2e/fixtures/intake/malformed.xml</verify>
  <acceptance_criteria>
    - apps/web/e2e/fixtures/intake/comfort-minimal.pdf.base64 exists and size > 10000 bytes
    - File decodes to a valid PDF (assert `echo "$(head -c 100 file)" | base64 -d | head -c 4` starts with `%PDF`)
    - malformed.xml exists and contains `<not-closed-properly>`
    - xrechnung-with-warnings.xml exists and is valid XML (passes `xmllint --noout` if available; otherwise non-empty)
    - README.md documents the sha256 of each fixture
  </acceptance_criteria>
  <done>Deterministic fixtures committed.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright spec — intake-upload-flow (full inbound roundtrip)</name>
  <files>apps/web/e2e/functional/intake-upload-flow.spec.ts</files>
  <read_first>apps/web/e2e/functional/invoice-einvoice-flow.spec.ts, apps/web/e2e/functional/billing-flow.spec.ts, apps/web/playwright.config.ts, .planning/phases/62-zugferd-e-invoicing/62-UI-SPEC.md</read_first>
  <action>
Write `intake-upload-flow.spec.ts` using the project's existing Playwright harness + `test.describe` conventions (grep the existing specs for the `login`, `seedOrg`, `enableFlag` helpers — reuse verbatim).

Scenarios (`test.describe('Phase 62 inbound intake flow')`):

1. **Happy path — ZUGFeRD PDF with valid XRechnung**
   - Login + seed org + seed a Contractor matching the fixture supplier (VAT ID exact match).
   - Enable `EINVOICE_IMPORT_ENABLED` for the org.
   - Navigate to `/en/invoices`.
   - Open split-button menu, click "Import e-invoice".
   - In the Dialog, upload `comfort-minimal.pdf.base64` (decode and drop).
   - Expect `sonner` toast "Imported — review at /invoices/intake/…".
   - Expect URL now matches `/en/invoices/intake/[cuid]`.
   - On detail page: assert PDF preview pane rendered, parsed fields pane shows the supplier name from fixture, Status pill is "Parsed", Validation pill is "Valid", Profile level badge is "COMFORT".
   - Match pane shows the seeded Contractor pre-selected with reason "VAT ID match".
   - Click "Confirm match". Status pill flips to "Matched". Actions bar "Convert to invoice" becomes enabled.
   - Click "Convert to invoice". URL navigates to `/en/invoices/[newInvoiceId]`.
   - Assert new Invoice row visible in the invoices list.

2. **Dedup path**
   - Repeat the upload with the same PDF. Expect toast "Already imported — opening existing intake" and URL jumps to the *existing* intake id (not a new one).

3. **Hard-reject — malformed XML**
   - Upload `malformed.xml`. Expect the Dialog stays open; inline error shows the `errorFileTooLarge` / `CII_PARSE_FAILED` copy; "Try another file" button visible. No navigation.

4. **Soft-gate — schematron warning**
   - Upload `xrechnung-with-warnings.xml`. Navigate to its detail page. Validation pill is "Warnings". Convert button is disabled with tooltip "Acknowledge validation issues first".
   - Click "Accept despite issues". Validation banner appears noting acceptance. Convert button becomes enabled (if match exists).

5. **Flag-off gate**
   - Disable `EINVOICE_IMPORT_ENABLED`. Navigate to `/en/invoices/intake`. Expect 404 page.
   - Split-button on `/en/invoices` shows only "+ New invoice" (no dropdown menu).

6. **Reject flow**
   - On any intake row, click "Reject import". AlertDialog opens. Type reason "duplicate from sender". Click "Reject import" (destructive). Status pill flips to "Rejected". Convert button no longer rendered.

Use `test.step()` blocks liberally for readability. Avoid `page.waitForTimeout(ms)` — use `expect(locator).toBeVisible()` with auto-retry instead.

Use Pino via the project's e2e logger helper for any diagnostic output (grep existing specs for how they log — likely `test.info().annotations.push`). NEVER `console.log`.
  </action>
  <verify>pnpm test:e2e -- --grep "Phase 62 inbound intake flow"</verify>
  <acceptance_criteria>
    - intake-upload-flow.spec.ts contains `test.describe('Phase 62 inbound intake flow'`
    - Contains all 6 scenario names above (grep literally)
    - No `console.log` / `console.*` call
    - No `page.waitForTimeout(` call
    - All 6 scenarios pass
  </acceptance_criteria>
  <done>Full inbound flow tested end-to-end with real service + Prisma.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright spec — zugferd-download-flow (outbound)</name>
  <files>apps/web/e2e/functional/zugferd-download-flow.spec.ts</files>
  <read_first>apps/web/e2e/functional/invoice-einvoice-flow.spec.ts, apps/web/src/components/invoices/einvoice-tab/download-zugferd-pdf-button.tsx, apps/web/playwright.config.ts</read_first>
  <action>
Write `zugferd-download-flow.spec.ts`:

Scenarios (`test.describe('Phase 62 outbound ZUGFeRD download')`):

1. **First generation**
   - Login + seed an org + seed an Invoice with finalised state (reuse existing test helpers).
   - Navigate to `/en/invoices/[invoiceId]`. Open the e-invoice tab.
   - Scroll to the "ZUGFeRD" section. Assert "Not yet generated" copy visible. Assert button labelled "Download ZUGFeRD PDF" is visible and enabled.
   - Set up a download listener: `const [download] = await Promise.all([page.waitForEvent('download'), button.click()])`.
   - Assert `download.suggestedFilename()` matches `/invoice-.*-zugferd\.pdf/`.
   - Save to tmp dir. Assert downloaded file starts with `%PDF-` (magic bytes).
   - Reload the invoice page. Assert the "ZUGFeRD" section now shows "Generated on {date}".

2. **Idempotent re-download**
   - On the same invoice, click "Download ZUGFeRD PDF" again. Assert another download triggers.
   - Assert the server did NOT create a second `EInvoiceLifecycleEvent` of type `ZUGFERD_GENERATED` (query Prisma directly or via a test-only tRPC helper).

3. **Generation failure toast**
   - Use the project's existing request-interception pattern to force the tRPC call to fail with a `ZUGFERD_WRAPPING_FAILED` error (intercept the POST and respond 500 with a tRPC-shaped error body).
   - Click the button. Assert the error toast with copy `EInvoice.intake.genFailureToast` appears. Button returns to enabled state.
   - Remove interception. Click again — succeeds.

Reuse the project's Playwright harness helpers (login, seedInvoice, interceptTrpc) — grep existing specs for the exact names.
  </action>
  <verify>pnpm test:e2e -- --grep "Phase 62 outbound ZUGFeRD download"</verify>
  <acceptance_criteria>
    - zugferd-download-flow.spec.ts contains `test.describe('Phase 62 outbound ZUGFeRD download'`
    - File contains `page.waitForEvent('download')`
    - File asserts `%PDF-` magic bytes
    - File contains an intercept/mock test for the failure path
    - No `console.log` or `page.waitForTimeout`
    - All 3 scenarios pass
  </acceptance_criteria>
  <done>Outbound flow verified end-to-end including the idempotent + failure-toast paths.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm test:e2e -- --grep "Phase 62"` exits 0 with 9 scenarios green (6 inbound + 3 outbound)
- [ ] All 3 fixtures exist and pass their sanity checks
- [ ] No `console.*` and no `page.waitForTimeout` anywhere in new spec files
- [ ] Specs collectively complete in under 120 seconds (check `--reporter=list` wall time)
</verification>

<success_criteria>
- Two e2e spec files committed
- Three deterministic fixtures committed
- 9 scenarios green
- Both EINV-02 + EINV-03 success criteria demonstrably satisfied end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/62-zugferd-e-invoicing/62-07-SUMMARY.md`
</output>
