---
phase: 62-zugferd-e-invoicing
plan: 06
type: execute
wave: 5
depends_on: ["05"]
files_modified:
  - packages/feature-flags/src/registry.ts
  - apps/web/messages/en.json
  - apps/web/messages/de.json
  - apps/web/messages/gb.json
  - packages/validators/src/legal/de.ts
  - apps/web/src/components/invoices/intake/intake-status-pill.tsx
  - apps/web/src/components/invoices/intake/intake-validation-status-pill.tsx
  - apps/web/src/components/invoices/intake/intake-profile-level-badge.tsx
  - apps/web/src/components/invoices/intake/intake-filter-chips.tsx
  - apps/web/src/components/invoices/intake/intake-list.tsx
  - apps/web/src/components/invoices/intake/intake-upload-dialog.tsx
  - apps/web/src/components/invoices/intake/intake-detail-pdf-pane.tsx
  - apps/web/src/components/invoices/intake/intake-detail-fields-pane.tsx
  - apps/web/src/components/invoices/intake/intake-detail-validation-pane.tsx
  - apps/web/src/components/invoices/intake/intake-detail-match-pane.tsx
  - apps/web/src/components/invoices/intake/intake-detail-actions-bar.tsx
  - apps/web/src/components/invoices/intake/import-split-button.tsx
  - apps/web/src/components/invoices/intake/__tests__/intake-upload-dialog.test.tsx
  - apps/web/src/components/invoices/intake/__tests__/intake-status-pill.test.tsx
  - apps/web/src/components/invoices/intake/__tests__/intake-filter-chips.test.tsx
  - apps/web/src/components/invoices/intake/__tests__/intake-detail-actions-bar.test.tsx
  - apps/web/src/components/invoices/einvoice-tab/download-zugferd-pdf-button.tsx
  - apps/web/src/components/invoices/einvoice-tab/__tests__/download-zugferd-pdf-button.test.tsx
  - apps/web/src/components/invoices/einvoice-tab/einvoice-tab.tsx
  - apps/web/src/app/[locale]/(dashboard)/invoices/page.tsx
  - apps/web/src/app/[locale]/(dashboard)/invoices/intake/page.tsx
  - apps/web/src/app/[locale]/(dashboard)/invoices/intake/[id]/page.tsx
  - apps/web/src/app/[locale]/(dashboard)/invoices/intake/__tests__/page.test.tsx
  - apps/web/src/app/[locale]/(dashboard)/invoices/intake/[id]/__tests__/page.test.tsx
  - apps/web/src/components/layout/sidebar.tsx
autonomous: true
requirements: [EINV-02, EINV-03]
user_setup: []

must_haves:
  truths:
    - "Users can import an XRechnung XML or ZUGFeRD PDF from the /invoices page via a split-button"
    - "The /invoices/intake list shows all intakes with status filter chips and a feature-flag-gated 'Imports' sidebar entry"
    - "The /invoices/intake/[id] detail page renders the PDF preview, parsed fields, validation status, match candidates, and action bar"
    - "Users can click 'Download ZUGFeRD PDF' on an existing invoice's e-invoice tab to generate and download the PDF"
    - "All new UI strings ship in EN + DE + GB locales; DE statutory copy is locked via packages/validators/src/legal/de.ts"
    - "The Imports sidebar entry, the split-button 'Import e-invoice' item, and the /intake routes are all hidden / 404 when feature flag EINVOICE_IMPORT_ENABLED is off"
  artifacts:
    - path: apps/web/src/app/[locale]/(dashboard)/invoices/intake/page.tsx
      provides: "Intake list page with filter chips, empty state, pagination"
      min_lines: 40
    - path: apps/web/src/app/[locale]/(dashboard)/invoices/intake/[id]/page.tsx
      provides: "Intake detail with all 5 panes + actions bar"
      min_lines: 60
    - path: apps/web/src/components/invoices/intake/intake-upload-dialog.tsx
      provides: "Drop-zone dialog with error and loading states"
      min_lines: 80
    - path: apps/web/src/components/invoices/einvoice-tab/download-zugferd-pdf-button.tsx
      provides: "Button that calls generateZugferdPdf mutation and triggers download"
      min_lines: 40
    - path: packages/feature-flags/src/registry.ts
      provides: "EINVOICE_IMPORT_ENABLED flag registration"
      contains: "EINVOICE_IMPORT_ENABLED"
  key_links:
    - from: apps/web/src/app/[locale]/(dashboard)/invoices/intake/page.tsx
      to: packages/api/src/routers/invoice-intake.ts
      via: "trpc.invoiceIntake.listByOrg.useQuery"
      pattern: "invoiceIntake\\.listByOrg"
    - from: apps/web/src/components/invoices/einvoice-tab/download-zugferd-pdf-button.tsx
      to: packages/api/src/routers/einvoice.ts
      via: "trpc.einvoice.generateZugferdPdf.useMutation"
      pattern: "generateZugferdPdf"
    - from: apps/web/src/components/layout/sidebar.tsx
      to: packages/feature-flags/src/registry.ts
      via: "Feature flag check EINVOICE_IMPORT_ENABLED"
      pattern: "EINVOICE_IMPORT_ENABLED"
---

<objective>
Deliver every user-facing surface for Phase 62: the outbound "Download ZUGFeRD PDF" button, the inbound split-button + drop-zone dialog, the intake list and detail pages, the sidebar nav entry, and all locale strings — strictly honouring 62-UI-SPEC.md (no new design primitives; tokens reused verbatim; DE statutory copy locked).

Purpose: The code written in Plans 01–05 is invisible to users until this plan ships. Every surface here must be accessible, internationalised, flag-gated (inbound only), and visually coherent with the "Precision Craft" design system.

Output: Complete, tested, a11y-checked UI surfaces plus feature-flag registration and a 3-locale messages update.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62-zugferd-e-invoicing/62-CONTEXT.md
@.planning/phases/62-zugferd-e-invoicing/62-UI-SPEC.md
@apps/web/src/components/invoices/einvoice-tab/einvoice-tab.tsx
@apps/web/src/components/invoices/einvoice-status-cell.tsx
@apps/web/src/app/[locale]/(dashboard)/invoices/page.tsx
@apps/web/src/components/ui/button.tsx
@apps/web/src/components/ui/dialog.tsx
@apps/web/src/components/ui/alert-dialog.tsx
@packages/feature-flags/src/registry.ts
@packages/validators/src/legal/de.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register EINVOICE_IMPORT_ENABLED feature flag</name>
  <files>packages/feature-flags/src/registry.ts</files>
  <read_first>packages/feature-flags/src/registry.ts, packages/feature-flags/src/, .claude/get-shit-done/references/ already-referenced memory note on feature flags</read_first>
  <action>
Locate the project's existing feature-flag registry — grep for a typed map of flag names (per MEMORY.md `feature_flags_strategy`: "tiny typed registry wraps SDK"). If the package is `packages/feature-flags/src/registry.ts`, append a new entry:

```ts
export const EINVOICE_IMPORT_ENABLED = {
  key: "EINVOICE_IMPORT_ENABLED",
  defaultValue: false,
  description: "Enables inbound invoice intake: XRechnung XML + ZUGFeRD PDF upload, intake list/detail routes, and the sidebar Imports entry. Outbound ZUGFeRD PDF generation is NOT flag-gated.",
  jurisdictions: ["EU-DE", "EU"],
} as const;
```

Append to whatever shape the existing registry uses (array / object map / union). If the registry uses a type-level union of flag keys, add `EINVOICE_IMPORT_ENABLED` to that union.

Export it from the package's public API.

If the project has no existing flag registry helper (unlikely given MEMORY.md), create a minimal one:
```ts
// packages/feature-flags/src/registry.ts
export type FlagRegistryEntry = { key: string; defaultValue: boolean; description: string; jurisdictions: readonly string[] };
export const FLAGS = { EINVOICE_IMPORT_ENABLED: { ... } } as const;
export type FlagKey = keyof typeof FLAGS;
```
— but prefer extending whatever exists.
  </action>
  <verify>grep -q "EINVOICE_IMPORT_ENABLED" packages/feature-flags/src/registry.ts && pnpm --filter @contractor-ops/feature-flags exec tsc --noEmit</verify>
  <acceptance_criteria>
    - packages/feature-flags/src/registry.ts contains the exact string `EINVOICE_IMPORT_ENABLED`
    - Entry has `defaultValue: false`
    - Entry description mentions both "XRechnung" and "ZUGFeRD"
    - Entry description explicitly notes outbound generation is NOT flagged
    - tsc --noEmit passes in that package
  </acceptance_criteria>
  <done>Flag is registered and consumable from the web app.</done>
</task>

<task type="auto">
  <name>Task 2: Extend locale messages + locked DE legal phrases</name>
  <files>apps/web/messages/en.json, apps/web/messages/de.json, apps/web/messages/gb.json, packages/validators/src/legal/de.ts</files>
  <read_first>apps/web/messages/en.json, apps/web/messages/de.json, apps/web/messages/gb.json, packages/validators/src/legal/de.ts, .planning/phases/62-zugferd-e-invoicing/62-UI-SPEC.md</read_first>
  <action>
Add every string from 62-UI-SPEC.md § "Copywriting Contract" under an `EInvoice.intake` sub-namespace (or extend the existing `EInvoice` namespace). Use exactly the EN / DE / GB wording from the spec — do NOT paraphrase.

Example structure (adjust to the project's actual `EInvoice` namespace layout — grep first):

```json
{
  "EInvoice": {
    "intake": {
      "splitButtonImport": "Import e-invoice",
      "sidebarImports": "Imports",
      "pageTitle": "Invoice imports",
      "pageSubtitle": "Inbound XRechnung and ZUGFeRD invoices awaiting review",
      "uploadDialogTitle": "Import an e-invoice",
      "dropZonePrimary": "Drop an XRechnung XML or ZUGFeRD PDF here",
      "dropZoneSecondary": "or click to choose a file (max 5 MB, .xml or .pdf)",
      "ctaConvert": "Convert to invoice",
      "ctaConfirmMatch": "Confirm match",
      "ctaAcceptDespiteIssues": "Accept despite issues",
      "ctaCreateContractor": "Create new contractor from this data",
      "ctaDownloadZugferd": "Download ZUGFeRD PDF",
      "emptyStateHeading": "No imports yet",
      "emptyStateBody": "Inbound XRechnung XML and ZUGFeRD PDF files appear here. Use 'Import e-invoice' from the invoices page to upload one.",
      "errorXsdReject": "The XML does not conform to the CII schema — ask the sender to re-issue. First errors: {errors}",
      "errorNoXmlAttachment": "This PDF does not contain an embedded XRechnung/ZUGFeRD XML attachment. Only ZUGFeRD-conformant PDFs can be imported.",
      "errorLevelTooLow": "This invoice uses the {level} ZUGFeRD profile, which lacks line-item data. Ask the sender to provide a COMFORT or XRECHNUNG profile.",
      "errorFileTooLarge": "File exceeds 5 MB. Ask the sender for a smaller file.",
      "bannerExtendedBestEffort": "This invoice uses the EXTENDED ZUGFeRD profile. Some sender-specific fields could not be mapped. Review the data carefully before converting.",
      "tooltipConvertDisabledNeedsAck": "Acknowledge validation issues first",
      "tooltipConvertDisabledNeedsMatch": "Match a contractor before converting",
      "matchReasonVat": "VAT ID match",
      "matchReasonLeitweg": "Leitweg-ID match",
      "matchReasonExactName": "Exact name match",
      "matchReasonFuzzyName": "Fuzzy name match (distance {n})",
      "rejectDialogTitle": "Reject this import?",
      "rejectDialogBody": "Rejected imports remain in the audit log but cannot be converted to an invoice. This action cannot be undone.",
      "rejectDialogConfirm": "Reject import",
      "rejectReasonPlaceholder": "Reason (required) — e.g., duplicate, wrong recipient, sender mistake",
      "genFailureToast": "ZUGFeRD generation failed. Please try again or contact support if the problem persists.",
      "filter": { "all": "All", "needsReview": "Needs review", "matched": "Matched", "converted": "Converted", "rejected": "Rejected" },
      "status": { "PARSED": "Parsed", "NEEDS_REVIEW": "Needs review", "MATCHED": "Matched", "CONVERTED": "Converted", "REJECTED": "Rejected" },
      "validation": { "VALID": "Valid", "WARNINGS": "Warnings", "INVALID": "Invalid" },
      "level": { "COMFORT": "COMFORT", "XRECHNUNG": "XRECHNUNG", "EXTENDED": "EXTENDED" }
    }
  }
}
```

German translations go in `de.json` using the DE strings from 62-UI-SPEC.md § Copywriting Contract. GB strings (tiny deltas from EN — e.g., "E-rechnung" stays EN spelling) go in `gb.json`.

**Locked legal phrases:** in `packages/validators/src/legal/de.ts`, add:
```ts
export const EINVOICE_INTAKE_XSD_REJECT_DE = "Die XML entspricht nicht dem CII-Schema — bitten Sie den Absender, erneut auszustellen.";
export const EINVOICE_INTAKE_LEVEL_TOO_LOW_DE = "Diese Rechnung verwendet das ZUGFeRD-Profil {level}, dem die Positionsdaten fehlen. Bitten Sie den Absender um ein COMFORT- oder XRECHNUNG-Profil.";
```
Re-use these constants in `de.json` (reference the constant through a build-time codegen if the existing `legal/de.ts` has one; otherwise duplicate verbatim and add a parity test under `packages/validators/src/legal/__tests__/` asserting the DE JSON strings match these constants — follow the exact pattern already established in Phase 56 for locked phrases).
  </action>
  <verify>pnpm --filter @contractor-ops/validators test -- --run src/legal/__tests__/ && node -e "const en=require('./apps/web/messages/en.json'); if(!en.EInvoice?.intake?.ctaConvert) process.exit(1)"</verify>
  <acceptance_criteria>
    - apps/web/messages/en.json contains `"ctaConvert": "Convert to invoice"` (exact)
    - apps/web/messages/de.json contains `"ctaConvert": "In Rechnung übernehmen"` (exact)
    - apps/web/messages/gb.json contains `"ctaConvert": "Convert to invoice"` (exact)
    - packages/validators/src/legal/de.ts contains `EINVOICE_INTAKE_XSD_REJECT_DE`
    - Parity test between `legal/de.ts` constants and `messages/de.json` strings passes
    - All three JSON files parse successfully
  </acceptance_criteria>
  <done>3 locales + locked DE phrases in place; parity test green.</done>
</task>

<task type="auto">
  <name>Task 3: Build intake primitive components (status pill, validation pill, level badge, filter chips)</name>
  <files>apps/web/src/components/invoices/intake/intake-status-pill.tsx, apps/web/src/components/invoices/intake/intake-validation-status-pill.tsx, apps/web/src/components/invoices/intake/intake-profile-level-badge.tsx, apps/web/src/components/invoices/intake/intake-filter-chips.tsx, apps/web/src/components/invoices/intake/__tests__/intake-status-pill.test.tsx, apps/web/src/components/invoices/intake/__tests__/intake-filter-chips.test.tsx</files>
  <read_first>apps/web/src/components/invoices/einvoice-status-cell.tsx, apps/web/src/components/ui/badge.tsx, apps/web/src/components/contractors/classification/economic-dependency-alerts/band-chip.tsx, apps/web/src/components/contractors/vat-validation-status-pill.tsx, .planning/phases/62-zugferd-e-invoicing/62-UI-SPEC.md</read_first>
  <action>
Build the four small primitives as compositions of the shadcn `Badge` + lucide icons. Follow 62-UI-SPEC.md § Color exactly:

1. `intake-status-pill.tsx` — prop `status: InvoiceIntakeStatus`; uses the token map from 62-UI-SPEC.md Color section (PARSED → `bg-muted text-muted-foreground`, NEEDS_REVIEW → `bg-warning/10 text-warning`, etc.). Icon: `Clock`, `AlertTriangle`, `Link2`, `Check`, `X`. `aria-label={t(\`EInvoice.intake.status.\${status}\`)}`.

2. `intake-validation-status-pill.tsx` — prop `status: 'VALID' | 'WARNINGS' | 'INVALID'`; same icon pairing pattern.

3. `intake-profile-level-badge.tsx` — prop `level: 'COMFORT' | 'XRECHNUNG' | 'EXTENDED'`; COMFORT + XRECHNUNG use `bg-teal-100 text-teal-800`; EXTENDED uses `bg-warning/10 text-warning`.

4. `intake-filter-chips.tsx` — renders 5 button-chips: All / Needs review / Matched / Converted / Rejected. Selected chip uses `bg-primary text-primary-foreground`; others use `bg-muted text-muted-foreground`. Keyboard navigation: arrow-left/right between chips; Enter/Space selects. Updates URL query via Next.js `useRouter().replace({ query: { status: ... } })`.

Tests (RTL + vitest) — 8 total cases across 2 test files:

`intake-status-pill.test.tsx`:
- Renders all 5 statuses with the correct background class (assert `getByRole('status').className` contains the expected token).
- Includes icon with `aria-hidden="true"`.
- `aria-label` set to translated status string.

`intake-filter-chips.test.tsx`:
- All chip renders 5 chips (check via `getAllByRole('button').length === 5`).
- Selected chip has `data-state="active"`.
- Clicking a chip updates the router query.
- Keyboard: ArrowRight on "All" focuses "Needs review".
- Clicking "All" clears the `status` query param.
  </action>
  <verify>pnpm --filter apps-web test -- --run src/components/invoices/intake/__tests__/</verify>
  <acceptance_criteria>
    - All 4 new component files exist and export a React component
    - intake-status-pill.tsx contains `aria-label=`
    - intake-filter-chips.tsx contains `useRouter` import from `next/navigation`
    - intake-filter-chips.tsx contains at least one `onKeyDown` handler (keyboard nav)
    - All 8 test cases pass
    - No `console.` in any new file
  </acceptance_criteria>
  <done>Primitive components match 62-UI-SPEC.md token map and pass a11y + interaction tests.</done>
</task>

<task type="auto">
  <name>Task 4: Build upload dialog + import split button + intake list + actions bar</name>
  <files>apps/web/src/components/invoices/intake/intake-upload-dialog.tsx, apps/web/src/components/invoices/intake/import-split-button.tsx, apps/web/src/components/invoices/intake/intake-list.tsx, apps/web/src/components/invoices/intake/intake-detail-actions-bar.tsx, apps/web/src/components/invoices/intake/__tests__/intake-upload-dialog.test.tsx, apps/web/src/components/invoices/intake/__tests__/intake-detail-actions-bar.test.tsx</files>
  <read_first>apps/web/src/components/ui/dialog.tsx, apps/web/src/components/ui/dropdown-menu.tsx, apps/web/src/components/ui/alert-dialog.tsx, apps/web/src/components/ui/table.tsx, apps/web/src/components/invoices/intake/intake-status-pill.tsx, .planning/phases/62-zugferd-e-invoicing/62-UI-SPEC.md</read_first>
  <action>
Build four compositional components. Follow 62-UI-SPEC.md § Interaction & State Contracts exactly — every loading/error/empty state documented there MUST be handled.

**`intake-upload-dialog.tsx`** — `<Dialog>` with drop-zone. Uses `trpc.invoiceIntake.upload.useMutation()`. Flow:
- File selected/dropped → base64-encode client-side → call mutation.
- Pending state: disable inputs, show skeleton-style spinner + "Validating…" copy.
- Success: close dialog, `sonner` toast with "Imported — review at …", `router.push(/invoices/intake/{id})`.
- DEDUP_RETURNED: toast "Already imported — opening existing intake" then router.push.
- Hard-reject error: keep dialog open, replace drop-zone with inline error block + "Try another file" button.
- Accept only `.xml`, `.pdf` (HTML `accept` + JS guard). Wrong type → inline error (no toast).
- Max 5 MB — check BEFORE calling mutation (`file.size > 5 * 1024 * 1024`).

**`import-split-button.tsx`** — Button + DropdownMenu combo. Primary action linked to existing "+ New invoice". Secondary menu item "Import e-invoice" (visible only when the `EINVOICE_IMPORT_ENABLED` flag is on for the current org; use the project's flag-check hook — e.g., `useFeatureFlag('EINVOICE_IMPORT_ENABLED')`). Clicking it opens the upload dialog.

**`intake-list.tsx`** — Consumes `trpc.invoiceIntake.listByOrg.useQuery({ status, cursor })`. Renders:
- `<IntakeFilterChips>` at top.
- `<Table>` (shadcn) with columns: Supplier | Invoice # | Date | Total (tabular-nums) | Level badge | Status pill | Validation pill.
- Skeleton: 5 `<Skeleton>` rows while loading.
- Empty state: display-sized heading + body + "Import e-invoice" CTA (opens dialog).
- Cursor pagination: "Load more" button at bottom.
- Each row is a `Link` to `/invoices/intake/{id}`. Hover state uses `--accent`.

**`intake-detail-actions-bar.tsx`** — Sticky bottom on mobile, inline on desktop. Renders 4 possible buttons based on intake state:
- `Convert to invoice` — `disabled` + tooltip when not MATCHED or validation not acknowledged.
- `Confirm match` — visible when candidate is selected but status < MATCHED.
- `Accept despite issues` — visible when validationStatus IN ('WARNINGS', 'INVALID') AND `validationAcknowledgedAt` is null.
- `Reject import` — destructive ghost button, opens `<AlertDialog>` with textarea for reason (min 3 chars).

All buttons call the corresponding mutations via `trpc.invoiceIntake.*.useMutation()`. Optimistic updates NOT used (state transitions must reflect server state). On mutation success, invalidate `trpc.invoiceIntake.getById` and `trpc.invoiceIntake.listByOrg`.

Tests — 10 total cases across 2 test files:

`intake-upload-dialog.test.tsx` (6 cases):
1. Renders drop-zone with correct a11y (`<label>` wrapping input).
2. Wrong file type → inline error appears, mutation NOT called.
3. File > 5 MB → inline error, mutation NOT called.
4. Successful upload → toast + router.push invoked with correct path.
5. DEDUP_RETURNED response → toast with "Already imported" string, router.push with same id.
6. Hard-reject server error → dialog stays open, error message visible.

`intake-detail-actions-bar.test.tsx` (4 cases):
1. Convert button disabled (with tooltip) when status=PARSED, validationStatus=WARNINGS, no ack.
2. Convert enabled when status=MATCHED AND validationAcknowledgedAt set.
3. Reject button opens AlertDialog with textarea; Tab order: Reason → Cancel → Reject.
4. Reject with reason < 3 chars shows inline validation error, mutation NOT called.
  </action>
  <verify>pnpm --filter apps-web test -- --run src/components/invoices/intake/__tests__/intake-upload-dialog.test.tsx src/components/invoices/intake/__tests__/intake-detail-actions-bar.test.tsx</verify>
  <acceptance_criteria>
    - intake-upload-dialog.tsx contains the string `5 * 1024 * 1024` (size guard)
    - intake-upload-dialog.tsx uses `trpc.invoiceIntake.upload.useMutation`
    - import-split-button.tsx checks `EINVOICE_IMPORT_ENABLED`
    - intake-detail-actions-bar.tsx contains `AlertDialog` import
    - intake-detail-actions-bar.tsx disables Convert button with tooltip via a conditional (`disabled={!canConvert}`)
    - All 10 test cases across the two test files pass
    - No `console.` in any file
  </acceptance_criteria>
  <done>Core interactive surfaces built and tested.</done>
</task>

<task type="auto">
  <name>Task 5: Build intake detail panes (PDF/XML preview, parsed fields, validation, match)</name>
  <files>apps/web/src/components/invoices/intake/intake-detail-pdf-pane.tsx, apps/web/src/components/invoices/intake/intake-detail-fields-pane.tsx, apps/web/src/components/invoices/intake/intake-detail-validation-pane.tsx, apps/web/src/components/invoices/intake/intake-detail-match-pane.tsx</files>
  <read_first>.planning/phases/62-zugferd-e-invoicing/62-UI-SPEC.md, apps/web/src/components/invoices/einvoice-tab/einvoice-tab.tsx, apps/web/src/components/contractors/vat-validation-status-pill.tsx</read_first>
  <action>
Build the four card-based panes as pure display components (data passed via props; mutations handled in the actions bar):

1. **`intake-detail-pdf-pane.tsx`** — shadcn `<Card>` wrapping either a `react-pdf` viewer (when `sourceKind === 'UPLOAD_PDF'`) or a `<pre className="font-mono text-sm">` with the raw XML (when `sourceKind === 'UPLOAD_XML'`). Include a visually-hidden "Skip preview" link for keyboard users (jumps to `#parsed-fields`). Uses `trpc.invoiceIntake.downloadRawFile.useQuery` to get the signed URL.

2. **`intake-detail-fields-pane.tsx`** — `<Card>` with `<dl>` definition list: Supplier name, VAT ID (mono), Leitweg-ID (mono, if present), Invoice #, Date (`Intl.DateTimeFormat` via `next-intl`), Currency, Total gross (mono + tabular-nums, formatted via the project's `formatMoney(minor, currency, locale)` helper — grep for the existing helper), Line count, Profile level badge. Include collapsible "Advanced / technical" section at bottom showing `unmappedFieldsJson` if non-empty (for EXTENDED profile warnings per D-13).

3. **`intake-detail-validation-pane.tsx`** — `<Card>` with validation status pill, summary count, link to signed R2 URL of the full KoSIT HTML report (fetched via `trpc.invoiceIntake.downloadValidationReport.useQuery`; renders "No report available" if null). Inline list of first 5 schematron issues from `validationReportSummary`. Shows the "Accept despite issues" banner when `validationAcknowledgedAt` is set (says "Issues accepted on {date} by {user}").

4. **`intake-detail-match-pane.tsx`** — `<Card>` consuming `trpc.invoiceIntake.getMatchCandidates.useQuery({ intakeId })`. Renders a vertical list of candidate cards. Each: Contractor name, VAT ID (via `vat-validation-status-pill`), match reason chips ("VAT ID match", "Leitweg-ID match", etc. — translated). If only one candidate, pre-select it (selected ring using `--primary` border). Second section: "Create new contractor from this data" button (prefills the Contractor form with parsed data via router.push + query-string). Empty state: "No candidates. Create a new contractor from this data."

All panes use shadcn `Skeleton` for loading states. All use 62-UI-SPEC.md tokens (no custom CSS; Tailwind utility classes only). All icons from `lucide-react`.

No dedicated test files for these pure-display panes — they're exercised by the page integration test in Task 6.
  </action>
  <verify>pnpm --filter apps-web exec tsc --noEmit</verify>
  <acceptance_criteria>
    - All 4 pane files exist and export a React component
    - intake-detail-pdf-pane.tsx imports from `react-pdf` OR renders a `<pre className="font-mono">` for XML
    - intake-detail-fields-pane.tsx uses `Intl.DateTimeFormat` or `next-intl`'s date formatter
    - intake-detail-match-pane.tsx calls `trpc.invoiceIntake.getMatchCandidates.useQuery`
    - No `console.` in any file
    - `pnpm --filter apps-web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Detail page sub-components ready for composition.</done>
</task>

<task type="auto">
  <name>Task 6: Build routes (list + detail) + wire split-button + sidebar entry + e-invoice tab section</name>
  <files>apps/web/src/app/[locale]/(dashboard)/invoices/intake/page.tsx, apps/web/src/app/[locale]/(dashboard)/invoices/intake/[id]/page.tsx, apps/web/src/app/[locale]/(dashboard)/invoices/page.tsx, apps/web/src/app/[locale]/(dashboard)/invoices/intake/__tests__/page.test.tsx, apps/web/src/app/[locale]/(dashboard)/invoices/intake/[id]/__tests__/page.test.tsx, apps/web/src/components/layout/sidebar.tsx, apps/web/src/components/invoices/einvoice-tab/download-zugferd-pdf-button.tsx, apps/web/src/components/invoices/einvoice-tab/einvoice-tab.tsx, apps/web/src/components/invoices/einvoice-tab/__tests__/download-zugferd-pdf-button.test.tsx</files>
  <read_first>apps/web/src/app/[locale]/(dashboard)/invoices/page.tsx, apps/web/src/components/layout/sidebar.tsx, apps/web/src/components/invoices/einvoice-tab/einvoice-tab.tsx, apps/web/src/app/[locale]/(dashboard)/classification/page.tsx, .planning/phases/62-zugferd-e-invoicing/62-UI-SPEC.md</read_first>
  <action>
**Route `/invoices/intake/page.tsx`:**
- Server Component that checks `EINVOICE_IMPORT_ENABLED` for the active org; if disabled, returns `notFound()` (Next.js).
- Client boundary that renders `<IntakeFilterChips>` + `<IntakeList>`.
- Page title + subtitle from `EInvoice.intake.pageTitle` / `pageSubtitle`.
- Header split-button "Import e-invoice" also available here (second entry point beyond `/invoices`).

**Route `/invoices/intake/[id]/page.tsx`:**
- Server Component that checks flag; otherwise `notFound()`.
- Fetches `trpc.invoiceIntake.getById.prefetch({ intakeId })` via the RSC-aware tRPC helper.
- Client boundary renders:
  - 2-column grid on `md+` (PDF pane left, 3 right-panes stacked: fields, validation, match).
  - Sticky (mobile) / inline (desktop) `<IntakeDetailActionsBar>` at bottom.
  - Above the action bar, when `profileLevel === 'EXTENDED'`, show a full-width `<Alert>` with the `bannerExtendedBestEffort` copy.

**Invoices list page `/invoices/page.tsx`:**
- Replace the existing `+ New invoice` button with `<ImportSplitButton>` (primary action unchanged; new "Import e-invoice" secondary item only visible when flag on).

**Sidebar `components/layout/sidebar.tsx`:**
- Under the existing "Invoices" section, add an entry:
  ```
  { key: 'imports', labelKey: 'EInvoice.intake.sidebarImports', href: '/invoices/intake', icon: Inbox, flag: 'EINVOICE_IMPORT_ENABLED' }
  ```
- The sidebar's existing flag-filtering hook (grep for how other flag-gated entries work) determines visibility. Active-state styling inherits from the existing pattern (62-UI-SPEC.md § Color — active uses `--primary`).

**E-invoice tab section `components/invoices/einvoice-tab/einvoice-tab.tsx`:**
- Append a "ZUGFeRD" section below the existing XRechnung section. Shows:
  - If `lifecycle?.zugferdPdfKey` is null: "Not yet generated" + a `<DownloadZugferdPdfButton>` labelled "Download ZUGFeRD PDF".
  - Else: "Generated on {lifecycle.zugferdGeneratedAt}" + the same button (regenerates-or-returns-existing per server idempotency).
- No feature flag — outbound is always available wherever the e-invoice tab is.

**`download-zugferd-pdf-button.tsx`:**
```tsx
const mut = trpc.einvoice.generateZugferdPdf.useMutation();
const onClick = async () => {
  try {
    const { signedUrl } = await mut.mutateAsync({ invoiceId });
    // trigger browser download: set window.location.href = signedUrl, or create an <a download> and click it
    const a = document.createElement("a");
    a.href = signedUrl;
    a.download = `invoice-${invoiceId}-zugferd.pdf`;
    a.click();
    toast.success(t("EInvoice.intake.ctaDownloadZugferd") + " ✓");
  } catch (e) {
    toast.error(t("EInvoice.intake.genFailureToast"));
  }
};
```
- While `mut.isPending`: show a skeleton-styled "Generating…" label; button disabled.

Tests — 5 cases across 3 test files:

`intake/__tests__/page.test.tsx` (1 case):
- When flag is off, `GET /en/invoices/intake` returns 404 (Playwright OR a Next `notFound` assertion in RTL).

`intake/[id]/__tests__/page.test.tsx` (2 cases):
- Renders all 4 panes + actions bar when intake data is seeded.
- When `profileLevel === 'EXTENDED'`, the `bannerExtendedBestEffort` alert appears.

`einvoice-tab/__tests__/download-zugferd-pdf-button.test.tsx` (2 cases):
- Clicking the button calls the mutation and creates a download anchor (assert `createElement('a')` spy).
- On mutation error, the error toast copy matches `EInvoice.intake.genFailureToast`.
  </action>
  <verify>pnpm --filter apps-web test -- --run 'src/app/**/invoices/intake/__tests__/' 'src/components/invoices/einvoice-tab/__tests__/download-zugferd-pdf-button.test.tsx' 'src/components/invoices/intake/__tests__/'</verify>
  <acceptance_criteria>
    - apps/web/src/app/[locale]/(dashboard)/invoices/intake/page.tsx calls `notFound()` when flag is off
    - apps/web/src/app/[locale]/(dashboard)/invoices/intake/[id]/page.tsx renders all 4 panes (grep for `IntakeDetailPdfPane`, `IntakeDetailFieldsPane`, `IntakeDetailValidationPane`, `IntakeDetailMatchPane`)
    - apps/web/src/components/layout/sidebar.tsx references `EINVOICE_IMPORT_ENABLED`
    - apps/web/src/components/invoices/einvoice-tab/einvoice-tab.tsx contains "ZUGFeRD" (the new section heading)
    - download-zugferd-pdf-button.tsx uses `trpc.einvoice.generateZugferdPdf.useMutation`
    - All 5 test cases pass
    - `pnpm --filter apps-web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All user-facing surfaces live; flag-gating verified; outbound download wired.</done>
</task>

</tasks>

<verification>
- [ ] `pnpm --filter apps-web test -- --run` exits 0 (all new test files pass)
- [ ] `pnpm --filter apps-web exec tsc --noEmit` exits 0
- [ ] `pnpm --filter @contractor-ops/validators test -- --run` exits 0 (locked-phrase parity)
- [ ] No `console.*` in any new or modified file
- [ ] All strings in 62-UI-SPEC.md Copywriting Contract are present in all 3 locale JSONs
</verification>

<success_criteria>
- Feature-flag registered
- 3-locale messages extended
- DE legal phrases locked + parity-tested
- All 12 intake components + the ZUGFeRD download button delivered
- Both new routes wired with flag gating
- Sidebar entry flag-gated
- Invoices page split-button integrated
- E-invoice tab shows the ZUGFeRD section + download button
</success_criteria>

<output>
After completion, create `.planning/phases/62-zugferd-e-invoicing/62-06-SUMMARY.md`
</output>
