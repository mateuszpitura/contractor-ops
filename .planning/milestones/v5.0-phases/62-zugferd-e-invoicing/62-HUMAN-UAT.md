---
status: partial
phase: 62-zugferd-e-invoicing
source: [62-VERIFICATION.md]
started: 2026-04-15T01:44:11Z
updated: 2026-04-15T01:44:11Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. veraPDF PDF/A-3 B conformance against generated ZUGFeRD output
expected: All 3 fixtures generated by `pnpm --filter @contractor-ops/einvoice exec ts-node scripts/generate-zugferd-fixtures.ts` pass `docker run --rm -v $(pwd):/data verapdf/cli:1.26 --flavour 3b /data/<fixture>.pdf` with `isCompliant="true"` in the XML report. CI gate at `.github/workflows/verapdf.yml` should turn green when triggered against these outputs.
result: [pending]

### 2. Playwright inbound intake flow — green run (not skip)
expected: All 6 scenarios in `apps/web/e2e/functional/intake-upload-flow.spec.ts` execute (do NOT graceful-skip) and pass when run against a live server with: (a) authenticated org session, (b) feature flag `einvoice.import-enabled` enabled for that org, (c) seeded Contractor with VAT ID `DE123456789` (matches fixture). Run: `pnpm --filter @contractor-ops/web exec playwright test -c playwright.functional.config.ts intake-upload-flow.spec.ts`.
result: [pending]

### 3. Playwright outbound ZUGFeRD download flow — green run (not skip)
expected: All 3 scenarios in `apps/web/e2e/functional/zugferd-download-flow.spec.ts` execute (do NOT graceful-skip) and pass when run against a live server with: (a) authenticated org session, (b) at least one finalized invoice row for that org. Verify the downloaded file starts with `%PDF-` and contains an embedded `factur-x.xml` AFRelationship. Run: `pnpm --filter @contractor-ops/web exec playwright test -c playwright.functional.config.ts zugferd-download-flow.spec.ts`.
result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
