---
phase: 58
plan: 05
type: execute
wave: 3
depends_on: [58-01, 58-02, 58-03]
files_modified:
  - apps/web/src/app/[locale]/contractors/[id]/engagements/[engagementId]/classification/[assessmentId]/page.tsx
  - apps/web/src/app/[locale]/contractors/[id]/classification/page.tsx
  - apps/web/src/components/contractors/classification/classification-disclaimer-dialog.tsx
  - apps/web/src/components/contractors/classification/classification-tile.tsx
  - apps/web/src/components/contractors/classification/classification-engagement-cta.tsx
  - apps/web/src/components/contractors/classification/classification-assessment-list.tsx
  - apps/web/src/components/contractors/classification/outcome/verdict-banner.tsx
  - apps/web/src/components/contractors/classification/outcome/ir35-area-card.tsx
  - apps/web/src/components/contractors/classification/outcome/drv-category-bar.tsx
  - apps/web/src/components/contractors/classification/outcome/drv-criterion-breakdown-list.tsx
  - apps/web/src/components/contractors/classification/outcome/outcome-print-layout.tsx
  - apps/web/src/components/contractors/compliance/country-compliance-section.tsx
  - apps/web/src/components/contractors/classification/__tests__/classification-tile.test.tsx
  - apps/web/src/components/contractors/compliance/__tests__/country-compliance-section.test.tsx
  - apps/web/src/components/contractors/classification/__tests__/classification-disclaimer-dialog.test.tsx
  - apps/web/src/components/contractors/classification/__tests__/a11y.test.tsx
  - apps/web/src/app/[locale]/contractors/[id]/engagements/[engagementId]/classification/[assessmentId]/__tests__/outcome.test.tsx
autonomous: false
requirements: [CLASS-02, CLASS-05, CLASS-11]
requirements_addressed: [CLASS-02, CLASS-05, CLASS-11]

must_haves:
  truths:
    - "Navigating to /[locale]/contractors/[id]/engagements/[engagementId]/classification/[assessmentId] renders SSR with the stored outcome; if disclaimerAcknowledgedAt is null, the blocking AlertDialog is rendered on top and cannot be closed via Escape or overlay click."
    - "After the user ticks 'I understand' and clicks 'I understand — view outcome', trpc.classification.acknowledgeDisclaimer fires and the modal closes."
    - "The IR35 outcome page renders the verdict banner (colour + icon + text label — semantic triad) + 5 area cards (one per Ir35Area) with driving questions per area; the DRV outcome page renders a traffic-light banner + 4 category bars with weighted fill + expandable criterion breakdown tables."
    - "The outcome page reads question prompts from `assessment.questionsSnapshot.questions[i].prompt[locale]` — NEVER from the live rule-set constant (Pitfall 1). A test mocks the live constant post-snapshot and asserts the page's rendered prompts are unchanged."
    - "Disclaimer modal renders DISCLAIMER_IR35_BODY or DISCLAIMER_SCHEIN_BODY verbatim (locale-selected) from packages/validators/src/legal/disclaimers.ts; role='alertdialog'; initial focus on the checkbox."
    - "Phase 56 CountryComplianceSection is extended with one <ClassificationTile> per engagement whose contractor.countryCode is GB or DE; tile displays latest verdict pill + completedAt + 'View details' + 'Re-run assessment' (or the empty state + 'Run classification assessment' CTA if no completed row exists)."
    - "ClassificationAssessmentList route (/[locale]/contractors/[id]/classification) shows a table ≥1024px / vertical card list below — draft-first then completedAt DESC."
    - "Print layout (@media print) force-expands Collapsibles, hides chrome, preserves semantic-triad colours, and matches UI-SPEC §Interaction 9."
    - "WCAG 2.1 AA proven: axe-core scan of outcome page + disclaimer dialog + tile returns 0 violations; verdict banner never communicates by colour alone."
    - "Before the phase ships, two external reviewers (Steuerberater + UK tax-adviser) have signed off the DE DRV criterion inventory + IR35 question wording. Checkpoint pauses execution until approvals land."
  artifacts:
    - path: apps/web/src/app/[locale]/contractors/[id]/engagements/[engagementId]/classification/[assessmentId]/page.tsx
      provides: "SSR outcome page — renders IR35 or DRV variant + blocking disclaimer modal until acknowledged"
      contains: "disclaimerAcknowledgedAt"
    - path: apps/web/src/components/contractors/classification/classification-disclaimer-dialog.tsx
      provides: "AlertDialog with role='alertdialog', Escape + overlay disabled, checkbox + 'I understand' gate"
      contains: "onEscapeKeyDown"
    - path: apps/web/src/components/contractors/classification/outcome/verdict-banner.tsx
      provides: "Shared verdict banner component — colour + icon + text across IR35 and DRV"
      contains: "role=\"status\""
    - path: apps/web/src/components/contractors/classification/outcome/drv-category-bar.tsx
      provides: "Weighted bar per category with threshold markers + expandable criterion breakdown"
      contains: "weightedScore"
    - path: apps/web/src/components/contractors/classification/classification-tile.tsx
      provides: "Per-engagement tile on CountryComplianceSection: latest verdict / empty state / CTAs"
      contains: "Run classification assessment"
    - path: apps/web/src/components/contractors/compliance/country-compliance-section.tsx
      provides: "Extended with ClassificationTile dispatch per engagement for GB/DE"
      contains: "ClassificationTile"
  key_links:
    - from: apps/web/src/app/[locale]/contractors/[id]/engagements/[engagementId]/classification/[assessmentId]/page.tsx
      to: packages/api/src/routers/classification.ts
      via: "trpc.classification.getLatest / acknowledgeDisclaimer"
      pattern: "trpc\\.classification\\."
    - from: apps/web/src/components/contractors/classification/classification-disclaimer-dialog.tsx
      to: packages/validators/src/legal/disclaimers.ts
      via: "verbatim import of DISCLAIMER_*_BODY and DISCLAIMER_*_ACKNOWLEDGEMENT"
      pattern: "DISCLAIMER_(IR35|SCHEIN)_"
    - from: apps/web/src/app/[locale]/contractors/[id]/engagements/[engagementId]/classification/[assessmentId]/page.tsx
      to: packages/classification/src/snapshot.ts (indirect — via persisted questionsSnapshot)
      via: "page reads from assessment.questionsSnapshot, NEVER from IR35_QUESTIONS / SCHEIN_QUESTIONS"
      pattern: "questionsSnapshot"
---

<objective>
Complete the user-visible surface of Phase 58: the SSR outcome page (IR35 5-area variant + DRV 4-category traffic-light variant), the blocking disclaimer AlertDialog, the per-engagement Classification tile on the contractor profile (via Phase 56 CountryComplianceSection extension), the assessment list route, and the print layout. Gate the phase on two human-verify checkpoints: Steuerberater sign-off of DE criterion wording and UK tax-adviser sign-off of IR35 question wording — both MUST be approved before VALIDATION.md nyquist_compliant flips to true.

Purpose: Users can see the outcome of their completed assessment, are legally protected by the mandatory blocking disclaimer (D-12), can navigate between contractor profile → tile → outcome page, and can print/export. External regulatory experts confirm the legal-content correctness that automated tests cannot prove.

Output: A shippable Phase 58 surface with every user success criterion from ROADMAP §Phase 58 satisfied, extended CountryComplianceSection integrating the classification view, and a two-track human-verify gate producing a REVIEWED.md artifact.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.planning/phases/58-classification-engine-rule-sets/58-UI-SPEC.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.planning/phases/58-classification-engine-rule-sets/58-CONTEXT.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.planning/phases/58-classification-engine-rule-sets/58-RESEARCH.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.planning/phases/58-classification-engine-rule-sets/58-VALIDATION.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.planning/phases/58-classification-engine-rule-sets/58-03-SUMMARY.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.planning/phases/58-classification-engine-rule-sets/58-04-SUMMARY.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/CLAUDE.md

<interfaces>
<!-- shadcn primitives -->
- AlertDialog (+ AlertDialogContent, AlertDialogHeader, AlertDialogTitle, AlertDialogDescription, AlertDialogFooter, AlertDialogAction, AlertDialogCancel)
- Card + CardHeader + CardTitle + CardContent + CardFooter
- Badge
- Collapsible + CollapsibleTrigger + CollapsibleContent
- Table + TableHeader + TableRow + TableHead + TableBody + TableCell + TableCaption
- Bdi (Phase 50 — wrapping user/contractor names in mixed-direction text)
- Button

<!-- Locked disclaimer constants (Plan 01) -->
import {
  DISCLAIMER_IR35_BODY, DISCLAIMER_IR35_ACKNOWLEDGEMENT,
  DISCLAIMER_SCHEIN_BODY, DISCLAIMER_SCHEIN_ACKNOWLEDGEMENT,
} from '@contractor-ops/validators/legal/disclaimers';

<!-- tRPC client procedures -->
trpc.classification.getLatest.useQuery({ contractorAssignmentId })
trpc.classification.acknowledgeDisclaimer.useMutation()
trpc.classification.listByContractor.useQuery({ contractorId })

<!-- Outcome types (Plan 01) -->
import type { Ir35Outcome, ScheinselbstandigkeitOutcome, Outcome, Ir35AreaResult, ScheinCategoryResult } from '@contractor-ops/classification';

<!-- Phase 56 integration point (READ BEFORE MODIFY) -->
apps/web/src/components/contractors/compliance/country-compliance-section.tsx:
  - Dispatches UK vs DE field groups by org.countryCode.
  - This plan ADDS per-engagement ClassificationTile dispatch for GB/DE engagements.
  - MUST NOT remove or rename any existing Phase 56 exports or props.
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → outcome route | Unauthenticated user could try `/classification/[unknownId]` → must 404 through tenantProcedure |
| browser → disclaimer modal | User could try to bypass via direct URL, back button, or devtools — must be SSR-enforced |
| rationale → DOM | Free-text rationale from user input rendered to outcome page |

## STRIDE Threat Register (applicable ASVS: V4, V5, V7, V10, V12, WCAG 2.1 AA)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-22 | Repudiation (user claims they didn't see disclaimer) | outcome page | mitigate | SSR checks assessment.disclaimerAcknowledgedAt; if null, force-render the AlertDialog; re-opens on every page load until acknowledged. (D-12, Pitfall 6) |
| T-58-23 | Tampering | disclaimer modal bypass | mitigate | Escape + overlay click disabled (onEscapeKeyDown / onInteractOutside preventDefault); initial focus on checkbox not confirm button; Radix AlertDialog enforces focus trap. (V4) |
| T-58-24 | Stored XSS via rationale | outcome page | mitigate | React auto-escapes; no dangerouslySetInnerHTML anywhere; CSP already repo-wide. (V10) |
| T-58-25 | IDOR on assessmentId | outcome page | mitigate | getLatest / getDraft only return rows in the caller's org; outcome route 404s on non-org rows. (V4) |
| T-58-26 | Info Disclosure | outcome PDF export | mitigate | window.print() runs client-side on already-authorised data; no server-side PDF in Phase 58 (deferred to Phase 59). (V12) |
| T-58-27 | Tampering | Live rule-set consumed by outcome | mitigate | Outcome page imports NOTHING from packages/classification/src/profiles/*/rule-set.*; reads all prompts/weights from assessment.questionsSnapshot. Grep assertion + unit test. (Pitfall 1) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Disclaimer dialog + SSR outcome page (IR35 + DRV variants) + snapshot-not-live test + print layout</name>
  <files>apps/web/src/app/[locale]/contractors/[id]/engagements/[engagementId]/classification/[assessmentId]/page.tsx, apps/web/src/components/contractors/classification/classification-disclaimer-dialog.tsx, apps/web/src/components/contractors/classification/outcome/verdict-banner.tsx, apps/web/src/components/contractors/classification/outcome/ir35-area-card.tsx, apps/web/src/components/contractors/classification/outcome/drv-category-bar.tsx, apps/web/src/components/contractors/classification/outcome/drv-criterion-breakdown-list.tsx, apps/web/src/components/contractors/classification/outcome/outcome-print-layout.tsx, apps/web/src/components/contractors/classification/__tests__/classification-disclaimer-dialog.test.tsx, apps/web/src/app/[locale]/contractors/[id]/engagements/[engagementId]/classification/[assessmentId]/__tests__/outcome.test.tsx, apps/web/src/components/contractors/classification/__tests__/a11y.test.tsx</files>
  <read_first>
    - .planning/phases/58-classification-engine-rule-sets/58-UI-SPEC.md §Interaction 6–9 (disclaimer, IR35 banner + area cards, DRV banner + category bars, print) + §Accessibility Contract
    - .planning/phases/58-classification-engine-rule-sets/58-RESEARCH.md §Code Examples → Disclaimer modal + §Pitfall 1 (snapshot not live) + §Pitfall 6 (disclaimer bypass)
    - packages/validators/src/legal/disclaimers.ts (Plan 01 constants — use verbatim)
    - apps/web/src/components/ui/alert-dialog.tsx (shadcn primitive shape)
    - apps/web/src/app/[locale]/contractors/[id]/engagements/[engagementId]/page.tsx (engagement detail page — mirror SSR + auth pattern)
  </read_first>
  <behavior>
    ### Tests FIRST:

    **classification-disclaimer-dialog.test.tsx:**
    - TEST DD-1: Renders DISCLAIMER_IR35_BODY verbatim when countryCode='GB' (assert text content matches the constant string).
    - TEST DD-2: Renders DISCLAIMER_SCHEIN_BODY verbatim when countryCode='DE'.
    - TEST DD-3: role='alertdialog' on the dialog content.
    - TEST DD-4: Initial focus lands on the checkbox (not the confirm button) — assert document.activeElement.
    - TEST DD-5: Pressing Escape does nothing (onEscapeKeyDown preventDefault) — modal stays open.
    - TEST DD-6: Clicking the overlay does nothing — modal stays open.
    - TEST DD-7: Confirm button is disabled when checkbox unticked; enabled after tick.
    - TEST DD-8: Clicking Confirm fires trpc.classification.acknowledgeDisclaimer.

    **outcome.test.tsx (IR35 + DRV + snapshot):**
    - TEST OC-1 (IR35): Given a completed IR35 assessment with verdict='outside', renders VerdictBanner with success colour + CircleCheck icon + text "Outside IR35" (EN) / "Außerhalb IR35" (DE).
    - TEST OC-2 (IR35): 5 <Ir35AreaCard /> rendered in fixed order (substitution, control, financial-risk, part-and-parcel, moo) each with its area verdict pill.
    - TEST OC-3 (DRV): Given a completed DRV assessment with riskLevel='amber' and totalScore=45, renders VerdictBanner amber + ShieldAlert + "Moderate risk" / "Mittleres Risiko".
    - TEST OC-4 (DRV): 4 <DrvCategoryBar /> rendered with weighted fill matching each category's weightedScore; threshold markers at 0.3 × max and 0.6 × max.
    - TEST OC-5 (DRV): Expand a category bar → criterion breakdown Table renders with 4-6 criterion rows (per category count).
    - **TEST OC-6 (Pitfall 1 — LOAD-BEARING):** Build an assessment with questionsSnapshot containing a prompt 'Original prompt Q1'. Use `vi.doMock` to replace the live IR35_QUESTIONS constant with `[{ ...q, prompt: { en: 'TAMPERED', pl: 'TAMPERED', de: 'TAMPERED' } }]`. Render the outcome page. Assert the rendered prompt text is 'Original prompt Q1' (from snapshot) and NOT 'TAMPERED' (from the live constant). This test MUST fail if the outcome page imports from `@contractor-ops/classification/profiles/*/rule-set` — its entire reason to exist is to catch that regression.
    - TEST OC-7 (disclaimer gate): Render the page with `assessment.disclaimerAcknowledgedAt = null` → assert the AlertDialog is in the DOM with open=true and the outcome content is either behind the modal OR rendered but inert. Set `disclaimerAcknowledgedAt` to a Date → assert the modal is gone.
    - TEST OC-8 (IDOR): Mock getLatest to return null (tenant scoping) → page renders a 404 / empty state.

    **a11y.test.tsx:**
    - TEST AX-1: axe-core scan of outcome page (IR35 variant) returns 0 violations.
    - TEST AX-2: axe-core scan of outcome page (DRV variant) returns 0 violations.
    - TEST AX-3: Disclaimer dialog has aria-labelledby (title) + aria-describedby (full body paragraph).
    - TEST AX-4: Verdict banner has role="status" + aria-label matching the text label (not colour-only — Pitfall WCAG 1.4.1).
    - TEST AX-5: DRV category bar wrapper has role="img" + aria-label "{category}: {weightedScore} of {max}. Risk level {green/amber/red}." (UI-SPEC §Accessibility).

    ### Implementation:
  </behavior>
  <action>
1. Create `classification-disclaimer-dialog.tsx` per RESEARCH §Code Examples → Disclaimer modal + UI-SPEC §Interaction 6:
   - Props: `{ assessmentId: string; countryCode: 'GB'|'DE'; open: boolean; onAcknowledged: () => void; onDeferred: () => void; }`.
   - Import DISCLAIMER_IR35_* / DISCLAIMER_SCHEIN_* from `@contractor-ops/validators/legal/disclaimers` — verbatim.
   - onInteractOutside / onEscapeKeyDown both call preventDefault().
   - Initial focus on the Checkbox: use `autoFocus` prop OR `useRef` + `useEffect` (Radix may reclaim focus — test which approach works and document in comment).
   - Width `max-w-lg`; layout per UI-SPEC §Interaction 6 (AlertTriangle amber icon in title, body paragraph, checkbox + label, two buttons).
   - Trigger `trpc.classification.acknowledgeDisclaimer` on confirm; on success invoke onAcknowledged.

2. Create outcome route `apps/web/src/app/[locale]/contractors/[id]/engagements/[engagementId]/classification/[assessmentId]/page.tsx` as an RSC per UI-SPEC Open Item 2 (SSR):
   - Load the assessment via server-side tRPC (use the repo's existing server-caller helper — match engagement detail page pattern). If findUnique returns null → render a `notFound()`.
   - If `assessment.disclaimerAcknowledgedAt === null`, render the page content wrapped in a `'use client'` island that mounts `<ClassificationDisclaimerDialog open={true} />` on top. The modal re-opens on every page load until acknowledged (Pitfall 6). After `onAcknowledged`, the client island refetches the assessment to update the flag.
   - Read `assessment.questionsSnapshot` and pass the prompts/help-text TO children. Do NOT import IR35_QUESTIONS / SCHEIN_QUESTIONS anywhere in this file or its children (Pitfall 1).
   - Dispatch on `assessment.countryCode`:
     - 'GB' → render `<VerdictBanner kind="ir35" />` + 5 `<Ir35AreaCard />` in a grid.
     - 'DE' → render `<VerdictBanner kind="drv" />` + 4 `<DrvCategoryBar />` stacked.
   - Wrap in `<OutcomePrintLayout>`.
   - Include a "Print / Save as PDF" Button (primary teal) that calls `window.print()`.
   - Include a "Re-run assessment" Button that navigates to the wizard route (Plan 04) — the new route invocation will trigger createDraft which correctly returns a new row since the old one is completed.

3. `outcome/verdict-banner.tsx` ('use client' for interactivity OR RSC if no client state — prefer RSC):
   - Discriminated union props: `{ kind: 'ir35'; outcome: Ir35Outcome; ruleSetVersion: string; locale: string }` | `{ kind: 'drv'; outcome: ScheinselbstandigkeitOutcome; ruleSetVersion: string; locale: string }`.
   - Render the large pill per UI-SPEC §Interaction 7/8 with colour + Lucide icon + text (semantic triad — NEVER colour alone).
   - role="status", aria-label = the text label.
   - 128 px tall, border-left 4 px in semantic colour, bg tinted at 10% opacity.

4. `outcome/ir35-area-card.tsx`: Renders one card per Ir35AreaResult. Title uses `t('ir35.step.{area}')` (from Plan 04's Classification namespace). Area verdict pill (colour + icon per semantic triad table in UI-SPEC). Top-3 driving questions list: map each `drivingQuestionIds[i]` to the corresponding `questionsSnapshot.questions[...]` entry and render prompt + user's answer. Expandable "All questions in this area" Collapsible.

5. `outcome/drv-category-bar.tsx`: Props `{ category: ScheinCategoryResult; locale: string; questionsSnapshot: QuestionsSnapshot }`. Horizontal bar per UI-SPEC §Interaction 8. Fill width = (weightedScore / maxWeightedScore) × 100%. Colour by category-level threshold (< 30% × maxWeighted → green; ≤ 60% → amber; else red) — NOT the total threshold. Threshold markers at 0.3 × max and 0.6 × max. role="img" + aria-label. Expandable → renders `<DrvCriterionBreakdownList>`.

6. `outcome/drv-criterion-breakdown-list.tsx`: shadcn Table with columns Criterion | Answer | Score. Reads prompts from `questionsSnapshot.questions` filtered by category. Include TableCaption for accessibility.

7. `outcome/outcome-print-layout.tsx`: Wrapper component that applies `@media print` styles via a `<style jsx global>` block OR a dedicated className that the app's global CSS handles. Force-expand Collapsibles, hide chrome, preserve semantic-triad colours per UI-SPEC §Interaction 9. Include print header + footer with the rule-set version + timestamp + "unofficial preview" note.

8. Write the three test files per the behavior contract. For OC-6 (snapshot vs live) use `vi.mock('@contractor-ops/classification/profiles/ir35/rule-set', ...)` — assert the mocked value is NOT reflected in rendered DOM.
  </action>
  <acceptance_criteria>
    - `grep -l "role=\"alertdialog\"" apps/web/src/components/contractors/classification/classification-disclaimer-dialog.tsx` returns the file.
    - `grep -l "onEscapeKeyDown" apps/web/src/components/contractors/classification/classification-disclaimer-dialog.tsx` returns the file.
    - `grep -l "DISCLAIMER_IR35_BODY\|DISCLAIMER_SCHEIN_BODY" apps/web/src/components/contractors/classification/classification-disclaimer-dialog.tsx` returns the file.
    - `grep -l "questionsSnapshot" apps/web/src/app/[locale]/contractors/[id]/engagements/[engagementId]/classification/[assessmentId]/page.tsx` returns the file.
    - `grep -rE "from '.*profiles/.*/rule-set" apps/web/src/app/[locale]/contractors/[id]/engagements/[engagementId]/classification/[assessmentId]/` returns NOTHING (catches both relative and `@contractor-ops/classification/profiles/*/rule-set` scoped-package imports — Pitfall 1 enforced).
    - `grep -l "role=\"status\"" apps/web/src/components/contractors/classification/outcome/verdict-banner.tsx` returns the file.
    - `pnpm --filter @contractor-ops/web test -- classification` runs all disclaimer + outcome + a11y tests green.
    - `pnpm --filter @contractor-ops/web typecheck` passes.
    - Building `pnpm --filter @contractor-ops/web build` succeeds (route registered, no TS errors).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/web test -- classification && pnpm --filter @contractor-ops/web typecheck</automated>
  </verify>
  <done>Outcome page renders SSR for both rule sets, force-shows blocking disclaimer until acknowledged, reads exclusively from questionsSnapshot, is WCAG 2.1 AA, and prints cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classification tile on CountryComplianceSection + assessment list route + engagement CTA</name>
  <files>apps/web/src/components/contractors/classification/classification-tile.tsx, apps/web/src/components/contractors/classification/classification-engagement-cta.tsx, apps/web/src/components/contractors/classification/classification-assessment-list.tsx, apps/web/src/app/[locale]/contractors/[id]/classification/page.tsx, apps/web/src/components/contractors/compliance/country-compliance-section.tsx, apps/web/src/components/contractors/classification/__tests__/classification-tile.test.tsx, apps/web/src/components/contractors/compliance/__tests__/country-compliance-section.test.tsx</files>
  <read_first>
    - apps/web/src/components/contractors/compliance/country-compliance-section.tsx (Phase 56 — existing dispatch; extend WITHOUT removing any export)
    - .planning/phases/58-classification-engine-rule-sets/58-UI-SPEC.md §Interaction 10–11 (tile + list layouts)
    - .planning/phases/58-classification-engine-rule-sets/58-VALIDATION.md CLASS-11 test rows (listByContractor ordering)
    - apps/web/src/app/[locale]/contractors/[id]/page.tsx (contractor detail page)
  </read_first>
  <behavior>
    ### RTL tests (add `apps/web/src/components/contractors/classification/__tests__/classification-tile.test.tsx` and extend `apps/web/src/components/contractors/compliance/__tests__/country-compliance-section.test.tsx`):

    **ClassificationTile:**
    - TEST CT-1 (loading): Mock `trpc.classification.getLatest.useQuery` to return `{ isLoading: true, data: undefined }` → renders a Skeleton (assert `data-testid="classification-tile-skeleton"` or `[aria-busy="true"]`), NO verdict pill, NO empty-state copy.
    - TEST CT-2 (empty / no assessment): Mock getLatest to return `{ isLoading: false, data: null }` → renders the empty-state copy (`t('emptyState.noAssessment')`) + a primary CTA button whose label matches `t('emptyState.runAssessment')` (copy: "Run classification assessment"); asserts clicking the CTA navigates to the wizard route `/[locale]/contractors/[id]/engagements/[engagementId]/classification`.
    - TEST CT-3 (completed): Mock getLatest to return a completed IR35 assessment with verdict="outside" and `completedAt` = now−48h → renders the verdict pill (colour + icon + text label — semantic triad; assert text matches "Outside IR35" in EN), a relative-date string produced via `Intl.RelativeTimeFormat` (assert output contains "2 days" or locale equivalent — use `vi.useFakeTimers` + fixed `Date.now()`), a "View details" Link whose href === `/[locale]/contractors/[id]/engagements/[engagementId]/classification/{assessmentId}`, and a "Re-run assessment" button.

    **CountryComplianceSection extension:**
    - TEST CCS-1 (GB dispatch): Given a contractor with two ContractorAssignments, both with `contractor.countryCode = 'GB'` → the Classification sub-section renders exactly 2 `<ClassificationTile />` (one per engagement). Use `screen.getAllByTestId('classification-tile')` and assert length === 2.
    - TEST CCS-2 (DE dispatch): Same but with `countryCode = 'DE'` → 2 tiles, DE-variant (tile consumes DRV verdict types when data lands — behavior-level the same).
    - TEST CCS-3 (non-GB/DE skip): Given an engagement with `countryCode = 'FR'` → NO ClassificationTile rendered (`queryByTestId('classification-tile')` returns null). Existing Phase-56 fields still render (regression guard: `getByTestId('country-compliance-gb-section')` or equivalent pre-existing testid still resolves).
    - TEST CCS-4 (mixed): One engagement GB + one engagement FR → exactly 1 tile rendered (for the GB engagement).

    Place `data-testid="classification-tile"` on the tile root and `data-testid="classification-tile-skeleton"` on the loading state so the CCS tests can query without coupling to internal structure.
  </behavior>
  <action>
1. Create `classification-tile.tsx` ('use client'):
   - Props: `{ engagement: ContractorAssignment; locale: string }`.
   - Calls `trpc.classification.getLatest.useQuery({ contractorAssignmentId: engagement.id })`.
   - If loading → Skeleton. If null → empty state ("No classification assessment yet" per UI-SPEC) + `<ClassificationEngagementCta>`. If present → verdict pill + completedAt (date-fns `format(completedAt, 'PP', { locale })`) + rule-set-version note + "View details" + "Re-run assessment" buttons.
   - Uses `<Bdi>` around engagement name.

2. Create `classification-engagement-cta.tsx` ('use client'): Primary teal Button that navigates to the Plan 04 wizard route. Prop: `{ engagement; contractorId; locale }`. Handles loading state while createDraft is in flight if we choose eager-call; simpler: just `<Link>` navigation — the page itself handles createDraft.

3. Create `classification-assessment-list.tsx` ('use client'):
   - Calls `trpc.classification.listByContractor.useQuery({ contractorId })`.
   - Renders shadcn Table ≥1024 px with columns per UI-SPEC §Interaction 11 (Engagement | Country | Rule-set version | Verdict pill | Completed at | Actions).
   - < 1024 px: vertical card list (stack the same info). Use `hidden lg:table-row-group` / `lg:hidden` pattern.
   - Draft rows get a `[Draft]` Badge; sorted by the tRPC response order (already draft-first then completedAt DESC per Plan 03).
   - TableCaption with sr-only text per UI-SPEC §Accessibility.
   - Empty state per UI-SPEC.

4. Create `apps/web/src/app/[locale]/contractors/[id]/classification/page.tsx` as an RSC that renders `<ClassificationAssessmentList contractorId={params.id} />` inside the standard contractor-profile layout.

5. Edit `apps/web/src/components/contractors/compliance/country-compliance-section.tsx`:
   - Load the contractor's ContractorAssignments (where status='active' or similar) server-side — match whatever data the component already has access to.
   - For each engagement whose contractor.countryCode is in ['GB', 'DE'], render `<ClassificationTile engagement={...} locale={locale} />` inside a dedicated "Classification" sub-section.
   - Do NOT remove or reorder any existing Phase-56 fields. Append the classification block after the existing country-specific field group. Guard with a feature comment: `// Phase 58 addition — per-engagement classification per CLASS-11`.
  </action>
  <acceptance_criteria>
    - `grep -l "ClassificationTile" apps/web/src/components/contractors/compliance/country-compliance-section.tsx` returns the file.
    - `grep -l "getLatest" apps/web/src/components/contractors/classification/classification-tile.tsx` returns the file.
    - `grep -l "listByContractor" apps/web/src/components/contractors/classification/classification-assessment-list.tsx` returns the file.
    - Phase 56 component tests still pass (no regression): `pnpm --filter @contractor-ops/web test -- country-compliance-section`.
    - `pnpm --filter @contractor-ops/web typecheck` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/web test -- country-compliance-section classification-tile classification-assessment-list && pnpm --filter @contractor-ops/web typecheck</automated>
  </verify>
  <done>Classification tile appears on contractor profile for GB/DE engagements; assessment list route works; engagement CTA launches wizard; Phase 56 pattern extended without regression.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Steuerberater + UK tax-adviser regulatory sign-off before phase ships</name>
  <files>packages/classification/REVIEWED.md, .planning/phases/58-classification-engine-rule-sets/58-VALIDATION.md</files>
  <read_first>
    - packages/classification/src/profiles/ir35/rule-set.ts (Plan 02 — question inventory)
    - packages/classification/src/profiles/scheinselbstandigkeit/rule-set.ts (Plan 02 — criterion inventory)
    - packages/validators/src/legal/disclaimers.ts (Plan 01 — disclaimer bodies)
    - .planning/phases/58-classification-engine-rule-sets/58-VALIDATION.md §Manual-Only Verifications
    - .planning/phases/56-country-foundations-german-i18n/56-*-SUMMARY.md (reference: Phase 56 Steuerberater review track pattern)
  </read_first>
  <what-built>
    - IR35 rule-set inventory (25 questions across 5 areas, each with caseLawCitation) — Plan 02
    - DRV Scheinselbständigkeit rule-set (20 criteria across 4 categories, each with drvReference) — Plan 02
    - Locked German legal phrases (CLASSIFICATION_SCHEIN_*) — Plan 01
    - Bilingual disclaimer bodies (DISCLAIMER_IR35_BODY, DISCLAIMER_SCHEIN_BODY) — Plan 01
    - Wizard + outcome pages (with all prompts rendered in EN/PL/DE) — Plans 04 + 05
    - REVIEWED.md artifact captures both sign-offs before phase ships
  </what-built>
  <action>
Before pausing the pipeline for human verification, the executor prepares and commits `packages/classification/REVIEWED.md` with the following sections (ready to receive external review):

1. `# Classification Rule-Set Review — Phase 58`
2. `## Scope`: what the reviewers are being asked to sign off on (IR35 inventory + DRV inventory + weights/thresholds + disclaimers + DE register).
3. `## IR35 Question Inventory`: full EN wording, caseLawCitation per question, PL + DE translation notes flagged `REVIEW:PL` / `REVIEW:DE`.
4. `## DRV Criterion Inventory`: full DE verbatim (formal Sie) + EN translation; drvReference per criterion; CATEGORY_WEIGHTS (30/30/25/15); THRESHOLDS (29.9 / 30 / 60 / 60.1); justification excerpt linking RESEARCH §Regulatory Domain.
5. `## Disclaimer Bodies`: DISCLAIMER_IR35_BODY + DISCLAIMER_SCHEIN_BODY + DISCLAIMER_*_ACKNOWLEDGEMENT — verbatim.
6. `## Screenshots`: paths to committed PNGs showing IR35 outcome (inside / outside / undetermined), DRV outcome (green / amber / red), disclaimer modal (EN + DE) on a seeded assessment.
7. `## Checklist`: case-law citations accurate; formal Sie register consistent; drvReference accurate; weight split acceptable; legally-locked phrases unmodified.
8. `## Steuerberater sign-off` — placeholder for name, date, signature/reference, amendments list.
9. `## UK tax-adviser sign-off` — same placeholder block.

Send the package to (1) the commissioned Steuerberater (same reviewer track as Phase 56) and (2) a qualified UK tax adviser (CIOT/ATT or CTA certified). Capture each reviewer's corrections, name, date, and signature/reference inside the respective sign-off section in REVIEWED.md.

If either reviewer returns corrections:
- Record them under the sign-off section.
- If corrections are non-material (typos, phrasing) — fix in a follow-up commit referencing REVIEWED.md and re-run the phase verify suite.
- If corrections are material (inventory additions/removals, weight changes, scoring-rule changes) — open a follow-up `58-06-PLAN.md` scoped to the amendments; do NOT silently edit Plan-02 rule-set files without re-triggering this checkpoint.

After BOTH sign-offs land:
- Flip `.planning/phases/58-classification-engine-rule-sets/58-VALIDATION.md` frontmatter `nyquist_compliant: false` → `nyquist_compliant: true` and set `status: approved YYYY-MM-DD`.
- Commit REVIEWED.md + VALIDATION.md with a single commit: `docs(58): Steuerberater + UK tax-adviser sign-off landed`.
  </action>
  <how-to-verify>
    Manual verification (in addition to the automated checks below):
    1. Run the full IR35 wizard end-to-end (start → 5 steps → Submit → disclaimer → outcome) locally — confirm draft autosave on reload, rule-set drift notice when expected, disclaimer modal non-dismissable via Escape/overlay, print output preserves semantic triad.
    2. Run the full DRV wizard end-to-end (4 steps including economic-dependency numeric input → Submit → disclaimer → traffic-light outcome) — confirm category bar fill is proportional, criterion breakdown expands, DE Sie register throughout.
    3. Verify REVIEWED.md has BOTH `## Steuerberater sign-off` and `## UK tax-adviser sign-off` sections populated with reviewer name + date + signature reference.
    4. Confirm VALIDATION.md frontmatter `nyquist_compliant: true` and `status: approved YYYY-MM-DD` landed.
  </how-to-verify>
  <verify>
    <automated>grep -l 'Steuerberater sign-off' packages/classification/REVIEWED.md && grep -l 'UK tax-adviser sign-off' packages/classification/REVIEWED.md && grep -l 'nyquist_compliant: true' .planning/phases/58-classification-engine-rule-sets/58-VALIDATION.md</automated>
  </verify>
  <done>REVIEWED.md exists with both sign-off sections filled in (reviewer name + date + signature reference + amendments captured); VALIDATION.md nyquist_compliant=true and status=approved; any amendments have been integrated OR scheduled as 58-06-PLAN.md; manual wizard end-to-end smoke passes both IR35 and DRV paths.</done>
  <resume-signal>Type "approved" + paste the REVIEWED.md sign-off block summary (Steuerberater name + date; UK tax-adviser name + date; amendments list or "none"). If amendments required, list them and set plan status back to in-progress — executor addresses them and re-invokes this checkpoint.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter @contractor-ops/web test` — all Phase 58 component + integration + a11y tests green
- `pnpm --filter @contractor-ops/web typecheck` — strict
- `pnpm --filter @contractor-ops/classification test && pnpm --filter @contractor-ops/api test && pnpm --filter @contractor-ops/validators test` — all still green (no regression across the stack)
- `grep -rE "from '.*profiles/.*/rule-set" apps/web/src/app/[locale]/contractors/[id]/engagements/[engagementId]/classification/[assessmentId]/` returns NOTHING (catches both relative and scoped-package imports — snapshot enforcement)
- REVIEWED.md exists with both sign-offs; VALIDATION.md `nyquist_compliant` flipped true
</verification>

<success_criteria>
- Outcome page SSR, disclaimer-gated, snapshot-driven, semantic-triad rendered.
- Classification tile integrated into Phase 56 CountryComplianceSection per-engagement for GB/DE.
- Assessment list route works with the draft-first, completedAt DESC ordering from Plan 03.
- All 4 ROADMAP §Phase 58 success criteria satisfied (start assessment + select rule set / IR35 5-area result / DRV 4-category weighted traffic-light / per-engagement independent history).
- External-reviewer sign-off captured in REVIEWED.md for both Steuerberater + UK tax-adviser.
- VALIDATION.md `nyquist_compliant: true` after both sign-offs + manual e2e smoke.
- Phase 56 pattern extended without regression (CountryComplianceSection tests still pass).
</success_criteria>

<output>
After completion, create `.planning/phases/58-classification-engine-rule-sets/58-05-SUMMARY.md` summarizing: outcome pages built, disclaimer dialog behaviour, CountryComplianceSection extension, list route, print-layout strategy, REVIEWED.md status (pending vs landed), any amendments captured from the human-verify checkpoint, VALIDATION.md final state.
</output>
