---
phase: 58
plan: 02
type: tdd
wave: 2
depends_on: [58-01]
files_modified:
  - packages/classification/src/profiles/ir35/index.ts
  - packages/classification/src/profiles/ir35/rule-set.ts
  - packages/classification/src/profiles/ir35/scoring.ts
  - packages/classification/src/profiles/ir35/area-scoring.ts
  - packages/classification/src/profiles/ir35/__tests__/scoring.test.ts
  - packages/classification/src/profiles/ir35/__tests__/rule-set.test.ts
  - packages/classification/src/profiles/scheinselbstandigkeit/index.ts
  - packages/classification/src/profiles/scheinselbstandigkeit/rule-set.ts
  - packages/classification/src/profiles/scheinselbstandigkeit/scoring.ts
  - packages/classification/src/profiles/scheinselbstandigkeit/__tests__/scoring.test.ts
  - packages/classification/src/profiles/scheinselbstandigkeit/__tests__/rule-set.test.ts
  - packages/classification/src/__tests__/snapshot.test.ts
  - packages/classification/src/index.ts
  - packages/validators/src/legal/de.ts
autonomous: true
requirements: [CLASS-01, CLASS-02, CLASS-05]
requirements_addressed: [CLASS-01, CLASS-02, CLASS-05]

must_haves:
  truths:
    - "IR35 scoring obeys the D-13 dispositive rules: strong-inside on Substitution OR MOO → inside (even against any number of leaning-outside elsewhere); strong-outside on Substitution → outside (unless dispositive-inside fires first)."
    - "IR35 composite rule counts leaning signals across the 5 areas: ≥3 leaning-inside → inside; ≥3 leaning-outside → outside; otherwise undetermined; ≥2 neutral critical areas → undetermined."
    - "DRV scoring uses the exact D-14 category weights (integration 30, entrepreneurial 30, personal-dep 25, economic-dep 15 — summing to 100) and exact thresholds (< 30 green, 30–60 amber, > 60 red) with the boundary points 29.9 → green, 30 → amber, 60 → amber, 60.1 → red."
    - "DRV 'Nicht anwendbar' answer is stored as rawScore=0 with isNotApplicable=true and is distinguishable from a missing answer (missing blocks submit)."
    - "DRV economic-dependency billing-ratio input is mapped to rawScore bands per D-15 / §2 Nr 9 SGB VI: 0–50% → 0, 50–70% → 1, 70–83% → 2, >83% → 3 (verified at 50, 70, 83, 84)."
    - "Every IR35 question carries a caseLawCitation; every DRV criterion carries a drvReference (enforced by rule-set tests)."
    - "Profile classes register themselves on module import so `getProfileForCountry('GB')` returns IR35Profile and `getProfileForCountry('DE')` returns ScheinselbstandigkeitProfile."
    - "buildQuestionsSnapshot preserves the rule set deterministically; a test that mocks a later rule-set constant change does NOT mutate a previously-built snapshot."
  artifacts:
    - path: packages/classification/src/profiles/ir35/scoring.ts
      provides: "scoreIr35 pure function — dispositive-first order per D-13"
      contains: "scoreIr35"
    - path: packages/classification/src/profiles/ir35/rule-set.ts
      provides: "IR35_QUESTIONS as const + RULE_SET_VERSION='IR35-2024-CEST'"
      contains: "IR35-2024-CEST"
    - path: packages/classification/src/profiles/ir35/index.ts
      provides: "IR35Profile class implementing ClassificationProfile + self-registration"
      contains: "registerProfile"
    - path: packages/classification/src/profiles/scheinselbstandigkeit/scoring.ts
      provides: "scoreSchein pure function — weighted sum + thresholds per D-14"
      contains: "scoreSchein"
    - path: packages/classification/src/profiles/scheinselbstandigkeit/rule-set.ts
      provides: "SCHEIN_QUESTIONS + CATEGORY_WEIGHTS + THRESHOLDS + RULE_SET_VERSION='SCHEINSELBSTANDIGKEIT-DRV-2024'"
      contains: "SCHEINSELBSTANDIGKEIT-DRV-2024"
    - path: packages/classification/src/profiles/scheinselbstandigkeit/index.ts
      provides: "ScheinselbstandigkeitProfile class + self-registration"
      contains: "registerProfile"
  key_links:
    - from: packages/classification/src/profiles/ir35/index.ts
      to: packages/classification/src/registry.ts
      via: "side-effect registerProfile call on module import"
      pattern: "registerProfile\\("
    - from: packages/classification/src/profiles/ir35/rule-set.ts
      to: packages/validators/src/legal/de.ts
      via: "import of CLASSIFICATION_SCHEIN_* constants for DE prompts"
      pattern: "from .*validators/legal"
    - from: packages/classification/src/profiles/scheinselbstandigkeit/rule-set.ts
      to: packages/validators/src/legal/de.ts
      via: "verbatim import of CLASSIFICATION_SCHEIN_* category titles"
      pattern: "CLASSIFICATION_SCHEIN_"
---

<objective>
Implement the two country rule sets (IR35, Scheinselbständigkeit) as typed `as const` TypeScript constants (D-06), the two pure scoring functions (D-13 composite-rule for IR35, D-14 weighted-sum for DRV), and the two profile classes that register themselves into the registry on import. The DE rule set imports category titles verbatim from packages/validators/src/legal/de.ts so the locked-phrase guard catches any regression.

This plan is the regulatory heart of the phase. Scoring is the only code in Phase 58 that has legal-liability exposure — test coverage MUST include every dispositive combination, every threshold boundary, and the "Nicht anwendbar ≠ missing" distinction.

Purpose: Unlock Plan 03 (router submit handler calls scoreIr35 / scoreSchein), Plan 04 (wizard renders rule-set constants), and Plan 05 (outcome page reads snapshot questions).

Output: `getProfileForCountry('GB')` returns a working IR35Profile, `getProfileForCountry('DE')` returns a working ScheinselbstandigkeitProfile, both with full scoreAssessment + buildAssessment + renderOutcome implementations and exhaustive tests.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.planning/phases/58-classification-engine-rule-sets/58-CONTEXT.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.planning/phases/58-classification-engine-rule-sets/58-RESEARCH.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.planning/phases/58-classification-engine-rule-sets/58-VALIDATION.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.planning/phases/58-classification-engine-rule-sets/58-UI-SPEC.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.planning/phases/58-classification-engine-rule-sets/58-01-SUMMARY.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/CLAUDE.md

<interfaces>
<!-- Types declared in Plan 01 this plan consumes -->

From packages/classification/src/types/profile.ts:
```typescript
export interface ClassificationProfile {
  readonly profileId: string;
  readonly country: string;
  readonly displayName: string;
  readonly ruleSetVersion: string;
  buildAssessment(engagementId: string): AssessmentShell;
  scoreAssessment(answers: AnswerMap): Outcome;
  renderOutcome(assessment: Assessment): OutcomeView;
}
```

From packages/classification/src/types/outcome.ts:
```typescript
export type Ir35AreaVerdict =
  | 'strong-outside' | 'leaning-outside' | 'neutral' | 'leaning-inside' | 'strong-inside';
export type Ir35Area =
  | 'substitution' | 'control' | 'financial-risk' | 'part-and-parcel' | 'moo';
export interface Ir35AreaResult {
  area: Ir35Area;
  verdict: Ir35AreaVerdict;
  drivingQuestionIds: string[]; // max 3
}
export interface Ir35Outcome {
  kind: 'IR35';
  verdict: 'inside' | 'outside' | 'undetermined';
  areaResults: Ir35AreaResult[]; // length 5
  reasoning: string;
}
export type ScheinCategory = 'integration' | 'entrepreneurial' | 'personal-dep' | 'economic-dep';
export interface ScheinCategoryResult {
  category: ScheinCategory;
  weight: number;
  weightedScore: number;
  maxWeightedScore: number;
  criterionAnswers: { questionId: string; rawScore: 0 | 1 | 2 | 3 }[];
}
export interface ScheinselbstandigkeitOutcome {
  kind: 'SCHEINSELBSTANDIGKEIT';
  riskLevel: 'green' | 'amber' | 'red';
  totalScore: number;
  categoryBreakdown: ScheinCategoryResult[]; // length 4
}
export type Outcome = Ir35Outcome | ScheinselbstandigkeitOutcome;
```

From packages/classification/src/types/assessment.ts:
```typescript
export type AnswerMap = Record<string, { rawScore?: 0|1|2|3; value?: unknown; isNotApplicable?: boolean }>;
export interface AssessmentShell { ruleSetVersion: string; questions: readonly RuleSetQuestion[]; }
```

From packages/classification/src/types/rule-set.ts — see Plan 01.

From packages/classification/src/registry.ts:
```typescript
export function registerProfile(profile: ClassificationProfile): void;
export function getProfileForCountry(countryCode: string): ClassificationProfile;
export function clearProfiles(): void;
```

From packages/classification/src/snapshot.ts:
```typescript
export function buildQuestionsSnapshot(profile: ClassificationProfile): {
  ruleSetVersion: string; profileId: string; questions: readonly RuleSetQuestion[];
};
```

From packages/validators/src/legal/de.ts (Plan 01 additions):
```typescript
export const CLASSIFICATION_SCHEIN_INTEGRATION: 'Eingliederung in die Arbeitsorganisation';
export const CLASSIFICATION_SCHEIN_ENTREPRENEURIAL: 'Unternehmerische Selbstständigkeit';
export const CLASSIFICATION_SCHEIN_PERSONAL_DEP: 'Persönliche Abhängigkeit';
export const CLASSIFICATION_SCHEIN_ECONOMIC_DEP: 'Wirtschaftliche Abhängigkeit';
export const CLASSIFICATION_SCHEIN_NOT_APPLICABLE: 'Nicht anwendbar';
```
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source → persisted outcome | Scoring function output is persisted verbatim; a bug here becomes a permanent audit artifact |
| rule-set constant → persisted snapshot | Once serialized, snapshot is load-bearing for regulatory audit |

## STRIDE Threat Register (applicable ASVS: V5 Input Validation, V10 Malicious Code)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-05 | Tampering | packages/classification/src/profiles/*/scoring.ts | mitigate | Scoring is a pure function with no I/O; test coverage of every dispositive + composite + threshold case catches drift. |
| T-58-06 | Integrity | rule-set constants | mitigate | `as const` TypeScript — compile-time frozen; buildQuestionsSnapshot Object.freeze prevents post-load mutation; snapshot test asserts mutation of later constant does NOT affect earlier snapshot. |
| T-58-07 | Tampering | DRV CATEGORY_WEIGHTS | mitigate | Static test asserts weights sum to exactly 100; any PR that shifts weights without updating fails CI. |
| T-58-08 | Malicious Code | rule-set prompts | mitigate | All prompts are string literals in source; no dynamic eval; no template-injection path (V10). |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement IR35 rule-set constants, scoring function, profile class + exhaustive tests</name>
  <files>packages/classification/src/profiles/ir35/rule-set.ts, packages/classification/src/profiles/ir35/area-scoring.ts, packages/classification/src/profiles/ir35/scoring.ts, packages/classification/src/profiles/ir35/index.ts, packages/classification/src/profiles/ir35/__tests__/scoring.test.ts, packages/classification/src/profiles/ir35/__tests__/rule-set.test.ts, packages/classification/src/index.ts</files>
  <read_first>
    - packages/einvoice/src/profiles/ksef/index.ts (profile class pattern — constructor + method shape)
    - packages/classification/src/registry.ts (Plan 01 — self-registration pattern)
    - packages/classification/src/types/rule-set.ts, outcome.ts, assessment.ts (Plan 01)
    - .planning/phases/58-classification-engine-rule-sets/58-RESEARCH.md §Regulatory Domain → IR35 (five areas, case law, ~22-26 question inventory with exact IDs Q-SUB-01..Q-MOO-05)
    - .planning/phases/58-classification-engine-rule-sets/58-RESEARCH.md §Code Examples → IR35 scoring test coverage (every dispositive + composite case — use this as the test skeleton)
    - .planning/phases/58-classification-engine-rule-sets/58-RESEARCH.md §Pitfall 3 (dispositive ORDER matters)
    - .planning/phases/58-classification-engine-rule-sets/58-VALIDATION.md CLASS-02 rows (test requirements)
  </read_first>
  <behavior>
    ### Tests FIRST (RED) — write before implementation:

    **scoring.test.ts — dispositive rules (D-13):**
    - TEST Dispositive-1: sub=strong-inside, ctrl=leaning-outside, fin=leaning-outside, pp=leaning-outside, moo=leaning-outside → verdict='inside' (dispositive inside wins over 4 leaning-outside).
    - TEST Dispositive-2: sub=neutral, ctrl=neutral, fin=neutral, pp=neutral, moo=strong-inside → 'inside'.
    - TEST Dispositive-3: sub=strong-outside, ctrl=leaning-inside, fin=leaning-inside, pp=leaning-inside, moo=neutral → 'outside' (strong-outside on sub wins; no MOO strong-inside to trump).
    - TEST Dispositive-4 (precedence): sub=strong-outside, ctrl=neutral, fin=neutral, pp=neutral, moo=strong-inside → 'inside' (MOO dispositive-inside wins over sub dispositive-outside per evaluation ORDER).
    - TEST Dispositive-5: sub=strong-inside, moo=strong-inside, others neutral → 'inside'.

    **scoring.test.ts — composite rule:**
    - TEST Composite-1: 3× leaning-inside, 2× neutral → 'inside'.
    - TEST Composite-2: 3× leaning-outside, 2× neutral → 'outside'.
    - TEST Composite-3: all 5 neutral → 'undetermined'.
    - TEST Composite-4: sub=neutral, moo=neutral, ctrl=leaning-inside, fin=leaning-outside, pp=leaning-inside → 'undetermined' (2 neutral critical).
    - TEST Composite-5: sub=leaning-inside, moo=leaning-outside, ctrl=leaning-inside, fin=leaning-inside, pp=neutral → 'inside' (≥3 leaning-inside counting sub).
    - TEST Composite-6: 2× leaning-inside, 2× leaning-outside, 1× neutral (non-critical) → 'undetermined'.

    **scoring.test.ts — areaResults shape:**
    - TEST Shape-1: Output has areaResults array of length 5 with one entry per area in fixed order [substitution, control, financial-risk, part-and-parcel, moo].
    - TEST Shape-2: drivingQuestionIds returns up to 3 question IDs per area (the answers that contributed most to the verdict).

    **rule-set.test.ts:**
    - TEST Inventory-1: IR35_QUESTIONS.length is between 22 and 26 inclusive.
    - TEST Inventory-2: Each of the 5 Ir35Area values has at least 3 questions.
    - TEST Inventory-3: Every question has a non-empty caseLawCitation.
    - TEST Inventory-4: Every question has prompt.en, prompt.pl, prompt.de all non-empty.
    - TEST Inventory-5: Every questionId is unique.
    - TEST Inventory-6: RULE_SET_VERSION === 'IR35-2024-CEST'.
    - TEST Inventory-7: IR35Profile.country === 'GB', profileId === 'ir35', displayName includes 'IR35'.

    ### Implementation (GREEN):
  </behavior>
  <action>
1. Create `packages/classification/src/profiles/ir35/rule-set.ts`:
   - Export `RULE_SET_VERSION = 'IR35-2024-CEST' as const`.
   - Export `IR35_QUESTIONS` as a typed `as const` array of RuleSetQuestion covering the inventory from RESEARCH §Regulatory Domain → IR35:
     - Area 'substitution': Q-SUB-01..Q-SUB-05 (5 questions, all answerType 'yes-no', required: Q-01..Q-03 true, Q-04..Q-05 false).
     - Area 'control': Q-CTRL-01..Q-CTRL-06 (6 questions).
     - Area 'financial-risk': Q-FIN-01..Q-FIN-05 (5 questions). Q-FIN-02 has an answerType 'likert-5' to capture "somewhat" nuance per 2025 CEST sharpening.
     - Area 'part-and-parcel': Q-PP-01..Q-PP-04 (4 questions).
     - Area 'moo': Q-MOO-01..Q-MOO-05 (5 questions).
   - Total: 25 questions. Every question MUST have caseLawCitation (Ready Mixed Concrete [1968] 2 QB 497, Atholl House [2022] UKSC, PGMOL [2024] UKSC, Hall v Lorimer [1994] ICR 218 — attribute per area per RESEARCH table).
   - prompt.en verbatim from RESEARCH; prompt.pl + prompt.de good-faith translations (Steuerberater / UK tax-adviser will review — mark with `// REVIEW:PL` / `// REVIEW:DE` comment above each).
   - helpText short (≤ 160 chars per locale) — explains jurisprudential context in plain language.

2. Create `packages/classification/src/profiles/ir35/area-scoring.ts`:
   - Export `scoreIr35Area(area: Ir35Area, answers: AnswerMap): { verdict: Ir35AreaVerdict; drivingQuestionIds: string[] }`.
   - Algorithm: for each question in the area, map the answer to a directional weight:
     - Per the RESEARCH inventory, each question carries an inline `// INFLUENCE: yes→outside|yes→inside` comment. Implement a lookup table in `area-scoring.ts` mapping questionId → (expected Yes direction: 'outside-leaning' | 'inside-leaning' | 'outside-strong' | 'inside-strong' | 'neutral').
     - Tally: strongInside, strongOutside, leaningInside, leaningOutside counters.
     - Verdict rules per area:
       - Any strong-inside answer (e.g. Q-SUB-05 'substitution prohibited' = Yes) → area verdict 'strong-inside'.
       - Any strong-outside answer (e.g. Q-SUB-01 unrestricted substitution right = Yes) → area verdict 'strong-outside'.
       - ≥ 2 leaning-inside and 0 leaning-outside → 'leaning-inside'.
       - ≥ 2 leaning-outside and 0 leaning-inside → 'leaning-outside'.
       - Otherwise → 'neutral'.
     - drivingQuestionIds: top 3 answered questions (by influence magnitude) contributing to the verdict.

3. Create `packages/classification/src/profiles/ir35/scoring.ts`:
   - Implement `scoreIr35(answers: AnswerMap): Ir35Outcome` VERBATIM per RESEARCH §Regulatory Domain reference implementation + Pitfall 3:
     ```typescript
     // ORDER IS LOAD-BEARING. Dispositive rules FIRST:
     //   1. areas.substitution === 'strong-inside' OR areas.moo === 'strong-inside' → inside
     //   2. areas.substitution === 'strong-outside' → outside
     // Then composite:
     //   count leaning-inside (incl strong-inside), leaning-outside (incl strong-outside)
     //   ≥3 leaning-inside → inside
     //   ≥3 leaning-outside → outside
     //   ≥2 neutral critical (substitution + moo) → undetermined
     //   else → undetermined
     ```
   - `reasoning` is a human-readable sentence built from the triggering rule: e.g. "Inside IR35 — Mutuality of Obligation strong-inside is dispositive per PGMOL [2024] UKSC." or "Outside IR35 — 3 of 5 areas lean outside with Substitution neutral."

4. Create `packages/classification/src/profiles/ir35/index.ts`:
   - Export `IR35Profile` class implementing `ClassificationProfile` per D-02.
   - `buildAssessment(engagementId)` returns `{ ruleSetVersion: RULE_SET_VERSION, questions: IR35_QUESTIONS }`.
   - `scoreAssessment(answers)` delegates to `scoreIr35(answers)`.
   - `renderOutcome(assessment)` asserts `assessment.outcome.kind === 'IR35'` + returns a view model with the verdict label, per-area verdicts, and reasoning string.
   - At module bottom, side-effect: `registerProfile(new IR35Profile())` so `import '@contractor-ops/classification'` auto-registers on first load.

5. Edit `packages/classification/src/index.ts` to add: `export * from './profiles/ir35/index.js';` — ensures any module-level side effect on import triggers registration.

6. Port the verbatim RESEARCH §Code Examples → IR35 scoring test coverage into `__tests__/scoring.test.ts` and expand per the behavior contract above. Use a helper `mkAnswers({ sub, ctrl, fin, pp, moo })` that builds an AnswerMap yielding specific area verdicts (may require a second helper `forceAreaVerdict(area, verdict)` that injects synthetic answers producing that area verdict — document this in the helper).

7. Port rule-set.test.ts per behavior contract.
  </action>
  <acceptance_criteria>
    - `grep -c 'describe\|it\(' packages/classification/src/profiles/ir35/__tests__/scoring.test.ts` ≥ 20 (broad coverage per Pitfall 3).
    - `grep -l "IR35-2024-CEST" packages/classification/src/profiles/ir35/rule-set.ts` returns the file.
    - `grep -l "registerProfile(new IR35Profile" packages/classification/src/profiles/ir35/index.ts` returns the file.
    - `pnpm --filter @contractor-ops/classification test src/profiles/ir35` is GREEN — all dispositive + composite + inventory tests pass.
    - Running `node -e "const c = await import('@contractor-ops/classification'); console.log(c.getProfileForCountry('GB').profileId)"` prints `ir35`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/classification test src/profiles/ir35 && pnpm --filter @contractor-ops/classification typecheck</automated>
  </verify>
  <done>scoreIr35 is pure, dispositive-first, 25-question inventory with case-law citations; IR35Profile self-registers; all tests green including the order-of-evaluation precedence test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement DRV Scheinselbständigkeit rule-set, scoring, profile class + boundary + snapshot tests</name>
  <files>packages/classification/src/profiles/scheinselbstandigkeit/rule-set.ts, packages/classification/src/profiles/scheinselbstandigkeit/scoring.ts, packages/classification/src/profiles/scheinselbstandigkeit/index.ts, packages/classification/src/profiles/scheinselbstandigkeit/__tests__/scoring.test.ts, packages/classification/src/profiles/scheinselbstandigkeit/__tests__/rule-set.test.ts, packages/classification/src/__tests__/snapshot.test.ts, packages/classification/src/index.ts, packages/validators/src/legal/de.ts</files>
  <read_first>
    - packages/validators/src/legal/de.ts (Plan 01 — import CLASSIFICATION_SCHEIN_* constants verbatim)
    - packages/classification/src/profiles/ir35/index.ts (Task 1 — self-registration pattern)
    - packages/classification/src/snapshot.ts + __tests__/snapshot.test.ts (Plan 01)
    - .planning/phases/58-classification-engine-rule-sets/58-RESEARCH.md §Regulatory Domain → Scheinselbständigkeit (20-criterion inventory: DRV-INT-01..06, DRV-ENT-01..05, DRV-PER-01..05, DRV-ECO-01..04)
    - .planning/phases/58-classification-engine-rule-sets/58-RESEARCH.md §Code Examples → DRV scoring reference implementation + scoring test coverage (boundary tests 29.9/30/60/60.1)
    - .planning/phases/58-classification-engine-rule-sets/58-RESEARCH.md §Pitfall 4 (arithmetic drift) + §Pitfall 5 (Not Applicable ≠ missing)
    - .planning/phases/58-classification-engine-rule-sets/58-CONTEXT.md D-14, D-15
  </read_first>
  <behavior>
    ### Tests FIRST (RED):

    **scoring.test.ts:**
    - TEST Weight-1: `Object.values(CATEGORY_WEIGHTS).reduce((a,b)=>a+b)` === exactly 100.
    - TEST Weight-2: Every CATEGORY_WEIGHTS key matches a ScheinCategory literal exactly ('integration', 'entrepreneurial', 'personal-dep', 'economic-dep').
    - TEST Score-1: All 20 criteria rawScore=0 → totalScore === 0, riskLevel === 'green'.
    - TEST Score-2: All 20 criteria rawScore=3 → totalScore === 100, riskLevel === 'red'.
    - TEST Boundary-1: answer set yielding totalScore === 29.9 → riskLevel === 'green'.
    - TEST Boundary-2: answer set yielding totalScore === 30.0 → riskLevel === 'amber'.
    - TEST Boundary-3: answer set yielding totalScore === 60.0 → riskLevel === 'amber'.
    - TEST Boundary-4: answer set yielding totalScore === 60.1 → riskLevel === 'red'.
    - TEST NotApplicable-1: answer with rawScore=0, isNotApplicable=true contributes 0 to category sum AND is treated as an answered question (not missing).
    - TEST NotApplicable-2: missing answer (no key in answers map) for a required criterion throws a MissingAnswerError (or causes scoreSchein to throw an Error whose message contains 'missing required'). Rationale: Pitfall 5.
    - TEST EconDep-1: billing-ratio 50 → rawScore 0 for DRV-ECO-01.
    - TEST EconDep-2: billing-ratio 70 → rawScore 1.
    - TEST EconDep-3: billing-ratio 83 → rawScore 2.
    - TEST EconDep-4: billing-ratio 84 → rawScore 3.
    - TEST CategoryZero: a category with 0 answered criteria yields weightedScore 0 (no NaN — Pitfall 4).
    - TEST CategoryBreakdown: output.categoryBreakdown has length 4, order = [integration, entrepreneurial, personal-dep, economic-dep].

    **rule-set.test.ts:**
    - TEST Inventory-1: SCHEIN_QUESTIONS.length === exactly 20.
    - TEST Inventory-2: category counts: integration 6, entrepreneurial 5, personal-dep 5, economic-dep 4.
    - TEST Inventory-3: Every criterion has a non-empty drvReference.
    - TEST Inventory-4: Every criterion has prompt.de non-empty.
    - TEST Inventory-5: Every questionId is unique.
    - TEST Inventory-6: RULE_SET_VERSION === 'SCHEINSELBSTANDIGKEIT-DRV-2024'.
    - TEST Inventory-7: Category titles in the rule-set (prompt.de per category header) are the exact CLASSIFICATION_SCHEIN_* constants (imported from packages/validators/src/legal/de.ts — reference equality).
    - TEST Inventory-8: DRV-ECO-01 answerType === 'billing-ratio'.
    - TEST Inventory-9: ScheinselbstandigkeitProfile.country === 'DE', profileId === 'scheinselbstandigkeit'.

    **snapshot.test.ts (extended):**
    - TEST Snapshot-Immutable: Build a snapshot with buildQuestionsSnapshot(new ScheinselbstandigkeitProfile()); then simulate a rule-set update by mutating a local copy of SCHEIN_QUESTIONS (e.g. push a fake question into a clone) — assert the snapshot is unchanged (structuredClone was effective).
    - TEST Snapshot-LiveMock: Use `vi.mock('./rule-set.js', ...)` to inject a different questions array after snapshot creation; assert the SAVED snapshot still reflects the original.

    ### Implementation (GREEN):
  </behavior>
  <action>
1. Create `packages/classification/src/profiles/scheinselbstandigkeit/rule-set.ts`:
   - Import the 4 category title constants + 'Nicht anwendbar' from `@contractor-ops/validators/legal/de.js` — VERBATIM reference equality (important: rule-set.test.ts Inventory-7 checks reference equality, so use the imported const directly, not a re-declared string).
   - Export `RULE_SET_VERSION = 'SCHEINSELBSTANDIGKEIT-DRV-2024' as const`.
   - Export `CATEGORY_WEIGHTS = { integration: 30, entrepreneurial: 30, 'personal-dep': 25, 'economic-dep': 15 } as const;`
   - Export `THRESHOLDS = { green: 30, amber: 60 } as const;` (< green → green; ≤ amber → amber; > amber → red per D-14).
   - Export `CATEGORY_TITLES = { integration: CLASSIFICATION_SCHEIN_INTEGRATION, entrepreneurial: CLASSIFICATION_SCHEIN_ENTREPRENEURIAL, 'personal-dep': CLASSIFICATION_SCHEIN_PERSONAL_DEP, 'economic-dep': CLASSIFICATION_SCHEIN_ECONOMIC_DEP } as const;`
   - Export `SCHEIN_QUESTIONS` as a typed `as const` array of exactly 20 RuleSetQuestion items covering the inventory from RESEARCH §Regulatory Domain → Scheinselbständigkeit (DRV-INT-01..06, DRV-ENT-01..05, DRV-PER-01..05, DRV-ECO-01..04).
     - answerType 'score-0-3' for 19 of them; DRV-ECO-01 answerType 'billing-ratio'.
     - required: true for all 20 (D-11 — Nicht anwendbar is a valid answer).
     - Category 2 (Entrepreneurial Independence) criteria phrased in the NEGATIVE per RESEARCH scoring direction clarification ("Kein eigenes Betriebskapital", etc.) with a `// NOTE:` comment above the block explaining the direction invariant.
     - drvReference: every item carries a section reference (e.g. 'DRV Rundschreiben RS 2022/1 Abschnitt 3.2' or '§ 7a SGB IV' or 'BSG Urteil 12.12.2019 B 12 R 7/19 R').
     - prompt.de uses formal Sie register (per UI-SPEC Copywriting Contract); prompt.en + prompt.pl bilingual.

2. Create `packages/classification/src/profiles/scheinselbstandigkeit/scoring.ts` per RESEARCH reference implementation:
   - Implement `billingRatioToScore(ratio: number): 0|1|2|3`:
     - ratio < 50 → 0
     - 50 ≤ ratio < 70 → 1
     - 70 ≤ ratio < 84 → 2
     - ratio ≥ 84 → 3
     Inline comment the executor MUST include in the file: `// D-15 bands (§2 Nr 9 SGB VI): Zod enforces integer input (z.number().int()), so '< 84' ≡ '<= 83' and matches D-15 wording "> 83% → 3". Boundary tests: 50→1, 70→2, 83→2, 84→3.`
   - Implement `scoreSchein(answers: AnswerMap): ScheinselbstandigkeitOutcome`:
     - Validate every `required` criterion has an entry in `answers`; else throw `Error('scoreSchein: missing required answer for questionId=<id>')`.
     - For DRV-ECO-01: convert `answers['DRV-ECO-01'].value` (number 0-100) via billingRatioToScore.
     - For every other criterion: use `answers[id].rawScore` directly (Nicht anwendbar is rawScore=0 with isNotApplicable=true — still counted).
     - For each category: compute sumRaw, maxRaw (criterionCount × 3), weightedScore = maxRaw === 0 ? 0 : (sumRaw / maxRaw) × categoryWeight.
     - totalScore = sum of all four weightedScore values.
     - riskLevel: totalScore < THRESHOLDS.green ? 'green' : totalScore <= THRESHOLDS.amber ? 'amber' : 'red'.
     - Return full ScheinselbstandigkeitOutcome shape matching outcome.ts.

3. Create `packages/classification/src/profiles/scheinselbstandigkeit/index.ts`:
   - Export `ScheinselbstandigkeitProfile` class implementing ClassificationProfile.
   - profileId 'scheinselbstandigkeit', country 'DE', displayName 'Scheinselbständigkeit (DE)', ruleSetVersion from rule-set.
   - `buildAssessment` returns { ruleSetVersion, questions: SCHEIN_QUESTIONS }.
   - `scoreAssessment` delegates to scoreSchein.
   - `renderOutcome` returns view model with riskLevel, totalScore, per-category breakdown + category title (from CATEGORY_TITLES).
   - Side-effect at module bottom: `registerProfile(new ScheinselbstandigkeitProfile())`.

4. Edit `packages/classification/src/index.ts`: add `export * from './profiles/scheinselbstandigkeit/index.js';` ensuring self-registration fires on barrel import.

5. Write scoring.test.ts + rule-set.test.ts per behavior contract. For boundary tests, construct `mkAnswers` helpers that arithmetically produce targeted totalScore values. Worked example: with 6 integration criteria at weight 30, each score point contributes (30/18) ≈ 1.667 points. Document the math in test comments so future refactors can reverify.

6. Extend `packages/classification/src/__tests__/snapshot.test.ts` with the Snapshot-Immutable + Snapshot-LiveMock tests described in the behavior contract. Use `vi.doMock('./profiles/scheinselbstandigkeit/rule-set.js', ...)` after snapshot is built to simulate a v2 rule-set — assert the saved snapshot still contains the v1 questions.

7. Do NOT modify packages/validators/src/legal/de.ts content — only ensure this plan imports the constants (Plan 01 already created them). The "packages/validators/src/legal/de.ts" line in files_modified is there because TypeScript resolves the imports and the build graph treats this file as a dependency — if an export is missing at TDD-GREEN time, add it here with a back-reference comment to Plan 01.
  </action>
  <acceptance_criteria>
    - `grep -c 'it\(\|test\(' packages/classification/src/profiles/scheinselbstandigkeit/__tests__/scoring.test.ts` ≥ 15 (covers all behavior rows).
    - `grep -l "SCHEINSELBSTANDIGKEIT-DRV-2024" packages/classification/src/profiles/scheinselbstandigkeit/rule-set.ts` returns the file.
    - `grep -l "CATEGORY_WEIGHTS = { integration: 30, entrepreneurial: 30" packages/classification/src/profiles/scheinselbstandigkeit/rule-set.ts` returns the file (or matches after Prettier).
    - `grep -l "CLASSIFICATION_SCHEIN_INTEGRATION" packages/classification/src/profiles/scheinselbstandigkeit/rule-set.ts` returns the file (verifies locked-phrase import).
    - `grep -c 'questionId' packages/classification/src/profiles/scheinselbstandigkeit/rule-set.ts` ≥ 20.
    - `grep -l "registerProfile(new ScheinselbstandigkeitProfile" packages/classification/src/profiles/scheinselbstandigkeit/index.ts` returns the file.
    - `pnpm --filter @contractor-ops/classification test` GREEN for IR35 + DRV + snapshot + registry.
    - `pnpm --filter @contractor-ops/validators test` still GREEN (locked-phrase guard unaffected).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/classification test && pnpm --filter @contractor-ops/classification typecheck && pnpm --filter @contractor-ops/validators test</automated>
  </verify>
  <done>20-criterion DRV inventory with weights summing to 100, thresholds 29.9/30/60/60.1 verified, Nicht-anwendbar distinguishable from missing, billing-ratio banding 50/70/83/84 verified, snapshot immutable under constant drift, ScheinselbstandigkeitProfile self-registers.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @contractor-ops/classification test` — all IR35 dispositive + composite + DRV weighted + threshold + snapshot tests green
- `pnpm --filter @contractor-ops/classification typecheck` — strict TS, no `any`
- `pnpm --filter @contractor-ops/validators test` — locked-phrase guard still green (no regressions from rule-set imports)
- `node -e "import('@contractor-ops/classification').then(c => { console.log(c.getProfileForCountry('GB').profileId); console.log(c.getProfileForCountry('DE').profileId); })"` prints `ir35` and `scheinselbstandigkeit`
</verification>

<success_criteria>
- `scoreIr35` obeys D-13 dispositive-first ordering with explicit precedence test (MOO strong-inside beats Substitution strong-outside).
- `scoreSchein` weights sum exactly 100 and thresholds 29.9/30/60/60.1 verified at boundaries.
- 25 IR35 questions and 20 DRV criteria in source with required citations/references.
- Both profile classes self-register; `getProfileForCountry('GB')` / `'DE'` return the implementations.
- Snapshot test proves snapshot stability under live-constant mutation (Pitfall 1 enforced).
- Every test file shows ≥ the required count of it/test invocations; no `.skip` or `.todo` remaining on critical behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/58-classification-engine-rule-sets/58-02-SUMMARY.md` summarizing: IR35 question-count actual, DRV criterion-count actual, scoring test count, snapshot test approach, any flagged REVIEW:DE / REVIEW:PL translations. Note open items for Steuerberater / UK tax-adviser review so Plan 05 checkpoint can reference them.
</output>
