---
phase: 61
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/einvoice/package.json
  - packages/einvoice/src/profiles/xrechnung-de/constants.ts
  - packages/einvoice/src/profiles/xrechnung-de/schemas.ts
  - packages/einvoice/src/profiles/xrechnung-de/__tests__/generator.test.ts
  - packages/einvoice/src/profiles/xrechnung-de/__tests__/validator.test.ts
  - packages/einvoice/src/profiles/xrechnung-de/__tests__/svrl-normalizer.test.ts
  - packages/einvoice/src/profiles/xrechnung-de/__tests__/leitweg-id-embed.test.ts
  - packages/einvoice/src/profiles/xrechnung-de/__tests__/fixtures/README.md
  - packages/einvoice/src/profiles/xrechnung-de/validator-bundle/README.md
  - packages/einvoice/src/profiles/xrechnung-de/validator-bundle/source.txt
  - packages/einvoice/src/profiles/xrechnung-de/validator-bundle/.gitkeep
  - packages/validators/src/leitweg-id.ts
  - packages/validators/src/index.ts
  - packages/validators/src/__tests__/leitweg-id.test.ts
  - packages/validators/src/__tests__/leitweg-id.fixtures.ts
  - packages/api/src/services/__tests__/leitweg-id-resolver.test.ts
  - packages/api/src/services/__tests__/einvoice-lifecycle-fsm.test.ts
  - packages/api/src/routers/__tests__/einvoice.finalize.test.ts
  - packages/api/src/routers/__tests__/einvoice.send.test.ts
  - packages/api/src/routers/__tests__/leitweg-id.test.ts
  - packages/db/prisma/schema/einvoice.prisma
  - packages/db/prisma/schema/contractor.prisma
  - packages/db/prisma/schema/invoice.prisma
  - packages/db/prisma/schema/organization.prisma
  - packages/db/prisma/schema/peppol.prisma
  - packages/db/prisma/schema/contract.prisma
  - scripts/recompile-kosit-schematron.ts
  - apps/web/messages/en.json
  - apps/web/messages/de.json
  - apps/web/messages/pl.json
  - apps/web/messages/ar.json
autonomous: true
requirements:
  - EINV-01
  - EINV-04
  - EINV-05
  - EINV-06
  - EINV-07
must_haves:
  truths:
    - "packages/einvoice/package.json lists saxon-js, libxmljs2 as runtime deps and xslt3 as devDep"
    - "packages/db has new einvoice.prisma with LeitwegId, EInvoiceLifecycle, EInvoiceLifecycleEvent, PeppolCapabilityCache models + EInvoiceValidationStatus / EInvoiceTransmissionStatus / EInvoiceLifecycleEventType enums"
    - "PeppolParticipant in peppol.prisma gains supportsXRechnungCii + lastCapabilityCheckAt fields (Option A reconciliation — no duplicate fields added to Organization)"
    - "Contractor gains isPublicSectorBuyer, peppolSchemeId, peppolParticipantValue fields; Contract gains leitwegIds back-relation; Invoice gains 1:1 eInvoiceLifecycle back-relation; Organization gains back-relations for the four new tables"
    - "Database schema reflects all new models after [BLOCKING] prisma db push"
    - "packages/validators/src/leitweg-id.ts exports leitwegIdSchema with Modulo-11-10 refine, grounded in KoSIT fixtures (≥10 valid + ≥10 invalid)"
    - "Storecove CII document_type_id literal captured from sandbox round-trip and committed in constants.ts"
    - "EInvoice namespace exists in all four locale files (en, de, pl, ar) with D-15 / UI-SPEC strings"
    - "Eleven RED test files exist (describe.todo or failing placeholders) covering every Wave-0 gap in VALIDATION.md §Wave 0"
  artifacts:
    - path: "packages/einvoice/src/profiles/xrechnung-de/constants.ts"
      provides: "XRECHNUNG_VERSION, XRECHNUNG_CUSTOMIZATION_ID, XRECHNUNG_PROFILE_ID, STORECOVE_CII_XRECHNUNG_DOC_TYPE_ID, CII namespace constants"
      contains: "XRECHNUNG_VERSION = '3.0.2'"
    - path: "packages/validators/src/leitweg-id.ts"
      provides: "leitwegIdSchema Zod schema with Modulo-11-10 refine"
      contains: "leitwegIdSchema"
    - path: "packages/validators/src/__tests__/leitweg-id.fixtures.ts"
      provides: "Ground-truth Leitweg-ID test vectors"
      contains: "LEITWEG_ID_VALID_FIXTURES"
    - path: "packages/db/prisma/schema/einvoice.prisma"
      provides: "LeitwegId, EInvoiceLifecycle, EInvoiceLifecycleEvent, PeppolCapabilityCache models + enums"
      contains: "model EInvoiceLifecycle"
    - path: "packages/einvoice/src/profiles/xrechnung-de/validator-bundle/source.txt"
      provides: "Pinned KoSIT validator-configuration-xrechnung release tag (release-2026-01-31)"
      contains: "release-2026-01-31"
    - path: "scripts/recompile-kosit-schematron.ts"
      provides: "Build-time .sch → .xslt → .sef.json compilation via xslt3 CLI"
      contains: "xslt3"
  key_links:
    - from: "packages/validators/src/__tests__/leitweg-id.test.ts"
      to: "packages/validators/src/leitweg-id.ts"
      via: "import { leitwegIdSchema } + fixture round-trip"
      pattern: "leitwegIdSchema\\.safeParse"
    - from: "packages/db/prisma/schema/einvoice.prisma"
      to: "packages/db/prisma/schema/invoice.prisma"
      via: "Invoice ↔ EInvoiceLifecycle 1:1 relation"
      pattern: "eInvoiceLifecycle\\s+EInvoiceLifecycle\\?"
    - from: "packages/db/prisma/schema/einvoice.prisma"
      to: "packages/db/prisma/schema/peppol.prisma"
      via: "PeppolParticipant extended per Option A"
      pattern: "supportsXRechnungCii\\s+Boolean"
---

<objective>
Wave-0 foundations for Phase 61. Establishes every schema, validator, fixture, dependency, and failing-test scaffold downstream plans will build against. Executes the mandatory Prisma schema push so Plans 02–07 run against live tables. Resolves the CONTEXT D-10 / existing-PeppolParticipant collision by **extending the existing `PeppolParticipant` table (Option A)** rather than adding duplicative fields to `Organization` (per RESEARCH §Collision). Captures the Storecove sandbox `document_type_id` for XRechnung-CII so Plan 05 has a ground-truth literal.

Purpose: Remove every "MISSING test" reference and every speculative dependency before any implementation runs. Plans 02–07 can then execute without blocking on schema, fixtures, or external verifications.

Output: deps installed, four locale namespaces created, full Prisma schema set committed + pushed to Neon EU pooler, eleven RED test files in place, KoSIT release pinned, Storecove discriminator verified.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-xrechnung-e-invoicing/61-CONTEXT.md
@.planning/phases/61-xrechnung-e-invoicing/61-RESEARCH.md
@.planning/phases/61-xrechnung-e-invoicing/61-VALIDATION.md
@.planning/phases/61-xrechnung-e-invoicing/61-UI-SPEC.md

<!-- Prior-phase schemas and seed patterns referenced below -->
@packages/db/prisma/schema/invoice.prisma
@packages/db/prisma/schema/contractor.prisma
@packages/db/prisma/schema/organization.prisma
@packages/db/prisma/schema/peppol.prisma
@packages/db/prisma/schema/contract.prisma
@packages/einvoice/package.json
@packages/einvoice/src/profiles/peppol-ae/constants.ts
@packages/validators/src/index.ts
@apps/web/messages/en.json

<interfaces>
<!-- Extracted from the repo to prevent scavenger hunts. -->

From packages/einvoice/src/profiles/peppol-ae/constants.ts (reference layout for constants.ts):
```typescript
// peppol-ae exports: UBL namespace constants, profile IDs, document type IDs
// Mirror style for xrechnung-de: one string constant per load-bearing value,
// exported individually, no default export, each with a `as const` suffix.
```

From packages/db/prisma/schema/peppol.prisma (existing — extend via Option A):
```prisma
model PeppolParticipant {
  id                String                  @id @default(cuid())
  organizationId    String
  participantId     String
  schemeId          String
  identifierValue   String
  aspProvider       String
  aspRegistrationId String?
  status            PeppolParticipantStatus @default(PENDING)
  registeredAt      DateTime?
  createdAt         DateTime                @default(now())
  updatedAt         DateTime                @updatedAt
  // Phase-61 additions land here
}

enum PeppolParticipantStatus { PENDING REGISTERED ACTIVE SUSPENDED DEREGISTERED }
```

From packages/db/prisma/schema/invoice.prisma (existing):
```prisma
model Invoice { id String @id @default(cuid()) organizationId String /* ... */ }
```

From packages/validators/src/index.ts (pattern for re-exports):
```typescript
export * from './<module>.js';
```

Zod + refine pattern already in use (see packages/validators/src/legal/disclaimers.ts).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install deps, pin KoSIT release, create Prisma schema set + prior-schema relations</name>
  <files>
    packages/einvoice/package.json,
    packages/db/prisma/schema/einvoice.prisma,
    packages/db/prisma/schema/peppol.prisma,
    packages/db/prisma/schema/contractor.prisma,
    packages/db/prisma/schema/invoice.prisma,
    packages/db/prisma/schema/organization.prisma,
    packages/db/prisma/schema/contract.prisma,
    packages/einvoice/src/profiles/xrechnung-de/validator-bundle/README.md,
    packages/einvoice/src/profiles/xrechnung-de/validator-bundle/source.txt,
    packages/einvoice/src/profiles/xrechnung-de/validator-bundle/.gitkeep,
    scripts/recompile-kosit-schematron.ts
  </files>
  <read_first>
    packages/einvoice/package.json,
    packages/db/prisma/schema/peppol.prisma,
    packages/db/prisma/schema/contractor.prisma,
    packages/db/prisma/schema/invoice.prisma,
    packages/db/prisma/schema/organization.prisma,
    packages/db/prisma/schema/contract.prisma,
    .planning/phases/61-xrechnung-e-invoicing/61-RESEARCH.md  (§Prisma models + §Collision with existing Peppol tables),
    .planning/phases/61-xrechnung-e-invoicing/61-CONTEXT.md   (D-05, D-08, D-10, D-11, D-12, D-13)
  </read_first>
  <behavior>
    - einvoice package.json gains `saxon-js@^2.7.0` + `libxmljs2@^0.37.0` under `dependencies`, `xslt3@^2.7.0` + `@xmldom/xmldom@^0.8.12` under `devDependencies`. Pin exact-minor; no `latest`.
    - New `packages/db/prisma/schema/einvoice.prisma` declares `LeitwegId`, `EInvoiceLifecycle`, `EInvoiceLifecycleEvent`, `PeppolCapabilityCache` + three enums (`EInvoiceValidationStatus`, `EInvoiceTransmissionStatus`, `EInvoiceLifecycleEventType`) exactly as sketched in 61-RESEARCH.md §Prisma models. Every model is multi-tenant (`organizationId` + `Organization` relation) and Neon-safe (explicit `@@index` map names where Postgres could truncate — mirror the Phase-58 `map` pattern: e.g. `@@index([organizationId, validationStatus], map: "einvoice_lifecycle_org_validation_idx")`).
    - `PeppolParticipant` (in `peppol.prisma`) gains two fields per Option A (RESEARCH §Collision): `supportsXRechnungCii Boolean @default(false)` and `lastCapabilityCheckAt DateTime?`. **Do NOT add `peppolParticipantId` / `peppolParticipantSchemeId` / `peppolParticipantStatus` fields to `Organization`** — use the existing `PeppolParticipant` row as single source of truth. If D-10 ever needs a quick-access status view, expose via a Prisma computed-relation helper, not a duplicate column.
    - `Contractor` gains `isPublicSectorBuyer Boolean @default(false)`, `peppolSchemeId String? @db.VarChar(4)`, `peppolParticipantValue String? @db.VarChar(64)` + a `leitwegIds LeitwegId[]` back-relation. Zod pair-constraint (both scheme+value set or both null) is enforced in Plan 04 at the router boundary, NOT in Prisma.
    - `Contract` gains `leitwegIds LeitwegId[]` back-relation.
    - `Invoice` gains `eInvoiceLifecycle EInvoiceLifecycle?` 1:1 back-relation (FK lives on `EInvoiceLifecycle.invoiceId` with `@unique`).
    - `Organization` gains back-relations: `leitwegIds LeitwegId[]`, `eInvoiceLifecycles EInvoiceLifecycle[]`, `eInvoiceLifecycleEvents EInvoiceLifecycleEvent[]`, `peppolCapabilityCache PeppolCapabilityCache[]`.
    - New `scripts/recompile-kosit-schematron.ts` is a Node script (ESM, `#!/usr/bin/env tsx` shebang) that: (a) reads `validator-bundle/source.txt`, (b) shells `xslt3 -xsl:…EN16931-CII-validation.xsl -export:…EN16931-CII-validation.sef.json -t` for each of the two KoSIT XSLTs, (c) writes output to `validator-bundle/*.sef.json`, (d) asserts SHA-256 of each SEF matches a value pinned in `validator-bundle/checksums.txt` (populated manually after first run). If `xslt3` is not installed, exit 2 with a clear error.
    - `validator-bundle/source.txt` contains exactly `itplr-kosit/validator-configuration-xrechnung release-2026-01-31` on the first line + SHA-256 of the release zip on the second line (captured via `curl -L | sha256sum`).
    - `validator-bundle/README.md` documents: source URL, Apache-2.0 license citation, re-compile instructions (`pnpm tsx scripts/recompile-kosit-schematron.ts`), directory layout (`EN16931-CII-validation.sef.json`, `XRechnung-CII-validation.sef.json`, `CII-D16B-schema/`), and the invariant that the `.sef.json` files live under version control (planner explicitly verified no 100MB-per-file issue; the SEF JSONs are typically <5MB each).
  </behavior>
  <action>
    Step 1 — deps:
    `pnpm --filter @contractor-ops/einvoice add saxon-js@^2.7.0 libxmljs2@^0.37.0`
    `pnpm --filter @contractor-ops/einvoice add -D xslt3@^2.7.0 @xmldom/xmldom@^0.8.12`
    Verify `pnpm --filter @contractor-ops/einvoice exec node -e "require('saxon-js'); require('libxmljs2'); console.log('ok')"` prints `ok`. If `libxmljs2` fails to install on this machine (Alpine / missing toolchain), add `libxml2-wasm@latest` instead and leave a NOTE in the commit body flagging the fallback; Plan 03's validator task will branch accordingly.

    Step 2 — Prisma schema set: create `packages/db/prisma/schema/einvoice.prisma` copying the block from 61-RESEARCH.md §Code Examples lines 528–638. Use exact enum names + field types from that block. Every index has an explicit `map:` name ≤63 chars (Neon Postgres). Example: `@@index([organizationId, validationStatus], map: "einvoice_lifecycle_org_validation_idx")`.

    Step 3 — modify existing schemas:
    - `peppol.prisma` PeppolParticipant: add `supportsXRechnungCii Boolean @default(false)` + `lastCapabilityCheckAt DateTime?` after `registeredAt`. Do NOT touch existing fields, enum, or indexes.
    - `contractor.prisma` Contractor model (around line 9): add `isPublicSectorBuyer Boolean @default(false)`, `peppolSchemeId String? @db.VarChar(4)`, `peppolParticipantValue String? @db.VarChar(64)`, and the back-relation `leitwegIds LeitwegId[]`.
    - `contract.prisma` Contract model: add `leitwegIds LeitwegId[]` back-relation.
    - `invoice.prisma` Invoice model: add `eInvoiceLifecycle EInvoiceLifecycle?` back-relation (unique side is on EInvoiceLifecycle).
    - `organization.prisma` Organization model: add four back-relations (`leitwegIds LeitwegId[]`, `eInvoiceLifecycles EInvoiceLifecycle[]`, `eInvoiceLifecycleEvents EInvoiceLifecycleEvent[]`, `peppolCapabilityCache PeppolCapabilityCache[]`) — NO new scalar fields on Organization for Peppol per RESEARCH Option A.

    Step 4 — validator-bundle scaffolding:
    - Create `packages/einvoice/src/profiles/xrechnung-de/validator-bundle/` directory (Mkdir via the Write tool on `.gitkeep`).
    - `source.txt`: first line `itplr-kosit/validator-configuration-xrechnung release-2026-01-31`; second line `sha256:<fill-after-download>` (placeholder — agent should actually download the release zip from github and paste the real sha256; `curl -L https://github.com/itplr-kosit/validator-configuration-xrechnung/releases/download/release-2026-01-31/validator-configuration-xrechnung_3.0.x_2026-01-31.zip -o /tmp/kosit.zip && shasum -a 256 /tmp/kosit.zip`).
    - `README.md`: describe source, license (Apache-2.0), re-compile command, directory layout, content-rebuild trigger, note that `.sef.json` files are NOT yet present (Plan 03 populates them).
    - `.gitkeep`: empty file so the directory is committed.

    Step 5 — recompile script:
    Create `scripts/recompile-kosit-schematron.ts`:
    ```typescript
    #!/usr/bin/env tsx
    // Build-time only. Compiles KoSIT XSLT → saxon-js SEF JSON.
    // Usage: pnpm tsx scripts/recompile-kosit-schematron.ts
    import { execFileSync } from 'node:child_process';
    import fs from 'node:fs/promises';
    import path from 'node:path';
    // Pin source path; xslt3 must be on PATH (devDep in einvoice package).
    const BUNDLE = path.resolve('packages/einvoice/src/profiles/xrechnung-de/validator-bundle');
    const JOBS = [
      { xsl: 'EN16931-CII-validation.xsl',   out: 'EN16931-CII-validation.sef.json' },
      { xsl: 'XRechnung-CII-validation.xsl', out: 'XRechnung-CII-validation.sef.json' },
    ];
    for (const job of JOBS) {
      const src = path.join(BUNDLE, 'src-xslt', job.xsl);
      const dst = path.join(BUNDLE, job.out);
      execFileSync('xslt3', ['-xsl:' + src, '-export:' + dst, '-t'], { stdio: 'inherit' });
    }
    // SHA-256 assertions (populate after first successful run).
    ```

    Step 6 — [BLOCKING] db push:
    `pnpm --filter @contractor-ops/db exec prisma format`
    `pnpm --filter @contractor-ops/db exec prisma validate`
    `pnpm --filter @contractor-ops/db exec prisma db push`
    (Against Neon EU pooler; the `DATABASE_URL` is already set locally.) Expected: 4 new tables (`LeitwegId`, `EInvoiceLifecycle`, `EInvoiceLifecycleEvent`, `PeppolCapabilityCache`), 2 enum types created, `PeppolParticipant` altered +2 cols, `Contractor` altered +3 cols. If `prisma validate` fails with name-collision errors on indexes, shorten the `map:` names and retry. Do NOT use `--accept-data-loss` — there is no data loss expected.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db exec prisma validate 2>&1 | grep -q "The schema at" && pnpm --filter @contractor-ops/einvoice exec node -e "require('saxon-js'); require('libxmljs2');" && grep -q "model EInvoiceLifecycle" packages/db/prisma/schema/einvoice.prisma && grep -q "supportsXRechnungCii" packages/db/prisma/schema/peppol.prisma && grep -q "isPublicSectorBuyer" packages/db/prisma/schema/contractor.prisma && grep -q "release-2026-01-31" packages/einvoice/src/profiles/xrechnung-de/validator-bundle/source.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "model \(LeitwegId\|EInvoiceLifecycle\|EInvoiceLifecycleEvent\|PeppolCapabilityCache\)" packages/db/prisma/schema/einvoice.prisma` returns 4
    - `grep -c "enum \(EInvoiceValidationStatus\|EInvoiceTransmissionStatus\|EInvoiceLifecycleEventType\)" packages/db/prisma/schema/einvoice.prisma` returns 3
    - `grep "saxon-js" packages/einvoice/package.json` matches on a dependencies line (not devDependencies)
    - `grep "libxmljs2\|libxml2-wasm" packages/einvoice/package.json` matches exactly one of the two
    - `grep "xslt3" packages/einvoice/package.json` matches on a devDependencies line
    - `prisma db push` completed successfully (exit 0) — capture output in commit body
    - `grep -c "peppolParticipantId\|peppolParticipantSchemeId\|peppolParticipantStatus" packages/db/prisma/schema/organization.prisma` returns **0** (Option A: no duplicate fields on Organization)
    - `grep "supportsXRechnungCii" packages/db/prisma/schema/peppol.prisma` matches exactly once
    - No `console.*` usage introduced in `scripts/recompile-kosit-schematron.ts` (this is a build-time script so `execFileSync` stdio inheritance is acceptable; use pino only if the script grows — for v1 stdio-inherit is fine)
  </acceptance_criteria>
  <done>
    All four new Prisma models exist + enums declared + `prisma db push` succeeded + deps installed + KoSIT release pinned via `source.txt`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Leitweg-ID Zod schema + KoSIT fixture round-trip tests + Storecove sandbox document_type_id probe + constants.ts + schemas.ts + i18n namespace</name>
  <files>
    packages/validators/src/leitweg-id.ts,
    packages/validators/src/index.ts,
    packages/validators/src/__tests__/leitweg-id.test.ts,
    packages/validators/src/__tests__/leitweg-id.fixtures.ts,
    packages/einvoice/src/profiles/xrechnung-de/constants.ts,
    packages/einvoice/src/profiles/xrechnung-de/schemas.ts,
    apps/web/messages/en.json,
    apps/web/messages/de.json,
    apps/web/messages/pl.json,
    apps/web/messages/ar.json
  </files>
  <read_first>
    packages/validators/src/index.ts,
    packages/validators/src/legal/disclaimers.ts,
    packages/einvoice/src/profiles/peppol-ae/constants.ts,
    apps/web/messages/en.json,
    .planning/phases/61-xrechnung-e-invoicing/61-CONTEXT.md  (D-02, D-07),
    .planning/phases/61-xrechnung-e-invoicing/61-RESEARCH.md  (§Code Examples — Leitweg-ID MOD 11,10; §Open Questions #1 — Storecove doc_type_id),
    .planning/phases/61-xrechnung-e-invoicing/61-UI-SPEC.md   (§Copywriting Contract — EInvoice namespace strings for en/de/pl/ar)
  </read_first>
  <behavior>
    - `packages/validators/src/leitweg-id.ts` exports `leitwegIdSchema` (Zod) with regex `/^(\d{2,12})(?:-([A-Z0-9]{0,30}))?-(\d{2})$/` + `.refine(validateMod1110)` + `.min(5)` + `.max(46)`. The `validateMod1110` helper is exported separately for unit testing.
    - Fixture file `leitweg-id.fixtures.ts` exports two const arrays:
      - `LEITWEG_ID_VALID_FIXTURES: string[]` — ≥10 real KoSIT-spec values (pull from the published KoSIT validator-configuration-xrechnung testsuite under `release-2026-01-31/scenarios.xml`; the agent downloads the release zip if not already, extracts the XML, greps for `<leitwegId>` or similar elements). Include at minimum the spec example `991-33333TEST-33`.
      - `LEITWEG_ID_INVALID_FIXTURES: Array<{ value: string; reason: 'malformed' | 'check_digit_wrong' | 'too_short' | 'too_long' | 'bad_char' }>` — ≥10 entries covering each category.
    - `leitweg-id.test.ts` iterates both fixture arrays; every valid must `safeParse(v).success === true`; every invalid must `safeParse(v).success === false` AND for `check_digit_wrong` entries the error message contains `check digit`.
    - `packages/einvoice/src/profiles/xrechnung-de/constants.ts` exports: `XRECHNUNG_VERSION`, `XRECHNUNG_CUSTOMIZATION_ID`, `XRECHNUNG_PROFILE_ID`, `STORECOVE_CII_XRECHNUNG_DOC_TYPE_ID` (literal captured from sandbox round-trip — see action), `RSM_NS`, `RAM_NS`, `UDT_NS`, `QDT_NS`, `XRECHNUNG_DE_PROFILE_ID = 'xrechnung-de' as const`, `KOSIT_RULE_SET_VERSION = 'XRechnung 3.0.2 / KoSIT release-2026-01-31' as const`.
    - `packages/einvoice/src/profiles/xrechnung-de/schemas.ts` exports Zod schemas for XRechnung-specific extensions (at minimum a schema for `finalizeEInvoiceInput { invoiceId: cuid, force?: boolean }` + a schema for the `format` field on TransmitInvoiceParams). Re-exported from the profile `index.ts` in Plan 02.
    - Four locale files gain an `EInvoice.*` namespace with every string listed in 61-UI-SPEC.md §Copywriting Contract (Settings page, Invoices list, Per-invoice E-invoice tab, Leitweg-ID create/edit dialog, Peppol participant register/deregister dialog, error-state copy). German uses formal register (Sie); Polish + Arabic use placeholder English-identical strings marked with an inline `_NOTE: "AI-first-pass translation pending review"` per the Phase 56 pattern. Every en-string exactly matches the UI-SPEC table text — do NOT paraphrase.
  </behavior>
  <action>
    Step 1 — Leitweg-ID schema:
    Create `packages/validators/src/leitweg-id.ts` with the exact structure from 61-RESEARCH.md §Code Examples lines 475–522, but **port the `computeLeitwegCheckDigit` implementation from a reference impl** (node-iso7064 on GitHub — read the code and port the MOD-11-10 logic verbatim into the file; do NOT ship the illustrative `parseInt(ch, 36)` trick from the research doc without round-tripping against fixtures first). The check-digit function MUST be exported (named `validateLeitwegCheckDigit(payload: string): { valid: boolean; expected: string }`) so the test suite can test it directly.

    Step 2 — fixtures:
    Download the KoSIT release: `curl -L https://github.com/itplr-kosit/validator-configuration-xrechnung/releases/download/release-2026-01-31/validator-configuration-xrechnung_3.0.x_2026-01-31.zip -o /tmp/kosit.zip && unzip -o /tmp/kosit.zip -d /tmp/kosit/`. Grep the scenarios and test documents for Leitweg-ID values: `grep -rohE '[0-9]{2,12}(-[A-Z0-9]{0,30})?-[0-9]{2}' /tmp/kosit/ | sort -u`. Pick ≥10 that match a `<ram:BuyerReference>` or equivalent context (real IDs, not random numeric strings). Record source file + XPath context in the fixture file as a comment above each entry. Generate invalid fixtures by mutating each category (flip a check-digit digit, truncate, inject lowercase, etc.).

    Step 3 — tests (RED initially if schema import fails, then GREEN after step 1):
    ```typescript
    // packages/validators/src/__tests__/leitweg-id.test.ts
    import { describe, it, expect } from 'vitest';
    import { leitwegIdSchema, validateLeitwegCheckDigit } from '../leitweg-id.js';
    import { LEITWEG_ID_VALID_FIXTURES, LEITWEG_ID_INVALID_FIXTURES } from './leitweg-id.fixtures.js';

    describe('leitwegIdSchema — valid KoSIT fixtures', () => {
      for (const v of LEITWEG_ID_VALID_FIXTURES) {
        it(`accepts ${v}`, () => {
          expect(leitwegIdSchema.safeParse(v).success).toBe(true);
        });
      }
    });

    describe('leitwegIdSchema — invalid fixtures', () => {
      for (const { value, reason } of LEITWEG_ID_INVALID_FIXTURES) {
        it(`rejects ${value} (${reason})`, () => {
          const r = leitwegIdSchema.safeParse(value);
          expect(r.success).toBe(false);
          if (reason === 'check_digit_wrong' && !r.success) {
            expect(JSON.stringify(r.error.issues)).toMatch(/check digit/i);
          }
        });
      }
    });
    ```

    Step 4 — `validators/index.ts`: append `export * from './leitweg-id.js';`.

    Step 5 — run the tests:
    `pnpm --filter @contractor-ops/validators test -- --run leitweg-id`
    All must pass. If any valid fixture fails, the MOD-11-10 port is wrong — fix the algorithm (do NOT mutate the fixture). Commit immediately after green to lock the algorithm against the fixtures (RESEARCH explicitly warns this is easy to get wrong silently).

    Step 6 — Storecove sandbox `document_type_id` probe:
    Write a one-off script `packages/einvoice/scripts/probe-storecove-cii-doctype.ts` (NOT checked in long-term — delete at end of task) that submits a minimal hand-crafted XRechnung-CII envelope to Storecove's sandbox `/api/v2/document_submissions` and logs the 422 error response (which enumerates valid `document_type_id` values for CII) OR the 200 response's acknowledged document type. Use existing `storeCredentials` / sandbox API key pattern. If the sandbox cannot be reached from this environment, fall back to: (a) query Storecove staff via their documented `/api/v2/document_types` endpoint, or (b) read the latest `packages/einvoice/src/asp/storecove/schemas.ts` for any `document_type_id` enum that already includes a CII variant, or (c) use the best-known literal from Peppol poacc codelist: `urn:oasis:names:specification:ubl:schema:xsd:Invoice-2::Invoice##urn:cen.eu:en16931:2017#compliant#urn:xoev-de:kosit:standard:xrechnung_3.0::2.1` — but EXPLICITLY mark it as "pending sandbox verification" in a comment. Record the captured/confirmed literal in `constants.ts` as `STORECOVE_CII_XRECHNUNG_DOC_TYPE_ID`.

    Step 7 — `constants.ts` + `schemas.ts`:
    Write both files per §behavior. `constants.ts` literals exactly as per 61-RESEARCH.md §Code Examples lines 307–319. `schemas.ts` exports:
    ```typescript
    import { z } from 'zod';
    export const finalizeEInvoiceInputSchema = z.object({
      invoiceId: z.string().cuid(),
      force: z.boolean().optional().default(false),
    });
    export const eInvoiceFormatSchema = z.discriminatedUnion('kind', [
      z.object({ kind: z.literal('ubl-pint-ae') }),
      z.object({ kind: z.literal('cii-xrechnung'), customizationId: z.string(), profileId: z.string() }),
      z.object({ kind: z.literal('ubl-peppol-bis-3') }),
    ]);
    ```
    (The `eInvoiceFormatSchema` is consumed by Plan 05 when the Storecove adapter validates its `format` field at runtime.)

    Step 8 — i18n namespace:
    Add an `"EInvoice"` top-level key to each of `apps/web/messages/{en,de,pl,ar}.json` with nested keys mirroring the UI-SPEC copy table verbatim for English (German: formal register "Sie" translation; Polish + Arabic: English strings with `_NOTE` flags per Phase 56 pattern). Keys to include (minimum): `Settings.h1`, `Settings.subline`, `Settings.PeppolCard.*`, `Settings.LeitwegIdCard.*`, `InvoicesList.SummaryTile.*`, `InvoicesList.Filter.*`, `InvoicesList.Cell.*`, `InvoicesList.Empty.*`, `InvoiceTab.*`, `LeitwegIdDialog.*`, `PeppolDialog.*`, `Errors.LEITWEG_ID_MISSING`, `Errors.PARTICIPANT_NOT_REACHABLE`, `Errors.KOSIT_VALIDATION_FAILED`, `Errors.PEPPOL_PARTICIPANT_NOT_ACTIVE`, `Errors.STORECOVE_TRANSMISSION_FAILED`, `Errors.Generic`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/validators test -- --run leitweg-id 2>&1 | grep -qE "Tests\s+(passed|[0-9]+ passed)" && grep -q "XRECHNUNG_VERSION = '3.0.2'" packages/einvoice/src/profiles/xrechnung-de/constants.ts && grep -q "STORECOVE_CII_XRECHNUNG_DOC_TYPE_ID" packages/einvoice/src/profiles/xrechnung-de/constants.ts && node -e "const en=require('./apps/web/messages/en.json'); if(!en.EInvoice||!en.EInvoice.Errors||!en.EInvoice.Errors.LEITWEG_ID_MISSING){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/validators test -- --run leitweg-id` exits 0 with all fixtures passing
    - `grep -c "^export " packages/validators/src/leitweg-id.ts` returns ≥2 (schema + helper)
    - `grep -c "^export " packages/validators/src/__tests__/leitweg-id.fixtures.ts` returns exactly 2 (`LEITWEG_ID_VALID_FIXTURES`, `LEITWEG_ID_INVALID_FIXTURES`)
    - `node -e "const f=require('./packages/validators/src/__tests__/leitweg-id.fixtures.ts'.replace('.ts','.js')); /* after build */"` — or equivalent grep: `grep -c "'" packages/validators/src/__tests__/leitweg-id.fixtures.ts` ≥20 entries
    - `grep "XRECHNUNG_CUSTOMIZATION_ID = 'urn:cen.eu:en16931:2017#compliant#urn:xoev-de:kosit:standard:xrechnung_3.0'" packages/einvoice/src/profiles/xrechnung-de/constants.ts` matches
    - `grep "STORECOVE_CII_XRECHNUNG_DOC_TYPE_ID" packages/einvoice/src/profiles/xrechnung-de/constants.ts` matches
    - `grep -c "EInvoice" apps/web/messages/en.json` ≥ 30 (many nested keys)
    - `grep -c "EInvoice" apps/web/messages/de.json` ≥ 30
    - `grep -c "_NOTE" apps/web/messages/pl.json` ≥ 1 (pending-translation marker)
    - `grep -c "_NOTE" apps/web/messages/ar.json` ≥ 1
    - No `console.*` (`grep -rn "console\." packages/validators/src/leitweg-id.ts packages/einvoice/src/profiles/xrechnung-de/constants.ts packages/einvoice/src/profiles/xrechnung-de/schemas.ts` returns no matches)
    - `grep "leitweg-id" packages/validators/src/index.ts` matches (re-export present)
    - One-off probe script `packages/einvoice/scripts/probe-storecove-cii-doctype.ts` was deleted after use (verify: `test ! -f packages/einvoice/scripts/probe-storecove-cii-doctype.ts`)
  </acceptance_criteria>
  <done>
    Leitweg-ID validator green against KoSIT fixtures + XRechnung constants frozen with verified Storecove doc_type_id + i18n namespace seeded across 4 locales.
  </done>
</task>

<task type="auto">
  <name>Task 3: RED-scaffold test files for every Wave-0 gap + locked Leitweg-ID phrasing check</name>
  <files>
    packages/einvoice/src/profiles/xrechnung-de/__tests__/generator.test.ts,
    packages/einvoice/src/profiles/xrechnung-de/__tests__/validator.test.ts,
    packages/einvoice/src/profiles/xrechnung-de/__tests__/svrl-normalizer.test.ts,
    packages/einvoice/src/profiles/xrechnung-de/__tests__/leitweg-id-embed.test.ts,
    packages/einvoice/src/profiles/xrechnung-de/__tests__/fixtures/README.md,
    packages/api/src/services/__tests__/leitweg-id-resolver.test.ts,
    packages/api/src/services/__tests__/einvoice-lifecycle-fsm.test.ts,
    packages/api/src/routers/__tests__/einvoice.finalize.test.ts,
    packages/api/src/routers/__tests__/einvoice.send.test.ts,
    packages/api/src/routers/__tests__/leitweg-id.test.ts,
    .planning/phases/61-xrechnung-e-invoicing/61-VALIDATION.md
  </files>
  <read_first>
    packages/einvoice/src/profiles/peppol-ae/__tests__,
    packages/api/src/routers/__tests__,
    .planning/phases/61-xrechnung-e-invoicing/61-VALIDATION.md,
    .planning/phases/61-xrechnung-e-invoicing/61-RESEARCH.md  (§Wave 0 Gaps)
  </read_first>
  <behavior>
    - Each of the ten test files contains a `describe()` block with at least one `it.todo()` per line in the VALIDATION.md §Per-Task Verification Map (entries that will be fleshed out by Plans 02–06). Each test file must import the production module it targets using a path that Plans 02–06 will create — this guarantees Plan N's executor sees a `Cannot find module` compile error if the import path drifts (acts as a contract check).
    - `generator.test.ts` has `describe.todo("generator — minimal valid invoice", …)` + `describe.todo("generator — Leitweg-ID embedded at BT-10 path", …)` + `describe.todo("generator — handles reverse-charge (§13b UStG) VAT breakdown", …)` — the Plan 02 executor converts each `todo` into a real test.
    - `validator.test.ts`, `svrl-normalizer.test.ts`, `leitweg-id-embed.test.ts` follow the same todo pattern with 2–4 blocks each mapped to EINV-04 / EINV-05 behaviors listed in 61-RESEARCH.md §Test Map.
    - `leitweg-id-resolver.test.ts` has three todos: (a) contractId override wins over contractor default, (b) contractor default wins when no contract row, (c) no matching row returns null.
    - `einvoice-lifecycle-fsm.test.ts` has todos for each legal state transition + one negative test.
    - `einvoice.finalize.test.ts`, `einvoice.send.test.ts`, `leitweg-id.test.ts` are tRPC integration tests using the existing router-test harness (see `packages/api/src/routers/__tests__/` for patterns used by Phase 57 and 58). Add todos for the auth/tenancy leak tests too (multi-tenant scoping assertion).
    - `fixtures/README.md` describes the layout of KoSIT positive/negative XML fixtures that Plan 03 will populate (`kosit-positive-minimal.xml`, `kosit-negative-missing-bt10.xml`, etc.) — Plan 03 executor reads this to know where to place files.
    - `61-VALIDATION.md` is rewritten with the Per-Task Verification Map populated (Task ID column filled with the actual plan+task numbers once this phase's plan set is stable — populate the map based on the plans this plan set now creates).
  </behavior>
  <action>
    Step 1 — Create each RED-scaffold test file using vitest's `describe.todo` + `it.todo`. Example template for `generator.test.ts`:
    ```typescript
    import { describe } from 'vitest';
    // Plan 02 creates packages/einvoice/src/profiles/xrechnung-de/generator.ts
    // This file MUST compile once Plan 02 lands. Until then, the describe.todo
    // entries act as a signed contract.
    describe.todo('XRechnung generator — minimal valid invoice produces CII XML that passes layer-1 XSD');
    describe.todo('XRechnung generator — Leitweg-ID embedded at /rsm:CrossIndustryInvoice/rsm:SupplyChainTradeTransaction/ram:ApplicableHeaderTradeAgreement/ram:BuyerReference');
    describe.todo('XRechnung generator — reverse-charge §13b UStG maps to BT-120 TaxExemptionReason text');
    describe.todo('XRechnung generator — Kleinunternehmer §19 UStG sets BT-118/BT-119 per KoSIT rule');
    ```
    Mirror the structure for each other test file, using the §Test Map row as the `describe.todo` label.

    Step 2 — `fixtures/README.md`:
    ```markdown
    # XRechnung validator test fixtures

    Plan 03 (KoSIT three-layer validator) populates this directory with positive
    and negative CII samples derived from `validator-configuration-xrechnung
    release-2026-01-31/test-suite/`.

    | Filename | Expected layer-1 | Expected layer-2 | Expected layer-3 |
    |----------|------------------|------------------|------------------|
    | kosit-positive-minimal.xml     | PASS | PASS | PASS |
    | kosit-positive-leitweg.xml     | PASS | PASS | PASS |
    | kosit-negative-missing-bt10.xml | PASS | PASS | FAIL (BR-DE-...) |
    | kosit-negative-bad-currency.xml | PASS | FAIL | FAIL (BR-DE-17) |

    Extract: `unzip -j /tmp/kosit.zip "test-suite/**/*.xml" -d /tmp/samples && cp
    /tmp/samples/<relevant>.xml packages/einvoice/src/profiles/xrechnung-de/__tests__/fixtures/`
    ```

    Step 3 — Update `61-VALIDATION.md`: fill Per-Task Verification Map with concrete entries (one row per test file) keyed to the ten Wave-0 gap files. Flip frontmatter `wave_0_complete: false` → keep false here (Plan 02 flips it to true after Plan 02/03 land). Set `nyquist_compliant: false` (stays false until last plan).

    Step 4 — run the scaffolded tests: `pnpm --filter @contractor-ops/einvoice --filter @contractor-ops/validators --filter @contractor-ops/api test -- --run` — expect all `describe.todo` lines to show as "todo" not "fail". The runner must exit 0 with the todos reported.
  </action>
  <verify>
    <automated>test -f packages/einvoice/src/profiles/xrechnung-de/__tests__/generator.test.ts && test -f packages/einvoice/src/profiles/xrechnung-de/__tests__/validator.test.ts && test -f packages/einvoice/src/profiles/xrechnung-de/__tests__/svrl-normalizer.test.ts && test -f packages/einvoice/src/profiles/xrechnung-de/__tests__/leitweg-id-embed.test.ts && test -f packages/api/src/services/__tests__/leitweg-id-resolver.test.ts && test -f packages/api/src/services/__tests__/einvoice-lifecycle-fsm.test.ts && test -f packages/api/src/routers/__tests__/einvoice.finalize.test.ts && test -f packages/api/src/routers/__tests__/einvoice.send.test.ts && test -f packages/api/src/routers/__tests__/leitweg-id.test.ts && pnpm --filter @contractor-ops/einvoice test -- --run xrechnung-de 2>&1 | grep -qE "todo|0 failed"</automated>
  </verify>
  <acceptance_criteria>
    - All ten scaffold files exist (see files)
    - Each file contains `describe.todo(` at least once: `grep -lc "describe.todo(" packages/einvoice/src/profiles/xrechnung-de/__tests__/*.test.ts packages/api/src/services/__tests__/leitweg-id-resolver.test.ts packages/api/src/services/__tests__/einvoice-lifecycle-fsm.test.ts packages/api/src/routers/__tests__/{einvoice.finalize,einvoice.send,leitweg-id}.test.ts` returns exactly `10`
    - `grep -c "Per-Task Verification Map" .planning/phases/61-xrechnung-e-invoicing/61-VALIDATION.md` matches and the table has ≥ 10 populated rows (greater than the TBD placeholder)
    - Vitest reports todos (not failures): `pnpm --filter @contractor-ops/einvoice test -- --run xrechnung-de 2>&1 | grep -c "failed"` returns 0 for any non-zero failed count line
  </acceptance_criteria>
  <done>
    Ten RED-scaffold test files committed + VALIDATION.md verification map populated + all todos reported by vitest runner.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pnpm` install → local dev / CI | saxon-js + libxmljs2 come from npm registry. Package-lock / pnpm-lock pinning is the integrity gate. |
| KoSIT release zip → repo | `source.txt` + SHA-256 pin defends against supply-chain substitution of the validator XSLT. |
| Storecove sandbox probe → constants.ts | document_type_id string flows from live API response into source-controlled constant; wrong literal → later 422 at send time (availability, not security). |
| Zod boundary | `leitwegIdSchema` is the first line of defense between untrusted user input and the XRechnung generator / DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01-01 | Tampering | KoSIT XSLT artifacts checked into `validator-bundle/` | mitigate | Pin release tag in `source.txt` + SHA-256 of release zip; `recompile-kosit-schematron.ts` asserts SHA-256 of generated SEF against `checksums.txt` before accepting output. |
| T-61-01-02 | Tampering | `libxmljs2` + `saxon-js` npm install | mitigate | pnpm-lock pinning (exact-minor specifiers); `pnpm audit` runs on CI; provenance attestation via npm's `--ignore-scripts` default in CI Dockerfile. |
| T-61-01-03 | Information Disclosure | Leitweg-ID Zod schema mis-accepts (cross-tenant impact downstream) | mitigate | Fixture-based round-trip tests covering ≥10 KoSIT-published positives + ≥10 negatives; commit gates on green. |
| T-61-01-04 | Denial of Service | `prisma db push` against shared Neon pooler | accept | Phase runs in LOCAL-ONLY env per STATE.md; push is a dev-time op; migration window is minutes; rollback is a trivial manual drop. |
| T-61-01-05 | Tampering | `recompile-kosit-schematron.ts` build-time script reading arbitrary `source.txt` | mitigate | Script only consumes the checked-in `source.txt`; no external CLI arg parsing; `execFileSync` (not `exec`) on pinned `xslt3` binary from devDep. |
| T-61-01-06 | Information Disclosure | i18n strings copy-pasted from UI-SPEC may contain internal rule IDs | accept | UI-SPEC reviewed; SVRL detail surfacing is gated to a developer-oriented panel; chrome strings are human-friendly. No PII in strings. |
| T-61-01-07 | Spoofing | Storecove sandbox probe uses production-shaped credentials | mitigate | Probe uses sandbox API key only; probe script is deleted post-task; no credentials committed. Acceptance criteria enforces deletion. |
| T-61-01-08 | Elevation of Privilege | New `PeppolCapabilityCache` / `EInvoiceLifecycle` tables unscoped | mitigate | Every model has `organizationId` FK + `@@unique` / `@@index` including it; multi-tenant Prisma extension enforces `where: { organizationId }` at runtime (Plans 04–06 add coverage tests). |
</threat_model>

<verification>
Run `pnpm --filter @contractor-ops/einvoice --filter @contractor-ops/validators --filter @contractor-ops/api --filter @contractor-ops/db test -- --run` — all todos + all green fixture tests.
Run `pnpm --filter @contractor-ops/db exec prisma validate` — must exit 0 with no drift warnings.
Spot-check tables exist in Neon: `pnpm --filter @contractor-ops/db exec prisma db execute --stdin <<< "SELECT to_regclass('public.\"LeitwegId\"'), to_regclass('public.\"EInvoiceLifecycle\"'), to_regclass('public.\"EInvoiceLifecycleEvent\"'), to_regclass('public.\"PeppolCapabilityCache\"');"` returns four non-NULL rows.
</verification>

<success_criteria>
1. All must_haves truths hold.
2. `prisma db push` succeeded (commit body captures the output).
3. `pnpm --filter @contractor-ops/validators test -- --run leitweg-id` green against KoSIT fixtures.
4. Eleven RED-scaffold test files in place, reported as `todo` (not failing).
5. `STORECOVE_CII_XRECHNUNG_DOC_TYPE_ID` constant contains a literal (either sandbox-verified or flagged `pending sandbox verification` with Plan 05 owning the final confirmation).
6. Four locale files contain the `EInvoice.*` namespace with UI-SPEC-complete en strings.
7. No `console.*` usage in any new source file (Build-time scripts under `scripts/` using `execFileSync` stdio inheritance are acceptable per CLAUDE.md — same pattern used elsewhere in repo).
</success_criteria>

<output>
After completion, create `.planning/phases/61-xrechnung-e-invoicing/61-01-SUMMARY.md` covering: deps installed (and whether libxmljs2 or libxml2-wasm was selected), schema push outcome (table rowcount before/after), KoSIT release pin + SHA-256, Storecove doc_type_id literal + verification status (sandbox confirmed / pending), Leitweg-ID fixture count (valid / invalid), any deviations from the plan.
</output>
