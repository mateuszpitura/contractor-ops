---
phase: 61
plan: 07
type: execute
wave: 4
depends_on: [61-04, 61-05, 61-06]
files_modified:
  - apps/web/src/app/[locale]/(dashboard)/settings/e-invoicing/page.tsx
  - apps/web/src/app/[locale]/(dashboard)/settings/e-invoicing/__tests__/page.test.tsx
  - apps/web/src/components/settings/e-invoicing/peppol-participant-card.tsx
  - apps/web/src/components/settings/e-invoicing/peppol-participant-register-dialog.tsx
  - apps/web/src/components/settings/e-invoicing/peppol-participant-status-pill.tsx
  - apps/web/src/components/settings/e-invoicing/leitweg-id-list-card.tsx
  - apps/web/src/components/settings/e-invoicing/leitweg-id-row.tsx
  - apps/web/src/components/settings/e-invoicing/leitweg-id-create-dialog.tsx
  - apps/web/src/components/settings/e-invoicing/leitweg-id-delete-dialog.tsx
  - apps/web/src/components/settings/e-invoicing/__tests__/peppol-participant-card.test.tsx
  - apps/web/src/components/settings/e-invoicing/__tests__/leitweg-id-row.test.tsx
  - apps/web/src/components/settings/e-invoicing/__tests__/leitweg-id-create-dialog.test.tsx
  - apps/web/src/components/contractors/leitweg-id-inline-selector.tsx
  - apps/web/src/components/contractors/__tests__/leitweg-id-inline-selector.test.tsx
  - apps/web/src/components/contractors/peppol-identifier-fields.tsx
  - apps/web/messages/en.json
  - apps/web/messages/de.json
autonomous: true
requirements:
  - EINV-05
  - EINV-06
must_haves:
  truths:
    - "Settings → E-invoicing page renders under /[locale]/(dashboard)/settings/e-invoicing/ with Peppol participant card + Leitweg-ID list card"
    - "Peppol participant card shows status pill (NOT_REGISTERED / PENDING / REGISTERED / ACTIVE / SUSPENDED / DEREGISTERED) with semantic-triad (colour + icon + text)"
    - "Register dialog submits scheme + value, receives response, flips card to PENDING pill; webhook-driven status changes surface without manual refresh"
    - "Leitweg-ID list card renders LeitwegIdRow per row with mono value, description, contractor/contract badges, and a dropdown-menu for Edit / Set default / Delete"
    - "Create Leitweg-ID dialog validates real-time via leitwegIdSchema; save disabled until format + check-digit pass AND no duplicate"
    - "LeitwegIdInlineSelector on contractor profile and contract pages lets the user pick/add a Leitweg-ID; 'Add new' opens the dialog pre-filled with the current contractorId or contractId"
    - "PeppolIdentifierFields form group on contractor profile enforces pair-constraint (both scheme + value or neither) via peppolParticipantPairSchema"
    - "All visible strings come from the EInvoice.* namespace in apps/web/messages (no hard-coded UI strings)"
    - "A11y: every interactive control is keyboard-reachable, focus-visible, with an aria-label/live region per UI-SPEC §Accessibility Contract"
    - "Destructive confirmations (delete Leitweg-ID, deregister participant) use shadcn AlertDialog per UI-SPEC"
  artifacts:
    - path: "apps/web/src/app/[locale]/(dashboard)/settings/e-invoicing/page.tsx"
      provides: "Settings → E-invoicing page RSC"
      contains: "E-invoicing"
    - path: "apps/web/src/components/settings/e-invoicing/peppol-participant-card.tsx"
      provides: "Peppol participant status + register/deregister CTAs"
      contains: "PeppolParticipantCard"
    - path: "apps/web/src/components/settings/e-invoicing/leitweg-id-create-dialog.tsx"
      provides: "Leitweg-ID create/edit dialog"
      contains: "LeitwegIdCreateDialog"
    - path: "apps/web/src/components/contractors/leitweg-id-inline-selector.tsx"
      provides: "Contractor / contract page Leitweg-ID selector"
      contains: "LeitwegIdInlineSelector"
  key_links:
    - from: "apps/web/src/app/[locale]/(dashboard)/settings/e-invoicing/page.tsx"
      to: "packages/api/src/routers/peppol.ts"
      via: "trpc.peppol.listParticipants / lookupCapabilities"
      pattern: "trpc\\.peppol\\.(listParticipants|lookupCapabilities)"
    - from: "apps/web/src/components/settings/e-invoicing/leitweg-id-list-card.tsx"
      to: "packages/api/src/routers/leitweg-id.ts"
      via: "trpc.leitwegId.list"
      pattern: "trpc\\.leitwegId\\.list"
    - from: "apps/web/src/components/settings/e-invoicing/leitweg-id-create-dialog.tsx"
      to: "packages/validators/src/leitweg-id.ts"
      via: "leitwegIdSchema via react-hook-form resolver"
      pattern: "leitwegIdSchema"
---

<objective>
Ship the Settings → E-invoicing surface + the Leitweg-ID selection/editing UI on contractor + contract pages (EINV-05 + EINV-06 UI half). Reuses the existing Precision Craft design tokens + shadcn primitives per 61-UI-SPEC.md; introduces zero new third-party registries. This plan does NOT touch the invoices list or E-invoice tab — those are Plan 08.

Purpose: the tRPC routers from Plans 04-06 need their user-facing CRUD surface. Without this plan, users cannot register a Peppol participant or manage Leitweg-IDs, blocking every downstream flow.

Output: fully functional Settings page + five Leitweg-ID dialog/row components + contractor-profile Peppol identifier fields with pair-constraint + Leitweg-ID inline selector component. All tests green under RTL.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-xrechnung-e-invoicing/61-CONTEXT.md
@.planning/phases/61-xrechnung-e-invoicing/61-UI-SPEC.md
@.planning/phases/61-xrechnung-e-invoicing/61-04-SUMMARY.md
@.planning/phases/61-xrechnung-e-invoicing/61-05-SUMMARY.md
@.planning/phases/61-xrechnung-e-invoicing/61-06-SUMMARY.md
@apps/web/src/app/globals.css
@apps/web/components.json
@apps/web/src/components/ui/card.tsx
@apps/web/src/components/ui/badge.tsx
@apps/web/src/components/ui/dialog.tsx
@apps/web/src/components/ui/alert-dialog.tsx
@apps/web/src/components/ui/dropdown-menu.tsx
@apps/web/src/components/ui/table.tsx
@apps/web/src/components/ui/bdi.tsx
@apps/web/src/app/[locale]/(dashboard)/settings/page.tsx
@apps/web/messages/en.json
@packages/validators/src/leitweg-id.ts

<interfaces>
<!-- tRPC hooks available from Plans 04/05/06 (verify before use). -->

- `trpc.leitwegId.list.useQuery()` → { data: LeitwegIdWithRelations[], isLoading, error }
- `trpc.leitwegId.listByContractor.useQuery({ contractorId })` → same shape, filtered
- `trpc.leitwegId.create.useMutation()` / `update` / `delete` / `setDefault`
- `trpc.peppol.listParticipants.useQuery()` → PeppolParticipant[] with supportsXRechnungCii + lastCapabilityCheckAt
- `trpc.peppol.connect.useMutation()` (existing; still used for registration)
- `trpc.peppol.disconnect.useMutation()` (existing; used for deregister)
- `trpc.peppol.lookupCapabilities.useQuery({ schemeId, value })` — fresh or cached result
- leitwegIdSchema + peppolParticipantPairSchema from @contractor-ops/validators — used as react-hook-form zodResolver

Semantic-triad pill palette (from UI-SPEC §Color):
  VALID / ACTIVE / DELIVERED: border-success + CircleCheck icon + text
  WARNINGS / PENDING / SUSPENDED / LEITWEG_ID_MISSING: border-warning + ShieldAlert
  INVALID / FAILED / NOT_REACHABLE: border-destructive + ShieldX
  QUEUED / SENT / in-flight: border-info + CircleDashed
  NOT_* / unknown: border-muted + Circle
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings → E-invoicing page + PeppolParticipantCard + register/deregister dialogs + status pill</name>
  <files>
    apps/web/src/app/[locale]/(dashboard)/settings/e-invoicing/page.tsx,
    apps/web/src/app/[locale]/(dashboard)/settings/e-invoicing/__tests__/page.test.tsx,
    apps/web/src/components/settings/e-invoicing/peppol-participant-card.tsx,
    apps/web/src/components/settings/e-invoicing/peppol-participant-register-dialog.tsx,
    apps/web/src/components/settings/e-invoicing/peppol-participant-status-pill.tsx,
    apps/web/src/components/settings/e-invoicing/__tests__/peppol-participant-card.test.tsx,
    apps/web/messages/en.json,
    apps/web/messages/de.json
  </files>
  <read_first>
    .planning/phases/61-xrechnung-e-invoicing/61-UI-SPEC.md,
    apps/web/src/app/[locale]/(dashboard)/settings/page.tsx,
    apps/web/src/components/ui/card.tsx,
    apps/web/src/components/ui/dialog.tsx,
    apps/web/src/components/ui/alert-dialog.tsx,
    apps/web/src/components/ui/badge.tsx,
    apps/web/messages/en.json,
    apps/web/messages/de.json
  </read_first>
  <behavior>
    - `page.tsx` is a React Server Component. Reads `params.locale`, sets `const t = await getTranslations('EInvoice.Settings')`. Layout:
      - Breadcrumb (Dashboard > Settings > E-invoicing) — reuses existing Breadcrumb shadcn primitive.
      - Page header: `<h1 className="text-3xl font-display">{t('h1')}</h1>` + `<p className="text-sm text-muted-foreground">{t('subline')}</p>`.
      - Two vertically-stacked cards with `space-y-6`: `<PeppolParticipantCard />` followed by `<LeitwegIdListCard />` (Task 2 ships the second).
      - The page itself uses tRPC server caller (via `createCaller` in SSR) to prefetch `peppol.listParticipants` + `leitwegId.list`, passing as `initialData` to the client cards so first paint has no loading flash. Fallback: if the prefetch fails (e.g. no participant yet), passes `undefined` and the client components render skeletons.

    - `PeppolParticipantCard` is a client component. Reads `trpc.peppol.listParticipants.useQuery()`. If no active participant (status === 'DEREGISTERED' or no row at all) → renders empty-state:
      - Icon (lucide `Globe` at 48px muted),
      - `<h3>{t('PeppolCard.Empty.heading')}</h3>` = "Not registered on Peppol",
      - `<p>{t('PeppolCard.Empty.body')}</p>` = UI-SPEC copy,
      - `<Button>{t('PeppolCard.RegisterCTA')}</Button>` opens `PeppolParticipantRegisterDialog`.
    - Active participant → renders participant card with:
      - `<PeppolParticipantStatusPill status={...} supportsXRechnungCii={...} />` at top-right,
      - `<dl>` labelled list: participant ID (mono, wrapped in `<bdi dir="ltr">`), scheme, ASP provider, last capability check timestamp,
      - `<Button variant="destructive">{t('PeppolCard.DeregisterCTA')}</Button>` opens AlertDialog confirmation → calls `trpc.peppol.disconnect.useMutation`.
    - `PeppolParticipantRegisterDialog` form controlled via react-hook-form + zodResolver(peppolParticipantPairSchema + schemeId + value). Fields: scheme (select with 0060, 0088, 0106 + "Other" → free-text), value (input). Save button disabled until form valid. On submit → `trpc.peppol.connect.useMutation({ onSuccess: () => queryClient.invalidateQueries(['peppol','listParticipants']) })`. Dialog closes; card shows PENDING.
    - `PeppolParticipantStatusPill` is a stateless `<Badge variant="outline" className={semanticTriadClass(status)}>` with a lucide icon: mapping exactly per UI-SPEC §Color semantic triad.

    - Tests (RTL):
      1. `page — renders with no participant`: mock tRPC caller to return empty array; assert heading "E-invoicing" visible; assert "Not registered on Peppol" rendered; assert Register CTA is an `<button>` keyboard-focusable.
      2. `page — renders ACTIVE participant`: mock with an ACTIVE row; assert pill shows "Active" + success icon; deregister button rendered.
      3. `PeppolParticipantCard — register dialog opens`: click Register CTA → dialog renders with scheme + value fields.
      4. `PeppolParticipantRegisterDialog — save disabled when invalid`: type scheme='99' (non-4-digit) → save button disabled.
      5. `PeppolParticipantRegisterDialog — save enabled when valid`: type scheme='0060' + value='12345678' → save button enabled.
      6. `status pill — a11y semantic triad`: for each of 6 statuses, assert the pill has a visible text label AND an aria-hidden icon (colour alone never conveys state).
      7. `i18n — German locale renders Sie-register strings`: render with locale='de'; assert "Peppol-Teilnehmer" (or the exact locked German label from `apps/web/messages/de.json`) present.

    - i18n additions: validate every string already seeded by Plan 01 in `apps/web/messages/en.json` + `de.json` exists under `EInvoice.Settings.PeppolCard.*`, `EInvoice.Settings.h1`, etc. If any gap surfaces during implementation, add the missing key to both files (en = UI-SPEC verbatim; de = formal Sie register translation).
  </behavior>
  <action>
    Step 1 — `peppol-participant-status-pill.tsx` first (pure stateless, easiest to test). Include a `semanticTriadClass(status, supportsXRechnungCii?)` helper exporting the three `<Badge>` className variants for success/warning/destructive/muted/info.

    Step 2 — `peppol-participant-register-dialog.tsx` using react-hook-form + existing `@hookform/resolvers/zod` (check package.json — if not present, add the dep). Form uses the UI-SPEC copy (header "Register Peppol participant", body copy, scheme helper "`0060` = UK Companies House, `0088` = GLN, `0106` = Dun & Bradstreet").

    Step 3 — `peppol-participant-card.tsx` composing the above + the deregister AlertDialog.

    Step 4 — `page.tsx` RSC — prefetch + compose.

    Step 5 — RTL tests + a11y assertions. Use `axe-core` via `jest-axe` or the existing e2e a11y harness (read any `packages/` test-utils for the current pattern; Phase 58/59 likely set one up).

    Step 6 — Run:
    `pnpm --filter web test -- --run "settings/e-invoicing|peppol-participant"`

    Step 7 — `pnpm --filter web exec tsc --noEmit`.
  </action>
  <verify>
    <automated>pnpm --filter web test -- --run "settings/e-invoicing|peppol-participant" 2>&1 | grep -qE "Tests\s+[0-9]+ passed" && test -f apps/web/src/app/[locale]/\(dashboard\)/settings/e-invoicing/page.tsx && test -f apps/web/src/components/settings/e-invoicing/peppol-participant-card.tsx && grep -q "getTranslations\|useTranslations" apps/web/src/app/\[locale\]/\(dashboard\)/settings/e-invoicing/page.tsx && grep -q "EInvoice.Settings" apps/web/src/app/\[locale\]/\(dashboard\)/settings/e-invoicing/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Page file exists and uses next-intl `getTranslations('EInvoice.Settings')` (server) — no hard-coded UI strings
    - `grep -c "from ['\"]@/components/ui/" apps/web/src/components/settings/e-invoicing/*.tsx` ≥4 (composes shadcn primitives)
    - No third-party shadcn registry block used: `grep -rn "tremor\|recharts\|shadcn-blocks\|mantine" apps/web/src/components/settings/e-invoicing/` returns no matches
    - `grep "semanticTriadClass\|border-success\|border-destructive\|border-warning" apps/web/src/components/settings/e-invoicing/peppol-participant-status-pill.tsx` matches (semantic-triad pattern)
    - `grep "bdi dir=\"ltr\"\|<Bdi" apps/web/src/components/settings/e-invoicing/peppol-participant-card.tsx` matches (mono-ID LTR in RTL locales)
    - RTL tests green: 7 tests from §behavior all passing
    - `pnpm --filter web exec tsc --noEmit` exits 0
    - No `console.*` in any new .tsx file: `grep -rn "console\." apps/web/src/components/settings/e-invoicing/` returns no matches
    - New strings added (if any) exist in both `en.json` AND `de.json`
  </acceptance_criteria>
  <done>
    Settings page + Peppol card + register/deregister flow working, i18n-driven, a11y-green, typecheck clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LeitwegIdListCard + row + create/edit/delete dialogs + inline selector for contractor/contract pages + PeppolIdentifierFields pair-constraint form</name>
  <files>
    apps/web/src/components/settings/e-invoicing/leitweg-id-list-card.tsx,
    apps/web/src/components/settings/e-invoicing/leitweg-id-row.tsx,
    apps/web/src/components/settings/e-invoicing/leitweg-id-create-dialog.tsx,
    apps/web/src/components/settings/e-invoicing/leitweg-id-delete-dialog.tsx,
    apps/web/src/components/settings/e-invoicing/__tests__/leitweg-id-row.test.tsx,
    apps/web/src/components/settings/e-invoicing/__tests__/leitweg-id-create-dialog.test.tsx,
    apps/web/src/components/contractors/leitweg-id-inline-selector.tsx,
    apps/web/src/components/contractors/peppol-identifier-fields.tsx,
    apps/web/src/components/contractors/__tests__/leitweg-id-inline-selector.test.tsx
  </files>
  <read_first>
    .planning/phases/61-xrechnung-e-invoicing/61-UI-SPEC.md  (§Copywriting Contract Leitweg-ID create / edit dialog + §Interaction Contracts Leitweg-ID rows),
    apps/web/src/components/ui/table.tsx,
    apps/web/src/components/ui/dropdown-menu.tsx,
    apps/web/src/components/ui/dialog.tsx,
    apps/web/src/components/ui/alert-dialog.tsx,
    apps/web/src/components/ui/select.tsx,
    apps/web/src/components/ui/popover.tsx,
    apps/web/src/components/ui/calendar.tsx,
    apps/web/src/components/ui/switch.tsx,
    packages/validators/src/leitweg-id.ts,
    apps/web/messages/en.json
  </read_first>
  <behavior>
    - `LeitwegIdListCard` client component — uses `trpc.leitwegId.list.useQuery()`. Renders `<Card>` with heading + "Create Leitweg-ID" CTA in header-right. Body:
      - Empty: lucide `Inbox` icon + "No Leitweg-IDs registered" + body from UI-SPEC.
      - Non-empty: `<Table>` with columns `Value | Description | Assigned to | Default? | Valid period | Actions` — each row is `<LeitwegIdRow />`.
      - Clicking Create → opens `LeitwegIdCreateDialog` in create mode.

    - `LeitwegIdRow` — mono-font value cell (JetBrains Mono) wrapped in `<Bdi dir="ltr">` for RTL safety. Badges for contractor / contract assignment. Default indicator: small check badge if `isDefaultForContractor=true`. Actions: shadcn `DropdownMenu` with items Edit (opens dialog in edit mode), Set default (optimistic, calls `trpc.leitwegId.setDefault.useMutation`), Delete (opens `LeitwegIdDeleteDialog`).

    - `LeitwegIdCreateDialog` — react-hook-form + zodResolver(createLeitwegIdInput from Plan 04). Fields per UI-SPEC: value (text, mono), description (optional text), contractor (select, lists org's contractors via `trpc.contractor.listAll.useQuery` or similar existing procedure; read the existing contractor router to pick the right query), contract (select, dependent on selected contractor), isDefaultForContractor (switch, disabled unless contractor selected), validFrom + validTo (date pickers via `Popover`+`Calendar`), notes (textarea). Real-time validation on `value` field via `leitwegIdSchema.safeParse` — error message from Zod shown under the input. Save button disabled until `formState.isValid`. On success → dialog closes; optimistic update adds the row to the list.

    - `LeitwegIdDeleteDialog` — shadcn `AlertDialog` with UI-SPEC-locked copy ("Delete this Leitweg-ID?" + body + destructive button "Delete Leitweg-ID"). On confirm → `trpc.leitwegId.delete.useMutation` + optimistic row removal.

    - `LeitwegIdInlineSelector` — drop-in for contractor profile + contract pages. Props: `{ mode: 'contractor' | 'contract'; contractorId?: string; contractId?: string; value: string | null; onChange: (leitwegIdRowId: string | null) => void }`. UI:
      - shadcn `Select` populated via `trpc.leitwegId.listByContractor.useQuery({ contractorId })` (or `listByContract`).
      - An "Add new" item at the bottom opens `LeitwegIdCreateDialog` pre-filled with the current contractorId or contractId. On create success, auto-selects the new id via onChange.
    - When rendered on a DE public-sector buyer and no Leitweg-ID is selected → an inline `<Alert variant="warning">` with UI-SPEC copy "Leitweg-ID missing for German public-sector buyer" + body.

    - `PeppolIdentifierFields` — reusable form-field group for contractor profile (and potentially Contract). Uses react-hook-form context (assumes parent form). Two fields: peppolSchemeId (select: 0060 / 0088 / 0106 / custom), peppolParticipantValue (input). On any change, re-validates the pair via `peppolParticipantPairSchema`; if the user sets one but not the other, a form-level error appears ("Both fields are required or both must be empty"). Errors render via shadcn `FormMessage`.

    - Tests (RTL):
      1. `LeitwegIdRow — mono value, dropdown opens`: render one row; click the three-dots → dropdown shows Edit / Set default / Delete.
      2. `LeitwegIdCreateDialog — real-time check-digit validation`: type a malformed value ("12-34") → inline error "Leitweg-ID's check digit doesn't match" + Save disabled. Type `991-33333TEST-33` → Save enabled.
      3. `LeitwegIdCreateDialog — duplicate shows server error`: mock create mutation to throw CONFLICT → dialog shows UI-SPEC duplicate error copy.
      4. `LeitwegIdCreateDialog — i18n German`: render in locale=de, assert formal-Sie translations present.
      5. `LeitwegIdDeleteDialog — confirms and calls mutation`: confirm button triggers `trpc.leitwegId.delete.useMutation`.
      6. `LeitwegIdInlineSelector — Add new opens dialog pre-filled`: click "Add new" → dialog opens with contractorId pre-populated (read-only or preselected).
      7. `LeitwegIdInlineSelector — warning on DE public-sector buyer without ID`: assert the warning alert rendered only when mode indicates public-sector DE buyer.
      8. `PeppolIdentifierFields — pair constraint`: type scheme only → form shows pair-error; clear scheme → error clears; enter both → no error.
  </behavior>
  <action>
    Step 1 — `LeitwegIdCreateDialog` first — most complex; react-hook-form + zodResolver + dependent contract select + date pickers.
    Step 2 — `LeitwegIdDeleteDialog` (simplest; AlertDialog wrapper).
    Step 3 — `LeitwegIdRow` + `LeitwegIdListCard` composing them.
    Step 4 — `LeitwegIdInlineSelector` + `PeppolIdentifierFields` — lift pattern-reusable logic from the dialog (don't duplicate zod resolution).
    Step 5 — RTL tests per §behavior.
    Step 6 — Wire `PeppolIdentifierFields` into the existing contractor profile form. Identify the existing file (likely `apps/web/src/components/contractors/contractor-profile-form.tsx` or similar; grep for `isPublicSectorBuyer` / existing pattern under `apps/web/src/components/contractors/`). Add the fields + the `LeitwegIdInlineSelector` to the DE-country-specific section (Phase 56's CountryComplianceSection-DE already has a hook for DE-only fields — use it).
    Step 7 — `pnpm --filter web test -- --run "leitweg-id|peppol-identifier"` + `pnpm --filter web exec tsc --noEmit`.
  </action>
  <verify>
    <automated>pnpm --filter web test -- --run "leitweg-id|peppol-identifier" 2>&1 | grep -qE "Tests\s+[0-9]+ passed" && grep -q "leitwegIdSchema" apps/web/src/components/settings/e-invoicing/leitweg-id-create-dialog.tsx && grep -q "peppolParticipantPairSchema" apps/web/src/components/contractors/peppol-identifier-fields.tsx && grep -q "DropdownMenu" apps/web/src/components/settings/e-invoicing/leitweg-id-row.tsx && grep -q "AlertDialog" apps/web/src/components/settings/e-invoicing/leitweg-id-delete-dialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "zodResolver.*leitwegIdSchema\|zodResolver(createLeitwegIdInput" apps/web/src/components/settings/e-invoicing/leitweg-id-create-dialog.tsx` matches (schema-driven validation)
    - `grep "font-mono\|var(--font-mono)" apps/web/src/components/settings/e-invoicing/leitweg-id-row.tsx` matches (mono value rendering)
    - `grep "<Bdi\|bdi dir" apps/web/src/components/settings/e-invoicing/leitweg-id-row.tsx` matches (LTR wrapper for RTL locales)
    - `grep "EInvoice.LeitwegIdDialog\|EInvoice.Settings.LeitwegIdCard\|t(['\"]EInvoice" apps/web/src/components/settings/e-invoicing/leitweg-id-create-dialog.tsx` matches (i18n)
    - `grep "peppolParticipantPairSchema" apps/web/src/components/contractors/peppol-identifier-fields.tsx` matches (pair constraint used)
    - `grep "isPublicSectorBuyer" apps/web/src/components/contractors/leitweg-id-inline-selector.tsx` matches (DE public-sector warning gate)
    - `grep -c "describe\|test\|it(" apps/web/src/components/settings/e-invoicing/__tests__/leitweg-id-create-dialog.test.tsx` ≥5 (≥5 RTL tests in this file)
    - All tests green; typecheck clean
    - No third-party registry blocks used: `grep -rn "tremor\|recharts\|mantine\|@shadcn-blocks" apps/web/src/components/settings/e-invoicing/ apps/web/src/components/contractors/` returns no matches
    - No `console.*` in any new .tsx file
  </acceptance_criteria>
  <done>
    All Leitweg-ID UI + contractor-profile pair-constraint fields live and tested.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → tRPC mutations | UI collects and submits; validator Zod runs both client-side (UX) and server-side (authoritative). |
| tRPC response → UI state | Includes sensitive org-scoped participant IDs — must not leak cross-tenant. |
| Rendered SVG/HTML from server | React escape by default; never `dangerouslySetInnerHTML`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-07-01 | Injection (XSS) | User-supplied Leitweg-ID description / notes rendered | mitigate | React auto-escapes text children; never `dangerouslySetInnerHTML`; no user-supplied HTML paths. |
| T-61-07-02 | Information Disclosure | Deep-linked Settings page accessed without session | mitigate | Route lives under `/[locale]/(dashboard)/` layout which enforces auth via middleware; RSC prefetch call carries session cookie. |
| T-61-07-03 | Elevation of Privilege | UI surfaces write CTAs to users without the required RBAC | mitigate | Server-side router enforces `requirePermission`; client also hides CTAs based on `trpc.auth.can({ permission: 'settings:update' })` query (existing pattern) — defence in depth. |
| T-61-07-04 | Information Disclosure | Participant ID / Leitweg-ID copy-to-clipboard leaks to unintended window | accept | Standard browser clipboard semantics; data is user's own; acceptable. |
| T-61-07-05 | Tampering | Optimistic delete roll-back on error | mitigate | TanStack Query `onMutate` returns a `rollback` fn; `onError` restores state; test #5 covers roll-back. |
| T-61-07-06 | Spoofing | LeitwegIdInlineSelector "Add new" dialog missing contractorId context | mitigate | Dialog accepts pre-fill via controlled prop; contractorId is never read from URL (avoids tampering). |
| T-61-07-07 | Repudiation | No user-visible log of who created a Leitweg-ID | accept | Server-side `EInvoiceLifecycleEvent` + Prisma `createdAt` suffice; UI surfacing deferred. |
</threat_model>

<verification>
`pnpm --filter web test -- --run "settings/e-invoicing|leitweg-id|peppol-identifier"` — green.
`pnpm --filter web exec tsc --noEmit` — clean.
Manual smoke: `pnpm --filter web dev` → `/en/settings/e-invoicing/` renders + Register Peppol flow works end-to-end against a seeded dev org.
</verification>

<success_criteria>
1. All must_haves truths hold.
2. Settings page renders with correct semantic-triad pill + all UI-SPEC copy keys resolved.
3. Leitweg-ID dialogs enforce check-digit via Zod before save enables.
4. Contractor profile shows pair-constrained Peppol identifier fields + Leitweg-ID inline selector on DE public-sector path.
5. RTL tests cover all critical a11y + pair-constraint + check-digit + i18n behaviors.
6. Typecheck clean; no new third-party registries; no console.*.
</success_criteria>

<output>
After completion, create `.planning/phases/61-xrechnung-e-invoicing/61-07-SUMMARY.md` covering: component inventory delivered vs UI-SPEC, any deviations from UI-SPEC (with rationale), a11y-axe score, i18n-key count added, optimistic-update rollback behaviour confirmed, bundle-size delta (should be ~0 since only shadcn primitives + existing deps).
</output>
