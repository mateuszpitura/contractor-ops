---
phase: 61
plan: 02
type: execute
wave: 1
depends_on: [61-01]
files_modified:
  - packages/einvoice/src/profiles/xrechnung-de/generator.ts
  - packages/einvoice/src/profiles/xrechnung-de/parser.ts
  - packages/einvoice/src/profiles/xrechnung-de/leitweg-id-embed.ts
  - packages/einvoice/src/profiles/xrechnung-de/index.ts
  - packages/einvoice/src/profiles/xrechnung-de/__tests__/generator.test.ts
  - packages/einvoice/src/profiles/xrechnung-de/__tests__/leitweg-id-embed.test.ts
  - packages/einvoice/src/registry.ts
  - packages/einvoice/src/index.ts
autonomous: true
requirements:
  - EINV-01
must_haves:
  truths:
    - "Generator produces CII XML passing basic libxmljs2 well-formedness check for a synthetic minimal EInvoice envelope"
    - "Generated XML embeds BT-10 Leitweg-ID at /rsm:CrossIndustryInvoice/rsm:SupplyChainTradeTransaction/ram:ApplicableHeaderTradeAgreement/ram:BuyerReference when a Leitweg-ID is supplied"
    - "Generator omits BT-10 when Leitweg-ID is null (not empty string)"
    - "Generator maps Phase-57 reverse-charge (isReverseCharge=true) to a CII tax category code 'AE' with TaxExemptionReason text using the Phase-56 locked §13b UStG phrase"
    - "Generator maps Kleinunternehmer (§19 UStG) invoices to category 'E' with the locked §19 UStG phrase"
    - "XRechnungDEProfile class registered in the einvoice registry under XRECHNUNG_DE_PROFILE_ID and discoverable via getProfileById('xrechnung-de')"
    - "packages/einvoice/src/index.ts re-exports the profile + XRECHNUNG_DE_PROFILE_ID constant"
  artifacts:
    - path: "packages/einvoice/src/profiles/xrechnung-de/generator.ts"
      provides: "generateXRechnungCii(invoice, leitwegId): string"
      contains: "generateXRechnungCii"
    - path: "packages/einvoice/src/profiles/xrechnung-de/leitweg-id-embed.ts"
      provides: "embedLeitwegIdIntoCii helper used by generator"
      contains: "BuyerReference"
    - path: "packages/einvoice/src/profiles/xrechnung-de/index.ts"
      provides: "XRechnungDEProfile class + registry registration"
      contains: "class XRechnungDEProfile"
    - path: "packages/einvoice/src/registry.ts"
      provides: "Registry entry for xrechnung-de"
      contains: "xrechnung-de"
  key_links:
    - from: "packages/einvoice/src/profiles/xrechnung-de/generator.ts"
      to: "packages/einvoice/src/profiles/xrechnung-de/constants.ts"
      via: "XRECHNUNG_CUSTOMIZATION_ID + namespace constant imports"
      pattern: "XRECHNUNG_CUSTOMIZATION_ID"
    - from: "packages/einvoice/src/profiles/xrechnung-de/index.ts"
      to: "packages/einvoice/src/registry.ts"
      via: "registerProfile(XRechnungDEProfile)"
      pattern: "registerProfile"
    - from: "packages/einvoice/src/profiles/xrechnung-de/generator.ts"
      to: "packages/validators/src/legal/de.ts"
      via: "Phase-56 locked §13b UStG / §19 UStG phrases"
      pattern: "STEUERSCHULDNERSCHAFT_DES_LEISTUNGSEMPFAENGERS|KLEINUNTERNEHMER_19_USTG"
---

<objective>
Implement the XRechnung 3.0.2 CII generator (EINV-01). Produces EN-16931-shaped Cross Industry Invoice XML from the existing `EInvoice` envelope, embeds Leitweg-ID at BT-10 via a dedicated helper, reuses the Phase-56 locked §13b UStG / §19 UStG phrases for tax-exemption reasoning, and registers `XRechnungDEProfile` in the einvoice registry. Parser stays a minimal stub — real CII parsing is Phase 62.

Purpose: unlock Plan 03 (validator) which needs the generator output to validate, Plan 05 (Storecove send path) which needs `profile.generateXml()`, and Plan 06 (einvoice router finalize mutation) which orchestrates both.

Output: a profile class discoverable via `getProfileById('xrechnung-de')`, generator returning deterministic CII XML, green generator + leitweg-id-embed tests (replacing the describe.todo stubs from Plan 01).
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-xrechnung-e-invoicing/61-CONTEXT.md
@.planning/phases/61-xrechnung-e-invoicing/61-RESEARCH.md
@.planning/phases/61-xrechnung-e-invoicing/61-01-SUMMARY.md
@packages/einvoice/src/profiles/peppol-ae/generator.ts
@packages/einvoice/src/profiles/peppol-ae/index.ts
@packages/einvoice/src/profiles/peppol-ae/constants.ts
@packages/einvoice/src/profiles/xrechnung-de/constants.ts
@packages/einvoice/src/profiles/xrechnung-de/schemas.ts
@packages/einvoice/src/registry.ts
@packages/einvoice/src/engine/xml-utils.ts
@packages/einvoice/src/types
@packages/validators/src/legal/de.ts

<interfaces>
<!-- Extract the core profile / EInvoice interfaces so the executor does not explore. -->

From packages/einvoice/src/index.ts — EInvoice shape (read the source for the exact exports; key types used here):
```typescript
export interface EInvoice {
  id: string;
  invoiceTypeCode: string;       // UN/CEFACT 1001 code — e.g. '380' (commercial invoice)
  issueDate: Date;
  dueDate?: Date;
  currencyCode: string;          // ISO 4217
  supplier: Party;
  customer: Party;
  lines: EInvoiceLine[];
  taxBreakdown: TaxSubtotal[];
  paymentMeans?: PaymentMeans[];
  totalAmountMinor: bigint;
  taxTotalMinor: bigint;
  isReverseCharge?: boolean;
  isKleinunternehmer?: boolean;
  // Phase 57 additions — see existing code
}

export interface EInvoiceProfile {
  readonly profileId: string;
  readonly displayName: string;
  generateXml(invoice: EInvoice, opts?: unknown): Promise<string> | string;
  parseXml(xml: string): Promise<EInvoice> | EInvoice;
  getComplianceStatus(organizationId: string): Promise<ComplianceStatus>;
}
```

From packages/einvoice/src/registry.ts:
```typescript
export function registerProfile(profile: EInvoiceProfile): void;
export function getProfileById(id: string): EInvoiceProfile | undefined;
export function listProfiles(): EInvoiceProfile[];
```

From packages/validators/src/legal/de.ts (Phase-56 locked phrases):
```typescript
export const STEUERSCHULDNERSCHAFT_DES_LEISTUNGSEMPFAENGERS = "Steuerschuldnerschaft des Leistungsempfängers";
// Plus Kleinunternehmer §19 UStG phrase — exact constant name to be read from the file; do NOT re-invent
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CII generator + Leitweg-ID embed helper against replacing-todo tests</name>
  <files>
    packages/einvoice/src/profiles/xrechnung-de/generator.ts,
    packages/einvoice/src/profiles/xrechnung-de/leitweg-id-embed.ts,
    packages/einvoice/src/profiles/xrechnung-de/__tests__/generator.test.ts,
    packages/einvoice/src/profiles/xrechnung-de/__tests__/leitweg-id-embed.test.ts
  </files>
  <read_first>
    packages/einvoice/src/profiles/peppol-ae/generator.ts,
    packages/einvoice/src/profiles/xrechnung-de/constants.ts,
    packages/einvoice/src/profiles/xrechnung-de/schemas.ts,
    packages/einvoice/src/engine/xml-utils.ts,
    packages/einvoice/src/types/invoice.ts,
    packages/validators/src/legal/de.ts,
    .planning/phases/61-xrechnung-e-invoicing/61-RESEARCH.md  (§Code Examples — CII skeleton; §Pitfalls 5–6)
  </read_first>
  <behavior>
    - Tests first (replace the Plan 01 `describe.todo` stubs with real tests):
      1. `generator — minimal valid invoice produces parseable CII`: build an `EInvoice` with a single line, EUR currency, 19% VAT; call `generateXRechnungCii(invoice, null)`; assert the output starts with `<?xml version="1.0"` and contains `<rsm:CrossIndustryInvoice` + the namespace declarations + the exact `XRECHNUNG_CUSTOMIZATION_ID` literal under `<ram:ID>` inside `GuidelineSpecifiedDocumentContextParameter` + invoice ID under `<ram:ID>` inside `<rsm:ExchangedDocument>`. Parse result with libxmljs2 to assert it is well-formed XML (do not run full XSD here — that's Plan 03).
      2. `generator — Leitweg-ID embedded at BT-10`: supply `leitwegId = '991-33333TEST-33'`; assert output contains `<ram:BuyerReference>991-33333TEST-33</ram:BuyerReference>` nested under `<ram:ApplicableHeaderTradeAgreement>` (use xpath via libxmljs2 to walk: `/rsm:CrossIndustryInvoice/rsm:SupplyChainTradeTransaction/ram:ApplicableHeaderTradeAgreement/ram:BuyerReference`).
      3. `generator — Leitweg-ID omitted when null`: assert output does NOT contain `<ram:BuyerReference>` when `leitwegId === null`.
      4. `generator — reverse-charge (§13b UStG)`: build invoice with `isReverseCharge: true`; assert `<ram:ApplicableTradeTax>` contains `<ram:CategoryCode>AE</ram:CategoryCode>` + `<ram:ExemptionReason>Steuerschuldnerschaft des Leistungsempfängers</ram:ExemptionReason>` with the EXACT phrase from the `packages/validators/src/legal/de.ts` constant (import and compare).
      5. `generator — Kleinunternehmer (§19 UStG)`: build invoice with `isKleinunternehmer: true`, zero-VAT lines; assert `<ram:CategoryCode>E</ram:CategoryCode>` + `<ram:ExemptionReason>` matches the locked Kleinunternehmer phrase constant (read the constant name from `legal/de.ts`; do NOT hard-code the German string in the test).
      6. `leitweg-id-embed.test.ts` unit: pass an arbitrary XML tree (or call the helper directly); assert the helper inserts the BT-10 element at the correct path and does not mutate other parts of the tree.

    - Implementation: `leitweg-id-embed.ts` exports `embedLeitwegIdIntoCii(doc: CiiDocShape, leitwegId: string): CiiDocShape` — returns a new object with `ram:BuyerReference` set on `ram:ApplicableHeaderTradeAgreement`. Uses structured clone, not mutation.

    - `generator.ts` exports `generateXRechnungCii(invoice: EInvoice, leitwegId: string | null): string`. Uses `fast-xml-parser` XMLBuilder (same library as `peppol-ae/generator.ts`; no string templating per RESEARCH anti-pattern). Namespace prefixes exactly match constants (`rsm:`, `ram:`, `udt:`, `qdt:`). Follows the skeleton from 61-RESEARCH.md §Code Examples lines 304–375, extended with:
      - `taxBreakdown.map(toCiiTax)` emits one `<ram:ApplicableTradeTax>` per breakdown row. For reverse-charge rows → `CategoryCode=AE`, `ExemptionReason` = Phase-56 §13b UStG constant. For Kleinunternehmer rows → `CategoryCode=E`, `ExemptionReason` = Phase-56 §19 UStG constant. For standard rows → `CategoryCode=S` with `RateApplicablePercent`.
      - `SpecifiedTradeSettlementHeaderMonetarySummation` emits `LineTotalAmount`, `TaxBasisTotalAmount`, `TaxTotalAmount` (with `currencyID` attr), `GrandTotalAmount`, `DuePayableAmount` — all as BigDecimal strings with exactly 2 decimals computed from the `bigint` minor units.
      - `toCiiDate(date)` returns `YYYYMMDD` (format `102`).
      - `BigInt → decimal string` helper uses `toFixed(2)` via a safe minor→major converter that does not rely on `Number` precision for amounts > 2^53.
    - Output ALWAYS starts with `<?xml version="1.0" encoding="UTF-8"?>` on its own line (2026-RESEARCH notes this is mandatory for KoSIT XSD).
  </behavior>
  <action>
    Step 1 — update the scaffolded test files from Plan 01 to replace each `describe.todo` with a real `describe/it` block per §behavior tests 1–6. Import `validateLeitwegCheckDigit` or `leitwegIdSchema` from `@contractor-ops/validators` only if needed for fixture validity.

    Step 2 — write `leitweg-id-embed.ts`:
    ```typescript
    import type { CiiDocShape } from './generator.js'; // structural type re-exported below

    export function embedLeitwegIdIntoCii(doc: CiiDocShape, leitwegId: string): CiiDocShape {
      const clone = structuredClone(doc);
      const root = clone['rsm:CrossIndustryInvoice'];
      const txn = root['rsm:SupplyChainTradeTransaction'];
      const agreement = txn['ram:ApplicableHeaderTradeAgreement'];
      agreement['ram:BuyerReference'] = leitwegId;
      return clone;
    }
    ```

    Step 3 — write `generator.ts` against the tests. Export both `generateXRechnungCii` AND the internal `CiiDocShape` type (for embed helper). Import constants from `./constants.js` and legal phrases from `@contractor-ops/validators/legal` (check the exact import path — `packages/validators/src/legal/de.ts` is re-exported via `packages/validators/src/index.ts`). Place the Leitweg-ID insertion via `embedLeitwegIdIntoCii(doc, leitwegId)` when `leitwegId !== null`.

    Step 4 — run the tests green:
    `pnpm --filter @contractor-ops/einvoice test -- --run xrechnung-de/__tests__/generator.test.ts xrechnung-de/__tests__/leitweg-id-embed.test.ts`

    Step 5 — assert no console.*, no `xml-crypto` imports (digital signature is explicitly deferred), no `String.raw` / template-string XML construction.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/einvoice test -- --run xrechnung-de/__tests__/generator.test.ts xrechnung-de/__tests__/leitweg-id-embed.test.ts 2>&1 | grep -qE "Tests\s+[0-9]+ passed" && ! grep -rn "xml-crypto" packages/einvoice/src/profiles/xrechnung-de/ && ! grep -rn "console\." packages/einvoice/src/profiles/xrechnung-de/generator.ts packages/einvoice/src/profiles/xrechnung-de/leitweg-id-embed.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export " packages/einvoice/src/profiles/xrechnung-de/generator.ts` returns ≥2 (generator fn + CiiDocShape type)
    - `grep "XMLBuilder" packages/einvoice/src/profiles/xrechnung-de/generator.ts` matches (fast-xml-parser used, not string templates)
    - `grep "embedLeitwegIdIntoCii" packages/einvoice/src/profiles/xrechnung-de/generator.ts` matches (helper invoked)
    - `grep -c "describe.todo\|it.todo" packages/einvoice/src/profiles/xrechnung-de/__tests__/generator.test.ts` returns 0 (all todos replaced with real tests)
    - `grep -c "describe.todo\|it.todo" packages/einvoice/src/profiles/xrechnung-de/__tests__/leitweg-id-embed.test.ts` returns 0
    - Tests pass: `pnpm --filter @contractor-ops/einvoice test -- --run xrechnung-de/__tests__/generator.test.ts xrechnung-de/__tests__/leitweg-id-embed.test.ts` exits 0
    - Locked §13b UStG phrase imported by constant name (not string-literal): `grep "STEUERSCHULDNERSCHAFT\|import.*legal" packages/einvoice/src/profiles/xrechnung-de/generator.ts` matches
    - No `xml-crypto` or `dangerouslySetInnerHTML` in any new file
    - Output of a spot-run starts with `<?xml version="1.0" encoding="UTF-8"?>` (confirm via `pnpm --filter @contractor-ops/einvoice exec node -e "const g=require('./dist/profiles/xrechnung-de/generator.js'); console.log(g.generateXRechnungCii(/*min invoice*/).slice(0,50))"` if dist is built; otherwise the test covers this)
  </acceptance_criteria>
  <done>
    Generator + embed helper implemented, tests green, no console/xml-crypto slippage.
  </done>
</task>

<task type="auto">
  <name>Task 2: XRechnungDEProfile class + registry registration + stub parser + package re-exports</name>
  <files>
    packages/einvoice/src/profiles/xrechnung-de/parser.ts,
    packages/einvoice/src/profiles/xrechnung-de/index.ts,
    packages/einvoice/src/registry.ts,
    packages/einvoice/src/index.ts
  </files>
  <read_first>
    packages/einvoice/src/profiles/peppol-ae/index.ts,
    packages/einvoice/src/registry.ts,
    packages/einvoice/src/index.ts,
    packages/einvoice/src/profiles/xrechnung-de/constants.ts,
    packages/einvoice/src/profiles/xrechnung-de/generator.ts
  </read_first>
  <behavior>
    - `parser.ts` exports `parseXRechnungCii(xml: string): EInvoice` as a **minimal stub** that throws `new Error('XRechnung CII inbound parsing is a Phase 62 feature')` — Plan 06 does NOT call this; it exists only to satisfy the `EInvoiceProfile.parseXml` contract. Documented as such in a TSDoc comment referencing `Phase 62 tracker`.
    - `index.ts` in `xrechnung-de/` exports:
      - `XRechnungDEProfile` class implementing `EInvoiceProfile`. `profileId = 'xrechnung-de'`, `displayName = 'XRechnung 3.0.2 (Germany, CII)'`.
      - `generateXml(invoice, opts?)` — calls `generateXRechnungCii(invoice, opts?.leitwegId ?? null)`.
      - `parseXml(xml)` — calls `parseXRechnungCii(xml)`.
      - `getComplianceStatus(organizationId)` — returns `{ profileId, displayName, state: 'active', degradedReasons: [] }` (minimal — Plan 06 enriches with actual org-scoped signal by querying `EInvoiceLifecycle` counts).
      - `registerProfile(new XRechnungDEProfile())` call at module-bottom so `import './profiles/xrechnung-de/index.js'` auto-registers.
    - `packages/einvoice/src/registry.ts` — if side-effect auto-register is already the pattern (peppol-ae registers via its own index.ts import at the package root), follow the pattern. Otherwise add an explicit import in `packages/einvoice/src/index.ts`.
    - `packages/einvoice/src/index.ts` — append re-exports:
      ```typescript
      export { XRechnungDEProfile } from './profiles/xrechnung-de/index.js';
      export {
        XRECHNUNG_DE_PROFILE_ID,
        XRECHNUNG_VERSION,
        XRECHNUNG_CUSTOMIZATION_ID,
        XRECHNUNG_PROFILE_ID,
        STORECOVE_CII_XRECHNUNG_DOC_TYPE_ID,
        KOSIT_RULE_SET_VERSION,
      } from './profiles/xrechnung-de/constants.js';
      export { generateXRechnungCii } from './profiles/xrechnung-de/generator.js';
      export { finalizeEInvoiceInputSchema, eInvoiceFormatSchema } from './profiles/xrechnung-de/schemas.js';
      import './profiles/xrechnung-de/index.js'; // side-effect: registry registration
      ```
    - Registry contains `xrechnung-de` after first `import '@contractor-ops/einvoice'` — verified by a tiny assertion test.
  </behavior>
  <action>
    Step 1 — write `parser.ts` stub with the throw + TSDoc.

    Step 2 — write `xrechnung-de/index.ts` per §behavior. Use the peppol-ae `index.ts` as structural reference for `EInvoiceProfile` conformance, but DO NOT copy UBL-specific logic. Profile class constructor takes no args (stateless).

    Step 3 — modify `packages/einvoice/src/index.ts` to add the re-exports + side-effect import. Verify the import path handles ESM correctly (`.js` extension suffix in TypeScript-NodeNext mode is already the package convention — see existing peppol-ae import).

    Step 4 — add a tiny registry test to `packages/einvoice/src/__tests__/registry.test.ts` (or create it if missing) asserting `getProfileById('xrechnung-de')` returns a defined profile with `profileId === 'xrechnung-de'`.

    Step 5 — run full einvoice suite:
    `pnpm --filter @contractor-ops/einvoice test -- --run`
    Must be green. `pnpm --filter @contractor-ops/einvoice tsc --noEmit` must be clean.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/einvoice test -- --run 2>&1 | grep -qE "Tests\s+[0-9]+ passed" && pnpm --filter @contractor-ops/einvoice exec tsc --noEmit 2>&1 | tail -5 | grep -qE "^$|Done" ; grep -q "XRechnungDEProfile" packages/einvoice/src/index.ts && grep -q "xrechnung-de" packages/einvoice/src/profiles/xrechnung-de/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class XRechnungDEProfile" packages/einvoice/src/profiles/xrechnung-de/index.ts` matches
    - `grep "registerProfile" packages/einvoice/src/profiles/xrechnung-de/index.ts` matches (auto-registration)
    - `grep "XRechnungDEProfile\|xrechnung-de" packages/einvoice/src/index.ts` matches (re-export + side-effect import)
    - `grep "Phase 62" packages/einvoice/src/profiles/xrechnung-de/parser.ts` matches (inbound-parse deferred marker)
    - `pnpm --filter @contractor-ops/einvoice exec tsc --noEmit` exits 0 (type-check clean)
    - `pnpm --filter @contractor-ops/einvoice test -- --run` exits 0 and registers xrechnung-de in `getProfileById`
  </acceptance_criteria>
  <done>
    `XRechnungDEProfile` discoverable in registry, package re-exports in place, typecheck clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `EInvoice` envelope → CII XML | Generator consumes already-authenticated org-scoped data; output is user-visible and transmitted. |
| Locked-phrase constants → XML ExemptionReason | Phase-56 locked German legal phrases flow into CII output. Any drift breaks KoSIT BR-DE rules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-02-01 | Tampering | CII XML construction via string concat → XXE / escape bugs | mitigate | Use fast-xml-parser XMLBuilder (RESEARCH anti-pattern explicitly forbids string templates); test asserts well-formedness. |
| T-61-02-02 | Tampering | Locked legal phrase drift via typo in ExemptionReason | mitigate | Import Phase-56 constants by name (NOT string literal); test compares XML output equality against imported constant. Locked-phrases CI guard (Phase 56) catches constant-value drift. |
| T-61-02-03 | Information Disclosure | BigInt → decimal conversion using `Number()` loses precision for amounts > 2^53 | mitigate | Helper uses string-based division from bigint minor units; no `Number()` coercion. |
| T-61-02-04 | Repudiation | Missing `CustomizationID` → Schematron applies wrong ruleset and "passes" invalid document | mitigate | Generator reads `XRECHNUNG_CUSTOMIZATION_ID` from constants (single source); test asserts exact literal equality in output. RESEARCH Pitfall 5 addressed. |
| T-61-02-05 | Elevation of Privilege | Registry auto-registration on module import | accept | Standard Node ESM side-effect; existing peppol-ae profile registers identically. Not a privilege boundary. |
</threat_model>

<verification>
`pnpm --filter @contractor-ops/einvoice test -- --run` green.
`pnpm --filter @contractor-ops/einvoice exec tsc --noEmit` clean.
Manual spot check: write a throwaway node one-liner `node -e "const e=require('@contractor-ops/einvoice'); console.log(e.generateXRechnungCii(mockInvoice, '991-33333TEST-33').includes('BuyerReference>991-33333TEST-33'))"` → prints `true`.
</verification>

<success_criteria>
1. All must_haves truths hold.
2. Generator + embed helper tests green; all Plan-01 todos replaced.
3. `getProfileById('xrechnung-de')` returns the profile.
4. Locked §13b / §19 phrases used by constant import — NOT string literal.
5. Typecheck clean across einvoice workspace.
</success_criteria>

<output>
After completion, create `.planning/phases/61-xrechnung-e-invoicing/61-02-SUMMARY.md` covering: generator behavior coverage (which BT-codes emitted), exact KoSIT paths for BT-10 / BT-120 / BT-118 confirmed, test count (before/after todo replacement), registry registration confirmed, any surprises (e.g. fast-xml-parser quirks with xmlns declaration ordering).
</output>
