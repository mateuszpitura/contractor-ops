---
phase: 67-phase-56-58-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/58-classification-engine-rule-sets/58-VERIFICATION.md
autonomous: true
requirements:
  - CLASS-01
  - CLASS-02
  - CLASS-05
  - CLASS-11

must_haves:
  truths:
    - "58-VERIFICATION.md exists at .planning/phases/58-classification-engine-rule-sets/58-VERIFICATION.md with frontmatter mirroring 63-VERIFICATION.md (phase, verified, status, score, gaps, manual_only) per CONTEXT.md D-14"
    - "Every requirement CLASS-01, CLASS-02, CLASS-05, CLASS-11 has an explicit row in `requirements_verified[]` mapping requirement-ID to test IDs + commit hashes (CONTEXT.md D-15)"
    - "Re-validation evidence captures live v2 HEAD command + pass/fail + count for each Phase 58 SUMMARY's Verification Results (CONTEXT.md D-05/D-07)"
    - "CLASS-01 evidence cites the registry (`packages/classification/src/registry.ts`) AND demonstrates the engine accepts UK + DE rule sets via `getProfileForCountry('GB' | 'DE')`"
    - "CLASS-02 evidence cites the IR35 + Scheinselbständigkeit profiles under `packages/classification/src/profiles/{ir35,scheinselbstandigkeit}/` AND the rule-set inventory tests"
    - "CLASS-05 evidence cites the DRV 20-criteria scoring tests (Weight, Score, Boundary, NotApplicable, EconDep cases) AND the IR35 5-area dispositive + 3-count leaning composite-rule tests"
    - "CLASS-11 evidence cites the per-engagement schema (`packages/db/prisma/schema/classification.prisma` ClassificationAssessment with contractorAssignmentId) AND the multi-tenant + single-draft enforcement tests in the tRPC router"
    - "Phase 64 feature-flag interaction acknowledged — verification confirms classification engine behaviour with the flag enabled (test environment); does NOT contradict Phase 64's 'inaccessible when disabled' invariant (CONTEXT.md `code_context.Integration Points`)"
    - "Adjacent fixes (if any) land as `fix(67-02): <summary>` commits BEFORE the `docs(67-02): write 58-VERIFICATION.md` commit (CONTEXT.md D-13, D-18)"
    - "If status is verified, gaps[] is empty (CONTEXT.md D-17)"
    - "Phase 58 manager flags (roadmap_complete, has_verification) flip after the docs commit lands (CONTEXT.md D-04)"
  artifacts:
    - path: ".planning/phases/58-classification-engine-rule-sets/58-VERIFICATION.md"
      provides: "Phase 58 formal verification report with CLASS-01/02/05/11 status"
      contains: "requirements_verified"
  key_links:
    - from: ".planning/phases/58-classification-engine-rule-sets/58-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "requirements_verified[] table maps CLASS-01/02/05/11"
      pattern: "CLASS-"
    - from: ".planning/phases/58-classification-engine-rule-sets/58-VERIFICATION.md"
      to: "packages/classification/src/profiles/"
      via: "test_ids reference IR35 + Scheinselbständigkeit profile test files"
      pattern: "scoring.test.ts"
    - from: ".planning/phases/58-classification-engine-rule-sets/58-VERIFICATION.md"
      to: "packages/db/prisma/schema/classification.prisma"
      via: "CLASS-11 evidence cites ClassificationAssessment model + contractorAssignmentId column"
      pattern: "contractorAssignmentId"
---

<objective>
Produce `.planning/phases/58-classification-engine-rule-sets/58-VERIFICATION.md` confirming CLASS-01, CLASS-02, CLASS-05, CLASS-11 are programmatically satisfied on current `v2` HEAD.

Purpose: Phase 58 shipped (5 plans, 58-01..58-05) with no formal VERIFICATION.md. The classification engine + IR35/DRV rule sets + per-engagement Prisma model are present and tested (245 classification tests + 46 api router tests + 32 locked-phrases-guard tests + 16 wizard tests + 6 disclaimer tests per the SUMMARY files), but `REQUIREMENTS.md` traceability shows CLASS-01/02/05 reset from "Complete" to "Pending" pending the audit-grade VERIFICATION.md (gap closure phase 67). This plan re-runs every Phase 58 acceptance command on `v2` HEAD (mirroring Phase 65 D-04 / Phase 66 D-04 — Phase 65 demonstrated 5/7 enumerated bugs were already closed by intervening commits, so we trust nothing without a fresh run), captures real pass/fail counts, and emits the report in the canonical `63-VERIFICATION.md` shape with the Phase 67 D-15/D-16 enrichments (`requirements_verified[]` + `disposition` field on `manual_only[]` rows).

Phase 64 feature-flag interaction (per CONTEXT.md `code_context.Integration Points`): the classification engine is gated behind the `classification-engine` feature flag (LEGAL-08/09). This verification confirms the engine works correctly with the flag enabled (the test environment); it does NOT re-verify Phase 64's "inaccessible when disabled" invariant (that's Phase 64's own verification scope). Phase 64 deferred items, if any (e.g. additional German DRV terminology / Scheinselbständigkeit legal review), land in `manual_only[]` with `disposition: pre_deploy_legal_review` mirroring the Phase 56 Steuerberater pattern (CONTEXT.md D-10).

Output: One new file (`58-VERIFICATION.md`), zero or more atomic `fix(67-02): <summary>` commits if re-validation surfaces one-touch breakage (CONTEXT.md D-11), one `docs(67-02): write 58-VERIFICATION.md` commit, and Phase 58 manager flags flipped (`roadmap_complete=true`, `has_verification=true`).
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/67-phase-56-58-verification/67-CONTEXT.md
@.planning/phases/63-uk-payments-financial-features/63-VERIFICATION.md
@.planning/phases/58-classification-engine-rule-sets/58-CONTEXT.md
@.planning/phases/58-classification-engine-rule-sets/58-VALIDATION.md
@.planning/phases/58-classification-engine-rule-sets/58-RESEARCH.md
@.planning/phases/58-classification-engine-rule-sets/58-01-SUMMARY.md
@.planning/phases/58-classification-engine-rule-sets/58-02-SUMMARY.md
@.planning/phases/58-classification-engine-rule-sets/58-03-SUMMARY.md
@.planning/phases/58-classification-engine-rule-sets/58-04-SUMMARY.md
@.planning/phases/58-classification-engine-rule-sets/58-05-SUMMARY.md
@.planning/phases/64-legal-compliance-hardening/64-CONTEXT.md
</context>

<threat_model>
- THREAT: Verification cites stale SUMMARY snapshot evidence instead of live `v2` HEAD runs → MITIGATION: CONTEXT.md D-05/D-07 mandate re-execution; every Programmatic Evidence row in 58-VERIFICATION.md MUST include the actual command, exit code, and pass count from a Task 1 re-run on `v2` HEAD (current HEAD: `850f03a3`). The acceptance criteria check that the report contains the literal commit hash from `git rev-parse --short HEAD` recorded at re-validation time.
- THREAT: CLASS-11 (per-engagement assessment storage) verified by a single test rather than by tracing the schema constraint AND the application-layer single-draft enforcement → MITIGATION: CLASS-11 row in `requirements_verified[]` cites BOTH the Prisma schema (`packages/db/prisma/schema/classification.prisma` — `ClassificationAssessment` keyed on `contractorAssignmentId`, intentional NO `@@unique([contractorAssignmentId, status])` per Phase 58 D-04 to allow append-only completed history) AND the router test (`createDraft` handler enforces single-draft via `findFirst status='draft'` → return existing). Both layers are necessary; either alone is insufficient evidence per CONTEXT.md D-15 ("granularity matches the precision of the requirement claim").
- THREAT: CLASS-05 (IR35 5-area + DRV 20-criteria scoring) flagged as verified based on count of test files rather than coverage of the scoring algorithm corners → MITIGATION: CLASS-05 evidence cites the specific named test cases for the algorithm corners that prove correctness: IR35 Dispositive-1..5 (substitution + control + financial-risk + part-and-parcel + MoO dispositive verdicts) + Composite-1..6 (≥3 leaning count vs neutral-critical guard); DRV Weight-1..2 (30/30/25/15 category weights) + Score-1..2 (weighted-sum thresholds at 30/60) + Boundary-1..4 (band edges) + NotApplicable-1..2 + EconDep-1..4+edge (5/6-test billing-ratio bands, §2 SGB VI). Rule-set Inventory-1..7/9 cited separately as CLASS-02 evidence.
- THREAT: CLASS-01 (generic engine accepts multiple country rule sets) and CLASS-02 (per-country rule sets) get conflated into one row → MITIGATION: CLASS-01 specifically cites the registry contract (`registerProfile` / `getProfileForCountry` / duplicate rejection / unknown-country error message) + the Plan 01 registry tests; CLASS-02 specifically cites the IR35 + Scheinselbständigkeit rule-set inventory tests + the answer-type Zod schemas (`yes-no` / `likert-5` / `score-0-3` / `billing-ratio` / `rationale`) that prove the engine accepts heterogeneous rule shapes per country. CLASS-01 is "engine pluggability"; CLASS-02 is "rule sets exist for both countries with the right shape".
- THREAT: Phase 64 feature-flag short-circuit accidentally treated as a Phase 58 gap (e.g. "engine doesn't run when flag is off") → MITIGATION: CONTEXT.md `code_context.Integration Points` is explicit — Phase 58 verification confirms engine behaviour with flag enabled; the "inaccessible when disabled" invariant is Phase 64's scope. Acceptance criterion forbids any `gaps[]` row referencing the feature flag being off as a Phase 58 deficiency. If a related flag-related test fails on `v2` HEAD, it lands in `manual_only[]` with `disposition: pre_deploy_legal_review` (CONTEXT.md D-10) OR as a `gaps[]` entry with `follow_up_phase: Phase 64-VERIFICATION` if that's where the test belongs.
- THREAT: Adjacent-gap fix that needs investigation gets force-fitted as a one-touch fix → MITIGATION: identical CONTEXT.md D-11 vs D-12 strict triage as 67-01 Task 2.
- THREAT: 58-RESEARCH.md is large and contains German legal terminology that could leak as raw text into the verification report → MITIGATION: Task 3 references locked German phrases by their constant name (e.g. `CLASSIFICATION_SCHEIN_*` from `packages/validators/src/legal/de.ts`), never inlines the German text. The locked-phrases-guard test (R-05 in this plan, R-05 in 67-01 too) is the canonical assertion of phrase fidelity.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Re-validate Phase 58 acceptance commands on current v2 HEAD and capture Programmatic Evidence</name>
  <files>
    (read-only — captures evidence into Task 3's verification doc)
  </files>
  <read_first>
    .planning/phases/67-phase-56-58-verification/67-CONTEXT.md,
    .planning/phases/58-classification-engine-rule-sets/58-VALIDATION.md,
    .planning/phases/58-classification-engine-rule-sets/58-01-SUMMARY.md,
    .planning/phases/58-classification-engine-rule-sets/58-02-SUMMARY.md,
    .planning/phases/58-classification-engine-rule-sets/58-03-SUMMARY.md,
    .planning/phases/58-classification-engine-rule-sets/58-04-SUMMARY.md,
    .planning/phases/58-classification-engine-rule-sets/58-05-SUMMARY.md,
    packages/db/prisma/schema/classification.prisma,
    packages/classification/src/registry.ts
  </read_first>
  <action>
Capture the current HEAD short hash for the report:

```bash
HEAD_HASH=$(git -C /Users/mateusz.pitura/Repos/projects/contractor-ops rev-parse --short HEAD)
echo "Verification HEAD: $HEAD_HASH on branch v2"
```

Re-run each acceptance command extracted from the Phase 58 SUMMARY "Verification Results" sections (and from CONTEXT.md `code_context.Established Patterns` for the locked-phrases-guard expansion) and note the live exit code + pass count.

Re-validation matrix (all commands run from repo root unless otherwise noted; record actual results):

| #     | Source SUMMARY      | Requirement(s)              | Command |
|-------|---------------------|------------------------------|---------|
| R-01  | 58-01               | CLASS-01                     | `pnpm --filter @contractor-ops/classification typecheck` |
| R-02  | 58-01, 58-02        | CLASS-01, CLASS-02, CLASS-05 | `pnpm --filter @contractor-ops/classification test` |
| R-03  | 58-01               | CLASS-01                     | `pnpm --filter @contractor-ops/classification exec vitest run src/__tests__/registry.test.ts` |
| R-04  | 58-01               | CLASS-01, CLASS-02           | `pnpm --filter @contractor-ops/classification exec vitest run src/__tests__/snapshot.test.ts` |
| R-05  | 58-02               | CLASS-02, CLASS-05           | `pnpm --filter @contractor-ops/classification exec vitest run src/profiles/ir35/__tests__/scoring.test.ts src/profiles/ir35/__tests__/rule-set.test.ts` |
| R-06  | 58-02               | CLASS-02, CLASS-05           | `pnpm --filter @contractor-ops/classification exec vitest run src/profiles/scheinselbstandigkeit/__tests__/scoring.test.ts src/profiles/scheinselbstandigkeit/__tests__/rule-set.test.ts` |
| R-07  | 58-03               | CLASS-01, CLASS-11           | `pnpm --filter @contractor-ops/api test -- classification` |
| R-08  | 58-03               | CLASS-11                     | `pnpm --filter @contractor-ops/api exec vitest run src/middleware/__tests__/classification-rate-limit.test.ts` |
| R-09  | 58-04, 58-05        | CLASS-02, CLASS-05           | `pnpm --filter @contractor-ops/web test -- classification` |
| R-10  | 58-01, 58-04        | CLASS-02 (locked phrases)    | `pnpm --filter @contractor-ops/validators test --run locked-phrases-guard` |
| R-11  | 58-01, 58-02        | CLASS-01                     | `pnpm --filter @contractor-ops/classification build` |
| R-12  | 58-03               | CLASS-11                     | `grep -c "classification: classificationRouter" packages/api/src/root.ts` (expect: ≥1 — engine wired into tRPC root) |
| R-13  | 58-03               | CLASS-11                     | `grep -cE "tenantProcedure\|contractorUpdateProcedure" packages/api/src/routers/classification.ts` (expect: ≥10 per 58-03 SUMMARY) |
| R-14  | 58-01, 58-03        | CLASS-11                     | `grep -c "contractorAssignmentId" packages/db/prisma/schema/classification.prisma` (expect: ≥1 — per-engagement keying on ClassificationAssessment) |
| R-15  | 58-01               | CLASS-11                     | `grep -c "ClassificationAssessment " packages/db/prisma/schema/classification.prisma` (expect: ≥1 — model exists) |
| R-16  | 58-01               | CLASS-01                     | `grep -E "registerProfile\|getProfileForCountry\|getProfile\b\|listProfiles\|clearProfiles" packages/classification/src/registry.ts \| wc -l` (expect: ≥5 per Plan 01 SUMMARY) |
| R-17  | 58-02               | CLASS-02                     | `grep -E "ir35\|scheinselbstandigkeit" packages/classification/src/profiles/ir35/index.ts packages/classification/src/profiles/scheinselbstandigkeit/index.ts 2>/dev/null \| wc -l` (expect: ≥1 each, both profile barrels exist) |
| R-18  | 58-01               | CLASS-02 (locked phrases)    | `grep -c "CLASSIFICATION_SCHEIN_" packages/validators/src/legal/de.ts` (expect: ≥9 per Plan 01 SUMMARY) |
| R-19  | 58-01               | CLASS-02                     | `grep -c "RESERVED_DISCLAIMER_KEYS\|DISCLAIMER_IR35_BODY\|DISCLAIMER_SCHEIN_BODY" packages/validators/src/legal/disclaimers.ts` (expect: ≥3) |
| R-20  | 58-03               | CLASS-11                     | `grep -nE "findFirst.*status.*draft\|status: 'draft'" packages/api/src/routers/classification.ts \| head -5` (single-draft enforcement at handler layer per D-04 — append-only schema + handler gate) |

Execute each command in order. For each row capture:

```
{
  id: "R-NN",
  command: "<exact command>",
  exit_code: <int>,
  passed: <int>,    # extract from vitest output; for grep return the count; for build OK/FAIL
  failed: <int>,
  status: "PASS" | "FAIL",
  notes: "<one-line context — e.g. pre-existing baseline noise per 63-VERIFICATION.md>"
}
```

For any failure, classify per CONTEXT.md D-11 vs D-12:

- **One-touch fix candidate** (D-11): single mock-harness extension, single typo, single import-path correction. Schedule for Task 2.
- **Investigation needed** (D-12): root cause unclear, ripples across multiple files, requires schema migration, or already in `deferred-items.md`. Schedule for Task 3 as a `gaps[]` entry with `follow_up_phase` pointer (Phase 68 or backlog 999.x).
- **Pre-existing baseline noise**: failure documented in 63-VERIFICATION.md "Pre-existing baseline noise" paragraph (e.g. 57 `db.$queryRawUnsafe` failures across @contractor-ops/api KSeF/token-refresh — predates Phase 58, outside its surface area) OR in any phase's `deferred-items.md`. Capture in the report's "Pre-existing baseline noise" paragraph; do NOT count against CLASS-XX.
- **Phase 64 / feature-flag boundary**: if a failure is specifically about flag-disabled behaviour, it's NOT a Phase 58 gap — flag-disabled invariants belong to Phase 64's verification scope. Record as a `manual_only[]` row with `disposition: pre_deploy_legal_review` if it represents a future legal-review checkpoint, or as a cross-phase gap with `follow_up_phase: Phase 64-VERIFICATION` if it's a verification-shape gap rather than a code-level one.

Save the captured matrix to a session scratch buffer (no commits in this task — Task 3 writes the doc).

DO NOT modify any source files in this task. DO NOT touch Phase 65 or Phase 66 surface area (`packages/api/src/routers/{skonto,late-payment-interest,contractor,invoice,organization}.ts`, `packages/api/src/services/late-payment-interest.ts`, `packages/api/src/gov-api-clients.ts`).

If the working tree is dirty before re-validation begins, stop and write a STATE.md blocker.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && git status --porcelain | grep -v "^?? .planning/phases/67-phase-56-58-verification/" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Re-validation matrix has 20 rows (R-01..R-20) all populated with exit_code, passed count, and PASS/FAIL status
    - HEAD_HASH captured at start of Task 1 is recorded (used by Task 3)
    - Each FAIL row is classified into one of four buckets (one-touch fix / investigation / pre-existing baseline / Phase 64 boundary) with explicit reasoning
    - Working tree shows zero modifications outside `.planning/phases/67-phase-56-58-verification/`
    - Captured matrix is available as input to Task 2 (one-touch fixes) and Task 3 (verification doc)
  </acceptance_criteria>
  <done>The actual state of CLASS-01/02/05/11 on `v2` HEAD is known. Stale SUMMARY claims are replaced with live evidence. Failures are triaged into the four CONTEXT.md buckets ready for Tasks 2 and 3.</done>
</task>

<task type="auto">
  <name>Task 2: Apply one-touch adjacent fixes (D-11) — atomic fix(67-02) commits BEFORE the docs commit</name>
  <files>
    (depends on Task 1 — files determined per failure)
  </files>
  <read_first>
    .planning/phases/67-phase-56-58-verification/67-CONTEXT.md,
    .planning/phases/58-classification-engine-rule-sets/58-CONTEXT.md
  </read_first>
  <action>
**Skip this task entirely if Task 1's matrix has zero rows in the "one-touch fix candidate" bucket.** A 0-fix Task 2 is the expected case if Phase 58 is already green on `v2` HEAD.

For each one-touch fix candidate from Task 1 (CONTEXT.md D-11):

1. Make the single-file or single-touch change that repairs the failing acceptance signal. Allowed kinds (verbatim from CONTEXT.md D-11):
   - Single mock-harness extension (e.g. add a missing Prisma client method to a vitest mock — same shape as Phase 66 D-07 added `contract.count` / `contract.groupBy`)
   - Single typo correction
   - Single import-path correction
   - Single test-fixture data update

2. Re-run the SPECIFIC failing command from the Task 1 matrix to confirm the fix closes the signal. If the fix does NOT make the targeted command green, REVERT and reclassify as D-12 (investigation needed → escalate to gaps[] in Task 3).

3. Commit atomically with subject:

```
fix(67-02): <one-line description referencing the specific failure>
```

Body MUST include:
- The Task 1 matrix row ID (R-NN) the fix unblocks
- Which CLASS-XX requirement it unblocks
- The before/after grep or test output that proves the fix is one-touch
- Reference to CONTEXT.md D-11

Do NOT batch unrelated fixes (CONTEXT.md D-13). One commit per repaired failure. Do NOT use `--no-verify` (CONTEXT.md D-19). If a pre-commit hook fails, fix the underlying issue and create a NEW commit (project workflow rule, never amend).

Per-package execution-gate inheritance (CONTEXT.md D-19): for each touched package, ensure `pnpm --filter <changed-package> typecheck` is clean and the package's full test suite passes. If typecheck/test fails for an unrelated reason inside the same package, REVERT the fix commit and reclassify as a gap.

After ALL one-touch fixes land, update the Task 1 matrix in the session scratch buffer to reflect new PASS status (and note the fix commit hash). Task 3's report will distinguish "originally PASS" from "PASS after fix(67-02:NN)".
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && git log v2 --oneline | grep -cE '^[0-9a-f]+ fix\(67-02\):' || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - Zero or more `fix(67-02): <summary>` commits exist on the current branch
    - Every `fix(67-02)` commit body references a R-NN matrix row, a CLASS-XX requirement, and CONTEXT.md D-11
    - For each touched package: `pnpm --filter <package> typecheck` exits 0 AND the full package test suite passes
    - No `--no-verify` was used
    - Updated Task 1 matrix shows post-fix PASS status with the unblocking commit hash for each formerly-failing R-NN row
    - Phase 65/66 surface area untouched
  </acceptance_criteria>
  <done>One-touch failures (if any) are repaired with atomic, hook-clean commits scoped strictly to Phase 67-02. Multi-touch failures correctly punt to Task 3 gaps[] for follow-up phase scheduling.</done>
</task>

<task type="auto">
  <name>Task 3: Write 58-VERIFICATION.md with frontmatter + body in 63-VERIFICATION.md shape + Phase 67 D-15/D-16 enrichments</name>
  <files>
    .planning/phases/58-classification-engine-rule-sets/58-VERIFICATION.md
  </files>
  <read_first>
    .planning/phases/63-uk-payments-financial-features/63-VERIFICATION.md,
    .planning/phases/67-phase-56-58-verification/67-CONTEXT.md,
    .planning/phases/58-classification-engine-rule-sets/58-CONTEXT.md,
    .planning/phases/64-legal-compliance-hardening/64-CONTEXT.md,
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
Create `.planning/phases/58-classification-engine-rule-sets/58-VERIFICATION.md` with the following exact structure. Frontmatter MUST mirror 63-VERIFICATION.md's shape (CONTEXT.md D-14) plus the Phase 67 enrichments (D-15 `requirements_verified[]`, D-16 `manual_only[]` with `disposition`).

Score numerator = count of CLASS-XX with status `verified`; denominator is 4 (CLASS-01, CLASS-02, CLASS-05, CLASS-11).

Frontmatter `status` field MUST be exactly `verified` or `gaps_found` (CONTEXT.md D-17 — gaps and verified mutually exclusive).

Write the file using the following template, filling in `${...}` placeholders from Task 1's matrix and Task 2's commit list:

```markdown
---
phase: 58-classification-engine-rule-sets
verified: ${ISO_TIMESTAMP_AT_TASK1_START}
status: ${verified | gaps_found}
score: ${N}/4 must-haves verified                # N = count of CLASS-XX with status PASS
verified_at_commit: ${HEAD_HASH}
verified_branch: v2

requirements_verified:
  - id: CLASS-01
    description: "User can run a contractor classification risk assessment using a generic pluggable engine that supports multiple country rule sets"
    test_ids: [R-01, R-02, R-03, R-07, R-11, R-16]
    evidence_files:
      - "packages/classification/src/registry.ts"
      - "packages/classification/src/types/profile.ts"
      - "packages/classification/src/__tests__/registry.test.ts"
    status: ${PASS | FAIL}
    evidence_commits: [${from `git log -- packages/classification/src/registry.ts packages/classification/src/types/profile.ts`}]

  - id: CLASS-02
    description: "User can assess IR35 status for a UK contractor engagement using CEST-aligned questions across 5 assessment areas (substitution, control, financial risk, part-and-parcel, mutuality of obligation) with inside/outside/undetermined outcomes"
    test_ids: [R-02, R-05, R-09, R-10, R-17]
    evidence_files:
      - "packages/classification/src/profiles/ir35/"
      - "packages/classification/src/profiles/ir35/__tests__/scoring.test.ts"
      - "packages/classification/src/profiles/ir35/__tests__/rule-set.test.ts"
    status: ${PASS | FAIL}
    evidence_commits: [${from `git log -- packages/classification/src/profiles/ir35/`}]

  - id: CLASS-05
    description: "User can assess Scheinselbständigkeit risk for a German contractor engagement using ~20 DRV criteria across 4 categories (integration, entrepreneurial independence, personal dependency, economic dependency) with weighted risk scoring"
    test_ids: [R-02, R-06, R-09, R-18, R-19]
    evidence_files:
      - "packages/classification/src/profiles/scheinselbstandigkeit/"
      - "packages/classification/src/profiles/scheinselbstandigkeit/__tests__/scoring.test.ts"
      - "packages/classification/src/profiles/scheinselbstandigkeit/__tests__/rule-set.test.ts"
      - "packages/validators/src/legal/de.ts (CLASSIFICATION_SCHEIN_* constants)"
      - "packages/validators/src/legal/disclaimers.ts (DISCLAIMER_SCHEIN_BODY § 7a SGB IV reference)"
    status: ${PASS | FAIL}
    evidence_commits: [${from `git log -- packages/classification/src/profiles/scheinselbstandigkeit/ packages/validators/src/legal/de.ts packages/validators/src/legal/disclaimers.ts`}]

  - id: CLASS-11
    description: "Classification assessments are stored per-engagement (not per-contractor), supporting contractors with multiple concurrent engagements having independent assessments"
    test_ids: [R-07, R-08, R-12, R-13, R-14, R-15, R-20]
    evidence_files:
      - "packages/db/prisma/schema/classification.prisma (ClassificationAssessment.contractorAssignmentId column)"
      - "packages/api/src/routers/classification.ts (createDraft single-draft enforcement at handler layer per Phase 58 D-04)"
      - "packages/api/src/routers/__tests__/classification.test.ts (multi-tenant + single-draft router integration tests)"
      - "packages/api/src/middleware/__tests__/classification-rate-limit.test.ts"
    status: ${PASS | FAIL}
    evidence_commits: [${from `git log -- packages/db/prisma/schema/classification.prisma packages/api/src/routers/classification.ts`}]

gaps: ${YAML list — empty if status is verified; else one row per D-12 investigation-required failure}
# Example gap row when status is gaps_found:
# gaps:
#   - id: GAP-67-02-01
#     requirement: CLASS-XX
#     description: "<one-line failure summary>"
#     test_id: R-NN
#     follow_up_phase: "Phase 68 OR backlog 999.X"

manual_only: []   # default empty for Phase 58 — no Phase 56 Steuerberater equivalent gates Phase 58 closure;
                  # but if Task 1 surfaced any flag-related or German legal-review item that belongs in
                  # manual_only[], add it here with a justified disposition.
                  # Example (only if needed):
                  # manual_only:
                  #   - test: "DRV legal expert review of Scheinselbständigkeit weighting + thresholds + locked German phrases"
                  #     artifact: "(future review file under .planning/phases/58-classification-engine-rule-sets/ if commissioned)"
                  #     why_human: "DRV § 7a SGB IV interpretation + CLASSIFICATION_SCHEIN_* phrase fidelity require professional German legal review per CONTEXT.md D-10"
                  #     disposition: pre_deploy_legal_review

re_verification:
  previous_status: never_verified  # Phase 58 had no prior VERIFICATION.md
  ${if any fix(67-02) commits landed during Task 2:}
  fix_commits:
    - hash: "<short hash>"
      scope: "fix(67-02): <subject>"
      files: [<list>]
      changes: [<one-line per change>]
      unblocks_test_id: R-NN
      unblocks_requirement: CLASS-XX
---

# Phase 58: Classification Engine & Rule Sets Verification Report

**Phase Goal:** Users can run contractor classification assessments using a pluggable engine supporting per-country rule sets, with IR35 5-area scoring for UK and Scheinselbständigkeit 20-criteria scoring for Germany, and assessments stored per-engagement (not per-contractor) for independent multi-engagement tracking (per ROADMAP.md Phase 58 Success Criteria).

**Verified:** ${ISO_TIMESTAMP}
**Verified at commit:** `${HEAD_HASH}` on branch `v2`
**Status:** ${verified | gaps_found}
**Re-verification:** First pass (Phase 58 had no prior VERIFICATION.md — gap closure under Phase 67 per the v5.0 audit-gap closure trio with Phases 65 and 66).

## Goal Achievement

### Programmatic Evidence

| #     | Source SUMMARY  | Requirement(s)          | Command                                                                                       | Result                            | Status |
| ----- | --------------- | ----------------------- | --------------------------------------------------------------------------------------------- | --------------------------------- | ------ |
| R-01  | 58-01           | CLASS-01                | `pnpm --filter @contractor-ops/classification typecheck`                                      | ${exit 0 / non-zero}              | ${PASS\|FAIL} |
| R-02  | 58-01,02        | CLASS-01,02,05          | `pnpm --filter @contractor-ops/classification test`                                           | ${X passed / Y failed}            | ${PASS\|FAIL} |
| R-03  | 58-01           | CLASS-01                | `pnpm --filter @contractor-ops/classification exec vitest run src/__tests__/registry.test.ts` | ${X passed / Y failed}            | ${PASS\|FAIL} |
| R-04  | 58-01           | CLASS-01,02             | `pnpm --filter @contractor-ops/classification exec vitest run src/__tests__/snapshot.test.ts` | ${X passed / Y failed}            | ${PASS\|FAIL} |
| R-05  | 58-02           | CLASS-02,05             | `pnpm --filter @contractor-ops/classification exec vitest run src/profiles/ir35/__tests__/scoring.test.ts src/profiles/ir35/__tests__/rule-set.test.ts` | ${X passed / Y failed} | ${PASS\|FAIL} |
| R-06  | 58-02           | CLASS-02,05             | `pnpm --filter @contractor-ops/classification exec vitest run src/profiles/scheinselbstandigkeit/__tests__/scoring.test.ts src/profiles/scheinselbstandigkeit/__tests__/rule-set.test.ts` | ${X passed / Y failed} | ${PASS\|FAIL} |
| R-07  | 58-03           | CLASS-01,11             | `pnpm --filter @contractor-ops/api test -- classification`                                    | ${X passed / Y failed}            | ${PASS\|FAIL} |
| R-08  | 58-03           | CLASS-11                | `pnpm --filter @contractor-ops/api exec vitest run src/middleware/__tests__/classification-rate-limit.test.ts` | ${X passed / Y failed} | ${PASS\|FAIL} |
| R-09  | 58-04,05        | CLASS-02,05             | `pnpm --filter @contractor-ops/web test -- classification`                                    | ${X passed / Y failed}            | ${PASS\|FAIL} |
| R-10  | 58-01,04        | CLASS-02 (locked phrases) | `pnpm --filter @contractor-ops/validators test --run locked-phrases-guard`                  | ${X passed / Y failed}            | ${PASS\|FAIL} |
| R-11  | 58-01,02        | CLASS-01                | `pnpm --filter @contractor-ops/classification build`                                          | ${exit 0 / non-zero}              | ${PASS\|FAIL} |
| R-12  | 58-03           | CLASS-11                | `grep -c "classification: classificationRouter" packages/api/src/root.ts`                     | ${count, expect ≥1}               | ${PASS\|FAIL} |
| R-13  | 58-03           | CLASS-11                | `grep -cE "tenantProcedure\|contractorUpdateProcedure" packages/api/src/routers/classification.ts` | ${count, expect ≥10}         | ${PASS\|FAIL} |
| R-14  | 58-01,03        | CLASS-11                | `grep -c "contractorAssignmentId" packages/db/prisma/schema/classification.prisma`            | ${count, expect ≥1}               | ${PASS\|FAIL} |
| R-15  | 58-01           | CLASS-11                | `grep -c "ClassificationAssessment " packages/db/prisma/schema/classification.prisma`         | ${count, expect ≥1}               | ${PASS\|FAIL} |
| R-16  | 58-01           | CLASS-01                | `grep -E "registerProfile\|getProfileForCountry\|getProfile\b\|listProfiles\|clearProfiles" packages/classification/src/registry.ts \| wc -l` | ${count, expect ≥5} | ${PASS\|FAIL} |
| R-17  | 58-02           | CLASS-02                | `grep -E "ir35\|scheinselbstandigkeit" packages/classification/src/profiles/ir35/index.ts packages/classification/src/profiles/scheinselbstandigkeit/index.ts \| wc -l` | ${count, expect ≥2} | ${PASS\|FAIL} |
| R-18  | 58-01           | CLASS-02 (locked phrases)| `grep -c "CLASSIFICATION_SCHEIN_" packages/validators/src/legal/de.ts`                       | ${count, expect ≥9}               | ${PASS\|FAIL} |
| R-19  | 58-01           | CLASS-02                | `grep -c "RESERVED_DISCLAIMER_KEYS\|DISCLAIMER_IR35_BODY\|DISCLAIMER_SCHEIN_BODY" packages/validators/src/legal/disclaimers.ts` | ${count, expect ≥3} | ${PASS\|FAIL} |
| R-20  | 58-03           | CLASS-11                | `grep -nE "findFirst.*status.*draft\|status: 'draft'" packages/api/src/routers/classification.ts` | ${count, expect ≥1}            | ${PASS\|FAIL} |

**Score:** ${N_pass}/${N_total} re-validation rows green. ${N_fixed_in_67_02} originally-failing rows closed by `fix(67-02:NN)` commits this iteration. ${N_gaps} rows remain as `gaps[]` entries with follow-up phase pointers (status: gaps_found). ${N_pre_existing_baseline} rows excluded as pre-existing baseline noise per the 63-VERIFICATION.md carve-out (e.g. KSeF/token-refresh `db.$queryRawUnsafe` failures predate Phase 58).

### Requirements Coverage

| Requirement | Source Plans     | Description                                                                                                                                                            | Status              | Evidence                                                                                                                                                                                                                                                                                                                |
| ----------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CLASS-01    | 58-01, 58-03     | Generic pluggable classification engine with `registerProfile` / `getProfileForCountry` / duplicate rejection + unknown-country error contract                          | ${✓ VERIFIED \| ✗ GAP}  | R-01 (typecheck), R-02 (full test suite per Plan 02 SUMMARY: 245/245 by Plan 05 closure), R-03 (registry tests: unknown country, idempotent clear/register, duplicate rejection, ID + country lookup, extensibility proof — 6 tests per Plan 01 SUMMARY), R-11 (build), R-16 (5 contract symbols exported)                |
| CLASS-02    | 58-01, 58-02     | Per-country rule sets: IR35 (5 CEST areas) + Scheinselbständigkeit (4 DRV categories, ~20 criteria) profiles registered + answer-type Zod schemas (yes-no/likert-5/score-0-3/billing-ratio/rationale) | ${✓ VERIFIED \| ✗ GAP}  | R-05 (IR35 scoring 21 + rule-set 8 tests per Plan 02 SUMMARY), R-06 (DRV scoring 21 + rule-set 13 tests), R-10 (locked-phrases-guard 32/32 — guards CLASSIFICATION_SCHEIN_* phrase fidelity), R-17 (both profile barrels exist), R-18 (9 CLASSIFICATION_SCHEIN_* constants registered)                                                          |
| CLASS-05    | 58-02            | IR35 5-area dispositive + 3-count leaning composite-rule scoring (Dispositive-1..5, Composite-1..6) AND DRV 30/30/25/15 weighted-sum scoring with thresholds at 30/60 + Boundary-1..4 + EconDep-1..4+edge (5/6-test billing-ratio bands, §2 SGB VI) | ${✓ VERIFIED \| ✗ GAP}  | R-05 (IR35 scoring tests cover Dispositive + Composite + Shape + area helpers), R-06 (DRV scoring tests cover Weight + Score + Boundary + NotApplicable + EconDep + CategoryBreakdown + Zero + kind/version/drvRefs + targetTotalScore helper), R-09 (web wizard tests confirm UI consumes scoring outcomes correctly)                                       |
| CLASS-11    | 58-01, 58-03     | Per-engagement assessment storage (Prisma `ClassificationAssessment.contractorAssignmentId`), append-only completed history (no @@unique([contractorAssignmentId, status]) per Phase 58 D-04), single-draft enforced at `createDraft` handler layer | ${✓ VERIFIED \| ✗ GAP}  | R-07 (api router classification suite: 36/36 in Plan 04 then 46/46 by Plan 05 close — multi-tenant cross-org rejection, single-draft idempotency, immutableAfter on submit, getLatest by completedAt), R-08 (rate-limit middleware), R-12 (router wired in root.ts), R-13 (≥10 procedures with tenantProcedure or contractorUpdateProcedure), R-14 (contractorAssignmentId column present), R-15 (model exists), R-20 (single-draft handler enforcement) |

All 4 requirement IDs declared for Phase 58 in REQUIREMENTS.md are accounted for. ${If status is verified:} Status flips from `Pending` to `Complete` for all 4 in REQUIREMENTS.md after this VERIFICATION.md commits and Phase 58 manager flags flip per CONTEXT.md D-04. ${If status is gaps_found:} ${N_passing} of 4 close; ${N_failing} remain pending until the follow-up phase referenced in `gaps[]` lands.

### Phase 64 Feature-Flag Boundary

This verification confirms classification engine behaviour with the `classification-engine` feature flag enabled (the test environment). Phase 64 owns the "completely inaccessible when disabled" invariant (LEGAL-08/09); that invariant is OUT OF SCOPE for Phase 58 verification per CONTEXT.md `code_context.Integration Points`. Any "flag off => engine still runs" failure observed during Task 1 was therefore routed to either:
- a `gaps[]` entry with `follow_up_phase: Phase 64-VERIFICATION` (cross-phase gap shape) if the failure represents a verification-shape gap rather than a code-level Phase 58 issue; OR
- a `manual_only[]` entry with `disposition: pre_deploy_legal_review` if the item represents a future legal-review checkpoint (per CONTEXT.md D-10 Steuerberater-equivalent for German DRV terminology).

${If neither bucket was used:} No Phase 64 cross-phase items surfaced during re-validation — Phase 58 verification scope cleanly contained.

### Manual / Deferred Verification

${If manual_only[] is empty:} **None.** Unlike Phase 56 (Steuerberater sign-off), Phase 58 has no pre-commissioned legal-review artifact in the phase directory. The CLASSIFICATION_SCHEIN_* locked phrases + DISCLAIMER_SCHEIN_BODY § 7a SGB IV reference are guarded by R-10 (locked-phrases-guard) which is a programmatic CI gate; future legal review of DRV interpretation thresholds (e.g. the 70%/83.33% billing-ratio bands per § 2 SGB VI in Plan 02 SUMMARY notes) is a Phase 60 / pre-deploy ops concern, not a Phase 58 closure gate.

${If manual_only[] has entries:} See structured `manual_only` items in frontmatter. Each row carries a `disposition` field (CONTEXT.md D-16) so downstream tooling can group items by closure type.

### Pre-existing Baseline Noise (Not Phase 58 Gaps)

${List any failures from Task 1 matrix that were classified as pre-existing baseline noise. Reference 63-VERIFICATION.md "Pre-existing baseline noise" precedent.}

- ${e.g. "57 `db.$queryRawUnsafe is not a function` test failures across `@contractor-ops/api` (KSeF/token-refresh) — documented in 63-VERIFICATION.md as predating Phase 58 source files; outside Phase 58 surface area."}
- ${e.g. "32 unrelated tsc errors in @contractor-ops/api workflow-execution / billing-webhook / etc. — same carve-out as 63-VERIFICATION.md."}

### Gaps Summary

${If status is verified:} **No code-level gaps.** All CLASS-01, CLASS-02, CLASS-05, CLASS-11 requirements close on `v2` HEAD `${HEAD_HASH}`. Phase 64 feature-flag interaction is correctly scoped to Phase 64's own verification.

${If status is gaps_found:} ${count} gap(s) require follow-up:

${one bullet per gaps[] entry from frontmatter, naming the requirement and the follow-up phase}

**Status rationale:** Per CONTEXT.md D-17, `verified` requires zero entries in `gaps[]`.

---

_Verified: ${ISO_TIMESTAMP}_
_Verifier: Claude (Phase 67-02)_
_Verification iteration: 1_
_Score: ${N}/4_
```

After writing the file, validate the YAML frontmatter parses cleanly (same node/python parser fallback as 67-01 Task 3 — see that plan for exact commands). If neither parser is available, do the minimal manual structural check (file starts with `---`, contains closing `---` within first 100 lines, contains required keys: `phase`, `verified`, `status`, `score`, `requirements_verified`, `gaps`, `manual_only`).

Then commit:

```
docs(67-02): write 58-VERIFICATION.md
```

Body MUST reference:
- The HEAD_HASH the report was verified against
- The number of CLASS-XX requirements verified (N/4)
- Whether any `fix(67-02)` commits preceded this docs commit (and their hashes)
- Reference to CONTEXT.md D-14/D-15/D-16/D-17

Do NOT use `--no-verify`.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && test -f .planning/phases/58-classification-engine-rule-sets/58-VERIFICATION.md && head -1 .planning/phases/58-classification-engine-rule-sets/58-VERIFICATION.md | grep -q '^---$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/58-classification-engine-rule-sets/58-VERIFICATION.md` exists
    - Frontmatter parses as valid YAML and contains required keys: `phase`, `verified`, `status`, `score`, `verified_at_commit`, `requirements_verified`, `gaps`, `manual_only`
    - `requirements_verified[]` has exactly 4 entries with `id` matching the regex `^CLASS-(01|02|05|11)$`
    - Each `requirements_verified` entry has `test_ids[]` referencing R-NN matrix rows AND `evidence_files[]` listing the source files cited
    - Frontmatter `status` is exactly `verified` OR `gaps_found`
    - If `status: verified`, `gaps[]` is empty (CONTEXT.md D-17)
    - If `gaps[]` is non-empty, `status` is `gaps_found` and every gap has a `follow_up_phase` field
    - Body contains a Programmatic Evidence table with 20 rows (R-01..R-20)
    - Body contains a Requirements Coverage table with 4 rows (CLASS-01, CLASS-02, CLASS-05, CLASS-11)
    - Body contains a "Phase 64 Feature-Flag Boundary" section explicitly scoping out the disabled-flag invariant
    - The verified_at_commit field matches the HEAD short hash captured at start of Task 1
    - No `gaps[]` entry references "feature flag" or "classification-engine flag" as a Phase 58 deficiency (those belong to Phase 64's verification scope, not Phase 58's)
    - HEAD commit subject matches the regex `^docs\(67-02\):`
    - Commit body references D-14, D-15, D-16, and D-17
  </acceptance_criteria>
  <done>58-VERIFICATION.md exists, parses, and accurately reflects CLASS-01/02/05/11 status on `v2` HEAD with audit-grade Programmatic Evidence + Requirements Coverage tables. Phase 64 feature-flag boundary is explicitly scoped out. Steuerberater-equivalent legal review (if any) correctly modelled in `manual_only[]` with `disposition: pre_deploy_legal_review`.</done>
</task>

<task type="auto">
  <name>Task 4: Flip Phase 58 manager flags (roadmap_complete + has_verification) per CONTEXT.md D-04</name>
  <files>
    (manager-flag store — typically `.planning/STATE.md` or per-phase metadata; depends on gsd-sdk implementation)
  </files>
  <read_first>
    .planning/phases/67-phase-56-58-verification/67-CONTEXT.md,
    .planning/STATE.md
  </read_first>
  <action>
Per CONTEXT.md D-04, manager-flag flips happen at the end of each plan, not as a Phase 67 epilogue. Phase 67-02 ends by flipping the Phase 58 flags to reflect the new state:

- `roadmap_complete: true` (Phase 58 ships + verifies under Phase 67-02)
- `has_verification: true` (58-VERIFICATION.md just landed in Task 3)

Use the canonical gsd-sdk command, mirroring how Phase 65 D-11 / Phase 66 D-20 flip per-plan flags. Inspect the SDK first:

```bash
gsd-sdk query manager.help 2>/dev/null || gsd-sdk help 2>/dev/null
```

Then issue the flag-flip equivalent for Phase 58. Likely shape (verify against actual SDK):

```bash
gsd-sdk query manager.set-flag --phase 58 --flag roadmap_complete --value true
gsd-sdk query manager.set-flag --phase 58 --flag has_verification --value true
```

If the SDK does not expose direct flag-set commands, the flags may live in `.planning/STATE.md` under a phase-status table — in that case, edit STATE.md to reflect:

- Phase 58 status: `Complete` (was `In Progress` or similar)
- Add a "Verified: 2026-04-26 (Phase 67-02)" line to Phase 58's STATE entry

If STATE.md schema is unclear, inspect a sibling already-complete phase's STATE.md row for the format precedent and mirror it exactly. Do NOT invent a new format.

After the flag flip lands (whether via SDK or STATE.md edit), commit it as a sibling docs commit IF a file change is required:

```
docs(67-02): flip Phase 58 manager flags after 58-VERIFICATION.md
```

If the SDK call is purely transient (writes to a non-tracked store), skip the commit step but log the SDK invocation in this task's done summary.

Do NOT use `--no-verify`.

If the flag-flip mechanism is unclear or fails, STOP and write a STATE.md blocker per the Phase 67 prompt's blocker-handling rule. Do NOT silently work around the failure.

**Note on parallelism:** Phase 67-01 also flips manager flags (for Phase 56) in its own Task 4. Both plans run in wave 1 in parallel; the flag-flip mechanism MUST be safe to run concurrently for two different phase numbers (56 and 58). If the SDK requires a lock or a STATE.md edit collides between the two plans, the second writer should retry (idempotent flag set) — not abort.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && grep -A2 "Phase 58" .planning/STATE.md 2>/dev/null | head -10 || echo "STATE.md absent or no Phase 58 row"</automated>
  </verify>
  <acceptance_criteria>
    - Phase 58 manager-side state reflects `roadmap_complete=true` AND `has_verification=true` (verified via SDK query OR by inspecting the persistent state file)
    - If the flag flip required a file change, a `docs(67-02): flip Phase 58 manager flags ...` commit exists; otherwise the SDK invocation is logged in the task's done message
    - No `--no-verify` used on any commit produced by this task
    - The flag-flip mechanism handled concurrency with Phase 67-01's parallel Phase 56 flag flip without dropping either update
  </acceptance_criteria>
  <done>Phase 58 closure is reflected in the manager dashboard / STATE.md per CONTEXT.md D-04. The Phase 67 plan exits with the parent phase's status correctly updated, not waiting on a Phase 67 epilogue.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `test -f .planning/phases/58-classification-engine-rule-sets/58-VERIFICATION.md && echo OK` → OK
2. `grep -c '^  - id: CLASS-\(01\|02\|05\|11\)$' .planning/phases/58-classification-engine-rule-sets/58-VERIFICATION.md` → ≥4 (every CLASS requirement has a requirements_verified entry)
3. `grep -A1 'gaps:' .planning/phases/58-classification-engine-rule-sets/58-VERIFICATION.md | head -2` — if frontmatter says `status: verified`, the next line under `gaps:` should be `[]` or empty (D-17 invariant)
4. `git log --oneline | grep -E '^[0-9a-f]+ docs\(67-02\):' | head -1` → matches (the docs commit landed)
5. For each `fix(67-02:NN)` commit: precedes the `docs(67-02)` commit in `git log` chronological order (D-18: docs reference post-fix state)
6. `git status --porcelain | wc -l` → 0 (clean working tree after Task 4)
7. Phase 65/66 surface area unchanged — `git log v2 --oneline -- packages/api/src/routers/skonto.ts packages/api/src/routers/late-payment-interest.ts packages/api/src/services/late-payment-interest.ts packages/api/src/routers/contractor.ts packages/api/src/routers/invoice.ts packages/api/src/routers/organization.ts packages/api/src/gov-api-clients.ts | head -1` matches the pre-Phase-67 HEAD on those files
</verification>
