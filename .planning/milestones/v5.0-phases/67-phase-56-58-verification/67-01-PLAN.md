---
phase: 67-phase-56-58-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06

must_haves:
  truths:
    - "56-VERIFICATION.md exists at .planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md with frontmatter mirroring 63-VERIFICATION.md (phase, verified, status, score, gaps, manual_only) per CONTEXT.md D-14"
    - "Every requirement FOUND-01..FOUND-06 has an explicit row in a `requirements_verified[]` table mapping requirement-ID to test IDs + commit hashes (CONTEXT.md D-15)"
    - "Re-validation evidence captures live v2 HEAD command + pass/fail + count for each Phase 56 SUMMARY's Verification Results (CONTEXT.md D-05/D-07) — not stale SUMMARY-snapshot citations"
    - "Steuerberater sign-off (56-STEUERBERATER-REVIEW.md) appears as a manual_only[] row with disposition `pre_deploy_legal_review` and link to the review file (CONTEXT.md D-09, D-16)"
    - "Phase 67 plan does NOT block 56-VERIFICATION.md status on legal sign-off — programmatic FOUND-01..06 evidence green => status: verified (CONTEXT.md D-09)"
    - "Adjacent fixes (if any) land as `fix(67-01): <summary>` commits BEFORE the `docs(67-01): write 56-VERIFICATION.md` commit so docs reference post-fix state (CONTEXT.md D-13, D-18)"
    - "If status is verified, gaps[] is empty (CONTEXT.md D-17 — gaps and verified are mutually exclusive)"
    - "Phase 56 manager flags (roadmap_complete, has_verification) flip after the docs commit lands (CONTEXT.md D-04)"
  artifacts:
    - path: ".planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md"
      provides: "Phase 56 formal verification report with FOUND-01..06 status"
      contains: "requirements_verified"
  key_links:
    - from: ".planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md"
      to: ".planning/phases/56-country-foundations-german-i18n/56-STEUERBERATER-REVIEW.md"
      via: "manual_only[] row with disposition pre_deploy_legal_review"
      pattern: "pre_deploy_legal_review"
    - from: ".planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "requirements_verified[] table maps FOUND-01..06"
      pattern: "FOUND-0"
---

<objective>
Produce `.planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md` confirming FOUND-01..FOUND-06 are programmatically satisfied on current `v2` HEAD.

Purpose: Phase 56 shipped (8 plans, 56-01..56-08) with no formal VERIFICATION.md. The v5.0 audit gap-closure trio (Phases 65, 66, 67) closes this. Phase 67-01 produces the audit-grade artifact: re-runs every test command from each `56-NN-SUMMARY.md` Verification Results section against live `v2` HEAD, captures real pass/fail counts (mirroring Phase 65 D-04 / Phase 66 D-04 — Phase 65 demonstrated 5/7 enumerated bugs were already closed by intervening commits, so we trust nothing without a fresh run), and emits the report in the canonical `63-VERIFICATION.md` shape with the Phase 67 D-15/D-16 enrichments (`requirements_verified[]` table + `disposition` field on `manual_only[]` rows).

Steuerberater sign-off (`56-STEUERBERATER-REVIEW.md`) is NOT a closure gate per STATE.md "Standing Project Constraints" + CONTEXT.md D-08/D-09 — it lands as a `manual_only[]` row with `disposition: pre_deploy_legal_review`, mirroring how Phase 66 will defer HMRC sandbox provisioning under `pre_deploy_ops`.

Output: One new file (`56-VERIFICATION.md`), zero or more atomic `fix(67-01): <summary>` commits if re-validation surfaces one-touch breakage (CONTEXT.md D-11), one `docs(67-01): write 56-VERIFICATION.md` commit, and Phase 56 manager flags flipped (`roadmap_complete=true`, `has_verification=true`).
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/67-phase-56-58-verification/67-CONTEXT.md
@.planning/phases/63-uk-payments-financial-features/63-VERIFICATION.md
@.planning/phases/56-country-foundations-german-i18n/56-CONTEXT.md
@.planning/phases/56-country-foundations-german-i18n/56-VALIDATION.md
@.planning/phases/56-country-foundations-german-i18n/56-01-SUMMARY.md
@.planning/phases/56-country-foundations-german-i18n/56-02-SUMMARY.md
@.planning/phases/56-country-foundations-german-i18n/56-03-SUMMARY.md
@.planning/phases/56-country-foundations-german-i18n/56-04-SUMMARY.md
@.planning/phases/56-country-foundations-german-i18n/56-05-SUMMARY.md
@.planning/phases/56-country-foundations-german-i18n/56-06-SUMMARY.md
@.planning/phases/56-country-foundations-german-i18n/56-07-SUMMARY.md
@.planning/phases/56-country-foundations-german-i18n/56-08-SUMMARY.md
@.planning/phases/56-country-foundations-german-i18n/56-STEUERBERATER-REVIEW.md
@.planning/phases/56-country-foundations-german-i18n/deferred-items.md
</context>

<threat_model>
- THREAT: Verification cites stale SUMMARY snapshot evidence instead of live `v2` HEAD runs → MITIGATION: CONTEXT.md D-05/D-07 mandate re-execution; every Programmatic Evidence row in 56-VERIFICATION.md MUST include the actual command, exit code, and pass count from a Task 1 re-run on `v2` HEAD (current HEAD: `850f03a3`). The acceptance criteria check that the report contains the literal commit hash from `git rev-parse --short HEAD` recorded at re-validation time.
- THREAT: Steuerberater legal sign-off accidentally treated as a blocking gate → MITIGATION: CONTEXT.md D-09 explicitly maps it to `manual_only[]` with `disposition: pre_deploy_legal_review`. Acceptance criterion forbids any `gaps[]` row referencing legal/Steuerberater/sign-off/STEUERBERATER. The status flip is governed by programmatic evidence only.
- THREAT: Adjacent-gap fix that needs investigation gets force-fitted as a one-touch fix → MITIGATION: CONTEXT.md D-11 vs D-12 strict distinction. Task 2 includes a triage decision: if the broken signal can be repaired with a single `<file, change>` (single mock-harness extension, single typo, single import fix) AND a confident root cause, it's `fix(67-01)`; otherwise it MUST land as a `gaps[]` row with `status: gaps_found` and a follow-up phase pointer (Phase 68 or 999.x backlog), and the verification artifact's frontmatter `status` flips to `gaps_found`.
- THREAT: requirements_verified[] table format drifts from sibling verification artifacts → MITIGATION: 63-VERIFICATION.md uses a markdown `Requirements Coverage` table; this plan reuses the exact same column shape (Requirement | Source Plans | Description | Status | Evidence). The new requirements_verified[] frontmatter list (CONTEXT.md D-15) maps requirement-ID → list of test IDs + commit hashes for tooling consumption; the body table preserves human readability.
- THREAT: Re-validation re-runs cost too much time and produce noise → MITIGATION: Each command is scoped (`pnpm --filter <package> test --run <pattern>`) rather than full-monorepo `pnpm turbo test`. The full plan executes in <5 wall-clock minutes per CONTEXT.md "Specific Ideas" precedent (Phase 65 D-04 reduced 7 enumerated bugs to 2 in one re-run pass).
- THREAT: Phase 56 has a deferred-items.md with pre-existing failures that get mis-attributed to FOUND-01..06 → MITIGATION: Task 1 reads `.planning/phases/56-country-foundations-german-i18n/deferred-items.md` and the resulting Programmatic Evidence section explicitly excludes any failure listed there (per Phase 65/66 D-04 carve-out pattern; 63-VERIFICATION.md "Pre-existing baseline noise" paragraph is the canonical phrasing).
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Re-validate Phase 56 acceptance commands on current v2 HEAD and capture Programmatic Evidence</name>
  <files>
    (read-only — captures evidence into Task 3's verification doc)
  </files>
  <read_first>
    .planning/phases/67-phase-56-58-verification/67-CONTEXT.md,
    .planning/phases/56-country-foundations-german-i18n/56-VALIDATION.md,
    .planning/phases/56-country-foundations-german-i18n/56-01-SUMMARY.md,
    .planning/phases/56-country-foundations-german-i18n/56-02-SUMMARY.md,
    .planning/phases/56-country-foundations-german-i18n/56-03-SUMMARY.md,
    .planning/phases/56-country-foundations-german-i18n/56-04-SUMMARY.md,
    .planning/phases/56-country-foundations-german-i18n/56-05-SUMMARY.md,
    .planning/phases/56-country-foundations-german-i18n/56-06-SUMMARY.md,
    .planning/phases/56-country-foundations-german-i18n/56-07-SUMMARY.md,
    .planning/phases/56-country-foundations-german-i18n/56-08-SUMMARY.md,
    .planning/phases/56-country-foundations-german-i18n/deferred-items.md
  </read_first>
  <action>
Capture the current HEAD short hash for the report:

```bash
HEAD_HASH=$(git -C /Users/mateusz.pitura/Repos/projects/contractor-ops rev-parse --short HEAD)
echo "Verification HEAD: $HEAD_HASH on branch v2"
```

Re-run each acceptance command extracted from the Phase 56 SUMMARY "Verification Results" sections and note the live exit code + pass count. Capture stdout/stderr to a scratch buffer; the captured tuple `(command, status, count, notes)` becomes the row in the Programmatic Evidence table written by Task 3.

Re-validation matrix (all commands run from repo root unless otherwise noted; record actual results, not the SUMMARY's historical claim):

| # | Source SUMMARY | Requirement(s) | Command |
|---|----------------|----------------|---------|
| R-01 | 56-02 | FOUND-01 | `pnpm --filter @contractor-ops/validators test --run uk-validators` |
| R-02 | 56-03 | FOUND-02 | `pnpm --filter @contractor-ops/validators test --run de-validators` |
| R-03 | 56-04 | FOUND-01, FOUND-02 | `pnpm --filter @contractor-ops/validators test --run country-fields` |
| R-04 | 56-04 | FOUND-02 | `pnpm --filter @contractor-ops/validators test --run steuernummer-formats` |
| R-05 | 56-03, 56-05, 56-07, 58-01 | FOUND-04 | `pnpm --filter @contractor-ops/validators test --run locked-phrases-guard` |
| R-06 | 56-05 | FOUND-03 | `pnpm --filter @contractor-ops/web test --run de-locale` |
| R-07 | 56-06 | FOUND-01, FOUND-02, FOUND-04 | `pnpm --filter @contractor-ops/web test --run uk-compliance-fields de-compliance-fields country-compliance-section` |
| R-08 | 56-07 | FOUND-05 | `pnpm --filter @contractor-ops/web test --run privacy-gb` |
| R-09 | 56-07 | FOUND-06 | `pnpm --filter @contractor-ops/web test --run privacy-de` |
| R-10 | 56-07 | FOUND-05, FOUND-06 | `pnpm --filter @contractor-ops/web test --run privacy-eu user-menu` |
| R-11 | 56-08 | FOUND-01..06 | `pnpm --filter @contractor-ops/web test --run onboarding-consent-step` |
| R-12 | 56-08 | FOUND-01..06 | `pnpm --filter @contractor-ops/validators test --run consent` |
| R-13 | 56-03 | FOUND-04 | `grep -c "Verantwortlicher im Sinne der DSGVO" packages/validators/src/legal/de.ts` (expect: ≥1) |
| R-14 | 56-05 | FOUND-04 | `grep -cE '\\b(Du\|Dir\|Dein[a-z]*)\\b' apps/web/messages/de.json` (expect: 0 — Sie register guard) |
| R-15 | 56-07 | FOUND-05, FOUND-06 | `test -f apps/web/src/app/[locale]/\(legal\)/privacy/\(content\)/gb.mdx && test -f apps/web/src/app/[locale]/\(legal\)/privacy/\(content\)/de.mdx && test -f apps/web/src/app/[locale]/\(legal\)/privacy/\(content\)/eu.mdx && echo OK` |
| R-16 | 56-07 | FOUND-06 | `grep -c "LOCKED_DE_PHRASES" apps/web/src/app/[locale]/\(legal\)/privacy/\(content\)/de.mdx` (expect: ≥1) |
| R-17 | 56-05 | FOUND-03 | `grep -c "'de'" apps/web/src/i18n/routing.ts` (expect: ≥1 — locale registered) |
| R-18 | 56-07 | FOUND-05, FOUND-06 | `grep -E "resolveJurisdiction\|SupportedJurisdiction" packages/validators/src/privacy-notices/jurisdiction.ts \| wc -l` (expect: ≥1) |

Execute each command in order. For each row capture:

```
{
  id: "R-NN",
  command: "<exact command>",
  exit_code: <int>,
  passed: <int>,    # extract from vitest "Test Files  X passed" line; 1/0 for grep; OK/FAIL for test -f
  failed: <int>,
  status: "PASS" | "FAIL",
  notes: "<one-line context if relevant — e.g. pre-existing baseline failure deferred per deferred-items.md>"
}
```

For any command that fails, classify per CONTEXT.md D-11 vs D-12:

- **One-touch fix candidate** (D-11): single file change, single mock-harness extension, single typo, single import-path correction, root cause is obvious and contained. Schedule for Task 2.
- **Investigation needed** (D-12): root cause unclear, ripples across multiple files, requires schema migration, or already documented in `.planning/phases/56-country-foundations-german-i18n/deferred-items.md`. Schedule for Task 3 as a `gaps[]` entry with a follow-up phase pointer.
- **Pre-existing baseline noise**: failure was already failing before any Phase 56 commit (cross-check against `deferred-items.md` listings and 63-VERIFICATION.md "Pre-existing baseline noise" paragraph). Capture in the report's "Pre-existing baseline noise" paragraph; do NOT count against FOUND-XX. The only items that legitimately fall into this bucket are: (a) entries explicitly listed in `deferred-items.md`, and (b) test-infrastructure failures unrelated to Phase 56 source files (e.g. KSeF/token-refresh `db.$queryRawUnsafe` Phase 63 noise documented in 63-VERIFICATION.md).

Save the captured matrix to a session scratch buffer (do NOT commit anything in this task — Task 3 writes the doc). The matrix is the input to Task 3's `### Programmatic Evidence` and `### Requirements Coverage` sections.

DO NOT modify any source files in this task. DO NOT touch any file in the Phase 65 or Phase 66 surface area (`packages/api/src/routers/{skonto,late-payment-interest,contractor,invoice,organization}.ts`, `packages/api/src/services/late-payment-interest.ts`, `packages/api/src/gov-api-clients.ts`) — the concurrent Phase 65 executor and Phase 66 planner own those.

If the working tree is dirty before re-validation begins, stop and write a STATE.md blocker; verification needs a clean tree to attribute results unambiguously to `v2` HEAD `850f03a3` (or whatever the recorded `HEAD_HASH` is at start of run).
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && git status --porcelain | grep -v "^?? .planning/phases/67-phase-56-58-verification/" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Re-validation matrix has 18 rows (R-01..R-18) all populated with exit_code, passed count, and PASS/FAIL status
    - HEAD_HASH captured at start of Task 1 is recorded (used by Task 3 in the report's "Verified at" line)
    - Each FAIL row is classified into one of three buckets (one-touch fix / investigation / pre-existing baseline) with explicit reasoning
    - Working tree shows zero modifications outside `.planning/phases/67-phase-56-58-verification/` (Task 1 captures evidence only; no source touched)
    - Captured matrix is available as input to Task 2 (one-touch fixes) and Task 3 (verification doc)
  </acceptance_criteria>
  <done>The actual state of FOUND-01..06 on `v2` HEAD is known. Stale SUMMARY claims are replaced with live evidence. Failures are triaged into the three CONTEXT.md D-11/D-12 buckets ready for Tasks 2 and 3.</done>
</task>

<task type="auto">
  <name>Task 2: Apply one-touch adjacent fixes (D-11) — atomic fix(67-01) commits BEFORE the docs commit</name>
  <files>
    (depends on Task 1 — files determined per failure)
  </files>
  <read_first>
    .planning/phases/67-phase-56-58-verification/67-CONTEXT.md,
    .planning/phases/56-country-foundations-german-i18n/56-CONTEXT.md,
    .planning/phases/56-country-foundations-german-i18n/deferred-items.md
  </read_first>
  <action>
**Skip this task entirely if Task 1's matrix has zero rows in the "one-touch fix candidate" bucket.** A 0-fix Task 2 is the expected case if Phase 56 is already green on `v2` HEAD (mirrors Phase 65 D-04 outcome where 5/7 enumerated bugs were already closed).

For each one-touch fix candidate from Task 1 (CONTEXT.md D-11):

1. Make the single-file or single-touch change that repairs the failing acceptance signal. Allowed kinds (verbatim from CONTEXT.md D-11):
   - Single mock-harness extension (e.g. add a missing Prisma client method to a vitest mock)
   - Single typo correction
   - Single import-path correction
   - Single test-fixture data update (e.g. correcting a stale court code as in 56-03 SUMMARY Deviation 2)

2. Re-run the SPECIFIC failing command from the Task 1 matrix to confirm the fix closes the signal. If the fix does NOT make the targeted command green, REVERT and reclassify as D-12 (investigation needed → escalate to gaps[] in Task 3).

3. Commit atomically with subject:

```
fix(67-01): <one-line description referencing the specific failure>
```

Body MUST include:
- The Task 1 matrix row ID (R-NN) the fix unblocks
- Which FOUND-XX requirement it unblocks
- The before/after grep or test output that proves the fix is one-touch
- Reference to CONTEXT.md D-11 (so future readers see the policy under which the fix was scoped)

Do NOT batch unrelated fixes (CONTEXT.md D-13). One commit per repaired failure. Do NOT use `--no-verify` (CONTEXT.md D-19). If a pre-commit hook fails, fix the underlying issue and create a NEW commit (project workflow rule, never amend).

After each `fix(67-01)` commit, re-run the targeted command and the immediately related command to confirm no new regression (e.g. if the fix touches `packages/validators/src/__tests__/locked-phrases-guard.test.ts`, also re-run `pnpm --filter @contractor-ops/validators test --run locked-phrases-guard de-validators` to catch a sibling break).

After ALL one-touch fixes land, update the Task 1 matrix in the session scratch buffer to reflect the new PASS status (and note the fix commit hash). Task 3's report will distinguish "originally PASS" from "PASS after fix(67-01:NN)".

Per-package execution-gate inheritance (CONTEXT.md D-19, mirrors Phase 65 D-11 / Phase 66 D-20): for each touched package, ensure `pnpm --filter <changed-package> typecheck` is clean and the package's full test suite passes (no regression in unrelated tests). If typecheck/test fails for an unrelated reason inside the same package, that's the original CONTEXT.md D-12 trap door — REVERT the fix commit and reclassify as a gap.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && git log v2 --oneline | grep -cE '^[0-9a-f]+ fix\(67-01\):' || echo 0</automated>
  </verify>
  <acceptance_criteria>
    - Zero or more `fix(67-01): <summary>` commits exist on the current branch
    - Every `fix(67-01)` commit body references a R-NN matrix row, a FOUND-XX requirement, and CONTEXT.md D-11
    - For each touched package: `pnpm --filter <package> typecheck` exits 0 AND the full package test suite passes (no regressions in unrelated tests)
    - No `--no-verify` was used (verifiable via `git log --pretty=format:'%H %B' | grep -i 'no-verify\|nogit'` returning empty for the new commits)
    - Updated Task 1 matrix shows post-fix PASS status with the unblocking commit hash for each formerly-failing R-NN row
    - Phase 65/66 surface area (`packages/api/src/routers/{skonto,late-payment-interest,contractor,invoice,organization}.ts`, `packages/api/src/services/late-payment-interest.ts`, `packages/api/src/gov-api-clients.ts`) is untouched
  </acceptance_criteria>
  <done>One-touch failures (if any) are repaired with atomic, hook-clean commits scoped strictly to Phase 67-01. Multi-touch failures correctly punt to Task 3 gaps[] for follow-up phase scheduling.</done>
</task>

<task type="auto">
  <name>Task 3: Write 56-VERIFICATION.md with frontmatter + body in 63-VERIFICATION.md shape + Phase 67 D-15/D-16 enrichments</name>
  <files>
    .planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md
  </files>
  <read_first>
    .planning/phases/63-uk-payments-financial-features/63-VERIFICATION.md,
    .planning/phases/67-phase-56-58-verification/67-CONTEXT.md,
    .planning/phases/56-country-foundations-german-i18n/56-CONTEXT.md,
    .planning/phases/56-country-foundations-german-i18n/56-STEUERBERATER-REVIEW.md,
    .planning/phases/56-country-foundations-german-i18n/deferred-items.md,
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
Create `.planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md` with the following exact structure. Frontmatter MUST mirror 63-VERIFICATION.md's shape (CONTEXT.md D-14) plus the new Phase 67 fields (CONTEXT.md D-15 `requirements_verified[]`, D-16 `manual_only[]` with `disposition`).

The score numerator is the count of FOUND-XX requirements with status `verified`; denominator is 6 (FOUND-01..06).

The frontmatter `status` field MUST be exactly one of `verified` or `gaps_found` (CONTEXT.md D-17 rule: gaps and verified are mutually exclusive — if `gaps[]` is non-empty, `status` MUST be `gaps_found`; if `gaps[]` is empty, `status` MUST be `verified`). Steuerberater sign-off does NOT count as a gap (CONTEXT.md D-09).

Write the file with the following template, filling in `${...}` placeholders from Task 1's matrix and Task 2's commit list:

```markdown
---
phase: 56-country-foundations-german-i18n
verified: ${ISO_TIMESTAMP_AT_TASK1_START}   # e.g. 2026-04-26T08:42:00Z
status: ${verified | gaps_found}
score: ${N}/6 must-haves verified           # N = count of FOUND-XX with status PASS in Task 1's matrix
verified_at_commit: ${HEAD_HASH}            # captured at start of Task 1 (e.g. 850f03a3)
verified_branch: v2

requirements_verified:
  - id: FOUND-01
    description: "User can add UK-specific contractor fields (UTR, Companies House number, VAT registration number) to contractor profiles for UK-based organizations"
    test_ids: [R-01, R-03, R-07]
    status: ${PASS | FAIL}
    evidence_commits: [${comma-separated commit hashes from `git log -- packages/validators/src/uk-validators.ts packages/validators/src/country-fields.ts apps/web/src/components/contractors/compliance/uk-compliance-fields.tsx`}]
  - id: FOUND-02
    description: "User can add German-specific contractor fields (Steuernummer, Handelsregister number, USt-IdNr, Sozialversicherungsnummer) to contractor profiles for German-based organizations"
    test_ids: [R-02, R-03, R-04, R-07]
    status: ${PASS | FAIL}
    evidence_commits: [${from `git log -- packages/validators/src/de-validators.ts packages/validators/src/steuernummer-formats.ts packages/validators/src/handelsregister-courts.ts apps/web/src/components/contractors/compliance/de-compliance-fields.tsx`}]
  - id: FOUND-03
    description: "User can switch the platform UI to German (full i18n as third language alongside Polish and English)"
    test_ids: [R-06, R-17]
    status: ${PASS | FAIL}
    evidence_commits: [${from `git log -- apps/web/src/i18n/routing.ts apps/web/src/i18n/request.ts apps/web/messages/de.json`}]
  - id: FOUND-04
    description: "User sees German-localized legal terminology with correct formal register (Sie, mandatory tax phrases like 'Steuerschuldnerschaft des Leistungsempfängers')"
    test_ids: [R-05, R-13, R-14, R-07]
    status: ${PASS | FAIL}
    evidence_commits: [${from `git log -- packages/validators/src/legal/de.ts packages/validators/src/__tests__/locked-phrases-guard.test.ts`}]
  - id: FOUND-05
    description: "User can view UK GDPR-compliant privacy notices and data processing information"
    test_ids: [R-08, R-10, R-15, R-18]
    status: ${PASS | FAIL}
    evidence_commits: [${from `git log -- packages/validators/src/privacy-notices/gb.ts apps/web/src/app/'[locale]'/(legal)/privacy/(content)/gb.mdx`}]
  - id: FOUND-06
    description: "User can view German GDPR-compliant privacy notices (Datenschutzerklärung) with BfDI-aligned language"
    test_ids: [R-09, R-10, R-15, R-16, R-18]
    status: ${PASS | FAIL}
    evidence_commits: [${from `git log -- packages/validators/src/privacy-notices/de.ts apps/web/src/app/'[locale]'/(legal)/privacy/(content)/de.mdx`}]

gaps: ${YAML list — empty if status is verified; else one row per D-12 investigation-required failure with id, description, follow_up_phase}
# Example gap row when status is gaps_found:
# gaps:
#   - id: GAP-67-01-01
#     requirement: FOUND-04
#     description: "<one-line failure summary>"
#     test_id: R-NN
#     follow_up_phase: "Phase 68 OR backlog 999.X"

manual_only:
  - test: "Steuerberater (German tax adviser) sign-off on the locked DSGVO/tax phrases, Datenschutzerklärung copy, messages/de.json formal-Sie register, SV-Nummer algorithm, 16-Bundesland Steuernummer regex map, Handelsregister court list, and BfDI-aligned privacy notice content"
    artifact: ".planning/phases/56-country-foundations-german-i18n/56-STEUERBERATER-REVIEW.md"
    why_human: "German tax-adviser professional review of locked legal terminology and DSGVO content cannot be automated. Per Phase 56 Plan 08 deliverable (commit 4228e5c), the deliverable file exists with the review checklist; the Steuerberater sign-off itself is the manual ops step."
    disposition: pre_deploy_legal_review

  ${if Task 1 matrix surfaced any other "manual UI flow / browser-only / running-server-required" check the SUMMARY files routed to manual verification, add additional entries here following the same shape with a justified disposition value. Otherwise omit and keep only the Steuerberater row.}

re_verification:
  previous_status: never_verified  # Phase 56 had no prior VERIFICATION.md
  ${if any fix(67-01) commits landed during Task 2:}
  fix_commits:
    - hash: "<short hash>"
      scope: "fix(67-01): <subject>"
      files: [<list>]
      changes: [<one-line per change>]
      unblocks_test_id: R-NN
      unblocks_requirement: FOUND-XX
---

# Phase 56: Country Foundations & German i18n Verification Report

**Phase Goal:** Users in UK and German organisations can complete contractor profile setup with country-specific tax/registration fields, switch the platform to German with formally-correct legal terminology, and view jurisdiction-appropriate GDPR/DSGVO privacy notices (per ROADMAP.md Phase 56 Success Criteria 1-4).

**Verified:** ${ISO_TIMESTAMP}
**Verified at commit:** `${HEAD_HASH}` on branch `v2`
**Status:** ${verified | gaps_found}
**Re-verification:** First pass (Phase 56 had no prior VERIFICATION.md — gap closure under Phase 67 per the v5.0 audit-gap closure trio with Phases 65 and 66).

## Goal Achievement

### Programmatic Evidence

| #     | Source SUMMARY | Requirement(s)   | Command                                                                                              | Result                                  | Status |
| ----- | -------------- | ---------------- | ---------------------------------------------------------------------------------------------------- | --------------------------------------- | ------ |
| R-01  | 56-02          | FOUND-01         | `pnpm --filter @contractor-ops/validators test --run uk-validators`                                 | ${X passed / Y failed}                  | ${PASS\|FAIL} |
| R-02  | 56-03          | FOUND-02         | `pnpm --filter @contractor-ops/validators test --run de-validators`                                 | ${X passed / Y failed}                  | ${PASS\|FAIL} |
| R-03  | 56-04          | FOUND-01,-02     | `pnpm --filter @contractor-ops/validators test --run country-fields`                                | ${X passed / Y failed}                  | ${PASS\|FAIL} |
| R-04  | 56-04          | FOUND-02         | `pnpm --filter @contractor-ops/validators test --run steuernummer-formats`                          | ${X passed / Y failed}                  | ${PASS\|FAIL} |
| R-05  | 56-03,05,07    | FOUND-04         | `pnpm --filter @contractor-ops/validators test --run locked-phrases-guard`                          | ${X passed / Y failed}                  | ${PASS\|FAIL} |
| R-06  | 56-05          | FOUND-03         | `pnpm --filter @contractor-ops/web test --run de-locale`                                            | ${X passed / Y failed}                  | ${PASS\|FAIL} |
| R-07  | 56-06          | FOUND-01,-02,-04 | `pnpm --filter @contractor-ops/web test --run uk-compliance-fields de-compliance-fields country-compliance-section` | ${X passed / Y failed} | ${PASS\|FAIL} |
| R-08  | 56-07          | FOUND-05         | `pnpm --filter @contractor-ops/web test --run privacy-gb`                                           | ${X passed / Y failed}                  | ${PASS\|FAIL} |
| R-09  | 56-07          | FOUND-06         | `pnpm --filter @contractor-ops/web test --run privacy-de`                                           | ${X passed / Y failed}                  | ${PASS\|FAIL} |
| R-10  | 56-07          | FOUND-05,-06     | `pnpm --filter @contractor-ops/web test --run privacy-eu user-menu`                                 | ${X passed / Y failed}                  | ${PASS\|FAIL} |
| R-11  | 56-08          | FOUND-01..06     | `pnpm --filter @contractor-ops/web test --run onboarding-consent-step`                              | ${X passed / Y failed}                  | ${PASS\|FAIL} |
| R-12  | 56-08          | FOUND-01..06     | `pnpm --filter @contractor-ops/validators test --run consent`                                       | ${X passed / Y failed}                  | ${PASS\|FAIL} |
| R-13  | 56-03          | FOUND-04         | `grep -c "Verantwortlicher im Sinne der DSGVO" packages/validators/src/legal/de.ts`                 | ${count}                                | ${PASS\|FAIL} |
| R-14  | 56-05          | FOUND-04         | `grep -cE '\\b(Du\|Dir\|Dein[a-z]*)\\b' apps/web/messages/de.json`                                  | ${count, expect 0}                      | ${PASS\|FAIL} |
| R-15  | 56-07          | FOUND-05,-06     | `test -f .../gb.mdx && test -f .../de.mdx && test -f .../eu.mdx`                                    | ${OK\|FAIL}                             | ${PASS\|FAIL} |
| R-16  | 56-07          | FOUND-06         | `grep -c "LOCKED_DE_PHRASES" .../de.mdx`                                                            | ${count}                                | ${PASS\|FAIL} |
| R-17  | 56-05          | FOUND-03         | `grep -c "'de'" apps/web/src/i18n/routing.ts`                                                       | ${count}                                | ${PASS\|FAIL} |
| R-18  | 56-07          | FOUND-05,-06     | `grep -E "resolveJurisdiction\|SupportedJurisdiction" .../jurisdiction.ts \| wc -l`                 | ${count}                                | ${PASS\|FAIL} |

**Score:** ${N_pass}/${N_total} re-validation rows green. ${N_fixed_in_67_01} originally-failing rows closed by `fix(67-01:NN)` commits this iteration. ${N_gaps} rows remain as `gaps[]` entries with follow-up phase pointers (status: gaps_found). ${N_pre_existing_baseline} rows excluded as pre-existing baseline noise per `deferred-items.md` carve-out.

### Requirements Coverage

| Requirement | Source Plans                  | Description                                                                                                                                                  | Status              | Evidence                                                                                                                                                  |
| ----------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FOUND-01    | 56-01, 56-02, 56-04, 56-06    | UK contractor fields (UTR, Companies House, VAT) with HMRC-grade checksum validation and entity-type-driven required markers                                  | ${✓ VERIFIED \| ✗ GAP}  | R-01 (UTR/VAT/CH validators 57/57), R-03 (UK Zod schema), R-07 (UI rendering) — all green on `${HEAD_HASH}`                                              |
| FOUND-02    | 56-01, 56-03, 56-04, 56-06    | German contractor fields (Steuernummer per-Bundesland, USt-IdNr ISO 7064, Handelsregister composite, SV-Nummer DRV-spec)                                     | ${✓ VERIFIED \| ✗ GAP}  | R-02 (DE validators 43/43), R-03 (DE Zod schema), R-04 (50 Steuernummer self-consistency assertions), R-07 (UI rendering) — all green                    |
| FOUND-03    | 56-01, 56-05                  | German locale registered as fourth platform language (`['en','pl','ar','de']`) with formal-Sie register enforced                                              | ${✓ VERIFIED \| ✗ GAP}  | R-06 (de-locale 5/5), R-17 (routing.ts contains 'de'), full key parity en→de (3639 leaf keys per 56-05 SUMMARY)                                          |
| FOUND-04    | 56-01, 56-03, 56-05           | Locked DSGVO/tax phrases as compile-time constants in validators/legal/de.ts; CI guard forbids reserved keys leaking into messages/*.json or informal register | ${✓ VERIFIED \| ✗ GAP}  | R-05 (locked-phrases-guard 32/32 — Phase 58 expanded the count from 10), R-13 (Verantwortlicher phrase present), R-14 (zero Du/Dir/Dein tokens in de.json) |
| FOUND-05    | 56-01, 56-07, 56-08           | UK GDPR privacy notice MDX page + React-PDF download with IDOR-safe tRPC mutation                                                                            | ${✓ VERIFIED \| ✗ GAP}  | R-08 (privacy-gb), R-10 (privacy-eu+user-menu), R-15 (gb.mdx exists), R-18 (jurisdiction resolver wired) — all green                                      |
| FOUND-06    | 56-01, 56-03, 56-07, 56-08    | German Datenschutzerklärung MDX page rendering verbatim LOCKED_DE_PHRASES + BfDI-aligned content + IDOR-safe PDF                                              | ${✓ VERIFIED \| ✗ GAP}  | R-09 (privacy-de), R-15 (de.mdx exists), R-16 (LOCKED_DE_PHRASES referenced in de.mdx), R-18 — all green                                                  |

All 6 requirement IDs declared for Phase 56 in REQUIREMENTS.md are accounted for. ${If status is verified:} Status flips from `Pending` to `Complete` for all 6 in REQUIREMENTS.md after this VERIFICATION.md commits and Phase 56 manager flags flip per CONTEXT.md D-04. ${If status is gaps_found:} ${N_passing} of 6 close; ${N_failing} remain pending until the follow-up phase referenced in `gaps[]` lands.

### Manual / Deferred Verification

See structured `manual_only` items in frontmatter. ${If only Steuerberater row:} One item total — Steuerberater sign-off — deferred per CONTEXT.md D-08/D-09 with disposition `pre_deploy_legal_review`. The 56-STEUERBERATER-REVIEW.md deliverable already ships in the phase directory (Plan 08 commit 4228e5c); only the external sign-off remains.

### Pre-existing Baseline Noise (Not Phase 56 Gaps)

${List any failures from Task 1 matrix that were classified as pre-existing baseline noise. Reference deferred-items.md and 63-VERIFICATION.md "Pre-existing baseline noise" precedent. Examples:}

- ${e.g. "3 pre-existing failures in `packages/validators/src/__tests__/invoice.test.ts` rejecting `currency: 'EUR'` — documented in `.planning/phases/56-country-foundations-german-i18n/deferred-items.md` since Plan 56-03; predates Phase 56 source files. Not counted against FOUND-XX."}
- ${e.g. "57 `db.$queryRawUnsafe is not a function` test failures across `@contractor-ops/api` (KSeF/token-refresh) per 63-VERIFICATION.md — outside Phase 56 surface area."}

### Gaps Summary

${If status is verified:} **No code-level gaps.** All FOUND-01..06 requirements close on `v2` HEAD `${HEAD_HASH}`. The only outstanding item is the Steuerberater sign-off (manual_only, disposition `pre_deploy_legal_review`).

${If status is gaps_found:} ${count} gap(s) require follow-up:

${one bullet per gaps[] entry from frontmatter, naming the requirement and the follow-up phase}

**Status rationale:** Per CONTEXT.md D-17, `verified` requires zero entries in `gaps[]`. Steuerberater deferral does NOT count as a gap (CONTEXT.md D-09 — local-only deploy posture per STATE.md "Standing Project Constraints"); it is `manual_only[]` with `disposition: pre_deploy_legal_review`.

---

_Verified: ${ISO_TIMESTAMP}_
_Verifier: Claude (Phase 67-01)_
_Verification iteration: 1_
_Score: ${N}/6_
```

After writing the file, validate the YAML frontmatter parses cleanly:

```bash
node -e "const fs=require('fs'); const yaml=require('js-yaml'); const m=fs.readFileSync('.planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md','utf8').match(/^---\\n([\\s\\S]*?)\\n---/); if(!m){console.error('no frontmatter');process.exit(1)} try{yaml.load(m[1]);console.log('YAML OK')}catch(e){console.error('YAML INVALID:',e.message);process.exit(1)}"
```

If `js-yaml` is not installed at the workspace root, fall back to:

```bash
python3 -c "import yaml,sys,re; t=open('.planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md').read(); m=re.match(r'^---\\n(.*?)\\n---', t, re.S); assert m; yaml.safe_load(m.group(1)); print('YAML OK')"
```

If neither parser is available, do a minimal manual structural check: file starts with `---`, contains a closing `---` within the first 100 lines, contains the required keys (`phase`, `verified`, `status`, `score`, `requirements_verified`, `gaps`, `manual_only`).

Then commit:

```
docs(67-01): write 56-VERIFICATION.md
```

Body MUST reference:
- The HEAD_HASH the report was verified against
- The number of FOUND-XX requirements verified (N/6)
- Whether any `fix(67-01)` commits preceded this docs commit (and their hashes)
- Reference to CONTEXT.md D-14/D-15/D-16/D-17 (the artifact-shape decisions)

Do NOT use `--no-verify`. If the docs-commit pre-commit hook fails, fix the root issue and create a NEW commit (project workflow rule).
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && test -f .planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md && head -1 .planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md | grep -q '^---$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md` exists
    - Frontmatter parses as valid YAML and contains the required keys: `phase`, `verified`, `status`, `score`, `verified_at_commit`, `requirements_verified`, `gaps`, `manual_only`
    - `requirements_verified[]` has exactly 6 entries with `id` matching the regex `^FOUND-0[1-6]$` (one per FOUND requirement)
    - `manual_only[]` contains at least one entry with `disposition: pre_deploy_legal_review` and `artifact` pointing to `56-STEUERBERATER-REVIEW.md`
    - Frontmatter `status` is exactly `verified` OR `gaps_found` (no other values)
    - If `status: verified`, `gaps[]` is empty (per CONTEXT.md D-17)
    - If `gaps[]` is non-empty, `status` is `gaps_found` and every gap entry has a `follow_up_phase` field
    - Body contains a Programmatic Evidence table with 18 rows (R-01..R-18)
    - Body contains a Requirements Coverage table with 6 rows (FOUND-01..FOUND-06)
    - The verified_at_commit field matches the HEAD short hash captured at start of Task 1
    - No `gaps[]` entry references "Steuerberater", "legal sign-off", "STEUERBERATER", or "tax adviser" (those belong in `manual_only[]`)
    - HEAD commit subject matches the regex `^docs\(67-01\):`
    - Commit body references D-14, D-15, D-16, and D-17
  </acceptance_criteria>
  <done>56-VERIFICATION.md exists, parses, and accurately reflects FOUND-01..06 status on `v2` HEAD with audit-grade Programmatic Evidence + Requirements Coverage tables and the Steuerberater deferral correctly modelled as `manual_only[]` with `disposition: pre_deploy_legal_review`.</done>
</task>

<task type="auto">
  <name>Task 4: Flip Phase 56 manager flags (roadmap_complete + has_verification) per CONTEXT.md D-04</name>
  <files>
    (manager-flag store — typically `.planning/STATE.md` or per-phase metadata; depends on gsd-sdk implementation)
  </files>
  <read_first>
    .planning/phases/67-phase-56-58-verification/67-CONTEXT.md,
    .planning/STATE.md
  </read_first>
  <action>
Per CONTEXT.md D-04, manager-flag flips happen at the end of each plan, not as a Phase 67 epilogue. Phase 67-01 ends by flipping the Phase 56 flags to reflect the new state:

- `roadmap_complete: true` (Phase 56 ships + verifies under Phase 67-01)
- `has_verification: true` (56-VERIFICATION.md just landed in Task 3)

Use the canonical gsd-sdk command, mirroring how Phase 65 D-11 / Phase 66 D-20 flip per-plan flags. The exact command depends on the gsd-sdk surface — check the help output first:

```bash
gsd-sdk query manager.help 2>/dev/null || gsd-sdk help 2>/dev/null
```

Then issue the flag-flip equivalent for Phase 56. Likely shape (verify against actual SDK):

```bash
gsd-sdk query manager.set-flag --phase 56 --flag roadmap_complete --value true
gsd-sdk query manager.set-flag --phase 56 --flag has_verification --value true
```

If the gsd-sdk does not expose direct flag-set commands, the flags may live in `.planning/STATE.md` under a phase-status table — in that case, edit STATE.md to reflect:

- Phase 56 status: `Complete` (was `In Progress` or similar)
- Add a "Verified: 2026-04-26 (Phase 67-01)" line to Phase 56's STATE entry

If STATE.md schema is unclear, inspect a sibling already-complete phase's STATE.md row for the format precedent and mirror it exactly. Do NOT invent a new format.

After the flag flip lands (whether via SDK call or STATE.md edit), commit it as a sibling docs commit IF and only IF a file change is required:

```
docs(67-01): flip Phase 56 manager flags after 56-VERIFICATION.md
```

If the SDK call is purely transient (writes to a non-tracked store like `.gsd-state.json` outside the repo), skip the commit step but log the SDK invocation in this task's done summary.

Do NOT use `--no-verify`.

If the flag-flip mechanism is unclear or fails (SDK errors, STATE.md schema mismatch), STOP and write a STATE.md blocker per the Phase 67 prompt's blocker-handling rule. Do NOT silently work around the failure.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && grep -A2 "Phase 56" .planning/STATE.md 2>/dev/null | head -10 || echo "STATE.md absent or no Phase 56 row"</automated>
  </verify>
  <acceptance_criteria>
    - Phase 56 manager-side state reflects `roadmap_complete=true` AND `has_verification=true` (verified via SDK query OR by inspecting the persistent state file)
    - If the flag flip required a file change, a `docs(67-01): flip Phase 56 manager flags ...` commit exists; otherwise the SDK invocation is logged in the task's done message
    - No `--no-verify` used on any commit produced by this task
  </acceptance_criteria>
  <done>Phase 56 closure is reflected in the manager dashboard / STATE.md per CONTEXT.md D-04. The Phase 67 plan exits with the parent phase's status correctly updated, not waiting on a Phase 67 epilogue.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `test -f .planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md && echo OK` → OK
2. `grep -c '^- id: FOUND-0[1-6]$' .planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md` → ≥6 (every FOUND requirement has a requirements_verified entry)
3. `grep -c 'disposition: pre_deploy_legal_review' .planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md` → ≥1 (Steuerberater row present with the correct disposition)
4. `grep -A1 'gaps:' .planning/phases/56-country-foundations-german-i18n/56-VERIFICATION.md | head -2` — if frontmatter says `status: verified`, the next line under `gaps:` should be `[]` or empty (D-17 invariant)
5. `git log --oneline | grep -E '^[0-9a-f]+ docs\(67-01\):' | head -1` → matches (the docs commit landed)
6. For each `fix(67-01:NN)` commit: precedes the `docs(67-01)` commit in `git log` chronological order (D-18: docs reference post-fix state)
7. `git status --porcelain | wc -l` → 0 (clean working tree after Task 4)
8. Phase 65/66 surface area unchanged — `git log v2 --oneline -- packages/api/src/routers/skonto.ts packages/api/src/routers/late-payment-interest.ts packages/api/src/services/late-payment-interest.ts packages/api/src/routers/contractor.ts packages/api/src/routers/invoice.ts packages/api/src/routers/organization.ts packages/api/src/gov-api-clients.ts | head -1` matches the pre-Phase-67 HEAD on those files (i.e. no Phase 67-01 commit appears in the log for them)
</verification>
