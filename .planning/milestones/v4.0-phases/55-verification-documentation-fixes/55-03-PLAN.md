---
phase: 55
plan: 3
title: "Update Phase 48 and 49 VERIFICATION.md files"
wave: 1
depends_on: []
autonomous: true
requirements_addressed: [ZATCA-05, ZATCA-07]
files_modified:
  - .planning/phases/48-zatca-fatoorah-integration/48-VERIFICATION.md
  - .planning/phases/49-peppol-pint-ae-integration/49-VERIFICATION.md
---

<objective>
Update Phase 48 VERIFICATION.md to mark ZATCA-05 and ZATCA-07 as fully satisfied (reflecting gap closure plans 48-07/48-08). Update Phase 49 VERIFICATION.md to reflect the resolved React Rules of Hooks violation in compliance-widget.tsx — the fix has already been applied in code (both useQuery calls now appear before the isLoading early return).
</objective>

## Tasks

<task id="55-03-01">
<title>Verify Phase 48 VERIFICATION.md is already current</title>
<read_first>
- .planning/phases/48-zatca-fatoorah-integration/48-VERIFICATION.md
</read_first>
<action>
Read the Phase 48 VERIFICATION.md and check whether ZATCA-05 and ZATCA-07 are already marked as SATISFIED. Based on research, the file already shows:
- Frontmatter: `status: human_needed`, `score: 18/18`, `gaps_remaining: []`
- Requirements Coverage table: ZATCA-05 marked SATISFIED, ZATCA-07 marked SATISFIED
- Re-verification Summary: Both gaps documented as CLOSED

**If ZATCA-05 and ZATCA-07 are already SATISFIED and gaps_remaining is empty:** No changes needed. Document this in the task output — the verification file was already updated during Phase 48 gap closure.

**If any gap remains:** Update the specific rows to SATISFIED with evidence from plans 48-07/48-08, update the score, and set `gaps_remaining: []`.
</action>
<acceptance_criteria>
- `grep 'ZATCA-05' .planning/phases/48-zatca-fatoorah-integration/48-VERIFICATION.md` shows `SATISFIED`
- `grep 'ZATCA-07' .planning/phases/48-zatca-fatoorah-integration/48-VERIFICATION.md` shows `SATISFIED`
- `grep 'gaps_remaining' .planning/phases/48-zatca-fatoorah-integration/48-VERIFICATION.md` shows `[]`
</acceptance_criteria>
</task>

<task id="55-03-02">
<title>Update Phase 49 VERIFICATION.md to reflect resolved hooks violation</title>
<read_first>
- .planning/phases/49-peppol-pint-ae-integration/49-VERIFICATION.md
- apps/web/src/components/einvoice/compliance-widget.tsx
</read_first>
<action>
The Phase 49 VERIFICATION.md currently shows `status: gaps_found` with score 15/16, due to a React Rules of Hooks violation in compliance-widget.tsx (useQuery at line 94 called after isLoading early return at line 81).

**First, verify the fix:** Read `compliance-widget.tsx` and confirm that both `useQuery` calls are now at lines 73-74, BEFORE the `isLoading` early return at line 76. The research confirms this is the case — the hooks violation has been fixed.

**Then update 49-VERIFICATION.md:**

1. Update YAML frontmatter:
   - Change `status: passed` (was `gaps_found`) — the code-level gap is resolved; only human verification items remain
   - Change `score: 16/16 must-haves verified` (was `15/16`)
   - Add to `re_verification.gaps_closed`: "React Rules of Hooks violation in compliance-widget.tsx — both useQuery calls moved before isLoading early return"
   - Set `re_verification.regressions` to `[]` (the previous entry about hooks violation is now resolved)

2. Update the `gaps:` section in frontmatter:
   - Remove or mark as resolved the entry about compliance-widget.tsx hooks violation

3. Update Truth 17 in the Observable Truths table:
   - Change status from `✗ FAILED` to `✓ VERIFIED`
   - Change evidence to: "PeppolComplianceWidget imported at line 10, useQuery(trpc.peppol.getStatus) at line 74 — before isLoading guard. Both hooks declared unconditionally at component top."

4. Update the Required Artifacts table:
   - Change compliance-widget.tsx from `✗ PARTIAL` to `✓ VERIFIED`
   - Update details to reflect the fix

5. Update Anti-Patterns Found table:
   - Remove the hooks violation row or mark it as RESOLVED

6. Update Gaps Summary section:
   - State that the hooks violation has been resolved
   - Note the fix: both useQuery calls are now at lines 73-74, before the isLoading early return

7. Update the Score line in the body: "16/16 truths verified"
</action>
<acceptance_criteria>
- 49-VERIFICATION.md frontmatter has `status: passed` (not `gaps_found`)
- 49-VERIFICATION.md frontmatter has `score: 16/16`
- Truth 17 row shows `✓ VERIFIED` (not `✗ FAILED`)
- Anti-Patterns table no longer lists the hooks violation as active
- Gaps Summary mentions the hooks violation is resolved
- `grep 'status: passed' .planning/phases/49-peppol-pint-ae-integration/49-VERIFICATION.md` succeeds
- `grep '16/16' .planning/phases/49-peppol-pint-ae-integration/49-VERIFICATION.md` succeeds
</acceptance_criteria>
</task>

## Verification Criteria

- [ ] Phase 48 VERIFICATION.md has ZATCA-05 and ZATCA-07 as SATISFIED with no remaining gaps
- [ ] Phase 49 VERIFICATION.md status changed from `gaps_found` to `passed`
- [ ] Phase 49 VERIFICATION.md score changed from 15/16 to 16/16
- [ ] Phase 49 Truth 17 (compliance widget) marked as VERIFIED
- [ ] Phase 49 hooks violation documented as resolved

## must_haves
- [ ] Phase 48 VERIFICATION.md confirms ZATCA-05/07 satisfied (verify already current or update)
- [ ] Phase 49 VERIFICATION.md updated to reflect resolved hooks violation
- [ ] Phase 49 score updated to 16/16
