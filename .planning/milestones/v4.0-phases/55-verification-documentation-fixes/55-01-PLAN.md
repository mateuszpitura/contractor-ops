---
phase: 55
plan: 1
title: "Locale-aware formatters and call-site updates"
wave: 1
depends_on: []
autonomous: true
requirements_addressed: []
files_modified:
  - apps/web/src/lib/format-currency.ts
  - apps/web/src/lib/format-relative-date.ts
  - apps/web/src/lib/__tests__/format-currency.test.ts
  - apps/web/src/lib/__tests__/format-relative-date.test.ts
  - apps/web/src/components/payments/payment-run-side-panel.tsx
  - apps/web/src/components/payments/wht-summary-card.tsx
  - apps/web/src/components/payments/wht-certificate-preview-dialog.tsx
  - apps/web/src/components/approvals/approval-queue/side-panel.tsx
  - apps/web/src/components/approvals/approval-queue/columns.tsx
---

<objective>
Add an optional `locale` parameter (default `"en"`) to the shared `formatMinorUnits()`, `formatAmount()`, and `formatRelativeDate()` functions, removing all hardcoded `"pl-PL"` locale strings. Update every component that imports from `@/lib/format-currency` or `@/lib/format-relative-date` to pass the current locale via `useLocale()`. Update tests to cover `en`, `pl`, and `ar` locales.
</objective>

## Tasks

<task id="55-01-01">
<title>Add locale parameter to formatMinorUnits and formatAmount</title>
<read_first>
- apps/web/src/lib/format-currency.ts
</read_first>
<action>
Edit `apps/web/src/lib/format-currency.ts`:

1. Add `locale` as the last parameter to `formatMinorUnits`:
   ```ts
   export function formatMinorUnits(minor: number, currency?: string | null, locale: string = "en"): string {
   ```
2. Replace `"pl-PL"` on line 14 with `locale`.
3. Add `locale` as the last parameter to `formatAmount`:
   ```ts
   export function formatAmount(minor: number, currency: string, locale: string = "en"): string {
   ```
4. Replace `"pl-PL"` on line 27 with `locale`.
5. Update the JSDoc examples to show the locale parameter.
</action>
<acceptance_criteria>
- `format-currency.ts` contains `locale: string = "en"` in both function signatures
- `format-currency.ts` does NOT contain the string `"pl-PL"`
- `grep -c 'locale: string = "en"' apps/web/src/lib/format-currency.ts` returns `2`
- `grep -c 'pl-PL' apps/web/src/lib/format-currency.ts` returns `0`
</acceptance_criteria>
</task>

<task id="55-01-02">
<title>Add locale parameter to formatRelativeDate</title>
<read_first>
- apps/web/src/lib/format-relative-date.ts
</read_first>
<action>
Edit `apps/web/src/lib/format-relative-date.ts`:

1. Add `locale` as the second parameter:
   ```ts
   export function formatRelativeDate(dateStr: Date | string, locale: string = "en"): string {
   ```
2. Replace `"pl-PL"` on line 13 with `locale`.
3. Update the JSDoc to mention the locale parameter.
</action>
<acceptance_criteria>
- `format-relative-date.ts` contains `locale: string = "en"` in the function signature
- `format-relative-date.ts` does NOT contain the string `"pl-PL"`
- `grep -c 'locale: string = "en"' apps/web/src/lib/format-relative-date.ts` returns `1`
- `grep -c 'pl-PL' apps/web/src/lib/format-relative-date.ts` returns `0`
</acceptance_criteria>
</task>

<task id="55-01-03">
<title>Update format-currency tests for multi-locale</title>
<read_first>
- apps/web/src/lib/__tests__/format-currency.test.ts
- apps/web/src/lib/format-currency.ts
</read_first>
<action>
Edit `apps/web/src/lib/__tests__/format-currency.test.ts`:

1. Update existing tests to pass `"pl"` as locale where they expect comma-decimal output (e.g. `"100,50"`), since the default is now `"en"`:
   - `formatMinorUnits(10050, undefined, "pl")` → `"100,50"`
   - `formatMinorUnits(0, undefined, "pl")` → `"0,00"`
   - etc.
2. Add tests for `"en"` locale (the new default):
   - `formatMinorUnits(10050)` → `"100.50"` (en uses period as decimal separator)
   - `formatAmount(10050, "PLN")` → `"100.50 PLN"`
3. Add tests for `"ar"` locale:
   - `formatMinorUnits(10050, undefined, "ar")` → verify it contains Arabic-locale formatting (match against a regex with digit characters)
   - `formatAmount(10050, "PLN", "ar")` → verify it ends with ` PLN`
4. Keep the large-number thousands-separator test, updating for the `"pl"` locale:
   - `formatMinorUnits(1234567, undefined, "pl")` → matches `/12[\s\u00a0]345,67/`
</action>
<acceptance_criteria>
- Test file contains at least one test with locale `"en"` verifying period decimal separator
- Test file contains at least one test with locale `"pl"` verifying comma decimal separator
- Test file contains at least one test with locale `"ar"`
- `grep -c '"en"' apps/web/src/lib/__tests__/format-currency.test.ts` returns at least `1`
- `grep -c '"ar"' apps/web/src/lib/__tests__/format-currency.test.ts` returns at least `1`
- `npx vitest run apps/web/src/lib/__tests__/format-currency.test.ts` exits 0
</acceptance_criteria>
</task>

<task id="55-01-04">
<title>Update format-relative-date tests for multi-locale</title>
<read_first>
- apps/web/src/lib/__tests__/format-relative-date.test.ts
- apps/web/src/lib/format-relative-date.ts
</read_first>
<action>
Edit `apps/web/src/lib/__tests__/format-relative-date.test.ts`:

1. The `"today"`, `"yesterday"`, and `"Xd ago"` tests remain unchanged (those paths don't use locale).
2. Update the "30+ days ago" test to pass `"pl"` locale:
   - `formatRelativeDate("2024-05-01T10:00:00Z", "pl")` → matches a pl date format (DD.MM.YYYY)
3. Add a test for `"en"` locale (default) with 30+ days:
   - `formatRelativeDate("2024-05-01T10:00:00Z")` → matches an en date format (M/D/YYYY or similar)
4. Add a test for `"ar"` locale with 30+ days:
   - `formatRelativeDate("2024-05-01T10:00:00Z", "ar")` → matches a date string containing digits
5. Update the boundary test to pass `"pl"` locale since it was previously relying on pl-PL formatting.
</action>
<acceptance_criteria>
- Test file contains at least one test calling `formatRelativeDate` with `"pl"` locale
- Test file contains at least one test calling `formatRelativeDate` with `"ar"` locale
- Test file contains at least one test calling `formatRelativeDate` without locale (uses default `"en"`)
- `npx vitest run apps/web/src/lib/__tests__/format-relative-date.test.ts` exits 0
</acceptance_criteria>
</task>

<task id="55-01-05">
<title>Update shared-import call sites to pass locale</title>
<read_first>
- apps/web/src/components/payments/payment-run-side-panel.tsx
- apps/web/src/components/payments/wht-summary-card.tsx
- apps/web/src/components/payments/wht-certificate-preview-dialog.tsx
- apps/web/src/components/approvals/approval-queue/side-panel.tsx
- apps/web/src/components/approvals/approval-queue/columns.tsx
</read_first>
<action>
For each of the 5 files that import from `@/lib/format-currency` or `@/lib/format-relative-date`:

1. Add `import { useLocale } from "next-intl";` if not already present.
2. Inside the component function, add `const locale = useLocale();` if not already present.
3. Update every call to `formatMinorUnits(...)`, `formatAmount(...)`, or `formatRelativeDate(...)` to pass `locale` as the last argument.

Specific files and changes:

**`payment-run-side-panel.tsx`** (imports both formatMinorUnits and formatRelativeDate):
- Add `useLocale` import
- Add `const locale = useLocale();` at top of component
- Change `formatMinorUnits(run.totalMinor, run.currency)` → `formatMinorUnits(run.totalMinor, run.currency, locale)`
- Change `formatMinorUnits(item.amountMinor, item.currency)` → `formatMinorUnits(item.amountMinor, item.currency, locale)`
- Change `formatRelativeDate(run.createdAt)` → `formatRelativeDate(run.createdAt, locale)`

**`wht-summary-card.tsx`** (imports formatMinorUnits):
- Add `useLocale` import
- Add `const locale = useLocale();` at top of component
- Change all 3 `formatMinorUnits(totalGross/totalWht/totalNet, currency)` calls to add `locale` as third arg

**`wht-certificate-preview-dialog.tsx`** (imports formatMinorUnits):
- Add `useLocale` import
- Add `const locale = useLocale();` at top of component
- Change all 3 `formatMinorUnits(certificate.grossAmountMinor/whtAmountMinor/netAmountMinor, certificate.currency)` calls to add `locale` as third arg

**`approvals/approval-queue/side-panel.tsx`** (imports formatAmount):
- Add `useLocale` import (may already have useTranslations from next-intl)
- Add `const locale = useLocale();` at top of component
- Find every `formatAmount(...)` call and add `locale` as third arg

**`approvals/approval-queue/columns.tsx`** (imports formatMinorUnits):
- This file defines column definitions (not a React component with hooks). The columns are used in a table component. If `useLocale()` cannot be called here (no React component context), pass locale through the column meta or use a wrapper. Check the file structure first — if columns are defined inside a component, add `useLocale()` there. If columns are defined at module level, add a `locale` parameter to the column factory or use the `meta` pattern from TanStack Table.
</action>
<acceptance_criteria>
- `payment-run-side-panel.tsx` contains `useLocale` import and passes `locale` to all format calls
- `wht-summary-card.tsx` contains `useLocale` import and passes `locale` to all formatMinorUnits calls
- `wht-certificate-preview-dialog.tsx` contains `useLocale` import and passes `locale` to all formatMinorUnits calls
- `approvals/approval-queue/side-panel.tsx` contains `useLocale` import and passes `locale` to formatAmount calls
- `approvals/approval-queue/columns.tsx` passes `locale` to formatMinorUnits calls (via hook, meta, or parameter)
- `grep -rn 'formatMinorUnits\|formatAmount\|formatRelativeDate' apps/web/src/components/payments/payment-run-side-panel.tsx` shows locale in every call
- No file imports from `@/lib/format-currency` or `@/lib/format-relative-date` without passing locale
</acceptance_criteria>
</task>

## Verification Criteria

- [ ] `formatMinorUnits`, `formatAmount`, `formatRelativeDate` all accept an optional `locale` parameter defaulting to `"en"`
- [ ] No hardcoded `"pl-PL"` remains in `format-currency.ts` or `format-relative-date.ts`
- [ ] All 5 components that import from the shared formatters pass the current locale
- [ ] Tests pass with `en`, `pl`, and `ar` locales
- [ ] `npx vitest run apps/web/src/lib/__tests__/format-currency.test.ts apps/web/src/lib/__tests__/format-relative-date.test.ts` exits 0

## must_haves
- [ ] `formatMinorUnits()` accepts locale parameter, no hardcoded pl-PL
- [ ] `formatAmount()` accepts locale parameter, no hardcoded pl-PL
- [ ] `formatRelativeDate()` accepts locale parameter, no hardcoded pl-PL
- [ ] All shared-import call sites pass locale from `useLocale()`
- [ ] Tests cover en, pl, ar locales
