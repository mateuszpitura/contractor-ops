---
phase: 50-arabic-localization-rtl-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/messages/ar.json
autonomous: true
requirements: [L10N-01, L10N-05]
must_haves:
  truths:
    - "apps/web/messages/ar.json exists with same key structure as en.json"
    - "All UI strings translated to Arabic with financial domain accuracy"
    - "Financial terms use correct Gulf Arabic business terminology"
  artifacts:
    - apps/web/messages/ar.json
  key_links:
    - "ar.json loaded by request.ts when locale is 'ar' via dynamic import"
---

<objective>
Arabic Translation — Create Complete Arabic Message File

Purpose: Create the Arabic translation file (apps/web/messages/ar.json) covering all UI strings, using the English file as the source. This delivers the L10N-01 requirement (full Arabic translation) and supports L10N-05 (locale-appropriate formatting). Per D-03, use AI translation as the first pass — professional review is a separate manual step. Per D-04, Western/Latin numerals are handled at the formatting layer (Plan 01), not in the translation strings.

Output: Complete ar.json with all keys from en.json translated to Arabic.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-arabic-localization-rtl-layout/50-CONTEXT.md
@apps/web/messages/en.json
@apps/web/messages/pl.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Arabic translation file from English source</name>
  <files>apps/web/messages/ar.json</files>
  <read_first>
    - apps/web/messages/en.json (source — ~4261 lines, all keys to translate)
    - apps/web/messages/pl.json (reference — existing translation pattern and structure)
  </read_first>
  <action>
  **Per D-03 (AI translation + professional review) and D-04 (Western/Latin numerals):**

  Create `apps/web/messages/ar.json` by translating every key from `apps/web/messages/en.json` to Arabic.

  **Translation guidelines:**
  1. **Structure:** Mirror the exact JSON key structure of en.json — same namespaces, same nesting, same keys. Only values change.
  2. **Language variant:** Modern Standard Arabic (MSA) suitable for Gulf business context. Prefer Gulf Arabic business terminology where applicable.
  3. **Financial domain terms** (these MUST be accurate for Gulf business context):
     - Invoice = فاتورة
     - Payment = دفعة / مدفوعات
     - Contract = عقد
     - Contractor = مقاول / متعاقد
     - Approval = موافقة
     - Tax = ضريبة
     - VAT = ضريبة القيمة المضافة
     - Credit note = إشعار دائن
     - Bank transfer = تحويل بنكي
     - Due date = تاريخ الاستحقاق
     - Amount = المبلغ
     - Balance = الرصيد
     - Currency = العملة
     - Status = الحالة
     - Pending = قيد الانتظار
     - Approved = معتمد / تمت الموافقة
     - Rejected = مرفوض
     - Overdue = متأخر
     - Draft = مسودة
     - Settings = الإعدادات
     - Dashboard = لوحة المعلومات
     - Report = تقرير
     - Export = تصدير
     - Import = استيراد
     - Upload = رفع
     - Download = تحميل
     - Search = بحث
     - Filter = تصفية
     - Sort = ترتيب
     - Delete = حذف
     - Edit = تعديل
     - Save = حفظ
     - Cancel = إلغاء
     - Confirm = تأكيد
     - Submit = إرسال
     - Organization = المؤسسة
     - Team = الفريق
     - Member = عضو
     - Role = الدور
     - Admin = المسؤول
     - Notification = إشعار
     - Equipment = المعدات
     - Document = مستند / وثيقة
     - Workflow = سير العمل
     - Template = قالب
     - Billing = الفوترة
     - Subscription = الاشتراك
     - Plan = الخطة
     - Compliance = الامتثال
  4. **ICU MessageFormat:** Preserve all `{variable}` placeholders, `{count, plural, ...}` blocks, and `{value, number}` format strings exactly. Only translate the human-readable text parts.
     - Example: `"items": "{count, plural, one {# item} other {# items}}"` becomes `"items": "{count, plural, one {# عنصر} few {# عناصر} many {# عنصرًا} other {# عنصر}}"`
     - Arabic has 6 plural forms: zero, one, two, few, many, other. Add all relevant forms where the English only has one/other.
  5. **Do NOT translate:**
     - Brand names (e.g., "KSeF", "Peppol", "SWIFT")
     - Technical identifiers
     - Format patterns (date/number format strings)
     - API error codes
  6. **Text direction in strings:** Do NOT add Unicode direction marks (LRM/RLM) in the JSON — direction handling is done at the HTML/CSS level.

  Write the complete ar.json file. Given the size (~4261 lines), this is a large file but must be complete — every key in en.json must have a corresponding Arabic value.
  </action>
  <acceptance_criteria>
    - apps/web/messages/ar.json exists
    - ar.json is valid JSON (parseable by `node -e "JSON.parse(require('fs').readFileSync('apps/web/messages/ar.json', 'utf8'))"`)
    - ar.json has the same top-level key count as en.json
    - ar.json contains Arabic characters (Unicode range \u0600-\u06FF)
    - ar.json preserves all `{variable}` ICU placeholders from en.json
    - ar.json contains "فاتورة" (invoice) and "دفعة" or "مدفوعات" (payment)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const ar = JSON.parse(require('fs').readFileSync('apps/web/messages/ar.json','utf8')); const en = JSON.parse(require('fs').readFileSync('apps/web/messages/en.json','utf8')); const arKeys = Object.keys(ar).length; const enKeys = Object.keys(en).length; console.log('AR keys:', arKeys, 'EN keys:', enKeys, arKeys === enKeys ? 'PASS' : 'FAIL: key count mismatch'); if(!JSON.stringify(ar).match(/[\u0600-\u06FF]/)) console.log('FAIL: no Arabic chars'); else console.log('PASS: Arabic chars present');"</automated>
  </verify>
  <done>Complete Arabic translation file exists at apps/web/messages/ar.json with all keys from en.json translated, proper Gulf Arabic business terminology, correct ICU MessageFormat pluralization with Arabic plural forms, and valid JSON structure</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Translation strings | Static JSON file — no user input at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-04 | Tampering | ar.json | accept | Static file loaded at build time — cannot be modified at runtime. XSS prevention is handled by React's JSX escaping, not translation strings. |
| T-50-05 | Information Disclosure | ar.json | accept | Translation strings are public-facing UI text — no secrets |
</threat_model>

<verification>
- `node -e "JSON.parse(require('fs').readFileSync('apps/web/messages/ar.json'))"` succeeds (valid JSON)
- Key count matches between ar.json and en.json
- Arabic characters present in values
- ICU MessageFormat placeholders preserved
</verification>

<success_criteria>
- apps/web/messages/ar.json exists with complete translations
- All namespaces and keys from en.json have Arabic counterparts
- Gulf Arabic business terminology used for financial domain
- ICU MessageFormat pluralization includes Arabic-specific plural forms (zero, one, two, few, many, other)
- File is valid JSON loadable by next-intl
</success_criteria>

<output>
After completion, create `.planning/phases/50-arabic-localization-rtl-layout/50-02-SUMMARY.md`
</output>
