---
phase: 74
plan: 03
type: execute
wave: 1
depends_on: [74-01]
files_modified:
  - packages/auth/src/permissions.ts
  - packages/auth/src/roles.ts
  - packages/auth/src/__tests__/permissions-override-blocking-task.test.ts
autonomous: true
requirements: [OFFB-07, OFFB-10]
requirements_addressed: [OFFB-07, OFFB-10]

must_haves:
  truths:
    - "Better Auth `accessControlStatement.workflow` array includes the new action `'override_blocking_task'`"
    - "Only the `owner` role grants `workflow:override_blocking_task`; nine other roles do NOT"
    - "CI table-test iterates all 10 actual lowercase role names from packages/auth/src/roles.ts and asserts the OWNER-only invariant"
  artifacts:
    - path: packages/auth/src/permissions.ts
      provides: extended accessControlStatement with override_blocking_task action
      contains: "'override_blocking_task'"
    - path: packages/auth/src/roles.ts
      provides: owner-only assignment of override_blocking_task
      contains: "'override_blocking_task'"
    - path: packages/auth/src/__tests__/permissions-override-blocking-task.test.ts
      provides: GREEN table-driven RBAC test across 10 roles
      contains: "describe('workflow:override_blocking_task — D-09"
  key_links:
    - from: packages/auth/src/permissions.ts
      to: packages/auth/src/roles.ts
      via: action key in accessControlStatement consumed by ac.newRole
      pattern: 'workflow:.*override_blocking_task'
    - from: packages/auth/src/__tests__/permissions-override-blocking-task.test.ts
      to: packages/auth/src/roles.ts
      via: imports `roles` map and asserts permission per role
      pattern: 'import.*roles.*from.*roles'
---

<objective>
Wave 1 — Register the new `workflow:override_blocking_task` permission in Better Auth's `accessControlStatement`, grant it to the `owner` role only, and ship a CI table-test that iterates all 10 actual role names asserting OWNER-only access. Closes OFFB-10's "permission registered OWNER-only and CI-tested" half (the `WorkflowTaskType.IP_VERIFICATION` enum half lands in Plan 74-04).

Purpose: The override mutation in Plan 74-08 gates on `requirePermission({ workflow: ['override_blocking_task'] })`. That check must resolve to `owner`-true / 9-others-false at runtime. Pitfall 2 (permission-registration regression via inheritance/spread) is mitigated by an explicit table-test that fails CI if any non-owner role is granted the action.

Output: Two-file edit (permissions.ts + roles.ts) + one GREEN test file.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-CONTEXT.md
@.planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-RESEARCH.md
@packages/auth/src/permissions.ts
@packages/auth/src/roles.ts
@packages/api/src/middleware/rbac.ts
@packages/api/src/middleware/__tests__/rbac.test.ts

<interfaces>
<!-- This plan extends Better Auth's accessControlStatement. Downstream consumers: 74-08 (mutation) + 74-05 (getCurrentUserPermissions query). -->

Existing `accessControlStatement.workflow` (verified in research):
```typescript
workflow: ['create', 'read', 'update', 'delete', 'execute']
```

After this plan:
```typescript
workflow: ['create', 'read', 'update', 'delete', 'execute', 'override_blocking_task']
```

Role assignment shape (must match exact form used in roles.ts):
```typescript
owner: ac.newRole({ ...allPermissions, workflow: ['create','read','update','delete','execute','override_blocking_task'], ... })
admin: ac.newRole({ workflow: ['create','read','update','delete','execute'], ... })  // unchanged — NO override
// 8 more roles, all unchanged
```

Actual role names (lowercase, from research):
`owner, admin, finance_admin, ops_manager, team_manager, legal_compliance_viewer, it_admin, external_accountant, readonly, platform_operator`
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend accessControlStatement.workflow with override_blocking_task action</name>
  <files>packages/auth/src/permissions.ts</files>
  <behavior>
    - Module exports `accessControlStatement` whose `workflow` array contains exactly: 'create', 'read', 'update', 'delete', 'execute', 'override_blocking_task' (in this order)
    - No other resource arrays gain new actions
    - The `as const` modifier preserves type narrowing
  </behavior>
  <read_first>
    - packages/auth/src/permissions.ts (entire file — observe `as const` placement, statement key alphabetical conventions, current workflow array)
    - .planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-RESEARCH.md § Code Examples > Permission registration extension (lines ~707-727)
  </read_first>
  <action>
    1. Open `packages/auth/src/permissions.ts`. Locate the `accessControlStatement` definition.
    2. Find the `workflow:` line. The current value is `['create', 'read', 'update', 'delete', 'execute']`.
    3. Replace it with: `workflow: ['create', 'read', 'update', 'delete', 'execute', 'override_blocking_task'],`
    4. Preserve the `as const` modifier on the surrounding object literal. Do NOT touch any other resource (organization, member, contractor, contract, document, invoice, payment, report, settings, integration, time, equipment, 'admin:boe-rate', etc.).
    5. Verify the file still compiles: `pnpm --filter @contractor-ops/auth typecheck`.
  </action>
  <acceptance_criteria>
    - `grep "workflow: \[" packages/auth/src/permissions.ts` outputs exactly one line containing `'override_blocking_task'`
    - `grep -E "workflow:\s*\[.*'override_blocking_task'.*\]" packages/auth/src/permissions.ts` matches
    - `grep -E "workflow:\s*\['create',\s*'read',\s*'update',\s*'delete',\s*'execute',\s*'override_blocking_task'\]" packages/auth/src/permissions.ts` matches (exact order preserved)
    - `pnpm --filter @contractor-ops/auth typecheck` exits 0
    - `git diff packages/auth/src/permissions.ts | grep "^+" | grep -v "^+++" | wc -l` outputs ≤ 2 (single-line edit; one removed line / one added line)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/auth typecheck</automated>
  </verify>
  <done>accessControlStatement extended with override_blocking_task; no other resource modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Grant override_blocking_task to owner role only; verify 9 other roles unchanged</name>
  <files>packages/auth/src/roles.ts</files>
  <behavior>
    - `roles.owner.statements.workflow` includes `override_blocking_task`
    - `roles.admin.statements.workflow` does NOT include `override_blocking_task` (still 5 actions)
    - 8 other roles' workflow arrays are byte-identical to before this plan (any role that does not already include `workflow` keeps the absence)
    - Total count of `'override_blocking_task'` literals in roles.ts is exactly 1 (only in owner's allPermissions object)
  </behavior>
  <read_first>
    - packages/auth/src/roles.ts (entire file — observe whether owner uses `allPermissions` spread or per-role `ac.newRole({...})` literal, which actions each non-owner role currently has on `workflow`)
    - .planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-RESEARCH.md § Code Examples (lines ~728-758)
  </read_first>
  <action>
    1. Open `packages/auth/src/roles.ts`.
    2. Identify how `owner` is constructed. Two cases:
       - **Case A:** `owner` uses an `allPermissions` object that spreads or is reused. Locate that object's `workflow:` field and append `'override_blocking_task'` so it becomes `workflow: ['create', 'read', 'update', 'delete', 'execute', 'override_blocking_task']`.
       - **Case B:** `owner` is constructed via inline `ac.newRole({ workflow: [...], ... })`. Append `'override_blocking_task'` to that inline workflow array.
    3. For each of the other 9 roles (admin, finance_admin, ops_manager, team_manager, legal_compliance_viewer, it_admin, external_accountant, readonly, platform_operator):
       - Read the current `workflow:` value (some roles may not have a `workflow:` key at all — that's fine, leave them unchanged).
       - DO NOT add `override_blocking_task` to any of them.
       - DO NOT touch any other key in their `ac.newRole({...})` object.
    4. If `owner` is constructed via `ac.newRole(allPermissions)` and `allPermissions` is shared with non-owner roles via spread, you must split owner's permission set OR ensure the non-owner roles override `workflow` to exclude `override_blocking_task`. Read the actual pattern and adapt — RESEARCH.md § Code Examples shows the case where owner uses `allPermissions` and non-owner roles each define their own (no spread); the current codebase pattern dictates the safe edit.
    5. Verify ONLY ONE occurrence of the new action in the file: `grep -c "override_blocking_task" packages/auth/src/roles.ts` MUST output exactly `1`.
    6. Run `pnpm --filter @contractor-ops/auth typecheck`.
  </action>
  <acceptance_criteria>
    - `grep -c "override_blocking_task" packages/auth/src/roles.ts` outputs exactly `1`
    - The single occurrence is within the `owner` role's permission set (verify by reading the surrounding ~5 lines via `grep -n "override_blocking_task" packages/auth/src/roles.ts` then reading those line numbers)
    - `pnpm --filter @contractor-ops/auth typecheck` exits 0
    - `pnpm --filter @contractor-ops/auth test --run` exits 0 (existing rbac tests still pass — Pitfall 2 regression check)
    - `git diff packages/auth/src/roles.ts | grep "^+" | grep "override_blocking_task" | wc -l` outputs exactly `1` (only one added occurrence)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/auth test --run</automated>
  </verify>
  <done>owner role grants override_blocking_task; 9 other roles unchanged; existing RBAC tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fill RED scaffold with table-driven CI test asserting OWNER-only invariant</name>
  <files>packages/auth/src/__tests__/permissions-override-blocking-task.test.ts</files>
  <behavior>
    - Test iterates all 10 role names (lowercase, from roles.ts): owner, admin, finance_admin, ops_manager, team_manager, legal_compliance_viewer, it_admin, external_accountant, readonly, platform_operator
    - For role `'owner'`: assertion succeeds (override_blocking_task granted)
    - For each of 9 other roles: assertion fails-as-expected (override_blocking_task NOT granted)
    - Test uses the canonical role-introspection mechanism for the installed Better Auth version (per Researcher Assumption A1: read role.statements.workflow array directly OR call auth.api.hasPermission — pick whichever the existing rbac.test.ts pattern uses)
  </behavior>
  <read_first>
    - packages/auth/src/__tests__/permissions-override-blocking-task.test.ts (Wave 0 scaffold from Plan 74-01)
    - packages/auth/src/__tests__/ (any existing permissions test that introspects roles — observe pattern)
    - packages/api/src/middleware/__tests__/rbac.test.ts (existing role-driven test pattern to mirror)
    - packages/auth/src/roles.ts (observe the `roles` export shape — is it an object map keyed by role name, or an array of role objects?)
    - .planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-RESEARCH.md § Code Examples > Permission CI test (lines ~762-786) — shape with `role.statements?.workflow?.includes(...)`
  </read_first>
  <action>
    1. Open the Wave 0 scaffold `packages/auth/src/__tests__/permissions-override-blocking-task.test.ts`.
    2. Replace the body. Two options based on what you observed:

       **Option A — direct role introspection (preferred if `roles` exports a `Record<RoleName, Role>` shape with inspectable statements):**
       ```typescript
       import { describe, it, expect } from 'vitest';
       import { roles } from '../roles.js';

       const ALL_ROLE_NAMES = [
         'owner', 'admin', 'finance_admin', 'ops_manager', 'team_manager',
         'legal_compliance_viewer', 'it_admin', 'external_accountant', 'readonly', 'platform_operator',
       ] as const;

       describe('workflow:override_blocking_task — D-09 / SC#5 OWNER-only', () => {
         it.each(ALL_ROLE_NAMES)('role %s — override_blocking_task is %s', (roleName) => {
           const role = roles[roleName as keyof typeof roles];
           expect(role).toBeDefined();
           // Better Auth role objects expose .statements via internal property
           const grantedActions = (role.statements?.workflow ?? []) as readonly string[];
           const hasOverride = grantedActions.includes('override_blocking_task');
           if (roleName === 'owner') {
             expect(hasOverride).toBe(true);
           } else {
             expect(hasOverride).toBe(false);
           }
         });

         it('exactly one role grants override_blocking_task (Pitfall 2 regression)', () => {
           const granters = ALL_ROLE_NAMES.filter((name) => {
             const r = roles[name as keyof typeof roles];
             return ((r.statements?.workflow ?? []) as readonly string[]).includes('override_blocking_task');
           });
           expect(granters).toEqual(['owner']);
         });
       });
       ```

       **Option B — auth.api.hasPermission integration (fallback if `role.statements` is not directly inspectable):**
       Mirror the existing rbac.test.ts pattern (whatever shape it uses to verify role permissions). Adapt to call `auth.api.hasPermission({ permissions: { workflow: ['override_blocking_task'] }, role: roleName })` for each role and assert `success === (roleName === 'owner')`.

       Pick Option A first; if the assertion `(role.statements?.workflow ?? []).includes(...)` does not actually reflect what `ac.newRole(...)` produces (verify by running the test against owner — should pass), fall back to Option B.

    3. Run `pnpm --filter @contractor-ops/auth test --run -- permissions-override-blocking-task`. ALL 11 cases (10 each + 1 invariant) MUST pass.
    4. Add a comment block at the top of the test file: `// Phase 74 Plan 74-03 — Pitfall 2 regression test (RESEARCH.md § Common Pitfalls)`.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/auth test --run -- permissions-override-blocking-task` exits 0
    - Test output mentions 11 passing cases (10 role-each + 1 invariant) and zero pending/skipped
    - `grep -c "ALL_ROLE_NAMES\|allRoleNames" packages/auth/src/__tests__/permissions-override-blocking-task.test.ts` outputs ≥ 1
    - `grep -c "platform_operator" packages/auth/src/__tests__/permissions-override-blocking-task.test.ts` outputs ≥ 1 (all 10 names listed)
    - `grep -c "owner" packages/auth/src/__tests__/permissions-override-blocking-task.test.ts` outputs ≥ 2 (referenced in array AND in the if-branch)
    - `grep -c "describe.todo\|it.todo" packages/auth/src/__tests__/permissions-override-blocking-task.test.ts` outputs `0` (all RED markers turned GREEN)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/auth test --run -- permissions-override-blocking-task</automated>
  </verify>
  <done>Pitfall 2 regression test live; 11/11 cases green; CI fails immediately if any non-owner role accidentally gains override_blocking_task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → tRPC mutation | Plan 74-08's overrideBlockingTask mutation gates on requirePermission; this plan establishes the source-of-truth grant table |
| developer commit → CI | Pitfall 2 regression test fails CI if any role-table edit accidentally widens the grant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-03-permission-regression | Elevation of Privilege | packages/auth/src/roles.ts | mitigate | Pitfall 2 — explicit `it.each` over all 10 lowercase role names; test asserts owner=true, 9 others=false; plus invariant test that exactly one role grants the action. CI fails on any role-table widening |
| T-74-03-spread-leak | Elevation of Privilege | shared `allPermissions` object if used | mitigate | Task 2 step 4 explicitly checks for spread/inheritance pattern; if owner uses allPermissions and non-owner roles spread it, the plan splits owner's set OR overrides workflow array on non-owner roles. The grep `grep -c "override_blocking_task" packages/auth/src/roles.ts == 1` enforces single occurrence |
| T-74-03-action-typo | Tampering | accessControlStatement.workflow | mitigate | exact-match grep `'create', 'read', 'update', 'delete', 'execute', 'override_blocking_task'` in Task 1 acceptance prevents misspellings |
| T-74-03-cross-resource-leak | Elevation of Privilege | other resources (contractor, invoice, etc.) | mitigate | Task 1 explicit "DO NOT touch any other resource" + git-diff line-count cap (≤2 changed lines on permissions.ts) |
| T-74-03-mutation-bypass | Elevation of Privilege | Plan 74-08 override mutation | mitigate (downstream) | Plan 74-08 must use `requirePermission({ workflow: ['override_blocking_task'] })` middleware; this plan supplies the source-of-truth grant table; downstream test in 74-08 verifies mutation rejection for 9 non-owner roles |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/auth typecheck` exits 0
- `pnpm --filter @contractor-ops/auth test --run` exits 0 (all RBAC tests including the new file)
- `grep -c "override_blocking_task" packages/auth/src/permissions.ts` outputs exactly `1`
- `grep -c "override_blocking_task" packages/auth/src/roles.ts` outputs exactly `1`
- The single roles.ts occurrence is within the `owner` role's allPermissions object (manual line-context check)
- `pnpm lint:logs` exits 0 (no console.* introduced)
</verification>

<success_criteria>
Permission registered; OWNER-only grant; CI table-test live; existing RBAC tests unaffected. SC#5 ("OWNER-only and CI-tested") satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-03-SUMMARY.md` recording: which Option (A or B) the test used and why; total occurrences of `override_blocking_task` in each touched file (1, 1, ≥3 respectively); confirmation that 8 non-owner roles were untouched.
</output>
