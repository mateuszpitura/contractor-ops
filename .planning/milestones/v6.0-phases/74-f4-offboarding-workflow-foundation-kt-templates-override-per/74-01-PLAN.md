---
phase: 74
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - packages/offboarding-templates/package.json
  - packages/offboarding-templates/tsconfig.json
  - packages/offboarding-templates/vitest.config.ts
  - packages/offboarding-templates/src/types.ts
  - packages/offboarding-templates/src/index.ts
  - packages/offboarding-templates/src/__tests__/seeds.test.ts
  - packages/offboarding-templates/src/__tests__/pto-keywords.test.ts
  - packages/offboarding-templates/src/__tests__/upsert-on-boot.test.ts
  - packages/api/src/routers/__tests__/workflow-execution-template-selection.test.ts
  - packages/api/src/services/__tests__/pto-detector.test.ts
  - packages/api/src/routers/__tests__/role-template-crud.test.ts
  - packages/api/src/routers/__tests__/workflow-override-blocking-task.test.ts
  - packages/auth/src/__tests__/permissions-override-blocking-task.test.ts
  - apps/web/src/components/offboarding/__tests__/override-dialog.test.tsx
  - apps/web/src/components/offboarding/__tests__/override-badge.test.tsx
  - apps/web/src/app/[locale]/(admin)/settings/workflow-roles/__tests__/page.test.tsx
  - packages/integrations/src/adapters/__tests__/google-calendar-adapter-freebusy.test.ts
  - packages/integrations/src/adapters/__tests__/outlook-calendar-adapter-freebusy.test.ts
  - packages/feature-flags/src/signoff-registry-flags.json
autonomous: true
requirements: [OFFB-01, OFFB-02, OFFB-03, OFFB-07, OFFB-10, OFFB-11]
requirements_addressed: [OFFB-01, OFFB-02, OFFB-03, OFFB-07, OFFB-10, OFFB-11]

must_haves:
  truths:
    - "New @contractor-ops/offboarding-templates workspace package exists and resolves"
    - "RED test scaffolds exist for every Wave 1-3 deliverable (13 test files; describe.todo or skipped expect-shape)"
    - "offboarding-ip-foundation flag registered PENDING in signoff-registry-flags.json"
    - "Wave 0 baseline `pnpm i18n:parity` and `pnpm lint:schema` and `pnpm lint:logs` continue to pass"
  artifacts:
    - path: packages/offboarding-templates/package.json
      provides: workspace package skeleton (name @contractor-ops/offboarding-templates)
      contains: '"name": "@contractor-ops/offboarding-templates"'
    - path: packages/offboarding-templates/src/types.ts
      provides: Seed/TaskItem/Role typed exports for downstream plans
      exports: [Seed, TaskItem, Role, OffboardingTemplateSeedRole]
    - path: packages/feature-flags/src/signoff-registry-flags.json
      provides: offboarding-ip-foundation PENDING entry
      contains: '"offboarding-ip-foundation"'
    - path: packages/api/src/services/__tests__/pto-detector.test.ts
      provides: RED scaffold covering PTO detection branches
      contains: 'describe('
  key_links:
    - from: pnpm-workspace.yaml
      to: packages/offboarding-templates
      via: packages glob entry
      pattern: 'packages/offboarding-templates'
    - from: packages/feature-flags/src/signoff-registry-flags.json
      to: signoff-registry-flags.ts loader
      via: JSON Schema validation at module load
      pattern: '"offboarding-ip-foundation":\s*\{[^}]*"status":\s*"PENDING"'
---

<objective>
Wave 0 — RED scaffolds. Create the new `@contractor-ops/offboarding-templates` workspace package skeleton (no business logic yet, just type exports + empty seeds re-export contract), all 13 failing-test scaffolds enumerated in 74-VALIDATION.md Wave 0 Requirements, and the `offboarding-ip-foundation` PENDING entry in `signoff-registry-flags.json`.

Purpose: Establish the test-first baseline so Waves 1-3 turn RED → GREEN as each plan lands. Every downstream plan has a test file already in place to extend; no plan needs to bootstrap its own test scaffold. The signoff entry registers Phase 74's namespace before any code that references the flag ships, satisfying the Phase 70 boot gate ordering.

Output: Package skeleton + 13 test scaffolds + 1 signoff entry. No production logic. `pnpm test` runs the scaffolds; describe.todo / skipped expect-shape blocks count as RED markers, not failures (vitest treats `describe.todo` as pending).
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-CONTEXT.md
@.planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-RESEARCH.md
@.planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-VALIDATION.md
@packages/feature-flags/src/signoff-registry-flags.ts
@packages/feature-flags/src/signoff-registry-flags.json
@pnpm-workspace.yaml

<interfaces>
<!-- Types this plan creates that downstream plans consume. -->

From packages/offboarding-templates/src/types.ts (created here):
```typescript
export type OffboardingTemplateSeedRole =
  | 'software_engineer'
  | 'designer'
  | 'product_manager'
  | 'generic_consultant';

export type DocumentType = 'HANDOVER_DOCUMENT'; // Phase 75 will extend with IP_ASSIGNMENT etc.

export interface TaskItem {
  readonly titleI18nKey: string;
  readonly descriptionI18nKey: string;
  readonly dueDayOffset: number; // days from offboarding-start
  readonly requiredDocs?: readonly DocumentType[];
}

export interface Seed {
  readonly role: OffboardingTemplateSeedRole;
  readonly displayNameI18nKey: string;
  readonly taskItems: readonly TaskItem[];
}

export type SupportedLocale = 'en' | 'pl' | 'de';
export type PtoKeywords = Readonly<Record<SupportedLocale, readonly string[]>>;
```

From packages/offboarding-templates/src/index.ts (placeholder re-exports):
```typescript
export type { Seed, TaskItem, Role, OffboardingTemplateSeedRole, PtoKeywords, SupportedLocale, DocumentType } from './types.js';
// Plan 74-02 fills these:
export { OFFBOARDING_TEMPLATE_SEEDS } from './seeds.js';
export { PTO_KEYWORDS } from './pto-keywords.js';
export { upsertSeedTemplates } from './upsert-on-boot.js';
```
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create @contractor-ops/offboarding-templates workspace package skeleton</name>
  <files>pnpm-workspace.yaml, packages/offboarding-templates/package.json, packages/offboarding-templates/tsconfig.json, packages/offboarding-templates/vitest.config.ts, packages/offboarding-templates/src/types.ts, packages/offboarding-templates/src/index.ts, packages/offboarding-templates/src/seeds.ts, packages/offboarding-templates/src/pto-keywords.ts, packages/offboarding-templates/src/upsert-on-boot.ts</files>
  <read_first>
    - pnpm-workspace.yaml (entire file, observe glob pattern conventions)
    - packages/feature-flags/package.json (mirror sibling typed-const package's package.json shape per Phase 70 D-02)
    - packages/feature-flags/tsconfig.json (mirror compiler settings)
    - packages/feature-flags/vitest.config.ts (mirror test config)
    - packages/db/package.json (note exports map style for Prisma-aware packages — offboarding-templates depends on @contractor-ops/db typings only via type imports)
    - .planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-RESEARCH.md § Recommended Project Structure (lines ~248-258)
  </read_first>
  <action>
    1. Add `packages/offboarding-templates` to `pnpm-workspace.yaml` if its glob does not already capture it (typical glob: `packages/*` already does; verify and DO NOT duplicate the entry). If `packages/*` is the canonical glob, no edit is required.
    2. Create `packages/offboarding-templates/package.json` mirroring `packages/feature-flags/package.json`:
       - `"name": "@contractor-ops/offboarding-templates"`
       - `"version": "0.0.0"`
       - `"private": true`
       - `"type": "module"`
       - `"main": "./src/index.ts"` (or `"./dist/index.js"` if sibling uses dist; mirror sibling exactly)
       - `"types": "./src/index.ts"`
       - `"exports"` map mirroring sibling
       - `"scripts": { "test": "vitest run", "test:watch": "vitest", "typecheck": "tsc --noEmit" }`
       - `"dependencies": { "@contractor-ops/db": "workspace:*", "@date-fns/tz": "^4.0.0" }` (per Phase 71 D-07 TZ pin convergence)
       - `"devDependencies": { "vitest": "<sibling-pinned-version>", "typescript": "<sibling-pinned-version>" }`
    3. Create `packages/offboarding-templates/tsconfig.json` extending the repo root TS config (mirror `packages/feature-flags/tsconfig.json`).
    4. Create `packages/offboarding-templates/vitest.config.ts` (mirror `packages/feature-flags/vitest.config.ts`).
    5. Create `packages/offboarding-templates/src/types.ts` with the EXACT type shapes from the `<interfaces>` block in this PLAN's `<context>`. All types `readonly` where shown. No additional types.
    6. Create `packages/offboarding-templates/src/seeds.ts` with stub: `import type { Seed } from './types.js'; export const OFFBOARDING_TEMPLATE_SEEDS: readonly Seed[] = [] as const;` (Plan 74-02 fills the 4 seeds).
    7. Create `packages/offboarding-templates/src/pto-keywords.ts` with stub: `import type { PtoKeywords } from './types.js'; export const PTO_KEYWORDS: PtoKeywords = { en: [], de: [], pl: [] } as const;` (Plan 74-02 fills the keyword arrays).
    8. Create `packages/offboarding-templates/src/upsert-on-boot.ts` with stub: an exported async function `upsertSeedTemplates(prisma: unknown, organizationId: string): Promise<void>` whose body is `throw new Error('NOT_IMPLEMENTED — Plan 74-05 fills this');`. Use `unknown` for prisma to avoid premature DB-package coupling; Plan 74-05 will replace with `PrismaClient`.
    9. Create `packages/offboarding-templates/src/index.ts` re-exporting per the `<interfaces>` block.
    10. Run `pnpm install` from repo root to refresh the workspace; verify `pnpm --filter @contractor-ops/offboarding-templates typecheck` passes (no compile errors — stubs are valid).
  </action>
  <acceptance_criteria>
    - `cat packages/offboarding-templates/package.json | jq -r '.name'` outputs exactly `@contractor-ops/offboarding-templates`
    - `cat packages/offboarding-templates/package.json | jq -r '.dependencies."@date-fns/tz"'` outputs a 4.x version string (NOT null, NOT date-fns-tz legacy)
    - `pnpm --filter @contractor-ops/offboarding-templates typecheck` exits 0
    - `grep -c "OFFBOARDING_TEMPLATE_SEEDS" packages/offboarding-templates/src/index.ts` outputs ≥ 1
    - `grep -c "PTO_KEYWORDS" packages/offboarding-templates/src/index.ts` outputs ≥ 1
    - `grep -c "upsertSeedTemplates" packages/offboarding-templates/src/index.ts` outputs ≥ 1
    - `grep -c "OffboardingTemplateSeedRole" packages/offboarding-templates/src/types.ts` outputs ≥ 1
    - `pnpm lint:logs` exits 0 (no console.* introduced)
    - `pnpm lint:schema` exits 0 (no Prisma changes in this task)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/offboarding-templates typecheck</automated>
  </verify>
  <done>Workspace package resolves; types compile; downstream Plans 74-02, 74-05, 74-06 can import from `@contractor-ops/offboarding-templates`.</done>
</task>

<task type="auto">
  <name>Task 2: Create 13 RED test scaffolds covering all Wave 1-3 deliverables</name>
  <files>packages/offboarding-templates/src/__tests__/seeds.test.ts, packages/offboarding-templates/src/__tests__/pto-keywords.test.ts, packages/offboarding-templates/src/__tests__/upsert-on-boot.test.ts, packages/api/src/routers/__tests__/workflow-execution-template-selection.test.ts, packages/api/src/services/__tests__/pto-detector.test.ts, packages/api/src/routers/__tests__/role-template-crud.test.ts, packages/api/src/routers/__tests__/workflow-override-blocking-task.test.ts, packages/auth/src/__tests__/permissions-override-blocking-task.test.ts, apps/web/src/components/offboarding/__tests__/override-dialog.test.tsx, apps/web/src/components/offboarding/__tests__/override-badge.test.tsx, apps/web/src/app/[locale]/(admin)/settings/workflow-roles/__tests__/page.test.tsx, packages/integrations/src/adapters/__tests__/google-calendar-adapter-freebusy.test.ts, packages/integrations/src/adapters/__tests__/outlook-calendar-adapter-freebusy.test.ts</files>
  <read_first>
    - .planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-VALIDATION.md (Wave 0 Requirements list)
    - .planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-RESEARCH.md § Phase Requirements → Test Map
    - packages/api/src/middleware/__tests__/rbac.test.ts (mirror table-driven role assertion shape)
    - apps/web/src/app/admin/feature-flags/classification-engine/__tests__/page.test.tsx (mirror RTL setup conventions)
    - packages/integrations/src/adapters/google-calendar-adapter.ts (read existing event-CRUD methods so the freebusy scaffold mirrors the adapter's class shape)
  </read_first>
  <action>
    Create each scaffold using vitest's `describe.todo` for sub-cases that Wave 1-3 plans fill in. Each scaffold MUST:
    - Import `describe, it, expect` from `vitest` and (where relevant) `render, screen` from `@testing-library/react`.
    - Have at minimum ONE `describe(...)` block per requirement listed in 74-VALIDATION.md row that this file owns.
    - Use `describe.todo('case description')` for each test case the downstream plan must implement (see RESEARCH.md § Phase Requirements → Test Map for the canonical case list per requirement).
    - Include a single `it.todo('TODO — filled by Plan 74-NN')` placeholder so the file has at least one runnable assertion shape and vitest treats it as pending, not failing.

    Per-file scaffold contents (minimum):

    1. `packages/offboarding-templates/src/__tests__/seeds.test.ts` (Plan 74-02):
       - `describe('OFFBOARDING_TEMPLATE_SEEDS — D-04 shape contract')` with `it.todo('exports exactly 4 seeds with roles software_engineer, designer, product_manager, generic_consultant')`, `it.todo('every seed has 6-9 task items per CONTEXT.md SC#1')`, `it.todo('every i18n key has the form Offboarding.Templates.{Role}.{itemKey}.{title|description}')`, `it.todo('dueDayOffset values are non-negative integers')`, `it.todo('readonly enforced — Object.isFrozen on seeds and taskItems')`.

    2. `packages/offboarding-templates/src/__tests__/pto-keywords.test.ts` (Plan 74-02):
       - `describe('PTO_KEYWORDS — D-08 typed-const')` with todos: `'has en/de/pl keys; no ar'`, `'en includes PTO, OOO, Out of Office, Vacation'`, `'de includes Urlaub, Krank'`, `'pl includes Urlop, Wakacje'`.

    3. `packages/offboarding-templates/src/__tests__/upsert-on-boot.test.ts` (Plan 74-05):
       - `describe('upsertSeedTemplates — first-boot idempotency')` with todos: `'inserts 4 WorkflowRoleTemplate rows on first call'`, `'is idempotent — second call does not duplicate or error'`, `'sets isSeed: true for all 4 rows'`.

    4. `packages/api/src/routers/__tests__/workflow-execution-template-selection.test.ts` (Plan 74-05/74-08):
       - `describe('startOffboardingRun — D-02 + D-03 template auto-selection')` with todos: `'auto-selects template by Contractor.workflowRoleId'`, `'falls back to generic_consultant when workflowRoleId is NULL'`, `'manual override writes overriddenTemplateId/overriddenByUserId/overriddenAt on WorkflowRun'`, `'mid-workflow swap is rejected (D-03)'`.

    5. `packages/api/src/services/__tests__/pto-detector.test.ts` (Plan 74-06):
       - `describe('resolveAssigneeWithPto — D-05/D-06/D-07/D-08')` with todos: `'routes to manager when no PTO detected'`, `'routes to fallback when calendar shows all-day busy with PTO_KEYWORDS title'`, `'routes to fallback when User.outOfOffice is set today'`, `'honours User.outOfOffice.fallbackUserId override before Team.fallbackApproverId'`, `'skips calendar lookup entirely when no integration connected (D-07)'`, `'falls through to OWNER role users + admin-attention badge when team has no fallback'`, `'no PTO-spam — single resolution at task creation time'`.

    6. `packages/api/src/routers/__tests__/role-template-crud.test.ts` (Plan 74-05):
       - `describe('workflowRoles router — D-01/D-14 CRUD')` with todos: `'createRoleTemplate writes per-locale columns titleEn/Pl/De'`, `'listRoleTemplates returns seed + ops rows scoped to organizationId'`, `'updateRoleTemplate enforces tenant isolation'`, `'deleteRoleTemplate refuses to delete isSeed=true rows'`, `'getCurrentUserPermissions returns workflow.override_blocking_task only for owner role'`.

    7. `packages/api/src/routers/__tests__/workflow-override-blocking-task.test.ts` (Plan 74-08):
       - `describe('overrideBlockingTask mutation — D-10/D-11/D-12 + Pitfall 5')` with todos: `'requires workflow:override_blocking_task permission (rejects 9 non-owner roles)'`, `'rejects reason shorter than 20 chars (Zod min)'`, `'rejects acknowledged: false (Zod literal)'`, `'writes WorkflowRun.overrideMetadata + AuditLog row + WorkflowTaskRun status SKIPPED in same $transaction'`, `'returns PRECONDITION_FAILED when no IP_VERIFICATION task is open'`.

    8. `packages/auth/src/__tests__/permissions-override-blocking-task.test.ts` (Plan 74-03):
       - `describe('workflow:override_blocking_task — D-09 / SC#5 OWNER-only')` with `it.todo('owner role grants override_blocking_task')` and `it.todo.each(['admin', 'finance_admin', 'ops_manager', 'team_manager', 'legal_compliance_viewer', 'it_admin', 'external_accountant', 'readonly', 'platform_operator'])('role %s does NOT grant override_blocking_task')` — the executor in Plan 74-03 fills the assertion body.

    9. `apps/web/src/components/offboarding/__tests__/override-dialog.test.tsx` (Plan 74-08):
       - `describe('OverrideDialog — D-10 dual-validation')` with todos: `'submit disabled until reason >= 20 chars AND acknowledged'`, `'discard-confirm AlertDialog opens on ESC when dirty'`, `'server error renders inline above CTA on Zod failure'`, `'focus traps within dialog; restores to override-button on close'`.

    10. `apps/web/src/components/offboarding/__tests__/override-badge.test.tsx` (Plan 74-08):
        - `describe('OverrideBadge — D-11 permanent badge')` with todos: `'renders when WorkflowRun.overrideMetadata is present'`, `'does not render when overrideMetadata is null'`, `'tooltip shows reason + actor + date + blockedTaskKind'`, `'badge is keyboard-focusable button (a11y)'`.

    11. `apps/web/src/app/[locale]/(admin)/settings/workflow-roles/__tests__/page.test.tsx` (Plan 74-07):
        - `describe('Settings > Workflow Roles — D-14/D-15')` with todos: `'renders 3-locale-input form (en/pl/de tabs)'`, `'Copy from English helper populates pl + de fields'`, `'(English) indicator visible suffix on rows missing pl or de translation'`, `'no AR tab rendered (Phase 79 only)'`.

    12. `packages/integrations/src/adapters/__tests__/google-calendar-adapter-freebusy.test.ts` (Plan 74-06):
        - `describe('GoogleCalendarAdapter.getFreeBusy — Plan 74-06')` with todos: `'POSTs to /freeBusy and merges events.list for titles + isAllDay'`, `'throws with non-2xx response body in error message'`, `'redacts access token from any error log via createLogger'`.

    13. `packages/integrations/src/adapters/__tests__/outlook-calendar-adapter-freebusy.test.ts` (Plan 74-06):
        - `describe('OutlookCalendarAdapter.getFreeBusy — Plan 74-06')` with todos: `'calls /me/calendar/getSchedule with manager email + ISO time window'`, `'parses availabilityView into BusyRange[] with summary + isAllDay flags'`, `'throws when Microsoft Graph returns >=400'`.

    Use `import { describe, it, expect } from 'vitest';` at top of every file. Use `// TODO(Plan 74-NN): implement` comment above each `describe.todo` block to make the deferred ownership explicit.
  </action>
  <acceptance_criteria>
    - `find packages apps -path '*__tests__*' -name '*override*' -o -name '*pto-detector*' -o -name '*role-template-crud*' -o -name '*workflow-execution-template-selection*' -o -name '*permissions-override-blocking-task*' -o -name '*english-fallback-indicator*' -o -name 'seeds.test.ts' -path '*offboarding-templates*' | wc -l` returns ≥ 13 (the 13 scaffold files this task creates plus existing matches)
    - `grep -lr "describe.todo\|it.todo" packages/offboarding-templates/src/__tests__ packages/api/src/routers/__tests__ packages/api/src/services/__tests__ packages/auth/src/__tests__ apps/web/src/components/offboarding/__tests__ apps/web/src/app/\\[locale\\]/\\(admin\\)/settings/workflow-roles/__tests__ packages/integrations/src/adapters/__tests__ 2>/dev/null | wc -l` returns ≥ 13
    - `pnpm --filter @contractor-ops/offboarding-templates test --run` exits 0 (vitest treats `describe.todo` as pending, not failing)
    - `grep -c "TODO(Plan 74-" packages/api/src/services/__tests__/pto-detector.test.ts` outputs ≥ 1
    - All scaffold files compile under `tsc --noEmit` (no syntax errors, no untyped imports)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/offboarding-templates test --run && pnpm --filter @contractor-ops/auth test --run -- permissions-override-blocking-task</automated>
  </verify>
  <done>13 RED scaffolds exist; vitest treats them as pending (not failing); every Wave 1-3 plan has a test file ready to extend.</done>
</task>

<task type="auto">
  <name>Task 3: Register offboarding-ip-foundation PENDING signoff entry</name>
  <files>packages/feature-flags/src/signoff-registry-flags.json</files>
  <read_first>
    - packages/feature-flags/src/signoff-registry-flags.ts (entire file — observe FlagSignoffRegistrySchema shape, GATED_FLAG_NAMESPACE_PREFIXES, status enum)
    - packages/feature-flags/src/signoff-registry-flags.json (current state — confirm whether other flags already exist or it's `{}`)
    - .planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-RESEARCH.md § Sources (verifies 'offboarding-ip-' prefix is canonical, NOT 'offboarding-hardening-')
    - .planning/milestones/v6.0-phases/70-v6-foundation-cross-cutting-ci-guards-observability-baseline/70-05-SUMMARY.md (read the schema shape established by Phase 70 Plan 05/07)
  </read_first>
  <action>
    1. Read `packages/feature-flags/src/signoff-registry-flags.ts` to determine the EXACT JSON Schema shape that `FlagSignoffRegistrySchema` validates against. The schema enforces a structure approximately:
       ```json
       {
         "<flag-namespace>": {
           "status": "PENDING" | "APPROVED",
           "owner": "<role-or-team>",
           "reason": "<legal-context-summary>",
           "registered_at": "<ISO-date>",
           "approved_at"?: "<ISO-date>",
           "approval_ticket"?: "<ticket-ref>"
         }
       }
       ```
       Use the EXACT field names the loader expects — do not invent fields.
    2. Edit `packages/feature-flags/src/signoff-registry-flags.json` to add a new key `"offboarding-ip-foundation"` (NOT `offboarding-hardening-foundation` — research confirmed `GATED_FLAG_NAMESPACE_PREFIXES` includes `'offboarding-ip-'` per RESEARCH.md § Sources).
    3. Field values:
       - `status`: `"PENDING"`
       - `owner`: `"legal-compliance"` (or whatever value Phase 70 Plan 05/07 uses for legal-sensitive owner; mirror sibling entries if any exist; if registry is `{}`, use `"legal-compliance"` as the LOCAL-ONLY-deferred posture marker per Standing Project Constraint)
       - `reason`: `"F4 Offboarding workflow foundation + KT templates + override permission. Werkvertrag wording deferred to Phase 75; this flag gates the workflow:override_blocking_task permission and PTO-aware fallback routing. Needs verification by legal entity before production deploy (override-dialog acknowledgement copy)."`
       - `registered_at`: `"2026-04-27"` (today per the planning context)
    4. Do NOT add `approved_at` or `approval_ticket` — those land post-deploy when LOCAL-ONLY lifts.
    5. Run `pnpm --filter @contractor-ops/feature-flags test --run` to confirm `FlagSignoffRegistrySchema` accepts the new entry (boot gate parses and validates the JSON at module load — invalid shapes fail-fast).
    6. Run `pnpm --filter @contractor-ops/feature-flags typecheck` to confirm no TS regression.
  </action>
  <acceptance_criteria>
    - `cat packages/feature-flags/src/signoff-registry-flags.json | jq -r '."offboarding-ip-foundation".status'` outputs exactly `PENDING`
    - `cat packages/feature-flags/src/signoff-registry-flags.json | jq -r '."offboarding-ip-foundation".reason'` contains the substring `"F4 Offboarding"` AND the substring `"Phase 75"` AND the substring `"production deploy"`
    - `cat packages/feature-flags/src/signoff-registry-flags.json | jq -r '."offboarding-hardening-foundation"'` outputs exactly `null` (the wrong-named flag from CONTEXT.md MUST NOT appear)
    - `pnpm --filter @contractor-ops/feature-flags test --run` exits 0 (boot-gate schema validation passes)
    - `grep -c "offboarding-ip-foundation" packages/feature-flags/src/signoff-registry-flags.json` outputs ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/feature-flags test --run</automated>
  </verify>
  <done>Phase 70 boot gate accepts the new PENDING entry; Plan 74-08's runtime flag check (when the override mutation lands) will resolve against this entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → CI | Phase 70 boot gate validates signoff-registry-flags.json at module load; malformed entries fail-fast |
| developer → workspace | New package added to pnpm-workspace; lockfile change must not introduce parallel TZ libs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01-01 | Tampering | signoff-registry-flags.json | mitigate | FlagSignoffRegistrySchema (Phase 70 Plan 05) validates shape at module load; Task 3 acceptance criteria assert correct field shape via jq |
| T-74-01-02 | Information Disclosure | RED test scaffolds | accept | describe.todo blocks contain no real data; only structural shape names and TODO comments. Risk: low |
| T-74-01-03 | Spoofing | offboarding-ip-foundation flag namespace | mitigate | Use canonical prefix 'offboarding-ip-' per Phase 70 D-11 (research-verified); CONTEXT.md alternate name 'offboarding-hardening-foundation' would silently fail the boot gate match — acceptance criteria explicitly assert the wrong name is absent |
| T-74-01-04 | Denial of Service | pnpm install / Turborepo cache | accept | New workspace package addition triggers Turborepo cache invalidation by default; minor build delay on first CI run is acceptable |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/offboarding-templates typecheck` exits 0
- `pnpm --filter @contractor-ops/offboarding-templates test --run` exits 0 (all describes are .todo or pending)
- `pnpm --filter @contractor-ops/feature-flags test --run` exits 0
- `pnpm i18n:parity` exits 0 (no message-key changes in Wave 0)
- `pnpm lint:schema` exits 0 (no Prisma changes)
- `pnpm lint:logs` exits 0 (no console.* introduced)
- `grep -c "offboarding-ip-foundation" packages/feature-flags/src/signoff-registry-flags.json` ≥ 1
- 13 RED scaffold files present and pending
</verification>

<success_criteria>
Wave 0 complete when: (1) the workspace package skeleton exists and typechecks, (2) all 13 RED scaffolds exist and run as pending under vitest, (3) the `offboarding-ip-foundation` PENDING signoff entry validates under the Phase 70 boot gate, (4) all four CI guards (`pnpm i18n:parity`, `pnpm lint:schema`, `pnpm lint:logs`, package tests) exit 0.
</success_criteria>

<output>
After completion, create `.planning/phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-01-SUMMARY.md` recording: package skeleton path, list of 13 scaffold files (with the wave/plan that owns each), signoff entry shape, exact `@date-fns/tz` version pinned, and any deviations from the plan with rationale.
</output>
