---
phase: 74
plan: 02
type: execute
wave: 1
depends_on: [74-01]
files_modified:
  - packages/offboarding-templates/src/seeds.ts
  - packages/offboarding-templates/src/pto-keywords.ts
  - packages/offboarding-templates/src/__tests__/seeds.test.ts
  - packages/offboarding-templates/src/__tests__/pto-keywords.test.ts
  - apps/web/messages/en.json
  - apps/web/messages/pl.json
  - apps/web/messages/de.json
autonomous: true
requirements: [OFFB-01, OFFB-11]
requirements_addressed: [OFFB-01, OFFB-11]

must_haves:
  truths:
    - "Four typed-const KT seed templates (software_engineer / designer / product_manager / generic_consultant) exported with 6-9 task items each per CONTEXT.md SC#1"
    - "PTO_KEYWORDS typed const ships with curated en/de/pl arrays matching D-08 (no ar)"
    - "Every seed i18n key resolves in en.json AND pl.json AND de.json — `pnpm i18n:parity` passes"
    - "Override dialog copy + Settings UI copy + locale-fallback indicator copy under Offboarding.* / Settings.WorkflowRoles.* / Settings.PtoKeywords.* / Workflow.Start.* namespaces ship en/pl/de parity (NO Arabic, NO Werkvertrag wording)"
  artifacts:
    - path: packages/offboarding-templates/src/seeds.ts
      provides: 4 KT seed templates as typed-const
      contains: "OFFBOARDING_TEMPLATE_SEEDS"
      min_lines: 80
    - path: packages/offboarding-templates/src/pto-keywords.ts
      provides: PTO_KEYWORDS curated per locale
      contains: "PTO_KEYWORDS"
    - path: apps/web/messages/en.json
      provides: Offboarding.Templates.* + Offboarding.OverrideDialog.* + Offboarding.OverrideBadge.* + Offboarding.PtoBadge.* + Offboarding.LocaleFallback.* + Settings.WorkflowRoles.* + Settings.PtoKeywords.* + Workflow.Start.* keys
      contains: "Offboarding.Templates.SoftwareEngineer.displayName"
  key_links:
    - from: packages/offboarding-templates/src/seeds.ts
      to: apps/web/messages/en.json
      via: i18n key reference under Offboarding.Templates.{Role}.{itemKey}.{title|description}
      pattern: 'Offboarding\.Templates\.(SoftwareEngineer|Designer|ProductManager|GenericConsultant)\.'
    - from: pnpm i18n:parity guard
      to: en/pl/de message catalogues
      via: Phase 70 Plan 70-04 base ⊂ peers parity rule
      pattern: 'Offboarding\.|Settings\.WorkflowRoles\.|Settings\.PtoKeywords\.|Workflow\.Start\.'
---

<objective>
Wave 1 — Ship the 4 KT seed templates as typed constants, the `PTO_KEYWORDS` typed const, and ALL en/pl/de i18n keys that Phase 74 introduces (seed templates, override dialog, badges, settings UI, locale-fallback indicator). One commit lands all three locales together to keep `pnpm i18n:parity` green (Pitfall 3).

Purpose: Single source of truth for offboarding role-template content (D-01 + D-04 + D-13 + D-08) and for every user-visible string Phase 74 introduces. Subsequent plans (74-05 Settings UI, 74-07 Settings UI, 74-08 dialog/badge) consume keys; the i18n:parity guard catches drift across en/pl/de.

Output: Filled seeds.ts (~80 LOC), filled pto-keywords.ts, 3 message catalogues extended (Werkvertrag wording explicitly OUT — Phase 75), Wave 0 scaffolds for seeds.test.ts and pto-keywords.test.ts turn GREEN.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-CONTEXT.md
@.planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-RESEARCH.md
@.planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-UI-SPEC.md
@apps/web/messages/en.json
@apps/web/messages/pl.json
@apps/web/messages/de.json
@packages/offboarding-templates/src/types.ts
@packages/offboarding-templates/src/seeds.ts
@packages/offboarding-templates/src/pto-keywords.ts

<interfaces>
<!-- This plan FILLS Plan 74-01's stubbed exports. Downstream consumers: 74-05, 74-06, 74-07, 74-08. -->

Seeds export shape (from Plan 74-01 types):
```typescript
export const OFFBOARDING_TEMPLATE_SEEDS: readonly Seed[] = [...] as const;
// 4 seeds, each with 6-9 task items per CONTEXT.md SC#1
```

PTO_KEYWORDS shape (from Plan 74-01 types):
```typescript
export const PTO_KEYWORDS: PtoKeywords = {
  en: ['PTO', 'OOO', 'Out of Office', 'Vacation'],
  de: ['Urlaub', 'Krank'],
  pl: ['Urlop', 'Wakacje'],
} as const;
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill 4 KT seed templates with task items + PTO_KEYWORDS</name>
  <files>packages/offboarding-templates/src/seeds.ts, packages/offboarding-templates/src/pto-keywords.ts, packages/offboarding-templates/src/__tests__/seeds.test.ts, packages/offboarding-templates/src/__tests__/pto-keywords.test.ts</files>
  <behavior>
    - seeds.test.ts: OFFBOARDING_TEMPLATE_SEEDS has exactly 4 entries with roles software_engineer/designer/product_manager/generic_consultant
    - seeds.test.ts: every seed has between 6 and 9 task items (per CONTEXT.md SC#1 wording "6-9 task rows")
    - seeds.test.ts: every titleI18nKey + descriptionI18nKey + displayNameI18nKey starts with `Offboarding.Templates.`
    - seeds.test.ts: dueDayOffset values are non-negative integers; sortOrder implicit by array position
    - seeds.test.ts: const arrays are deeply readonly (Object.isFrozen at runtime via `as const`)
    - pto-keywords.test.ts: PTO_KEYWORDS has exactly 3 keys (en/de/pl), no `ar` key
    - pto-keywords.test.ts: each locale array has at least 2 keywords; en includes 'PTO' and 'Vacation'; de includes 'Urlaub'; pl includes 'Urlop'
  </behavior>
  <read_first>
    - packages/offboarding-templates/src/types.ts (reference Seed/TaskItem/PtoKeywords types from Plan 74-01)
    - packages/offboarding-templates/src/seeds.ts (current stub from Plan 74-01)
    - .planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-RESEARCH.md § Code Examples > 4 KT Seed Templates (lines ~600-702 — Researcher-drafted seed task lists, USE THESE VERBATIM as the production wording per CONTEXT.md "Claude's Discretion")
    - .planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-CONTEXT.md § specifics (PTO_KEYWORDS sized ~5 per locale)
  </read_first>
  <action>
    1. Replace the stub in `packages/offboarding-templates/src/seeds.ts` with the EXACT 4-seed array from RESEARCH.md § Code Examples > "4 KT Seed Templates (D-04 — Researcher-drafted)" (lines ~600-702). Use the i18n keys EXACTLY as drafted (Offboarding.Templates.SoftwareEngineer.handoverDocs.title etc.). Software Engineer: 7 items. Designer: 6 items. Product Manager: 6 items. Generic Consultant: 5 items — BUT CONTEXT.md SC#1 requires 6-9 items per template, and Generic Consultant has only 5 in the research draft. Add ONE more task item to Generic Consultant (titleI18nKey: `Offboarding.Templates.GenericConsultant.contactDirectory.title`, descriptionI18nKey: `Offboarding.Templates.GenericConsultant.contactDirectory.description`, dueDayOffset: 5) so all 4 seeds satisfy SC#1's 6-9 range. Cite the SC#1 derivation as a comment above the seed.
    2. Type the export as `readonly Seed[]` and use `as const` for the array literal so TypeScript treats it as deeply readonly.
    3. Replace the stub in `packages/offboarding-templates/src/pto-keywords.ts` with:
       ```typescript
       import type { PtoKeywords } from './types.js';
       export const PTO_KEYWORDS: PtoKeywords = {
         en: ['PTO', 'OOO', 'Out of Office', 'Vacation'],
         de: ['Urlaub', 'Krank'],
         pl: ['Urlop', 'Wakacje'],
       } as const;
       ```
    4. Open `packages/offboarding-templates/src/__tests__/seeds.test.ts` (Wave 0 RED scaffold) and replace each `it.todo(...)` with a real `it(...)` assertion implementing the behavior block above. Use `import { OFFBOARDING_TEMPLATE_SEEDS } from '../seeds.js';`. For the i18n-key shape test, regex-match `^Offboarding\.Templates\.(SoftwareEngineer|Designer|ProductManager|GenericConsultant)\.[a-zA-Z]+\.(title|description|displayName)$` (note: displayName uses `Offboarding.Templates.{Role}.displayName` without an itemKey — adjust regex to handle both shapes).
    5. Open `packages/offboarding-templates/src/__tests__/pto-keywords.test.ts` and replace `it.todo(...)` with real assertions. Assert `Object.keys(PTO_KEYWORDS).sort()` equals `['de', 'en', 'pl']`. Assert each locale array contains its required keyword.
    6. Run `pnpm --filter @contractor-ops/offboarding-templates test --run`. ALL tests must pass.
    7. Verify no console.* introduced: `pnpm lint:logs`.
  </action>
  <acceptance_criteria>
    - `node -e "import('./packages/offboarding-templates/src/seeds.ts').then(m => console.log(m.OFFBOARDING_TEMPLATE_SEEDS.length))" 2>/dev/null || pnpm --filter @contractor-ops/offboarding-templates exec tsx -e "import('./src/seeds.js').then(m => process.stdout.write(String(m.OFFBOARDING_TEMPLATE_SEEDS.length)))"` outputs `4`
    - `grep -c "role: 'software_engineer'" packages/offboarding-templates/src/seeds.ts` outputs `1`
    - `grep -c "role: 'designer'" packages/offboarding-templates/src/seeds.ts` outputs `1`
    - `grep -c "role: 'product_manager'" packages/offboarding-templates/src/seeds.ts` outputs `1`
    - `grep -c "role: 'generic_consultant'" packages/offboarding-templates/src/seeds.ts` outputs `1`
    - `grep -E "titleI18nKey: 'Offboarding\.Templates\." packages/offboarding-templates/src/seeds.ts | wc -l` outputs ≥ 24 (4 seeds × ≥6 items)
    - `grep -c "Urlaub" packages/offboarding-templates/src/pto-keywords.ts` outputs ≥ 1
    - `grep -c "Urlop" packages/offboarding-templates/src/pto-keywords.ts` outputs ≥ 1
    - `grep -c "'PTO'" packages/offboarding-templates/src/pto-keywords.ts` outputs ≥ 1
    - `grep -c "ar:" packages/offboarding-templates/src/pto-keywords.ts` outputs `0` (no Arabic locale per CONTEXT.md D-13)
    - `pnpm --filter @contractor-ops/offboarding-templates test --run` exits 0 with all tests passing (no .todo remaining)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/offboarding-templates test --run</automated>
  </verify>
  <done>4 seeds + PTO_KEYWORDS exported as deeply-readonly typed const; Wave 0 scaffolds for seeds + pto-keywords turn GREEN; SC#1's 6-9 task-items-per-template invariant holds.</done>
</task>

<task type="auto">
  <name>Task 2: Add en/pl/de message-catalogue keys for ALL Phase 74 surfaces</name>
  <files>apps/web/messages/en.json, apps/web/messages/pl.json, apps/web/messages/de.json</files>
  <read_first>
    - apps/web/messages/en.json (find existing `workflow.templates.offboarding.*` namespace if any; observe top-level structure to choose insertion point — Researcher noted line ~4732 has an existing namespace; ADD a NEW top-level `Offboarding` and `Settings` and `Workflow` keys, NOT under `workflow.templates.offboarding`, to avoid namespace collision)
    - apps/web/messages/pl.json (mirror same structure)
    - apps/web/messages/de.json (mirror same structure)
    - .planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-UI-SPEC.md § Copywriting Contract (CANONICAL en strings + planner-fill markers for pl/de)
    - .planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-UI-SPEC.md § i18n keyspace structure (full key list)
    - packages/lint-guards/src/i18n-parity/run-guard.ts (understand what the guard considers "parity" — base ⊂ peers means en is the source-of-truth, pl/de must have all en keys)
  </read_first>
  <action>
    Add the COMPLETE keyspace below to `en.json`, `pl.json`, `de.json` simultaneously. ALL three files must commit together (Pitfall 3 — Phase 70 i18n:parity guard rejects partial adds).

    **EN keys (use UI-SPEC.md § Copywriting Contract exact wording for every key marked canonical; for keys marked `(planner)` in UI-SPEC, author en wording yourself per the canonical-style in the same section):**

    Under top-level `Offboarding` namespace, add:
    - `Offboarding.Templates.SoftwareEngineer.displayName`: "Software Engineer"
    - `Offboarding.Templates.SoftwareEngineer.handoverDocs.title`: "Hand over technical documentation"
    - `Offboarding.Templates.SoftwareEngineer.handoverDocs.description`: "Hand over architecture diagrams, runbooks, and any internal technical documentation owned by the contractor."
    - `Offboarding.Templates.SoftwareEngineer.codeWalkthrough.title`: "Walk successor through codebase"
    - `Offboarding.Templates.SoftwareEngineer.codeWalkthrough.description`: "Pair with the successor on a guided tour of the services, repos, and CI workflows the contractor owned."
    - `Offboarding.Templates.SoftwareEngineer.openPRs.title`: "Resolve or hand off open pull requests"
    - `Offboarding.Templates.SoftwareEngineer.openPRs.description`: "Close, merge, or transfer ownership of every open PR authored by the contractor."
    - `Offboarding.Templates.SoftwareEngineer.deploymentRunbook.title`: "Update deployment runbook"
    - `Offboarding.Templates.SoftwareEngineer.deploymentRunbook.description`: "Annotate the deployment runbook with the contractor's tribal knowledge — quirks, escalation paths, post-deploy checks."
    - `Offboarding.Templates.SoftwareEngineer.onCallRotation.title`: "Remove from on-call rotation"
    - `Offboarding.Templates.SoftwareEngineer.onCallRotation.description`: "Replace the contractor in PagerDuty / Opsgenie schedules and notify the rotation."
    - `Offboarding.Templates.SoftwareEngineer.architectureNotes.title`: "Capture architecture decision records"
    - `Offboarding.Templates.SoftwareEngineer.architectureNotes.description`: "Document any in-flight architectural decisions the contractor was driving."
    - `Offboarding.Templates.SoftwareEngineer.knownIssues.title`: "Document known issues + WIP fixes"
    - `Offboarding.Templates.SoftwareEngineer.knownIssues.description`: "List known production issues the contractor was tracking and the state of any in-progress fixes."

    Designer (6 items): displayName "Designer" + designSystemHandover / figmaTransfer / assetLibraryAccess / activeProjects / brandGuidelinesUpdate / researchArchive (mirror SE wording style, design-domain-specific).

    Product Manager (6 items): displayName "Product Manager" + roadmapTransfer / stakeholderIntros / activeInitiatives / metricsContext / researchInsights / vendorRelationships.

    Generic Consultant (6 items): displayName "Generic Consultant" + handoverDocs / activeProjectStatus / clientStakeholderHandover / deliverableArchive / knowledgeRepoIndex / contactDirectory.

    Override dialog (use UI-SPEC.md exact en strings): Offboarding.OverrideDialog.title / description / reasonLabel / reasonPlaceholder / reasonClientError / reasonServerError / acknowledgement / cta / ctaLoading / cancel / discardConfirm.title / discardConfirm.body / discardConfirm.confirm / discardConfirm.cancel.

    Override badge: Offboarding.OverrideBadge.label / tooltipReason / tooltipActor / tooltipBlockedTask.

    PTO admin badge: Offboarding.PtoBadge.label / tooltip.

    Locale fallback: Offboarding.LocaleFallback.suffix (" (English)") / srDescription.

    Settings.WorkflowRoles.{heading, subtitle, addCta, emptyHeading, emptyBody, copyFromEnglish, taskTitleLabel, taskDescriptionLabel, dueDayLabel, saveCta, savingCta}.

    Settings.PtoKeywords.{heading, subtitle, empty, addCta}.

    Workflow.Start.{heading, templateLabel, autoSelectHint, manualOverrideHint, cta}.

    Common.toasts.{saved, saveError} — only ADD if these keys do not already exist in en.json (grep first; if present, skip).

    **PL translations (planner authors, native-speaker quality, mirror tone of UI-SPEC.md examples):**
    - SoftwareEngineer.displayName: "Inżynier oprogramowania"
    - Designer.displayName: "Projektant"
    - ProductManager.displayName: "Product Manager"
    - GenericConsultant.displayName: "Konsultant"
    - PTO badge: "Brak zastępcy — manager na urlopie"
    - LocaleFallback.suffix: " (angielski)"
    - Use UI-SPEC.md examples for keys it lists; author the rest in matching tone (formal Polish, IT vocabulary).

    **DE translations (planner authors, native-speaker quality):**
    - SoftwareEngineer.displayName: "Software-Engineer"
    - Designer.displayName: "Designer"
    - ProductManager.displayName: "Product Manager"
    - GenericConsultant.displayName: "Berater"
    - PTO badge: "Kein Vertreter — Manager im Urlaub"
    - LocaleFallback.suffix: " (Englisch)"
    - DO NOT introduce Werkvertrag / Schöpferprinzip / Nutzungsrechte vocabulary — those are Phase 75 scope per CONTEXT.md SC#6 + Standing Constraints.

    **JSON insertion procedure:**
    1. Read en.json with `Read` (full file). Identify the alphabetical insertion point for new top-level keys `Offboarding`, `Settings.WorkflowRoles`, `Settings.PtoKeywords`, `Workflow.Start` (these may be sub-objects of existing `Settings` and `Workflow` namespaces — read existing structure first, NEST appropriately).
    2. Use `Edit` to insert each key block. Maintain valid JSON throughout (no trailing commas, double quotes, proper nesting).
    3. Repeat identically for pl.json and de.json with the translated values.
    4. Run `pnpm i18n:parity` from repo root. MUST exit 0. If it reports missing keys, fix before declaring complete.
  </action>
  <acceptance_criteria>
    - `cat apps/web/messages/en.json | jq -r '.Offboarding.Templates.SoftwareEngineer.displayName'` outputs `Software Engineer`
    - `cat apps/web/messages/pl.json | jq -r '.Offboarding.Templates.SoftwareEngineer.displayName'` outputs `Inżynier oprogramowania`
    - `cat apps/web/messages/de.json | jq -r '.Offboarding.Templates.SoftwareEngineer.displayName'` outputs `Software-Engineer`
    - `cat apps/web/messages/en.json | jq '.Offboarding.OverrideDialog.acknowledgement'` outputs a string containing both `intentionally bypassed` AND `compliance gap` (UI-SPEC canonical wording)
    - `cat apps/web/messages/en.json | jq '.Offboarding.OverrideBadge.label'` outputs `IP verification overridden`
    - `cat apps/web/messages/en.json | jq '.Offboarding.LocaleFallback.suffix'` outputs ` (English)` (with leading space)
    - `cat apps/web/messages/pl.json | jq '.Offboarding.LocaleFallback.suffix'` outputs ` (angielski)`
    - `cat apps/web/messages/de.json | jq '.Offboarding.LocaleFallback.suffix'` outputs ` (Englisch)`
    - For all 4 seed roles × all task items: `jq -r '.Offboarding.Templates.SoftwareEngineer.handoverDocs.title' apps/web/messages/{en,pl,de}.json` returns 3 non-empty strings
    - `cat apps/web/messages/ar.json | jq '.Offboarding'` outputs `null` (Arabic NOT extended this phase per CONTEXT.md D-13)
    - `grep -i "werkvertrag\|schöpferprinzip\|nutzungsrechte" apps/web/messages/de.json` returns no matches (Werkvertrag deferred to Phase 75)
    - `pnpm i18n:parity` exits 0
    - `cat apps/web/messages/en.json | jq -r 'keys' | grep -c "Offboarding"` outputs ≥ 1
    - `cat apps/web/messages/en.json | python3 -c "import json,sys; json.load(sys.stdin)"` exits 0 (valid JSON)
    - `cat apps/web/messages/pl.json | python3 -c "import json,sys; json.load(sys.stdin)"` exits 0
    - `cat apps/web/messages/de.json | python3 -c "import json,sys; json.load(sys.stdin)"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm i18n:parity</automated>
  </verify>
  <done>All 3 message catalogues extended with full Phase 74 keyspace; en/pl/de parity holds; no Arabic regression; no Werkvertrag wording leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source → CI i18n:parity guard | Phase 70 Plan 70-04 enforces base ⊂ peers; partial adds break CI |
| repo source → app runtime | next-intl reads messages/*.json; missing key throws at render time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-02-01 | Tampering | seed i18n key references | mitigate | Both source (seeds.ts string keys) and consumer (messages/*.json keys) lock into a `Offboarding.Templates.{Role}.{itemKey}.{title|description}` regex; the seeds.test.ts assertion in Task 1 verifies code side; pnpm i18n:parity verifies catalogue side |
| T-74-02-02 | Information Disclosure | de.json | mitigate | Werkvertrag-related copy is explicitly NOT shipped this phase (CONTEXT.md SC#6); acceptance grep for 'werkvertrag\|schöpferprinzip\|nutzungsrechte' returns zero matches; Phase 75 owns those terms with legal review annotation |
| T-74-02-03 | Repudiation | override dialog acknowledgement copy | mitigate | UI-SPEC.md canonical en wording locked verbatim ("I confirm IP verification is being intentionally bypassed and I accept responsibility for any compliance gap"); copy ships under "Needs verification by legal entity before production deploy" annotation in Plan 74-08 SUMMARY per LOCAL-ONLY Standing Constraint |
| T-74-02-04 | Denial of Service | i18n parity guard CI failure | mitigate | All three locales committed in the same task (Pitfall 3); jq + JSON parse acceptance criteria catch malformed JSON before commit |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/offboarding-templates test --run` exits 0 (Wave 0 scaffolds for seeds + pto-keywords turn GREEN)
- `pnpm i18n:parity` exits 0
- `grep -c "Werkvertrag\|Schöpferprinzip\|Nutzungsrechte" apps/web/messages/de.json` outputs 0
- `cat apps/web/messages/{en,pl,de}.json | python3 -c "import json,sys; [json.load(open(p)) for p in ['apps/web/messages/en.json','apps/web/messages/pl.json','apps/web/messages/de.json']]"` succeeds
- All 4 seed roles' displayName resolves in en/pl/de
- `pnpm lint:logs` exits 0
</verification>

<success_criteria>
4 KT seed templates exported as typed-const with 6-9 task items each; PTO_KEYWORDS exported with curated en/de/pl arrays; i18n:parity green across 3 locales for ALL Phase 74 surfaces (seeds, override dialog, badges, settings UI, locale fallback); zero Werkvertrag wording; zero Arabic regression.
</success_criteria>

<output>
After completion, create `.planning/phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-02-SUMMARY.md` recording: total i18n keys added per locale, grep proof of Werkvertrag absence, list of 4 seed roles + their task-item counts (must each be 6-9), and any deviation from RESEARCH.md § Code Examples drafted seeds (with rationale).
</output>
