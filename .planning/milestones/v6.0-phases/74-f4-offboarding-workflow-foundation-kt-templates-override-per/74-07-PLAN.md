---
phase: 74
plan: 07
type: execute
wave: 3
depends_on: [74-02, 74-04, 74-05, 74-06]
files_modified:
  - apps/web/src/app/[locale]/(dashboard)/admin/settings/workflow-roles/page.tsx
  - apps/web/src/app/[locale]/(dashboard)/admin/settings/workflow-roles/_components/role-template-list.tsx
  - apps/web/src/app/[locale]/(dashboard)/admin/settings/workflow-roles/_components/role-template-form.tsx
  - apps/web/src/app/[locale]/(dashboard)/admin/settings/workflow-roles/_components/copy-from-english-button.tsx
  - apps/web/src/app/[locale]/(dashboard)/admin/settings/workflow-roles/__tests__/page.test.tsx
  - apps/web/src/app/[locale]/(dashboard)/admin/settings/calendar-pto-keywords/page.tsx
  - apps/web/src/app/[locale]/(dashboard)/admin/settings/calendar-pto-keywords/__tests__/page.test.tsx
  - apps/web/src/app/[locale]/(dashboard)/admin/teams/[teamId]/_components/fallback-approver-select.tsx
  - apps/web/src/app/[locale]/(dashboard)/admin/users/[userId]/out-of-office/page.tsx
  - apps/web/src/components/offboarding/english-fallback-indicator.tsx
  - apps/web/src/components/offboarding/__tests__/english-fallback-indicator.test.tsx
  - apps/web/messages/en.json
  - apps/web/messages/pl.json
  - apps/web/messages/de.json
  - packages/api/src/routers/workflow-roles.ts
  - packages/api/src/routers/users.ts
  - packages/api/src/routers/teams.ts
autonomous: true
requirements: [OFFB-03, OFFB-11]
requirements_addressed: [OFFB-03, OFFB-11]

must_haves:
  truths:
    - "Settings > Workflow Roles page renders list of seed + ops templates; ops user can create / edit / delete (refusing isSeed=true) custom templates"
    - "Per-task-item form ships 3 inputs per locale (en/pl/de) with 'Copy from English' helper; Tab keyboard navigation works"
    - "(English) locale-fallback indicator renders muted suffix + hover-revealed Info icon + sr-only description per UI-SPEC D-15"
    - "Settings > Calendar PTO Keywords page allows ops to add/remove keywords per locale; mutation persists to a per-org store"
    - "Per-team Fallback Approver selector (Settings > Teams) and per-user Out-of-Office page both write the underlying schema columns"
    - "All Phase 74 settings surfaces have logical-property Tailwind classes only (ms-/me-/ps-/pe-) — no ml-/mr-/pl-/pr-"
    - "shadcn primitives ONLY (no third-party registries); all UI elements traced to apps/web/src/components/ui/"
  artifacts:
    - path: apps/web/src/app/[locale]/(dashboard)/admin/settings/workflow-roles/page.tsx
      provides: Settings > Workflow Roles list + form host page
      contains: "WorkflowRolesPage\\|workflow-roles"
      min_lines: 30
    - path: apps/web/src/app/[locale]/(dashboard)/admin/settings/workflow-roles/_components/role-template-form.tsx
      provides: 3-locale-input form with Copy from English helper
      contains: "Copy from English\\|copyFromEnglish"
    - path: apps/web/src/components/offboarding/english-fallback-indicator.tsx
      provides: D-15 muted (English) suffix + Info icon + sr-only description
      contains: "EnglishFallbackIndicator"
    - path: apps/web/src/app/[locale]/(dashboard)/admin/settings/calendar-pto-keywords/page.tsx
      provides: PTO keywords ops-extension UI
      contains: "PtoKeywords\\|pto-keywords"
  key_links:
    - from: apps/web/src/app/[locale]/(dashboard)/admin/settings/workflow-roles/page.tsx
      to: packages/api/src/routers/workflow-roles.ts
      via: trpc.workflowRoles.{list,create,update,delete}
      pattern: 'trpc\.workflowRoles\.'
    - from: apps/web/src/components/offboarding/english-fallback-indicator.tsx
      to: apps/web/messages/{en,pl,de}.json (Offboarding.LocaleFallback.*)
      via: useTranslations('Offboarding.LocaleFallback')
      pattern: 'Offboarding\.LocaleFallback'
    - from: apps/web/src/app/[locale]/(dashboard)/admin/users/[userId]/out-of-office/page.tsx
      to: packages/api/src/routers/users.ts (setOutOfOffice mutation)
      via: User.outOfOffice JSONB write
      pattern: 'setOutOfOffice'
---

<objective>
Wave 3 — Ship the four Settings UI surfaces that admins use to manage Phase 74's offboarding configuration: (1) Settings > Workflow Roles (list + 3-locale-input CRUD form + Copy from English helper + (English) fallback indicator on rows with locale gaps), (2) Settings > Calendar PTO Keywords (add/remove ops-extension keywords per locale), (3) per-team Fallback Approver selector embedded into the Teams settings page, (4) per-user Out-of-Office page (manual OOO setting with from/until/fallbackUserId).

Purpose: SC#3's "ops extends role taxonomy without engineering involvement" means ops needs a working UI. SC#2's PTO-aware routing only works if admins can configure team-level fallback + manager-level OOO. SC#6's en/pl/de parity holds because every new string is in messages/*.json (Plan 74-02 added the keys; this plan consumes them).

Output: 1 host page per surface + form components + RTL tests + 2 supporting tRPC mutations (`users.setOutOfOffice`, `teams.setFallbackApprover`) + the (English) indicator component.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-CONTEXT.md
@.planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-RESEARCH.md
@.planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-UI-SPEC.md
@apps/web/src/components/admin/boe-rate/boe-rate-table.tsx
@apps/web/src/components/admin/boe-rate/add-boe-rate-dialog.tsx
@apps/web/src/components/ui/dialog.tsx
@apps/web/src/components/ui/tabs.tsx
@apps/web/src/components/ui/badge.tsx
@apps/web/src/components/ui/tooltip.tsx
@apps/web/messages/en.json
@packages/api/src/routers/workflow-roles.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Settings > Workflow Roles page + role-template-form (3-locale tabs + Copy from English) + (English) indicator component</name>
  <files>apps/web/src/app/[locale]/(dashboard)/admin/settings/workflow-roles/page.tsx, apps/web/src/app/[locale]/(dashboard)/admin/settings/workflow-roles/_components/role-template-list.tsx, apps/web/src/app/[locale]/(dashboard)/admin/settings/workflow-roles/_components/role-template-form.tsx, apps/web/src/app/[locale]/(dashboard)/admin/settings/workflow-roles/_components/copy-from-english-button.tsx, apps/web/src/app/[locale]/(dashboard)/admin/settings/workflow-roles/__tests__/page.test.tsx, apps/web/src/components/offboarding/english-fallback-indicator.tsx, apps/web/src/components/offboarding/__tests__/english-fallback-indicator.test.tsx</files>
  <behavior>
    - page.tsx: server component fetches `trpc.workflowRoles.list`; renders RoleTemplateList client component
    - role-template-list.tsx: client component; per-row Edit / Delete actions; opens role-template-form.tsx Dialog on Edit / Add
    - role-template-form.tsx: shadcn Dialog with shadcn Tabs (en / pl / de); each tab has displayName input + per-task-item titleEn/Pl/De + descriptionEn/Pl/De + dueDayOffset; "Copy from English" button copies en → pl + de (disabled when titleEn empty)
    - delete confirmation via shadcn AlertDialog; refuses isSeed=true rows (display "Built-in template — cannot be deleted")
    - english-fallback-indicator.tsx: renders muted " (English)" suffix (always visible) + hover-revealed Info icon (lucide) + sr-only span with `aria-describedby` linking to "This template field has not been translated to {targetLocale}; the English value is shown as a fallback."
    - tests: 4 cases per RTL — (a) renders 3-locale form with Tabs, (b) Copy-from-English populates pl + de, (c) (English) indicator visible on missing-locale rows, (d) no AR tab rendered
  </behavior>
  <read_first>
    - apps/web/src/components/admin/boe-rate/ (entire directory — observe the existing CRUD list + add-dialog pattern; mirror)
    - apps/web/src/components/ui/tabs.tsx (Tabs primitive shape)
    - apps/web/src/components/ui/dialog.tsx (Dialog primitive shape)
    - apps/web/src/components/ui/alert-dialog.tsx (AlertDialog for delete confirm)
    - apps/web/src/components/ui/tooltip.tsx (Tooltip primitive for Info icon)
    - .planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-UI-SPEC.md § Component Inventory > Settings > Workflow Roles list + edit form (lines ~286-301), § Accessibility (lines ~534-545), § (English) locale-fallback indicator (lines ~565-572)
    - apps/web/src/app/[locale]/(dashboard)/ (PLACEMENT ANALOG — admin pages live as a non-grouped `admin/` segment INSIDE the `(dashboard)` route group; no new `(admin)` group is created. Inherit i18n + auth gating from `(dashboard)/layout.tsx`)
    - apps/web/src/app/[locale]/(dashboard)/admin/ (any existing settings page — mirror server-component + client-island split)
    - apps/web/messages/en.json (verify Settings.WorkflowRoles.* keys from Plan 74-02 are present)
  </read_first>
  <action>
    1. **Page host** (`page.tsx`) — server component with `'use server'` semantics. Fetches initial data via `trpc.workflowRoles.list` and passes to client component. Renders `<h1>{t('Settings.WorkflowRoles.heading')}</h1>` + `<p>{t('Settings.WorkflowRoles.subtitle')}</p>` + `<RoleTemplateList initialTemplates={data}/>`. Use logical properties for spacing.

    2. **role-template-list.tsx** — client component. State: `[selectedForEdit, setSelectedForEdit] = useState<Template | null>(null)`. Renders Card per template (mirror boe-rate-table pattern) with Edit + Delete buttons. Edit opens `<RoleTemplateForm template={selectedForEdit} onClose={...} />` Dialog. Delete opens AlertDialog. For isSeed rows: display Edit + Delete buttons but Delete is disabled with tooltip "Built-in template — cannot be deleted". Empty state with Settings.WorkflowRoles.emptyHeading + emptyBody copy. Add CTA button: opens form Dialog with template=null (creates new).

    3. **role-template-form.tsx** — client component. Form state via react-hook-form (use existing repo convention). Layout:
       - shadcn Tabs with 3 triggers: en, pl, de (NEVER ar — UI-SPEC.md confirms project's components.json declares rtl:false; Phase 79 owns AR for Gulf only)
       - Each tab content: `<Label>` + `<Input>` for displayName + per-task-item array (variable count). Each task item has 3 sub-tabs (or just 3 stacked inputs) for title/description in the active locale plus one shared dueDayOffset Input type=number.
       - "Copy from English" button: positioned ABOVE pl/de input groups; click handler copies titleEn → titlePl + titleDe and descriptionEn → descriptionPl + descriptionDe in one operation. Disabled when titleEn is empty (use react-hook-form watch).
       - Save button calls `trpc.workflowRoles.create.useMutation()` or `update`. Uses Settings.WorkflowRoles.saveCta / savingCta keys.
       - On submit: optimistic close + Sonner toast Common.toasts.saved; on error inline error per react-hook-form Zod resolver shape.
       - Logical-property Tailwind throughout.

    4. **copy-from-english-button.tsx** — extracted button component. Receives `form` (react-hook-form instance) and `taskIndex` props. On click: `form.setValue('taskItems[i].titlePl', form.getValues('taskItems[i].titleEn'))` etc. Disabled state via `form.watch('taskItems[i].titleEn')?.length > 0 ? false : true`. ARIA: `aria-describedby` to a hidden span explaining the action, per UI-SPEC.md § Accessibility row.

    5. **english-fallback-indicator.tsx** (in `apps/web/src/components/offboarding/`):
       ```tsx
       'use client';
       import { useTranslations } from 'next-intl';
       import { Info } from 'lucide-react';
       import { Tooltip, TooltipTrigger, TooltipContent } from '@/components/ui/tooltip';

       export function EnglishFallbackIndicator({ targetLocale, taskId }: { targetLocale: string; taskId: string }) {
         const t = useTranslations('Offboarding.LocaleFallback');
         const descId = `locale-fallback-${taskId}`;
         return (
           <span className="inline-flex items-center gap-1 group" aria-describedby={descId}>
             <span className="text-xs text-muted-foreground ms-1">{t('suffix')}</span>
             <Tooltip>
               <TooltipTrigger asChild>
                 <Info size={12} className="opacity-0 group-hover:opacity-100 group-focus-visible:opacity-100 text-info" aria-hidden="true" />
               </TooltipTrigger>
               <TooltipContent>{t('srDescription', { targetLocale })}</TooltipContent>
             </Tooltip>
             <span className="sr-only" id={descId}>{t('srDescription', { targetLocale })}</span>
           </span>
         );
       }
       ```

    6. **Tests:**
       - `apps/web/src/app/[locale]/(dashboard)/admin/settings/workflow-roles/__tests__/page.test.tsx` (Wave 0 scaffold) — replace .todo with assertions: renders Tabs with en/pl/de triggers (and assert no `ar` text trigger via `screen.queryByText('ar')` returning null); Copy-from-English populates pl + de fields; (English) indicator visible on rows with empty pl/de; isSeed rows show disabled Delete with tooltip.
       - `apps/web/src/components/offboarding/__tests__/english-fallback-indicator.test.tsx` — new test file. Assert: visible " (English)" suffix; hidden Info icon revealed on hover/focus; sr-only description present with referenced aria-describedby.

    7. Run `pnpm --filter web test --run -- workflow-roles english-fallback-indicator`.
  </action>
  <acceptance_criteria>
    - `find apps/web/src/app -path "*workflow-roles*" -name "*.tsx" | wc -l` returns ≥ 4 (page + 3 components)
    - `grep -c "ms-\\|me-\\|ps-\\|pe-" apps/web/src/app/\\[locale\\]/\\(dashboard\\)/admin/settings/workflow-roles/_components/role-template-form.tsx` outputs ≥ 1 (logical-property usage)
    - `grep -c "\\bml-\\|\\bmr-\\|\\bpl-\\|\\bpr-" apps/web/src/app/\\[locale\\]/\\(dashboard\\)/admin/settings/workflow-roles/_components/role-template-form.tsx` outputs `0` (no physical margins/paddings)
    - `grep -c "Tabs\\|TabsList\\|TabsTrigger\\|TabsContent" apps/web/src/app/\\[locale\\]/\\(dashboard\\)/admin/settings/workflow-roles/_components/role-template-form.tsx` outputs ≥ 4
    - `grep -ic "TabsTrigger value=\"ar\"\\|TabsTrigger value='ar'" apps/web/src/app/\\[locale\\]/\\(dashboard\\)/admin/settings/workflow-roles/_components/role-template-form.tsx` outputs `0` (NO Arabic tab)
    - `grep -c "EnglishFallbackIndicator" apps/web/src/components/offboarding/english-fallback-indicator.tsx` outputs ≥ 1
    - `grep -c "useTranslations.*Offboarding.LocaleFallback" apps/web/src/components/offboarding/english-fallback-indicator.tsx` outputs ≥ 1
    - `grep -c "console\\." apps/web/src/app/\\[locale\\]/\\(dashboard\\)/admin/settings/workflow-roles/_components/*.tsx` outputs `0`
    - `pnpm --filter web test --run -- workflow-roles` exits 0
    - `pnpm --filter web test --run -- english-fallback-indicator` exits 0
    - `pnpm --filter web typecheck` exits 0
    - `pnpm i18n:parity` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web test --run -- workflow-roles english-fallback-indicator</automated>
  </verify>
  <done>Settings > Workflow Roles UI live; ops can CRUD ops templates; isSeed rows protected; (English) indicator renders per UI-SPEC; en/pl/de parity holds; no AR; no third-party registries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Settings > Calendar PTO Keywords + per-team Fallback Approver + per-user OOO surfaces</name>
  <files>apps/web/src/app/[locale]/(dashboard)/admin/settings/calendar-pto-keywords/page.tsx, apps/web/src/app/[locale]/(dashboard)/admin/settings/calendar-pto-keywords/__tests__/page.test.tsx, apps/web/src/app/[locale]/(dashboard)/admin/teams/[teamId]/_components/fallback-approver-select.tsx, apps/web/src/app/[locale]/(dashboard)/admin/users/[userId]/out-of-office/page.tsx, packages/api/src/routers/workflow-roles.ts, packages/api/src/routers/users.ts, packages/api/src/routers/teams.ts</files>
  <behavior>
    - PTO Keywords page: per-locale tabs (en/pl/de) → outline Badge chips with X-to-remove + Add input row; persists ops-extension keywords to a per-org store (new model: PtoKeywordOverride or JSONB on Organization — pick the simpler shape)
    - Fallback Approver select: dropdown of users in the team's org; on change, calls trpc.teams.setFallbackApprover; reflected in Settings > Teams view
    - OOO page: form with from/until DatePicker (from existing repo if available; else native input type=date) + fallbackUserId Select; on submit, calls trpc.users.setOutOfOffice writing User.outOfOffice JSONB
    - Server-side (in workflow-roles.ts or new router): Zod-validated mutations for setOutOfOffice + setFallbackApprover + extendPtoKeyword + removePtoKeyword
    - All mutations: tenant-scoped + actor identity from session (T-74-05 mitigation continues)
  </behavior>
  <read_first>
    - apps/web/src/app/[locale]/(dashboard)/admin/ (existing settings pages — mirror)
    - apps/web/src/components/ui/alert.tsx (info-card analog pattern for the PTO keywords deferral notice — preferred over disabled add input)
    - apps/web/src/components/ui/card.tsx (fallback container if Alert variant doesn't fit the visual)
    - packages/api/src/routers/users.ts (if exists; otherwise read Member router that handles user-scoped mutations) — observe protectedProcedure conventions
    - packages/api/src/routers/teams.ts (if exists; or a settings/teams router)
    - packages/api/src/routers/workflow-roles.ts (Plan 74-05; this plan extends it OR sits in a sibling)
    - .planning/milestones/v6.0-phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-UI-SPEC.md § Settings > Calendar PTO Keywords editor (lines ~325-333)
  </read_first>
  <action>
    1. **PTO_KEYWORDS extensibility surface (read-only with honest deferral notice):** Phase 74 ships the 3-locale Tabs + the 5+2+2=9 typed-const PTO_KEYWORDS as outline Badge chips. The keyword set is extensible by engineering via the typed-const in `@contractor-ops/offboarding-templates` for now. **Do NOT render a disabled Add input** — a non-functional input field looks broken on first encounter and contradicts D-08 / SC#3 spirit by training ops users to expect "Add" interaction that does nothing. Instead, render a compact info card BELOW the chips that explains:
       - The keyword list is curated by engineering and ships via code
       - To request a custom keyword, contact engineering (static "contact engineering for now" empty-state copy is sufficient — no follow-up tracking issue convention exists yet)
       - Persistence layer (per-org `PtoKeywordOverride` table or `Organization.ptoKeywordOverrides` JSONB) is deferred to a follow-up phase

       i18n keys (Plan 74-02 added these; verify presence in messages/*.json):
       - `Settings.PtoKeywords.extensibilityNotice.title` ("These keywords are managed by engineering")
       - `Settings.PtoKeywords.extensibilityNotice.body` ("To add a custom PTO keyword for your organization, contact engineering. Self-service editing ships in a future release.")

    2. **Calendar PTO Keywords page** — render Tabs of typed-const Badge chips + info-card extensibility notice, NO disabled Add input anywhere:
       ```tsx
       // apps/web/src/app/[locale]/(dashboard)/admin/settings/calendar-pto-keywords/page.tsx
       'use client';
       import { useTranslations } from 'next-intl';
       import { PTO_KEYWORDS } from '@contractor-ops/offboarding-templates';
       import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/components/ui/tabs';
       import { Badge } from '@/components/ui/badge';
       import { Alert, AlertTitle, AlertDescription } from '@/components/ui/alert';
       import { Info } from 'lucide-react';

       export default function PtoKeywordsPage() {
         const t = useTranslations('Settings.PtoKeywords');
         return (
           <div className="px-6 py-12">
             <h1 className="text-2xl font-bold">{t('heading')}</h1>
             <p className="text-base text-muted-foreground">{t('subtitle')}</p>
             <Tabs defaultValue="en" className="mt-6">
               <TabsList>
                 <TabsTrigger value="en">EN</TabsTrigger>
                 <TabsTrigger value="pl">PL</TabsTrigger>
                 <TabsTrigger value="de">DE</TabsTrigger>
               </TabsList>
               {(['en', 'pl', 'de'] as const).map(locale => (
                 <TabsContent key={locale} value={locale} className="mt-4 space-y-2">
                   <div className="flex flex-wrap gap-2">
                     {PTO_KEYWORDS[locale].map(kw => (
                       <Badge key={kw} variant="outline">{kw}</Badge>
                     ))}
                   </div>
                 </TabsContent>
               ))}
             </Tabs>

             {/* Extensibility notice — replaces a disabled Add input.
                 Renders ONCE below the tabs; communicates that the keyword set
                 is code-managed today and self-service editing is deferred. */}
             <Alert className="mt-6" role="status">
               <Info className="h-4 w-4" aria-hidden="true" />
               <AlertTitle>{t('extensibilityNotice.title')}</AlertTitle>
               <AlertDescription>{t('extensibilityNotice.body')}</AlertDescription>
             </Alert>
           </div>
         );
       }
       ```

       **Hard rule:** No `<Input>` / `<input>` / `<Button disabled>` / `<Badge>` with X-to-remove anywhere on this page. The page is a transparent read-only view + an honest extensibility notice. If the executor finds themselves writing `disabled={true}` on a form control, they have misread this section.

    3. **Fallback Approver select:**
       - `apps/web/src/app/[locale]/(dashboard)/admin/teams/[teamId]/_components/fallback-approver-select.tsx` — client component with shadcn Select. Fetches team members via existing trpc query. On change, calls `trpc.teams.setFallbackApprover.useMutation()`.
       - Add `setFallbackApprover` mutation to `packages/api/src/routers/teams.ts` (or create the router if missing). Zod input: `{ teamId: string, fallbackApproverId: string | null }`. requirePermission: `member: ['update']` or owner-only — match existing teams-router permission shape. Server-side: `prisma.team.update({ where: { id, organizationId }, data: { fallbackApproverId } })`.

    4. **Per-user OOO page:**
       - `apps/web/src/app/[locale]/(dashboard)/admin/users/[userId]/out-of-office/page.tsx` — form with from / until / fallbackUserId fields. Uses Zod schema: `{ from: z.string().date(), until: z.string().date(), fallbackUserId: z.string().min(1).optional() }`.
       - Add `setOutOfOffice` mutation to `packages/api/src/routers/users.ts`. Zod validates ISO-date strings. Server-side: `prisma.user.update({ where: { id }, data: { outOfOffice: { from, until, fallbackUserId } } })`. Permission: actor must be userId themselves OR have `member: ['update']`.

    5. **Tests:**
       - `apps/web/src/app/[locale]/(dashboard)/admin/settings/calendar-pto-keywords/__tests__/page.test.tsx` — assert 3 Tab triggers, assert each tab shows the typed-const keywords as `Badge variant="outline"`, assert the extensibility-notice `Alert` is rendered with the expected i18n title + body, assert NO `<input>` / `<button disabled>` / X-to-remove control is in the document (`screen.queryByRole('textbox')` returns null; `screen.queryAllByRole('button', { name: /add|remove/i })` returns []`).
       - Existing test files for teams + users (if any) extend with the new mutation cases. If no test files exist yet, create `packages/api/src/routers/__tests__/teams-fallback-approver.test.ts` and `users-out-of-office.test.ts` with integration tests (mock prisma, call mutation, assert update). 4-6 cases total.

    6. Run all tests + typecheck + i18n:parity.
  </action>
  <acceptance_criteria>
    - `find apps/web/src/app -path "*calendar-pto-keywords*" -name "*.tsx" | wc -l` returns ≥ 1
    - `find apps/web/src/app -path "*out-of-office*" -name "*.tsx" | wc -l` returns ≥ 1
    - `grep -c "fallbackApproverId" apps/web/src/app/\\[locale\\]/\\(dashboard\\)/admin/teams | head -5` returns ≥ 1 line containing the new selector
    - `grep -c "setOutOfOffice" packages/api/src/routers/users.ts` outputs ≥ 1
    - `grep -c "setFallbackApprover" packages/api/src/routers/teams.ts` outputs ≥ 1
    - `grep -c "ar" apps/web/src/app/\\[locale\\]/\\(dashboard\\)/admin/settings/calendar-pto-keywords/page.tsx` outputs `0` (or only finds `ar` substrings inside other words — verify manually if false positive)
    - `grep -c "console\\." apps/web/src/app/\\[locale\\]/\\(dashboard\\)/admin/settings/calendar-pto-keywords/page.tsx apps/web/src/app/\\[locale\\]/\\(dashboard\\)/admin/users/\\[userId\\]/out-of-office/page.tsx` outputs `0`
    - `grep -cE "<Input|<input|disabled" apps/web/src/app/\\[locale\\]/\\(dashboard\\)/admin/settings/calendar-pto-keywords/page.tsx` outputs `0` (NO disabled-but-visible Add input)
    - `grep -c "Alert" apps/web/src/app/\\[locale\\]/\\(dashboard\\)/admin/settings/calendar-pto-keywords/page.tsx` outputs ≥ 1 (info card present)
    - `grep -c "extensibilityNotice" apps/web/src/app/\\[locale\\]/\\(dashboard\\)/admin/settings/calendar-pto-keywords/page.tsx` outputs ≥ 1
    - `pnpm --filter web test --run -- calendar-pto-keywords` exits 0
    - `pnpm --filter @contractor-ops/api test --run` exits 0 (existing tests + new teams + users tests)
    - `pnpm --filter web typecheck` exits 0
    - `pnpm i18n:parity` exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter web test --run -- calendar-pto-keywords && pnpm --filter @contractor-ops/api test --run</automated>
  </verify>
  <done>3 settings surfaces live; PTO Keywords UI renders typed-const Badge chips + extensibility-notice info card (NO disabled Add input); per-team Fallback Approver and per-user OOO mutations Zod-validated and tenant-scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → tRPC mutations (setOutOfOffice / setFallbackApprover / role-template CRUD) | Zod-validated; actor identity from session; tenant scoping |
| client → next-intl message catalogue | Static at build time (Plan 74-02); runtime data merged via i18n keys |
| ops-extension UI → DB | Phase 74 ships read-only keyword UI; persistence deferred to Phase 74.1 (documented, not silent gap) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-07-empty-locale-render | Information Disclosure (UX) | Ops-added template with empty pl/de fields | mitigate | (English) fallback indicator (D-15) renders muted suffix + Info icon + sr-only description per UI-SPEC.md § A11y; never silent fallback |
| T-74-07-cross-tenant-template-write | Tampering | trpc.workflowRoles.update / .delete | mitigate | tenantProcedure scopes ctx.organizationId; Plan 74-05 acceptance test asserts cross-tenant write returns NOT_FOUND |
| T-74-07-isSeed-mutation | Tampering | trpc.workflowRoles.update / .delete on isSeed=true rows | mitigate | Plan 74-05 server FORBIDDEN guard; UI disables Delete with tooltip "Built-in template — cannot be deleted" |
| T-74-07-arabic-leak | Information Disclosure | Tabs / message keys | mitigate | UI-SPEC.md mandate: NO `ar` TabTrigger; acceptance grep asserts `TabsTrigger value="ar"` count = 0; messages/ar.json never extended |
| T-74-07-rtl-physical-margin | Tampering (UX) | Tailwind classes on Phase 74 components | mitigate | UI-SPEC.md § RTL Safety mandates ms-/me-/ps-/pe- only; acceptance grep asserts `\bml-|\bmr-|\bpl-|\bpr-` count = 0 |
| T-74-07-third-party-registry | Spoofing / Supply Chain | shadcn / base-ui primitives | mitigate | UI-SPEC.md § Component Inventory: zero third-party registries declared; primitives traced to apps/web/src/components/ui/; no `npx shadcn add @<external>` invocations |
| T-74-07-keyword-persistence-deferred | Information Disclosure (UX) | Calendar PTO Keywords extensibility-notice info card | accept | Persistence layer deferred to a follow-up phase; UI displays an `Alert` info card explaining the keyword set is engineering-managed today + how to request additions. NO disabled-but-visible Add input is rendered (avoids fake-functional UX). Acceptance grep asserts absence of `<Input>`/`disabled` attributes on this page. Mitigation: info card is honest about the deferral |
| T-74-07-ooo-self-write-only | Tampering | setOutOfOffice mutation | mitigate | requirePermission: actor === target user OR has member:['update']; Zod validates ISO date strings (no JS Date hacks) |
| T-74-07-fk-validation | Integrity violation | Team.fallbackApproverId + User.outOfOffice.fallbackUserId | mitigate | Both write paths validate the referenced user exists in the same org (Prisma FK constraint + tenant filter); cross-org reference rejected at FK level |
</threat_model>

<verification>
- `pnpm --filter web test --run` exits 0
- `pnpm --filter @contractor-ops/api test --run` exits 0
- `pnpm --filter web typecheck` exits 0
- `pnpm --filter @contractor-ops/api typecheck` exits 0
- `pnpm i18n:parity` exits 0
- `pnpm lint:logs` exits 0
- `grep -rn "ar:.*['\"]" apps/web/messages/ar.json | grep -c "Offboarding\\|Settings.WorkflowRoles\\|Workflow.Start"` outputs `0`
- `grep -rn "TabsTrigger value=[\"']ar[\"']" apps/web/src/app/\\[locale\\]/\\(dashboard\\)/admin/settings/` outputs no matches
</verification>

<success_criteria>
4 settings UI surfaces shipped (Workflow Roles CRUD, Calendar PTO Keywords read-only with 74.1 follow-up annotation, per-team Fallback Approver, per-user OOO); (English) locale-fallback indicator renders per UI-SPEC; en/pl/de parity holds; zero AR; logical-property classes throughout; only shadcn primitives.
</success_criteria>

<output>
After completion, create `.planning/phases/74-f4-offboarding-workflow-foundation-kt-templates-override-per/74-07-SUMMARY.md` recording:
- 4 surfaces shipped + their file paths
- Phase 74.1 follow-up: ops-extensible PTO_KEYWORDS persistence (new `PtoKeywordOverride` table or `Organization.ptoKeywordOverrides` JSONB column + matching CRUD + UI add/remove handlers)
- Total RTL test count
- Confirmation of zero third-party registries
- Logical-property class enforcement note for downstream Phase 79 ESLint guard
</output>
