---
phase: 72
plan: 07
type: execute
wave: 3
depends_on: [72-01, 72-04]
files_modified:
  - apps/web-vite/src/components/payments/payment-block-modal.tsx
  - apps/web-vite/src/components/payments/__tests__/payment-block-modal.test.tsx
  - apps/web-vite/src/components/payments/hooks/use-payment-run-step-review.ts
  - apps/web-vite/src/components/payments/new-payment-run-dialog/step-review-container.tsx
  - apps/web-vite/messages/en.json
  - apps/web-vite/messages/de.json
  - apps/web-vite/messages/pl.json
  - apps/web-vite/messages/ar.json
autonomous: true
requirements: [COMPL-05]
requirements_addressed: [COMPL-05]

must_haves:
  truths:
    - "`apps/web-vite/src/components/payments/payment-block-modal.tsx` exports `PaymentBlockModal` (presentational component, props in / JSX out) rendering one collapsible section per contractor with deep-link list of expired items per D-10"
    - "Modal uses `Dialog`/`DialogBody`/`DialogFooter` from `@contractor-ops/ui/components/shadcn/dialog` (content in DialogBody, actions in sticky DialogFooter per web-vite Dialog convention) + `Accordion` + `Alert` from `@contractor-ops/ui/components/shadcn/*`"
    - "Each item link uses `Link` from `apps/web-vite/src/i18n/navigation.js` (locale-aware TanStack Router wrapper) with `href = deepLinkPath` from `cause.contractorReasons[].reasons[].deepLinkPath` (e.g., `/contractors/{id}/compliance#item-{itemId}`); NOT next/link"
    - "i18n: title, body, document-type labels resolve via the custom `useTranslations` hook from `apps/web-vite/src/i18n/useTranslations.js` under a new `Compliance` namespace; the 4 flat message files (`apps/web-vite/messages/{en,de,pl,ar}.json`) gain `Compliance.paymentBlockModal.{title,subtitle,expiredOn,close,...}` plus `Compliance.documentType.<key>` entries for every Phase 71 `policyRuleId` and `DocumentType` enum value"
    - "The wizard's mutation hook `apps/web-vite/src/components/payments/hooks/use-payment-run-step-review.ts` catches tRPC `PRECONDITION_FAILED` from `payment.create` and `payment.lockAndExport`, and surfaces the `cause.contractorReasons` payload to the container/step so it opens `PaymentBlockModal` (Page→Container→Hook→Component: the hook owns the tRPC boundary, the container/step renders the modal)"
    - "Modal handles empty `contractorReasons[]` gracefully — shows generic 'No specific reasons' fallback (defensive — should not occur in practice but defends against malformed payloads)"
    - "All tests in `apps/web-vite/src/components/payments/__tests__/payment-block-modal.test.tsx` GREEN"
    - "Accessibility: modal has proper `aria-labelledby` linking title; keyboard-navigable accordion; meets WCAG AA contrast per project memory; RTL verified for `ar` locale"
    - "If the flag-OFF (would_block) path is hit, the step renders a non-blocking `Alert` warning (NOT the modal) with the same payload structure — pin the soft-warn variant"
  artifacts:
    - path: "apps/web-vite/src/components/payments/payment-block-modal.tsx"
      provides: "Payment-block modal — collapsible per-contractor reasons + deep links"
      contains: "PaymentBlockModal"
    - path: "apps/web-vite/messages/en.json"
      provides: "English i18n keys for compliance modal + document type labels (Compliance namespace)"
      contains: "paymentBlockModal"
    - path: "apps/web-vite/messages/de.json"
      provides: "German i18n keys (Compliance namespace)"
      contains: "paymentBlockModal"
    - path: "apps/web-vite/messages/pl.json"
      provides: "Polish i18n keys (Compliance namespace)"
      contains: "paymentBlockModal"
    - path: "apps/web-vite/messages/ar.json"
      provides: "Arabic (RTL) i18n keys (Compliance namespace)"
      contains: "paymentBlockModal"
  key_links:
    - from: "apps/web-vite/src/components/payments/payment-block-modal.tsx"
      to: "apps/web-vite/src/components/contractors/classification/drv-clearance/drv-clearance-form.tsx"
      via: "structured-error display + deep link pattern"
      pattern: "Accordion + Link"
    - from: "apps/web-vite/src/components/payments/payment-block-modal.tsx"
      to: "apps/web-vite/src/components/payments/new-payment-run-dialog/step-review.tsx"
      via: "web-vite Dialog convention (DialogBody + sticky DialogFooter)"
      pattern: "DialogBody + DialogFooter"
---

<objective>
Build the user-facing block modal (web-vite SPA) that surfaces the structured `PRECONDITION_FAILED.cause.contractorReasons` payload (D-10) at the payment-write entry points. Wire it into the new-payment-run dialog flow following the web-vite Page→Container→Hook→Component architecture: the tRPC mutations (`payment.create`, `payment.lockAndExport`) live in `use-payment-run-step-review.ts`; that hook catches `PRECONDITION_FAILED` and exposes the `contractorReasons` to the step/container which renders `PaymentBlockModal`. Ship i18n keys under a new `Compliance` namespace in the 4 flat locale files (en/de/pl with stub copy + ar RTL; Phase 73 owns full parity per CONTEXT.md).

After this plan: COMPL-05 admin UX is complete — admins see a clear, accessible modal listing exactly which contractor + which document blocks the run, with deep links into the affected item.

**UI workflow:** This is a web-vite UI plan — the executor MUST read the `frontend-design` skill (`.claude/skills/frontend-design/SKILL.md`) and follow `apps/web-vite/ARCHITECTURE.md` (Page→Container→Hook→Component) before editing any UI file.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-f1-compliance-reminder-cascade-payment-block/72-CONTEXT.md
@.planning/phases/72-f1-compliance-reminder-cascade-payment-block/72-RESEARCH.md
@.planning/phases/72-f1-compliance-reminder-cascade-payment-block/72-PATTERNS.md
@apps/web-vite/src/components/contractors/classification/drv-clearance/drv-clearance-form.tsx
@apps/web-vite/src/components/payments/new-payment-run-dialog/step-review.tsx
@apps/web-vite/src/components/payments/new-payment-run-dialog/step-review-container.tsx
@apps/web-vite/src/components/payments/hooks/use-payment-run-step-review.ts
@apps/web-vite/src/i18n/useTranslations.ts
@apps/web-vite/src/i18n/navigation.tsx
@packages/ui/src/components/shadcn/dialog.tsx
@packages/api/src/services/compliance-payment-gate.ts
</context>

<threat_model>
| ID | Threat | Mitigation |
|----|--------|-----------|
| T-72-07-01 | XSS via `contractorReasons[].contractorName` if backend writes unsanitised HTML to a contractor name field | React escapes string interpolation by default; we render `{c.contractorName}` not `dangerouslySetInnerHTML`. The backend further sanitises contractor names at write time (existing pattern) |
| T-72-07-02 | Deep link `deepLinkPath` could be an absolute URL crafted by an attacker via DB injection — phishing risk | The backend constructs `deepLinkPath` from a fixed template `/contractors/{id}/compliance#item-{itemId}` server-side. No external URLs accepted. The locale-aware `Link` (from `apps/web-vite/src/i18n/navigation.tsx`, a TanStack Router wrapper) treats `href` as an internal path |
| T-72-07-03 | i18n key fallback fails for missing keys — modal shows raw key "Compliance.paymentBlockModal.title" | The custom `useTranslations` (`apps/web-vite/src/i18n/useTranslations.ts`) resolves keys against the flat locale bundle; Plan 72-07 ships en/de/pl/ar entries for every key used. Missing translations surface in the repo's i18n-parity lint guard. Phase 73 owns full copy parity |
| T-72-07-04 | Modal opens on every block but does not auto-focus the first action — keyboard users get stuck | shadcn `Dialog` auto-traps focus on the close button; the accordion items are tab-navigable. Test asserts `screen.getByRole('button', { name: /close/i })` is keyboard-reachable |
| T-72-07-05 | Long contractor names overflow the modal — visual breakage | CSS uses `truncate` on the AccordionTrigger label with full name in `title` attr (tooltip on hover) — existing pattern |
| T-72-07-06 | The wizard catches PRECONDITION_FAILED but the cause payload is malformed — modal crashes on undefined fields | Modal defensively handles missing fields with `??` fallbacks; test case asserts `contractorReasons={[]}` renders the empty-state path without crashing |
| T-72-07-07 | The flag-OFF would-block path renders the modal incorrectly (it should be a soft Alert, not a blocking Dialog) | The wizard distinguishes blocking vs would-block at the call site: PRECONDITION_FAILED → modal; success-with-warning property in the response → Alert. Two display modes; same payload shape |
</threat_model>

<tasks>

<task id="72-07-01">
<action>
Create the presentational component `apps/web-vite/src/components/payments/payment-block-modal.tsx` (props in / JSX out — no tRPC, per web-vite ARCHITECTURE.md). Follow the existing web-vite payment-dialog conventions (read the analogs first):

- This is a Vite SPA — NO `'use client'`, NO `next-intl`, NO `next/link`.
- i18n: `import { useTranslations } from '../../i18n/useTranslations.js'`; `const t = useTranslations('Compliance')`. Keys are namespace-relative, e.g. `t('paymentBlockModal.title')`, `t('paymentBlockModal.subtitle', { contractorCount, itemCount })`. The document-type label uses the relative key `t(`documentType.${labelKeyTail}`)` — note the server sends `documentTypeLabelKey` as `compliance.documentType.<x>`; either (a) strip the leading `compliance.` and resolve under the `Compliance` namespace, or (b) build the label key client-side from `policyRuleId`/`documentType`. Pin the chosen approach against `useTranslations.ts` semantics (namespace prefix is prepended, so pass the tail only).
- Dialog: `import { Dialog, DialogBody, DialogContent, DialogFooter, DialogHeader, DialogTitle, DialogDescription } from '@contractor-ops/ui/components/shadcn/dialog'` — put the accordion/list in `DialogBody` (scrollable) and the Close button in `DialogFooter` (sticky), per the web-vite Dialog convention (see `step-review.tsx`). Confirm exact exports from `packages/ui/src/components/shadcn/dialog.tsx`.
- Accordion + Alert + Button: import from `@contractor-ops/ui/components/shadcn/accordion`, `.../alert`, `.../button` (confirm exact subpaths; the repo uses `@contractor-ops/ui/components/shadcn/*`).
- Deep links: `import { Link } from '../../i18n/navigation.js'` (locale-aware TanStack Router wrapper); render as `<Link href={reason.deepLinkPath}>…</Link>` (use `href`, not `to`; do NOT use `next/link` or `target="_blank"` unless an existing link pattern does).
- Icons: `lucide-react` (`AlertTriangle`, `ExternalLink`) — confirm web-vite already depends on `lucide-react`.

Exported types (mirror the server `ContractorReason`/`ItemReason` shape from `packages/api/src/services/compliance-payment-gate.ts`):
```ts
export interface ItemReason {
  itemId: string;
  policyRuleId: string | null;
  documentTypeLabelKey: string;
  expiredOnDate: string;
  jurisdictionTz: string;
  deepLinkPath: string;
}
export interface ContractorReason {
  contractorId: string;
  contractorName: string;
  reasons: ItemReason[];
}
export interface PaymentBlockModalProps {
  open: boolean;
  onClose: () => void;
  contractorReasons: ContractorReason[];
}
```

Behaviour: one `AccordionItem` per contractor (truncate the name with full value in `title`); each reason is a list item with a `Link` to `deepLinkPath` + `expiredOn` text; empty `contractorReasons[]` renders a destructive `Alert` fallback. WCAG: dialog `aria-labelledby` → title; keyboard-navigable accordion; verify RTL for `ar`.
</action>
<read_first>
- `apps/web-vite/src/components/payments/new-payment-run-dialog/step-review.tsx` — existing payment dialog (DialogBody/DialogFooter convention, `useTranslations('Payments')`)
- `packages/ui/src/components/shadcn/dialog.tsx` — confirm `Dialog`/`DialogBody`/`DialogContent`/`DialogFooter`/`DialogHeader`/`DialogTitle` exports
- `apps/web-vite/src/i18n/useTranslations.ts` — confirm `(key, values?) => string` namespace-prefix semantics
- `apps/web-vite/src/i18n/navigation.tsx` — confirm `Link` `href` prop
- `apps/web-vite/src/components/contractors/classification/drv-clearance/drv-clearance-form.tsx` — structured-display + link precedent
</read_first>
<acceptance_criteria>
- `apps/web-vite/src/components/payments/payment-block-modal.tsx` exists
- `grep -q 'export function PaymentBlockModal' apps/web-vite/src/components/payments/payment-block-modal.tsx`
- `grep -q "useTranslations('Compliance')" apps/web-vite/src/components/payments/payment-block-modal.tsx`
- `grep -q 'Accordion' apps/web-vite/src/components/payments/payment-block-modal.tsx`
- No `next-intl`, `next/link`, or `'use client'` in the file
- `pnpm --filter @contractor-ops/web-vite typecheck` exits 0
</acceptance_criteria>
</task>

<task id="72-07-02">
<action>
Add the `Compliance` namespace to the 4 flat web-vite locale files: `apps/web-vite/messages/{en,de,pl,ar}.json`. These are flat per-locale bundles keyed by PascalCase top-level namespace (e.g. `Payments`, `Classification`); there is NO per-namespace `messages/en/compliance.json`, NO next-intl. Add a top-level `Compliance` key with the structure below. English is canonical; de/pl ship as English copy prefixed `[de] `/`[pl] ` and ar prefixed `[ar] ` so reviewers spot un-translated strings (Phase 73 owns full parity). All 4 files MUST have identical key sets (the repo's `i18n-parity` lint guard enforces parity across locales).

Confirm whether the custom `useTranslations` supports ICU plural syntax (`{count, plural, ...}`) by reading `apps/web-vite/src/i18n/useTranslations.ts` / `messages.ts`; if it only does simple `{var}` interpolation, replace the `itemCountSuffix` ICU plural with a plain `({count} document(s))` form.

Add under a top-level `"Compliance"` object in `apps/web-vite/messages/en.json`:
```json
{
  "paymentBlockModal": {
    "title": "Compliance EXPIRED — payment blocked",
    "subtitle": "{contractorCount} contractor(s), {itemCount} expired document(s) prevent this payment run.",
    "noReasonsTitle": "Payment blocked",
    "noReasonsDescription": "The payment run cannot proceed. Contact the compliance team for details.",
    "itemCountSuffix": "({count, plural, one {# document} other {# documents}})",
    "expiredOn": "expired on {date} ({tz})",
    "deepLinkAriaLabel": "Open {label} compliance details",
    "close": "Close",
    "wouldBlockBannerTitle": "Compliance check would block this payment when feature is enabled",
    "wouldBlockBannerDescription": "Feature flag compliance-payment-block is OFF. Engineering may force-enable with FLAG_SIGNOFF_BYPASS=local."
  },
  "documentType": {
    "uk_right_to_work": "UK Right-to-Work share code",
    "uk_utr": "HMRC UTR",
    "uk_business_registration": "Companies House registration",
    "uk_sds": "Status Determination Statement",
    "de_a1": "A1-Bescheinigung",
    "de_aufenthaltstitel": "Aufenthaltstitel residence permit",
    "de_eight_b_estg": "§48b EStG Freistellungsbescheinigung",
    "pl_zus_a1": "ZUS A1",
    "pl_udt": "UDT certification",
    "ksa_iqama": "Iqama residency permit",
    "ksa_work_permit_qiwa": "Work permit + Qiwa authorisation",
    "uae_emirates_id": "Emirates ID",
    "uae_free_zone_license": "Free-zone trade license"
  }
}
```

`apps/web-vite/messages/de.json` — add the same `Compliance` object, prefixing every string value with `[de] `. `pl.json` → prefix `[pl] `. `ar.json` → prefix `[ar] ` (RTL locale; keys still required for parity). Phase 73 owns final translations.

Confirm the exact `documentType.<key>` set against the real Phase 71 policy registry — the keys MUST match what the server's `getDocumentTypeLabelKey` emits (`compliance.documentType.<policyRuleId>` or `compliance.documentType.<documentType.toLowerCase()>`). Align the namespace tail used here with the modal's label-resolution approach chosen in task 72-07-01.
</action>
<read_first>
- `apps/web-vite/messages/en.json` — confirm flat PascalCase-namespace structure (no `Compliance` key today); identify a sibling namespace (e.g. `Payments`, `Classification`) to mirror nesting/ICU conventions
- `apps/web-vite/src/i18n/useTranslations.ts` + `messages.ts` — confirm interpolation/ICU support and how locales load
- `packages/compliance-policy/src/policies/*` + `packages/compliance-policy/src/registry.ts` — confirm exact `policyRuleId` keys (UK / DE / PL / KSA / UAE) so `documentType.<key>` entries match the server's label keys
- `packages/lint-guards/src/i18n-parity/run-guard.ts` + `packages/lint-guards/src/__fixtures__/messages/` — confirm the cross-locale parity rule the 4 files must satisfy
</read_first>
<acceptance_criteria>
- All 4 locale files (`en`, `de`, `pl`, `ar`) gain the `Compliance` namespace with identical key sets
- `node -e "process.exit('Compliance' in require('./apps/web-vite/messages/en.json') && require('./apps/web-vite/messages/en.json').Compliance.paymentBlockModal ? 0 : 1)"` exits 0
- Every `documentType.<key>` the modal can reference has a matching entry in all 4 files
- `pnpm --filter @contractor-ops/web-vite typecheck` exits 0
- `pnpm --filter @contractor-ops/lint-guards test --run --testNamePattern='i18n-parity'` exits 0 (locale parity preserved)
</acceptance_criteria>
</task>

<task id="72-07-03">
<action>
Wire `PaymentBlockModal` into the new-payment-run dialog flow following Page→Container→Hook→Component. The tRPC mutations live in the hook `apps/web-vite/src/components/payments/hooks/use-payment-run-step-review.ts` (`trpc.payment.create` + `trpc.payment.lockAndExport` via `useResourceMutation`). The hook is the ONLY tRPC boundary — it catches the `PRECONDITION_FAILED` error and exposes the block state; the container/step renders the modal.

In `use-payment-run-step-review.ts`:
- Add block state to the hook's return: `paymentBlock: { open: boolean; reasons: ContractorReason[] }` + `dismissPaymentBlock(): void`.
- Wrap the `create` and `lockAndExport` `mutateAsync` calls (or their `onError`) so a `PRECONDITION_FAILED` error whose `cause.contractorReasons` is an array sets the block state instead of surfacing a generic toast. Use a type guard:
```ts
function isPaymentBlock(err: unknown): err is { data?: { code?: string }; cause?: { contractorReasons: ContractorReason[] } } {
  const e = err as { data?: { code?: string }; cause?: { contractorReasons?: unknown } };
  return e?.data?.code === 'PRECONDITION_FAILED' && Array.isArray(e?.cause?.contractorReasons);
}
```
  Confirm how `useResourceMutation` / the tRPC v11 client exposes `error.data.code` and the serialised `cause` (read the hook + an existing error-handling hook). Import `ContractorReason` from `../payment-block-modal.js`.

In `step-review-container.tsx` (and/or `new-payment-run-dialog-container.tsx`): render `<PaymentBlockModal open={review.paymentBlock.open} onClose={review.dismissPaymentBlock} contractorReasons={review.paymentBlock.reasons} />`. For the flag-OFF would-block soft-warn path, render a non-blocking `Alert` (not the modal) when the success response carries a `wouldBlock` signal.

Verify the exact mutation call sites:
```bash
grep -rn 'payment\.create\|payment\.lockAndExport' apps/web-vite/src/components/payments/
```
</action>
<read_first>
- `apps/web-vite/src/components/payments/hooks/use-payment-run-step-review.ts` — the mutation host (`payment.create`, `payment.lockAndExport`, `isLocking`)
- `apps/web-vite/src/components/payments/new-payment-run-dialog/step-review-container.tsx` + `step-review.tsx` — where the modal renders
- `apps/web-vite/src/i18n/use-translated-error.ts` — existing tRPC error-handling helper conventions
- `apps/web-vite/src/components/payments/payment-block-modal.tsx` — the `ContractorReason` type + modal props (task 72-07-01)
</read_first>
<acceptance_criteria>
- `apps/web-vite/src/components/payments/hooks/use-payment-run-step-review.ts` catches `PRECONDITION_FAILED` with `cause.contractorReasons` from both `payment.create` and `payment.lockAndExport` and exposes block state
- `grep -q 'PaymentBlockModal' apps/web-vite/src/components/payments/new-payment-run-dialog/step-review-container.tsx`
- `pnpm --filter @contractor-ops/web-vite typecheck` exits 0
</acceptance_criteria>
</task>

<task id="72-07-04">
<action>
Flip `apps/web-vite/src/components/payments/__tests__/payment-block-modal.test.tsx` from RED to GREEN. Use the web-vite RTL + i18n test conventions (read an existing web-vite component test in `apps/web-vite/src/components/payments/__tests__/` to mirror the render wrapper / i18n provider setup).

Modal test cases:
- Renders modal with one accordion item per contractor; expanding shows the item-level deep links
- Renders empty-state alert when `contractorReasons={[]}` (defensive)
- Each item link's `href` matches `deepLinkPath` (locale-aware `Link`)
- Close button calls `onClose` callback
- Long contractor name (50+ chars) renders with `title` attribute holding the full name (truncate UX)
- i18n labels resolve via the web-vite test i18n wrapper (NOT next-intl)
- Accessibility: `screen.getByRole('dialog')` finds it; `getByRole('button', { name: /close/i })` is reachable

Hook test (`payment-wizard error-handling`):
- Mock `trpc.payment.create` / `trpc.payment.lockAndExport` (via the web-vite trpc test harness) to reject with `data.code = 'PRECONDITION_FAILED'` and serialised `cause.contractorReasons = [...]`
- Assert the hook surfaces `paymentBlock.open === true` with the expected reason payload for both mutations

Replace file header with `// Phase 72 Wave 3 — GREEN tests for payment-block-modal`.
</action>
<read_first>
- The current Wave 0 scaffold for the test file
- `apps/web-vite/src/components/payments/__tests__/payment-run-side-panel.test.tsx` — existing web-vite RTL test wrapper + i18n provider conventions
- `apps/web-vite/src/components/portal/__tests__/use-portal-payments.test.tsx` — existing hook-test + trpc-mock pattern to mirror
</read_first>
<acceptance_criteria>
- File no longer contains `throw new Error(...)` placeholders
- `pnpm --filter @contractor-ops/web-vite test --run --testNamePattern='payment-block-modal'` exits 0
- `pnpm --filter @contractor-ops/web-vite test --run --testNamePattern='payment-wizard error-handling'` exits 0
- File header reads `// Phase 72 Wave 3 — GREEN tests for payment-block-modal`
</acceptance_criteria>
</task>

</tasks>

<verification>
- `pnpm --filter @contractor-ops/web-vite typecheck` exits 0
- `pnpm --filter @contractor-ops/web-vite test --run --testNamePattern='payment-block-modal|payment-wizard'` exits 0 (scope the filter — never run the full web-vite suite)
- `pnpm --filter @contractor-ops/web-vite build` exits 0 (i18n keys resolve at build time)
- `pnpm --filter @contractor-ops/lint-guards test --run --testNamePattern='i18n-parity'` exits 0
- Manual: dev server boot, simulate a blocked payment via `FLAG_SIGNOFF_BYPASS=local`, confirm modal renders with seeded contractor's expired item with working deep link
- Manual: keyboard nav — tab to accordion, arrow-key expand, tab to link, tab to close — all reachable; RTL verified for `ar`
</verification>
