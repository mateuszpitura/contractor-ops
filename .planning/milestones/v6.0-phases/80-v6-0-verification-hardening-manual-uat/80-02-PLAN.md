---
phase: 80-v6-0-verification-hardening-manual-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/80-v6-0-verification-hardening-manual-uat/80-HUMAN-UAT.md
autonomous: true
requirements: []   # verification phase — 0 ROADMAP requirement IDs
must_haves:
  truths:
    - "80-HUMAN-UAT.md lists every manual UI UAT scenario across F1, F2, F3, and F4 — one of the three separate milestone-close docs (D-05)"
    - "F2 IdP appears in the UAT doc (it is deliberately excluded from the integration test per D-01, so manual UAT is its only milestone-close coverage)"
    - "Each scenario carries expected (repro steps + expected behaviour), why_human (why grep cannot verify), and result: [pending] with a post-deploy disposition"
    - "The frontmatter + Tests + Summary structure mirrors 79-HUMAN-UAT.md / 63-HUMAN-UAT.md"
  artifacts:
    - path: ".planning/phases/80-v6-0-verification-hardening-manual-uat/80-HUMAN-UAT.md"
      provides: "Consolidated manual UI UAT scenario list for v6.0 milestone close"
      contains: "## Tests"
  key_links:
    - from: ".planning/phases/80-v6-0-verification-hardening-manual-uat/80-HUMAN-UAT.md"
      to: "F1/F2/F3/F4 feature surfaces (phases 71-79)"
      via: "one UAT scenario per human-verify item, deduped against per-phase HUMAN-UAT docs"
      pattern: "why_human"
---

<objective>
Produce `80-HUMAN-UAT.md` — every manual UI UAT scenario across F1/F2/F3/F4 with reproduction steps, expected behaviour, and a "post-deploy" disposition (SC#2, D-05). Mirrors the v5.0/v6.0 HUMAN-UAT format. **F2 IdP MUST appear here** because D-01 deliberately excludes F2 from the integration test (its ACCESS_REVOKE saga runs post-offboarding-completion, off the blocked path), so manual UAT is F2's only milestone-close coverage.

Purpose: capture the human-verify items that grep/vitest cannot prove — visual render, dialog flow, Arabic RTL, role gating, PDF/byte inspection, real-IdP/real-gov-API edges — as a runnable manual checklist for when LOCAL-ONLY status flips.
Output: `.planning/phases/80-v6-0-verification-hardening-manual-uat/80-HUMAN-UAT.md`.

Out of scope (enforce): running the UAT (LOCAL-ONLY, no human present — every result is `[pending]`); new feature code; restating items already exhaustively covered by per-phase HUMAN-UAT docs without a cross-reference (dedup or cross-link, per the CONTEXT discretion bullet).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-v6-0-verification-hardening-manual-uat/80-CONTEXT.md
@.planning/phases/80-v6-0-verification-hardening-manual-uat/80-PATTERNS.md

# Precedent doc to mirror (frontmatter + Tests + Summary + Gaps structure):
@.planning/milestones/v6.0-phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble F1/F2/F3/F4 manual UAT scenarios into 80-HUMAN-UAT.md</name>
  <files>.planning/phases/80-v6-0-verification-hardening-manual-uat/80-HUMAN-UAT.md</files>
  <read_first>
    - .planning/milestones/v6.0-phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-HUMAN-UAT.md (PRIMARY FORMAT ANALOG — frontmatter status/phase/source/started/updated, "## Current Test", "## Tests" with per-test expected/why_human/result, "## Summary" with totals, "## Gaps"; its 3 scenarios already cover Arabic RTL + de/pl genuineness + Arabic statutory sign-off — cross-reference, do not blindly re-list)
    - .planning/milestones/v5.0-phases/63-de-i18n-parity-fix/63-HUMAN-UAT.md OR any 63-*-HUMAN-UAT.md if present (richer multi-step expected/why_human example) — if the exact path differs, use the most recent v5.0 *-HUMAN-UAT.md the executor can locate via `ls .planning/milestones/v5.0-phases/*/*-HUMAN-UAT.md`
    - .planning/phases/80-v6-0-verification-hardening-manual-uat/80-PATTERNS.md (the 80-HUMAN-UAT section — names the F2-must-appear rule and the dedup-vs-relist disposition)
  </read_first>
  <action>
    Create `80-HUMAN-UAT.md` with the `79-HUMAN-UAT.md` frontmatter shape (`status: partial`, `phase: 80-v6-0-verification-hardening-manual-uat`, `source: [80-VERIFICATION.md]`, `started`/`updated` = 2026-06-03), a "## Current Test" line noting the app must run with all v6.0 feature flags enabled, then "## Tests" grouped by feature. Write one scenario per human-verify item across all four features, each with `expected:` (numbered repro steps + expected behaviour), `why_human:` (the specific reason grep/vitest cannot prove it — visual render, dialog flow, RTL mirroring, role-gated button absence, PDF/byte inspection, real-provider sandbox edge), and `result: [pending]`. Minimum coverage: F1 — at-risk compliance dashboard widgets render + payment-block PRECONDITION_FAILED modal lists per-contractor reasons with deep link + portal one-click upload-replacement flips SATISFIED + manual override→WAIVED audit row visible. F2 (MUST APPEAR) — ACCESS_REVOKE per-IdP describeImpact preview before deprovision, GWS suspend+OAuth-revoke+sign-out, Slack session-invalidate+SCIM-deactivate, Entra disable+revokeSignInSessions with Conditional-Access pre-flight warning + hybrid-AD hard-warning, Okta deactivate+session-clear, GitHub member-remove+per-PAT-revoke+outside-collab manual flag, PARTIAL_FAILURE reconcile-queue manual-retry, idempotent second click returns LIKELY_GONE. F3 — Arabic RTL visual on every Gulf surface, free-zone form + scope-mismatch advisory + auto-NOC, Saudization dashboard band/rate/Qiwa-gap/Iqama rollup + manual band entry + drift-override "Custom — verify with adviser" badge, pre-offboarding band-trajectory advisory banner, de/pl translation genuineness (cross-reference 79 scenarios 1-2). F4 — KT template auto-select + PTO-aware routing, OWNER-only override dialog (min-20-char reason + acknowledgement) with non-OWNER seeing no button, IP-clause LIKELY_MISSING surfaces a STANDARD item + e-sign ratification flow, CredentialReference paste-an-AWS-key returns 400. End with a "## Summary" block (`total: N`, `passed: 0`, `issues: 0`, `pending: N`, `skipped: 0`, `blocked: 0`) and an empty "## Gaps". Every `result` is `[pending]` (LOCAL-ONLY post-deploy disposition; never blocking). Where a scenario is fully covered by a per-phase HUMAN-UAT doc, cross-reference that doc (e.g. "see 79-HUMAN-UAT.md test 1") rather than duplicating verbatim, per the dedup discretion.
  </action>
  <verify>
    <automated>test -f .planning/phases/80-v6-0-verification-hardening-manual-uat/80-HUMAN-UAT.md && grep -qE "^### .*[Ff]2|IdP|Entra|Okta|GitHub|Slack|GWS|ACCESS_REVOKE" .planning/phases/80-v6-0-verification-hardening-manual-uat/80-HUMAN-UAT.md && echo F2_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/80-v6-0-verification-hardening-manual-uat/80-HUMAN-UAT.md`.
    - Frontmatter has `status:`, `phase: 80-v6-0-verification-hardening-manual-uat`, `source:`, `started:`, `updated:` — verifiable: `head -8` shows all five keys.
    - Sections `## Current Test`, `## Tests`, `## Summary`, `## Gaps` all present — verifiable: `grep -c "^## " …80-HUMAN-UAT.md` >= 4.
    - F2 IdP coverage present — verifiable: `grep -Ec "IdP|Entra|Okta|GitHub|Slack|GWS|ACCESS_REVOKE|describeImpact" …80-HUMAN-UAT.md` >= 1 (the `F2_PRESENT` echo in verify must print).
    - All four features covered — verifiable: each of the strings/themes for F1 (compliance/payment-block), F3 (free-zone/Saudization/Arabic), F4 (offboarding/IP/credential) appears at least once.
    - Every scenario has the canonical triplet — verifiable: `grep -c "why_human:" …80-HUMAN-UAT.md` equals `grep -c "result:" …80-HUMAN-UAT.md` and both equal the scenario count, and `grep -c "result: \[pending\]" …80-HUMAN-UAT.md` equals the total (all pending — LOCAL-ONLY).
  </acceptance_criteria>
  <done>80-HUMAN-UAT.md lists every F1/F2/F3/F4 manual UAT scenario (F2 present) with expected/why_human/result:[pending] and a Summary, mirroring 79-HUMAN-UAT.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan writes ONE planning markdown doc. No code, no inputs, no data crosses any runtime boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-02-01 | Information Disclosure | 80-HUMAN-UAT.md | mitigate | UAT repro steps describe flows with placeholder identifiers (e.g. `ana@acme.com`); the doc exposes NO credentials, real tokens, or live secrets. Reviewer confirms no real env values are pasted into repro steps. |
| T-80-02-SC | Tampering | supply chain | accept | No package installs; documentation-only plan. Nothing to verify. |

Threat surface is effectively zero: a markdown checklist with synthetic example identifiers only.
</threat_model>

<verification>
- `80-HUMAN-UAT.md` exists with the mirrored frontmatter + Tests + Summary + Gaps structure.
- F2 IdP scenarios present (D-01 exclusion from the test means UAT is F2's only milestone-close coverage).
- Every `result` is `[pending]`; no real secrets in repro steps.
</verification>

<success_criteria>
`80-HUMAN-UAT.md` lists every manual UI UAT scenario across F1/F2/F3/F4 with reproduction steps, expected behaviour, and "post-deploy" disposition; F2 IdP is included; format mirrors 79-/63-HUMAN-UAT.md (SC#2, D-05).
</success_criteria>

<output>
Create `.planning/phases/80-v6-0-verification-hardening-manual-uat/80-02-SUMMARY.md` when done. Record the scenario count per feature and confirm F2 IdP is present.
</output>
