---
phase: 80-v6-0-verification-hardening-manual-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/__tests__/v6-cross-feature-composition.test.ts
autonomous: true
requirements: []   # verification phase — 0 ROADMAP requirement IDs (covers all v6.0 surfaces)
must_haves:
  truths:
    - "One seeded contractor (UAE free-zone + expiring/EXPIRED license + IP-clause LIKELY_MISSING item + Saudi-national assignment) drives F1+F3+F4 gates in a single test run"
    - "Payment is hard-blocked once the free-zone BLOCKING item flips to EXPIRED (assertContractorPaymentEligibility throws PRECONDITION_FAILED)"
    - "Offboarding is hard-blocked while the IP_VERIFICATION task is open (assertRunCompletable throws PRECONDITION_FAILED with cause.blockedTaskKind=IP_VERIFICATION)"
    - "Saudization band-trajectory render is advisory-only and non-gating (projectOffboardingTrajectory returns advisory:true, authoritative:false, no projectedBand)"
    - "The F1/F3 gate path's writeAuditLog rows (runComplianceReminderScan + compliance-payment-gate) are asserted via a spy with expect.objectContaining; the F4 composed hard-block path emits no audit row"
    - "The locked-phrase guard is green inside the test (Gulf AE/SA locked phrases match verbatim, no drift)"
  artifacts:
    - path: "packages/api/src/__tests__/v6-cross-feature-composition.test.ts"
      provides: "Cross-feature F1+F3+F4 composition integration test"
      min_lines: 120
  key_links:
    - from: "packages/api/src/__tests__/v6-cross-feature-composition.test.ts"
      to: "packages/api/src/services/compliance-reminder-scan.ts"
      via: "runComplianceReminderScan flips PENDING->EXPIRED on the shared store"
      pattern: "runComplianceReminderScan"
    - from: "packages/api/src/__tests__/v6-cross-feature-composition.test.ts"
      to: "packages/api/src/services/compliance-payment-gate.ts"
      via: "assertContractorPaymentEligibility reads status='EXPIRED' from the same store"
      pattern: "assertContractorPaymentEligibility"
    - from: "packages/api/src/__tests__/v6-cross-feature-composition.test.ts"
      to: "packages/api/src/routers/workflow/workflow-shared.ts"
      via: "assertRunCompletable IP_VERIFICATION hard-block"
      pattern: "assertRunCompletable"
    - from: "packages/api/src/__tests__/v6-cross-feature-composition.test.ts"
      to: "packages/api/src/services/saudization-dashboard.ts"
      via: "projectOffboardingTrajectory advisory render"
      pattern: "projectOffboardingTrajectory"
---

<objective>
Write the single cross-feature integration test that proves F1 (compliance) + F3 (Gulf) + F4 (offboarding) compose correctly on ONE seeded contractor (SC#1, D-01/D-02/D-03). The test wires REAL services against one shared mutable mock-Prisma store — exactly the `free-zone-record-then-expire.test.ts` composition primitive — so the cross-boundary flip arms the gate in the same run.

Purpose: SC#1 is the only code this phase ships and the riskiest deliverable. It is the milestone's proof that the four gate primitives (already independently unit-tested) actually compose on the SC#1 mega-scenario.
Output: `packages/api/src/__tests__/v6-cross-feature-composition.test.ts` (one new test file; no feature source modified).

Out of scope (enforce): F2 IdP composition (its ACCESS_REVOKE saga runs post-offboarding-completion, off the blocked path — D-01; F2 belongs only in `80-HUMAN-UAT.md`); new feature code; modifying any service under test; seeding via `seed-dev.ts` (heavyweight live-DB seeder, no Gulf section — use the DB-free fixtures + hoisted mock-Prisma store instead).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-v6-0-verification-hardening-manual-uat/80-CONTEXT.md
@.planning/phases/80-v6-0-verification-hardening-manual-uat/80-PATTERNS.md

<interfaces>
<!-- Exact service signatures the test composes. Extracted from the live tree. Use these directly — no codebase exploration needed. -->

From packages/api/src/services/compliance-payment-gate.ts:
  assertContractorPaymentEligibility(contractorIds: string[], opts: { organizationId: string }): Promise<{ blocked: boolean; wouldBlock: boolean; contractorReasons: Array<...> }>
  // Selects compliance items WHERE severity='BLOCKING' AND status='EXPIRED'; throws TRPCError code 'PRECONDITION_FAILED' (cause.contractorReasons carries doc name + expired-on + deep link) when a blocking-expired item exists.

From packages/api/src/services/compliance-reminder-scan.ts:
  runComplianceReminderScan(now: Date): Promise<...>
  // Region-aware cron pass; calls reEvaluateFreeZoneStatus to flip free-zone PENDING items to EXPIRED once the Asia/Dubai expiry boundary is crossed.

From packages/api/src/routers/workflow/workflow-shared.ts:
  assertRunCompletable(client: RunGateClient, runId: string, orgId: string): Promise<void>
  // Throws TRPCError code 'PRECONDITION_FAILED' with cause { blockedTaskKind: 'IP_VERIFICATION', openTaskIds: string[] } when an IP_VERIFICATION task is open and no Phase-74 override applied. RunGateClient is structural: { workflowTaskRun.findMany, workflowRun.findUniqueOrThrow, credentialReference.findMany }.

From packages/api/src/services/saudization-dashboard.ts:
  projectOffboardingTrajectory(params: { headcount: { totalHeadcount: number; saudiHeadcount: number } | null; currentBand: string | null; offboardingContractorIsSaudi: boolean }): { advisory: true; authoritative: false; currentBand: string | null; currentRate: number | null; projectedRate: number | null }
  // Pure function (single params arg, no DB client). Recomputes projectedRate; NEVER returns a projectedBand (locked anti-feature).

From packages/api/src/__tests__/__fixtures__/gulf-fixtures.ts:
  makeMeOrg(overrides?) -> MeOrgFixture            // ME-region org, countryCode 'AE'/'SA'
  makeFreeZoneComplianceItem(overrides?) -> ...    // documentType 'UAE_FREE_ZONE_LICENSE', severity 'BLOCKING', policyRuleId 'uae.free_zone_license@v2', expiryJurisdictionTz 'Asia/Dubai', status param-driven
  makeFreeZoneAssignment(overrides?) -> ...

From packages/validators (re-exported legal constants):
  LOCKED_AE_PHRASES, RESERVED_AE_LEGAL_KEYS  (from legal/ae)
  LOCKED_SA_PHRASES, RESERVED_SA_LEGAL_KEYS  (from legal/sa)  // NITAQAT_BAND_* literals are the UPPER_SNAKE enum strings
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compose the F1+F3 payment-block + F3 advisory legs on the shared mock store</name>
  <files>packages/api/src/__tests__/v6-cross-feature-composition.test.ts</files>
  <read_first>
    - packages/api/src/__tests__/free-zone-record-then-expire.test.ts (PRIMARY ANALOG — copy the vi.hoisted store, regionClientFactory, vi.mock('@contractor-ops/db', …), vi.mock feature-flags/logger/audit-writer block, and the record-then-expire → gate-blocks flow verbatim, then extend)
    - packages/api/src/__tests__/__fixtures__/gulf-fixtures.ts (the DB-free factories — makeMeOrg, makeFreeZoneComplianceItem)
    - packages/api/src/__tests__/free-zone-payment-block.test.ts (the gateRow() joined-contractor shape + the PRECONDITION_FAILED cause.contractorReasons deep-link assertion to replicate)
    - packages/api/src/__tests__/saudization-derivation.test.ts (the projectOffboardingTrajectory advisory assertions — advisory:true, authoritative:false, no projectedBand)
  </read_first>
  <behavior>
    - Test: a free-zone BLOCKING item recorded valid (status PENDING, expiresAt in the past relative to the cron tick) is NOT blocking before the scan — assertContractorPaymentEligibility returns { blocked: false }.
    - Test: runComplianceReminderScan(now) with `now` after the Asia/Dubai boundary flips the item PENDING -> EXPIRED on the shared store.
    - Test: after the scan, assertContractorPaymentEligibility([CONTRACTOR_ID], { organizationId: ME_ORG.id }) rejects with code 'PRECONDITION_FAILED' and the cause names the free-zone doc (F1+F3 payment hard-block fires).
    - Test: projectOffboardingTrajectory({ headcount: { totalHeadcount, saudiHeadcount }, currentBand: 'MID_GREEN', offboardingContractorIsSaudi: true }) returns advisory:true, authoritative:false, a recomputed projectedRate < currentRate, and has NO 'projectedBand' property (advisory render is non-gating).
  </behavior>
  <action>
    Create `v6-cross-feature-composition.test.ts`. Copy the `free-zone-record-then-expire.test.ts` scaffold: the `vi.hoisted(() => ({ store: { items: [] } }))` shared store, the `regionClientFactory` (honouring `where.severity` / `where.status.in` / `expiresAt` / `expiryJurisdictionTz`), the `vi.mock('@contractor-ops/db', …)` with `getRegionalClient` + region-keyed `clientCache`, and the `vi.mock` block for `@contractor-ops/feature-flags` (`isPaymentBlockEnforced: () => true`), `@contractor-ops/logger`, `@contractor-ops/logger/metrics`, `notification-service`, `rbac-recipients`, `cron-dedup`, and `../i18n/email-i18n`. Also declare the audit-writer spy here via `vi.hoisted` (`auditWriteSpy`) and `vi.mock('../services/audit-writer', () => ({ writeAuditLog: auditWriteSpy }))` — the F1/F3 cron + payment-gate path is what populates the audit rows this test asserts (Task 2 reuses this same mock; the F4 leg adds no audit writer). Build the composed contractor with `makeMeOrg()` (countryCode 'AE') and `makeFreeZoneComplianceItem({ organizationId, contractorId, expiresAt, status: 'PENDING' })` pushed onto the store with a joined `contractor` sub-object (mirror `recordValidFreeZoneItem`). Import `assertContractorPaymentEligibility` from `../services/compliance-payment-gate` and `runComplianceReminderScan` from `../services/compliance-reminder-scan` AFTER the mock block. Assert the record -> cross-boundary -> block sequence. Add a sibling `describe` for the F3 advisory leg importing `projectOffboardingTrajectory` from `../services/saudization-dashboard`; assert `advisory`/`authoritative`/`projectedRate` and `expect(traj).not.toHaveProperty('projectedBand')`. All enum-shaped fixture strings stay UPPER_SNAKE_CASE (severity 'BLOCKING', status 'EXPIRED', documentType 'UAE_FREE_ZONE_LICENSE', expiryJurisdictionTz 'Asia/Dubai') per `db:audit-enum-casing`. No real secrets, no real network, no real DB.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test v6-cross-feature-composition</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test v6-cross-feature-composition` passes (all assertions green).
    - Source contains `runComplianceReminderScan(` followed (in the same flow) by `assertContractorPaymentEligibility(` rejecting `.toMatchObject({ code: 'PRECONDITION_FAILED' })` — verifiable: `grep -c "PRECONDITION_FAILED" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` >= 1.
    - Source asserts `projectOffboardingTrajectory` returns `advisory` true + `authoritative` false and `.not.toHaveProperty('projectedBand')` — verifiable: `grep -c "projectedBand" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` >= 1.
    - No `seed-dev` import: `grep -c "seed-dev" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` == 0.
    - No real credential literals: `grep -inE "AKIA|sk_live|ghp_|-----BEGIN" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` returns nothing.
  </acceptance_criteria>
  <done>The F1+F3 payment hard-block (record -> scan -> EXPIRED -> PRECONDITION_FAILED) and the F3 Saudization advisory (non-gating, no band) legs pass in one file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose the F4 offboarding IP hard-block, assert the F1/F3 audit rows, and the green locked-phrase legs</name>
  <files>packages/api/src/__tests__/v6-cross-feature-composition.test.ts</files>
  <read_first>
    - packages/api/src/routers/__tests__/workflow-execution-ip-block.test.ts (PRIMARY ANALOG for the F4 leg — copy the structural makeGateClient and the assertRunCompletable PRECONDITION_FAILED + cause.blockedTaskKind='IP_VERIFICATION' assertions; note assertRunCompletable does NOT call writeAuditLog)
    - packages/api/src/__tests__/gulf-override-audit.test.ts (the writeAuditLog spy pattern — vi.hoisted auditWriteSpy, vi.mock('../services/audit-writer', …), expect.objectContaining assertion incl. the tx-threaded variant)
    - packages/validators/src/__tests__/locked-phrases-guard.test.ts (the canonical Gulf locked-phrase invariant — imports LOCKED_AE_PHRASES/RESERVED_AE_LEGAL_KEYS and LOCKED_SA_PHRASES/RESERVED_SA_LEGAL_KEYS; NITAQAT_BAND_* are the literal UPPER_SNAKE enum strings)
    - packages/api/src/services/contract-health/run-health-check.ts (where LIKELY_MISSING materialises the IP ContractorComplianceItem — confirm the IP item is the offboarding-block origin; read lines 130-225)
  </read_first>
  <behavior>
    - Test: assertRunCompletable(client, runId, orgId) with an open IP_VERIFICATION task and no override rejects with code 'PRECONDITION_FAILED' and cause.blockedTaskKind === 'IP_VERIFICATION' (offboarding hard-block fires from the IP-clause LIKELY_MISSING gate).
    - Test: a Phase-74 override (overrideMetadata.blockedTaskKind='IP_VERIFICATION') clears the block (resolves) — documents that the gate is overridable, not the block being absent.
    - Test: the writeAuditLog spy captured the F1/F3 audit rows — the audit rows emitted by `runComplianceReminderScan` + the compliance-payment-gate path exercised in Task 1 (asserted via expect.objectContaining the expected action/organizationId). The composed F4 hard-block path (assertRunCompletable, no override) emits NO audit row of its own; the only audit rows in this composed SC#1 (hard-block) scenario are the F1/F3 rows from the shared mock. (An override path would write an audit row, but SC#1 here is the hard-block, not the override.)
    - Test: the Gulf locked-phrase invariant is green — RESERVED_AE_LEGAL_KEYS mirrors LOCKED_AE_PHRASES keys, RESERVED_SA_LEGAL_KEYS mirrors LOCKED_SA_PHRASES keys, and the NITAQAT_BAND_* literals equal their UPPER_SNAKE enum strings (no drift).
  </behavior>
  <action>
    Extend `v6-cross-feature-composition.test.ts` with the F4 + audit-assertion + locked-phrase legs. F4: import `assertRunCompletable` from `../routers/workflow/workflow-shared`; build the in-memory structural `makeGateClient({ openIpTaskIds: ['task_ip'] })` (mirror the ip-block analog: `workflowTaskRun.findMany`, `workflowRun.findUniqueOrThrow`, `credentialReference.findMany`); assert the open-IP path rejects PRECONDITION_FAILED with `cause.blockedTaskKind === 'IP_VERIFICATION'`, and that the `overrideMetadata.blockedTaskKind: 'IP_VERIFICATION'` path resolves. Do NOT add an audit-row assertion to the F4 leg — `assertRunCompletable` does not call `writeAuditLog`; in the composed SC#1 hard-block scenario the audit rows come only from the F1/F3 path. Audit assertion: reuse the `auditWriteSpy` declared in Task 1's mock block; after the F1/F3 cron + payment-gate path has run (Task 1's flow), assert `auditWriteSpy` was called for those rows via `toHaveBeenCalledWith(expect.objectContaining({ … }))` matching the compliance-reminder-scan + compliance-payment-gate action/organizationId. Document in-test (comment or test name) that the F4 hard-block leg writes no audit row, so the spy's call set is exactly the F1/F3 rows. Locked-phrase: import `LOCKED_AE_PHRASES`/`RESERVED_AE_LEGAL_KEYS`/`LOCKED_SA_PHRASES`/`RESERVED_SA_LEGAL_KEYS` from `@contractor-ops/validators` and assert the mirror invariants + the NITAQAT_BAND_* literals — reuse the guard invariant in-test rather than re-implementing a parser (D-02 "green locked-phrase guard"). Keep this a verification of existing constants; do not add or rename any phrase.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test v6-cross-feature-composition</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test v6-cross-feature-composition` passes with all four legs (payment-block, offboarding-block, advisory, locked-phrase) + the F1/F3 audit-row assertion green.
    - Source asserts `assertRunCompletable` rejects with `blockedTaskKind` `'IP_VERIFICATION'` — verifiable: `grep -c "IP_VERIFICATION" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` >= 1.
    - Source asserts the F1/F3 `auditWriteSpy` / `writeAuditLog` rows via `expect.objectContaining` — verifiable: `grep -Ec "objectContaining|toHaveBeenCalledWith" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` >= 1. The F4 leg carries no audit-spy assertion (assertRunCompletable writes none).
    - Source imports and asserts the Gulf locked phrases — verifiable: `grep -Ec "LOCKED_AE_PHRASES|LOCKED_SA_PHRASES" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` >= 1.
    - API typecheck clean for the package: `pnpm typecheck --filter @contractor-ops/api` exits 0.
  </acceptance_criteria>
  <done>All four SC#1 gates assert in one file: payment hard-block, offboarding hard-block (IP_VERIFICATION), Saudization advisory (non-gating), the F1/F3 audit rows (the only rows in the composed hard-block scenario), and a green Gulf locked-phrase invariant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → mock Prisma store | All inputs are in-process plain objects; no external/untrusted data crosses any boundary. No real DB, no network, no IdP/gov API calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Information Disclosure | new test file | mitigate | Test seeds only synthetic IDs (`clme…` cuids) + fixture strings; NO real secrets, API keys, or PII. Acceptance criterion greps for `AKIA`/`sk_live`/`ghp_`/PEM headers and requires zero hits. |
| T-80-02 | Tampering | npm/pip/cargo installs | accept | This plan adds NO dependencies (only imports existing `@contractor-ops/*` workspace packages + `vitest`/`@trpc/server` already present). No package-legitimacy checkpoint required. |
| T-80-SC | Tampering | supply chain | accept | No installs in this plan; nothing to verify. Surface is read-only services + one new test file. |

Threat surface is minimal by design: this plan writes ONE test that composes already-shipped, already-tested services against in-memory mocks. No production code path is modified.
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test v6-cross-feature-composition` passes.
- `pnpm typecheck --filter @contractor-ops/api` exits 0.
- The test asserts all four SC#1 gates + the F1/F3 audit rows + green locked-phrase invariant in a single run.
- No `seed-dev` import; no real secrets in the file.
</verification>

<success_criteria>
The cross-feature integration test exercises the full F1+F3+F4 composition on one seeded contractor: payment hard-blocked AND offboarding hard-blocked AND Saudization band-trajectory advisory shown; every gate fires; the F1/F3 gate path's audit rows are asserted (the F4 hard-block path writes none); locked-phrase invariant green (SC#1, D-01/D-02/D-03).
</success_criteria>

<output>
Create `.planning/phases/80-v6-0-verification-hardening-manual-uat/80-01-SUMMARY.md` when done. Record the final test file path, the pass count, and confirm F2 was deliberately NOT composed (per D-01). The pass/fail result of this test is an input to Plan 80-04 (retrospective) — state it explicitly in the summary.
</output>
</content>
