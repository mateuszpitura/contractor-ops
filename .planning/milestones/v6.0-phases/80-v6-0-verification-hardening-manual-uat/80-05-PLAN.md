---
phase: 80-v6-0-verification-hardening-manual-uat
plan: 05
type: execute
wave: 3
gap_closure: true
depends_on: []
files_modified:
  - packages/api/src/__tests__/v6-cross-feature-composition.test.ts
autonomous: true
requirements: []   # verification phase — 0 ROADMAP requirement IDs (covers all v6.0 surfaces)
must_haves:
  truths:
    - "ONE seeded contractor's shared mutable mock-Prisma store drives all three gates in a single sequential flow: runComplianceReminderScan flips the free-zone BLOCKING item PENDING->EXPIRED, then assertContractorPaymentEligibility throws PRECONDITION_FAILED (enforced hard-block), then projectOffboardingTrajectory renders the advisory from headcount derived from the SAME seeded org/contractor context, then assertRunCompletable throws PRECONDITION_FAILED on the open IP_VERIFICATION task — proving F1+F3+F4 COMPOSE, not just pass in isolation (SC#1, D-01/D-02/D-03)"
    - "The composed scenario seeds a SECOND org + SECOND contractor row so tenant-isolation and taskType filters are load-bearing: the payment-gate prisma mock honours where.contractorId.in AND where.contractor.is.organizationId (WR-02), and makeGateClient.workflowTaskRun.findMany applies the where.taskType='IP_VERIFICATION' + where.workflowRunId predicate (WR-03)"
    - "The enforced-vs-audit tension is resolved honestly per the real service: the composed flow asserts the ENFORCED throw (flag ON, no audit row written), and a SEPARATE assertion drives the would-block branch (flagEnabled:false) to capture the compliance.payment.would_block audit row — with an inline comment citing compliance-payment-gate.ts:106-120; the test never asserts hard-block AND audit-on-the-same-call (the service cannot do both)"
    - "The pre-existing passing assertions are preserved with no net coverage regression: the F3 advisory shape (advisory:true / authoritative:false / no projectedBand), the Phase-74 override-clears-block path, and the green Gulf locked-phrase invariant (LOCKED_AE/SA_PHRASES) all still assert"
    - "F2 IdP is NOT added to the integration test (D-01 — its ACCESS_REVOKE saga runs post-offboarding-completion, off the blocked path; F2 lives only in 80-HUMAN-UAT.md)"
  artifacts:
    - path: "packages/api/src/__tests__/v6-cross-feature-composition.test.ts"
      provides: "Cross-feature F1+F3+F4 composition integration test with one composed-scenario test body threading shared state through all three gates"
      min_lines: 120
  key_links:
    - from: "packages/api/src/__tests__/v6-cross-feature-composition.test.ts"
      to: "packages/api/src/services/compliance-reminder-scan.ts"
      via: "runComplianceReminderScan flips PENDING->EXPIRED on the shared store inside the composed flow"
      pattern: "runComplianceReminderScan"
    - from: "packages/api/src/__tests__/v6-cross-feature-composition.test.ts"
      to: "packages/api/src/services/compliance-payment-gate.ts"
      via: "assertContractorPaymentEligibility reads the EXPIRED row from the same store; prisma mock honours contractorId+organizationId"
      pattern: "assertContractorPaymentEligibility"
    - from: "packages/api/src/__tests__/v6-cross-feature-composition.test.ts"
      to: "packages/api/src/services/saudization-dashboard.ts"
      via: "projectOffboardingTrajectory fed headcount derived from the same seeded org context"
      pattern: "projectOffboardingTrajectory"
    - from: "packages/api/src/__tests__/v6-cross-feature-composition.test.ts"
      to: "packages/api/src/routers/workflow/workflow-shared.ts"
      via: "assertRunCompletable IP_VERIFICATION hard-block via a gate client that honours where.taskType"
      pattern: "assertRunCompletable"
---

<objective>
Close the single BLOCKER gap from 80-VERIFICATION.md (SC#1). The existing `v6-cross-feature-composition.test.ts` exercises the F1/F3/F4 gates in five ISOLATED describe blocks with `beforeEach` resetting the store per `it`, F3 fed hardcoded literals disconnected from the store, and F4 using a gate-client that never reads `store.items`. SC#1 requires a SINGLE composed scenario: ONE seeded contractor whose shared mutable mock-Prisma store drives all three gates in sequence, proving the features COMPOSE.

This plan MODIFIES the existing test (one file) to add a composed-scenario describe block and to make the mocks load-bearing. It preserves every currently-green assertion.

Purpose: SC#1 is the only unmet must-have on the v6.0 milestone-close phase (SC#2/SC#3/SC#4 are VERIFIED). Closing it completes the milestone-verification proof that F1 (compliance) + F3 (Gulf) + F4 (offboarding) interact on shared state, not merely pass side-by-side.
Output: the modified `packages/api/src/__tests__/v6-cross-feature-composition.test.ts` (no feature source touched).

Out of scope (enforce): F2 IdP composition (D-01 — its saga runs post-offboarding-completion, off the blocked path; F2 belongs only in `80-HUMAN-UAT.md`); SC#2/SC#3/SC#4 (already VERIFIED — do NOT re-plan or touch their docs); new feature code; modifying any service under test; asserting service behaviour that does not exist (see Task 2 enforced-vs-audit constraint); seeding via `seed-dev.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-v6-0-verification-hardening-manual-uat/80-CONTEXT.md
@.planning/phases/80-v6-0-verification-hardening-manual-uat/80-VERIFICATION.md
@.planning/phases/80-v6-0-verification-hardening-manual-uat/80-REVIEW.md
@.planning/phases/80-v6-0-verification-hardening-manual-uat/80-PATTERNS.md

<interfaces>
<!-- Exact service contracts the composed test threads. Extracted from the live tree. Use these directly — no codebase exploration needed. -->

From packages/api/src/services/compliance-payment-gate.ts (lines 75-123, 165-187):
  assertContractorPaymentEligibility(contractorIds: string[], opts?: { tx?; throwOnFail?: boolean; flagEnabled?: boolean; organizationId?: string }): Promise<{ blocked: boolean; wouldBlock: boolean; contractorReasons: ContractorReason[] }>
  // When no `tx`, the gate falls back to `prisma` (line 80). It calls:
  //   db.contractorComplianceItem.findMany({ where: {
  //       contractorId: { in: contractorIds },
  //       severity: 'BLOCKING',
  //       status: 'EXPIRED',
  //       ...(organizationId ? { contractor: { is: { organizationId } } } : {}),
  //   }, include: { contractor: { select: { id, displayName, organizationId } } } })
  // enforce = flagEnabled ?? isPaymentBlockEnforced().  (the mock returns true by default)
  //   enforce + reasons>0  => blocked=true  => THROWS PRECONDITION_FAILED, writes NO audit row (lines 114-120)
  //   !enforce + reasons>0 => wouldBlock=true => recordWouldBlock(...) writes ONE 'compliance.payment.would_block' AuditLog row (lines 106-112, 165-187)
  // cause shape on throw: { contractorReasons: Array<{ contractorId, contractorName, reasons: Array<{ itemId, policyRuleId, ..., deepLinkPath }> }> }
  // deepLinkPath = `/contractors/${contractorId}/compliance#item-${itemId}` (line 155)

From packages/api/src/services/compliance-reminder-scan.ts (lines 204-300):
  runComplianceReminderScan(now: Date): Promise<...>   // region fan-out wrapper
  // For region 'ME' it reads items WHERE severity='BLOCKING', status: { in: ['PENDING','EXPIRED'] },
  //   expiresAt: { not: null }, expiryJurisdictionTz: { not: null } (lines 212-230), then flipExpiredFreeZoneItems
  //   calls reEvaluateFreeZoneStatus for policyRuleId starting 'uae.free_zone' + status==='PENDING',
  //   issuing client.contractorComplianceItem.update({ where: { id }, data: { status: 'EXPIRED' } }) once the
  //   Asia/Dubai boundary is crossed (lines 270-300). Per-region failures are caught and return zero counts (251-257).

From packages/api/src/routers/workflow/workflow-shared.ts (lines 315-368):
  assertRunCompletable(tx: RunGateClient, workflowRunId: string, organizationId: string): Promise<void>
  // Reads tx.workflowRun.findUniqueOrThrow({ where: { id: workflowRunId }, select: { overrideMetadata: true } }).
  // overrideApplied when overrideMetadata?.blockedTaskKind === 'IP_VERIFICATION'.
  // If NOT overridden: tx.workflowTaskRun.findMany({ where: {
  //     workflowRunId, organizationId, taskType: 'IP_VERIFICATION', status: { in: ['TODO','IN_PROGRESS','BLOCKED'] }
  //   }, select: { id: true } }); if length>0 THROWS PRECONDITION_FAILED with
  //   cause { blockedTaskKind: 'IP_VERIFICATION', openTaskIds: string[] }.
  // Then checks tx.credentialReference.findMany WHERE status='PENDING'. assertRunCompletable NEVER calls writeAuditLog.

From packages/api/src/services/saudization-dashboard.ts (lines 164-218):
  projectOffboardingTrajectory(params: { headcount: { totalHeadcount: number; saudiHeadcount: number } | null; currentBand: NitaqatBand | null; offboardingContractorIsSaudi: boolean | null }): { currentRate; projectedRate; currentBand; advisory: true; authoritative: false }
  // Pure (single params arg, no DB, no throw). projectedTotal = total-1; projectedSaudi = isSaudi ? saudi-1 : saudi;
  //   projectedRate = projectedTotal>0 ? projectedSaudi/projectedTotal : null. NEVER returns a projectedBand (locked anti-feature).

From packages/api/src/__tests__/__fixtures__/gulf-fixtures.ts:
  makeMeOrg(overrides?) -> { id, name, dataRegion:'ME', countryCode:'AE'|'SA' }  // DEFAULT_ORG_ID = 'clmeorgaaaaaaaaaaaaaaaaaaaa'
  makeFreeZoneComplianceItem(overrides?) -> { id:'clmefzitemaaaaaaaaaaaaaaaaa', organizationId, contractorId, documentType:'UAE_FREE_ZONE_LICENSE', severity:'BLOCKING', policyRuleId:'uae.free_zone_license@v2', expiryJurisdictionTz:'Asia/Dubai', expiresAt, status }
  // DEFAULT_CONTRACTOR_ID = 'clmectraaaaaaaaaaaaaaaaaaaa' (matches CONTRACTOR_ID in the existing test).

From packages/api/src/__tests__/v6-cross-feature-composition.test.ts (the file being modified — read it fully first):
  - Existing hoisted `store`, `regionClientFactory`, `clientCache`, `auditWriteSpy`.
  - Existing `vi.mock('@contractor-ops/db', ...)` whose `prisma.contractorComplianceItem.findMany` (lines 100-108) currently filters ONLY on where.severity + where.status — this is the WR-02 defect to fix.
  - Existing `makeGateClient` (lines 278-294) whose `workflowTaskRun.findMany` (line 285) returns openIpTaskIds unconditionally — this is the WR-03 defect to fix.
  - Existing `recordValidFreeZoneItem(expiresAt)` helper, the F1+F3 / F3 / F4 / audit / locked-phrase describes, and `beforeEach` reset (lines 181-185).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the prisma + gate-client mocks honour their where predicates and seed a second org/contractor (WR-02 + WR-03)</name>
  <files>packages/api/src/__tests__/v6-cross-feature-composition.test.ts</files>
  <read_first>
    - packages/api/src/__tests__/v6-cross-feature-composition.test.ts (FULL — lines 1-387; this is the file you are modifying. Note the existing prisma mock at 100-108 and makeGateClient at 278-294.)
    - packages/api/src/services/compliance-payment-gate.ts (lines 86-99 — the exact where shape: contractorId.in + severity='BLOCKING' + status='EXPIRED' + contractor.is.organizationId. Read once.)
    - packages/api/src/routers/workflow/workflow-shared.ts (lines 315-368 — the exact workflowTaskRun.findMany where: workflowRunId + organizationId + taskType='IP_VERIFICATION' + status.in. Read once.)
    - packages/api/src/__tests__/__fixtures__/gulf-fixtures.ts (DEFAULT_ORG_ID / DEFAULT_CONTRACTOR_ID — the existing fixtures; you will add a SECOND org id + contractor id constant.)
  </read_first>
  <behavior>
    - Test: the `prisma.contractorComplianceItem.findMany` mock EXCLUDES a row whose contractorId is not in `where.contractorId.in` (a second-contractor EXPIRED BLOCKING row is not returned when only CONTRACTOR_ID is queried).
    - Test: the `prisma.contractorComplianceItem.findMany` mock EXCLUDES a row whose organizationId does not match `where.contractor.is.organizationId` (a second-org EXPIRED BLOCKING row for the same contractorId is not returned when ME_ORG is queried).
    - Test: `makeGateClient.workflowTaskRun.findMany` RETURNS the open IP task only when `where.taskType === 'IP_VERIFICATION'` AND `where.workflowRunId` matches; a non-IP open task (or a mismatched runId) is filtered out.
  </behavior>
  <action>
    Edit the existing `vi.mock('@contractor-ops/db', ...)` prisma mock (currently lines 100-108) so its `contractorComplianceItem.findMany` honours the full gate `where`: keep the existing `where.severity` and `where.status` (string-equality) checks, and ADD a `where.contractorId.in` membership check (`(where.contractorId as { in?: string[] })?.in` — when present, the row's `contractorId` must be in the array) and a `where.contractor.is.organizationId` check (`(where.contractor as { is?: { organizationId?: string } })?.is?.organizationId` — when present, the row's `organizationId` must equal it), mirroring the real gate query at compliance-payment-gate.ts:86-99 and the WR-02 fix snippet in 80-REVIEW.md. Then edit `makeGateClient` (lines 278-294): change `workflowTaskRun.findMany` from the unconditional `async () => (opts.openIpTaskIds ?? []).map(...)` to `async (args: { where?: { taskType?: string; workflowRunId?: string } }) => { ... }` that returns the `openIpTaskIds` rows ONLY when `args.where?.taskType === 'IP_VERIFICATION'` and (when the test supplies an expected runId via a new `opts.workflowRunId`) `args.where?.workflowRunId` matches; otherwise return `[]`, mirroring workflow-shared.ts:332-340 and the WR-03 fix in 80-REVIEW.md. Add module-level constants for a SECOND tenant: `OTHER_ORG_ID = 'clmeorgbbbbbbbbbbbbbbbbbbbb'` and `OTHER_CONTRACTOR_ID = 'clmectrbbbbbbbbbbbbbbbbbbbb'` (UPPER_SNAKE / cuid-shaped synthetic ids; no real secrets). Extend `recordValidFreeZoneItem` (or add a sibling `recordFreeZoneItemFor({ organizationId, contractorId, expiresAt, status })`) so a second-org / second-contractor EXPIRED BLOCKING row can be pushed onto the same `store`. Keep all enum-shaped fixture strings UPPER_SNAKE_CASE (severity 'BLOCKING', status 'EXPIRED'/'PENDING', documentType 'UAE_FREE_ZONE_LICENSE', expiryJurisdictionTz 'Asia/Dubai') per db:audit-enum-casing. Do NOT weaken any existing assertion. No real secrets, no network, no real DB.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test v6-cross-feature-composition</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test v6-cross-feature-composition` passes (all assertions green, including the pre-existing ones).
    - The prisma mock filters on contractorId.in — verifiable: `grep -nE "where\.contractorId|contractorId.*in" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` returns a line inside the `@contractor-ops/db` prisma findMany mock.
    - The prisma mock filters on organizationId via `where.contractor.is.organizationId` — verifiable: `grep -nE "contractor.*is.*organizationId|organizationId" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` returns a line inside the prisma findMany mock.
    - `makeGateClient.workflowTaskRun.findMany` inspects `where.taskType` — verifiable: `grep -nE "taskType.*IP_VERIFICATION|where\.taskType" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` returns a line inside `makeGateClient`.
    - A second org + contractor are seeded — verifiable: `grep -Ec "OTHER_ORG_ID|OTHER_CONTRACTOR_ID" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` >= 1.
    - No real credential literals: `grep -inE "AKIA|sk_live|ghp_|-----BEGIN" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` returns nothing.
  </acceptance_criteria>
  <done>The payment-gate prisma mock honours contractorId+organizationId, makeGateClient.workflowTaskRun.findMany honours taskType+workflowRunId, a second org/contractor row is available on the shared store, and the file's tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the single composed-scenario describe that threads one contractor's shared state through F1 -> F3 -> F4, plus the honest enforced-vs-audit split</name>
  <files>packages/api/src/__tests__/v6-cross-feature-composition.test.ts</files>
  <read_first>
    - packages/api/src/__tests__/v6-cross-feature-composition.test.ts (the in-progress file from Task 1 — reuse its store, mocks, recordValidFreeZoneItem, makeGateClient, OTHER_ORG_ID/OTHER_CONTRACTOR_ID)
    - packages/api/src/services/compliance-payment-gate.ts (lines 102-120, 165-187 — enforced throw writes NO audit row; would-block branch (flagEnabled:false) writes the compliance.payment.would_block row. This is the honest resolution constraint.)
    - packages/api/src/services/saudization-dashboard.ts (lines 191-218 — projectOffboardingTrajectory: pure, projectedRate = projectedSaudi/projectedTotal, no projectedBand)
  </read_first>
  <behavior>
    - Test (the composed scenario, ONE it body): seed the SAME contractor (CONTRACTOR_ID under ME_ORG) with a free-zone BLOCKING PENDING item on `store`, AND seed a second-org/second-contractor EXPIRED BLOCKING row on the same `store` (tenant-isolation control), AND build a gate client with an open IP_VERIFICATION task. Then in sequence on the shared state:
        1. runComplianceReminderScan(now) flips CONTRACTOR_ID's PENDING free-zone item to EXPIRED (assert store row status === 'EXPIRED').
        2. assertContractorPaymentEligibility([CONTRACTOR_ID], { organizationId: ME_ORG.id }) THROWS PRECONDITION_FAILED (enforced/default flag ON); cause.contractorReasons has length 1 and the single reason's contractorId === CONTRACTOR_ID and policyRuleId === 'uae.free_zone_license@v2' (the OTHER_ORG/OTHER_CONTRACTOR row is excluded by the where filters — load-bearing tenant isolation).
        3. projectOffboardingTrajectory({ headcount: derived from the SAME seeded org context (a headcount object defined alongside the seeded contractor, NOT free literals), currentBand: 'MID_GREEN', offboardingContractorIsSaudi: true }) returns advisory:true, authoritative:false, projectedRate < currentRate, and `.not.toHaveProperty('projectedBand')`.
        4. assertRunCompletable(gateClient, runId, ME_ORG.id) THROWS PRECONDITION_FAILED with cause.blockedTaskKind === 'IP_VERIFICATION' and openTaskIds containing the seeded task id.
      All four steps run against ONE coherent contractor state in a single flow (no beforeEach reset mid-flow).
    - Test (enforced path writes no audit row): within the composed flow, after the enforced throw at step 2, assert auditWriteSpy was NOT called by the payment gate (the enforced branch writes none) — documenting the real service behaviour.
    - Test (would-block audit evidence, SEPARATE assertion): in a sibling assertion using the SAME EXPIRED store row, call assertContractorPaymentEligibility([CONTRACTOR_ID], { organizationId: ME_ORG.id, flagEnabled: false }); assert result.wouldBlock===true, result.blocked===false, and auditWriteSpy was called with expect.objectContaining({ action: 'compliance.payment.would_block', organizationId: ME_ORG.id, metadata: objectContaining({ contractorReasons: [{ contractorId: CONTRACTOR_ID }] (exactly one, via toHaveLength(1) on the captured metadata) }) }). Inline comment cites compliance-payment-gate.ts:106-120 (enforced throws no row; would-block branch is the documented audit-emitting path) so the deviation is honest, not faked.
  </behavior>
  <action>
    Add ONE new describe block, e.g. `describe('SC#1 — F1+F3+F4 compose on ONE seeded contractor (single shared store)', ...)`, near the top of the test bodies (after the helpers/mocks, before or replacing the five isolated describes per your judgment — but you MUST preserve the F3-advisory shape assertion, the Phase-74 override-clears-block assertion, and the locked-phrase invariant somewhere in the file; do not delete net coverage). Inside the composed `it`, build a single `SEEDED` context object that holds the contractor id, org id, and a `headcount` ({ totalHeadcount, saudiHeadcount }) so step 3 derives its numbers from the SAME context the free-zone item and IP task reference — not from disconnected literals. Push the CONTRACTOR_ID PENDING free-zone item AND the OTHER_ORG/OTHER_CONTRACTOR EXPIRED BLOCKING row onto `store` (the second row proves the where filters from Task 1 are load-bearing). Run the four steps in sequence exactly as the behavior block specifies; use the `try/catch` + `instanceof TRPCError` + `.code === 'PRECONDITION_FAILED'` assertion style already in the file (do not introduce a different rejection-assertion style). For the enforced-vs-audit split: assert the enforced throw writes no row (`expect(auditWriteSpy).not.toHaveBeenCalled()` immediately after the enforced step, before any would-block call), then a SEPARATE step (or sibling `it` in the same describe) drives `flagEnabled: false` to capture the `compliance.payment.would_block` row and asserts `toHaveLength(1)` on the captured metadata.contractorReasons plus an exact `contractorId: CONTRACTOR_ID` match (NOT arrayContaining alone — pin it). Add the inline comment citing the real service branch. Keep the gate client's open IP task threaded with the same runId the assertion passes (Task 1 made the runId/taskType filter load-bearing). Do NOT add F2 (D-01). Do NOT assert a projectedBand. Do NOT assert "hard-block AND audit row on the same call" — that behaviour does not exist in the service. All enum strings UPPER_SNAKE_CASE; no real secrets; no network; no real DB.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test v6-cross-feature-composition</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api test v6-cross-feature-composition` passes with the new composed-scenario test plus all preserved assertions green.
    - A single test body threads runComplianceReminderScan -> assertContractorPaymentEligibility -> projectOffboardingTrajectory -> assertRunCompletable on shared state — verifiable: the new describe contains all four call identifiers within one `it`; `grep -nE "runComplianceReminderScan|assertContractorPaymentEligibility|projectOffboardingTrajectory|assertRunCompletable" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` shows all four, and the new describe's `it` body (read it) contains all four in sequence.
    - The enforced throw is asserted to write NO audit row, and a separate would-block assertion captures the audit row — verifiable: file contains both `not.toHaveBeenCalled` (enforced) and `compliance.payment.would_block` (would-block) — `grep -c "compliance.payment.would_block" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` >= 1 AND `grep -c "flagEnabled" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` >= 1.
    - The would-block contractorReasons are pinned to exactly one entry for CONTRACTOR_ID — verifiable: `grep -c "toHaveLength(1)" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` >= 1.
    - The composed flow derives headcount from a shared seeded context (not disconnected literals) — verifiable by reading the new `it`: the `projectOffboardingTrajectory` headcount arg references the same SEEDED context object the free-zone item / IP task use; no inline `{ totalHeadcount: 100, saudiHeadcount: 50 }` literal inside the composed it that is unconnected to the seeded contractor.
    - F2 is NOT composed — verifiable: `grep -inE "ACCESS_REVOKE|deprovision|idp" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` returns nothing (or only a comment explaining the D-01 exclusion).
    - The F3 advisory shape, the override-clears-block path, and the Gulf locked-phrase invariant are still present — verifiable: `grep -Ec "projectedBand" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` >= 1 AND `grep -Ec "LOCKED_AE_PHRASES|LOCKED_SA_PHRASES" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` >= 1 AND `grep -Ec "overrideMetadata|blockedTaskKind" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` >= 1.
    - API typecheck clean for the package: `pnpm typecheck --filter @contractor-ops/api` exits 0.
    - No real credential literals: `grep -inE "AKIA|sk_live|ghp_|-----BEGIN" packages/api/src/__tests__/v6-cross-feature-composition.test.ts` returns nothing.
  </acceptance_criteria>
  <done>One composed-scenario test seeds a single contractor's shared store, runs scan -> EXPIRED -> enforced payment hard-block -> advisory from the same seeded headcount -> IP_VERIFICATION offboarding hard-block in sequence; the enforced path is asserted to write no audit row while a separate would-block assertion captures the documented audit row (pinned to exactly CONTRACTOR_ID); the second org/contractor row makes tenant isolation + the taskType filter load-bearing; all pre-existing assertions (F3 shape, override, locked-phrase) remain green; F2 stays excluded (D-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → mock Prisma store | All inputs are in-process plain objects; no external/untrusted data crosses any boundary. No real DB, no network, no IdP/gov API calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Information Disclosure | modified test file | mitigate | Test seeds only synthetic cuid-shaped ids + fixture strings; NO real secrets, API keys, or PII. Both tasks' acceptance criteria grep for `AKIA`/`sk_live`/`ghp_`/PEM headers and require zero hits. The new second-org/second-contractor ids are synthetic constants. |
| T-80-02 | Tampering | npm/pip/cargo installs | accept | This plan adds NO dependencies (only edits one existing test that imports existing `@contractor-ops/*` workspace packages + `vitest`/`@trpc/server` already present). No package-legitimacy checkpoint required. |
| T-80-SC | Tampering | supply chain | accept | No installs in this plan; nothing to verify. Surface is one already-existing test file edited in place; no production code path is modified. |

Threat surface is minimal by design: this plan edits ONE test that composes already-shipped, already-tested services against in-memory mocks. No production source is modified.
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test v6-cross-feature-composition` passes (composed-scenario test + all preserved assertions green).
- `pnpm typecheck --filter @contractor-ops/api` exits 0.
- A single `it` body threads runComplianceReminderScan -> assertContractorPaymentEligibility (enforced throw) -> projectOffboardingTrajectory (advisory from shared headcount) -> assertRunCompletable (IP_VERIFICATION hard-block) on ONE shared store.
- The prisma mock honours contractorId+organizationId; makeGateClient honours taskType+workflowRunId; a second org/contractor row makes both load-bearing.
- The enforced path asserts NO audit row; a separate would-block assertion captures the documented `compliance.payment.would_block` row pinned to exactly CONTRACTOR_ID.
- No `seed-dev` import; no real secrets; F2 not composed (D-01).
</verification>

<success_criteria>
SC#1 is met: the cross-feature integration test exercises the full F1+F3+F4 composition on ONE seeded contractor in a single sequential flow against shared store state — payment hard-blocked (enforced) AND offboarding hard-blocked (IP_VERIFICATION) AND Saudization band-trajectory advisory shown from the same seeded headcount; the gate-path audit row is captured via the honest would-block branch (the enforced path writes none, per the real service); tenant isolation + the IP taskType filter are load-bearing via a second seeded org/contractor; locked-phrase guard green; F2 deliberately excluded (D-01/D-02/D-03).
</success_criteria>

<output>
Create `.planning/phases/80-v6-0-verification-hardening-manual-uat/80-05-SUMMARY.md` when done. Record the final test file path, the pass count, the new composed-scenario test name, confirm the WR-02/WR-03 mock fixes landed, confirm the enforced-vs-audit split is documented honestly (enforced throws no row; would-block branch captures the row), and confirm F2 was deliberately NOT composed (per D-01). State the test pass/fail result explicitly — it is the SC#1 verification input for re-verification.
</output>
