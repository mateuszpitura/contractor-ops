---
phase: 80-v6-0-verification-hardening-manual-uat
plan: 04
type: execute
wave: 2
depends_on:
  - 80-01-cross-feature-composition-test
files_modified:
  - .planning/phases/80-v6-0-verification-hardening-manual-uat/80-RETROSPECTIVE.md
autonomous: true
requirements: []   # verification phase — 0 ROADMAP requirement IDs
must_haves:
  truths:
    - "Every D-04 v6.0 gate is RE-RUN and its result RECORDED in 80-RETROSPECTIVE.md (PASS / pre-existing-offender-documented), including pnpm audit + pnpm security:scan"
    - "The cross-feature integration test result (from Plan 80-01) is recorded in the retrospective"
    - "All 24 PENDING Unleash flags are enumerated by namespace with their post-deploy approval-ticket pointers (from signoff-registry-flags.json notes / getAllPending())"
    - "Hard dependencies that played out as planned vs differed are documented"
    - "Plan-completion velocity vs the v5.0 baseline is computed and recorded"
  artifacts:
    - path: ".planning/phases/80-v6-0-verification-hardening-manual-uat/80-RETROSPECTIVE.md"
      provides: "v6.0 milestone-close retrospective with hardening-gate results, PENDING-flag inventory, velocity"
      contains: "## "
  key_links:
    - from: ".planning/phases/80-v6-0-verification-hardening-manual-uat/80-RETROSPECTIVE.md"
      to: "packages/feature-flags/src/signoff-registry-flags.json"
      via: "getAllPending() / notes → PENDING-flag-by-namespace inventory"
      pattern: "gulf|idp-deprovisioning|compliance"
    - from: ".planning/phases/80-v6-0-verification-hardening-manual-uat/80-RETROSPECTIVE.md"
      to: ".planning/phases/80-v6-0-verification-hardening-manual-uat/80-01-SUMMARY.md"
      via: "records the cross-feature integration test pass/fail result"
      pattern: "composition"
---

<objective>
Run the D-04 hardening pass (milestone-wide re-run of all v6.0 CI gates + dependency/security scan, results RECORDED) and write `80-RETROSPECTIVE.md` (SC#4, D-05). The retrospective documents: (a) hard dependencies planned-vs-differed, (b) all PENDING Unleash flags by namespace + post-deploy approval ticket pointers, (c) plan-completion velocity vs v5.0 baseline — plus the recorded gate results and the cross-feature integration-test outcome.

Purpose: this is the milestone-close artifact. It proves the v6.0 gates still pass milestone-wide, inventories the deferred PENDING-flag approvals, and records the velocity learning.
Output: `.planning/phases/80-v6-0-verification-hardening-manual-uat/80-RETROSPECTIVE.md`.

Why Wave 2: the retrospective records the Plan 80-01 integration-test result and the hardening-gate results — both must be KNOWN before the retrospective is written. This plan runs the gates itself, then writes the doc.

Out of scope (enforce): FIXING pre-existing unrelated gate offenders (record them as known/documented, do not fix — Standing Constraint + phase scope); perf/load testing (LOCAL-ONLY, no prod traffic); new feature code; modifying any gate script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-v6-0-verification-hardening-manual-uat/80-CONTEXT.md
@.planning/phases/80-v6-0-verification-hardening-manual-uat/80-PATTERNS.md

# Integration-test outcome to record (this plan depends on it):
@.planning/phases/80-v6-0-verification-hardening-manual-uat/80-01-SUMMARY.md

# PENDING-flag inventory source:
@packages/feature-flags/src/signoff-registry-flags.json

# Retrospective layout precedent (Goal Recap + per-gate regression table + Post-Deploy + Verdict):
@.planning/milestones/v5.0-phases/69-de-i18n-parity-fix/69-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run all D-04 v6.0 gates + dependency/security scan and record each result</name>
  <files>.planning/phases/80-v6-0-verification-hardening-manual-uat/80-RETROSPECTIVE.md</files>
  <read_first>
    - .planning/phases/80-v6-0-verification-hardening-manual-uat/80-CONTEXT.md (D-04 — the exact gate list + the "record results, no perf pass" framing)
    - .planning/milestones/v5.0-phases/69-de-i18n-parity-fix/69-VERIFICATION.md (lines 43-47 "Cross-Phase Regression Check" — the per-gate PASS/evidence table format to mirror)
    - package.json (root scripts: lint:audit-log, lint:raw-sql, lint:logs, lint:silent-catch, lint:schema, i18n:parity, check:web-vite-{data-layer,page-shells,presentational,table-pattern,dialog-pattern}, security:scan)
    - packages/db/package.json (db:audit-enum-casing lives here — run via `pnpm --filter @contractor-ops/db db:audit-enum-casing`)
  </read_first>
  <action>
    Run each D-04 gate from the repo root and capture exit code + a one-line evidence summary for each: `pnpm lint:audit-log`, `pnpm lint:raw-sql`, `pnpm lint:logs`, `pnpm lint:silent-catch`, `pnpm lint:schema`, `pnpm i18n:parity`, `pnpm --filter @contractor-ops/db db:audit-enum-casing`, `pnpm check:web-vite-data-layer`, `pnpm check:web-vite-page-shells`, `pnpm check:web-vite-presentational`, `pnpm check:web-vite-table-pattern`, `pnpm check:web-vite-dialog-pattern`, `pnpm audit`, `pnpm security:scan`. Do NOT run the full web-vite vitest suite (memory: full unscoped run exhausts Mac RAM) — these are static/lint gates, not the test suite. Start `80-RETROSPECTIVE.md` with the 69-VERIFICATION header shape (frontmatter `status`, `phase`, `verified`, a short Goal Recap that v6.0 = phases 70-80) and a "## Hardening Gate Re-Run (D-04)" section: a table with columns Gate / Command / Result / Evidence, one row per gate above, each recording PASS or, for any gate that surfaces a pre-existing unrelated offender, "PRE-EXISTING — documented, out of scope to fix" with the offender named. The criterion is re-run-and-record; a gate surfacing a known pre-existing failure is RECORDED, never fixed. If `pnpm audit` reports advisories, record the count + severity, note any that pre-date this phase, and do NOT change dependencies (7-day release-age policy + scope: record only).
  </action>
  <verify>
    <automated>test -f .planning/phases/80-v6-0-verification-hardening-manual-uat/80-RETROSPECTIVE.md && grep -qiE "Hardening Gate Re-Run|D-04" .planning/phases/80-v6-0-verification-hardening-manual-uat/80-RETROSPECTIVE.md && echo GATES_RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - `80-RETROSPECTIVE.md` exists with a "## Hardening Gate Re-Run (D-04)" section.
    - Every gate from the D-04 list has a recorded result row — verifiable: each of `lint:audit-log`, `lint:raw-sql`, `lint:logs`, `lint:silent-catch`, `lint:schema`, `i18n:parity`, `audit-enum-casing`, `web-vite-data-layer`, `web-vite-page-shells`, `web-vite-presentational`, `web-vite-table-pattern`, `web-vite-dialog-pattern`, `audit`, `security:scan` appears at least once in the doc (`grep -F` each token returns a hit).
    - Each gate row records PASS or a documented pre-existing offender (not a silent gap) — verifiable: the table has a Result/evidence column populated for every row; any non-PASS row carries "pre-existing"/"out of scope" wording.
    - No gate script or dependency was modified by this plan — verifiable: `git status --porcelain` shows only `.planning/...80-RETROSPECTIVE.md` (and the SUMMARY) as changed; no `package.json` / `scripts/` / `pnpm-lock.yaml` diff.
  </acceptance_criteria>
  <done>All 14 D-04 gates re-run; each result recorded in the retrospective; no pre-existing offender fixed, no dependency changed.</done>
</task>

<task type="auto">
  <name>Task 2: Write the dependency-playout, PENDING-flag inventory, and velocity sections + verdict</name>
  <files>.planning/phases/80-v6-0-verification-hardening-manual-uat/80-RETROSPECTIVE.md</files>
  <read_first>
    - packages/feature-flags/src/signoff-registry-flags.json (THE INVENTORY SOURCE — 24 PENDING entries; each `notes` field is the post-deploy approval pointer; group by namespace prefix: idp-deprovisioning*, compliance-payment-block, compliance-policy-engine.*, compliance-portal-self-service, offboarding-ip-foundation, gulf.free-zone-tracking, gulf.saudization-dashboard)
    - packages/validators/src/legal/signoff-registry.ts (getAllPending() — the parallel helper to programmatically enumerate PENDING keys; cite it as the enumeration method)
    - .planning/ROADMAP.md (the v6.0 "Hard dependencies" block: 70→all, 71→79, 71→75, 74→75, 75→76, 76→77→78 — record each as played-out-as-planned vs differed; STATE.md notes the 75-08 e-sign DEFERRED and the 73 re-plan-against-web-vite as deviations)
    - .planning/phases/80-v6-0-verification-hardening-manual-uat/80-01-SUMMARY.md (record the cross-feature integration-test pass/fail outcome)
    - .planning/phases/80-v6-0-verification-hardening-manual-uat/80-PATTERNS.md (the 80-RETROSPECTIVE section — the three required sections + the velocity computation source: count *-PLAN.md / *-SUMMARY.md per phase dir under v5.0-phases and v6.0-phases)
  </read_first>
  <action>
    Append three sections to `80-RETROSPECTIVE.md`. (1) "## Hard Dependency Play-Out" — a table/list of each v6.0 hard dependency from the ROADMAP dependency block, marked planned-as-expected or differed, with the differences named (e.g. 75-08 e-sign signing + webhook IP-ratification atomic flow DEFERRED per STATE.md; Phase 73 re-planned against `apps/web-vite` after `apps/web` removal; Phase 78 concurrent-executor handoff). Also record the cross-feature integration-test result from the 80-01 summary (compose-proven F1+F3+F4; F2 deliberately UAT-only per D-01). (2) "## PENDING Unleash Flags by Namespace" — enumerate all 24 PENDING flags grouped by namespace prefix (idp-deprovisioning* [6], compliance-* [policy-engine.{uk,de,pl,ksa,uae} + payment-block + portal-self-service], offboarding-ip-foundation, gulf.{free-zone-tracking,saudization-dashboard}); for each, give the post-deploy approval pointer from its `notes`; state the enumeration source is `getAllPending()` / `signoff-registry-flags.json`. (3) "## Velocity vs v5.0 Baseline" — compute plan-completion velocity: count `*-PLAN.md` (or `*-SUMMARY.md`) per phase dir under `.planning/milestones/v5.0-phases/` (phases 56-69) and `.planning/milestones/v6.0-phases/` (phases 70-80), report total plans + a plans/phase (or plans/day if SUMMARY dates are used) metric for each milestone, and a one-line comparison (computation method at discretion — state which denominator you used). Close with a "## Verdict" mirroring 69-VERIFICATION.md: milestone-complete-pending-post-deploy under the LOCAL-ONLY / DEFERRED Standing Constraint (post-deploy legal + UAT items do not block closure).
  </action>
  <verify>
    <automated>grep -qiE "PENDING Unleash Flags by Namespace|Velocity vs v5.0|Hard Dependency Play-Out" .planning/phases/80-v6-0-verification-hardening-manual-uat/80-RETROSPECTIVE.md && grep -qiE "Verdict" .planning/phases/80-v6-0-verification-hardening-manual-uat/80-RETROSPECTIVE.md && echo SECTIONS_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - All three SC#4 sections present — verifiable: `grep -Eic "Hard Dependency Play-Out" …`, `grep -Eic "PENDING Unleash Flags by Namespace" …`, `grep -Eic "Velocity vs v5.0" …` each >= 1.
    - All 24 PENDING namespaces enumerated — verifiable: each of the namespace prefixes `idp-deprovisioning`, `compliance-payment-block`, `compliance-policy-engine`, `compliance-portal-self-service`, `offboarding-ip-foundation`, `gulf.free-zone-tracking`, `gulf.saudization-dashboard` appears in the doc; the section names `getAllPending()` or `signoff-registry-flags.json` as the source.
    - Each PENDING flag has a post-deploy approval pointer — verifiable: the flag inventory references the `notes`/approval-ticket source (not just a bare flag list).
    - Hard dependencies recorded planned-vs-differed AND the 80-01 integration-test result recorded — verifiable: `grep -Eic "as planned|differed|deferred" …` >= 1 and `grep -Eic "composition|F1.*F3.*F4|integration test" …` >= 1.
    - Velocity metric computed for both milestones — verifiable: the section cites a plan count for v5.0 (phases 56-69) and v6.0 (phases 70-80) and a comparison.
    - Verdict states milestone-complete-pending-post-deploy under LOCAL-ONLY/DEFERRED — verifiable: `grep -Eic "Verdict" …` >= 1 and `grep -Eic "post-deploy|LOCAL-ONLY|DEFERRED|do not block" …` >= 1.
  </acceptance_criteria>
  <done>80-RETROSPECTIVE.md records hard-dependency play-out + the integration-test result, enumerates all 24 PENDING flags by namespace with approval pointers, computes velocity vs v5.0, and closes with a LOCAL-ONLY-aware verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source → gate scripts (read-only execution) | This plan EXECUTES existing static/lint gate scripts and reads an in-repo JSON registry. It writes one markdown doc. No untrusted input crosses any boundary; the gates run against the committed tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-04-01 | Information Disclosure | 80-RETROSPECTIVE.md | mitigate | Gate output summaries and the flag inventory restate registry `notes` + gate PASS/FAIL; the doc exposes NO credentials. `pnpm audit` output is summarised (counts/severity), not pasted with any token. Reviewer confirms no secret leakage. |
| T-80-04-02 | Tampering | dependency tree | mitigate | `pnpm audit` runs read-only; this plan records advisories and does NOT change any dependency (7-day release-age policy + scope: record-only). Acceptance criterion asserts no `package.json`/`pnpm-lock.yaml` diff. |
| T-80-04-SC | Tampering | supply chain | accept | No package installs in this plan; gates are existing scripts. `pnpm audit` + `pnpm security:scan` are themselves the supply-chain check and their results are recorded. |

Threat surface: running already-checked-in gate scripts read-only and writing a markdown summary. No production code path modified.
</threat_model>

<verification>
- `80-RETROSPECTIVE.md` exists with the hardening-gate table (all 14 D-04 gates recorded), hard-dependency play-out (incl. 80-01 test result), 24-PENDING-flag inventory, velocity vs v5.0, and a LOCAL-ONLY-aware verdict.
- No gate script or dependency modified (`git status` shows only the retrospective + SUMMARY).
</verification>

<success_criteria>
The v6.0 retrospective documents (a) hard dependencies planned-vs-differed, (b) all PENDING Unleash flags by namespace with post-deploy approval pointers, (c) plan-completion velocity vs the v5.0 baseline — and records the D-04 milestone-wide gate re-run results + the cross-feature integration-test outcome (SC#4, D-04, D-05).
</success_criteria>

<output>
Create `.planning/phases/80-v6-0-verification-hardening-manual-uat/80-04-SUMMARY.md` when done. Record the gate pass/fail tally, the velocity numbers, and that all 24 PENDING flags were enumerated.
</output>
