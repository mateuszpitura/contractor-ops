---
phase: 78
plan: 02
type: execute
wave: 1
depends_on: [78-01]
files_modified:
  - packages/integrations/src/idp/error-classifier.ts
  - packages/integrations/src/idp/__tests__/error-classifier.test.ts
autonomous: true
requirements: [IDP-05, IDP-06, IDP-07]
requirements_addressed: [IDP-05, IDP-06, IDP-07]

must_haves:
  truths:
    - "D-13: the Phase 77 D-07 error classifier is extended for the 3 new providers (Entra/Okta/GitHub) with the per-error-code → ErrorClass mappings pinned from each provider's response shapes (resolved in RESEARCH.md); the `ErrorClass` closed enum is NOT widened"
    - "`packages/integrations/src/idp/error-classifier.ts` `classifyError` (Phase 77 D-07) handles `provider` values `'ENTRA_ID' | 'OKTA' | 'GITHUB'` IN ADDITION to existing `'GOOGLE_WORKSPACE' | 'SLACK'` — existing cases are NOT changed"
    - "Entra mapping: 401 → `PERMANENT_AUTH_EXPIRED`; 403 with Graph error code `Authorization_RequestDenied` → `PERMANENT_FORBIDDEN`; 404 → `PERMANENT_NOT_FOUND`; 429 → `TRANSIENT_RATE_LIMIT`; 5xx/503 → `TRANSIENT_RATE_LIMIT` or `PERMANENT_OTHER` per existing 5xx convention; network errors → `TRANSIENT_NETWORK`"
    - "Okta mapping: 401 → `PERMANENT_AUTH_EXPIRED`; 403 → `PERMANENT_FORBIDDEN`; 404 → `PERMANENT_NOT_FOUND`; 429 → `TRANSIENT_RATE_LIMIT`"
    - "GitHub mapping: 401 → `PERMANENT_AUTH_EXPIRED`; 403 → `PERMANENT_FORBIDDEN`; 403 with `require_two_factor_authentication` message → a GitHub-specific `PERMANENT_FORBIDDEN` with descriptive errorClass detail; 404 → `PERMANENT_NOT_FOUND`; 429 → `TRANSIENT_RATE_LIMIT`"
    - "GitHub secondary-rate-limit (403 with `Retry-After`/`x-ratelimit-remaining: 0`) is classified `TRANSIENT_RATE_LIMIT`, NOT `PERMANENT_FORBIDDEN` — GitHub returns 403 for rate limits, distinct from a true auth-forbidden"
    - "`error-classifier.test.ts` extended with explicit cases enumerating each HTTP status × each of the 3 new providers; `pnpm --filter @contractor-ops/integrations test error-classifier` exits 0"
    - "`pnpm --filter @contractor-ops/integrations typecheck` exits 0 — the `ErrorClass` closed enum is not widened"
  artifacts:
    - path: "packages/integrations/src/idp/error-classifier.ts"
      provides: "classifyError extended for Entra/Okta/GitHub"
      contains: "ENTRA_ID"
    - path: "packages/integrations/src/idp/__tests__/error-classifier.test.ts"
      provides: "per-provider status classification tests"
      contains: "require_two_factor_authentication"
  key_links:
    - from: "packages/integrations/src/idp/error-classifier.ts"
      to: "packages/integrations/src/idp/error-classifier.ts (Phase 77 D-07)"
      via: "extend existing classifyError switch — additive provider cases"
      pattern: "classifyError"
---

<objective>
Extend the Phase 77 D-07 error classifier (`packages/integrations/src/idp/error-classifier.ts`) with per-provider HTTP-status → `ErrorClass` mappings for Entra ID, Okta, and GitHub, so the saga's per-class retry budgeting works for the three new providers. The `ErrorClass` closed enum is unchanged; only new provider cases are added. The GitHub case must correctly distinguish a 403 rate-limit (TRANSIENT) from a 403 auth-forbidden (PERMANENT) — GitHub overloads 403 for both, and misclassifying it wastes retry budget or gives up too early.

This is a small, focused plan deliberately separated from the adapters so it can land in Wave 1 (after the type foundation) and be consumed by all three adapter plans in Wave 2.

Output: 1 classifier extension + 1 test file extension.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v6.0-phases/78-f2-idp-entra-id-okta-github-adapters-the-differentiator/78-CONTEXT.md
@.planning/milestones/v6.0-phases/78-f2-idp-entra-id-okta-github-adapters-the-differentiator/78-RESEARCH.md
@.planning/milestones/v6.0-phases/77-f2-idp-gws-slack-adapters-the-wedge/77-CONTEXT.md
@packages/integrations/src/idp/error-classifier.ts
</context>

<dependency_gate>
Requires `packages/integrations/src/idp/error-classifier.ts` (Phase 77 D-07) to exist with the `ErrorClass` closed enum and a `classifyError` function keyed by provider + HTTP status. If the file does not exist, STOP and report "Phase 78-02 blocked: Phase 77 error-classifier not merged." Do not create a new classifier from scratch.
</dependency_gate>

<threat_model>
| ID | Threat | Mitigation |
|----|--------|-----------|
| T-78-02-01 | GitHub 403 rate-limit misclassified as PERMANENT_FORBIDDEN → step gives up instead of retrying | Inspect `x-ratelimit-remaining: 0` and/or `Retry-After` header / `secondary rate limit` body marker → classify `TRANSIENT_RATE_LIMIT`. Only a 403 WITHOUT rate-limit signals + with a forbidden/SSO body is `PERMANENT_FORBIDDEN`. Test both branches. |
| T-78-02-02 | Entra 403 `Authorization_RequestDenied` (missing app permission) retried forever | Map to `PERMANENT_FORBIDDEN` immediately; surface a scope-config hint (mirrors Phase 77 `lint:scopes`). Test asserts no-retry class. |
| T-78-02-03 | Widening the `ErrorClass` enum breaks Phase 76/77 step-runner consumers | Do NOT add enum members; reuse the existing six classes. GitHub 2FA-required maps to `PERMANENT_FORBIDDEN` with a descriptive message field, not a new enum value. |
| T-78-02-04 | A provider returns a non-numeric/opaque error and falls through to a wrong default | Default fall-through for unmatched 4xx → `PERMANENT_OTHER`; unmatched network/timeout → `TRANSIENT_NETWORK`. Test the default branch per provider. |
</threat_model>

<tasks>

<task id="78-02-01">
<action>
Extend `classifyError` in `packages/integrations/src/idp/error-classifier.ts`. Read the full file first to learn the exact function signature (provider param name, how it receives status/body/headers, and the existing GWS/Slack branch structure). Add `'ENTRA_ID'`, `'OKTA'`, `'GITHUB'` handling consistent with how the existing providers are structured (switch arm, lookup table, or per-provider helper — match the existing style, do not introduce a new style).

Per-provider mapping (CONTEXT.md D-13):
- **Entra:** 401 → `PERMANENT_AUTH_EXPIRED`; 403 where the Graph error body code is `Authorization_RequestDenied` → `PERMANENT_FORBIDDEN`; 404 → `PERMANENT_NOT_FOUND`; 429 → `TRANSIENT_RATE_LIMIT`; 503 → `TRANSIENT_RATE_LIMIT`; transport/network → `TRANSIENT_NETWORK`; other 4xx → `PERMANENT_OTHER`.
- **Okta:** 401 → `PERMANENT_AUTH_EXPIRED`; 403 → `PERMANENT_FORBIDDEN`; 404 → `PERMANENT_NOT_FOUND`; 429 → `TRANSIENT_RATE_LIMIT`; transport → `TRANSIENT_NETWORK`; other 4xx → `PERMANENT_OTHER`.
- **GitHub:** 401 → `PERMANENT_AUTH_EXPIRED`; **403 with a rate-limit signal** (`x-ratelimit-remaining: 0` header OR body contains `secondary rate limit` OR a `Retry-After` header) → `TRANSIENT_RATE_LIMIT`; 403 with `require_two_factor_authentication` message OR generic forbidden → `PERMANENT_FORBIDDEN`; 404 → `PERMANENT_NOT_FOUND`; 429 → `TRANSIENT_RATE_LIMIT`; transport → `TRANSIENT_NETWORK`; other 4xx → `PERMANENT_OTHER`.

Where the classifier supports an optional descriptive detail/message field on the result (Phase 77 had `errorClass`/`lastErrorMessage` semantics), set a descriptive string for the GitHub 2FA case and the Entra `Authorization_RequestDenied` case so the saga UI can show an actionable hint. Do NOT add new `ErrorClass` enum members.
</action>
<read_first>
- packages/integrations/src/idp/error-classifier.ts (full file — signature, existing provider branches, ErrorClass enum)
- .planning/milestones/v6.0-phases/78-f2-idp-entra-id-okta-github-adapters-the-differentiator/78-CONTEXT.md (D-13)
- packages/integrations/src/adapters/slack-adapter.ts (how Phase 77 surfaces `slack_not_on_enterprise_grid` descriptive errorClass — mirror for descriptive details)
</read_first>
<acceptance_criteria>
- `grep -q "ENTRA_ID" packages/integrations/src/idp/error-classifier.ts` succeeds
- `grep -q "Authorization_RequestDenied" packages/integrations/src/idp/error-classifier.ts` succeeds
- classifier handles GitHub 403-rate-limit distinctly from 403-forbidden (both branches present in source)
- no new value added to the `ErrorClass` type (the six Phase 77 classes are unchanged)
- `pnpm --filter @contractor-ops/integrations typecheck` exits 0
</acceptance_criteria>
</task>

<task id="78-02-02">
<action>
Extend `packages/integrations/src/idp/__tests__/error-classifier.test.ts` (read it first to match the existing test structure) with explicit cases for each of the three new providers. For EACH provider add cases asserting:
- 401 → `PERMANENT_AUTH_EXPIRED`
- 403 (forbidden) → `PERMANENT_FORBIDDEN`
- 404 → `PERMANENT_NOT_FOUND`
- 429 → `TRANSIENT_RATE_LIMIT`
- default 4xx (e.g. 400/422) → `PERMANENT_OTHER`
- network/timeout error → `TRANSIENT_NETWORK`

Plus provider-specific cases:
- Entra: 403 body `{ error: { code: 'Authorization_RequestDenied' } }` → `PERMANENT_FORBIDDEN` with descriptive detail set
- GitHub: 403 with header `x-ratelimit-remaining: 0` → `TRANSIENT_RATE_LIMIT` (NOT forbidden)
- GitHub: 403 with body `require_two_factor_authentication` → `PERMANENT_FORBIDDEN` with descriptive detail

If the classifier source file (Phase 77) is the test file location convention differs (e.g. `__tests__/` vs co-located), follow whatever the existing classifier test uses.
</action>
<read_first>
- packages/integrations/src/idp/__tests__/error-classifier.test.ts (existing test shape, import path, how it constructs error inputs)
- packages/integrations/src/idp/error-classifier.ts (the extended classifier)
</read_first>
<acceptance_criteria>
- test file references `ENTRA_ID`, `OKTA`, `GITHUB`
- test file contains a case for GitHub 403 rate-limit → `TRANSIENT_RATE_LIMIT`
- test file contains a case for `require_two_factor_authentication`
- `pnpm --filter @contractor-ops/integrations test error-classifier` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
- `pnpm --filter @contractor-ops/integrations test error-classifier` exits 0
- `pnpm --filter @contractor-ops/integrations typecheck` exits 0
- existing GWS/Slack classification cases still pass (no regression)
</verification>
</content>
