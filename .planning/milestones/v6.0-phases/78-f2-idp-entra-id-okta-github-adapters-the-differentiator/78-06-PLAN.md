---
phase: 78
plan: 06
type: execute
wave: 3
depends_on: [78-03, 78-04, 78-05]
files_modified:
  - packages/integrations/src/adapters/register-all.ts
  - packages/feature-flags/src/signoff-registry-flags.json
  - packages/api/src/routers/integrations/entra.ts
  - packages/api/src/routers/integrations/okta.ts
  - packages/api/src/routers/integrations/github.ts
  - packages/api/src/routers/integrations/index.ts
  - packages/api/src/root.ts
  - packages/api/src/routers/integrations/__tests__/idp-deprovision-connections.test.ts
autonomous: true
requirements: [IDP-05, IDP-06, IDP-07]
requirements_addressed: [IDP-05, IDP-06, IDP-07]

must_haves:
  truths:
    - "`packages/integrations/src/adapters/register-all.ts` adds three HEAVY-tier dynamic imports for `EntraIdAdapter`, `OktaAdapter`, `GitHubAdapter`, each registered with BOTH `registerAdapter(instance)` AND `registerDeprovisionableAdapter('ENTRA_ID'|'OKTA'|'GITHUB', instance)` — the SAME instance passed to both registries (mirrors Phase 76-09 GWS)"
    - "D-12: THREE independent PENDING entries are added to the signoff registry (`idp-deprovisioning-entra`, `idp-deprovisioning-okta`, `idp-deprovisioning-github`) mirroring the Phase 77 D-15 pattern, and the Settings > Compliance per-provider table gains 3 new rows (UI in Plan 78-07); each provider is independently enable/disable-able for enterprise rollout"
    - "`packages/feature-flags/src/signoff-registry-flags.json` gains THREE PENDING entries (array-of-tuples shape): `idp-deprovisioning-entra`, `idp-deprovisioning-okta`, `idp-deprovisioning-github`, each `{ status: 'PENDING', notes: '...legal review deferred per Standing Constraint; dev via FLAG_SIGNOFF_BYPASS=local' }`; `getAllPendingFlags()` returns all three"
    - "Three per-provider connection routers (`entra.ts`, `okta.ts`, `github.ts`) under `packages/api/src/routers/integrations/` expose connection-lifecycle mutations (connect/store-credential, disconnect, health/test, enableProviderForOrg toggle) with Zod input schemas; tenant `organizationId` derived from session context, NEVER from client input"
    - "`enableProviderForOrg(provider)` toggle persists per-org per-provider (`Setting.idpDeprovisioningEnabled.{provider}` or the Phase 77 D-15 storage shape) and is only enableable when the provider's flag is APPROVED (or `FLAG_SIGNOFF_BYPASS=local`) — gated via `isFlagSignoffSatisfied('idp-deprovisioning-{provider}')`"
    - "Sensitive connection mutations call `writeAuditLog` (packages/api/src/services/audit-writer.ts); credentials stored encrypted via the existing credential/secret service, never plaintext, never echoed in the response"
    - "The three routers are barreled in `routers/integrations/index.ts` and mounted in `packages/api/src/root.ts` (extending the existing `integration` namespace or as sibling namespaces consistent with how 76/77 mount idp-deprovisioning); router count in root.ts increases accordingly"
    - "`pnpm --filter @contractor-ops/api typecheck` exits 0; `pnpm --filter @contractor-ops/integrations typecheck` exits 0"
    - "`pnpm --filter @contractor-ops/api test idp-deprovision-connections` exits 0"
  artifacts:
    - path: "packages/integrations/src/adapters/register-all.ts"
      provides: "3 HEAVY-tier adapter registrations + registerDeprovisionableAdapter calls"
      contains: "registerDeprovisionableAdapter"
    - path: "packages/feature-flags/src/signoff-registry-flags.json"
      provides: "3 PENDING idp-deprovisioning flags"
      contains: "idp-deprovisioning-entra"
    - path: "packages/api/src/routers/integrations/entra.ts"
      provides: "Entra connection lifecycle mutations"
      contains: "organizationId"
  key_links:
    - from: "packages/integrations/src/adapters/register-all.ts"
      to: "packages/integrations/src/registry.ts"
      via: "registerDeprovisionableAdapter (Phase 76-03)"
      pattern: "registerDeprovisionableAdapter"
    - from: "packages/api/src/routers/integrations/entra.ts"
      to: "packages/feature-flags/src/signoff-registry-flags.ts"
      via: "isFlagSignoffSatisfied('idp-deprovisioning-entra') gates enableProviderForOrg"
      pattern: "isFlagSignoffSatisfied"
    - from: "packages/api/src/routers/integrations/index.ts"
      to: "packages/api/src/root.ts"
      via: "barrel export mounted in appRouter integration namespace"
      pattern: "integrationRouter"
---

<objective>
Wire the three adapters into the running system: register them in the HEAVY adapter tier as `Deprovisionable` (so the saga can resolve them by provider), add the three PENDING feature-flag entries (per-provider enterprise rollout, LOCAL-ONLY bypass), and expose per-provider connection-lifecycle tRPC mutations (connect / store-credential / disconnect / health / per-org enable toggle) with tenant-from-session, Zod inputs, encrypted credential storage, and audit logging.

This is the integration seam between the adapters (Wave 2) and the UI (Wave 4). It does NOT modify the saga or schema (Phase 76/77 own those).

Output: register-all wiring + 3 flag entries + 3 connection routers + root.ts mount + 1 test file.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v6.0-phases/78-f2-idp-entra-id-okta-github-adapters-the-differentiator/78-CONTEXT.md
@.planning/milestones/v6.0-phases/78-f2-idp-entra-id-okta-github-adapters-the-differentiator/78-RESEARCH.md
@.planning/milestones/v6.0-phases/77-f2-idp-gws-slack-adapters-the-wedge/77-CONTEXT.md
@packages/integrations/src/adapters/register-all.ts
@packages/integrations/src/registry.ts
@packages/feature-flags/src/signoff-registry-flags.json
@packages/feature-flags/src/signoff-registry-flags.ts
@packages/api/src/routers/integrations/google-workspace.ts
@packages/api/src/routers/integrations/index.ts
@packages/api/src/root.ts
@packages/api/src/services/audit-writer.ts
</context>

<dependency_gate>
Requires 78-03/04/05 (the three adapter classes) and the Phase 76 `registerDeprovisionableAdapter` registry helper. If `registerDeprovisionableAdapter` is not exported from `packages/integrations/src/registry.ts`, STOP and report "Phase 78-06 blocked: Phase 76 registry helper not merged." If Phase 76/77 created a dedicated `idp-deprovisioning` router, PREFER extending it over creating sibling integration routers — read root.ts + routers/ to determine the actual mount point at execute time.
</dependency_gate>

<threat_model>
| ID | Threat | Mitigation |
|----|--------|-----------|
| T-78-06-01 | Client supplies `organizationId` to a connection mutation → cross-tenant credential write (IDOR) | Derive `organizationId` from session context (ctx), never from input. Zod input omits tenant id. Connection rows scoped to ctx org; RLS where available. |
| T-78-06-02 | A provider gets enabled before its flag is APPROVED → unreviewed deprovisioning in production | `enableProviderForOrg` checks `isFlagSignoffSatisfied('idp-deprovisioning-{provider}')` (APPROVED or `FLAG_SIGNOFF_BYPASS=local`); rejects with a clear error otherwise. Flags land PENDING. |
| T-78-06-03 | Stored credential (Graph secret / Okta token / GitHub token) echoed in a mutation response or audit | Store via the existing encrypted credential/secret service; return only connection status + masked metadata; audit allow-list excludes secrets (token_last_eight only for GitHub). |
| T-78-06-04 | Double registration error in register-all (registerAdapter + registerDeprovisionableAdapter are separate maps) | Pass the SAME instance to both; the two registries are distinct Maps (Phase 76 design). Mirror the GWS 76-09 pattern exactly. |
| T-78-06-05 | HEAVY-tier dynamic import increases cold start | The three adapters are lazy dynamic imports in the HEAVY tier (not eager ESSENTIAL) — no cold-start regression; matches existing HEAVY-tier providers. |
| T-78-06-06 | Missing audit on a sensitive mutation | Every connect/disconnect/enable mutation calls `writeAuditLog` with the org + actor + action; test asserts the audit write. |
</threat_model>

<tasks>

<task id="78-06-01">
<action>
Update `packages/integrations/src/adapters/register-all.ts`. Read the full file first to find the HEAVY-tier dynamic-import block and the existing GWS Deprovisionable registration (Phase 76-09 pattern). Add three HEAVY-tier registrations:
```ts
const { EntraIdAdapter } = await import('./entra-id-adapter.js');
const entra = new EntraIdAdapter();
registerAdapter(entra);
registerDeprovisionableAdapter('ENTRA_ID', entra);

const { OktaAdapter } = await import('./okta-adapter.js');
const okta = new OktaAdapter();
registerAdapter(okta);
registerDeprovisionableAdapter('OKTA', okta);

const { GitHubAdapter } = await import('./github-adapter.js');
const github = new GitHubAdapter();
registerAdapter(github);
registerDeprovisionableAdapter('GITHUB', github);
```
Place them in the HEAVY tier alongside the other lazy adapters; import `registerDeprovisionableAdapter` from `../registry.js` if not already imported. Use the SAME instance for both registries. Match the exact provider-key strings to the `ImpactPreview` union literals and the scope-registry provider list (`ENTRA_ID`, `OKTA`, `GITHUB`).
</action>
<read_first>
- packages/integrations/src/adapters/register-all.ts (full file — HEAVY tier block + existing registerDeprovisionableAdapter for GWS)
- packages/integrations/src/registry.ts (registerDeprovisionableAdapter signature + provider key type)
</read_first>
<acceptance_criteria>
- `grep -c "registerDeprovisionableAdapter" packages/integrations/src/adapters/register-all.ts` >= 3 new (GWS + Slack + Entra + Okta + GitHub once 76/77 present)
- `grep -q "EntraIdAdapter" packages/integrations/src/adapters/register-all.ts` succeeds
- the same instance variable is passed to both register calls for each provider
- `pnpm --filter @contractor-ops/integrations typecheck` exits 0
</acceptance_criteria>
</task>

<task id="78-06-02">
<action>
Add three PENDING entries to `packages/feature-flags/src/signoff-registry-flags.json`. Read the file first — it is an array of `[key, { status, notes }]` tuples. Append (preserving existing entries + JSON validity):
```json
["idp-deprovisioning-entra", { "status": "PENDING", "notes": "F2 IdP Entra ID deprovisioning (account disable + revokeSignInSessions + CA pre-flight + hybrid-AD hard-block). Legal review deferred per Standing Constraint; dev via FLAG_SIGNOFF_BYPASS=local." }],
["idp-deprovisioning-okta", { "status": "PENDING", "notes": "F2 IdP Okta deprovisioning (deactivate + revoke sessions). Legal review deferred per Standing Constraint; dev via FLAG_SIGNOFF_BYPASS=local." }],
["idp-deprovisioning-github", { "status": "PENDING", "notes": "F2 IdP GitHub deprovisioning (org-member removal + per-PAT SAML revoke + outside-collab manual flag). Legal review deferred per Standing Constraint; dev via FLAG_SIGNOFF_BYPASS=local." }]
```
If the flag keys must also be declared in `packages/feature-flags/src/registry.ts` (the typed registry), add them there following the existing `idp-deprovisioning-gws`/`-slack` (Phase 77) declarations. Confirm `getAllPendingFlags()` returns the three new keys.
</action>
<read_first>
- packages/feature-flags/src/signoff-registry-flags.json (tuple shape + existing entries)
- packages/feature-flags/src/signoff-registry-flags.ts (getAllPendingFlags / isFlagSignoffSatisfied / isGatedFlag)
- packages/feature-flags/src/registry.ts (typed key registry — add keys if required)
</read_first>
<acceptance_criteria>
- `node -e "JSON.parse(require('fs').readFileSync('packages/feature-flags/src/signoff-registry-flags.json','utf8'))"` exits 0 (valid JSON)
- `grep -q "idp-deprovisioning-entra" packages/feature-flags/src/signoff-registry-flags.json` succeeds (and okta + github)
- all three new entries have `"status": "PENDING"`
- `pnpm --filter @contractor-ops/feature-flags typecheck` exits 0
</acceptance_criteria>
</task>

<task id="78-06-03">
<action>
Create three per-provider connection routers under `packages/api/src/routers/integrations/`. Read `google-workspace.ts` (existing connection-lifecycle router) + `index.ts` + `root.ts` first to match the exact `createTRPCRouter` / procedure / ctx-org / audit conventions. If Phase 76/77 created a dedicated `idp-deprovisioning` router, ADD these mutations there instead (read root.ts at execute time to decide). For each provider (`entra.ts`, `okta.ts`, `github.ts`) expose:
- `connect` / `storeCredential` — protected mutation; Zod input = provider credential payload (Entra: tenantId + clientId + clientSecret for app-only; Okta: orgUrl + apiToken; GitHub: installationId/token + org login) WITHOUT any tenant id. `organizationId` from `ctx`. Store encrypted via the credential/secret service; create/update the `IntegrationConnection` row with the deprovision `subKind`. Call `writeAuditLog`. Return masked status only.
- `disconnect` — protected mutation; deletes/deactivates the connection; audit.
- `testConnection` / `health` — protected query/mutation; runs the adapter's health/getUser probe; returns status (no secret).
- `enableProviderForOrg` — protected mutation; Zod input `{ enabled: boolean }`; GATE: reject unless `isFlagSignoffSatisfied('idp-deprovisioning-{provider}')`; persist the per-org toggle (`Setting.idpDeprovisioningEnabled.{provider}` or the Phase 77 D-15 shape — read how 77 stores GWS/Slack toggles and match); audit.

Use the existing protected-procedure + permission middleware; reuse the Phase 77 `idp:override_step_failure` permission ONLY where step-override applies (not for connection mutations — those use the standard admin/integration-manage permission the existing integration router uses).
</action>
<read_first>
- packages/api/src/routers/integrations/google-workspace.ts (connection-lifecycle router conventions: createTRPCRouter, protectedProcedure, ctx.organizationId, audit)
- packages/api/src/routers/integrations/index.ts (barrel)
- packages/api/src/root.ts (mount pattern + integration namespace)
- packages/api/src/services/audit-writer.ts (writeAuditLog signature)
- packages/feature-flags/src/signoff-registry-flags.ts (isFlagSignoffSatisfied)
- packages/integrations/src/services/credential-service.ts (encrypted credential storage)
</read_first>
<acceptance_criteria>
- `grep -q "organizationId" packages/api/src/routers/integrations/entra.ts` succeeds (tenant from ctx, present in all three)
- `grep -q "isFlagSignoffSatisfied" packages/api/src/routers/integrations/entra.ts` succeeds (enable gate)
- `grep -q "writeAuditLog" packages/api/src/routers/integrations/entra.ts` succeeds
- no client-supplied `organizationId` in any Zod input schema (input schemas omit tenant id)
- `pnpm --filter @contractor-ops/api typecheck` exits 0
</acceptance_criteria>
</task>

<task id="78-06-04">
<action>
Barrel the three routers in `packages/api/src/routers/integrations/index.ts` and mount them in `packages/api/src/root.ts`, consistent with how the existing `integration` namespace and per-provider routers are composed (read both files). If the existing pattern nests provider routers under one `integration` namespace, nest these; if 76/77 created an `idpDeprovisioning` namespace, mount the connection mutations there. Keep the root.ts namespace comment block updated.
</action>
<read_first>
- packages/api/src/routers/integrations/index.ts (export + composition)
- packages/api/src/root.ts (namespace mount + comment conventions)
</read_first>
<acceptance_criteria>
- the three routers are reachable from `appRouter` (or `idpDeprovisioning`/`integration` sub-namespace)
- `pnpm --filter @contractor-ops/api typecheck` exits 0
- `pnpm typecheck` exits 0 monorepo-wide
</acceptance_criteria>
</task>

<task id="78-06-05">
<action>
Create `packages/api/src/routers/integrations/__tests__/idp-deprovision-connections.test.ts`. Read an existing integration-router test first to match the tRPC caller + mocked-ctx + Prisma-mock conventions. Assert:
1. `enableProviderForOrg` REJECTS when the provider flag is PENDING and `FLAG_SIGNOFF_BYPASS` is unset (gate works).
2. `enableProviderForOrg` SUCCEEDS when `FLAG_SIGNOFF_BYPASS=local` (or flag APPROVED) and persists the per-org toggle.
3. `connect`/`storeCredential` uses `ctx.organizationId` (a client-supplied org id in input is impossible — the input schema has no such field) and calls `writeAuditLog`.
4. mutation responses never contain the raw credential/secret.
</action>
<read_first>
- packages/api/src/routers/integrations (an existing router test for tRPC-caller + ctx-mock pattern)
- packages/api/src/routers/integrations/entra.ts (the routers under test)
</read_first>
<acceptance_criteria>
- test asserts the PENDING-flag enable rejection
- test asserts no secret in the connect response
- `pnpm --filter @contractor-ops/api test idp-deprovision-connections` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
- `pnpm --filter @contractor-ops/integrations typecheck` exits 0
- `pnpm --filter @contractor-ops/api typecheck` exits 0
- `pnpm --filter @contractor-ops/feature-flags typecheck` exits 0
- `pnpm typecheck` exits 0 monorepo-wide
- `pnpm --filter @contractor-ops/api test idp-deprovision-connections` exits 0
- `getAllPendingFlags()` includes the three new keys; the three adapters resolve via `getDeprovisionableAdapter('ENTRA_ID'|'OKTA'|'GITHUB')`
</verification>
</content>
