---
phase: 70
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/lint-guards/package.json
  - packages/lint-guards/tsconfig.json
  - packages/lint-guards/vitest.config.ts
  - packages/lint-guards/src/index.ts
  - packages/lint-guards/src/__tests__/schema-guard.test.ts
  - packages/lint-guards/src/__tests__/logs-guard.test.ts
  - packages/lint-guards/src/__tests__/i18n-parity.test.ts
  - packages/lint-guards/src/__fixtures__/clean.prisma
  - packages/lint-guards/src/__fixtures__/missing-org-id.prisma
  - packages/lint-guards/src/__fixtures__/allowlisted-global.prisma
  - packages/lint-guards/src/__fixtures__/clean-router.ts
  - packages/lint-guards/src/__fixtures__/leaky-router.ts
  - packages/lint-guards/src/__fixtures__/messages/en.json
  - packages/lint-guards/src/__fixtures__/messages/de.json
  - packages/lint-guards/src/__fixtures__/messages/pl.json
  - packages/lint-guards/src/__fixtures__/messages/ar.json
  - packages/feature-flags/src/__tests__/signoff-registry-flags.test.ts
  - packages/feature-flags/src/__tests__/boot-gate.test.ts
  - packages/logger/src/__tests__/default-body-redact.test.ts
  - packages/logger/src/__tests__/with-body-logging.test.ts
  - packages/logger/src/__tests__/idp-audit-logger.test.ts
  - packages/db/src/__tests__/scope-capabilities-backfill.test.ts
  - apps/web/src/components/integrations/__tests__/google-workspace-reconnect-banner.test.tsx
  - docs/lint-remediation/.gitkeep
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
autonomous: true
requirements: [FOUND6-01, FOUND6-02, FOUND6-03, FOUND6-04, FOUND6-05, FOUND6-06]
requirements_addressed: [FOUND6-01, FOUND6-02, FOUND6-03, FOUND6-04, FOUND6-05, FOUND6-06]

must_haves:
  truths:
    - "Every Wave 0 test file exists and currently fails with a clear 'not implemented' / import-error signal — failures must NOT be silent skips"
    - "`pnpm --filter @contractor-ops/lint-guards test` reports 3 failing test suites (schema-guard, logs-guard, i18n-parity)"
    - "`pnpm --filter @contractor-ops/feature-flags test` includes the 2 new failing suites (signoff-registry-flags, boot-gate)"
    - "`pnpm --filter @contractor-ops/logger test` includes 3 new failing suites (default-body-redact, with-body-logging, idp-audit-logger)"
    - "`pnpm --filter @contractor-ops/db test` includes the new failing suite (scope-capabilities-backfill)"
    - "`pnpm --filter @contractor-ops/web test` recognises the new failing component test (google-workspace-reconnect-banner)"
    - "New `@contractor-ops/lint-guards` package added to `pnpm-workspace.yaml` packages list"
    - "Locked-phrases-guard 78/78 still passes (no regression): `pnpm --filter @contractor-ops/validators exec vitest run locked-phrases-guard`"
  artifacts:
    - path: "packages/lint-guards/package.json"
      provides: "@contractor-ops/lint-guards workspace package with vitest, ts-morph, and tsx scripts wired"
      contains: "@contractor-ops/lint-guards"
    - path: "packages/lint-guards/src/__tests__/schema-guard.test.ts"
      provides: "FOUND6-01 failing test scaffold (parses fixture .prisma files, asserts allowlist behaviour)"
    - path: "packages/lint-guards/src/__tests__/logs-guard.test.ts"
      provides: "FOUND6-02 failing test scaffold (parses fixture .ts files via ts-morph, asserts body-log detection)"
    - path: "packages/lint-guards/src/__tests__/i18n-parity.test.ts"
      provides: "FOUND6-03 failing test scaffold (loads fixture en/de/pl/ar JSON, asserts EN ⊂ {DE, PL, AR})"
    - path: "packages/feature-flags/src/__tests__/signoff-registry-flags.test.ts"
      provides: "FOUND6-04 failing test scaffold for Zod schema (LEGAL-\\d+ regex + APPROVED refine)"
    - path: "packages/logger/src/__tests__/default-body-redact.test.ts"
      provides: "FOUND6-02 failing test scaffold for default body redaction (currently bodies are NOT redacted)"
    - path: "packages/logger/src/__tests__/idp-audit-logger.test.ts"
      provides: "FOUND6-06 failing test scaffold for getIdpAuditLogger() (function does not yet exist)"
    - path: "packages/db/src/__tests__/scope-capabilities-backfill.test.ts"
      provides: "FOUND6-05 failing test scaffold for backfill script (script does not yet exist)"
    - path: "apps/web/src/components/integrations/__tests__/google-workspace-reconnect-banner.test.tsx"
      provides: "FOUND6-05 failing component test (component does not yet exist)"
    - path: "docs/lint-remediation/.gitkeep"
      provides: "tracked dir so plans 02–04 can drop remediation .md files"
  key_links:
    - from: "packages/lint-guards/src/__tests__/schema-guard.test.ts"
      to: "packages/lint-guards/src/schema-guard/run-guard.ts (future)"
      via: "import { runSchemaGuard } from '../schema-guard/run-guard'"
      pattern: "import.*schema-guard/run-guard"
    - from: "packages/lint-guards/src/__tests__/logs-guard.test.ts"
      to: "packages/lint-guards/src/logs-guard/run-guard.ts (future)"
      via: "import { runLogsGuard } from '../logs-guard/run-guard'"
      pattern: "import.*logs-guard/run-guard"
    - from: "packages/feature-flags/src/__tests__/signoff-registry-flags.test.ts"
      to: "packages/feature-flags/src/signoff-registry-flags-schema.ts (future)"
      via: "import { FlagSignoffEntrySchema } from '../signoff-registry-flags-schema'"
      pattern: "import.*signoff-registry-flags-schema"
    - from: "packages/logger/src/__tests__/idp-audit-logger.test.ts"
      to: "packages/logger/src/index.ts (future)"
      via: "import { getIdpAuditLogger, IDP_AUDIT_ALLOWED_FIELDS } from '../index'"
      pattern: "getIdpAuditLogger"
---

<objective>
Create Wave 0 test infrastructure for Phase 70 per VALIDATION.md — failing test stubs covering FOUND6-01..06 and a new `@contractor-ops/lint-guards` workspace package skeleton. Per Nyquist sampling rate (≤3s feedback per commit) tests must exist before implementation. No production code changes in this plan; tests fail with clear "not implemented" / import-error signals so subsequent waves can implement against a functioning test runner.

Purpose: Establish the failing-test contract for every guard, factory, and migration the phase introduces. Mirrors the v5.0 Phase 56 Wave 0 pattern (`56-01-PLAN.md`).

Output: New `@contractor-ops/lint-guards` package skeleton, fixture trees, ~10 failing test files, `docs/lint-remediation/` placeholder. No production behavior change.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v6.0-phases/70-v6-0-foundation-cross-cutting-ci-guards-observability-baseli/70-CONTEXT.md
@.planning/milestones/v6.0-phases/70-v6-0-foundation-cross-cutting-ci-guards-observability-baseli/70-RESEARCH.md
@.planning/milestones/v6.0-phases/70-v6-0-foundation-cross-cutting-ci-guards-observability-baseli/70-VALIDATION.md
@.planning/milestones/v6.0-phases/70-v6-0-foundation-cross-cutting-ci-guards-observability-baseli/70-PATTERNS.md
@packages/validators/package.json
@packages/validators/vitest.config.ts
@packages/validators/src/__tests__/locked-phrases-guard.test.ts
@packages/logger/src/__tests__/pii-mask.test.ts
@apps/web/vitest.config.ts
@pnpm-workspace.yaml
</context>

<threat_model>
**Phase 70 itself is a defensive phase — its threats are second-order: a guard that doesn't actually fail.**

| ID | Threat | Mitigation in this plan |
|----|--------|-------------------------|
| T-70-01-01 | A failing test scaffold silently passes (e.g., `it.skip` or empty body) — Wave 1 ships an unfinished guard but CI looks green | Each new test file uses `expect(...).toBe(false)` or imports-not-yet-existing — never `it.skip`; smoke task at end of plan asserts each suite REPORTS A FAILURE |
| T-70-01-02 | Fixture files contain real PII (e.g., realistic UTR/USt-IdNr) and leak via test snapshot reports | Fixtures use placeholder shapes (`utr-EXAMPLE`, `[REDACTED-FIXTURE]`); no real-format identifiers |
| T-70-01-03 | New `@contractor-ops/lint-guards` package depends on production runtime accidentally (e.g., imports `@contractor-ops/db`) — bloats production bundle | `package.json` declares **only devDependencies + ts-morph as direct dep**; package-internal-only |
</threat_model>

<tasks>

<task id="70-01-01">
<action>
Create the `@contractor-ops/lint-guards` workspace package skeleton.

1. Create `packages/lint-guards/package.json` with:
```json
{
  "name": "@contractor-ops/lint-guards",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ts-morph": "^24.0.0"
  },
  "devDependencies": {
    "@contractor-ops/typescript-config": "workspace:*",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.2"
  }
}
```
Pin `ts-morph` to the version already in the workspace lockfile if different — run `grep -A1 '"ts-morph"' pnpm-lock.yaml | head -5` to confirm.

2. Create `packages/lint-guards/tsconfig.json`:
```json
{
  "extends": "@contractor-ops/typescript-config/base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src/**/*"],
  "exclude": ["dist", "node_modules"]
}
```

3. Create `packages/lint-guards/vitest.config.ts`:
```ts
import { defineConfig } from 'vitest/config';
export default defineConfig({
  test: {
    include: ['src/**/__tests__/**/*.test.ts'],
    environment: 'node',
  },
});
```

4. Create `packages/lint-guards/src/index.ts` with stub exports:
```ts
// Public API — populated by Plans 02, 03, 04
export const PLACEHOLDER = 'lint-guards' as const;
```

5. Add `'packages/lint-guards'` to `pnpm-workspace.yaml` if not already present.

6. Run `pnpm install` to register the package in the workspace.
</action>

<read_first>
- `pnpm-workspace.yaml` (current workspace package list — ensure new entry doesn't duplicate)
- `packages/validators/package.json` (analog package shape — ESM type, vitest scripts)
- `packages/validators/tsconfig.json` (analog tsconfig — extends shared base)
- `packages/validators/vitest.config.ts` (analog vitest config — node env, src/**/__tests__ pattern)
</read_first>

<acceptance_criteria>
- `test -f packages/lint-guards/package.json && jq -r '.name' packages/lint-guards/package.json` returns `@contractor-ops/lint-guards`
- `jq -r '.dependencies."ts-morph"' packages/lint-guards/package.json` returns a non-null version string
- `jq -r '.scripts.test' packages/lint-guards/package.json` returns `vitest run`
- `grep -q "packages/lint-guards" pnpm-workspace.yaml`
- `pnpm install` exits 0 with no warnings about the new package
- `pnpm --filter @contractor-ops/lint-guards exec node -e "console.log('ok')"` exits 0 (proves package resolves)
</acceptance_criteria>
</task>

<task id="70-01-02">
<action>
Create fixture trees for the three lint guards inside `packages/lint-guards/src/__fixtures__/`.

**Schema fixtures (3 files):**
1. `clean.prisma`:
```prisma
model Invoice {
  id              String   @id @default(cuid())
  organizationId  String
  total           Decimal
  organization    Organization @relation(fields: [organizationId], references: [id])
  @@index([organizationId])
}
```

2. `missing-org-id.prisma` (the violation we expect to detect):
```prisma
model SecretLeak {
  id     String @id @default(cuid())
  amount Decimal
}
```

3. `allowlisted-global.prisma` (allowlist exempts global-lookup tables):
```prisma
model Country {
  code String @id
  name String
}
```

**Logger fixtures (2 files):**
4. `clean-router.ts`:
```ts
import { createTrpcLogger } from '@contractor-ops/logger';
const log = createTrpcLogger({ procedure: 'contractor.create', type: 'mutation' });
log.info({ contractorId: 'c_1' }, 'created'); // no body — clean
```

5. `leaky-router.ts` (the violation we expect to detect):
```ts
import { createTrpcLogger } from '@contractor-ops/logger';
const log = createTrpcLogger({ procedure: 'contractor.create', type: 'mutation' });
log.info({ body: { ssn: '123' } }, 'received'); // body — must be flagged
```

**i18n fixtures (4 files):**
6. `messages/en.json`:
```json
{ "Greeting": { "hello": "Hello", "world": "World" } }
```

7. `messages/de.json`:
```json
{ "Greeting": { "hello": "Hallo", "world": "Welt" } }
```

8. `messages/pl.json` (intentionally missing `world` — the violation):
```json
{ "Greeting": { "hello": "Cześć" } }
```

9. `messages/ar.json`:
```json
{ "Greeting": { "hello": "مرحبا", "world": "عالم" } }
```

All file paths must be referenced in tests by `path.join(__dirname, '..', '__fixtures__', ...)` — exact directory layout above.
</action>

<read_first>
- `packages/db/prisma/schema/integration.prisma` (real Prisma model shape — `model X { id String @id ... organizationId String @@index([organizationId]) }`)
- `apps/web/messages/en.json` (real JSON shape with nested objects + ICU placeholders)
- `apps/web/messages/de.json` (parity reference)
- `apps/web/src/i18n/__tests__/de-locale.test.ts` (existing parity-detection pattern at lines 19–33 — `flattenKeys` recursive helper)
</read_first>

<acceptance_criteria>
- All 9 files exist under `packages/lint-guards/src/__fixtures__/`
- `cat packages/lint-guards/src/__fixtures__/missing-org-id.prisma | grep -q 'model SecretLeak'`
- `cat packages/lint-guards/src/__fixtures__/leaky-router.ts | grep -q "body: { ssn:"`
- `jq -r '.Greeting.world' packages/lint-guards/src/__fixtures__/messages/pl.json` returns `null` (intentional gap)
- `jq -e '.Greeting.world' packages/lint-guards/src/__fixtures__/messages/en.json` exits 0
- No fixture contains a real-format UTR/USt-IdNr/SSN (T-70-01-02 mitigation): `grep -E 'GB[0-9]{9}|DE[0-9]{9}|[0-9]{3}-[0-9]{2}-[0-9]{4}' packages/lint-guards/src/__fixtures__/ -r` returns no matches
</acceptance_criteria>
</task>

<task id="70-01-03">
<action>
Create the three failing CI guard test scaffolds in `packages/lint-guards/src/__tests__/`.

**`schema-guard.test.ts`** — imports the not-yet-existing `runSchemaGuard` from `../schema-guard/run-guard`, asserts:
- Clean fixture returns 0 offences
- Missing-org-id fixture returns 1 offence with `model: 'SecretLeak'`
- Allowlisted fixture returns 0 offences when `Country` is in the allowlist

```ts
import { describe, expect, it } from 'vitest';
import { join } from 'node:path';
// Plan 70-02 implements this — test fails with import-error until then.
import { runSchemaGuard } from '../schema-guard/run-guard.js';

const FX = join(__dirname, '..', '__fixtures__');
const ALLOWLIST = ['Country'] as const;

describe('schema-guard (FOUND6-01)', () => {
  it('returns 0 offences for a clean multi-tenant model', async () => {
    const offences = await runSchemaGuard({ files: [join(FX, 'clean.prisma')], allowlist: ALLOWLIST });
    expect(offences).toEqual([]);
  });

  it('flags a multi-tenant model missing organizationId', async () => {
    const offences = await runSchemaGuard({ files: [join(FX, 'missing-org-id.prisma')], allowlist: ALLOWLIST });
    expect(offences).toHaveLength(1);
    expect(offences[0]?.model).toBe('SecretLeak');
    expect(offences[0]?.kind).toBe('missing-organization-id');
  });

  it('does not flag an allowlisted global-lookup model', async () => {
    const offences = await runSchemaGuard({ files: [join(FX, 'allowlisted-global.prisma')], allowlist: ALLOWLIST });
    expect(offences).toEqual([]);
  });
});
```

**`logs-guard.test.ts`** — same structure for `runLogsGuard`:
```ts
import { describe, expect, it } from 'vitest';
import { join } from 'node:path';
import { runLogsGuard } from '../logs-guard/run-guard.js';

const FX = join(__dirname, '..', '__fixtures__');
const INCLUDE_PREFIXES: readonly string[] = []; // empty default

describe('logs-guard (FOUND6-02)', () => {
  it('returns 0 offences for a router with no body logs', async () => {
    const offences = await runLogsGuard({ files: [join(FX, 'clean-router.ts')], includePrefixes: INCLUDE_PREFIXES });
    expect(offences).toEqual([]);
  });

  it('flags a router that logs body without an include-prefix', async () => {
    const offences = await runLogsGuard({ files: [join(FX, 'leaky-router.ts')], includePrefixes: INCLUDE_PREFIXES });
    expect(offences).toHaveLength(1);
    expect(offences[0]?.kind).toBe('unredacted-body-log');
    expect(offences[0]?.file).toContain('leaky-router.ts');
  });

  it('does not flag a body log when its router prefix is in includePrefixes', async () => {
    const offences = await runLogsGuard({ files: [join(FX, 'leaky-router.ts')], includePrefixes: ['contractor.create'] });
    expect(offences).toEqual([]);
  });
});
```

**`i18n-parity.test.ts`** — same structure for `runI18nParity`:
```ts
import { describe, expect, it } from 'vitest';
import { join } from 'node:path';
import { runI18nParity } from '../i18n-parity/run-guard.js';

const FX = join(__dirname, '..', '__fixtures__', 'messages');

describe('i18n-parity (FOUND6-03)', () => {
  it('reports keys in en.json missing from any of {de,pl,ar}.json', async () => {
    const offences = await runI18nParity({ messagesDir: FX, base: 'en', peers: ['de', 'pl', 'ar'] });
    expect(offences).toHaveLength(1);
    expect(offences[0]?.locale).toBe('pl');
    expect(offences[0]?.missingKey).toBe('Greeting.world');
  });
});
```
</action>

<read_first>
- `packages/validators/src/__tests__/locked-phrases-guard.test.ts` (existing CI-guard test pattern — describe/it/expect with structured offence list)
- `apps/web/src/i18n/__tests__/de-locale.test.ts` (existing flattenKeys pattern at lines 19–33)
- The fixture files created in task 70-01-02
</read_first>

<acceptance_criteria>
- All 3 test files exist under `packages/lint-guards/src/__tests__/`
- `pnpm --filter @contractor-ops/lint-guards test 2>&1 | grep -E 'FAIL|Cannot find module'` matches at least 3 lines (proves all 3 suites fail)
- None of the test bodies contain `it.skip(` or `it.todo(` — `! grep -rE 'it\.(skip|todo)' packages/lint-guards/src/__tests__/`
- All test files import from a path that does NOT yet exist (`../{schema-guard,logs-guard,i18n-parity}/run-guard.js`) — proves Wave 1 has implementation work to do
</acceptance_criteria>
</task>

<task id="70-01-04">
<action>
Create failing test scaffolds for the feature-flags signoff and boot gate at `packages/feature-flags/src/__tests__/`.

**`signoff-registry-flags.test.ts`:**
```ts
import { describe, expect, it } from 'vitest';
// Plan 70-05 implements this.
import { FlagSignoffEntrySchema } from '../signoff-registry-flags-schema.js';

describe('FlagSignoffEntrySchema (FOUND6-04)', () => {
  it('accepts a minimal PENDING entry', () => {
    const result = FlagSignoffEntrySchema.safeParse({ status: 'PENDING' });
    expect(result.success).toBe(true);
  });

  it('accepts an APPROVED entry with all required fields including legalTicketRef LEGAL-123', () => {
    const result = FlagSignoffEntrySchema.safeParse({
      status: 'APPROVED',
      approvedBy: 'jane@contractor-ops.local',
      approvedAt: '2026-04-26T00:00:00.000Z',
      approverRole: 'LEGAL_LEAD',
      legalTicketRef: 'LEGAL-123',
    });
    expect(result.success).toBe(true);
  });

  it('accepts a URL legalTicketRef', () => {
    const result = FlagSignoffEntrySchema.safeParse({
      status: 'APPROVED',
      approvedBy: 'jane@x',
      approvedAt: '2026-04-26T00:00:00.000Z',
      approverRole: 'LEGAL_LEAD',
      legalTicketRef: 'https://issues.local/legal/123',
    });
    expect(result.success).toBe(true);
  });

  it('rejects APPROVED entry missing legalTicketRef', () => {
    const result = FlagSignoffEntrySchema.safeParse({
      status: 'APPROVED',
      approvedBy: 'jane',
      approvedAt: '2026-04-26T00:00:00.000Z',
      approverRole: 'LEGAL_LEAD',
    });
    expect(result.success).toBe(false);
  });

  it('rejects malformed legalTicketRef', () => {
    const result = FlagSignoffEntrySchema.safeParse({
      status: 'APPROVED',
      approvedBy: 'jane',
      approvedAt: '2026-04-26T00:00:00.000Z',
      approverRole: 'LEGAL_LEAD',
      legalTicketRef: 'TICKET-XYZ',
    });
    expect(result.success).toBe(false);
  });
});
```

**`boot-gate.test.ts`:**
```ts
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';

describe('feature-flags boot-time signoff gate (FOUND6-04)', () => {
  let exitSpy: ReturnType<typeof vi.spyOn>;
  let stderrSpy: ReturnType<typeof vi.spyOn>;

  beforeEach(() => {
    vi.resetModules();
    exitSpy = vi.spyOn(process, 'exit').mockImplementation(() => undefined as never);
    stderrSpy = vi.spyOn(process.stderr, 'write').mockImplementation(() => true);
    delete process.env.FLAG_SIGNOFF_BYPASS;
  });

  afterEach(() => {
    exitSpy.mockRestore();
    stderrSpy.mockRestore();
  });

  it('exits 1 when a gated namespace flag is missing from signoff registry', async () => {
    // Plan 70-07 wires this — for now the registry has no gated flags so this fails to provoke an exit.
    // The test expectation will become true after Plan 70-07 adds the boot-time iteration AND a gated test fixture flag.
    await import('../registry.js');
    expect(exitSpy).toHaveBeenCalledWith(1);
  });

  it('does NOT exit when FLAG_SIGNOFF_BYPASS=local is set', async () => {
    process.env.FLAG_SIGNOFF_BYPASS = 'local';
    await import('../registry.js');
    expect(exitSpy).not.toHaveBeenCalled();
  });
});
```
</action>

<read_first>
- `packages/feature-flags/src/registry.ts` (where the boot gate will be inserted in Plan 70-07)
- `packages/validators/src/legal/signoff-registry-schema.ts` (Phase 64 schema — analog for the new flag-signoff schema)
- `packages/validators/src/__tests__/signoff-registry-schema.test.ts` if it exists; otherwise model on test file naming convention in `packages/validators/src/__tests__/`
</read_first>

<acceptance_criteria>
- Both test files exist
- `pnpm --filter @contractor-ops/feature-flags test 2>&1 | grep -cE 'FAIL.*signoff-registry-flags|FAIL.*boot-gate'` returns 2
- Tests do NOT use `it.skip` / `it.todo`
- `signoff-registry-flags.test.ts` imports from `../signoff-registry-flags-schema.js` (file not yet created — proves Wave 1 work)
</acceptance_criteria>
</task>

<task id="70-01-05">
<action>
Create three failing logger test scaffolds at `packages/logger/src/__tests__/`.

**`default-body-redact.test.ts`:**
```ts
import { describe, expect, it, vi } from 'vitest';
import { createLogger } from '../index.js';

describe('default body redaction (FOUND6-02 — D-05)', () => {
  it('redacts top-level `body` field by default in any child logger', () => {
    const captured: string[] = [];
    const log = createLogger({ service: 'test' });
    // Pino writes JSON to its destination; capture via a serializer hook.
    // Implementation detail tested: serialized JSON does NOT contain the literal
    // payload "secret-payload" but DOES contain "[REDACTED]" for that path.
    const writeSpy = vi.spyOn(process.stdout, 'write').mockImplementation((s: any) => {
      captured.push(typeof s === 'string' ? s : s.toString());
      return true;
    });
    log.info({ body: { ssn: 'secret-payload' } }, 'hi');
    writeSpy.mockRestore();
    const joined = captured.join('');
    expect(joined).not.toContain('secret-payload');
    expect(joined).toContain('[REDACTED]');
  });
});
```

**`with-body-logging.test.ts`:**
```ts
import { describe, expect, it, vi } from 'vitest';
import { createTrpcLogger, withBodyLogging } from '../index.js';

describe('withBodyLogging opt-in (FOUND6-02 — D-05/D-06)', () => {
  it('child returned by withBodyLogging emits plaintext body for matching procedure prefix', () => {
    const captured: string[] = [];
    const writeSpy = vi.spyOn(process.stdout, 'write').mockImplementation((s: any) => {
      captured.push(typeof s === 'string' ? s : s.toString());
      return true;
    });
    const base = createTrpcLogger({ procedure: 'contractor.create', type: 'mutation' });
    const log = withBodyLogging(base, ['contractor.create']);
    log.info({ body: { name: 'Acme' } }, 'created');
    writeSpy.mockRestore();
    const joined = captured.join('');
    expect(joined).toContain('Acme');
    expect(joined).not.toContain('[REDACTED]');
  });
});
```

**`idp-audit-logger.test.ts`:**
```ts
import { describe, expect, it, vi } from 'vitest';
import { getIdpAuditLogger, IDP_AUDIT_ALLOWED_FIELDS } from '../index.js';

describe('getIdpAuditLogger (FOUND6-06 — D-15)', () => {
  it('emits externalUserId in plaintext while password is redacted', () => {
    const captured: string[] = [];
    const writeSpy = vi.spyOn(process.stdout, 'write').mockImplementation((s: any) => {
      captured.push(typeof s === 'string' ? s : s.toString());
      return true;
    });
    const log = getIdpAuditLogger();
    log.info({ auditEvent: 'deprovision', externalUserId: 'usr_123', password: 'should-redact' }, 'audit');
    writeSpy.mockRestore();
    const joined = captured.join('');
    expect(joined).toContain('usr_123');
    expect(joined).not.toContain('should-redact');
  });

  it('IDP_AUDIT_ALLOWED_FIELDS contains the expected keys', () => {
    expect(IDP_AUDIT_ALLOWED_FIELDS).toContain('externalUserId');
    expect(IDP_AUDIT_ALLOWED_FIELDS).toContain('actionResult');
    expect(IDP_AUDIT_ALLOWED_FIELDS).toContain('scopeDelta');
    expect(IDP_AUDIT_ALLOWED_FIELDS).toContain('auditEvent');
  });
});
```

The `withBodyLogging` and `getIdpAuditLogger` and `IDP_AUDIT_ALLOWED_FIELDS` symbols are NOT yet exported from `packages/logger/src/index.ts` — they ship in Plans 03 and 08. This task only creates the failing test scaffolds.
</action>

<read_first>
- `packages/logger/src/index.ts` (current factory file — verify `withBodyLogging` etc. are NOT yet present)
- `packages/logger/src/__tests__/pii-mask.test.ts` if exists (for vitest stdout-capture pattern); otherwise the body-redact test above stands as the new pattern
</read_first>

<acceptance_criteria>
- 3 test files exist
- `pnpm --filter @contractor-ops/logger test 2>&1 | grep -cE 'FAIL.*default-body-redact|FAIL.*with-body-logging|FAIL.*idp-audit-logger'` returns 3
- No `it.skip`/`it.todo` usage
- All three import symbols from `../index.js` that don't yet exist (`withBodyLogging`, `getIdpAuditLogger`, `IDP_AUDIT_ALLOWED_FIELDS`)
</acceptance_criteria>
</task>

<task id="70-01-06">
<action>
Create failing scope-capabilities backfill test scaffold at `packages/db/src/__tests__/scope-capabilities-backfill.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
// Plan 70-09 implements this — file does not yet exist.
import { backfillScopeCapabilities } from '../scripts/backfill-scope-capabilities.js';
import type { ScopeCapabilities } from '../types/scope-capabilities.js';

describe('scopeCapabilities backfill (FOUND6-05 — D-14)', () => {
  it('writes directory.read + group.read for every existing GOOGLE_WORKSPACE connection with null scopeCapabilities', async () => {
    const connections = [
      { id: 'c1', provider: 'GOOGLE_WORKSPACE' as const, scopeCapabilities: null },
      { id: 'c2', provider: 'GOOGLE_WORKSPACE' as const, scopeCapabilities: null },
      { id: 'c3', provider: 'SLACK' as const, scopeCapabilities: null },
    ];
    const updates = await backfillScopeCapabilities({ connections, dryRun: true });
    expect(updates).toHaveLength(2);
    const gws = updates.filter(u => u.connectionId.startsWith('c1') || u.connectionId.startsWith('c2'));
    for (const u of gws) {
      const caps = u.scopeCapabilities as ScopeCapabilities;
      expect(caps.provider).toBe('google');
      expect(caps.capabilities).toContain('directory.read');
      expect(caps.capabilities).toContain('group.read');
    }
  });

  it('is idempotent — connections with non-null scopeCapabilities are skipped', async () => {
    const connections = [
      {
        id: 'c1',
        provider: 'GOOGLE_WORKSPACE' as const,
        scopeCapabilities: { provider: 'google', scopes: [], capabilities: ['directory.read'], grantedAt: '2026-04-26T00:00:00.000Z' },
      },
    ];
    const updates = await backfillScopeCapabilities({ connections, dryRun: true });
    expect(updates).toEqual([]);
  });
});
```

The `backfillScopeCapabilities` function and `ScopeCapabilities` type don't yet exist. The test fails with import-error until Plan 70-09 ships them.
</action>

<read_first>
- `packages/db/prisma/schema/integration.prisma` (the model that gets a new field)
- `packages/db/scripts/push-all-regions.ts` (analog migration script — same shape we follow)
- `packages/db/package.json` (verify vitest already wired; if not, add to devDependencies)
</read_first>

<acceptance_criteria>
- File exists at `packages/db/src/__tests__/scope-capabilities-backfill.test.ts`
- `pnpm --filter @contractor-ops/db test 2>&1 | grep -E 'FAIL.*scope-capabilities-backfill|Cannot find module.*scope-capabilities'` matches at least 1 line
- Test imports `backfillScopeCapabilities` from `../scripts/backfill-scope-capabilities.js` (file not yet created)
- Test imports `ScopeCapabilities` from `../types/scope-capabilities.js` (file not yet created)
</acceptance_criteria>
</task>

<task id="70-01-07">
<action>
Create failing component test for the GWS reconnect banner at `apps/web/src/components/integrations/__tests__/google-workspace-reconnect-banner.test.tsx`:

```tsx
import { render, screen } from '@testing-library/react';
import { describe, expect, it } from 'vitest';
// Plan 70-10 implements this.
import { GoogleWorkspaceReconnectBanner } from '../google-workspace-reconnect-banner.js';

describe('GoogleWorkspaceReconnectBanner (FOUND6-05 — D-16)', () => {
  it('renders when scopeCapabilities lacks user.deprovision', () => {
    render(
      <GoogleWorkspaceReconnectBanner
        scopeCapabilities={{ provider: 'google', scopes: [], capabilities: ['directory.read'], grantedAt: '2026-04-26T00:00:00.000Z' }}
      />,
    );
    expect(screen.getByRole('button', { name: /reconnect/i })).toBeInTheDocument();
  });

  it('does not render when capabilities already include user.deprovision', () => {
    const { container } = render(
      <GoogleWorkspaceReconnectBanner
        scopeCapabilities={{ provider: 'google', scopes: [], capabilities: ['directory.read', 'user.deprovision'], grantedAt: '2026-04-26T00:00:00.000Z' }}
      />,
    );
    expect(container).toBeEmptyDOMElement();
  });

  it('renders the banner when scopeCapabilities is null (legacy v3.0 connection)', () => {
    render(<GoogleWorkspaceReconnectBanner scopeCapabilities={null} />);
    expect(screen.getByRole('button', { name: /reconnect/i })).toBeInTheDocument();
  });
});
```

If the test runner needs i18n provider wrapping (next-intl), import the existing test util (`apps/web/src/test-utils/render-with-locale.tsx` or equivalent — research the exact path before writing the import). If no util exists, mock `useTranslations` inline at the top of the file.
</action>

<read_first>
- `apps/web/src/components/integrations/google-workspace-provider-section.tsx` (host component — banner is rendered inside)
- `apps/web/src/components/equipment/return-approval-banner.tsx` (analog banner component shape)
- `apps/web/src/components/equipment/equipment-table/__tests__/equipment-table.test.tsx` (existing component test pattern — find next-intl wrapping helper if any)
- `apps/web/vitest.config.ts` (confirms jsdom env + setup file)
</read_first>

<acceptance_criteria>
- File exists
- `pnpm --filter @contractor-ops/web test 2>&1 | grep -E 'FAIL.*google-workspace-reconnect-banner|Cannot find module.*google-workspace-reconnect-banner'` matches at least 1 line
- Component import is from `../google-workspace-reconnect-banner.js` (file not yet created)
</acceptance_criteria>
</task>

<task id="70-01-08">
<action>
Create the placeholder for remediation docs:

```bash
mkdir -p docs/lint-remediation
touch docs/lint-remediation/.gitkeep
```

Add a tiny README at `docs/lint-remediation/README.md`:
```markdown
# Lint Remediation Docs

Per-guard fix-up procedures (Phase 70 D-03). Each guard's structured-diff
failure output points here via an anchor link.

- `lint-schema.md` — multi-tenant schema guard (Phase 70-02)
- `lint-logs.md` — body-redaction logger guard (Phase 70-03)
- `i18n-parity.md` — message-key parity guard (Phase 70-04)
```
</action>

<read_first>
- (no prior reads needed — net-new directory)
</read_first>

<acceptance_criteria>
- `test -d docs/lint-remediation`
- `test -f docs/lint-remediation/.gitkeep`
- `test -f docs/lint-remediation/README.md`
- `grep -q 'Phase 70 D-03' docs/lint-remediation/README.md`
</acceptance_criteria>
</task>

<task id="70-01-09">
<action>
**Smoke-fail validation (T-70-01-01 mitigation):** assert that every test scaffold introduced in this plan FAILS — not silently skipped, not green.

Run each suite and assert at least 1 failing test or 1 import-error per suite:

```bash
set +e
EXIT_CODES=()
pnpm --filter @contractor-ops/lint-guards test --reporter=verbose 2>&1 | tee /tmp/p70-01-lint-guards.log; EXIT_CODES+=($?)
pnpm --filter @contractor-ops/feature-flags test --reporter=verbose 2>&1 | tee /tmp/p70-01-ff.log; EXIT_CODES+=($?)
pnpm --filter @contractor-ops/logger test --reporter=verbose 2>&1 | tee /tmp/p70-01-logger.log; EXIT_CODES+=($?)
pnpm --filter @contractor-ops/db test --reporter=verbose 2>&1 | tee /tmp/p70-01-db.log; EXIT_CODES+=($?)
pnpm --filter @contractor-ops/web test --reporter=verbose google-workspace-reconnect-banner 2>&1 | tee /tmp/p70-01-web.log; EXIT_CODES+=($?)
set -e

# Each suite must exit non-zero (failing) — no silent green.
for code in "${EXIT_CODES[@]}"; do
  test "$code" -ne 0 || (echo "FATAL: a suite passed when it should be RED" && exit 1)
done

# Locked-phrases-guard must STILL pass — regression check.
pnpm --filter @contractor-ops/validators exec vitest run locked-phrases-guard
```

Commit message: `test(70-01): add Wave 0 failing test scaffolds for FOUND6-01..06 + lint-guards package`

Stage ONLY files in:
- `packages/lint-guards/`
- `packages/feature-flags/src/__tests__/`
- `packages/logger/src/__tests__/`
- `packages/db/src/__tests__/`
- `apps/web/src/components/integrations/__tests__/`
- `docs/lint-remediation/`
- `pnpm-workspace.yaml`
- `pnpm-lock.yaml`

Do NOT use `git add -A` (risk of staging the many unstaged modifications already present in the working tree — see CLAUDE.md lint-staged warning).
</action>

<read_first>
- `.husky/pre-commit` (so executor knows lint-staged will run on commit)
- `package.json` `lint-staged` entry at line 43–46 (so executor knows which patterns lint-staged formats)
</read_first>

<acceptance_criteria>
- All 5 `pnpm --filter ... test` invocations exit non-zero (failing — RED state proven)
- `pnpm --filter @contractor-ops/validators exec vitest run locked-phrases-guard` exits 0 (regression-free)
- `git status --short` shows the new files staged and committed; no unintended files in the commit
- `git log -1 --pretty=%s` returns `test(70-01): add Wave 0 failing test scaffolds for FOUND6-01..06 + lint-guards package`
- `git show --stat HEAD | head -40` does NOT list any of the unstaged-modification files from CLAUDE.md (e.g., `apps/web/messages/de.json`, `apps/public-api/...`)
</acceptance_criteria>
</task>

</tasks>

<verification>

## must_haves verification

After all tasks land:
- Run all 5 `pnpm --filter ... test` commands; each fails with import-error or a specific assertion failure (RED state)
- `pnpm --filter @contractor-ops/validators exec vitest run locked-phrases-guard` exits 0
- `git status` clean for files in scope; no other working-tree files modified by this plan
- `pnpm install` exits 0 (workspace integrity)

## Threat model verification

- T-70-01-01: Verified by task 70-01-09 smoke-fail check — every suite must exit non-zero
- T-70-01-02: `grep -E 'GB[0-9]{9}|DE[0-9]{9}|[0-9]{3}-[0-9]{2}-[0-9]{4}' packages/lint-guards/src/__fixtures__/ -r` returns no matches
- T-70-01-03: `cat packages/lint-guards/package.json | jq -r '.dependencies | keys[]'` returns only `ts-morph` (no `@contractor-ops/db`, `@contractor-ops/api`, etc.)

</verification>
