---
phase: 79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-
plan: 08
type: execute
wave: 5
depends_on: ["79-06", "79-07"]
files_modified:
  - apps/web-vite/messages/en.json
  - apps/web-vite/messages/de.json
  - apps/web-vite/messages/pl.json
  - apps/web-vite/messages/ar.json
autonomous: false
requirements: [GULF-08, GULF-09]
must_haves:
  truths:
    - "Every Gulf user-facing key used by Plans 06/07 exists in en/de/pl/ar with REAL values (not en placeholders for de/pl) — D-16"
    - "pnpm i18n:parity passes (key existence across all 4 locales)"
    - "No LOCKED_AE_PHRASES / LOCKED_SA_PHRASES statutory key leaks into any messages/*.json (locked-phrases guard green)"
    - "pnpm check:rtl-logical-props passes across the whole Gulf surface (GULF-08)"
    - "Arabic Gulf surfaces render RTL correctly (human-verify)"
  artifacts:
    - path: "apps/web-vite/messages/ar.json"
      provides: "Arabic Gulf keys (RTL)"
      contains: "Gulf"
    - path: "apps/web-vite/messages/de.json"
      provides: "real German Gulf translations (D-16)"
      contains: "Gulf"
  key_links:
    - from: "apps/web-vite/messages/en.json"
      to: "apps/web-vite/messages/de.json"
      via: "i18n:parity key coverage"
      pattern: "Gulf"
---

<objective>
Wave 5: complete the Arabic + 4-locale i18n for every Gulf surface (GULF-08/09), with REAL de/pl/ar values (D-16 — not en placeholders), pass i18n:parity + the locked-phrases guard + the RTL logical-property guard, and human-verify Arabic RTL rendering + de/pl translation genuineness (the two D-16/legal items i18n:parity cannot gate).

Purpose: Turns C10 GREEN and closes GULF-08/09 at the translation layer. Statutory identifiers come from LOCKED_AE/SA_PHRASES (Plan 02), NOT these JSON files.
Output: Gulf keys added to all 4 message catalogs with genuine values.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-UI-SPEC.md
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-RESEARCH.md
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-CONTEXT.md

<interfaces>
<!-- Extracted from the live tree + Plans 06/07 key usage. -->

i18n boundary: apps/web-vite/src/i18n/useTranslations.ts; catalogs apps/web-vite/messages/{en,de,pl,ar}.json; pnpm i18n:parity (tsx scripts/i18n-parity.mjs) checks KEY EXISTENCE only (en → de/pl/ar) — NOT value distinctness (Pitfall 21). So D-16 genuine de/pl is a REVIEW discipline + human-verify, not an automated gate.

locked-phrases guard (packages/validators/src/__tests__/locked-phrases-guard.test.ts) — RESERVED_AE_LEGAL_KEYS + RESERVED_SA_LEGAL_KEYS must NOT appear in any messages/*.json (Plan 02 added them to the reserved spread). Statutory band labels / authority legal names come from LOCKED_*_PHRASES, NOT these JSON files (D-14).

check:rtl-logical-props (Plan 01) — Gulf-surface ml-/mr- ban (GULF-08).

Keys consumed by Plans 06/07 (collect the exact namespaces used): free-zone form labels/placeholders/empty/error/toast, scope-mismatch advisory, Saudization dashboard (rate label, manual-nature callout, quarterly prompt, Qiwa gap, Iqama roll-up, headcount labels, side-by-side), override dialog + "Custom — verify with adviser" badge, offboarding trajectory advisory, destructive confirmation copy. Use the UI-SPEC Copywriting Contract as the EN source of truth.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Gulf keys to all 4 locales with real values (GULF-08/09, D-16)</name>
  <read_first>
    - apps/web-vite/messages/en.json (existing namespace structure + an existing real de/pl/ar namespace to model genuine-translation quality on)
    - .planning/phases/79-.../79-UI-SPEC.md (Copywriting Contract — the EN source strings for every Gulf element)
    - the Plan 06 + Plan 07 components (the exact useTranslations keys they reference — these are the keys to define)
    - packages/validators/src/legal/ae.ts + sa.ts (statutory constants that must NOT be duplicated as JSON keys — D-14)
    - scripts/i18n-parity.mjs (what the parity gate checks)
  </read_first>
  <action>
    Collect every Gulf useTranslations key referenced by Plans 06/07 and define them under a `Gulf` namespace (or the established naming convention) in en.json with the UI-SPEC Copywriting values. Then add GENUINE translations in de.json, pl.json, ar.json — real German, Polish, and Arabic (NOT en copies; D-16). Arabic values must be RTL-correct (no stray LTR marks; mirror the quality of existing real ar namespaces, e.g. the Phase-73 Compliance namespace). Do NOT add any LOCKED_AE/SA statutory identifier as a JSON key (band labels, authority legal names, Qiwa status terms stay in LOCKED_*_PHRASES — D-14). Interpolation placeholders ({band}, {rate}, {date}, {projectedBand}, {projectedRate}) are identical across locales. Keep the diff minimal — only add Gulf keys; do not touch unrelated namespaces (D-17).
  </action>
  <verify>
    <automated>pnpm i18n:parity 2>&1 | tail -8 && pnpm --filter @contractor-ops/validators test -- locked-phrases-guard 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Gulf\|freeZone\|saudization\|nitaqat" apps/web-vite/messages/de.json apps/web-vite/messages/pl.json apps/web-vite/messages/ar.json` ≥ 3 (Gulf keys present in all non-en locales).
    - pnpm i18n:parity GREEN (every en Gulf key has a peer in de/pl/ar).
    - locked-phrases-guard GREEN (no AE/SA reserved key leaked into any messages/*.json).
    - de.json Gulf values are NOT byte-identical to en.json (spot-check ≥ 3 keys differ — recorded in SUMMARY; genuine de/pl review is the Task 2 human-verify).
    - No `MISSING_MESSAGE` at runtime for any Gulf key (every key used by Plans 06/07 is defined).
  </acceptance_criteria>
  <done>Gulf keys exist in all 4 locales with real values; parity + locked-phrases guards GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Full Gulf-surface RTL guard + locked-phrase + parity sweep (C10)</name>
  <read_first>
    - packages/db/scripts/check-rtl-logical-props.mjs (Plan 01 — the Gulf ml-/mr- guard)
    - package.json (lint:ci chain)
    - .planning/phases/79-.../79-VALIDATION.md (C10 — RTL guard + locked-phrase guard + 4-locale parity)
  </read_first>
  <action>
    Run the full Gulf i18n-integrity sweep now that all Gulf components (Plans 06/07) and keys (Task 1) exist: pnpm check:rtl-logical-props (Gulf surfaces — GULF-08), pnpm i18n:parity, pnpm --filter @contractor-ops/validators test -- locked-phrases-guard, pnpm --filter @contractor-ops/db db:audit-enum-casing. Fix any Gulf-surface offender introduced by Plans 06/07/08 (logical-prop violations, missing key, leaked statutory key). Do NOT touch pre-existing unrelated offenders elsewhere (D-17 scope-boundary). This task asserts C10 (RTL + locked-phrase + parity) is GREEN across the whole Gulf surface.
  </action>
  <verify>
    <automated>pnpm check:rtl-logical-props 2>&1 | tail -4 && pnpm i18n:parity 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - pnpm check:rtl-logical-props GREEN across apps/web-vite/src/components/{saudization,contractors/free-zone}.
    - pnpm i18n:parity GREEN; locked-phrases-guard GREEN; db:audit-enum-casing GREEN.
    - `grep -rcE "\b(ml|mr|pl|pr)-" apps/web-vite/src/components/saudization apps/web-vite/src/components/contractors/free-zone` == 0 (sum).
  </acceptance_criteria>
  <done>C10 GREEN: RTL guard + locked-phrase guard + 4-locale parity all pass across the Gulf surface.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Arabic RTL rendering + genuine de/pl translations (D-16, GULF-08/09)</name>
  <what-built>
    Every Gulf surface (free-zone form, scope-mismatch banner, Saudization dashboard, override dialog, offboarding trajectory banner) is fully translated in en/de/pl/ar with real values, RTL logical properties, and RTL charts. i18n:parity + locked-phrase + RTL guards are GREEN. This checkpoint covers the two things automation cannot: (a) Arabic RTL renders correctly (mirrored layout, no broken bidi, charts reversed), and (b) the de/pl Gulf translations are genuine (i18n:parity only checks key existence — Pitfall 21).
  </what-built>
  <how-to-verify>
    1. Run the app, switch locale to `ar`. Visit each Gulf surface and confirm: layout mirrors (labels start-aligned, controls flow right-to-left), the band donut/progress chart is reversed via useRtlChartConfig, no Latin-LTR leakage in Arabic strings, no MISSING_MESSAGE.
    2. Switch to `de` and `pl`. Confirm the Gulf copy reads as genuine German/Polish (NOT English placeholders) — spot-check the dashboard manual-nature callout, the scope-mismatch advisory, and the override badge.
    3. Statutory identifiers (Nitaqat band labels, free-zone authority legal names, Qiwa status) render from LOCKED_AE/SA_PHRASES and are flagged PENDING legal review — record them in the SUMMARY's "Manual-Only Verifications" for the Phase 80 consolidated legal sign-off (Arabic/UAE/KSA legal). Do NOT hard-block on legal sign-off (LOCAL-ONLY Standing Constraint).
  </how-to-verify>
  <resume-signal>Type "approved" once Arabic RTL renders correctly and de/pl read as genuine translations; or list the surfaces/keys needing fixes. Record outstanding legal sign-off (Arabic statutory copy) as a post-deploy item, not a blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| message catalog → rendered UI | Statutory text must come from locked constants, not translatable JSON. |
| de/pl translation → user | Genuine translation is a legal/comprehension concern; review-gated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-08-01 | Tampering | statutory label drift via translatable JSON | mitigate | LOCKED_AE/SA_PHRASES + locked-phrases guard (Plan 02); keys banned from messages/*.json. |
| T-79-08-02 | Repudiation | en-placeholder de/pl shipped as "translated" | mitigate | D-16 review discipline + human-verify (Task 3); spot-check distinctness (Task 1). |
| T-79-08-03 | Information Disclosure | Arabic RTL break exposing mis-rendered legal copy | mitigate | RTL guard (Task 2) + human RTL verify (Task 3). |
| T-79-08-LEGAL | Repudiation | unverified Arabic statutory copy | accept | PENDING legal review recorded for Phase 80; LOCAL-ONLY — not a build gate (Standing Constraint). |
| T-79-08-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. slopcheck + blocking checkpoint if any added. |
</threat_model>

<verification>
- `pnpm i18n:parity` GREEN.
- `pnpm --filter @contractor-ops/validators test -- locked-phrases-guard` GREEN.
- `pnpm check:rtl-logical-props` GREEN across the Gulf surface.
- Human-verify (Task 3): Arabic RTL + genuine de/pl confirmed; Arabic statutory legal sign-off recorded as post-deploy.
</verification>

<success_criteria>
- All Gulf surfaces fully translated in 4 locales with real values; RTL + locked-phrase + parity guards GREEN; Arabic RTL human-verified. GULF-08/09 complete; C10 GREEN.
</success_criteria>

<output>
Create `.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-08-SUMMARY.md` when done. Record outstanding Arabic statutory legal sign-off + de/pl native review as post-deploy items.
</output>
