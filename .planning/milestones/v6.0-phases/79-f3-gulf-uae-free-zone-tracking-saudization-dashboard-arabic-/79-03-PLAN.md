---
phase: 79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-
plan: 03
type: execute
wave: 2
depends_on: ["79-02"]
files_modified:
  - packages/compliance-policy/src/policies/uae.ts
  - packages/compliance-policy/src/registry.ts
  - packages/api/src/services/free-zone-compliance.ts
  - packages/api/src/services/compliance-reminder-scan.ts
  - packages/db/scripts/lint-region-leakage.ts
  - packages/db/package.json
  - package.json
  - packages/api/src/__tests__/free-zone-payment-block.test.ts
  - packages/api/src/__tests__/free-zone-mainland-exclusion.test.ts
  - packages/api/src/__tests__/free-zone-supersession-isolation.test.ts
  - packages/api/src/__tests__/reminder-region-fanout.test.ts
autonomous: true
requirements: [GULF-02, GULF-11]
must_haves:
  truths:
    - "An EXPIRED free-zone ContractorComplianceItem (BLOCKING, @v2) hard-blocks payment (C1)"
    - "A MAINLAND FreeZoneAssignment writes no compliance item and is not blocked (C2, D-04)"
    - "The reminder scan fans out across SUPPORTED_REGIONS so ME-region items enter the cascade (C3, Pitfall 18)"
    - "A free-zone item survives an unrelated classification supersession — not WAIVED (C4, Pitfall 2)"
    - "uae.free_zone_license bumped WARNING → BLOCKING @v2 via supersession (D-03); classification path never materialises it; Mainland never armed"
    - "lint-region-leakage fails on any default-client read of the 4 Gulf models (C8, Pitfall 19)"
  artifacts:
    - path: "packages/api/src/services/free-zone-compliance.ts"
      provides: "write/supersede free-zone ContractorComplianceItem keyed off FreeZoneAssignment; Mainland gate; supersession isolation"
      min_lines: 60
    - path: "packages/db/scripts/lint-region-leakage.ts"
      provides: "GULF-11 no-leakage CI script"
      min_lines: 30
  key_links:
    - from: "packages/api/src/services/free-zone-compliance.ts"
      to: "ContractorComplianceItem (BLOCKING/EXPIRED)"
      via: "create with policyRuleId uae.free_zone_license@v2 + isExpired-derived status"
      pattern: "uae\\.free_zone_license@v2"
    - from: "packages/api/src/services/compliance-reminder-scan.ts"
      to: "SUPPORTED_REGIONS / getRegionalClient"
      via: "region fan-out loop threading the regional client"
      pattern: "SUPPORTED_REGIONS|getRegionalClient"
---

<objective>
Wave 2 (backend, highest leverage): make free-zone license expiry flow into the existing F1 cascade + payment hard-block, with the three landmines handled exactly. Bump the policy rule to `BLOCKING @v2`, write the compliance item directly from the FreeZoneAssignment service (NOT the classification path — Pitfall 2), gate on `zone !== MAINLAND` (D-04), thread the reminder scan through a regional client and fan out over SUPPORTED_REGIONS (Pitfall 18), and build the GULF-11 leakage lint.

Purpose: GULF-02 is the money gate — the entire reminder + payment-block machinery already keys off `severity='BLOCKING' AND status='EXPIRED'`; the only new backend work is writing the row and making the cron region-aware. Turns C1/C2/C3/C4/C8 GREEN.
Output: edited `uae.ts`, new `free-zone-compliance.ts`, refactored `compliance-reminder-scan.ts`, new `lint-region-leakage.ts`.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-RESEARCH.md
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-PATTERNS.md
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-CONTEXT.md

<interfaces>
<!-- Extracted from the live tree. -->

payment gate (packages/api/src/services/compliance-payment-gate.ts:86-100) queries:
  contractorComplianceItem.findMany WHERE severity='BLOCKING' AND status='EXPIRED' (+ org guard). Status is LITERAL 'EXPIRED' — NOT derived from expiresAt<now at query time.

materialiseFromPolicy (packages/api/src/services/compliance-supersession.ts:77) creates items at status:'MISSING'. supersedeAndMaterialise (line 113) fetches non-WAIVED rows (findMany WHERE status NOT 'WAIVED', line 118) and WAIVES every row not re-emitted by resolvePolicyRules — THIS is the orphan risk for free-zone rows written out-of-band (Pitfall 2).

isExpired(expiresAt, expiryJurisdictionTz, now?) (packages/compliance-policy/src/expiry.ts:27) — TZ-aware boundary; returns true when start-of-day(now) is AFTER start-of-day(expiresAt) in the TZ. EXPORTED from packages/compliance-policy index. There is NO codebase sweep that flips items PENDING→EXPIRED — so the free-zone service sets status via isExpired at write/re-evaluate time.

reminder scan (packages/api/src/services/compliance-reminder-scan.ts) closes over module-level prismaRaw (=DATABASE_URL=EU). runComplianceReminderScan(now) is the entry; processItem/persistBandFire/dispatchDigest all use prismaRaw; claimCronNotificationDedup keys are global (cron-dedup.ts).

exchange-rates region fan-out template (apps/cron-worker/src/jobs/handlers/exchange-rates.ts) loops SUPPORTED_REGIONS calling getRegionalClient(region) inside try/catch, accumulating per-region errors.

existing uae.free_zone_license@v1 (packages/compliance-policy/src/policies/uae.ts:25-37): severity 'WARNING', appliesIf () => true, documentType 'UAE_FREE_ZONE_LICENSE', expiryJurisdictionTz 'Asia/Dubai', expirySemantic 'fixed_months', expiryMonths 12.
parsePolicyRuleId regex accepts '@v2'. POLICY_RULE_SET_VERSION must bump.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bump policy rule to BLOCKING @v2 + free-zone-compliance service (C1, C2, C4)</name>
  <behavior>
    - C1: writeFreeZoneComplianceItem for a non-Mainland zone with a past licenseExpiresAt creates a ContractorComplianceItem(severity BLOCKING, status EXPIRED, policyRuleId 'uae.free_zone_license@v2') → assertContractorPaymentEligibility blocks.
    - C2 (D-04): writeFreeZoneComplianceItem for zone MAINLAND writes NO item (returns early); payment not blocked.
    - Future-dated license → status PENDING (so the cascade fires reminders), not EXPIRED.
    - C4 (Pitfall 2): supersedeAndMaterialise does NOT WAIVE the free-zone row after an unrelated classification recompute.
  </behavior>
  <read_first>
    - packages/compliance-policy/src/policies/uae.ts (lines 25-37 — the @v1 registration to edit in place)
    - packages/compliance-policy/src/registry.ts (registerPolicyRule + POLICY_RULE_SET_VERSION + parsePolicyRuleId)
    - packages/api/src/services/compliance-supersession.ts (SupersessionClient interface 51-57; materialiseFromPolicy 77-102; supersedeAndMaterialise 113+ incl. the findMany WHERE status NOT WAIVED line 118)
    - packages/api/src/services/compliance-payment-gate.ts (the BLOCKING+EXPIRED query the C1 test asserts against)
    - packages/compliance-policy/src/expiry.ts (isExpired 27)
    - packages/api/src/services/audit-writer.ts (writeAuditLog signature — free-zone migration/write is a sensitive mutation per D-17)
    - .planning/phases/79-.../79-PATTERNS.md (free-zone-compliance.ts section + Pattern 2 + Pitfall 2 supersession-exclusion)
  </read_first>
  <action>
    Edit `packages/compliance-policy/src/policies/uae.ts`: change the existing `uae.free_zone_license@v1` registration in place to `@v2` with `severity: 'BLOCKING'` (D-03) and `appliesIf: () => false` (Pitfall 2 — the classification path must NEVER materialise free-zone; the row is written out-of-band from the service). Keep documentType `UAE_FREE_ZONE_LICENSE`, expiryJurisdictionTz `Asia/Dubai`, expiryMonths 12, draftLegalText `// PENDING legal review`. Bump `POLICY_RULE_SET_VERSION` in registry.ts.
    Create `packages/api/src/services/free-zone-compliance.ts` exporting `writeFreeZoneComplianceItem(client, ctx)` where `client` is the structural `SupersessionClient`-style param (compose inside/outside a tx, mirror compliance-supersession). Logic:
    - If `freeZoneAssignment.zone === 'MAINLAND'` → return `{ written: false }` (D-04 narrowing lives HERE, NOT in appliesIf — Pitfall 3).
    - Else upsert the item: documentType `UAE_FREE_ZONE_LICENSE`, name from displayName, severity `BLOCKING`, policyRuleId `uae.free_zone_license@v2`, expiryJurisdictionTz `Asia/Dubai`, expiresAt `licenseExpiresAt`, status `isExpired(licenseExpiresAt, 'Asia/Dubai') ? 'EXPIRED' : 'PENDING'`. Call `writeAuditLog` with `tx` for the write (sensitive mutation, D-17).
    - Export `reEvaluateFreeZoneStatus(client, item, now)` that flips PENDING→EXPIRED when `isExpired` becomes true (consumed by the cron fan-out so Gulf items transition without inventing a new sweep — Open Q2 resolution: reuse isExpired, mirror how the cascade already derives bands).
    Add supersession isolation: edit `supersedeAndMaterialise`'s non-WAIVED findMany (compliance-supersession.ts:118) to EXCLUDE free-zone rows from the WAIVE scope — add `policyRuleId: { not: { startsWith: 'uae.free_zone' } }` (or an equivalent NOT filter) so an unrelated classification recompute never orphans the free-zone item (Pitfall 2, C4). Add a code comment citing Pitfall 2.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- free-zone-payment-block free-zone-mainland-exclusion free-zone-supersession-isolation 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "uae.free_zone_license@v2" packages/compliance-policy/src/policies/uae.ts` ≥ 1; `grep -c "severity: 'BLOCKING'" packages/compliance-policy/src/policies/uae.ts` ≥ 1; `grep -c "appliesIf: () => false" packages/compliance-policy/src/policies/uae.ts` ≥ 1.
    - `grep -c "MAINLAND" packages/api/src/services/free-zone-compliance.ts` ≥ 1 (D-04 gate).
    - `grep -c "not:\|startsWith: 'uae.free_zone'\|free_zone" packages/api/src/services/compliance-supersession.ts` ≥ 1 (supersession exclusion).
    - free-zone-payment-block, free-zone-mainland-exclusion, free-zone-supersession-isolation tests GREEN.
    - `pnpm --filter @contractor-ops/api lint:audit-log 2>&1 | tail -3` does not flag the new mutation.
  </acceptance_criteria>
  <done>Policy is BLOCKING @v2; free-zone item written from the service with Mainland gate + isExpired status; supersession isolated; C1/C2/C4 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reminder-cron region fan-out (C3, Pitfall 18)</name>
  <behavior>
    - C3: runComplianceReminderScan iterates SUPPORTED_REGIONS; an ME-region BLOCKING free-zone item enters the 90/60/30/15/7 cascade.
    - A region whose env (DATABASE_URL_ME) is unset is skipped gracefully (try/catch around getRegionalClient), and the EU scan still runs.
    - Dedup keys do not collide across regions (region-prefixed).
  </behavior>
  <read_first>
    - packages/api/src/services/compliance-reminder-scan.ts (runComplianceReminderScan entry ~120; the prismaRaw scan query 125-142; processItem 250; persistBandFire 297; dispatchDigest 379; the prismaRaw close-over throughout)
    - packages/api/src/services/cron-dedup.ts (claimCronNotificationDedup key shape)
    - apps/cron-worker/src/jobs/handlers/exchange-rates.ts (the SUPPORTED_REGIONS fan-out template with per-region try/catch)
    - packages/db/src/region.ts (getRegionalClient, SUPPORTED_REGIONS, preWarmRegionalClients graceful-skip pattern 69-77)
    - apps/cron-worker/src/jobs/handlers/reminders/index.ts (line 389 — the single runComplianceReminderScan() call site)
  </read_first>
  <action>
    Refactor `runComplianceReminderScan` to accept a Prisma client: introduce `runComplianceReminderScanForClient(client, now)` that takes the regional client and threads it through `processItem`/`persistBandFire`/`dispatchDigest` (replace every `prismaRaw` with the param). Region-prefix the `claimCronNotificationDedup` keys with the region string to prevent cross-region collisions (Open Q3). Keep `runComplianceReminderScan(now)` as the public entry but have it loop `SUPPORTED_REGIONS`, calling `getRegionalClient(region)` inside try/catch (skip + log a Pino warn on unconfigured regions — no silent catch, D-17), accumulating ScanResult across regions. Do NOT change the band math or digest throttle. The cron handler call site (reminders/index.ts:389) stays `runComplianceReminderScan()` (signature-compatible). Confirm DATABASE_URL_ME presence is a runtime warning, not a hard failure.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- reminder-region-fanout 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SUPPORTED_REGIONS\|getRegionalClient" packages/api/src/services/compliance-reminder-scan.ts` ≥ 2.
    - `grep -c "runComplianceReminderScanForClient" packages/api/src/services/compliance-reminder-scan.ts` ≥ 1.
    - Dedup keys include the region (`grep -c "region" packages/api/src/services/compliance-reminder-scan.ts` shows region-prefixing in the dedup-key construction).
    - reminder-region-fanout test GREEN; no `console.*` introduced (`pnpm lint:silent-catch` + grep for console clean on the file).
  </acceptance_criteria>
  <done>Reminder scan fans out across regions threading a regional client; ME items enter the cascade; graceful skip on unset env; C3 GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: GULF-11 region-leakage lint (C8, Pitfall 19)</name>
  <read_first>
    - packages/db/scripts/audit-enum-casing.ts (tsx-script skeleton: readdir/readFile, build offenders[], print + process.exit(1))
    - packages/db/package.json (scripts block — register the new lint alongside db:audit-enum-casing)
    - .planning/phases/79-.../79-RESEARCH.md (Pitfall 19 + Open Question 4 — a pure "not on EU schema" check is WRONG; the models ARE on both schemas)
    - .planning/phases/79-.../79-PATTERNS.md (lint-region-leakage.ts section — net-new assertion logic)
  </read_first>
  <action>
    Create `packages/db/scripts/lint-region-leakage.ts` (tsx, audit-enum-casing skeleton). Assertion (Pitfall 19): grep `packages/api/src` + `apps/cron-worker/src` source for default-client reads of the 4 Gulf models — fail on any `prisma.freeZoneAssignment` / `prismaRaw.freeZoneAssignment` / same for `saudizationConfig`, `saudiHeadcount`, `uaeFreeZone` — i.e. these models may ONLY be reached via a region-aware path (`ctx.db.<model>` or a client param threaded from `getRegionalClient`). Allowlist the free-zone-compliance + reminder-scan service files where the client is an explicitly-threaded param (they receive a regional client, not the default). Print offenders with file:line; `process.exit(1)` on any. Register `db:lint:region-leakage` in `packages/db/package.json` and expose `lint:region-leakage` at root + append to the `lint:ci` chain. Use `grep -v` / comment-stripping so header comments naming the models are not self-tripping.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db db:lint:region-leakage 2>&1 | tail -8; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "freeZoneAssignment\|saudizationConfig\|saudiHeadcount\|uaeFreeZone" packages/db/scripts/lint-region-leakage.ts` ≥ 4 (all 4 models checked).
    - The lint exits 0 against the current tree (no default-client reads exist yet) and is registered in package.json + lint:ci.
    - A seeded `prisma.freeZoneAssignment` in a scratch test file makes the lint exit 1 (confirm in SUMMARY, then remove the scratch).
  </acceptance_criteria>
  <done>lint-region-leakage flags default-client reads of all 4 Gulf models; wired into lint:ci; C8 lint green on clean tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FreeZoneAssignment service → ContractorComplianceItem | Server-authoritative severity/status; never client-trusted. |
| cron → ME DB | Cron lacks a tenant frame; must use getRegionalClient per region, never the default EU client. |
| API/service code → 4 Gulf models | Must only read via region-aware client (ctx.db / threaded regional client). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-03-01 | Denial of Service | wrongful payment-block on Mainland | mitigate | D-04 zone!=='MAINLAND' gate in free-zone-compliance.ts; C2 test. |
| T-79-03-02 | Information Disclosure | ME free-zone data read via EU default client | mitigate | lint-region-leakage (Task 3) + cron fan-out via getRegionalClient (Task 2). |
| T-79-03-03 | Repudiation | free-zone item silently WAIVED by unrelated supersession | mitigate | supersession exclusion filter + C4 isolation test (Task 1). |
| T-79-03-04 | Tampering | free-zone write without audit trail | mitigate | writeAuditLog on the service write (D-17); lint:audit-log gate. |
| T-79-03-05 | Information Disclosure | dedup-key cross-region collision suppresses a Gulf reminder | mitigate | region-prefixed dedup keys (Task 2). |
| T-79-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (zero new deps). slopcheck + blocking checkpoint if any added. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- free-zone reminder-region` — C1/C2/C3/C4 GREEN.
- `pnpm --filter @contractor-ops/db db:lint:region-leakage` exit 0.
- `pnpm --filter @contractor-ops/api lint:audit-log` + `pnpm lint:silent-catch` clean for touched files.
- `pnpm --filter @contractor-ops/compliance-policy test` GREEN (policy bump didn't break existing rules).
</verification>

<success_criteria>
- Free-zone license expiry hard-blocks payment via the existing gate; Mainland excluded; ME items enter the cascade; supersession isolated; no cross-region leakage. C1/C2/C3/C4/C8 GREEN.
</success_criteria>

<output>
Create `.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-03-SUMMARY.md` when done.
</output>
