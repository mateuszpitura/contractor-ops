---
phase: 79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-
plan: 02
type: execute
wave: 1
depends_on: ["79-01"]
files_modified:
  - packages/db/prisma/schema/gulf.prisma
  - packages/db/prisma/schema/contractor.prisma
  - packages/db/prisma/schema/contract.prisma
  - packages/validators/src/legal/ae.ts
  - packages/validators/src/legal/sa.ts
  - packages/validators/src/index.ts
  - packages/validators/src/__tests__/locked-phrases-guard.test.ts
  - packages/feature-flags/src/signoff-registry-flags.json
  - packages/feature-flags/src/registry.ts
autonomous: false
requirements: [GULF-01, GULF-04, GULF-05, GULF-09, GULF-11]
user_setup: []
must_haves:
  truths:
    - "FreeZoneAssignment (per-contractor — D-01), SaudizationConfig, SaudiHeadcount, UaeFreeZone models exist with organizationId-first tenant scoping + ME-region doc-comment annotation"
    - "ContractorAssignment gains isSaudi/nationality/qiwaContractAuthenticated (D-09); Contract gains activityIsicCodes"
    - "NitaqatBand + UaeFreeZoneCode enums are UPPER_SNAKE and pass db:audit-enum-casing (no CRITICAL added to Severity) — codebase standards over plan templates (D-17)"
    - "LOCKED_AE_PHRASES + LOCKED_SA_PHRASES exist (separate legal/ae.ts + legal/sa.ts — D-15; statutory identifiers only — D-14), are exported from validators index, and are guarded out of messages/*.json"
    - "Gulf feature flags land PENDING in signoff-registry-flags.json"
    - "Migration generated + applied to BOTH regions; prisma client regenerated; enum-casing audit green"
  artifacts:
    - path: "packages/db/prisma/schema/gulf.prisma"
      provides: "4 ME-region Gulf models + NitaqatBand + UaeFreeZoneCode enums"
      contains: "model FreeZoneAssignment"
    - path: "packages/validators/src/legal/ae.ts"
      provides: "LOCKED_AE_PHRASES + RESERVED_AE_LEGAL_KEYS + LockedAePhraseKey"
      contains: "LOCKED_AE_PHRASES"
    - path: "packages/validators/src/legal/sa.ts"
      provides: "LOCKED_SA_PHRASES (Nitaqat band labels + Qiwa terms)"
      contains: "LOCKED_SA_PHRASES"
  key_links:
    - from: "packages/validators/src/index.ts"
      to: "packages/validators/src/legal/ae.ts"
      via: "re-export of LOCKED_AE_PHRASES + RESERVED_AE_LEGAL_KEYS"
      pattern: "LOCKED_AE_PHRASES"
    - from: "packages/validators/src/__tests__/locked-phrases-guard.test.ts"
      to: "RESERVED_AE_LEGAL_KEYS"
      via: "reserved-key spread"
      pattern: "RESERVED_AE_LEGAL_KEYS"
---

<objective>
Wave 1: the data + statutory-constant foundation. Add the 4 ME-region Gulf Prisma models + enums, the additive columns on ContractorAssignment + Contract, the locked-phrase registries (`ae.ts`/`sa.ts`), feature-flag PENDING entries — then run the [BLOCKING] multi-region migrate + generate + enum-casing audit so downstream waves type-check against the live schema.

Purpose: Everything in Waves 2-4 (free-zone service, routers, dashboard, UI) consumes these models, enums, and constants. They must exist and be type-resolvable first.
Output: `gulf.prisma`, edited `contractor.prisma`/`contract.prisma`, `legal/ae.ts`+`sa.ts` (+ index export + guard extension), feature-flag PENDING entries, and an applied migration.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-PATTERNS.md
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-RESEARCH.md
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-CONTEXT.md

<interfaces>
<!-- Extracted from the live tree — executor uses these directly, no exploration needed. -->

ContractorComplianceItem tenant-scoped model shape (packages/db/prisma/schema/contractor.prisma:237-271):
  id String @id @default(cuid()); organizationId String (FIRST field); @@index([organizationId]); FK relations to Organization + Contractor.

Severity enum (contractor.prisma:333) is BLOCKING | WARNING | INFO — DO NOT add CRITICAL (D-03, Pitfall 4).
ContractorAssignment model at contractor.prisma:160; AssignmentStatus enum at 315; Phase 76 added `endedAt DateTime?` (nullable-additive precedent).
Contract model at contract.prisma:3; only `description String?` (line 94) exists today — no activity/ISIC field.

validators index export block (packages/validators/src/index.ts ~355-375) — gb.ts is re-exported as:
  export type { LockedGbPhraseKey } from './legal/gb.js';
  export { LOCKED_GB_PHRASES, RESERVED_GB_LEGAL_KEYS, ...individual consts } from './legal/gb.js';

locked-phrases-guard reserved spread (packages/validators/src/__tests__/locked-phrases-guard.test.ts ~68):
  const reserved = [...RESERVED_LEGAL_KEYS, ...RESERVED_EN_LEGAL_KEYS, ...RESERVED_GB_LEGAL_KEYS];
  plus a `RESERVED_*_KEYS mirrors Object.keys(LOCKED_*_PHRASES)` assertion per registry.

feature-flags signoff gate (packages/feature-flags/src/registry.ts + signoff-registry-flags.json) — gated namespaces require a PENDING entry; boot gate throws if a gated flag lacks one (Phase 70 D-10/D-11).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gulf Prisma models + enums + additive columns</name>
  <read_first>
    - packages/db/prisma/schema/contractor.prisma (ContractorComplianceItem lines 237-271 model convention; Severity enum 333; ContractorAssignment line 160; Phase 76 `endedAt DateTime?` nullable-additive pattern at ~175)
    - packages/db/prisma/schema/contract.prisma (model Contract line 3; description String? line 94)
    - .planning/phases/79-.../79-PATTERNS.md ("gulf.prisma (model, CRUD)" section — model shape, enum convention, ME-region doc-comment, ContractorAssignment column add)
    - .planning/phases/79-.../79-RESEARCH.md (Pattern: "Adding the 3 ContractorAssignment columns" lines 424-438; Pitfall 19 region annotation = doc-comment not @region)
    - packages/api/src/services/regional-storage.ts (REGION_BUCKET_MAP doc-comment style = the GULF-11 annotation precedent)
  </read_first>
  <action>
    Create `packages/db/prisma/schema/gulf.prisma` with four tenant-scoped models, each leading with `id`+`organizationId` and `@@index([organizationId])`, each preceded by a doc-comment declaring ME-region routing intent (GULF-11; "// REGION: ME — accessed only via ctx.db / getRegionalClient(org.dataRegion); never default prisma/prismaRaw. Pitfall 19."):
    - `FreeZoneAssignment` — per-contractor (D-01): `contractorId` (unique per contractor), `zone UaeFreeZoneCode`, `licenseNumber String?`, `licenseCategory String?`, `licenseExpiresAt DateTime? @db.Date`, `permittedActivitiesText String?`, `permittedActivityIsicCodes String[]` (D-05), timestamps. `@@unique([organizationId, contractorId])`, `@@index([organizationId, licenseExpiresAt])`.
    - `SaudizationConfig` — per-org (D-05/GULF-05): `band NitaqatBand?`, `industrySegment String?`, `bandLastUpdatedAt DateTime?`, custom-override fields for GULF-10 drift (`thresholdsCustom Boolean @default(false)`, `permittedActivityCatalogueCustom Boolean @default(false)`), timestamps. `@@unique([organizationId])`.
    - `SaudiHeadcount` — per-org manual (D-10): `totalHeadcount Int`, `saudiHeadcount Int`, `recordedAt DateTime @default(now())`, timestamps. `@@index([organizationId, recordedAt])`.
    - `UaeFreeZone` — global lookup seed (10 zones + Mainland): `code UaeFreeZoneCode @unique`, `authorityLegalNameKey String` (points at a LOCKED_AE_PHRASES key — NOT free text), `isMainland Boolean @default(false)`. This is a global-lookup model (no organizationId) — register it in the FOUND6-01 lint:schema allowlist so the no-organizationId guard does not flag it.
    Add enums to gulf.prisma (values UPPER_SNAKE per D-17): `enum NitaqatBand { PLATINUM HIGH_GREEN MID_GREEN LOW_GREEN YELLOW RED }`, `enum UaeFreeZoneCode { DIFC DMCC IFZA DUBAI_INTERNET_CITY DUBAI_MEDIA_CITY MEYDAN_FZ JAFZA SHAMS RAKEZ ADGM MAINLAND }`. DO NOT add CRITICAL to Severity.
    Edit `contractor.prisma` ContractorAssignment (line 160): add `isSaudi Boolean?`, `nationality String?`, `qiwaContractAuthenticated Boolean? @default(false)` (GULF-04, all nullable-additive). Add back-relation `freeZoneAssignment FreeZoneAssignment?` on Contractor + `saudizationConfig`/`saudiHeadcounts` on Organization as needed for FK integrity.
    Edit `contract.prisma` Contract (line 3): add `activityIsicCodes String[]` (Open Q5 resolution — uncoded contract = empty array → permitted-activity check SKIPS per D-08).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db exec prisma validate 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "model FreeZoneAssignment\|model SaudizationConfig\|model SaudiHeadcount\|model UaeFreeZone" packages/db/prisma/schema/gulf.prisma` == 4.
    - `grep -c "CRITICAL" packages/db/prisma/schema/gulf.prisma packages/db/prisma/schema/contractor.prisma` (filtered `grep -v '^//'`) == 0 (no CRITICAL enum value).
    - Each of the 4 models is preceded by a `// REGION: ME` doc-comment.
    - `prisma validate` passes; `activityIsicCodes` present on Contract; 3 new columns present on ContractorAssignment.
  </acceptance_criteria>
  <done>gulf.prisma + additive columns validate; enums UPPER_SNAKE; ME-region doc-comments present; no CRITICAL.</done>
</task>

<task type="auto">
  <name>Task 2: Locked-phrase registries ae.ts + sa.ts + index export + guard extension (GULF-09, D-14/D-15)</name>
  <read_first>
    - packages/validators/src/legal/gb.ts (EXACT shape: `export const X = '...' as const`, RESERVED_GB_LEGAL_KEYS array, LOCKED_GB_PHRASES record, LockedGbPhraseKey type)
    - packages/validators/src/index.ts (~355-375 gb.ts re-export block to mirror)
    - packages/validators/src/__tests__/locked-phrases-guard.test.ts (~68 reserved spread; ~138 RESERVED mirrors Object.keys assertion; ~145 non-empty-string-leaf assertion)
    - .planning/phases/79-.../79-PATTERNS.md ("legal/ae.ts + sa.ts" section + Pattern 4)
  </read_first>
  <action>
    Create `packages/validators/src/legal/ae.ts` mirroring gb.ts: UAE free-zone authority legal-name constants (e.g. `DMCC_AUTHORITY_LEGAL_NAME`, `DIFC_AUTHORITY_LEGAL_NAME`, `JAFZA_AUTHORITY_LEGAL_NAME`, `ADGM_AUTHORITY_LEGAL_NAME` — one per zone authority, marked `// PENDING legal review` per Standing Constraint), plus `RESERVED_AE_LEGAL_KEYS`, `LOCKED_AE_PHRASES`, `type LockedAePhraseKey`.
    Create `packages/validators/src/legal/sa.ts`: Nitaqat band labels (`NITAQAT_BAND_PLATINUM` … `NITAQAT_BAND_RED`, the literal UPPER_SNAKE band strings, D-14) + Qiwa-auth status terms, plus `RESERVED_SA_LEGAL_KEYS`, `LOCKED_SA_PHRASES`, `type LockedSaPhraseKey`.
    Edit `packages/validators/src/index.ts`: re-export both registries mirroring the gb.ts block (type + LOCKED_*_PHRASES + RESERVED_*_LEGAL_KEYS + individual consts), import path `./legal/ae.js` / `./legal/sa.js`.
    Edit `locked-phrases-guard.test.ts`: add `...RESERVED_AE_LEGAL_KEYS, ...RESERVED_SA_LEGAL_KEYS` to the `reserved` spread, and add the two mirror assertions (`RESERVED_AE_LEGAL_KEYS sorted === Object.keys(LOCKED_AE_PHRASES) sorted`, same for SA) + the non-empty-string-leaf assertion. These keys must NEVER appear in any `messages/*.json`.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/validators test -- locked-phrases-guard 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LOCKED_AE_PHRASES" packages/validators/src/legal/ae.ts packages/validators/src/index.ts` ≥ 2.
    - `grep -c "RESERVED_AE_LEGAL_KEYS\|RESERVED_SA_LEGAL_KEYS" packages/validators/src/__tests__/locked-phrases-guard.test.ts` ≥ 2.
    - locked-phrases-guard suite GREEN (no AE/SA reserved key leaked into any messages/*.json yet).
    - Band labels in sa.ts are the literal UPPER_SNAKE strings matching the NitaqatBand enum.
  </acceptance_criteria>
  <done>ae.ts/sa.ts exist with locked statutory constants, are exported + guard-covered; suite GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Gulf feature flags PENDING + lint:schema allowlist for UaeFreeZone</name>
  <read_first>
    - packages/feature-flags/src/signoff-registry-flags.json (existing PENDING entries shape)
    - packages/feature-flags/src/registry.ts (flag-key registration + gated-namespace gate)
    - the FOUND6-01 lint:schema allowlist (run `pnpm lint:schema` once to find the global-lookup allowlist file path it references)
  </read_first>
  <action>
    Add `gulf-free-zone-tracking` and `gulf-saudization-dashboard` flag keys to `packages/feature-flags/src/registry.ts` and matching PENDING entries to `signoff-registry-flags.json` (Phase 70 D-10 pattern — both are legal-sensitive Gulf namespaces, so they MUST land PENDING; flipping to APPROVED is a post-deploy code commit referencing the legal ticket). Register `UaeFreeZone` (the only new model without `organizationId`) in the FOUND6-01 global-lookup allowlist so `pnpm lint:schema` does not flag it.
  </action>
  <verify>
    <automated>pnpm lint:schema 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gulf-free-zone-tracking\|gulf-saudization-dashboard" packages/feature-flags/src/signoff-registry-flags.json` ≥ 2 with status PENDING.
    - `pnpm lint:schema` GREEN (UaeFreeZone allowlisted; the 3 org-scoped models pass on their own organizationId).
  </acceptance_criteria>
  <done>Two Gulf flags PENDING; UaeFreeZone allowlisted; lint:schema GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [BLOCKING] Multi-region migrate + generate + enum-casing audit</name>
  <what-built>
    Schema for 4 Gulf models + enums + additive columns is written and validates. This task generates the migration, applies it to BOTH regions, regenerates the Prisma client, and runs the enum-casing audit so downstream waves type-check against the live schema. It pauses for human confirmation because the migration commands prompt interactively and the repo is MULTI-REGION (per schema_push_requirement + STATE.md: this repo uses `db:migrate:dev` then `db:migrate:all`, NOT raw `npx prisma db push`, and NOT raw npx from repo root which hits the .npmrc release-age bug).
  </what-built>
  <how-to-verify>
    Run, in order, and confirm each succeeds:
    1. `pnpm --filter @contractor-ops/db db:migrate:dev` — generates the additive migration (name it `phase79_gulf_free_zone_saudization`). Additive-only: CREATE TABLE x4 + CREATE TYPE x2 + ADD COLUMN x4, zero DROP/RENAME.
    2. `pnpm --filter @contractor-ops/db db:generate` — regenerate the prisma-client generator so new model types resolve.
    3. `pnpm --filter @contractor-ops/db db:audit-enum-casing` — MUST be GREEN; ALL new enum values UPPER_SNAKE (NitaqatBand, UaeFreeZoneCode). There is NO CRITICAL severity value — confirm none was added.
    4. Multi-region apply (`pnpm --filter @contractor-ops/db db:migrate:all` against $DATABASE_URL_EU then $DATABASE_URL_ME) is a DEFERRED post-deploy item under LOCAL-ONLY (Phase 70/73/74/76 precedent) — record it in the SUMMARY "Deferred Items" with the migration name; do NOT hard-block local execution on it. NOTE: local `prisma db push` is blocked by the pre-existing `Contractor.search_vector` GENERATED column — if `db:migrate:dev` is blocked the same way, fall back to `db:generate` for type resolution and record the apply as deferred (STATE.md Phase 73 precedent).
  </how-to-verify>
  <resume-signal>Type "approved" once migration generated + client regenerated + enum-casing audit GREEN (or describe the blocker if db:migrate:dev cannot run locally).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema → both physical DBs | Same schema deploys to EU + ME; region is a runtime routing concern (Pitfall 19), not a schema attribute. |
| locked phrase → messages/*.json | Statutory constants must never leak into translatable JSON (drift/mistranslation risk). |
| feature flag → staging | Legal-sensitive Gulf flags must be PENDING (FOUND6-04 boot gate). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-02-01 | Tampering | mistranslated statutory band/authority label | mitigate | LOCKED_AE/SA_PHRASES as code constants + locked-phrases-guard (Task 2); legal review PENDING. |
| T-79-02-02 | Information Disclosure | UaeFreeZone global model missing tenant scope misread cross-org | accept | It is a non-PII global lookup (zone codes + authority-name keys); allowlisted in lint:schema by design. |
| T-79-02-03 | Elevation of Privilege | Gulf flag shipped APPROVED without legal signoff | mitigate | PENDING entries + FOUND6-04 boot gate (Task 3). |
| T-79-02-04 | Tampering | CRITICAL enum value breaks tri-tier contract | mitigate | Task 1 acceptance asserts zero CRITICAL; Task 4 enum-casing audit gate. |
| T-79-02-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (RESEARCH: zero new deps). slopcheck + blocking human checkpoint if any [ASSUMED]/[SUS] added. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/db exec prisma validate` passes.
- `pnpm --filter @contractor-ops/validators test -- locked-phrases-guard` GREEN.
- `pnpm lint:schema` GREEN.
- `pnpm --filter @contractor-ops/db db:audit-enum-casing` GREEN (Task 4).
</verification>

<success_criteria>
- 4 Gulf models + enums + additive columns exist, validate, and are applied/generated; locked-phrase registries + guard cover AE/SA statutory terms; Gulf flags PENDING.
</success_criteria>

<output>
Create `.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-02-SUMMARY.md` when done. Record the multi-region apply as a deferred post-deploy item (migration name + additive-only note).
</output>
