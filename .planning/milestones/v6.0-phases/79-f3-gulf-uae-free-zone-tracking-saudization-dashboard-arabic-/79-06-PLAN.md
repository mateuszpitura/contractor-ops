---
phase: 79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-
plan: 06
type: execute
wave: 4
depends_on: ["79-05"]
files_modified:
  - apps/web-vite/src/components/contractors/free-zone/free-zone-assignment-form.tsx
  - apps/web-vite/src/components/contractors/free-zone/free-zone-assignment-container.tsx
  - apps/web-vite/src/components/contractors/free-zone/hooks/use-free-zone-assignment.ts
  - apps/web-vite/src/components/contractors/free-zone/scope-mismatch-banner.tsx
  - apps/web-vite/src/components/contractors/country-compliance-section.tsx
autonomous: true
requirements: [GULF-01, GULF-03]
must_haves:
  truths:
    - "Admin can record a free-zone assignment (zone Select of 10 zones + Mainland, license number/category/expiry, permitted-activities text + ISIC code tags) and save it"
    - "Free-zone form follows Page → Container → Hook → presentational layering; the hook is the only tRPC boundary"
    - "Scope-mismatch advisory banner renders (--warning, non-blocking) with link to the engagement compliance list"
    - "Old UAE freeform inputs (freeZone/tradeLicense*) are hidden in country-compliance-section (D-02)"
    - "RTL: reuse v4.0 infra, logical properties only (ms-/me-/ps-/pe-) — D-13; loading/empty/error states present"
  artifacts:
    - path: "apps/web-vite/src/components/contractors/free-zone/hooks/use-free-zone-assignment.ts"
      provides: "the only tRPC boundary for the free-zone form (useTRPC + useQuery + useMutation)"
      min_lines: 25
    - path: "apps/web-vite/src/components/contractors/free-zone/free-zone-assignment-form.tsx"
      provides: "presentational free-zone form (props in, JSX out, useTranslations)"
      min_lines: 40
  key_links:
    - from: "apps/web-vite/src/components/contractors/free-zone/free-zone-assignment-container.tsx"
      to: "use-free-zone-assignment hook"
      via: "container calls hook, spreads into form"
      pattern: "useFreeZoneAssignment"
    - from: "apps/web-vite/src/components/contractors/free-zone/hooks/use-free-zone-assignment.ts"
      to: "trpc.gulf.freeZone"
      via: "useMutation(trpc.gulf.freeZone.upsertAssignment...)"
      pattern: "gulf.freeZone"
---

<objective>
Wave 4 (UI): the UAE free-zone assignment surface in web-vite — a Page → Container → Hook → presentational form for recording the free-zone license, the non-blocking scope-mismatch advisory banner, and the D-02 removal of the old UAE freeform inputs. All copy via useTranslations (keys added in Plan 08), RTL logical properties only, full loading/empty/error states.

Purpose: GULF-01 UI + the GULF-03 advisory surface. Binds to the 79-UI-SPEC.md visual contract (focal point = zone Select; --warning for the advisory banner; accent reserved for the single Save CTA).
Output: free-zone form + container + hook + scope-mismatch banner; edited country-compliance-section.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-UI-SPEC.md
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-PATTERNS.md
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-CONTEXT.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
<!-- Extracted from the live tree + Plan 05 router contract. -->

Container analog (apps/web-vite/src/components/settings/expiry-reminder-defaults-container.tsx): thin — calls the hook, spreads into the view, NO direct tRPC.
Read hook analog (apps/web-vite/src/components/dashboard/hooks/use-tax-obligations-widget.ts): useTRPC() → useQuery(trpc.X.queryOptions()) → { isLoading, data }.
Mutation hook analog (apps/web-vite/src/components/settings/hooks/use-expiry-reminder-defaults.ts:38-64): useMutation(trpc.X.mutationOptions({ onSuccess → toast + invalidateQueries, onError → toast })); useTranslations for copy; useId for a11y.
Presentational + form analog (apps/web-vite/src/components/contractors/country-compliance-section.tsx): shadcn primitives from @contractor-ops/ui/components/shadcn/*, useTranslations for ALL copy, country-conditional render. D-02: hide the UAE freeform freeZone/tradeLicense* block here.

tRPC contract (Plan 05): trpc.gulf.freeZone.getAssignment({ contractorId }), trpc.gulf.freeZone.upsertAssignment({ contractorId, zone, licenseNumber, licenseCategory, licenseExpiresAt, permittedActivitiesText, permittedActivityIsicCodes }).

UI-SPEC bindings: focal point = the zone Select (first interactive element, 10 zones + Mainland); accent (Deep Teal --primary) reserved for the single "Save free-zone assignment" CTA; scope-mismatch banner uses --warning alert; logical props only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Free-zone form hook + container + presentational form (GULF-01)</name>
  <read_first>
    - apps/web-vite/ARCHITECTURE.md (Page → Container → Hook → Component; the data-layer/presentational gates)
    - apps/web-vite/src/components/settings/hooks/use-expiry-reminder-defaults.ts (mutation hook: mutationOptions onSuccess toast + invalidateQueries onError toast; useTranslations; useId)
    - apps/web-vite/src/components/dashboard/hooks/use-tax-obligations-widget.ts (read hook shape)
    - apps/web-vite/src/components/settings/expiry-reminder-defaults-container.tsx (thin container)
    - apps/web-vite/src/components/contractors/country-compliance-section.tsx (shadcn imports + form-field render + useTranslations density)
    - apps/web-vite/src/hooks/use-rtl-chart-config.ts (isRtl — not needed for the form, but confirm logical-prop discipline)
    - .planning/phases/79-.../79-UI-SPEC.md (Component Inventory: free-zone form primitives; Focal Points; Copywriting empty/error copy; Mandatory states)
  </read_first>
  <action>
    Create the free-zone surface under apps/web-vite/src/components/contractors/free-zone/:
    - hooks/use-free-zone-assignment.ts — the ONLY tRPC boundary: useTRPC() + useQuery(trpc.gulf.freeZone.getAssignment.queryOptions({ contractorId })) for read, useMutation(trpc.gulf.freeZone.upsertAssignment.mutationOptions({ onSuccess: toast.success + invalidateQueries(getAssignment.queryKey), onError: toast.error })). Return { isLoading, data, save, isSaving }. useTranslations for toast copy keys.
    - free-zone-assignment-container.tsx — thin: call the hook, render loading (skeleton/spinner), empty (UI-SPEC "No free-zone assignment recorded" copy), error (UI-SPEC load-failure copy + Retry), else spread into the form. NO direct tRPC.
    - free-zone-assignment-form.tsx — presentational (props in, JSX out): zone Select (10 zones + Mainland — focal point, first element), licenseNumber/licenseCategory inputs, licenseExpiresAt calendar/date input, permittedActivitiesText textarea, permittedActivityIsicCodes badge-tag input, single "Save free-zone assignment" teal CTA (the ONLY accent). useTranslations for every label/placeholder; useId for a11y; tabular-nums on any numeric; logical props only (ms-/me-/ps-/pe-). WCAG: keyboard nav, ring-primary focus, semantic HTML.
    Zone labels come from i18n keys (Plan 08); zone authority legal names (where shown) come from LOCKED_AE_PHRASES (D-14), NOT translation keys.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite exec tsc --noEmit 2>&1 | grep -i "free-zone" | head; echo "tsc-done"; pnpm check:web-vite-data-layer 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTRPC\|useQuery\|useMutation" apps/web-vite/src/components/contractors/free-zone/free-zone-assignment-form.tsx apps/web-vite/src/components/contractors/free-zone/free-zone-assignment-container.tsx` == 0 (no tRPC outside the hook).
    - `grep -c "gulf.freeZone" apps/web-vite/src/components/contractors/free-zone/hooks/use-free-zone-assignment.ts` ≥ 1.
    - `grep -cE "\b(ml|mr|pl|pr)-" apps/web-vite/src/components/contractors/free-zone/*.tsx` == 0 (RTL logical props only).
    - `grep -ci "useTranslations" apps/web-vite/src/components/contractors/free-zone/free-zone-assignment-form.tsx` ≥ 1; no hardcoded user-facing string literals in JSX.
    - pnpm check:web-vite-data-layer + check:web-vite-presentational pass; pnpm check:rtl-logical-props passes for these files.
  </acceptance_criteria>
  <done>Free-zone form records an assignment via the gulf router; layering + RTL + states conform; GULF-01 UI complete.</done>
</task>

<task type="auto">
  <name>Task 2: Scope-mismatch advisory banner + D-02 hide of UAE freeform inputs (GULF-03/D-02)</name>
  <read_first>
    - apps/web-vite/src/components/contractors/country-compliance-section.tsx (the UAE freeform freeZone/tradeLicense* block to hide; the country-conditional render)
    - .planning/phases/79-.../79-UI-SPEC.md (scope-mismatch banner: --warning alert, non-blocking, link to engagement compliance list; Copywriting advisory copy)
    - .planning/phases/79-.../79-CONTEXT.md (D-02 hide; D-07 non-blocking advisory + auto-NOC)
  </read_first>
  <action>
    Create scope-mismatch-banner.tsx — a presentational --warning alert rendered when contract-create returns { mismatch: true } (the contract-create flow passes the flag down; this banner is non-blocking and links to the affected engagement's compliance list). Copy via useTranslations (UI-SPEC advisory copy: "This contract's activity falls outside the contractor's permitted free-zone activities. An NOC document has been added..."). Use the alert primitive with --warning styling (NOT --destructive). Logical props only.
    Edit country-compliance-section.tsx: for the AE country branch, hide the old freeform UAE inputs (freeZone switch, tradeLicenseNumber, tradeLicenseExpiry) — they are superseded by the new free-zone form (D-02). Do this via the conditional render that reads the trimmed getCountryFieldsConfig field list (Plan 05 removed them server-side); ensure the section degrades cleanly (no dangling label). Minimal diff — do NOT touch the Saudi branch or unrelated fields (D-17).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite exec tsc --noEmit 2>&1 | grep -iE "scope-mismatch|country-compliance" | head; echo "tsc-done"</automated>
  </verify>
  <acceptance_criteria>
    - scope-mismatch-banner.tsx uses the --warning alert (`grep -ci "warning" apps/web-vite/src/components/contractors/free-zone/scope-mismatch-banner.tsx` ≥ 1; no --destructive).
    - country-compliance-section.tsx no longer renders the freeZone/tradeLicense* inputs for AE (the hidden block is gated on the trimmed field list).
    - `grep -cE "\b(ml|mr|pl|pr)-" apps/web-vite/src/components/contractors/free-zone/scope-mismatch-banner.tsx` == 0.
    - No hardcoded advisory string in JSX (useTranslations).
  </acceptance_criteria>
  <done>Non-blocking --warning scope-mismatch banner exists; old UAE freeform inputs hidden; D-02 + GULF-03 UI complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| form → gulf router | All writes go through the Zod-validated, tenant-scoped router (Plan 05); the form is presentational. |
| advisory banner → user | Non-blocking; informs only, never gates contract creation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-06-01 | Tampering | client-side validation bypass | accept | Server Zod (Plan 05) is authoritative; client validation is UX only. |
| T-79-06-02 | Information Disclosure | RTL/layout leaks via ml-/mr- breaking Arabic | mitigate | Logical-props-only + check:rtl-logical-props gate (Plan 01). |
| T-79-06-03 | Repudiation | mistranslated advisory asserting a determination | mitigate | Advisory copy via i18n keys; statutory names from LOCKED_AE_PHRASES; tone is "verify with adviser". |
| T-79-06-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (in-tree shadcn primitives only — UI-SPEC Registry Safety). slopcheck + blocking checkpoint if any added. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/web-vite exec tsc --noEmit` clean for the new free-zone files.
- `pnpm check:web-vite-data-layer && pnpm check:web-vite-presentational && pnpm check:web-vite-page-shells` pass.
- `pnpm check:rtl-logical-props` passes for the free-zone surface.
</verification>

<success_criteria>
- Free-zone assignment form (layered, RTL-clean, all states) records a license; scope-mismatch advisory banner renders non-blocking; old UAE freeform inputs hidden. GULF-01/03 UI complete.
</success_criteria>

<output>
Create `.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-06-SUMMARY.md` when done.
</output>
