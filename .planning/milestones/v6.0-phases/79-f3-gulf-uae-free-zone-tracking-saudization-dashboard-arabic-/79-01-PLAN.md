---
phase: 79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/api/src/__tests__/free-zone-payment-block.test.ts
  - packages/api/src/__tests__/free-zone-mainland-exclusion.test.ts
  - packages/api/src/__tests__/free-zone-supersession-isolation.test.ts
  - packages/api/src/__tests__/reminder-region-fanout.test.ts
  - packages/api/src/__tests__/permitted-activity-noc.test.ts
  - packages/api/src/__tests__/saudization-derivation.test.ts
  - packages/api/src/__tests__/gulf-override-audit.test.ts
  - packages/api/src/__tests__/__fixtures__/gulf-fixtures.ts
  - packages/db/scripts/check-rtl-logical-props.mjs
  - package.json
autonomous: true
requirements: [GULF-02, GULF-03, GULF-05, GULF-06, GULF-07, GULF-08, GULF-10, GULF-11]
must_haves:
  truths:
    - "Every C1-C10 critical behavior has a RED (failing or skipped-pending) test file checked into packages/api/src/__tests__"
    - "A shared Gulf test fixture factory (ME-region org + UAE/KSA contractor + FreeZoneAssignment) exists for C3/C8 reuse"
    - "The ml-/mr- RTL logical-property guard for Gulf surfaces is confirmed-present or built and runs in lint:ci"
  artifacts:
    - path: "packages/api/src/__tests__/__fixtures__/gulf-fixtures.ts"
      provides: "ME-region org + contractor + FreeZoneAssignment + ContractorComplianceItem factories"
      min_lines: 40
    - path: "packages/db/scripts/check-rtl-logical-props.mjs"
      provides: "GULF-08 ml-/mr- ban guard for Gulf web-vite surfaces"
      min_lines: 20
  key_links:
    - from: "package.json"
      to: "scripts/check-rtl-logical-props.mjs"
      via: "lint:ci or a check:rtl-logical-props script"
      pattern: "check:rtl-logical-props|check-rtl-logical-props"
---

<objective>
Wave 0: lay down the failing-test scaffolds that pin Nyquist critical behaviors C1-C10, the shared ME-region Gulf test fixture factory, and resolve the two Wave-0 validation prerequisites (region-aware fixtures + the `ml-`/`mr-` RTL guard whose existence RESEARCH could not confirm — Pitfall 20).

Purpose: Establish the verification surface BEFORE implementation so every downstream plan has a concrete `<automated>` command to turn GREEN. Per VALIDATION.md, no behavior C1-C10 may ship without a test task; this plan creates those test files in RED state.
Output: 7 RED test files (use `describe.todo`/`it.todo` or `expect(...).toThrow('not implemented')` so the suite is RED-but-runnable), one fixture factory, and the verified/built RTL guard.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-VALIDATION.md
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-RESEARCH.md
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED test scaffolds for C1-C10</name>
  <read_first>
    - .planning/phases/79-.../79-VALIDATION.md (Critical Behaviors table C1-C10 + Per-Task Verification Map)
    - .planning/phases/79-.../79-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map; Wave 0 Gaps list)
    - packages/api/src/services/__tests__/compliance-payment-gate.test.ts (analog: how an EXPIRED ContractorComplianceItem fixture is built — status:'EXPIRED', expiresAt, severity:'BLOCKING')
    - packages/api/src/services/compliance-payment-gate.ts (the BLOCKING+EXPIRED query the C1/C2 tests must exercise)
  </read_first>
  <action>
    Create one test file per behavior cluster, each describing the target behavior with `describe.todo`/`it.todo` (or a single `it('...', () => { throw new Error('NOT_IMPLEMENTED — Wave 2'); })`) so the suite is RED-but-runnable and self-documents the contract:
    - `free-zone-payment-block.test.ts` — C1 (GULF-02): an EXPIRED free-zone `ContractorComplianceItem` (severity BLOCKING, status EXPIRED, policyRuleId 'uae.free_zone_license@v2') causes `assertContractorPaymentEligibility` to block.
    - `free-zone-mainland-exclusion.test.ts` — C2 (GULF-01/02, D-04): recording a MAINLAND `FreeZoneAssignment` writes NO compliance item → not blocked.
    - `free-zone-supersession-isolation.test.ts` — C4 (Pitfall 2): a free-zone item survives `supersedeAndMaterialise` (NOT WAIVED) after an unrelated classification recompute.
    - `reminder-region-fanout.test.ts` — C3 (Pitfall 18): the reminder scan iterates SUPPORTED_REGIONS so an ME-region BLOCKING item enters the cascade.
    - `permitted-activity-noc.test.ts` — C5 (GULF-03, D-05..D-08): ISIC-code overlap miss fires advisory + creates a WARNING NOC `ContractorComplianceItem`; uncoded contract (no activityIsicCodes) → no advisory.
    - `saudization-derivation.test.ts` — C6+C7 (GULF-05/06/07): rate computed from manual SaudiHeadcount numbers (NOT platform contractors); band never auto-computed; trajectory is ephemeral/advisory.
    - `gulf-override-audit.test.ts` — C9 (GULF-10): a Nitaqat-threshold / permitted-activity override calls writeAuditLog with `metadata.custom: true`.
    Each file MUST reference the C# and GULF-ID in a top-of-file comment so downstream executors map test→behavior. Do NOT implement production code here.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- free-zone-payment-block free-zone-mainland-exclusion free-zone-supersession-isolation reminder-region-fanout permitted-activity-noc saudization-derivation gulf-override-audit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 7 test files exist under packages/api/src/__tests__/ matching the names in files_modified.
    - `grep -l "C1\|C2\|C3\|C4\|C5\|C6\|C7\|C9\|GULF-0" packages/api/src/__tests__/free-zone-*.test.ts` returns each file (behavior traceability comment present).
    - Vitest runs the files without a collection error (RED via todo/throw is acceptable; a syntax/import error is NOT).
  </acceptance_criteria>
  <done>7 RED test files run under vitest, each annotated with its C#/GULF-ID; no production code added.</done>
</task>

<task type="auto">
  <name>Task 2: Shared ME-region Gulf fixture factory</name>
  <read_first>
    - packages/api/src/services/__tests__/compliance-payment-gate.test.ts (fixture-building style)
    - packages/db/src/region.ts (SUPPORTED_REGIONS = ['EU','ME']; getRegionalClient; DataRegion)
    - packages/db/prisma/schema/contractor.prisma (ContractorComplianceItem shape lines 237-271; ContractorAssignment line 160)
  </read_first>
  <action>
    Create `packages/api/src/__tests__/__fixtures__/gulf-fixtures.ts` exporting factory helpers used by C3/C8 and downstream service tests: `makeMeOrg(overrides?)` (org with `dataRegion: 'ME'`, `countryCode: 'AE'`/`'SA'`), `makeFreeZoneAssignment(overrides?)` (zone defaulting to `'DMCC'`, `licenseExpiresAt`, `permittedActivityIsicCodes`), and `makeFreeZoneComplianceItem(overrides?)` (severity `'BLOCKING'`, policyRuleId `'uae.free_zone_license@v2'`, documentType `'UAE_FREE_ZONE_LICENSE'`, expiryJurisdictionTz `'Asia/Dubai'`, status param-driven). Keep these as plain object factories (no DB writes) so unit tests stay fast; integration tests can pass them to a mocked Prisma client. Values use UPPER_SNAKE enum strings per D-17.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api exec tsc --noEmit -p tsconfig.json 2>&1 | grep -i "gulf-fixtures" | head; echo "exit-ok"</automated>
  </verify>
  <acceptance_criteria>
    - File exports `makeMeOrg`, `makeFreeZoneAssignment`, `makeFreeZoneComplianceItem`.
    - `grep -c "dataRegion: 'ME'" packages/api/src/__tests__/__fixtures__/gulf-fixtures.ts` ≥ 1.
    - No TypeScript error references the fixtures file.
  </acceptance_criteria>
  <done>Fixture factory compiles and is importable by the C1-C10 test files.</done>
</task>

<task type="auto">
  <name>Task 3: Verify-or-build the ml-/mr- RTL logical-property guard (GULF-08, Pitfall 20)</name>
  <read_first>
    - package.json (root) — the `lint:ci` chain (line 16) + the `check:web-vite-*` script registrations (lines 39-43)
    - packages/db/scripts/audit-enum-casing.ts (tsx/node script skeleton: readdir/readFile, build offenders[], process.exit(1))
    - .planning/phases/79-.../79-RESEARCH.md Pitfall 20 + 79-PATTERNS.md "No Analog Found" (guard could not be located; web-vite uses Biome)
  </read_first>
  <action>
    First CONFIRM whether a guard banning `ml-`/`mr-`/`pl-`/`pr-`/`left-`/`right-` on web-vite surfaces already exists (grep `package.json` + `biome.ci.json` + `apps/web-vite` config for `ml-`/`mr-`/`noRestrictedSyntax`/`logical`). If absent (expected per A5), build `packages/db/scripts/check-rtl-logical-props.mjs` (Node ESM, mirroring audit-enum-casing.ts structure): walk `apps/web-vite/src/components/{saudization,contractors/free-zone}` plus any file importing a Gulf message namespace, fail on a `\bm[lr]-` / `\bp[lr]-` Tailwind class match, print offenders, `process.exit(1)`. Register it as a `check:rtl-logical-props` script in root package.json and append it to the `lint:ci` chain. Scope the walk to Gulf surfaces only (do NOT flag pre-existing offenders elsewhere — D-17 scope-boundary). Use a `\b(ms|me|ps|pe|start|end)-` allowlist note in a header comment.
  </action>
  <verify>
    <automated>pnpm check:rtl-logical-props 2>&1 | tail -5; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "check:rtl-logical-props" package.json` ≥ 2 (script def + lint:ci chain entry) OR a documented existing guard is cited in the SUMMARY with its exact script name.
    - The guard greps for `\bm[lr]-` and exits non-zero when a Gulf surface contains `ml-`/`mr-`.
    - Header comment uses `grep -v '^#'`-safe token references (the script's own match list is not self-tripping).
  </acceptance_criteria>
  <done>`pnpm check:rtl-logical-props` runs clean on the empty Gulf-surface set and is wired into lint:ci; GULF-08 is now enforceable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → fixtures | Fixtures are dev-only; no production trust crossing. |
| CI guard → source tree | The RTL guard reads source files read-only; no execution of source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01-01 | Tampering | fixture factory leaking into prod bundle | accept | Fixtures live under `__tests__/__fixtures__` — excluded from build; no runtime import path from app code. |
| T-79-01-02 | Repudiation | RTL guard false-negative (misses `ml-`) | mitigate | Guard regex `\bm[lr]-` covers both; unit-confirm against a seeded offender in the SUMMARY. |
| T-79-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH: zero new deps). If any added, slopcheck + blocking human checkpoint for [ASSUMED]/[SUS]. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- free-zone permitted-activity saudization gulf-override reminder-region` collects and runs all 7 RED files.
- `pnpm check:rtl-logical-props` exits 0 on the (currently empty) Gulf surface set.
- No production source under `packages/api/src/services` or `packages/api/src/routers` modified.
</verification>

<success_criteria>
- 7 RED test files + fixture factory + RTL guard exist and run; every C1-C10 behavior has a named failing test for a downstream plan to turn GREEN.
</success_criteria>

<output>
Create `.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-01-SUMMARY.md` when done.
</output>
