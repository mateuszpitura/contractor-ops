---
phase: 79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-
plan: 05
type: execute
wave: 3
depends_on: ["79-03", "79-04"]
files_modified:
  - packages/api/src/routers/gulf/free-zone.ts
  - packages/api/src/routers/gulf/saudization.ts
  - packages/api/src/routers/gulf/index.ts
  - packages/api/src/root.ts
  - packages/api/src/routers/core/contractor.ts
  - packages/api/src/routers/core/contract.ts
  - packages/db/scripts/backfill-free-zone-assignment.ts
  - packages/api/src/__tests__/gulf-override-audit.test.ts
autonomous: true
requirements: [GULF-01, GULF-04, GULF-10]
must_haves:
  truths:
    - "Admin can CRUD a FreeZoneAssignment (zone + license number/category/expiry + permitted-activities text + ISIC codes) via tenant-scoped, Zod-validated, region-aware tRPC"
    - "Saving a free-zone assignment writes/supersedes its compliance item via the Plan 03 service"
    - "Admin can record per-engagement isSaudi/nationality/qiwaContractAuthenticated and manual SaudizationConfig/SaudiHeadcount"
    - "Drift override (Nitaqat threshold / permitted-activity catalogue) is audit-logged with metadata.custom=true (C9, GULF-10)"
    - "Contract-create invokes the permitted-activity ISIC check (auto-NOC on mismatch, skip on uncoded)"
    - "D-02: existing UAE freeform countryFields are backfilled into FreeZoneAssignment and AE freeform inputs are removed from getCountryFieldsConfig"
    - "Gulf models are accessed only via ctx.db (region-aware) — region-leakage lint stays green"
  artifacts:
    - path: "packages/api/src/routers/gulf/free-zone.ts"
      provides: "free-zone assignment CRUD + per-engagement Saudi fields"
      exports: ["freeZoneRouter"]
    - path: "packages/api/src/routers/gulf/saudization.ts"
      provides: "config/headcount CRUD + dashboard query + drift overrides"
      exports: ["saudizationRouter"]
    - path: "packages/db/scripts/backfill-free-zone-assignment.ts"
      provides: "D-02 idempotent countryFields → FreeZoneAssignment backfill"
      min_lines: 30
  key_links:
    - from: "packages/api/src/root.ts"
      to: "gulfRouter"
      via: "gulf: gulfRouter in appRouter"
      pattern: "gulf: gulfRouter"
    - from: "packages/api/src/routers/gulf/saudization.ts"
      to: "writeAuditLog"
      via: "override mutation audit log with metadata.custom"
      pattern: "writeAuditLog"
    - from: "packages/api/src/routers/core/contract.ts"
      to: "checkPermittedActivityScope"
      via: "contract-create invocation"
      pattern: "checkPermittedActivityScope|permitted-activity"
---

<objective>
Wave 3: expose the Gulf backend through tRPC. New `gulf` namespace with free-zone CRUD, per-engagement Saudi fields, Saudization config/headcount CRUD + dashboard query, and GULF-10 drift overrides — all tenant-scoped, Zod-validated, region-aware (ctx.db), audit-logged. Wire the permitted-activity check into contract-create, mount the namespace, and run the D-02 backfill + AE-field hide.

Purpose: Connects the Plan 03/04 services to the UI layer (Plans 06-08) and the contract-create path. Turns C9 GREEN.
Output: `gulf/free-zone.ts`, `gulf/saudization.ts`, `gulf/index.ts`, root mount, contract/contractor edits, backfill script.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-PATTERNS.md
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-RESEARCH.md
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-CONTEXT.md

<interfaces>
<!-- Extracted from the live tree. -->

zatca router pattern (packages/api/src/routers/compliance/zatca.ts:36-42):
  router({ proc: tenantProcedure.use(requirePermission({ settings: ['update'] })).input(z.object({...})).mutation/query(async ({ ctx, input }) => ...) })

ctx.db is ALREADY region-aware (packages/api/src/middleware/tenant.ts resolves org.dataRegion → getRegionalClient). Gulf routers use ctx.db.<model> directly; ME orgs transparently hit ME DB. NEVER prisma/prismaRaw for the 4 Gulf models (Pitfall 19 / lint-region-leakage).

contractor.ts updateCountryFields (lines 1340-1375): Zod schema map + safeParse + BAD_REQUEST + tenant-scoped ctx.db update — the validate-then-write pattern.
contractor.ts getCountryFieldsConfig (lines 1312-1325): AE field list ['freelancePermitNumber','tradeLicenseNumber','freeZone','tradeLicenseExpiry'] — D-02 removes the free-zone ones (freeZone/tradeLicense*) from the AE list (Saudi list untouched).

root.ts appRouter = router({...}) (~line 150) — add gulf: gulfRouter alongside the other namespaces. Barrel-export from packages/api/src/routers/gulf/index.ts mirroring packages/api/src/routers/finance/index.ts.

writeAuditLog (packages/api/src/services/audit-writer.ts): { organizationId, actorType:'USER', action, resourceType, resourceId, metadata, tx }. GULF-10 override → metadata: { before, after, custom: true } drives the "Custom — verify with adviser" badge.

services from prior waves: writeFreeZoneComplianceItem (Plan 03); checkPermittedActivityScope, computeSaudizationDashboard, projectOffboardingTrajectory (Plan 04).
contract create path (packages/api/src/routers/core/contract.ts) — invoke checkPermittedActivityScope after the contract row is created, inside the same tx.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gulf free-zone + saudization routers + namespace mount (GULF-01/04/10, C9)</name>
  <behavior>
    - gulf.freeZone.upsertAssignment validates (Zod: zone enum, license fields, ISIC codes array) and writes the FreeZoneAssignment via ctx.db, then calls writeFreeZoneComplianceItem in the same tx; saving a non-Mainland expired license yields a BLOCKING+EXPIRED item.
    - gulf.freeZone.setSaudiAssignmentFields persists isSaudi/nationality/qiwaContractAuthenticated on ContractorAssignment (Zod-validated).
    - gulf.saudization.upsertConfig / upsertHeadcount persist manual band/segment/headcount with bandLastUpdatedAt; band is taken from input, never computed.
    - gulf.saudization.dashboard returns computeSaudizationDashboard output.
    - gulf.saudization.applyNitaqatThresholdOverride / applyPermittedActivityOverride set the *Custom flag + writeAuditLog with metadata.custom=true (C9, GULF-10).
    - Every mutation is tenant-scoped (ctx.organizationId) + requirePermission; reads use ctx.db only.
  </behavior>
  <read_first>
    - packages/api/src/routers/compliance/zatca.ts (tenantProcedure + requirePermission + Zod proc shape)
    - packages/api/src/routers/core/contractor.ts (updateCountryFields safeParse pattern 1340-1375; computeComplianceHealth read)
    - packages/api/src/routers/finance/index.ts (barrel shape) + packages/api/src/root.ts (~150 appRouter mount)
    - packages/api/src/services/free-zone-compliance.ts (Plan 03) + permitted-activity-check.ts + saudization-dashboard.ts (Plan 04)
    - packages/api/src/services/audit-writer.ts (writeAuditLog)
    - .planning/phases/79-.../79-PATTERNS.md (gulf/free-zone.ts + saudization.ts sections; Shared Patterns: audit/region/tenant)
  </read_first>
  <action>
    Create packages/api/src/routers/gulf/free-zone.ts (freeZoneRouter): getAssignment (query, ctx.db.freeZoneAssignment.findUnique by contractorId, org-scoped), upsertAssignment (mutation — Zod schema: zone UaeFreeZoneCode enum, licenseNumber/licenseCategory strings, licenseExpiresAt date, permittedActivitiesText string, permittedActivityIsicCodes string[]; safeParse to BAD_REQUEST; ctx.db upsert inside a ctx.db.$transaction that also calls writeFreeZoneComplianceItem), setSaudiAssignmentFields (mutation — isSaudi/nationality/qiwaContractAuthenticated on an assignment). All requirePermission({ contractor: ['update'] }) (or the closest existing permission), Zod inputs, audit-logged where D-17 classifies sensitive (free-zone write + Saudi-field write).
    Create packages/api/src/routers/gulf/saudization.ts (saudizationRouter): getConfig/upsertConfig (band NitaqatBand enum from input + industrySegment + bandLastUpdatedAt set server-side on change), upsertHeadcount (totalHeadcount/saudiHeadcount ints), dashboard (query → computeSaudizationDashboard with ctx.db reads for headcount/config/qiwa-gap/Iqama items), offboardingTrajectory (query → projectOffboardingTrajectory, ephemeral), applyNitaqatThresholdOverride + applyPermittedActivityOverride (mutations — set the *Custom flag on SaudizationConfig + writeAuditLog action gulf.nitaqat_threshold.override / gulf.permitted_activity.override, metadata { before, after, custom: true }, tx). requirePermission({ settings: ['update'] }).
    Create packages/api/src/routers/gulf/index.ts barrel exporting gulfRouter = router({ freeZone: freeZoneRouter, saudization: saudizationRouter }). Mount gulf: gulfRouter in root.ts appRouter.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- gulf-override-audit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gulf: gulfRouter" packages/api/src/root.ts` == 1.
    - `grep -c "ctx.db.freeZoneAssignment\|ctx.db.saudizationConfig\|ctx.db.saudiHeadcount" packages/api/src/routers/gulf/*.ts` ≥ 3; zero default-client (`prisma.`/`prismaRaw.`) reads of the 4 Gulf models in the routers.
    - `grep -c "writeAuditLog" packages/api/src/routers/gulf/saudization.ts` ≥ 1 with `custom: true` metadata.
    - `grep -c "z.object\|safeParse\|requirePermission" packages/api/src/routers/gulf/free-zone.ts` ≥ 2 (Zod + permission on every proc).
    - gulf-override-audit test GREEN (C9); `pnpm --filter @contractor-ops/api exec tsc --noEmit` shows no error referencing gulf routers.
  </acceptance_criteria>
  <done>gulf namespace mounted; free-zone + saudization CRUD + dashboard + overrides tenant-scoped, region-aware, audit-logged; C9 GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Wire permitted-activity check into contract-create + D-02 backfill + AE-field hide</name>
  <read_first>
    - packages/api/src/routers/core/contract.ts (the create mutation + its transaction)
    - packages/api/src/services/permitted-activity-check.ts (Plan 04 — checkPermittedActivityScope signature)
    - packages/api/src/routers/core/contractor.ts (getCountryFieldsConfig 1312-1325 AE field list to trim)
    - packages/db/scripts/audit-enum-casing.ts (tsx-script skeleton for the backfill) + STATE.md Phase 70/74 multi-region backfill precedent (idempotent, dry-run, deferred post-deploy)
    - packages/validators/src/country-fields.ts (UaeCountryFields — backfill source shape: tradeLicenseNumber/freeZone/tradeLicenseExpiry)
    - .planning/phases/79-.../79-RESEARCH.md (Runtime State Inventory — D-02 migration: hide inputs, backfill, do NOT delete countryFields)
  </read_first>
  <action>
    Edit packages/api/src/routers/core/contract.ts create path: after the contract row is created (inside the same tx), if the contractor has a FreeZoneAssignment and the contract has activityIsicCodes, call checkPermittedActivityScope (auto-NOC on mismatch, skip on uncoded). Surface the { mismatch } result so the UI can render the banner (Plan 06). Non-blocking — creation always proceeds (D-07).
    Edit packages/api/src/routers/core/contractor.ts getCountryFieldsConfig: remove the free-zone freeform fields (tradeLicenseNumber, freeZone, tradeLicenseExpiry) from the AE field list so the old UAE inputs no longer render (D-02). Keep the Saudi list untouched; keep freelancePermitNumber only if it is NOT a free-zone field being migrated (confirm against country-fields.ts).
    Create packages/db/scripts/backfill-free-zone-assignment.ts (tsx, idempotent, --dry-run supported, multi-region per Phase 70/74 precedent): read each AE-org contractor's countryFields JSONB (tradeLicenseNumber/freeZone/tradeLicenseExpiry), insert a FreeZoneAssignment row WHERE one does not already exist (idempotent guard), mapping freeZone bool to a zone per a documented default rule, do NOT delete the JSONB (audit/rollback). Saudi countryFields are NOT migrated (D-02). The multi-region apply (EU then ME) is a DEFERRED post-deploy item under LOCAL-ONLY — record it in the SUMMARY.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/db exec tsx scripts/backfill-free-zone-assignment.ts --dry-run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "checkPermittedActivityScope\|permitted-activity" packages/api/src/routers/core/contract.ts` ≥ 1 (wired into create).
    - The AE field list in getCountryFieldsConfig no longer contains tradeLicenseNumber/freeZone/tradeLicenseExpiry (`grep -c "tradeLicenseNumber" packages/api/src/routers/core/contractor.ts` reduced in the AE branch).
    - backfill script supports `--dry-run`, is idempotent (WHERE-not-exists guard), and does NOT delete countryFields (`grep -ci "delete\|deleteMany" packages/db/scripts/backfill-free-zone-assignment.ts` against countryFields == 0).
    - `pnpm --filter @contractor-ops/api exec tsc --noEmit` shows no new error in contract.ts/contractor.ts.
  </acceptance_criteria>
  <done>Contract-create runs the scope check; AE freeform inputs hidden; idempotent backfill written (apply deferred); D-02 satisfied at the data + config layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → gulf router | Untrusted input crosses here — Zod + tenant scope mandatory. |
| router → ME DB | ctx.db region-aware; no default-client reads of Gulf models. |
| override mutation → audit log | Sensitive drift override must be audit-logged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-05-01 | Tampering | spoofed organizationId / contractorId in router input | mitigate | Tenant scope from ctx.organizationId; findUnique/upsert org-scoped; never client tenant. |
| T-79-05-02 | Information Disclosure | IDOR read of another org's FreeZoneAssignment/Saudization | mitigate | ctx.db tenant client + org-scoped where clauses on every proc. |
| T-79-05-03 | Repudiation | drift override without audit trail | mitigate | writeAuditLog with metadata.custom on both override mutations (C9). |
| T-79-05-04 | Information Disclosure | Gulf model read via default EU client (cross-region) | mitigate | ctx.db only; lint-region-leakage (Plan 03) covers routers. |
| T-79-05-05 | Tampering | injection via permitted-activities text / ISIC codes | mitigate | Zod-validated strings/arrays; stored as data, never interpolated into SQL (Prisma parameterised). |
| T-79-05-06 | Tampering | D-02 backfill data loss | mitigate | Idempotent WHERE-not-exists; countryFields JSONB retained (no delete). |
| T-79-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. slopcheck + blocking checkpoint if any added. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- gulf-override-audit` — C9 GREEN.
- `pnpm --filter @contractor-ops/api exec tsc --noEmit` clean for gulf routers + contract/contractor edits.
- `pnpm --filter @contractor-ops/db db:lint:region-leakage` exit 0 (routers use ctx.db).
- `pnpm --filter @contractor-ops/api lint:audit-log` clean (override + write mutations logged).
- `pnpm --filter @contractor-ops/db exec tsx scripts/backfill-free-zone-assignment.ts --dry-run` runs without writing.
</verification>

<success_criteria>
- gulf namespace exposes free-zone + Saudization CRUD + dashboard + overrides, all tenant-scoped/region-aware/audit-logged; contract-create runs the ISIC check; D-02 data + config migration in place. C9 GREEN.
</success_criteria>

<output>
Create `.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-05-SUMMARY.md` when done. Record the multi-region backfill apply as a deferred post-deploy item.
</output>
