---
phase: 79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-
plan: 07
type: execute
wave: 4
depends_on: ["79-05"]
files_modified:
  - apps/web-vite/src/components/saudization/saudization-dashboard-container.tsx
  - apps/web-vite/src/components/saudization/saudization-dashboard.tsx
  - apps/web-vite/src/components/saudization/hooks/use-saudization-dashboard.ts
  - apps/web-vite/src/components/saudization/hooks/use-saudization-config.ts
  - apps/web-vite/src/components/saudization/nitaqat-override-dialog.tsx
  - apps/web-vite/src/components/saudization/offboarding-trajectory-banner.tsx
autonomous: true
requirements: [GULF-05, GULF-06, GULF-07, GULF-10]
must_haves:
  truths:
    - "Saudization dashboard shows nationalisation rate (hero stat), current band (locked label, neutral badge), Qiwa-auth gap count (visibility-only — D-11), Iqama roll-up, and total/Saudi headcount with platform-derived counts side-by-side (subordinate)"
    - "Admin can manually update the band (6-value enum) + industry-segment; last-updated timestamp + quarterly re-entry prompt shown; system never auto-computes the band"
    - "Drift override dialog applies a Nitaqat threshold / permitted-activity override and shows the 'Custom — verify with adviser' badge"
    - "Offboarding a Saudi-national renders the advisory, non-authoritative, non-gating band-trajectory banner"
    - "Layering (Page→Container→Hook→presentational); RTL logical props + RTL charts via useRtlChartConfig; loading/empty/error states"
  artifacts:
    - path: "apps/web-vite/src/components/saudization/hooks/use-saudization-dashboard.ts"
      provides: "read-only tRPC boundary for the dashboard"
      min_lines: 12
    - path: "apps/web-vite/src/components/saudization/saudization-dashboard.tsx"
      provides: "presentational dashboard (hero rate, neutral band badge, side-by-side headcount, charts)"
      min_lines: 50
    - path: "apps/web-vite/src/components/saudization/offboarding-trajectory-banner.tsx"
      provides: "advisory, non-authoritative band-trajectory banner"
      min_lines: 20
  key_links:
    - from: "apps/web-vite/src/components/saudization/saudization-dashboard-container.tsx"
      to: "use-saudization-dashboard hook"
      via: "container calls hook, spreads into view"
      pattern: "useSaudizationDashboard"
    - from: "apps/web-vite/src/components/saudization/saudization-dashboard.tsx"
      to: "useRtlChartConfig"
      via: "RTL-aware band donut/progress chart"
      pattern: "useRtlChartConfig"
---

<objective>
Wave 4 (UI, parallel with Plan 06): the Saudization dashboard, the manual band/headcount entry, the GULF-10 drift-override dialog with the "Custom — verify with adviser" badge, and the GULF-07 offboarding band-trajectory banner. Layered web-vite surfaces, RTL charts via useRtlChartConfig, full states.

Purpose: GULF-05/06/07/10 UI. The band is a neutral locked label (never colorized to assert a judgement); the rate is the hero stat from manual headcount; the trajectory banner is advisory-only.
Output: dashboard container/view/hooks, override dialog, trajectory banner.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-UI-SPEC.md
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-PATTERNS.md
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-CONTEXT.md
@apps/web-vite/ARCHITECTURE.md

<interfaces>
<!-- Extracted from the live tree + Plan 05 router contract. -->

Read hook analog (apps/web-vite/src/components/dashboard/hooks/use-tax-obligations-widget.ts).
Mutation hook analog (apps/web-vite/src/components/settings/hooks/use-expiry-reminder-defaults.ts:38-64).
Container analog (apps/web-vite/src/components/settings/expiry-reminder-defaults-container.tsx).
RTL chart config (apps/web-vite/src/hooks/use-rtl-chart-config.ts): useRtlChartConfig() → { xAxisProps reversed, yAxisProps orientation, chartStyle direction:rtl }; isRtl(locale==='ar').

tRPC contract (Plan 05): trpc.gulf.saudization.dashboard (returns nationalisationRate, band, bandLastUpdatedAt, quarterlyReentryDue, qiwaGapCount, iqamaRollup, platformDerived, total/saudi headcount), .upsertConfig, .upsertHeadcount, .applyNitaqatThresholdOverride, .applyPermittedActivityOverride, .offboardingTrajectory.

UI-SPEC bindings: focal point = nationalisation rate % (Display role, text-2xl, tabular-nums, text-primary). Band label is locked statutory text (LOCKED_SA_PHRASES / NitaqatBand), rendered as a NEUTRAL badge (variant outline/secondary) — MUST NOT colorize to assert a band judgement (D-12 / Pitfall 8). --warning for: manual-nature callout, quarterly prompt, Qiwa-gap, override badge, trajectory banner. Headcount cross-check: manual numbers authoritative, platform-derived visually subordinate (--muted-foreground). Charts via useRtlChartConfig. DialogBody+DialogFooter convention for the band/headcount/override dialogs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Saudization dashboard hook + container + presentational view (GULF-05/06)</name>
  <read_first>
    - apps/web-vite/ARCHITECTURE.md (layering + gates)
    - apps/web-vite/src/components/dashboard/hooks/use-tax-obligations-widget.ts (read hook)
    - apps/web-vite/src/components/settings/hooks/use-expiry-reminder-defaults.ts (mutation hook)
    - apps/web-vite/src/components/settings/expiry-reminder-defaults-container.tsx (thin container)
    - apps/web-vite/src/hooks/use-rtl-chart-config.ts (RTL chart config)
    - .planning/phases/79-.../79-UI-SPEC.md (Saudization dashboard: focal point hero rate; neutral band badge; side-by-side headcount; manual-nature + quarterly callouts; empty/error copy; Component Inventory)
    - .planning/phases/79-.../79-CONTEXT.md (D-10 side-by-side; D-12 never assert band)
  </read_first>
  <action>
    Create apps/web-vite/src/components/saudization/:
    - hooks/use-saudization-dashboard.ts — read-only tRPC boundary: useQuery(trpc.gulf.saudization.dashboard.queryOptions()) → { isLoading, data }.
    - hooks/use-saudization-config.ts — mutation boundary: useMutation for upsertConfig (band/segment), upsertHeadcount, with onSuccess toast + invalidateQueries(dashboard.queryKey), onError toast. useTranslations.
    - saudization-dashboard-container.tsx — thin: call both hooks; render loading (skeleton), empty (UI-SPEC "Saudization not configured" copy), error (load-failure + Retry); else spread into the view.
    - saudization-dashboard.tsx — presentational: hero nationalisation-rate % (Display role, text-2xl, tabular-nums, text-primary = focal point); current band as a NEUTRAL badge with the locked label (NOT colorized — D-12/Pitfall 8); manual-nature callout (--warning) + quarterly-re-entry prompt (--warning, shown when quarterlyReentryDue); Qiwa-auth gap count (--warning); Iqama roll-up table (table-pattern); total/Saudi headcount with the platform-derived contractor counts shown SIDE-BY-SIDE and visually subordinate (--muted-foreground); band donut/progress via useRtlChartConfig (--chart-1 teal). Band/headcount edit via a dialog (DialogBody + DialogFooter, single teal CTA "Update Nitaqat band" / "Save headcount") calling the config hook. The band value comes from admin input only — NO computed band anywhere in the view.
    useTranslations for all copy; logical props only; WCAG states.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite exec tsc --noEmit 2>&1 | grep -i "saudization" | head; echo "tsc-done"; pnpm check:web-vite-data-layer 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTRPC\|useQuery\|useMutation" apps/web-vite/src/components/saudization/saudization-dashboard.tsx apps/web-vite/src/components/saudization/saudization-dashboard-container.tsx` == 0 (tRPC only in hooks).
    - `grep -c "useRtlChartConfig" apps/web-vite/src/components/saudization/saudization-dashboard.tsx` ≥ 1.
    - Band badge is neutral: `grep -ci "bg-destructive\|variant=\"destructive\"" apps/web-vite/src/components/saudization/saudization-dashboard.tsx` against the band badge == 0 (no authoritative colorization).
    - `grep -cE "\b(ml|mr|pl|pr)-" apps/web-vite/src/components/saudization/*.tsx` == 0.
    - check:web-vite-data-layer + presentational + dialog-pattern + table-pattern pass for these files.
  </acceptance_criteria>
  <done>Dashboard shows hero rate + neutral band + side-by-side headcount + Qiwa gap + Iqama roll-up; manual band entry; layered/RTL/states; GULF-05/06 UI complete.</done>
</task>

<task type="auto">
  <name>Task 2: Nitaqat drift-override dialog + offboarding trajectory banner (GULF-07/10)</name>
  <read_first>
    - apps/web-vite/src/components/settings/hooks/use-expiry-reminder-defaults.ts (mutation hook for the override)
    - .planning/phases/79-.../79-UI-SPEC.md (override badge "Custom — verify with adviser"; reset-override destructive confirmation; offboarding trajectory advisory copy + tone; --warning)
    - .planning/phases/79-.../79-CONTEXT.md (D-10 GULF-10 override + badge; D-12 trajectory advisory/non-authoritative/non-gating)
    - the existing offboarding open flow (packages/api/src/routers/workflow/workflow-shared.ts referenced; UI offboarding surface where the banner mounts — locate the offboarding detail/open component in web-vite)
  </read_first>
  <action>
    Create nitaqat-override-dialog.tsx — a dialog (DialogBody + DialogFooter) for applying a Nitaqat-threshold / permitted-activity override (CTA "Apply Nitaqat override" / "Apply activity override", teal). On success it calls trpc.gulf.saudization.applyNitaqatThresholdOverride / applyPermittedActivityOverride (via a hook — keep tRPC in the hook layer; reuse use-saudization-config or add a use-nitaqat-override hook). Render the "Custom — verify with adviser" --warning badge next to any overridden value. Reset-to-default uses an alert-dialog destructive confirmation (UI-SPEC copy "Reset override to default?"). All copy via useTranslations.
    Create offboarding-trajectory-banner.tsx — a presentational --warning alert rendered at offboarding-open for a Saudi-national contractor. It consumes trpc.gulf.saudization.offboardingTrajectory (via a hook) and renders the UI-SPEC advisory copy: "Offboarding this Saudi national may lower your projected Saudization. Current: {band} at {rate}% → projected: {projectedBand} at {projectedRate}%. Advisory only — verify in Qiwa. The system does not set your band." It is non-gating, ephemeral, non-authoritative — NO confirm/block action. Mount it in the existing offboarding-open surface (minimal diff; gate on isSaudi). Logical props only.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web-vite exec tsc --noEmit 2>&1 | grep -iE "override|trajectory" | head; echo "tsc-done"</automated>
  </verify>
  <acceptance_criteria>
    - override dialog: `grep -c "applyNitaqatThresholdOverride\|applyPermittedActivityOverride" apps/web-vite/src/components/saudization/*.ts apps/web-vite/src/components/saudization/*.tsx` ≥ 1 (in the hook layer); "Custom — verify with adviser" badge keyed via i18n.
    - trajectory banner: `grep -c "offboardingTrajectory" apps/web-vite/src/components/saudization/*.ts apps/web-vite/src/components/saudization/*.tsx` ≥ 1; no confirm/block/gate action present (`grep -ci "block\|prevent\|disabled.*offboard" apps/web-vite/src/components/saudization/offboarding-trajectory-banner.tsx` against gating == 0).
    - banner uses --warning alert, advisory tone copy via i18n (no hardcoded string).
    - `grep -cE "\b(ml|mr|pl|pr)-" apps/web-vite/src/components/saudization/nitaqat-override-dialog.tsx apps/web-vite/src/components/saudization/offboarding-trajectory-banner.tsx` == 0.
  </acceptance_criteria>
  <done>Override dialog with "Custom" badge + reset confirmation; advisory non-gating offboarding trajectory banner; GULF-07/10 UI complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard view → user | Band is informational/manual; never asserted as a computed judgement. |
| override dialog → gulf router | Audit-logged server-side (Plan 05); UI surfaces the custom badge. |
| trajectory banner → user | Ephemeral, advisory, non-gating. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-07-01 | Repudiation | UI colorizing band as authoritative | mitigate | Neutral badge only (D-12); Task 1 acceptance asserts no destructive colorization of the band. |
| T-79-07-02 | Repudiation | trajectory presented as a set band | mitigate | Advisory copy + non-gating banner; no confirm action (Task 2). |
| T-79-07-03 | Information Disclosure | rate misread from platform-only counts | mitigate | Manual numbers authoritative; platform-derived shown subordinate (--muted-foreground). |
| T-79-07-04 | Information Disclosure | RTL break via ml-/mr- | mitigate | Logical props + check:rtl-logical-props gate. |
| T-79-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (in-tree shadcn + recharts only). slopcheck + blocking checkpoint if any added. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/web-vite exec tsc --noEmit` clean for saudization files.
- `pnpm check:web-vite-data-layer && pnpm check:web-vite-presentational && pnpm check:web-vite-dialog-pattern && pnpm check:web-vite-table-pattern` pass.
- `pnpm check:rtl-logical-props` passes for the saudization surface.
</verification>

<success_criteria>
- Saudization dashboard (hero rate, neutral band, side-by-side headcount, Qiwa gap, Iqama roll-up), manual band entry, drift-override dialog with custom badge, and advisory offboarding trajectory banner — all layered, RTL-clean, with full states. GULF-05/06/07/10 UI complete.
</success_criteria>

<output>
Create `.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-07-SUMMARY.md` when done.
</output>
