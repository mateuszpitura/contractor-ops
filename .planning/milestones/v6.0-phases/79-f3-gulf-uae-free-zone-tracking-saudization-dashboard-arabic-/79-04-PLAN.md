---
phase: 79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-
plan: 04
type: execute
wave: 2
depends_on: ["79-02"]
files_modified:
  - packages/api/src/services/permitted-activity-check.ts
  - packages/api/src/services/saudization-dashboard.ts
  - packages/api/src/__tests__/permitted-activity-noc.test.ts
  - packages/api/src/__tests__/saudization-derivation.test.ts
autonomous: true
requirements: [GULF-03, GULF-05, GULF-06, GULF-07]
must_haves:
  truths:
    - "A contract whose activityIsicCodes have zero overlap with the contractor's permitted set (ISIC-code overlap match — D-06) fires a non-blocking advisory + creates a WARNING NOC ContractorComplianceItem (C5, D-07)"
    - "A contract with empty activityIsicCodes skips the check entirely — no false advisory (C5, D-08)"
    - "Saudization nationalisation rate is computed from manual SaudiHeadcount numbers only — never from platform contractors (C6, D-10)"
    - "The Nitaqat band is never auto-computed; the dashboard surfaces last-updated + quarterly-prompt state (C6, D-05/anti-feature)"
    - "Offboarding band-trajectory is a live ephemeral recompute (SaudiHeadcount minus 1), advisory, not stored, not gating (C7, D-12)"
  artifacts:
    - path: "packages/api/src/services/permitted-activity-check.ts"
      provides: "ISIC-overlap check + auto-NOC item creation; skip-on-uncoded"
      min_lines: 40
    - path: "packages/api/src/services/saudization-dashboard.ts"
      provides: "rate-from-manual derivation + Qiwa gap count + Iqama roll-up + ephemeral trajectory"
      min_lines: 50
  key_links:
    - from: "packages/api/src/services/permitted-activity-check.ts"
      to: "ContractorComplianceItem (WARNING NOC)"
      via: "create on ISIC-overlap miss"
      pattern: "NOC|severity: 'WARNING'"
    - from: "packages/api/src/services/saudization-dashboard.ts"
      to: "SaudiHeadcount manual numbers"
      via: "rate = saudiHeadcount / totalHeadcount"
      pattern: "saudiHeadcount|totalHeadcount"
---

<objective>
Wave 2 (backend, parallel with Plan 03): the permitted-activity ISIC scope check (GULF-03) and the Saudization dashboard derivation + offboarding trajectory (GULF-05/06/07). Pure deterministic logic mirroring `computeComplianceHealth`; no engine changes.

Purpose: Turns C5/C6/C7 GREEN. The scope check is deterministic code-overlap with skip-on-uncoded (no fuzzy matching, no tristate — D-06/D-08). The dashboard rate comes from MANUAL headcount (Pitfall 7); the band is NEVER auto-computed (Pitfall 8); the trajectory is advisory-only (D-12).
Output: `permitted-activity-check.ts`, `saudization-dashboard.ts`.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-RESEARCH.md
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-PATTERNS.md
@.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-CONTEXT.md

<interfaces>
<!-- Extracted from the live tree. -->

computeComplianceHealth (packages/api/src/routers/core/contractor.ts:56-125) — pure derivation: typed params object → structured result with HealthFactor[]; the pattern saudization-dashboard.ts mirrors.

ContractorComplianceItem create shape (materialiseFromPolicy compliance-supersession.ts:84-99) — documentType, name, severity, policyRuleId, expiryJurisdictionTz, status. NOC item: severity 'WARNING', documentType a NOC_* DocumentType value, contractId set to the affected engagement, status 'MISSING' (default, no expiry).

Contract.activityIsicCodes String[] (added in Plan 02) — empty array == uncoded == skip (D-08).
FreeZoneAssignment.permittedActivityIsicCodes String[] (Plan 02) — the contractor's permitted set.
SaudiHeadcount { totalHeadcount Int, saudiHeadcount Int } (Plan 02) — manual numbers; rate = saudiHeadcount/totalHeadcount.
ContractorAssignment.isSaudi/qiwaContractAuthenticated (Plan 02) — Qiwa gap = count WHERE qiwaContractAuthenticated=false (D-11, visibility-only).
ksa.iqama ContractorComplianceItem — Iqama expiry roll-up reuses existing F1 expiry data (D — reuse, do not re-derive).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Permitted-activity ISIC scope check + auto-NOC (C5, GULF-03)</name>
  <behavior>
    - Contract activityIsicCodes with zero overlap against the contractor's FreeZoneAssignment.permittedActivityIsicCodes → returns `{ mismatch: true }` and creates a WARNING NOC ContractorComplianceItem scoped to that contractId. Contract creation still proceeds (non-blocking, D-07).
    - Contract with non-empty activityIsicCodes that DO overlap → `{ mismatch: false }`, no NOC item.
    - Contract with empty activityIsicCodes (uncoded) → `{ skipped: true }`, no advisory, no item (D-08).
    - Mismatch is detected by set-overlap only — no fuzzy text matching (D-06).
  </behavior>
  <read_first>
    - packages/api/src/services/compliance-supersession.ts (materialiseFromPolicy 84-99 — the item-create shape for the NOC row)
    - packages/api/src/services/audit-writer.ts (writeAuditLog — NOC auto-creation is a system-side write; log it if D-17 classifies it sensitive; otherwise a Pino info is sufficient)
    - packages/db/prisma/schema/contractor.prisma (DocumentType enum — confirm/extend with a NOC value; if absent add NOC_NO_OBJECTION_CERTIFICATE UPPER_SNAKE — coordinate with Plan 02 schema if a migration is needed, else use an existing generic doc type and note it)
    - .planning/phases/79-.../79-PATTERNS.md (permitted-activity-check.ts section + Pitfall 6 uncoded-skip)
    - .planning/phases/79-.../79-CONTEXT.md (D-05..D-08)
  </read_first>
  <action>
    Create `packages/api/src/services/permitted-activity-check.ts` exporting `checkPermittedActivityScope(client, ctx)` taking the contractor's `permittedActivityIsicCodes`, the contract's `activityIsicCodes`, and the contractId. Logic:
    - If `contract.activityIsicCodes.length === 0` → return `{ skipped: true }` (D-08, no advisory).
    - Compute overlap = intersection(contractCodes, permittedCodes). If overlap is non-empty → `{ mismatch: false }`.
    - If overlap is empty → create a ContractorComplianceItem(severity `WARNING`, documentType the NOC value, contractId, status `MISSING`) for the engagement and return `{ mismatch: true, nocItemCreated: true }`. Pino-log the advisory; the banner copy lives in i18n (Plan 08). NOC severity stays WARNING per D-07 (surfaced, not payment-blocking) — no regulatory source justified escalation.
    This service is invoked from the contract-create path (wired in Plan 05). Keep it pure-ish: accept a structural client so it composes in the contract-create transaction.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- permitted-activity-noc 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "skipped: true\|length === 0" packages/api/src/services/permitted-activity-check.ts` ≥ 1 (uncoded skip, D-08).
    - `grep -c "WARNING" packages/api/src/services/permitted-activity-check.ts` ≥ 1 (NOC severity, D-07).
    - No fuzzy/text-match library imported (set-overlap only, D-06).
    - permitted-activity-noc test GREEN (mismatch creates NOC; overlap creates none; uncoded skips).
  </acceptance_criteria>
  <done>Scope check fires advisory + auto-NOC on ISIC-overlap miss, skips uncoded; C5 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Saudization dashboard derivation + offboarding trajectory (C6, C7, GULF-05/06/07)</name>
  <behavior>
    - computeSaudizationDashboard returns nationalisationRate computed from MANUAL SaudiHeadcount (saudiHeadcount/totalHeadcount), the manual numbers, the current band (read, never computed), bandLastUpdatedAt, a quarterly-re-entry-due flag, the Qiwa-auth gap count (contracts WHERE qiwaContractAuthenticated=false), an Iqama roll-up (reusing ksa.iqama ContractorComplianceItem expiry data), and a platform-derived contractor breakdown returned side-by-side (subordinate).
    - The rate is NEVER derived from platform contractors alone (Pitfall 7) — a test asserts that platform-only counts do not drive the rate.
    - The band is NEVER auto-computed (Pitfall 8) — no code path sets/returns a computed band.
    - projectOffboardingTrajectory(headcount, band) returns an ephemeral { currentRate, projectedRate, advisory: true } from SaudiHeadcount minus 1 Saudi national; it does NOT persist anything and returns advisory wording flags (D-12); never asserts a band authoritatively.
  </behavior>
  <read_first>
    - packages/api/src/routers/core/contractor.ts (computeComplianceHealth 56-125 — the derivation-function pattern to mirror)
    - packages/db/prisma/schema/contractor.prisma (ContractorComplianceItem — ksa.iqama items for the Iqama roll-up; ContractorAssignment.qiwaContractAuthenticated)
    - .planning/phases/79-.../79-PATTERNS.md (saudization-dashboard.ts section + Pitfalls 7/8)
    - .planning/phases/79-.../79-CONTEXT.md (D-10/D-11/D-12)
  </read_first>
  <action>
    Create `packages/api/src/services/saudization-dashboard.ts` exporting:
    - `computeSaudizationDashboard(params)` — pure derivation mirroring computeComplianceHealth. `nationalisationRate = totalHeadcount > 0 ? saudiHeadcount / totalHeadcount : null` (from MANUAL numbers — D-10/Pitfall 7). Return `band` (read-through, never computed — Pitfall 8), `bandLastUpdatedAt`, `quarterlyReentryDue` (true if bandLastUpdatedAt older than ~90 days), `qiwaGapCount` (count of qiwaContractAuthenticated=false — D-11), `iqamaRollup` (counts bucketed from ksa.iqama compliance items' expiresAt — reuse F1 expiry data), and `platformDerived` (contractor-only counts, returned side-by-side and clearly subordinate). The function MUST NOT compute or infer a band.
    - `projectOffboardingTrajectory(params)` — live ephemeral recompute (D-12): given current SaudiHeadcount and the offboarded contractor's isSaudi, return `{ currentRate, projectedRate, currentBand, advisory: true, authoritative: false }` projecting saudiHeadcount-1 (only if the contractor isSaudi). It returns advisory flags only — never sets a band, never persists, never gates. The projected band label, if shown, is non-authoritative copy (Plan 07 renders the wording).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test -- saudization-derivation 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "saudiHeadcount\|totalHeadcount" packages/api/src/services/saudization-dashboard.ts` ≥ 1 (rate from manual numbers).
    - No code path returns a computed band: `grep -ci "computeBand\|deriveBand\|inferBand\|autoBand" packages/api/src/services/saudization-dashboard.ts` == 0.
    - `grep -c "authoritative: false\|advisory" packages/api/src/services/saudization-dashboard.ts` ≥ 1 (trajectory is advisory, D-12).
    - saudization-derivation test GREEN (rate-source, never-auto-band, ephemeral-trajectory cases).
  </acceptance_criteria>
  <done>Dashboard derivation computes rate from manual headcount, never the band; trajectory is ephemeral/advisory; C6/C7 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract-create → NOC item | System-side advisory write; non-gating. |
| manual headcount → rate | Admin-asserted numbers drive the rate; never computed from platform alone. |
| trajectory query → UI | Ephemeral, non-persisted, non-authoritative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-04-01 | Repudiation | band asserted as authoritative by trajectory | mitigate | projectOffboardingTrajectory returns authoritative:false + advisory flags; C7 test. |
| T-79-04-02 | Tampering | rate understated by deriving from platform contractors | mitigate | rate computed from manual SaudiHeadcount only; C6 test (Pitfall 7). |
| T-79-04-03 | Denial of Service | over-flagging scope mismatch on uncoded contracts | mitigate | skip-on-uncoded (D-08); C5 uncoded test. |
| T-79-04-04 | Spoofing | client-supplied isSaudi/qiwa flags | accept | Admin-asserted, advisory/non-gating (D-11); server Zod-validated at the router (Plan 05); low blast radius. |
| T-79-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. slopcheck + blocking checkpoint if any added. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test -- permitted-activity-noc saudization-derivation` — C5/C6/C7 GREEN.
- `pnpm lint:silent-catch` clean for the two new service files; no `console.*`.
</verification>

<success_criteria>
- Deterministic ISIC scope check with auto-NOC + uncoded-skip; dashboard rate from manual headcount; band never computed; trajectory advisory-only. C5/C6/C7 GREEN.
</success_criteria>

<output>
Create `.planning/phases/79-f3-gulf-uae-free-zone-tracking-saudization-dashboard-arabic-/79-04-SUMMARY.md` when done.
</output>
