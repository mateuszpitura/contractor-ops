---
phase: 73
plan: 08
type: execute
wave: 3
depends_on: [73-01, 73-02, 73-03, 73-04, 73-05, 73-07]
files_modified:
  - packages/api/src/routers/compliance/classification.ts
  - packages/api/src/__tests__/compliance-upload-review.test.ts
  - packages/api/src/services/notification-service.ts
  - packages/feature-flags/src/signoff-registry-flags.json
  - packages/feature-flags/src/__tests__/compliance-portal-self-service-entry.test.ts
  - apps/web-vite/src/components/contractors/compliance/override-compliance-item-dialog.tsx
  - apps/web-vite/src/components/contractors/compliance/override-compliance-item-dialog-container.tsx
  - apps/web-vite/src/components/contractors/compliance/override-compliance-item-button.tsx
  - apps/web-vite/src/components/contractors/compliance/compliance-item-history.tsx
  - apps/web-vite/src/components/contractors/compliance/upload-review-dialog.tsx
  - apps/web-vite/src/components/contractors/compliance/upload-review-dialog-container.tsx
  - apps/web-vite/src/components/contractors/compliance/hooks/use-override-compliance-item.ts
  - apps/web-vite/src/components/contractors/compliance/hooks/use-compliance-item-history.ts
  - apps/web-vite/src/components/contractors/compliance/hooks/use-upload-review.ts
  - apps/web-vite/src/components/contractors/contractor-profile/tab-compliance.tsx
  - apps/web-vite/src/components/contractors/compliance/__tests__/override-compliance-item-dialog.test.tsx
  - apps/web-vite/src/components/contractors/compliance/__tests__/compliance-item-history.test.tsx
  - apps/web-vite/messages/en.json
  - apps/web-vite/messages/de.json
  - apps/web-vite/messages/pl.json
  - apps/web-vite/messages/ar.json
autonomous: true
requirements: [COMPL-01, COMPL-04, COMPL-11]
requirements_addressed: [COMPL-01, COMPL-04, COMPL-11]

must_haves:
  truths:
    - "BACKEND D-08: `packages/api/src/routers/compliance/classification.ts` exports `approveUploadReplacement` AND `rejectUploadReplacement` admin mutations, both using `tenantProcedure` + `requirePermission({ compliance: ['override'] })`"
    - "BACKEND: approve mutation (single $transaction): flips `ContractorComplianceItem.status='SATISFIED'`, sets `satisfiedByDocumentId`, sets `expiresAt` from the admin-confirmed input; flips `Document.status='ACTIVE'`; writes `AuditLog action='compliance.upload.approved'` (metadata.itemId + metadata.documentId); dispatches notification `compliance.upload.approved` to the contractor"
    - "BACKEND: reject mutation (single $transaction): closed-enum `reasonCategory ∈ {wrong_document_type, illegible, already_expired, forged_or_altered, other}` + optional free-text; flips `Document.status='ARCHIVED'`; KEEPS `ContractorComplianceItem.status` MISSING/EXPIRED; writes `AuditLog action='compliance.upload.rejected'` with reasonCategory + freeText; dispatches notification `compliance.upload.rejected` to the contractor with a re-upload deep link. `Document.rejectionReason` schema column DEFERRED — captured via audit log only (Researcher pin)"
    - "BACKEND: `packages/api/src/services/notification-service.ts` notification-type registry includes `compliance.upload.rejected` AND `compliance.upload.approved`"
    - "BACKEND: `packages/feature-flags/src/signoff-registry-flags.json` contains a new `compliance-portal-self-service` PENDING entry (with notes referencing F1 portal self-service) — validated by the Phase 70 signoff-registry-flags-schema"
    - "UI D-12: ONE shared override modal in two places. `override-compliance-item-dialog.tsx` (view) + `-container.tsx` + `hooks/use-override-compliance-item.ts` mirror the Phase 71 `recompute-compliance-*` triplet; the hook calls `trpc.classification.overrideItem` (Plan 73-03). `override-compliance-item-button.tsx` renders only when `usePermissions().can('compliance', ['override'])` AND item.severity='BLOCKING' AND item.status in (MISSING, EXPIRED); it is mounted (a) inline per row on the Compliance tab and (b) via the `renderRowActions` slot Plan 73-06 left on the dashboard at-risk + blocked-payments tables"
    - "UI: override dialog uses `Select` (6-enum WaivedReasonCategory) + `Textarea` (reasonNote >=20 chars), submit disabled until both valid; uses `DialogBody`/`DialogFooter` (web-vite dialog convention); on success toasts + invalidates the compliance query"
    - "UI D-13: `compliance-item-history.tsx` + `hooks/use-compliance-item-history.ts` render an audit-log timeline from `trpc.classification.itemAuditTrail` (Plan 73-03) as a Disclosure/accordion per row; `tab-compliance.tsx` is modified to render, per item row: the OverrideComplianceItemButton + the History disclosure + a WAIVED badge with category icon + tooltip"
    - "UI D-07/D-08: `upload-review-dialog.tsx` + `-container.tsx` + `hooks/use-upload-review.ts` give admins the approve/reject UI for a PENDING_REVIEW Document linked to a compliance item (approve = confirm/adjust expiresAt then call approve mutation; reject = closed-enum reason + free-text then call reject mutation)"
    - "All Wave 0 RED scaffolds in `override-compliance-item-dialog.test.tsx` flip to GREEN; new `compliance-item-history.test.tsx` GREEN (>=3 cases); `compliance-upload-review.test.ts` flips to GREEN; `compliance-portal-self-service-entry.test.ts` GREEN (asserts the PENDING flag entry exists)"
    - "New `Compliance.override.*` + `Compliance.uploadReview.*` + `Compliance.history.*` message namespaces added to `apps/web-vite/messages/{en,de,pl,ar}.json` at full 4-locale parity (ar required by i18n:parity)"
    - "`pnpm --filter @contractor-ops/api test --run --testNamePattern='compliance-upload-review'` exits 0; `pnpm --filter @contractor-ops/feature-flags test --run --testNamePattern='compliance-portal-self-service-entry'` exits 0; `pnpm --filter @contractor-ops/web-vite test --run --testNamePattern='override-compliance-item-dialog|compliance-item-history'` exits 0; `pnpm --filter @contractor-ops/web-vite check:data-layer` + `check:page-shells` + `pnpm check:web-vite-presentational` + `pnpm check:web-vite-dialog-pattern` + `pnpm i18n:parity` + `pnpm typecheck` all exit 0"
  artifacts:
    - path: "packages/api/src/routers/compliance/classification.ts"
      provides: "approveUploadReplacement + rejectUploadReplacement admin mutations + notification dispatch"
      contains: "approveUploadReplacement"
    - path: "packages/feature-flags/src/signoff-registry-flags.json"
      provides: "compliance-portal-self-service PENDING flag entry"
      contains: "compliance-portal-self-service"
    - path: "apps/web-vite/src/components/contractors/compliance/override-compliance-item-dialog.tsx"
      provides: "Manual override modal view (Select category + Textarea rationale, DialogBody/DialogFooter)"
      contains: "OverrideComplianceItemDialogView"
    - path: "apps/web-vite/src/components/contractors/compliance/compliance-item-history.tsx"
      provides: "Audit-log driven timeline disclosure on the Compliance tab"
      contains: "ComplianceItemHistory"
    - path: "apps/web-vite/src/components/contractors/compliance/upload-review-dialog.tsx"
      provides: "Admin approve/reject UI for a PENDING_REVIEW upload"
      contains: "UploadReviewDialog"
  key_links:
    - from: "apps/web-vite/src/components/contractors/compliance/override-compliance-item-dialog.tsx"
      to: "apps/web-vite/src/components/contractors/compliance/recompute-compliance-dialog.tsx"
      via: "Phase 71 D-15 modal twin (web-vite View + Container + hook triplet)"
      pattern: "AlertDialog/Dialog + Select + mutation hook + sonner toast"
    - from: "apps/web-vite/src/components/contractors/compliance/hooks/use-override-compliance-item.ts"
      to: "packages/api/src/routers/compliance/classification.ts"
      via: "Plan 73-03 overrideItem mutation"
      pattern: "useMutation(trpc.classification.overrideItem.mutationOptions(...)) + invalidate"
    - from: "apps/web-vite/src/components/contractors/compliance/hooks/use-compliance-item-history.ts"
      to: "packages/api/src/routers/compliance/classification.ts"
      via: "Plan 73-03 itemAuditTrail query"
      pattern: "useQuery + per-action label resolution via useTranslations"
    - from: "packages/feature-flags/src/signoff-registry-flags.json"
      to: "packages/feature-flags/src/signoff-registry-flags-schema.ts"
      via: "Phase 70 D-09 PENDING signoff registry schema"
      pattern: "{ \"<flag>\": { \"status\": \"PENDING\", \"notes\": \"...\" } }"
---

<objective>
Complete the Compliance tab UX + the admin upload-review flow + the override surface. Split into a BACKEND slice (tasks 73-08-01..04: approve/reject admin mutations + notification types + the `compliance-portal-self-service` PENDING flag + RED-to-GREEN API/flag tests) and a UI slice (tasks 73-08-05..11: the shared override modal triplet, the override button, the audit-log history timeline, the admin upload-review dialog, the `tab-compliance.tsx` wiring + WAIVED badge, and i18n).

This plan closes D-08 (admin reject with structured reason + contractor notification + re-upload deep link), D-12 (one override modal in two places — Compliance tab inline + dashboard rows via the slot Plan 73-06 exposed), and D-13 (inline audit-log timeline + WAIVED badge). All UI in `apps/web-vite` (Page->Container->Hook->Component, DialogBody/DialogFooter, custom i18n with ar parity), mandatory loading/empty/error states + WCAG. The override mutation itself + audit-trail query are delivered by Plan 73-03; this plan adds the approve/reject mutations + the UI that consumes all of them.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-f1-compliance-admin-dashboard-portal-self-service-i18n/73-CONTEXT.md
@.planning/phases/73-f1-compliance-admin-dashboard-portal-self-service-i18n/73-RESEARCH.md
@apps/web-vite/ARCHITECTURE.md
@packages/api/src/routers/compliance/classification.ts
@packages/api/src/services/notification-service.ts
@packages/api/src/services/audit-writer.ts
@packages/feature-flags/src/signoff-registry-flags.json
@packages/feature-flags/src/signoff-registry-flags-schema.ts
@apps/web-vite/src/components/contractors/compliance/recompute-compliance-dialog.tsx
@apps/web-vite/src/components/contractors/compliance/recompute-compliance-dialog-container.tsx
@apps/web-vite/src/components/contractors/compliance/recompute-compliance-button.tsx
@apps/web-vite/src/components/contractors/hooks/use-recompute-compliance.ts
@apps/web-vite/src/components/contractors/contractor-profile/tab-compliance.tsx
@apps/web-vite/src/hooks/use-permissions.ts
@apps/web-vite/src/i18n/useTranslations.ts
</context>

<deep_work_rules_note>
Follow the `frontend-design` skill before UI work. Use `semble search` to find the exact notification-type registry shape in `notification-service.ts`, the `dispatch()` signature, and the `recompute-compliance` triplet split before mirroring it for override + upload-review. Confirm `DialogBody`/`DialogFooter` are imported from `@contractor-ops/ui/components/shadcn/dialog` (check:web-vite-dialog-pattern enforces content-in-DialogBody + actions-in-DialogFooter).
</deep_work_rules_note>

<threat_model>
| ID | Threat | Mitigation |
|----|--------|-----------|
| T-73-08-01 | Privilege escalation — a non-admin approves/rejects a compliance upload or overrides an item | approve/reject/override all chain `requirePermission({ compliance: ['override'] })`; the UI button is gated on `can('compliance', ['override'])` but the server is the source of truth. tenantProcedure auto-scopes by org |
| T-73-08-02 | IDOR — admin approves an upload for a contractor in another org | tenantProcedure (Prisma tenant extension) scopes the item + document to the session org; the mutation loads the item by id within the tenant client and 404s cross-org |
| T-73-08-03 | Audit gap — a status flip lands without an AuditLog row (forensics hole) | Both approve and reject write the AuditLog inside the SAME `prisma.$transaction` as the status flip (tx-joined writeAuditLog), so a failed audit rolls back the flip |
| T-73-08-04 | Notification dispatch failure aborts the approve/reject mutation | `dispatch()` for the contractor notification runs best-effort outside the critical write (or is try/caught) so a transient notification failure does not roll back an approved/rejected document; mirror the Phase 72 dispatch pattern |
| T-73-08-05 | XSS via admin free-text reject reason rendered in the contractor notification | The free-text is stored + rendered as plain text (React escapes); the notification template interpolates via i18next params, not raw HTML |
| T-73-08-06 | Override modal shipped in two places drifts out of sync | ONE view component (`override-compliance-item-dialog.tsx`) + ONE container + ONE hook; both mount points import the same triplet. Dashboard mounts via the `renderRowActions` slot Plan 73-06 exposed |
| T-73-08-07 | Dialog-pattern lint fails (actions not in DialogFooter / content not in DialogBody) | Both new dialogs put scrollable content in `DialogBody` and actions in `DialogFooter`; `pnpm check:web-vite-dialog-pattern` verifies |
| T-73-08-08 | Flag entry lands APPROVED instead of PENDING — bypasses Phase 70 D-09 sign-off gate | The `compliance-portal-self-service` entry lands `status: PENDING`; the signoff-registry-flags-schema rejects unknown statuses; the entry test asserts PENDING |
| T-73-08-09 | i18n:parity fails (missing ar keys) | All new namespaces added to en/de/pl/ar; `pnpm i18n:parity` in verification |
</threat_model>

<tasks>

<task id="73-08-01">
<action>
BACKEND. In `packages/api/src/routers/compliance/classification.ts` add `approveUploadReplacement` and `rejectUploadReplacement` admin mutations, both `tenantProcedure.use(requirePermission({ compliance: ['override'] }))`. Approve input `{ itemId, documentId, expiresAt }` (admin-confirmed expiry); in one `$transaction`: flip `ContractorComplianceItem.status='SATISFIED'`, set `satisfiedByDocumentId=documentId`, set `expiresAt`; flip `Document.status='ACTIVE'`; `writeAuditLog({ action: 'compliance.upload.approved', resourceType: 'CONTRACTOR', resourceId: item.contractorId, metadata: { itemId, documentId } }, tx)`. Reject input `{ itemId, documentId, reasonCategory: z.enum([...5]), freeText: z.string().max(1000).optional() }`; in one `$transaction`: flip `Document.status='ARCHIVED'`; KEEP item status; `writeAuditLog({ action: 'compliance.upload.rejected', ..., metadata: { itemId, documentId, reasonCategory, freeText } }, tx)`. After each tx, dispatch the contractor notification (best-effort) per task 73-08-02. Mirror the transactional + audit shape of `recreateComplianceAssessment` in the same file.
</action>
<read_first>
- `packages/api/src/routers/compliance/classification.ts` — recreateComplianceAssessment transactional + audit + permission pattern; confirm router export
- `packages/api/src/services/audit-writer.ts` — writeAuditLog(args, tx?)
- `packages/db/prisma/schema/contract.prisma` — DocumentStatus values (ACTIVE/PENDING_REVIEW/ARCHIVED)
</read_first>
<acceptance_criteria>
- `approveUploadReplacement` + `rejectUploadReplacement` exist with `requirePermission({ compliance: ['override'] })`
- approve flips item SATISFIED + sets satisfiedByDocumentId + expiresAt + Document ACTIVE + audit `compliance.upload.approved`
- reject flips Document ARCHIVED, keeps item status, audit `compliance.upload.rejected` with reasonCategory + freeText
- `pnpm --filter @contractor-ops/api typecheck` exits 0
</acceptance_criteria>
</task>

<task id="73-08-02">
<action>
BACKEND. In `packages/api/src/services/notification-service.ts` add `compliance.upload.approved` and `compliance.upload.rejected` to the notification-type registry/union (the same place the existing compliance / classification notification types are declared). Wire the approve/reject mutations (task 73-08-01) to `dispatch()` the contractor recipient (the contractor's `userId`), with the reject notification carrying a re-upload deep link to `/portal/compliance/upload-replacement?itemId=...&policyRuleId=...` (re-fires the Plan 73-07 portal flow). Best-effort dispatch — do not let a notification failure roll back the mutation.
</action>
<read_first>
- `packages/api/src/services/notification-service.ts` — notification-type union/registry + dispatch() signature + recipient resolution
- `packages/api/src/services/compliance-reminder-scan.ts` — Phase 72 dispatch usage to a contractor recipient (pattern)
</read_first>
<acceptance_criteria>
- notification-type registry includes `compliance.upload.rejected` AND `compliance.upload.approved`
- reject notification includes a re-upload deep link to the portal upload-replacement route
- dispatch is best-effort (try/catch or post-tx), not inside the critical write
</acceptance_criteria>
</task>

<task id="73-08-03">
<action>
BACKEND. Add the `compliance-portal-self-service` PENDING entry to `packages/feature-flags/src/signoff-registry-flags.json` (status `PENDING`, notes referencing F1 portal self-service). Create `packages/feature-flags/src/__tests__/compliance-portal-self-service-entry.test.ts` asserting the entry exists with status PENDING and validates against the signoff-registry-flags-schema. Confirm the flag is also resolvable via the `@contractor-ops/feature-flags` registry barrel (`useFlag('compliance-portal-self-service')` works in web-vite).
</action>
<read_first>
- `packages/feature-flags/src/signoff-registry-flags.json` — existing PENDING entry shape
- `packages/feature-flags/src/signoff-registry-flags-schema.ts` — schema the entry must satisfy
- `packages/feature-flags/src/registry.ts` — flag-key registry (add the key if registry is the source of truth)
</read_first>
<acceptance_criteria>
- `compliance-portal-self-service` PENDING entry exists in signoff-registry-flags.json
- `pnpm --filter @contractor-ops/feature-flags test --run --testNamePattern='compliance-portal-self-service-entry'` exits 0
</acceptance_criteria>
</task>

<task id="73-08-04">
<action>
BACKEND. Flip the Wave 0 RED scaffold `packages/api/src/__tests__/compliance-upload-review.test.ts` to GREEN: approve flips item SATISFIED + satisfiedByDocumentId + expiresAt + Document ACTIVE + audit `compliance.upload.approved`; permission gate rejects non-override callers FORBIDDEN; reject flips Document ARCHIVED, keeps item status, audit `compliance.upload.rejected` with reasonCategory + freeText + dispatches the contractor notification; reasonCategory enum validation. Evolve the scaffold; reuse the Plan 73-03 router test harness.
</action>
<read_first>
- `packages/api/src/__tests__/compliance-upload-review.test.ts` — Wave 0 RED scaffold
- `packages/api/src/__tests__/compliance-override-mutation.test.ts` — Plan 73-03 router test harness + writeAuditLog spy
</read_first>
<acceptance_criteria>
- `pnpm --filter @contractor-ops/api test --run --testNamePattern='compliance-upload-review'` exits 0 with at least 9 passing test cases
</acceptance_criteria>
</task>

<task id="73-08-05">
<action>
UI. Build the shared override modal as a triplet mirroring the Phase 71 `recompute-compliance-*` files: `override-compliance-item-dialog.tsx` (exports `OverrideComplianceItemDialogView` — `Dialog` with content in `DialogBody`, a `Select` of the 6 `WaivedReasonCategory` values + a `Textarea` for reasonNote, submit in `DialogFooter` disabled until reasonCategory chosen AND reasonNote >= 20 chars), `override-compliance-item-dialog-container.tsx` (wires the hook), and `hooks/use-override-compliance-item.ts` (`useMutation(trpc.classification.overrideItem.mutationOptions(...))` -> on success toast + `queryClient.invalidateQueries(trpc.classification.pathFilter())`, on error toast). Use `useTranslations('Compliance.override')`.
</action>
<read_first>
- `apps/web-vite/src/components/contractors/compliance/recompute-compliance-dialog.tsx` — View shape (Select + AlertDialog/Dialog)
- `apps/web-vite/src/components/contractors/compliance/recompute-compliance-dialog-container.tsx` — container wiring
- `apps/web-vite/src/components/contractors/hooks/use-recompute-compliance.ts` — mutation hook + invalidate + toast pattern
- `@contractor-ops/ui` Dialog/DialogBody/DialogFooter — dialog convention
</read_first>
<acceptance_criteria>
- The triplet exists; `OverrideComplianceItemDialogView` puts content in `DialogBody` + actions in `DialogFooter`
- Submit disabled until reasonCategory chosen AND reasonNote >= 20 chars; renders all 6 WaivedReasonCategory options
- hook calls `trpc.classification.overrideItem`; `pnpm check:web-vite-dialog-pattern` + `check:web-vite-presentational` exit 0
</acceptance_criteria>
</task>

<task id="73-08-06">
<action>
UI. Create `override-compliance-item-button.tsx` exporting `OverrideComplianceItemButton({ itemId, contractorId, severity, status })`. Render the button (opens the dialog container) ONLY when `usePermissions().can('compliance', ['override'])` AND `severity === 'BLOCKING'` AND `status in ('MISSING','EXPIRED')`; otherwise null. Mirror `recompute-compliance-button.tsx` (button + local open state + dialog container). This is the SINGLE component mounted on both the Compliance tab (task 73-08-09) and the dashboard tables (via the `renderRowActions` slot Plan 73-06 exposed).
</action>
<read_first>
- `apps/web-vite/src/components/contractors/compliance/recompute-compliance-button.tsx` — button + dialog-launcher pattern
- `apps/web-vite/src/hooks/use-permissions.ts` — can(resource, actions)
</read_first>
<acceptance_criteria>
- `OverrideComplianceItemButton` renders only under the permission + severity + status gate, else null
- Opens `OverrideComplianceItemDialogContainer`
- Keyboard-focusable button with aria-label
</acceptance_criteria>
</task>

<task id="73-08-07">
<action>
UI. Create `compliance-item-history.tsx` exporting `ComplianceItemHistory({ itemId })` + `hooks/use-compliance-item-history.ts` (only tRPC boundary — `trpc.classification.itemAuditTrail`, Plan 73-03). Render a `Collapsible`/Disclosure per row that, when expanded, shows a chronological timeline of audit-log entries (created / satisfied / expired / overridden / recomputed / upload submitted / approved / rejected) with translated action labels + actor + timestamp. Loading/empty/error states inside the disclosure. Read-only.
</action>
<read_first>
- `packages/api/src/routers/compliance/classification.ts` — itemAuditTrail query shape (Plan 73-03)
- `apps/web-vite/src/components/portal/status-timeline.tsx` — sibling timeline visual idiom
- `apps/web-vite/src/i18n/typed-keys.ts` — tDynLoose for dynamic action-label keys
</read_first>
<acceptance_criteria>
- `ComplianceItemHistory` renders a disclosure timeline from `itemAuditTrail`; tRPC confined to `use-compliance-item-history.ts`
- Loading/empty/error variants present inside the disclosure
- `pnpm --filter @contractor-ops/web-vite check:data-layer` exits 0
</acceptance_criteria>
</task>

<task id="73-08-08">
<action>
UI. Build the admin upload-review dialog triplet: `upload-review-dialog.tsx` (exports `UploadReviewDialog` view — tabs/sections for Approve and Reject; Approve: editable `expiresAt` pre-filled with the contractor-suggested + policy-default value (D-07), confirm button; Reject: `Select` of the 5 rejection reasonCategory values + optional `Textarea` free-text), `upload-review-dialog-container.tsx`, and `hooks/use-upload-review.ts` (calls `trpc.classification.approveUploadReplacement` / `rejectUploadReplacement`; on success toast + invalidate). DialogBody/DialogFooter convention.
</action>
<read_first>
- `apps/web-vite/src/components/contractors/compliance/recompute-compliance-dialog.tsx` — dialog triplet shape
- `packages/api/src/routers/compliance/classification.ts` — approve/reject mutation inputs (task 73-08-01)
- `apps/web-vite/src/lib/format/use-date-formatter.ts` — date input formatting
</read_first>
<acceptance_criteria>
- `UploadReviewDialog` gives Approve (editable expiresAt) + Reject (5-enum reason + free-text) paths
- Calls `approveUploadReplacement` / `rejectUploadReplacement` via the hook; content in DialogBody, actions in DialogFooter
- `pnpm check:web-vite-dialog-pattern` exits 0
</acceptance_criteria>
</task>

<task id="73-08-09">
<action>
UI. Modify `apps/web-vite/src/components/contractors/contractor-profile/tab-compliance.tsx` to render, per item row: (a) `<OverrideComplianceItemButton itemId contractorId severity status />` (hover-revealed but keyboard-focusable), (b) a `<ComplianceItemHistory itemId />` disclosure, and (c) a WAIVED badge with a category icon + tooltip ("Overridden by {actor} on {date} — reason: {category}") when `status === 'WAIVED'`. Preserve the existing item rendering + the `CountryComplianceSectionContainer`/`ContractorEInvoicingSectionContainer` composition. Extend the `ComplianceItem` type with `severity` + waived fields as needed (the contractor-detail query that feeds this tab must already provide them, or extend it minimally).
</action>
<read_first>
- `apps/web-vite/src/components/contractors/contractor-profile/tab-compliance.tsx` — current per-row rendering + ComplianceItem type
- `apps/web-vite/src/components/contractors/compliance/recompute-compliance-button.tsx` — hover-reveal + button mount idiom
- `@contractor-ops/ui` Tooltip / Badge primitives
</read_first>
<acceptance_criteria>
- `tab-compliance.tsx` renders OverrideComplianceItemButton + ComplianceItemHistory per row + a WAIVED badge with category icon + tooltip
- Existing tab composition (country compliance + e-invoicing sections) preserved
- `pnpm --filter @contractor-ops/web-vite check:page-shells` + `check:web-vite-presentational` exit 0
</acceptance_criteria>
</task>

<task id="73-08-10">
<action>
UI. Add the `Compliance.override.*`, `Compliance.uploadReview.*`, and `Compliance.history.*` message namespaces to all four catalogs `apps/web-vite/messages/{en,de,pl,ar}.json` at full parity: override dialog (title, category options labels, note placeholder/label, submit, success/error toasts), upload-review dialog (approve/reject tabs, expiresAt label, reason options, free-text, confirm), history (action labels for each audit action, actor/date labels, empty/error), and the WAIVED-badge tooltip. ar at full structural parity (interim-mirror en where untranslated). Run `pnpm i18n:parity`.
</action>
<read_first>
- `apps/web-vite/messages/en.json` — existing `Compliance` + `Contractors.Compliance.Recompute` namespaces (extend, don't duplicate)
- `apps/web-vite/messages/ar.json` — ar structure
</read_first>
<acceptance_criteria>
- `Compliance.override`, `Compliance.uploadReview`, `Compliance.history` namespaces exist in en, de, pl, AND ar
- `pnpm i18n:parity` exits 0
</acceptance_criteria>
</task>

<task id="73-08-11">
<action>
UI. Flip the Wave 0 RED scaffolds to GREEN and add the history test: evolve `override-compliance-item-dialog.test.tsx` (renders Select + Textarea, 6 options, disabled-until-valid, enables when valid, submit invokes the hook, success toast + close); create `apps/web-vite/src/components/contractors/compliance/__tests__/compliance-item-history.test.tsx` (>=3 cases: renders timeline from itemAuditTrail, empty state, action-label resolution). Mirror the web-vite component test setup.
</action>
<read_first>
- `apps/web-vite/src/components/contractors/compliance/__tests__/override-compliance-item-dialog.test.tsx` — Wave 0 scaffold to evolve
- `apps/web-vite/src/components/contractors/compliance/recompute-compliance-dialog.tsx` — sibling for test-setup reference
</read_first>
<acceptance_criteria>
- All cases in `override-compliance-item-dialog.test.tsx` + the new `compliance-item-history.test.tsx` pass
- `pnpm --filter @contractor-ops/web-vite test --run --testNamePattern='override-compliance-item-dialog|compliance-item-history'` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `pnpm --filter @contractor-ops/api test --run --testNamePattern='compliance-upload-review'` — exits 0.
2. `pnpm --filter @contractor-ops/feature-flags test --run --testNamePattern='compliance-portal-self-service-entry'` — exits 0.
3. `pnpm --filter @contractor-ops/web-vite test --run --testNamePattern='override-compliance-item-dialog|compliance-item-history'` — exits 0.
4. `pnpm --filter @contractor-ops/web-vite check:data-layer` + `check:page-shells` + `pnpm check:web-vite-presentational` + `pnpm check:web-vite-dialog-pattern` — exit 0.
5. `pnpm i18n:parity` — exits 0.
6. `pnpm typecheck` — exits 0.
7. Manual (deferred post-deploy UAT under LOCAL-ONLY): as an admin with `compliance:override`, override a BLOCKING item (status -> WAIVED + history shows the entry), and review a PENDING_REVIEW upload (approve -> item SATISFIED + contractor notified; reject -> Document ARCHIVED + contractor notified with re-upload link).
</verification>
