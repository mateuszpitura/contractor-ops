---
phase: 73
plan: 07
type: execute
wave: 3
depends_on: [73-01, 73-02, 73-03, 73-04, 73-05]
files_modified:
  - packages/api/src/routers/portal/portal.ts
  - packages/api/src/routers/compliance/classification.ts
  - packages/api/src/__tests__/compliance-portal-upload.test.ts
  - apps/web-vite/src/router/portal-routes.tsx
  - apps/web-vite/src/pages/portal/compliance.tsx
  - apps/web-vite/src/pages/portal/compliance-upload-replacement.tsx
  - apps/web-vite/src/components/portal/compliance/portal-compliance-container.tsx
  - apps/web-vite/src/components/portal/compliance/portal-compliance-list.tsx
  - apps/web-vite/src/components/portal/compliance/portal-upload-replacement-container.tsx
  - apps/web-vite/src/components/portal/compliance/portal-upload-replacement-form.tsx
  - apps/web-vite/src/components/portal/compliance/hooks/use-portal-compliance.ts
  - apps/web-vite/src/components/portal/compliance/hooks/use-portal-upload-replacement.ts
  - apps/web-vite/src/components/portal/portal-home-compliance-banner.tsx
  - apps/web-vite/src/components/portal/portal-index-container.tsx
  - apps/web-vite/src/components/portal/compliance/__tests__/portal-upload-replacement-form.test.tsx
  - apps/web-vite/messages/en.json
  - apps/web-vite/messages/de.json
  - apps/web-vite/messages/pl.json
  - apps/web-vite/messages/ar.json
autonomous: true
requirements: [COMPL-04]
requirements_addressed: [COMPL-04]

must_haves:
  truths:
    - "BACKEND: `packages/api/src/routers/portal/portal.ts` exports `portal.complianceItems` query returning the logged-in contractor's `ContractorComplianceItem[]` scoped to the portal session contractor (`item.contractorId === ctx.contractorId`); no organizationId/contractorId is trusted from client input"
    - "BACKEND: `packages/api/src/routers/compliance/classification.ts` exports `submitUploadReplacement` portal mutation (uses the portal procedure/middleware, NOT a staff procedure) accepting `{ itemId, documentId, suggestedExpiresAt? }`, asserts `item.contractorId === ctx.contractorId` (rejects cross-contractor itemId with NOT_FOUND), sets the uploaded `Document.status = 'PENDING_REVIEW'`, KEEPS `ContractorComplianceItem.status` at MISSING/EXPIRED (D-06 — flip happens only on admin approve in Plan 73-08), and writes `AuditLog action='compliance.upload.submitted'` with `metadata.itemId` + `metadata.documentId`"
    - "BACKEND: the mutation is Zod-validated; `itemId`/`documentId` are cuids; `suggestedExpiresAt` (when present) is an ISO date validated server-side"
    - "UI: portal routes `portal/compliance` + `portal/compliance/upload-replacement` registered in `apps/web-vite/src/router/portal-routes.tsx` (lazy pages); page shells `apps/web-vite/src/pages/portal/compliance.tsx` + `compliance-upload-replacement.tsx` compose containers only (check:page-shells)"
    - "UI: `portal-compliance-container.tsx` is the decisive container (loading/empty/error + composition); `hooks/use-portal-compliance.ts` is the only tRPC boundary (calls `trpc.portal.complianceItems`); `portal-compliance-list.tsx` renders items as cards with per-item 'Renew now' deep-link buttons to `/portal/compliance/upload-replacement?itemId=...&policyRuleId=...` via the locale-aware `Link`"
    - "UI: `portal-upload-replacement-container.tsx` reads `?itemId=...&policyRuleId=...` from the route (TanStack `useSearchParams`/`useParams` in the container, NOT the page), auto-derives `documentType` from the policyRule, auto-fills `expiresAt` via `defaultExpiryFromUploadDate` (Plan 73-05), and renders `portal-upload-replacement-form.tsx` with the `DropZone` (MIME pdf/png/jpeg, 10 MB cap from drop-zone-constants)"
    - "UI: `hooks/use-portal-upload-replacement.ts` orchestrates the existing R2 upload pipeline (`document.requestUpload` -> R2 PUT -> `document.confirmUpload`) then calls `trpc.classification.submitUploadReplacement` — a ~3-click flow per D-06; on success toasts + navigates to `/portal/compliance`"
    - "UI: the contractor can override the auto-filled `expiresAt` via an editable date picker (D-07); the form's submit is disabled until a file is selected"
    - "UI: `portal-home-compliance-banner.tsx` exports `PortalHomeComplianceBanner` rendering an `Alert` linking to `/portal/compliance` when the contractor has any MISSING/EXPIRED/30d-band item; it is mounted in `portal-index-container.tsx` (D-09 — surface only; no cron)"
    - "All Wave 0 RED scaffolds in `compliance-portal-upload.test.ts` flip to GREEN (>=5 passing); the `portal-upload-replacement-form.test.tsx` web-vite scaffold flips to GREEN (>=7 passing)"
    - "New `Portal.compliance.*` namespace added to `apps/web-vite/messages/{en,de,pl,ar}.json` at full 4-locale parity (ar required by i18n:parity)"
    - "`pnpm --filter @contractor-ops/api test --run --testNamePattern='compliance-portal-upload'` exits 0; `pnpm --filter @contractor-ops/web-vite test --run --testNamePattern='portal-compliance-upload-replacement|portal-home-compliance-banner'` exits 0; `pnpm --filter @contractor-ops/web-vite check:data-layer` + `check:page-shells` + `pnpm check:web-vite-presentational` + `pnpm i18n:parity` + `pnpm typecheck` all exit 0"
  artifacts:
    - path: "packages/api/src/routers/portal/portal.ts"
      provides: "portal.complianceItems query for the logged-in contractor"
      contains: "complianceItems"
    - path: "packages/api/src/routers/compliance/classification.ts"
      provides: "submitUploadReplacement portal mutation (PENDING_REVIEW + audit, item-status-stable)"
      contains: "submitUploadReplacement"
    - path: "apps/web-vite/src/components/portal/compliance/portal-upload-replacement-form.tsx"
      provides: "One-click upload-replacement form (DropZone + auto-filled expiresAt)"
      contains: "PortalUploadReplacementForm"
    - path: "apps/web-vite/src/components/portal/portal-home-compliance-banner.tsx"
      provides: "Portal home banner surfacing compliance attention items"
      contains: "PortalHomeComplianceBanner"
  key_links:
    - from: "packages/api/src/routers/compliance/classification.ts"
      to: "packages/api/src/routers/portal/portal.ts"
      via: "portal session scoping (ctx.contractorId from the portal procedure/middleware)"
      pattern: "assert item.contractorId === ctx.contractorId before any write"
    - from: "apps/web-vite/src/components/portal/compliance/hooks/use-portal-upload-replacement.ts"
      to: "apps/web-vite/src/components/documents/drop-zone-container.tsx"
      via: "Existing R2 upload pipeline (requestUpload -> R2 PUT -> confirmUpload)"
      pattern: "DropZone + document upload mutation chain, then submitUploadReplacement"
    - from: "apps/web-vite/src/components/portal/compliance/portal-upload-replacement-form.tsx"
      to: "packages/compliance-policy/src/expiry.ts"
      via: "Plan 73-05 defaultExpiryFromUploadDate helper"
      pattern: "auto-fill expiresAt input on mount; contractor can override"
    - from: "apps/web-vite/src/components/portal/portal-home-compliance-banner.tsx"
      to: "apps/web-vite/src/components/portal/portal-index-container.tsx"
      via: "portal home composition (D-09 surface only)"
      pattern: "Alert with Link to /portal/compliance when attention items > 0"
---

<objective>
Ship the contractor portal self-service surface (COMPL-04) over the SHIPPED Phase 71/72 backend: a `/portal/compliance` list, a one-click `/portal/compliance/upload-replacement` flow, and a portal-home attention banner. Split cleanly into a BACKEND slice (tasks 73-07-01..03: `portal.complianceItems` read + `submitUploadReplacement` portal mutation + RED-to-GREEN API tests) and a UI slice (tasks 73-07-04..09: web-vite Page->Container->Hook->Component + banner + i18n).

Per D-06/D-09 this is a pure surface over Phase 72: the cron that emits the reminder notification is owned by Phase 72 (`apps/cron-worker/.../reminders`) and NOT touched here. The contractor uploads a replacement document; the server marks the `Document` PENDING_REVIEW and keeps the compliance item MISSING/EXPIRED until an admin approves it (admin approve/reject is Plan 73-08). No OCR / AI verdict on COMPL surfaces (Standing Constraint). All UI in `apps/web-vite`, TanStack Router, custom i18n with ar parity, mandatory loading/empty/error states + WCAG.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-f1-compliance-admin-dashboard-portal-self-service-i18n/73-CONTEXT.md
@.planning/phases/73-f1-compliance-admin-dashboard-portal-self-service-i18n/73-RESEARCH.md
@apps/web-vite/ARCHITECTURE.md
@packages/api/src/routers/portal/portal.ts
@packages/api/src/routers/compliance/classification.ts
@apps/web-vite/src/router/portal-routes.tsx
@apps/web-vite/src/pages/portal/index.tsx
@apps/web-vite/src/components/portal/portal-index-container.tsx
@apps/web-vite/src/components/portal/invoice-submit-form-container.tsx
@apps/web-vite/src/components/portal/invoice-submit-upload.tsx
@apps/web-vite/src/components/documents/drop-zone.tsx
@apps/web-vite/src/components/documents/drop-zone-container.tsx
@apps/web-vite/src/components/documents/drop-zone-constants.ts
@apps/web-vite/src/hooks/use-upload-new-version.ts
@apps/web-vite/src/i18n/navigation.ts
</context>

<deep_work_rules_note>
Follow the `frontend-design` skill before UI work. Use `semble search` to find the exact portal procedure/middleware name used by existing portal queries (`portal.*`), the document upload mutation names (`document.requestUpload`/`confirmUpload` or equivalent), and the `DropZone` props before writing code. Verify the portal procedure exposes `ctx.contractorId` — do not assume.
</deep_work_rules_note>

<threat_model>
| ID | Threat | Mitigation |
|----|--------|-----------|
| T-73-07-01 | IDOR — a contractor submits an itemId/documentId belonging to another contractor | `submitUploadReplacement` and `portal.complianceItems` both scope strictly to `ctx.contractorId` from the portal session; the mutation re-asserts `item.contractorId === ctx.contractorId` and throws NOT_FOUND otherwise. No contractorId/organizationId from client input |
| T-73-07-02 | Malicious upload — oversized / wrong-MIME / malware file | Reuse the existing `Document` upload pipeline only: MIME whitelist pdf/png/jpeg + 10 MB cap (from `drop-zone-constants`), server-side virus scan + signed-URL R2 PUT already in place. No new upload path |
| T-73-07-03 | Privilege escalation — contractor self-approves by setting item status SATISFIED | The portal mutation NEVER writes `ContractorComplianceItem.status`; it only sets `Document.status=PENDING_REVIEW`. SATISFIED flip is admin-only (Plan 73-08, `compliance:override`) |
| T-73-07-04 | Forged expiresAt — contractor sets a far-future expiry to dodge renewal | `suggestedExpiresAt` is only a suggestion stored for the admin's review; the authoritative `expiresAt` is written by the admin on approve (Plan 73-08). Server validates the ISO date shape but does not trust it for the item |
| T-73-07-05 | Page-shell / data-layer lint fails (route hooks or tRPC in the page) | `useSearchParams`/`useParams`/tRPC live in the container + hook; pages compose containers only. `check:page-shells` + `check:data-layer` verify |
| T-73-07-06 | Open-redirect via the 'Renew now' deep link | Deep links are internal relative paths built from server-issued cuids via the locale-aware `Link`; never an absolute/external URL |
| T-73-07-07 | i18n:parity fails (missing ar keys) | All new `Portal.compliance.*` keys added to en/de/pl/ar; `pnpm i18n:parity` in verification |
</threat_model>

<tasks>

<task id="73-07-01">
<action>
BACKEND. In `packages/api/src/routers/portal/portal.ts` add a `complianceItems` query using the same portal procedure the other `portal.*` queries use. It returns the logged-in contractor's `ContractorComplianceItem[]` filtered by `contractorId === ctx.contractorId`, selecting the fields the portal list + banner need (id, name, documentType, policyRuleId, status, severity, expiresAt). No client-supplied contractorId/organizationId.
</action>
<read_first>
- `packages/api/src/routers/portal/portal.ts` — existing portal procedure name + ctx.contractorId access + an existing portal query shape
- `packages/api/src/routers/portal/portal-time.ts` — a second portal-procedure example
- `packages/db/prisma/schema/contractor.prisma` — ContractorComplianceItem fields
</read_first>
<acceptance_criteria>
- `packages/api/src/routers/portal/portal.ts` exports a `complianceItems` query on the portal router
- The query filters by `ctx.contractorId` (verified by reading the procedure) — no client contractorId
- `pnpm --filter @contractor-ops/api typecheck` exits 0
</acceptance_criteria>
</task>

<task id="73-07-02">
<action>
BACKEND. In `packages/api/src/routers/compliance/classification.ts` add the `submitUploadReplacement` portal mutation using the portal procedure/middleware (the SAME one `portal.*` uses for `ctx.contractorId`). Zod input `{ itemId: z.cuid(), documentId: z.cuid(), suggestedExpiresAt: z.string().date().optional() }`. Steps in a single `prisma.$transaction`: (1) load the item, assert `item.contractorId === ctx.contractorId` else `TRPCError NOT_FOUND`; (2) update the `Document` (documentId) `status='PENDING_REVIEW'`; (3) do NOT touch `ContractorComplianceItem.status`; (4) `writeAuditLog({ action: 'compliance.upload.submitted', resourceType: 'CONTRACTOR', resourceId: item.contractorId, metadata: { itemId, documentId, suggestedExpiresAt } }, tx)`. Mirror the transactional + audit shape of the existing `recreateComplianceAssessment` mutation in the same file.
</action>
<read_first>
- `packages/api/src/routers/compliance/classification.ts` — recreateComplianceAssessment transactional + audit pattern; confirm router export + how to add a portal-scoped procedure here
- `packages/api/src/services/audit-writer.ts` — writeAuditLog(args, tx?) signature
- `packages/db/prisma/schema/contract.prisma` — DocumentStatus PENDING_REVIEW (Plan 73-02)
</read_first>
<acceptance_criteria>
- `submitUploadReplacement` exists in `classification.ts` using the portal procedure (asserts ctx.contractorId)
- Sets `Document.status='PENDING_REVIEW'`; does NOT write `ContractorComplianceItem.status`
- Writes `AuditLog action='compliance.upload.submitted'` inside the same transaction as the Document update
- `pnpm --filter @contractor-ops/api typecheck` exits 0
</acceptance_criteria>
</task>

<task id="73-07-03">
<action>
BACKEND. Flip the Wave 0 RED scaffold `packages/api/src/__tests__/compliance-portal-upload.test.ts` to GREEN: assert PENDING_REVIEW set, item-status-stable invariant, cross-contractor NOT_FOUND, unauthenticated rejection, audit emission, and the mutation input shape. Evolve the scaffold's assertions to the real implementation; reuse the router test harness from the Plan 73-03 sibling test.
</action>
<read_first>
- `packages/api/src/__tests__/compliance-portal-upload.test.ts` — Wave 0 RED scaffold
- `packages/api/src/__tests__/compliance-override-mutation.test.ts` — sibling router test harness (Plan 73-03)
</read_first>
<acceptance_criteria>
- `pnpm --filter @contractor-ops/api test --run --testNamePattern='compliance-portal-upload'` exits 0 with at least 5 passing test cases
</acceptance_criteria>
</task>

<task id="73-07-04">
<action>
UI. Register the two portal routes in `apps/web-vite/src/router/portal-routes.tsx`: lazy imports + `{ path: 'portal/compliance', element: page(<PortalCompliancePage />) }` and `{ path: 'portal/compliance/upload-replacement', element: page(<PortalComplianceUploadReplacementPage />) }`. Create the page shells `apps/web-vite/src/pages/portal/compliance.tsx` (composes `<PortalComplianceContainer/>`) and `apps/web-vite/src/pages/portal/compliance-upload-replacement.tsx` (composes `<PortalUploadReplacementContainer/>`), each Suspense-wrapped and importing only the container (page-shell rule).
</action>
<read_first>
- `apps/web-vite/src/router/portal-routes.tsx` — lazy declaration + page() wrapper + portalRoutes array
- `apps/web-vite/src/pages/portal/index.tsx` — thin portal page-shell reference
</read_first>
<acceptance_criteria>
- `portal-routes.tsx` contains both `portal/compliance` and `portal/compliance/upload-replacement` route entries + lazy imports
- Both page files compose containers only (no tRPC / route hooks / useTranslations)
- `pnpm --filter @contractor-ops/web-vite check:page-shells` exits 0
</acceptance_criteria>
</task>

<task id="73-07-05">
<action>
UI. Create the compliance list: hook `hooks/use-portal-compliance.ts` (only tRPC boundary — `trpc.portal.complianceItems`; returns `{ isPending, error, isEmpty, items }`), decisive container `portal-compliance-container.tsx` (loading/empty/error branching + composes the list), and presentational `portal-compliance-list.tsx` rendering each item as a card with a 'Renew now' button linking to `/portal/compliance/upload-replacement?itemId=...&policyRuleId=...` via the locale-aware `Link`. Use `useComplDocName` (Plan 73-06 shared hook) for the per-locale doc name.
</action>
<read_first>
- `apps/web-vite/src/components/portal/portal-index-container.tsx` + `hooks/` — portal container + hook idiom
- `apps/web-vite/src/components/portal/summary-card.tsx` — portal card visual
- `apps/web-vite/src/i18n/navigation.ts` — Link
- `apps/web-vite/src/components/compliance/hooks/use-compl-doc-name.ts` — Plan 73-06 doc-name hook
</read_first>
<acceptance_criteria>
- `use-portal-compliance.ts` is the only tRPC boundary; container branches loading/empty/error; list is presentational
- 'Renew now' links to `/portal/compliance/upload-replacement?itemId=...&policyRuleId=...` via `Link`
- `pnpm --filter @contractor-ops/web-vite check:data-layer` + `check:web-vite-presentational` exit 0
</acceptance_criteria>
</task>

<task id="73-07-06">
<action>
UI. Create the upload-replacement flow: container `portal-upload-replacement-container.tsx` reads `?itemId` + `?policyRuleId` from the route (TanStack search params IN THE CONTAINER), resolves the policy rule, computes the default expiry via `defaultExpiryFromUploadDate` (Plan 73-05), and renders `portal-upload-replacement-form.tsx`. The form (`PortalUploadReplacementForm`) renders the `DropZone` (MIME pdf/png/jpeg + 10 MB cap from `drop-zone-constants`), an editable `expiresAt` date picker pre-filled with the computed default (D-07), and a submit button disabled until a file is chosen. Hook `hooks/use-portal-upload-replacement.ts` orchestrates `document.requestUpload` -> R2 PUT -> `document.confirmUpload` -> `trpc.classification.submitUploadReplacement`; on success toasts + `useNavigate()` to `/portal/compliance`.
</action>
<read_first>
- `apps/web-vite/src/components/portal/invoice-submit-form-container.tsx` + `invoice-submit-upload.tsx` — portal upload submit precedent (container + hook + view split, R2 chain)
- `apps/web-vite/src/components/documents/drop-zone-container.tsx` + `drop-zone-constants.ts` — DropZone props + MIME/size constants
- `apps/web-vite/src/hooks/use-upload-new-version.ts` — document upload mutation chain
- `packages/compliance-policy/src/expiry.ts` — defaultExpiryFromUploadDate (Plan 73-05)
</read_first>
<acceptance_criteria>
- `PortalUploadReplacementForm` renders DropZone (pdf/png/jpeg, 10 MB) + editable expiresAt + disabled-until-file submit
- The hook runs the requestUpload -> R2 PUT -> confirmUpload -> submitUploadReplacement chain and navigates to `/portal/compliance` on success
- search params are read in the container, not the page (`check:page-shells` passes)
</acceptance_criteria>
</task>

<task id="73-07-07">
<action>
UI. Create `apps/web-vite/src/components/portal/portal-home-compliance-banner.tsx` exporting `PortalHomeComplianceBanner`. It consumes the portal compliance items (via `use-portal-compliance` or a thin banner hook) and renders an `Alert` (with a `Link` to `/portal/compliance`) when the contractor has any MISSING/EXPIRED item OR any item expiring within 30 days; renders null otherwise. Mount it in `apps/web-vite/src/components/portal/portal-index-container.tsx` (D-09 — surface only, no cron). Keep tRPC in the hook layer.
</action>
<read_first>
- `apps/web-vite/src/components/portal/portal-index-container.tsx` — portal home composition point
- `apps/web-vite/src/components/portal/pending-change-banner.tsx` — existing portal Alert-banner idiom
- `apps/web-vite/src/components/portal/compliance/hooks/use-portal-compliance.ts` — items source (from task 73-07-05)
</read_first>
<acceptance_criteria>
- `PortalHomeComplianceBanner` exists, renders an Alert + Link to `/portal/compliance` when attention items > 0, null otherwise
- It is mounted in `portal-index-container.tsx`
- No direct react-query in the banner component (uses the hook)
</acceptance_criteria>
</task>

<task id="73-07-08">
<action>
UI. Add the `Portal.compliance.*` namespace to all four catalogs `apps/web-vite/messages/{en,de,pl,ar}.json` at full parity: list heading/empty/error, 'Renew now' label, upload-form labels (drop-zone hint, expiresAt label, submit), success toast, banner title/body/CTA. ar at full structural parity (interim-mirror en where untranslated). Run `pnpm i18n:parity`.
</action>
<read_first>
- `apps/web-vite/messages/en.json` — existing `Portal` namespace (extend, don't duplicate)
- `apps/web-vite/messages/ar.json` — ar structure
</read_first>
<acceptance_criteria>
- `Portal.compliance` namespace exists in en, de, pl, AND ar
- `pnpm i18n:parity` exits 0
</acceptance_criteria>
</task>

<task id="73-07-09">
<action>
UI. Flip the Wave 0 RED scaffold `apps/web-vite/src/components/portal/compliance/__tests__/portal-upload-replacement-form.test.tsx` to GREEN: form renders, documentType auto-derived from policyRuleId, expiresAt auto-filled + editable, submit invokes the upload-replacement hook, success navigates to `/portal/compliance`, and `PortalHomeComplianceBanner` renders when attention items exist. Evolve the scaffold; mirror the web-vite form test setup.
</action>
<read_first>
- `apps/web-vite/src/components/portal/compliance/__tests__/portal-upload-replacement-form.test.tsx` — the Wave 0 scaffold to evolve
- `apps/web-vite/src/components/portal/__tests__/` — portal component test setup (provider mocks + render helper)
</read_first>
<acceptance_criteria>
- All test cases in `portal-upload-replacement-form.test.tsx` pass
- `pnpm --filter @contractor-ops/web-vite test --run --testNamePattern='portal-compliance-upload-replacement|portal-home-compliance-banner'` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `pnpm --filter @contractor-ops/api test --run --testNamePattern='compliance-portal-upload'` — exits 0.
2. `pnpm --filter @contractor-ops/web-vite test --run --testNamePattern='portal-compliance-upload-replacement|portal-home-compliance-banner'` — exits 0.
3. `pnpm --filter @contractor-ops/web-vite check:data-layer` + `check:page-shells` + `pnpm check:web-vite-presentational` — exit 0.
4. `pnpm i18n:parity` — exits 0.
5. `pnpm typecheck` — exits 0.
6. Manual (deferred post-deploy UAT under LOCAL-ONLY): from a portal session with an EXPIRED item, see the home banner -> /portal/compliance list -> 'Renew now' -> upload a PDF -> see auto-filled expiry -> submit -> Document is PENDING_REVIEW + item still MISSING/EXPIRED until admin approves.
</verification>
