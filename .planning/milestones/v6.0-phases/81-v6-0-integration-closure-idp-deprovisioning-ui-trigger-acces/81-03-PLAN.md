---
phase: 81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces
plan: 03
type: execute
wave: 1
depends_on:
  - 81-01
files_modified:
  - packages/api/src/routers/compliance/compliance-admin.ts
autonomous: true
requirements:
  - COMPL-07
  - COMPL-08
  - COMPL-11
user_setup: []

must_haves:
  truths:
    - "Approving a portal upload replacement re-asserts contractor eligibility and releases any PENDING_COMPLIANCE ApprovalFlow holding that item — the contractor's payment unblocks."
    - "The recovery runs inside the same $transaction as the SATISFIED flip (atomic)."
    - "A post-tx contractor-notification dispatch failure never rolls back the approval."
  artifacts:
    - path: "packages/api/src/routers/compliance/compliance-admin.ts"
      provides: "onComplianceItemSatisfied called in approveUploadReplacement tx for the approved item"
      contains: "onComplianceItemSatisfied"
  key_links:
    - from: "compliance-admin.ts approveUploadReplacement $transaction"
      to: "onComplianceItemSatisfied"
      via: "in-tx call after SATISFIED flip, before return"
      pattern: "onComplianceItemSatisfied\\(tx"
    - from: "onComplianceItemSatisfied"
      to: "ApprovalFlow PENDING_COMPLIANCE → PENDING + payment gate release"
      via: "compliance-recovery hook"
      pattern: "compliance-recovery"
---

<objective>
Close the INT-02 server seam: import and call `onComplianceItemSatisfied(tx, { itemId, contractorId, organizationId })` inside `approveUploadReplacement`'s existing `$transaction`, after the compliance item flips to SATISFIED and before the return — for the approved item ONLY (per-item, not the all-BLOCKING loop). Preserve the post-tx best-effort contractor notification exactly.

Purpose: An approved portal upload currently leaves the contractor payment-blocked because the recovery hook is wired into exactly one path (classification reconcile) and is not even imported here. This wires it into the admin-approve path so the E2E "portal upload → admin approve → payment unblock" completes.
Output: `compliance-admin.ts` with the in-tx recovery call. Turns 81-01 INT-02 RED cases GREEN.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-CONTEXT.md
@.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-RESEARCH.md
@.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-PATTERNS.md

<interfaces>
<!-- Verified anchors — no exploration needed. -->

compliance-admin.ts approveUploadReplacement (:236):
- `:19-40` import block — `onComplianceItemSatisfied` is NOT imported today (ADD from '../../services/compliance-recovery').
- `:237` `.use(requirePermission({ compliance: ['override'] }))` (existing RBAC — unchanged).
- `:246-312` the $transaction; `before.contractorId` in scope at :249.
- `:284-291` item → SATISFIED (+satisfiedByDocumentId, +expiresAt).
- `:296-310` writeAuditLog compliance.upload.approved (inside tx, with `tx`).
- `:311` `return { item: updated, contractorId: before.contractorId }`.
- `:314-317` post-tx `dispatchComplianceUploadOutcome(...)` — best-effort, OUTSIDE tx (DO NOT MOVE — D-14 / T-73-08-04).

compliance-recovery.ts:43 — hook signature:
`onComplianceItemSatisfied(tx: RecoveryClient, { itemId, contractorId, organizationId }): Promise<{ resumedFlowIds: string[] }>`
`RecoveryClient` (:29-35) is satisfied structurally by the $transaction callback client (needs $queryRaw + approvalFlow.update + payment-gate + audit-writer clients — all present on tx).

classification.ts:91-103 — the call shape to MIRROR (per-item):
`await onComplianceItemSatisfied(tx, { itemId: item.id, contractorId, organizationId });`
tx typing analog: `Parameters<typeof onComplianceItemSatisfied>[0]` (:92). Mirror the per-item CALL, NOT the all-BLOCKING loop in releaseHeldApprovalsForContractor.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire onComplianceItemSatisfied into approveUploadReplacement tx</name>
  <files>packages/api/src/routers/compliance/compliance-admin.ts</files>
  <read_first>
    - packages/api/src/routers/compliance/compliance-admin.ts (:19-40 imports, :236 approveUploadReplacement, :246-312 $transaction, :284-291 SATISFIED flip, :296-310 audit, :311 return, :314-317 post-tx notification)
    - packages/api/src/services/compliance-recovery.ts (:29-35 RecoveryClient, :43-57 onComplianceItemSatisfied — $queryRaw bound ::jsonb param, no interpolation)
    - packages/api/src/routers/compliance/classification.ts (:91-103 the per-item call shape + tx typing to mirror)
    - packages/api/src/__tests__/compliance-upload-review.test.ts (81-01 RED cases this task makes GREEN)
  </read_first>
  <behavior>
    - approve an upload whose item is in a held PENDING_COMPLIANCE ApprovalFlow → that flow flips PENDING_COMPLIANCE → PENDING and holds clear (recovery fired in-tx)
    - approve an item NOT in any held flow → no flow change, approval still succeeds
    - post-tx dispatchComplianceUploadOutcome throws → approval + recovery still commit (not rolled back)
  </behavior>
  <action>
    Add `import { onComplianceItemSatisfied } from '../../services/compliance-recovery';` to the import block (:19-40 — not currently imported). Inside the existing `$transaction` in `approveUploadReplacement`, AFTER the SATISFIED flip (:284-291) and the audit write (:296-310), and BEFORE `return { item: updated, contractorId: before.contractorId }` (:311), call `await onComplianceItemSatisfied(tx as Parameters<typeof onComplianceItemSatisfied>[0], { itemId: input.itemId, contractorId: before.contractorId, organizationId: ctx.organizationId })` for the APPROVED item ONLY (per D-12 — mirror classification.ts:101, do NOT call releaseHeldApprovalsForContractor / loop all BLOCKING items; exactly one item flips per approval). The hook re-asserts FULL contractor eligibility for every ApprovalFlow whose complianceHoldsJson contains this item and writes the existing `approval.compliance_resolved` audit row — no NEW audit/notification on resume (D-13). Do NOT touch the post-tx `dispatchComplianceUploadOutcome` (:314-317): it MUST stay OUTSIDE the tx, unchanged, best-effort — a dispatch failure must never roll back the approval (D-14 / T-73-08-04, Pitfall 3). `ctx.organizationId` from session — never client input. Use `@contractor-ops/logger` — no console.*.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test compliance-upload-review 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "onComplianceItemSatisfied" packages/api/src/routers/compliance/compliance-admin.ts` shows BOTH the import and the in-tx call.
    - The call passes `itemId: input.itemId`, `contractorId: before.contractorId`, `organizationId: ctx.organizationId` (per-item, not a loop over BLOCKING items).
    - `dispatchComplianceUploadOutcome` is unchanged and still OUTSIDE the $transaction (post-tx best-effort).
    - `pnpm --filter @contractor-ops/api test compliance-upload-review` GREEN (D-12 recovery fires + D-14 isolation).
    - `pnpm typecheck --filter @contractor-ops/api` clean.
  </acceptance_criteria>
  <done>Recovery call wired in-tx for the approved item; notification stays post-tx best-effort; 81-01 INT-02 RED cases GREEN; api typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → approveUploadReplacement | Already RBAC-gated (`compliance:override`); the recovery is a downstream in-tx effect, no new external input. |
| recovery → held ApprovalFlow query | `complianceHoldsJson` containment query must use bound parameters (no string interpolation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-03-01 | Denial of Service | Recovery rollback of a valid approval | mitigate | Recovery is in-tx (atomic with SATISFIED — intended, D-14); the post-tx contractor notification stays OUTSIDE the tx so a transient dispatch failure never undoes a legitimate approval (T-73-08-04, Pitfall 3). |
| T-81-03-02 | Tampering | SQL injection in the held-flow containment query | mitigate | `onComplianceItemSatisfied` uses `$queryRaw` with a bound `${...}::jsonb` parameter (no interpolation) — unchanged, verified compliance-recovery.ts:49. This plan only calls the hook. |
| T-81-03-03 | Information Disclosure (IDOR) | Cross-tenant flow release | mitigate | `organizationId: ctx.organizationId` (session) passed to the hook; recovery only touches flows scoped to that org. |
| T-81-03-04 | Tampering | Over-release via all-BLOCKING loop | mitigate | Per-item call only (D-12) — exactly one item flips per approval; the all-BLOCKING loop (supersession) is NOT used here. |
| T-81-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit — zero external packages this phase). |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test compliance-upload-review` GREEN (D-12 + D-14).
- `pnpm typecheck --filter @contractor-ops/api` clean.
- `onComplianceItemSatisfied` imported + called in-tx for the approved item; notification unchanged post-tx.
</verification>

<success_criteria>
INT-02 server seam closed: `onComplianceItemSatisfied` called in-tx (per-item, after SATISFIED, before return) so an approved portal upload releases held PENDING_COMPLIANCE flows and unblocks payment; the best-effort contractor notification stays post-tx and cannot roll back the approval. 81-01 INT-02 RED cases GREEN.
</success_criteria>

<output>
Create `.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-03-SUMMARY.md` when done.
</output>
