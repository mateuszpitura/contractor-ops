---
phase: 81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces
plan: 02
type: execute
wave: 1
depends_on:
  - 81-01
files_modified:
  - packages/auth/src/permissions.ts
  - packages/auth/src/roles.ts
  - packages/api/src/services/idp-token-resolver.ts
  - packages/api/src/routers/integrations/deprovisioning.ts
autonomous: true
requirements:
  - IDP-01
  - IDP-02
  - IDP-03
  - IDP-04
  - IDP-05
  - IDP-06
  - IDP-07
  - IDP-08
  - IDP-09
  - IDP-10
  - IDP-12
  - IDP-13
  - IDP-15
user_setup: []

must_haves:
  truths:
    - "A run's provider set is derived per-org (enabled ∩ signoff ∩ resolver-backed), not hardcoded to GOOGLE_WORKSPACE."
    - "An org with no enabled+signoff+resolver-backed provider cannot start a zero-step run — startDeprovisioningRun throws DEPROVISIONING_INTEGRATION_NOT_CONFIGURED."
    - "startDeprovisioningRun and getDeprovisioningEligibility reject callers lacking idp:start_run; owner+admin+it_admin hold it, override_step_failure stays owner/admin-only."
    - "A WorkflowRun.contractorId can be resolved server-side to the most-recent ENDED ContractorAssignment id, org-scoped."
  artifacts:
    - path: "packages/auth/src/permissions.ts"
      provides: "idp:start_run action in the access-control statement"
      contains: "start_run"
    - path: "packages/auth/src/roles.ts"
      provides: "start_run granted to owner, admin, it_admin"
      contains: "start_run"
    - path: "packages/api/src/services/idp-token-resolver.ts"
      provides: "RESOLVER_BACKED_PROVIDERS exported const driving DeprovisionProvider"
      contains: "RESOLVER_BACKED_PROVIDERS"
    - path: "packages/api/src/routers/integrations/deprovisioning.ts"
      provides: "Dynamic provider derivation + idp:start_run gate on 2 procs + contractorId→assignmentId resolver + org settings read"
      contains: "DEPROVISIONING_INTEGRATION_NOT_CONFIGURED"
  key_links:
    - from: "deprovisioning.ts startDeprovisioningRun"
      to: "Organization.settingsJson.idpDeprovisioningEnabled"
      via: "organization.findUnique select settingsJson"
      pattern: "idpDeprovisioningEnabled"
    - from: "deprovisioning.ts derivation"
      to: "idp-token-resolver RESOLVER_BACKED_PROVIDERS"
      via: "import + intersection"
      pattern: "RESOLVER_BACKED_PROVIDERS"
    - from: "deprovisioning.ts startDeprovisioningRun + getDeprovisioningEligibility"
      to: "requirePermission idp:start_run"
      via: ".use(requirePermission(...))"
      pattern: "requirePermission\\(\\{ idp: \\['start_run'\\]"
---

<objective>
Close the INT-01 server seam: add the `idp:start_run` permission (granting owner+admin+it_admin), gate the two currently-ungated procedures with it, replace the hardcoded `PROVIDERS_FOR_RUN` const with a per-org dynamic derivation (enabled ∩ signoff ∩ resolver-backed) backed by an exported `RESOLVER_BACKED_PROVIDERS`, add the missing org-settings read, throw `DEPROVISIONING_INTEGRATION_NOT_CONFIGURED` on an empty set, and add the server-side `contractorId → assignmentId` resolver.

Purpose: Turn the saga from "GWS-only, ungated, server-only" into a per-org, RBAC-gated, multi-provider trigger the web-vite UI (81-05) can call through a single tRPC boundary, while keeping the resolver server-side so `check:web-vite-data-layer` stays green.
Output: Updated auth statement/roles + a `RESOLVER_BACKED_PROVIDERS` export + a dynamic-derivation, gated, resolver-extended deprovisioning router. Turns 81-01 RED cases GREEN.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-CONTEXT.md
@.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-RESEARCH.md
@.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-PATTERNS.md

<interfaces>
<!-- Verified anchors the executor uses directly — no exploration needed. -->

deprovisioning.ts (the single file with every derivation primitive):
- `:32-38` DEPROVISIONING_TOGGLE_PROVIDERS — 5-provider tuple (schema-derived).
- `:53-56` isProviderSignoffSatisfied(provider) — signoff-flag gate (local bypass aware).
- `:68` `const PROVIDERS_FOR_RUN = ['GOOGLE_WORKSPACE'] as const;` (REPLACE) — only consumer is :200 `steps: { create: PROVIDERS_FOR_RUN.flatMap(...) }`.
- `:99` getDeprovisioningEligibility (tenantProcedure only — GATE).
- `:147` startDeprovisioningRun (tenantProcedure only — GATE); fetches ONLY the assignment :155-168 (NO settingsJson read today — ADD it).
- `:256-272` P2002 handler — returns existing run filtered by organizationId_idempotencyKey (UNCHANGED — D-09).
- `:491` overrideStepFailure uses `.use(requirePermission({ idp: ['override_step_failure'] }))` — gate pattern to mirror.
- `:594-615` getProviderToggleState — verbatim org-settings read pattern:
    org.findUnique({ where: { id: ctx.organizationId }, select: { settingsJson: true } });
    const settings = (org?.settingsJson as Record<string, unknown>) ?? {};
    const enabledMap = (settings.idpDeprovisioningEnabled as Record<string, boolean>) ?? {};

idp-token-resolver.ts:10 — `type DeprovisionProvider = 'GOOGLE_WORKSPACE' | 'SLACK';` (authoritative resolver-backed list).

permissions.ts:25 — existing `idp: ['override_step_failure']` action array.
roles.ts — `:28` allPermissions.idp, `:58` admin idp, `:124-133` it_admin block (NO idp key today).
rbac.ts:19 — `requirePermission({ group: ['action'] })` middleware.

contractor.prisma — ContractorAssignment has `status` (AssignmentStatus = ACTIVE|ENDED|PLANNED, :322-326) + `endedAt: DateTime?`. WorkflowRun has NO assignmentId (workflow.prisma:111, contractorId only).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add idp:start_run permission + grant to owner/admin/it_admin</name>
  <files>packages/auth/src/permissions.ts, packages/auth/src/roles.ts</files>
  <read_first>
    - packages/auth/src/permissions.ts (:25 idp action array; :21 workflow:override_blocking_task analog split)
    - packages/auth/src/roles.ts (:28 allPermissions.idp owner-inherit via ac.newRole, :46 newRole, :58 admin idp, :124-133 it_admin block)
    - packages/auth/src/__tests__/roles.test.ts (the invariants 81-01 rewrote — this task makes them GREEN)
  </read_first>
  <behavior>
    - owner.statements.idp === ['override_step_failure', 'start_run'] (order-tolerant)
    - admin.statements.idp === ['override_step_failure', 'start_run'] (order-tolerant)
    - it_admin.statements.idp === ['start_run'] (exactly — NO override_step_failure)
    - every other role: statements.idp === undefined
    - it_admin still cannot read invoices or contractors (unchanged)
  </behavior>
  <action>
    In permissions.ts:25 add `start_run` to the `idp` action array so the access-control statement is `idp: ['override_step_failure', 'start_run']` (per D-10). In roles.ts: add `start_run` to `allPermissions.idp` (:28 — owner inherits via `ac.newRole(allPermissions)`); add `start_run` to the admin block's `idp` (:58); and add a NEW `idp: ['start_run']` key to the `it_admin` block (:124-133, which has no idp key today) per D-10 / A1 — it_admin is the seeded ACCESS_REVOKE assignee (`workflow-templates.ts:396`) so the D-01 inline-card path must be reachable by it_admin. `override_step_failure` stays owner/admin-only — do NOT add it to it_admin. Do not touch any other role. RBAC enforcement is code (Better Auth evaluates roles.ts per request via authApi.hasPermission) — no DB migration. After this task the 81-01 roles.test.ts invariants must pass.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/auth test roles 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "start_run" packages/auth/src/permissions.ts` shows it inside the `idp` array.
    - `pnpm --filter @contractor-ops/auth test roles` is GREEN (the 81-01 invariants pass).
    - it_admin holds EXACTLY `['start_run']`; no `override_step_failure` on it_admin.
  </acceptance_criteria>
  <done>idp:start_run added to statement + granted to owner/admin/it_admin; roles.test.ts GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dynamic PROVIDERS_FOR_RUN + gate both procs + org-settings read + contractorId→assignmentId resolver</name>
  <files>packages/api/src/services/idp-token-resolver.ts, packages/api/src/routers/integrations/deprovisioning.ts</files>
  <read_first>
    - packages/api/src/services/idp-token-resolver.ts (:10 DeprovisionProvider union — convert to derive from a new RESOLVER_BACKED_PROVIDERS const)
    - packages/api/src/routers/integrations/deprovisioning.ts (:11 DEPROVISIONING_INTEGRATION_NOT_CONFIGURED import, :32-38 DEPROVISIONING_TOGGLE_PROVIDERS, :53-56 isProviderSignoffSatisfied, :68 PROVIDERS_FOR_RUN, :99 getDeprovisioningEligibility, :147 startDeprovisioningRun, :155-168 assignment fetch, :200 step create, :256-272 P2002, :491 overrideStepFailure gate, :594-615 getProviderToggleState settings read)
    - packages/api/src/middleware/rbac.ts (:19-61 requirePermission)
    - packages/db/prisma/schema/contractor.prisma (:161-202 ContractorAssignment, :322-326 AssignmentStatus)
    - packages/api/src/__tests__/deprovisioning-start.test.ts + contractor-assignment-resolver.test.ts (81-01 RED cases this task makes GREEN)
  </read_first>
  <behavior>
    - org with idpDeprovisioningEnabled {GOOGLE_WORKSPACE:true, SLACK:true} + signoff → run creates steps for BOTH providers
    - org with only GOOGLE_WORKSPACE enabled → GWS-only steps
    - org with no enabled+signoff+resolver-backed provider → startDeprovisioningRun throws PRECONDITION_FAILED / DEPROVISIONING_INTEGRATION_NOT_CONFIGURED, no run created
    - caller lacking idp:start_run → both startDeprovisioningRun and getDeprovisioningEligibility throw FORBIDDEN
    - double-trigger with the same assignment-derived idempotencyKey → returns the existing run (P2002, unchanged)
    - resolver: contractor with two ENDED assignments returns the most-recent-by-endedAt id; contractor with no ENDED assignment → null / NOT_FOUND
  </behavior>
  <action>
    In idp-token-resolver.ts export `RESOLVER_BACKED_PROVIDERS = ['GOOGLE_WORKSPACE', 'SLACK'] as const` and redefine `DeprovisionProvider = (typeof RESOLVER_BACKED_PROVIDERS)[number]` so the resolver-backed list has a single source of truth (D-05 / RESEARCH A3 — do NOT add a 4th literal that can drift). In deprovisioning.ts: (1) import `RESOLVER_BACKED_PROVIDERS`; replace the hardcoded `PROVIDERS_FOR_RUN` const (:68) with a `deriveProvidersForRun(settingsJson)` helper that returns `DEPROVISIONING_TOGGLE_PROVIDERS.filter(p => enabledMap[p] === true && isProviderSignoffSatisfied(p) && RESOLVER_BACKED_PROVIDERS.includes(p))` (D-05). (2) Inside `startDeprovisioningRun` add the org-settings read mirroring getProviderToggleState:597-601 (`organization.findUnique({ where: { id: ctx.organizationId }, select: { settingsJson: true } })`) BEFORE the derivation (Pitfall 5 — the mutation does not read settingsJson today). (3) Wire the derived list into the existing step-create at :200 (replace `PROVIDERS_FOR_RUN.flatMap` with the derived array). (4) If the derived array is empty, `throw new TRPCError({ code: 'PRECONDITION_FAILED', message: DEPROVISIONING_INTEGRATION_NOT_CONFIGURED })` (D-06 — error key already imported :11; do NOT create a zero-step run). The fan-out loop at :229 already iterates run.steps so multi-provider works automatically. (5) Gate BOTH procedures by inserting `.use(requirePermission({ idp: ['start_run'] }))` into the chain — `getDeprovisioningEligibility` (:99) and `startDeprovisioningRun` (:147) — mirroring overrideStepFailure (:491) (D-10). (6) Add a NEW tenantProcedure resolver `resolveAssignmentForContractor` (Claude's-discretion placement — server-side per Architectural Responsibility Map so the hook is one tRPC call and the data-layer guard stays green): Zod input `{ contractorId: z.string().cuid() }` (or runId variant), also gated by `idp:start_run`; query `contractorAssignment.findFirst({ where: { contractorId, organizationId: ctx.organizationId, status: 'ENDED' }, orderBy: { endedAt: 'desc' }, select: { id: true } })` (D-01 / A2 most-recent ENDED disambiguation), org-scoped, returning `{ assignmentId: string | null }` so a null result lets the UI disable the trigger with a reason. Leave the P2002 handler (:256-272) untouched — the deterministic per-assignment idempotencyKey is the UI's job (D-09). Do NOT intersect with ∩connected (CONTEXT D-05 verbatim — enabled∩signoff∩resolver-backed only; SLACK with no org-grid connection fails-closed at the step-runner and surfaces in run-view, RESEARCH Pitfall 4 / A4). Use `@contractor-ops/logger` (getIdpAuditLogger already in file) — no console.*.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test deprovisioning-start && pnpm --filter @contractor-ops/api test contractor-assignment-resolver 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "PROVIDERS_FOR_RUN" packages/api/src/routers/integrations/deprovisioning.ts | grep -v '^#'` shows the hardcoded const is gone (replaced by the derivation helper).
    - `grep -c "requirePermission({ idp: \['start_run'\]" packages/api/src/routers/integrations/deprovisioning.ts` ≥ 3 (eligibility, start, resolver).
    - `pnpm --filter @contractor-ops/api test deprovisioning-start` GREEN (multi-provider, empty-set throw, gate, per-assignment idempotency).
    - `pnpm --filter @contractor-ops/api test contractor-assignment-resolver` GREEN (most-recent ENDED + null path).
    - `pnpm typecheck --filter @contractor-ops/api` clean (RESOLVER_BACKED_PROVIDERS type wiring).
  </acceptance_criteria>
  <done>Dynamic derivation, empty-set throw, both procs gated, settings read added, resolver added; 81-01 INT-01 RED cases GREEN; api typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web-vite/IT_ADMIN → deprovisioning.startDeprovisioningRun | Untrusted UI request crosses into a destructive, irreversible provider-suspension mutation. |
| client → resolveAssignmentForContractor | contractorId from client must be re-scoped to the session org. |
| session → org settings / signoff flags | Provider set derivation must read org state from the session org, never client input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-02-01 | Elevation of Privilege | startDeprovisioningRun / getDeprovisioningEligibility (was UNGATED) | mitigate | Add `.use(requirePermission({ idp: ['start_run'] }))` to both procedures (D-10); grant only owner/admin/it_admin (Task 1). Closes the first-real-caller destructive-mutation gap. |
| T-81-02-02 | Information Disclosure (IDOR) | resolveAssignmentForContractor | mitigate | `where` includes `organizationId: ctx.organizationId` (session, never client); cross-tenant contractorId returns null (not another org's assignment). |
| T-81-02-03 | Tampering | Cross-tenant idempotency-key squat | mitigate | Unchanged 76-WR1 `@@unique([organizationId, idempotencyKey])` + the org-filtered P2002 handler (:256-272). Verified present locally in 81-01 (A5). |
| T-81-02-04 | Tampering | Provider-set derivation drift (a 4th hardcoded literal) | mitigate | Derive from the single exported `RESOLVER_BACKED_PROVIDERS` const driving `DeprovisionProvider` — no second copy can drift from the token-resolver capability (D-05). |
| T-81-02-05 | Denial of Service | Zero-step run on misconfigured org | mitigate | Empty derived set throws `DEPROVISIONING_INTEGRATION_NOT_CONFIGURED` (D-06) — no orphaned zero-step run. |
| T-81-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit — zero external packages this phase). |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/auth test roles` GREEN.
- `pnpm --filter @contractor-ops/api test deprovisioning-start` GREEN.
- `pnpm --filter @contractor-ops/api test contractor-assignment-resolver` GREEN.
- `pnpm typecheck --filter @contractor-ops/api` clean.
- Hardcoded `PROVIDERS_FOR_RUN` const removed; both procs + resolver gated by idp:start_run.
</verification>

<success_criteria>
INT-01 server seam closed: per-org dynamic provider derivation (enabled∩signoff∩resolver-backed) with empty-set throw, both procedures + the new resolver gated by `idp:start_run` (owner+admin+it_admin), org-settings read added, single-source `RESOLVER_BACKED_PROVIDERS`. All INT-01 server RED cases from 81-01 are GREEN.
</success_criteria>

<output>
Create `.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-02-SUMMARY.md` when done.
</output>
