---
phase: 81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/integrations/src/adapters/__tests__/slack-adapter.test.ts
autonomous: true
requirements:
  - IDP-04
  - IDP-08
user_setup: []

must_haves:
  truths:
    - "SlackAdapter.suspendAccount issues a SCIM PATCH active=false with the org-grid bearer token."
    - "SlackAdapter.revokeAllSessions calls admin.users.session.invalidate with the org-grid bearer token."
    - "SlackAdapter.describeImpact returns the Slack impact shape used by the deprovisioning run."
  artifacts:
    - path: "packages/integrations/src/adapters/__tests__/slack-adapter.test.ts"
      provides: "Regression coverage for the Slack deprovision execution path (suspend/revoke/impact)"
      contains: "admin.users.session.invalidate"
  key_links:
    - from: "slack-adapter.test.ts"
      to: "SlackAdapter.withOrgGridToken suspend/revoke/impact"
      via: "stubbed fetch asserting SCIM PATCH + admin.users.session.invalidate"
      pattern: "withOrgGridToken"
---

<objective>
Add the missing regression tests for the Slack deprovisioning execution path (D-08). The audit flagged Slack as "claimed complete, unverified"; research confirmed it executes E2E via the org-grid token but the suspend/revoke/impact methods are NOT unit-tested today. This plan adds those tests — no behavior change to the adapter.

Purpose: Lock the confirmed Slack leg (`{GOOGLE_WORKSPACE, SLACK}` resolver-backed set) against regression now that multi-provider runs (81-02) will actually create Slack steps.
Output: New `it(...)` cases in `slack-adapter.test.ts` asserting the SCIM-deactivate + session-invalidate calls fire with the org-grid bearer.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-CONTEXT.md
@.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-RESEARCH.md
@.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-PATTERNS.md

<interfaces>
<!-- Verified anchors — no exploration needed. -->

slack-adapter.ts (the deprovision execution path — implemented, NOT yet unit-tested):
- `:136` `withOrgGridToken(token)` — returns an adapter bound to the org-grid bearer (#orgGridToken).
- `:481-484` `suspendAccount` — SCIM PATCH `active=false`.
- `:516-520` `revokeAllSessions` — `admin.users.session.invalidate`.
- `:540` `describeImpact` — Slack impact shape.

slack-adapter.test.ts (current coverage — verified via describe/it grep):
- Covers ONLY getOAuthConfig / exchangeCodeForTokens / refreshToken / verifyWebhookSignature.
- The deprovision path is implemented but has ZERO test cases — these are NEW cases (not extends of an existing assertion).

idp-deprovisioning-step-runner.ts:204-216 — resolves `SlackAdapter().withOrgGridToken(token)` directly (not via the lazy registry); idp-token-resolver.ts:10 declares SLACK resolver-backed. This is the path the test guards.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Slack suspend/revoke/impact regression cases (D-08)</name>
  <files>packages/integrations/src/adapters/__tests__/slack-adapter.test.ts</files>
  <read_first>
    - packages/integrations/src/adapters/__tests__/slack-adapter.test.ts (full — existing fetch-stub pattern for OAuth/webhook cases)
    - packages/integrations/src/adapters/slack-adapter.ts (:136 withOrgGridToken, :481-484 suspendAccount SCIM active=false, :516-520 revokeAllSessions admin.users.session.invalidate, :540 describeImpact)
  </read_first>
  <behavior>
    - suspendAccount: stubbed fetch receives a SCIM PATCH with body active=false and Authorization: Bearer <org-grid token>
    - revokeAllSessions: stubbed fetch receives a POST to admin.users.session.invalidate with the org-grid bearer
    - describeImpact: returns the expected Slack impact shape (provider=SLACK, action descriptors)
  </behavior>
  <action>
    Mirror the existing fetch-stub pattern in slack-adapter.test.ts (the OAuth/webhook cases) to add three NEW `it(...)` cases against `new SlackAdapter().withOrgGridToken('<org-grid-token>')`: (1) `suspendAccount` issues a SCIM PATCH whose request body sets `active: false` and whose `Authorization` header carries the org-grid bearer (assert URL + method + parsed body + header on the stubbed fetch). (2) `revokeAllSessions` calls `admin.users.session.invalidate` (assert the endpoint + org-grid bearer). (3) `describeImpact` returns the Slack impact shape (assert provider/action descriptors). Stub `fetch` (or the adapter's HTTP client) per the file's existing convention; do NOT hit a real network. This is a regression test only — make NO change to slack-adapter.ts (D-08 = confirm + lock, no behavior change). Never log the org-grid token raw.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/integrations test slack-adapter 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `slack-adapter.test.ts` contains the three new cases referencing `withOrgGridToken`, a SCIM PATCH `active=false`, and `admin.users.session.invalidate`.
    - `pnpm --filter @contractor-ops/integrations test slack-adapter` GREEN.
    - `git diff --stat packages/integrations/src/adapters/slack-adapter.ts` shows NO change to the adapter source (test-only).
  </acceptance_criteria>
  <done>Three Slack deprovision regression cases added and GREEN; adapter source unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter → Slack SCIM/admin API | org-grid bearer token crosses to the external Slack API; must never be logged raw. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-04-01 | Information Disclosure | org-grid token in test logs/fixtures | mitigate | Use a placeholder token in the stub; assert the bearer is sent without printing it; no real credential in the test. |
| T-81-04-02 | Spoofing | Test hits real Slack API | mitigate | `fetch`/HTTP client is stubbed per the file convention; no network egress. |
| T-81-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit — zero external packages this phase). |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/integrations test slack-adapter` GREEN.
- Adapter source `slack-adapter.ts` unchanged (test-only diff).
</verification>

<success_criteria>
The Slack deprovision execution path (suspend SCIM active=false / revoke session-invalidate / describeImpact) has regression coverage asserting the org-grid bearer is used, with no change to the adapter (D-08 confirmed + locked).
</success_criteria>

<output>
Create `.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-04-SUMMARY.md` when done.
</output>
