---
phase: 81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces
plan: 06
type: tdd
wave: 3
depends_on:
  - 81-02
  - 81-03
  - 81-05
files_modified:
  - packages/api/src/__tests__/81-int-closure.test.ts
autonomous: true
requirements:
  - IDP-01
  - IDP-09
  - COMPL-07
  - COMPL-08
  - COMPL-11
user_setup: []

must_haves:
  truths:
    - "E2E flow 1 composes: offboarding ACCESS_REVOKE → resolve assignment → startDeprovisioningRun → multi-provider run created with steps fanned out."
    - "E2E flow 2 composes: portal upload → admin approveUploadReplacement → onComplianceItemSatisfied → held flow PENDING_COMPLIANCE→PENDING → payment gate releases."
    - "Both flows were previously broken (INT-01 unreachable, INT-02 unwired); the composition test now proves they complete end-to-end."
  artifacts:
    - path: "packages/api/src/__tests__/81-int-closure.test.ts"
      provides: "Cross-feature composition asserting both E2E integration flows complete"
      contains: "startDeprovisioningRun"
  key_links:
    - from: "81-int-closure.test.ts flow 1"
      to: "resolveAssignmentForContractor → startDeprovisioningRun"
      via: "createCaller composition"
      pattern: "resolveAssignmentForContractor"
    - from: "81-int-closure.test.ts flow 2"
      to: "approveUploadReplacement → payment gate release"
      via: "createCaller composition + assertContractorPaymentEligibility"
      pattern: "approveUploadReplacement"
---

<objective>
Add the binding E2E composition test for BOTH integration flows the milestone audit flagged as broken. The existing `v6-cross-feature-composition.test.ts` deliberately excluded F2 (IdP deprovisioning) at lines 28-30; this fills that gap with a dedicated `81-int-closure.test.ts` exercising the full seams now wired by 81-02/81-03/81-05.

Purpose: Per-phase verification could not see these cross-phase breaks; this composition proves the seams compose end-to-end (CONTEXT deferred-note — both flows MUST be test-covered). This is the phase gate.
Output: `81-int-closure.test.ts` with two composition cases, both GREEN.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-CONTEXT.md
@.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-RESEARCH.md
@.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-VALIDATION.md

<interfaces>
<!-- Verified harness + seam anchors — no exploration needed. -->

Composition harness:
- v6-cross-feature-composition.test.ts:28-30 — comment recording F2 was DELIBERATELY excluded (the gap this plan fills).
- deprovisioning-start.test.ts:5-90 — `vi.hoisted` mockPrisma store; `$transaction: vi.fn(async fn => fn(mockPrisma))`; `vi.mock('@contractor-ops/auth')` hasPermission `{ success: true }`; `createCaller = createCallerFactory(appRouter)`; makeCaller session (user.role + activeOrganizationId).
- QStash `publishJSON` is mocked (spy) — fan-out is asserted via the spy / created steps, not a real network call.

Flow 1 seam (INT-01, 81-02 + 81-05):
- resolveAssignmentForContractor({ contractorId }) → { assignmentId }
- startDeprovisioningRun({ assignmentId, idempotencyKey }) → run with steps for the derived providers (org settingsJson enabled both GOOGLE_WORKSPACE + SLACK + signoff → 2-provider steps); status resolves COMPLETED/PARTIAL_FAILURE/FAILED (IDP-09).

Flow 2 seam (INT-02, 81-03):
- approveUploadReplacement({ itemId, documentId, expiresAt }) → item SATISFIED + onComplianceItemSatisfied in-tx → held ApprovalFlow PENDING_COMPLIANCE → PENDING → assertContractorPaymentEligibility no longer blocks.
- mock store needs $queryRaw (held flows) + approvalFlow.update + the payment-gate read.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compose both E2E integration flows (81-int-closure)</name>
  <files>packages/api/src/__tests__/81-int-closure.test.ts</files>
  <read_first>
    - packages/api/src/__tests__/v6-cross-feature-composition.test.ts (:28-30 the F2-excluded comment; full — harness composition style)
    - packages/api/src/__tests__/deprovisioning-start.test.ts (hoisted mockPrisma store, $transaction mock, makeCaller, publishJSON spy)
    - packages/api/src/__tests__/compliance-upload-review.test.ts (approve harness + recovery mock store from 81-01)
    - packages/api/src/services/compliance-payment-gate.ts (assertContractorPaymentEligibility — the release the flow 2 assertion checks)
  </read_first>
  <behavior>
    - Flow 1: given a contractor with a most-recent ENDED assignment and an org with GOOGLE_WORKSPACE+SLACK enabled+signoff, resolveAssignmentForContractor returns the assignmentId and startDeprovisioningRun creates a run whose steps cover BOTH providers; the QStash publishJSON spy is invoked per step (no Promise.allSettled aggregation, IDP-09).
    - Flow 2: given a held PENDING_COMPLIANCE ApprovalFlow whose complianceHoldsJson contains an item, approveUploadReplacement flips the item SATISFIED and the held flow resumes to PENDING; a subsequent assertContractorPaymentEligibility for that contractor no longer blocks on this item.
    - Both flows assert the post-closure state — they would FAIL against pre-81 code (INT-01 ungated/GWS-only/no resolver; INT-02 no recovery call).
  </behavior>
  <action>
    Create 81-int-closure.test.ts composing both seams with the verified hoisted-prisma + createCaller harness (mirror deprovisioning-start.test.ts + compliance-upload-review.test.ts). Flow 1 (INT-01): seed a contractor with a most-recent ENDED ContractorAssignment + an org `settingsJson.idpDeprovisioningEnabled = { GOOGLE_WORKSPACE: true, SLACK: true }` with signoff satisfied; call `resolveAssignmentForContractor({ contractorId })`, derive the deterministic idempotencyKey from the returned assignmentId, call `startDeprovisioningRun({ assignmentId, idempotencyKey })`, and assert the created run has steps for BOTH providers and the QStash `publishJSON` spy fired per step (IDP-09 independent jobs). Flow 2 (INT-02): seed a held `ApprovalFlow` (status PENDING_COMPLIANCE) whose `complianceHoldsJson` contains the item, plus a PENDING_REVIEW document; call `approveUploadReplacement(...)` and assert (a) the item is SATISFIED, (b) the held flow resumed to PENDING (recovery fired in-tx), (c) a follow-up `assertContractorPaymentEligibility([contractorId], { tx, throwOnFail: false })` returns not-blocked for that item. Add `$queryRaw` + `approvalFlow.update` + the payment-gate read to the mock store. Reference the v6-cross-feature-composition.test.ts:28-30 exclusion in a top comment noting this file closes that gap. caller session uses an owner/admin/it_admin role with `idp:start_run` (hasPermission → success). Scoped run only.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test 81-int-closure 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `81-int-closure.test.ts` contains two composition cases referencing `resolveAssignmentForContractor` + `startDeprovisioningRun` (flow 1) and `approveUploadReplacement` + `assertContractorPaymentEligibility` (flow 2).
    - Flow 1 asserts BOTH providers create steps and the publishJSON spy fired per step.
    - Flow 2 asserts item SATISFIED + held flow PENDING and the payment gate no longer blocks.
    - `pnpm --filter @contractor-ops/api test 81-int-closure` GREEN.
    - `pnpm --filter @contractor-ops/api test` (full api suite) GREEN at the phase gate (no regressions from 81-02/81-03).
  </acceptance_criteria>
  <done>Both E2E integration flows composed and GREEN; the F2 composition gap is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Composition test exercising already-gated seams; no new runtime boundary. Asserts the RBAC + tenant + atomicity boundaries that 81-02/81-03 enforce. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-06-01 | Elevation of Privilege | Composition relies on the idp:start_run gate | mitigate | Flow 1 caller holds `idp:start_run`; a companion assertion (already in 81-01/81-02) covers the deny path. This test proves the authorized happy path composes end-to-end. |
| T-81-06-02 | Tampering | Idempotency double-start | mitigate | Flow 1 uses the deterministic per-assignment idempotencyKey; the composition proves a single run is created (no duplicate). |
| T-81-06-03 | Denial of Service | Recovery rollback breaks the chain | mitigate | Flow 2 asserts the approval + recovery commit atomically and the payment gate releases — proves D-14 atomicity holds in composition. |
| T-81-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH Package Legitimacy Audit — zero external packages this phase). |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test 81-int-closure` GREEN (both flows).
- `pnpm --filter @contractor-ops/api test` full suite GREEN (no regressions).
- Phase gate (per 81-VALIDATION.md): both E2E flows green + `pnpm lint:ci` (check:web-vite-data-layer + i18n parity) before /gsd:verify-work.
</verification>

<success_criteria>
Both source-confirmed E2E integration flows ("offboarding ACCESS_REVOKE → IdP deprovisioning run" and "portal upload → admin approve → payment unblock") are exercised by a composition test and GREEN — closing the F2 composition gap left in v6-cross-feature-composition.test.ts.
</success_criteria>

<output>
Create `.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-06-SUMMARY.md` when done.
</output>
