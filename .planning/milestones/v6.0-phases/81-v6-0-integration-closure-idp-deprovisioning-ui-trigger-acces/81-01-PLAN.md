---
phase: 81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/api/src/__tests__/deprovisioning-start.test.ts
  - packages/api/src/__tests__/compliance-upload-review.test.ts
  - packages/api/src/__tests__/contractor-assignment-resolver.test.ts
  - packages/auth/src/__tests__/roles.test.ts
autonomous: true
requirements:
  - IDP-01
  - IDP-05
  - IDP-06
  - IDP-09
  - IDP-10
  - COMPL-07
  - COMPL-08
  - COMPL-11
user_setup: []

must_haves:
  truths:
    - "Both E2E seams have RED unit tests asserting the post-wiring behavior before any source is touched."
    - "roles.test.ts asserts owner+admin+it_admin hold idp:start_run and only owner/admin hold idp:override_step_failure."
    - "The 76-WR1 per-org unique index on DeprovisioningRun is confirmed present in the local schema before any P2002 idempotency test relies on it."
  artifacts:
    - path: "packages/api/src/__tests__/deprovisioning-start.test.ts"
      provides: "RED cases — multi-provider derivation, empty-set throw (D-06), idp:start_run gate (D-10), per-assignment idempotency key (D-09)"
      contains: "DEPROVISIONING_INTEGRATION_NOT_CONFIGURED"
    - path: "packages/api/src/__tests__/compliance-upload-review.test.ts"
      provides: "RED cases — onComplianceItemSatisfied fires (D-12) + notification-failure isolation (D-14)"
      contains: "onComplianceItemSatisfied"
    - path: "packages/api/src/__tests__/contractor-assignment-resolver.test.ts"
      provides: "RED case — most-recent ENDED assignment disambiguation (D-01)"
      contains: "status: 'ENDED'"
    - path: "packages/auth/src/__tests__/roles.test.ts"
      provides: "Updated idp invariants for the start_run action grant"
      contains: "start_run"
  key_links:
    - from: "deprovisioning-start.test.ts"
      to: "deprovisioning.startDeprovisioningRun"
      via: "createCaller + mocked hasPermission"
      pattern: "startDeprovisioningRun"
    - from: "compliance-upload-review.test.ts"
      to: "onComplianceItemSatisfied"
      via: "mocked $queryRaw + approvalFlow.update assertion"
      pattern: "onComplianceItemSatisfied"
---

<objective>
Create/extend the failing unit-test scaffolds for BOTH integration seams (INT-01 IdP-trigger server, INT-02 compliance recovery), update the auth `roles.test.ts` invariants for the new `idp:start_run` action, and verify the 76-WR1 idempotency index exists locally. This is the Wave 0 RED gate per 81-VALIDATION.md — every behavior the Wave 1 source plans implement must have a test asserting it first.

Purpose: Establish the Nyquist verification surface so Wave 1 source changes turn RED → GREEN, and lock the `roles.test.ts` invariant rewrite into the same milestone (Pitfall 1 — adding `idp:start_run` reds the existing exact-equality assertions).
Output: 3 extended/new api test files + 1 updated auth test file, all RED (awaiting Wave 1 source), plus a confirmed-present local DB index.
</objective>

<execution_context>
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-CONTEXT.md
@.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-VALIDATION.md
@.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-PATTERNS.md

<interfaces>
<!-- Verified harness shapes the executor uses directly — no codebase exploration needed. -->

Server-caller harness (deprovisioning-start.test.ts:5-90, compliance-upload-review.test.ts:1-60):
- `vi.hoisted(() => ({ mockPrisma, ... }))` builds an in-memory store.
- `$transaction: vi.fn(async fn => fn(mockPrisma))`.
- `vi.mock('@contractor-ops/auth', ...)` with `authApi.hasPermission` resolving `{ success: true | false }` (flip to false for the D-10 reject case).
- `vi.mock('@contractor-ops/db', ...)` returns `mockPrisma`; noop `@contractor-ops/logger` mock.
- `createCaller = createCallerFactory(appRouter)`; `makeCaller` builds a session with `user.role` + `activeOrganizationId`.
- Existing P2002 case is at deprovisioning-start.test.ts:227 — extend it for a per-assignment idempotencyKey (D-09).

roles.test.ts current invariants (MUST be rewritten — Pitfall 1):
- `:75-78` asserts `roles.owner.statements.idp` / `roles.admin.statements.idp` `toEqual(['override_step_failure'])` (exact).
- `:80-86` asserts every non-owner/admin role has `statements.idp` `toBeUndefined()`.
- `:100-103` asserts `it_admin cannot read invoices or contractors` (must still hold).

Hook signature being tested (compliance-recovery.ts:43):
`onComplianceItemSatisfied(tx, { itemId, contractorId, organizationId }): Promise<{ resumedFlowIds: string[] }>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend INT-01 server test scaffolds (deprovisioning-start) + new resolver test (RED)</name>
  <files>packages/api/src/__tests__/deprovisioning-start.test.ts, packages/api/src/__tests__/contractor-assignment-resolver.test.ts</files>
  <read_first>
    - packages/api/src/__tests__/deprovisioning-start.test.ts (full — hoisted mockPrisma store, $transaction mock, existing P2002 case :227, makeCaller session shape)
    - packages/api/src/routers/integrations/deprovisioning.ts (:53 isProviderSignoffSatisfied, :68 PROVIDERS_FOR_RUN, :99 getDeprovisioningEligibility, :147 startDeprovisioningRun, :200 step create, :256-272 P2002 handler)
    - packages/api/src/services/idp-token-resolver.ts (:10 DeprovisionProvider union — authoritative resolver-backed set)
    - packages/db/prisma/schema/contractor.prisma (:161-202 ContractorAssignment, :322-326 AssignmentStatus enum = ACTIVE|ENDED|PLANNED)
  </read_first>
  <action>
    In deprovisioning-start.test.ts add RED cases that will pass once 81-02 lands: (a) multi-provider derivation — org with `settingsJson.idpDeprovisioningEnabled = { GOOGLE_WORKSPACE: true, SLACK: true }` + signoff satisfied creates steps for BOTH providers; org with only GOOGLE_WORKSPACE enabled creates GWS-only steps (assert distinct `provider` values in the `steps.create` payload captured on `mockPrisma.deprovisioningRun.create`). (b) D-06 empty-set — org with no enabled+signoff+resolver-backed provider makes `startDeprovisioningRun` reject with TRPCError code `PRECONDITION_FAILED` and message `DEPROVISIONING_INTEGRATION_NOT_CONFIGURED`; assert NO run is created. (c) D-10 gate — mock `authApi.hasPermission` to resolve `{ success: false }` and assert both `startDeprovisioningRun` AND `getDeprovisioningEligibility` reject with FORBIDDEN. (d) D-09 per-assignment key — assert the existing P2002 case (:227) uses an idempotencyKey deterministically derived from `assignmentId` and returns the EXISTING run rather than throwing. Add the `organization.findUnique` read to the mock store (returns `{ settingsJson }`) since 81-02 will add that read. Create contractor-assignment-resolver.test.ts (new) using the same hoisted-prisma pattern: seed a contractor with two ENDED assignments (different `endedAt`) + one ACTIVE; assert the new resolver procedure returns the most-recent ENDED assignment id (D-01 / A2 disambiguation), and returns null/NOT_FOUND when the contractor has no ENDED assignment. Tests assert the post-wiring contract; they are EXPECTED to RED until 81-02. No fenced implementation in source — follow the existing harness mocks.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test deprovisioning-start 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `deprovisioning-start.test.ts` contains the four new `it(...)` cases referencing `DEPROVISIONING_INTEGRATION_NOT_CONFIGURED`, multi-provider steps, the `hasPermission → false` FORBIDDEN gate, and a per-assignment idempotencyKey.
    - `contractor-assignment-resolver.test.ts` exists and asserts `status: 'ENDED'` + `orderBy endedAt desc` selection plus the no-ENDED null path.
    - The run prints RED on the new behaviors (source not yet wired) — failing assertions reference the new procedures/derivation, NOT a harness/compile error. Compile/import errors are NOT acceptable RED.
  </acceptance_criteria>
  <done>Both files contain RED cases for D-05/D-06/D-09/D-10 (deprovisioning-start) and D-01 (resolver); failures are assertion-level, not harness-level.</done>
</task>

<task type="auto">
  <name>Task 2: Extend INT-02 recovery test scaffold (compliance-upload-review) (RED)</name>
  <files>packages/api/src/__tests__/compliance-upload-review.test.ts</files>
  <read_first>
    - packages/api/src/__tests__/compliance-upload-review.test.ts (full — existing approveUploadReplacement harness + mock store)
    - packages/api/src/routers/compliance/compliance-admin.ts (:236 approveUploadReplacement, :246-312 $transaction, :284-291 SATISFIED flip, :296-310 audit, :311 return, :314 post-tx dispatchComplianceUploadOutcome)
    - packages/api/src/services/compliance-recovery.ts (:29-35 RecoveryClient, :43-57 onComplianceItemSatisfied — $queryRaw bound ::jsonb param + approvalFlow.update)
    - packages/api/src/routers/compliance/classification.ts (:91-103 releaseHeldApprovalsForContractor call shape to mirror)
  </read_first>
  <action>
    Add RED cases that pass once 81-03 lands. (a) D-12 recovery fires — seed a held `ApprovalFlow` with `status: 'PENDING_COMPLIANCE'` whose `complianceHoldsJson` contains `input.itemId`; approve the upload; assert `onComplianceItemSatisfied` ran inside the tx (assert via `mockPrisma.approvalFlow.update` called to flip that flow PENDING_COMPLIANCE → PENDING and clear holds). Add `$queryRaw` (returns the held-flow rows) + `approvalFlow.update` to the hoisted mock store. (b) D-14 isolation — make the post-tx `dispatchComplianceUploadOutcome` reject (mock it to throw); assert the approval STILL commits (item is SATISFIED, run returns normally) and the recovery flip is NOT rolled back — i.e. a notification failure does not undo the approval. Use the existing approve harness session shape. Tests assert the post-wiring contract and are EXPECTED to RED until 81-03 (the recovery call is not yet imported/called in compliance-admin.ts). No fenced source — extend the existing mocks.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/api test compliance-upload-review 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `compliance-upload-review.test.ts` references `onComplianceItemSatisfied` and asserts `approvalFlow.update` flips a held flow to PENDING within the approve tx.
    - A D-14 case mocks `dispatchComplianceUploadOutcome` to throw and asserts the approval/recovery is NOT rolled back.
    - Mock store includes `$queryRaw` + `approvalFlow.update`. Failures are assertion-level (recovery not yet wired), not harness/compile errors.
  </acceptance_criteria>
  <done>Two RED cases (D-12 fires, D-14 isolation) present; failures are assertion-level.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite roles.test.ts idp invariants + verify 76-WR1 local index</name>
  <files>packages/auth/src/__tests__/roles.test.ts</files>
  <read_first>
    - packages/auth/src/__tests__/roles.test.ts (:26-37 owner-vs-statement equality, :75-78 owner/admin idp exact equality, :80-86 "no other role holds idp", :100-103 it_admin cannot read invoices/contractors)
    - packages/auth/src/permissions.ts (:25 existing idp action array)
    - packages/auth/src/roles.ts (:28 allPermissions.idp, :58 admin.idp, :124-133 it_admin block — no idp key today)
    - packages/db/prisma/schema/idp-deprovisioning.prisma (:8-61 DeprovisioningRun, @@unique([organizationId, idempotencyKey]) — 76-WR1)
  </read_first>
  <action>
    Update the three idp invariants to the post-wiring shape (these RED until 81-02 lands roles.ts/permissions.ts): (a) :75-78 — change the owner/admin `idp` equality to expect `['override_step_failure', 'start_run']` (sort-tolerant compare, do not assume array order). (b) :80-86 — rewrite the "no role other than owner/admin holds idp" invariant to allow `it_admin` to hold EXACTLY `['start_run']`, assert `it_admin` does NOT hold `override_step_failure`, and keep every OTHER role's `statements.idp` `toBeUndefined()`. (c) verify the :26-37 owner-vs-full-statement equality still passes with the 2-action idp group (the sort-compare at :35 already handles it). Leave :100-103 (it_admin cannot read invoices/contractors) unchanged — it_admin gains ONLY `idp:start_run`. Then verify the 76-WR1 idempotency index exists locally: run a read-only Prisma/psql check (or `pnpm --filter @contractor-ops/db ...` describe) that confirms a unique index on `DeprovisioningRun(organizationId, idempotencyKey)` exists in the local DB; if absent, document it in the SUMMARY as a setup blocker for the E2E P2002 case (do NOT apply migrations without approval — git-safety). This addresses RESEARCH open question A5.
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/auth test roles 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `roles.test.ts` asserts owner+admin hold `['override_step_failure', 'start_run']` (sort-tolerant) and `it_admin` holds EXACTLY `['start_run']` with NO `override_step_failure`.
    - All other roles still assert `statements.idp` `toBeUndefined()`; the it_admin invoice/contractor exclusion (:100-103) is unchanged.
    - The run REDs only on the start_run grant assertions (roles.ts not yet changed) — assertion-level, not compile error.
    - SUMMARY records whether the `DeprovisioningRun(organizationId, idempotencyKey)` unique index is present locally (A5).
  </acceptance_criteria>
  <done>roles.test.ts invariants rewritten for idp:start_run (RED until 81-02); local 76-WR1 index presence recorded in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only creates/edits test files + a read-only DB index check. No runtime trust boundary is introduced; tests assert the boundaries that Wave 1 implements. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01-01 | Tampering | 76-WR1 unique index absent locally → idempotency P2002 path untested | mitigate | Task 3 verifies the `@@unique([organizationId, idempotencyKey])` index exists locally before any P2002 test (E2E in 81-06) relies on it; absence is recorded as a setup blocker (RESEARCH A5). |
| T-81-01-02 | Elevation of Privilege | Test asserts the `idp:start_run` gate that 81-02 enforces | accept | Test-only plan; the actual gate ships in 81-02. This plan locks the assertion so a missing gate reds CI. |
| T-81-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (test edits only); RESEARCH Package Legitimacy Audit confirms zero external packages this phase. |
</threat_model>

<verification>
- `pnpm --filter @contractor-ops/api test deprovisioning-start` — new RED cases present, assertion-level failures.
- `pnpm --filter @contractor-ops/api test compliance-upload-review` — new RED cases present, assertion-level failures.
- `pnpm --filter @contractor-ops/api test contractor-assignment-resolver` — new RED case present.
- `pnpm --filter @contractor-ops/auth test roles` — idp invariants RED on start_run grant.
- SUMMARY records 76-WR1 local index presence (A5).
</verification>

<success_criteria>
All four test files carry RED cases for the exact behaviors 81-02/81-03 implement; failures are assertion-level (not harness/compile); the 76-WR1 index presence is recorded. No source/runtime files modified.
</success_criteria>

<output>
Create `.planning/phases/81-v6-0-integration-closure-idp-deprovisioning-ui-trigger-acces/81-01-SUMMARY.md` when done.
</output>
