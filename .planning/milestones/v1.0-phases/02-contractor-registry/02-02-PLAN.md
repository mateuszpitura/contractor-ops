---
phase: 02-contractor-registry
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/web/src/app/[locale]/(dashboard)/contractors/page.tsx
  - apps/web/src/components/contractors/contractor-table/columns.tsx
  - apps/web/src/components/contractors/contractor-table/data-table.tsx
  - apps/web/src/components/contractors/contractor-table/data-table-toolbar.tsx
  - apps/web/src/components/contractors/contractor-table/data-table-pagination.tsx
  - apps/web/src/components/contractors/contractor-table/data-table-column-toggle.tsx
  - apps/web/src/components/contractors/contractor-table/data-table-bulk-actions.tsx
  - apps/web/src/components/contractors/contractor-table/use-contractor-filters.ts
  - apps/web/src/components/contractors/contractor-side-panel.tsx
  - apps/web/src/components/contractors/contractor-wizard/wizard-dialog.tsx
  - apps/web/src/components/contractors/contractor-wizard/step-company.tsx
  - apps/web/src/components/contractors/contractor-wizard/step-billing.tsx
  - apps/web/src/components/contractors/contractor-wizard/step-assignment.tsx
  - apps/web/src/components/contractors/compliance-health-badge.tsx
  - apps/web/messages/en.json
  - apps/web/messages/pl.json
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05, CONT-06]

must_haves:
  truths:
    - "User can see a paginated, sortable contractor list with 12 columns and configurable column visibility"
    - "User can search contractors by name, company, NIP, or email with instant results"
    - "User can filter contractors by status, owner, team, billing model, contract end date, compliance health"
    - "User can select rows and perform bulk actions: assign owner, export CSV/XLSX, archive"
    - "User can click a row to open a slide-out side panel with contractor summary"
    - "User can add a contractor via 3-step wizard with GUS NIP autofill"
  artifacts:
    - path: "apps/web/src/app/[locale]/(dashboard)/contractors/page.tsx"
      provides: "Contractor list page (server component shell)"
    - path: "apps/web/src/components/contractors/contractor-table/data-table.tsx"
      provides: "TanStack Table wrapper with server-side data fetching"
    - path: "apps/web/src/components/contractors/contractor-wizard/wizard-dialog.tsx"
      provides: "Multi-step add contractor dialog"
    - path: "apps/web/src/components/contractors/contractor-side-panel.tsx"
      provides: "Slide-out Sheet panel with contractor summary"
  key_links:
    - from: "apps/web/src/components/contractors/contractor-table/data-table.tsx"
      to: "packages/api/src/routers/contractor.ts"
      via: "trpc.contractor.list.queryOptions"
      pattern: "trpc\\.contractor\\.list"
    - from: "apps/web/src/components/contractors/contractor-wizard/wizard-dialog.tsx"
      to: "packages/api/src/routers/contractor.ts"
      via: "trpc.contractor.create.mutationOptions"
      pattern: "trpc\\.contractor\\.create"
    - from: "apps/web/src/components/contractors/contractor-table/use-contractor-filters.ts"
      to: "nuqs"
      via: "useQueryStates for URL state"
      pattern: "useQueryStates"
---

<objective>
Build the contractor list page with TanStack Table (server-side pagination, sorting, filtering, full-text search), row selection with bulk actions, slide-out side panel, and 3-step add contractor wizard with GUS NIP autofill. All copy in Polish and English per UI-SPEC.

Purpose: This is the primary contractor management interface. Users will spend most of their time on this view.
Output: Fully functional contractor list page at `/contractors` with all interactions from UI-SPEC.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-contractor-registry/02-CONTEXT.md
@.planning/phases/02-contractor-registry/02-RESEARCH.md
@.planning/phases/02-contractor-registry/02-UI-SPEC.md
@.planning/phases/02-contractor-registry/02-01-SUMMARY.md

<interfaces>
<!-- Key types from Plan 01 that this plan consumes -->

From packages/validators/src/contractor.ts (created in Plan 01):
```typescript
export const contractorCreateSchema: z.ZodObject<{
  legalName, displayName, type, taxId, vatId, registrationNumber,
  email, phone, countryCode, currency, addressLine1, addressLine2,
  city, postalCode, billingModel, rateValueGrosze, bankAccount,
  paymentTermsDays, ownerUserId, primaryTeamId, primaryProjectId,
  defaultCostCenterId
}>;
export const contractorListSchema: z.ZodObject<{
  page, pageSize, search, sortBy, sortOrder, filters
}>;
export const gusLookupSchema: z.ZodObject<{ nip: string }>;
export function isValidNip(nip: string): boolean;
```

tRPC procedures available (from Plan 01):
```typescript
trpc.contractor.list.queryOptions({ page, pageSize, search, sortBy, sortOrder, filters })
// Returns: { items: ContractorRow[], total: number, page: number, pageSize: number }

trpc.contractor.create.mutationOptions(...)
trpc.contractor.gusLookup.mutationOptions(...)
trpc.contractor.bulkArchive.mutationOptions(...)
trpc.contractor.bulkAssignOwner.mutationOptions(...)
trpc.contractor.export.mutationOptions(...)
```

Existing UI patterns (from Phase 1):
```typescript
// apps/web/src/components/settings/users-table.tsx
const membersQuery = useQuery(trpc.user.list.queryOptions());
const mutation = useMutation(trpc.user.invite.mutationOptions({ onSuccess, onError }));
// Uses useTranslations("Users") for i18n
// Uses usePermissions().can("member", ["update"]) for RBAC
```

Existing shadcn components: table, badge, card, dialog, sheet, button, input, label, select,
skeleton, tabs, avatar, dropdown-menu, tooltip, sonner, separator, breadcrumb, command, alert-dialog
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, add shadcn components, create contractor list page with TanStack Table, search, filters, and pagination</name>
  <files>
    apps/web/src/app/[locale]/(dashboard)/contractors/page.tsx
    apps/web/src/components/contractors/contractor-table/columns.tsx
    apps/web/src/components/contractors/contractor-table/data-table.tsx
    apps/web/src/components/contractors/contractor-table/data-table-toolbar.tsx
    apps/web/src/components/contractors/contractor-table/data-table-pagination.tsx
    apps/web/src/components/contractors/contractor-table/data-table-column-toggle.tsx
    apps/web/src/components/contractors/contractor-table/data-table-bulk-actions.tsx
    apps/web/src/components/contractors/contractor-table/use-contractor-filters.ts
    apps/web/src/components/contractors/compliance-health-badge.tsx
    apps/web/src/components/contractors/contractor-side-panel.tsx
    apps/web/messages/en.json
    apps/web/messages/pl.json
  </files>
  <read_first>
    apps/web/src/components/settings/users-table.tsx
    apps/web/src/hooks/use-permissions.ts
    apps/web/src/app/[locale]/(dashboard)/layout.tsx
    apps/web/src/app/[locale]/(dashboard)/page.tsx
    apps/web/src/components/layout/top-bar.tsx
    apps/web/src/lib/navigation.ts
    apps/web/messages/en.json
    apps/web/messages/pl.json
    apps/web/src/trpc/init.ts
    .planning/phases/02-contractor-registry/02-UI-SPEC.md
    .planning/phases/02-contractor-registry/02-RESEARCH.md
  </read_first>
  <action>
**Step A: Install dependencies and add shadcn components**

```bash
cd /Users/mateusz.pitura/Repos/projects/contractor-ops
pnpm --filter @contractor-ops/web add @tanstack/react-table nuqs
npx shadcn@latest add checkbox popover calendar progress scroll-area radio-group collapsible --cwd apps/web --yes
```

**Step B: Create compliance-health-badge.tsx**

Reusable badge component at `apps/web/src/components/contractors/compliance-health-badge.tsx`:
- Props: `health: "green" | "yellow" | "red"`, optional `size: "sm" | "md"`
- Green: `bg-green-600/10 text-green-600` with CheckCircle icon
- Yellow: `bg-amber-500/10 text-amber-600` with AlertTriangle icon
- Red: `bg-red-500/10 text-red-500` with XCircle icon
- Pill shape: rounded-full, px-2 py-0.5, text-xs font-medium, flex items-center gap-1
- Uses `useTranslations("Contractors")` for labels: "Healthy", "Warning", "Critical" / "Zdrowy", "Ostrzezenie", "Krytyczny"

**Step C: Create use-contractor-filters.ts**

URL state management hook using `nuqs`:
```typescript
import { parseAsInteger, parseAsString, parseAsArrayOf, useQueryStates } from "nuqs";

export function useContractorFilters() {
  return useQueryStates({
    page: parseAsInteger.withDefault(1),
    pageSize: parseAsInteger.withDefault(25),
    search: parseAsString.withDefault(""),
    sortBy: parseAsString.withDefault("createdAt"),
    sortOrder: parseAsString.withDefault("desc"),
    status: parseAsArrayOf(parseAsString).withDefault([]),
    lifecycleStage: parseAsArrayOf(parseAsString).withDefault([]),
    owner: parseAsArrayOf(parseAsString).withDefault([]),
    team: parseAsArrayOf(parseAsString).withDefault([]),
    billingModel: parseAsArrayOf(parseAsString).withDefault([]),
    health: parseAsArrayOf(parseAsString).withDefault([]),
  });
}
```

**Step D: Create column definitions (columns.tsx)**

Define all 12 columns per UI-SPEC + select checkbox column:
1. Select (checkbox, enableSorting: false, enableHiding: false)
2. Name/Company — displayName as primary, legalName as secondary (text-xs text-muted-foreground)
3. Type — Badge with `--secondary` background
4. Status — Badge with lifecycle stage colors from UI-SPEC (Draft=muted, Onboarding=info, Active=success, Offboarding=warning, Inactive=muted, Archived=muted/50%)
5. Owner — Avatar + name, or "—" if null
6. Billing model — from customFieldsJson
7. Rate — formatted with currency, monospace font
8. Currency — 3-letter code
9. Next invoice expected — date or "—"
10. Team/Project — team name, or "—"
11. Contract end date — date or "—"
12. Last activity — relative time (e.g., "2 hours ago") or "—"
13. Compliance health — ComplianceHealthBadge component

All column headers use `t("columns.{key}")` from i18n. Accept a `t` function parameter.

**Step E: Create data-table.tsx (TanStack Table wrapper)**

The main table component:
- Uses `useReactTable` with `manualPagination: true`, `manualSorting: true`, `manualFiltering: true`
- Fetches data via `useQuery(trpc.contractor.list.queryOptions(filterParams))` where filterParams come from useContractorFilters
- Passes `pageCount` computed from `Math.ceil(total / pageSize)`
- Row selection enabled via `enableRowSelection: true`
- Column visibility state stored in localStorage key `contractor-table-columns`
- On row click (NOT on checkbox click), call `onRowClick(row.original)` to open side panel
- Loading state: 8 skeleton rows with column-width-matched bars (follow users-table.tsx pattern)
- Empty state: centered with Users icon, heading "No contractors yet" / "Brak kontrahentow", body text, "Add contractor" CTA button
- No search results state: "No contractors found" / "Nie znaleziono kontrahentow" with "Clear filters" CTA
- Wrap in `<div className="rounded-xl border bg-background">`

**Step F: Create data-table-toolbar.tsx**

Toolbar above the table:
- Search input with Search icon, placeholder from UI-SPEC, 300ms debounce, min 2 chars
- Filter button that opens a Popover with filter sections:
  - Status: multi-select checkboxes for lifecycle stages (DRAFT, ONBOARDING, ACTIVE, OFFBOARDING, ENDED)
  - Owner: multi-select (fetch users list via trpc.user.list)
  - Team: multi-select (placeholder for now — teams not yet queryable)
  - Billing model: multi-select (FIXED, HOURLY, PROJECT, MILESTONE)
  - Compliance health: single-select (green, yellow, red)
- Active filters shown as Badges below toolbar with X to remove. "Clear all" link when any filter active.
- "Add contractor" primary CTA button (variant="default") on the right

**Step G: Create data-table-pagination.tsx**

Pagination component below table:
- Page size selector (10, 25, 50) using Select
- "Page X of Y" text
- Previous/Next buttons (ChevronLeft, ChevronRight icons)
- "{count} selected" text when rows selected
- "of {total}" total count

**Step H: Create data-table-column-toggle.tsx**

Column visibility dropdown:
- SlidersHorizontal icon button
- DropdownMenu with checkbox items for each hideable column
- Persist to localStorage on change

**Step I: Create data-table-bulk-actions.tsx**

Bulk action toolbar (appears when 1+ rows selected):
- "{count} selected" / "{count} zaznaczonych"
- Assign owner button -> Popover with user selector
- Export button -> DropdownMenu with CSV and XLSX options
- Archive button -> AlertDialog confirmation
- Launch workflow button (disabled with tooltip "Coming in Phase 4")
- All actions call respective tRPC mutations. On success: toast, deselect rows, invalidate list query.

**Step J: Create contractor-side-panel.tsx**

Sheet component sliding from right:
- Width: 480px on desktop (w-[480px]), 400px on tablet (1024-1279px)
- Content: contractor displayName (Heading), status badge, type badge
- Compact card with key fields: NIP (monospace), email, billing model, rate, owner, team
- "Open full profile" CTA button linking to `/contractors/{id}`
- Close on backdrop click, Escape, or X button
- Use ScrollArea for content

**Step K: Add i18n translations**

Add "Contractors" namespace to both en.json and pl.json with ALL copy from the UI-SPEC copywriting contract. Structure:

```json
{
  "Contractors": {
    "pageTitle": "Contractors",
    "addContractor": "Add contractor",
    "searchPlaceholder": "Search by name, company, NIP, or email...",
    "filters": "Filters",
    "clearAll": "Clear all",
    "columns": {
      "name": "Name / Company",
      "type": "Type",
      "status": "Status",
      "owner": "Owner",
      "billingModel": "Billing model",
      "rate": "Rate",
      "currency": "Currency",
      "nextInvoice": "Next invoice expected",
      "teamProject": "Team / Project",
      "contractEnd": "Contract end date",
      "lastActivity": "Last activity",
      "health": "Compliance",
      "selectAll": "Select all",
      "selectRow": "Select row"
    },
    "health": { "green": "Healthy", "yellow": "Warning", "red": "Critical" },
    "lifecycle": { "DRAFT": "Draft", "ONBOARDING": "Onboarding", "ACTIVE": "Active", "OFFBOARDING": "Offboarding", "ENDED": "Inactive" },
    "type": { "SOLE_TRADER": "Sole proprietorship (JDG)", "COMPANY": "Company", "INDIVIDUAL_FREELANCER": "Freelancer", "OTHER": "Other" },
    "bulkActions": {
      "selected": "{count} selected",
      "assignOwner": "Assign owner",
      "export": "Export contractors",
      "exportCsv": "Export as CSV",
      "exportXlsx": "Export as XLSX",
      "archive": "Archive",
      "launchWorkflow": "Launch workflow",
      "comingInPhase4": "Coming in Phase 4"
    },
    "archive": {
      "title": "Archive {name}?",
      "titleBulk": "Archive {count} contractors?",
      "body": "This contractor will be moved to archived status. They will no longer appear in default views. This action can be reversed.",
      "bodyBulk": "These contractors will be moved to archived status. They will no longer appear in default views. This action can be reversed.",
      "cta": "Archive contractor",
      "ctaBulk": "Archive {count} contractors"
    },
    "empty": {
      "heading": "No contractors yet",
      "body": "Add your first contractor to start managing your registry.",
      "cta": "Add contractor"
    },
    "noResults": {
      "heading": "No contractors found",
      "body": "Try adjusting your search or filters.",
      "cta": "Clear filters"
    },
    "pagination": {
      "rowsPerPage": "Rows per page",
      "of": "of {total}",
      "page": "Page {page} of {totalPages}"
    },
    "sidePanel": {
      "openFullProfile": "Open full profile",
      "details": "Details",
      "recentActivity": "Recent activity"
    },
    "exported": "Exported {count} contractors",
    "ownerAssigned": "Owner assigned to {count} contractors",
    "archived": "Archived {count} contractors",
    "error": {
      "loadFailed": "Could not load contractors. Check your connection and try again.",
      "retry": "Try again"
    }
  }
}
```

Add equivalent Polish translations using the copy from 02-UI-SPEC.md (all the PL column entries are documented there).

**Step L: Create contractors/page.tsx**

Server component shell at `apps/web/src/app/[locale]/(dashboard)/contractors/page.tsx`:
- Import and render the DataTable component (client component)
- Page title uses i18n
- Breadcrumb: Dashboard / Contractors

Wire the "Add contractor" button in the top bar to open the wizard (the top bar quick action button in top-bar.tsx already exists — wire its onClick to trigger a global state or URL param).
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web build</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/[locale]/(dashboard)/contractors/page.tsx exists
    - apps/web/src/components/contractors/contractor-table/data-table.tsx imports from @tanstack/react-table
    - apps/web/src/components/contractors/contractor-table/columns.tsx defines 13 columns (select + 12 data columns)
    - apps/web/src/components/contractors/contractor-table/use-contractor-filters.ts imports from nuqs
    - apps/web/src/components/contractors/contractor-side-panel.tsx uses Sheet from shadcn
    - apps/web/src/components/contractors/compliance-health-badge.tsx renders green/yellow/red badges
    - en.json contains "Contractors" namespace with all keys from UI-SPEC
    - pl.json contains "Contractors" namespace with Polish translations
    - Web app builds without TypeScript errors
  </acceptance_criteria>
  <done>Contractor list page renders with TanStack Table, search, filters, bulk actions, side panel, and full i18n in Polish and English</done>
</task>

<task type="auto">
  <name>Task 2: Create add contractor wizard with 3-step form and GUS NIP autofill</name>
  <files>
    apps/web/src/components/contractors/contractor-wizard/wizard-dialog.tsx
    apps/web/src/components/contractors/contractor-wizard/step-company.tsx
    apps/web/src/components/contractors/contractor-wizard/step-billing.tsx
    apps/web/src/components/contractors/contractor-wizard/step-assignment.tsx
    apps/web/messages/en.json
    apps/web/messages/pl.json
  </files>
  <read_first>
    apps/web/src/components/auth/register-form.tsx
    apps/web/src/components/settings/org-settings-form.tsx
    apps/web/src/components/contractors/contractor-table/data-table.tsx
    apps/web/messages/en.json
    apps/web/messages/pl.json
    .planning/phases/02-contractor-registry/02-UI-SPEC.md
  </read_first>
  <action>
**Create the 3-step wizard following the React Hook Form pattern from register-form.tsx and org-settings-form.tsx.**

**wizard-dialog.tsx:**
- Dialog (max-width 640px) containing the wizard
- Props: `open: boolean`, `onOpenChange: (open: boolean) => void`
- Single React Hook Form instance using zodResolver with the full contractorCreateSchema
- State: `currentStep` (0, 1, 2)
- Step progress indicator at top: 3 circles connected by lines. Completed = Indigo fill + checkmark. Current = Indigo ring. Future = gray.
- Step labels: "Company details" / "Billing" / "Assignment" (i18n)
- "Back" button (secondary) on steps 1 and 2
- "Next: Billing" / "Next: Assignment" / "Add contractor" CTA buttons. "Next" validates current step fields before advancing using per-step schema picks.
- On final step submit: call `trpc.contractor.create.mutationOptions` with full form data. On success: close dialog, toast "Contractor added" / "Kontrahent dodany", invalidate contractor list query. On error: toast error message.
- Dismiss behavior: if form pristine, close button says "Close" / "Zamknij". If form dirty, button says "Discard changes" / "Odrzuc zmiany" and shows AlertDialog confirmation.
- Min-height for step content: 320px (from UI-SPEC)

**step-company.tsx:**
- Step 1 fields: NIP (with "Fetch from GUS" button), Legal name, Contractor type (RadioGroup), Contact email, VAT-EU (optional), Address (line1, line2, city, postalCode, country)
- NIP field: Input with monospace font. "Fetch from GUS" button next to it. On click: call `trpc.contractor.gusLookup.mutationOptions` with NIP value. Show spinner on button during fetch ("Fetching company data..." / "Pobieranie danych firmy..."). On success: auto-fill legalName, addressLine1, city, postalCode, registrationNumber (REGON) using form.setValue(). Toast success. On error: toast error "Could not fetch data from GUS. Enter details manually." Fields remain editable after autofill.
- Contractor type RadioGroup: SOLE_TRADER ("Sole proprietorship (JDG)"), COMPANY ("Company (sp. z o.o. / S.A.)"), INDIVIDUAL_FREELANCER ("Freelancer"), OTHER ("Other")
- All field labels and validation messages from UI-SPEC copywriting contract
- Validate on "Next" click: legalName required, type required, NIP required + valid, email required + format

**step-billing.tsx:**
- Step 2 fields: Billing model (Select: Fixed / Hourly / Project / Milestone), Currency (Select: PLN, EUR, USD), Default rate (number Input, stored as grosze — display as zloty with 2 decimal places, multiply by 100 on save), Bank account IBAN (Input with monospace font, optional, IBAN validated when present), Payment terms days (number Input, optional)
- Rate display: show formatted value (e.g., "150.00 PLN"), store as 15000 grosze
- Validate on "Next": billingModel required, currency required, rate required + positive

**step-assignment.tsx:**
- Step 3 fields: Owner (Select, required — fetch users from trpc.user.list and show as options), Team (Select, optional — placeholder for now with empty options list), Project (Select, optional — placeholder), Cost center (Select, optional — placeholder)
- Owner select shows user name + email
- Validate on submit: ownerUserId required

**i18n additions:**

Add "ContractorWizard" namespace to en.json and pl.json with all wizard copy from UI-SPEC:

```json
{
  "ContractorWizard": {
    "title": "Add contractor",
    "step1": "Company details",
    "step2": "Billing",
    "step3": "Assignment",
    "next1": "Next: Billing",
    "next2": "Next: Assignment",
    "submit": "Add contractor",
    "back": "Back",
    "close": "Close",
    "discardChanges": "Discard changes",
    "discardConfirm": {
      "title": "Discard changes?",
      "body": "All entered data will be lost.",
      "discard": "Discard changes",
      "keep": "Keep editing"
    },
    "fields": {
      "nip": "NIP (Tax ID)",
      "nipFetch": "Fetch from GUS",
      "nipFetching": "Fetching company data...",
      "nipSuccess": "Company data loaded from GUS",
      "nipError": "Could not fetch data from GUS. Enter details manually.",
      "legalName": "Legal name",
      "type": "Contractor type",
      "typeOptions": {
        "SOLE_TRADER": "Sole proprietorship (JDG)",
        "COMPANY": "Company (sp. z o.o. / S.A.)",
        "INDIVIDUAL_FREELANCER": "Freelancer",
        "OTHER": "Other"
      },
      "email": "Contact email",
      "vatId": "VAT-EU",
      "address": "Registered address",
      "addressLine1": "Street address",
      "addressLine2": "Line 2",
      "city": "City",
      "postalCode": "Postal code",
      "billingModel": "Billing model",
      "currency": "Currency",
      "rate": "Default rate",
      "bankAccount": "Bank account (IBAN)",
      "paymentTerms": "Payment terms (days)",
      "owner": "Owner",
      "team": "Team",
      "project": "Project",
      "costCenter": "Cost center"
    },
    "billingModelOptions": {
      "FIXED": "Fixed",
      "HOURLY": "Hourly",
      "PROJECT": "Project",
      "MILESTONE": "Milestone"
    },
    "success": "Contractor added",
    "error": "Failed to add contractor. Try again."
  }
}
```

Add equivalent Polish translations using copy from 02-UI-SPEC.md.

**Validation messages** (add to "Validation" namespace in both locales):
```json
{
  "contractor": {
    "legalNameRequired": "Legal name is required",
    "nipRequired": "NIP is required",
    "nipFormat": "NIP must be 10 digits",
    "nipChecksum": "Invalid NIP number",
    "emailRequired": "Contact email is required",
    "emailFormat": "Enter a valid email address",
    "billingModelRequired": "Select a billing model",
    "currencyRequired": "Select a currency",
    "rateRequired": "Default rate is required",
    "ratePositive": "Rate must be a positive number",
    "ownerRequired": "Select an owner",
    "ibanFormat": "Enter a valid IBAN"
  }
}
```

Polish equivalents per UI-SPEC form validation table.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web build</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/contractors/contractor-wizard/wizard-dialog.tsx exists and uses Dialog + React Hook Form
    - step-company.tsx, step-billing.tsx, step-assignment.tsx exist as separate components
    - Wizard uses single React Hook Form instance with zodResolver(contractorCreateSchema)
    - GUS autofill calls trpc.contractor.gusLookup and fills form fields on success
    - Rate field converts between display (zloty) and storage (grosze) values
    - en.json contains "ContractorWizard" namespace and validation messages
    - pl.json contains equivalent Polish translations
    - Web app builds without TypeScript errors
  </acceptance_criteria>
  <done>3-step add contractor wizard works with per-step validation, GUS NIP autofill, rate grosze conversion, and full i18n</done>
</task>

</tasks>

<verification>
- `pnpm --filter @contractor-ops/web build` passes
- Contractor list page exists at the correct route path
- All 12+ table columns defined in columns.tsx
- URL state sync works via nuqs (filters in URL)
- Side panel uses Sheet component
- Wizard uses single React Hook Form with 3 steps
- All UI copy matches UI-SPEC in both EN and PL
</verification>

<success_criteria>
- User can navigate to /contractors and see the data table with search, filters, and pagination
- User can click a row to see the side panel with contractor summary
- User can select rows and use bulk actions (archive, assign owner, export)
- User can click "Add contractor" to open the 3-step wizard
- Wizard validates each step independently and supports GUS NIP autofill
- All text appears correctly in both Polish and English
</success_criteria>

<output>
After completion, create `.planning/phases/02-contractor-registry/02-02-SUMMARY.md`
</output>
