---
phase: 10-onboarding-polish
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - apps/web/src/components/import/import-wizard-dialog.tsx
  - apps/web/src/components/import/step-upload.tsx
  - apps/web/src/components/import/step-mapping.tsx
  - apps/web/src/components/import/step-preview.tsx
  - apps/web/src/components/import/step-duplicates.tsx
  - apps/web/src/components/import/step-confirm.tsx
  - apps/web/src/app/[locale]/(dashboard)/contractors/page.tsx
  - apps/web/src/app/[locale]/(dashboard)/contracts/page.tsx
autonomous: true
requirements: [IMP-01, IMP-02, IMP-03]

must_haves:
  truths:
    - "User can upload a CSV or XLSX file and see auto-mapped column suggestions"
    - "User can manually override column mappings via dropdown selects"
    - "User sees validation results with per-cell error indicators for invalid rows"
    - "User can choose per-row duplicate resolution: skip, update existing, or create new"
    - "User sees import progress and completion summary with counts"
  artifacts:
    - path: "apps/web/src/components/import/import-wizard-dialog.tsx"
      provides: "5-step import wizard dialog"
      exports: ["ImportWizardDialog"]
    - path: "apps/web/src/components/import/step-upload.tsx"
      provides: "File upload step with entity type selection"
      exports: ["StepUpload"]
    - path: "apps/web/src/components/import/step-mapping.tsx"
      provides: "Column mapping step with auto-match and manual override"
      exports: ["StepMapping"]
    - path: "apps/web/src/components/import/step-preview.tsx"
      provides: "Validation preview with error highlighting"
      exports: ["StepPreview"]
    - path: "apps/web/src/components/import/step-duplicates.tsx"
      provides: "Duplicate resolution step with per-row actions"
      exports: ["StepDuplicates"]
    - path: "apps/web/src/components/import/step-confirm.tsx"
      provides: "Confirmation and progress step"
      exports: ["StepConfirm"]
  key_links:
    - from: "apps/web/src/components/import/import-wizard-dialog.tsx"
      to: "trpc.import.parse"
      via: "useMutation for file parsing"
      pattern: "import\\.parse"
    - from: "apps/web/src/components/import/step-preview.tsx"
      to: "trpc.import.validate"
      via: "useMutation for row validation"
      pattern: "import\\.validate"
    - from: "apps/web/src/components/import/step-confirm.tsx"
      to: "trpc.import.commit"
      via: "useMutation for final import"
      pattern: "import\\.commit"
---

<objective>
Build the multi-step import wizard dialog for CSV/XLSX import of contractors and contracts, with column mapping, validation preview, duplicate resolution, and confirmation steps.

Purpose: Enables users to import existing data from spreadsheets (D-01 through D-04). Entry points from contractor and contract list page toolbars.
Output: 6 component files for import wizard, integration into contractor and contract list pages
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-onboarding-polish/10-CONTEXT.md
@.planning/phases/10-onboarding-polish/10-RESEARCH.md
@.planning/phases/10-onboarding-polish/10-UI-SPEC.md
@.planning/phases/10-onboarding-polish/10-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (import router) -->
From packages/api/src/routers/import.ts:
```typescript
// parse mutation returns:
{ headers: string[], sampleRows: Record<string, string>[], suggestedMapping: Record<string, string | null>, totalRows: number }

// validate mutation returns:
ImportResult = { validRows: ImportRow[], invalidRows: ImportRow[], duplicateRows: ImportRow[], totalRows: number, columnMapping: Record<string, string | null> }
// where ImportRow = { rowNumber: number, data: Record<string, unknown>, status: "valid" | "invalid" | "duplicate", errors: Array<{ field: string; message: string }>, duplicateOf?: string }

// commit mutation returns:
{ created: number, updated: number, skipped: number, failed: number }
```

From apps/web/src/components/contractors/contractor-wizard/wizard-dialog.tsx:
```typescript
// Pattern: Dialog + DialogContent + DialogHeader + DialogTitle
// StepIndicator with step numbers, completed check marks
// AlertDialog for discard confirmation on close
// Local Zod schema mirroring validators
// useState for currentStep, useForm per step
```

From apps/web/src/components/documents/drop-zone.tsx:
```typescript
// Uses react-dropzone useDropzone hook
// accept prop for file types
// onDrop callback
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import wizard dialog shell with upload and mapping steps</name>
  <files>
    apps/web/src/components/import/import-wizard-dialog.tsx
    apps/web/src/components/import/step-upload.tsx
    apps/web/src/components/import/step-mapping.tsx
  </files>
  <read_first>
    apps/web/src/components/contractors/contractor-wizard/wizard-dialog.tsx
    apps/web/src/components/documents/drop-zone.tsx
    apps/web/src/components/ui/dialog.tsx
    apps/web/src/components/ui/select.tsx
    apps/web/src/components/ui/radio-group.tsx
    apps/web/src/components/ui/alert-dialog.tsx
    apps/web/src/trpc/init.ts
  </read_first>
  <action>
    Create `apps/web/src/components/import/import-wizard-dialog.tsx`:

    "use client" component. Props: `{ open: boolean; onOpenChange: (open: boolean) => void; defaultEntityType?: "contractor" | "contract" }`.

    1. 5-step wizard following contractor wizard-dialog.tsx pattern exactly:
       - State: `currentStep` (0-4), `fileBase64` (string | null), `entityType` ("contractor" | "contract"), `parseResult` (headers, sampleRows, suggestedMapping, totalRows), `columnMapping` (Record<string, string | null>), `validateResult` (ImportResult | null), `duplicateActions` (Record<string, "skip" | "update" | "create">)

    2. StepIndicator at top showing 5 steps: Upload, Map columns, Review data, Resolve duplicates, Confirm import
       - Step 4 (duplicates) conditionally shown only if duplicateRows.length > 0

    3. Step rendering: switch on currentStep, render appropriate step component, pass props down

    4. Footer: Back button (disabled on step 0) + Next/Import button. "Discard" confirmation via AlertDialog when closing mid-flow (reuse pattern from contractor wizard)

    5. Dialog width: `className="max-w-[720px]"` per UI-SPEC

    Export: `export function ImportWizardDialog({ open, onOpenChange, defaultEntityType }: ImportWizardDialogProps)`

    Create `apps/web/src/components/import/step-upload.tsx`:

    "use client" component. Props: `{ entityType, onEntityTypeChange, onFileSelected: (base64: string, fileName: string) => void }`.

    1. Entity type RadioGroup: "Contractors" / "Contracts" options (per UI-SPEC step 1)
    2. DropZone using react-dropzone useDropzone:
       - accept: `{ "text/csv": [".csv"], "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet": [".xlsx"] }`
       - maxSize: 10 * 1024 * 1024 (10MB)
       - onDrop: read file as ArrayBuffer, convert to base64 string via btoa/Buffer
    3. Drop zone visual: dashed border, Upload icon (48px, text-muted-foreground), heading "Drop your file here", body "CSV or Excel file with contractor or contract data", "Browse files" secondary button
    4. After file selected: show file name + size + "Remove" ghost button to clear
    5. Error state: invalid file type or too large → sonner toast with error message per UI-SPEC copy

    Create `apps/web/src/components/import/step-mapping.tsx`:

    "use client" component. Props: `{ headers: string[], sampleRows: Record<string, string>[], suggestedMapping: Record<string, string | null>, entityType: "contractor" | "contract", columnMapping: Record<string, string | null>, onMappingChange: (mapping: Record<string, string | null>) => void }`.

    1. Target fields list based on entityType:
       - Contractors: legalName*, displayName, taxId*, email*, type, vatId, phone, countryCode, currency (* = required)
       - Contracts: title*, type*, startDate*, endDate, contractorTaxId* (* = required)

    2. Two-column grid (per UI-SPEC: source 40%, target 60%):
       - Each row: source header name (14px, readonly) + sample value preview (12px, text-muted-foreground, first non-empty value from sampleRows) + Select dropdown with target field options + "Skip" option
       - Auto-matched rows (suggestedMapping[header] !== null): show CheckCircle icon in text-success
       - Unmatched rows: show AlertCircle icon in text-warning

    3. Select options: all target fields for entity type + "Skip (do not import)" option (value: null)
    4. Prevent duplicate mappings: if a target field is already mapped by another source column, show it as disabled in other selects
    5. Required fields validation: Next button disabled if any required target field is unmapped. Show asterisk on required field labels
    6. Note at bottom: "Unmapped columns will be ignored" in 12px text-muted-foreground
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && ls apps/web/src/components/import/import-wizard-dialog.tsx apps/web/src/components/import/step-upload.tsx apps/web/src/components/import/step-mapping.tsx 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/import/import-wizard-dialog.tsx contains `export function ImportWizardDialog(`
    - apps/web/src/components/import/import-wizard-dialog.tsx contains `max-w-[720px]`
    - apps/web/src/components/import/import-wizard-dialog.tsx contains `currentStep`
    - apps/web/src/components/import/import-wizard-dialog.tsx contains `AlertDialog`
    - apps/web/src/components/import/step-upload.tsx contains `export function StepUpload(`
    - apps/web/src/components/import/step-upload.tsx contains `useDropzone`
    - apps/web/src/components/import/step-upload.tsx contains `.csv`
    - apps/web/src/components/import/step-upload.tsx contains `.xlsx`
    - apps/web/src/components/import/step-mapping.tsx contains `export function StepMapping(`
    - apps/web/src/components/import/step-mapping.tsx contains `CheckCircle`
    - apps/web/src/components/import/step-mapping.tsx contains `Select`
  </acceptance_criteria>
  <done>Import wizard dialog with upload (DropZone + entity type) and column mapping (auto-match + manual override + required field validation) steps are complete.</done>
</task>

<task type="auto">
  <name>Task 2: Preview, duplicates, confirm steps and list page integration</name>
  <files>
    apps/web/src/components/import/step-preview.tsx
    apps/web/src/components/import/step-duplicates.tsx
    apps/web/src/components/import/step-confirm.tsx
    apps/web/src/app/[locale]/(dashboard)/contractors/page.tsx
    apps/web/src/app/[locale]/(dashboard)/contracts/page.tsx
  </files>
  <read_first>
    apps/web/src/components/import/import-wizard-dialog.tsx
    apps/web/src/components/import/step-mapping.tsx
    apps/web/src/components/ui/table.tsx
    apps/web/src/components/ui/radio-group.tsx
    apps/web/src/components/ui/progress.tsx
    apps/web/src/components/ui/badge.tsx
    apps/web/src/components/ui/tooltip.tsx
    apps/web/src/components/ui/scroll-area.tsx
    apps/web/src/app/[locale]/(dashboard)/contractors/page.tsx
    apps/web/src/app/[locale]/(dashboard)/contracts/page.tsx
  </read_first>
  <action>
    Create `apps/web/src/components/import/step-preview.tsx`:

    "use client" component. Props: `{ validRows: ImportRow[], invalidRows: ImportRow[], totalRows: number }`.

    1. Stats bar at top: "{X} valid rows" in text-success font-semibold + "{Y} invalid rows" in text-destructive font-semibold + "{Z} total rows" in text-muted-foreground
    2. Toggle Button group: "Show all" / "Show errors only" (local state, default "Show all")
    3. ScrollArea with max-h-[360px] containing Table:
       - Columns: Row # + all mapped field columns
       - Valid rows: normal rendering
       - Invalid rows: row bg `bg-destructive/5`, invalid cells have `border-l-2 border-destructive` + AlertCircle icon (14px) + Tooltip showing error message on hover
    4. If no invalid rows, skip toggle and just show table header "All {X} rows are valid" with CheckCircle icon in text-success

    Create `apps/web/src/components/import/step-duplicates.tsx`:

    "use client" component. Props: `{ duplicateRows: ImportRow[], duplicateActions: Record<string, "skip" | "update" | "create">, onActionsChange: (actions: Record<string, "skip" | "update" | "create">) => void }`.

    1. Banner at top: AlertTriangle icon + "We found {N} potential duplicates based on NIP/tax ID" in text-warning, bg-warning/10, rounded border
    2. Bulk action buttons above table: "Skip all duplicates" outline + "Update all" outline
    3. ScrollArea with max-h-[320px] containing Table:
       - Columns: NIP/taxId, Name (from file), Name (existing), Action
       - Action column: RadioGroup per row with 3 options: Skip / Update existing / Create new
       - Default: Skip
    4. On bulk action click: update all rows' actions at once via onActionsChange

    Create `apps/web/src/components/import/step-confirm.tsx`:

    "use client" component. Props: `{ entityType, counts: { newRecords: number, updates: number, skippedDuplicates: number, skippedErrors: number }, onImport: () => Promise<void>, importResult: { created: number, updated: number, skipped: number, failed: number } | null, isImporting: boolean }`.

    1. Pre-import state: Summary card with "Ready to import {X} {entity}". Breakdown: "{A} new records" + "{B} updates to existing" + "{C} skipped duplicates" + "{D} skipped errors". "Import" primary Button + "Back" outline Button
    2. Importing state: Progress bar with "{current}/{total}" text + "Importing..." centered
    3. Completion state: CheckCircle icon (48px, text-success) + "Import complete" heading (20px) + summary stats + "View {entity}" primary CTA Button linking to `/contractors` or `/contracts`
    4. Error state: If import fails, show error message with "Try again" Button

    Integration -- update `apps/web/src/app/[locale]/(dashboard)/contractors/page.tsx`:
    - Add `useState` for `importWizardOpen`
    - Add "Import" Button in the toolbar area (next to existing "Add contractor" button): `<Button variant="outline" onClick={() => setImportWizardOpen(true)}>Import</Button>`
    - Render `<ImportWizardDialog open={importWizardOpen} onOpenChange={setImportWizardOpen} defaultEntityType="contractor" />`

    Integration -- update `apps/web/src/app/[locale]/(dashboard)/contracts/page.tsx`:
    - Same pattern: "Import" button + ImportWizardDialog with `defaultEntityType="contract"`
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && grep -c "ImportWizardDialog" apps/web/src/app/[locale]/(dashboard)/contractors/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/import/step-preview.tsx contains `export function StepPreview(`
    - apps/web/src/components/import/step-preview.tsx contains `bg-destructive/5`
    - apps/web/src/components/import/step-preview.tsx contains `border-destructive`
    - apps/web/src/components/import/step-preview.tsx contains `ScrollArea`
    - apps/web/src/components/import/step-duplicates.tsx contains `export function StepDuplicates(`
    - apps/web/src/components/import/step-duplicates.tsx contains `RadioGroup`
    - apps/web/src/components/import/step-duplicates.tsx contains `AlertTriangle`
    - apps/web/src/components/import/step-confirm.tsx contains `export function StepConfirm(`
    - apps/web/src/components/import/step-confirm.tsx contains `Progress`
    - apps/web/src/components/import/step-confirm.tsx contains `CheckCircle`
    - apps/web/src/app/[locale]/(dashboard)/contractors/page.tsx contains `ImportWizardDialog`
    - apps/web/src/app/[locale]/(dashboard)/contracts/page.tsx contains `ImportWizardDialog`
  </acceptance_criteria>
  <done>Import wizard has validation preview with error highlighting, duplicate resolution with per-row actions, confirmation with progress tracking. Import button integrated into contractor and contract list pages.</done>
</task>

</tasks>

<verification>
- All 6 import wizard components exist and export correctly
- Contractor list page has Import button triggering ImportWizardDialog
- Contract list page has Import button triggering ImportWizardDialog
- Wizard follows 5-step flow: Upload -> Mapping -> Preview -> Duplicates -> Confirm
</verification>

<success_criteria>
- User can upload CSV/XLSX file and see auto-mapped column suggestions with manual override
- User sees validation preview with per-cell error indicators
- User can choose per-row duplicate resolution (skip/update/create)
- Import wizard accessible from both contractor and contract list page toolbars
</success_criteria>

<output>
After completion, create `.planning/phases/10-onboarding-polish/10-03-SUMMARY.md`
</output>
