---
phase: 04-workflow-engine
plan: 05
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - apps/web/src/components/contractors/contractor-profile/profile-tabs.tsx
  - apps/web/src/components/contractors/contractor-profile/workflows-tab.tsx
  - apps/web/src/components/contractors/contractor-profile/profile-header.tsx
  - apps/web/src/components/contractors/contractor-table/data-table-bulk-actions.tsx
  - apps/web/src/components/workflows/workflow-nav-badge.tsx
  - apps/web/src/components/layout/sidebar.tsx
  - packages/api/src/routers/workflow.ts
autonomous: true
requirements: [WKFL-05, WKFL-09, ORG-09]

must_haves:
  truths:
    - "Contractor profile Workflows tab shows runs for that contractor with start CTA"
    - "Contractor profile header Start onboarding and Start offboarding buttons are functional"
    - "Contractor bulk action Launch workflow opens template picker"
    - "Sidebar Workflows nav item shows overdue count badge"
    - "Two starter templates (Onboarding, Offboarding) are seeded on first Templates tab visit"
  artifacts:
    - path: "apps/web/src/components/contractors/contractor-profile/workflows-tab.tsx"
      provides: "Workflows tab replacing TabPlaceholder in contractor profile"
    - path: "apps/web/src/components/workflows/workflow-nav-badge.tsx"
      provides: "Overdue count badge for sidebar nav"
  key_links:
    - from: "apps/web/src/components/contractors/contractor-profile/workflows-tab.tsx"
      to: "packages/api/src/routers/workflow.ts"
      via: "trpc.workflow.listRuns with contractorId filter"
      pattern: "trpc\\.workflow\\.listRuns"
    - from: "apps/web/src/components/workflows/workflow-nav-badge.tsx"
      to: "packages/api/src/routers/workflow.ts"
      via: "trpc.workflow.overdueCount query"
      pattern: "trpc\\.workflow\\.overdueCount"
    - from: "apps/web/src/components/contractors/contractor-profile/profile-header.tsx"
      to: "apps/web/src/components/workflows/template-picker-dialog.tsx"
      via: "TemplatePicker with preFilterType prop"
      pattern: "TemplatePicker.*preFilterType"
---

<objective>
Wire all integration points: contractor profile Workflows tab (replacing placeholder), contractor header workflow buttons, bulk action launch workflow, sidebar overdue badge, and starter template seeding. This connects the workflow engine to the rest of the app.

Purpose: WKFL-05 (start run from multiple entry points), WKFL-09 (overdue badge), ORG-09 (template management with starter templates) require integration with existing contractor and navigation components.
Output: Fully connected workflow engine accessible from contractor profiles, bulk actions, and sidebar.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-workflow-engine/04-CONTEXT.md
@.planning/phases/04-workflow-engine/04-RESEARCH.md
@.planning/phases/04-workflow-engine/04-UI-SPEC.md
@.planning/phases/04-workflow-engine/04-01-SUMMARY.md

<interfaces>
From packages/api/src/routers/workflow.ts:
- workflow.listRuns (query): accepts contractorId filter
- workflow.startRun (mutation): input { templateId, contractorId }
- workflow.overdueCount (query): returns { count }
- workflow.listTemplates (query): returns templates

From apps/web/src/components/contractors/contractor-profile/profile-tabs.tsx:
- Uses TabPlaceholder for workflows, invoices, payments tabs
- Tab state via URL query param (?tab=)

From apps/web/src/components/contractors/contractor-profile/profile-header.tsx:
- "Start onboarding" and "Start offboarding" buttons exist (disabled, Phase 4 tooltip)
- Contractor data available in header props

From apps/web/src/components/contractors/contractor-table/data-table-bulk-actions.tsx:
- "Launch workflow" bulk action exists (disabled comment)

From apps/web/src/components/workflows/template-picker-dialog.tsx:
- Props: open, onOpenChange, contractorId, contractId?, preFilterType?
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create contractor profile Workflows tab and wire header buttons</name>
  <files>
    apps/web/src/components/contractors/contractor-profile/workflows-tab.tsx,
    apps/web/src/components/contractors/contractor-profile/profile-tabs.tsx,
    apps/web/src/components/contractors/contractor-profile/profile-header.tsx
  </files>
  <read_first>
    apps/web/src/components/contractors/contractor-profile/profile-tabs.tsx
    apps/web/src/components/contractors/contractor-profile/profile-header.tsx
    apps/web/src/components/contractors/contractor-profile/tab-placeholder.tsx
    apps/web/src/components/contractors/contractor-profile/contracts-tab.tsx
    .planning/phases/04-workflow-engine/04-UI-SPEC.md
    .planning/phases/04-workflow-engine/04-CONTEXT.md
  </read_first>
  <action>
**Create workflows-tab.tsx:**
Component replacing the TabPlaceholder for the Workflows tab. Props: contractorId (string). Fetches workflow runs via trpc.workflow.listRuns with contractorId filter. Displays as a list of workflow runs (same pattern as contractor Contracts tab mini table). Each row: workflow name, status Badge, progress (X/Y), started date. Click row navigates to /workflows/[id]. "Start workflow" Button top-right opens TemplatePicker dialog with contractorId pre-set. Empty state: centered GitBranch icon, "No workflows" heading, body "Start a workflow for this contractor to track onboarding, offboarding, or other processes.", "Start workflow" CTA. All text via useTranslations("Workflows").

**Update profile-tabs.tsx:**
Import WorkflowsTab from "./workflows-tab". Replace the TabPlaceholder for the "workflows" tab case with <WorkflowsTab contractorId={contractorId} />. Keep TabPlaceholder for invoices and payments tabs (Phase 5 and 8). Remove the workflows entry from the TabPlaceholder usage comment if present.

**Update profile-header.tsx:**
Find the existing disabled "Start onboarding" and "Start offboarding" buttons. Remove the disabled prop and Phase 4 tooltip. Add state: const [pickerOpen, setPickerOpen] = useState(false) and const [pickerType, setPickerType] = useState<"ONBOARDING"|"OFFBOARDING"|null>(null). "Start onboarding" onClick: setPickerType("ONBOARDING"), setPickerOpen(true). "Start offboarding" onClick: setPickerType("OFFBOARDING"), setPickerOpen(true). Render TemplatePicker dialog with: open={pickerOpen}, onOpenChange={setPickerOpen}, contractorId={contractor.id}, preFilterType={pickerType}. If no matching template exists for the type, the TemplatePicker already handles empty state. Import TemplatePicker from "@/components/workflows/template-picker-dialog".
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/contractors/contractor-profile/workflows-tab.tsx exists and contains "listRuns"
    - apps/web/src/components/contractors/contractor-profile/workflows-tab.tsx contains "contractorId"
    - apps/web/src/components/contractors/contractor-profile/workflows-tab.tsx contains "TemplatePicker"
    - apps/web/src/components/contractors/contractor-profile/profile-tabs.tsx contains "WorkflowsTab"
    - apps/web/src/components/contractors/contractor-profile/profile-tabs.tsx does NOT contain "TabPlaceholder" for workflows tab (still has it for invoices/payments)
    - apps/web/src/components/contractors/contractor-profile/profile-header.tsx contains "TemplatePicker"
    - apps/web/src/components/contractors/contractor-profile/profile-header.tsx contains "preFilterType"
    - apps/web/src/components/contractors/contractor-profile/profile-header.tsx does NOT contain "disabled" on start onboarding button
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>Contractor profile Workflows tab functional, header Start onboarding/offboarding buttons wired to template picker</done>
</task>

<task type="auto">
  <name>Task 2: Wire bulk action, sidebar badge, and seed starter templates</name>
  <files>
    apps/web/src/components/contractors/contractor-table/data-table-bulk-actions.tsx,
    apps/web/src/components/workflows/workflow-nav-badge.tsx,
    apps/web/src/components/layout/sidebar.tsx,
    packages/api/src/routers/workflow.ts
  </files>
  <read_first>
    apps/web/src/components/contractors/contractor-table/data-table-bulk-actions.tsx
    apps/web/src/components/layout/sidebar.tsx
    apps/web/src/lib/navigation.ts
    packages/api/src/routers/workflow.ts
    .planning/phases/04-workflow-engine/04-UI-SPEC.md
    .planning/phases/04-workflow-engine/04-CONTEXT.md
  </read_first>
  <action>
**Update data-table-bulk-actions.tsx:**
Find the disabled "Launch workflow" bulk action. Remove the disabled state and the comment. Add state: const [pickerOpen, setPickerOpen] = useState(false). On click: setPickerOpen(true). Render TemplatePicker dialog with: open={pickerOpen}, onOpenChange={setPickerOpen}, contractorId passed as the first selected contractor's ID (for single selection) or as array for bulk (TemplatePicker handles bulk via sequential Promise.all). For bulk: pass contractorIds array prop (the TemplatePicker needs to accept either contractorId or contractorIds). Update TemplatePicker to accept contractorIds?: string[] prop. When contractorIds provided, show "Start workflow for {count} contractors" and create runs for each. Import TemplatePicker from "@/components/workflows/template-picker-dialog".

**Create workflow-nav-badge.tsx:**
Client component. Fetches overdue count via trpc.workflow.overdueCount with refetchInterval of 60000ms (1 minute polling). Renders a small red circle badge with count (max display "9+"). If count is 0, renders nothing. Props: none (fetches for current user). Styling: absolute positioned top-right of nav item, red background, white text, 18px diameter circle, text-xs.

**Update sidebar.tsx:**
Find where the navigation items are rendered (likely mapping over navigationItems). For the "workflows" nav item (key === "workflows"), render WorkflowNavBadge as a child positioned relative to the nav item text/icon. Import WorkflowNavBadge. The badge renders inside the nav item wrapper with position relative on the wrapper and absolute on the badge.

**Add seedStarterTemplates procedure to workflow router:**
Add a new procedure `seedStarterTemplates` in packages/api/src/routers/workflow.ts. This is a mutation that requires workflow create permission. Logic: check if any templates exist for this org. If templates already exist, return early (no-op). If no templates exist, create 2 templates in DRAFT status:

Template 1 - "Contractor Onboarding" (type: ONBOARDING):
Tasks (in order, all with assigneeMode: ROLE_BASED, required: true):
1. "Collect NDA" - DOCUMENT_COLLECTION, assigneeRole: OPS_MANAGER, dueOffsetDays: 2
2. "Sign contract" - APPROVAL, assigneeRole: LEGAL_VIEWER, dueOffsetDays: 5
3. "Set up IT access" - ACCESS_GRANT, assigneeRole: IT_ADMIN, dueOffsetDays: 3
4. "Set up finance" - FINANCE_SETUP, assigneeRole: FINANCE_ADMIN, dueOffsetDays: 3
5. "Provision equipment" - EQUIPMENT, assigneeRole: OPS_MANAGER, dueOffsetDays: 5
6. "Team introduction meeting" - MEETING, assigneeRole: TEAM_MANAGER, dueOffsetDays: 7
7. "Knowledge transfer" - KNOWLEDGE_TRANSFER, assigneeRole: TEAM_MANAGER, dueOffsetDays: 14

Template 2 - "Contractor Offboarding" (type: OFFBOARDING):
Tasks:
1. "Knowledge transfer" - KNOWLEDGE_TRANSFER, assigneeRole: TEAM_MANAGER, dueOffsetDays: 7
2. "Revoke IT access" - ACCESS_REVOKE, assigneeRole: IT_ADMIN, dueOffsetDays: 1
3. "Return equipment" - EQUIPMENT, assigneeRole: OPS_MANAGER, dueOffsetDays: 5
4. "Finance wrap-up" - FINANCE_SETUP, assigneeRole: FINANCE_ADMIN, dueOffsetDays: 3
5. "Final documentation" - DOCUMENT_COLLECTION, assigneeRole: OPS_MANAGER, dueOffsetDays: 5

The Templates tab in the workflows page (templates-table.tsx from plan 03) should call seedStarterTemplates on mount if it detects no templates (call seed mutation then invalidate listTemplates). This is the "seed if empty" pattern from RESEARCH.md.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/contractors/contractor-table/data-table-bulk-actions.tsx contains "TemplatePicker"
    - apps/web/src/components/contractors/contractor-table/data-table-bulk-actions.tsx does NOT contain the disabled Launch workflow comment
    - apps/web/src/components/workflows/workflow-nav-badge.tsx exists and contains "overdueCount"
    - apps/web/src/components/workflows/workflow-nav-badge.tsx contains "refetchInterval"
    - apps/web/src/components/layout/sidebar.tsx contains "WorkflowNavBadge"
    - packages/api/src/routers/workflow.ts contains "seedStarterTemplates"
    - packages/api/src/routers/workflow.ts contains "Contractor Onboarding"
    - packages/api/src/routers/workflow.ts contains "Contractor Offboarding"
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>Bulk action wired, sidebar overdue badge live, starter templates seeded on first visit</done>
</task>

</tasks>

<verification>
- Contractor profile Workflows tab shows runs and Start workflow CTA
- Start onboarding/offboarding buttons open template picker with correct type filter
- Bulk action Launch workflow opens picker for selected contractors
- Sidebar shows overdue count badge (polls every 60s)
- First visit to Templates tab seeds Onboarding and Offboarding templates
- TypeScript compiles clean
</verification>

<success_criteria>
- All entry points for starting workflows are functional (profile header, profile tab, bulk action, /workflows page)
- Overdue count badge visible in sidebar when tasks are overdue
- Starter templates available in DRAFT status for admin customization
- No broken integrations with existing contractor components
</success_criteria>

<output>
After completion, create `.planning/phases/04-workflow-engine/04-05-SUMMARY.md`
</output>
