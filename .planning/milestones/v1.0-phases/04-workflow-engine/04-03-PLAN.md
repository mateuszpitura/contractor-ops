---
phase: 04-workflow-engine
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - apps/web/src/app/[locale]/(dashboard)/workflows/page.tsx
  - apps/web/src/components/workflows/workflow-runs-table/columns.tsx
  - apps/web/src/components/workflows/workflow-runs-table/data-table.tsx
  - apps/web/src/components/workflows/workflow-runs-table/data-table-toolbar.tsx
  - apps/web/src/components/workflows/workflow-runs-table/data-table-filters.tsx
  - apps/web/src/components/workflows/workflow-runs-table/data-table-pagination.tsx
  - apps/web/src/components/workflows/workflow-runs-table/use-workflow-filters.ts
  - apps/web/src/components/workflows/my-tasks-list.tsx
  - apps/web/src/components/workflows/templates-table.tsx
  - apps/web/src/components/workflows/workflow-side-panel.tsx
  - apps/web/src/components/workflows/template-picker-dialog.tsx
autonomous: true
requirements: [WKFL-05, WKFL-09, WKFL-10]

must_haves:
  truths:
    - "User can view active workflow runs in a sortable, filterable table"
    - "User can view their assigned tasks across all workflows in My Tasks tab"
    - "Admin can view and manage templates in the Templates tab"
    - "User can start a workflow via template picker dialog"
    - "User can click a run row to see summary in side panel"
    - "Overdue tasks are visually highlighted in the runs table and my tasks list"
    - "Workflow progress (X/Y tasks) is visible in runs table and side panel"
  artifacts:
    - path: "apps/web/src/app/[locale]/(dashboard)/workflows/page.tsx"
      provides: "Main workflows page with 3 tabs"
    - path: "apps/web/src/components/workflows/workflow-runs-table/data-table.tsx"
      provides: "TanStack Table for workflow runs"
    - path: "apps/web/src/components/workflows/my-tasks-list.tsx"
      provides: "My tasks flat list with overdue sorting"
    - path: "apps/web/src/components/workflows/template-picker-dialog.tsx"
      provides: "Dialog for selecting template and starting a workflow run"
  key_links:
    - from: "apps/web/src/components/workflows/workflow-runs-table/data-table.tsx"
      to: "packages/api/src/routers/workflow.ts"
      via: "trpc.workflow.listRuns query"
      pattern: "trpc\\.workflow\\.listRuns"
    - from: "apps/web/src/components/workflows/template-picker-dialog.tsx"
      to: "packages/api/src/routers/workflow.ts"
      via: "trpc.workflow.startRun mutation"
      pattern: "trpc\\.workflow\\.startRun"
---

<objective>
Build the main /workflows page with three tabs (Active runs, My tasks, Templates), the workflow runs TanStack Table, My Tasks flat list, Templates table, side panel for run preview, and template picker dialog for starting workflows.

Purpose: WKFL-05 (start runs), WKFL-09 (overdue detection visual), WKFL-10 (progress tracking) require the main workflows page as the primary entry point.
Output: Functional /workflows page with all tabs, run listing, task listing, and workflow start flow.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-workflow-engine/04-CONTEXT.md
@.planning/phases/04-workflow-engine/04-RESEARCH.md
@.planning/phases/04-workflow-engine/04-UI-SPEC.md
@.planning/phases/04-workflow-engine/04-01-SUMMARY.md

<interfaces>
From packages/api/src/routers/workflow.ts:
- workflow.listRuns (query): returns { items, total, page, pageSize }
- workflow.myTasks (query): returns { items, total }
- workflow.listTemplates (query): returns { items, total, page, pageSize }
- workflow.startRun (mutation): input { templateId, contractorId, contractId? }
- workflow.overdueCount (query): returns { count }
- workflow.getRun (query): returns run with tasks, comments, template, contractor

From packages/validators/src/workflow.ts:
- workflowRunListSchema: { page, pageSize, search, sortBy, sortOrder, contractorId, filters }
- startRunSchema: { templateId, contractorId, contractId? }
- myTasksListSchema: { page, pageSize, overdueOnly? }

Enums:
- WorkflowRunStatus: NOT_STARTED, IN_PROGRESS, COMPLETED, CANCELLED, BLOCKED, OVERDUE
- WorkflowTaskStatus: TODO, IN_PROGRESS, DONE, BLOCKED, SKIPPED, CANCELLED, OVERDUE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workflow runs table and My Tasks list components</name>
  <files>
    apps/web/src/components/workflows/workflow-runs-table/columns.tsx,
    apps/web/src/components/workflows/workflow-runs-table/data-table.tsx,
    apps/web/src/components/workflows/workflow-runs-table/data-table-toolbar.tsx,
    apps/web/src/components/workflows/workflow-runs-table/data-table-filters.tsx,
    apps/web/src/components/workflows/workflow-runs-table/data-table-pagination.tsx,
    apps/web/src/components/workflows/workflow-runs-table/use-workflow-filters.ts,
    apps/web/src/components/workflows/my-tasks-list.tsx,
    apps/web/src/components/workflows/templates-table.tsx
  </files>
  <read_first>
    apps/web/src/components/contracts/contract-table/columns.tsx
    apps/web/src/components/contracts/contract-table/data-table.tsx
    apps/web/src/components/contracts/contract-table/data-table-toolbar.tsx
    apps/web/src/components/contracts/contract-table/data-table-filters.tsx
    apps/web/src/components/contracts/contract-table/data-table-pagination.tsx
    apps/web/src/components/contracts/contract-table/use-contract-filters.ts
    .planning/phases/04-workflow-engine/04-UI-SPEC.md
  </read_first>
  <action>
**Mirror the contract-table pattern exactly** for the workflow-runs-table directory. Same file structure, same TanStack Table configuration pattern (manualPagination, manualSorting, manualFiltering).

**use-workflow-filters.ts** -- nuqs-based filter state hook mirroring use-contract-filters.ts. URL params: page, pageSize, search, sortBy (default "dueAt"), sortOrder (default "asc"), status (array), templateId (array), overdueOnly (boolean). Returns { filters, setFilters, resetFilters }.

**columns.tsx** -- Define columns for workflow runs table per UI-SPEC: Workflow name (template name + run name), Contractor (name + company), Template (type badge), Status (badge with colors per UI-SPEC status table: IN_PROGRESS=primary, COMPLETED=success, CANCELLED=muted, BLOCKED=warning, OVERDUE=destructive), Progress (X/Y text), Started by (user name), Started date (formatted), Due date (formatted, destructive color if overdue). Default sort: dueAt ascending.

**data-table.tsx** -- TanStack Table with manual mode. Data from trpc.workflow.listRuns. Row click opens side panel (onRowClick prop). Overdue rows have destructive at 5% opacity background. Loading: 8 skeleton rows.

**data-table-toolbar.tsx** -- Search input (debounce 300ms, min 2 chars), filter triggers.

**data-table-filters.tsx** -- Status multi-select (NOT_STARTED, IN_PROGRESS, COMPLETED, CANCELLED, BLOCKED), Template multi-select (from trpc.workflow.listTemplates), Overdue only toggle. Active filters as badges.

**data-table-pagination.tsx** -- Same as contract table pagination (page size 10/25/50, prev/next, page X of Y).

**my-tasks-list.tsx** -- Flat task list component. Fetches data from trpc.workflow.myTasks. Each task as horizontal Card: left status icon (per UI-SPEC task status icons table), center task title + workflow name as Link to /workflows/[runId], right assignee avatar + due date. Overdue tasks sorted to top with red AlertCircle icon and destructive text on due date. Click navigates to /workflows/[runId]. Empty state: centered CheckCircle icon + "No tasks assigned" heading + body text. Loading: 5 skeleton cards. Optional overdueOnly filter toggle.

**templates-table.tsx** -- Simple table for templates tab. Columns: name, type Badge, status Badge (DRAFT=muted, ACTIVE=success, ARCHIVED=muted/50%), task count, created by, last updated. Row click navigates to /workflows/templates/[id]. Row actions dropdown: Edit, Duplicate, Activate/Archive, Delete (draft only). Data from trpc.workflow.listTemplates. "New template" CTA top-right. Empty state with "No workflow templates" per UI-SPEC.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/workflows/workflow-runs-table/columns.tsx exists and contains "dueAt"
    - apps/web/src/components/workflows/workflow-runs-table/data-table.tsx exists and contains "useReactTable"
    - apps/web/src/components/workflows/workflow-runs-table/use-workflow-filters.ts exists and contains "useQueryStates"
    - apps/web/src/components/workflows/my-tasks-list.tsx exists and contains "myTasks"
    - apps/web/src/components/workflows/my-tasks-list.tsx contains "AlertCircle"
    - apps/web/src/components/workflows/templates-table.tsx exists and contains "listTemplates"
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>Runs table, My Tasks list, and Templates table components built following established contract-table patterns</done>
</task>

<task type="auto">
  <name>Task 2: Create workflows page, side panel, and template picker dialog</name>
  <files>
    apps/web/src/app/[locale]/(dashboard)/workflows/page.tsx,
    apps/web/src/components/workflows/workflow-side-panel.tsx,
    apps/web/src/components/workflows/template-picker-dialog.tsx
  </files>
  <read_first>
    apps/web/src/app/[locale]/(dashboard)/contracts/page.tsx
    apps/web/src/components/contracts/contract-side-panel.tsx
    .planning/phases/04-workflow-engine/04-UI-SPEC.md
    .planning/phases/04-workflow-engine/04-CONTEXT.md
  </read_first>
  <action>
**Create /workflows/page.tsx:**
Client component wrapped in Suspense (for nuqs). Page title "Workflows" (Heading). Three tabs using shadcn Tabs: "Active runs" (default), "My tasks", "Templates" (admin only -- conditionally rendered using usePermissions().can("workflow", ["create"])). Tab state via URL param (?tab=runs|tasks|templates) using nuqs. "Start workflow" primary CTA in top-right (opens TemplatePicker). Active runs tab: WorkflowRunsDataTable with onRowClick opening WorkflowSidePanel. My tasks tab: MyTasksList. Templates tab: TemplatesTable with "New template" CTA linking to /workflows/templates/new.

**Create workflow-side-panel.tsx:**
Sheet component (480px, slide from right, 200ms ease-out). Props: runId (string|null), onClose. When runId set, fetch trpc.workflow.getRun. Content: workflow name Heading, status Badge, template name. Progress bar (shadcn Progress, 8px height, primary fill) with "{X} of {Y} tasks complete" label. Task summary counts (done, in progress, overdue). Contractor name as Link to /contractors/[id]. Started by + date metadata. "Open workflow" primary CTA at bottom linking to /workflows/[runId]. Loading: skeleton heading + progress bar + 4 field placeholders. Close: backdrop click, Escape, X button.

**Create template-picker-dialog.tsx:**
Dialog component (480px max-width). Props: open, onOpenChange, contractorId (required), contractId? (optional), preFilterType? (optional WorkflowTemplateType for header button pre-filtering). Search input at top. Fetches active templates via trpc.workflow.listTemplates with status=["ACTIVE"]. If preFilterType provided, filter to matching type (user can clear). Each row: template name, type Badge, task count, truncated description. Click row to select (visual highlight with primary ring). "Start" CTA at bottom. On confirm: calls trpc.workflow.startRun with { templateId, contractorId, contractId }. Success: toast "Workflow started", close dialog, invalidate queries. Error: toast. When triggered from bulk action (multiple contractorIds), show count "Start workflow for {count} contractors" and create runs sequentially with Promise.all. Empty: "No templates available" + "Ask your admin to create a workflow template." Loading: skeleton search + 4 skeleton rows.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/[locale]/(dashboard)/workflows/page.tsx exists and contains "Tabs"
    - apps/web/src/app/[locale]/(dashboard)/workflows/page.tsx contains "Active runs"
    - apps/web/src/app/[locale]/(dashboard)/workflows/page.tsx contains "My tasks"
    - apps/web/src/app/[locale]/(dashboard)/workflows/page.tsx contains "Templates"
    - apps/web/src/app/[locale]/(dashboard)/workflows/page.tsx contains "Suspense"
    - apps/web/src/components/workflows/workflow-side-panel.tsx exists and contains "Sheet"
    - apps/web/src/components/workflows/workflow-side-panel.tsx contains "Progress"
    - apps/web/src/components/workflows/template-picker-dialog.tsx exists and contains "startRun"
    - apps/web/src/components/workflows/template-picker-dialog.tsx contains "Dialog"
    - TypeScript compilation succeeds
  </acceptance_criteria>
  <done>Workflows page with 3 tabs, side panel for run preview, and template picker dialog for starting workflows</done>
</task>

</tasks>

<verification>
- /workflows page renders with 3 tabs (Active runs, My tasks, Templates)
- Templates tab only visible to admin/ops manager
- Runs table supports search, filters, sorting, pagination
- My tasks shows current user's assigned tasks with overdue sorting
- Side panel opens on row click with run summary
- Template picker allows starting a workflow run
- TypeScript compiles clean
</verification>

<success_criteria>
- Active runs table displays with all columns per UI-SPEC
- My tasks tab shows personal task queue
- Templates tab shows template management for admins
- Template picker dialog enables workflow start flow
- Overdue runs/tasks highlighted visually
- Progress (X/Y) displayed correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-workflow-engine/04-03-SUMMARY.md`
</output>
