---
phase: 08-payments
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/api/vitest.config.ts
  - packages/api/src/routers/__tests__/payment.test.ts
  - packages/api/src/services/__tests__/payment-export.test.ts
autonomous: true
requirements:
  - PAY-01
  - PAY-02
  - PAY-03
  - PAY-04
  - PAY-05

must_haves:
  truths:
    - "Vitest is configured and runs in the api package"
    - "Test stub files exist so Plan 01 and 02 can run tests as part of verify"
  artifacts:
    - path: "packages/api/vitest.config.ts"
      provides: "Vitest configuration for api package"
      min_lines: 5
    - path: "packages/api/src/routers/__tests__/payment.test.ts"
      provides: "Test stubs for payment router procedures"
      min_lines: 20
    - path: "packages/api/src/services/__tests__/payment-export.test.ts"
      provides: "Test stubs for export generators and bank statement parser"
      min_lines: 20
  key_links:
    - from: "packages/api/vitest.config.ts"
      to: "packages/api/package.json"
      via: "vitest script in package.json"
      pattern: '"test".*vitest'
---

<objective>
Wave 0: Set up vitest test infrastructure in the api package and create stub test files for payment router and export services.

Purpose: VALIDATION.md declares wave_0_complete: false and nyquist_compliant: false. Plans 08-01 and 08-02 need automated test verification. This plan creates the test scaffolding so subsequent plans can run `pnpm --filter @contractor-ops/api test` as part of their verify step.
Output: vitest.config.ts, payment.test.ts stub, payment-export.test.ts stub, test script in package.json.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-payments/08-CONTEXT.md
@.planning/phases/08-payments/08-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest config, test script, and stub test files for payment router and export services</name>
  <files>
    packages/api/vitest.config.ts,
    packages/api/package.json,
    packages/api/src/routers/__tests__/payment.test.ts,
    packages/api/src/services/__tests__/payment-export.test.ts
  </files>
  <read_first>
    packages/api/package.json,
    packages/api/tsconfig.json,
    packages/api/src/routers/payment.ts,
    packages/api/src/services/payment-export.ts
  </read_first>
  <action>
**1. Install vitest as devDependency in api package:**
Run `cd packages/api && pnpm add -D vitest` to install vitest.

**2. Create `packages/api/vitest.config.ts`:**
Minimal vitest config:
```typescript
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    globals: true,
    environment: "node",
    include: ["src/**/__tests__/**/*.test.ts"],
  },
});
```

**3. Add test script to `packages/api/package.json`:**
Add `"test": "vitest run"` to the scripts section. If a test script already exists, verify it uses vitest.

**4. Create `packages/api/src/routers/__tests__/payment.test.ts`:**
Stub test file with describe blocks for each payment router procedure. These are placeholder tests that Plan 08-01 Task 2 will flesh out. For now, each test uses `it.todo()` to mark as pending:

```typescript
import { describe, it } from "vitest";

describe("payment router", () => {
  describe("readyForPayment", () => {
    it.todo("returns invoices with paymentStatus READY");
    it.todo("filters by currency when provided");
    it.todo("filters by contractor when provided");
  });

  describe("create", () => {
    it.todo("creates a DRAFT run with sequential number PR-{year}-{seq}");
    it.todo("sets invoice paymentStatus to IN_RUN");
    it.todo("rejects invoices not in READY status");
    it.todo("groups by currency when groupByCurrency is true");
  });

  describe("listByContractor", () => {
    it.todo("returns payment items filtered by contractorId");
  });

  describe("removeFromRun", () => {
    it.todo("removes invoice from DRAFT run and resets paymentStatus to READY");
    it.todo("rejects removal from non-DRAFT runs");
    it.todo("recalculates totalGrosze and invoiceCount after removal");
  });

  describe("lockAndExport", () => {
    it.todo("transitions DRAFT to EXPORTED and returns file base64");
    it.todo("rejects transition from invalid status");
  });

  describe("updateItemStatus", () => {
    it.todo("marks item as PAID and updates invoice paymentStatus");
    it.todo("marks item as FAILED and releases invoice to READY");
    it.todo("requires failureReason when status is FAILED");
  });

  describe("markAllPaid", () => {
    it.todo("marks all PENDING items as PAID atomically");
    it.todo("sets run status to COMPLETED");
  });

  describe("cancel", () => {
    it.todo("cancels DRAFT run and releases invoices to READY");
    it.todo("requires admin role to cancel EXPORTED run");
    it.todo("rejects cancellation of COMPLETED run");
  });

  describe("importStatement", () => {
    it.todo("parses MT940 file and returns match results");
    it.todo("parses CSV file and returns match results");
  });

  describe("confirmStatementMatches", () => {
    it.todo("marks matched items as PAID");
    it.todo("auto-completes run when all items are terminal");
  });
});
```

**5. Create `packages/api/src/services/__tests__/payment-export.test.ts`:**
Stub test file for export generators and bank statement parser:

```typescript
import { describe, it } from "vitest";

describe("payment-export", () => {
  describe("generateCsv", () => {
    it.todo("generates CSV buffer with correct columns per D-06");
    it.todo("includes UTF-8 BOM for Excel compatibility");
    it.todo("formats amounts as decimal strings");
  });

  describe("generateElixir", () => {
    it.todo("generates Elixir type 110 format with CRLF line endings");
    it.todo("strips Polish diacritics to ASCII");
    it.todo("formats multiline fields with pipe delimiters");
    it.todo("uses grosze integer for amount field");
  });

  describe("generateSepaXml", () => {
    it.todo("generates valid pain.001.001.03 XML");
    it.todo("escapes XML special characters");
    it.todo("formats amounts with 2 decimal places");
    it.todo("limits MsgId to 35 characters");
  });

  describe("resolveTransferTitle", () => {
    it.todo("replaces {invoice_number} placeholder");
    it.todo("replaces multiple placeholders");
    it.todo("trims whitespace from result");
  });
});

describe("bank-statement", () => {
  describe("parseMt940", () => {
    it.todo("parses MT940 transactions with amounts in grosze");
  });

  describe("parseCsvStatement", () => {
    it.todo("parses CSV with comma separator");
    it.todo("handles semicolon separator");
    it.todo("handles comma decimal separator in amounts");
  });

  describe("parseBankStatement", () => {
    it.todo("routes .mt940 files to parseMt940");
    it.todo("routes .csv files to parseCsvStatement");
    it.todo("throws for unrecognized file extensions");
  });

  describe("matchStatementToRun", () => {
    it.todo("returns exact match when IBAN and amount match");
    it.todo("returns partial match within 1 grosze tolerance");
    it.todo("returns unmatched when no match found");
    it.todo("does not double-match items");
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/api test -- --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Vitest configured in api package. Test script runs. Stub test files exist at packages/api/src/routers/__tests__/payment.test.ts and packages/api/src/services/__tests__/payment-export.test.ts with it.todo() placeholders for all payment procedures and export functions. `pnpm --filter @contractor-ops/api test` executes without errors (all tests show as todo/skipped).</done>
</task>

</tasks>

<verification>
- Vitest runs: `pnpm --filter @contractor-ops/api test -- --reporter=verbose`
- Test files exist: `ls packages/api/src/routers/__tests__/payment.test.ts packages/api/src/services/__tests__/payment-export.test.ts`
- Test script exists: `grep '"test"' packages/api/package.json`
</verification>

<success_criteria>
Vitest is configured and running in the api package. Stub test files provide the scaffolding for Plans 08-01 and 08-02 to use `pnpm --filter @contractor-ops/api test` in their automated verify steps. VALIDATION.md Wave 0 requirements are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/08-payments/08-00-SUMMARY.md`
</output>
