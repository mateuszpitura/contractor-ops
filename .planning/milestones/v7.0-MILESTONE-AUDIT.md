---
milestone: v7.0
milestone_name: GTM Expansion
audited: 2026-07-16
resolved: 2026-07-16
status: passed  # all 3 audit gaps resolved — see Resolution section
scores:
  phases: 20/20 complete (all SUMMARYs present)
  integration_spines: 3/3 compose (treaty engine A, EmployeeProfile B, API-key actor model C)
  integration_gaps: 2 built-but-unwired producer seams + 1 partial
gaps:
  integration:
    - id: WEBHOOK-PRODUCER-UNWIRED
      severity: blocker
      state: dark-masked (behind module.outbound-webhooks — no live break yet)
      seam: "Theme C — 100 (webhook fan-out) ↔ every domain phase that should emit"
      evidence: "packages/api/src/services/webhooks/enqueue.ts:12 enqueueWebhookEvent has ZERO callers repo-wide; consumer handleWebhookPublish registered in services/outbox/handlers.ts; catalog packages/validators/src/webhooks/index.ts:11 (16 events); testFire (routers/core/webhook-subscription.ts:302) bypasses the producer so it does not mask the gap; wired sibling enqueueHrisEmployeePush IS called at routers/compliance/classification-submit.ts + routers/finance/payment-run-ops.ts"
      impact: "When module.outbound-webhooks is granted, subscribers to invoice.paid (and all 16 events) receive nothing; the P101 marketplace/n8n/Zapier triggers advertise 16 events that never fire."
      fix: "Add enqueueWebhookEvent(...) inside the existing $transaction at the domain mutations that own each event (invoice.*, payment_run.*, contractor.*, workflow.*, classification.outcome, compliance_doc.*) — mirror the enqueueHrisEmployeePush wiring already present at the same sites. Small, mechanical."
    - id: TIN-MATCH-TRIGGER-UNWIRED
      severity: warning
      state: dark-masked (behind module.us-expansion)
      seam: "Theme A — 86 (TIN match / backup withholding) trigger"
      evidence: "packages/api/src/services/tin-match.service.ts:165 matchRecipientTin + :295/:344 persistence/writer have ZERO callers; read side finance/payment-shared.ts applyWithholding + form-1099-nec.service.ts box 4 read Contractor.backupWithholdingFlagged (set by nothing); 88/deferred-items.md:35-63 assigned the trigger to Phase 86, which completed without wiring it."
      impact: "W-9 intake + year-end paths never run TIN matching; backup-withholding flag never sets, so box-4 withholding never triggers even after IRS PAF enrollment."
      fix: "Call matchRecipientTin + the backup-withholding writer from the W-9 intake path and/or the year-end 1099 build, inside the mutation transaction."
    - id: UAE-EMIRATISATION-STUB
      severity: warning
      state: partial
      seam: "Theme B — 97 HR dashboard nationalisation rollup"
      evidence: "packages/api/src/routers/workforce/hr-dashboard.ts:241 hardcodes uae = undefined while the endpoint advertises KSA Saudization + UAE Emiratisation; KSA computes, UAE returns nothing."
      impact: "UAE Emiratisation dashboard widget shows no data. Likely intentional (formula pending) but not recorded as a deferral."
      fix: "Implement the Emiratisation rate (mirror computeNationalisationDashboard for KSA) or record UAE Emiratisation as an explicit v7.x deferral."
  requirements_traceability:
    - id: TRACEABILITY-STATUS-STALE
      severity: tech_debt
      evidence: ".planning/REQUIREMENTS.md traceability table shows 43 Complete / 62 Pending, but many 'Pending' rows are for phases that ARE complete (US-PAY-* → P88 done, EMP-REG-* → P90 done, AKTA-* → P91 done). The status column + [ ] checkboxes were not updated as phases finished."
      impact: "Traceability under-reports delivery; the milestone looks 43/105 on paper while all 20 phases + their SUMMARYs are complete. Bookkeeping drift, not unbuilt work."
      fix: "Reconcile the REQUIREMENTS.md status column + checkboxes against the per-phase SUMMARYs (skip the known v7.1/v7.5/external deferrals)."
tech_debt:
  - phase: cross-milestone
    items:
      - "Per-phase VERIFICATION.md missing on ~12 phases (86,87,93,94,95,96,97,98,99,100,101) — fleet executed without running verify. 8 older phases (82-85,88-91) have VERIFICATION.md; 92+98 have UAT+SECURITY (reconciled this session)."
      - "Requirements traceability status column stale (see TRACEABILITY-STATUS-STALE)."
  - phase: known-intentional-deferrals
    items:
      - "ADP native payroll API → v7.1 (Marketplace + mTLS); CSV export ships."
      - "e-ZLA / eAU / HMRC RTI direct submission → v7.5; export-only ships."
      - "Marketplace submissions (Zapier 2-4wk / Make 1-2wk review) → external, EXTERNAL-ENABLEMENT; packages build + bundle-test now, n8n npm publish is launch-day."
      - "SDK 1.0 / Speakeasy publish + _initiatePayoutForRun → deferred."
      - "Human-visual-QA passes (portal/dashboard light/dark/mobile/RTL) + adviser legal sign-offs (local-only posture, PENDING legal review) → post-deploy."
      - "US statutory leave/WT correctly absent (no federal floor / no FLSA cap) — documented in policies/us.ts, not a gap."
---

# v7.0 GTM Expansion — Milestone Audit

**Audited:** 2026-07-16 · **Status:** `gaps_found` (2 built-but-unwired producer seams, both dark-masked; 1 partial; + traceability bookkeeping drift)

## Verdict

All 20 phases are complete with full SUMMARYs. The three shared spines most at risk of composition
failure across the parallel-built phases — the **treaty engine** (Theme A), the **EmployeeProfile registry**
(Theme B), and the **API-key actor model** (Theme C) — all **compose end-to-end**, and the 6-market
leave/working-time policy registry is correctly seed-wired including the UAE/KSA rules added this session.

Two genuine **built-but-unwired producer seams** remain, both currently masked by ship-dark flags (so no
live production break today), plus one partial and a milestone-level requirements-traceability staleness:

1. **WEBHOOK-PRODUCER-UNWIRED (BLOCKER, dark-masked)** — `enqueueWebhookEvent` has zero callers. The entire
   P100 outbound-webhook + P101 marketplace-trigger deliverable silently produces nothing the moment
   `module.outbound-webhooks` is granted. Fix = add the emit call at the domain mutations (mirror the
   already-wired `enqueueHrisEmployeePush`). **This is the one to close before enabling the flag.**
2. **TIN-MATCH-TRIGGER-UNWIRED (WARNING, dark-masked)** — TIN-match verify + backup-withholding writer have
   zero callers; the read side reads a flag nothing sets.
3. **UAE-EMIRATISATION-STUB (WARNING, partial)** — HR dashboard returns UAE Emiratisation as `undefined`.

None are live regressions (all behind default-off flags), so the milestone MAY be archived with these
recorded — but gaps #1/#2 must be wired (or explicitly logged in EXTERNAL-ENABLEMENT) before their flags flip.

## Integration spines (verified COMPOSES)

- **Theme A:** region routing (83, US→us-region client/bucket) → US profile/locale (84) → W-form + treaty (85, treaty table feeds 1042-S withholding + payment basis + portal W-8 claim) → 1099/IRIS (86) → 1042-S + classification (87) → USD ACH/Fedwire rail (88). All wired (dark behind `module.us-expansion`).
- **Theme B:** EmployeeProfile (90) feeds payroll-feed (94), HRIS apply-patch/push (95), portal session/reports (96), leave-accrual + wt-limit-scan (92), saudization-dashboard (97). Portal reuses P91/P92 domain mutations. HR dashboard composes compliance-policy expiry + probation + vacation + nationalisation. Registry seeds all 6 markets incl. UAE/KSA.
- **Theme C:** P99 api-key actor model (`apiKeyActingUserId`) fills the P98 write-half user FKs (ownerUserId/startedByUserId/completedBy/createdByUserId, fail-closed). marketplace-manifests sources triggers from the P100 event catalog + actions from the P98 OpenAPI snapshot; n8n-nodes/zapier-app derive from it (drift-tested). Marketplace dashboard calls the `marketplaceListing` router; status page fetches `/v1/status.json`. All wired — EXCEPT the producer emit (gap #1).

## Recommendation

`gaps_found` → close gap #1 (webhook producer wiring) before archiving or before the flag is enabled;
record #2/#3 as tracked follow-ons. Requirements-traceability status column should be reconciled to reflect
actual 20/20 delivery.

## Resolution (2026-07-16) — status → passed

All three integration findings resolved the same session, each via a shield-gated worktree builder + merge + verify:

- **Gap #1 WEBHOOK-PRODUCER-UNWIRED → CLOSED.** All 16 catalog events now emit `enqueueWebhookEvent(tx, …)`
  from the domain mutation that owns each transition, inside that mutation's `$transaction` (durable-iff-commit);
  shared-helper emits cover single+bulk / all settlement sources once; `contractor.update` wrapped in a tx (T1);
  cron emits `compliance_doc.*` post-durable-write. Shield S1/S2/T1 PASS (unmocked producer→outbox seam test
  `webhook-producer-emit.test.ts`); S4 partial (3 additional sibling entry points recorded — no event type left
  unemitted). Full `@contractor-ops/api` suite 4435 pass in isolation; on main: typecheck 17/17,
  webhook+owasp+tin-match 29/29. Commits under `100-11`.
- **Gap #2 TIN-MATCH-TRIGGER → CLOSED.** `matchRecipientTin` + backup-withholding writer now called from
  `tax1099.generateBatch` (year-end `revalidateYearEndTins`, box-4 populated from fresh mismatches) + the intake
  sibling `contractor.updateUsProfile`, tx-bound with `writeAuditLog({tx})`. Invariant held: a mismatch **flags,
  never blocks**; T9 fail-closed on unresolvable TIN; PII server-side only; live IRS client dark behind PAF
  enrollment (MockTinMatchClient ships; EXTERNAL-ENABLEMENT #5 updated). 14 tin-match tests (5 new incl. unmocked
  seam) + 60 adjacent green. Commits under `86-09`. Phase-88 deferred item marked RESOLVED.
- **Gap #3 UAE-EMIRATISATION-STUB → NO CHANGE (false positive).** `hr-dashboard.ts:237-241` already documents
  `uae = undefined` as intentional — UAE shares KSA's manual-headcount posture but has no UAE headcount store yet,
  so the UI shows a "record manual headcount" prompt (never a fabricated rate). Same correct-by-design class as
  US statutory-leave absence. No code change warranted.

**Remaining tech-debt (non-blocking, tracked):** the requirements-traceability status column staleness
(TRACEABILITY-STATUS-STALE) and the missing per-phase VERIFICATION.md on ~12 phases — bookkeeping, not delivery.
Milestone integration verdict upgraded to **SOUND**; safe to archive.
