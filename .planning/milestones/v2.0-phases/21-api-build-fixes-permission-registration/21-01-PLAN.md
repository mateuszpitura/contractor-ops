---
phase: 21-api-build-fixes-permission-registration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/integrations/package.json
  - packages/auth/src/permissions.ts
  - packages/auth/src/roles.ts
autonomous: true
requirements: [DOCS-01, DOCS-02, CAL-01, CAL-02, TIME-02]
gap_closure: true

must_haves:
  truths:
    - "packages/validators dist contains calendar.js, calendar.d.ts, docs.js, docs.d.ts"
    - "packages/integrations resolves notion-adapter, confluence-adapter, google-calendar-adapter, outlook-calendar-adapter subpath imports"
    - "time resource registered in permissions.ts statement with read and approve actions"
    - "All roles in roles.ts that need time permissions have them assigned"
  artifacts:
    - path: "packages/integrations/package.json"
      provides: "4 new adapter subpath exports"
      contains: "notion-adapter"
    - path: "packages/auth/src/permissions.ts"
      provides: "time resource registration"
      contains: "time:"
    - path: "packages/auth/src/roles.ts"
      provides: "time permission assignments to roles"
      contains: "time:"
  key_links:
    - from: "packages/api/src/routers/time.ts"
      to: "packages/auth/src/permissions.ts"
      via: "requirePermission({ time: [...] })"
      pattern: "time.*read.*approve"
    - from: "packages/api/src/services/calendar-event-service.ts"
      to: "packages/integrations/package.json"
      via: "import from @contractor-ops/integrations/adapters/google-calendar-adapter"
      pattern: "google-calendar-adapter"
---

<objective>
Fix the three foundation-level blockers preventing packages/api from compiling: stale validators dist, missing integrations subpath exports, and unregistered time permission.

Purpose: These are prerequisite fixes — until validators dist is rebuilt, integrations exports exist, and permissions compile, no individual file fix in Plan 02 can be verified.
Output: All three foundation packages build cleanly; downstream API compilation errors reduced from 44+ to only the individual file bugs.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-api-build-fixes-permission-registration/21-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 adapter subpath exports to integrations package.json and rebuild validators + integrations</name>
  <files>packages/integrations/package.json</files>
  <read_first>
    packages/integrations/package.json
    packages/validators/src/index.ts
  </read_first>
  <action>
1. Add 4 new subpath exports to `packages/integrations/package.json` in the `"exports"` object, after the existing `"./adapters/jira-adapter"` entry. Each follows the exact same 3-field pattern (types/import/default) as existing adapter exports:

```json
"./adapters/notion-adapter": {
  "types": "./dist/adapters/notion-adapter.d.ts",
  "import": "./dist/adapters/notion-adapter.js",
  "default": "./dist/adapters/notion-adapter.js"
},
"./adapters/confluence-adapter": {
  "types": "./dist/adapters/confluence-adapter.d.ts",
  "import": "./dist/adapters/confluence-adapter.js",
  "default": "./dist/adapters/confluence-adapter.js"
},
"./adapters/google-calendar-adapter": {
  "types": "./dist/adapters/google-calendar-adapter.d.ts",
  "import": "./dist/adapters/google-calendar-adapter.js",
  "default": "./dist/adapters/google-calendar-adapter.js"
},
"./adapters/outlook-calendar-adapter": {
  "types": "./dist/adapters/outlook-calendar-adapter.d.ts",
  "import": "./dist/adapters/outlook-calendar-adapter.js",
  "default": "./dist/adapters/outlook-calendar-adapter.js"
}
```

2. Rebuild validators package to generate dist with calendar and docs modules:
   `pnpm --filter @contractor-ops/validators build`

3. Rebuild integrations package to generate dist with new adapter exports:
   `pnpm --filter @contractor-ops/integrations build`
  </action>
  <verify>
    <automated>ls packages/validators/dist/calendar.js packages/validators/dist/docs.js && node -e "const pkg = require('./packages/integrations/package.json'); const exports = pkg.exports; ['notion-adapter','confluence-adapter','google-calendar-adapter','outlook-calendar-adapter'].forEach(a => { if (!exports['./adapters/' + a]) throw new Error('Missing: ' + a); }); console.log('All 4 adapter exports present');"</automated>
  </verify>
  <acceptance_criteria>
    - packages/integrations/package.json contains `"./adapters/notion-adapter"` export key
    - packages/integrations/package.json contains `"./adapters/confluence-adapter"` export key
    - packages/integrations/package.json contains `"./adapters/google-calendar-adapter"` export key
    - packages/integrations/package.json contains `"./adapters/outlook-calendar-adapter"` export key
    - packages/validators/dist/calendar.js exists
    - packages/validators/dist/calendar.d.ts exists
    - packages/validators/dist/docs.js exists
    - packages/validators/dist/docs.d.ts exists
    - `pnpm --filter @contractor-ops/integrations exec tsc --noEmit` exits 0
    - `pnpm --filter @contractor-ops/validators build` exits 0
  </acceptance_criteria>
  <done>All 4 adapter subpath exports added to integrations package.json. Validators and integrations packages build cleanly with dist artifacts for calendar and docs modules.</done>
</task>

<task type="auto">
  <name>Task 2: Register time resource in permissions.ts and assign to roles in roles.ts</name>
  <files>packages/auth/src/permissions.ts, packages/auth/src/roles.ts</files>
  <read_first>
    packages/auth/src/permissions.ts
    packages/auth/src/roles.ts
    packages/api/src/routers/time.ts
  </read_first>
  <action>
1. In `packages/auth/src/permissions.ts`, add `time: ["read", "approve"],` to the `statement` object after the `integration` line (before `} as const`). The result should be:
```typescript
  integration: ["read", "update"],
  time: ["read", "approve"],
} as const;
```

2. In `packages/auth/src/roles.ts`, add `time: ["read", "approve"],` to the `allPermissions` object after the `integration` line:
```typescript
  integration: ["read", "update"],
  time: ["read", "approve"],
} as const;
```

3. Add time permissions to individual roles in `roles.ts`:
   - `admin` role: add `time: ["read", "approve"],` after `integration: ["read", "update"],`
   - `finance_admin` role: add `time: ["read"],` after `settings: ["read"],`
   - `ops_manager` role: add `time: ["read", "approve"],` after `settings: ["read"],`
   - `team_manager` role: add `time: ["read", "approve"],` after `report: ["read"],`

4. Rebuild auth package: `pnpm --filter @contractor-ops/auth build`
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/auth exec tsc --noEmit && grep -c 'time:' packages/auth/src/permissions.ts packages/auth/src/roles.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/auth/src/permissions.ts contains `time: ["read", "approve"],`
    - packages/auth/src/roles.ts allPermissions contains `time: ["read", "approve"],`
    - packages/auth/src/roles.ts admin role contains `time: ["read", "approve"],`
    - packages/auth/src/roles.ts finance_admin role contains `time: ["read"],`
    - packages/auth/src/roles.ts ops_manager role contains `time: ["read", "approve"],`
    - packages/auth/src/roles.ts team_manager role contains `time: ["read", "approve"],`
    - `pnpm --filter @contractor-ops/auth exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>time resource registered in permission statement with read and approve actions. All 4 appropriate roles have time permissions. Auth package compiles cleanly.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pnpm --filter @contractor-ops/validators build` exits 0
2. `pnpm --filter @contractor-ops/integrations exec tsc --noEmit` exits 0
3. `pnpm --filter @contractor-ops/auth exec tsc --noEmit` exits 0
4. `grep 'time:' packages/auth/src/permissions.ts` shows the registration
5. `node -e "..."` confirms all 4 adapter exports in package.json
</verification>

<success_criteria>
- Validators dist contains calendar and docs modules
- Integrations package.json has 4 new adapter subpath exports and builds
- Auth package has time resource registered and builds
- These 3 foundation fixes unblock all individual file fixes in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/21-api-build-fixes-permission-registration/21-01-SUMMARY.md`
</output>
