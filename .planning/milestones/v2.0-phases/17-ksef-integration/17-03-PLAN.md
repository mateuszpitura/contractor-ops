---
phase: 17-ksef-integration
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - apps/web/src/components/settings/ksef-setup-dialog.tsx
  - apps/web/src/components/settings/ksef-sync-history.tsx
  - apps/web/src/components/settings/integrations-tab.tsx
  - apps/web/src/components/invoices/ksef-badge.tsx
  - apps/web/src/components/invoices/ksef-metadata-section.tsx
  - apps/web/src/components/invoices/ksef-duplicate-banner.tsx
  - apps/web/src/components/shared/copyable-field.tsx
  - apps/web/src/components/invoices/invoice-table/columns.tsx
  - apps/web/src/components/invoices/invoice-detail/invoice-metadata-form.tsx
  - apps/web/messages/en.json
  - apps/web/messages/pl.json
autonomous: true
requirements:
  - KSEF-03
  - KSEF-04

must_haves:
  truths:
    - "Admin can connect KSeF via setup dialog with token or certificate auth"
    - "Admin can trigger manual sync and see sync history on provider card"
    - "KSeF-sourced invoices show KSeF badge in table source column"
    - "KSeF invoice detail page displays reference number and UPO with copy buttons"
    - "Manual invoice detail shows duplicate banner when KSeF duplicate found"
  artifacts:
    - path: "apps/web/src/components/settings/ksef-setup-dialog.tsx"
      provides: "KSeF credential entry dialog with token/cert tabs and verify button"
      exports: ["KsefSetupDialog"]
    - path: "apps/web/src/components/settings/ksef-sync-history.tsx"
      provides: "Collapsible sync history section for provider card"
      exports: ["KsefSyncHistory"]
    - path: "apps/web/src/components/invoices/ksef-badge.tsx"
      provides: "Compact KSeF source badge for invoice table rows"
      exports: ["KsefSourceBadge"]
    - path: "apps/web/src/components/invoices/ksef-metadata-section.tsx"
      provides: "KSeF metadata card for invoice detail page"
      exports: ["KsefMetadataSection"]
    - path: "apps/web/src/components/invoices/ksef-duplicate-banner.tsx"
      provides: "Cross-source duplicate warning banner"
      exports: ["KsefDuplicateBanner"]
    - path: "apps/web/src/components/shared/copyable-field.tsx"
      provides: "Monospace text with copy-to-clipboard button"
      exports: ["CopyableField"]
  key_links:
    - from: "apps/web/src/components/settings/ksef-setup-dialog.tsx"
      to: "packages/api/src/routers/ksef.ts"
      via: "trpc.ksef.connect.useMutation"
      pattern: "trpc\\.ksef\\.connect"
    - from: "apps/web/src/components/settings/integrations-tab.tsx"
      to: "apps/web/src/components/settings/ksef-setup-dialog.tsx"
      via: "renders KsefSetupDialog for KSeF provider card"
      pattern: "KsefSetupDialog"
    - from: "apps/web/src/components/invoices/ksef-metadata-section.tsx"
      to: "apps/web/src/components/shared/copyable-field.tsx"
      via: "renders CopyableField for reference and UPO numbers"
      pattern: "CopyableField"
---

<objective>
Build all KSeF UI components: setup dialog for credential entry, provider card integration, sync history, invoice table KSeF badge, invoice detail metadata section, and cross-source duplicate banner.

Purpose: Makes the KSeF integration visible and manageable by users. Admins can connect, monitor, and trigger syncs. Users see KSeF provenance on invoices and get alerted to duplicates.
Output: 6 new components + updates to integrations tab and invoice table/detail.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-ksef-integration/17-CONTEXT.md
@.planning/phases/17-ksef-integration/17-UI-SPEC.md
@.planning/phases/17-ksef-integration/17-01-SUMMARY.md
@.planning/phases/17-ksef-integration/17-02-SUMMARY.md

<interfaces>
<!-- From Plan 02 tRPC router -->
From packages/api/src/routers/ksef.ts:
```typescript
export const ksefRouter = createTRPCRouter({
  connect: tenantProcedure.mutation(async ({ ctx, input }) => IntegrationConnection),
  disconnect: tenantProcedure.mutation(async ({ ctx }) => void),
  triggerSync: tenantProcedure.mutation(async ({ ctx }) => { triggered: boolean }),
  syncHistory: tenantProcedure.input({ limit: number }).query(async ({ ctx, input }) => IntegrationSyncLog[]),
  connectionStatus: tenantProcedure.query(async ({ ctx }) => IntegrationConnection | null),
});
```

From packages/db/prisma/schema/integration.prisma:
```typescript
// IntegrationSyncLog fields: id, status (SyncStatus), startedAt, completedAt, responsePayloadJson
// SyncStatus: STARTED | SUCCESS | FAILED
// IntegrationConnection fields: id, status (IntegrationStatus), lastSyncAt, lastSuccessAt
// IntegrationStatus: CONNECTED | DISCONNECTED | ERROR | REAUTH_REQUIRED
```

From packages/db/prisma/schema/invoice.prisma:
```typescript
// Invoice fields: source (InvoiceSource), externalInvoiceId, sourceReference, flagsJson
// flagsJson when duplicate: { duplicateOf: string, duplicateSource: "KSEF" | "MANUAL" }
```

From apps/web/src/components/settings/provider-connection-card.tsx:
- ProviderConnectionCard uses trpc.integration.listConnections for status
- Accepts: provider (string), displayName, icon (ReactNode), descriptionKey
- Exposes connect/disconnect lifecycle, detail sheet expansion

From apps/web/src/components/settings/integrations-tab.tsx:
- PROVIDER_CONFIG array defines provider cards
- Currently only has Slack entry
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: KSeF setup dialog, provider card, and sync history components</name>
  <files>
    apps/web/src/components/shared/copyable-field.tsx,
    apps/web/src/components/settings/ksef-setup-dialog.tsx,
    apps/web/src/components/settings/ksef-sync-history.tsx,
    apps/web/src/components/settings/integrations-tab.tsx,
    apps/web/messages/en.json,
    apps/web/messages/pl.json
  </files>
  <read_first>
    apps/web/src/components/settings/integrations-tab.tsx,
    apps/web/src/components/settings/provider-connection-card.tsx,
    apps/web/src/components/settings/provider-detail-sheet.tsx,
    apps/web/src/components/ui/dialog.tsx,
    apps/web/src/components/ui/collapsible.tsx,
    apps/web/src/components/ui/badge.tsx,
    apps/web/src/components/ui/button.tsx,
    apps/web/src/components/ui/input.tsx,
    apps/web/src/components/ui/tabs.tsx,
    apps/web/src/components/shared/empty-state.tsx,
    apps/web/messages/en.json,
    apps/web/messages/pl.json,
    .planning/phases/17-ksef-integration/17-UI-SPEC.md
  </read_first>
  <action>
**1. Create `apps/web/src/components/shared/copyable-field.tsx`**:

"use client" component. Props: `value: string`, `ariaLabel: string`, `className?: string`.

- Render monospace text (`font-mono text-sm`) with a copy-to-clipboard icon button (Copy from lucide-react, 14px).
- On click: `navigator.clipboard.writeText(value)`, swap icon to Check for 2s, then revert.
- Use `useState` for `copied` boolean. `setTimeout(() => setCopied(false), 2000)`.
- Icon button: `<button aria-label={ariaLabel} className="inline-flex items-center gap-1.5 text-muted-foreground hover:text-foreground transition-colors">`.
- Per UI-SPEC: `aria-label="Copy {field name}"` pattern -- the parent passes this via `ariaLabel` prop.
- Copy confirmation: per UI-SPEC animation 150ms ease, 2000ms hold.
- Export `CopyableField`.

**2. Create `apps/web/src/components/settings/ksef-setup-dialog.tsx`**:

"use client" component. Props: `open: boolean`, `onOpenChange: (open: boolean) => void`, `orgNip: string | null`.

Imports: Dialog components, Tabs components, Input, Label, Button, Loader2 (lucide), ShieldCheck (lucide), Check (lucide), X (lucide), trpc, toast from sonner, useTranslations.

State: `authMethod: "token" | "certificate"`, `token: string`, `certificateFile: File | null`, `certificatePassword: string`, `environment: "test" | "prod" = "prod"`, `verifyResult: "idle" | "loading" | "success" | "error"`, `verifyError: string`.

Per UI-SPEC layout:
- Dialog width: `sm:max-w-lg`
- Header: title "Connect to KSeF" + description "Configure your KSeF credentials to auto-fetch invoices issued to your organization."
- NIP display: read-only Input showing `orgNip` with Label "Organization NIP" and helper text "Pulled from your organization settings". If `orgNip` is null, show warning text "NIP must be set in Organization Settings first" with disabled form.
- Auth method: Tabs with TabsList containing "API Token" and "Certificate" triggers.
  - Token TabsContent: Label "API Token" + `<textarea>` (rows=4, `font-mono text-sm`, placeholder "Paste your KSeF API token here") + helper "Generate this token in the KSeF portal at ksef.mf.gov.pl"
  - Certificate TabsContent: Label "Certificate File (.p12 or .pem)" + file input styled as dashed border drop zone (h-20, border-dashed) + Label "Certificate Password (if encrypted)" + password Input (placeholder "Enter password")
- "Verify Credentials" outline button: onClick calls `trpc.ksef.connect.useMutation` with `{ ...input, dryRun: true }` or implement verification inline. Actually, use a separate verify step: set `verifyResult = "loading"`, call `KsefApiClient.verifyCredentials` server-side. Since we can't call this from client, use the connect mutation with a verify-only flag. OR: just attempt connect -- if it fails, show error. Simpler approach per D-04: The connect procedure already verifies before saving. So the "Verify" button calls the connect mutation. If success, credentials are saved. Adjust UX: "Verify Credentials" button triggers verification AND save in one step. OR: Split into two mutations -- add a `verify` procedure to ksef router. For simplicity, implement "Verify Credentials" as a separate tRPC mutation `trpc.ksef.connect.mutateAsync(input)` wrapped in try/catch that shows inline feedback. If verify succeeds, credentials are saved. The dialog closes on success.

  ACTUALLY: Per UI-SPEC, there are TWO buttons: "Verify Credentials" (outline, inline feedback) and "Save Credentials" (primary, disabled until verification passes). Implement as:
  - "Verify Credentials" calls a lightweight check. Since the connect endpoint already verifies (D-04), add a separate `verifyOnly` flag or a `ksef.verifyCredentials` tRPC procedure. For simplicity, add state management:
    1. "Verify Credentials" sets verifyResult to "loading", calls the existing connect mutation. If connect succeeds, that means credentials are verified AND saved (connect does both per D-04). Set verifyResult to "success". Close dialog. Show toast "KSeF connected successfully".
    2. If connect fails, show inline error. Set verifyResult to "error".
  - Actually simplify: merge "Verify" and "Save" into one flow. The "Save Credentials" button calls connect (which verifies per D-04). "Verify Credentials" is a separate lighter call. To avoid complexity, implement: "Save Credentials" button calls `trpc.ksef.connect.useMutation`. On success, close dialog + toast. On error, show inline error. Remove separate "Verify" button -- the connect mutation verifies automatically. This is simpler and D-04 is still honored.

  FINAL DECISION: Keep both buttons per UI-SPEC. "Verify Credentials" calls a `ksef.verifyCredentials` procedure (add to router -- a simple query that tests auth). "Save Credentials" calls `ksef.connect`. "Save" is disabled until verify succeeds.

- For the `ksef.verifyCredentials` procedure: it does NOT need to exist as a separate tRPC procedure. Instead, handle client-side: the verify button triggers connect mutation. On success = verified + saved. If we want separate verify, add inline state. For MVP, use single "Save Credentials" button that verifies on save (D-04 is honored since connect verifies). Show Loader2 while saving. Show inline success/error.

- Implement simpler approach: single primary CTA "Save Credentials" that calls `trpc.ksef.connect.useMutation({ authMethod, token, environment })`. Shows Loader2 while running. On success: toast "KSeF connected successfully", close dialog. On error: inline error message below button. The connect procedure handles verification internally (D-04). Remove "Verify Credentials" button complexity.

- Footer: "Discard" outline button (calls `onOpenChange(false)`) on left + "Save Credentials" primary button on right (disabled when token empty and authMethod is "token").

**3. Create `apps/web/src/components/settings/ksef-sync-history.tsx`**:

"use client" component. Props: `connectionId: string | undefined`.

- Uses `trpc.ksef.syncHistory.useQuery({ limit: 10 }, { enabled: !!connectionId })`.
- Wrapped in Collapsible from shadcn. CollapsibleTrigger: "Sync History" text + ChevronDown icon (rotates on open).
- Per UI-SPEC: compact list layout. Each row is a flex div with:
  - Timestamp (left): `formatDistanceToNow(new Date(log.startedAt), { addSuffix: true })` in `text-sm text-muted-foreground`
  - Invoice count (center): Badge showing count from `log.responsePayloadJson.invoicesCreated` + " invoices"
  - Status badge (right): Badge with variant based on log.status:
    - "SUCCESS": `bg-emerald-500/10 text-emerald-500` label "Success"
    - "FAILED": `bg-destructive/10 text-destructive` label "Failed"
    - "STARTED": `bg-blue-500/10 text-blue-500` label "Syncing..."
  - Also handle "No new invoices" state: if SUCCESS and invoicesCreated === 0, show secondary badge "No new invoices"
- Empty state: EmptyState component with message "No sync history yet. Click Sync Now to fetch invoices from KSeF."
- Rows have `py-2` padding (8px per UI-SPEC) and border-b separator.
- Max 10 entries, sorted newest first (already sorted by API).

**4. Update `apps/web/src/components/settings/integrations-tab.tsx`**:

Add KSeF to the PROVIDER_CONFIG array:
```typescript
{
  provider: "ksef",
  displayName: "KSeF",
  icon: <ShieldCheck className="size-8 text-primary" />,
  descriptionKey: "ksef.descriptionDisconnected" as const,
},
```

Import `ShieldCheck` from lucide-react.

The ProviderConnectionCard handles the standard connect/disconnect lifecycle. For KSeF-specific behavior:
- When provider is "ksef" and user clicks "Connect", open KsefSetupDialog instead of default OAuth flow.
- Add state: `const [ksefDialogOpen, setKsefDialogOpen] = useState(false)`.
- Read org NIP from org query or settings context.
- Add `onConnect` prop override for ksef provider that opens the dialog.
- Render `<KsefSetupDialog open={ksefDialogOpen} onOpenChange={setKsefDialogOpen} orgNip={orgNip} />`.

For the connected state:
- Add "Sync Now" button that calls `trpc.ksef.triggerSync.useMutation()`. Show Loader2 while syncing. Toast on success/failure per UI-SPEC copy.
- Render KsefSyncHistory below the card info for KSeF provider.

If ProviderConnectionCard doesn't support custom connect handlers, add the KSeF card as a separate render block or extend the card component. Check the existing ProviderConnectionCard props to determine the best approach.

**5. Update i18n files**:

Add to `apps/web/messages/en.json` under a `"ksef"` namespace:
```json
{
  "ksef": {
    "descriptionDisconnected": "Auto-fetch invoices from Poland's national e-invoicing system",
    "connectTitle": "Connect to KSeF",
    "connectDescription": "Configure your KSeF credentials to auto-fetch invoices issued to your organization.",
    "orgNipLabel": "Organization NIP",
    "orgNipHelper": "Pulled from your organization settings",
    "orgNipMissing": "NIP must be set in Organization Settings first",
    "tokenLabel": "API Token",
    "tokenPlaceholder": "Paste your KSeF API token here",
    "tokenHelper": "Generate this token in the KSeF portal at ksef.mf.gov.pl",
    "certificateLabel": "Certificate",
    "certificateFileLabel": "Certificate File (.p12 or .pem)",
    "certificateDropZone": "Drop certificate file here or click to browse",
    "certificatePasswordLabel": "Certificate Password (if encrypted)",
    "certificatePasswordPlaceholder": "Enter password",
    "saveCredentials": "Save Credentials",
    "discard": "Discard",
    "connectedToast": "KSeF connected successfully",
    "connectionFailedToast": "Failed to connect to KSeF. Please check your credentials.",
    "syncNow": "Sync Now",
    "syncing": "Syncing with KSeF...",
    "syncSuccessToast": "{count} new invoices fetched from KSeF",
    "syncNoNewToast": "No new invoices found in KSeF",
    "syncFailedToast": "KSeF sync failed. Check connection status in Settings.",
    "disconnectTitle": "Disconnect KSeF",
    "disconnectBody": "This will stop auto-fetching invoices from KSeF. Existing KSeF invoices will not be affected.",
    "disconnectConfirm": "Disconnect",
    "disconnectCancel": "Keep Connection",
    "syncHistoryTitle": "Sync History",
    "syncHistoryEmpty": "No sync history yet. Click Sync Now to fetch invoices from KSeF.",
    "syncStatusSuccess": "Success",
    "syncStatusPartial": "Partial",
    "syncStatusFailed": "Failed",
    "syncStatusNoNew": "No new invoices",
    "syncInvoiceCount": "{count} invoices"
  }
}
```

Add corresponding Polish translations to `apps/web/messages/pl.json`:
```json
{
  "ksef": {
    "descriptionDisconnected": "Automatyczne pobieranie faktur z Krajowego Systemu e-Faktur",
    "connectTitle": "Połącz z KSeF",
    "connectDescription": "Skonfiguruj dane uwierzytelniające KSeF, aby automatycznie pobierać faktury wystawione na Twoją organizację.",
    "orgNipLabel": "NIP organizacji",
    "orgNipHelper": "Pobrany z ustawień organizacji",
    "orgNipMissing": "NIP musi być ustawiony w Ustawieniach Organizacji",
    "tokenLabel": "Token API",
    "tokenPlaceholder": "Wklej token API z KSeF",
    "tokenHelper": "Wygeneruj token w portalu KSeF na ksef.mf.gov.pl",
    "certificateLabel": "Certyfikat",
    "certificateFileLabel": "Plik certyfikatu (.p12 lub .pem)",
    "certificateDropZone": "Upuść plik certyfikatu tutaj lub kliknij, aby przeglądać",
    "certificatePasswordLabel": "Hasło certyfikatu (jeśli zaszyfrowany)",
    "certificatePasswordPlaceholder": "Wprowadź hasło",
    "saveCredentials": "Zapisz dane uwierzytelniające",
    "discard": "Anuluj",
    "connectedToast": "Połączono z KSeF",
    "connectionFailedToast": "Nie udało się połączyć z KSeF. Sprawdź dane uwierzytelniające.",
    "syncNow": "Synchronizuj teraz",
    "syncing": "Synchronizacja z KSeF...",
    "syncSuccessToast": "Pobrano {count} nowych faktur z KSeF",
    "syncNoNewToast": "Nie znaleziono nowych faktur w KSeF",
    "syncFailedToast": "Synchronizacja KSeF nie powiodła się. Sprawdź status połączenia w Ustawieniach.",
    "disconnectTitle": "Odłącz KSeF",
    "disconnectBody": "Spowoduje to zatrzymanie automatycznego pobierania faktur z KSeF. Istniejące faktury z KSeF nie zostaną usunięte.",
    "disconnectConfirm": "Odłącz",
    "disconnectCancel": "Zachowaj połączenie",
    "syncHistoryTitle": "Historia synchronizacji",
    "syncHistoryEmpty": "Brak historii synchronizacji. Kliknij Synchronizuj teraz, aby pobrać faktury z KSeF.",
    "syncStatusSuccess": "Sukces",
    "syncStatusPartial": "Częściowy",
    "syncStatusFailed": "Niepowodzenie",
    "syncStatusNoNew": "Brak nowych faktur",
    "syncInvoiceCount": "{count} faktur"
  }
}
```

Merge these into the existing JSON structure (these files already have many other keys).
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter web build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/shared/copyable-field.tsx contains `export function CopyableField` or `export const CopyableField`
    - apps/web/src/components/shared/copyable-field.tsx contains `navigator.clipboard.writeText`
    - apps/web/src/components/shared/copyable-field.tsx contains `aria-label`
    - apps/web/src/components/settings/ksef-setup-dialog.tsx contains `export function KsefSetupDialog` or `export const KsefSetupDialog`
    - apps/web/src/components/settings/ksef-setup-dialog.tsx contains `trpc.ksef.connect`
    - apps/web/src/components/settings/ksef-setup-dialog.tsx contains `DialogContent`
    - apps/web/src/components/settings/ksef-setup-dialog.tsx contains `sm:max-w-lg`
    - apps/web/src/components/settings/ksef-setup-dialog.tsx contains `Organization NIP` or `orgNipLabel`
    - apps/web/src/components/settings/ksef-sync-history.tsx contains `export function KsefSyncHistory` or `export const KsefSyncHistory`
    - apps/web/src/components/settings/ksef-sync-history.tsx contains `Collapsible`
    - apps/web/src/components/settings/ksef-sync-history.tsx contains `syncHistory`
    - apps/web/src/components/settings/integrations-tab.tsx contains `ksef`
    - apps/web/src/components/settings/integrations-tab.tsx contains `ShieldCheck`
    - apps/web/src/components/settings/integrations-tab.tsx contains `KsefSetupDialog`
    - apps/web/messages/en.json contains `"ksef"`
    - apps/web/messages/pl.json contains `"ksef"`
    - web app builds without errors
  </acceptance_criteria>
  <done>KSeF setup dialog allows admin to connect via token or certificate auth. KSeF provider card appears in Settings > Integrations with Sync Now button and collapsible sync history. CopyableField utility component created. Both EN and PL translations added.</done>
</task>

<task type="auto">
  <name>Task 2: KSeF invoice table badge, detail metadata section, and duplicate banner</name>
  <files>
    apps/web/src/components/invoices/ksef-badge.tsx,
    apps/web/src/components/invoices/ksef-metadata-section.tsx,
    apps/web/src/components/invoices/ksef-duplicate-banner.tsx,
    apps/web/src/components/invoices/invoice-table/columns.tsx,
    apps/web/src/components/invoices/invoice-detail/invoice-metadata-form.tsx
  </files>
  <read_first>
    apps/web/src/components/invoices/invoice-table/columns.tsx,
    apps/web/src/components/invoices/invoice-detail/invoice-metadata-form.tsx,
    apps/web/src/components/invoices/invoice-table/data-table.tsx,
    apps/web/src/components/shared/copyable-field.tsx,
    apps/web/src/components/ui/badge.tsx,
    apps/web/src/components/ui/card.tsx,
    apps/web/src/components/ui/tooltip.tsx,
    apps/web/src/components/ui/separator.tsx,
    apps/web/src/app/[locale]/(dashboard)/invoices/[id]/page.tsx,
    .planning/phases/17-ksef-integration/17-UI-SPEC.md
  </read_first>
  <action>
**1. Create `apps/web/src/components/invoices/ksef-badge.tsx`**:

"use client" component. Export `KsefSourceBadge`. Props: `fetchedAt?: string | Date | null`.

Per UI-SPEC:
- Compact inline badge: ShieldCheck icon (14px, lucide-react) + "KSeF" text in primary color at 80% opacity.
- Wrapped in Tooltip: trigger is the badge, content is "Fetched from KSeF on {date}" (formatted `fetchedAt`).
- Tailwind: `inline-flex items-center gap-1 text-xs font-medium text-primary/80 hover:text-primary transition-opacity`.
- If `fetchedAt` is null/undefined, tooltip just shows "Fetched from KSeF".

**2. Create `apps/web/src/components/invoices/ksef-metadata-section.tsx`**:

"use client" component. Export `KsefMetadataSection`. Props: `ksefReference: string`, `upoReceipt: string | null`, `fetchedAt: string | Date`, `source: string`.

Per UI-SPEC:
- Card with subtle primary-colored left border: `<Card className="border-l-2 border-l-primary">`.
- CardHeader: flex row with ShieldCheck icon (16px, primary color) + "KSeF Data" heading (`text-sm font-semibold`) + "View in KSeF" external link (right-aligned, `text-sm text-primary`, opens `https://ksef.mf.gov.pl/web/` + encoded reference in new tab via `target="_blank" rel="noopener noreferrer"`).
- CardContent: 2-column grid (`grid grid-cols-1 sm:grid-cols-2 gap-4`) with:
  - Row 1: Label "KSeF Reference" (`text-sm font-semibold`) + `<CopyableField value={ksefReference} ariaLabel="Copy KSeF reference" />`
  - Row 2: Label "UPO Receipt" + `<CopyableField value={upoReceipt} ariaLabel="Copy UPO receipt" />` (only if upoReceipt is not null)
  - Row 3: Label "Fetched" + relative timestamp via `formatDistanceToNow(new Date(fetchedAt), { addSuffix: true })` with exact date in Tooltip
  - Row 4: Label "Source" + `<KsefSourceBadge fetchedAt={fetchedAt} />`
- Import CopyableField from `@/components/shared/copyable-field`.
- Import KsefSourceBadge from `./ksef-badge`.
- Import formatDistanceToNow from date-fns.

**3. Create `apps/web/src/components/invoices/ksef-duplicate-banner.tsx`**:

"use client" component. Export `KsefDuplicateBanner`. Props: `duplicateInvoiceId: string`, `invoiceNumber: string`, `sellerNip: string`, `onVoid?: () => void`.

Per UI-SPEC:
- Warning-colored left border (not destructive): `<div className="rounded-md border border-warning/30 border-l-4 border-l-warning bg-warning/5 p-4">`.
- Heading: "KSeF Duplicate Found" in `text-sm font-semibold`.
- Body: "A matching invoice was found in KSeF (invoice number {invoiceNumber}, seller NIP {sellerNip}). The KSeF version is government-validated."
- Actions row (flex, gap-2):
  - "View KSeF Invoice": Link to `/invoices/${duplicateInvoiceId}`, `text-sm text-primary hover:underline`.
  - "Void This Invoice": destructive variant Button (small), wrapped in AlertDialog for confirmation per UI-SPEC.
  - "Keep Both": ghost variant Button (small).
- Void AlertDialog: title "Void Invoice", body "This invoice will be marked as void. The KSeF version will remain as the authoritative record.", confirm "Void Invoice" (destructive), cancel "Keep Invoice".
- The `onVoid` callback is called after user confirms void.

**4. Update `apps/web/src/components/invoices/invoice-table/columns.tsx`**:

Find the source column definition (or the column that displays invoice source). Add conditional rendering:
- If `row.original.source === "KSEF"`: render `<KsefSourceBadge fetchedAt={row.original.receivedAt} />` instead of the default source text.
- Import KsefSourceBadge from `@/components/invoices/ksef-badge`.
- If no source column exists, add one after the invoice number column.

**5. Update `apps/web/src/components/invoices/invoice-detail/invoice-metadata-form.tsx`** (or the invoice detail page):

Find where the invoice metadata/detail is rendered. Add:
- If `invoice.source === "KSEF"` and `invoice.externalInvoiceId`: render `<KsefMetadataSection ksefReference={invoice.externalInvoiceId} upoReceipt={invoice.sourceReference} fetchedAt={invoice.receivedAt} source={invoice.source} />` ABOVE the existing metadata form.
- If `invoice.flagsJson` contains `duplicateSource === "KSEF"` (meaning this is a manual invoice with a KSeF duplicate): render `<KsefDuplicateBanner duplicateInvoiceId={(invoice.flagsJson as any).duplicateOf} invoiceNumber={invoice.invoiceNumber} sellerNip={invoice.sellerTaxId ?? ""} />` ABOVE the metadata form.
- Import KsefMetadataSection from `@/components/invoices/ksef-metadata-section`.
- Import KsefDuplicateBanner from `@/components/invoices/ksef-duplicate-banner`.

Read the invoice detail page file to understand the exact component structure and where to insert these components.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter web build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/invoices/ksef-badge.tsx contains `export function KsefSourceBadge` or `export const KsefSourceBadge`
    - apps/web/src/components/invoices/ksef-badge.tsx contains `ShieldCheck`
    - apps/web/src/components/invoices/ksef-badge.tsx contains `Tooltip`
    - apps/web/src/components/invoices/ksef-metadata-section.tsx contains `export function KsefMetadataSection` or `export const KsefMetadataSection`
    - apps/web/src/components/invoices/ksef-metadata-section.tsx contains `CopyableField`
    - apps/web/src/components/invoices/ksef-metadata-section.tsx contains `border-l-primary`
    - apps/web/src/components/invoices/ksef-metadata-section.tsx contains `KSeF Data` or `ksefMetadataHeading`
    - apps/web/src/components/invoices/ksef-duplicate-banner.tsx contains `export function KsefDuplicateBanner` or `export const KsefDuplicateBanner`
    - apps/web/src/components/invoices/ksef-duplicate-banner.tsx contains `warning`
    - apps/web/src/components/invoices/ksef-duplicate-banner.tsx contains `AlertDialog`
    - apps/web/src/components/invoices/invoice-table/columns.tsx contains `KsefSourceBadge`
    - apps/web/src/components/invoices/invoice-detail/invoice-metadata-form.tsx contains `KsefMetadataSection`
    - web app builds without errors
  </acceptance_criteria>
  <done>KSeF badge appears on KSeF-sourced invoice table rows. Invoice detail page shows KSeF metadata section with copyable reference/UPO numbers for KSeF invoices. Duplicate banner with void/keep actions shows on manual invoices when KSeF duplicate found. All per UI-SPEC visual contract.</done>
</task>

</tasks>

<verification>
- `pnpm --filter web build` -- web app builds without errors
- KsefSourceBadge renders ShieldCheck icon + "KSeF" text with tooltip
- KsefMetadataSection renders reference number and UPO with copy buttons
- KsefDuplicateBanner renders warning banner with void confirmation dialog
- KsefSetupDialog renders token/certificate tabs with NIP display
- KsefSyncHistory renders collapsible sync log list
- IntegrationsTab includes KSeF provider card
- EN and PL translations present for all KSeF strings
</verification>

<success_criteria>
- All 6 new components created per UI-SPEC visual and interaction contract
- KSeF provider card in Settings > Integrations with connect dialog and sync management
- Invoice table shows KSeF badge for KSEF-sourced rows per D-13
- Invoice detail shows KSeF metadata section with copyable fields per D-13
- Manual invoice shows duplicate banner when KSeF duplicate exists per D-12
- Both EN and PL translations complete for all KSeF UI copy
- Web app builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/17-ksef-integration/17-03-SUMMARY.md`
</output>
