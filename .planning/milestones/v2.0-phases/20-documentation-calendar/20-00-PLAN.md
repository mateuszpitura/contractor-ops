---
phase: 20-documentation-calendar
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/integrations/src/adapters/__tests__/notion-adapter.test.ts
  - packages/integrations/src/adapters/__tests__/confluence-adapter.test.ts
  - packages/integrations/src/adapters/__tests__/google-calendar-adapter.test.ts
  - packages/integrations/src/adapters/__tests__/outlook-calendar-adapter.test.ts
  - packages/api/src/services/__tests__/doc-link-service.test.ts
  - packages/api/src/services/__tests__/calendar-sync.test.ts
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - CAL-01
  - CAL-02

must_haves:
  truths:
    - "Every requirement has a test stub file before implementation begins"
  artifacts:
    - path: "packages/integrations/src/adapters/__tests__/notion-adapter.test.ts"
      provides: "Notion adapter test stubs"
    - path: "packages/integrations/src/adapters/__tests__/confluence-adapter.test.ts"
      provides: "Confluence adapter test stubs"
    - path: "packages/integrations/src/adapters/__tests__/google-calendar-adapter.test.ts"
      provides: "Google Calendar adapter test stubs"
    - path: "packages/integrations/src/adapters/__tests__/outlook-calendar-adapter.test.ts"
      provides: "Outlook Calendar adapter test stubs"
    - path: "packages/api/src/services/__tests__/doc-link-service.test.ts"
      provides: "Doc link service test stubs"
    - path: "packages/api/src/services/__tests__/calendar-sync.test.ts"
      provides: "Calendar sync service test stubs"
  key_links: []
---

<objective>
Create Wave 0 test stubs for all Phase 20 requirements (Nyquist compliance).

Purpose: Ensure every requirement has a failing test placeholder before any implementation begins, following the established ksef-sync.test.ts pattern.
Output: 6 test stub files with `it.todo()` entries covering DOCS-01, DOCS-02, CAL-01, CAL-02 behaviors.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for all Phase 20 services and adapters</name>
  <files>
    packages/integrations/src/adapters/__tests__/notion-adapter.test.ts,
    packages/integrations/src/adapters/__tests__/confluence-adapter.test.ts,
    packages/integrations/src/adapters/__tests__/google-calendar-adapter.test.ts,
    packages/integrations/src/adapters/__tests__/outlook-calendar-adapter.test.ts,
    packages/api/src/services/__tests__/doc-link-service.test.ts,
    packages/api/src/services/__tests__/calendar-sync.test.ts
  </files>
  <read_first>
    packages/api/src/services/__tests__/ksef-sync.test.ts
  </read_first>
  <action>
Create 6 test stub files following the exact ksef-sync.test.ts pattern (`describe` + `it.todo` entries).

**notion-adapter.test.ts:**
```typescript
import { describe, it } from "vitest";

describe("NotionAdapter", () => {
  it.todo("returns correct OAuthConfig with Notion authorization URL");
  it.todo("exchanges code for tokens using HTTP Basic auth header");
  it.todo("refreshes token using refresh_token grant type");
  it.todo("searchPages returns page id, title, icon, and lastEditedTime");
  it.todo("searchPages sends Notion-Version 2022-06-28 header");
  it.todo("searchPages filters results to pages only");
});
```

**confluence-adapter.test.ts:**
```typescript
import { describe, it } from "vitest";

describe("ConfluenceAdapter", () => {
  it.todo("returns correct OAuthConfig with Atlassian authorization URL");
  it.todo("exchanges code for tokens using JSON body auth");
  it.todo("refreshes token using refresh_token grant type");
  it.todo("searchPages returns page id, title, spaceKey, spaceName, and url");
  it.todo("searchPages uses CQL title search query");
  it.todo("discoverCloudId returns cloudId and siteName");
});
```

**google-calendar-adapter.test.ts:**
```typescript
import { describe, it } from "vitest";

describe("GoogleCalendarAdapter", () => {
  it.todo("returns correct OAuthConfig with Google authorization URL");
  it.todo("exchanges code for tokens using JSON body");
  it.todo("refreshes token using refresh_token grant type");
  it.todo("createEvent returns eventId and htmlLink");
  it.todo("updateEvent sends If-Match header with etag");
  it.todo("deleteEvent sends DELETE request to event endpoint");
});
```

**outlook-calendar-adapter.test.ts:**
```typescript
import { describe, it } from "vitest";

describe("OutlookCalendarAdapter", () => {
  it.todo("returns correct OAuthConfig with Microsoft authorization URL");
  it.todo("exchanges code for tokens using JSON body");
  it.todo("refreshes token using refresh_token grant type");
  it.todo("createEvent formats dateTime without timezone offset");
  it.todo("createEvent returns eventId and webLink");
  it.todo("deleteEvent sends DELETE request to MS Graph event endpoint");
});
```

**doc-link-service.test.ts:**
```typescript
import { describe, it } from "vitest";

describe("DocLinkService", () => {
  it.todo("attachDocLink creates ExternalLink with NOTION_PAGE externalType");
  it.todo("attachDocLink creates ExternalLink with CONFLUENCE_PAGE externalType");
  it.todo("attachDocLink stores title, icon, lastEditedTime in metadataJson");
  it.todo("detachDocLink deletes ExternalLink by id");
  it.todo("getDocLinks returns all ExternalLinks for a WORKFLOW_TASK_RUN entity");
  it.todo("refreshMetadata updates metadataJson when stale > 24h");
  it.todo("searchDocs proxies to Notion search when provider is notion");
  it.todo("searchDocs proxies to Confluence CQL when provider is confluence");
});
```

**calendar-sync.test.ts:**
```typescript
import { describe, it } from "vitest";

describe("CalendarEventService", () => {
  it.todo("createCalendarEvent calls Google Calendar events.insert");
  it.todo("createCalendarEvent calls MS Graph POST /me/calendar/events");
  it.todo("createCalendarEvent stores eventId in ExternalLink");
  it.todo("updateCalendarEvent updates existing event via provider API");
  it.todo("deleteCalendarEvent removes event and ExternalLink");
  it.todo("pushes to both personal and org calendar when both connected");
});

describe("CalendarDeadlineSync", () => {
  it.todo("creates calendar event for contract expiry date");
  it.todo("creates calendar event for approval SLA deadline");
  it.todo("creates calendar event for payment due date");
  it.todo("updates calendar event when source entity date changes");
  it.todo("deletes calendar event when source entity is deleted");
  it.todo("formats event title as [Contractor Ops] Contract expiry: {contractor} - {contract}");
  it.todo("includes deep link to entity in event description");
});

describe("CalendarTaskEventService", () => {
  it.todo("creates calendar event when task run activates with calendarEnabled config");
  it.todo("substitutes {contractor}, {contract}, {task} in title template");
  it.todo("sets event duration from configJson duration field");
  it.todo("adds attendees from configJson plus contractor and assignee");
});
```
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/api test -- --run 2>&1 | tail -5 && pnpm --filter @contractor-ops/integrations test -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/integrations/src/adapters/__tests__/notion-adapter.test.ts contains `describe("NotionAdapter"`
    - packages/integrations/src/adapters/__tests__/confluence-adapter.test.ts contains `describe("ConfluenceAdapter"`
    - packages/integrations/src/adapters/__tests__/google-calendar-adapter.test.ts contains `describe("GoogleCalendarAdapter"`
    - packages/integrations/src/adapters/__tests__/outlook-calendar-adapter.test.ts contains `describe("OutlookCalendarAdapter"`
    - packages/api/src/services/__tests__/doc-link-service.test.ts contains `describe("DocLinkService"`
    - packages/api/src/services/__tests__/calendar-sync.test.ts contains `describe("CalendarEventService"`
    - packages/api/src/services/__tests__/calendar-sync.test.ts contains `describe("CalendarDeadlineSync"`
    - All files use `it.todo(` pattern exclusively
    - Test suite passes (todos are not failures)
  </acceptance_criteria>
  <done>6 test stub files exist with it.todo entries covering all DOCS-01, DOCS-02, CAL-01, CAL-02 behaviors. Test suites pass.</done>
</task>

</tasks>

<verification>
All test stub files exist and test suites pass with todos reported.
</verification>

<success_criteria>
- 6 test stub files created
- Test suites pass in both packages/api and packages/integrations
- Every phase requirement has at least one it.todo covering its behavior
</success_criteria>

<output>
After completion, create `.planning/phases/20-documentation-calendar/20-00-SUMMARY.md`
</output>
