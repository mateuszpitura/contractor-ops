---
phase: 20-documentation-calendar
plan: 01
type: execute
wave: 1
depends_on:
  - 20-00
files_modified:
  - packages/db/prisma/schema/integration.prisma
  - packages/db/prisma/schema/contract.prisma
  - packages/validators/src/docs.ts
  - packages/validators/src/calendar.ts
  - packages/validators/src/index.ts
  - packages/integrations/src/adapters/notion-adapter.ts
  - packages/integrations/src/adapters/confluence-adapter.ts
  - packages/integrations/src/adapters/google-calendar-adapter.ts
  - packages/integrations/src/adapters/outlook-calendar-adapter.ts
  - packages/integrations/src/adapters/register-all.ts
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - CAL-01
  - CAL-02
user_setup:
  - service: notion
    why: "Documentation page linking and search"
    env_vars:
      - name: NOTION_CLIENT_ID
        source: "Notion Integrations page -> Create integration -> OAuth section"
      - name: NOTION_CLIENT_SECRET
        source: "Notion Integrations page -> Create integration -> OAuth section"
      - name: NOTION_ENCRYPTION_KEY
        source: "Generate with openssl rand -hex 32"
  - service: confluence
    why: "Documentation page linking and search"
    env_vars:
      - name: CONFLUENCE_CLIENT_ID
        source: "Atlassian Developer Console -> Create new OAuth 2.0 app (separate from Jira)"
      - name: CONFLUENCE_CLIENT_SECRET
        source: "Atlassian Developer Console -> OAuth 2.0 app settings"
      - name: CONFLUENCE_ENCRYPTION_KEY
        source: "Generate with openssl rand -hex 32"
  - service: google-calendar
    why: "Calendar deadline sync"
    env_vars:
      - name: GOOGLE_CALENDAR_CLIENT_ID
        source: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client"
      - name: GOOGLE_CALENDAR_CLIENT_SECRET
        source: "Google Cloud Console -> APIs & Services -> Credentials -> OAuth 2.0 Client"
      - name: GOOGLE_CALENDAR_ENCRYPTION_KEY
        source: "Generate with openssl rand -hex 32"
  - service: outlook
    why: "Calendar deadline sync"
    env_vars:
      - name: OUTLOOK_CLIENT_ID
        source: "Azure Portal -> App registrations -> Application (client) ID"
      - name: OUTLOOK_CLIENT_SECRET
        source: "Azure Portal -> App registrations -> Certificates & secrets -> New client secret"
      - name: OUTLOOK_ENCRYPTION_KEY
        source: "Generate with openssl rand -hex 32"

must_haves:
  truths:
    - "Four new IntegrationProvider enum values exist in Prisma schema"
    - "IntegrationConnection has optional userId field for per-user connections"
    - "Four adapter classes are registered and instantiable"
    - "Zod validators exist for doc link metadata and calendar event config"
  artifacts:
    - path: "packages/db/prisma/schema/integration.prisma"
      provides: "NOTION, CONFLUENCE, GOOGLE_CALENDAR, OUTLOOK_CALENDAR enum values + userId field"
      contains: "NOTION"
    - path: "packages/integrations/src/adapters/notion-adapter.ts"
      provides: "Notion OAuth adapter with search capability"
      exports: ["NotionAdapter"]
    - path: "packages/integrations/src/adapters/confluence-adapter.ts"
      provides: "Confluence OAuth adapter with CQL search"
      exports: ["ConfluenceAdapter"]
    - path: "packages/integrations/src/adapters/google-calendar-adapter.ts"
      provides: "Google Calendar OAuth adapter with event CRUD"
      exports: ["GoogleCalendarAdapter"]
    - path: "packages/integrations/src/adapters/outlook-calendar-adapter.ts"
      provides: "Outlook Calendar OAuth adapter with MS Graph event CRUD"
      exports: ["OutlookCalendarAdapter"]
    - path: "packages/validators/src/docs.ts"
      provides: "Doc link metadata and search schemas"
      exports: ["docLinkMetadataSchema", "docSearchResultSchema"]
    - path: "packages/validators/src/calendar.ts"
      provides: "Calendar event config and metadata schemas"
      exports: ["calendarTaskConfigSchema", "calendarEventMetadataSchema"]
  key_links:
    - from: "packages/integrations/src/adapters/register-all.ts"
      to: "All four new adapters"
      via: "registerAdapter() calls"
      pattern: "registerAdapter\\(new NotionAdapter"
---

<objective>
Create DB schema changes, Zod validators, and all four integration adapters (Notion, Confluence, Google Calendar, Outlook Calendar).

Purpose: Establish the foundation layer that all subsequent doc and calendar plans depend on — provider enum values, per-user connection model, adapter OAuth flows, and typed validator schemas.
Output: 4 adapter files, 2 validator files, updated Prisma schema, updated adapter registration.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-documentation-calendar/20-CONTEXT.md
@.planning/phases/20-documentation-calendar/20-RESEARCH.md

<interfaces>
<!-- Existing adapter pattern to follow -->

From packages/integrations/src/adapters/base-adapter.ts:
```typescript
export abstract class BaseAdapter implements IntegrationProviderAdapter {
  abstract readonly slug: string;
  abstract readonly displayName: string;
  abstract readonly supportsOAuth: boolean;
  abstract readonly supportsWebhooks: boolean;
  getOAuthConfig?(): OAuthConfig;
  exchangeCodeForTokens?(_code: string, _redirectUri: string): Promise<CredentialBlob>;
  refreshToken?(_credentials: CredentialBlob): Promise<CredentialBlob>;
  verifyWebhookSignature?(_rawBody: string, _headers: Record<string, string>): WebhookVerificationResult;
  handleWebhook?(_payload: unknown, _organizationId: string, _connectionId: string): Promise<void>;
  getHealthStatus?(_connectionId: string): Promise<ProviderHealthStatus>;
}
```

From packages/integrations/src/adapters/jira-adapter.ts (pattern reference):
```typescript
const JIRA_OAUTH_CONFIG: OAuthConfig = {
  clientIdEnvVar: "JIRA_CLIENT_ID",
  clientSecretEnvVar: "JIRA_CLIENT_SECRET",
  authorizationUrl: "https://auth.atlassian.com/authorize",
  tokenUrl: "https://auth.atlassian.com/oauth/token",
  scopes: ["read:jira-work", "write:jira-work", "manage:jira-webhook", "offline_access"],
  redirectPath: "/api/oauth/jira/callback",
};
export class JiraAdapter extends BaseAdapter { ... }
```

From packages/validators/src/jira.ts (task config pattern):
```typescript
export const jiraTaskConfigSchema = z.object({
  jiraEnabled: z.boolean().default(false),
  jiraProjectId: z.string().optional(),
  jiraProjectKey: z.string().optional(),
  jiraProjectName: z.string().optional(),
  jiraIssueTypeId: z.string().optional(),
  jiraIssueTypeName: z.string().optional(),
});
```

From packages/db/prisma/schema/integration.prisma:
```prisma
enum IntegrationProvider {
  SLACK, RESEND, GOOGLE_WORKSPACE, MICROSOFT_365, JIRA, ESIGN, DOCUSIGN, AUTENTI, KSEF, ACCOUNTING, OPEN_BANKING, GITHUB, GITLAB, CLOCKIFY
}
model IntegrationConnection {
  id, organizationId, provider, status, displayName, configJson, credentialsRef, connectedByUserId, connectedAt, lastSyncAt, lastSuccessAt, lastErrorAt, lastErrorMessage, tokenExpiresAt, refreshLockedAt, createdAt, updatedAt
  // NO userId field yet
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB schema migration + Zod validators for docs and calendar</name>
  <files>
    packages/db/prisma/schema/integration.prisma,
    packages/db/prisma/schema/contract.prisma,
    packages/validators/src/docs.ts,
    packages/validators/src/calendar.ts,
    packages/validators/src/index.ts
  </files>
  <read_first>
    packages/db/prisma/schema/integration.prisma,
    packages/db/prisma/schema/contract.prisma,
    packages/validators/src/jira.ts,
    packages/validators/src/index.ts
  </read_first>
  <action>
**1. Update integration.prisma — add 4 provider enum values:**
Add to `enum IntegrationProvider` (after CLOCKIFY):
```
NOTION
CONFLUENCE
GOOGLE_CALENDAR
OUTLOOK_CALENDAR
```

**2. Update integration.prisma — add userId to IntegrationConnection:**
Add field after `connectedByUserId`:
```prisma
userId            String?           // null = org-level, set = per-user personal connection (D-12, D-14)
user              User?             @relation("personalConnections", fields: [userId], references: [id])
```
Add composite index:
```prisma
@@index([organizationId, userId, provider])
```

**3. Update contract.prisma — no EntityType changes needed** (WORKFLOW_TASK_RUN already exists).

**4. Create packages/validators/src/docs.ts:**
```typescript
import { z } from "zod";

// Doc link metadata cached in ExternalLink.metadataJson (D-04)
export const notionPageMetadataSchema = z.object({
  title: z.string(),
  icon: z.string().nullable(),
  lastEditedTime: z.string(),
  workspaceName: z.string().optional(),
});
export type NotionPageMetadata = z.infer<typeof notionPageMetadataSchema>;

export const confluencePageMetadataSchema = z.object({
  title: z.string(),
  spaceKey: z.string(),
  spaceName: z.string(),
  lastEditedTime: z.string().optional(),
});
export type ConfluencePageMetadata = z.infer<typeof confluencePageMetadataSchema>;

// Search result shapes returned by adapters
export const docSearchResultSchema = z.object({
  id: z.string(),
  title: z.string(),
  icon: z.string().nullable().optional(),
  subtitle: z.string(), // workspace name or space name
  url: z.string(),
  provider: z.enum(["notion", "confluence"]),
});
export type DocSearchResult = z.infer<typeof docSearchResultSchema>;

// Attach doc input
export const attachDocInputSchema = z.object({
  workflowTaskRunId: z.string().cuid(),
  externalId: z.string(),
  externalUrl: z.string().url(),
  externalType: z.enum(["NOTION_PAGE", "CONFLUENCE_PAGE"]),
  metadata: z.union([notionPageMetadataSchema, confluencePageMetadataSchema]),
});
export type AttachDocInput = z.infer<typeof attachDocInputSchema>;

// Search input
export const docSearchInputSchema = z.object({
  query: z.string().min(1).max(200),
  provider: z.enum(["notion", "confluence", "all"]).default("all"),
});
export type DocSearchInput = z.infer<typeof docSearchInputSchema>;
```

**5. Create packages/validators/src/calendar.ts:**
```typescript
import { z } from "zod";

// Per-task calendar config stored in WorkflowTaskTemplate.configJson (D-09)
export const calendarTaskConfigSchema = z.object({
  calendarEnabled: z.boolean().default(false),
  titleTemplate: z.string().max(200).optional(),
  duration: z.enum(["30m", "1h", "2h", "4h", "full_day"]).default("1h"),
  attendees: z.array(z.string().email()).default([]),
});
export type CalendarTaskConfig = z.infer<typeof calendarTaskConfigSchema>;

// Calendar event metadata stored in ExternalLink.metadataJson (D-11)
export const calendarEventMetadataSchema = z.object({
  eventId: z.string(),
  calendarId: z.string().optional(),
  title: z.string(),
  startTime: z.string(),
  endTime: z.string(),
  link: z.string().optional(),
  etag: z.string().optional(), // Google Calendar etag for concurrency (Pitfall 4)
  provider: z.enum(["google_calendar", "outlook_calendar"]),
});
export type CalendarEventMetadata = z.infer<typeof calendarEventMetadataSchema>;

// Deadline event types (D-08)
export const deadlineTypeSchema = z.enum([
  "CONTRACT_EXPIRY",
  "APPROVAL_SLA",
  "PAYMENT_DUE",
]);
export type DeadlineType = z.infer<typeof deadlineTypeSchema>;

// Calendar event creation input
export const createCalendarEventInputSchema = z.object({
  summary: z.string().max(300),
  description: z.string().max(2000).optional(),
  startDateTime: z.string(), // ISO 8601
  endDateTime: z.string(),
  attendees: z.array(z.string().email()).optional(),
});
export type CreateCalendarEventInput = z.infer<typeof createCalendarEventInputSchema>;
```

**6. Update packages/validators/src/index.ts:**
Add exports:
```typescript
export * from "./docs.js";
export * from "./calendar.js";
```

**7. Run prisma db push** to apply schema changes.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && npx prisma db push --schema packages/db/prisma/schema 2>&1 | tail -5 && pnpm --filter @contractor-ops/validators exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/db/prisma/schema/integration.prisma contains `NOTION`
    - packages/db/prisma/schema/integration.prisma contains `CONFLUENCE`
    - packages/db/prisma/schema/integration.prisma contains `GOOGLE_CALENDAR`
    - packages/db/prisma/schema/integration.prisma contains `OUTLOOK_CALENDAR`
    - packages/db/prisma/schema/integration.prisma contains `userId            String?`
    - packages/db/prisma/schema/integration.prisma contains `@@index([organizationId, userId, provider])`
    - packages/validators/src/docs.ts contains `export const docSearchResultSchema`
    - packages/validators/src/docs.ts contains `export const attachDocInputSchema`
    - packages/validators/src/calendar.ts contains `export const calendarTaskConfigSchema`
    - packages/validators/src/calendar.ts contains `export const calendarEventMetadataSchema`
    - packages/validators/src/calendar.ts contains `export const deadlineTypeSchema`
    - packages/validators/src/index.ts contains `export * from "./docs`
    - packages/validators/src/index.ts contains `export * from "./calendar`
    - prisma db push succeeds
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Prisma schema has 4 new providers + userId field. Two validator files export all doc and calendar schemas. DB migration applied.</done>
</task>

<task type="auto">
  <name>Task 2: Create four integration adapters and register them</name>
  <files>
    packages/integrations/src/adapters/notion-adapter.ts,
    packages/integrations/src/adapters/confluence-adapter.ts,
    packages/integrations/src/adapters/google-calendar-adapter.ts,
    packages/integrations/src/adapters/outlook-calendar-adapter.ts,
    packages/integrations/src/adapters/register-all.ts
  </files>
  <read_first>
    packages/integrations/src/adapters/jira-adapter.ts,
    packages/integrations/src/adapters/base-adapter.ts,
    packages/integrations/src/adapters/register-all.ts,
    packages/integrations/src/types/provider.ts,
    packages/integrations/src/types/credentials.ts
  </read_first>
  <action>
**1. Create notion-adapter.ts** following JiraAdapter pattern exactly:

OAuth config:
```typescript
const NOTION_OAUTH_CONFIG: OAuthConfig = {
  clientIdEnvVar: "NOTION_CLIENT_ID",
  clientSecretEnvVar: "NOTION_CLIENT_SECRET",
  authorizationUrl: "https://api.notion.com/v1/oauth/authorize",
  tokenUrl: "https://api.notion.com/v1/oauth/token",
  scopes: [], // Notion uses integration capabilities, not scopes (Pitfall 2)
  redirectPath: "/api/oauth/notion/callback",
};
```

Class: `export class NotionAdapter extends BaseAdapter`
- `slug = "notion"`, `displayName = "Notion"`, `supportsOAuth = true`, `supportsWebhooks = false`
- `getOAuthConfig()` returns NOTION_OAUTH_CONFIG
- `exchangeCodeForTokens()` — CRITICAL: Use HTTP Basic auth header `Authorization: Basic ${Buffer.from(clientId + ':' + clientSecret).toString('base64')}` (Pitfall 1). Do NOT put client_id/client_secret in JSON body.
- `refreshToken()` — Same HTTP Basic auth pattern, `grant_type: "refresh_token"`
- `getHealthStatus()` — Follow JiraAdapter pattern with prisma imports
- Add `searchPages(accessToken: string, query: string)` method — POST to `https://api.notion.com/v1/search` with `Notion-Version: 2022-06-28` header (Pitfall 7), filter `{ property: "object", value: "page" }`, `page_size: 10`. Return `Array<{ id, title, icon, lastEditedTime, url }>`. Extract title from `page.properties?.title?.title?.[0]?.plain_text ?? "Untitled"`. Build URL as `https://notion.so/${page.id.replace(/-/g, "")}`.

**2. Create confluence-adapter.ts:**

OAuth config (separate app from Jira per Pitfall 3):
```typescript
const CONFLUENCE_OAUTH_CONFIG: OAuthConfig = {
  clientIdEnvVar: "CONFLUENCE_CLIENT_ID",
  clientSecretEnvVar: "CONFLUENCE_CLIENT_SECRET",
  authorizationUrl: "https://auth.atlassian.com/authorize",
  tokenUrl: "https://auth.atlassian.com/oauth/token",
  scopes: ["search:confluence", "read:confluence-content.summary", "offline_access"],
  redirectPath: "/api/oauth/confluence/callback",
};
export const CONFLUENCE_EXTRA_AUTH_PARAMS: Record<string, string> = {
  audience: "api.atlassian.com",
  prompt: "consent",
};
```

Class: `export class ConfluenceAdapter extends BaseAdapter`
- `slug = "confluence"`, `displayName = "Confluence"`, `supportsOAuth = true`, `supportsWebhooks = false`
- `exchangeCodeForTokens()` — JSON body auth (same as JiraAdapter pattern)
- `refreshToken()` — standard OAuth refresh
- `discoverCloudId(accessToken)` — same as JiraAdapter.discoverCloudId (accessible-resources endpoint)
- `searchPages(accessToken: string, cloudId: string, query: string)` — GET `https://api.atlassian.com/ex/confluence/${cloudId}/wiki/rest/api/search?cql=${encodeURIComponent(`type=page AND title~"${query}"`)}&limit=10`. Return `Array<{ id, title, spaceKey, spaceName, url }>`. Build URL as `https://${cloudId}.atlassian.net/wiki${r.content._links?.webui}`.
- `getHealthStatus()` — Follow JiraAdapter pattern

**3. Create google-calendar-adapter.ts:**

OAuth config:
```typescript
const GOOGLE_CALENDAR_OAUTH_CONFIG: OAuthConfig = {
  clientIdEnvVar: "GOOGLE_CALENDAR_CLIENT_ID",
  clientSecretEnvVar: "GOOGLE_CALENDAR_CLIENT_SECRET",
  authorizationUrl: "https://accounts.google.com/o/oauth2/v2/auth",
  tokenUrl: "https://oauth2.googleapis.com/token",
  scopes: ["https://www.googleapis.com/auth/calendar.events"],
  redirectPath: "/api/oauth/google-calendar/callback",
};
export const GOOGLE_CALENDAR_EXTRA_AUTH_PARAMS: Record<string, string> = {
  access_type: "offline",
  prompt: "consent",
};
```

Class: `export class GoogleCalendarAdapter extends BaseAdapter`
- `slug = "google-calendar"`, `displayName = "Google Calendar"`, `supportsOAuth = true`, `supportsWebhooks = false`
- `exchangeCodeForTokens()` — JSON body auth
- `refreshToken()` — standard OAuth refresh with `https://oauth2.googleapis.com/token`
- `createEvent(accessToken, event: { summary, description, startDateTime, endDateTime, attendees? })` — POST to `https://www.googleapis.com/calendar/v3/calendars/primary/events`. Set `reminders: { useDefault: true }`. Return `{ eventId: data.id, htmlLink: data.htmlLink, etag: data.etag }`.
- `updateEvent(accessToken, eventId, event, etag)` — PATCH to `https://www.googleapis.com/calendar/v3/calendars/primary/events/${eventId}` with `If-Match: ${etag}` header (Pitfall 4). Return updated event.
- `deleteEvent(accessToken, eventId)` — DELETE to same endpoint.
- `getHealthStatus()` — Follow JiraAdapter pattern

**4. Create outlook-calendar-adapter.ts:**

OAuth config:
```typescript
const OUTLOOK_OAUTH_CONFIG: OAuthConfig = {
  clientIdEnvVar: "OUTLOOK_CLIENT_ID",
  clientSecretEnvVar: "OUTLOOK_CLIENT_SECRET",
  authorizationUrl: "https://login.microsoftonline.com/common/oauth2/v2.0/authorize",
  tokenUrl: "https://login.microsoftonline.com/common/oauth2/v2.0/token",
  scopes: ["Calendars.ReadWrite", "offline_access"],
  redirectPath: "/api/oauth/outlook/callback",
};
```

Class: `export class OutlookCalendarAdapter extends BaseAdapter`
- `slug = "outlook-calendar"`, `displayName = "Outlook Calendar"`, `supportsOAuth = true`, `supportsWebhooks = false`
- `exchangeCodeForTokens()` — JSON body auth
- `refreshToken()` — standard OAuth refresh
- `createEvent(accessToken, event: { subject, bodyHtml, startDateTime, endDateTime, attendees? })` — POST to `https://graph.microsoft.com/v1.0/me/calendar/events`. Format dates as `{ dateTime: "YYYY-MM-DDTHH:mm:ss", timeZone: "UTC" }` (Pitfall 5). Return `{ eventId: data.id, webLink: data.webLink }`.
- `updateEvent(accessToken, eventId, event)` — PATCH to `https://graph.microsoft.com/v1.0/me/calendar/events/${eventId}`.
- `deleteEvent(accessToken, eventId)` — DELETE to same endpoint.
- `getHealthStatus()` — Follow JiraAdapter pattern

**5. Update register-all.ts:**
Add imports and registrations for all 4 new adapters:
```typescript
import { NotionAdapter } from "./notion-adapter.js";
import { ConfluenceAdapter } from "./confluence-adapter.js";
import { GoogleCalendarAdapter } from "./google-calendar-adapter.js";
import { OutlookCalendarAdapter } from "./outlook-calendar-adapter.js";
// ... in registerAllAdapters():
registerAdapter(new NotionAdapter());
registerAdapter(new ConfluenceAdapter());
registerAdapter(new GoogleCalendarAdapter());
registerAdapter(new OutlookCalendarAdapter());
```
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/integrations exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - packages/integrations/src/adapters/notion-adapter.ts contains `export class NotionAdapter extends BaseAdapter`
    - packages/integrations/src/adapters/notion-adapter.ts contains `Notion-Version`
    - packages/integrations/src/adapters/notion-adapter.ts contains `Basic ${Buffer.from`
    - packages/integrations/src/adapters/confluence-adapter.ts contains `export class ConfluenceAdapter extends BaseAdapter`
    - packages/integrations/src/adapters/confluence-adapter.ts contains `search:confluence`
    - packages/integrations/src/adapters/google-calendar-adapter.ts contains `export class GoogleCalendarAdapter extends BaseAdapter`
    - packages/integrations/src/adapters/google-calendar-adapter.ts contains `If-Match`
    - packages/integrations/src/adapters/outlook-calendar-adapter.ts contains `export class OutlookCalendarAdapter extends BaseAdapter`
    - packages/integrations/src/adapters/outlook-calendar-adapter.ts contains `graph.microsoft.com`
    - packages/integrations/src/adapters/register-all.ts contains `registerAdapter(new NotionAdapter())`
    - packages/integrations/src/adapters/register-all.ts contains `registerAdapter(new ConfluenceAdapter())`
    - packages/integrations/src/adapters/register-all.ts contains `registerAdapter(new GoogleCalendarAdapter())`
    - packages/integrations/src/adapters/register-all.ts contains `registerAdapter(new OutlookCalendarAdapter())`
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Four adapters extend BaseAdapter with OAuth flows and provider-specific API methods. All registered in registerAllAdapters. TypeScript clean.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @contractor-ops/integrations exec tsc --noEmit` passes
- `pnpm --filter @contractor-ops/validators exec tsc --noEmit` passes
- `npx prisma db push` applied successfully
- All 4 adapters registered in register-all.ts
</verification>

<success_criteria>
- 4 new IntegrationProvider enum values in Prisma schema
- IntegrationConnection.userId field exists for per-user connections
- 4 adapter classes created following JiraAdapter pattern
- 2 validator files with all doc/calendar Zod schemas
- All adapters registered in registerAllAdapters()
- TypeScript compilation clean across packages
</success_criteria>

<output>
After completion, create `.planning/phases/20-documentation-calendar/20-01-SUMMARY.md`
</output>
