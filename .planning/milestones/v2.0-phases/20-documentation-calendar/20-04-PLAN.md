---
phase: 20-documentation-calendar
plan: 04
type: execute
wave: 3
depends_on:
  - 20-02
files_modified:
  - apps/web/src/components/integrations/doc-link-chip.tsx
  - apps/web/src/components/integrations/doc-links-section.tsx
  - apps/web/src/components/integrations/attach-doc-dialog.tsx
  - apps/web/src/components/integrations/provider-icons.tsx
  - apps/web/src/components/search/command-palette.tsx
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "User can see linked Notion/Confluence pages as clickable chips on workflow task cards"
    - "User can attach a doc page via search dialog on workflow task card"
    - "User can remove a doc link from a workflow task card"
    - "User can search for doc pages from Cmd+K and open them in a new tab"
    - "Cmd+K shows a Docs group alongside existing search result groups"
  artifacts:
    - path: "apps/web/src/components/integrations/doc-link-chip.tsx"
      provides: "Clickable chip for linked Notion/Confluence page"
      exports: ["DocLinkChip"]
    - path: "apps/web/src/components/integrations/doc-links-section.tsx"
      provides: "Section with doc chips + attach button for workflow task cards"
      exports: ["DocLinksSection"]
    - path: "apps/web/src/components/integrations/attach-doc-dialog.tsx"
      provides: "Search dialog for finding and attaching doc pages"
      exports: ["AttachDocDialog"]
    - path: "apps/web/src/components/search/command-palette.tsx"
      provides: "Extended Cmd+K with Docs search group"
      contains: "Docs"
  key_links:
    - from: "apps/web/src/components/integrations/doc-links-section.tsx"
      to: "packages/api/src/routers/docs.ts"
      via: "trpc.docs.list and trpc.docs.detach"
      pattern: "trpc\\.docs\\."
    - from: "apps/web/src/components/integrations/attach-doc-dialog.tsx"
      to: "packages/api/src/routers/docs.ts"
      via: "trpc.docs.search and trpc.docs.attach"
      pattern: "trpc\\.docs\\.(search|attach)"
    - from: "apps/web/src/components/search/command-palette.tsx"
      to: "packages/api/src/routers/docs.ts"
      via: "trpc.docs.search"
      pattern: "trpc\\.docs\\.search"
---

<objective>
Build the documentation UI: doc link chips, attach dialog with search, doc links section for workflow task cards, and Cmd+K "Docs" group extension.

Purpose: Delivers the user-facing surfaces for DOCS-01 (attach/view doc links on workflow steps) and DOCS-02 (search docs from Cmd+K).
Output: 3 new components + command palette extension + provider icons.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-documentation-calendar/20-CONTEXT.md
@.planning/phases/20-documentation-calendar/20-UI-SPEC.md
@.planning/phases/20-documentation-calendar/20-02-SUMMARY.md

<interfaces>
From packages/api/src/routers/docs.ts (created in Plan 02):
```typescript
export const docsRouter = router({
  attach: tenantProcedure.input(attachDocInputSchema).mutation(...),
  detach: tenantProcedure.input(z.object({ externalLinkId: z.string().cuid() })).mutation(...),
  list: tenantProcedure.input(z.object({ workflowTaskRunId: z.string().cuid() })).query(...),
  search: tenantProcedure.input(docSearchInputSchema).query(...),
  refreshMetadata: tenantProcedure.input(z.object({ externalLinkId: z.string().cuid() })).mutation(...),
});
```

From apps/web/src/components/integrations/jira-issue-chip.tsx (pattern reference):
```typescript
// Clickable chip with provider icon, title text, tooltip on hover, external link behavior
export function JiraIssueChip({ issueKey, summary, statusName, url }: Props) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DocLinkChip, provider icons, DocLinksSection, and AttachDocDialog components</name>
  <files>
    apps/web/src/components/integrations/doc-link-chip.tsx,
    apps/web/src/components/integrations/doc-links-section.tsx,
    apps/web/src/components/integrations/attach-doc-dialog.tsx,
    apps/web/src/components/integrations/provider-icons.tsx
  </files>
  <read_first>
    apps/web/src/components/integrations/jira-issue-chip.tsx,
    apps/web/src/components/search/command-palette.tsx,
    apps/web/src/components/ui/dialog.tsx,
    apps/web/src/components/ui/badge.tsx,
    apps/web/src/components/ui/tooltip.tsx,
    apps/web/src/components/ui/scroll-area.tsx,
    apps/web/src/components/shared/empty-state.tsx
  </read_first>
  <action>
**1. Create provider-icons.tsx** — SVG icon components for Notion, Confluence, Google Calendar, Outlook:

```typescript
// NotionIcon: monochrome Notion "N" logo, accepts className for sizing
// ConfluenceIcon: Atlassian blue (#0052CC) Confluence logo
// GoogleCalendarIcon: Google Calendar multi-color logo
// OutlookCalendarIcon: Microsoft blue (#0078D4) Outlook logo
// All accept { className?: string } prop, render SVG with aria-hidden="true"
```

**2. Create doc-link-chip.tsx** per UI-SPEC DocLinkChip spec:

Props: `{ id: string; title: string; url: string; provider: "notion" | "confluence"; lastEditedTime?: string; readOnly?: boolean; onRemove?: (id: string) => void }`

Layout:
- Render as `<a href={url} target="_blank" rel="noopener noreferrer" aria-label="Open ${title} in ${provider === 'notion' ? 'Notion' : 'Confluence'} (new tab)">`
- Container: `bg-muted/50 border border-border rounded-md py-1 px-2 inline-flex items-center gap-1 max-w-[220px] hover:bg-muted transition-colors duration-150`
- Provider icon: NotionIcon or ConfluenceIcon at 14px (`h-3.5 w-3.5`)
- Title: `<span className="text-xs font-medium text-foreground truncate">{title}</span>`
- Remove button (when !readOnly and onRemove provided): `<button>` with X icon (12px), `opacity-0 group-hover:opacity-100 transition-opacity duration-150 text-muted-foreground hover:text-destructive`, `aria-label="Remove link to ${title}"`
- Add `group` class to container for hover-based remove button visibility
- Tooltip on hover: "Last edited {relative time}" using lastEditedTime
- Clicking remove button: `e.preventDefault(); e.stopPropagation();` then trigger AlertDialog confirmation per UI-SPEC copy

**3. Create doc-links-section.tsx** per UI-SPEC DocLinksSection spec:

Props: `{ workflowTaskRunId: string; readOnly?: boolean }`

- Header row: `<FileText className="h-3.5 w-3.5" />` + `<span className="text-sm font-semibold">Documents</span>` + (when !readOnly) `<Button variant="ghost" size="sm">Attach Document</Button>` right-aligned
- Body: `trpc.docs.list.useQuery({ workflowTaskRunId })` — render DocLinkChip for each result in `flex flex-wrap gap-2`
- Empty state: `<p className="text-xs text-muted-foreground">No documents attached.</p>`
- Loading state: 2 Skeleton pills (140px wide, 24px height)
- Remove handler: calls `trpc.docs.detach.useMutation()`, invalidates list query, shows toast "Document link removed" via sonner
- AlertDialog for remove confirmation with UI-SPEC copy: title "Remove Document Link", body 'This will remove the link to "{title}" from this step. The original page will not be affected.', confirm "Remove Link", cancel "Keep Link"
- "Attach Document" button opens AttachDocDialog

**4. Create attach-doc-dialog.tsx** per UI-SPEC AttachDocDialog spec:

Props: `{ workflowTaskRunId: string; open: boolean; onOpenChange: (open: boolean) => void }`

- Dialog width: `sm:max-w-lg`
- Header: "Attach Document" title + "Search for a page to link to this step." description
- Search input: `<Input placeholder="Search Notion and Confluence pages..." />` with 200ms debounce (use setTimeout pattern from command-palette.tsx)
- Provider filter: inline toggle group "All" | "Notion" (with NotionIcon) | "Confluence" (with ConfluenceIcon)
- Results: `trpc.docs.search.useQuery({ query: debouncedQuery, provider: selectedFilter }, { enabled: debouncedQuery.length > 0 })`
- Each result row in ScrollArea (max-h-80): provider icon (14px) + page title (`text-sm font-medium flex-1 truncate`) + workspace/space name (`text-xs text-muted-foreground`)
- Click on result: call `trpc.docs.attach.useMutation()` with result data, close dialog on success, show toast '"{page title}" linked to this step', invalidate docs.list
- Loading: 3 Skeleton rows (icon circle + title 60% + subtitle 30%)
- Empty: "No pages found matching your search."
- No providers connected: "Connect Notion or Confluence in Settings to search for pages."
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/integrations/provider-icons.tsx contains `export function NotionIcon`
    - apps/web/src/components/integrations/provider-icons.tsx contains `export function ConfluenceIcon`
    - apps/web/src/components/integrations/doc-link-chip.tsx contains `export function DocLinkChip`
    - apps/web/src/components/integrations/doc-link-chip.tsx contains `target="_blank"`
    - apps/web/src/components/integrations/doc-link-chip.tsx contains `Remove link to`
    - apps/web/src/components/integrations/doc-links-section.tsx contains `export function DocLinksSection`
    - apps/web/src/components/integrations/doc-links-section.tsx contains `trpc.docs.list`
    - apps/web/src/components/integrations/doc-links-section.tsx contains `Attach Document`
    - apps/web/src/components/integrations/attach-doc-dialog.tsx contains `export function AttachDocDialog`
    - apps/web/src/components/integrations/attach-doc-dialog.tsx contains `trpc.docs.search`
    - apps/web/src/components/integrations/attach-doc-dialog.tsx contains `trpc.docs.attach`
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>4 UI components created: provider icons, doc link chip with hover remove and tooltip, doc links section with CRUD, attach dialog with search and filter.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Cmd+K command palette with Docs search group</name>
  <files>apps/web/src/components/search/command-palette.tsx</files>
  <read_first>
    apps/web/src/components/search/command-palette.tsx,
    apps/web/src/components/integrations/provider-icons.tsx
  </read_first>
  <action>
Modify the existing command-palette.tsx to add a "Docs" search group per D-05, D-06, D-07.

**1. Add doc type to SearchResultItem union and TYPE_BADGE_CLASSES:**
- Extend the `SearchResultItem.type` to include `"doc"`
- Add to TYPE_BADGE_CLASSES: `doc: "bg-chart-3/10 text-chart-3 border-transparent"` (per UI-SPEC)

**2. Add doc search query:**
- Add a new tRPC query: `trpc.docs.search.useQuery({ query: debouncedQuery, provider: "all" }, { enabled: isOpen && debouncedQuery.length >= 2 })`
- This runs alongside the existing global search query

**3. Add "Docs" CommandGroup:**
- Position: after entity results (Contractors, Contracts, Invoices), before "Pages" group
- Group heading: "Docs"
- Max 5 results shown in Cmd+K (UI-SPEC limit)
- Each result: CommandItem with `onSelect={() => window.open(result.url, "_blank")}` (D-07 — opens in new tab, does NOT navigate within app)
- Result layout: provider icon (NotionIcon or ConfluenceIcon at 14px) + page title (`text-sm font-medium`) + workspace/space name (`text-xs text-muted-foreground` right-aligned)
- Badge: `<Badge className={TYPE_BADGE_CLASSES.doc}>doc</Badge>`

**4. Loading state:**
- When doc search is loading: 3 Skeleton rows within "Docs" CommandGroup
- Matches existing skeleton pattern in the command palette

**5. Import provider icons:**
- Import NotionIcon and ConfluenceIcon from `@/components/integrations/provider-icons`

Key behaviors to preserve:
- Existing search for contractors, contracts, invoices must continue to work unchanged
- Existing keyboard navigation, pinning, recent items must remain functional
- Doc results must NOT interfere with existing debounce or focus management
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/search/command-palette.tsx contains `"Docs"`
    - apps/web/src/components/search/command-palette.tsx contains `doc: "bg-chart-3/10 text-chart-3 border-transparent"`
    - apps/web/src/components/search/command-palette.tsx contains `trpc.docs.search`
    - apps/web/src/components/search/command-palette.tsx contains `window.open`
    - apps/web/src/components/search/command-palette.tsx contains `_blank`
    - apps/web/src/components/search/command-palette.tsx contains `NotionIcon`
    - TypeScript compilation passes
  </acceptance_criteria>
  <done>Cmd+K command palette extended with "Docs" group showing Notion/Confluence search results that open in new tabs. Existing search functionality preserved.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @contractor-ops/web exec tsc --noEmit` passes
- All 4 new component files exist
- Command palette has "Docs" group
- Doc chips open in new tabs per D-07
</verification>

<success_criteria>
- DocLinkChip renders as clickable link opening in new tab with provider icon and title
- DocLinksSection shows attached docs with attach/remove capability
- AttachDocDialog provides search with provider filter and single-click attach
- Cmd+K shows "Docs" group with max 5 results, opening in new tab on select
- All accessibility requirements met (aria-labels, keyboard navigation, semantic HTML)
</success_criteria>

<output>
After completion, create `.planning/phases/20-documentation-calendar/20-04-SUMMARY.md`
</output>
