---
phase: 31-google-workspace-directory-import
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/integrations/google-workspace-provider-section.tsx
autonomous: true
gap_closure: true
requirements:
  - GOOG-01

must_haves:
  truths:
    - "ProviderConnectionCard receives provider slug matching adapter registration ('google_workspace' with underscore)"
    - "Health check query uses 'google_workspace' so toUpperCase produces valid GOOGLE_WORKSPACE Prisma enum"
    - "Post-OAuth redirect param check uses 'google_workspace' matching the callback URL param"
  artifacts:
    - path: "apps/web/src/components/integrations/google-workspace-provider-section.tsx"
      provides: "Google Workspace provider section with correct underscore slug"
      contains: "google_workspace"
  key_links:
    - from: "google-workspace-provider-section.tsx"
      to: "packages/integrations/src/adapters/google-workspace-adapter.ts"
      via: "ProviderConnectionCard provider prop -> getAdapter(slug)"
      pattern: 'provider.*"google_workspace"'
---

<objective>
Fix provider slug mismatch that breaks OAuth connect, health check, disconnect, and post-OAuth wizard auto-open.

Purpose: The adapter is registered as "google_workspace" (underscore) but the provider section component passes "google-workspace" (hyphen) in 3 places, breaking all connection functionality (GOOG-01 blocker).
Output: Corrected provider section component with consistent underscore slug.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-google-workspace-directory-import/31-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix provider slug from hyphen to underscore in google-workspace-provider-section.tsx</name>
  <files>apps/web/src/components/integrations/google-workspace-provider-section.tsx</files>
  <action>
    In `apps/web/src/components/integrations/google-workspace-provider-section.tsx`, change exactly 3 string literals from "google-workspace" (hyphen) to "google_workspace" (underscore):

    1. Line 25: `provider: "google-workspace"` -> `provider: "google_workspace"` (health query options)
    2. Line 35: `searchParams.get("google-workspace")` -> `searchParams.get("google_workspace")` (post-OAuth redirect detection)
    3. Line 43: `provider="google-workspace"` -> `provider="google_workspace"` (ProviderConnectionCard prop)

    This aligns the component with the adapter slug registered in `packages/integrations/src/adapters/google-workspace-adapter.ts` (slug = "google_workspace"), so that:
    - `getAdapter("google_workspace")` resolves the adapter (Connect button works)
    - `"google_workspace".toUpperCase()` produces `"GOOGLE_WORKSPACE"` matching the Prisma enum (health check and disconnect work)
    - `searchParams.get("google_workspace")` matches the OAuth callback redirect param (auto-open wizard works)

    Do NOT change anything else in this file. This is a surgical 3-line fix.
  </action>
  <verify>
    <automated>grep -c "google-workspace" apps/web/src/components/integrations/google-workspace-provider-section.tsx | grep -q "^0$" && grep -c "google_workspace" apps/web/src/components/integrations/google-workspace-provider-section.tsx | grep -q "^3$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All 3 occurrences of "google-workspace" in the provider section are replaced with "google_workspace". Zero hyphenated slugs remain. The file contains exactly 3 underscore slug references.</done>
</task>

</tasks>

<verification>
1. `grep "google-workspace" apps/web/src/components/integrations/google-workspace-provider-section.tsx` returns no matches
2. `grep "google_workspace" apps/web/src/components/integrations/google-workspace-provider-section.tsx` returns exactly 3 matches
3. `npx tsc --noEmit` passes (type check confirms string literal is still valid)
</verification>

<success_criteria>
- Provider section uses "google_workspace" (underscore) in all 3 locations
- No "google-workspace" (hyphen) string literals remain in the file
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/31-google-workspace-directory-import/31-04-SUMMARY.md`
</output>
