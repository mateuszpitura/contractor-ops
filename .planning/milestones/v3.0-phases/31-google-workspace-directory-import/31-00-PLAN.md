---
phase: 31-google-workspace-directory-import
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/integrations/src/__tests__/google-workspace-adapter.test.ts
  - packages/integrations/src/__tests__/google-workspace-directory.test.ts
  - packages/api/src/__tests__/google-workspace-sync.test.ts
  - packages/validators/src/__tests__/google-workspace.test.ts
autonomous: true
requirements:
  - GOOG-01
  - GOOG-02
  - GOOG-03
  - GOOG-04
  - GOOG-05

must_haves:
  truths:
    - "Test stubs exist for all GOOG requirements before any implementation begins"
    - "Running pnpm test in integrations and api packages succeeds with pending/skipped stubs"
  artifacts:
    - path: "packages/integrations/src/__tests__/google-workspace-adapter.test.ts"
      provides: "Test stubs for OAuth config, token exchange, refresh (GOOG-01)"
      contains: "GoogleWorkspaceAdapter"
    - path: "packages/integrations/src/__tests__/google-workspace-directory.test.ts"
      provides: "Test stubs for user listing, group listing, response parsing (GOOG-02, GOOG-04)"
      contains: "listAllDirectoryUsers"
    - path: "packages/api/src/__tests__/google-workspace-sync.test.ts"
      provides: "Test stubs for sync detection logic (GOOG-05)"
      contains: "processDirectorySync"
    - path: "packages/validators/src/__tests__/google-workspace.test.ts"
      provides: "Test stubs for Zod schema validation"
      contains: "directoryImportInputSchema"
  key_links: []
---

<objective>
Create Wave 0 test stubs for all GOOG requirements before implementation begins.

Purpose: Establishes the Nyquist-compliant feedback loop so every implementation task can verify behavior, not just type-checking.
Output: 4 test files with describe blocks and skipped/todo tests covering GOOG-01 through GOOG-05.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-google-workspace-directory-import/31-CONTEXT.md
@.planning/phases/31-google-workspace-directory-import/31-RESEARCH.md
@.planning/phases/31-google-workspace-directory-import/31-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for integrations package (GOOG-01, GOOG-02, GOOG-04)</name>
  <files>
    packages/integrations/src/__tests__/google-workspace-adapter.test.ts,
    packages/integrations/src/__tests__/google-workspace-directory.test.ts
  </files>
  <read_first>
    packages/integrations/src/__tests__,
    packages/integrations/vitest.config.ts,
    packages/integrations/src/adapters/google-calendar-adapter.ts,
    packages/integrations/src/adapters/base-adapter.ts
  </read_first>
  <action>
**1. Create `packages/integrations/src/__tests__/google-workspace-adapter.test.ts`** (GOOG-01)

```typescript
import { describe, it, expect } from "vitest";

describe("GoogleWorkspaceAdapter", () => {
  describe("OAuth configuration", () => {
    it.todo("returns OAuth config with Admin SDK scopes (admin.directory.user.readonly, admin.directory.group.readonly)");
    it.todo("uses GOOGLE_WORKSPACE_CLIENT_ID and GOOGLE_WORKSPACE_CLIENT_SECRET env vars");
    it.todo("sets access_type=offline and prompt=consent in extra auth params");
    it.todo("has slug 'google-workspace' and displayName 'Google Workspace'");
    it.todo("sets supportsOAuth=true and supportsWebhooks=false");
  });

  describe("exchangeCodeForTokens", () => {
    it.todo("exchanges authorization code for access token and refresh token");
    it.todo("throws on non-ok response from Google token endpoint");
  });

  describe("refreshToken", () => {
    it.todo("refreshes expired access token using refresh token");
    it.todo("throws on non-ok response from Google token endpoint");
  });
});
```

**2. Create `packages/integrations/src/__tests__/google-workspace-directory.test.ts`** (GOOG-02, GOOG-04)

```typescript
import { describe, it, expect } from "vitest";

describe("GoogleWorkspaceAdapter - Directory API", () => {
  describe("listAllDirectoryUsers", () => {
    it.todo("fetches all directory users with pagination (follows nextPageToken)");
    it.todo("filters out suspended users from results");
    it.todo("extracts department from organizations[].primary.department");
    it.todo("handles empty directory (no users) without error");
    it.todo("throws on 403 Forbidden (non-admin account)");
    it.todo("throws on non-ok response with status code in error message");
  });

  describe("listUserGroups", () => {
    it.todo("fetches groups for a single user email");
    it.todo("paginates through multiple pages of groups");
    it.todo("returns empty array on 404 (user not in any groups)");
    it.todo("throws on non-ok response other than 404");
  });
});
```

Ensure the `__tests__` directory exists (create if needed). Use vitest import style matching existing test files in the integrations package.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/integrations test 2>&1 | tail -20</automated>
  </verify>
  <done>Test stubs exist for GoogleWorkspaceAdapter OAuth (GOOG-01) and directory methods (GOOG-02, GOOG-04), all showing as todo/skipped in test output</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for API package and validators (GOOG-03, GOOG-05)</name>
  <files>
    packages/api/src/__tests__/google-workspace-sync.test.ts,
    packages/validators/src/__tests__/google-workspace.test.ts
  </files>
  <read_first>
    packages/api/src/__tests__,
    packages/api/vitest.config.ts,
    packages/validators/src/__tests__,
    packages/validators/vitest.config.ts
  </read_first>
  <action>
**1. Create `packages/api/src/__tests__/google-workspace-sync.test.ts`** (GOOG-03, GOOG-05)

```typescript
import { describe, it, expect } from "vitest";

describe("Google Workspace Sync Orchestrator", () => {
  describe("processDirectorySync", () => {
    it.todo("detects new hires (in Google directory but not in org members or previous sync)");
    it.todo("detects departures (in previous sync and org members but not in current Google directory)");
    it.todo("dispatches DIRECTORY_NEW_HIRE notification for each new hire");
    it.todo("dispatches DIRECTORY_DEPARTURE notification for each departure");
    it.todo("does NOT auto-create users during sync (no createInvitation calls)");
    it.todo("does NOT auto-delete or deactivate users during sync");
    it.todo("updates syncedEmails snapshot in configJson after sync");
    it.todo("creates sync log entry with IN_PROGRESS then COMPLETED status");
    it.todo("marks sync log as FAILED on error and re-throws");
    it.todo("refreshes expired token before fetching directory");
  });
});

describe("Google Workspace Bulk Import", () => {
  describe("role resolution", () => {
    it.todo("uses userRoleOverride when present for a user");
    it.todo("uses group-to-role mapping when user has no override but is in a mapped group");
    it.todo("falls back to defaultRole when user has no override and no group mapping");
    it.todo("calls createInvitation for each user with resolved role");
    it.todo("collects partial failures without stopping the loop");
    it.todo("returns succeeded and failed arrays");
  });

  describe("security", () => {
    it.todo("re-fetches group memberships server-side instead of trusting client data");
  });
});
```

**2. Create `packages/validators/src/__tests__/google-workspace.test.ts`**

```typescript
import { describe, it, expect } from "vitest";

describe("Google Workspace Validators", () => {
  describe("googleDirectoryUserSchema", () => {
    it.todo("accepts valid Google directory user with all fields");
    it.todo("accepts user with optional fields omitted");
    it.todo("rejects user with invalid email");
    it.todo("rejects user with missing required name fields");
  });

  describe("directoryImportInputSchema", () => {
    it.todo("accepts valid import input with users, defaultRole, and optional mappings");
    it.todo("defaults groupRoleMappings to empty array");
    it.todo("defaults userRoleOverrides to empty object");
    it.todo("defaults userGroupMemberships to empty object");
    it.todo("rejects import with empty users array");
    it.todo("rejects import with invalid role value");
  });

  describe("groupRoleMappingSchema", () => {
    it.todo("accepts valid group-to-role mapping");
    it.todo("rejects mapping with invalid group email");
    it.todo("rejects mapping with invalid role");
  });
});
```

Ensure `__tests__` directories exist in both packages.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/api test 2>&1 | tail -20 && pnpm --filter @contractor-ops/validators test 2>&1 | tail -20</automated>
  </verify>
  <done>Test stubs exist for sync orchestrator (GOOG-05), bulk import (GOOG-03), and Zod validators, all showing as todo/skipped in test output</done>
</task>

</tasks>

<verification>
- `pnpm --filter @contractor-ops/integrations test` runs without error (stubs show as todo)
- `pnpm --filter @contractor-ops/api test` runs without error (stubs show as todo)
- `pnpm --filter @contractor-ops/validators test` runs without error (stubs show as todo)
- All 4 test files exist at the paths defined in RESEARCH.md Validation Architecture
</verification>

<success_criteria>
- Every GOOG requirement (01-05) has at least one describe block in a test file
- Test stubs are todo/skipped (not failing) so they don't block implementation
- Implementation plans (01-03) can reference these test files in their verify blocks
</success_criteria>

<output>
After completion, create `.planning/phases/31-google-workspace-directory-import/31-00-SUMMARY.md`
</output>
