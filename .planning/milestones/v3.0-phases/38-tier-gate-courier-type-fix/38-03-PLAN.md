---
phase: 38-tier-gate-courier-type-fix
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/integrations/teams-channel-mapping-card.tsx
  - apps/web/src/components/integrations/google-workspace/directory-import-wizard.tsx
  - apps/web/src/components/onboarding/import-wizard.tsx
autonomous: true
requirements: [BILL-09]

must_haves:
  truths:
    - "STARTER-tier users see UpgradeInlineBanner instead of Teams channel mapping card content"
    - "STARTER-tier users see UpgradeInlineBanner instead of Google Workspace directory import wizard"
    - "STARTER-tier users see UpgradeInlineBanner instead of onboarding import wizard"
  artifacts:
    - path: "apps/web/src/components/integrations/teams-channel-mapping-card.tsx"
      provides: "FeatureGate-wrapped Teams channel mapping"
      contains: "FeatureGate"
    - path: "apps/web/src/components/integrations/google-workspace/directory-import-wizard.tsx"
      provides: "FeatureGate-wrapped GWS directory import"
      contains: "FeatureGate"
    - path: "apps/web/src/components/onboarding/import-wizard.tsx"
      provides: "FeatureGate-wrapped onboarding import wizard"
      contains: "FeatureGate"
  key_links:
    - from: "apps/web/src/components/integrations/teams-channel-mapping-card.tsx"
      to: "apps/web/src/components/billing/feature-gate.tsx"
      via: "FeatureGate import"
      pattern: "import.*FeatureGate.*from.*billing/feature-gate"
    - from: "apps/web/src/components/integrations/google-workspace/directory-import-wizard.tsx"
      to: "apps/web/src/components/billing/feature-gate.tsx"
      via: "FeatureGate import"
      pattern: "import.*FeatureGate.*from.*billing/feature-gate"
    - from: "apps/web/src/components/onboarding/import-wizard.tsx"
      to: "apps/web/src/components/billing/feature-gate.tsx"
      via: "FeatureGate import"
      pattern: "import.*FeatureGate.*from.*billing/feature-gate"
---

<objective>
Wrap Teams channel mapping card, Google Workspace directory import wizard, and onboarding import wizard with FeatureGate component to show upgrade banners for STARTER-tier users, implementing defense-in-depth alongside API requireTier gates.

Purpose: Per D-09 and D-10, UI gates prevent STARTER users from even seeing PRO-only UI (they see UpgradeInlineBanner instead), complementing the API-level tier gates added in Plan 01. This is the first actual usage of the FeatureGate component in the app.
Output: 3 component files wrapped with FeatureGate, showing UpgradeInlineBanner for insufficient tiers.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-tier-gate-courier-type-fix/38-CONTEXT.md

<interfaces>
<!-- FeatureGate component from apps/web/src/components/billing/feature-gate.tsx -->
```typescript
interface FeatureGateProps {
  requiredTier: "Pro" | "Enterprise";
  featureName: string;
  children: ReactNode;
}

// Usage:
<FeatureGate requiredTier="Pro" featureName="Teams channel mapping">
  <ChildContent />
</FeatureGate>
// Shows children during loading (no flash)
// Shows UpgradeInlineBanner when tier insufficient
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap Teams channel mapping card with FeatureGate</name>
  <files>apps/web/src/components/integrations/teams-channel-mapping-card.tsx</files>
  <read_first>
    apps/web/src/components/integrations/teams-channel-mapping-card.tsx,
    apps/web/src/components/billing/feature-gate.tsx
  </read_first>
  <action>
    1. Add import at top of file: `import { FeatureGate } from "@/components/billing/feature-gate";`
    2. Find the component's return statement. Wrap the ENTIRE returned JSX tree (the Card or outermost container element) with:
       ```tsx
       <FeatureGate requiredTier="Pro" featureName="Teams channel mapping">
         {/* existing card content */}
       </FeatureGate>
       ```
    3. The FeatureGate wraps at component level -- the card itself is dedicated to channel mapping, so wrapping the whole return value is correct per D-09.
    4. Do NOT change any other logic in the component.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && grep -c "FeatureGate" apps/web/src/components/integrations/teams-channel-mapping-card.tsx</automated>
  </verify>
  <acceptance_criteria>
    - teams-channel-mapping-card.tsx contains `import { FeatureGate } from "@/components/billing/feature-gate"`
    - teams-channel-mapping-card.tsx contains `<FeatureGate requiredTier="Pro" featureName="Teams channel mapping">`
    - teams-channel-mapping-card.tsx contains matching `</FeatureGate>` closing tag
    - grep for "FeatureGate" returns at least 2 matches (import + JSX usage)
  </acceptance_criteria>
  <done>Teams channel mapping card wrapped with FeatureGate showing upgrade banner for STARTER tier</done>
</task>

<task type="auto">
  <name>Task 2: Wrap GWS directory import wizard and onboarding import wizard with FeatureGate</name>
  <files>
    apps/web/src/components/integrations/google-workspace/directory-import-wizard.tsx,
    apps/web/src/components/onboarding/import-wizard.tsx
  </files>
  <read_first>
    apps/web/src/components/integrations/google-workspace/directory-import-wizard.tsx,
    apps/web/src/components/onboarding/import-wizard.tsx,
    apps/web/src/components/billing/feature-gate.tsx
  </read_first>
  <action>
    **directory-import-wizard.tsx (per D-09):**
    1. Add import: `import { FeatureGate } from "@/components/billing/feature-gate";`
    2. Find the exported component function (likely `DirectoryImportWizard`). Wrap the entire returned JSX with:
       ```tsx
       <FeatureGate requiredTier="Pro" featureName="Google Workspace directory import">
         {/* existing wizard content */}
       </FeatureGate>
       ```
    3. The component is a Dialog-based wizard. Wrap at the component level (inside the component's return, around the Dialog or outermost element). This way the UpgradeInlineBanner replaces the entire wizard UI for STARTER users.

    **import-wizard.tsx (per D-09):**
    1. Add import: `import { FeatureGate } from "@/components/billing/feature-gate";`
    2. Find the exported component function (`ImportWizard`). Wrap the entire returned JSX with:
       ```tsx
       <FeatureGate requiredTier="Pro" featureName="Onboarding import wizard">
         {/* existing wizard steps content */}
       </FeatureGate>
       ```
    3. Wrapping at the component level (not route level) keeps navigation/breadcrumbs visible while replacing the wizard content with upgrade banner for STARTER users.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && grep -c "FeatureGate" apps/web/src/components/integrations/google-workspace/directory-import-wizard.tsx apps/web/src/components/onboarding/import-wizard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - directory-import-wizard.tsx contains `import { FeatureGate } from "@/components/billing/feature-gate"`
    - directory-import-wizard.tsx contains `<FeatureGate requiredTier="Pro" featureName="Google Workspace directory import">`
    - import-wizard.tsx contains `import { FeatureGate } from "@/components/billing/feature-gate"`
    - import-wizard.tsx contains `<FeatureGate requiredTier="Pro" featureName="Onboarding import wizard">`
    - Both files have matching `</FeatureGate>` closing tags
    - grep for "FeatureGate" returns at least 2 matches per file
  </acceptance_criteria>
  <done>GWS directory import wizard and onboarding import wizard both wrapped with FeatureGate for defense-in-depth tier gating</done>
</task>

</tasks>

<verification>
cd /Users/mateusz.pitura/Repos/projects/contractor-ops && npx turbo build --filter=web 2>&1 | tail -10
</verification>

<success_criteria>
- All 3 components wrapped with FeatureGate (requiredTier="Pro")
- FeatureGate import present in all 3 files
- featureName prop provides descriptive text for each gate
- Web app builds without TypeScript errors
- No other component logic changed
</success_criteria>

<output>
After completion, create `.planning/phases/38-tier-gate-courier-type-fix/38-03-SUMMARY.md`
</output>
