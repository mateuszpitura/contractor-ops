---
phase: 33-inpost-courier-integration
plan: 03
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - apps/web/src/components/equipment/paczkomat-picker.tsx
  - apps/web/src/components/equipment/paczkomat-display.tsx
  - apps/web/src/components/equipment/inpost-shipment-form.tsx
  - apps/web/src/components/equipment/shipment-label-view.tsx
  - apps/web/src/components/equipment/return-approval-banner.tsx
  - apps/web/src/components/portal/portal-equipment-tab.tsx
  - apps/web/src/components/portal/portal-return-flow.tsx
  - apps/web/src/app/[locale]/(portal)/portal/equipment/page.tsx
  - apps/web/messages/en.json
  - apps/web/messages/pl.json
autonomous: false
requirements:
  - EQUIP-05
  - EQUIP-11
user_setup:
  - service: inpost-shipx
    why: "InPost ShipX API access for shipment creation and label generation"
    env_vars:
      - name: INPOST_SHIPX_API_TOKEN
        source: "InPost Manager portal -> Settings -> API -> Generate token"
      - name: INPOST_SHIPX_ORGANIZATION_ID
        source: "InPost Manager portal -> Settings -> Organization ID (numeric)"
      - name: INPOST_GEOWIDGET_TOKEN
        source: "InPost Manager portal -> Settings -> Geowidget -> Token"
      - name: INPOST_WEBHOOK_SECRET
        source: "InPost Manager portal -> Settings -> Webhooks -> Secret (optional, for signature verification)"
      - name: INPOST_SANDBOX
        source: "Set to 'true' for sandbox, 'false' for production"
    dashboard_config:
      - task: "Register webhook URL"
        location: "InPost Manager -> Settings -> Webhooks -> Add endpoint -> URL: https://your-domain.com/api/webhooks/inpost"
must_haves:
  truths:
    - "Admin can open Paczkomat picker modal and select a locker from the Geowidget map"
    - "Admin can create an InPost shipment with Paczkomat destination and parcel size"
    - "Admin can view and download the shipping label/QR after shipment creation"
    - "Admin can approve or reject a pending return request"
    - "Contractor sees assigned equipment in portal Equipment tab"
    - "Contractor can initiate a return via portal with Paczkomat picker and sees pending approval state"
    - "Contractor can view and download return label after approval"
  artifacts:
    - path: "apps/web/src/components/equipment/paczkomat-picker.tsx"
      provides: "InPost Geowidget modal wrapper"
      min_lines: 60
    - path: "apps/web/src/components/equipment/inpost-shipment-form.tsx"
      provides: "InPost shipment creation dialog"
      min_lines: 80
    - path: "apps/web/src/components/equipment/shipment-label-view.tsx"
      provides: "Label/QR display with download and print"
      min_lines: 40
    - path: "apps/web/src/components/equipment/return-approval-banner.tsx"
      provides: "Admin return approval/rejection banner"
      min_lines: 40
    - path: "apps/web/src/components/portal/portal-equipment-tab.tsx"
      provides: "Contractor equipment list with return button"
      min_lines: 60
    - path: "apps/web/src/components/portal/portal-return-flow.tsx"
      provides: "Multi-step return modal"
      min_lines: 100
    - path: "apps/web/src/app/[locale]/(portal)/portal/equipment/page.tsx"
      provides: "Portal equipment route page"
  key_links:
    - from: "apps/web/src/components/equipment/paczkomat-picker.tsx"
      to: "https://geowidget.inpost.pl"
      via: "iframe with postMessage communication"
      pattern: "geowidget\\.inpost\\.pl"
    - from: "apps/web/src/components/equipment/inpost-shipment-form.tsx"
      to: "packages/api/src/routers/equipment.ts"
      via: "tRPC equipment.createInPostShipment mutation"
      pattern: "createInPostShipment"
    - from: "apps/web/src/components/portal/portal-return-flow.tsx"
      to: "packages/api/src/routers/portal.ts"
      via: "tRPC portal.requestReturn mutation"
      pattern: "requestReturn"
    - from: "apps/web/src/components/equipment/return-approval-banner.tsx"
      to: "packages/api/src/routers/equipment.ts"
      via: "tRPC equipment.approveReturnRequest and rejectReturnRequest mutations"
      pattern: "approveReturnRequest|rejectReturnRequest"
---

<objective>
Build all UI components for the InPost courier integration: Paczkomat picker modal, InPost shipment creation form, label/QR viewer, admin return approval banner, portal Equipment tab, and contractor return flow. Add i18n translations for en/pl.

Purpose: Completes the user-facing experience so admins can create InPost shipments and approve returns, and contractors can initiate returns and download labels via the portal.
Output: 7 new components, 1 new portal page, i18n translations, visual verification checkpoint.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-inpost-courier-integration/33-CONTEXT.md
@.planning/phases/33-inpost-courier-integration/33-RESEARCH.md
@.planning/phases/33-inpost-courier-integration/33-UI-SPEC.md
@.planning/phases/33-inpost-courier-integration/33-01-SUMMARY.md
@.planning/phases/33-inpost-courier-integration/33-02-SUMMARY.md

<interfaces>
<!-- tRPC endpoints from Plan 02 that UI calls -->

From packages/api/src/routers/equipment.ts (Plan 02 additions):
```typescript
// New mutations/queries available on equipmentRouter:
createInPostShipment: input(inpostShipmentCreateSchema) => Shipment with events
approveReturnRequest: input({ id: string, parcelSize: "small" | "medium" | "large" }) => ReturnRequest
rejectReturnRequest: input({ id: string, reason?: string }) => ReturnRequest
listReturnRequests: input({ status?: ReturnRequestStatus }) => ReturnRequest[]
getShipmentLabel: input({ shipmentId: string }) => { data: string /* base64 */, contentType: string }
```

From packages/api/src/routers/portal.ts (Plan 02 additions):
```typescript
// New mutations/queries available on portalRouter:
listEquipment: no input => Array<{ assignmentId, assignedAt, equipment: { id, name, serialNumber, type, status }, latestShipment: { ... } | null }>
getReturnStatus: no input => ReturnRequest | null
requestReturn: input({ targetPointId, targetPointName, targetPointAddress }) => ReturnRequest
cancelReturn: input({ id: string }) => ReturnRequest
getReturnLabel: input({ returnRequestId: string }) => { data: string /* base64 */, contentType: string }
```

From packages/validators/src/equipment.ts:
```typescript
export const inpostShipmentCreateSchema = z.object({ equipmentIds: z.array(z.string()), targetPointId: z.string(), targetPointName: z.string(), targetPointAddress: z.string(), parcelSize: z.enum(["small", "medium", "large"]), direction: shipmentDirectionEnum, ... });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin UI components -- Paczkomat picker, InPost shipment form, label view, return approval banner</name>
  <read_first>
    - apps/web/src/components/equipment/shipment-form.tsx
    - apps/web/src/components/equipment/shipment-timeline.tsx
    - apps/web/src/components/equipment/equipment-detail/
    - apps/web/messages/en.json
    - apps/web/messages/pl.json
    - .planning/phases/33-inpost-courier-integration/33-UI-SPEC.md
  </read_first>
  <files>
    apps/web/src/components/equipment/paczkomat-picker.tsx,
    apps/web/src/components/equipment/paczkomat-display.tsx,
    apps/web/src/components/equipment/inpost-shipment-form.tsx,
    apps/web/src/components/equipment/shipment-label-view.tsx,
    apps/web/src/components/equipment/return-approval-banner.tsx,
    apps/web/messages/en.json,
    apps/web/messages/pl.json
  </files>
  <action>
    **1. Create `paczkomat-picker.tsx`** — Per D-01, D-04, and UI-SPEC:
    - "use client" component
    - Props: `{ open: boolean; onOpenChange: (open: boolean) => void; onSelect: (point: { id: string; name: string; address: string }) => void; geowidgetToken: string }`
    - Use shadcn `Dialog` with `DialogContent` class `max-w-2xl h-[600px] p-0` (per UI-SPEC)
    - DialogHeader with title "Select Paczkomat" and close button
    - Render an `<iframe>` pointing to `https://geowidget.inpost.pl?token=${geowidgetToken}&language=pl&config=parcelcollect`
    - iframe attributes: `sandbox="allow-scripts allow-same-origin allow-popups"`, `title="InPost Paczkomat locker map"` (per accessibility spec), `className="w-full h-[400px]"`, `allow=""` (no special permissions)
    - `useEffect` to listen for `window.addEventListener("message", handler)` — ONLY accept messages from origin `https://geowidget.inpost.pl` (per UI-SPEC security)
    - On point selection message: show selected locker info below the iframe (name + address, per UI-SPEC layout)
    - "Confirm locker" button (disabled until a point is selected, per UI-SPEC), calls `onSelect` and `onOpenChange(false)`
    - "Cancel" button calls `onOpenChange(false)`
    - Cleanup event listener in useEffect return
    - Skeleton loader while iframe loads (per UI-SPEC)
    - Error state if iframe fails to load: "Could not load the Paczkomat map. Check your connection and try again." (per copywriting contract)

    **2. Create `paczkomat-display.tsx`** — read-only display of selected Paczkomat:
    - Props: `{ pointId: string; pointName: string; pointAddress: string; onChangeClick: () => void }`
    - shadcn `Card` with locker name (bold), address (text-muted-foreground), "Change" button
    - Per UI-SPEC: compact card layout

    **3. Create `inpost-shipment-form.tsx`** — Per UI-SPEC InPost Shipment Creation layout:
    - "use client" component
    - Props: `{ open: boolean; onOpenChange: (open: boolean) => void; equipmentIds: string[]; contractorName: string; preferredPaczkomat?: { id: string; name: string; address: string } | null; onSuccess: () => void }`
    - shadcn `Dialog` with title "Create InPost shipment"
    - Pre-filled read-only contractor name field
    - Paczkomat section: If `preferredPaczkomat`, show `PaczkomatDisplay` with "Change" button; otherwise show "Select Paczkomat" button (per D-03 pre-fill)
    - "Select Paczkomat" / "Change" opens `PaczkomatPicker` modal
    - Parcel size radio group: Small (A), Medium (B), Large (C) — per UI-SPEC
    - "Create shipment" button calls `trpc.equipment.createInPostShipment.useMutation()` with equipmentIds, selected target point, parcel size, direction "OUTBOUND"
    - Loading state on button, form disabled during submission
    - On success: sonner toast "InPost shipment created", close dialog, call `onSuccess`
    - On error: sonner toast with error message

    **4. Create `shipment-label-view.tsx`** — Per UI-SPEC label layout:
    - Props: `{ shipmentId: string }` (or `{ labelData: string; contentType: string; trackingNumber: string; paczkomatName: string }` if pre-fetched)
    - Fetch label via `trpc.equipment.getShipmentLabel.useQuery({ shipmentId })` if not pre-fetched
    - Render label as image if PNG, or embedded PDF viewer for PDF, or QR code image — centered at 240x240px on desktop, scales on mobile (per UI-SPEC)
    - Tracking number in font-mono text-xs (per UI-SPEC typography)
    - Paczkomat name display
    - "Download label" button: creates blob URL from base64 data and triggers download via anchor click
    - "Print" button: opens browser print dialog with label content only
    - Empty state: "Label not yet available" + "The shipping label will appear here once the shipment is created with InPost." (per copywriting contract)
    - Loading skeleton while fetching

    **5. Create `return-approval-banner.tsx`** — Per UI-SPEC admin banner:
    - Props: `{ returnRequest: { id: string; contractorName: string; itemCount: number; targetPointName: string; createdAt: string } }`
    - Card with warning background (`bg-warning/10` with `border-l-4 border-warning`), per UI-SPEC
    - Display: "Return requested by {contractorName}", item count, drop-off Paczkomat, request date
    - "Approve return" button (accent): calls `trpc.equipment.approveReturnRequest.useMutation()` with id and default parcelSize "large"
    - "Reject return" button (destructive variant): opens AlertDialog with confirmation text per copywriting contract, then calls `trpc.equipment.rejectReturnRequest.useMutation()`
    - Loading states on both buttons
    - On approve success: sonner toast "Return approved -- shipment created"
    - On reject success: sonner toast "Return rejected"

    **6. Add i18n translations** to both `en.json` and `pl.json`:
    - Add keys under `Equipment.inpost.*`, `Equipment.paczkomat.*`, `Equipment.label.*`, `Equipment.return.*` namespaces
    - All copywriting contract strings in both languages
    - Portal keys under `Portal.equipment.*`, `Portal.return.*`
    - Include: createInPostShipment, selectPaczkomat, confirmLocker, change, returnEquipment, requestReturn, approveReturn, rejectReturn, downloadLabel, print, emptyState headings/bodies, pendingApproval, returnApproved, errorStates, reject confirmation, cancel confirmation
    - Polish translations should be natural Polish, not machine-translated English
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter web run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/equipment/paczkomat-picker.tsx contains `geowidget.inpost.pl`
    - apps/web/src/components/equipment/paczkomat-picker.tsx contains `sandbox="allow-scripts allow-same-origin allow-popups"`
    - apps/web/src/components/equipment/paczkomat-picker.tsx contains `title="InPost Paczkomat locker map"`
    - apps/web/src/components/equipment/paczkomat-picker.tsx contains `origin.*geowidget.inpost.pl`
    - apps/web/src/components/equipment/paczkomat-display.tsx contains `PaczkomatDisplay`
    - apps/web/src/components/equipment/inpost-shipment-form.tsx contains `createInPostShipment`
    - apps/web/src/components/equipment/inpost-shipment-form.tsx contains `PaczkomatPicker`
    - apps/web/src/components/equipment/inpost-shipment-form.tsx contains `parcelSize`
    - apps/web/src/components/equipment/shipment-label-view.tsx contains `Download label`
    - apps/web/src/components/equipment/shipment-label-view.tsx contains `Print`
    - apps/web/src/components/equipment/return-approval-banner.tsx contains `approveReturnRequest`
    - apps/web/src/components/equipment/return-approval-banner.tsx contains `rejectReturnRequest`
    - apps/web/messages/en.json contains `Equipment.inpost`
    - apps/web/messages/en.json contains `Portal.equipment`
    - apps/web/messages/pl.json contains `Equipment.inpost`
    - apps/web/messages/pl.json contains `Portal.equipment`
  </acceptance_criteria>
  <done>
    All 5 admin components render correctly. Paczkomat picker communicates with Geowidget iframe via postMessage with origin validation. InPost shipment form pre-fills preferred Paczkomat and calls createInPostShipment mutation. Label view renders with download/print. Return approval banner with approve/reject actions. i18n translations complete for en/pl.
  </done>
</task>

<task type="auto">
  <name>Task 2: Portal equipment page and contractor return flow</name>
  <read_first>
    - apps/web/src/app/[locale]/(portal)/portal/page.tsx
    - apps/web/src/components/portal/portal-settings-page.tsx
    - apps/web/src/components/portal/portal-top-bar.tsx
    - apps/web/src/components/equipment/paczkomat-picker.tsx (just created)
    - apps/web/src/components/equipment/shipment-label-view.tsx (just created)
    - .planning/phases/33-inpost-courier-integration/33-UI-SPEC.md
  </read_first>
  <files>
    apps/web/src/app/[locale]/(portal)/portal/equipment/page.tsx,
    apps/web/src/components/portal/portal-equipment-tab.tsx,
    apps/web/src/components/portal/portal-return-flow.tsx
  </files>
  <action>
    **1. Create `apps/web/src/app/[locale]/(portal)/portal/equipment/page.tsx`**:
    - Server component page that renders `PortalEquipmentTab`
    - Follow pattern from other portal pages (e.g., `portal/contracts/page.tsx`)
    - Metadata/title: "Equipment"

    **2. Create `portal-equipment-tab.tsx`** — Per D-08, UI-SPEC Portal Equipment Tab:
    - "use client" component
    - Fetch equipment list via `trpc.portal.listEquipment.useQuery()`
    - Fetch current return status via `trpc.portal.getReturnStatus.useQuery()`
    - Render each equipment item as a Card (per UI-SPEC layout):
      - Equipment type icon (reuse `EquipmentTypeIcon` from Phase 30 components)
      - Equipment name, serial number (font-mono text-xs), status badge (reuse `EquipmentStatusBadge`)
      - Latest shipment delivery date if available
      - "Return" button (visible only when equipment status is ASSIGNED or DELIVERED, per UI-SPEC)
    - If a ReturnRequest exists and is PENDING_APPROVAL: show status banner "Return pending approval" with "Cancel return" button
    - If a ReturnRequest exists and is SHIPMENT_CREATED: show "Return approved -- your label is ready" banner with button to view label
    - "Return" button click: Opens `PortalReturnFlow` dialog (processes ALL assigned equipment, per D-11)
    - Empty state: "No equipment assigned" heading + "You don't have any equipment assigned to you yet. Contact your manager if you expect to receive equipment." (per copywriting contract)
    - Loading skeleton while fetching

    **3. Update portal navigation** to include Equipment tab:
    - Find the portal navigation component (likely in `portal-top-bar.tsx` or a layout file) and add "Equipment" link pointing to `/portal/equipment`
    - Position between "Time" and "Settings" tabs per UI-SPEC tab order

    **4. Create `portal-return-flow.tsx`** — Per D-08, D-10, D-11, UI-SPEC multi-step return modal:
    - "use client" component
    - Props: `{ open: boolean; onOpenChange: (open: boolean) => void; equipmentItems: Array<{ name: string; serialNumber: string | null }>; onSuccess: () => void }`
    - 3-step flow per UI-SPEC:

    **Step 1 — Select drop-off Paczkomat:**
    - List items being returned (per D-11 all items)
    - Embed `PaczkomatPicker` (reuse from admin, per D-04)
    - "Next" button disabled until Paczkomat selected
    - "Cancel" button closes modal

    **Step 2 — Confirm return:**
    - Summary: items listed, selected Paczkomat name and address
    - Notice: "This return requires admin approval. You will receive the shipping label once approved." (per D-10 copywriting contract)
    - "Request return" button calls `trpc.portal.requestReturn.useMutation()` with selected target point
    - "Back" button returns to step 1
    - Loading state on submit

    **Step 3 — Confirmation / Label (after approval):**
    - Show "Return approved" heading
    - Embed `ShipmentLabelView` component (reuse from admin, per D-13) — but call `trpc.portal.getReturnLabel.useQuery({ returnRequestId })` instead of equipment router
    - Show drop-off Paczkomat name and deadline
    - "Download label" and "Print" buttons (from ShipmentLabelView)

    - Step indicator at top: 3 dots with active step in accent color (per UI-SPEC)
    - On success of step 2: sonner toast "Return requested", modal shows pending state
    - When return is approved (detected by `getReturnStatus` query refetch or websocket), auto-advance to step 3

    **5. Wire return flow into equipment detail page** (admin side):
    - In the equipment detail page's shipments tab, check for pending ReturnRequests for the equipment's current contractor
    - If exists with PENDING_APPROVAL status, render `ReturnApprovalBanner` at the top (per UI-SPEC)
    - After approval/rejection, refetch equipment data to update timeline
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter web run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/[locale]/(portal)/portal/equipment/page.tsx exists and contains `PortalEquipmentTab`
    - apps/web/src/components/portal/portal-equipment-tab.tsx contains `listEquipment`
    - apps/web/src/components/portal/portal-equipment-tab.tsx contains `getReturnStatus`
    - apps/web/src/components/portal/portal-equipment-tab.tsx contains `No equipment assigned`
    - apps/web/src/components/portal/portal-equipment-tab.tsx contains `Return`
    - apps/web/src/components/portal/portal-return-flow.tsx contains `requestReturn`
    - apps/web/src/components/portal/portal-return-flow.tsx contains `PaczkomatPicker`
    - apps/web/src/components/portal/portal-return-flow.tsx contains `Step 1`
    - apps/web/src/components/portal/portal-return-flow.tsx contains `This return requires admin approval`
    - apps/web/src/components/portal/portal-return-flow.tsx contains `getReturnLabel`
  </acceptance_criteria>
  <done>
    Portal Equipment page renders at /portal/equipment. Equipment tab shows assigned items with status. Return flow modal works through 3 steps. Navigation updated with Equipment link. Web app builds without errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete InPost integration</name>
  <files>all components from Tasks 1-2</files>
  <action>
    Human verifies the complete InPost courier integration end-to-end:
    - Admin InPost shipment creation with Paczkomat picker
    - Automatic status tracking via webhooks/polling
    - Contractor portal Equipment tab with return flow
    - Return approval workflow
    - Label/QR delivery
  </action>
  <what-built>Complete InPost courier integration: admin InPost shipment creation with Paczkomat picker, automatic status tracking via webhooks/polling, contractor portal Equipment tab with return flow, return approval workflow, label/QR delivery</what-built>
  <how-to-verify>
    1. Navigate to Equipment page in admin panel, open an equipment detail with an assigned contractor
    2. Click "Create InPost shipment" — verify Paczkomat picker modal opens with map iframe from geowidget.inpost.pl
    3. Select a Paczkomat, confirm, choose parcel size, create shipment — verify success toast and timeline updates
    4. View shipment detail — verify label/QR section appears with download/print buttons
    5. Navigate to /portal/equipment as a contractor — verify Equipment tab shows assigned items with status
    6. Click "Return equipment" — verify 3-step return flow modal opens with all assigned items listed
    7. Select drop-off Paczkomat, proceed to step 2 — verify "requires admin approval" notice shown
    8. Submit return request — verify "Return pending approval" state in portal
    9. In admin panel, verify return approval banner appears on equipment detail with approve/reject buttons
    10. Approve return — verify shipment created, label available to contractor
    11. Check all i18n strings render correctly in both English and Polish
  </how-to-verify>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter web run build 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms all 11 verification steps pass. InPost courier integration is visually and functionally complete.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm --filter web run build` — web app builds without errors
- `pnpm --filter api run build` — API builds without errors
- `cd packages/api && pnpm vitest run --reporter=verbose` — all tests pass
- Visual verification of all 7 new components and portal page
</verification>

<success_criteria>
- Paczkomat picker modal renders Geowidget iframe with origin-validated postMessage communication
- InPost shipment form creates shipment via ShipX API with correct payload
- Label view renders PDF/QR with working download and print buttons
- Return approval banner shows on equipment detail for pending returns
- Portal Equipment tab lists assigned items with status badges
- Contractor return flow works through 3 steps (select Paczkomat, confirm, view label)
- All UI strings available in en.json and pl.json
- Portal navigation includes Equipment link
- Web and API packages build without errors
</success_criteria>

<output>
After completion, create `.planning/phases/33-inpost-courier-integration/33-03-SUMMARY.md`
</output>
