---
phase: 28-stripe-billing-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 28-01
  - 28-02
files_modified:
  - apps/web/src/components/billing/billing-tab.tsx
  - apps/web/src/components/billing/plan-comparison-grid.tsx
  - apps/web/src/components/billing/plan-card.tsx
  - apps/web/src/components/billing/current-plan-summary.tsx
  - apps/web/src/components/billing/credit-usage-card.tsx
  - apps/web/src/components/billing/trial-banner.tsx
  - apps/web/src/components/billing/soft-block-modal.tsx
  - apps/web/src/components/billing/proration-preview.tsx
  - apps/web/src/components/billing/premium-badge.tsx
  - apps/web/src/components/billing/credit-exhausted-inline.tsx
  - apps/web/src/components/billing/top-up-dialog.tsx
  - apps/web/src/app/[locale]/(dashboard)/settings/page.tsx
  - apps/web/src/app/[locale]/(dashboard)/layout.tsx
autonomous: false
requirements:
  - BILL-01
  - BILL-02
  - BILL-03
  - BILL-04
  - BILL-05
  - BILL-06
  - BILL-07
  - BILL-08

must_haves:
  truths:
    - "Settings page has a Billing tab visible to admin users showing current plan and credit usage"
    - "Plan comparison grid displays Starter/Pro/Enterprise with correct prices (199/449/849 PLN), per-seat pricing, and feature lists"
    - "Clicking a plan CTA redirects to Stripe Checkout via the tRPC billing.createCheckoutSession mutation"
    - "Upgrade/downgrade shows proration preview before confirming"
    - "Manage billing button redirects to Stripe-hosted portal"
    - "Trial banner appears in last 7 days of trial with correct copy"
    - "Soft-block modal appears on trial expiry with plan comparison"
    - "Credit exhausted inline replaces OCR trigger when credits are zero"
    - "Top-up dialog allows purchasing additional credits"
    - "Premium badge (diamond icon) marks features requiring higher tier"
  artifacts:
    - path: "apps/web/src/components/billing/billing-tab.tsx"
      provides: "Main billing tab container"
      min_lines: 30
    - path: "apps/web/src/components/billing/plan-comparison-grid.tsx"
      provides: "3-column responsive plan grid"
      min_lines: 40
    - path: "apps/web/src/components/billing/plan-card.tsx"
      provides: "Individual plan tier card with CTA"
      min_lines: 50
    - path: "apps/web/src/components/billing/current-plan-summary.tsx"
      provides: "Active plan status card"
      min_lines: 30
    - path: "apps/web/src/components/billing/credit-usage-card.tsx"
      provides: "Credit progress bar and top-up action"
      min_lines: 30
    - path: "apps/web/src/components/billing/trial-banner.tsx"
      provides: "Global trial expiry banner"
      min_lines: 30
    - path: "apps/web/src/components/billing/soft-block-modal.tsx"
      provides: "Full-screen trial expired overlay"
      min_lines: 40
    - path: "apps/web/src/app/[locale]/(dashboard)/settings/page.tsx"
      provides: "Settings page with Billing tab added"
      contains: "billing"
  key_links:
    - from: "apps/web/src/components/billing/plan-card.tsx"
      to: "billing.createCheckoutSession"
      via: "tRPC mutation on CTA click"
      pattern: "createCheckoutSession"
    - from: "apps/web/src/components/billing/billing-tab.tsx"
      to: "billing.getSubscription"
      via: "tRPC query for current subscription"
      pattern: "getSubscription"
    - from: "apps/web/src/components/billing/trial-banner.tsx"
      to: "billing.getSubscription"
      via: "tRPC query checking trial status"
      pattern: "trialEnd"
---

<objective>
Build all billing UI components: Settings Billing tab with plan comparison, current plan summary, credit usage, trial banner, soft-block modal, proration preview, premium badge, credit exhausted inline, and top-up dialog. Wire all components to the tRPC billing router.

Purpose: Provides the admin-facing billing management UI per D-15 through D-19, trial experience per D-07 through D-10, and credit management UI per D-13/D-14. All payment collection redirects to Stripe Checkout (no PCI scope).
Output: Complete billing UI integrated into the Settings page and dashboard layout.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-stripe-billing-foundation/28-CONTEXT.md
@.planning/phases/28-stripe-billing-foundation/28-RESEARCH.md
@.planning/phases/28-stripe-billing-foundation/28-UI-SPEC.md
@.planning/phases/28-stripe-billing-foundation/28-01-SUMMARY.md
@.planning/phases/28-stripe-billing-foundation/28-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 tRPC router -->

From packages/api/src/routers/billing.ts:
```typescript
export const billingRouter = router({
  getSubscription: tenantProcedure.query(...),           // Returns Subscription | null
  createCheckoutSession: adminProcedure.mutation(...),    // Input: { priceId } -> { url }
  getProrationPreview: adminProcedure.query(...),         // Input: { newPriceId } -> { lines, totalGrosze }
  createPortalSession: adminProcedure.mutation(...),      // No input -> { url }
  getPlanConfig: tenantProcedure.query(...),              // Returns static plan config
});
```

From packages/api/src/services/billing-constants.ts (single source of truth):
```typescript
export const TIER_CREDIT_ALLOWANCE = { STARTER: 20, PRO: 100, ENTERPRISE: 500 };
export const TRIAL_CREDIT_ALLOWANCE = 5;
```

From packages/db/prisma/schema/billing.prisma:
```prisma
enum SubscriptionTier { STARTER PRO ENTERPRISE }
enum SubscriptionStatus { TRIALING ACTIVE PAST_DUE CANCELED UNPAID INCOMPLETE INCOMPLETE_EXPIRED PAUSED }
```

From apps/web/src/app/[locale]/(dashboard)/settings/page.tsx (existing tabs):
```typescript
// Uses nuqs for URL-synced tab state: parseAsString.withDefault("general")
// Tabs: general, approvals, notifications, integrations (admin), audit-log (admin), members
// Permission check: can("organization", ["update"]) for integrations
// Components: AnimateIn for animations, Suspense boundary
```

From 28-UI-SPEC.md key specs:
- PlanCard: role="radio" within role="radiogroup"
- TrialBanner: role="alert" aria-live="polite", muted warning bg, left accent border
- SoftBlockModal: aria-modal="true" role="alertdialog", focus trap, not dismissible via Escape
- CreditProgress: aria-valuenow/min/max, aria-label="OCR credits: {used} of {total} used"
- PremiumBadge: Lucide Gem icon 14px, aria-label="Requires {tier} plan"
- Responsive: PlanGrid 1-col mobile, 2-col md, 3-col lg
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Billing tab, plan cards, current plan summary, and credit usage</name>
  <files>
    apps/web/src/components/billing/billing-tab.tsx,
    apps/web/src/components/billing/plan-comparison-grid.tsx,
    apps/web/src/components/billing/plan-card.tsx,
    apps/web/src/components/billing/current-plan-summary.tsx,
    apps/web/src/components/billing/credit-usage-card.tsx,
    apps/web/src/components/billing/proration-preview.tsx,
    apps/web/src/components/billing/premium-badge.tsx,
    apps/web/src/components/billing/top-up-dialog.tsx,
    apps/web/src/app/[locale]/(dashboard)/settings/page.tsx
  </files>
  <read_first>
    apps/web/src/app/[locale]/(dashboard)/settings/page.tsx,
    apps/web/src/components/settings/integrations-tab.tsx,
    apps/web/src/components/ui/card.tsx,
    apps/web/src/components/ui/badge.tsx,
    apps/web/src/components/ui/button.tsx,
    apps/web/src/components/ui/progress.tsx,
    apps/web/src/components/ui/dialog.tsx,
    apps/web/src/components/ui/tabs.tsx,
    apps/web/src/components/ui/skeleton.tsx,
    apps/web/src/components/ui/separator.tsx,
    apps/web/src/components/ui/select.tsx,
    apps/web/src/components/ui/switch.tsx,
    apps/web/src/components/ui/tooltip.tsx,
    apps/web/src/hooks/use-permissions.ts,
    .planning/phases/28-stripe-billing-foundation/28-UI-SPEC.md
  </read_first>
  <action>
    **1. Create `apps/web/src/components/billing/premium-badge.tsx`:**

    Small reusable component. Lucide `Gem` icon at 14px (`size={14}`), `text-muted-foreground` color. Wrapped in Tooltip with content "Available on {tierName} plan". Icon has `aria-label="Requires {tierName} plan"`. Props: `tier: "Pro" | "Enterprise"`.

    **2. Create `apps/web/src/components/billing/plan-card.tsx`:**

    Props: `{ tier: { name: string; priceGrosze: number; seatPriceGrosze: number; includedSeats: number; creditAllowance: number; features: string[]; excludedFeatures: string[] }; isCurrentPlan: boolean; isRecommended: boolean; onSelect: () => void; disabled?: boolean }`.

    Layout per UI-SPEC:
    - Card component with `role="radio"` and `aria-checked={isCurrentPlan}` and `tabIndex={0}`
    - If isCurrentPlan: accent ring border (`ring-2 ring-primary`), "Current plan" Badge variant
    - If isRecommended: "Recommended" Badge with accent styling
    - Tier name: Label role (14px font-semibold)
    - Price: Display size (28px font-semibold font-display) showing `{price} PLN` with `tabular-nums` class. Prices in human-readable PLN (divide grosze by 100): Starter=199, Pro=449, Enterprise=849
    - Below price: Body text (14px muted) "+{seatPrice} PLN/seat above {includedSeats} included" per copywriting contract
    - Credit allowance: "{N} OCR credits/month included" per copywriting contract
    - Feature list: each feature with lucide `Check` icon (text-primary) or `X` icon (text-muted-foreground) for excluded
    - CTA Button at bottom:
      - No subscription: "Choose a plan" (default variant)
      - Current plan lower tier: "Upgrade plan" (default variant)
      - Current plan higher tier: "Change plan" (outline variant)
      - Current plan same tier: disabled, shows "Current plan"
    - Plan descriptions per copywriting contract:
      - Starter: "Everything you need to manage contractors"
      - Pro: "Integrations, OCR, and advanced workflows"
      - Enterprise: "Full platform access with audit and API"

    **3. Create `apps/web/src/components/billing/plan-comparison-grid.tsx`:**

    Uses `role="radiogroup"` wrapping 3 PlanCard instances. Responsive grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8` (gap-8 = 32px = xl spacing). On md screens, third card spans full width with `md:col-span-2 lg:col-span-1`.

    Static plan config data (per D-01 through D-06):
    ```typescript
    const PLANS = [
      {
        tier: "STARTER",
        name: "Starter",
        priceGrosze: 19900,
        seatPriceGrosze: 1900,
        includedSeats: 2,
        creditAllowance: 20,
        features: ["Contractor management", "Contracts & documents", "Invoice intake & matching", "Approval workflows", "Payment batching"],
        excludedFeatures: ["Integrations (Jira, Linear, Calendar)", "OCR invoice parsing", "Advanced workflows", "Audit log export", "API access"],
      },
      {
        tier: "PRO",
        name: "Pro",
        priceGrosze: 44900,
        seatPriceGrosze: 2900,
        includedSeats: 5,
        creditAllowance: 100,
        features: ["Everything in Starter", "Integrations (Jira, Linear, Calendar)", "OCR invoice parsing", "Advanced workflows", "E-signatures"],
        excludedFeatures: ["Audit log export", "API access"],
      },
      {
        tier: "ENTERPRISE",
        name: "Enterprise",
        priceGrosze: 84900,
        seatPriceGrosze: 4900,
        includedSeats: 15,
        creditAllowance: 500,
        features: ["Everything in Pro", "Audit log export", "API access", "Priority support", "Custom onboarding"],
        excludedFeatures: [],
      },
    ];
    ```

    Props: `{ currentTier?: SubscriptionTier; onSelectPlan: (priceId: string) => void; compact?: boolean }`. When `compact` is true, reduce padding and hide excluded features (used in soft-block modal).

    **4. Create `apps/web/src/components/billing/current-plan-summary.tsx`:**

    Card showing:
    - Plan name with tier Badge (using variant: ACTIVE="success", TRIALING="warning", PAST_DUE="destructive", CANCELED="muted")
    - Status text: "Active", "Trial - {N} days remaining", "Past due", "Canceled - access until {date}"
    - Renewal date: "Renews on {date}" or "Trial ends {date}"
    - Seat count: "{N} of {included} seats used"
    - Loading state: Skeleton with 3 lines
    - Error state: Card with "Failed to load billing data" and retry button
    - Empty state (no subscription): heading "No active subscription", body per copywriting contract

    Uses `api.billing.getSubscription.useQuery()` for data.

    **5. Create `apps/web/src/components/billing/credit-usage-card.tsx`:**

    Card showing:
    - Heading: "OCR Credits"
    - Progress bar: value = used, max = allowance. Uses shadcn Progress with `aria-valuenow={used}`, `aria-valuemin={0}`, `aria-valuemax={allowance}`, `aria-label="OCR credits: {used} of {allowance} used"`
    - Text below: "{remaining} of {allowance} credits remaining"
    - Low threshold warning (< 20% remaining): warning text color
    - "Buy credits" Button (outline variant)
    - Auto-renewal toggle (Switch component) for Pro/Enterprise only per D-13. When enabled, show threshold input and bundle size selector inline
    - Loading state: Skeleton with progress bar placeholder

    **6. Create `apps/web/src/components/billing/proration-preview.tsx`:**

    Props: `{ newPriceId: string; onConfirm: () => void; onCancel: () => void }`.
    Uses `api.billing.getProrationPreview.useQuery({ newPriceId })`.
    Shows:
    - Line items from preview (description + amount in PLN)
    - Total: "You will be charged {total} PLN today" (if positive) or "You will receive a credit of {total} PLN" (if negative) per copywriting contract
    - Confirm and Cancel buttons
    - Loading state: Skeleton
    - Amounts displayed as PLN (divide grosze by 100), formatted with `tabular-nums`

    **7. Create `apps/web/src/components/billing/top-up-dialog.tsx`:**

    Dialog with:
    - Heading: "Buy OCR Credits"
    - Bundle selection using Select component. Default bundles: 10 credits, 25 credits, 50 credits
    - Price per bundle (display only -- actual price from Stripe Checkout)
    - Confirm button redirects to Stripe Checkout for one-time purchase
    - Cancel button closes dialog

    **8. Create `apps/web/src/components/billing/billing-tab.tsx`:**

    Container component that assembles all billing sub-components:
    - Top row: 2-column grid on md+ (`grid grid-cols-1 md:grid-cols-2 gap-8`): CurrentPlanSummary + CreditUsageCard
    - Below: PlanComparisonGrid (full width)
    - Below: "Manage billing" Button (outline variant) that calls `api.billing.createPortalSession.useMutation()` and redirects to the returned URL per D-22
    - Separator between sections

    Plan selection flow:
    1. User clicks plan CTA in PlanCard
    2. If user has existing subscription on different tier: show ProrationPreview inline
    3. On confirm (or if no existing subscription): call `api.billing.createCheckoutSession.useMutation({ priceId })` and `window.location.href = result.url`
    4. Handle success return from Stripe: check URL for `session_id` param, show success toast

    **9. Update `apps/web/src/app/[locale]/(dashboard)/settings/page.tsx`:**

    Add import: `import { BillingTab } from "@/components/billing/billing-tab";`

    Add permission check: `const canManageBilling = can("organization", ["update"]);`

    Add tab trigger AFTER integrations, BEFORE audit-log:
    ```tsx
    {canManageBilling && (
      <TabsTrigger value="billing">
        {t("tabs.billing")}
      </TabsTrigger>
    )}
    ```

    Add tab content:
    ```tsx
    {canManageBilling && (
      <TabsContent value="billing" className="mt-6 space-y-8">
        <BillingTab />
      </TabsContent>
    )}
    ```

    Add i18n key `tabs.billing` to the translation files (value: "Billing" in English, "Rozliczenia" in Polish). Check existing translation file locations.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && ls apps/web/src/components/billing/*.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/billing/billing-tab.tsx exists and contains `createPortalSession` and `createCheckoutSession`
    - apps/web/src/components/billing/plan-comparison-grid.tsx contains `role="radiogroup"` and `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
    - apps/web/src/components/billing/plan-card.tsx contains `role="radio"` and `19900` and `44900` and `84900`
    - apps/web/src/components/billing/plan-card.tsx contains "Choose a plan" and "Upgrade plan" and "Change plan"
    - apps/web/src/components/billing/current-plan-summary.tsx contains `getSubscription` and `Skeleton`
    - apps/web/src/components/billing/credit-usage-card.tsx contains `Progress` and `aria-valuenow` and `aria-label`
    - apps/web/src/components/billing/credit-usage-card.tsx contains `Buy credits`
    - apps/web/src/components/billing/proration-preview.tsx contains `getProrationPreview`
    - apps/web/src/components/billing/premium-badge.tsx contains `Gem` and `aria-label`
    - apps/web/src/components/billing/top-up-dialog.tsx contains `Dialog`
    - apps/web/src/app/[locale]/(dashboard)/settings/page.tsx contains `BillingTab` and `value="billing"`
    - `ls apps/web/src/components/billing/*.tsx | wc -l` outputs at least 9
  </acceptance_criteria>
  <done>All billing components created with correct pricing, accessibility attributes, responsive grid, and wired to tRPC billing router. Billing tab added to Settings page gated by admin permission</done>
</task>

<task type="auto">
  <name>Task 2: Trial banner, soft-block modal, and credit exhausted inline</name>
  <files>
    apps/web/src/components/billing/trial-banner.tsx,
    apps/web/src/components/billing/soft-block-modal.tsx,
    apps/web/src/components/billing/credit-exhausted-inline.tsx,
    apps/web/src/app/[locale]/(dashboard)/layout.tsx
  </files>
  <read_first>
    apps/web/src/app/[locale]/(dashboard)/layout.tsx,
    apps/web/src/components/billing/plan-comparison-grid.tsx,
    apps/web/src/components/billing/billing-tab.tsx,
    apps/web/src/components/ui/dialog.tsx,
    apps/web/src/components/ui/alert-dialog.tsx,
    apps/web/src/components/ui/button.tsx,
    .planning/phases/28-stripe-billing-foundation/28-UI-SPEC.md
  </read_first>
  <action>
    **1. Create `apps/web/src/components/billing/trial-banner.tsx`:**

    Global banner per D-17 -- appears ONLY during last 7 days of trial.

    Props: `{ trialEnd: Date; onUpgrade: () => void }`.

    Calculate days remaining: `Math.ceil((trialEnd.getTime() - Date.now()) / (1000 * 60 * 60 * 24))`.

    Only render if daysRemaining > 0 AND daysRemaining <= 7.

    Layout per UI-SPEC:
    - Full-width, positioned below nav, above page content
    - Background: `bg-warning/10` (muted warning), left accent border: `border-l-4 border-warning`
    - `role="alert"` with `aria-live="polite"`
    - Copy varies by days remaining per copywriting contract:
      - 7 days: "Your trial ends in 7 days. Upgrade to keep your data and full access."
      - 3 days: "Your trial ends in 3 days. Choose a plan to continue without interruption."
      - 1 day: "Your trial ends tomorrow. Upgrade now to avoid losing access to features."
      - Other (2-6): "Your trial ends in {N} days. Upgrade to keep your data and full access."
    - Action: "Choose a plan" Button (primary variant) that calls onUpgrade
    - Dismissible per session via close button (X icon). Use React state or sessionStorage. Reappears on next login
    - Responsive: On mobile (< 640px) text wraps and button below text. On md+ text and button on same row (`flex items-center justify-between`)

    **2. Create `apps/web/src/components/billing/soft-block-modal.tsx`:**

    Full viewport overlay per D-09 -- appears when trial has ended (subscription status is CANCELED or expired and was TRIALING).

    Props: `{ isOpen: boolean; onSelectPlan: (priceId: string) => void }`.

    Layout per UI-SPEC:
    - Uses Dialog component with `open={isOpen}` but NO `onOpenChange` handler that closes
    - `aria-modal="true"`, `role="alertdialog"`
    - NOT dismissible via backdrop click: set `onPointerDownOutside={(e) => e.preventDefault()}` and `onEscapeKeyDown={(e) => e.preventDefault()}`
    - Close button visible but triggers re-show on next navigation (set state, but parent re-opens on route change)
    - Content:
      - Heading (Display 28px): "Your trial has ended" per copywriting contract
      - Body: "Your data is safe. Choose a plan to continue using Contractor Ops."
      - PlanComparisonGrid with `compact={true}` variant (less padding, no excluded features)
      - Each plan CTA redirects to Stripe Checkout via onSelectPlan

    **3. Create `apps/web/src/components/billing/credit-exhausted-inline.tsx`:**

    Inline component per D-14 that replaces OCR trigger when credits are zero.

    Props: `{ onUpgrade: () => void; onBuyCredits: () => void }`.

    Layout per UI-SPEC:
    - Inline, not modal -- replaces normal extraction CTA area
    - Warning icon (lucide `AlertTriangle`), `text-destructive`
    - Heading: "OCR credits exhausted" per copywriting contract
    - Body: "You have used all OCR credits this month. Upgrade your plan for more credits or purchase a top-up bundle."
    - Two buttons: "Upgrade plan" (default variant) and "Buy credits" (outline variant)
    - `role="alert"` for screen reader announcement

    **4. Update `apps/web/src/app/[locale]/(dashboard)/layout.tsx`:**

    Add the TrialBanner and SoftBlockModal to the dashboard layout so they appear globally.

    Import: `import { TrialBanner } from "@/components/billing/trial-banner";` and `import { SoftBlockModal } from "@/components/billing/soft-block-modal";`

    Query subscription status: Use `api.billing.getSubscription.useQuery()` (or a lighter tRPC procedure if available).

    Render TrialBanner:
    - Only if subscription exists AND status is TRIALING AND trialEnd is within 7 days
    - Position: after the nav/header component, before the main content `{children}`
    - onUpgrade: navigate to `/settings?tab=billing`

    Render SoftBlockModal:
    - Only if subscription exists AND status is TRIALING AND trialEnd is in the past (trial expired)
    - OR if subscription status is CANCELED and last status was TRIALING
    - isOpen determined by subscription state
    - onSelectPlan: call billing.createCheckoutSession and redirect

    Ensure these are wrapped in the existing tRPC context provider. The layout likely already has the tRPC provider wrapping children.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && grep -l "trial-banner\|soft-block-modal" apps/web/src/app/\[locale\]/\(dashboard\)/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/billing/trial-banner.tsx contains `role="alert"` and `aria-live="polite"`
    - apps/web/src/components/billing/trial-banner.tsx contains "Your trial ends in" and "Choose a plan"
    - apps/web/src/components/billing/trial-banner.tsx contains `border-l-4` and `border-warning`
    - apps/web/src/components/billing/trial-banner.tsx renders only when daysRemaining <= 7 and > 0
    - apps/web/src/components/billing/soft-block-modal.tsx contains `role="alertdialog"` and `aria-modal="true"`
    - apps/web/src/components/billing/soft-block-modal.tsx contains `onPointerDownOutside` and `onEscapeKeyDown` with `preventDefault`
    - apps/web/src/components/billing/soft-block-modal.tsx contains "Your trial has ended"
    - apps/web/src/components/billing/soft-block-modal.tsx contains `PlanComparisonGrid`
    - apps/web/src/components/billing/credit-exhausted-inline.tsx contains `role="alert"` and "OCR credits exhausted"
    - apps/web/src/components/billing/credit-exhausted-inline.tsx contains "Upgrade plan" and "Buy credits"
    - apps/web/src/app/[locale]/(dashboard)/layout.tsx contains `TrialBanner` and `SoftBlockModal`
  </acceptance_criteria>
  <done>Trial banner shows during last 7 days with correct copy per D-17/D-10, soft-block modal blocks on trial expiry per D-09, credit exhausted inline replaces OCR trigger per D-14, all wired into dashboard layout</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete billing UI</name>
  <files>apps/web/src/components/billing/billing-tab.tsx, apps/web/src/components/billing/plan-comparison-grid.tsx, apps/web/src/components/billing/trial-banner.tsx, apps/web/src/components/billing/soft-block-modal.tsx</files>
  <action>
    Human verifies the complete billing UI visually and interactively.

    What was built: Complete billing UI with Settings Billing tab showing plan comparison grid (Starter 199 PLN / Pro 449 PLN / Enterprise 849 PLN), current plan summary, credit usage with progress bar, trial banner (appears in last 7 days), soft-block modal (appears on trial expiry), proration preview, credit exhausted inline, top-up dialog, and premium diamond badge. All plan CTAs redirect to Stripe Checkout. Manage billing button accesses Stripe portal.

    How to verify:
    1. Start dev server: `pnpm dev`
    2. Navigate to Settings page and verify "Billing" tab appears (admin user only)
    3. Click Billing tab and verify: Current Plan Summary card, Credit Usage card with progress bar, Plan Comparison Grid with 3 cards (Starter/Pro/Enterprise), Pro card has "Recommended" badge, responsive layout (1-col mobile, 2-col tablet, 3-col desktop), "Manage billing" button at bottom
    4. Verify accessibility: Tab through plan cards with keyboard, check ARIA roles (radiogroup, radio, alert)
    5. If org has trial status: verify trial banner appears at top of dashboard with correct day count
    6. Verify premium badge (diamond icon) renders with tooltip
  </action>
  <verify>Human visual inspection confirms all billing components render correctly</verify>
  <done>User approves billing UI visual appearance, responsiveness, and accessibility</done>
</task>

</tasks>

<verification>
1. All billing component files exist: `ls apps/web/src/components/billing/*.tsx | wc -l` >= 9
2. Settings page includes billing tab: `grep "billing" apps/web/src/app/[locale]/(dashboard)/settings/page.tsx`
3. Dashboard layout includes trial banner and soft-block: `grep -l "TrialBanner\|SoftBlockModal" apps/web/src/app/[locale]/(dashboard)/layout.tsx`
4. Plan prices match decisions: `grep "19900\|44900\|84900" apps/web/src/components/billing/plan-comparison-grid.tsx`
5. Accessibility: `grep "role=" apps/web/src/components/billing/*.tsx` shows radiogroup, radio, alert, alertdialog
6. Build succeeds: `pnpm --filter web build` exits 0
</verification>

<success_criteria>
- All 11 billing UI components created matching UI-SPEC layout contract
- Settings page has Billing tab gated by admin permission
- Plan cards show correct pricing per D-01/D-02/D-03 with feature lists per D-05
- Plan selection redirects to Stripe Checkout via tRPC mutation
- Proration preview shown for upgrade/downgrade per D-19
- Trial banner appears in last 7 days per D-17 with correct copy per D-10
- Soft-block modal appears on trial expiry per D-09, not permanently dismissible
- Credit exhausted inline shows at OCR trigger point per D-14
- Premium badge uses Gem icon per D-16
- All accessibility requirements met per UI-SPEC (radiogroup, alert, alertdialog, aria-labels, focus trap)
- Human verification confirms visual correctness
</success_criteria>

<output>
After completion, create `.planning/phases/28-stripe-billing-foundation/28-03-SUMMARY.md`
</output>
