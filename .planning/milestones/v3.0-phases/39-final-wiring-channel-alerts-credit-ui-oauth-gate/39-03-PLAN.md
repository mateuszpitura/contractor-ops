---
phase: 39-final-wiring-channel-alerts-credit-ui-oauth-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/integrations/linear-provider-section.tsx
  - apps/web/src/components/integrations/google-workspace-provider-section.tsx
  - apps/web/src/components/integrations/teams-provider-section.tsx
  - apps/web/src/components/integrations/__tests__/linear-provider-section.test.tsx
  - apps/web/src/components/integrations/__tests__/google-workspace-provider-section.test.tsx
  - apps/web/src/components/integrations/__tests__/teams-provider-section.test.tsx
autonomous: true
requirements: [BILL-09]

must_haves:
  truths:
    - "STARTER-tier users see UpgradeInlineBanner instead of OAuth connect buttons for Linear, Google Workspace, and Teams"
    - "PRO/ENTERPRISE users see the normal provider section with connect button"
    - "FeatureGate renders children during loading to avoid flash"
  artifacts:
    - path: "apps/web/src/components/integrations/linear-provider-section.tsx"
      provides: "FeatureGate wrapping with featureName='Linear integration'"
      contains: "FeatureGate"
    - path: "apps/web/src/components/integrations/google-workspace-provider-section.tsx"
      provides: "FeatureGate wrapping with featureName='Google Workspace integration'"
      contains: "FeatureGate"
    - path: "apps/web/src/components/integrations/teams-provider-section.tsx"
      provides: "FeatureGate wrapping with featureName='Microsoft Teams integration'"
      contains: "FeatureGate"
  key_links:
    - from: "apps/web/src/components/integrations/linear-provider-section.tsx"
      to: "apps/web/src/components/billing/feature-gate.tsx"
      via: "import { FeatureGate }"
      pattern: "FeatureGate"
    - from: "apps/web/src/components/integrations/google-workspace-provider-section.tsx"
      to: "apps/web/src/components/billing/feature-gate.tsx"
      via: "import { FeatureGate }"
      pattern: "FeatureGate"
    - from: "apps/web/src/components/integrations/teams-provider-section.tsx"
      to: "apps/web/src/components/billing/feature-gate.tsx"
      via: "import { FeatureGate }"
      pattern: "FeatureGate"
---

<objective>
Wrap Linear, Google Workspace, and Teams OAuth provider sections with FeatureGate to require PRO tier.

Purpose: Closes MISSING-06 from v3.0 audit — STARTER users can currently click OAuth connect buttons and only hit API-level tier gate (bad UX). FeatureGate shows a graceful upgrade prompt instead.
Output: All three provider sections are wrapped with FeatureGate requiring PRO tier.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-final-wiring-channel-alerts-credit-ui-oauth-gate/39-RESEARCH.md
@.planning/phases/39-final-wiring-channel-alerts-credit-ui-oauth-gate/39-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From apps/web/src/components/billing/feature-gate.tsx:
```typescript
interface FeatureGateProps {
  requiredTier: "Pro" | "Enterprise";
  featureName: string;
  children: React.ReactNode;
}

export function FeatureGate({ requiredTier, featureName, children }: FeatureGateProps)
```

FeatureGate behavior:
- During loading: renders children (no flash — Phase 35 decision)
- STARTER tier: renders UpgradeInlineBanner instead of children
- PRO/ENTERPRISE: renders children normally

Import path: `@/components/billing/feature-gate`

Feature names per UI-SPEC:
- Linear: "Linear integration"
- Google Workspace: "Google Workspace integration"
- Teams: "Microsoft Teams integration"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap provider sections with FeatureGate</name>
  <files>apps/web/src/components/integrations/linear-provider-section.tsx, apps/web/src/components/integrations/google-workspace-provider-section.tsx, apps/web/src/components/integrations/teams-provider-section.tsx</files>
  <read_first>
    - apps/web/src/components/integrations/linear-provider-section.tsx
    - apps/web/src/components/integrations/google-workspace-provider-section.tsx
    - apps/web/src/components/integrations/teams-provider-section.tsx
    - apps/web/src/components/billing/feature-gate.tsx
  </read_first>
  <action>
For each of the three provider section files, apply the same pattern:

1. Add import at the top:
```typescript
import { FeatureGate } from "@/components/billing/feature-gate";
```

2. Wrap the entire return JSX with FeatureGate. The pattern for each file:

**linear-provider-section.tsx:**
```typescript
return (
  <FeatureGate requiredTier="Pro" featureName="Linear integration">
    {/* existing JSX unchanged */}
  </FeatureGate>
);
```

**google-workspace-provider-section.tsx:**
```typescript
return (
  <FeatureGate requiredTier="Pro" featureName="Google Workspace integration">
    {/* existing JSX unchanged */}
  </FeatureGate>
);
```

**teams-provider-section.tsx:**
```typescript
return (
  <FeatureGate requiredTier="Pro" featureName="Microsoft Teams integration">
    {/* existing JSX unchanged */}
  </FeatureGate>
);
```

Key points:
- Wrap at the component return level (per Phase 38 pattern: "FeatureGate wraps at component return level for dedicated feature components")
- requiredTier is "Pro" (capital P — matches FeatureGateProps type)
- featureName strings must match exactly as listed above (these feed into the i18n template)
- All three files are already "use client" components, so FeatureGate (which uses hooks) works without issues
- Do NOT wrap inner elements or individual buttons — wrap the entire section
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/components/integrations/__tests__/linear-provider-section.test.tsx src/components/integrations/__tests__/google-workspace-provider-section.test.tsx src/components/integrations/__tests__/teams-provider-section.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - linear-provider-section.tsx contains `import { FeatureGate } from "@/components/billing/feature-gate"`
    - linear-provider-section.tsx contains `<FeatureGate requiredTier="Pro" featureName="Linear integration">`
    - google-workspace-provider-section.tsx contains `import { FeatureGate } from "@/components/billing/feature-gate"`
    - google-workspace-provider-section.tsx contains `<FeatureGate requiredTier="Pro" featureName="Google Workspace integration">`
    - teams-provider-section.tsx contains `import { FeatureGate } from "@/components/billing/feature-gate"`
    - teams-provider-section.tsx contains `<FeatureGate requiredTier="Pro" featureName="Microsoft Teams integration">`
  </acceptance_criteria>
  <done>All three OAuth provider sections are wrapped with FeatureGate requiring PRO tier with correct feature names</done>
</task>

<task type="auto">
  <name>Task 2: Add FeatureGate wrapping tests</name>
  <files>apps/web/src/components/integrations/__tests__/linear-provider-section.test.tsx, apps/web/src/components/integrations/__tests__/google-workspace-provider-section.test.tsx, apps/web/src/components/integrations/__tests__/teams-provider-section.test.tsx</files>
  <read_first>
    - apps/web/src/components/integrations/__tests__/linear-provider-section.test.tsx
    - apps/web/src/components/integrations/__tests__/google-workspace-provider-section.test.tsx
    - apps/web/src/components/integrations/__tests__/teams-provider-section.test.tsx
    - apps/web/src/components/billing/feature-gate.tsx
  </read_first>
  <action>
Add a test case to each of the three test files verifying FeatureGate wrapping:

**In linear-provider-section.test.tsx, add:**
- **"wraps content with FeatureGate requiring Pro tier"**: Mock billing.getSubscription to return STARTER tier. Render LinearProviderSection. Assert that UpgradeInlineBanner text appears (e.g., text matching "Linear integration" and "Pro"). Assert that the connect button / ProviderConnectionCard is NOT visible.

**In google-workspace-provider-section.test.tsx, add:**
- **"wraps content with FeatureGate requiring Pro tier"**: Same pattern. Mock STARTER tier. Assert "Google Workspace integration" text appears in upgrade banner. Assert connect button is NOT visible.

**In teams-provider-section.test.tsx, add:**
- **"wraps content with FeatureGate requiring Pro tier"**: Same pattern. Mock STARTER tier. Assert "Microsoft Teams integration" text appears in upgrade banner. Assert connect button is NOT visible.

Follow the existing test patterns in each file for mock setup and rendering approach. The FeatureGate internally calls `billing.getSubscription` tRPC query — mock it at the tRPC level consistent with how other tests mock tRPC queries in this codebase.
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run src/components/integrations/__tests__/linear-provider-section.test.tsx src/components/integrations/__tests__/google-workspace-provider-section.test.tsx src/components/integrations/__tests__/teams-provider-section.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - linear-provider-section.test.tsx contains test case with description matching "FeatureGate" and "Pro"
    - google-workspace-provider-section.test.tsx contains test case with description matching "FeatureGate" and "Pro"
    - teams-provider-section.test.tsx contains test case with description matching "FeatureGate" and "Pro"
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>Three test files verify that STARTER-tier users see upgrade prompt instead of provider section content</done>
</task>

</tasks>

<verification>
cd apps/web && npx vitest run src/components/integrations/__tests__/linear-provider-section.test.tsx src/components/integrations/__tests__/google-workspace-provider-section.test.tsx src/components/integrations/__tests__/teams-provider-section.test.tsx -x
</verification>

<success_criteria>
- All three provider sections import and render FeatureGate with requiredTier="Pro"
- Feature names match exactly: "Linear integration", "Google Workspace integration", "Microsoft Teams integration"
- STARTER-tier users see UpgradeInlineBanner, PRO/ENTERPRISE see normal section
- All existing + new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/39-final-wiring-channel-alerts-credit-ui-oauth-gate/39-03-SUMMARY.md`
</output>
