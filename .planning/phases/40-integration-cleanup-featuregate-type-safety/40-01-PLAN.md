---
phase: 40-integration-cleanup-featuregate-type-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/integrations/jira-provider-section.tsx
  - apps/web/src/components/settings/org-calendar-section.tsx
  - apps/web/src/components/settings/my-calendar-section.tsx
  - packages/api/dist/
autonomous: true
requirements:
  - BILL-09

must_haves:
  truths:
    - "STARTER-tier user sees FeatureGate upgrade banner on Jira provider section instead of raw connection UI"
    - "STARTER-tier user sees FeatureGate upgrade banner on both Calendar sections instead of raw connection UI"
    - "API package builds successfully with tsc producing updated dist/index.d.ts"
  artifacts:
    - path: "apps/web/src/components/integrations/jira-provider-section.tsx"
      provides: "FeatureGate-wrapped Jira provider section"
      contains: "FeatureGate"
    - path: "apps/web/src/components/settings/org-calendar-section.tsx"
      provides: "FeatureGate-wrapped org calendar section"
      contains: "FeatureGate"
    - path: "apps/web/src/components/settings/my-calendar-section.tsx"
      provides: "FeatureGate-wrapped personal calendar section"
      contains: "FeatureGate"
    - path: "packages/api/dist/index.d.ts"
      provides: "Rebuilt API dist types with all routers"
  key_links:
    - from: "apps/web/src/components/integrations/jira-provider-section.tsx"
      to: "apps/web/src/components/billing/feature-gate.tsx"
      via: "import FeatureGate"
      pattern: 'import.*FeatureGate.*from.*billing/feature-gate'
    - from: "apps/web/src/components/settings/org-calendar-section.tsx"
      to: "apps/web/src/components/billing/feature-gate.tsx"
      via: "import FeatureGate"
      pattern: 'import.*FeatureGate.*from.*billing/feature-gate'
    - from: "apps/web/src/components/settings/my-calendar-section.tsx"
      to: "apps/web/src/components/billing/feature-gate.tsx"
      via: "import FeatureGate"
      pattern: 'import.*FeatureGate.*from.*billing/feature-gate'
---

<objective>
Wrap Jira and Calendar OAuth provider sections with FeatureGate (PRO tier) and rebuild API dist types to unblock proxy removal.

Purpose: Closes FINDING-01 (Jira/Calendar missing FeatureGate) and prepares for FINDING-02 (stale dist types) from v3.0 milestone audit. After this plan, all OAuth provider sections are consistently gated, and API types are fresh for Plan 02 proxy cleanup.

Output: 3 FeatureGate-wrapped components + rebuilt API dist directory
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/web/src/components/billing/feature-gate.tsx
@apps/web/src/components/integrations/linear-provider-section.tsx
</context>

<interfaces>
<!-- FeatureGate component API (from apps/web/src/components/billing/feature-gate.tsx) -->
<!-- Executor must use this exact component with these exact props -->

```tsx
import { FeatureGate } from "@/components/billing/feature-gate";

// Usage (from linear-provider-section.tsx reference):
<FeatureGate requiredTier="Pro" featureName="Linear integration">
  {/* wrapped content */}
</FeatureGate>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wrap Jira and Calendar provider sections with FeatureGate</name>
  <files>
    apps/web/src/components/integrations/jira-provider-section.tsx
    apps/web/src/components/settings/org-calendar-section.tsx
    apps/web/src/components/settings/my-calendar-section.tsx
  </files>
  <read_first>
    apps/web/src/components/integrations/jira-provider-section.tsx
    apps/web/src/components/settings/org-calendar-section.tsx
    apps/web/src/components/settings/my-calendar-section.tsx
    apps/web/src/components/integrations/linear-provider-section.tsx
    apps/web/src/components/billing/feature-gate.tsx
  </read_first>
  <action>
    For each of the 3 files, apply the exact same FeatureGate wrapping pattern used in linear-provider-section.tsx:

    1. **jira-provider-section.tsx**: Add `import { FeatureGate } from "@/components/billing/feature-gate";` to imports. Wrap the outermost `return` JSX with `<FeatureGate requiredTier="Pro" featureName="Jira integration">...</FeatureGate>`.

    2. **org-calendar-section.tsx**: Add `import { FeatureGate } from "@/components/billing/feature-gate";` to imports. Wrap the outermost `return` JSX with `<FeatureGate requiredTier="Pro" featureName="Calendar integration">...</FeatureGate>`.

    3. **my-calendar-section.tsx**: Add `import { FeatureGate } from "@/components/billing/feature-gate";` to imports. Wrap the outermost `return` JSX with `<FeatureGate requiredTier="Pro" featureName="Calendar integration">...</FeatureGate>`.

    Pattern to follow exactly (from linear-provider-section.tsx):
    ```tsx
    return (
      <FeatureGate requiredTier="Pro" featureName="Jira integration">
        <div className="space-y-4">
          {/* existing content */}
        </div>
      </FeatureGate>
    );
    ```

    Do NOT change any existing content inside the wrapped JSX. Only add the import and the wrapper element.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && grep -l "FeatureGate" apps/web/src/components/integrations/jira-provider-section.tsx apps/web/src/components/settings/org-calendar-section.tsx apps/web/src/components/settings/my-calendar-section.tsx | wc -l | grep -q "3" && echo "PASS: All 3 files have FeatureGate"</automated>
  </verify>
  <acceptance_criteria>
    - jira-provider-section.tsx contains `import { FeatureGate } from "@/components/billing/feature-gate"`
    - jira-provider-section.tsx contains `<FeatureGate requiredTier="Pro" featureName="Jira integration">`
    - org-calendar-section.tsx contains `import { FeatureGate } from "@/components/billing/feature-gate"`
    - org-calendar-section.tsx contains `<FeatureGate requiredTier="Pro" featureName="Calendar integration">`
    - my-calendar-section.tsx contains `import { FeatureGate } from "@/components/billing/feature-gate"`
    - my-calendar-section.tsx contains `<FeatureGate requiredTier="Pro" featureName="Calendar integration">`
    - No existing JSX content inside the wrapper is modified
  </acceptance_criteria>
  <done>All 3 provider sections wrapped with FeatureGate requiring PRO tier, matching Linear/GWS/Teams pattern</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild API dist types</name>
  <files>
    packages/api/dist/
  </files>
  <read_first>
    packages/api/package.json
    packages/api/tsconfig.json
    packages/api/src/root.ts
  </read_first>
  <action>
    Run the API package build to regenerate dist types:

    ```bash
    pnpm --filter @contractor-ops/api build
    ```

    This executes `tsc` which outputs to `packages/api/dist/`. The root router in `packages/api/src/root.ts` registers all sub-routers (billing, teams, equipment, portal, settings, etc.), so the rebuilt `dist/index.d.ts` will export the complete `AppRouter` type with all procedures.

    If the build fails with type errors:
    - Read the error output carefully
    - Fix any type errors in the API package source files (typically in recently added routers)
    - Re-run the build until it succeeds

    After successful build, verify that the dist types include the routers that were previously missing:
    - `billing` router (getUsageDashboard)
    - `teams` router (getTeams, getChannels, saveChannelMapping, getChannelMapping)
    - `equipment` router (createInPostShipment, createDpdShipment, createUpsShipment, getShipmentLabel, etc.)
    - `portal` router (listEquipment, getReturnStatus, requestReturn)
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/api build && echo "PASS: API build succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @contractor-ops/api build` exits with code 0
    - `packages/api/dist/index.d.ts` exists and was modified within the last minute
    - `packages/api/dist/index.d.ts` contains `billing` router type
    - `packages/api/dist/index.d.ts` contains `teams` router type
    - `packages/api/dist/index.d.ts` contains `equipment` router type
    - `packages/api/dist/index.d.ts` contains `portal` router type
  </acceptance_criteria>
  <done>API dist types rebuilt with all routers, unblocking proxy removal in Plan 02</done>
</task>

</tasks>

<verification>
- All 3 FeatureGate wrappers in place: `grep -c "FeatureGate" apps/web/src/components/integrations/jira-provider-section.tsx apps/web/src/components/settings/org-calendar-section.tsx apps/web/src/components/settings/my-calendar-section.tsx`
- API build succeeds: `pnpm --filter @contractor-ops/api build`
- No regressions: `pnpm --filter web tsc --noEmit`
</verification>

<success_criteria>
- Jira provider section wrapped with FeatureGate requiredTier="Pro"
- Both Calendar sections wrapped with FeatureGate requiredTier="Pro"
- API dist types rebuilt successfully with complete AppRouter type
- TypeScript compilation passes: `pnpm --filter web tsc --noEmit`
</success_criteria>

<output>
After completion, create `.planning/phases/40-integration-cleanup-featuregate-type-safety/40-01-SUMMARY.md`
</output>
