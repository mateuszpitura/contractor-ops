---
phase: 40-integration-cleanup-featuregate-type-safety
plan: 02
type: execute
wave: 2
depends_on:
  - 40-01
files_modified:
  - apps/web/src/components/billing/usage-dashboard.tsx
  - apps/web/src/components/integrations/teams-channel-mapping-card.tsx
  - apps/web/src/components/equipment/carrier-shipment-form.tsx
  - apps/web/src/components/equipment/inpost-shipment-form.tsx
  - apps/web/src/components/equipment/shipment-label-view.tsx
  - apps/web/src/components/equipment/return-approval-banner.tsx
  - apps/web/src/components/portal/portal-equipment-tab.tsx
  - apps/web/src/components/portal/portal-return-flow.tsx
  - apps/web/src/components/settings/carrier-credential-form.tsx
  - apps/web/src/components/settings/default-return-carrier-select.tsx
  - apps/web/src/components/settings/dpd-provider-section.tsx
  - apps/web/src/components/settings/ups-provider-section.tsx
  - apps/web/src/app/[locale]/(dashboard)/equipment/[id]/page.tsx
autonomous: true
requirements:
  - EQUIP-05
  - EQUIP-06
  - EQUIP-07
  - TEAM-02
  - BILL-10

must_haves:
  truths:
    - "No file in apps/web/src contains (trpc as any) proxy workaround"
    - "All 13 files use properly typed trpc.router.procedure calls"
    - "TypeScript compilation passes without errors"
    - "All existing tests pass without modification or with updated mocks"
  artifacts:
    - path: "apps/web/src/components/billing/usage-dashboard.tsx"
      provides: "Typed tRPC billing calls"
      contains: "trpc.billing.getUsageDashboard"
    - path: "apps/web/src/components/integrations/teams-channel-mapping-card.tsx"
      provides: "Typed tRPC teams calls"
      contains: "trpc.teams."
    - path: "apps/web/src/components/equipment/carrier-shipment-form.tsx"
      provides: "Typed tRPC equipment calls"
      contains: "trpc.equipment."
    - path: "apps/web/src/components/equipment/inpost-shipment-form.tsx"
      provides: "Typed tRPC equipment calls"
      contains: "trpc.equipment."
  key_links:
    - from: "apps/web/src/components/billing/usage-dashboard.tsx"
      to: "packages/api/dist/index.d.ts"
      via: "typed trpc client"
      pattern: "trpc\\.billing\\.getUsageDashboard"
    - from: "apps/web/src/components/integrations/teams-channel-mapping-card.tsx"
      to: "packages/api/dist/index.d.ts"
      via: "typed trpc client"
      pattern: "trpc\\.teams\\."
---

<objective>
Remove all 13 `(trpc as any)` proxy workarounds now that API dist types are rebuilt, restoring full type safety across the web app.

Purpose: Closes FINDING-02 from v3.0 milestone audit. With fresh dist types from Plan 01, all tRPC router procedures are properly typed and the proxy workarounds are no longer needed. This eliminates all `as any` type-safety escapes for tRPC calls.

Output: 13 files cleaned of proxy workarounds, zero `(trpc as any)` remaining in codebase
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-integration-cleanup-featuregate-type-safety/40-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove proxy workarounds from equipment and portal files (8 files)</name>
  <files>
    apps/web/src/components/equipment/carrier-shipment-form.tsx
    apps/web/src/components/equipment/inpost-shipment-form.tsx
    apps/web/src/components/equipment/shipment-label-view.tsx
    apps/web/src/components/equipment/return-approval-banner.tsx
    apps/web/src/components/portal/portal-equipment-tab.tsx
    apps/web/src/components/portal/portal-return-flow.tsx
    apps/web/src/components/settings/carrier-credential-form.tsx
    apps/web/src/app/[locale]/(dashboard)/equipment/[id]/page.tsx
  </files>
  <read_first>
    apps/web/src/components/equipment/carrier-shipment-form.tsx
    apps/web/src/components/equipment/inpost-shipment-form.tsx
    apps/web/src/components/equipment/shipment-label-view.tsx
    apps/web/src/components/equipment/return-approval-banner.tsx
    apps/web/src/components/portal/portal-equipment-tab.tsx
    apps/web/src/components/portal/portal-return-flow.tsx
    apps/web/src/components/settings/carrier-credential-form.tsx
    apps/web/src/app/[locale]/(dashboard)/equipment/[id]/page.tsx
  </read_first>
  <action>
    For each of the 8 files, apply the same 3-step transformation:

    **Step 1: Delete the proxy block.** Remove ALL of:
    - The section comment (if present, e.g., `// tRPC proxy for ...` or `// ---------------------------------------------------------------------------`)
    - The `// eslint-disable-next-line @typescript-eslint/no-explicit-any` comment (if present)
    - The `const xxxProxy = (trpc as any).xxx as { ... };` declaration (entire block including the type assertion object)

    **Step 2: Replace proxy references with direct tRPC calls.** Find-replace throughout the file:
    - `equipmentProxy.` -> `trpc.equipment.`
    - `portalProxy.` -> `trpc.portal.`
    - `courierConfigProxy.` -> `trpc.equipment.` (in equipment/[id]/page.tsx only)

    **Step 3: Verify no orphaned references.** Ensure no remaining references to `equipmentProxy`, `portalProxy`, or `courierConfigProxy` exist.

    **Specific file details:**

    1. **carrier-shipment-form.tsx** (line ~38-43): Remove `equipmentProxy` block. Replace `equipmentProxy.createInPostShipment`, `equipmentProxy.createDpdShipment`, `equipmentProxy.createUpsShipment` with `trpc.equipment.createInPostShipment`, `trpc.equipment.createDpdShipment`, `trpc.equipment.createUpsShipment`.

    2. **inpost-shipment-form.tsx** (line ~33-36): Remove `equipmentProxy` block. Replace `equipmentProxy.createInPostShipment` with `trpc.equipment.createInPostShipment`.

    3. **shipment-label-view.tsx** (line ~14-17): Remove `equipmentProxy` block. Replace `equipmentProxy.getShipmentLabel` with `trpc.equipment.getShipmentLabel`.

    4. **return-approval-banner.tsx** (line ~15-19): Remove `equipmentProxy` block. Replace `equipmentProxy.approveReturnRequest` and `equipmentProxy.rejectReturnRequest` with `trpc.equipment.approveReturnRequest` and `trpc.equipment.rejectReturnRequest`.

    5. **portal-equipment-tab.tsx** (line ~31-34): Remove `portalProxy` block. Replace `portalProxy.listEquipment` and `portalProxy.getReturnStatus` with `trpc.portal.listEquipment` and `trpc.portal.getReturnStatus`.

    6. **portal-return-flow.tsx** (line ~29-33): Remove `portalProxy` block. Replace `portalProxy.requestReturn` and `portalProxy.getReturnStatus` with `trpc.portal.requestReturn` and `trpc.portal.getReturnStatus`.

    7. **carrier-credential-form.tsx** (line ~24-27): Remove `equipmentProxy` block. Replace `equipmentProxy.saveCourierConfig` and `equipmentProxy.getCourierConfigs` with `trpc.equipment.saveCourierConfig` and `trpc.equipment.getCourierConfigs`.

    8. **equipment/[id]/page.tsx** (lines ~16-21 and ~33-37): Remove BOTH proxy blocks (`equipmentProxy` and `courierConfigProxy`). Replace `equipmentProxy.listReturnRequests` with `trpc.equipment.listReturnRequests` and `courierConfigProxy.getCourierConfigs` with `trpc.equipment.getCourierConfigs`.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && ! grep -r "trpc as any" apps/web/src/components/equipment/ apps/web/src/components/portal/ apps/web/src/components/settings/carrier-credential-form.tsx apps/web/src/app/[locale]/\(dashboard\)/equipment/ && echo "PASS: No proxies in equipment/portal files"</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `(trpc as any)` in carrier-shipment-form.tsx, inpost-shipment-form.tsx, shipment-label-view.tsx, return-approval-banner.tsx
    - Zero occurrences of `(trpc as any)` in portal-equipment-tab.tsx, portal-return-flow.tsx
    - Zero occurrences of `(trpc as any)` in carrier-credential-form.tsx
    - Zero occurrences of `(trpc as any)` in equipment/[id]/page.tsx
    - carrier-shipment-form.tsx contains `trpc.equipment.createDpdShipment`
    - inpost-shipment-form.tsx contains `trpc.equipment.createInPostShipment`
    - portal-equipment-tab.tsx contains `trpc.portal.listEquipment`
    - portal-return-flow.tsx contains `trpc.portal.requestReturn`
    - No references to `equipmentProxy`, `portalProxy`, or `courierConfigProxy` remain in any file
  </acceptance_criteria>
  <done>8 equipment/portal files use properly typed trpc calls with zero proxy workarounds</done>
</task>

<task type="auto">
  <name>Task 2: Remove proxy workarounds from billing, teams, and settings files (5 files) + verify tests and full type safety</name>
  <files>
    apps/web/src/components/billing/usage-dashboard.tsx
    apps/web/src/components/integrations/teams-channel-mapping-card.tsx
    apps/web/src/components/settings/default-return-carrier-select.tsx
    apps/web/src/components/settings/dpd-provider-section.tsx
    apps/web/src/components/settings/ups-provider-section.tsx
  </files>
  <read_first>
    apps/web/src/components/billing/usage-dashboard.tsx
    apps/web/src/components/integrations/teams-channel-mapping-card.tsx
    apps/web/src/components/settings/default-return-carrier-select.tsx
    apps/web/src/components/settings/dpd-provider-section.tsx
    apps/web/src/components/settings/ups-provider-section.tsx
  </read_first>
  <action>
    Apply the same 3-step transformation to the remaining 5 files:

    **Step 1: Delete the proxy block** (comments + eslint-disable + const declaration).
    **Step 2: Replace proxy references** with direct tRPC calls.
    **Step 3: Verify no orphaned references.**

    **Specific file details:**

    1. **usage-dashboard.tsx** (lines ~23-29): Remove the section comment block (`// ---------------------------------------------------------------------------` x2) and the `billingProxy` declaration. Replace `billingProxy.getUsageDashboard` with `trpc.billing.getUsageDashboard` throughout the file.

    2. **teams-channel-mapping-card.tsx** (line ~30-35): Remove `teamsProxy` block. Replace all `teamsProxy.getTeams`, `teamsProxy.getChannels`, `teamsProxy.saveChannelMapping`, `teamsProxy.getChannelMapping` with `trpc.teams.getTeams`, `trpc.teams.getChannels`, `trpc.teams.saveChannelMapping`, `trpc.teams.getChannelMapping`.

    3. **default-return-carrier-select.tsx** (lines ~21-28): Remove BOTH proxy blocks (`equipmentProxy` on line ~21-24 and `settingsProxy` on line ~26-28). Replace `equipmentProxy.getCourierConfigs` with `trpc.equipment.getCourierConfigs`, and `settingsProxy.updateOrgSettings` / `settingsProxy.getOrgSettings` with `trpc.settings.updateOrgSettings` / `trpc.settings.getOrgSettings`.

    4. **dpd-provider-section.tsx** (line ~25-28): Remove `equipmentProxy` block. Replace `equipmentProxy.getCourierConfigs` with `trpc.equipment.getCourierConfigs`.

    5. **ups-provider-section.tsx** (line ~25-28): Remove `equipmentProxy` block. Replace `equipmentProxy.getCourierConfigs` with `trpc.equipment.getCourierConfigs`.

    **After all 13 files are cleaned**, run full type check to confirm no regressions:
    ```bash
    pnpm --filter web tsc --noEmit
    ```

    If type errors appear, fix them in the affected files. Common fixes:
    - If a procedure's return type changed, update the consuming code to match
    - If a procedure expects different input shape, align the call site

    **Test mock audit (RESEARCH.md Pitfall 4):** After proxy removal, verify that existing test files still pass. The test files mock `@/trpc/init` with `trpc.{router}.{procedure}` paths directly (not the proxy variables), so they should be compatible with the new direct calls. Specifically verify these test files:
    - `apps/web/src/components/billing/__tests__/usage-dashboard.test.tsx` (mocks `trpc.billing.getUsageDashboard`)
    - `apps/web/src/components/integrations/__tests__/teams-channel-mapping-card.test.tsx` (mocks `trpc.teams.*`)
    - `apps/web/src/components/equipment/__tests__/inpost-shipment-form.test.tsx` (mocks `trpc.equipment.*`)
    - `apps/web/src/components/equipment/__tests__/shipment-label-view.test.tsx` (mocks `trpc.equipment.*`)
    - `apps/web/src/components/equipment/__tests__/return-approval-banner.test.tsx` (mocks `trpc.equipment.*`)
    - `apps/web/src/components/portal/__tests__/portal-equipment-tab.test.tsx` (mocks `trpc.portal.*`)
    - `apps/web/src/components/settings/__tests__/default-return-carrier-select.test.tsx` (mocks `trpc.equipment.*` and `trpc.settings.*`)
    - `apps/web/src/components/settings/__tests__/carrier-credential-form.test.tsx` (mocks `trpc.equipment.*`)

    Run the test suite to confirm:
    ```bash
    pnpm --filter web test -- --run
    ```

    If any test fails due to mock path mismatches (e.g., a test was mocking the proxy pattern instead of the direct tRPC path), update the mock in the test file to use the correct `trpc.{router}.{procedure}` path. Based on audit, all 8 test files above already mock the correct paths, so no changes should be needed, but verify by running the tests.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && ! grep -r "trpc as any" apps/web/src/ && pnpm --filter web tsc --noEmit && pnpm --filter web test -- --run && echo "PASS: Zero proxies, TypeScript clean, tests pass"</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `(trpc as any)` anywhere in apps/web/src/
    - usage-dashboard.tsx contains `trpc.billing.getUsageDashboard`
    - teams-channel-mapping-card.tsx contains `trpc.teams.getTeams`
    - default-return-carrier-select.tsx contains `trpc.equipment.getCourierConfigs` and `trpc.settings.updateOrgSettings`
    - dpd-provider-section.tsx contains `trpc.equipment.getCourierConfigs`
    - ups-provider-section.tsx contains `trpc.equipment.getCourierConfigs`
    - No references to `billingProxy`, `teamsProxy`, `equipmentProxy`, `settingsProxy`, `portalProxy`, or `courierConfigProxy` remain anywhere in apps/web/src/
    - `pnpm --filter web tsc --noEmit` exits with code 0
    - `pnpm --filter web test -- --run` exits with code 0
  </acceptance_criteria>
  <done>All 13 proxy workarounds removed, zero `(trpc as any)` in codebase, TypeScript compilation clean, all tests pass</done>
</task>

</tasks>

<verification>
- Zero proxies: `grep -r "trpc as any" apps/web/src/ | wc -l` returns 0
- Zero proxy variable references: `grep -rE "(billing|teams|equipment|portal|settings|courierConfig)Proxy" apps/web/src/ | wc -l` returns 0
- TypeScript clean: `pnpm --filter web tsc --noEmit` exits 0
- Tests pass: `pnpm --filter web test -- --run`
</verification>

<success_criteria>
- All 13 files use direct trpc.router.procedure calls instead of proxy workarounds
- Zero occurrences of `(trpc as any)` in the entire web app source
- TypeScript compilation passes without errors
- Existing tests continue to pass (verified by `pnpm --filter web test -- --run`)
</success_criteria>

<output>
After completion, create `.planning/phases/40-integration-cleanup-featuregate-type-safety/40-02-SUMMARY.md`
</output>
