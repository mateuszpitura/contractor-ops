---
phase: 34-intelligent-onboarding-wizard
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - apps/web/src/app/[locale]/(dashboard)/onboarding/import/page.tsx
  - apps/web/src/components/onboarding/import-wizard.tsx
  - apps/web/src/components/onboarding/source-selection-step.tsx
  - apps/web/src/components/onboarding/source-card.tsx
  - apps/web/src/components/onboarding/people-review-step.tsx
  - apps/web/src/components/onboarding/conflict-resolution-popover.tsx
  - apps/web/src/components/onboarding/project-import-step.tsx
  - apps/web/src/components/onboarding/confirm-import-step.tsx
  - apps/web/src/components/onboarding/import-progress-tracker.tsx
  - apps/web/src/components/onboarding/onboarding-checklist.tsx
  - apps/web/messages/en.json
  - apps/web/messages/pl.json
autonomous: false
requirements: [ONBD-01, ONBD-02, ONBD-03, ONBD-04, ONBD-05]

must_haves:
  truths:
    - "User sees 4 source cards (Jira, Linear, Google Workspace, Slack) with connection status at /onboarding/import"
    - "Unconnected tools show Connect button that triggers OAuth popup"
    - "Connected tools show Import toggle switch"
    - "User sees merged preview table with Name, Email, Sources badges, Status badge, Role dropdown"
    - "Conflict badge opens popover showing source values with radio selection"
    - "Batch toolbar appears with Import Selected / Skip Selected / Assign Role"
    - "Filter tabs work: All / New / Conflicts / Existing"
    - "User sees project cards with editable steps list per Jira project / Linear team"
    - "Async import shows progress bar with per-item status and retry for failed items"
    - "Onboarding checklist step 2 links to /onboarding/import"
    - "Skip link on step 1 redirects to members page"
    - "Wizard accessible from Settings > Integrations"
  artifacts:
    - path: "apps/web/src/app/[locale]/(dashboard)/onboarding/import/page.tsx"
      provides: "Full-page wizard route"
      min_lines: 10
    - path: "apps/web/src/components/onboarding/import-wizard.tsx"
      provides: "4-step wizard container with step state management"
      min_lines: 80
    - path: "apps/web/src/components/onboarding/source-selection-step.tsx"
      provides: "Step 1: source cards with OAuth"
      min_lines: 60
    - path: "apps/web/src/components/onboarding/people-review-step.tsx"
      provides: "Step 2: merged preview table with dedup, conflict resolution, batch actions"
      min_lines: 150
    - path: "apps/web/src/components/onboarding/project-import-step.tsx"
      provides: "Step 3: project cards with editable workflow steps"
      min_lines: 80
    - path: "apps/web/src/components/onboarding/confirm-import-step.tsx"
      provides: "Step 4: summary + async progress"
      min_lines: 80
  key_links:
    - from: "apps/web/src/components/onboarding/import-wizard.tsx"
      to: "packages/api/src/routers/onboarding-import.ts"
      via: "trpc.onboardingImport.* queries and mutations"
      pattern: "trpc\\.onboardingImport\\."
    - from: "apps/web/src/components/onboarding/onboarding-checklist.tsx"
      to: "apps/web/src/app/[locale]/(dashboard)/onboarding/import/page.tsx"
      via: "ctaHref link to /onboarding/import"
      pattern: 'ctaHref.*onboarding/import'
    - from: "apps/web/src/components/onboarding/confirm-import-step.tsx"
      to: "packages/api/src/routers/onboarding-import.ts"
      via: "trpc.onboardingImport.startImport mutation + getProgress polling"
      pattern: "startImport|getProgress|refetchInterval"
---

<objective>
Build the complete wizard UI for the intelligent onboarding import flow: 4-step full-page wizard with source selection, people review with conflict resolution, project import with step editing, and async import with progress tracking.

Purpose: Give new organizations a guided flow to bootstrap their account from connected tools. Modify the existing onboarding checklist to route through this wizard instead of the plain members page.

Output: Full-page wizard at `/onboarding/import`, 8 new components, modified onboarding checklist, i18n strings for en/pl.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-intelligent-onboarding-wizard/34-CONTEXT.md
@.planning/phases/34-intelligent-onboarding-wizard/34-RESEARCH.md
@.planning/phases/34-intelligent-onboarding-wizard/34-UI-SPEC.md
@.planning/phases/34-intelligent-onboarding-wizard/34-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 that the wizard UI consumes -->

From packages/api/src/routers/onboarding-import.ts (created in Plan 01):
```typescript
// tRPC endpoints available:
// trpc.onboardingImport.listSources.useQuery() -> { provider, connected, selected }[]
// trpc.onboardingImport.fetchPeople.useQuery({ sources }) -> MergedPerson[]
// trpc.onboardingImport.fetchProjects.useQuery({ sources }) -> ImportedProject[]
// trpc.onboardingImport.startImport.useMutation({ people, projects }) -> { jobId }
// trpc.onboardingImport.getProgress.useQuery({ jobId }) -> ImportProgress
// trpc.onboardingImport.retryFailedItem.useMutation({ jobId, email }) -> void
```

From packages/validators/src/onboarding-import.ts (created in Plan 01):
```typescript
// MergedPerson shape:
// { email, name, sources: [{ source, name, avatarUrl?, metadata? }], status: "new"|"conflict"|"exists", conflicts?: [{ field, values: [{ source, value }], resolved? }] }

// ImportedProject shape:
// { sourceProvider, externalId, name, statuses: [{ id, name, color? }] }

// ImportProgress shape:
// { jobId, status: "pending"|"processing"|"completed"|"failed", totalItems, completedItems, failedItems: [{ email, error }] }
```

From apps/web/src/components/onboarding/onboarding-checklist.tsx:
```typescript
const ONBOARDING_STEPS: OnboardingStep[] = [
  { id: "org-details", icon: Building2, optional: false, stepKey: "orgDetails", ctaHref: "/settings" },
  { id: "invite-team", icon: UserPlus, optional: false, stepKey: "inviteTeam", ctaHref: "/settings?tab=members" }, // CHANGE to "/onboarding/import"
  // ...
];
```

From apps/web/src/components/integrations/google-workspace/directory-import-wizard.tsx:
```typescript
// Reference pattern: useState<number> for step navigation
// Sticky footer with Back/Continue buttons
// Step indicator at top
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wizard page route + all 4 step components + shared components</name>
  <files>
    apps/web/src/app/[locale]/(dashboard)/onboarding/import/page.tsx,
    apps/web/src/components/onboarding/import-wizard.tsx,
    apps/web/src/components/onboarding/source-selection-step.tsx,
    apps/web/src/components/onboarding/source-card.tsx,
    apps/web/src/components/onboarding/people-review-step.tsx,
    apps/web/src/components/onboarding/conflict-resolution-popover.tsx,
    apps/web/src/components/onboarding/project-import-step.tsx,
    apps/web/src/components/onboarding/confirm-import-step.tsx,
    apps/web/src/components/onboarding/import-progress-tracker.tsx
  </files>
  <read_first>
    apps/web/src/components/integrations/google-workspace/directory-import-wizard.tsx,
    apps/web/src/components/integrations/google-workspace/directory-preview-table.tsx,
    apps/web/src/components/integrations/google-workspace/directory-summary-bar.tsx,
    apps/web/src/components/integrations/google-workspace/role-assignment-controls.tsx,
    apps/web/src/components/import/import-wizard-dialog.tsx,
    apps/web/src/components/onboarding/onboarding-checklist.tsx,
    apps/web/src/components/ui/card.tsx,
    apps/web/src/components/ui/progress.tsx,
    apps/web/src/components/ui/badge.tsx,
    apps/web/src/components/ui/table.tsx,
    apps/web/src/components/ui/tabs.tsx,
    apps/web/src/components/ui/checkbox.tsx,
    apps/web/src/components/ui/popover.tsx,
    apps/web/src/components/ui/switch.tsx,
    apps/web/src/components/ui/select.tsx,
    apps/web/src/components/ui/skeleton.tsx,
    apps/web/src/trpc/init.ts,
    .planning/phases/34-intelligent-onboarding-wizard/34-UI-SPEC.md,
    .planning/phases/34-intelligent-onboarding-wizard/34-01-SUMMARY.md
  </read_first>
  <action>
    **Create page route** `apps/web/src/app/[locale]/(dashboard)/onboarding/import/page.tsx`:
    - Server component that renders `<ImportWizard />` client component
    - No sidebar layout -- full page centered content with `max-w-[960px] mx-auto`
    - Page title "Import Your Team" using Display typography (Bricolage Grotesque, 28px, semibold)

    **Create `import-wizard.tsx`** (main container):
    - `"use client"` with `useState<1|2|3|4>(1)` for step tracking
    - Horizontal step indicator at top: 4 numbered circles with labels ("Select Sources", "Review People", "Import Projects", "Confirm & Import"). Active step uses `var(--primary)` color, completed steps show checkmark. Use `aria-current="step"` on active, `role="navigation"` on container.
    - `<Progress>` bar below steps showing 25/50/75/100%
    - Renders step component conditionally based on current step
    - Sticky footer with: ghost "Back" button (left, hidden on step 1), primary "Continue" button (right, text changes to "Start Import" on step 4)
    - State management: `selectedSources: string[]`, `mergedPeople: MergedPerson[]`, `selectedPeople: Map<string, { role: string; skip: boolean }>`, `projects: ImportedProject[]`, `selectedProjects: Map<string, { skip: boolean; steps: { name: string; sortOrder: number }[] }>`, `jobId: string | null`
    - Continue button disabled when: step 1 has no sources selected, step 2 has unresolved conflicts, step 4 import is in progress

    **Create `source-card.tsx`**:
    - Props: `provider: string, name: string, connected: boolean, selected: boolean, onToggle, onConnect`
    - Card component with: brand icon (SiJira from react-icons/si, SiLinear, SiGoogleworkspace -- note: check exact icon name, SiSlack), provider name (Label 14px), connection status text
    - Connected: show `<Switch>` toggle for "Import from {tool}" (D-05)
    - Not connected: show "Connect" Button triggering `onConnect` callback
    - Selected state: `ring-2 ring-primary` + `bg-teal-50/50` (from UI-SPEC)
    - `role="checkbox"` with `aria-checked={selected}`, keyboard toggle Space/Enter
    - Hover: subtle elevation lift

    **Create `source-selection-step.tsx`** (Step 1):
    - Heading: "Where do you manage your team?" (Heading 20px semibold Bricolage)
    - Subtitle: "Select the tools you want to import from. You can connect new tools right here."
    - Calls `trpc.onboardingImport.listSources.useQuery()` for connection status
    - 2x2 grid of `<SourceCard>` components (responsive: single column on mobile per UI-SPEC)
    - Connect button handler: `window.open()` centered OAuth popup using existing OAuth URL patterns from integration settings. Listen for popup close/postMessage, then invalidate listSources query.
    - "Skip -- I'll invite people manually" text link at bottom per D-04. On click: mark onboarding step as complete via Settings mutation, redirect to `/settings?tab=members` using `useRouter().push()`.
    - Loading: 4 Skeleton cards in 2x2 grid

    **Create `people-review-step.tsx`** (Step 2):
    - Heading: "Review team members"
    - Subtitle: "We found people across your connected tools. Review and confirm who to import."
    - Calls `trpc.onboardingImport.fetchPeople.useQuery({ sources: selectedSources })` (enabled only when step 2 is active)
    - **Summary bar** at top: counts for New (green), Conflicts (amber), Existing (blue), Total. Reuse pattern from `directory-summary-bar.tsx`.
    - **Filter tabs** (D-11): All | New | Conflicts | Existing using `<Tabs>` component. Filter `mergedPeople` by status.
    - **Preview table** using `<Table>`:
      - Columns: Checkbox | Name | Email | Sources | Status | Role | Action
      - Checkbox: `<Checkbox>` with select-all in header. Use `aria-label="Select {name}"` per row.
      - Sources column: row of small `<Badge variant="secondary">` with provider icon + abbreviated name (e.g., "Jira", "Slack")
      - Status column: `<Badge>` -- green variant for "New", amber for "Conflict" (clickable, opens conflict popover), blue for "Exists"
      - Role column: `<Select>` dropdown with 8 roles from Better Auth. Default "MEMBER" for new, show current role for existing.
      - Action column: "Skip" ghost button per row
      - Existing members: pre-set skip=true, `opacity-50` + line-through on name (D-10)
    - **Batch toolbar** (D-10): appears when 1+ rows checked. Contains: "Import Selected" button, "Skip Selected" button, "Assign Role" `<Select>` dropdown. Uses `aria-live="polite"` for selection count.
    - **Empty state**: illustration/icon + "No team members found" heading + "The selected sources didn't return any team members..." body text
    - Loading: Skeleton table with 5 rows

    **Create `conflict-resolution-popover.tsx`** (D-09):
    - Props: `conflicts: { field: string; values: { source: string; value: string }[]; resolved?: string }[], onResolve: (field: string, value: string) => void`
    - Trigger: clicking amber "Conflict" badge
    - Content: For each conflict field, show source values as radio options + "Custom" option with text input
    - Unresolved conflicts show warning icon. All must be resolved to enable Continue.
    - Focus trap, `aria-expanded`, Escape to dismiss (from UI-SPEC accessibility)
    - Tooltip on conflict badge: "Data mismatch -- click to resolve"

    **Create `project-import-step.tsx`** (Step 3):
    - Heading: "Import projects"
    - Subtitle: "These projects will become workflow templates with their statuses as steps."
    - Calls `trpc.onboardingImport.fetchProjects.useQuery({ sources: selectedSources.filter(s => s === "JIRA" || s === "LINEAR") })` (only PM tools)
    - One `<Card>` per project (D-12, D-13):
      - Project name as editable text input + source icon badge
      - Status steps listed as reorderable tags/chips (drag or arrow buttons for reorder, X to remove, + to add)
      - "Edit steps" expands inline editor for add/remove/rename/reorder
      - "Skip this project" text button to exclude
    - Note at bottom: "Bidirectional sync will be configured automatically based on imported statuses." (D-14)
    - Empty state: "No projects found" + "No projects were found in the selected PM tools. You can create workflow templates manually later."
    - Loading: 3 Skeleton cards

    **Create `confirm-import-step.tsx`** (Step 4):
    - Heading: "Ready to import"
    - Subtitle: "Review your selections and start the import. This may take a moment."
    - **Summary cards** side by side: "People to import" (count + role breakdown list) and "Projects to create" (count + total steps)
    - Before import: show summary + "Start Import" primary button
    - After clicking Start Import: call `trpc.onboardingImport.startImport.useMutation()` with people and projects data. On success, set jobId and switch to progress view.
    - **Progress view**: render `<ImportProgressTracker jobId={jobId} />`

    **Create `import-progress-tracker.tsx`** (ONBD-05):
    - Props: `jobId: string`
    - Calls `trpc.onboardingImport.getProgress.useQuery({ jobId }, { refetchInterval: (query) => query.state.data?.status === "completed" || query.state.data?.status === "failed" ? false : 2000 })`
    - Full-width `<Progress>` bar with `role="progressbar"`, `aria-valuenow={completedItems}`, `aria-valuemin={0}`, `aria-valuemax={totalItems}`
    - Label: "Importing {current} of {total}..."
    - Item list below: completed items (green CheckCircle icon), in-progress (Loader2 spinning), failed items (red XCircle icon + error text + "Retry" ghost button)
    - Retry button: calls `trpc.onboardingImport.retryFailedItem.useMutation({ jobId, email })`. Show inline spinner while retrying.
    - Completion state: Success card with "Import complete" heading, "{imported} people and {projects} projects imported successfully." body, "Go to Dashboard" primary CTA button linking to `/dashboard`

    **Responsive behavior** (from UI-SPEC):
    - >= 1024px: centered max-w-[960px], 2x2 source grid, full table
    - 768-1023px: full-width px-6, 2x2 grid, hide Sources column
    - < 768px: full-width px-4, single-column cards, table collapses to stacked card view

    **All components use `useTranslations("OnboardingImport")` for i18n strings.**
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/[locale]/(dashboard)/onboarding/import/page.tsx exists and contains `ImportWizard`
    - apps/web/src/components/onboarding/import-wizard.tsx contains `useState` and `"use client"`
    - apps/web/src/components/onboarding/import-wizard.tsx contains `step` state with values 1-4
    - apps/web/src/components/onboarding/import-wizard.tsx contains `aria-current`
    - apps/web/src/components/onboarding/source-selection-step.tsx contains `trpc.onboardingImport.listSources`
    - apps/web/src/components/onboarding/source-selection-step.tsx contains `window.open`
    - apps/web/src/components/onboarding/source-card.tsx contains `role="checkbox"`
    - apps/web/src/components/onboarding/source-card.tsx contains `ring-2 ring-primary`
    - apps/web/src/components/onboarding/people-review-step.tsx contains `trpc.onboardingImport.fetchPeople`
    - apps/web/src/components/onboarding/people-review-step.tsx contains `select-all` or `selectAll`
    - apps/web/src/components/onboarding/people-review-step.tsx contains `opacity-50`
    - apps/web/src/components/onboarding/conflict-resolution-popover.tsx contains `aria-expanded`
    - apps/web/src/components/onboarding/conflict-resolution-popover.tsx contains `onResolve`
    - apps/web/src/components/onboarding/project-import-step.tsx contains `trpc.onboardingImport.fetchProjects`
    - apps/web/src/components/onboarding/project-import-step.tsx contains `Skip this project`
    - apps/web/src/components/onboarding/confirm-import-step.tsx contains `trpc.onboardingImport.startImport`
    - apps/web/src/components/onboarding/import-progress-tracker.tsx contains `refetchInterval`
    - apps/web/src/components/onboarding/import-progress-tracker.tsx contains `retryFailedItem`
    - apps/web/src/components/onboarding/import-progress-tracker.tsx contains `role="progressbar"`
    - TypeScript compilation passes: `npx tsc --noEmit -p apps/web/tsconfig.json` exits 0
  </acceptance_criteria>
  <done>All 9 component files created. Wizard renders 4 steps with full interactivity: source selection with OAuth popup, merged people preview with conflict resolution popover and batch actions, project import with editable steps, and async import with progress tracking and per-item retry. All accessibility requirements met (ARIA roles, focus trap, keyboard navigation). Responsive layout matches UI-SPEC breakpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Onboarding checklist modification + Settings entry point + i18n</name>
  <files>
    apps/web/src/components/onboarding/onboarding-checklist.tsx,
    apps/web/messages/en.json,
    apps/web/messages/pl.json
  </files>
  <read_first>
    apps/web/src/components/onboarding/onboarding-checklist.tsx,
    apps/web/messages/en.json,
    apps/web/messages/pl.json,
    .planning/phases/34-intelligent-onboarding-wizard/34-UI-SPEC.md
  </read_first>
  <action>
    **Modify `onboarding-checklist.tsx`** (per D-01):
    - Change step 2 `ctaHref` from `"/settings?tab=members"` to `"/onboarding/import"`
    - This is the ONLY change needed in this file. The step id, icon, stepKey all remain the same.

    **Add i18n strings to `en.json`** under new `"OnboardingImport"` key:
    ```json
    "OnboardingImport": {
      "pageTitle": "Import Your Team",
      "pageSubtitle": "Pull in team members and projects from your connected tools. You can always add more later.",
      "step1": {
        "heading": "Where do you manage your team?",
        "subtitle": "Select the tools you want to import from. You can connect new tools right here.",
        "skipLink": "Skip -- I'll invite people manually"
      },
      "step2": {
        "heading": "Review team members",
        "subtitle": "We found people across your connected tools. Review and confirm who to import.",
        "summaryNew": "New",
        "summaryConflicts": "Conflicts",
        "summaryExisting": "Existing",
        "summaryTotal": "Total",
        "filterAll": "All",
        "filterNew": "New",
        "filterConflicts": "Conflicts",
        "filterExisting": "Existing",
        "columnName": "Name",
        "columnEmail": "Email",
        "columnSources": "Sources",
        "columnStatus": "Status",
        "columnRole": "Role",
        "columnAction": "Action",
        "skipRow": "Skip",
        "batchImport": "Import Selected",
        "batchSkip": "Skip Selected",
        "batchRole": "Assign Role",
        "emptyHeading": "No team members found",
        "emptyBody": "The selected sources didn't return any team members. Try connecting a different tool or invite people manually.",
        "conflictTooltip": "Data mismatch -- click to resolve",
        "conflictCustom": "Custom value"
      },
      "step3": {
        "heading": "Import projects",
        "subtitle": "These projects will become workflow templates with their statuses as steps.",
        "editSteps": "Edit steps",
        "skipProject": "Skip this project",
        "syncNote": "Bidirectional sync will be configured automatically based on imported statuses.",
        "addStep": "Add step",
        "emptyHeading": "No projects found",
        "emptyBody": "No projects were found in the selected PM tools. You can create workflow templates manually later."
      },
      "step4": {
        "heading": "Ready to import",
        "subtitle": "Review your selections and start the import. This may take a moment.",
        "peopleCard": "People to import",
        "projectsCard": "Projects to create",
        "startImport": "Start Import",
        "progressLabel": "Importing {current} of {total}...",
        "completeHeading": "Import complete",
        "completeBody": "{imported} people and {projects} projects imported successfully.",
        "completeCta": "Go to Dashboard",
        "retryButton": "Retry",
        "errorPrefix": "Failed to import {name}. Click Retry to try again, or skip this person."
      },
      "nav": {
        "back": "Back",
        "continue": "Continue",
        "step1Label": "Select Sources",
        "step2Label": "Review People",
        "step3Label": "Import Projects",
        "step4Label": "Confirm & Import"
      },
      "sourceCard": {
        "connected": "Connected",
        "notConnected": "Not connected",
        "connect": "Connect",
        "importToggle": "Import from {tool}"
      },
      "oauthError": "Could not connect to {tool}. Check your permissions and try again."
    }
    ```

    **Add i18n strings to `pl.json`** under new `"OnboardingImport"` key with Polish translations:
    ```json
    "OnboardingImport": {
      "pageTitle": "Importuj swoj zespol",
      "pageSubtitle": "Pobierz czlonkow zespolu i projekty z polaczonych narzedzi. Zawsze mozesz dodac wiecej pozniej.",
      "step1": {
        "heading": "Gdzie zarzadzasz swoim zespolem?",
        "subtitle": "Wybierz narzedzia, z ktorych chcesz importowac. Mozesz polaczyc nowe narzedzia tutaj.",
        "skipLink": "Pomin -- zaprosze osoby recznie"
      },
      "step2": {
        "heading": "Sprawdz czlonkow zespolu",
        "subtitle": "Znalezlismy osoby w polaczonych narzedziach. Sprawdz i potwierdz kogo zaimportowac.",
        "summaryNew": "Nowe",
        "summaryConflicts": "Konflikty",
        "summaryExisting": "Istniejace",
        "summaryTotal": "Lacznie",
        "filterAll": "Wszystkie",
        "filterNew": "Nowe",
        "filterConflicts": "Konflikty",
        "filterExisting": "Istniejace",
        "columnName": "Imie",
        "columnEmail": "Email",
        "columnSources": "Zrodla",
        "columnStatus": "Status",
        "columnRole": "Rola",
        "columnAction": "Akcja",
        "skipRow": "Pomin",
        "batchImport": "Importuj zaznaczone",
        "batchSkip": "Pomin zaznaczone",
        "batchRole": "Przypisz role",
        "emptyHeading": "Nie znaleziono czlonkow zespolu",
        "emptyBody": "Wybrane zrodla nie zwrocily zadnych czlonkow zespolu. Sprobuj polaczyc inne narzedzie lub zapros osoby recznie.",
        "conflictTooltip": "Niezgodnosc danych -- kliknij aby rozwiazac",
        "conflictCustom": "Wartosc niestandardowa"
      },
      "step3": {
        "heading": "Importuj projekty",
        "subtitle": "Te projekty stana sie szablonami workflow z ich statusami jako krokami.",
        "editSteps": "Edytuj kroki",
        "skipProject": "Pomin ten projekt",
        "syncNote": "Dwukierunkowa synchronizacja zostanie skonfigurowana automatycznie na podstawie importowanych statusow.",
        "addStep": "Dodaj krok",
        "emptyHeading": "Nie znaleziono projektow",
        "emptyBody": "Nie znaleziono projektow w wybranych narzedziach PM. Mozesz pozniej recznie utworzyc szablony workflow."
      },
      "step4": {
        "heading": "Gotowy do importu",
        "subtitle": "Sprawdz swoje wybory i rozpocznij import. To moze chwile potrwac.",
        "peopleCard": "Osoby do zaimportowania",
        "projectsCard": "Projekty do utworzenia",
        "startImport": "Rozpocznij import",
        "progressLabel": "Importowanie {current} z {total}...",
        "completeHeading": "Import zakonczony",
        "completeBody": "Zaimportowano {imported} osob i {projects} projektow.",
        "completeCta": "Przejdz do pulpitu",
        "retryButton": "Ponow",
        "errorPrefix": "Nie udalo sie zaimportowac {name}. Kliknij Ponow aby sprobowac ponownie, lub pomin te osobe."
      },
      "nav": {
        "back": "Wstecz",
        "continue": "Kontynuuj",
        "step1Label": "Wybierz zrodla",
        "step2Label": "Sprawdz osoby",
        "step3Label": "Importuj projekty",
        "step4Label": "Potwierdz i importuj"
      },
      "sourceCard": {
        "connected": "Polaczono",
        "notConnected": "Nie polaczono",
        "connect": "Polacz",
        "importToggle": "Importuj z {tool}"
      },
      "oauthError": "Nie udalo sie polaczyc z {tool}. Sprawdz uprawnienia i sprobuj ponownie."
    }
    ```

    **Settings entry point** (D-03): The wizard page at `/onboarding/import` is already accessible directly via URL. For the Settings > Integrations link, add a "Re-import from tools" link/button to the integrations settings page. Check if there is an existing integrations settings component that lists connected providers. If so, add a link to `/onboarding/import` at the top of the provider list. If the integrations page is complex, simply ensure the `/onboarding/import` route works when navigated to directly (it should, as a standalone page route). The wizard's `listSources` query will correctly show current connection statuses regardless of entry point.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && grep -c "onboarding/import" apps/web/src/components/onboarding/onboarding-checklist.tsx && grep -c "OnboardingImport" apps/web/messages/en.json && grep -c "OnboardingImport" apps/web/messages/pl.json</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/onboarding/onboarding-checklist.tsx contains `ctaHref: "/onboarding/import"` for the invite-team step
    - apps/web/src/components/onboarding/onboarding-checklist.tsx does NOT contain `ctaHref: "/settings?tab=members"` for the invite-team step
    - apps/web/messages/en.json contains `"OnboardingImport"` key
    - apps/web/messages/en.json contains `"Where do you manage your team?"`
    - apps/web/messages/en.json contains `"Import Your Team"`
    - apps/web/messages/en.json contains `"Skip -- I'll invite people manually"`
    - apps/web/messages/en.json contains `"Start Import"`
    - apps/web/messages/en.json contains `"Go to Dashboard"`
    - apps/web/messages/pl.json contains `"OnboardingImport"` key
    - apps/web/messages/pl.json contains `"Importuj swoj zespol"` or Polish equivalent
  </acceptance_criteria>
  <done>Onboarding checklist step 2 routes to /onboarding/import instead of /settings?tab=members. All wizard copy available in both English and Polish via OnboardingImport i18n namespace. Wizard accessible from Settings > Integrations for re-importing (D-03).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete onboarding wizard</name>
  <files>apps/web/src/app/[locale]/(dashboard)/onboarding/import/page.tsx</files>
  <action>Human verifies the complete wizard flow visually and interactively.</action>
  <what-built>Complete intelligent onboarding wizard: 4-step full-page wizard at /onboarding/import with source selection (OAuth popup), merged people preview (conflict resolution, batch actions, filters), project import (editable workflow steps), and async import with progress tracking and per-item retry. Onboarding checklist step 2 now links to the wizard. i18n for EN and PL.</what-built>
  <how-to-verify>
    1. Navigate to the dashboard. Check the onboarding checklist -- step 2 ("Invite Team") should link to /onboarding/import
    2. Click step 2 CTA or navigate directly to /onboarding/import
    3. Step 1: Verify 4 source cards (Jira, Linear, Google Workspace, Slack) with connection status
    4. Test "Connect" button on an unconnected tool -- should open OAuth popup
    5. Toggle an import switch on a connected tool, click Continue
    6. Step 2: Verify merged preview table with Name/Email/Sources/Status/Role columns
    7. Check filter tabs (All/New/Conflicts/Existing)
    8. Test checkbox select-all and batch toolbar
    9. If conflicts exist, click a conflict badge -- should open resolution popover
    10. Step 3: Verify project cards with editable status steps
    11. Step 4: Verify summary cards and Start Import button
    12. Test the "Skip -- I'll invite people manually" link on step 1
    13. Verify responsive layout on mobile viewport
  </how-to-verify>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && ls apps/web/src/components/onboarding/import-wizard.tsx apps/web/src/components/onboarding/source-selection-step.tsx apps/web/src/components/onboarding/people-review-step.tsx apps/web/src/components/onboarding/project-import-step.tsx apps/web/src/components/onboarding/confirm-import-step.tsx</automated>
  </verify>
  <done>User has visually verified all 4 wizard steps work correctly with proper layout, interactivity, and responsiveness.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `grep "onboarding/import" apps/web/src/components/onboarding/onboarding-checklist.tsx` -- confirms D-01
2. `ls apps/web/src/app/*/\(dashboard\)/onboarding/import/page.tsx` -- confirms D-02 route exists
3. `grep "OnboardingImport" apps/web/messages/en.json` -- confirms i18n
4. TypeScript compiles: `npx tsc --noEmit -p apps/web/tsconfig.json`
5. Visual verification of all 4 wizard steps via human checkpoint
</verification>

<success_criteria>
- Full-page wizard at /onboarding/import with 4 steps (D-02)
- Source selection cards with inline OAuth for unconnected tools (D-05, D-06, ONBD-01)
- Merged person preview with source badges, status indicators, conflict resolution, batch actions, filters (D-07 through D-11, ONBD-02, ONBD-04)
- Project import cards with editable steps and sync note (D-12, D-13, D-14, ONBD-03)
- Async import with progress bar, per-item status, retry for failures (ONBD-05)
- Onboarding checklist step 2 links to wizard (D-01)
- Skip link works (D-04)
- Wizard accessible from Settings > Integrations (D-03)
- i18n strings for EN and PL
- Accessibility: ARIA roles, keyboard navigation, focus trap on popovers
- Responsive: 3 breakpoints per UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/34-intelligent-onboarding-wizard/34-02-SUMMARY.md`
</output>
