---
phase: 48-zatca-fatoorah-integration
plan: 04
type: execute
wave: 2
depends_on: [48-01, 48-02, 48-03]
files_modified:
  - packages/einvoice/src/profiles/zatca/index.ts
  - packages/einvoice/src/profiles/zatca/api-client.ts
  - packages/integrations/src/services/secret-store.ts
  - packages/integrations/src/services/infisical-client.ts
  - packages/api/src/routers/zatca.ts
  - packages/api/src/services/zatca-submission.ts
  - packages/api/src/services/zatca-hash-chain.ts
  - .env.example
autonomous: true
requirements: [ZATCA-04, ZATCA-05, ZATCA-07]

must_haves:
  truths:
    - "Hash chain enforces sequential processing per org via advisory lock"
    - "Each invoice references the hash of the previous invoice for that org"
    - "First invoice PIH is SHA-256 of literal string '0'"
    - "B2B invoices submitted to clearance endpoint, B2C to reporting"
    - "Submission status tracked per invoice"
    - "Certificates stored in Infisical per D-02"
  artifacts:
    - path: "packages/api/src/services/zatca-hash-chain.ts"
      provides: "Hash chain with advisory lock and ICV tracking"
      exports: ["acquireChainLock", "getNextChainEntry", "recordChainEntry"]
    - path: "packages/api/src/services/zatca-submission.ts"
      provides: "QStash async submission pipeline"
      exports: ["submitToZatca", "handleZatcaSubmissionJob"]
    - path: "packages/api/src/routers/zatca.ts"
      provides: "tRPC router for ZATCA operations"
      exports: ["zatcaRouter"]
    - path: "packages/integrations/src/services/infisical-client.ts"
      provides: "Infisical per-org certificate management"
      exports: ["InfisicalSecretStore", "createZatcaSecretStore"]
  key_links:
    - from: "packages/api/src/services/zatca-submission.ts"
      to: "packages/api/src/services/zatca-hash-chain.ts"
      via: "acquires chain lock before signing"
      pattern: "acquireChainLock"
    - from: "packages/api/src/services/zatca-submission.ts"
      to: "packages/einvoice/src/profiles/zatca/index.ts"
      via: "uses ZatcaProfile for generation, signing, QR"
      pattern: "ZatcaProfile"
---

<objective>
Build the ZATCA submission pipeline: hash chain management with advisory locks, Infisical secret store, QStash async submission, ZATCA API client, and tRPC router. Wire ZatcaProfile with signer and QR.

Purpose: Core orchestration tying together profile (01), signer (02), and QR (03). Enforces sequential hash chain per D-03, stores certs in Infisical per D-02, submits async via QStash per D-05.

Output: Hash chain service, submission orchestrator, API client, secret store, tRPC router, wired ZatcaProfile
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-zatca-fatoorah-integration/48-CONTEXT.md
@.planning/phases/48-zatca-fatoorah-integration/48-RESEARCH.md
@.planning/phases/48-zatca-fatoorah-integration/48-01-SUMMARY.md
@.planning/phases/48-zatca-fatoorah-integration/48-02-SUMMARY.md
@.planning/phases/48-zatca-fatoorah-integration/48-03-SUMMARY.md

<interfaces>
From Plan 01 - ZatcaProfile:
```typescript
export class ZatcaProfile implements EInvoiceProfile {
  readonly profileId = "zatca"; readonly country = "SA";
  sign?: Signable; qrCode?: QRCodeable;
  async generate(invoice: EInvoice): Promise<string>;
}
```
From Plan 02 - Signer:
```typescript
export class ZatcaXAdESSigner implements Signable { ... }
```
From Plan 03 - QR:
```typescript
export class ZatcaTLVQRCode implements QRCodeable { ... }
```
From ksef.ts (router pattern):
```typescript
export const ksefRouter = router({ ... })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Infisical secret store and hash chain service</name>
  <files>packages/integrations/src/services/secret-store.ts, packages/integrations/src/services/infisical-client.ts, packages/api/src/services/zatca-hash-chain.ts, .env.example</files>
  <read_first>
    - packages/integrations/src/services/credential-service.ts (reference)
    - packages/integrations/src/index.ts
    - packages/db/prisma/schema/zatca.prisma
    - .env.example
    - .planning/phases/48-zatca-fatoorah-integration/48-RESEARCH.md (sections 4, 6)
  </read_first>
  <behavior>
    - Test 1: SecretStore interface has getSecret, setSecret, deleteSecret, listSecrets
    - Test 2: InfisicalSecretStore implements SecretStore
    - Test 3: createZatcaSecretStore binds to /zatca/{orgId}
    - Test 4: getNextChainEntry returns icv=1, PIH=SHA-256("0") for first invoice
    - Test 5: getNextChainEntry returns icv=N+1, PIH=last hash for subsequent
    - Test 6: acquireChainLock uses pg_advisory_xact_lock
  </behavior>
  <action>
Create `packages/integrations/src/services/secret-store.ts`:
- `SecretStore` interface: getSecret, setSecret, deleteSecret, listSecrets
- `SecretStoreError` extends Error with operation field

Create `packages/integrations/src/services/infisical-client.ts`:
- Install: `pnpm --filter @contractor-ops/integrations add @infisical/sdk`
- `InfisicalSecretStore` implements SecretStore
- Constructor: { siteUrl?, clientId, clientSecret, projectId, environment }
- Lazy-init SDK. All ops: try/catch, log path not values, rethrow as SecretStoreError
- `createZatcaSecretStore(organizationId)`: factory bound to `/zatca/${organizationId}`
- Secret names: X509_CERTIFICATE, PRIVATE_KEY, API_SECRET, COMPLIANCE_REQUEST_ID
- Export from integrations index.ts

Update `.env.example`:
```
# ZATCA Certificate Storage (Infisical)
INFISICAL_SITE_URL=https://app.infisical.com
INFISICAL_CLIENT_ID=
INFISICAL_CLIENT_SECRET=
INFISICAL_PROJECT_ID=
INFISICAL_ENVIRONMENT=production
```

Create `packages/api/src/services/zatca-hash-chain.ts`:
- `acquireChainLock(prisma, organizationId)`: `prisma.$executeRawUnsafe("SELECT pg_advisory_xact_lock(hashtext($1))", organizationId)`
- `getNextChainEntry(prisma, organizationId)`: query max ICV. First: icv=1, pih=SHA-256("0"). Subsequent: icv=last+1, pih=lastHash
- `recordChainEntry(prisma, data)`: create ZatcaInvoiceChain with PENDING status
  </action>
  <acceptance_criteria>
    - `secret-store.ts` contains `export interface SecretStore`
    - `infisical-client.ts` contains `class InfisicalSecretStore implements SecretStore`
    - `infisical-client.ts` contains `createZatcaSecretStore`
    - `zatca-hash-chain.ts` contains `acquireChainLock`
    - `zatca-hash-chain.ts` contains `pg_advisory_xact_lock`
    - `zatca-hash-chain.ts` contains `createHash('sha256').update('0')`
    - `.env.example` contains `INFISICAL_CLIENT_ID=`
    - `pnpm --filter @contractor-ops/integrations exec tsc --noEmit` exits 0
    - `pnpm --filter @contractor-ops/api exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/integrations exec tsc --noEmit && pnpm --filter @contractor-ops/api exec tsc --noEmit</automated>
  </verify>
  <done>Secret store and hash chain services created</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ZATCA API client, submission service, tRPC router, and profile wiring</name>
  <files>packages/einvoice/src/profiles/zatca/api-client.ts, packages/api/src/services/zatca-submission.ts, packages/api/src/routers/zatca.ts, packages/einvoice/src/profiles/zatca/index.ts</files>
  <read_first>
    - packages/api/src/routers/ksef.ts (reference router pattern)
    - packages/api/src/routers/einvoice.ts
    - packages/einvoice/src/profiles/ksef/api-client.ts
    - packages/einvoice/src/profiles/zatca/index.ts
    - packages/einvoice/src/profiles/zatca/signer.ts
    - packages/einvoice/src/profiles/zatca/qr-code.ts
    - .planning/phases/48-zatca-fatoorah-integration/48-RESEARCH.md (sections 7, 8)
  </read_first>
  <behavior>
    - Test 1: ZatcaApiClient.submitForClearance POSTs to /invoices/clearance
    - Test 2: ZatcaApiClient.submitForReporting POSTs to /invoices/reporting
    - Test 3: Auth uses Base64 {token}:{secret}
    - Test 4: submitToZatca orchestrates full pipeline
    - Test 5: zatcaRouter has getStatus, getInvoiceChain, resubmit, getComplianceStats
    - Test 6: ZatcaProfile.sign wired to ZatcaXAdESSigner
    - Test 7: ZatcaProfile.qrCode wired to ZatcaTLVQRCode
  </behavior>
  <action>
Create `packages/einvoice/src/profiles/zatca/api-client.ts`:
- `ZatcaApiClient` class
- Constructor: { baseUrl, binarySecurityToken, secret }
- Sandbox URL: "https://gw-fatoora.zatca.gov.sa/e-invoicing/developer-portal"
- Production URL: "https://gw-fatoora.zatca.gov.sa/e-invoicing/core"
- Auth: `Authorization: Basic ${Buffer.from(token + ':' + secret).toString('base64')}`
- Methods: submitForClearance, submitForReporting, requestComplianceCsid, requestProductionCsid, submitComplianceInvoice
- Response types: ZatcaApiResponse { status, validationResults }, ZatcaCsidResponse { binarySecurityToken, secret, requestID }
- Error classification: retryable (429, 5xx) vs non-retryable (4xx)

Create `packages/api/src/services/zatca-submission.ts`:
- `submitToZatca(invoiceId, organizationId)`: full pipeline per D-05:
  1. Prisma transaction with advisory lock
  2. getNextChainEntry -> ICV + PIH
  3. Build EInvoice with ZATCA extensions
  4. generate -> sign -> hash -> QR -> record
  5. Commit transaction
  6. Submit to ZATCA (clearance for standard, reporting for simplified)
  7. Update chain entry with response
- `handleZatcaSubmissionJob(payload)`: QStash handler, error classification
- Configure QStash: 3 retries, exponential backoff (1s, 4s, 16s)

Wire ZatcaProfile in `packages/einvoice/src/profiles/zatca/index.ts`:
- `this.sign = new ZatcaXAdESSigner()`
- `this.qrCode = new ZatcaTLVQRCode()`
- Remove TODO placeholders

Create `packages/api/src/routers/zatca.ts`:
- `zatcaRouter` with procedures: getStatus, getInvoiceChain, getInvoiceSubmission, resubmit, getComplianceStats
- Auth middleware: org membership check (pattern from ksef.ts)
- Register in main app router
  </action>
  <acceptance_criteria>
    - `api-client.ts` contains `class ZatcaApiClient`
    - `api-client.ts` contains `submitForClearance` and `submitForReporting`
    - `zatca-submission.ts` contains `submitToZatca`
    - `zatca-submission.ts` contains `acquireChainLock`
    - `zatca.ts` router contains `zatcaRouter`
    - ZatcaProfile index.ts contains `new ZatcaXAdESSigner()`
    - ZatcaProfile index.ts contains `new ZatcaTLVQRCode()`
    - `pnpm --filter @contractor-ops/api exec tsc --noEmit` exits 0
    - `pnpm --filter @contractor-ops/einvoice exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/api exec tsc --noEmit && pnpm --filter @contractor-ops/einvoice exec tsc --noEmit</automated>
  </verify>
  <done>Full submission pipeline, API client, router, and profile wiring complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App -> ZATCA API | Signed XML and credentials to external gov API |
| App -> Infisical | Private keys to/from external secret manager |
| Client -> tRPC | User input to DB queries |
| QStash -> handler | Job payloads from external queue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-09 | Information Disclosure | api-client.ts | mitigate | Never log Auth header. Strip auth from error responses before storing. |
| T-48-10 | Tampering | hash-chain.ts | mitigate | Advisory lock + @@unique([orgId, icv]) prevent concurrent/duplicate entries. |
| T-48-11 | Spoofing | zatca.ts router | mitigate | Auth middleware requires org membership on all procedures. |
| T-48-12 | Elevation of Privilege | zatca.ts router | mitigate | resubmit checks invoice belongs to caller's org. |
| T-48-13 | Denial of Service | submission.ts | mitigate | QStash: 3 retries, exponential backoff, dead letter queue. |
| T-48-14 | Information Disclosure | infisical-client.ts | mitigate | Never log values. Path /zatca/{orgId} isolates orgs. |
</threat_model>

<verification>
1. All three packages compile: api, einvoice, integrations
2. ZatcaProfile.sign and .qrCode are no longer undefined
3. submitToZatca follows the 10-step pipeline
4. zatcaRouter registered in main app router
5. .env.example has Infisical vars
</verification>

<success_criteria>
- Hash chain with advisory lock, sequential ICV, first PIH = SHA-256("0")
- Full submission pipeline: lock -> chain -> generate -> sign -> hash -> QR -> record -> submit
- B2B -> clearance, B2C -> reporting
- Certs in Infisical, tRPC router exposes all ZATCA operations
- ZatcaProfile fully wired
</success_criteria>

<output>
After completion, create `.planning/phases/48-zatca-fatoorah-integration/48-04-SUMMARY.md`
</output>
