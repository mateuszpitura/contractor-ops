---
phase: 48-zatca-fatoorah-integration
plan: 06
type: execute
wave: 4
depends_on: [05]
files_modified:
  - apps/web/src/components/zatca/zatca-onboarding-wizard.tsx
  - apps/web/src/components/zatca/zatca-step-indicator.tsx
  - apps/web/src/components/zatca/zatca-tax-details-form.tsx
  - apps/web/src/components/zatca/zatca-csr-step.tsx
  - apps/web/src/components/zatca/zatca-compliance-csid-step.tsx
  - apps/web/src/components/zatca/zatca-compliance-checks-step.tsx
  - apps/web/src/components/zatca/zatca-production-step.tsx
  - apps/web/src/components/zatca/zatca-status-badge.tsx
  - apps/web/src/components/zatca/zatca-submission-panel.tsx
  - apps/web/src/components/zatca/zatca-compliance-widget.tsx
  - apps/web/src/components/zatca/zatca-environment-toggle.tsx
  - apps/web/src/components/settings/integrations-tab.tsx
  - apps/web/src/components/invoices/invoice-side-panel.tsx
autonomous: false
requirements: [ZATCA-06]

user_setup:
  - service: infisical
    why: "ZATCA certificate storage"
    env_vars:
      - name: INFISICAL_CLIENT_ID
        source: "Infisical Dashboard -> Access Control -> Machine Identities"
      - name: INFISICAL_CLIENT_SECRET
        source: "Infisical Dashboard -> Access Control -> Machine Identities"
      - name: INFISICAL_PROJECT_ID
        source: "Infisical Dashboard -> Project Settings"

must_haves:
  truths:
    - "User can complete ZATCA onboarding through a 5-step wizard in Settings > Integrations"
    - "Each wizard step validates input before allowing Next"
    - "ZATCA submission status badge shows on invoice detail view for Saudi org invoices"
    - "ZATCA compliance widget shows connection status, certificate expiry, and invoice counts"
    - "Wizard state persists between steps so user can leave and resume"
  artifacts:
    - path: "apps/web/src/components/zatca/zatca-onboarding-wizard.tsx"
      provides: "Multi-step onboarding wizard component"
      exports: ["ZatcaOnboardingWizard"]
    - path: "apps/web/src/components/zatca/zatca-status-badge.tsx"
      provides: "ZATCA submission status badge for invoice views"
      exports: ["ZatcaStatusBadge"]
    - path: "apps/web/src/components/zatca/zatca-submission-panel.tsx"
      provides: "Collapsible ZATCA submission detail panel for invoice views"
      exports: ["ZatcaSubmissionPanel"]
    - path: "apps/web/src/components/zatca/zatca-compliance-widget.tsx"
      provides: "ZATCA compliance card for integrations settings page"
      exports: ["ZatcaComplianceWidget"]
  key_links:
    - from: "apps/web/src/components/zatca/zatca-onboarding-wizard.tsx"
      to: "packages/api/src/routers/zatca.ts"
      via: "tRPC mutations for each onboarding step"
      pattern: "api.zatca"
    - from: "apps/web/src/components/settings/integrations-tab.tsx"
      to: "apps/web/src/components/zatca/zatca-onboarding-wizard.tsx"
      via: "ZATCA card triggers wizard"
      pattern: "ZatcaOnboardingWizard"
    - from: "apps/web/src/components/invoices/invoice-side-panel.tsx"
      to: "apps/web/src/components/zatca/zatca-status-badge.tsx"
      via: "Badge rendered in invoice header"
      pattern: "ZatcaStatusBadge"
---

<objective>
Build the ZATCA frontend UI: onboarding wizard, invoice status badges, submission detail panel, and compliance widget per the UI-SPEC.md design contract.

Purpose: Provides the user-facing interface for ZATCA integration — a 5-step onboarding wizard (per D-01), real-time invoice submission status (per D-06), and a compliance dashboard widget. All components follow the UI-SPEC design contract for visual consistency.

Output: Complete ZATCA UI components integrated into settings and invoice views.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-zatca-fatoorah-integration/48-CONTEXT.md
@.planning/phases/48-zatca-fatoorah-integration/48-UI-SPEC.md
@.planning/phases/48-zatca-fatoorah-integration/48-05-SUMMARY.md

<interfaces>
From packages/api/src/routers/zatca.ts (Plan 05):
```typescript
// tRPC endpoints:
// zatca.getStatus -> { connected, config, ... }
// zatca.saveTaxDetails -> mutation(zatcaTaxDetailsSchema)
// zatca.generateCsr -> mutation -> { csrPreview }
// zatca.requestComplianceCsid -> mutation({ otp }) -> { received }
// zatca.runComplianceChecks -> mutation -> [{ type, status, result }]
// zatca.activateProduction -> mutation -> { activated }
// zatca.getSubmissionStatus -> query({ invoiceId }) -> { status, uuid, icv, ... }
// zatca.resubmit -> mutation({ invoiceId }) -> { scheduled }
// zatca.disconnect -> mutation -> { disconnected }
```

From apps/web/src/components/invoices/ksef-badge.tsx (reference pattern):
```typescript
// KSeF badge component — reference for ZATCA status badge structure
export function KsefBadge({ status }: { status: string }) { ... }
```

From apps/web/src/components/invoices/ksef-metadata-section.tsx (reference pattern):
```typescript
// KSeF metadata section — reference for ZATCA submission detail panel
```

From apps/web/src/components/settings/integrations-tab.tsx:
// Existing integrations page — ZATCA card added here
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ZATCA onboarding wizard with 5-step form flow</name>
  <files>apps/web/src/components/zatca/zatca-onboarding-wizard.tsx, apps/web/src/components/zatca/zatca-step-indicator.tsx, apps/web/src/components/zatca/zatca-tax-details-form.tsx, apps/web/src/components/zatca/zatca-csr-step.tsx, apps/web/src/components/zatca/zatca-compliance-csid-step.tsx, apps/web/src/components/zatca/zatca-compliance-checks-step.tsx, apps/web/src/components/zatca/zatca-production-step.tsx</files>
  <read_first>
    - .planning/phases/48-zatca-fatoorah-integration/48-UI-SPEC.md (full file — design contract)
    - apps/web/src/components/settings/integrations-tab.tsx (where wizard gets triggered)
    - apps/web/src/components/invoices/ksef-badge.tsx (reference badge component)
    - apps/web/src/components/invoices/ksef-metadata-section.tsx (reference metadata section)
    - apps/web/src/components/ui/card.tsx (shadcn Card)
    - apps/web/src/components/ui/button.tsx (shadcn Button)
    - apps/web/src/components/ui/input.tsx (shadcn Input)
    - apps/web/src/components/ui/badge.tsx (shadcn Badge)
    - apps/web/src/components/ui/checkbox.tsx (shadcn Checkbox)
    - apps/web/src/components/ui/alert.tsx (shadcn Alert)
    - apps/web/src/app/globals.css (theme variables)
  </read_first>
  <action>
    **Create `apps/web/src/components/zatca/zatca-step-indicator.tsx`:**
    - Props: `currentStep: number`, `completedSteps: number[]`, `steps: Array<{ label: string; shortLabel: string }>`
    - Horizontal stepper on desktop (>1024px), vertical on mobile (<768px), compact on tablet (768-1024px)
    - Step states per UI-SPEC:
      - Completed: filled circle with `Check` icon, `--success` color, solid connector line
      - Current: filled circle with step number, `--primary` color
      - Future: outlined circle with step number, `--muted-foreground` color, dashed connector line
    - Connector lines between steps: `--border` color, solid for completed, dashed for pending
    - Uses `role="tablist"` with `aria-current="step"` for active step
    - Each step has `aria-label="Step {N}: {label}"`
    - Arrow key navigation between steps

    **Create `apps/web/src/components/zatca/zatca-tax-details-form.tsx`:**
    - Step 1 form per UI-SPEC wireframe
    - Fields: VAT Registration Number (15-digit, starts with 3, validated), Organization Legal Name (Arabic), Address (Street, City, District, Postal Code), Invoice Types (checkboxes: Standard Tax / Simplified Tax)
    - Client-side validation using `zatcaTaxDetailsSchema` from einvoice package (import via tRPC utils or direct)
    - VAT number field: input mask showing `3` prefix, validates `^3\d{14}$`
    - "Cancel" returns to integrations page, "Next" calls `api.zatca.saveTaxDetails.mutate()`
    - Shows form errors inline below each field

    **Create `apps/web/src/components/zatca/zatca-csr-step.tsx`:**
    - Step 2 per UI-SPEC: informational text about CSR generation + ECDSA P-256 key type display
    - CSR preview in `font-mono text-xs` code block (CN, O, OU, C fields)
    - "Generate CSR" button with spinner during `api.zatca.generateCsr.mutate()`
    - After generation: shows preview, enables "Next"

    **Create `apps/web/src/components/zatca/zatca-compliance-csid-step.tsx`:**
    - Step 3 per UI-SPEC: OTP input (6-digit) + "Request Compliance CSID" button
    - Status list with animated spinner for in-progress, checkmark for complete:
      - Submitting CSR to ZATCA...
      - Compliance CSID received ✓
      - Certificate stored securely ✓
    - "Next" enabled only after all statuses complete
    - `api.zatca.requestComplianceCsid.mutate({ otp })`

    **Create `apps/web/src/components/zatca/zatca-compliance-checks-step.tsx`:**
    - Step 4 per UI-SPEC: "Run Compliance Checks" button
    - Results list: 6 test invoice types with status badges
      - CLEARED: green Badge, REPORTED: green Badge, REJECTED: red Badge, pending: gray
    - Progress bar below list: `████████░░ 4/6`
    - "Next" enabled only when all 6 pass
    - `api.zatca.runComplianceChecks.mutate()` — polls or waits for results

    **Create `apps/web/src/components/zatca/zatca-production-step.tsx`:**
    - Step 5 per UI-SPEC: warning alert about production activation
    - "Complete Onboarding" button (`variant="default"` — primary accent)
    - Certificate info card after successful exchange: Status (Active ●), Issued date, Expires date, Environment
    - Warning uses `--warning` background tint
    - `api.zatca.activateProduction.mutate()`

    **Create `apps/web/src/components/zatca/zatca-onboarding-wizard.tsx`:**
    - Props: `onComplete: () => void`, `onCancel: () => void`, `initialStep?: number`
    - State: currentStep (1-5), managed via `api.zatca.getStatus.useQuery()` to restore progress
    - Renders `ZatcaStepIndicator` + current step component inside `Card`
    - Step 5 steps defined per UI-SPEC step indicator layout
    - Back navigation to any completed step (but not forward past current)
    - Step transitions: subtle slide animation (200ms ease-out) via CSS transition
    - All async operations show inline loading states (not full-page spinners)
    - Wizard state persisted to server (IntegrationConnection.configJson) via each step's mutation

    All components use shadcn primitives (Card, Badge, Button, Input, Alert, Checkbox). No third-party UI libraries. Follow Tailwind v4 patterns from globals.css.
  </action>
  <acceptance_criteria>
    - `apps/web/src/components/zatca/zatca-onboarding-wizard.tsx` contains `export function ZatcaOnboardingWizard`
    - `apps/web/src/components/zatca/zatca-step-indicator.tsx` contains `role="tablist"`
    - `apps/web/src/components/zatca/zatca-tax-details-form.tsx` contains VAT number validation `^3`
    - `apps/web/src/components/zatca/zatca-compliance-checks-step.tsx` renders 6 test invoice results
    - `apps/web/src/components/zatca/zatca-production-step.tsx` contains "Complete Onboarding" text
    - Each step calls the corresponding tRPC mutation
    - TypeScript compiles for the web app
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter web exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>ZATCA onboarding wizard with 5 steps, step indicator, form validation, server persistence</done>
</task>

<task type="auto">
  <name>Task 2: ZATCA status badge, submission panel, compliance widget, and integration wiring</name>
  <files>apps/web/src/components/zatca/zatca-status-badge.tsx, apps/web/src/components/zatca/zatca-submission-panel.tsx, apps/web/src/components/zatca/zatca-compliance-widget.tsx, apps/web/src/components/zatca/zatca-environment-toggle.tsx, apps/web/src/components/settings/integrations-tab.tsx, apps/web/src/components/invoices/invoice-side-panel.tsx</files>
  <read_first>
    - .planning/phases/48-zatca-fatoorah-integration/48-UI-SPEC.md (full file — badge variants, panel layout, widget layout, toggle layout)
    - apps/web/src/components/invoices/ksef-badge.tsx (reference for ZATCA badge)
    - apps/web/src/components/invoices/ksef-metadata-section.tsx (reference for submission panel)
    - apps/web/src/components/invoices/invoice-side-panel.tsx (where badge and panel get added)
    - apps/web/src/components/settings/integrations-tab.tsx (where widget and wizard trigger go)
    - apps/web/src/components/ui/badge.tsx (shadcn Badge)
    - apps/web/src/components/ui/card.tsx (shadcn Card)
    - apps/web/src/components/ui/radio-group.tsx (shadcn RadioGroup — for environment toggle)
  </read_first>
  <action>
    **Create `apps/web/src/components/zatca/zatca-status-badge.tsx`:**
    - Props: `status: "PENDING" | "SUBMITTED" | "CLEARED" | "REPORTED" | "REJECTED" | "WARNING"`, `className?: string`
    - Badge variants per UI-SPEC:
      - PENDING: `--warning` bg, "ZATCA Pending"
      - SUBMITTED: `--info` bg, "ZATCA Submitted"
      - CLEARED: `--success` bg, "ZATCA Cleared"
      - REPORTED: `--success` bg, "ZATCA Reported"
      - REJECTED: `--destructive` bg, "ZATCA Rejected"
      - WARNING: `--warning` bg, "ZATCA Warning"
    - `aria-label="ZATCA status: {status} on {date}"` for screen reader

    **Create `apps/web/src/components/zatca/zatca-submission-panel.tsx`:**
    - Props: `invoiceId: string`
    - Collapsible Card section per UI-SPEC wireframe
    - Fetches `api.zatca.getSubmissionStatus.useQuery({ invoiceId })`
    - Displays: UUID (`font-mono text-xs`), ICV, Status with timestamp, Invoice type
    - Hash chain section: Previous Hash (truncated with copy), Invoice Hash (truncated with copy)
    - QR Code: 64x64px image with decoded fields beside it (seller, VAT, total, VAT amount) per UI-SPEC
    - Action buttons: "View Signed XML" (opens modal with syntax-highlighted XML), "Resubmit" (`variant="outline"`, only visible for REJECTED status, calls `api.zatca.resubmit.mutate({ invoiceId })`)
    - UUID and hashes: `font-mono text-xs` per UI-SPEC
    - Collapsible via shadcn Collapsible component

    **Create `apps/web/src/components/zatca/zatca-compliance-widget.tsx`:**
    - Props: `organizationId: string`
    - Card extending existing integration card pattern per UI-SPEC
    - Fetches `api.zatca.getStatus.useQuery()`
    - Displays: Status dot (green active, yellow sandbox, red error), Environment, Certificate expiry (yellow < 30 days, red < 7 days per UI-SPEC)
    - Invoice counts this period: Cleared, Reported, Pending, Rejected with status icons
    - Health bar: `████████████ 100%`
    - "Manage" button (opens ZATCA management panel), "Disconnect" (`variant="ghost"` with destructive color, requires confirmation dialog per UI-SPEC)
    - Certificate expiry warning copy from UI-SPEC copywriting contract

    **Create `apps/web/src/components/zatca/zatca-environment-toggle.tsx`:**
    - RadioGroup styled as selectable cards per UI-SPEC
    - Two options: Sandbox / Production
    - Selected card: `ring-2 ring-primary` border
    - Switching Production → Sandbox: confirmation dialog with copy from UI-SPEC
    - Switching Sandbox → Production: requires completed onboarding (Step 5)

    **Wire into existing views:**

    Update `apps/web/src/components/settings/integrations-tab.tsx`:
    - Add ZATCA integration card alongside existing integrations (KSeF, etc.)
    - "Connect ZATCA" button triggers `ZatcaOnboardingWizard` (dialog or inline)
    - After connection: show `ZatcaComplianceWidget` instead of connect button

    Update `apps/web/src/components/invoices/invoice-side-panel.tsx`:
    - Conditionally render `ZatcaStatusBadge` in invoice header for invoices with ZATCA chain entries
    - Add `ZatcaSubmissionPanel` as collapsible section below line items
    - Check: if invoice's organization has ZATCA connected AND ZatcaInvoiceChain record exists
  </action>
  <acceptance_criteria>
    - `apps/web/src/components/zatca/zatca-status-badge.tsx` contains `export function ZatcaStatusBadge`
    - `apps/web/src/components/zatca/zatca-status-badge.tsx` contains badge variants for all 6 statuses
    - `apps/web/src/components/zatca/zatca-submission-panel.tsx` contains `export function ZatcaSubmissionPanel`
    - `apps/web/src/components/zatca/zatca-compliance-widget.tsx` contains `export function ZatcaComplianceWidget`
    - `apps/web/src/components/settings/integrations-tab.tsx` contains `ZatcaOnboardingWizard` or `ZatcaComplianceWidget`
    - `apps/web/src/components/invoices/invoice-side-panel.tsx` contains `ZatcaStatusBadge`
    - Environment toggle uses `RadioGroup` with selectable card styling
    - TypeScript compiles for the web app
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter web exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>ZATCA status badges, submission panel, compliance widget, environment toggle wired into invoice and settings views</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of ZATCA frontend components</name>
  <files>apps/web/src/components/zatca/zatca-onboarding-wizard.tsx</files>
  <action>Verify the ZATCA frontend components render correctly and match the UI-SPEC design contract. This is a human visual verification checkpoint.</action>
  <what-built>Complete ZATCA Fatoorah integration frontend: 5-step onboarding wizard in Settings > Integrations, invoice ZATCA status badges, collapsible submission detail panel, compliance dashboard widget, and environment toggle.</what-built>
  <how-to-verify>
    1. Navigate to Settings > Integrations — verify ZATCA card appears with "Connect ZATCA" button
    2. Click "Connect ZATCA" — verify 5-step wizard opens with step indicator
    3. Step 1: Enter valid VAT number (15 digits starting with 3) — verify validation works
    4. Verify step indicator shows completed/current/future states correctly
    5. Navigate to an invoice detail view — verify ZATCA status badge renders (if applicable)
    6. Check responsive behavior: resize to mobile/tablet widths
    7. Verify keyboard navigation on wizard step indicator (arrow keys)
    8. Check all copy matches UI-SPEC copywriting contract
  </how-to-verify>
  <verify>Visual inspection of ZATCA components against UI-SPEC</verify>
  <done>All ZATCA UI components match UI-SPEC design contract and pass visual verification</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → wizard forms | Tax details, OTP entered by user |
| tRPC response → UI | Server data rendered in components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-21 | Spoofing | zatca-onboarding-wizard.tsx | mitigate | All mutations go through authenticated tRPC procedures. No client-side bypass possible. |
| T-48-22 | Information Disclosure | zatca-submission-panel.tsx | mitigate | Panel shows truncated hashes (first 8 chars + "..."). Full hash available via copy button. No signed XML content displayed inline — "View Signed XML" opens modal with explicit user action. |
| T-48-23 | Tampering | zatca-tax-details-form.tsx | mitigate | Client-side validation via Zod schema (same as server-side). Server re-validates all input. Client validation is UX convenience, not security boundary. |
</threat_model>

<verification>
- Onboarding wizard renders all 5 steps with correct form fields
- Status badges display correct colors for all 6 ZATCA statuses
- Submission panel shows UUID, ICV, hash chain, QR code
- Compliance widget shows connection status, certificate expiry, invoice counts
- All components accessible: keyboard nav, aria labels, screen reader friendly
</verification>

<success_criteria>
- User can navigate through all 5 onboarding wizard steps
- ZATCA status badge appears on invoices with ZATCA submissions
- Compliance widget shows real-time ZATCA connection health
- All UI matches the 48-UI-SPEC.md design contract
- Components are responsive across desktop/tablet/mobile
- Accessibility requirements from UI-SPEC met (keyboard nav, aria, contrast)
</success_criteria>

<output>
After completion, create `.planning/phases/48-zatca-fatoorah-integration/48-06-SUMMARY.md`
</output>
