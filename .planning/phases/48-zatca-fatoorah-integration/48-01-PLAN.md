---
phase: 48-zatca-fatoorah-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema/zatca.prisma
  - packages/db/prisma/schema/integration.prisma
  - packages/db/prisma/schema/invoice.prisma
  - packages/db/prisma/schema/organization.prisma
  - packages/einvoice/src/profiles/zatca/schemas.ts
  - packages/einvoice/src/profiles/zatca/types.ts
  - packages/einvoice/src/profiles/zatca/generator.ts
  - packages/einvoice/src/profiles/zatca/parser.ts
  - packages/einvoice/src/profiles/zatca/compliance.ts
  - packages/einvoice/src/profiles/zatca/index.ts
  - packages/validators/src/zatca.ts
autonomous: true
requirements: [ZATCA-01, ZATCA-04]

must_haves:
  truths:
    - "ZATCA profile generates valid UBL 2.1 XML with ICV, PIH, UUID, and ProfileID extensions"
    - "ZatcaInvoiceChain Prisma model tracks per-org sequential invoice chain with submission status"
    - "ZATCA profile plugs into the e-invoicing engine without modifying engine core"
    - "Zod schemas validate all ZATCA-specific input (VAT number, CSR attributes, invoice fields)"
  artifacts:
    - path: "packages/einvoice/src/profiles/zatca/index.ts"
      provides: "ZatcaProfile class implementing EInvoiceProfile"
      exports: ["ZatcaProfile"]
    - path: "packages/einvoice/src/profiles/zatca/generator.ts"
      provides: "UBL 2.1 XML generator with ZATCA extensions"
      exports: ["generateZatcaXml"]
    - path: "packages/db/prisma/schema/zatca.prisma"
      provides: "ZatcaInvoiceChain model and ZatcaSubmissionStatus enum"
      contains: "model ZatcaInvoiceChain"
  key_links:
    - from: "packages/einvoice/src/profiles/zatca/index.ts"
      to: "packages/einvoice/src/types/profile.ts"
      via: "implements EInvoiceProfile"
      pattern: "implements EInvoiceProfile"
    - from: "packages/db/prisma/schema/zatca.prisma"
      to: "packages/db/prisma/schema/invoice.prisma"
      via: "Invoice relation on ZatcaInvoiceChain"
      pattern: "invoiceId.*String.*@unique"
---

<objective>
Create the ZATCA country profile skeleton, UBL 2.1 XML generator with Saudi-specific extensions, Prisma models for invoice chain tracking, and Zod validation schemas.

Purpose: Establishes the foundational data model, type system, and XML generation that all subsequent ZATCA plans build upon. The generator produces unsigned UBL 2.1 XML with ZATCA mandatory fields (ICV, PIH, UUID, ProfileID, AdditionalDocumentReference) per D-04.

Output: ZatcaProfile class, UBL 2.1 generator/parser, Prisma schema, Zod validators, TypeScript types
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-zatca-fatoorah-integration/48-CONTEXT.md
@.planning/phases/48-zatca-fatoorah-integration/48-RESEARCH.md

<interfaces>
From packages/einvoice/src/types/profile.ts:
```typescript
export interface CertificateInfo { certificate: string; privateKey?: string; password?: string; }
export interface Signable { sign(xml: string, certificate: CertificateInfo): Promise<string>; verify(xml: string): Promise<SignatureVerificationResult>; }
export interface QRCodeable { generateQR(invoice: EInvoice): Promise<Buffer>; parseQR(data: Buffer): Promise<Partial<EInvoice>>; }
export interface EInvoiceProfile {
  readonly profileId: string; readonly country: string; readonly displayName: string;
  generate(invoice: EInvoice): Promise<string>;
  parse(xml: string, metadata?: Record<string, unknown>): Promise<EInvoice>;
  validate(xml: string): Promise<ValidationResult>;
  getComplianceStatus(organizationId: string): Promise<ComplianceStatus>;
  readonly sign?: Signable; readonly qrCode?: QRCodeable;
}
```

From packages/einvoice/src/types/invoice.ts:
```typescript
export interface EInvoice {
  id: string; issueDate: string; dueDate?: string; invoiceTypeCode: string; currencyCode: string;
  supplier: EInvoiceParty; customer: EInvoiceParty; lines: EInvoiceLine[];
  taxExclusiveAmount: number; taxInclusiveAmount: number; payableAmount: number;
  taxBreakdown: EInvoiceTaxSubtotal[]; paymentMeans?: EInvoicePaymentMeans;
  extensions?: Record<string, unknown>; profileId: string; externalReference?: string;
}
```

From packages/db/prisma/schema/integration.prisma:
```prisma
enum IntegrationProvider { SLACK RESEND ... MICROSOFT_TEAMS }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prisma schema — ZatcaInvoiceChain model, ZatcaSubmissionStatus enum, IntegrationProvider extension</name>
  <files>packages/db/prisma/schema/zatca.prisma, packages/db/prisma/schema/integration.prisma, packages/db/prisma/schema/invoice.prisma, packages/db/prisma/schema/organization.prisma</files>
  <read_first>
    - packages/db/prisma/schema/integration.prisma
    - packages/db/prisma/schema/invoice.prisma
    - packages/db/prisma/schema/organization.prisma
    - packages/db/prisma/schema/schema.prisma
  </read_first>
  <behavior>
    - Test 1: ZatcaInvoiceChain model has all fields per research section 4
    - Test 2: @@unique([organizationId, icv]) constraint exists
    - Test 3: ZATCA added to IntegrationProvider enum
    - Test 4: ZatcaSubmissionStatus has PENDING, SUBMITTED, CLEARED, REPORTED, REJECTED, WARNING
    - Test 5: prisma validate exits 0
  </behavior>
  <action>
Create `packages/db/prisma/schema/zatca.prisma`:

```prisma
model ZatcaInvoiceChain {
  id               String                @id @default(cuid())
  organizationId   String
  icv              Int
  invoiceId        String                @unique
  invoiceHash      String
  previousHash     String
  zatcaUuid        String                @unique
  zatcaStatus      ZatcaSubmissionStatus @default(PENDING)
  zatcaResponse    Json?
  submittedAt      DateTime?
  clearedAt        DateTime?
  reportedAt       DateTime?
  rejectedAt       DateTime?
  rejectionReason  String?
  createdAt        DateTime              @default(now())

  organization     Organization @relation(fields: [organizationId], references: [id])
  invoice          Invoice      @relation(fields: [invoiceId], references: [id])

  @@unique([organizationId, icv])
  @@index([organizationId])
  @@index([organizationId, zatcaStatus])
}

enum ZatcaSubmissionStatus {
  PENDING
  SUBMITTED
  CLEARED
  REPORTED
  REJECTED
  WARNING
}
```

Add `ZATCA` to `IntegrationProvider` enum in `integration.prisma` after `KSEF`.
Add `zatcaChainEntry ZatcaInvoiceChain?` to Invoice model in `invoice.prisma`.
Add `zatcaChainEntries ZatcaInvoiceChain[]` to Organization model in `organization.prisma`.
  </action>
  <acceptance_criteria>
    - `packages/db/prisma/schema/zatca.prisma` contains `model ZatcaInvoiceChain`
    - `packages/db/prisma/schema/zatca.prisma` contains `@@unique([organizationId, icv])`
    - `packages/db/prisma/schema/zatca.prisma` contains `enum ZatcaSubmissionStatus`
    - `packages/db/prisma/schema/integration.prisma` contains `ZATCA`
    - `npx prisma validate` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && npx prisma validate</automated>
  </verify>
  <done>ZatcaInvoiceChain model and enums exist in Prisma, schema validates</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ZATCA types, Zod schemas, UBL 2.1 generator, parser, compliance, and profile class</name>
  <files>packages/einvoice/src/profiles/zatca/types.ts, packages/einvoice/src/profiles/zatca/schemas.ts, packages/einvoice/src/profiles/zatca/generator.ts, packages/einvoice/src/profiles/zatca/parser.ts, packages/einvoice/src/profiles/zatca/compliance.ts, packages/einvoice/src/profiles/zatca/index.ts, packages/validators/src/zatca.ts</files>
  <read_first>
    - packages/einvoice/src/types/profile.ts
    - packages/einvoice/src/types/invoice.ts
    - packages/einvoice/src/types/compliance.ts
    - packages/einvoice/src/types/validation.ts
    - packages/einvoice/src/profiles/ksef/index.ts
    - packages/einvoice/src/profiles/ksef/generator.ts
    - packages/einvoice/src/profiles/ksef/compliance.ts
    - packages/einvoice/src/profiles/peppol-ae/index.ts
    - packages/einvoice/src/profiles/peppol-ae/generator.ts
    - packages/validators/src/ksef.ts
    - .planning/phases/48-zatca-fatoorah-integration/48-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: generateZatcaXml produces XML with `<cbc:ProfileID>` containing "reporting:1.0" or "clearance:1.0"
    - Test 2: Generated XML contains `<cbc:UUID>` with UUID value
    - Test 3: Generated XML contains AdditionalDocumentReference for ICV and PIH
    - Test 4: InvoiceTypeCode "388" with subtype @name attribute
    - Test 5: Simplified uses "reporting:1.0", standard uses "clearance:1.0"
    - Test 6: ZatcaProfile.profileId === "zatca", .country === "SA"
    - Test 7: zatcaTaxDetailsSchema rejects "123", accepts valid 15-digit VAT number
  </behavior>
  <action>
Create `packages/einvoice/src/profiles/zatca/types.ts`:
- `ZatcaInvoiceType = "standard" | "simplified"`
- `ZatcaInvoiceTypeCode = "388" | "381" | "383"`
- `ZatcaInvoiceSubtype` = "0100000" | "0100001" | "0200000" | "0200001"
- `ZatcaProfileId = "reporting:1.0" | "clearance:1.0"`
- `ZatcaOnboardingStep` = "tax_details" | "csr_generation" | "compliance_csid" | "compliance_checks" | "production_certificate"
- `ZatcaOnboardingState` interface: currentStep, taxDetails?, csrGenerated?, complianceCsidReceived?, complianceChecksPassed?, productionCertActive?
- `ZatcaTlvTag` enum: SELLER_NAME=1, VAT_NUMBER=2, TIMESTAMP=3, TOTAL_WITH_VAT=4, VAT_AMOUNT=5, INVOICE_HASH=6, ECDSA_SIGNATURE=7, PUBLIC_KEY=8

Create `packages/einvoice/src/profiles/zatca/schemas.ts`:
- `zatcaTaxDetailsSchema` = z.object({ vatNumber: z.string().regex(/^3\d{13}3$/), orgNameArabic: z.string().min(1), street: z.string().min(1), city: z.string().min(1), district: z.string().min(1), postalCode: z.string().regex(/^\d{5}$/), invoiceTypes: z.array(z.enum(["standard","simplified"])).min(1) })
- `zatcaCsrAttributesSchema` = z.object({ commonName: z.string(), orgName: z.string(), vatNumber: z.string(), country: z.literal("SA"), serialNumber: z.string(), title: z.enum(["0100","1000","1100"]), registeredAddress: z.string(), businessCategory: z.string() })
- `zatcaInvoiceFieldsSchema` = z.object({ invoiceTypeCode: z.enum(["388","381","383"]), invoiceSubtype: z.string(), icv: z.number().int().positive(), pih: z.string().regex(/^[a-f0-9]{64}$/), uuid: z.string().uuid() })
- `zatcaEnvironmentSchema` = z.enum(["sandbox","production"])
- `zatcaOnboardingStepSchema` = z.enum([...all 5 steps])
- `zatcaConnectionConfigSchema` wrapping environment, currentStep, taxDetails, certificateStatus
- Export inferred types via z.infer<>

Create `packages/einvoice/src/profiles/zatca/generator.ts`:
- Export `generateZatcaXml(invoice: EInvoice): string`
- Use `xmlbuilder2` (per D-04) to build UBL 2.1 Invoice XML per ZATCA spec
- Root `<Invoice>` with namespaces: cac, cbc, ext, sig, sac, ds, xades
- `<ext:UBLExtensions>` — empty placeholder for signature (Plan 02 fills)
- `<cbc:ProfileID>` — "reporting:1.0" for simplified, "clearance:1.0" for standard (based on extensions.invoiceType)
- `<cbc:ID>`, `<cbc:UUID>`, `<cbc:IssueDate>`, `<cbc:IssueTime>`
- `<cbc:InvoiceTypeCode name="{subtype}">388</cbc:InvoiceTypeCode>` (381/383 for notes)
- `<cbc:DocumentCurrencyCode>` — invoice.currencyCode
- AdditionalDocumentReference for ICV: `<cbc:ID>ICV</cbc:ID><cbc:UUID>{icv}</cbc:UUID>`
- AdditionalDocumentReference for PIH: `<cbc:ID>PIH</cbc:ID><cac:Attachment><cbc:EmbeddedDocumentBinaryObject mimeCode="text/plain">{base64(previousHash)}</cbc:EmbeddedDocumentBinaryObject></cac:Attachment>`
- AdditionalDocumentReference for QR: `<cbc:ID>QR</cbc:ID>` placeholder
- AccountingSupplierParty, AccountingCustomerParty, TaxTotal, LegalMonetaryTotal, InvoiceLine
- All minor-unit amounts converted to decimal strings with 2dp

Create `packages/einvoice/src/profiles/zatca/parser.ts`:
- Export `parseZatcaXml(xml: string): EInvoice`
- Use `fast-xml-parser` — extract ICV, PIH, UUID from AdditionalDocumentReference into extensions

Create `packages/einvoice/src/profiles/zatca/compliance.ts`:
- Export `ZatcaConnectionData` interface: status, certificateExpiresAt?, clearance/reporting counts, error info
- Export `computeZatcaComplianceStatus(data: ZatcaConnectionData | null): ComplianceStatus`
- States: not_connected, onboarding, active, suspended, error. Capabilities: canSign=true, canQRCode=true

Create `packages/einvoice/src/profiles/zatca/index.ts`:
- `ZatcaProfile` class implementing `EInvoiceProfile`
- profileId="zatca", country="SA", displayName="ZATCA (Saudi Arabia)"
- sign=undefined (Plan 02 wires ZatcaXAdESSigner)
- qrCode=undefined (Plan 03 wires ZatcaTLVQRCode)
- generate/parse/validate/getComplianceStatus methods following KSeF/Peppol pattern

Create `packages/validators/src/zatca.ts`:
- Re-export zatcaEnvironmentSchema, zatcaTaxDetailsSchema from einvoice package
  </action>
  <acceptance_criteria>
    - `packages/einvoice/src/profiles/zatca/generator.ts` contains `export function generateZatcaXml`
    - `packages/einvoice/src/profiles/zatca/index.ts` contains `class ZatcaProfile implements EInvoiceProfile`
    - `packages/einvoice/src/profiles/zatca/index.ts` contains `profileId = "zatca"`
    - `packages/einvoice/src/profiles/zatca/schemas.ts` contains `zatcaTaxDetailsSchema`
    - `packages/einvoice/src/profiles/zatca/schemas.ts` contains `zatcaEnvironmentSchema`
    - `packages/einvoice/src/profiles/zatca/types.ts` contains `ZatcaTlvTag`
    - `packages/validators/src/zatca.ts` contains `zatcaEnvironmentSchema`
    - `pnpm --filter @contractor-ops/einvoice exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/einvoice exec tsc --noEmit && pnpm --filter @contractor-ops/validators exec tsc --noEmit</automated>
  </verify>
  <done>ZATCA profile, generator, parser, compliance, schemas, types created; TypeScript compiles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EInvoice input -> XML generator | Invoice data serialized into XML must be escaped |
| Prisma schema -> Database | Chain integrity enforced via unique constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | generator.ts | mitigate | Use xmlbuilder2 built-in XML escaping — never concatenate raw strings |
| T-48-02 | Information Disclosure | zatca.prisma | mitigate | Strip Authorization headers from zatcaResponse before persisting |
| T-48-03 | Denial of Service | ZatcaInvoiceChain | mitigate | Indexes on [organizationId] and [organizationId, zatcaStatus] |
</threat_model>

<verification>
1. `npx prisma validate` — Prisma schema valid
2. `pnpm --filter @contractor-ops/einvoice exec tsc --noEmit` — einvoice compiles
3. `pnpm --filter @contractor-ops/validators exec tsc --noEmit` — validators compile
4. ZatcaProfile.profileId === "zatca" and country === "SA"
5. generateZatcaXml output contains UBL 2.1 namespace and ZATCA extensions
</verification>

<success_criteria>
- ZatcaProfile implements EInvoiceProfile with all methods
- UBL 2.1 generator produces XML with ICV, PIH, UUID, ProfileID, InvoiceTypeCode
- ZatcaInvoiceChain model with unique constraints and indexes
- Zod schemas validate VAT number, CSR attributes, invoice fields, environment
- No engine core modifications
</success_criteria>

<output>
After completion, create `.planning/phases/48-zatca-fatoorah-integration/48-01-SUMMARY.md`
</output>
