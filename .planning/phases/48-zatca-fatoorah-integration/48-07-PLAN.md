---
phase: 48-zatca-fatoorah-integration
plan: 07
type: execute
wave: 3
depends_on: [01, 04]
files_modified:
  - packages/db/prisma/schema/zatca.prisma
autonomous: false
requirements: [ZATCA-04]

must_haves:
  truths:
    - "ZatcaInvoiceChain table exists in the database with all columns and indexes"
    - "ZatcaSubmissionStatus enum exists in the database"
    - "ZATCA value exists in IntegrationProvider enum in the database"
    - "Prisma client is regenerated with ZatcaInvoiceChain model"
  artifacts:
    - path: "packages/db/prisma/schema/zatca.prisma"
      provides: "ZatcaInvoiceChain model pushed to live database"
      contains: "model ZatcaInvoiceChain"
  key_links:
    - from: "packages/db/prisma/schema/zatca.prisma"
      to: "database"
      via: "prisma db push"
      pattern: "ZatcaInvoiceChain"
---

<objective>
[BLOCKING] Push Prisma schema changes to the database — ZatcaInvoiceChain model, ZatcaSubmissionStatus enum, and ZATCA IntegrationProvider value.

Purpose: Plans 01 and 04 modify Prisma schema files but do not push to the database. This plan pushes the schema to ensure the live database matches the Prisma schema. Without this, TypeScript types from Prisma Client will pass compilation (generated from schema config), but runtime queries to ZatcaInvoiceChain will fail because the table does not exist. This creates a false-positive verification state.

Output: Database schema updated, Prisma Client regenerated.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-zatca-fatoorah-integration/48-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push Prisma schema to database</name>
  <files>packages/db/prisma/schema/zatca.prisma</files>
  <read_first>
    - packages/db/prisma/schema/zatca.prisma (verify model exists from Plan 01)
    - packages/db/prisma/schema/integration.prisma (verify ZATCA in IntegrationProvider)
    - packages/db/prisma/schema/schema.prisma (datasource and generator config)
  </read_first>
  <action>
    1. Validate schema before push:
    ```bash
    npx prisma validate
    ```

    2. Push schema to database:
    ```bash
    npx prisma db push
    ```
    If the push requires interactive confirmation (destructive changes), use:
    ```bash
    npx prisma db push --accept-data-loss
    ```
    Only use `--accept-data-loss` if the changes are additive (new model, new enum values). The ZatcaInvoiceChain model and ZatcaSubmissionStatus enum are new — no existing data is affected.

    3. Regenerate Prisma Client:
    ```bash
    npx prisma generate
    ```

    4. Verify the push succeeded by checking the database:
    ```bash
    npx prisma db pull --print 2>&1 | grep -i "zatca"
    ```
    This should show the ZatcaInvoiceChain model in the introspected schema.

    If the push fails due to connection issues or environment configuration:
    - Check `DATABASE_URL` in `.env`
    - Verify database is accessible
    - Flag task for manual intervention with `autonomous: false`
  </action>
  <acceptance_criteria>
    - `npx prisma db push` exits with code 0
    - `npx prisma generate` exits with code 0
    - Database contains `ZatcaInvoiceChain` table
    - Database contains `ZatcaSubmissionStatus` enum
    - Database `IntegrationProvider` enum includes `ZATCA`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && npx prisma validate && echo "SCHEMA_VALID"</automated>
  </verify>
  <done>Prisma schema pushed to database, ZatcaInvoiceChain table created, Prisma Client regenerated</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify database schema push via Prisma Studio</name>
  <files>packages/db/prisma/schema/zatca.prisma</files>
  <action>Verify the Prisma schema push created the ZatcaInvoiceChain table and related enums in the live database. Open Prisma Studio to inspect.</action>
  <what-built>Prisma schema push creating ZatcaInvoiceChain table, ZatcaSubmissionStatus enum, and ZATCA IntegrationProvider value in the database.</what-built>
  <how-to-verify>
    1. Run `npx prisma studio` and verify ZatcaInvoiceChain table appears in the model list
    2. Verify the table has columns: id, organizationId, icv, invoiceId, invoiceHash, previousHash, zatcaUuid, zatcaStatus, zatcaResponse, submittedAt, clearedAt, reportedAt, rejectedAt, rejectionReason, createdAt
    3. Verify IntegrationProvider enum includes ZATCA in Prisma Studio
  </how-to-verify>
  <verify>Visual inspection of Prisma Studio</verify>
  <done>ZatcaInvoiceChain table and enums confirmed in database via Prisma Studio</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema push -> database | DDL commands modify database structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-24 | Denial of Service | prisma db push | mitigate | Push is additive (new table/enum). No data loss. Validate before push. |
</threat_model>

<verification>
- Prisma schema validates without errors
- Database push completes successfully
- Prisma Client regenerated with ZatcaInvoiceChain model
</verification>

<success_criteria>
- ZatcaInvoiceChain table exists in database with all columns and constraints
- ZatcaSubmissionStatus enum exists in database
- ZATCA exists in IntegrationProvider enum in database
- Prisma Client types include ZatcaInvoiceChain
</success_criteria>

<output>
After completion, create `.planning/phases/48-zatca-fatoorah-integration/48-07-SUMMARY.md`
</output>
