---
phase: 66-phase-57-completion-verification
plan: 04
type: execute
wave: 3
depends_on:
  - 66-01
  - 66-02
  - 66-03
files_modified:
  - .planning/phases/57-government-api-clients/57-VERIFICATION.md
autonomous: true
requirements:
  - PAY-02
  - PAY-03
  - PAY-04
  - PAY-05

must_haves:
  truths:
    - "57-VERIFICATION.md exists at .planning/phases/57-government-api-clients/57-VERIFICATION.md"
    - "57-VERIFICATION.md frontmatter mirrors the 63-VERIFICATION.md shape: phase, verified, status, score, re_verification (with previous_status, previous_score, gaps_closed, gaps_remaining, regressions, fix_commits[]), human_verification (a.k.a. manual_only)"
    - "Frontmatter status is 'verified' if-and-only-if (a) the per-phase re-run command exits 0 with all suites green, (b) every PAY-02..05 requirement maps to at least one passing test ID, (c) gaps_remaining is empty"
    - "Each PAY-02, PAY-03, PAY-04, PAY-05 requirement has its own row in the truth table with explicit test IDs (file path + describe-name + it-name)"
    - "manual_only[] enumerates: (a) WCAG / accessibility spot-check (per CONTEXT.md D-12), (b) HMRC live sandbox round-trip (per CONTEXT.md D-13/D-14)"
    - "fix_commits[] enumerates the actual commit hashes from Plans 66-01, 66-02, 66-03 (use git log to read them at write time)"
    - "After 57-VERIFICATION.md commits, `gsd-sdk query phase complete 57` reports updated=true (or equivalent success indicator) — closes Phase 57 in STATE.md / ROADMAP.md"
    - "Phase 66's own commit for the verification doc uses the docs() commit-type prefix per CONTEXT.md D-19"
  artifacts:
    - path: ".planning/phases/57-government-api-clients/57-VERIFICATION.md"
      provides: "Phase 57 goal-backward verification report"
      contains: "phase: 57-government-api-clients"
  key_links:
    - from: ".planning/phases/57-government-api-clients/57-VERIFICATION.md"
      to: ".planning/phases/57-government-api-clients/57-04-SUMMARY.md"
      via: "Truth list derived from SUMMARY's 'Acceptance Criteria Checklist' + 'Roadmap Traceability' tables"
      pattern: "PAY-02|PAY-03|PAY-04|PAY-05"
    - from: ".planning/phases/57-government-api-clients/57-VERIFICATION.md"
      to: ".planning/phases/63-uk-payments-financial-features/63-VERIFICATION.md"
      via: "frontmatter shape mirror per CONTEXT.md D-15"
      pattern: "human_verification|gaps_closed|fix_commits"
---

<objective>
Produce `57-VERIFICATION.md` — the goal-backward verification artifact for Phase 57 (Government API Clients) — and close Phase 57 in STATE.md / ROADMAP.md by running `gsd-sdk query phase complete 57`. This is the final closing action of Phase 66.

Purpose: Phase 57 reached `VALIDATION.md status: approved` on 2026-04-13 but never produced a `VERIFICATION.md`. CONTEXT.md decisions D-15..D-18 mandate a hybrid (programmatic + flagged-manual) doc that:

- Mirrors `63-VERIFICATION.md` frontmatter (D-15) so tooling consuming verification artifacts works uniformly across phases.
- Reports `status: verified` only when (D-16) all four conditions hold: (a) re-run commands all-green, (b) the 3 originally-failing contractor.test cases pass, (c) all 9 (sub-)scenario equivalents from Plan 57-04 Task 3 are either automated (linked to test IDs) or explicitly enumerated in `manual_only[]`, (d) every PAY-02..05 requirement maps to a passing test ID.
- Uses a hybrid evidence model (D-17) — Programmatic section captures grep + test results with command output excerpts; Manual-Only section explicitly lists the deferred items (a11y per D-12, HMRC live-credential round-trip per D-13/14). No silent omissions.
- Triggers a Phase 57 completion flag flip via `gsd-sdk query phase complete 57` (D-18) so the manager dashboard reflects Phase 57 as fully closed.

Output: `.planning/phases/57-government-api-clients/57-VERIFICATION.md` written, committed, and Phase 57 marked complete in STATE.md.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/66-phase-57-completion-verification/66-CONTEXT.md
@.planning/phases/66-phase-57-completion-verification/66-RESEARCH.md
@.planning/phases/57-government-api-clients/57-CONTEXT.md
@.planning/phases/57-government-api-clients/57-VALIDATION.md
@.planning/phases/57-government-api-clients/57-04-PLAN.md
@.planning/phases/57-government-api-clients/57-04-SUMMARY.md
@.planning/phases/63-uk-payments-financial-features/63-VERIFICATION.md
</context>

<threat_model>
- THREAT: 57-VERIFICATION.md flips status to `verified` even though one of the per-phase re-run commands fails silently (e.g. one suite reports tests-run > 0 but assertions fail) → MITIGATION: Task 1 mandates that the executor copy command outputs INTO the doc verbatim (a `## Re-run evidence` section with the trailing summary line of each `pnpm test` invocation). If the line says `Tests: <N> failed`, the doc cannot truthfully claim `status: verified`.
- THREAT: PAY-04's setKleinunternehmer truth row ends up citing only the new Plan 66-02 test (router gate) and silently drops the existing UI-toggle test → MITIGATION: Task 1's truth table format (mirroring 63-VERIFICATION.md's table at lines 80-99) requires multiple evidence pointers per truth row when they exist (Layer A, Layer B, UI). Audit each truth row for completeness.
- THREAT: The doc is committed BEFORE all gsd-sdk preconditions hold, leaving STATE.md / ROADMAP.md in a state where the gsd-sdk command refuses to mark 57 complete (e.g. missing SUMMARY.md, missing prior-phase prereqs) → MITIGATION: Task 2 runs `gsd-sdk query phase complete 57 --raw` and inspects the JSON. If the SDK refuses, capture the reason and STOP (do NOT silently force the flip). The user may need to intervene to align state — that's a known-unknown surfaced rather than papered over.
- THREAT: A new gap (e.g. Plan 66-01 / 66-02 / 66-03 commit didn't actually land green) is discovered during Task 1 by running the per-phase re-run, and the doc is written anyway with `status: gaps_found` → MITIGATION: write `status: gaps_found` honestly with `gaps_remaining[]` populated, and STOP before running the manager-flag flip in Task 2 (the flip should NOT execute when gaps_remaining is non-empty). Surface the new gap as STATE.md Pending Todo.
- THREAT: The fix_commits[] block falls out of sync with the actual commits because the executor uses placeholder hashes → MITIGATION: Task 1 step 2 explicitly mandates `git log -3 --oneline --grep "^\(test\|fix\)\(66-0" --pretty=format:'%H %s'` to read the three fix commits' real hashes at write time.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run the phase-closure acceptance commands and write 57-VERIFICATION.md</name>
  <files>
    .planning/phases/57-government-api-clients/57-VERIFICATION.md
  </files>
  <read_first>
    .planning/phases/57-government-api-clients/57-04-SUMMARY.md,
    .planning/phases/57-government-api-clients/57-04-PLAN.md,
    .planning/phases/57-government-api-clients/57-CONTEXT.md,
    .planning/phases/57-government-api-clients/57-VALIDATION.md,
    .planning/phases/63-uk-payments-financial-features/63-VERIFICATION.md,
    .planning/phases/66-phase-57-completion-verification/66-CONTEXT.md,
    .planning/phases/66-phase-57-completion-verification/66-RESEARCH.md,
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md
  </read_first>
  <action>
**Step 1: Run phase-closure re-run commands and capture outputs.** Execute these from repo root and CAPTURE the trailing summary line of each:

```bash
cd packages/api && npx tsc --noEmit ; cd -
pnpm --filter @contractor-ops/api test -- --run contractor.test invoice.test organization.test gov-api-clients
pnpm --filter @contractor-ops/gov-api test
pnpm --filter @contractor-ops/web test -- --run vat-validation-status-pill invoice-footer-legal-notices reverse-charge-line-toggle revalidate-vat-button country-compliance-section
pnpm --filter @contractor-ops/validators test -- --run locked-phrases-guard
```

Record each command's exit code and the trailing `Tests <N> passed (<N>)` summary line in your scratch buffer — they go into the `## Re-run evidence` section of the doc.

If ANY command exits non-zero or shows a `failed` count > 0, STOP. Set the doc's `status:` to `gaps_found`, populate `gaps_remaining[]`, write the doc, but DO NOT run Task 2's manager-flag flip — escalate the gap as a STATE.md Pending Todo.

**Step 2: Read the three Phase 66 fix commit hashes.**

```bash
git log -10 --oneline --pretty=format:'%H %s' | grep -E "(test|fix|docs)\(66-0[123]\)"
```

You should see exactly 3 entries (one per Plan 66-01..66-03). Capture each `<hash>` for the `fix_commits[]` block. If fewer than 3 entries appear, either an upstream plan didn't commit (STOP and surface) or commit grep prefix differs slightly (adjust the grep but verify each hash maps to the right plan).

**Step 3: Write `.planning/phases/57-government-api-clients/57-VERIFICATION.md`** using the template below. Substitute every `{TOKEN}` with the captured value. Match the indentation / quoting style of `63-VERIFICATION.md` exactly.

```markdown
---
phase: 57-government-api-clients
verified: {ISO-8601 timestamp at write time, e.g. 2026-04-26T15:30:00Z}
status: {verified | gaps_found — set per Step 1 outcome}
score: {N}/{M} truths verified  # M = total truths in table below; N = how many verified GREEN

re_verification:
  previous_status: pending  # 57 reached approved via VALIDATION.md but never produced a VERIFICATION.md
  previous_score: 0/{M}
  gaps_closed:
    - "Vitest alias chain repaired so packages/api router test suites can load (Plan 66-01)"
    - "§2 router-layer HMRC 404→invalid surface coverage added (Plan 66-02 contractor.test new case)"
    - "§6 organization.setKleinunternehmer DE-only gate coverage added (Plan 66-02 organization.test new describe block)"
    - "§2 MSW Layer A post-network 404 wire-level coverage added (Plan 66-03 hmrc-vat-client.msw.integration new case)"
    - "Plan 57-04 Task 3 9-scenario human-verify checkpoint converted to deterministic CI evidence (combination of pre-existing tests + Plan 66-02 + 66-03 additions; one scenario — accessibility spot-check — explicitly remains manual_only[])"
  gaps_remaining: []  # MUST be empty if status: verified; otherwise enumerate
  regressions: []  # any pre-existing test that started failing during Phase 66 — keep [] only if true
  fix_commits:
    - hash: "{HASH from Plan 66-01}"
      scope: "fix(66-01): repair @contractor-ops/einvoice subpath alias in api vitest config"
      files:
        - "packages/api/vitest.config.ts"
      changes:
        - "Replaced single-file alias '@contractor-ops/einvoice'→einvoice/src/index.ts with array-of-aliases pattern enumerating /zatca/schemas, /zatca/types, /compliance, and the bare entry. Subpath entries listed BEFORE the bare entry per Vite alias matching semantics."
    - hash: "{HASH from Plan 66-02}"
      scope: "test(66-02): close router-layer Phase 57 coverage gaps for §2 + §6"
      files:
        - "packages/api/src/routers/__tests__/contractor.test.ts"
        - "packages/api/src/routers/__tests__/organization.test.ts"
      changes:
        - "contractor.test.ts: added validateVat surfaces responseStatus='invalid' (HMRC 404 router-layer assertion)."
        - "organization.test.ts: added describe block 'organization.setKleinunternehmer' with 3 cases (DE flips flag, GB FORBIDDEN, PL FORBIDDEN)."
    - hash: "{HASH from Plan 66-03}"
      scope: "test(66-03): close MSW Layer A gap for §2 HMRC post-network 404"
      files:
        - "packages/gov-api/src/clients/__tests__/hmrc-vat-client.msw.integration.test.ts"
      changes:
        - "Added per-test server.use() override returning 404 for a checksum-passing VRN; asserts client returns { status: 'invalid', raw: null } AFTER a real fetch round-trip and that the override handler was actually invoked (handlerCallCount === 1)."

human_verification:
  - test: "WCAG / accessibility spot-check on Phase 57 UI surfaces"
    expected: "Tab through contractor profile → VAT pill + Revalidate button reachable via keyboard. Pill variants always render icon + text (no color-only signal). Stale pill tooltip is screen-reader accessible. Kleinunternehmer toggle has focus-visible state. RC line toggle dialog focus-traps the textarea."
    why_human: "No axe-core / matcher-based a11y test infrastructure currently exists in apps/web. Adding it is out of scope per Phase 66 CONTEXT.md decision D-12; this spot-check stays manual until a future test-infrastructure phase. Per Phase 57 D-09 the visual signal must always include text alongside color (WCAG AA), and the existing component tests grep the literal label strings ('Valid', 'Invalid', 'Stale', 'Unavailable', 'Not validated') as a proxy."
    deferral_pattern: "STATE.md 'Manual-Only Verifications' convention; mirrors Phase 57 VALIDATION.md §Manual-Only Verifications."

  - test: "HMRC live sandbox round-trip on a real GB VRN (PAY-03 production-onboarding evidence)"
    expected: "Once HMRC Developer Hub registration completes (~2-week wait per STATE.md Blockers), exercise validateVat against the live HMRC sandbox endpoint with real client_id / client_secret to confirm: (a) production-VRN guard tolerates empty platformVrn in sandbox, (b) OAuth 2.0 client-credentials Bearer flow round-trips, (c) lookup returns canonical sandbox valid (193054661 → 200 valid) and invalid (555555555 → 404 invalid), (d) fraud-prevention headers reach HMRC."
    why_human: "HMRC sandbox credentials are pre-deploy ops infrastructure tracked in STATE.md Blockers. Per Phase 66 CONTEXT.md decisions D-13 and D-14, this is NOT a Phase 66 gate — MSW deterministic tests cover the application code path; the live-credential exercise is pre-deploy ops hygiene, not pre-merge. Phase 66 explicitly removes it as a verification gate."
    deferral_pattern: "STATE.md 'Standing Project Constraints — Legal/regulatory verification deferred' convention extended to live-credential ops gates."
---

# Phase 57: Government API Clients — Verification Report

**Phase Goal:** Users can validate UK VAT numbers via HMRC, validate German USt-IdNr via VIES qualified confirmation with graceful degradation, see UK / DE VAT rates correctly applied to invoices, and use the Kleinunternehmer toggle + reverse-charge auto-detection with override-with-reason audit (per ROADMAP.md Phase 57 Success Criteria 1-4 and REQUIREMENTS.md PAY-02..05).

**Verified:** {ISO timestamp}
**Status:** {verified | gaps_found}
**Re-verification:** First — Phase 57 reached `VALIDATION.md status: approved` on 2026-04-13 but never produced a VERIFICATION.md; this is the goal-backward closure, made possible by the Plan 66-01 vitest-load fix and the Plan 66-02 / 66-03 coverage fills.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | UK VAT validation via HMRC end-to-end (HmrcVatClient → orchestrator → router → UI pill) — PAY-03 / Success Criterion 1 | ✓ VERIFIED | Layer A wire-level: `packages/gov-api/src/clients/__tests__/hmrc-vat-client.msw.integration.test.ts` `checkVatNumber returns valid result for sandbox VRN (193054661)`. Router layer: `packages/api/src/routers/__tests__/contractor.test.ts §781` `validateVat dispatches GB_VAT for a UK contractor`. Orchestrator dispatch: `validateTaxId` invoked with `taxIdType: 'GB_VAT'`. UI: `apps/web/src/components/contractors/__tests__/vat-validation-status-pill.test.tsx §5` `renders "Valid" badge for valid status`. |
| 2 | UK VAT validation 404 sad path surfaces as 'invalid' to UI — PAY-03 | ✓ VERIFIED — newly closed by Phase 66 | Layer A wire-level: hmrc-vat-client.msw.integration.test.ts `post-network sad path` (Plan 66-03) — checksum-passing VRN against MSW 404 returns `{status: 'invalid', raw: null}`. Router layer: `contractor.test.ts validateVat surfaces responseStatus=invalid` (Plan 66-02). UI: vat-validation-status-pill.test.tsx §14 `renders "Invalid" badge for invalid status` + §55 destructive variant assertion. |
| 3 | DE USt-IdNr validation via VIES qualified confirmation — PAY-05 / Success Criterion 2 | ✓ VERIFIED | Layer A: `packages/gov-api/src/clients/__tests__/vies-client.msw.integration.test.ts` `qualified checkVatNumber returns confirmationRef (requestIdentifier)`. Router layer: `contractor.test.ts §804` `validateVat dispatches DE_USTIDNR for a DE contractor`. |
| 4 | VIES outage graceful soft-fail — yellow stale pill — PAY-05 / D-08 | ✓ VERIFIED | Router layer: `contractor.test.ts §869` `revalidateVat returns stale responseStatus when orchestrator soft-fails (D-08)`. UI: vat-validation-status-pill.test.tsx §23 `renders "Stale" badge for stale status` (asserts visible label + `data-status='stale'`). Layer A coverage of MS_UNAVAILABLE branch lives in vies-client MSW integration suite. |
| 5 | UK VAT rate preselect — PAY-02 / Success Criterion 3 | ✓ VERIFIED | `packages/api/src/routers/__tests__/invoice.test.ts §994` `GB org invoice pre-selects the isDefault TaxRate code 20 — PAY-02`. Underlying: `getTaxRatesForCountry('GB')` returns 4 rates ordered isDefault-first (covered in `packages/api/src/services/__tests__/tax-rate.service.test.ts`). |
| 6 | DE VAT rate preselect — PAY-04 / Success Criterion 4 | ✓ VERIFIED | `invoice.test.ts §1007` `DE org invoice pre-selects the isDefault TaxRate code 19 — PAY-04`. |
| 7 | DE Kleinunternehmer forces 'KU' on all lines + footer notice — PAY-04 | ✓ VERIFIED | Router layer: `invoice.test.ts §1019` `DE Kleinunternehmer org forces invoice vatRate to KU — PAY-04 (§19 UStG)`. Footer: `apps/web/src/components/invoices/__tests__/invoice-footer-legal-notices.test.tsx §22` `renders Kleinunternehmer notice for DE org` (asserts the locked-phrase substring `Gemäß § 19 UStG …` from `@contractor-ops/validators`). Locked-phrase guard: `packages/validators/src/__tests__/locked-phrases-guard.test.ts` 32/32 green. |
| 8 | DE Kleinunternehmer toggle DE-only gate — PAY-04 / D-02 | ✓ VERIFIED — newly closed by Phase 66 | Router layer: `organization.test.ts organization.setKleinunternehmer (Phase 57 · Plan 04 / Phase 66)` — 3 cases (DE pass / GB FORBIDDEN / PL FORBIDDEN) added by Plan 66-02. UI gate: `KleinunternehmerToggle` returns null for non-DE orgs (component-render branch covered indirectly by the country-compliance-section component test). |
| 9 | Reverse-charge auto-detect (DE→GB B2B + DE domestic 13b UStG) — PAY-04 | ✓ VERIFIED | `invoice.test.ts §1032` `auto-selects RC when detectReverseCharge reports shouldApply=true`. Service-layer rules covered by `packages/api/src/services/__tests__/reverse-charge.service.test.ts`. |
| 10 | RC override requires reason (Zod refine + AlertDialog gate) and writes audit log — PAY-04 / D-13 | ✓ VERIFIED | `invoice.test.ts §1108` `persists AuditLog when user disables auto-detected RC with a reason (D-13)`. Refusal: `invoice.test.ts §1150` `rejects reverseChargeOverride=false without a reason (Zod refine)`. UI: `apps/web/src/components/invoices/__tests__/reverse-charge-line-toggle.test.tsx`. |
| 11 | UK reverse-charge footer locked phrase render — PAY-04 / D-14 | ✓ VERIFIED | `invoice-footer-legal-notices.test.tsx §47` `renders UK reverse charge notice` — asserts the literal locked phrase `Reverse charge: Customer to pay the VAT to HMRC` and `lang='en'`. Locked-phrase identifier imported from `@contractor-ops/validators` per Phase 57 D-12 (CI guard: locked-phrases-guard 32/32 green). |
| 12 | D-07 trigger 1: profile-save-with-changed-VAT-number dispatches inline validateTaxId | ✓ VERIFIED | `contractor.test.ts §911` `dispatches validateTaxId exactly once when a UK contractor VAT number changes`. Scope guard: §928 `does NOT dispatch validateTaxId when the VAT number is unchanged`. Clear path: §939 `clears summary fields without API call when updated row has null vatId`. |
| 13 | gov-api-clients factory — production VRN guard | ✓ VERIFIED | `packages/api/src/__tests__/gov-api-clients.test.ts` 6/6 green. Defense-in-depth: zod superRefine + runtime throw on `HMRC_PLATFORM_VRN` empty when `HMRC_ENV=production`; permits empty in sandbox. |
| 14 | Cross-org isolation on validateVat — defense in depth — Phase 57 Threat T-57-04-01 | ✓ VERIFIED | `contractor.test.ts §831` `validateVat throws NOT_FOUND for a contractor in another organization`. |
| 15 | Phase 57 ships intended UI components (5 new + 1 extended) | ✓ VERIFIED | All component files present (verified by `ls`): `vat-validation-status-pill.tsx`, `revalidate-vat-button.tsx`, `kleinunternehmer-toggle.tsx`, `reverse-charge-line-toggle.tsx`, `invoice-footer-legal-notices.tsx`, `country-compliance-section.tsx` (extended). Each has a co-located test file under `__tests__/` (verified via `find apps/web/src/components -name "*.test.tsx"`). |
| 16 | Microcopy present in all 4 locales (en/de/pl/ar) for Phase 57 namespaces | ✓ VERIFIED | Per 57-04-SUMMARY.md verification, `jq '.contractor.vatValidation.valid' apps/web/messages/{en,de,pl,ar}.json` returns the translated label in all 4 files. Locked-phrase identifiers (TAX_KLEINUNTERNEHMER_NOTICE etc.) are NOT mirrored in messages — guard test (locked-phrases-guard 32/32) enforces. |

**Score:** {N}/16 truths verified. {Plan 66 added/closed} truth(s) flipped to ✓ this iteration: #2 (newly closed via Plans 66-02 + 66-03), #8 (newly closed via Plan 66-02). 2 deferred items in `human_verification` block (a11y spot-check, HMRC live sandbox round-trip).

### Required Artifacts

| Artifact | Plan | Status | Details |
|----------|------|--------|---------|
| `packages/db/prisma/schema/*.prisma` (TaxIdValidation model + Contractor.latestVat* + Organization.isKleinunternehmer) | 57-01 | ✓ VERIFIED | Per 57-04-SUMMARY.md: schema models present, fields wired through. |
| `packages/api/src/gov-api-clients.ts` (env-driven factory) | 57-04 | ✓ VERIFIED | 6 tests green; production-VRN guard active. |
| `packages/api/src/routers/contractor.ts` (validateVat / revalidateVat / D-07 trigger 1 in update) | 57-04 | ✓ VERIFIED | Lines 1348 / 1409 / 1339 (per Phase 66 RESEARCH.md). 9 phase-57 router tests + 3 D-07 trigger tests + Phase 66 §2 surface test all green. |
| `packages/api/src/routers/organization.ts` (setKleinunternehmer DE-only) | 57-04 | ✓ VERIFIED — gate now tested | Line 108 mutation. Newly tested via Plan 66-02 organization.test setKleinunternehmer block (3 cases). |
| `packages/api/src/routers/invoice.ts` (upsertLine pipeline: preselect + RC + KU + audit) | 57-04 | ✓ VERIFIED | 8 phase-57 invoice.test cases green (§944-1158). |
| 6 UI components + extended country-compliance-section | 57-04 | ✓ VERIFIED | All present + co-located tests; locked-phrase guard 32/32 green. |
| 4 locales × 3 namespaces of microcopy | 57-04 | ✓ VERIFIED | 57-04-SUMMARY.md `jq` evidence; replicated. |
| MSW handlers for HMRC + VIES (test-utils) | 57-01 | ✓ VERIFIED | `packages/test-utils/src/msw/handlers/{hmrc,vies}.ts` exist; consumed by gov-api MSW integration tests + Plan 66-03 override. |

## Re-run Evidence

Captured during Phase 66 closure ({ISO timestamp}):

```
$ cd packages/api && npx tsc --noEmit
EXIT: 0

$ pnpm --filter @contractor-ops/api test -- --run contractor.test invoice.test organization.test gov-api-clients
{trailing summary line, e.g. "Test Files X passed (X)  Tests N passed (N)"}

$ pnpm --filter @contractor-ops/gov-api test
{trailing summary line}

$ pnpm --filter @contractor-ops/web test -- --run vat-validation-status-pill invoice-footer-legal-notices reverse-charge-line-toggle revalidate-vat-button country-compliance-section
{trailing summary line}

$ pnpm --filter @contractor-ops/validators test -- --run locked-phrases-guard
{trailing summary line — should be Tests 32 passed (32) or whatever the current count is}
```

All commands exit 0; no `Tests <N> failed` lines anywhere.

## Notes on the 3 originally-failing contractor.test cases

Per CONTEXT.md decision D-03, the mock-harness extension for `contract.count` / `contract.groupBy` was already landed by commit `0df1164f chore: snapshot v2 WIP before phase 61 execution` (2026-04-14, lines 73-77 of `contractor.test.ts`). The 3 failures recorded in `57-04-SUMMARY.md` "Verification Results" table (`archive`, `updateLifecycleStage INACTIVE`, `bulkArchive`) cannot fail on the harness cause anymore. Plan 66-01's vitest alias repair unblocked loading; the re-run above confirms all 3 cases pass on current HEAD.

## Phase 57 → Phase 66 Closure Trace

Phase 57 delivered the implementation (commits up to `d240752a` 2026-04-13). Phase 66 delivered the verification:

- Plan 66-01 ({hash}): repaired vitest alias chain → router test suites can load.
- Plan 66-02 ({hash}): closed router-layer §2 (HMRC 404 invalid surface) + §6 (setKleinunternehmer DE-only gate).
- Plan 66-03 ({hash}): closed Layer A wire-level §2 (HMRC post-network 404 vs. checksum preflight).
- Plan 66-04 (this commit): produced 57-VERIFICATION.md and flipped Phase 57 complete via gsd-sdk.

The 9 (sub-)scenarios from Plan 57-04 Task 3 are now either (a) directly mapped to passing test IDs in the truth table above, or (b) explicitly enumerated in `human_verification[]`. PAY-02 / PAY-03 / PAY-04 / PAY-05 each have ≥ 1 passing test ID — see the truth-table evidence column.
```

**Step 4: Confirm the doc is well-formed.**

```bash
# Frontmatter parses as YAML
head -50 .planning/phases/57-government-api-clients/57-VERIFICATION.md
# Substantive body present
wc -l .planning/phases/57-government-api-clients/57-VERIFICATION.md   # should be > 100 lines
# All four PAY-* IDs cited
grep -c "PAY-0[2345]" .planning/phases/57-government-api-clients/57-VERIFICATION.md   # should be ≥ 4
# Both manual_only items present
grep -c "WCAG\|HMRC live sandbox" .planning/phases/57-government-api-clients/57-VERIFICATION.md   # should be ≥ 2
# Three fix commit hashes appear (40-char SHAs)
grep -E "[0-9a-f]{40}" .planning/phases/57-government-api-clients/57-VERIFICATION.md | wc -l   # should be ≥ 3
```

If any check fails, fix the doc and re-run.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && test -f .planning/phases/57-government-api-clients/57-VERIFICATION.md && grep -c "PAY-0[2345]" .planning/phases/57-government-api-clients/57-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/57-government-api-clients/57-VERIFICATION.md` exists
    - The file's first non-empty line is `---` (YAML frontmatter delimiter)
    - The frontmatter contains the literal `phase: 57-government-api-clients`
    - The frontmatter contains a `status:` field set to either `verified` or `gaps_found`
    - `grep -c "PAY-0[2345]" .planning/phases/57-government-api-clients/57-VERIFICATION.md` ≥ 4 (every PAY-* id cited at least once)
    - `grep -c "human_verification\|deferral_pattern" .planning/phases/57-government-api-clients/57-VERIFICATION.md` ≥ 2
    - The frontmatter `fix_commits[]` block contains at least 3 entries with `hash:` fields populated by real 40-char SHAs (not placeholder text)
    - The body contains a `## Re-run Evidence` section with the 4 captured command outputs
    - The body's truth table contains at least 16 numbered rows mapping to the truths enumerated in the template
  </acceptance_criteria>
  <done>57-VERIFICATION.md is written with status determined by re-run evidence, every Phase 57 truth mapped to a passing test ID or explicitly deferred, and fix_commits[] anchored to real Plan 66-01..03 commit SHAs.</done>
</task>

<task type="auto">
  <name>Task 2: Atomic commit docs(66-04) + flip Phase 57 manager flag</name>
  <files>
    .planning/phases/57-government-api-clients/57-VERIFICATION.md
  </files>
  <read_first>
    .planning/phases/66-phase-57-completion-verification/66-CONTEXT.md
  </read_first>
  <action>
Per CONTEXT.md decisions D-18, D-19, D-20:

**Step 1: Confirm pre-flight conditions.**

1. `git status` shows ONLY `.planning/phases/57-government-api-clients/57-VERIFICATION.md` as new/modified.
2. The doc's `status:` is `verified`. If it's `gaps_found`, STOP — commit the doc but DO NOT run the manager-flag flip; surface the gap as a STATE.md Pending Todo via:
   ```
   gsd-sdk query state ... # if a state-todo write subcommand exists; otherwise edit STATE.md by hand
   ```
   and exit Task 2.
3. The doc cites at least 3 distinct fix commit SHAs (Plans 66-01, 66-02, 66-03).

**Step 2: Atomic commit.**

```
git add .planning/phases/57-government-api-clients/57-VERIFICATION.md
git commit
```

Use HEREDOC body:

```
docs(66-04): produce 57-VERIFICATION.md and close Phase 57

Phase 57 (Government API Clients) reached VALIDATION.md status: approved
on 2026-04-13 but never produced a goal-backward VERIFICATION.md. This
commit closes that gap: every PAY-02..05 truth is mapped to at least one
passing test ID, the 9 (sub-)scenarios from Plan 57-04 Task 3 are either
automated (linked) or enumerated in human_verification[] (a11y per Phase
66 D-12, HMRC live sandbox per D-13/14), and the re-run evidence section
captures the trailing summary line of all 5 closure-acceptance commands.

Frontmatter mirrors 63-VERIFICATION.md shape (re_verification block with
fix_commits[] anchored to the actual Phase 66 commit SHAs).

Refs: .planning/phases/66-phase-57-completion-verification/66-CONTEXT.md
      decisions D-15, D-16, D-17, D-18, D-19; plans 66-01, 66-02, 66-03.
```

Do NOT use `--no-verify`. If a pre-commit hook fails, fix the underlying issue and create a NEW commit.

**Step 3: Run the manager-flag flip via gsd-sdk.**

```bash
gsd-sdk query phase complete 57 --raw
```

Expected output: a JSON blob with `updated: true` (or equivalent success indicator). Capture the JSON for the SUMMARY.md.

If the output is `updated: false` with a reason field:

- If the reason is "no SUMMARY.md for plan 57-04" or similar prerequisite failure → STOP and report. Phase 57 already has 57-04-SUMMARY.md (verified during Task 1 read_first), so this would indicate a tooling drift — surface it.
- If the reason is "phase already complete" → benign; the flip is idempotent. Note in SUMMARY.md and proceed.
- For any other reason → STOP and surface as a STATE.md Pending Todo.

**Step 4: Confirm post-state.**

```bash
# Phase 57 should now show as complete in roadmap analysis
gsd-sdk query roadmap analyze --raw 2>&1 | head -20
# STATE.md last_updated should be today
grep "last_updated" .planning/STATE.md | head -1
```

If `gsd-sdk query phase complete 57` modified `.planning/STATE.md` or `.planning/ROADMAP.md`, those changes should be in `git status` as a follow-up. Per CONTEXT.md D-19 each unit of work is atomic; the STATE/ROADMAP edits made by gsd-sdk count as a SEPARATE commit:

```bash
# Inspect what gsd-sdk modified
git status

# If STATE.md and/or ROADMAP.md changed:
git add .planning/STATE.md .planning/ROADMAP.md
git commit -m "$(cat <<'EOF'
chore(66-04): mark Phase 57 complete in STATE.md / ROADMAP.md

Idempotent state flip via `gsd-sdk query phase complete 57` after
57-VERIFICATION.md commits with status: verified. Closes the Phase 57
roadmap entry; STATE.md last_updated refreshes to today.
EOF
)"
```

If gsd-sdk made no file changes (e.g. it only emits JSON to stdout), no second commit is needed.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && git log -2 --pretty=format:'%s' | grep -E '^docs\(66-04\)'</automated>
  </verify>
  <acceptance_criteria>
    - The HEAD or HEAD~1 commit subject matches the regex `^docs\(66-04\):`
    - That commit's diff includes ONLY `.planning/phases/57-government-api-clients/57-VERIFICATION.md` (the file is the artifact)
    - `gsd-sdk query phase complete 57 --raw` exits 0 with `updated: true` OR with a benign "already complete" reason
    - If gsd-sdk modified STATE.md / ROADMAP.md, a follow-up `chore(66-04):` commit lands those edits as a separate atomic commit (does NOT mix the doc commit with the state flip)
  </acceptance_criteria>
  <done>57-VERIFICATION.md is committed; Phase 57 is closed in STATE.md / ROADMAP.md; the manager dashboard reflects Phase 57 as fully verified. Phase 66 itself is now ready for its own SUMMARY/closure.</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. `test -f .planning/phases/57-government-api-clients/57-VERIFICATION.md` → exit 0
2. `head -1 .planning/phases/57-government-api-clients/57-VERIFICATION.md` → `---`
3. `grep -c "PAY-0[2345]" .planning/phases/57-government-api-clients/57-VERIFICATION.md` → ≥ 4
4. `grep "status:" .planning/phases/57-government-api-clients/57-VERIFICATION.md | head -1` → `status: verified` (or `status: gaps_found` if the re-run surfaced a regression)
5. `git log -3 --pretty=format:'%s' | grep -c "(66-0"` → ≥ 1 (this plan's commit)
6. `gsd-sdk query phase complete 57 --raw 2>&1` → JSON output containing `"updated"` key (true or false with benign reason)
7. `cd packages/api && npx tsc --noEmit` → still exit 0 (no type-system regressions from any Phase 66 commit)
</verification>
</content>
</invoke>