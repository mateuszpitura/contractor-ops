---
phase: 63-uk-payments-financial-features
verified: 2026-04-26T03:00:00Z
status: verified
score: 16/16 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 13/16
  gaps_closed:
    - "Late interest tRPC router (6 procedures) compiles and routes correctly (Plan 05 — CR-01 + FAILED_PRECONDITION typo + Decimal import + ctx.db narrowing all fixed)"
    - "Skonto eligibility service + router calculate correct amounts (Plan 06 — invoice.amountMinor → totalMinor, skontoTerm → skontoTerms[0], billingProfile → billingProfiles[0] with isDefault filter)"
    - "Late-payment-interest service Decimal-import path (TS2307 closed — replaced with structural RateValue alias)"
    - "v5.0-audit gap B-01 / PAY-07 (Skonto basis): skonto.evaluateForInvoice now reads invoice.amountToPayMinor (matches payment.applySkontoToItem) — closed by Phase 65 Plan 01 commit 599d2534. Locking regression test in routers/__tests__/skonto.test.ts uses a withholding fixture (totalMinor != amountToPayMinor) so the fix cannot regress silently."
    - "v5.0-audit gap B-05 / PAY-06 (LPCDA daysOverdue): late-payment-interest service now computes daysOverdue from overdueStartMs with inclusive-elapsed semantics per LPCDA Section 4(1) — closed by Phase 65 Plan 02 commit d5428ccf. Two boundary regression tests + 6 realigned existing assertions lock the corrected formula."
  gaps_remaining: []
  regressions: []
  phase_65_addendum:
    note: "Phase 65 ('phase-63-critical-bug-fixes') closed two v5.0-MILESTONE-AUDIT findings that were raised after this verification doc's previous iteration. Fix commits 599d2534 (B-01) and d5428ccf (B-05) land on branch v2. No regressions introduced. Per Phase 65 CONTEXT.md D-12 this re-verification flips the doc from `gaps_found` → `verified`; per D-13 a separate 65-VERIFICATION.md confirms the closure."
  fix_commits:
    - hash: "929b0e1d"
      scope: "fix(63-05): repair late-interest router type errors"
      files:
        - "packages/api/src/routers/late-payment-interest.ts"
      changes:
        - "6× requireFeatureFlag('PAY_LATE_INTEREST_ENABLED') → 'payments.late-interest-enabled'"
        - "2× TRPCError code 'FAILED_PRECONDITION' → 'PRECONDITION_FAILED'"
        - "3× loadBoeRateHistory(ctx.db) narrowed via ctx.db as unknown as Pick<PrismaClient, 'boEBaseRateHistory'>"
    - hash: "ece79f07"
      scope: "fix(63-05): drop Decimal type import with non-existent runtime path"
      files:
        - "packages/api/src/services/late-payment-interest.ts"
      changes:
        - "Removed import of Decimal from non-existent '@contractor-ops/db/generated/prisma/client/runtime/library'"
        - "Replaced with structural RateValue = number | { toNumber(): number } | string"
    - hash: "415794cb"
      scope: "fix(63-06): align skonto router with canonical schema relation names"
      files:
        - "packages/api/src/routers/skonto.ts"
      changes:
        - "invoice.skontoTerm → invoice.skontoTerms[0] (4 sites)"
        - "contractor.billingProfile → contractor.billingProfiles[0] with where: { isDefault: true }, take: 1"
        - "billingProfile.skontoTerm → billingProfiles[0].skontoTerms[0]"
        - "invoice.amountMinor → invoice.totalMinor (Skonto basis amount)"
        - "Test mocks updated to match canonical schema shape"
human_verification:
  - test: "Plan 07 Task 3 manual UI checklist (9 steps from VALIDATION.md 63-07-03)"
    expected: "Sequentially exercise: (1) /settings/payments/ BACS submitter admin gate; (2) PaymentRun GBP BACS preview + warnings + .txt download; (3) GB B2B invoice late-interest card ACCRUING state, claim dialog, waive dialog (10-char min reason), claim letter PDF; (4) dashboard 'Overdue receivables (UK)' tile for GB org; (5) DE invoice Skonto section with cascade preview + German locked phrase; (6) DE invoice Skonto banner eligibility state; (7) PaymentRun DE Skonto checkboxes within discount window; (8) /admin/boe-rate/ table + dialogs + poller status strip + non-super-admin 403; (9) toggle all 3 payments.* feature flags off and verify clean disappearance of all surfaces."
    why_human: "Visual rendering, dialog flows, PDF inspection, multi-tenant role gating, R2 signed-URL behaviour, and feature-flag-toggle UX cannot be reliably grep-verified. By design per Plan 07 Task 3 — Nyquist-compliant since this is the only manual checkpoint in the phase."

  - test: "BACS export end-to-end byte-level inspection on a GBP PaymentRun"
    expected: "After enabling 'payments.bacs-enabled': configure BACS submitter (SUN, sort code, account number, name); open a LOCKED PaymentRun with GBP items; preview BACS Std 18 file and verify VOL/HDR/UHL/detail/EOF/UTL records, 106-character detail records, transliteration warnings appear for European names, modulus check warnings appear for known-flagged sort codes, download produces a .txt file with CR/LF endings; download blocked when unmappable Unicode chars present (replaced.length > 0 check at server line 366 + client line 118); EUR runs rejected at router boundary with PRECONDITION_FAILED."
    why_human: "File-format byte-level inspection + UI interaction flow + R2 signed-URL behaviour can only be exercised against a running server with seeded data."

  - test: "Sort-code modulus validation on the contractor billing profile UK section (race-condition probe)"
    expected: "Open a GB contractor's billing profile, scroll to UK Bank Fields, enter 089999/66374958 (a known building-society edge case from VocaLink V8.40); validate sort code; verify pill is correctly tinted (VALID/WARN/INVALID) and shows the right reason; rapid-click the validate button 5+ times and verify only the most recent response wins (WR-07 ratchet)."
    why_human: "Visual pill tinting + rapid-click race exposure cannot be reliably grep-verified."

  - test: "BoE rate poller cron route handshake (auth gate + flag short-circuit + manual-edit preservation)"
    expected: "Send GET /api/cron/boe-rate-poll without Authorization → 401; with bogus bearer → 401; with CRON_SECRET bearer and 'payments.late-interest-enabled' flag OFF → 200 { skipped: true }; with flag ON → invokes pollBoeBaseRate(); verify a fresh BoEBaseRateHistory row is created only when the BoE-published effectiveFrom is new; verify a manual admin-entered row for the same effectiveFrom is preserved (WR-03 fix — poller line 359 uses findUnique-then-create-only pattern)."
    why_human: "Cron secret + flag toggle + DB inspection sequence requires runtime."

  - test: "Late payment interest card on a GB B2B GBP overdue invoice (claim PDF + waiver flow)"
    expected: "Navigate to a GB B2B overdue GBP invoice detail page; verify the card shows the ACCRUING state with: principal outstanding, days overdue, BoE rate from the statutory reference date + 8%, daily accrual, fixed compensation tier (£40/£70/£100), total claim. Test 'Claim statutory interest' → opens dialog → confirm creates InvoiceInterestClaim row + claim PDF in R2 (PDF includes LPCDA_CLAIM_FOOTER + LPCDA_SECTION_REF locked phrases). Test 'Waive interest' → AlertDialog → reason min 10 chars → creates InvoiceInterestWaiver."
    why_human: "Card render + dialog interaction + R2 PDF byte-level inspection requires seeded data + running server."

  - test: "Skonto banner + XRechnung BG-20 round-trip on a DE invoice (canonical-schema runtime check)"
    expected: "Configure a DE invoice with Skonto term (3% / 7 days / net 30); verify the eligibility banner shows ELIGIBLE within the discount window with discountAmountMinor = floor(totalMinor × 0.03); generate XRechnung via the existing einvoice flow; inspect the CII XML and confirm BG-20 ram:Description contains '#SKONTO#TAGE=7#PROZENT=3.00#BASISBETRAG={total}#'."
    why_human: "Visual banner state + KoSIT-validator pass against generated XML can only be exercised against the running einvoice generator with seeded DE invoice data. Canonical schema relations (skontoTerms[0], billingProfiles[0]) now compile and pass tests, but the runtime round-trip needs human confirmation."
---

# Phase 63: UK Payments & Financial Features Verification Report

**Phase Goal:** Users can export UK contractor payments via BACS Standard 18, see statutory late payment interest on overdue UK invoices, and configure German early payment discounts (per ROADMAP.md Phase 63 Success Criteria 1-3).

**Verified:** 2026-04-26T00:15:00Z
**Status:** human_needed
**Re-verification:** Yes — third pass after the type-error remediation commits (929b0e1d, ece79f07, 415794cb). All 3 code-level gaps from the previous report are closed. Only the by-design Plan 07 Task 3 manual UI checklist + 5 runtime spot-checks remain.

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                              | Status     | Evidence                                                                                                                                                                                                                                                                                                                              |
| --- | ------------------------------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | All 8 Prisma models exist (InvoicePayment, InvoiceInterestCompensation, InvoiceInterestWaiver, InvoiceInterestClaim, BoEBaseRateHistory, SkontoTerm, SkontoSnapshot, SkontoApplication) | ✓ VERIFIED | `grep "model X" packages/db/prisma/schema/{invoice,financial}.prisma` confirms 4 invoice + 4 financial models |
| 2   | PaymentExportFormat enum includes BACS_STD18                                                                       | ✓ VERIFIED | grep on payment.prisma confirms |
| 3   | BACS sort code, account number, SUN, submitter-name Zod validators with VocaLink modulus check exist               | ✓ VERIFIED | packages/validators/src/bacs.ts + bacs-modulus-tables.ts; 779 validator tests pass |
| 4   | GB locked statutory phrases exist for LPCDA claim letter footer (LPCDA_CLAIM_FOOTER, LPCDA_SECTION_REF, etc.)      | ✓ VERIFIED | packages/validators/src/legal/gb.ts; locked-phrases-guard.test.ts has Phase 63 GB describe block |
| 5   | DE locked Skonto phrase (SKONTO_DESCRIPTION_TEMPLATE_DE) exists with CI guard coverage and parity-mirrored to einvoice constants | ✓ VERIFIED | packages/validators/src/legal/de.ts + locked-phrase-parity.test.ts |
| 6   | Three payments.* feature flags are registered in the flag registry                                                 | ✓ VERIFIED | packages/feature-flags/src/registry.ts: payments.bacs-enabled, payments.late-interest-enabled, payments.skonto-enabled |
| 7   | BoE base rate seed data covers 21 entries (2021-01-01 → 2025-11-06)                                                | ✓ VERIFIED | boe-base-rate-history.json: 21 entries |
| 8   | generateBacsStandard18 produces valid fixed-width BACS Std 18 file (8-record structure, 106-char detail records, CR/LF endings) | ✓ VERIFIED | packages/api/src/services/payment-export.ts (667 lines) exports generateBacsStandard18, BacsExportItem, BacsOrgBankInfo; transliteration applied via shared transliterateToBacs |
| 9   | Format auto-detection routes GBP + UK account to BACS_STD18                                                        | ✓ VERIFIED | packages/api/src/services/payment-format-detection.ts: ExportFormat union extended with BACS_STD18; detectFormatForDestination checks `currency === 'GBP' && destination.ukSortCodeEncrypted && destination.ukAccountNumberEncrypted` BEFORE IBAN rules (per D-04) |
| 10  | BACS UI is complete and gated behind payments.bacs-enabled feature flag (settings page, preview, download, modulus + transliteration warnings, UK bank fields on billing profile) | ✓ VERIFIED | apps/web/src/app/[locale]/(dashboard)/settings/payments/page.tsx + 7 components in payments/bacs/ + 2 components in contractors/billing-profile/. bacsRouter wired at packages/api/src/root.ts. CR-01/CR-02 + WR-01..WR-08 all confirmed. |
| 11  | BoE rate poller fetches IUDBEDR series and upserts on rate change                                                  | ✓ VERIFIED | packages/integrations/src/services/boe-base-rate-poller.ts (399 lines): BOE_CSV_URL with IUDBEDR series code; CSV parser; WR-02 effectiveFrom keyed on latest.date (line 349); WR-03 insert-only manual-override safety (line 359 uses findUnique-then-create); cron route at apps/web/src/app/api/cron/boe-rate-poll/route.ts uses 'payments.late-interest-enabled' |
| 12  | Late interest tRPC router (6 procedures) compiles and routes correctly                                             | ✓ VERIFIED — newly closed | tsc --noEmit reports ZERO errors in late-payment-interest.ts. All 6 procedures use `requireFeatureFlag('payments.late-interest-enabled')` (lines 49, 178, 283, 343, 388, 567). Two TRPCError codes corrected to `'PRECONDITION_FAILED'` (lines 462, 469). Three `loadBoeRateHistory(ctx.db)` calls narrowed via `ctx.db as unknown as Pick<PrismaClient, 'boEBaseRateHistory'>` (lines 75-77, 222-224, 433-435). 88 router tests pass — now without type-system mock-only coverage. Commits: 929b0e1d. |
| 13  | Skonto eligibility service + router calculate correct amounts                                                       | ✓ VERIFIED — newly closed | tsc --noEmit reports ZERO errors in skonto.ts. `invoice.totalMinor` (line 280) — correct field. `invoice.skontoTerms[0]` (line 258) — correct array access. `invoice.contractor?.billingProfiles?.[0]` with `where: { isDefault: true }, take: 1` (lines 232-237, 267) — correct cardinality. `defaultProfile?.skontoTerms?.[0]` (line 268) — correct nested access. evaluateForInvoice now resolves real effective terms against canonical schema. Commit: 415794cb. |
| 14  | Admin BoE rate CRUD is gated with valid permission resource                                                        | ✓ VERIFIED | packages/auth/src/permissions.ts: 'admin:boe-rate': ['read', 'write'] registered in accessControlStatement. admin-boe-rate.ts uses requirePermission({ 'admin:boe-rate': ['write'] }) on 4 procedures (list, insert, update, delete) without TS errors. |
| 15  | calculateLateInterest + resolveStatutoryRate + getCompensationTier pure functions are substantive (LPCDA-compliant: statutory period rate, simple interest, compensation tiers, partial payments, waivers) | ✓ VERIFIED | packages/api/src/services/late-payment-interest.ts (257 lines) exports all 3 functions with full LPCDA Section 4(1) reference-date logic. Decimal-import path replaced with structural `RateValue = number \| { toNumber(): number } \| string` (line 20). ZERO tsc errors in service file. Commit: ece79f07. |
| 16  | Late interest UI card + Skonto UI surfaces exist and call correct tRPC endpoints                                   | ✓ VERIFIED | All 12 Plan 07 components exist; late-interest-card calls trpc.latePaymentInterest.getForInvoice; skonto-banner calls trpc.skonto.evaluateForInvoice; overdue-receivables-tile calls trpc.latePaymentInterest.getForOrg; skonto-apply-checkbox calls trpc.payment.applySkontoToItem. Backing routers now compile cleanly so UI data flow is real, not mocked. |

**Score:** 16/16 truths verified (up from 13/16). +3 closed this iteration: #12, #13, #15. ZERO failed truths. The only remaining work is the by-design manual UI checkpoint (Plan 07 Task 3) plus 5 runtime spot-checks for byte-level / dialog / round-trip behaviour.

### Required Artifacts

| Artifact                                                                                          | Plan  | Status        | Details                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------- | ----- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/db/prisma/schema/financial.prisma`                                                      | 01    | ✓ VERIFIED    | 4 models: BoEBaseRateHistory, SkontoTerm, SkontoSnapshot, SkontoApplication                                                                                   |
| `packages/db/prisma/schema/invoice.prisma`                                                        | 01    | ✓ VERIFIED    | 4 models added; back-relations skontoTerms (plural array), payments, interestCompensation, interestWaivers, interestClaims present                            |
| `packages/db/prisma/schema/payment.prisma`                                                        | 01    | ✓ VERIFIED    | BACS_STD18 in PaymentExportFormat enum                                                                                                                        |
| `packages/db/prisma/schema/contractor.prisma` + `organization.prisma`                             | 01    | ✓ VERIFIED    | UK encrypted bank fields on billing profile (UK fields), BACS submitter fields on Organization, billingProfiles[] relation                                    |
| `packages/validators/src/bacs.ts` + `bacs-modulus-tables.ts`                                      | 01    | ✓ VERIFIED    | sortCodeSchema, accountNumberSchema, modulusCheck, VOCALINK_MODULUS_TABLE_V840 exported                                                                       |
| `packages/validators/src/legal/gb.ts` + `de.ts`                                                   | 01    | ✓ VERIFIED    | LPCDA_CLAIM_FOOTER, LPCDA_SECTION_REF, SKONTO_DESCRIPTION_TEMPLATE_DE exported; locked-phrases-guard tests pass                                              |
| `packages/feature-flags/src/registry.ts`                                                          | 01    | ✓ VERIFIED    | payments.bacs-enabled, payments.late-interest-enabled, payments.skonto-enabled                                                                                |
| `packages/db/prisma/seed-data/boe-base-rate-history.json`                                         | 01    | ✓ VERIFIED    | 21 entries                                                                                                                                                    |
| `packages/shared/src/ascii-transliterate.ts` + `ascii-transliterate-table.ts`                     | 02    | ✓ VERIFIED    | transliterateToBacs exported; covers German/Polish/French/Nordic/Italian-Spanish; 42 shared tests pass                                                       |
| `packages/api/src/services/payment-export.ts`                                                     | 02    | ✓ VERIFIED    | generateBacsStandard18, BacsExportItem, BacsOrgBankInfo present; transliterateToBacs imported; uses createLogger (no console.*)                              |
| `packages/api/src/services/payment-format-detection.ts`                                           | 02    | ✓ VERIFIED    | ExportFormat = 'SEPA_XML' \| 'SWIFT_XML' \| 'BANK_FILE' \| 'CSV' \| 'BACS_STD18'; detectFormatForDestination routes GBP+UK before IBAN                       |
| `packages/api/src/services/late-payment-interest.ts`                                              | 03/05 | ✓ VERIFIED — newly closed | calculateLateInterest, resolveStatutoryRate, getCompensationTier present and substantive. Decimal-import path issue (TS2307) resolved by structural `RateValue` alias on line 20. ZERO tsc errors. |
| `packages/integrations/src/services/boe-base-rate-poller.ts`                                      | 03    | ✓ VERIFIED    | pollBoeBaseRate, BOE_CSV_URL, IUDBEDR parsing, WR-02 effectiveFrom-from-CSV, WR-03 insert-only-on-existing                                                   |
| `apps/web/src/app/api/cron/boe-rate-poll/route.ts`                                                | 03    | ✓ VERIFIED    | CRON_SECRET bearer auth, 'payments.late-interest-enabled' flag short-circuit, Sentry.withMonitor + withCronMonitor wrapping                                  |
| `packages/api/src/routers/bacs.ts`                                                                | 04    | ✓ VERIFIED    | 5 procedures: getSubmitterMasks, previewExport, generateExport, validateSortCode, saveSubmitterConfig. Wired into root.ts. 14 router tests pass.              |
| `apps/web/src/app/[locale]/(dashboard)/settings/payments/page.tsx`                                | 04    | ✓ VERIFIED    | RSC page with permission gate; renders BacsSubmitterForm                                                                                                      |
| 7× `apps/web/src/components/payments/bacs/*.tsx` + 2× `contractors/billing-profile/*.tsx` (UK)    | 04    | ✓ VERIFIED    | All 9 components present; bacs-preview-pre uses ScrollArea + role="region" + aria-label; transliteration banner uses AlertTriangle/AlertOctagon              |
| `packages/api/src/routers/late-payment-interest.ts`                                               | 05    | ✓ VERIFIED — newly closed | 6 procedures with canonical 'payments.late-interest-enabled' flag key. 'PRECONDITION_FAILED' codes. ctx.db narrowing via Pick<PrismaClient, 'boEBaseRateHistory'>. ZERO tsc errors. |
| `packages/api/src/routers/admin-boe-rate.ts`                                                      | 05    | ✓ VERIFIED    | 4 CRUD procedures with 'admin:boe-rate' permission resource                                                                                                   |
| `packages/api/src/pdf-templates/late-payment-claim.tsx`                                           | 05    | ✓ VERIFIED    | React-PDF template references LPCDA_CLAIM_FOOTER and LPCDA_SECTION_REF locked phrases                                                                         |
| `apps/web/src/app/admin/boe-rate/page.tsx` + 4 dialog components + poller-status-strip            | 05    | ✓ VERIFIED    | Admin BoE rate page wired to adminBoeRate.list / insert / update / delete; super-admin gate via /admin/layout.tsx                                             |
| `packages/api/src/services/skonto.ts`                                                             | 06    | ✓ VERIFIED    | evaluateSkontoEligibility + resolveSkontoTerm pure functions; 8 service tests pass                                                                            |
| `packages/api/src/routers/skonto.ts`                                                              | 06    | ✓ VERIFIED — newly closed | 5 procedures with canonical schema relations: invoice.skontoTerms[0], contractor.billingProfiles[0] (isDefault filtered), billingProfiles[0].skontoTerms[0]. invoice.totalMinor as Skonto basis. ZERO tsc errors. |
| `packages/einvoice/src/profiles/xrechnung-de/generator.ts`                                        | 06    | ✓ VERIFIED    | #SKONTO#TAGE= structured syntax in BG-20 block; uses mirrored XRECHNUNG_SKONTO_DESCRIPTION_TEMPLATE; 58 einvoice generator tests pass                          |
| 12× Plan 07 UI components (late-interest, skonto, dashboard tile, invoice-table columns + filters, payments/run/skonto-apply-checkbox) | 07 | ✓ VERIFIED | All present and call correct tRPC endpoints; backing routers compile so data flow is real, not mocked-only |

### Key Link Verification

| From                                                                                          | To                                                                                  | Via                                                  | Status         | Details                                                                                                                                          |
| --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ---------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| packages/api/src/services/payment-export.ts                                                   | packages/shared/src/ascii-transliterate.ts                                          | import transliterateToBacs                           | ✓ WIRED        | `import { minorToDecimalStr, transliterateToBacs } from '@contractor-ops/shared';`                                                                |
| packages/api/src/services/payment-format-detection.ts                                         | PaymentExportFormat.BACS_STD18                                                      | BACS_STD18 routing branch                            | ✓ WIRED        | ExportFormat extended; detectFormatForDestination checks GBP+UK before IBAN                                                                      |
| packages/api/src/routers/bacs.ts                                                              | packages/api/src/services/payment-export.ts                                         | import generateBacsStandard18                        | ✓ WIRED        | bacs.ts (626 lines); imports generateBacsStandard18 + BacsExportItem + BacsOrgBankInfo                                                            |
| packages/api/src/routers/bacs.ts                                                              | packages/api/src/root.ts                                                            | bacs: bacsRouter                                     | ✓ WIRED        | root.ts: `bacs: bacsRouter`                                                                                                                      |
| apps/web/src/app/api/cron/boe-rate-poll/route.ts                                              | packages/integrations/src/services/boe-base-rate-poller.ts                          | import pollBoeBaseRate                               | ✓ WIRED        | route.ts: `import { pollBoeBaseRate } from '@contractor-ops/integrations/services/boe-base-rate-poller';`                                        |
| packages/api/src/routers/late-payment-interest.ts                                             | packages/api/src/services/late-payment-interest.ts                                  | import calculateLateInterest, getCompensationTier   | ✓ WIRED        | Imports correct; ZERO tsc errors; 88 router tests pass                                                                                            |
| packages/api/src/routers/late-payment-interest.ts                                             | packages/api/src/services/boe-rate-cache.ts                                         | import loadBoeRateHistory + ctx.db Pick adapter      | ✓ WIRED        | 3× call sites use `ctx.db as unknown as Pick<PrismaClient, 'boEBaseRateHistory'>` (lines 75-77, 222-224, 433-435) — same WR-04 pattern as bacs.ts |
| packages/api/src/routers/late-payment-interest.ts                                             | packages/api/src/root.ts                                                            | latePaymentInterest: latePaymentInterestRouter       | ✓ WIRED        | root.ts                                                                                                                                          |
| packages/api/src/routers/skonto.ts                                                            | packages/api/src/services/skonto.ts                                                 | import evaluateSkontoEligibility, resolveSkontoTerm  | ✓ WIRED        | Imports correct; ZERO tsc errors                                                                                                                  |
| packages/api/src/routers/skonto.ts                                                            | packages/api/src/root.ts                                                            | skonto: skontoRouter                                 | ✓ WIRED        | root.ts                                                                                                                                          |
| packages/einvoice/src/profiles/xrechnung-de/generator.ts                                      | packages/validators/src/legal/de.ts                                                 | XRECHNUNG_SKONTO_DESCRIPTION_TEMPLATE (mirrored)     | ✓ WIRED        | Uses mirrored constant in einvoice constants.ts; locked-phrase-parity.test.ts guards drift                                                       |
| apps/web/src/components/invoices/late-interest/late-interest-card.tsx                         | packages/api/src/routers/late-payment-interest.ts                                   | trpc.latePaymentInterest.getForInvoice               | ✓ WIRED        | UI correctly wired AND backing router compiles cleanly                                                                                            |
| apps/web/src/components/invoices/skonto/skonto-banner.tsx                                     | packages/api/src/routers/skonto.ts                                                  | trpc.skonto.evaluateForInvoice                       | ✓ WIRED        | UI correctly wired; runtime data flow now resolves real effective terms (canonical schema relations)                                              |
| apps/web/src/components/dashboard/overdue-receivables-tile.tsx                                | packages/api/src/routers/late-payment-interest.ts                                   | trpc.latePaymentInterest.getForOrg                   | ✓ WIRED        | UI correctly wired; backing router compiles                                                                                                      |
| apps/web/src/components/payments/run/skonto-apply-checkbox.tsx                                | packages/api/src/routers/payment.ts                                                 | trpc.payment.applySkontoToItem                       | ✓ WIRED        | UI correctly wired; payment router pulls from skonto.ts which now compiles                                                                        |

### Data-Flow Trace (Level 4)

| Artifact                                                                | Data Variable                                | Source                                                                                          | Produces Real Data?                                                                                       | Status        |
| ----------------------------------------------------------------------- | -------------------------------------------- | ----------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ------------- |
| apps/web/src/components/invoices/late-interest/late-interest-card.tsx   | latePaymentInterest.getForInvoice result     | router → ctx.db.invoice.findFirst + loadBoeRateHistory(ctx.db) → calculateLateInterest          | Real DB queries; ctx.db narrowed via Pick adapter; calculation logic correct                              | ✓ FLOWING     |
| apps/web/src/components/dashboard/overdue-receivables-tile.tsx          | latePaymentInterest.getForOrg result         | router → ctx.db.invoice.findMany (GBP, dueDate < now, not PAID, GB B2B contractor)              | Real DB query; rate history cached and shared                                                              | ✓ FLOWING     |
| apps/web/src/components/admin/boe-rate/boe-rate-table.tsx               | adminBoeRate.list result                     | router → ctx.db.boEBaseRateHistory.findMany                                                     | Real DB query                                                                                             | ✓ FLOWING     |
| apps/web/src/components/invoices/skonto/skonto-banner.tsx               | skonto.evaluateForInvoice result             | router → ctx.db.invoice.findFirst with canonical include shape → resolveSkontoTerm cascade      | Real DB query; invoice.skontoTerms[0] + contractor.billingProfiles[0].skontoTerms[0] cascade now resolves real effectiveTerm against canonical schema | ✓ FLOWING     |
| apps/web/src/components/payments/bacs/bacs-preview-card.tsx             | bacs.previewExport result                    | router → loadDecryptedSubmitterConfig + loadRunWithBacsItems → generateBacsStandard18           | Real DB queries + R2 + BACS file generation                                                               | ✓ FLOWING     |
| apps/web/src/components/payments/bacs/bacs-submitter-form.tsx           | bacs.getSubmitterMasks result                | router → ctx.db.organization.findUnique                                                         | Real DB query; WR-06 throws NOT_FOUND on null org                                                         | ✓ FLOWING     |

### Behavioral Spot-Checks

| Behavior                                                            | Command                                                                                                                                            | Result                                                          | Status |
| ------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ------ |
| Phase-63 router + service tests pass under canonical schema         | `pnpm --filter @contractor-ops/api exec vitest run src/routers/__tests__/late-payment-interest.test.ts src/routers/__tests__/skonto.test.ts src/routers/__tests__/bacs.test.ts src/services/__tests__/payment-export.test.ts src/services/__tests__/payment-format-detection.test.ts src/services/__tests__/late-payment-interest.test.ts` | 6 files / 201 tests passed; mocks updated to canonical SkontoTerm[] / billingProfiles[] cardinality | ✓ PASS |
| ZERO tsc errors in phase-63 router/service files                    | `pnpm --filter @contractor-ops/api exec tsc --noEmit 2>&1 \| grep -cE "(late-payment-interest\|skonto\|admin-boe-rate)"`                              | 0 errors                                                        | ✓ PASS |
| API package compile baseline noise unchanged                        | `pnpm --filter @contractor-ops/api exec tsc --noEmit 2>&1 \| grep -cE "^src/"`                                                                       | 32 errors total — all in unrelated files (workflow-execution, billing-webhook, calendar-*, courier-*, equipment-workflow, jira/linear-webhook-handler, notification-service, privacy-notice, email-templates). NONE in phase-63 surface area. | ✓ PASS — phase-63 introduces zero compile errors |
| Search for stale anti-pattern strings                               | `grep -nE "PAY_LATE_INTEREST_ENABLED\|FAILED_PRECONDITION\|invoice\.amountMinor\|invoice\.skontoTerm[^s]\|contractor\.billingProfile[^s]\|runtime/library" packages/api/src/routers/late-payment-interest.ts packages/api/src/routers/skonto.ts packages/api/src/services/late-payment-interest.ts` | No matches — all 6 anti-pattern token classes scrubbed             | ✓ PASS |
| Search for canonical replacement strings                            | `grep -nE "payments\.late-interest-enabled\|PRECONDITION_FAILED\|invoice\.totalMinor\|invoice\.skontoTerms\|billingProfiles" packages/api/src/routers/late-payment-interest.ts packages/api/src/routers/skonto.ts` | 6× canonical flag key + 2× PRECONDITION_FAILED + 5× invoice.totalMinor + invoice.skontoTerms[0] + contractor.billingProfiles[0] all present | ✓ PASS |
| Fix commits exist on current branch                                 | `git log --oneline -- packages/api/src/routers/late-payment-interest.ts packages/api/src/routers/skonto.ts packages/api/src/services/late-payment-interest.ts`            | 415794cb, 929b0e1d, ece79f07 confirmed in history                | ✓ PASS |
| validators test suite passes                                        | `pnpm --filter @contractor-ops/validators exec vitest run`                                                                                          | 779 tests pass (incl. locked-phrases-guard Phase 63 + bacs validators) | ✓ PASS |
| einvoice generator tests pass (XRechnung BG-20 Skonto)              | `pnpm --filter @contractor-ops/einvoice exec vitest run xrechnung-de`                                                                              | 58 tests pass                                                   | ✓ PASS |
| ascii-transliterate test suite passes                               | `pnpm --filter @contractor-ops/shared run test -- --run ascii-transliterate`                                                                       | 42 tests pass                                                   | ✓ PASS |
| BoE poller cron handshake (auth gate, flag short-circuit, manual-edit preservation) | not runnable without server                                                                            | —                                                               | ? SKIP — routed to human verification |
| BACS file byte-level inspection on a real PaymentRun                | not runnable without seeded data + R2                                                                                                              | —                                                               | ? SKIP — routed to human verification |
| Plan 07 Task 3 manual UI checklist (9 steps)                        | not automatable per VALIDATION.md                                                                                                                  | —                                                               | ? SKIP — routed to human verification |

### Requirements Coverage

| Requirement | Source Plans                  | Description                                                                                                                                | Status     | Evidence                                                                                                                                                                                                                                                  |
| ----------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PAY-01      | 63-01, 63-02, 63-04, 63-07    | BACS Standard 18 Direct Credit file export with sort code validation and ASCII transliteration                                             | ✓ SATISFIED — pending human checklist | Schema+validators (Plan 01), generateBacsStandard18 (Plan 02), bacs router + UI (Plan 04), invoice-list overdue+skonto columns (Plan 07) all present, wired, and compile cleanly. End-to-end requires human run-through of preview + download (see human_verification #2 + #1 step 2). |
| PAY-06      | 63-01, 63-03, 63-05, 63-07    | Statutory late payment interest per LPCDA with BoE rate + 8% + compensation tiers                                                          | ✓ SATISFIED — pending human checklist | Service logic + UI cards + admin BoE rate CRUD + BoE poller all present and now compile cleanly (commits 929b0e1d + ece79f07 closed all 11 type errors that previously blocked production build). 88 router tests + 30 service tests pass. Runtime exercise of card + claim + waiver flow remains manual (see human_verification #5 + #1 step 3). |
| PAY-07      | 63-01, 63-06, 63-07           | Skonto early payment discount for German invoices with XRechnung BG-20 support                                                             | ✓ SATISFIED — pending human checklist | evaluateSkontoEligibility service correct; skonto router compiles against canonical schema (commit 415794cb closed all 9 type errors; canonical relations skontoTerms[0] / billingProfiles[0] now resolve real effective terms at runtime); XRechnung BG-20 emission correct; UI present. Runtime banner state + KoSIT round-trip remains manual (see human_verification #6 + #1 steps 5-7). |

All three requirement IDs declared across plan frontmatter are accounted for and their underlying code now compiles cleanly. No orphaned requirements (REQUIREMENTS.md confirms PAY-01, PAY-06, PAY-07 all map to Phase 63). Status flips from PENDING to SATISFIED for all three contingent on the manual UI checkpoint passing — the code-level contract is fully met.

### Anti-Patterns Found

| File                                                                     | Line(s)                       | Pattern                                                                                                            | Severity     | Impact                                                                                                                                                                                                                                                                                                |
| ------------------------------------------------------------------------ | ----------------------------- | ------------------------------------------------------------------------------------------------------------------ | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none in phase-63 source)                                                | —                             | —                                                                                                                  | —            | All 11 anti-patterns from the previous report are scrubbed: `'PAY_LATE_INTEREST_ENABLED'` (×6), `'FAILED_PRECONDITION'` (×2), `Decimal from runtime/library` (×1), `invoice.amountMinor`, `invoice.skontoTerm` (×4), `contractor.billingProfile` (×1), `loadBoeRateHistory(ctx.db)` narrowing (×3) — all resolved. |

### Human Verification Required

See structured `human_verification` items in frontmatter. Six items total:

1. **Plan 07 Task 3 manual UI checklist (9 sub-steps)** — by-design manual checkpoint. Per VALIDATION.md Nyquist analysis this is the only manual-only task in the entire phase (1-step contiguous manual span — well under the 3-step Nyquist threshold).
2. **BACS export end-to-end byte-level inspection** — needed to confirm CR-01 (replaced.length>0 unmappable-char guard) + CR-02 (currency==='GBP') + WR-01..WR-08 fixes manifest correctly through the full preview→warn→download flow against real R2.
3. **Sort-code modulus validation + WR-07 ratchet** — visual pill state + race-condition exposure.
4. **BoE rate poller cron handshake** — auth + flag-off short-circuit + manual-edit preservation.
5. **Late payment interest card + claim PDF + waiver dialog** — runtime exercise of the now-compiling router; verify PDF embeds LPCDA locked phrases.
6. **Skonto banner + XRechnung BG-20 round-trip** — runtime confirmation that the canonical-schema-aligned router resolves real effective terms; KoSIT validator pass on generated XML.

### Gaps Summary

**No code-level gaps remain.** All 16 must-haves verify and the 3 gaps from the previous report are closed:

1. **Late interest tRPC router (Plan 05) — CR-01 + FAILED_PRECONDITION + Decimal-import + ctx.db narrowing** → closed by commits 929b0e1d (router) and ece79f07 (service). All 11 phase-63 tsc errors in late-payment-interest.{ts,services.ts} are now zero.

2. **Skonto eligibility service + router (Plan 06) — wrong field name + wrong relation cardinality** → closed by commit 415794cb. invoice.skontoTerms[0], contractor.billingProfiles[0] (filtered isDefault, take 1), and invoice.totalMinor now align with the canonical Prisma schema. evaluateForInvoice resolves real effective terms at runtime instead of always falling through to NO_SKONTO_CONFIGURED. Test mocks updated to match canonical shape.

3. **Service-file Decimal import path (TS2307)** → closed via the structural `RateValue = number | { toNumber(): number } | string` alias on line 20 of services/late-payment-interest.ts. No hard dependency on the Prisma runtime/library subpath.

**Verification of the fix landings:** `pnpm --filter @contractor-ops/api exec tsc --noEmit 2>&1 | grep -cE "(late-payment-interest|skonto|admin-boe-rate)"` returns `0`. The 32 remaining tsc errors in the api package are all in unrelated files (workflow-execution, billing-webhook, calendar-*, courier-*, email-templates, equipment-workflow, jira/linear-webhook-handler, notification-service, privacy-notice). These are pre-existing baseline errors that predate this work — not phase-63 regressions.

**Pre-existing baseline noise (not phase 63 gaps):** the 57 `db.$queryRawUnsafe is not a function` test failures across `@contractor-ops/api` (KSeF / token-refresh / classification) predate this work and the failing test files are not in the phase-63 diff. Likewise the 32 unrelated tsc errors above. Per the user-specified carve-out, neither is counted against this phase.

**Outstanding work is the by-design manual UI checkpoint** (Plan 07 Task 3, plus 5 runtime spot-checks for byte-level BACS file inspection, modulus-validation race exposure, cron handshake, late-interest claim/waiver dialog flow, and Skonto + XRechnung round-trip). All six items live in the `human_verification` block and require a running server with seeded data. None of these are code gaps — they are the explicit manual-verification contract from VALIDATION.md `63-07-03` and Nyquist-compliant (1-step contiguous manual span).

**Status rationale:** Per the verification decision tree, when all truths are VERIFIED but the human verification section is non-empty, status MUST be `human_needed` (not `passed`). All 16/16 must-haves verify and no gaps remain — the only thing standing between this phase and `passed` is the human run-through of the 9-step UI checklist plus the 5 runtime confirmations. Once those land, the phase can flip to fully closed.

---

_Verified: 2026-04-26T00:15:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification iteration: 3_
_Score progression: 9/16 (initial) → 13/16 (post-orphan-cleanup) → 16/16 (post-type-error-remediation, this report)_
