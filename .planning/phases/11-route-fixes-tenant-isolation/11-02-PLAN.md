---
phase: 11-route-fixes-tenant-isolation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
gap_closure: true
requirements:
  - ORG-07

must_haves:
  truths:
    - "Tenant middleware wraps handler execution in tenantStore.run() with organizationId"
    - "AsyncLocalStorage context is available to Prisma extension for automatic tenant scoping"
  artifacts:
    - path: "packages/api/src/middleware/tenant.ts"
      provides: "Tenant middleware with AsyncLocalStorage wiring"
      contains: "tenantStore.run"
  key_links:
    - from: "packages/api/src/middleware/tenant.ts"
      to: "packages/db/src/tenant.ts"
      via: "import tenantStore + tenantStore.run()"
      pattern: "tenantStore\\.run.*organizationId"
---

<objective>
Verify that the ORG-07 audit finding (AsyncLocalStorage tenantStore.run() not called in tenant middleware) is already resolved in the current codebase.

Purpose: The v1.0 milestone audit flagged ORG-07 as a gap, but code inspection reveals that `packages/api/src/middleware/tenant.ts` already calls `tenantStore.run({ organizationId: orgId }, () => next(...))` on line 29. The VERIFICATION.md was written at an earlier point before the fix was applied. This plan confirms the fix and documents it.

Output: Verification that ORG-07 is already satisfied; no code changes needed.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify tenantStore.run() is wired in tenant middleware</name>
  <files></files>
  <read_first>
    packages/api/src/middleware/tenant.ts
    packages/db/src/tenant.ts
    packages/db/src/index.ts
  </read_first>
  <action>
**This is a verification-only task. No code changes are needed.**

Verify the following chain is intact:

1. `packages/db/src/tenant.ts` exports `tenantStore` (AsyncLocalStorage instance) and `withTenantScope` (Prisma extension that reads from tenantStore).
2. `packages/db/src/index.ts` re-exports `tenantStore` and applies `withTenantScope` to the Prisma client.
3. `packages/api/src/middleware/tenant.ts` imports `tenantStore` from `@contractor-ops/db` and calls `tenantStore.run({ organizationId: orgId }, () => next({ ctx: { ...ctx, organizationId: orgId } }))`.

The audit finding was based on a stale VERIFICATION.md snapshot. The code has since been updated.

Write the SUMMARY confirming this requirement is satisfied with no changes needed.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && grep -q 'tenantStore.run' packages/api/src/middleware/tenant.ts && grep -q 'tenantStore' packages/db/src/tenant.ts && grep -q 'withTenantScope' packages/db/src/index.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'tenantStore.run' packages/api/src/middleware/tenant.ts returns a match
    - grep 'organizationId' packages/api/src/middleware/tenant.ts returns matches showing orgId is passed to tenantStore.run
    - grep 'withTenantScope' packages/db/src/index.ts returns matches showing it is applied to prisma client
    - No code changes are required
  </acceptance_criteria>
  <done>ORG-07 confirmed satisfied: tenantStore.run() is called in tenant middleware, AsyncLocalStorage provides organizationId to Prisma extension for automatic tenant scoping.</done>
</task>

</tasks>

<verification>
1. `grep 'tenantStore.run' packages/api/src/middleware/tenant.ts` confirms the call exists
2. The full chain: tenantStore (AsyncLocalStorage) -> tenantStore.run() in middleware -> withTenantScope reads from tenantStore -> all Prisma queries auto-scoped
</verification>

<success_criteria>
- tenantStore.run() call exists in packages/api/src/middleware/tenant.ts
- No cross-tenant data leakage possible through the tRPC middleware chain
</success_criteria>

<output>
After completion, create `.planning/phases/11-route-fixes-tenant-isolation/11-02-SUMMARY.md`
</output>
