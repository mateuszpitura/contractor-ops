---
phase: 08-payments
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/web/src/app/[locale]/(app)/payments/page.tsx
  - apps/web/src/components/payments/payment-run-table/columns.tsx
  - apps/web/src/components/payments/payment-run-table/data-table.tsx
  - apps/web/src/components/payments/payment-run-table/data-table-toolbar.tsx
  - apps/web/src/components/payments/payment-run-side-panel.tsx
  - apps/web/src/components/payments/new-payment-run-dialog/index.tsx
  - apps/web/src/components/payments/new-payment-run-dialog/step-select.tsx
  - apps/web/src/components/payments/new-payment-run-dialog/step-review.tsx
  - apps/web/src/components/payments/new-payment-run-dialog/step-confirmation.tsx
  - apps/web/src/components/payments/invoice-selection-table/columns.tsx
  - apps/web/src/components/payments/invoice-selection-table/data-table.tsx
  - apps/web/src/components/payments/bank-statement-dialog.tsx
  - apps/web/src/components/payments/payment-run-badge.tsx
  - apps/web/src/lib/navigation.ts
autonomous: true
requirements:
  - PAY-01
  - PAY-02
  - PAY-03
  - PAY-04
  - PAY-05
  - PAY-06

must_haves:
  truths:
    - "Finance user can see payment run history table on /payments page"
    - "Finance user can navigate to /payments via sidebar navigation"
    - "Finance user can open new payment run dialog and select invoices with filters"
    - "Finance user can review selected invoices grouped by currency and lock+export"
    - "Finance user can download the generated export file"
    - "Finance user can mark items paid/failed via side panel"
    - "Finance user can remove invoices from a DRAFT run via side panel (per D-04)"
    - "Finance user can import bank statement and confirm auto-matches"
    - "Finance user can cancel a run with appropriate warnings per status"
  artifacts:
    - path: "apps/web/src/app/[locale]/(app)/payments/page.tsx"
      provides: "/payments page with TanStack Table and side panel"
      min_lines: 50
    - path: "apps/web/src/components/payments/new-payment-run-dialog/index.tsx"
      provides: "3-step dialog for payment run creation"
      min_lines: 80
    - path: "apps/web/src/components/payments/payment-run-side-panel.tsx"
      provides: "Side panel with run details, actions, item list"
      min_lines: 100
    - path: "apps/web/src/components/payments/bank-statement-dialog.tsx"
      provides: "Bank statement upload, parse, match, confirm dialog"
      min_lines: 80
  key_links:
    - from: "apps/web/src/lib/navigation.ts"
      to: "apps/web/src/app/[locale]/(app)/payments/page.tsx"
      via: "Payments nav item pointing to /payments route"
      pattern: "payments|Payments"
    - from: "apps/web/src/components/payments/new-payment-run-dialog/index.tsx"
      to: "payment.create tRPC mutation"
      via: "trpc.payment.create.useMutation"
      pattern: "payment\\.create"
    - from: "apps/web/src/components/payments/payment-run-side-panel.tsx"
      to: "payment.markAllPaid tRPC mutation"
      via: "trpc.payment.markAllPaid.useMutation"
      pattern: "payment\\.markAllPaid"
    - from: "apps/web/src/components/payments/payment-run-side-panel.tsx"
      to: "payment.removeFromRun tRPC mutation"
      via: "trpc.payment.removeFromRun.useMutation"
      pattern: "payment\\.removeFromRun"
    - from: "apps/web/src/app/[locale]/(app)/payments/page.tsx"
      to: "payment.list tRPC query"
      via: "trpc.payment.list.useQuery"
      pattern: "payment\\.list"
---

<objective>
Full /payments page UI with payment run history table, new payment run dialog (3-step: select invoices, review, lock & export), payment run side panel with status management and invoice removal from DRAFT runs, bank statement import dialog, and navigation wiring.

Purpose: Delivers the complete payment management UI for finance users to create, export, track, and close payment runs per all 16 locked decisions from CONTEXT.md. Registers the Payments nav item so the page is reachable from the sidebar.
Output: /payments route, all payment components, navigation entry, fully wired to tRPC payment router.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-payments/08-CONTEXT.md
@.planning/phases/08-payments/08-RESEARCH.md
@.planning/phases/08-payments/08-UI-SPEC.md
@.planning/phases/08-payments/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: /payments page with run history table, status filter chips, side panel, payment run badge, and navigation wiring</name>
  <files>
    apps/web/src/app/[locale]/(app)/payments/page.tsx,
    apps/web/src/components/payments/payment-run-table/columns.tsx,
    apps/web/src/components/payments/payment-run-table/data-table.tsx,
    apps/web/src/components/payments/payment-run-table/data-table-toolbar.tsx,
    apps/web/src/components/payments/payment-run-side-panel.tsx,
    apps/web/src/components/payments/payment-run-badge.tsx,
    apps/web/src/lib/navigation.ts
  </files>
  <read_first>
    apps/web/src/app/[locale]/(app)/invoices/page.tsx,
    apps/web/src/components/invoices/invoice-table/columns.tsx,
    apps/web/src/components/invoices/invoice-table/data-table.tsx,
    apps/web/src/components/invoices/invoice-table/data-table-toolbar.tsx,
    apps/web/src/components/invoices/invoice-detail/index.tsx,
    apps/web/src/components/approvals/approval-side-panel.tsx,
    apps/web/src/lib/navigation.ts,
    .planning/phases/08-payments/08-UI-SPEC.md,
    .planning/phases/08-payments/08-CONTEXT.md,
    packages/db/prisma/schema/payment.prisma
  </read_first>
  <action>
**0. Register Payments nav item in `apps/web/src/lib/navigation.ts`:**

Read the existing navigation configuration. Add a "Payments" nav item to the sidebar navigation array, following the same pattern as existing nav items (Invoices, Contractors, etc.). Use the Banknote icon from lucide-react. Route: `/payments`. Position: after Approvals (or after Invoices, wherever finance-related items are grouped). The permission guard should check for `payment: ["read"]` if the nav config supports permission-based visibility.

**1. Create `apps/web/src/components/payments/payment-run-badge.tsx`:**

Status badge component for PaymentRunStatus. Map per UI-SPEC color table:
- DRAFT: muted bg, muted-foreground text, FileEdit icon
- LOCKED: warning at 10% bg, warning text, Lock icon
- EXPORTED: info at 10% bg (use `bg-blue-500/10 text-blue-600 dark:text-blue-400`), Download icon
- COMPLETED: success at 10% bg (use `bg-green-500/10 text-green-600 dark:text-green-400`), CheckCircle2 icon
- CANCELLED: destructive at 10% bg, destructive text, XCircle icon

Use shadcn Badge with `variant="outline"` and className override. Icon size 14px (w-3.5 h-3.5). Props: `{ status: string }`.

Also create `PaymentItemBadge` for PaymentRunItemStatus:
- PENDING: muted bg, muted-foreground text
- PAID: success at 10% bg, success text
- FAILED: destructive at 10% bg, destructive text

**2. Create `apps/web/src/components/payments/payment-run-table/columns.tsx`:**

Mirror invoice-table columns pattern. Define columns using TanStack Table columnHelper:
- Run # (`runNumber`): font-semibold, clickable (calls onRowClick prop). 14px semibold per UI-SPEC.
- Status (`status`): PaymentRunBadge component.
- Created (`createdAt`): Relative date with tooltip for absolute. Use date-fns formatDistanceToNow or similar inline helper.
- Invoices (`invoiceCount`): Integer count.
- Total (`totalGrosze`): Formatted with Intl.NumberFormat, show currency. Display as `{formatted} {currency}`.
- Format (`exportFormat`): Plain text or null if DRAFT.
- Actions: DropdownMenu with "Download export" (visible if exportedAt), "Mark all paid" (visible if EXPORTED), "Cancel run" (visible per D-15 status rules: DRAFT/LOCKED always, EXPORTED only if admin).

**3. Create `apps/web/src/components/payments/payment-run-table/data-table.tsx`:**

Mirror invoice-table data-table.tsx pattern exactly. TanStack Table with server-side pagination (20 per page). Accept data, columns, isLoading, pagination props. 8 skeleton rows during loading. Row click opens side panel (calls onRowClick).

**4. Create `apps/web/src/components/payments/payment-run-table/data-table-toolbar.tsx`:**

Filter bar with:
- Status chip bar: Badge-style chips for All / Draft / Locked / Exported / Completed / Cancelled. Active chip: `bg-primary/10 text-primary`. Inactive: `bg-muted text-muted-foreground`. Each shows count from a separate count query or from data. Synced to URL via nuqs `parseAsString` key "status".
- Date range filter: Use shadcn Calendar + Popover pattern for date range picker. Synced to URL via nuqs with dateFrom/dateTo keys.

**5. Create `apps/web/src/components/payments/payment-run-side-panel.tsx`:**

Sheet (400px from right) per UI-SPEC. Content:
- Header: Run number (20px semibold) + PaymentRunBadge inline.
- Metadata grid: Created (relative date), Created by (user name), Export format, Invoice count, Total by currency.
- Action buttons (contextual per UI-SPEC Interaction Patterns):
  - DRAFT: "Lock & Export" primary (opens lock dialog or inline), "Cancel run" destructive outline.
  - EXPORTED: "Download export" outline, "Mark all paid" primary, "Import statement" outline, "Cancel run" destructive outline (admin only).
  - COMPLETED: "Download export" outline only.
  - CANCELLED: No actions.
- "Mark all paid": Inline confirmation pattern -- button text changes to "Confirm mark all paid" for 3 seconds, then reverts. On confirm: call trpc.payment.markAllPaid.useMutation.
- "Cancel run": If EXPORTED, show AlertDialog with D-15 warning text ("A bank file was already exported..."). If DRAFT/LOCKED, simple AlertDialog ("All invoices will be released back..."). On confirm: call trpc.payment.cancel.useMutation.
- "Download export": Fetch base64 from lockAndExport response or stored export. Create Blob and trigger browser download.
- Invoice list: ScrollArea. Each item row: Invoice # (link), Contractor name, Amount, PaymentItemBadge, Reference ID (if present, 12px muted-foreground).
- Per-item actions: DropdownMenu with:
  - "Mark paid" (Popover with optional reference Input + Confirm)
  - "Mark failed" (Popover with required failure reason Textarea + destructive Confirm)
  - **"Remove from run"** (visible only when run status is DRAFT, per D-04): On click, show confirmation Popover ("Remove this invoice from the run? It will return to the ready-for-payment pool.") with "Remove" destructive button. On confirm: call trpc.payment.removeFromRun.useMutation({ runId, invoiceId }). Invalidate payment.get and payment.list queries on success.
  Call trpc.payment.updateItemStatus.useMutation for mark paid/failed.

Use trpc.payment.get.useQuery({ runId }) to fetch data. Invalidate payment.list and payment.get on mutations.

**6. Create `apps/web/src/app/[locale]/(app)/payments/page.tsx`:**

Page layout per UI-SPEC:
- Heading "Payments" (use useTranslations("Payments") with t("title")). Hard-code i18n keys for now -- Plan 03 adds the actual translations.
- "New payment run" primary CTA button top-right aligned with title. Opens NewPaymentRunDialog.
- DataTableToolbar for filters.
- DataTable for payment run history.
- PaymentRunSidePanel (controlled open state via selectedRunId).
- Use trpc.payment.list.useQuery with nuqs URL params for status, dateFrom, dateTo, cursor.
- Empty state per UI-SPEC: Banknote icon (48px), "No payment runs yet" heading, description text, "New payment run" CTA.
- Use Suspense boundary for nuqs (consistent with existing pages).
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/[locale]/(app)/payments/page.tsx exists and contains "payment.list" tRPC query
    - payments/page.tsx contains useTranslations("Payments")
    - payments/page.tsx contains Suspense boundary
    - apps/web/src/lib/navigation.ts contains a "Payments" or "payments" nav item pointing to /payments
    - payment-run-table/columns.tsx defines columns: runNumber, status, createdAt, invoiceCount, totalGrosze, exportFormat
    - payment-run-table/data-table.tsx contains useReactTable
    - payment-run-table/data-table-toolbar.tsx contains nuqs parseAsString for status filter
    - payment-run-side-panel.tsx contains payment.get, payment.markAllPaid, payment.cancel, payment.updateItemStatus, payment.removeFromRun tRPC calls
    - payment-run-side-panel.tsx contains "Remove from run" action visible only for DRAFT status
    - payment-run-side-panel.tsx contains AlertDialog for cancel confirmation
    - payment-run-badge.tsx exports PaymentRunBadge and PaymentItemBadge
    - payment-run-badge.tsx contains FileEdit, Lock, Download, CheckCircle2, XCircle icon imports
    - TypeScript compiles without errors (or only i18n key warnings)
  </acceptance_criteria>
  <done>/payments page renders payment run history with TanStack Table, status filter chips synced to URL, date range filter, side panel with full run details and contextual actions (mark paid, cancel with D-15 warnings, download export, per-item status overrides, remove from DRAFT run per D-04). Payments nav item registered in sidebar navigation. Empty state shown when no runs exist.</done>
</task>

<task type="auto">
  <name>Task 2: New payment run dialog (3-step) and bank statement import dialog</name>
  <files>
    apps/web/src/components/payments/new-payment-run-dialog/index.tsx,
    apps/web/src/components/payments/new-payment-run-dialog/step-select.tsx,
    apps/web/src/components/payments/new-payment-run-dialog/step-review.tsx,
    apps/web/src/components/payments/new-payment-run-dialog/step-confirmation.tsx,
    apps/web/src/components/payments/invoice-selection-table/columns.tsx,
    apps/web/src/components/payments/invoice-selection-table/data-table.tsx,
    apps/web/src/components/payments/bank-statement-dialog.tsx
  </files>
  <read_first>
    apps/web/src/components/payments/payment-run-side-panel.tsx,
    apps/web/src/components/payments/payment-run-badge.tsx,
    apps/web/src/components/invoices/invoice-table/columns.tsx,
    apps/web/src/components/invoices/invoice-table/data-table.tsx,
    apps/web/src/components/contractors/contractor-table/data-table-toolbar.tsx,
    .planning/phases/08-payments/08-UI-SPEC.md,
    .planning/phases/08-payments/08-CONTEXT.md,
    packages/validators/src/payment.ts
  </read_first>
  <action>
**1. Create invoice selection table:**

`apps/web/src/components/payments/invoice-selection-table/columns.tsx`:
Columns: Checkbox (header checkbox for select-all-on-page), Invoice # (14px semibold), Contractor name, Amount (formatted grosze with Intl.NumberFormat), Currency, Due date (formatted), Contract #. Row disabled (checkbox disabled) if invoice is already IN_RUN (show warning badge "In run {runNumber}"). Row with warning background if contractor billing profile has no IBAN (show "Missing IBAN" badge).

`apps/web/src/components/payments/invoice-selection-table/data-table.tsx`:
TanStack Table with row selection enabled. Accept filters (currency, dateRange, contractorSearch) from parent. Minimum height 320px per UI-SPEC. 6 skeleton rows during loading. Use trpc.payment.readyForPayment.useQuery with filter params.

**2. Create new payment run dialog:**

`apps/web/src/components/payments/new-payment-run-dialog/index.tsx`:
Shadcn Dialog, 720px width (className="max-w-3xl"), max-h-[80vh]. Manage step state (1, 2, 3) and selection state. Step indicator: 3 dots horizontally centered below title. Active: bg-primary. Completed: bg-primary with check. Inactive: bg-muted. 8px dot diameter (w-2 h-2 rounded-full), gap-4 between dots. Render StepSelect / StepReview / StepConfirmation based on step. Pass selectedInvoiceIds, groupByCurrency, onBack, onNext callbacks.

`apps/web/src/components/payments/new-payment-run-dialog/step-select.tsx` (Step 1 per D-02):
- Filter row: Currency Select (All / PLN / EUR / USD), Due date range (Popover + Calendar for from/to), Contractor search Input (300ms debounce via setTimeout/clearTimeout).
- Invoice selection table below filters.
- "Select all matching" link above table -- calls trpc.payment.readyForPayment to get all IDs matching current filters (not just visible page), sets them as selected.
- Footer left: "{N} invoices selected -- {amount} {currency}" summary. Live-updated from selection state. If multi-currency selection, show each currency on separate line.
- "Group by currency" Switch toggle per D-02. Label updates to "Will create {N} runs" when enabled and multi-currency selected.
- Footer right: "Cancel" ghost button (closes dialog), "Review selection" primary button (disabled until >= 1 selected).
- Empty state per UI-SPEC when no ready invoices: FileSearch icon (48px), "No invoices ready for payment" heading, body text.

`apps/web/src/components/payments/new-payment-run-dialog/step-review.tsx` (Step 2 per D-08, D-03):
- Display auto-generated run number from create response, or "PR-{year}-XXX" placeholder before creation.
- Optional name Input (max 100 chars) and description Textarea (max 500 chars, called "notes" in schema).
- Read-only invoice list grouped by currency. Each group: "{currency} -- {count} invoices" heading, subtotal in 20px semibold. Compact table: Invoice #, Contractor, Amount. ScrollArea with max 10 rows per group.
- Grand total at bottom with thicker top border (border-t-2).
- Export format Select: "CSV (universal)" / "Elixir (PLN domestic)" (shown only if PLN invoices present) / "SEPA XML (EUR)" (shown only if EUR invoices present).
- "Back" ghost returns to Step 1. "Lock & Export" primary CTA.
- On "Lock & Export": first call trpc.payment.create.useMutation with invoiceIds, then trpc.payment.lockAndExport.useMutation with runId and exportFormat. Show spinner + "Locking..." during processing. Prevent dialog close. On success: store fileBase64 and fileName in state, advance to Step 3.

`apps/web/src/components/payments/new-payment-run-dialog/step-confirmation.tsx` (Step 3):
- Success icon: CheckCircle2 (48px, text-green-600 dark:text-green-400) centered.
- "Payment run {runNumber} locked and exported" heading.
- Summary stats: invoice count, total by currency, export format.
- "Download export file" outline primary button. On click: decode base64 from parent state, create Blob, create <a> with URL.createObjectURL, trigger download, revoke URL.
- "View payment run" link below (closes dialog and sets selectedRunId to open side panel).
- "Close" button in footer.

**3. Create bank statement import dialog:**

`apps/web/src/components/payments/bank-statement-dialog.tsx`:
Props: { runId: string; open: boolean; onOpenChange: (open: boolean) => void }.
- Step 1: DropZone area (dashed border, Upload icon 32px centered, "Drop bank statement file or click to browse" text). Accept .mt940, .csv files. Max 10MB. Use `<input type="file" accept=".mt940,.csv" />` hidden with ref click pattern (consistent with existing upload patterns).
- Step 2: Progress bar with "Parsing statement..." label. On file select: read file as text, call trpc.payment.importStatement.useMutation with fileContent + fileName.
- Step 3: Match results table. Columns: Amount (formatted grosze), IBAN (last 4 digits masked), Match status (Matched/Unmatched badge from PaymentItemBadge), Matched invoice # (if matched). Matched rows have Checkbox (pre-checked). Unmatched rows: warning bg (bg-yellow-500/10), no checkbox. Summary: "{N} of {M} items matched" above table.
- Footer: "Cancel" ghost + "Confirm {N} matches" primary. On confirm: call trpc.payment.confirmStatementMatches.useMutation with runId and matches array. Close dialog on success. Show sonner toast "Bank statement imported -- {count} payments matched".
- Error state: If parsing fails, show inline error with AlertCircle icon + error message + "Try again" link (resets to Step 1).

All components use `useTranslations("Payments")` with hard-coded fallback keys. Actual translations added in Plan 03.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - new-payment-run-dialog/index.tsx manages 3-step state with step indicator dots
    - new-payment-run-dialog/index.tsx has className containing "max-w-3xl" for 720px dialog width
    - step-select.tsx contains trpc.payment.readyForPayment query
    - step-select.tsx contains currency Select, date range filter, contractor search Input
    - step-select.tsx contains "Group by currency" Switch toggle
    - step-review.tsx contains trpc.payment.create.useMutation and trpc.payment.lockAndExport.useMutation
    - step-review.tsx contains export format Select with CSV, BANK_FILE, SEPA_XML options
    - step-confirmation.tsx contains CheckCircle2 icon and download logic with URL.createObjectURL
    - invoice-selection-table/columns.tsx defines checkbox column + Invoice #, Contractor, Amount, Currency, Due date, Contract # columns
    - bank-statement-dialog.tsx contains trpc.payment.importStatement and trpc.payment.confirmStatementMatches mutations
    - bank-statement-dialog.tsx contains file input accepting .mt940 and .csv
    - TypeScript compiles without errors (or only i18n key warnings)
  </acceptance_criteria>
  <done>New payment run dialog implements the full 3-step flow: invoice selection with filters/bulk select/group-by-currency (D-02), review with run number/totals/export format (D-08), and confirmation with download. Bank statement import dialog handles upload, MT940/CSV parsing, auto-match display, and confirmation. All wired to payment tRPC router.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit -p apps/web/tsconfig.json`
- /payments page exists: `ls apps/web/src/app/[locale]/(app)/payments/page.tsx`
- Navigation wired: `grep -i "payment" apps/web/src/lib/navigation.ts`
- Dialog components exist: `ls apps/web/src/components/payments/new-payment-run-dialog/`
- Side panel exists: `ls apps/web/src/components/payments/payment-run-side-panel.tsx`
- Bank statement dialog: `ls apps/web/src/components/payments/bank-statement-dialog.tsx`
- Remove from run: `grep "removeFromRun" apps/web/src/components/payments/payment-run-side-panel.tsx`
</verification>

<success_criteria>
The /payments page is fully functional with payment run history table, status filtering, new payment run creation dialog (3-step), side panel with status management including invoice removal from DRAFT runs (D-04), and bank statement import. Navigation sidebar includes Payments item. Finance users can create runs, lock+export, mark paid/failed, remove invoices from drafts, cancel, and import bank statements -- all per the 16 locked decisions in CONTEXT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/08-payments/08-02-SUMMARY.md`
</output>
