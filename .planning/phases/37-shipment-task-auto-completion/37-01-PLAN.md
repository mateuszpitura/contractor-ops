---
phase: 37-shipment-task-auto-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/services/courier/inpost-webhook-handler.ts
  - packages/api/src/services/courier/inpost-polling-service.ts
  - packages/api/src/services/courier/dpd-polling-service.ts
  - packages/api/src/services/courier/ups-polling-service.ts
  - packages/api/src/services/courier/__tests__/inpost-webhook-handler.test.ts
  - packages/api/src/services/courier/__tests__/inpost-polling-service.test.ts
  - packages/api/src/services/courier/__tests__/dpd-polling-service.test.ts
  - packages/api/src/services/courier/__tests__/ups-polling-service.test.ts
autonomous: true
requirements:
  - EQUIP-09
  - EQUIP-10

must_haves:
  truths:
    - "InPost webhook status update to DELIVERED triggers checkShipmentTaskCompletion and auto-completes linked workflow task"
    - "InPost polling status update to DELIVERED triggers checkShipmentTaskCompletion and auto-completes linked workflow task"
    - "DPD polling status update to DELIVERED triggers checkShipmentTaskCompletion and auto-completes linked workflow task"
    - "UPS polling status update to DELIVERED triggers checkShipmentTaskCompletion and auto-completes linked workflow task"
    - "Return shipments trigger checkShipmentTaskCompletion on RETURNED status for all carriers"
  artifacts:
    - path: "packages/api/src/services/courier/inpost-webhook-handler.ts"
      provides: "InPost webhook task completion wiring"
      contains: "checkShipmentTaskCompletion"
    - path: "packages/api/src/services/courier/inpost-polling-service.ts"
      provides: "InPost polling task completion wiring"
      contains: "checkShipmentTaskCompletion"
    - path: "packages/api/src/services/courier/dpd-polling-service.ts"
      provides: "DPD polling task completion wiring"
      contains: "checkShipmentTaskCompletion"
    - path: "packages/api/src/services/courier/ups-polling-service.ts"
      provides: "UPS polling task completion wiring"
      contains: "checkShipmentTaskCompletion"
    - path: "packages/api/src/services/courier/__tests__/dpd-polling-service.test.ts"
      provides: "DPD polling test coverage"
      contains: "checkShipmentTaskCompletion"
    - path: "packages/api/src/services/courier/__tests__/ups-polling-service.test.ts"
      provides: "UPS polling test coverage"
      contains: "checkShipmentTaskCompletion"
  key_links:
    - from: "packages/api/src/services/courier/inpost-webhook-handler.ts"
      to: "packages/api/src/services/equipment-workflow.ts"
      via: "import checkShipmentTaskCompletion"
      pattern: 'import.*checkShipmentTaskCompletion.*from.*equipment-workflow'
    - from: "packages/api/src/services/courier/inpost-polling-service.ts"
      to: "packages/api/src/services/equipment-workflow.ts"
      via: "import checkShipmentTaskCompletion"
      pattern: 'import.*checkShipmentTaskCompletion.*from.*equipment-workflow'
    - from: "packages/api/src/services/courier/dpd-polling-service.ts"
      to: "packages/api/src/services/equipment-workflow.ts"
      via: "import checkShipmentTaskCompletion"
      pattern: 'import.*checkShipmentTaskCompletion.*from.*equipment-workflow'
    - from: "packages/api/src/services/courier/ups-polling-service.ts"
      to: "packages/api/src/services/equipment-workflow.ts"
      via: "import checkShipmentTaskCompletion"
      pattern: 'import.*checkShipmentTaskCompletion.*from.*equipment-workflow'
---

<objective>
Wire checkShipmentTaskCompletion into all 4 courier status update paths so webhook and polling status changes trigger workflow task auto-completion.

Purpose: Closes MISSING-02 from v3.0 audit -- currently webhook/polling paths update shipment and equipment status but never call checkShipmentTaskCompletion, so workflow tasks linked to shipments never auto-complete via external status updates.
Output: 4 service files with fire-and-forget checkShipmentTaskCompletion calls, 4 test files covering the wiring.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-shipment-task-auto-completion/37-CONTEXT.md
@.planning/phases/37-shipment-task-auto-completion/37-RESEARCH.md

<interfaces>
<!-- Key function signature the executor needs -->

From packages/api/src/services/equipment-workflow.ts:
```typescript
export async function checkShipmentTaskCompletion(
  db: PrismaClient,
  organizationId: string,
  shipment: {
    id: string;
    workflowTaskRunId: string | null;
    direction: "OUTBOUND" | "RETURN";
    currentStatus: string;
  },
): Promise<void>
```

Existing fire-and-forget pattern (from packages/api/src/routers/equipment.ts:1265):
```typescript
void checkShipmentTaskCompletion(prisma, ctx.organizationId, {
  id: shipments[0].id,
  workflowTaskRunId: input.workflowTaskRunId,
  direction: input.direction,
  currentStatus: "CREATED",
}).catch(console.error);
```

Import path (ESM with .js extension):
```typescript
import { checkShipmentTaskCompletion } from "../equipment-workflow.js";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire checkShipmentTaskCompletion into 4 courier service files</name>
  <files>
    packages/api/src/services/courier/inpost-webhook-handler.ts,
    packages/api/src/services/courier/inpost-polling-service.ts,
    packages/api/src/services/courier/dpd-polling-service.ts,
    packages/api/src/services/courier/ups-polling-service.ts
  </files>
  <read_first>
    packages/api/src/services/courier/inpost-webhook-handler.ts,
    packages/api/src/services/courier/inpost-polling-service.ts,
    packages/api/src/services/courier/dpd-polling-service.ts,
    packages/api/src/services/courier/ups-polling-service.ts,
    packages/api/src/services/equipment-workflow.ts,
    packages/api/src/routers/equipment.ts (lines 1260-1270 for fire-and-forget pattern)
  </read_first>
  <behavior>
    - Test 1: InPost webhook handler calls checkShipmentTaskCompletion with correct args (shipment.id, shipment.workflowTaskRunId, shipment.direction cast to "OUTBOUND"|"RETURN", mappedStatus) after status update to DELIVERED
    - Test 2: InPost polling service calls checkShipmentTaskCompletion with correct args after status update to DELIVERED
    - Test 3: DPD polling service calls checkShipmentTaskCompletion with correct args after status update to DELIVERED
    - Test 4: UPS polling service calls checkShipmentTaskCompletion with correct args after status update to DELIVERED
    - Test 5: checkShipmentTaskCompletion is NOT called when shipment is not found (webhook) or status unchanged (polling)
  </behavior>
  <action>
    Per D-01 and D-02, add a direct import and fire-and-forget call to each of the 4 service files.

    **Import to add at top of each file** (after existing imports):
    ```typescript
    import { checkShipmentTaskCompletion } from "../equipment-workflow.js";
    ```

    **InPost Webhook Handler** (`inpost-webhook-handler.ts`):
    - Insert AFTER line 174 (`db.shipment.update` call) and BEFORE line 176 (the `// 7. Auto-advance equipment status` comment)
    - Add comment: `// 6b. Fire-and-forget: check workflow task auto-completion (per D-01/D-02)`
    - Add call:
      ```typescript
      void checkShipmentTaskCompletion(db, organizationId, {
        id: shipment.id,
        workflowTaskRunId: shipment.workflowTaskRunId,
        direction: shipment.direction as "OUTBOUND" | "RETURN",
        currentStatus: mappedStatus,
      }).catch(console.error);
      ```
    - The `shipment` variable is from `findFirst` (has all fields including `workflowTaskRunId`, `direction`, `id`)
    - `mappedStatus` is the newly mapped status (the value just written to `currentStatus`)
    - Per D-05: no try/catch wrapping -- `.catch(console.error)` handles it

    **InPost Polling Service** (`inpost-polling-service.ts`):
    - Insert AFTER line 141 (`db.shipment.update` call) and BEFORE line 143 (`// Auto-advance equipment status` comment)
    - Add comment: `// Fire-and-forget: check workflow task auto-completion (per D-01/D-02)`
    - Add identical call pattern:
      ```typescript
      void checkShipmentTaskCompletion(db, organizationId, {
        id: shipment.id,
        workflowTaskRunId: shipment.workflowTaskRunId,
        direction: shipment.direction as "OUTBOUND" | "RETURN",
        currentStatus: mappedStatus,
      }).catch(console.error);
      ```
    - The `shipment` is from the `for (const shipment of activeShipments)` loop (has all fields -- no explicit select)

    **DPD Polling Service** (`dpd-polling-service.ts`):
    - Identical pattern to InPost polling. Insert AFTER `db.shipment.update` (line 141) and BEFORE equipment status logic (line 145)
    - Same import, same call, same comment

    **UPS Polling Service** (`ups-polling-service.ts`):
    - Identical pattern to InPost polling. Insert AFTER `db.shipment.update` (line 141) and BEFORE equipment status logic (line 145)
    - Same import, same call, same comment

    NOTE on D-03/D-04: Research confirmed the unified cron route at `apps/web/src/app/api/cron/inpost-status-poll/route.ts` already calls all 3 carrier polling services (InPost, DPD, UPS). No new cron routes needed.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops/packages/api && npx vitest run src/services/courier/__tests__/ --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - inpost-webhook-handler.ts contains `import { checkShipmentTaskCompletion } from "../equipment-workflow.js"`
    - inpost-webhook-handler.ts contains `void checkShipmentTaskCompletion(db, organizationId,`
    - inpost-polling-service.ts contains `import { checkShipmentTaskCompletion } from "../equipment-workflow.js"`
    - inpost-polling-service.ts contains `void checkShipmentTaskCompletion(db, organizationId,`
    - dpd-polling-service.ts contains `import { checkShipmentTaskCompletion } from "../equipment-workflow.js"`
    - dpd-polling-service.ts contains `void checkShipmentTaskCompletion(db, organizationId,`
    - ups-polling-service.ts contains `import { checkShipmentTaskCompletion } from "../equipment-workflow.js"`
    - ups-polling-service.ts contains `void checkShipmentTaskCompletion(db, organizationId,`
    - All 4 files contain `.catch(console.error)` on the checkShipmentTaskCompletion call
    - All 4 files contain `direction: shipment.direction as "OUTBOUND" | "RETURN"`
    - All 4 files contain `currentStatus: mappedStatus`
  </acceptance_criteria>
  <done>All 4 service files import and call checkShipmentTaskCompletion after shipment status update, using fire-and-forget pattern with .catch(console.error)</done>
</task>

<task type="auto">
  <name>Task 2: Create and extend test suites for task completion wiring</name>
  <files>
    packages/api/src/services/courier/__tests__/inpost-webhook-handler.test.ts,
    packages/api/src/services/courier/__tests__/inpost-polling-service.test.ts,
    packages/api/src/services/courier/__tests__/dpd-polling-service.test.ts,
    packages/api/src/services/courier/__tests__/ups-polling-service.test.ts
  </files>
  <read_first>
    packages/api/src/services/courier/__tests__/inpost-webhook-handler.test.ts,
    packages/api/src/services/courier/__tests__/inpost-polling-service.test.ts,
    packages/api/src/services/courier/__tests__/dpd-polling-service.test.ts (if exists),
    packages/api/src/services/courier/__tests__/ups-polling-service.test.ts (if exists),
    packages/api/src/services/courier/__tests__/dpd-client.test.ts (for DPD mock pattern),
    packages/api/src/services/courier/__tests__/ups-client.test.ts (for UPS mock pattern)
  </read_first>
  <action>
    Per D-06: Unit tests with mocked checkShipmentTaskCompletion verifying function is called with correct arguments.

    **Mock setup for ALL test files** -- add at the top level of each test file (before imports of the module under test):
    ```typescript
    const mockCheckShipmentTaskCompletion = vi.fn().mockResolvedValue(undefined);
    vi.mock("../../equipment-workflow", () => ({
      checkShipmentTaskCompletion: mockCheckShipmentTaskCompletion,
    }));
    ```
    Note: path is `../../equipment-workflow` (not `../`) because tests are in `__tests__/` subdirectory.

    Reset in beforeEach:
    ```typescript
    mockCheckShipmentTaskCompletion.mockReset().mockResolvedValue(undefined);
    ```

    **Extend inpost-webhook-handler.test.ts** -- add 2 new tests to existing `describe("handleInPostWebhook")`:

    Test 1: "calls checkShipmentTaskCompletion after status update":
    - Use existing `validPayload` (status: "delivered" maps to "DELIVERED")
    - Mock shipment with `workflowTaskRunId: "task-1"`, `direction: "OUTBOUND"`, `id: "ship-1"`
    - Setup all mocks as in existing "creates ShipmentEvent" test
    - After `await handleInPostWebhook(db as any, "org-1", validPayload)`
    - Assert: `expect(mockCheckShipmentTaskCompletion).toHaveBeenCalledWith(db, "org-1", expect.objectContaining({ id: "ship-1", workflowTaskRunId: "task-1", direction: "OUTBOUND", currentStatus: "DELIVERED" }))`

    Test 2: "does not call checkShipmentTaskCompletion when shipment not found":
    - `db.shipment.findFirst.mockResolvedValue(null)` for both calls
    - Assert: `expect(mockCheckShipmentTaskCompletion).not.toHaveBeenCalled()`

    **Extend inpost-polling-service.test.ts** -- add 2 new tests to existing describe:

    Test 1: "calls checkShipmentTaskCompletion after polling status update":
    - Use existing mock pattern from "creates missing events" test
    - Set `workflowTaskRunId: "task-1"` on mock shipment (existing tests use `null`)
    - Mock `getStatus` returning `{ status: "delivered", externalId: "ext-1", trackingNumber: "T1" }`
    - Assert: `expect(mockCheckShipmentTaskCompletion).toHaveBeenCalledWith(db, "org-1", expect.objectContaining({ id: "ship-1", workflowTaskRunId: "task-1", direction: "OUTBOUND", currentStatus: "DELIVERED" }))`

    Test 2: "does not call checkShipmentTaskCompletion when status unchanged":
    - Mock `getStatus` returning status that maps to same as `currentStatus`
    - Assert: `expect(mockCheckShipmentTaskCompletion).not.toHaveBeenCalled()`

    **Create dpd-polling-service.test.ts** -- NEW FILE following inpost-polling-service.test.ts pattern exactly:
    - Mock `../dpd-client` with `DPDClient` class containing `mockGetStatus`
    - Mock `../../equipment-workflow` with `mockCheckShipmentTaskCompletion`
    - `createMockDb()` identical to InPost polling test
    - Import `pollDpdShipmentStatuses` from `../dpd-polling-service`
    - Tests:
      1. "fetches all active DPD shipments" -- carrier: "DPD", config uses `{ username, password, fid, sandbox }`
      2. "creates missing events for newer statuses" -- mock `getStatus` returning DPD status
      3. "returns early if no courier config found"
      4. "calls checkShipmentTaskCompletion after polling status update" -- same pattern as InPost test
      5. "does not call checkShipmentTaskCompletion when status unchanged"

    **Create ups-polling-service.test.ts** -- NEW FILE following inpost-polling-service.test.ts pattern exactly:
    - Mock `../ups-client` with `UPSClient` class containing `mockGetStatus`
    - Mock `../../equipment-workflow` with `mockCheckShipmentTaskCompletion`
    - `createMockDb()` identical to InPost polling test
    - Import `pollUpsShipmentStatuses` from `../ups-polling-service`
    - Tests:
      1. "fetches all active UPS shipments" -- carrier: "UPS", config uses `{ clientId, clientSecret, accountNumber, sandbox }`
      2. "creates missing events for newer statuses"
      3. "returns early if no courier config found"
      4. "calls checkShipmentTaskCompletion after polling status update"
      5. "does not call checkShipmentTaskCompletion when status unchanged"

    DPD mock status value: use "DOR" (maps to DELIVERED via mapDpdStatus)
    UPS mock status value: use "D" (maps to DELIVERED via mapUpsStatus)
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops/packages/api && npx vitest run src/services/courier/__tests__/ --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - inpost-webhook-handler.test.ts contains `vi.mock("../../equipment-workflow"`
    - inpost-webhook-handler.test.ts contains test "calls checkShipmentTaskCompletion after status update"
    - inpost-polling-service.test.ts contains `vi.mock("../../equipment-workflow"`
    - inpost-polling-service.test.ts contains test "calls checkShipmentTaskCompletion after polling status update"
    - dpd-polling-service.test.ts exists and contains `import { pollDpdShipmentStatuses }`
    - dpd-polling-service.test.ts contains `vi.mock("../../equipment-workflow"`
    - dpd-polling-service.test.ts contains test "calls checkShipmentTaskCompletion after polling status update"
    - ups-polling-service.test.ts exists and contains `import { pollUpsShipmentStatuses }`
    - ups-polling-service.test.ts contains `vi.mock("../../equipment-workflow"`
    - ups-polling-service.test.ts contains test "calls checkShipmentTaskCompletion after polling status update"
    - `npx vitest run src/services/courier/__tests__/ --reporter=verbose` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All 4 test files verify checkShipmentTaskCompletion is called with correct args on status update, and NOT called when status is unchanged or shipment not found. All tests pass.</done>
</task>

</tasks>

<verification>
1. All courier test suites pass: `cd packages/api && npx vitest run src/services/courier/__tests__/ --reporter=verbose`
2. Full API package test suite passes: `cd packages/api && npx vitest run`
3. Each of the 4 service files contains the import and fire-and-forget call
4. grep confirms wiring: `grep -r "checkShipmentTaskCompletion" packages/api/src/services/courier/`
</verification>

<success_criteria>
- checkShipmentTaskCompletion is called from all 4 courier status update paths (InPost webhook, InPost poll, DPD poll, UPS poll)
- Fire-and-forget pattern with .catch(console.error) matches existing codebase convention
- Unit tests verify correct arguments passed to checkShipmentTaskCompletion
- DPD and UPS polling services have dedicated test suites (previously missing)
- All tests pass green
</success_criteria>

<output>
After completion, create `.planning/phases/37-shipment-task-auto-completion/37-01-SUMMARY.md`
</output>
