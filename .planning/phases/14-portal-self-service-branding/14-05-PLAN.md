---
phase: 14-portal-self-service-branding
plan: 05
type: execute
wave: 1
depends_on: ["14-03"]
files_modified:
  - packages/api/src/services/__tests__/portal-change-request.test.ts
  - packages/api/src/routers/__tests__/portal-profile.test.ts
  - packages/api/src/routers/__tests__/portal-notification-prefs.test.ts
  - packages/api/src/routers/__tests__/portal-branding.test.ts
autonomous: true
gap_closure: true
requirements: [PORT-06, PORT-07, PORT-08]

must_haves:
  truths:
    - "Test stubs exist for all Phase 14 API behaviors per VALIDATION.md contract"
    - "Test stubs cover PORT-06 (profile edit + change request + approval)"
    - "Test stubs cover PORT-07 (notification preferences + defaults + SECURITY_ALERTS guard)"
    - "Test stubs cover PORT-08 (branding hex validation)"
  artifacts:
    - path: "packages/api/src/services/__tests__/portal-change-request.test.ts"
      provides: "Test stubs for change request service (create, approve, reject, duplicate guard)"
      contains: "createChangeRequest"
    - path: "packages/api/src/routers/__tests__/portal-profile.test.ts"
      provides: "Test stubs for portal profile endpoints (getProfile, updateContactInfo, submitFinancialChangeRequest)"
      contains: "updateContactInfo"
    - path: "packages/api/src/routers/__tests__/portal-notification-prefs.test.ts"
      provides: "Test stubs for notification preference endpoints"
      contains: "SECURITY_ALERTS"
    - path: "packages/api/src/routers/__tests__/portal-branding.test.ts"
      provides: "Test stubs for branding endpoint hex validation"
      contains: "updateBranding"
  key_links:
    - from: "packages/api/src/services/__tests__/portal-change-request.test.ts"
      to: "packages/api/src/services/portal-change-request.ts"
      via: "import"
      pattern: "portal-change-request"
---

<objective>
Close verification gap 2: create test stubs for all Phase 14 API functionality per the VALIDATION.md contract.

Purpose: VALIDATION.md established a Wave 0 testing contract requiring test stubs at specific paths covering PORT-06 (profile editing + change requests), PORT-07 (notification preferences), and PORT-08 (branding). None of these files exist. This plan creates the test stubs using the existing `it.todo()` pattern from the codebase.

Output: Four test stub files with `it.todo()` placeholders covering all behaviors specified in VALIDATION.md. Tests follow the existing pattern from `packages/api/src/services/__tests__/payment-export.test.ts`.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-portal-self-service-branding/14-RESEARCH.md
@.planning/phases/14-portal-self-service-branding/14-VERIFICATION.md
@.planning/phases/14-portal-self-service-branding/14-01-SUMMARY.md

<interfaces>
<!-- Existing test pattern from codebase -->

From packages/api/src/services/__tests__/payment-export.test.ts:
```typescript
import { describe, it } from "vitest";

describe("payment-export", () => {
  describe("generateCsv", () => {
    it.todo("generates CSV buffer with correct columns per D-06");
    it.todo("includes UTF-8 BOM for Excel compatibility");
  });
});
```

From packages/api/vitest.config.ts:
```typescript
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    globals: true,
    environment: "node",
    include: ["src/**/__tests__/**/*.test.ts"],
  },
});
```

From packages/api/src/services/portal-change-request.ts:
```typescript
export async function createChangeRequest(contractorId, organizationId, requestedChanges, previousValues);
export async function approveChangeRequest(requestId, organizationId, reviewerId, comment?);
export async function rejectChangeRequest(requestId, organizationId, reviewerId, comment?);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create change request service and portal profile test stubs</name>
  <files>
    packages/api/src/services/__tests__/portal-change-request.test.ts,
    packages/api/src/routers/__tests__/portal-profile.test.ts
  </files>
  <read_first>
    packages/api/src/services/__tests__/payment-export.test.ts,
    packages/api/src/services/portal-change-request.ts,
    packages/api/src/routers/portal.ts,
    packages/api/vitest.config.ts,
    .planning/phases/14-portal-self-service-branding/14-VERIFICATION.md
  </read_first>
  <action>
    **1. Create `packages/api/src/services/__tests__/portal-change-request.test.ts`:**
    Follow the existing `it.todo()` stub pattern from payment-export.test.ts.

    ```typescript
    import { describe, it } from "vitest";

    describe("portal-change-request", () => {
      describe("createChangeRequest", () => {
        it.todo("creates a PENDING change request with requestedChanges and previousValues JSON");
        it.todo("throws CONFLICT when a PENDING request already exists for the same contractor+org (PORT-06d)");
        it.todo("allows new request after previous one is APPROVED");
        it.todo("allows new request after previous one is REJECTED");
      });

      describe("approveChangeRequest", () => {
        it.todo("applies requestedChanges to default billing profile in a transaction (PORT-06c)");
        it.todo("sets status to APPROVED with reviewerId and reviewedAt");
        it.todo("stores optional reviewer comment");
        it.todo("throws NOT_FOUND for non-existent or already-reviewed request");
        it.todo("throws NOT_FOUND when billing profile does not exist");
      });

      describe("rejectChangeRequest", () => {
        it.todo("sets status to REJECTED with reviewerId and reviewedAt");
        it.todo("stores optional rejection comment");
        it.todo("throws NOT_FOUND for non-existent or already-reviewed request");
      });
    });
    ```

    **2. Create `packages/api/src/routers/__tests__/portal-profile.test.ts`:**

    ```typescript
    import { describe, it } from "vitest";

    describe("portal.getProfile", () => {
      it.todo("returns contractor contact info and masked billing profile");
      it.todo("never exposes bankAccountEncrypted in response");
      it.todo("includes pendingChangeRequest when one exists with status PENDING");
      it.todo("returns null pendingChangeRequest when none pending");
    });

    describe("portal.updateContactInfo", () => {
      it.todo("updates contractor contact fields immediately without approval (PORT-06a)");
      it.todo("validates displayName is non-empty and max 200 chars");
      it.todo("accepts optional nullable phone, address, city, postalCode, countryCode");
    });

    describe("portal.submitFinancialChangeRequest", () => {
      it.todo("creates ContractorChangeRequest with requested financial changes (PORT-06b)");
      it.todo("strips whitespace from bank account number and creates masked value");
      it.todo("snapshots current billing profile values as previousValues");
      it.todo("throws BAD_REQUEST when no changes are provided");
      it.todo("throws CONFLICT when a pending request already exists (PORT-06d)");
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/api test --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File packages/api/src/services/__tests__/portal-change-request.test.ts exists
    - File packages/api/src/routers/__tests__/portal-profile.test.ts exists
    - grep "createChangeRequest" packages/api/src/services/__tests__/portal-change-request.test.ts returns matches
    - grep "approveChangeRequest" packages/api/src/services/__tests__/portal-change-request.test.ts returns matches
    - grep "CONFLICT" packages/api/src/services/__tests__/portal-change-request.test.ts returns match for duplicate guard test
    - grep "bankAccountEncrypted" packages/api/src/routers/__tests__/portal-profile.test.ts returns match for security test
    - grep "updateContactInfo" packages/api/src/routers/__tests__/portal-profile.test.ts returns matches
    - grep "PORT-06" packages/api/src/routers/__tests__/portal-profile.test.ts returns at least 2 matches
    - pnpm --filter @contractor-ops/api test --run exits with code 0 (todo tests are not failures)
  </acceptance_criteria>
  <done>Change request service test stubs cover create (with duplicate guard), approve (transactional), and reject flows. Portal profile test stubs cover getProfile (masked-only security), updateContactInfo (immediate), and submitFinancialChangeRequest (approval flow). All stubs use it.todo() pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Create notification preferences and branding test stubs</name>
  <files>
    packages/api/src/routers/__tests__/portal-notification-prefs.test.ts,
    packages/api/src/routers/__tests__/portal-branding.test.ts
  </files>
  <read_first>
    packages/api/src/services/__tests__/payment-export.test.ts,
    packages/api/src/routers/portal.ts,
    packages/api/src/routers/settings.ts,
    .planning/phases/14-portal-self-service-branding/14-VERIFICATION.md
  </read_first>
  <action>
    **1. Create `packages/api/src/routers/__tests__/portal-notification-prefs.test.ts`:**

    ```typescript
    import { describe, it } from "vitest";

    describe("portal.getNotificationPreferences", () => {
      it.todo("returns all 5 categories with emailEnabled defaults for missing rows (PORT-07a)");
      it.todo("returns actual emailEnabled values for existing preference rows");
      it.todo("categories are: INVOICE_UPDATES, PAYMENT_CONFIRMATIONS, CONTRACT_CHANGES, DOCUMENT_UPLOADS, SECURITY_ALERTS");
    });

    describe("portal.updateNotificationPreference", () => {
      it.todo("upserts preference for a valid category (PORT-07b)");
      it.todo("creates new preference row when none exists for category");
      it.todo("updates existing preference row when one exists");
      it.todo("throws BAD_REQUEST when attempting to disable SECURITY_ALERTS (PORT-07c)");
      it.todo("allows enabling SECURITY_ALERTS (no-op but valid)");
      it.todo("validates category is one of the 5 allowed enum values");
    });
    ```

    **2. Create `packages/api/src/routers/__tests__/portal-branding.test.ts`:**

    ```typescript
    import { describe, it } from "vitest";

    describe("settings.updateBranding", () => {
      it.todo("saves brandColor as hex to organization settingsJson (PORT-08a)");
      it.todo("validates brandColor matches /^#[0-9a-fA-F]{6}$/ regex");
      it.todo("rejects invalid hex colors (e.g., '#xyz', '#12345', 'red')");
      it.todo("saves logoUrl to organization.logo field");
      it.todo("accepts null brandColor to remove brand color");
      it.todo("accepts null logoUrl to remove logo");
      it.todo("merges brandColor into existing settingsJson without overwriting other keys");
    });

    describe("settings.getBranding", () => {
      it.todo("returns current brandColor from settingsJson and logo from org");
      it.todo("returns null brandColor when settingsJson has no brandColor key");
      it.todo("returns null logo when org has no logo set");
    });

    describe("portal.getOrgBranding", () => {
      it.todo("returns brandColor and logo for portal consumption");
      it.todo("returns null values when no branding configured");
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/api test --run 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File packages/api/src/routers/__tests__/portal-notification-prefs.test.ts exists
    - File packages/api/src/routers/__tests__/portal-branding.test.ts exists
    - grep "SECURITY_ALERTS" packages/api/src/routers/__tests__/portal-notification-prefs.test.ts returns at least 2 matches
    - grep "PORT-07" packages/api/src/routers/__tests__/portal-notification-prefs.test.ts returns at least 3 matches (PORT-07a, PORT-07b, PORT-07c)
    - grep "PORT-08" packages/api/src/routers/__tests__/portal-branding.test.ts returns match
    - grep "updateBranding" packages/api/src/routers/__tests__/portal-branding.test.ts returns matches
    - grep "hex" packages/api/src/routers/__tests__/portal-branding.test.ts returns match for validation test
    - grep "getNotificationPreferences\|updateNotificationPreference" packages/api/src/routers/__tests__/portal-notification-prefs.test.ts returns matches
    - pnpm --filter @contractor-ops/api test --run exits with code 0
  </acceptance_criteria>
  <done>Notification preference test stubs cover getNotificationPreferences (5-category defaults, PORT-07a), updateNotificationPreference (upsert + SECURITY_ALERTS immutability, PORT-07b/c). Branding test stubs cover updateBranding (hex validation, settingsJson merge, PORT-08a), getBranding, and getOrgBranding. All stubs use it.todo() pattern and pass the test runner.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @contractor-ops/api test --run` passes (all todo stubs recognized, not failures)
- All 4 test stub files exist at the correct paths
- grep confirms PORT-06, PORT-07, PORT-08 requirement IDs are referenced in tests
- grep confirms all key behaviors from VALIDATION.md are represented as it.todo() stubs
</verification>

<success_criteria>
- packages/api/src/services/__tests__/portal-change-request.test.ts exists with stubs for create/approve/reject/duplicate guard
- packages/api/src/routers/__tests__/portal-profile.test.ts exists with stubs for getProfile/updateContactInfo/submitFinancialChangeRequest
- packages/api/src/routers/__tests__/portal-notification-prefs.test.ts exists with stubs for preferences + SECURITY_ALERTS guard
- packages/api/src/routers/__tests__/portal-branding.test.ts exists with stubs for hex validation + settingsJson merge
- pnpm --filter @contractor-ops/api test --run exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/14-portal-self-service-branding/14-05-SUMMARY.md`
</output>
