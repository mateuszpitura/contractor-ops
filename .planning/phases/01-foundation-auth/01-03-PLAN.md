---
phase: 01-foundation-auth
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - apps/web/src/app/[locale]/(auth)/layout.tsx
  - apps/web/src/app/[locale]/(auth)/login/page.tsx
  - apps/web/src/app/[locale]/(auth)/register/page.tsx
  - apps/web/src/app/[locale]/(auth)/invite/[token]/page.tsx
  - apps/web/src/app/[locale]/(auth)/verify-email/page.tsx
  - apps/web/src/components/auth/login-form.tsx
  - apps/web/src/components/auth/register-form.tsx
  - apps/web/src/components/auth/invite-accept-form.tsx
  - apps/web/src/components/auth/social-buttons.tsx
  - apps/web/src/app/[locale]/(dashboard)/layout.tsx
  - apps/web/src/app/[locale]/(dashboard)/page.tsx
  - apps/web/src/app/[locale]/(dashboard)/settings/page.tsx
  - apps/web/src/app/[locale]/(dashboard)/settings/members/page.tsx
  - apps/web/src/components/layout/sidebar.tsx
  - apps/web/src/components/layout/nav-items.tsx
  - apps/web/src/components/layout/org-switcher.tsx
  - apps/web/src/components/layout/top-bar.tsx
  - apps/web/src/components/layout/user-menu.tsx
  - apps/web/src/components/settings/org-settings-form.tsx
  - apps/web/src/components/settings/users-table.tsx
  - apps/web/src/components/settings/invite-dialog.tsx
  - apps/web/src/components/settings/deactivate-dialog.tsx
  - apps/web/src/hooks/use-permissions.ts
  - apps/web/src/lib/navigation.ts
  - apps/web/src/app/[locale]/not-found.tsx
  - apps/web/src/app/[locale]/error.tsx
  - apps/web/src/app/[locale]/(dashboard)/unauthorized/page.tsx
  - apps/web/src/app/globals.css
autonomous: false
requirements:
  - ORG-02
  - ORG-03
  - ORG-05

must_haves:
  truths:
    - "User can register with email, password, and org name and land on dashboard"
    - "User can login with email/password, magic link, or social OAuth"
    - "User can accept an invite by clicking the link and creating an account"
    - "Admin can view team members table with name, email, role, status, last login"
    - "Admin can invite a new member by email with a role"
    - "Admin can deactivate a user with confirmation dialog"
    - "Admin can change a user's role via dropdown in table row"
    - "Admin can configure org settings (name, fiscal year, timezone, language)"
    - "Sidebar collapses to icon-only, has org switcher at top, all 10 nav items"
    - "Top bar shows breadcrumb, search trigger, quick action buttons, notifications bell, user avatar"
    - "Unauthorized nav items are hidden based on user's role permissions"
    - "Dark mode works with system preference detection and manual toggle"
    - "404, 500, and unauthorized error pages are styled"
  artifacts:
    - path: "apps/web/src/components/layout/sidebar.tsx"
      provides: "Collapsible sidebar with nav items filtered by RBAC"
      contains: "SidebarProvider"
    - path: "apps/web/src/components/layout/top-bar.tsx"
      provides: "Top bar with breadcrumb, search, quick actions, notifications, avatar"
      contains: "Breadcrumb"
    - path: "apps/web/src/components/settings/users-table.tsx"
      provides: "User management table with role dropdown and deactivate action"
      contains: "columns"
    - path: "apps/web/src/components/auth/register-form.tsx"
      provides: "Registration form with org name, email, password"
      contains: "createOrganization"
    - path: "apps/web/src/app/globals.css"
      provides: "CSS custom properties for light/dark mode with Indigo accent"
      contains: "--primary: 238"
  key_links:
    - from: "apps/web/src/components/layout/nav-items.tsx"
      to: "apps/web/src/hooks/use-permissions.ts"
      via: "Filter nav items by user's role permissions"
      pattern: "usePermissions"
    - from: "apps/web/src/components/settings/users-table.tsx"
      to: "packages/api/src/routers/user.ts"
      via: "tRPC calls for list, invite, updateRole, deactivate"
      pattern: "trpc.user"
    - from: "apps/web/src/components/auth/register-form.tsx"
      to: "packages/auth/src/client.ts"
      via: "authClient.signUp.email and organization.create"
      pattern: "authClient"
---

<objective>
Build the complete UI layer: auth screens (register, login, invite accept, email verification), app shell (collapsible sidebar with org switcher, top bar with breadcrumb/search/actions), organization settings page, user management with invite/deactivate/role-change, error pages, dark mode, and information density toggle. Install and configure all shadcn/ui components. Apply the Indigo accent color palette and Stripe Dashboard aesthetic from the UI-SPEC.

Purpose: Users can authenticate, navigate the app, manage team members, and configure their organization.
Output: Fully functional auth flows, app shell, user management, and org settings.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-UI-SPEC.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize shadcn/ui, set up auth screens, app shell with sidebar and top bar, and globals.css with Indigo theme</name>
  <files>
    apps/web/src/app/globals.css,
    apps/web/src/app/[locale]/(auth)/layout.tsx,
    apps/web/src/app/[locale]/(auth)/login/page.tsx,
    apps/web/src/app/[locale]/(auth)/register/page.tsx,
    apps/web/src/app/[locale]/(auth)/invite/[token]/page.tsx,
    apps/web/src/app/[locale]/(auth)/verify-email/page.tsx,
    apps/web/src/components/auth/login-form.tsx,
    apps/web/src/components/auth/register-form.tsx,
    apps/web/src/components/auth/invite-accept-form.tsx,
    apps/web/src/components/auth/social-buttons.tsx,
    apps/web/src/app/[locale]/(dashboard)/layout.tsx,
    apps/web/src/app/[locale]/(dashboard)/page.tsx,
    apps/web/src/components/layout/sidebar.tsx,
    apps/web/src/components/layout/nav-items.tsx,
    apps/web/src/components/layout/org-switcher.tsx,
    apps/web/src/components/layout/top-bar.tsx,
    apps/web/src/components/layout/user-menu.tsx,
    apps/web/src/hooks/use-permissions.ts,
    apps/web/src/lib/navigation.ts,
    apps/web/src/app/[locale]/not-found.tsx,
    apps/web/src/app/[locale]/error.tsx,
    apps/web/src/app/[locale]/(dashboard)/unauthorized/page.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation-auth/01-UI-SPEC.md,
    .planning/phases/01-foundation-auth/01-CONTEXT.md,
    apps/web/src/lib/auth-client.ts,
    packages/auth/src/permissions.ts,
    packages/auth/src/roles.ts,
    apps/web/src/trpc/init.ts,
    apps/web/src/app/providers.tsx
  </read_first>
  <action>
    **Step 0: Initialize shadcn/ui CLI** in apps/web. Install all components listed in UI-SPEC Registry Safety section:
    sidebar, dropdown-menu, command, button, input, label, form, table, badge, avatar, dialog, alert-dialog, separator, skeleton, card, tabs, select, switch, breadcrumb, tooltip, sheet, sonner.
    Use the default shadcn preset with Zinc neutral. After installation, override the Indigo accent in globals.css.

    Also install: `pnpm --filter @contractor-ops/web add zustand react-hook-form @hookform/resolvers zod sonner next-themes lucide-react`

    **globals.css:** Set up CSS custom properties for light and dark mode per UI-SPEC Color section. Key values:
    - Light: `--background: 0 0% 100%`, `--muted: 240 4.8% 95.9%`, `--primary: 238 73% 56%` (Indigo-600), `--destructive: 0 84% 60%`
    - Dark: `--background: 240 10% 3.9%`, `--muted: 240 3.7% 15.9%`, `--primary: 238 73% 67%` (Indigo-400), `--destructive: 0 62% 54%`
    - Add semantic colors: `--success: 142 71% 45%`, `--warning: 38 92% 50%`, `--info: 217 91% 60%`
    - Add `.density-compact` class overrides: smaller padding, row height 32px
    - Font: Inter via next/font/google at 14px body, 13px label, 20px heading, 28px display
    - Monospace: JetBrains Mono at 13px for NIP/account numbers

    **Dark mode setup:** Use `next-themes` ThemeProvider in root layout. Inline script in `<head>` to read localStorage before paint (prevent FOUC). Manual toggle stored in localStorage.

    **(auth) layout.tsx:** Centered card layout, no sidebar. White/dark background. Card with 3xl (64px) vertical spacing. Used for login, register, invite.

    **register/page.tsx + register-form.tsx:**
    - Display heading "Create your organization" (28px semibold)
    - Fields: Organization name, Work email, Password (all with labels from UI-SPEC Copywriting)
    - Social OAuth buttons (Google + Microsoft) with divider "or continue with"
    - Primary CTA "Create organization" in Indigo
    - On submit: call authClient.signUp.email() then authClient.organization.create() then redirect to verify-email
    - Form validation using React Hook Form + Zod (createOrganizationSchema)
    - Loading state: spinner replaces button text, fields disabled

    **login/page.tsx + login-form.tsx:**
    - Display heading "Sign in to Contractor Ops"
    - Fields: Work email, Password
    - Primary CTA "Sign in" in Indigo
    - "Sign in with email link" secondary button (magic link flow)
    - Social OAuth buttons below divider
    - On submit: call authClient.signIn.email(), on success redirect to dashboard
    - Magic link: call authClient.signIn.magicLink(), show "Check your email" message

    **invite/[token]/page.tsx + invite-accept-form.tsx:**
    - Display heading "Join {orgName}" (org name from invite token)
    - Pre-filled email from invite
    - Password field (or social OAuth buttons)
    - CTA "Accept and join"
    - On submit: call authClient.signUp.email() then authClient.organization.acceptInvitation()

    **verify-email/page.tsx:** Simple page with "Check your email to verify your account." banner. Auto-redirect to dashboard after verification callback.

    **social-buttons.tsx:** Shared component with Google and Microsoft OAuth buttons. Each calls authClient.signIn.social({ provider: "google"|"microsoft" }).

    **(dashboard) layout.tsx:** SidebarProvider + Sidebar (collapsible="icon") + main content area. Structure from RESEARCH.md shadcn sidebar example.

    **sidebar.tsx:** Using shadcn Sidebar component:
    - SidebarHeader: OrgSwitcher component
    - SidebarContent: NavItems component (all 10 items)
    - SidebarFooter: UserMenu component
    - Expanded width: 240px. Collapsed: 48px.
    - Collapse animation: 200ms ease-out
    - Below 1024px: Sheet component for mobile sidebar

    **nav-items.tsx:** All 10 navigation items from UI-SPEC Navigation Items table:
    - Dashboard (LayoutDashboard), Contractors (Users), Contracts (FileText), Workflows (GitBranch), Invoices (Receipt), Approvals (CheckCircle), Payments (Banknote), Reports (BarChart3), Integrations (Plug), Settings (Settings)
    - Filter items by user's role permissions using usePermissions hook
    - Unauthorized items are completely HIDDEN (not disabled/grayed) per locked decision
    - Active item has Indigo indicator (left border or background highlight)

    **org-switcher.tsx:** Dropdown at top of sidebar showing current org name. If user has multiple orgs, shows list to switch. Calls authClient.organization.setActive() on switch.

    **top-bar.tsx:** Horizontal bar above main content:
    - Left: SidebarTrigger (hamburger) + Breadcrumb component (current page path)
    - Right: Quick action icon buttons (UserPlus for "Add contractor", Upload for "Upload invoice"), Search icon button (Cmd+K, triggers Command dialog placeholder), Bell icon (notifications placeholder), User Avatar with dropdown menu
    - Icons from Lucide React per UI-SPEC Top Bar Quick Actions table
    - All icon buttons have Tooltip with label

    **user-menu.tsx:** Avatar dropdown (DropdownMenu) with: user name/email, Settings link, dark mode toggle (Switch), density toggle (comfortable/compact), Sign out action.

    **use-permissions.ts:** Custom hook that reads current user's role from session and returns permission checker:
    ```typescript
    export function usePermissions() {
      const session = authClient.useSession();
      const role = session?.data?.member?.role;
      return {
        can: (resource: string, action: string) => {
          // Check against the roles definition
        },
        role,
      };
    }
    ```

    **navigation.ts:** Navigation config array defining all 10 items with: key, href, icon, requiredPermission (resource + action for visibility check).

    **Dashboard page.tsx:** Empty state with centered content:
    - Heading "Welcome to Contractor Ops" (20px semibold)
    - Body "Your organization is set up. Start by adding your first contractor." (14px)
    - CTA button "Add contractor" in Indigo (links to /contractors/new, placeholder)

    **Error pages per UI-SPEC:**
    - not-found.tsx: "Page not found" heading, "The page you are looking for does not exist or has been moved." body, "Go to dashboard" CTA
    - error.tsx: "Something went wrong" heading, body, "Try again" CTA (client component with reset)
    - unauthorized/page.tsx: "Access denied" heading, "You do not have permission..." body, "Go to dashboard" CTA

    All copy uses hardcoded EN strings for now (i18n in Plan 04).
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web build</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/globals.css contains "--primary: 238 73% 56%" (light) and "--primary: 238 73% 67%" (dark)
    - apps/web/src/app/globals.css contains "--success:" and "--warning:" and "--info:"
    - apps/web/src/app/globals.css contains "density-compact"
    - apps/web/src/components/auth/register-form.tsx contains "Create your organization" or uses a translation key
    - apps/web/src/components/auth/register-form.tsx contains "authClient" and "signUp"
    - apps/web/src/components/auth/login-form.tsx contains "Sign in" and "magicLink"
    - apps/web/src/components/auth/social-buttons.tsx contains "google" and "microsoft"
    - apps/web/src/components/layout/sidebar.tsx contains "SidebarProvider" and "collapsible"
    - apps/web/src/components/layout/nav-items.tsx contains "LayoutDashboard" and "Receipt" and "Banknote" and "Settings"
    - apps/web/src/components/layout/org-switcher.tsx contains "DropdownMenu" and "setActive"
    - apps/web/src/components/layout/top-bar.tsx contains "Breadcrumb" and "UserPlus" and "Bell"
    - apps/web/src/hooks/use-permissions.ts contains "usePermissions" and "can"
    - apps/web/src/lib/navigation.ts contains 10 navigation items (Dashboard through Settings)
    - apps/web/src/app/[locale]/not-found.tsx contains "Page not found"
    - apps/web/src/app/[locale]/(dashboard)/unauthorized/page.tsx contains "Access denied"
    - Next.js build succeeds
  </acceptance_criteria>
  <done>Auth screens (register, login, invite, verify-email), app shell (sidebar with org switcher, top bar, nav items filtered by RBAC), error pages, dark mode, and density toggle are implemented. shadcn/ui components installed with Indigo theme.</done>
</task>

<task type="auto">
  <name>Task 2: Build organization settings page and user management table with invite, role change, and deactivate</name>
  <files>
    apps/web/src/app/[locale]/(dashboard)/settings/page.tsx,
    apps/web/src/app/[locale]/(dashboard)/settings/members/page.tsx,
    apps/web/src/components/settings/org-settings-form.tsx,
    apps/web/src/components/settings/users-table.tsx,
    apps/web/src/components/settings/invite-dialog.tsx,
    apps/web/src/components/settings/deactivate-dialog.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation-auth/01-UI-SPEC.md,
    .planning/phases/01-foundation-auth/01-CONTEXT.md,
    packages/api/src/routers/user.ts,
    packages/api/src/routers/settings.ts,
    packages/validators/src/organization.ts,
    packages/validators/src/user.ts,
    apps/web/src/trpc/init.ts,
    apps/web/src/hooks/use-permissions.ts
  </read_first>
  <action>
    **settings/page.tsx:** Settings page with Tabs component (General, Members tabs). General tab shows OrgSettingsForm. Members tab links to /settings/members. Page title "Organization settings" (Heading size 20px semibold). Only accessible to users with settings.read permission.

    **settings/members/page.tsx:** Members page title "Team members" (Heading). Server-side prefetch of trpc.user.list. Renders UsersTable wrapped in HydrateClient. "Invite member" button at top (only shown if user has member.create permission). Button opens InviteDialog.

    **org-settings-form.tsx:** Form using React Hook Form + Zod (updateOrganizationSettingsSchema):
    - Organization name (Input)
    - Legal name (Input, optional)
    - Country (Select dropdown, ISO 3166-1 alpha-2 codes, at least PL, DE, GB, US, CZ, SK)
    - Default currency (Select, at least PLN, EUR, USD, GBP, CZK)
    - Timezone (Select, at least Europe/Warsaw, Europe/Berlin, Europe/London, UTC)
    - Language (Select: Polish, English)
    - Fiscal year start month (Select, 1-12)
    - Billing email (Input, email type)
    - Save button "Save changes" in Indigo
    - On submit: call trpc.settings.update mutate, show sonner toast "Settings saved" on success
    - Loading: button shows spinner + "Saving...", fields disabled
    - Fetch current values via trpc.settings.get query

    **users-table.tsx:** Table using shadcn Table component (not DataTable/TanStack for now, simple table is sufficient for v1 team sizes):
    - Columns: Name (first + last), Email, Role (Badge), Status (Badge with color), Last login (relative date)
    - Role column: DropdownMenu triggered by clicking the role badge. Lists all 8 roles. On select, calls trpc.user.updateRole. Only shown for admin role users.
    - Status column: "Active" in green badge, "Invited" in blue badge, "Disabled" in red badge
    - Actions column (visible to admins): "Deactivate" button (destructive variant) opens DeactivateDialog. For disabled users, show "Reactivate" button.
    - Empty state: "No team members yet" heading, "Invite your team to start collaborating." body (per UI-SPEC)
    - Skeleton loading: 5 skeleton rows matching column widths

    **invite-dialog.tsx:** Dialog component:
    - Title: "Invite a new member"
    - Body: "They will receive an email with a link to join {orgName}."
    - Form fields: Email (Input), Role (Select with all 8 roles)
    - CTA: "Send invite" button in Indigo
    - On submit: call trpc.user.invite, close dialog on success, show toast "Invitation sent"
    - Form validation with Zod (inviteUserSchema)
    - Loading state on submit button

    **deactivate-dialog.tsx:** AlertDialog component:
    - Title: "Deactivate {userName}?"
    - Body: "This will immediately revoke their access. They will be logged out of all sessions. This action can be reversed."
    - Cancel button + "Deactivate member" destructive button
    - On confirm: call trpc.user.deactivate, close dialog, show toast, refresh user list
    - Loading state on confirm button

    All tables respect the density setting (comfortable: 16px cell padding, 40px row height; compact: 8px, 32px). Read density from Zustand store with localStorage persistence.

    Role badge colors:
    - admin: Indigo
    - finance_admin: Emerald
    - ops_manager: Amber
    - team_manager: Sky
    - legal_compliance_viewer: Violet
    - it_admin: Slate
    - external_accountant: Teal
    - readonly: Gray
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web build</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/[locale]/(dashboard)/settings/page.tsx contains "Organization settings" and "Tabs"
    - apps/web/src/app/[locale]/(dashboard)/settings/members/page.tsx contains "Team members" and "Invite member"
    - apps/web/src/components/settings/org-settings-form.tsx contains "updateOrganizationSettingsSchema" or equivalent fields and "trpc" and "settings.update"
    - apps/web/src/components/settings/org-settings-form.tsx contains "Save changes" and "sonner" or "toast"
    - apps/web/src/components/settings/users-table.tsx contains "columns" or table structure and "trpc.user" and "DropdownMenu" for role change
    - apps/web/src/components/settings/users-table.tsx contains all 8 role names
    - apps/web/src/components/settings/invite-dialog.tsx contains "Invite a new member" and "Send invite" and "trpc.user.invite"
    - apps/web/src/components/settings/deactivate-dialog.tsx contains "Deactivate" and "AlertDialog" and "trpc.user.deactivate"
    - Next.js build succeeds
  </acceptance_criteria>
  <done>Organization settings page with General form and Members tab. User management table with name, email, role badge, status badge, last login. Invite dialog, role change dropdown, and deactivate confirmation dialog. All wired to tRPC routers. Density-aware table styling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify auth flows and app shell</name>
  <files>apps/web/src/app/[locale]/(dashboard)/layout.tsx</files>
  <action>Human verification checkpoint. Start dev server and verify all auth flows, app shell, user management, dark mode, and error pages visually and functionally.</action>
  <what-built>Complete auth flow (register with org, login, invite), app shell with collapsible sidebar (Linear-style), org switcher, top bar, dark mode, user management with invite/deactivate/role-change, and org settings.</what-built>
  <how-to-verify>
    1. Start dev server: `pnpm --filter @contractor-ops/web dev`
    2. Visit http://localhost:3000/register — verify registration form with org name, email, password, social buttons, Indigo CTA
    3. Create account — verify redirect to email verification page
    4. Visit http://localhost:3000/login — verify login form with email/password, magic link option, social buttons
    5. Login — verify landing on dashboard with "Welcome to Contractor Ops" empty state
    6. Check sidebar: all 10 nav items visible, org switcher at top, user menu at bottom
    7. Collapse sidebar — verify icon-only mode with tooltips
    8. Check top bar: breadcrumb, search icon, quick action buttons, notifications bell, user avatar
    9. Toggle dark mode in user menu — verify theme switches correctly with no flash
    10. Toggle density (comfortable vs compact) — verify table spacing changes
    11. Navigate to Settings > General — verify org settings form
    12. Navigate to Settings > Members — verify user table with invite button
    13. Click "Invite member" — verify dialog with email and role fields
    14. Check 404 page at /nonexistent — verify styled error page
    15. Resize browser to 1024px — verify sidebar collapses; below 1024px verify sheet/hamburger
  </how-to-verify>
  <verify>Human visual verification of auth flows, app shell, and user management</verify>
  <done>User confirms all auth flows work, sidebar collapses correctly, dark mode toggles, user management table renders, and error pages are styled</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Auth screens render correctly with Indigo theme
- Sidebar collapses/expands with 200ms animation
- All 10 nav items present, filtered by role
- User management table shows members with role badges
- Invite, role change, and deactivate flows work end-to-end
- Dark mode toggles without flash
- Error pages (404, 500, unauthorized) are styled
</verification>

<success_criteria>
- Users can register creating an org and land on authenticated dashboard
- Users can login via email/password, magic link, or social OAuth
- Admin can view, invite, change role, and deactivate team members
- Admin can configure organization settings
- Sidebar is collapsible Linear-style with org switcher and 10 nav items
- Top bar has breadcrumb, search trigger, quick actions, notifications, avatar
- Dark mode and density toggle work
- Error pages are styled
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-03-SUMMARY.md`
</output>
