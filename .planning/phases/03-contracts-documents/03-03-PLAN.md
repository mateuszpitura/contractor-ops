---
phase: 03-contracts-documents
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/web/src/app/[locale]/(dashboard)/contracts/page.tsx
  - apps/web/src/components/contracts/contract-table/columns.tsx
  - apps/web/src/components/contracts/contract-table/data-table.tsx
  - apps/web/src/components/contracts/contract-table/data-table-toolbar.tsx
  - apps/web/src/components/contracts/contract-table/data-table-filters.tsx
  - apps/web/src/components/contracts/contract-table/data-table-pagination.tsx
  - apps/web/src/components/contracts/contract-table/data-table-bulk-actions.tsx
  - apps/web/src/components/contracts/contract-table/data-table-column-toggle.tsx
  - apps/web/src/components/contracts/contract-table/use-contract-filters.ts
  - apps/web/src/components/contracts/contract-side-panel.tsx
  - apps/web/src/components/contracts/contract-wizard/wizard-dialog.tsx
  - apps/web/src/components/contracts/contract-wizard/step-details.tsx
  - apps/web/src/components/contracts/contract-wizard/step-financial.tsx
  - apps/web/src/components/contracts/contract-wizard/step-documents.tsx
  - apps/web/messages/en.json
  - apps/web/messages/pl.json
autonomous: true
requirements: [CNTR-01, CNTR-03]

must_haves:
  truths:
    - "User can see contract list at /contracts with TanStack Table (Title, Contractor, Type, Status, Start date, End date, Rate, Currency, Billing cycle, Owner, Compliance risk)"
    - "User can search contracts with FTS across title, contractor name, type"
    - "User can filter contracts by status, type, billing model, owner, end date range, compliance risk"
    - "User can click a row to open side panel with contract summary and 'Open contract' CTA"
    - "User can create a contract via 3-step wizard with pre-fill from contractor billing profile"
  artifacts:
    - path: "apps/web/src/app/[locale]/(dashboard)/contracts/page.tsx"
      provides: "Contract list page route"
      min_lines: 20
    - path: "apps/web/src/components/contracts/contract-table/data-table.tsx"
      provides: "TanStack Table with server-side pagination, sorting, filtering"
      min_lines: 50
    - path: "apps/web/src/components/contracts/contract-wizard/wizard-dialog.tsx"
      provides: "3-step contract creation wizard dialog"
      min_lines: 50
    - path: "apps/web/src/components/contracts/contract-side-panel.tsx"
      provides: "Slide-out side panel for contract summary on row click"
      min_lines: 30
  key_links:
    - from: "apps/web/src/components/contracts/contract-table/data-table.tsx"
      to: "trpc.contract.list"
      via: "tRPC query for server-side data"
      pattern: "trpc\\.contract\\.list"
    - from: "apps/web/src/components/contracts/contract-wizard/wizard-dialog.tsx"
      to: "trpc.contract.create"
      via: "tRPC mutation on form submit"
      pattern: "trpc\\.contract\\.create"
---

<objective>
Contract list page with full TanStack Table (search, filters, sorting, pagination, bulk actions), slide-out side panel on row click, and 3-step contract creation wizard with contractor billing pre-fill.

Purpose: Primary contract management interface. Follows exact same patterns as contractor list from Phase 2.
Output: Working /contracts page with data table, side panel, and creation wizard. Full i18n for Contracts namespace.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-contracts-documents/03-CONTEXT.md
@.planning/phases/03-contracts-documents/03-RESEARCH.md
@.planning/phases/03-contracts-documents/03-UI-SPEC.md
@.planning/phases/03-contracts-documents/03-01-SUMMARY.md

<interfaces>
<!-- Pattern files to follow exactly -->

From apps/web/src/components/contractors/contractor-table/ (mirror this structure):
- columns.tsx: Column definitions with accessorKey, header, cell renderers, sorting
- data-table.tsx: useReactTable with manualPagination, manualSorting, manualFiltering, tRPC query
- data-table-toolbar.tsx: Search input + filter buttons + column toggle
- data-table-bulk-actions.tsx: Appears when rows selected
- data-table-pagination.tsx: Page size selector + prev/next + page indicator
- data-table-column-toggle.tsx: Dropdown with column visibility checkboxes
- use-contractor-filters.ts: nuqs useQueryStates hook for URL-synced state

From apps/web/src/components/contractors/contractor-wizard/ (mirror this structure):
- wizard-dialog.tsx: Dialog with step progress indicator, form with react-hook-form + zodResolver
- step-company.tsx, step-billing.tsx, step-assignment.tsx: Individual step content components

From apps/web/src/components/contractors/contractor-side-panel.tsx (mirror this pattern):
- Sheet component from right (480px), contract summary card, CTA button

From packages/validators/src/contract.ts (created in Plan 01):
- contractCreateSchema, contractListSchema — used as form validation and query input
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contract TanStack Table with columns, filters, pagination, bulk actions, side panel, and URL state</name>
  <files>
    apps/web/src/app/[locale]/(dashboard)/contracts/page.tsx
    apps/web/src/components/contracts/contract-table/columns.tsx
    apps/web/src/components/contracts/contract-table/data-table.tsx
    apps/web/src/components/contracts/contract-table/data-table-toolbar.tsx
    apps/web/src/components/contracts/contract-table/data-table-filters.tsx
    apps/web/src/components/contracts/contract-table/data-table-pagination.tsx
    apps/web/src/components/contracts/contract-table/data-table-bulk-actions.tsx
    apps/web/src/components/contracts/contract-table/data-table-column-toggle.tsx
    apps/web/src/components/contracts/contract-table/use-contract-filters.ts
    apps/web/src/components/contracts/contract-side-panel.tsx
  </files>
  <read_first>
    apps/web/src/components/contractors/contractor-table/columns.tsx
    apps/web/src/components/contractors/contractor-table/data-table.tsx
    apps/web/src/components/contractors/contractor-table/data-table-toolbar.tsx
    apps/web/src/components/contractors/contractor-table/data-table-bulk-actions.tsx
    apps/web/src/components/contractors/contractor-table/data-table-pagination.tsx
    apps/web/src/components/contractors/contractor-table/data-table-column-toggle.tsx
    apps/web/src/components/contractors/contractor-table/use-contractor-filters.ts
    apps/web/src/components/contractors/contractor-side-panel.tsx
    apps/web/src/app/[locale]/(dashboard)/contractors/page.tsx
  </read_first>
  <action>
Mirror the contractor table structure exactly for contracts. Create all files under `apps/web/src/components/contracts/contract-table/` and `apps/web/src/components/contracts/contract-side-panel.tsx`.

**columns.tsx** — 11 columns per UI-SPEC:
1. Select checkbox (header: select all, cell: individual checkbox)
2. Title (accessorKey: "title", sortable)
3. Contractor (accessorKey: "contractor.companyName" or "contractor.legalName", cell: link to contractor profile)
4. Type (accessorKey: "type", cell: Badge with translated contract type)
5. Status (accessorKey: "status", cell: Badge with status-specific colors per UI-SPEC: Draft=muted, Active=success 10%, Expiring=warning 10%, Expired=destructive 10%, Terminated=muted, Superseded=muted 50%)
6. Start date (accessorKey: "startDate", cell: formatted with useFormatter)
7. End date (accessorKey: "endDate", cell: formatted, show "days remaining" in tooltip)
8. Rate (accessorKey: "rateValueGrosze", cell: formatted as currency dividing by 100, monospace font)
9. Currency (accessorKey: "currency")
10. Billing cycle (accessorKey: "billingModel", cell: translated enum)
11. Owner (accessorKey: "internalOwner.name")
12. Compliance risk (accessorKey: "complianceRiskLevel", cell: colored badge green/yellow/red)

Default sort: endDate ascending (expiring soonest first).

**use-contract-filters.ts** — nuqs useQueryStates hook with parsers for:
- search (parseAsString)
- page (parseAsInteger, default 1)
- pageSize (parseAsInteger, default 25)
- sortBy (parseAsString, default "endDate")
- sortOrder (parseAsString, default "asc")
- status (parseAsArrayOf parseAsString)
- type (parseAsArrayOf parseAsString)
- billingModel (parseAsArrayOf parseAsString)
- ownerUserId (parseAsArrayOf parseAsString)
- endDateFrom (parseAsString)
- endDateTo (parseAsString)
- complianceRiskLevel (parseAsArrayOf parseAsString)

**data-table.tsx** — useReactTable with:
- manualPagination, manualSorting, manualFiltering all true
- tRPC query: `trpc.contract.list.useQuery()` with filter state from use-contract-filters
- Column visibility persisted via localStorage key "contract-table-columns"
- Row selection state
- onRowClick: opens side panel (NOT navigation)
- Search debounce: 300ms, minimum 2 characters

**data-table-toolbar.tsx** — Search input with loading indicator + filter button + column toggle + "New contract" Indigo CTA button (opens wizard)

**data-table-filters.tsx** — Popover panels for multi-select status, type, owner. Date range for endDate. Active filters shown as badges below with "x" remove and "Clear all" link.

**data-table-pagination.tsx** — Bottom of table. Page size selector (10, 25, 50). Previous/Next. "Page X of Y".

**data-table-bulk-actions.tsx** — Toolbar when 1+ rows selected. Actions: Export contracts (CSV/XLSX dropdown), Terminate (confirmation dialog). "{count} selected" label.

**data-table-column-toggle.tsx** — Dropdown menu with checkboxes per column.

**contract-side-panel.tsx** — Sheet from right (480px), 200ms ease-out. Content per UI-SPEC:
- Contract title (Heading), status badge, contractor name as link
- Compact card: type, start/end dates, rate, currency, billing cycle, payment terms
- Documents count with icon
- Key dates with days-remaining calculation
- "Open contract" CTA button linking to `/contracts/{id}`

**page.tsx** — Contracts list page route:
- Suspense boundary wrapping the table (same as contractors page for nuqs)
- Page heading "Contracts" (translated)
- "New contract" button triggers wizard
- DataTable + SidePanel rendering

All text must use `useTranslations("Contracts")` — add all copy to i18n task in Plan 04.

Empty state per UI-SPEC: centered FileText icon, heading "No contracts yet", body text, "New contract" CTA.
No search results state: "No contracts found", "Try adjusting your search or filters", "Clear filters" CTA.
Loading state: 8 skeleton rows.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/[locale]/(dashboard)/contracts/page.tsx exists and contains `Suspense`
    - apps/web/src/components/contracts/contract-table/columns.tsx contains `title` and `status` and `contractor` and `endDate` and `rateValueGrosze`
    - apps/web/src/components/contracts/contract-table/data-table.tsx contains `useReactTable` and `manualPagination` and `trpc.contract.list`
    - apps/web/src/components/contracts/contract-table/data-table-toolbar.tsx contains `search` and `useTranslations`
    - apps/web/src/components/contracts/contract-table/data-table-filters.tsx contains `Popover`
    - apps/web/src/components/contracts/contract-table/data-table-bulk-actions.tsx contains `Terminate` or `bulkTransition`
    - apps/web/src/components/contracts/contract-table/use-contract-filters.ts contains `useQueryStates` and `parseAs`
    - apps/web/src/components/contracts/contract-side-panel.tsx contains `Sheet` and `480` and `Open contract`
    - apps/web/src/components/contracts/contract-table/data-table-pagination.tsx contains `pageSize`
  </acceptance_criteria>
  <done>Contract list page renders with TanStack Table, all columns, search with FTS, multi-facet filters, pagination, bulk actions, side panel on row click, and empty/loading states</done>
</task>

<task type="auto">
  <name>Task 2: 3-step contract creation wizard with contractor billing pre-fill</name>
  <files>
    apps/web/src/components/contracts/contract-wizard/wizard-dialog.tsx
    apps/web/src/components/contracts/contract-wizard/step-details.tsx
    apps/web/src/components/contracts/contract-wizard/step-financial.tsx
    apps/web/src/components/contracts/contract-wizard/step-documents.tsx
  </files>
  <read_first>
    apps/web/src/components/contractors/contractor-wizard/wizard-dialog.tsx
    apps/web/src/components/contractors/contractor-wizard/step-company.tsx
    apps/web/src/components/contractors/contractor-wizard/step-billing.tsx
    packages/validators/src/contract.ts
  </read_first>
  <action>
Mirror the contractor wizard pattern exactly. Create under `apps/web/src/components/contracts/contract-wizard/`.

**wizard-dialog.tsx** — Dialog (640px max-width) with:
- 3-step progress indicator at top: "Contract details" (step 1), "Financial terms" (step 2), "Documents" (step 3)
- Completed steps show checkmark icon, current step highlighted with `--primary`, future steps grayed
- Single react-hook-form with zodResolver using local Zod schema that mirrors contractCreateSchema from validators (same pattern as contractor wizard — local schema to avoid cross-package dependency)
- Step navigation: "Next: Financial terms" / "Next: Documents" / "Create contract" CTAs
- "Back" button on steps 2-3
- Per-step validation: step does not advance until fields valid
- Props: `open`, `onOpenChange`, `contractorId?: string` (for pre-fill when opened from contractor profile)
- When contractorId provided: contractor field auto-selected and read-only, fetch contractor's billing profile via `trpc.contractor.getById` and pre-fill financial terms (rateValueGrosze, currency, billingModel)
- Pre-filled fields show hint text: "Pre-filled from contractor billing profile" (translated)
- Dismiss logic: pristine form shows "Close", modified form shows "Discard changes" with confirmation dialog
- On submit: call `trpc.contract.create.mutate()`, on success close dialog, show sonner toast "Contract created", invalidate contract list query

**step-details.tsx** — Step 1 form fields:
- Contractor picker (Combobox/Select with search, fetching from `trpc.contractor.list`). Required. Read-only if contractorId prop provided.
- Contract title (Input, required)
- Contract type (Select with all ContractType enum values, translated)
- Start date (Calendar date picker, required)
- End date (Calendar date picker, optional)
- Notice period days (Input number, optional)
- Auto-renewal (Checkbox)

**step-financial.tsx** — Step 2 form fields:
- Rate (Input number, required, display as zloty but store as grosze — divide by 100 for display, multiply by 100 for storage)
- Currency (Select: PLN, EUR, USD)
- Billing model (Select with BillingModel enum values, translated)
- Rate type (Select with RateType enum values, translated)
- Payment terms days (Input number, required)
- Invoice cycle (Select with InvoiceCycle enum values, translated)
- Show "Pre-filled from contractor billing profile" hint below each pre-filled field (only when pre-filled from contractor)

**step-documents.tsx** — Step 3:
- Import DropZone component (will be created in Plan 04, for now create a placeholder or basic file input)
- Shows uploaded files with filename, size, progress (basic implementation)
- "Skip, add documents later" link below drop zone
- Documents uploaded immediately on selection (not deferred to form submit) — call `trpc.document.requestUpload` + direct PUT to presigned URL
- Each file shows upload progress bar, then scan status badge once confirmed

Note: For Step 3, since the DropZone component is created in Plan 04, use a basic implementation with react-dropzone directly here. The reusable DropZone from Plan 04 can be swapped in later if needed.

All text uses `useTranslations("Contracts")`. Use copy from UI-SPEC Copywriting Contract section.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/contracts/contract-wizard/wizard-dialog.tsx contains `Dialog` and `useForm` and `zodResolver`
    - apps/web/src/components/contracts/contract-wizard/wizard-dialog.tsx contains `trpc.contract.create`
    - apps/web/src/components/contracts/contract-wizard/wizard-dialog.tsx contains `contractorId`
    - apps/web/src/components/contracts/contract-wizard/step-details.tsx contains `contractor` and `title` and `type` and `startDate`
    - apps/web/src/components/contracts/contract-wizard/step-financial.tsx contains `rateValueGrosze` and `currency` and `billingModel` and `paymentTermsDays`
    - apps/web/src/components/contracts/contract-wizard/step-financial.tsx contains `Pre-filled` or `preFilled`
    - apps/web/src/components/contracts/contract-wizard/step-documents.tsx contains `useDropzone` or `DropZone` or `drop`
    - apps/web/src/components/contracts/contract-wizard/step-documents.tsx contains `skip` or `Skip`
  </acceptance_criteria>
  <done>3-step wizard creates contracts, pre-fills from contractor billing profile, uploads documents in step 3, shows progress and scan status</done>
</task>

</tasks>

<verification>
- `pnpm --filter @contractor-ops/web exec tsc --noEmit` exits 0
- /contracts page renders with table, search, filters, pagination
- Row click opens side panel with contract summary
- "New contract" opens 3-step wizard
- Wizard step validation prevents advancing without required fields
- Contractor pre-fill works when opened from contractor context
</verification>

<success_criteria>
- Contract list page at /contracts with TanStack Table, all 11 columns, server-side pagination/sorting/filtering/FTS
- URL-synced filter state via nuqs
- Side panel on row click with contract summary and "Open contract" CTA
- 3-step wizard with contractor billing pre-fill
- Bulk actions (export, terminate) on selected rows
- All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-contracts-documents/03-03-SUMMARY.md`
</output>
