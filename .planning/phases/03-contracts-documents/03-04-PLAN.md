---
phase: 03-contracts-documents
plan: 04
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - apps/web/src/app/[locale]/(dashboard)/contracts/[id]/page.tsx
  - apps/web/src/components/contracts/contract-detail/detail-header.tsx
  - apps/web/src/components/contracts/contract-detail/overview-tab.tsx
  - apps/web/src/components/contracts/contract-detail/documents-tab.tsx
  - apps/web/src/components/contracts/contract-detail/amendments-tab.tsx
  - apps/web/src/components/contracts/contract-detail/activity-tab.tsx
  - apps/web/src/components/documents/drop-zone.tsx
  - apps/web/src/components/documents/document-card.tsx
  - apps/web/src/components/documents/document-list.tsx
  - apps/web/src/components/documents/pdf-preview.tsx
  - apps/web/src/components/documents/upload-progress.tsx
  - apps/web/src/components/documents/version-history.tsx
  - apps/web/src/components/contractors/contractor-profile/profile-tabs.tsx
  - apps/web/src/components/contractors/contractor-profile/tab-contracts.tsx
  - apps/web/src/components/contractors/contractor-profile/tab-documents.tsx
  - apps/web/messages/en.json
  - apps/web/messages/pl.json
autonomous: true
requirements: [CNTR-02, CNTR-04, CNTR-05, DOCS-01, DOCS-02, DOCS-03, DOCS-04]

must_haves:
  truths:
    - "User can view contract detail at /contracts/{id} with Overview, Documents, Amendments, Activity tabs"
    - "User can upload documents via drag-and-drop with progress, MIME validation, and scan status display"
    - "User can download documents via signed URL (click Download button)"
    - "User can preview PDF documents inline in a dialog"
    - "User can upload new document versions and view version history"
    - "User can add amendments with timeline display showing chronological history"
    - "Contractor profile Contracts tab shows mini table filtered to that contractor"
    - "Contractor profile Documents tab shows document cards filtered to that contractor"
    - "All UI text available in Polish and English"
  artifacts:
    - path: "apps/web/src/app/[locale]/(dashboard)/contracts/[id]/page.tsx"
      provides: "Contract detail page route"
      min_lines: 20
    - path: "apps/web/src/components/documents/drop-zone.tsx"
      provides: "Reusable drag-and-drop upload component"
      min_lines: 30
    - path: "apps/web/src/components/contracts/contract-detail/amendments-tab.tsx"
      provides: "Amendment timeline view"
      min_lines: 30
    - path: "apps/web/src/components/contractors/contractor-profile/tab-contracts.tsx"
      provides: "Contractor profile contracts tab implementation"
      min_lines: 20
  key_links:
    - from: "apps/web/src/components/contracts/contract-detail/documents-tab.tsx"
      to: "trpc.document.requestUpload"
      via: "presigned URL upload flow"
      pattern: "trpc\\.document\\.requestUpload"
    - from: "apps/web/src/components/documents/drop-zone.tsx"
      to: "trpc.document.requestUpload"
      via: "tRPC mutation for presigned URL"
      pattern: "requestUpload|presigned"
    - from: "apps/web/src/components/contractors/contractor-profile/profile-tabs.tsx"
      to: "tab-contracts.tsx"
      via: "contractsContent prop replaces TabPlaceholder"
      pattern: "contractsContent|TabContracts"
---

<objective>
Contract detail page (4 tabs), reusable document components (drop zone, cards, PDF preview, versioning), contractor profile tab integration, expiry reminder settings, and full i18n for Phase 3.

Purpose: Complete the contract/document UI. Contract detail provides the full view of a single contract. Document components are reusable across contract detail, contractor profile, and wizard. Contractor profile tabs replace Phase 2 placeholders.
Output: Working contract detail page, document upload/download/preview, amendment timeline, contractor tab integration, all i18n.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-contracts-documents/03-CONTEXT.md
@.planning/phases/03-contracts-documents/03-RESEARCH.md
@.planning/phases/03-contracts-documents/03-UI-SPEC.md
@.planning/phases/03-contracts-documents/03-01-SUMMARY.md
@.planning/phases/03-contracts-documents/03-02-SUMMARY.md
@.planning/phases/03-contracts-documents/03-03-SUMMARY.md

<interfaces>
<!-- Existing profile tabs to modify -->
From apps/web/src/components/contractors/contractor-profile/profile-tabs.tsx:
- TAB_KEYS: ["overview", "contracts", "documents", "workflows", "invoices", "payments", "activity", "compliance"]
- ProfileTabsProps: { overviewContent, complianceContent, activityContent }
- Contracts and Documents tabs currently show TabPlaceholder
- Need to add contractsContent and documentsContent props

From packages/api/src/routers/contract.ts (Plan 01):
- trpc.contract.getById returns contract with contractor, amendments, document count
- trpc.contract.listAmendments returns amendments ordered by effectiveDate desc
- trpc.contract.createAmendment creates amendment

From packages/api/src/routers/document.ts (Plan 02):
- trpc.document.requestUpload returns { documentId, uploadUrl, storageKey }
- trpc.document.confirmUpload confirms upload after direct R2 PUT
- trpc.document.getDownloadUrl returns { url, expiresIn }
- trpc.document.list returns documents filtered by entityType/entityId
- trpc.document.uploadNewVersion returns { documentId, uploadUrl, storageKey }
- trpc.document.getVersionHistory returns version chain

From apps/web/src/components/contracts/ (Plan 03):
- contract-wizard already exists with step-documents.tsx
- contract-table/ already exists
- contract-side-panel.tsx already exists
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Contract detail page with 4 tabs, reusable document components, and amendment timeline</name>
  <files>
    apps/web/src/app/[locale]/(dashboard)/contracts/[id]/page.tsx
    apps/web/src/components/contracts/contract-detail/detail-header.tsx
    apps/web/src/components/contracts/contract-detail/overview-tab.tsx
    apps/web/src/components/contracts/contract-detail/documents-tab.tsx
    apps/web/src/components/contracts/contract-detail/amendments-tab.tsx
    apps/web/src/components/contracts/contract-detail/activity-tab.tsx
    apps/web/src/components/documents/drop-zone.tsx
    apps/web/src/components/documents/document-card.tsx
    apps/web/src/components/documents/document-list.tsx
    apps/web/src/components/documents/pdf-preview.tsx
    apps/web/src/components/documents/upload-progress.tsx
    apps/web/src/components/documents/version-history.tsx
  </files>
  <read_first>
    apps/web/src/app/[locale]/(dashboard)/contractors/[id]/page.tsx
    apps/web/src/components/contractors/contractor-profile/profile-header.tsx
    apps/web/src/components/contractors/contractor-profile/tab-overview.tsx
    apps/web/src/components/contractors/contractor-profile/tab-compliance.tsx
    apps/web/src/components/contractors/contractor-profile/profile-tabs.tsx
    apps/web/src/components/ui/tabs.tsx
    apps/web/src/components/ui/sheet.tsx
    apps/web/src/components/ui/card.tsx
    apps/web/src/components/ui/badge.tsx
  </read_first>
  <action>
**Contract detail page (`apps/web/src/app/[locale]/(dashboard)/contracts/[id]/page.tsx`):**
- Server component that fetches contract via `trpc.contract.getById` with SSR prefetch (follow contractor profile page.tsx pattern)
- URL: `/contracts/{id}` with `?tab={tabName}` for deep linking via nuqs
- Renders detail-header + Tabs component with 4 tabs

**detail-header.tsx:**
- Full-width header: Contract title (Heading 20px semibold), status badge (with status-specific colors from UI-SPEC), contractor name as link to `/contractors/{contractorId}`
- Breadcrumb: "Contracts / {contract title}" using Breadcrumb component
- Action buttons in DropdownMenu (right-aligned): Edit contract, Add amendment, Upload document (visible). Terminate contract, Mark as superseded (in overflow/destructive section)
- Terminate action: confirmation dialog per UI-SPEC copy: "Terminate {contractTitle}?" with body text and destructive CTA
- Supersede action: Dialog asking user to link to the new/replacement contract (select from list)

**overview-tab.tsx:**
- 2-column grid on desktop (>= 1280px), single column below
- Card 1: Contract details — type (translated badge), auto-renewal (yes/no), notice period (days), notes
- Card 2: Financial terms — rate (formatted as currency, grosze->zloty), currency, billing model, rate type, payment terms, invoice cycle
- Card 3: Key dates — start date, end date, notice deadline (calculated: endDate - noticePeriodDays), days remaining with color coding (green >60 days, yellow 30-60, red <30 or expired). Show "Expired X days ago" or "Expires in X days" per UI-SPEC
- Card 4: Linked contractor — name, NIP, status badge. Clickable link to contractor profile
- Expiry reminders display: "Reminders: 30, 60, 90 days before expiry" with "Edit" link opening inline editor that calls `trpc.contract.updateExpiryReminders`

**documents-tab.tsx:**
- Drop zone at top for new uploads (using reusable DropZone component)
- Document list below (using DocumentList component)
- "Upload document" CTA top-right
- Each document card shows: file type icon, filename, version badge, upload date, uploader, scan status, Download button, "Upload new version" link, "Preview" button (PDF only)
- Empty state: "No documents yet" with CTA

**amendments-tab.tsx:**
- Vertical timeline using custom CSS (flexbox + pseudo-elements)
- Timeline connector: 2px line, left offset 12px from dot center
- Nodes ordered chronologically (newest at top)
- Each node: 8px dot (12px for current/active, colored `--primary`), date, amendment title/number, change summary text
- Original contract at bottom of timeline with different styling
- Click node: expand to show full amendment details, linked documents, effective date
- "Add amendment" CTA button at top
- Add amendment dialog: standard form (not wizard). Fields: title, effective date (Calendar), description (textarea), document upload (optional DropZone). On submit: call `trpc.contract.createAmendment`, toast "Amendment added", refresh timeline
- Empty state: "No amendments" with body text

**activity-tab.tsx:**
- Vertical timeline (same pattern as contractor profile right-rail activity)
- For Phase 3: show contract-level events from audit trail or constructed from contract data
- Events: contract created (from createdAt), status changed, document uploaded, amendment added
- Each entry: icon + action text + relative timestamp
- Placeholder implementation is acceptable if full audit trail is Phase 9

**Reusable document components under `apps/web/src/components/documents/`:**

**drop-zone.tsx** — Reusable drag-and-drop component using react-dropzone:
- Dashed border container (2px dashed `--border`), min-height 160px
- Centered UploadCloud icon (32px), "Drag files here or" text, "Browse files" button
- Dragover: border becomes `--primary`, background `--primary` at 3% opacity, icon scales 1.1x
- Props: `onFilesAccepted: (files: File[]) => void`, `onFileRejected?`, `disabled?`, `entityType?`, `entityId?`
- ACCEPTED_TYPES map: PDF, DOCX, XLSX, PNG, JPG with MIME types
- MAX_FILE_SIZE: 25 MB
- Multiple files accepted
- All text via `useTranslations("Documents")`

**document-card.tsx** — Single document display card:
- File type icon (48x48 area, use FileText/FileSpreadsheet/Image icons from Lucide based on MIME)
- Filename, version badge ("Version N"), upload date, uploader name
- Scan status inline badge: Scanning (Loader2 spinning + muted), Clean (ShieldCheck green), Infected (ShieldAlert red), Failed (ShieldQuestion amber)
- Actions: Download button (calls `trpc.document.getDownloadUrl`, opens URL in new tab), Preview button (PDF only, opens PdfPreview dialog), "Upload new version" link
- For infected documents: Download button disabled with tooltip "Threat detected"

**document-list.tsx** — List of document cards:
- Props: `entityType`, `entityId` — fetches via `trpc.document.list`
- Renders DocumentCard for each document
- Empty state when no documents

**pdf-preview.tsx** — Dialog for inline PDF preview:
- Dialog max-width 960px, max-height 80vh
- Browser-native `<object>` tag (not react-pdf) with presigned URL as data source
- Download button in dialog header
- Close button top-right
- Fallback message if browser cannot render PDF

**upload-progress.tsx** — Individual file upload progress row:
- File icon (by type), filename, file size, progress bar (0-100%), cancel button (X)
- On complete: replace progress bar with scan status badge
- Uses XMLHttpRequest for progress tracking during direct R2 PUT

**version-history.tsx** — Expandable version history section:
- "View version history" link that expands to show all prior versions
- Each version: version number, upload date, uploader. Prior versions downloadable
- Fetches via `trpc.document.getVersionHistory`
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/[locale]/(dashboard)/contracts/[id]/page.tsx exists and contains `trpc.contract.getById`
    - apps/web/src/components/contracts/contract-detail/detail-header.tsx contains `Breadcrumb` and `Badge` and `DropdownMenu`
    - apps/web/src/components/contracts/contract-detail/overview-tab.tsx contains `Card` and `rateValueGrosze` and `days`
    - apps/web/src/components/contracts/contract-detail/documents-tab.tsx contains `DropZone` and `DocumentList`
    - apps/web/src/components/contracts/contract-detail/amendments-tab.tsx contains `timeline` and `createAmendment`
    - apps/web/src/components/documents/drop-zone.tsx contains `useDropzone` and `UploadCloud` and `ACCEPTED_TYPES` and `25 * 1024 * 1024`
    - apps/web/src/components/documents/document-card.tsx contains `ShieldCheck` and `Download` and `virusScanStatus`
    - apps/web/src/components/documents/pdf-preview.tsx contains `<object` or `object` and `Dialog`
    - apps/web/src/components/documents/upload-progress.tsx contains `progress` and `XMLHttpRequest` or `fetch`
    - apps/web/src/components/documents/version-history.tsx contains `getVersionHistory`
  </acceptance_criteria>
  <done>Contract detail page renders with all 4 tabs. Documents tab shows drop zone, document cards with scan status, download, PDF preview. Amendments tab shows timeline. All document components are reusable.</done>
</task>

<task type="auto">
  <name>Task 2: Contractor profile tab integration, expiry reminder settings, and full Phase 3 i18n</name>
  <files>
    apps/web/src/components/contractors/contractor-profile/profile-tabs.tsx
    apps/web/src/components/contractors/contractor-profile/tab-contracts.tsx
    apps/web/src/components/contractors/contractor-profile/tab-documents.tsx
    apps/web/messages/en.json
    apps/web/messages/pl.json
  </files>
  <read_first>
    apps/web/src/components/contractors/contractor-profile/profile-tabs.tsx
    apps/web/src/components/contractors/contractor-profile/tab-overview.tsx
    apps/web/src/components/contractors/contractor-profile/tab-compliance.tsx
    apps/web/src/app/[locale]/(dashboard)/contractors/[id]/page.tsx
    apps/web/messages/en.json
    apps/web/messages/pl.json
  </read_first>
  <action>
**1. Update `profile-tabs.tsx`:**

Modify ProfileTabsProps to add `contractsContent` and `documentsContent`:
```typescript
type ProfileTabsProps = {
  overviewContent: ReactNode;
  complianceContent: ReactNode;
  activityContent: ReactNode;
  contractsContent: ReactNode;   // NEW
  documentsContent: ReactNode;   // NEW
};
```

Replace the TabPlaceholder for "contracts" tab with `{contractsContent}`.
Replace the TabPlaceholder for "documents" tab with `{documentsContent}`.

Update the parent page (`contractors/[id]/page.tsx`) to pass the new props:
```tsx
<ProfileTabs
  overviewContent={<TabOverview ... />}
  complianceContent={<TabCompliance ... />}
  activityContent={...}
  contractsContent={<TabContracts contractorId={id} />}
  documentsContent={<TabDocuments contractorId={id} />}
/>
```

**2. Create `tab-contracts.tsx`:**

Mini TanStack Table filtered to current contractor. Columns: Title, Status, Start date, End date, Rate.
- Uses `trpc.contract.list` with `contractorId` filter
- "Add contract" CTA top-right opens wizard with contractor pre-filled (`contractorId` prop)
- Click row navigates to `/contracts/{id}` (not side panel — different from main list)
- Server-side pagination, sorting
- Empty state: "No contracts" / "Create a contract for this contractor." / "Add contract" CTA
- Loading: skeleton rows

**3. Create `tab-documents.tsx`:**

Document cards filtered to current contractor (across all contracts + standalone).
- Uses `trpc.document.list` with `entityType: "CONTRACTOR"` and `entityId: contractorId`
- "Upload document" CTA top-right
- DropZone for new uploads at top
- DocumentCard list below
- On upload: call `trpc.document.requestUpload` with `entityType: "CONTRACTOR"`, `entityId: contractorId`
- Empty state: "No documents" / "Upload documents for this contractor." / "Upload document" CTA

**4. Update contractor profile header:**

Wire the "Add contract" button in `profile-header.tsx` — remove the "Coming in Phase 3" tooltip and connect it to open the contract wizard with contractorId pre-filled.

**5. Add ALL Phase 3 i18n translations to `en.json` and `pl.json`:**

Add two new namespaces: "Contracts" and "Documents".

Use ALL copy from the UI-SPEC Copywriting Contract section verbatim. The i18n keys should be organized as:

**"Contracts" namespace (in en.json):**
```json
{
  "Contracts": {
    "pageTitle": "Contracts",
    "newContract": "New contract",
    "searchPlaceholder": "Search by title, contractor, or type...",
    "filtersButton": "Filters",
    "bulkExport": "Export contracts",
    "bulkTerminate": "Terminate",
    "emptyState": {
      "heading": "No contracts yet",
      "body": "Create your first contract to start managing agreements.",
      "cta": "New contract"
    },
    "noResults": {
      "heading": "No contracts found",
      "body": "Try adjusting your search or filters.",
      "cta": "Clear filters"
    },
    "selected": "{count} selected",
    "pagination": {
      "rowsPerPage": "Rows per page",
      "ofTotal": "of {total}"
    },
    "wizard": {
      "title": "New contract",
      "step1Label": "Contract details",
      "step2Label": "Financial terms",
      "step3Label": "Documents",
      "step1Next": "Next: Financial terms",
      "step2Next": "Next: Documents",
      "step3Submit": "Create contract",
      "back": "Back",
      "close": "Close",
      "discardChanges": "Discard changes",
      "contractorLabel": "Contractor",
      "contractorPlaceholder": "Select contractor...",
      "titleLabel": "Contract title",
      "typeLabel": "Contract type",
      "typeB2B": "B2B Service Agreement",
      "typeNDA": "Non-Disclosure Agreement",
      "typeSOW": "Statement of Work",
      "typeIPAssignment": "IP Assignment",
      "typeDPA": "Data Processing Agreement",
      "typeOther": "Other",
      "startDateLabel": "Start date",
      "endDateLabel": "End date",
      "noticePeriodLabel": "Notice period (days)",
      "autoRenewalLabel": "Auto-renewal",
      "rateLabel": "Rate",
      "currencyLabel": "Currency",
      "billingCycleLabel": "Billing cycle",
      "rateTypeLabel": "Rate type",
      "paymentTermsLabel": "Payment terms (days)",
      "invoiceCycleLabel": "Invoice cycle",
      "preFilled": "Pre-filled from contractor billing profile",
      "dropZoneHeading": "Upload contract documents",
      "skipDocuments": "Skip, add documents later",
      "successToast": "Contract created",
      "errorToast": "Failed to create contract. Try again."
    },
    "detail": {
      "breadcrumb": "Contracts",
      "editAction": "Edit contract",
      "addAmendmentAction": "Add amendment",
      "uploadDocumentAction": "Upload document",
      "terminateAction": "Terminate contract",
      "supersedeAction": "Mark as superseded",
      "tabOverview": "Overview",
      "tabDocuments": "Documents",
      "tabAmendments": "Amendments",
      "tabActivity": "Activity",
      "contractDetails": "Contract details",
      "financialTerms": "Financial terms",
      "keyDates": "Key dates",
      "linkedContractor": "Contractor",
      "startDate": "Start date",
      "endDate": "End date",
      "noticeDeadline": "Notice deadline",
      "daysRemaining": "{count} days remaining",
      "expired": "Expired {count} days ago",
      "expiringSoon": "Expires in {count} days",
      "reminders": "Reminders"
    },
    "amendments": {
      "addCTA": "Add amendment",
      "originalContract": "Original contract",
      "amendmentLabel": "Amendment #{n}",
      "effectiveDate": "Effective {date}",
      "changeSummaryLabel": "Changes",
      "emptyHeading": "No amendments",
      "emptyBody": "Amendments to this contract will appear here.",
      "successToast": "Amendment added"
    },
    "contractorTab": {
      "heading": "Contracts",
      "addCTA": "Add contract",
      "emptyHeading": "No contracts",
      "emptyBody": "Create a contract for this contractor.",
      "emptyCTA": "Add contract"
    },
    "sidePanel": {
      "openContract": "Open contract",
      "details": "Details",
      "documents": "Documents ({count})",
      "keyDates": "Key dates"
    },
    "terminate": {
      "confirmTitle": "Terminate {contractTitle}?",
      "confirmBody": "This contract will be marked as terminated. Linked invoices and documents will remain accessible. This action can be reversed by creating a new contract.",
      "confirmCTA": "Terminate contract"
    },
    "deleteDraft": {
      "confirmTitle": "Delete {contractTitle}?",
      "confirmBody": "This draft contract will be permanently deleted. Uploaded documents will also be removed. This cannot be undone.",
      "confirmCTA": "Delete draft"
    },
    "bulkTerminate": {
      "confirmTitle": "Terminate {count} contracts?",
      "confirmBody": "These contracts will be marked as terminated. Linked invoices and documents will remain accessible.",
      "confirmCTA": "Terminate {count} contracts"
    },
    "status": {
      "DRAFT": "Draft",
      "PENDING_SIGNATURE": "Pending signature",
      "ACTIVE": "Active",
      "EXPIRING": "Expiring",
      "EXPIRED": "Expired",
      "TERMINATED": "Terminated",
      "SUPERSEDED": "Superseded",
      "ARCHIVED": "Archived"
    },
    "billingModel": {
      "MONTHLY_RETAINER": "Monthly retainer",
      "HOURLY": "Hourly",
      "DAILY": "Daily",
      "MILESTONE": "Milestone",
      "DELIVERABLE_BASED": "Deliverable-based",
      "MIXED": "Mixed"
    },
    "rateType": {
      "MONTHLY_FIXED": "Monthly fixed",
      "PER_HOUR": "Per hour",
      "PER_DAY": "Per day",
      "PER_MILESTONE": "Per milestone",
      "PER_DELIVERABLE": "Per deliverable"
    },
    "invoiceCycle": {
      "WEEKLY": "Weekly",
      "BIWEEKLY": "Biweekly",
      "MONTHLY": "Monthly",
      "ON_DELIVERABLE": "On deliverable",
      "AD_HOC": "Ad hoc"
    },
    "errors": {
      "loadFailed": "Could not load contracts. Check your connection and try again.",
      "detailLoadFailed": "Could not load contract details. Try again.",
      "retry": "Try again"
    },
    "validation": {
      "titleRequired": "Contract title is required",
      "contractorRequired": "Select a contractor",
      "typeRequired": "Select a contract type",
      "startDateRequired": "Start date is required",
      "endDateAfterStart": "End date must be after start date",
      "noticePeriodPositive": "Notice period must be a positive number",
      "rateRequired": "Rate is required",
      "ratePositive": "Rate must be a positive number",
      "currencyRequired": "Select a currency",
      "billingCycleRequired": "Select a billing cycle",
      "paymentTermsRequired": "Payment terms are required",
      "paymentTermsPositive": "Payment terms must be a positive number"
    }
  }
}
```

**"Documents" namespace (in en.json):**
```json
{
  "Documents": {
    "dropZone": {
      "heading": "Upload documents",
      "body": "Drag files here or",
      "browse": "Browse files",
      "accepted": "PDF, DOCX, XLSX, PNG, JPG up to 25 MB"
    },
    "upload": {
      "uploading": "Uploading {filename}...",
      "success": "{filename} uploaded",
      "error": "Failed to upload {filename}. Try again.",
      "invalidType": "{filename} is not a supported file type.",
      "tooLarge": "{filename} exceeds the 25 MB limit."
    },
    "uploadCTA": "Upload document",
    "uploadNewVersion": "Upload new version",
    "download": "Download",
    "preview": "Preview",
    "versionLabel": "Version {n}",
    "currentVersion": "Current",
    "versionHistory": "View version history",
    "emptyHeading": "No documents yet",
    "emptyBody": "Upload contract documents to keep them organized.",
    "emptyCTA": "Upload document",
    "scanStatus": {
      "scanning": "Scanning for threats...",
      "clean": "Scan passed",
      "infected": "Threat detected — file quarantined",
      "failed": "Scan could not complete"
    },
    "metadata": {
      "uploadedBy": "Uploaded by {name}",
      "uploadedOn": "on {date}",
      "size": "{size}"
    },
    "contractorTab": {
      "heading": "Documents",
      "uploadCTA": "Upload document",
      "emptyHeading": "No documents",
      "emptyBody": "Upload documents for this contractor.",
      "emptyCTA": "Upload document"
    },
    "errors": {
      "uploadFailed": "Could not upload document. Check the file and try again.",
      "downloadFailed": "Could not generate download link. Try again.",
      "retry": "Try again"
    }
  }
}
```

Add the **Polish translations** for all above keys to `pl.json` using the exact copy from UI-SPEC Copywriting Contract section (PL columns).

Also add the Navigation sidebar entry for Contracts if not already present — check the navigation config and ensure "Contracts" route is wired to `/contracts`.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/contractors/contractor-profile/profile-tabs.tsx contains `contractsContent` and `documentsContent`
    - apps/web/src/components/contractors/contractor-profile/profile-tabs.tsx does NOT contain `TabPlaceholder` for contracts tab
    - apps/web/src/components/contractors/contractor-profile/profile-tabs.tsx does NOT contain `TabPlaceholder` for documents tab
    - apps/web/src/components/contractors/contractor-profile/tab-contracts.tsx exists and contains `trpc.contract.list` and `contractorId`
    - apps/web/src/components/contractors/contractor-profile/tab-documents.tsx exists and contains `trpc.document.list` and `CONTRACTOR`
    - apps/web/messages/en.json contains `"Contracts"` namespace with `"pageTitle": "Contracts"`
    - apps/web/messages/en.json contains `"Documents"` namespace with `"dropZone"` key
    - apps/web/messages/pl.json contains `"Contracts"` namespace with `"pageTitle": "Umowy"`
    - apps/web/messages/pl.json contains `"Documents"` namespace with `"dropZone"` key
    - apps/web/messages/en.json contains `"scanStatus"` with `"scanning"` and `"clean"` and `"infected"` keys
  </acceptance_criteria>
  <done>Contractor profile Contracts and Documents tabs show real data. All Phase 3 UI text is available in Polish and English. Navigation wired to /contracts.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @contractor-ops/web exec tsc --noEmit` exits 0
- /contracts/{id} page renders with Overview, Documents, Amendments, Activity tabs
- Documents tab shows drop zone, document cards with scan status
- PDF preview opens in dialog with browser-native embed
- Amendments tab shows timeline with chronological order
- Contractor profile Contracts tab shows mini table filtered to contractor
- Contractor profile Documents tab shows document cards filtered to contractor
- All UI text renders in both Polish and English
</verification>

<success_criteria>
- Contract detail page at /contracts/{id} with all 4 tabs and deep-linkable tab state
- Reusable document components (DropZone, DocumentCard, PdfPreview, UploadProgress, VersionHistory)
- Amendment timeline with add amendment dialog
- Contractor profile tabs replaced (Contracts + Documents no longer show placeholder)
- Full i18n: Contracts and Documents namespaces in both en.json and pl.json
- All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-contracts-documents/03-04-SUMMARY.md`
</output>
