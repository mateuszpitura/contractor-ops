# Deferred Items - Phase 03

## Pre-existing Issues (Out of Scope)

1. **document.ts compilation errors** - `packages/api/src/routers/document.ts` has multiple TS errors related to `Permission` type (no `documents` key) and null-safety issues. These are pre-existing and unrelated to the contract backend work.
