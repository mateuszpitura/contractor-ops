---
phase: 06-approval-workflow
plan: 05
type: execute
wave: 3
depends_on: ["06-02", "06-03", "06-04"]
files_modified:
  - apps/web/src/messages/en.json
  - apps/web/src/messages/pl.json
  - apps/web/src/components/approvals/approval-queue/columns.tsx
  - apps/web/src/components/approvals/approval-queue/data-table-toolbar.tsx
  - apps/web/src/components/approvals/approval-queue/side-panel.tsx
  - apps/web/src/components/approvals/chain-tracker.tsx
  - apps/web/src/components/approvals/audit-timeline.tsx
  - apps/web/src/components/settings/approval-chains-tab.tsx
  - apps/web/src/components/settings/chain-editor-dialog.tsx
  - apps/web/src/components/settings/condition-builder.tsx
  - apps/web/src/app/[locale]/(dashboard)/approvals/page.tsx
  - packages/api/src/routers/approval.ts
autonomous: true
requirements: [APPR-06]

must_haves:
  truths:
    - "All approval UI text renders in both English and Polish via next-intl"
    - "SLA breach events are surfaced in the getAuditTrail API response as computed system events. Phase 7 will query this endpoint."
    - "Approval queue, settings chains tab, chain editor, chain tracker, and audit timeline all use t() for every user-visible string"
  artifacts:
    - path: "apps/web/src/messages/en.json"
      provides: "English translations for Approvals namespace"
      contains: "Approvals"
    - path: "apps/web/src/messages/pl.json"
      provides: "Polish translations for Approvals namespace"
      contains: "Approvals"
  key_links:
    - from: "apps/web/src/components/approvals/**"
      to: "apps/web/src/messages/en.json"
      via: "useTranslations('Approvals')"
      pattern: "useTranslations.*Approvals"
    - from: "apps/web/src/components/settings/approval-chains-tab.tsx"
      to: "apps/web/src/messages/en.json"
      via: "useTranslations('Settings')"
      pattern: "useTranslations.*Settings"
---

<objective>
Add complete EN + PL translations for all approval workflow UI surfaces and ensure SLA breach events are properly surfaced in the audit trail API response for Phase 7 notification consumption (per APPR-06, D-09).

Purpose: Cross-cutting i18n ensures all approval text is translatable. SLA breach event verification completes the escalation story for Phase 7.
Output: Full Approvals translation namespace in both locales, updated Settings namespace, all components wired to i18n, SLA breach events verified in audit trail.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-approval-workflow/06-CONTEXT.md
@.planning/phases/06-approval-workflow/06-UI-SPEC.md
@.planning/phases/06-approval-workflow/06-02-SUMMARY.md
@.planning/phases/06-approval-workflow/06-03-SUMMARY.md
@.planning/phases/06-approval-workflow/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Approvals i18n namespace and update Settings namespace (EN + PL)</name>
  <files>apps/web/src/messages/en.json, apps/web/src/messages/pl.json</files>
  <read_first>
    - apps/web/src/messages/en.json (current EN translations -- MUST read to see structure and existing namespaces)
    - apps/web/src/messages/pl.json (current PL translations -- MUST read to see structure)
    - .planning/phases/06-approval-workflow/06-UI-SPEC.md (complete copywriting contract with all EN and PL strings)
    - apps/web/src/components/approvals/approval-queue/columns.tsx (verify which t() keys are used)
    - apps/web/src/components/approvals/approval-queue/data-table-toolbar.tsx (verify which t() keys are used)
    - apps/web/src/components/approvals/approval-queue/side-panel.tsx (verify which t() keys are used)
    - apps/web/src/components/approvals/chain-tracker.tsx (verify which t() keys are used)
    - apps/web/src/components/approvals/audit-timeline.tsx (verify which t() keys are used)
    - apps/web/src/components/settings/approval-chains-tab.tsx (verify Settings.tabs.approvals and chain-related keys)
    - apps/web/src/components/settings/chain-editor-dialog.tsx (verify chain editor keys)
    - apps/web/src/app/[locale]/(dashboard)/approvals/page.tsx (verify page-level keys)
    - apps/web/src/app/[locale]/(dashboard)/invoices/[id]/page.tsx (verify submit-for-approval key)
  </read_first>
  <action>
    **1. Add "Approvals" namespace to `apps/web/src/messages/en.json`:**

    Add a top-level "Approvals" key with all strings from the UI-SPEC copywriting contract. The exact key structure should match how components call `useTranslations("Approvals")`:

    ```json
    "Approvals": {
      "pageTitle": "Approvals",
      "tabs": {
        "my": "My Approvals",
        "all": "All"
      },
      "search": "Search approvals...",
      "status": {
        "all": "All",
        "pending": "Pending",
        "overdue": "Overdue",
        "approved": "Approved",
        "rejected": "Rejected"
      },
      "columns": {
        "invoice": "Invoice",
        "contractor": "Contractor",
        "amount": "Amount",
        "submitted": "Submitted",
        "sla": "SLA remaining",
        "priority": "Priority"
      },
      "empty": {
        "heading": "No pending approvals",
        "body": "All caught up! New invoices will appear here when they need your review."
      },
      "actions": {
        "approve": "Approve",
        "reject": "Reject",
        "more": "More",
        "requestClarification": "Request clarification",
        "delegateApproval": "Delegate approval"
      },
      "sidePanel": {
        "approveInvoice": "Approve invoice",
        "rejectInvoice": "Reject invoice"
      },
      "bulk": {
        "selected": "{count} selected",
        "approve": "Approve ({count})",
        "reject": "Reject ({count})",
        "clear": "Clear"
      },
      "reject": {
        "heading": "Reject invoice",
        "commentLabel": "Reason (required)",
        "commentPlaceholder": "Describe why this invoice is being rejected...",
        "confirm": "Reject invoice",
        "dismiss": "Keep pending"
      },
      "bulkReject": {
        "heading": "Reject {count} invoices",
        "commentLabel": "Shared reason (required)",
        "commentPlaceholder": "This reason will be applied to all selected invoices...",
        "confirm": "Reject {count} invoices",
        "dismiss": "Keep selected"
      },
      "clarification": {
        "heading": "Request clarification",
        "commentLabel": "Question",
        "commentPlaceholder": "What needs to be clarified?",
        "confirm": "Send request",
        "dismiss": "Don't send"
      },
      "delegate": {
        "heading": "Delegate approval",
        "userLabel": "Delegate to",
        "userPlaceholder": "Search users...",
        "noteLabel": "Note (optional)",
        "confirm": "Delegate approval",
        "dismiss": "Keep assigned"
      },
      "chainTracker": {
        "heading": "Approval chain",
        "level": "Level {n}",
        "pending": "Pending",
        "approved": "Approved",
        "rejected": "Rejected",
        "notStarted": "Not started",
        "slaHoursLeft": "{n}h left",
        "slaOverdue": "OVERDUE {n}h"
      },
      "auditTrail": {
        "heading": "Audit trail",
        "submitted": "Submitted for approval",
        "routed": "Routed to chain: {chainName}",
        "slaBreached": "SLA breached at {levelName}",
        "delegated": "Delegated from {from} to {to}",
        "escalated": "Escalated to {levelName}",
        "humanApproved": "Approved",
        "humanRejected": "Rejected",
        "humanClarification": "Clarification requested",
        "empty": "No activity yet",
        "showMore": "Show more"
      },
      "toasts": {
        "approved": "Invoice approved",
        "rejected": "Invoice rejected",
        "clarificationRequested": "Clarification requested",
        "delegated": "Approval delegated to {userName}",
        "bulkApproved": "{count} invoices approved",
        "bulkRejected": "{count} invoices rejected"
      },
      "errors": {
        "loadFailed": "Could not load approvals. Check your connection and try again.",
        "approveFailed": "Could not approve invoice. Try again.",
        "rejectFailed": "Could not reject invoice. Try again.",
        "delegateFailed": "Could not delegate approval. Try again.",
        "auditFailed": "Could not load audit trail. Try again.",
        "retry": "Try again"
      },
      "validation": {
        "rejectCommentMin": "Please provide a reason (at least 10 characters)",
        "conditionValueRequired": "Enter a value for the condition",
        "conditionAmountPositive": "Amount must be a positive number"
      }
    }
    ```

    **2. Update "Settings" namespace in EN:**

    Add under existing Settings keys:
    ```json
    "tabs": {
      ...(existing),
      "approvals": "Approvals"
    },
    "approvals": {
      "heading": "Approval chains",
      "description": "Configure approval chains for invoices. Chains are matched based on conditions like amount thresholds.",
      "createChain": "Create approval chain",
      "defaultBadge": "Default",
      "activeToggle": "Active",
      "levelsCount": "{n} levels",
      "conditionSummary": "Amount > {threshold} {currency}",
      "noConditions": "No conditions (default fallback)",
      "edit": "Edit",
      "delete": "Delete",
      "empty": {
        "heading": "No approval chains configured",
        "body": "Create an approval chain to route invoices through multi-level approvals before payment.",
        "cta": "Create approval chain"
      },
      "deleteConfirm": {
        "title": "Delete this approval chain?",
        "body": "This chain will be permanently deleted. In-flight approvals using this chain will not be affected.",
        "confirm": "Delete chain"
      },
      "editor": {
        "createTitle": "Create approval chain",
        "editTitle": "Edit approval chain",
        "chainName": "Chain name",
        "chainNamePlaceholder": "e.g. Standard Invoice Approval",
        "defaultToggle": "Set as default chain",
        "defaultHelp": "The default chain is used when no conditions match.",
        "levelsHeading": "Approval levels",
        "levelName": "Level name",
        "levelNamePlaceholder": "e.g. Manager Review",
        "approver": "Approver",
        "approverUser": "Specific user",
        "approverRole": "Role-based",
        "userPlaceholder": "Search users...",
        "rolePlaceholder": "Select role...",
        "slaHours": "SLA (hours)",
        "slaPlaceholder": "e.g. 24",
        "required": "Required",
        "addLevel": "Add level",
        "removeLevel": "Remove level",
        "maxLevels": "Maximum 3 levels reached",
        "conditionsHeading": "Routing conditions",
        "conditionsHelp": "When an invoice matches these conditions, this chain is used. Leave empty for the default chain.",
        "conditionField": "Select field...",
        "conditionOperator": "Select operator...",
        "conditionValue": "Enter value...",
        "fieldAmount": "Amount",
        "fieldContractorType": "Contractor type",
        "operatorGt": "Greater than",
        "operatorLt": "Less than",
        "operatorEq": "Equals",
        "addCondition": "Add condition",
        "removeCondition": "Remove",
        "save": "Save chain",
        "discard": "Discard changes"
      },
      "toasts": {
        "created": "Approval chain created",
        "updated": "Approval chain updated",
        "deleted": "Approval chain deleted",
        "saveFailed": "Could not save approval chain. Try again.",
        "deleteFailed": "Could not delete approval chain. Try again."
      },
      "validation": {
        "chainNameRequired": "Chain name is required",
        "chainNameMax": "Chain name must be under 100 characters",
        "levelNameRequired": "Level name is required",
        "approverRequired": "Select an approver",
        "slaRequired": "SLA must be a positive number",
        "slaRange": "SLA must be between 1 and 720 hours",
        "minOneLevel": "Add at least one approval level"
      }
    }
    ```

    **3. Update "Invoices" namespace in EN:**

    Add under existing Invoices.detail keys:
    ```json
    "detail": {
      ...(existing),
      "submitForApproval": "Submit for approval",
      "submitting": "Submitting..."
    }
    ```

    **4. Repeat for `apps/web/src/messages/pl.json`:**

    Add the exact same structure with Polish translations from the UI-SPEC copywriting contract. Key Polish translations:
    - "Approvals" -> "Akceptacje"
    - "My Approvals" -> "Moje akceptacje"
    - "No pending approvals" -> "Brak oczekujacych akceptacji"
    - "Approve" -> "Zaakceptuj"
    - "Reject" -> "Odrzuc"
    - "Approval chain" -> "Lancuch akceptacji"
    - "Audit trail" -> "Sciezka audytu"
    - (All other PL translations from UI-SPEC copywriting contract)

    **5. Verify all components use t() calls:**

    After adding translations, scan each approval component and ensure every hardcoded string is replaced with the appropriate `t()` call. If any component has hardcoded strings from Plans 02-04, update them to use t() with the keys defined above.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && node -e "const en = require('./apps/web/src/messages/en.json'); const pl = require('./apps/web/src/messages/pl.json'); const enKeys = JSON.stringify(Object.keys(en.Approvals || {}).sort()); const plKeys = JSON.stringify(Object.keys(pl.Approvals || {}).sort()); if (enKeys !== plKeys) { console.error('MISMATCH: EN and PL Approvals keys differ'); process.exit(1); } console.log('Approvals namespace keys match between EN and PL'); const enSettingsAppr = en.Settings?.approvals; const plSettingsAppr = pl.Settings?.approvals; if (!enSettingsAppr || !plSettingsAppr) { console.error('MISSING: Settings.approvals namespace'); process.exit(1); } console.log('Settings.approvals exists in both locales'); console.log('PASS');"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/messages/en.json contains top-level "Approvals" key with nested keys: pageTitle, tabs, search, status, columns, empty, actions, sidePanel, bulk, reject, bulkReject, clarification, delegate, chainTracker, auditTrail, toasts, errors, validation
    - apps/web/src/messages/pl.json contains matching "Approvals" key structure with Polish translations
    - EN Settings namespace has "tabs.approvals" key with value "Approvals"
    - PL Settings namespace has "tabs.approvals" key with value "Akceptacje"
    - EN Settings namespace has "approvals" sub-object with heading, description, editor sub-keys
    - PL Settings namespace has matching "approvals" sub-object with Polish translations
    - EN Invoices.detail has "submitForApproval" key with value "Submit for approval"
    - PL Invoices.detail has "submitForApproval" key with value "Zglos do akceptacji"
    - EN and PL Approvals namespaces have identical key structures
  </acceptance_criteria>
  <done>Complete EN + PL translations for all approval UI: queue page, side panel, bulk actions, reject/clarify/delegate popovers, chain tracker, audit timeline, settings chains tab, chain editor dialog, and invoice detail submit button. Both locales have matching key structures.</done>
</task>

<task type="auto">
  <name>Task 2: Wire approval queue and detail components to use i18n keys</name>
  <files>apps/web/src/components/approvals/approval-queue/columns.tsx, apps/web/src/components/approvals/approval-queue/data-table-toolbar.tsx, apps/web/src/components/approvals/approval-queue/side-panel.tsx, apps/web/src/components/approvals/chain-tracker.tsx, apps/web/src/components/approvals/audit-timeline.tsx, apps/web/src/app/[locale]/(dashboard)/approvals/page.tsx</files>
  <read_first>
    - apps/web/src/messages/en.json (just updated -- verify Approvals keys exist)
    - apps/web/src/components/approvals/approval-queue/columns.tsx (check for hardcoded strings)
    - apps/web/src/components/approvals/approval-queue/data-table-toolbar.tsx (check for hardcoded strings)
    - apps/web/src/components/approvals/approval-queue/side-panel.tsx (check for hardcoded strings)
    - apps/web/src/components/approvals/chain-tracker.tsx (check for hardcoded strings)
    - apps/web/src/components/approvals/audit-timeline.tsx (check for hardcoded strings)
    - apps/web/src/app/[locale]/(dashboard)/approvals/page.tsx (check for hardcoded strings)
  </read_first>
  <action>
    **Replace all hardcoded strings with t() calls in each approval component (6 files):**

    For each file listed, add `const t = useTranslations("Approvals");` and replace every user-visible string with the corresponding i18n key.

    Key replacements per component:

    **approvals/page.tsx:** "Approvals" -> t("pageTitle"), tab labels -> t("tabs.my"), t("tabs.all"), empty state -> t("empty.heading"), t("empty.body")

    **columns.tsx:** Column headers -> t("columns.invoice"), t("columns.contractor"), etc. Action labels -> t("actions.approve"), t("actions.reject"). Note: columns.tsx may need to accept `t` as a parameter or use a wrapper pattern since ColumnDef is not a component. Use the pattern from existing table columns in the project.

    **data-table-toolbar.tsx:** Status chips -> t("status.all"), t("status.pending"), t("status.overdue"), t("status.approved"), t("status.rejected"). Search placeholder -> t("search"). Bulk labels -> t("bulk.selected", { count }), t("bulk.approve", { count }), etc. Bulk reject dialog -> t("bulkReject.heading", { count }), etc.

    **side-panel.tsx:** Action labels -> t("sidePanel.approveInvoice"), t("sidePanel.rejectInvoice"), t("actions.requestClarification"), t("actions.delegateApproval"). Clarification popover -> t("clarification.*"). Delegate popover -> t("delegate.*").

    **chain-tracker.tsx:** Heading -> t("chainTracker.heading"), level labels -> t("chainTracker.level", { n }), status labels -> t("chainTracker.pending"), etc.

    **audit-timeline.tsx:** Heading -> t("auditTrail.heading"), event labels -> t("auditTrail.submitted"), t("auditTrail.routed", { chainName }), etc. Empty -> t("auditTrail.empty").

    **Sonner toast messages use i18n:**

    Ensure all sonner.success/error calls in approval components use t() for messages:
    - `toast.success(t("toasts.approved"))` instead of hardcoded "Invoice approved"
    - `toast.error(t("errors.approveFailed"))` instead of hardcoded error messages
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - Every approval queue and detail component file imports useTranslations and calls t() for user-visible strings
    - No hardcoded English strings remain in these 6 components (grep for common English words like "Approve", "Reject", "Pending" should only appear in i18n key names, not as raw strings)
    - Sonner toast calls use t() for messages (e.g., t("toasts.approved") not "Invoice approved")
    - TypeScript compiles cleanly
  </acceptance_criteria>
  <done>All 6 approval queue and detail components (page, columns, toolbar, side-panel, chain-tracker, audit-timeline) use next-intl t() for every user-visible string. Sonner toasts use translated messages.</done>
</task>

<task type="auto">
  <name>Task 3: Wire settings components to i18n and verify SLA breach events in audit trail</name>
  <files>apps/web/src/components/settings/approval-chains-tab.tsx, apps/web/src/components/settings/chain-editor-dialog.tsx, apps/web/src/components/settings/condition-builder.tsx, packages/api/src/routers/approval.ts</files>
  <read_first>
    - apps/web/src/messages/en.json (verify Settings.approvals keys exist)
    - apps/web/src/components/settings/approval-chains-tab.tsx (check for hardcoded strings)
    - apps/web/src/components/settings/chain-editor-dialog.tsx (check for hardcoded strings)
    - apps/web/src/components/settings/condition-builder.tsx (check for hardcoded strings)
    - packages/api/src/routers/approval.ts (check getAuditTrail for SLA breach event generation -- should already exist from Plan 01)
  </read_first>
  <action>
    **1. Replace hardcoded strings in settings components (3 files):**

    **approval-chains-tab.tsx:** Use `const t = useTranslations("Settings");` -> t("approvals.heading"), t("approvals.description"), t("approvals.createChain"), t("approvals.defaultBadge"), t("approvals.levelsCount", { n }), t("approvals.conditionSummary", { threshold, currency }), t("approvals.noConditions"), t("approvals.edit"), t("approvals.delete"), empty state -> t("approvals.empty.heading"), t("approvals.empty.body"), t("approvals.empty.cta"), delete confirm -> t("approvals.deleteConfirm.title"), t("approvals.deleteConfirm.body"), t("approvals.deleteConfirm.confirm"). Toasts: t("approvals.toasts.deleted"), t("approvals.toasts.deleteFailed").

    **chain-editor-dialog.tsx:** Use Settings translations -> t("approvals.editor.createTitle"), t("approvals.editor.editTitle"), t("approvals.editor.chainName"), t("approvals.editor.chainNamePlaceholder"), t("approvals.editor.defaultToggle"), t("approvals.editor.defaultHelp"), t("approvals.editor.levelsHeading"), t("approvals.editor.levelName"), t("approvals.editor.levelNamePlaceholder"), t("approvals.editor.approver"), t("approvals.editor.approverUser"), t("approvals.editor.approverRole"), t("approvals.editor.slaHours"), t("approvals.editor.slaPlaceholder"), t("approvals.editor.required"), t("approvals.editor.addLevel"), t("approvals.editor.removeLevel"), t("approvals.editor.maxLevels"), t("approvals.editor.save"), t("approvals.editor.discard"). Toasts: t("approvals.toasts.created"), t("approvals.toasts.updated"), t("approvals.toasts.saveFailed").

    **condition-builder.tsx:** Use Settings translations -> t("approvals.editor.conditionsHeading"), t("approvals.editor.conditionsHelp"), t("approvals.editor.conditionField"), t("approvals.editor.conditionOperator"), t("approvals.editor.conditionValue"), t("approvals.editor.fieldAmount"), t("approvals.editor.fieldContractorType"), t("approvals.editor.operatorGt"), t("approvals.editor.operatorLt"), t("approvals.editor.operatorEq"), t("approvals.editor.addCondition"), t("approvals.editor.removeCondition").

    **2. Verify SLA breach events in audit trail (APPR-06 per D-09):**

    In `packages/api/src/routers/approval.ts`, verify that the `getAuditTrail` query procedure (implemented in Plan 01) already includes computed SLA breach system events using the canonical shape: `{ type: "system", label: "sla_breached", levelName: step.name, timestamp: step.slaDeadline.toISOString() }` for steps where status is PENDING and slaDeadline < now.

    If the canonical SLA breach event shape is missing or incomplete from Plan 01's implementation, add it now. The event is computed at query time (no separate event storage needed per research recommendation). Do NOT duplicate logic -- this is a verification and gap-fill step, not a re-implementation.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -30 && npx tsc --noEmit -p packages/api/tsconfig.json 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Every settings approval component imports useTranslations("Settings") and calls t() for user-visible strings
    - No hardcoded English strings remain in settings approval components
    - packages/api/src/routers/approval.ts getAuditTrail includes computed system events for SLA breaches (steps where status=PENDING and slaDeadline < now) using the canonical shape from Plan 01
    - TypeScript compiles cleanly for both web app and API package
  </acceptance_criteria>
  <done>All 3 settings components use next-intl t() with Settings namespace. SLA breach events verified present in getAuditTrail API response as computed system events for Phase 7 consumption.</done>
</task>

</tasks>

<verification>
- All approval text renders via next-intl in both EN and PL
- SLA breach system events are surfaced in getAuditTrail API response for overdue steps
- TypeScript compiles cleanly across all packages
- No hardcoded English strings in approval components
</verification>

<success_criteria>
- Complete i18n coverage: every string in approval queue, side panel, chain editor, chain tracker, audit timeline is translated (EN + PL)
- SLA breach events are computed and surfaced in getAuditTrail API response (per APPR-06, D-09) -- Phase 7 will query this endpoint
- Settings.tabs.approvals renders correctly in both locales
- All sonner toasts use translated messages
</success_criteria>

<output>
After completion, create `.planning/phases/06-approval-workflow/06-05-SUMMARY.md`
</output>
