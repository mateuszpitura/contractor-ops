---
phase: 61
plan: 08
type: execute
wave: 4
depends_on: [61-06]
files_modified:
  - apps/web/src/components/invoices/einvoice-compliance-summary-tile.tsx
  - apps/web/src/components/invoices/einvoice-compliance-filter-chips.tsx
  - apps/web/src/components/invoices/einvoice-status-cell.tsx
  - apps/web/src/app/[locale]/(dashboard)/invoices/page.tsx
  - apps/web/src/app/[locale]/(dashboard)/invoices/__tests__/page.test.tsx
  - apps/web/src/app/[locale]/(dashboard)/invoices/[id]/_components/invoice-detail-tabs.tsx
  - apps/web/src/app/[locale]/(dashboard)/invoices/[id]/page.tsx
  - apps/web/src/components/invoices/einvoice-tab/einvoice-tab.tsx
  - apps/web/src/components/invoices/einvoice-tab/generation-section.tsx
  - apps/web/src/components/invoices/einvoice-tab/validation-section.tsx
  - apps/web/src/components/invoices/einvoice-tab/validation-layer-row.tsx
  - apps/web/src/components/invoices/einvoice-tab/svrl-issue-list.tsx
  - apps/web/src/components/invoices/einvoice-tab/transmission-section.tsx
  - apps/web/src/components/invoices/einvoice-tab/transmission-event-row.tsx
  - apps/web/src/components/invoices/einvoice-tab/leitweg-id-resolved-inline.tsx
  - apps/web/src/components/invoices/einvoice-tab/__tests__/einvoice-tab.test.tsx
  - apps/web/src/components/invoices/einvoice-tab/__tests__/validation-section.test.tsx
  - apps/web/src/components/invoices/einvoice-tab/__tests__/transmission-section.test.tsx
autonomous: false
requirements:
  - EINV-07
  - EINV-01
  - EINV-04
  - EINV-06
must_haves:
  truths:
    - "Invoices list surfaces compliance column + 7-filter-chip row + summary tile at top per UI-SPEC §InvoicesList"
    - "Filter chip selection backed by URL query param (?einvoiceStatus=invalid) so links share filter state"
    - "Summary tile renders 'X of Y invoices are EN 16931 compliant' + progress bar + Review CTA that sets the filter to invalid+failed multi-select"
    - "Per-invoice /[locale]/(dashboard)/invoices/[id]/ gains Details → E-invoice tab order; URL ?tab=e-invoice preselects the E-invoice tab"
    - "E-invoice tab has three sections (Generation, Validation, Transmission) with status pill + CTA + caption each"
    - "Validation section shows three layer rows (XSD, EN 16931 Schematron, XRechnung CIUS Schematron) each with per-layer status + issue count + collapsible SVRL issue list"
    - "SVRL issue list row pattern: [severity] ruleId — message at xpath in mono font; clicking a row expands full message via shadcn Collapsible"
    - "Transmission section shows event log (EInvoiceLifecycleEvent rows) with timestamps"
    - "Generate XML / Finalize + validate / Validate now / Download XML / Download full report / Send via Peppol CTAs are wired to the Plan-06 tRPC mutations with spinner + error handling via Alert"
    - "Send button disabled unless validation VALID|WARNINGS + participant ACTIVE + receiver supports XRechnung-CII; tooltip explains disabled reason"
    - "Human-verify checkpoint confirms end-to-end flow works against local dev before phase-ship"
  artifacts:
    - path: "apps/web/src/components/invoices/einvoice-compliance-summary-tile.tsx"
      provides: "Summary tile on invoice list"
      contains: "summaryForOrg"
    - path: "apps/web/src/components/invoices/einvoice-tab/einvoice-tab.tsx"
      provides: "Parent E-invoice tab composing 3 sections"
      contains: "EInvoiceTab"
    - path: "apps/web/src/components/invoices/einvoice-tab/svrl-issue-list.tsx"
      provides: "SVRL issue list with severity pills + collapsible rows"
      contains: "SvrlIssueList"
    - path: "apps/web/src/app/[locale]/(dashboard)/invoices/[id]/_components/invoice-detail-tabs.tsx"
      provides: "Tabs wrapping existing detail content + new E-invoice tab"
      contains: "InvoiceDetailTabs"
  key_links:
    - from: "apps/web/src/components/invoices/einvoice-tab/einvoice-tab.tsx"
      to: "packages/api/src/routers/einvoice.ts"
      via: "trpc.einvoice.finalize / revalidate / downloadXml / downloadReport / send"
      pattern: "trpc\\.einvoice\\."
    - from: "apps/web/src/components/invoices/einvoice-compliance-summary-tile.tsx"
      to: "packages/api/src/routers/einvoice.ts"
      via: "trpc.einvoice.summaryForOrg"
      pattern: "summaryForOrg"
    - from: "apps/web/src/app/[locale]/(dashboard)/invoices/page.tsx"
      to: "packages/api/src/routers/einvoice.ts"
      via: "trpc.einvoice.listByOrg"
      pattern: "listByOrg"
---

<objective>
Surface EINV-07 compliance data + the full per-invoice E-invoice tab (D-15) on the existing invoices list + detail routes. Also wires the CTAs that drive Plan 06's mutations (generate, finalize, validate, download, send). Closes Phase 61 with a blocking human-verify checkpoint exercising the end-to-end happy path before the phase ships.

Purpose: this is the visible delivery of Phase 61. Everything else was plumbing.

Output: compliance-aware invoice list, complete per-invoice E-invoice tab with three sections + SVRL issue list + transmission event log + all action CTAs wired + human-verified smoke test.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-xrechnung-e-invoicing/61-CONTEXT.md
@.planning/phases/61-xrechnung-e-invoicing/61-UI-SPEC.md
@.planning/phases/61-xrechnung-e-invoicing/61-06-SUMMARY.md
@.planning/phases/61-xrechnung-e-invoicing/61-07-SUMMARY.md
@apps/web/src/app/[locale]/(dashboard)/invoices/page.tsx
@apps/web/src/app/[locale]/(dashboard)/invoices/[id]/page.tsx
@apps/web/src/components/ui/tabs.tsx
@apps/web/src/components/ui/progress.tsx
@apps/web/src/components/ui/collapsible.tsx
@apps/web/src/components/ui/tooltip.tsx
@apps/web/src/components/ui/skeleton.tsx
@apps/web/messages/en.json
@apps/web/messages/de.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invoices list — compliance column + filter chips + summary tile + URL-query-param wiring</name>
  <files>
    apps/web/src/components/invoices/einvoice-compliance-summary-tile.tsx,
    apps/web/src/components/invoices/einvoice-compliance-filter-chips.tsx,
    apps/web/src/components/invoices/einvoice-status-cell.tsx,
    apps/web/src/app/[locale]/(dashboard)/invoices/page.tsx,
    apps/web/src/app/[locale]/(dashboard)/invoices/__tests__/page.test.tsx
  </files>
  <read_first>
    .planning/phases/61-xrechnung-e-invoicing/61-UI-SPEC.md  (§Copywriting Contract Invoices list + §Interaction Contracts Invoices list rows),
    apps/web/src/app/[locale]/(dashboard)/invoices/page.tsx,
    apps/web/src/components/ui/progress.tsx,
    apps/web/src/components/ui/badge.tsx,
    apps/web/src/components/ui/tooltip.tsx
  </read_first>
  <behavior>
    - `einvoice-compliance-summary-tile.tsx` client component. Uses `trpc.einvoice.summaryForOrg.useQuery()`. Shows a `<Card>` with:
      - Heading (`text-xl`, font-display): "E-invoicing compliance" (i18n key `EInvoice.InvoicesList.SummaryTile.heading`).
      - Body line: "84 of 100 invoices are EN 16931 compliant." — computed from `summary.valid + summary.warnings` over `summary.total`.
      - `<Progress value={percent} />` using shadcn `progress` primitive (single `--primary` fill per UI-SPEC Registry Safety).
      - Display KPI numeral (`text-3xl`, semibold) showing the percentage — coloured with `--primary` when ≥95% compliant, `--warning` else.
      - CTA `<Button variant="outline">` "Review N invoice(s)" when `summary.invalid + summary.failed > 0`; click → `setFilter(['invalid','failed'])` + scroll table into view.
    - `einvoice-compliance-filter-chips.tsx` — client component accepting `value: ComplianceStatus[] | 'all'` + `onChange`. Renders a row of 7 `<Badge role="button" tabIndex={0}>` chips per UI-SPEC. Single-select semantic for all chips except "Review" trigger (which multi-selects invalid+failed). URL-query-param binding via `next/navigation`'s `useSearchParams` + `useRouter`: chip click updates `?einvoiceStatus=<value>`. Loaded state reads the param.
    - `einvoice-status-cell.tsx` — stateless. Maps status → `<Badge>` + icon + text label per UI-SPEC semantic triad. Wrapped in a `<Link>` (internal) to `/invoices/${invoiceId}?tab=e-invoice`.
    - `page.tsx` — modify existing invoice list page:
      - Render `<EInvoiceComplianceSummaryTile />` above the existing table.
      - Render `<EInvoiceComplianceFilterChips />` between tile and table.
      - Table gets a new column "E-invoice" (`<EInvoiceStatusCell status={...} invoiceId={row.id} />`). The list query changes from whatever existing query is to `trpc.einvoice.listByOrg.useInfiniteQuery({ status: filter, limit: 50 })`. Cursor pagination replaces existing pagination.
      - Filter state derives from URL query param so links share state.

    - Tests (RTL):
      1. Summary tile renders with correct counts + percentage + KPI colour.
      2. Summary tile "Review" CTA is visible when `invalid+failed > 0`, hidden when both are 0.
      3. Filter chips: clicking "Invalid" updates URL to `?einvoiceStatus=invalid` + table re-queries.
      4. Filter chips keyboard: Tab + Enter activates a chip.
      5. "Review" CTA sets filter to multi-select `invalid,failed` in URL (`?einvoiceStatus=invalid,failed`).
      6. Status cell: each of 7 statuses renders the correct icon + text + variant class.
      7. Empty state: filter `notGenerated` with zero matches → "No invoices in this state" message rendered.
      8. i18n: de locale renders formal-Sie translations.
  </behavior>
  <action>
    Step 1 — Write the three components (cell → chips → tile, bottom-up) with RTL tests per-component first.
    Step 2 — Integrate into `page.tsx`. Identify existing invoice-list query/hook (grep for `trpc.invoice.list` or similar in `apps/web/src/app/[locale]/(dashboard)/invoices/page.tsx`) and replace with `trpc.einvoice.listByOrg`. Preserve any existing sorting / column layout — only ADD the compliance column, don't remove anything.
    Step 3 — URL binding via `useSearchParams`. Respect the locale prefix: `router.push('/' + locale + '/invoices?einvoiceStatus=' + newFilter)`.
    Step 4 — Run tests:
    `pnpm --filter web test -- --run "invoices/__tests__|einvoice-compliance"`.
    Step 5 — Typecheck: `pnpm --filter web exec tsc --noEmit`.
  </action>
  <verify>
    <automated>pnpm --filter web test -- --run "invoices/__tests__|einvoice-compliance" 2>&1 | grep -qE "Tests\s+[0-9]+ passed" && grep -q "summaryForOrg" apps/web/src/components/invoices/einvoice-compliance-summary-tile.tsx && grep -q "listByOrg" apps/web/src/app/\[locale\]/\(dashboard\)/invoices/page.tsx && grep -q "useSearchParams\|useRouter" apps/web/src/components/invoices/einvoice-compliance-filter-chips.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Filter chips: `grep -c "role=\"button\"" apps/web/src/components/invoices/einvoice-compliance-filter-chips.tsx` ≥1 (interactive shadcn Badge role)
    - URL sync: `grep "einvoiceStatus" apps/web/src/components/invoices/einvoice-compliance-filter-chips.tsx` ≥2 matches (read + write)
    - Progress bar: `grep "from ['\"]@/components/ui/progress" apps/web/src/components/invoices/einvoice-compliance-summary-tile.tsx` matches (shadcn primitive, not Recharts)
    - No third-party registry: `grep -rn "tremor\|recharts\|apexcharts" apps/web/src/components/invoices/` returns no matches
    - i18n: every visible string uses `t('EInvoice.InvoicesList.*')` — `grep -c "EInvoice.InvoicesList" apps/web/src/components/invoices/einvoice-compliance-filter-chips.tsx apps/web/src/components/invoices/einvoice-compliance-summary-tile.tsx apps/web/src/components/invoices/einvoice-status-cell.tsx` ≥5
    - Typecheck clean; tests green with 8 RTL tests
    - No `console.*` in any new .tsx file
  </acceptance_criteria>
  <done>
    Compliance surface on invoice list fully functional; URL-state-backed filters; summary tile wired.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-invoice E-invoice tab (3 sections + SVRL issue list + transmission event log + all CTA wiring) + Leitweg-ID resolved-inline</name>
  <files>
    apps/web/src/app/[locale]/(dashboard)/invoices/[id]/_components/invoice-detail-tabs.tsx,
    apps/web/src/app/[locale]/(dashboard)/invoices/[id]/page.tsx,
    apps/web/src/components/invoices/einvoice-tab/einvoice-tab.tsx,
    apps/web/src/components/invoices/einvoice-tab/generation-section.tsx,
    apps/web/src/components/invoices/einvoice-tab/validation-section.tsx,
    apps/web/src/components/invoices/einvoice-tab/validation-layer-row.tsx,
    apps/web/src/components/invoices/einvoice-tab/svrl-issue-list.tsx,
    apps/web/src/components/invoices/einvoice-tab/transmission-section.tsx,
    apps/web/src/components/invoices/einvoice-tab/transmission-event-row.tsx,
    apps/web/src/components/invoices/einvoice-tab/leitweg-id-resolved-inline.tsx,
    apps/web/src/components/invoices/einvoice-tab/__tests__/einvoice-tab.test.tsx,
    apps/web/src/components/invoices/einvoice-tab/__tests__/validation-section.test.tsx,
    apps/web/src/components/invoices/einvoice-tab/__tests__/transmission-section.test.tsx
  </files>
  <read_first>
    .planning/phases/61-xrechnung-e-invoicing/61-UI-SPEC.md  (§Component Inventory + §Copywriting Per-invoice E-invoice tab + §Interaction Contracts),
    apps/web/src/app/[locale]/(dashboard)/invoices/[id]/page.tsx,
    apps/web/src/components/ui/tabs.tsx,
    apps/web/src/components/ui/collapsible.tsx,
    apps/web/src/components/ui/tooltip.tsx,
    apps/web/src/components/ui/skeleton.tsx,
    apps/web/src/components/ui/alert.tsx
  </read_first>
  <behavior>
    - `invoice-detail-tabs.tsx` wraps the existing detail page content. Tab order: Details → E-invoice → (any other existing tabs). URL hash binding: `?tab=e-invoice` activates the E-invoice tab. Uses shadcn `Tabs` primitive.
    - `EInvoiceTab` accepts `{ invoiceId }`. Renders three sub-section cards stacked with `gap-xl` (48px vertical gap):
      1. `<GenerationSection />` — shows current state of generation (NOT_GENERATED → "No XRechnung XML…" + Generate CTA; GENERATED → caption "Generated {relativeTime} · Rule set {ruleSetVersion} · SHA-256 {hash[0:16]}…" + Finalize+validate CTA + Download XML CTA; FINALIZED → same as above).
      2. `<ValidationSection />` — VALIDATED or NOT_VALIDATED. For VALIDATED: three `<ValidationLayerRow />` + an `<SvrlIssueList />` if there are issues + "Validate now" re-run CTA + "Download full report" CTA. For NOT_VALIDATED: "Validation has not run yet." + Validate now CTA.
      3. `<TransmissionSection />` — NOT_SENT / QUEUED / SENT / DELIVERED / FAILED pills + "Send via Peppol" CTA (disabled state with tooltip) + event log table.
    - `ValidationLayerRow` — one per layer: `<Badge>` (semantic triad — PASS/FAIL/SKIPPED/WARNINGS), layer name + issue count (e.g. "Layer 2 — EN 16931 Schematron · 2 error(s)").
    - `SvrlIssueList` — `<Table>` of issues from `validationReportSummary`. Each row: severity pill (Fatal/Error/Warning/Info — semantic triad), mono ruleId (e.g. "BR-DE-17"), message (plain text, truncate at 100 chars with `...` + full via click), mono xpath at end. Click a row → shadcn `Collapsible` expands to show full message.
    - `TransmissionEventRow` — one row per `EInvoiceLifecycleEvent`: timestamp + event type badge + one-line summary (e.g. "Transmitted · message msg_abc123" / "Delivered" / "Delivery failed — RECEIVER_REJECTED").
    - `LeitwegIdResolvedInline` — renders above the Generation section when the invoice's buyer is DE public-sector: either "Resolved Leitweg-ID: `991-33333TEST-33` (from contract override)" OR the `<Alert variant="warning">` "Leitweg-ID missing for German public-sector buyer" + body if null.
    - CTA wiring:
      - Generate XML / Finalize + validate → `trpc.einvoice.finalize.useMutation({ onSuccess: () => queryClient.invalidateQueries(['einvoice','listByOrg']) })`. Button shows Spinner + disabled during pending. On success: scroll into Validation section, fire `aria-live="polite"` announcement "E-invoice finalized — validation {status} with {N} issue(s)".
      - Validate now → `trpc.einvoice.revalidate.useMutation`.
      - Download XML → `trpc.einvoice.downloadXml.useMutation` (or query); on response opens the signed URL via `window.open` or anchor programmatic click; button disabled for 1s to prevent double-click.
      - Download full report → same pattern.
      - Send via Peppol → opens an `<AlertDialog>` confirmation: "Send this e-invoice via Peppol to {receiverIdentifier}?" → on confirm → `trpc.einvoice.send.useMutation`. Disabled when:
        * `lifecycle.validationStatus` not in (`VALID`, `WARNINGS`), OR
        * org participant status !== `ACTIVE`, OR
        * buyer Peppol capability lookup negative.
        Tooltip wraps the disabled state with one of three UI-SPEC-locked explanations.
      - Error states: any mutation error renders `<Alert variant="destructive">` inline with the error-code-to-copy mapping from UI-SPEC §Error state copy. Never expose raw tRPC codes to end users.
    - Loading: `<Skeleton>` placeholders sized per UI-SPEC (pill 24x96, KPI 40, body 16×2, table row 48). No layout shift.
    - Empty states: icons per UI-SPEC (FileCode2, ShieldQuestion, SendHorizontal).
    - i18n: every visible string under `EInvoice.InvoiceTab.*` namespace.
    - Tests (RTL + mocked tRPC):
      1. `EInvoiceTab — renders NOT_GENERATED state with Generate CTA only`.
      2. `EInvoiceTab — renders GENERATED+VALID state with all CTAs + three layer rows PASS`.
      3. `EInvoiceTab — renders INVALID with SVRL issue list expandable`.
      4. `EInvoiceTab — Finalize CTA triggers mutation + shows aria-live announcement`.
      5. `EInvoiceTab — Send disabled + tooltip when participant not ACTIVE`.
      6. `EInvoiceTab — Send disabled + tooltip when capability lookup lacks CII`.
      7. `EInvoiceTab — Download XML opens signed URL`.
      8. `EInvoiceTab — LeitwegIdResolvedInline shows warning when DE public-sector buyer lacks ID`.
      9. `TransmissionSection — event log renders events in chronological order`.
      10. `SvrlIssueList — severity pill + mono ruleId + expandable message`.
      11. `i18n — German locale renders formal-Sie strings`.
      12. `Tab URL — ?tab=e-invoice preselects E-invoice tab`.
  </behavior>
  <action>
    Step 1 — Implement each sub-component bottom-up: TransmissionEventRow → SvrlIssueList → ValidationLayerRow → each Section → LeitwegIdResolvedInline → EInvoiceTab → InvoiceDetailTabs.
    Step 2 — Modify `apps/web/src/app/[locale]/(dashboard)/invoices/[id]/page.tsx` to wrap its body in `<InvoiceDetailTabs>`. Preserve all existing detail content in the "Details" tab — do not remove.
    Step 3 — Wire `LeitwegIdResolvedInline` via `trpc.invoice.get.useQuery({ id })` + the resolver results (expose via a new `trpc.invoice.getWithLeitwegId.useQuery` procedure if needed; otherwise call Plan 04's resolver indirectly by including the resolver result in finalize's response and caching client-side).
    Step 4 — RTL tests per §behavior. Use `msw` or in-memory tRPC mock caller (check existing test utilities in `apps/web/e2e` or `apps/web/src/__tests__/utils`).
    Step 5 — Run:
    `pnpm --filter web test -- --run "einvoice-tab|invoice-detail-tabs"` + full `pnpm --filter web test -- --run`.
    Step 6 — `pnpm --filter web exec tsc --noEmit` clean.
  </action>
  <verify>
    <automated>pnpm --filter web test -- --run "einvoice-tab|invoice-detail-tabs" 2>&1 | grep -qE "Tests\s+[0-9]+ passed" && grep -q "InvoiceDetailTabs" apps/web/src/app/\[locale\]/\(dashboard\)/invoices/\[id\]/_components/invoice-detail-tabs.tsx && grep -q "trpc.einvoice.finalize\|trpc.einvoice.revalidate\|trpc.einvoice.send" apps/web/src/components/invoices/einvoice-tab/einvoice-tab.tsx && grep -q "aria-live" apps/web/src/components/invoices/einvoice-tab/einvoice-tab.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tab preselection: `grep "searchParams\|useSearchParams" apps/web/src/app/\[locale\]/\(dashboard\)/invoices/\[id\]/_components/invoice-detail-tabs.tsx` matches
    - `grep -c "trpc.einvoice\.\(finalize\|revalidate\|downloadXml\|downloadReport\|send\)" apps/web/src/components/invoices/einvoice-tab/*.tsx` ≥5 (all 5 mutation hooks used)
    - `grep "aria-live" apps/web/src/components/invoices/einvoice-tab/einvoice-tab.tsx` matches (accessibility announcement)
    - `grep "Tooltip" apps/web/src/components/invoices/einvoice-tab/transmission-section.tsx` matches (disabled-send tooltip)
    - `grep "Collapsible" apps/web/src/components/invoices/einvoice-tab/svrl-issue-list.tsx` matches
    - `grep -c "EInvoice.InvoiceTab" apps/web/src/components/invoices/einvoice-tab/*.tsx` ≥10 (i18n keys everywhere)
    - No `dangerouslySetInnerHTML`: `grep -rn "dangerouslySetInnerHTML" apps/web/src/components/invoices/einvoice-tab/` returns no matches
    - No third-party blocks: `grep -rn "tremor\|recharts\|mantine" apps/web/src/components/invoices/einvoice-tab/` returns no matches
    - 12 RTL tests per §behavior, all passing
    - Typecheck clean across web
    - No `console.*` in any new .tsx file
  </acceptance_criteria>
  <done>
    E-invoice tab fully functional; all CTAs wired; SVRL issue list + transmission event log render; a11y + i18n covered.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify end-to-end XRechnung flow against local dev</name>
  <what-built>
    Full Phase 61 surface:
    - Settings → E-invoicing page (Plan 07).
    - Leitweg-ID CRUD (Plan 07).
    - Contractor profile: isPublicSectorBuyer + Peppol identifier fields + Leitweg-ID inline selector (Plan 07).
    - Invoice list: compliance column + filter chips + summary tile (Task 1).
    - Per-invoice E-invoice tab: Generate → Finalize + validate → Validate now → Download XML → Send via Peppol (Task 2).
    - Storecove webhook handler updates lifecycle + event log on delivery (Plan 06).
  </what-built>
  <how-to-verify>
    Run `pnpm --filter web dev` against a seeded dev org (use existing seed or create via `pnpm --filter @contractor-ops/db seed`).

    **Scenario 1 — Peppol participant registration:**
    1. Visit `/en/settings/e-invoicing/`.
    2. Click "Register Peppol participant", choose scheme `0060`, value `12345678`. Save.
    3. Expected: card flips to PENDING pill.
    4. (Optional) Trigger a sandbox Storecove webhook via existing test harness to flip status to ACTIVE. Confirm pill updates without page refresh (silent polling or websocket).

    **Scenario 2 — Leitweg-ID management:**
    5. Click "Create Leitweg-ID" on the same page.
    6. Type value `12-34` → inline error "Leitweg-ID's check digit doesn't match". Save button disabled.
    7. Type `991-33333TEST-33`. Assign to an existing contractor. Check "Set as default". Save.
    8. Expected: row appears in list with mono value, default badge, contractor assignment badge.

    **Scenario 3 — Contractor profile:**
    9. Visit a DE contractor's profile. Toggle `isPublicSectorBuyer = true`.
    10. Confirm the Leitweg-ID inline selector becomes visible. Confirm the warning alert shows when no Leitweg-ID selected.
    11. Select the Leitweg-ID created in step 7. Save. No warning should show.
    12. Try setting `peppolSchemeId = 0060` but leaving `peppolParticipantValue` blank → form error "Both fields are required or both must be empty".

    **Scenario 4 — Invoice list compliance:**
    13. Visit `/en/invoices/`.
    14. Expected: compliance summary tile at top + filter chips + new "E-invoice" column on table.
    15. Click the "Invalid" chip → URL updates to `?einvoiceStatus=invalid` + table filters.

    **Scenario 5 — E-invoice tab end-to-end:**
    16. Open any invoice for the DE public-sector contractor.
    17. Switch to the "E-invoice" tab.
    18. Expected: Generation section shows "No XRechnung XML has been generated yet" + Generate XML CTA.
    19. Click Generate XML → should finalize + validate in one shot (per D-04). Expected result: Validation section populates with 3 layer rows, status VALID (for a well-formed invoice) or WARNINGS / INVALID (with SVRL issues listed).
    20. Click Download XML → opens signed URL; browser downloads an `.xml` file. Open it: should contain `<rsm:CrossIndustryInvoice>` + `<ram:BuyerReference>991-33333TEST-33</ram:BuyerReference>`.
    21. Click Download full report → opens signed URL for the KoSIT HTML report.
    22. Click Send via Peppol. Expected: if everything is active + receiver supports CII, the button is enabled; confirm in AlertDialog → mutation runs; Transmission section moves to QUEUED then SENT.
    23. Trigger a sandbox Storecove webhook simulating delivery. Expected: Transmission section updates to DELIVERED + a new event row in the event log (no page refresh — must be polled or pushed).

    **Scenario 6 — Accessibility + i18n:**
    24. Tab through the E-invoice tab CTAs from keyboard only. All focus rings visible, all actions activatable via Enter/Space.
    25. Switch locale to German (`/de/invoices/…`). All strings render in German, formal register (Sie).
    26. Run `axe DevTools` on the E-invoice tab → zero critical or serious violations.

    **Report back:**
    - Any unexpected disabled CTA (with Plan-06 state it should not be disabled).
    - Any i18n key falling through to English under de locale.
    - Any a11y violation or focus-trap bug.
    - Any layout-shift issue between Skeleton and loaded state.
    - Storecove webhook → lifecycle update latency (subjective).
  </how-to-verify>
  <resume-signal>Type "approved" when all 6 scenarios pass end-to-end, or describe issues. If axe violations exist, list them by rule ID so they can be fixed before approval.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query params → filter state | `?einvoiceStatus=<val>` is user-controlled; must pass Zod enum on the tRPC input. |
| Rendered SVRL text content | From a KoSIT validator processing user-supplied invoice data; React escapes by default. |
| Signed R2 URL in client responses | Leaks an invoice's XML if shared; 300s TTL mitigates. |
| Send mutation button client-gating | Defence in depth only; server is the authoritative gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-08-01 | Injection (XSS) | SVRL message text rendered in SvrlIssueList | mitigate | React text children auto-escaped; never `dangerouslySetInnerHTML`; explicit grep acceptance criterion. |
| T-61-08-02 | Information Disclosure | Tab URL leaks invoice status pattern | accept | Tab name exposure is low-value; no sensitive data in URL. |
| T-61-08-03 | Tampering | `?einvoiceStatus=<arbitrary>` passed to server | mitigate | Plan 06's `listByOrg` input validates via `z.enum([...])` — invalid values return BAD_REQUEST. |
| T-61-08-04 | Information Disclosure | SVRL issue messages surfacing rule IDs and XPath paths | accept | UI-SPEC explicitly designates the SVRL-issue-list panel as developer/compliance-reviewer-facing; chrome strings elsewhere are human-friendly. |
| T-61-08-05 | Elevation of Privilege | Send button client-disabled but tRPC called directly | mitigate | Plan 06's send mutation enforces server-side send-gate + pre-flight; UI-gating is UX sugar. |
| T-61-08-06 | Denial of Service | User spamming Finalize + validate CTA | mitigate | Button disables during pending mutation; tRPC rate-limit (Phase 57 general limits); server-side idempotency via `force` flag. |
| T-61-08-07 | Spoofing | Webhook delivery simulated by attacker to flip status | mitigate | Handled in Plan 06 (HMAC-verified); UI merely reflects persisted state. |
</threat_model>

<verification>
`pnpm --filter web test -- --run "einvoice-tab|invoice-detail-tabs|einvoice-compliance"` — green.
`pnpm --filter web exec tsc --noEmit` — clean.
Full phase suite: `pnpm --filter @contractor-ops/einvoice --filter @contractor-ops/validators --filter @contractor-ops/api --filter @contractor-ops/db --filter web test -- --run` green.
Human-verify checkpoint approved.
</verification>

<success_criteria>
1. All must_haves truths hold.
2. Compliance surface on invoice list wired + URL-query-param backed filters.
3. E-invoice tab renders three sections with all CTAs; mutations round-trip; webhook updates reflected.
4. Human-verify checkpoint: all 6 scenarios pass end-to-end against local dev + axe clean.
5. Phase-wide typecheck + test suite green.
6. No third-party UI registries added; no `console.*` introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/61-xrechnung-e-invoicing/61-08-SUMMARY.md` covering: RTL test count (invoice list + E-invoice tab), human-verify outcomes per scenario, any axe findings + disposition, latency observations (finalize / send / webhook update), any UI-SPEC deviations + rationale, final Phase-61 phase-wide verification output (`pnpm ... test -- --run` summary).

Phase 61 concludes here. Next phase: Phase 62 (ZUGFeRD PDF/A-3 — depends on this phase's generator + validator + lifecycle infrastructure).
</output>
