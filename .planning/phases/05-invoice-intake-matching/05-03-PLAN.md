---
phase: 05-invoice-intake-matching
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/web/src/app/[locale]/(dashboard)/invoices/page.tsx
  - apps/web/src/components/invoices/invoice-table/columns.tsx
  - apps/web/src/components/invoices/invoice-table/data-table.tsx
  - apps/web/src/components/invoices/invoice-table/data-table-toolbar.tsx
  - apps/web/src/components/invoices/invoice-table/data-table-filters.tsx
  - apps/web/src/components/invoices/invoice-table/data-table-pagination.tsx
  - apps/web/src/components/invoices/invoice-table/use-invoice-filters.ts
  - apps/web/src/components/invoices/status-chip-bar.tsx
  - apps/web/src/components/invoices/invoice-side-panel.tsx
  - apps/web/src/components/invoices/invoice-upload-area.tsx
autonomous: true
requirements: [INV-01, INV-08, INV-10]

must_haves:
  truths:
    - "User can see invoice list with all columns (Invoice #, Contractor, dates, amounts, status, match status, source)"
    - "User can upload multiple PDFs via drag and drop, each creating a separate invoice draft"
    - "User can filter invoices by clicking status chips above the table"
    - "User can click a row to open a side panel with invoice summary"
    - "User can search invoices by number, contractor name, or NIP"
    - "Status chip counts update live from statusCounts query"
  artifacts:
    - path: "apps/web/src/app/[locale]/(dashboard)/invoices/page.tsx"
      provides: "Invoice list page route"
    - path: "apps/web/src/components/invoices/invoice-table/data-table.tsx"
      provides: "TanStack Table with server-side pagination, sorting, filtering"
    - path: "apps/web/src/components/invoices/status-chip-bar.tsx"
      provides: "Clickable status filter chips with live counts"
    - path: "apps/web/src/components/invoices/invoice-side-panel.tsx"
      provides: "Slide-out sheet with invoice summary on row click"
    - path: "apps/web/src/components/invoices/invoice-upload-area.tsx"
      provides: "DropZone wrapper for multi-file PDF upload"
  key_links:
    - from: "apps/web/src/components/invoices/invoice-table/data-table.tsx"
      to: "trpc.invoice.list"
      via: "server-side data fetching"
      pattern: "trpc\\.invoice\\.list"
    - from: "apps/web/src/components/invoices/status-chip-bar.tsx"
      to: "trpc.invoice.statusCounts"
      via: "live count fetching"
      pattern: "trpc\\.invoice\\.statusCounts"
    - from: "apps/web/src/components/invoices/invoice-upload-area.tsx"
      to: "trpc.document.requestUpload"
      via: "presigned URL upload flow"
      pattern: "trpc\\.document\\.requestUpload"
---

<objective>
Build the invoice list page with TanStack Table, status chip bar, side panel, upload area, and search/filter functionality.

Purpose: Main entry point for invoice management. Users land here to see all invoices, filter by status, upload new ones, and navigate to detail.
Output: Fully functional /invoices page with table, chips, side panel, and upload.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-invoice-intake-matching/05-CONTEXT.md
@.planning/phases/05-invoice-intake-matching/05-RESEARCH.md
@.planning/phases/05-invoice-intake-matching/05-UI-SPEC.md
@.planning/phases/05-invoice-intake-matching/05-01-SUMMARY.md

<interfaces>
From apps/web/src/components/contracts/contract-table/ (pattern to mirror exactly):
- columns.tsx: Column definitions with ColumnDef<T>[]
- data-table.tsx: TanStack Table with useReactTable, manualPagination, manualSorting, manualFiltering
- data-table-toolbar.tsx: Search with 300ms debounce, filter popover, CTA buttons
- data-table-filters.tsx: Multi-select filters with Popover
- data-table-pagination.tsx: Page size (10/25/50), prev/next navigation
- use-contract-filters.ts: nuqs useQueryStates for URL-synced filter state

From apps/web/src/components/documents/drop-zone.tsx:
- DropZone component accepting entityType, entityId, onUploadComplete, accept prop, multiple prop

From apps/web/src/components/contracts/contract-side-panel.tsx:
- Sheet component pattern for slide-out panel (480px width)

From trpc.invoice (created in 05-01):
- list: { items, totalCount, page, pageSize }
- statusCounts: Record<string, number>
- create: creates invoice from uploaded documents
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoice TanStack Table with columns, filters, pagination, status chip bar, and URL state</name>
  <files>
    apps/web/src/components/invoices/invoice-table/columns.tsx
    apps/web/src/components/invoices/invoice-table/data-table.tsx
    apps/web/src/components/invoices/invoice-table/data-table-toolbar.tsx
    apps/web/src/components/invoices/invoice-table/data-table-filters.tsx
    apps/web/src/components/invoices/invoice-table/data-table-pagination.tsx
    apps/web/src/components/invoices/invoice-table/use-invoice-filters.ts
    apps/web/src/components/invoices/status-chip-bar.tsx
  </files>
  <read_first>
    apps/web/src/components/contracts/contract-table/columns.tsx
    apps/web/src/components/contracts/contract-table/data-table.tsx
    apps/web/src/components/contracts/contract-table/data-table-toolbar.tsx
    apps/web/src/components/contracts/contract-table/data-table-filters.tsx
    apps/web/src/components/contracts/contract-table/data-table-pagination.tsx
    apps/web/src/components/contracts/contract-table/use-contract-filters.ts
    apps/web/src/components/contracts/contract-side-panel.tsx
    packages/validators/src/invoice.ts
  </read_first>
  <action>
    Mirror the contract-table pattern EXACTLY for invoice-table (same file structure, same component patterns).

    1. use-invoice-filters.ts: nuqs useQueryStates hook with: search (parseAsString), status (parseAsArrayOf parseAsString), matchStatus (parseAsString for chip bar), source (parseAsArrayOf parseAsString), contractorId (parseAsString), sortBy (parseAsString, default "receivedAt"), sortOrder (parseAsString, default "desc"), page (parseAsInteger, default 1), pageSize (parseAsInteger, default 25).

    2. columns.tsx: Column definitions (ColumnDef array):
       - select: Checkbox column (same pattern as contract-table)
       - invoiceNumber: monospace font (font-mono text-[13px]), accessor "invoiceNumber"
       - contractor: accessor "contractor.legalName", render as Link to /contractors/[id] in primary color
       - issueDate: formatted with locale date formatter
       - dueDate: formatted with locale date formatter. If overdue (past date AND status not PAID/VOID), text in destructive color
       - subtotalGrosze: right-aligned, format as currency (divide by 100, locale format with 2 decimals)
       - totalGrosze: right-aligned, same currency formatting
       - currency: 3-char display
       - status: Badge with colors per UI-SPEC (RECEIVED=muted, MATCHED=success/10%, UNMATCHED=warning/10%, DISCREPANCY=destructive/10%, APPROVAL_PENDING=info/10%, APPROVED=success/10%, REJECTED=destructive/10%, READY_FOR_PAYMENT=primary/10%, PAID=muted). Use icons: Inbox, CheckCircle2, AlertCircle, AlertTriangle, Clock, CheckCircle2, XCircle, Banknote, Check from lucide-react.
       - matchStatus: 8px colored dot (inline-block rounded-full) + label text. Green for MATCHED (score>=90), warning for PARTIAL (50-89), destructive for DISCREPANCY, muted-foreground for UNMATCHED, info for MANUALLY_CONFIRMED.
       - source: Icon only -- Upload icon for MANUAL_UPLOAD, Mail icon for EMAIL_INTAKE, in muted-foreground color.

    3. data-table.tsx: TanStack Table with useReactTable, manualPagination: true, manualSorting: true, manualFiltering: true. Fetches data via trpc.invoice.list with params from useInvoiceFilters. On row click: call onRowClick prop (for side panel). Overdue row highlighting: rows where dueDate < now AND status not in PAID/VOID get bg-destructive/5 class. Loading state: 8 skeleton rows. Empty state: see UI-SPEC empty state copy.

    4. data-table-toolbar.tsx: Search input with 300ms debounce (min 2 chars), filter popover trigger, "Upload invoices" primary CTA button (opens upload area). Search placeholder: "Search invoices..." (use useTranslations).

    5. data-table-filters.tsx: Popover with multi-select for Status (all InvoiceStatus values), Source (MANUAL_UPLOAD, EMAIL_INTAKE), Contractor (search-select). Active filters shown as badges below toolbar.

    6. data-table-pagination.tsx: Same pattern as contract-table. Page size (10/25/50), Previous/Next, "Page X of Y".

    7. status-chip-bar.tsx: Horizontal row of Badge-style chips above table. Props: counts (Record<string, number>). Uses nuqs parseAsString for matchStatus URL state. Chips: All, Received, Matched, Unmatched, Discrepancy, Pending Approval, Approved, Ready for Payment. Active chip: primary/10% bg + primary text. Inactive: muted bg + muted-foreground text. Each shows count in parentheses. On click: sets matchStatus filter (or clears for "All"). Overflow: horizontal scroll with fade gradient on < 1280px (overflow-x-auto). Fetches counts via trpc.invoice.statusCounts query.

    All text via useTranslations("Invoices") from next-intl. Use exact copy from UI-SPEC copywriting contract.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web exec tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "invoiceNumber" apps/web/src/components/invoices/invoice-table/columns.tsx
    - grep -q "manualPagination" apps/web/src/components/invoices/invoice-table/data-table.tsx
    - grep -q "trpc.invoice.list" apps/web/src/components/invoices/invoice-table/data-table.tsx
    - grep -q "trpc.invoice.statusCounts" apps/web/src/components/invoices/status-chip-bar.tsx
    - grep -q "useInvoiceFilters" apps/web/src/components/invoices/invoice-table/use-invoice-filters.ts
    - grep -q "parseAsString" apps/web/src/components/invoices/invoice-table/use-invoice-filters.ts
    - grep -q "Upload invoices" apps/web/src/components/invoices/invoice-table/data-table-toolbar.tsx || grep -q "useTranslations" apps/web/src/components/invoices/invoice-table/data-table-toolbar.tsx
  </acceptance_criteria>
  <done>Invoice TanStack Table with 10 columns, server-side pagination/sorting/filtering, status chip bar with live counts, URL-synced filter state, search with 300ms debounce, and overdue row highlighting.</done>
</task>

<task type="auto">
  <name>Task 2: Invoice page, side panel, upload area, and empty state</name>
  <files>
    apps/web/src/app/[locale]/(dashboard)/invoices/page.tsx
    apps/web/src/components/invoices/invoice-side-panel.tsx
    apps/web/src/components/invoices/invoice-upload-area.tsx
  </files>
  <read_first>
    apps/web/src/app/[locale]/(dashboard)/contracts/page.tsx
    apps/web/src/components/contracts/contract-side-panel.tsx
    apps/web/src/components/documents/drop-zone.tsx
    apps/web/src/components/invoices/invoice-table/data-table.tsx
    apps/web/src/components/invoices/status-chip-bar.tsx
  </read_first>
  <action>
    1. invoice-side-panel.tsx: Sheet component sliding from right (480px, 200ms ease-out). Props: invoice data object, open boolean, onOpenChange callback. Content:
       - Header: Invoice number in Heading (font-mono), status badge (same colors as table), source icon (Upload or Mail).
       - Amounts section: "Amounts" heading, Net (formatted currency), VAT (formatted), Gross (formatted), Currency.
       - Dates section: "Dates" heading, Issue date, Due date (destructive if overdue).
       - Matching section: "Matching" heading, match status dot + label, contractor name as link (if matched).
       - Bottom CTA: "Open invoice" primary button linking to /invoices/[id].
       All text via useTranslations("Invoices").

    2. invoice-upload-area.tsx: Wrapper around the existing DropZone component. Props: onUploadComplete callback, className.
       - Configures DropZone with accept: { "application/pdf": [".pdf"] }, multiple: true.
       - After files selected, fires upload immediately per file via presigned URL XHR (reuse existing DropZone pattern from Phase 3).
       - For each uploaded file: calls trpc.invoice.create mutation with source MANUAL_UPLOAD and the documentId.
       - Shows stacked list of files during upload: file name (truncated 40 chars), file size, progress bar (4px height, primary fill), status icon (Loader2 spinning, CheckCircle2 success, XCircle error).
       - On all complete: Sonner toast "{count} invoices uploaded. Review your drafts." with link to filter table to RECEIVED status.
       - Error handling: individual file failures show error icon + retry button, other files continue.
       - Below DropZone: email inbox tip "Or email invoices to invoices@{org-slug}.contractorhub.io" (fetched from org settings or constructed from org slug).

    3. invoices/page.tsx: Page component with Suspense boundary (same pattern as contracts/page.tsx).
       - Page title "Invoices" heading.
       - StatusChipBar component.
       - InvoiceUploadArea (initially collapsed, expanded on "Upload invoices" CTA click from toolbar).
       - InvoiceDataTable with onRowClick opening InvoiceSidePanel.
       - InvoiceSidePanel as controlled Sheet.
       - Empty state when no invoices: centered layout with FileText icon (64px), Heading "No invoices yet", body text about uploading or emailing, email inbox address with copy button, "Upload invoices" primary CTA, large DropZone below. Empty state shows when totalCount === 0 from list query.
       All text from UI-SPEC copywriting contract via useTranslations.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web exec tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "InvoiceSidePanel" apps/web/src/components/invoices/invoice-side-panel.tsx
    - grep -q "Sheet" apps/web/src/components/invoices/invoice-side-panel.tsx
    - grep -q "DropZone" apps/web/src/components/invoices/invoice-upload-area.tsx
    - grep -q "multiple" apps/web/src/components/invoices/invoice-upload-area.tsx
    - grep -q "Suspense" apps/web/src/app/[locale]/(dashboard)/invoices/page.tsx
    - grep -q "StatusChipBar" apps/web/src/app/[locale]/(dashboard)/invoices/page.tsx
    - grep -q "InvoiceDataTable\|DataTable" apps/web/src/app/[locale]/(dashboard)/invoices/page.tsx
  </acceptance_criteria>
  <done>Invoice list page at /invoices with status chip bar, TanStack Table, side panel on row click, multi-file upload area with progress indicators, email inbox tip, and empty state. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- /invoices page renders with table, chip bar, and upload area
- Status chips filter table via URL params
- Row click opens side panel with invoice summary
- Upload area accepts multiple PDFs and creates drafts
- Empty state shows when no invoices exist
- TypeScript compiles cleanly
</verification>

<success_criteria>
- Invoice list shows all 10 columns with proper formatting (monospace invoice #, currency, status badges)
- Status chip bar shows live counts and filters on click
- Side panel slides out at 480px with invoice summary and "Open invoice" CTA
- Multi-file upload creates separate drafts per PDF with progress indicators
- Search debounces at 300ms and searches across invoice number, contractor name, NIP
</success_criteria>

<output>
After completion, create `.planning/phases/05-invoice-intake-matching/05-03-SUMMARY.md`
</output>
