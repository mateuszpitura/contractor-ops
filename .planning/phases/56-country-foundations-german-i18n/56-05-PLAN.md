---
phase: 56
plan: 05
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - apps/web/src/i18n/routing.ts
  - apps/web/src/i18n/request.ts
  - apps/web/messages/de.json
  - apps/web/src/i18n/__tests__/de-locale.test.ts
autonomous: true
requirements: [FOUND-03, FOUND-04]
requirements_addressed: [FOUND-03, FOUND-04]

must_haves:
  truths:
    - "routing.locales includes 'de' as fourth locale in order ['en','pl','ar','de']"
    - "localeSettings.de equals { timeZone: 'Europe/Berlin', currency: 'EUR' }"
    - "apps/web/messages/de.json exists, mirrors every key in en.json, uses formal Sie register throughout"
    - "de.json contains NO keys matching RESERVED_LEGAL_KEYS (the 9 locked identifiers from Plan 03)"
    - "de.json contains NO whole-word Du|Dir|Dein[a-z]* tokens (formal register guard)"
    - "Wave 0 de-locale.test.ts GREEN; Plan 03 locked-phrases-guard.test.ts still GREEN with de.json present"
  artifacts:
    - path: "apps/web/src/i18n/routing.ts"
      provides: "next-intl routing with 4 locales including de"
      contains: "'de'"
    - path: "apps/web/src/i18n/request.ts"
      provides: "localeSettings with Europe/Berlin + EUR for de"
      contains: "Europe/Berlin"
    - path: "apps/web/messages/de.json"
      provides: "Initial German translation mirroring en.json namespace layout"
      min_lines: 50
  key_links:
    - from: "apps/web/messages/de.json"
      to: "apps/web/messages/en.json"
      via: "identical top-level namespace keys"
      pattern: "key parity"
    - from: "apps/web/src/i18n/request.ts"
      to: "apps/web/messages/de.json"
      via: "dynamic import of messages/{locale}.json"
      pattern: "messages/\\$\\{locale\\}"
---

<objective>
Ship German as the fourth platform locale per D-11, D-12, D-13. Minimal 2-line change to routing.ts, one-entry extension to localeSettings, and AI-first messages/de.json mirroring en.json namespace structure. Formal Sie register throughout; none of the 9 locked legal phrases appear as keys. Steuerberater review is a Phase 56 gate (owned by Plan 08) — this plan ships the AI draft that the Steuerberater corrects.

Purpose: Activate DE as a first-class locale so Plan 06 UI components and Plan 07 privacy pages can render German copy via useTranslations. Without this plan, routing.locales stays at ['en','pl','ar'] and every German string requires hard-coding.
Output: Updated routing + request config, full de.json (AI first-pass), green Wave 0 locale test, green locked-phrases-guard test with de.json present.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-country-foundations-german-i18n/56-CONTEXT.md
@.planning/phases/56-country-foundations-german-i18n/56-RESEARCH.md
@.planning/phases/56-country-foundations-german-i18n/56-UI-SPEC.md
@apps/web/src/i18n/routing.ts
@apps/web/src/i18n/request.ts
@apps/web/messages/en.json
@apps/web/messages/pl.json
@apps/web/messages/ar.json
@packages/validators/src/legal/de.ts
@packages/validators/src/__tests__/locked-phrases-guard.test.ts
@apps/web/src/i18n/__tests__/de-locale.test.ts

<interfaces>
Target end-state:

routing.ts:
```typescript
export const routing = defineRouting({
  locales: ['en', 'pl', 'ar', 'de'] as const,
  defaultLocale: 'pl',
});
```

request.ts localeSettings entry:
```typescript
de: { timeZone: 'Europe/Berlin', currency: 'EUR' },
```

UI-SPEC §Copywriting required DE strings (formal Sie):
- "Save compliance fields" → "Konformitätsfelder speichern"
- "Continue" (onboarding) → "Weiter"
- "Download as PDF" → "Als PDF herunterladen"
- "I have read and understood the privacy notice" → "Ich habe die Datenschutzerklärung gelesen und verstanden"
- Footer "Privacy" → "Datenschutz"
- Language switcher labels: "Englisch", "Polnisch", "Arabisch", "Deutsch"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add de to routing.locales and extend localeSettings</name>
  <files>apps/web/src/i18n/routing.ts, apps/web/src/i18n/request.ts</files>
  <read_first>
    - apps/web/src/i18n/routing.ts (current ['en','pl','ar'])
    - apps/web/src/i18n/request.ts (current localeSettings keys: en, pl, ar)
    - .planning/phases/56-country-foundations-german-i18n/56-CONTEXT.md D-11, D-12
    - .planning/phases/56-country-foundations-german-i18n/56-RESEARCH.md §Code Example 7
  </read_first>
  <action>
    In apps/web/src/i18n/routing.ts, change `locales: ['en', 'pl', 'ar'] as const` to `locales: ['en', 'pl', 'ar', 'de'] as const`. Do NOT change defaultLocale (stays 'pl'). Do NOT reorder existing locales — append 'de' at the end.

    In apps/web/src/i18n/request.ts, the localeSettings object currently has entries for en, pl, ar. Add a new entry `de: { timeZone: 'Europe/Berlin', currency: 'EUR' },` between the ar entry and the closing `};`. Do NOT include numberingSystem (DE uses default Latin digits). Do NOT touch the en/pl/ar entries.

    The type `Locale = (typeof routing.locales)[number]` derives automatically — no manual update.
  </action>
  <verify>
    <automated>grep -E "locales: \[['\"]en['\"], ['\"]pl['\"], ['\"]ar['\"], ['\"]de['\"]\\]" apps/web/src/i18n/routing.ts && grep -E "de: \{ timeZone: 'Europe/Berlin', currency: 'EUR' \}" apps/web/src/i18n/request.ts</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/i18n/routing.ts `locales` array equals `['en', 'pl', 'ar', 'de'] as const`.
    - apps/web/src/i18n/request.ts `localeSettings` object contains `de: { timeZone: 'Europe/Berlin', currency: 'EUR' }`.
    - `defaultLocale` unchanged ('pl').
    - `pnpm --filter @contractor-ops/web exec tsc --noEmit` produces no new errors.
  </acceptance_criteria>
  <done>
    DE locale registered in next-intl config; localeSettings extended; no other config changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate apps/web/messages/de.json (AI first-pass mirror of en.json)</name>
  <files>apps/web/messages/de.json, apps/web/src/i18n/__tests__/de-locale.test.ts</files>
  <read_first>
    - apps/web/messages/en.json (canonical EN source — read in full)
    - apps/web/messages/pl.json (PL reference for namespace structure)
    - apps/web/messages/ar.json (AR reference)
    - packages/validators/src/legal/de.ts (9 RESERVED_LEGAL_KEYS — must NOT appear as de.json keys)
    - .planning/phases/56-country-foundations-german-i18n/56-UI-SPEC.md §Copywriting Contract
    - .planning/phases/56-country-foundations-german-i18n/56-CONTEXT.md D-12, D-13
  </read_first>
  <action>
    Create apps/web/messages/de.json mirroring en.json namespace structure exactly.

    1. Read apps/web/messages/en.json fully.
    2. For each top-level namespace (Common, Dashboard, Contractors, Consent, Legal, etc.) copy the structure; translate each leaf string to German using formal register (Sie, Ihr, Ihnen, Ihre). NEVER use Du, Dir, Dein, Deine, Deiner, Deines.
    3. Lock the following exact German strings verbatim at the semantically-equivalent keys:
       - "Save compliance fields" key → "Konformitätsfelder speichern"
       - "Continue" (onboarding) key → "Weiter"
       - "Download as PDF" key → "Als PDF herunterladen"
       - Acknowledgement checkbox label key → "Ich habe die Datenschutzerklärung gelesen und verstanden"
       - Footer privacy link key → "Datenschutz"
       - Language switcher labels: "Englisch", "Polnisch", "Arabisch", "Deutsch"
    4. Translate every error string from UI-SPEC §Copywriting error states into formal Sie German. Example: "UTR must be 10 digits. Remove spaces and letters." → "Die UTR muss aus 10 Ziffern bestehen. Entfernen Sie Leerzeichen und Buchstaben."
    5. DO NOT use any of these identifiers as JSON keys (RESERVED_LEGAL_KEYS from Plan 03):
       GDPR_CONTROLLER_LABEL, GDPR_RIGHTS_HEADING, GDPR_DPO_LABEL, GDPR_COMPLAINT_HEADING, TAX_USTIDNR_LABEL, TAX_STEUERNUMMER_LABEL, TAX_HANDELSREGISTER_LABEL, TAX_SOZIALVERSICHERUNGSNUMMER_LABEL, TAX_KLEINUNTERNEHMER_LABEL.
    6. DO NOT include any whole-word occurrence of Du, Dir, Dein, Deine, Deiner, Deines as surface text.
    7. Preserve ICU MessageFormat syntax from en.json. German uses one/other plural; rewrite literal words inside plural arguments only.
    8. Preserve variable placeholders {name}, {date}, etc.
    9. Add top-level `_meta` namespace:
       ```json
       "_meta": {
         "status": "ai-first-draft",
         "register": "formal-Sie",
         "steuerberater_review_required": true,
         "generated": "2026-04-12"
       }
       ```
    10. Note: technical German words like "Steuernummer" / "USt-IdNr" MAY appear as string VALUES (UI labels, error messages) — only the 9 RESERVED_LEGAL_KEYS identifiers are forbidden as KEY names.

    Self-verification before commit:
    - Flatten both en.json and de.json key paths; en paths must all exist in de paths.
    - grep for "Du|Dir|Dein" as JSON string word-tokens — must be zero.
    - grep for any RESERVED_LEGAL_KEYS identifier as a key — must be zero.

    Finalise apps/web/src/i18n/__tests__/de-locale.test.ts (created in Plan 01 as a Wave 0 scaffold). Remove any `fs.existsSync` early-returns since de.json now exists. Add/ensure the parity assertion:
    ```typescript
    it('de.json contains every key present in en.json', () => {
      const en = flatKeys(loadMessages('en'));
      const de = flatKeys(loadMessages('de'));
      const missing = en.filter(k => !de.includes(k));
      expect(missing, `Missing keys: ${missing.join(', ')}`).toEqual([]);
    });
    ```
    Ensure tests for routing.locales includes 'de' and localeSettings.de.timeZone==='Europe/Berlin' also pass.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const en=JSON.parse(fs.readFileSync('apps/web/messages/en.json','utf8')); const de=JSON.parse(fs.readFileSync('apps/web/messages/de.json','utf8')); function flat(o,p){return Object.entries(o).flatMap(([k,v])=>v&&typeof v==='object'&&!Array.isArray(v)?flat(v,p+k+'.'):[p+k])} const enK=flat(en,''); const deK=flat(de,''); const missing=enK.filter(k=>!deK.includes(k)); if(missing.length){console.error('missing',missing);process.exit(1)} console.log('OK',enK.length,'keys')"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/messages/de.json exists and is valid JSON (`node -e "JSON.parse(require('fs').readFileSync('apps/web/messages/de.json','utf8'))"` exits 0).
    - Flat-key parity: every key path in en.json exists in de.json.
    - `grep -cE "\"(Du|Dir|Dein)[^a-zA-Z\"]" apps/web/messages/de.json` returns 0.
    - `grep -cE "\"(GDPR_CONTROLLER_LABEL|GDPR_RIGHTS_HEADING|GDPR_DPO_LABEL|GDPR_COMPLAINT_HEADING|TAX_USTIDNR_LABEL|TAX_STEUERNUMMER_LABEL|TAX_HANDELSREGISTER_LABEL|TAX_SOZIALVERSICHERUNGSNUMMER_LABEL|TAX_KLEINUNTERNEHMER_LABEL)\":" apps/web/messages/de.json` returns 0.
    - `grep "Konformitätsfelder speichern" apps/web/messages/de.json` matches.
    - `grep "Weiter" apps/web/messages/de.json` matches.
    - `grep "Als PDF herunterladen" apps/web/messages/de.json` matches.
    - `grep "Ich habe die Datenschutzerklärung gelesen und verstanden" apps/web/messages/de.json` matches.
    - `grep "Datenschutz" apps/web/messages/de.json` matches.
    - `grep "_meta" apps/web/messages/de.json` matches.
    - `pnpm --filter @contractor-ops/web test --run de-locale` GREEN.
    - `pnpm --filter @contractor-ops/validators test --run locked-phrases-guard` GREEN (now asserts against a present de.json).
  </acceptance_criteria>
  <done>
    de.json shipped as AI first-draft with full parity to en.json, formal Sie register throughout, no reserved keys, all UI-SPEC-mandated verbatim strings present. Locked-phrases guard validates cleanly against the new file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Locale files are build-time assets shipped with the app. No runtime user input reaches de.json. The addition of `de` to `routing.locales` affects middleware locale negotiation but all traffic already goes through `createMiddleware(routing)` without new attack surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-15 | Repudiation (informal register slips in) | messages/de.json | mitigate | CI guard test asserts no Du/Dir/Dein tokens; Steuerberater review (Plan 08) is the authoritative human gate. |
| T-56-16 | Compliance exposure (reserved legal key added) | messages/de.json | mitigate | Locked-phrases-guard iterates all locales; fails CI if any reserved identifier appears as a key (ASVS V10). |
| T-56-17 | Tampering (locale negotiation enabling disabled locales) | routing.locales | accept | next-intl `createMiddleware(routing)` enforces whitelist automatically; no disabled/sunset locales — all four are production-ready. |
</threat_model>

<verification>
- routing.ts contains 'de' in locales array
- request.ts has de entry with Europe/Berlin + EUR
- messages/de.json exists, is valid JSON, parity-checked against en.json
- Formal Sie register: zero Du/Dir/Dein hits
- Zero reserved-key collisions
- de-locale.test.ts GREEN
- locked-phrases-guard.test.ts GREEN (now with real de.json)
</verification>

<success_criteria>
DE is a first-class locale of the platform per D-11, D-12, D-13. Every en.json key has a DE translation. Formal Sie register is grep-verified. Reserved legal identifiers are structurally excluded from the translation file. Plan 06 and Plan 07 can now call `useTranslations('Consent')` etc. and receive DE copy.
</success_criteria>

<output>
Create `.planning/phases/56-country-foundations-german-i18n/56-05-SUMMARY.md` covering: routing.ts + request.ts diff summary, de.json namespace coverage (number of translated keys), verbatim locked-string inventory (which 6 UI-SPEC §Copywriting strings were placed where), Steuerberater review backlog items (any strings the executor flagged as uncertain — group under `_meta.steuerberater_review_notes` inline in de.json for Plan 08 pickup).
</output>
