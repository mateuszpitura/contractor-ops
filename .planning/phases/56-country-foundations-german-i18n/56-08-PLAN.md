---
phase: 56
plan: 08
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - packages/validators/src/consent.ts
  - apps/web/src/components/consent/onboarding-consent-step.tsx
  - apps/web/src/components/consent/privacy-notice-acknowledgement.tsx
  - apps/web/src/components/consent/__tests__/onboarding-consent-step.test.tsx
  - packages/logger/src/pii-mask.ts
  - apps/web/src/app/[locale]/(auth)/onboarding/create-organization.ts
  - .planning/phases/56-country-foundations-german-i18n/56-STEUERBERATER-REVIEW.md
autonomous: false
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-05, FOUND-06]
requirements_addressed: [FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-05, FOUND-06]

must_haves:
  truths:
    - "Onboarding consent step is shown for UK and DE orgs (in addition to existing UAE/SA)"
    - "UK/DE acknowledgement checkbox must be ticked before 'Continue'/'Weiter' is enabled"
    - "DE-country orgs default language='de' at creation"
    - "SV-Nummer is on the @contractor-ops/logger PII mask list (ASVS V8)"
    - "Steuerberater review document exists with deliverables list; completion gates phase sign-off"
    - "Full phase verification checklist satisfied; no regression against AE/SA existing behaviour"
  artifacts:
    - path: "packages/validators/src/consent.ts"
      provides: "Extended jurisdiction predicate covering UK/DE"
      contains: "requiresPrivacyAcknowledgement"
    - path: "apps/web/src/components/consent/onboarding-consent-step.tsx"
      provides: "Extended step rendering acknowledgement checkbox for UK/DE in addition to PDPL flow"
    - path: ".planning/phases/56-country-foundations-german-i18n/56-STEUERBERATER-REVIEW.md"
      provides: "Deliverable list + sign-off slots for German legal review (D-13 gate)"
  key_links:
    - from: "apps/web/src/components/consent/onboarding-consent-step.tsx"
      to: "packages/validators/src/consent.ts requiresPrivacyAcknowledgement"
      via: "predicate replacing narrow isPdplJurisdiction skip gate"
      pattern: "requiresPrivacyAcknowledgement"
    - from: "packages/logger/src/pii-mask.ts"
      to: "SV-Nummer validator"
      via: "mask list entry `sozialversicherungsnummer` added"
      pattern: "sozialversicherungsnummer"
---

<objective>
Close the remaining Phase 56 gaps: extend the Phase 51 blocking onboarding consent step to cover UK/DE per D-10; ensure DE orgs default `language='de'` at creation per D-11; add SV-Nummer to the PII mask list (ASVS V8 follow-up flagged by Plan 06); set up the Steuerberater review document and sign-off slot per D-13; run the full phase verification checklist. Includes a human checkpoint for Steuerberater review completion before phase ships.

Purpose: This plan is the phase integration + compliance gate. It wires the UK/DE consent acknowledgement, plugs the SV-Nummer logging hole, commissions the external Steuerberater review, and gates Phase 56 completion on review sign-off. Without this plan, FOUND-05/FOUND-06 are technically live but users never see the acknowledgement step, and the German translations lack legal sign-off.
Output: Extended onboarding consent + acknowledgement component + DE-defaults on org creation + PII-mask update + Steuerberater deliverable document + final verification.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/56-country-foundations-german-i18n/56-CONTEXT.md
@.planning/phases/56-country-foundations-german-i18n/56-RESEARCH.md
@.planning/phases/56-country-foundations-german-i18n/56-VALIDATION.md
@.planning/phases/56-country-foundations-german-i18n/56-UI-SPEC.md
@packages/validators/src/consent.ts
@apps/web/src/components/consent/onboarding-consent-step.tsx
@apps/web/src/components/consent/privacy-notice-display.tsx
@packages/validators/src/legal/de.ts
@apps/web/messages/de.json
@packages/logger/src/pii-mask.ts
@apps/web/src/app/[locale]/(legal)/privacy/__tests__/privacy-de.test.tsx

<interfaces>
From packages/validators/src/consent.ts (existing — Plan 08 extends):
```typescript
export function isPdplJurisdiction(code: string | null | undefined): boolean;
// Plan 08 adds:
export function requiresPrivacyAcknowledgement(code: string | null | undefined): boolean;
```

Existing onboarding-consent-step.tsx gate:
```typescript
if (!isPdplJurisdiction(orgCountryCode)) return null;  // Plan 08 widens to requiresPrivacyAcknowledgement
```

New component shape:
```typescript
// apps/web/src/components/consent/privacy-notice-acknowledgement.tsx
export interface PrivacyNoticeAcknowledgementProps {
  checked: boolean;
  onChange: (checked: boolean) => void;
  jurisdictionUrl: string;   // /legal/privacy/gb|de|eu
  error?: string;
}
export function PrivacyNoticeAcknowledgement(props): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend onboarding consent step for UK/DE + PII mask + DE-default language</name>
  <files>
    packages/validators/src/consent.ts,
    apps/web/src/components/consent/privacy-notice-acknowledgement.tsx,
    apps/web/src/components/consent/onboarding-consent-step.tsx,
    apps/web/src/components/consent/__tests__/onboarding-consent-step.test.tsx,
    packages/logger/src/pii-mask.ts,
    apps/web/src/app/[locale]/(auth)/onboarding/create-organization.ts
  </files>
  <read_first>
    - packages/validators/src/consent.ts (existing isPdplJurisdiction returns true only for AE/SA; widen or add new predicate per D-10)
    - apps/web/src/components/consent/onboarding-consent-step.tsx (existing flow; must preserve AE/SA behaviour)
    - apps/web/src/components/consent/privacy-notice-display.tsx (reused)
    - .planning/phases/56-country-foundations-german-i18n/56-UI-SPEC.md §Interaction 6 (onboarding consent extension flow)
    - .planning/phases/56-country-foundations-german-i18n/56-CONTEXT.md D-09, D-10, D-11
    - packages/logger/src/pii-mask.ts (existing PII mask list — confirm current keys, add SV-Nummer)
    - apps/web/src/app/[locale]/(auth)/onboarding — locate the org-creation handler (grep for `createOrganization` or `organization.create`); this is where the D-11 `language='de'` default applies for DE orgs
    - packages/api/src/routers/privacy-notice.ts or services/privacy-notice.ts (to understand the ConsentRecord write path that Plan 51 established — reuse verbatim)
  </read_first>
  <action>
    1. **Extend predicate in `packages/validators/src/consent.ts`:** add
    ```typescript
    export function requiresPrivacyAcknowledgement(code: string | null | undefined): boolean {
      return isPdplJurisdiction(code) || code === 'GB' || code === 'DE';
    }
    ```
    Keep `isPdplJurisdiction` unchanged so nothing else using it regresses. Export via barrel.

    2. **`privacy-notice-acknowledgement.tsx` (new):** `'use client';` checkbox-with-link component.
    ```tsx
    'use client';
    import { useTranslations } from 'next-intl';
    import { Checkbox } from '@/components/ui/checkbox';
    import { Label } from '@/components/ui/label';

    export interface PrivacyNoticeAcknowledgementProps {
      checked: boolean;
      onChange: (checked: boolean) => void;
      jurisdictionUrl: string;  // e.g. '/de/legal/privacy/de'
      error?: string;
    }

    export function PrivacyNoticeAcknowledgement({ checked, onChange, jurisdictionUrl, error }: PrivacyNoticeAcknowledgementProps) {
      const t = useTranslations('Consent.privacyAcknowledgement');
      return (
        <div className="space-y-2">
          <div className="flex items-start gap-3">
            <Checkbox
              id="privacy-ack"
              checked={checked}
              onCheckedChange={v => onChange(v === true)}
              aria-describedby={error ? 'privacy-ack-error' : undefined}
              aria-required="true"
            />
            <Label htmlFor="privacy-ack" className="text-sm leading-5">
              {t.rich('label', {
                link: (chunks) => (
                  <a href={jurisdictionUrl} target="_blank" rel="noopener noreferrer" className="text-primary underline">
                    {chunks}
                  </a>
                ),
              })}
            </Label>
          </div>
          {error && <p id="privacy-ack-error" role="alert" aria-live="polite" className="text-xs text-destructive">{error}</p>}
        </div>
      );
    }
    ```
    DE translation key `Consent.privacyAcknowledgement.label` in Plan 05's de.json must equal: `"Ich habe die <link>Datenschutzerklärung</link> gelesen und verstanden."` — verify Plan 05 shipped this; if missing, patch de.json here (extend messages file).

    3. **Modify `onboarding-consent-step.tsx`:**
       - Change the skip predicate from `isPdplJurisdiction(orgCountryCode)` to `requiresPrivacyAcknowledgement(orgCountryCode)`.
       - Add state `const [privacyAck, setPrivacyAck] = useState(false);`.
       - Render `<PrivacyNoticeAcknowledgement>` below the purpose toggles (not above — per UI-SPEC §Interaction 6 step order).
       - Compute `jurisdictionUrl` via `` `/${locale}/legal/privacy/${resolveJurisdictionForCountry(orgCountryCode)}` `` (a small helper mirroring `resolveJurisdiction` from Plan 07 but client-safe — can be inlined).
       - Change `allRequiredGranted = REQUIRED_PURPOSES.every(p => consents[p] === true)` to ALSO require `privacyAck === true`.
       - Change Continue button disabled state to use the new composite gate.
       - Continue button uses `useTranslations('Consent').t('continue')` producing `"Weiter"` on DE locale.
       - On accept, include `privacyNoticeVersion: 1` and `privacyNoticeJurisdiction: resolveJurisdictionForCountry(orgCountryCode)` in the ConsentRecord write — extend the existing mutation call to include these fields (if the Phase 51 mutation already accepts them, pass through; if not, raise as a Plan 08 follow-up in SUMMARY).
       - Preserve all UAE/SA behaviour bit-for-bit — only ADD the acknowledgement gate for GB/DE; AE/SA flows may also receive the acknowledgement (Phase 51 has its own consent purposes so this extension is compatible).

    4. **Extend Wave 0 `onboarding-consent-step.test.tsx` to GREEN:**
       - `it('skips entirely for non-PDPL, non-GB/DE orgs')`: pass `orgCountryCode='PL'` → component returns null.
       - `it('renders acknowledgement checkbox for GB orgs')`: `orgCountryCode='GB'` → checkbox visible; Continue disabled.
       - `it('renders acknowledgement checkbox for DE orgs')`: same for DE.
       - `it('Continue is disabled until acknowledgement checked')`: click checkbox → Continue transitions from aria-disabled=true to enabled.
       - `it('acknowledgement link points to jurisdiction URL with target blank noopener')`: assert link attributes.
       - `it('preserves UAE/SA behaviour when privacy ack present')`: `orgCountryCode='AE'` → existing consent purposes + new ack both required.

    5. **PII mask update — `packages/logger/src/pii-mask.ts`:** read current mask list; append:
    ```typescript
    // Added Phase 56 — Art. 9 DSGVO sensitive data (ASVS V8)
    'sozialversicherungsnummer',
    'svNummer',
    'sv_nummer',
    ```
    Ensure the mask function applies to these keys (case-insensitive) at all log entry points. Add a logger test asserting: `maskPii({ sozialversicherungsnummer: '65180539M032', other: 'x' })` returns `{ sozialversicherungsnummer: '[REDACTED]', other: 'x' }`.

    6. **DE-default language on org creation (D-11):** locate the organization-creation code path. This is the tRPC mutation that creates Organization rows (likely `packages/api/src/routers/organization.ts` `createOrganization` procedure, OR an onboarding wizard handler). After reading that file, find where `language` is set. If the code already does `language: input.language ?? 'pl'`, replace with:
    ```typescript
    const defaultLanguage =
      input.language ??
      (input.countryCode === 'DE' ? 'de' :
       input.countryCode === 'GB' ? 'en' :
       'pl');
    ```
    Add a test in the mutation's test file (if exists) or create one under the same path assertng: `createOrg({ countryCode: 'DE' })` yields an org with `language === 'de'` when no explicit language supplied.

    If the exact location differs from the heuristic path listed in frontmatter `files_modified`, update `files_modified` in the PR description accordingly (but commit the Plan 08 frontmatter as-is — the reconciliation happens at verify-work time).
  </action>
  <verify>
    <automated>pnpm --filter @contractor-ops/web test --run onboarding-consent-step 2>&1 | grep -E "Tests.*passed" && grep "requiresPrivacyAcknowledgement" packages/validators/src/consent.ts && grep "sozialversicherungsnummer" packages/logger/src/pii-mask.ts && grep -E "countryCode === 'DE'.*'de'" $(grep -rl "createOrganization\|organization.create" packages/api/src apps/web/src 2>/dev/null | head -5 | tr '\n' ' ')</automated>
  </verify>
  <acceptance_criteria>
    - `packages/validators/src/consent.ts` exports `requiresPrivacyAcknowledgement` returning true for AE/SA/GB/DE.
    - `apps/web/src/components/consent/privacy-notice-acknowledgement.tsx` exists and renders Checkbox + Label with external-link attributes.
    - `apps/web/src/components/consent/onboarding-consent-step.tsx` uses `requiresPrivacyAcknowledgement` as gate and requires `privacyAck === true` before Continue enables.
    - Continue button label sourced via `t('continue')` (DE renders "Weiter").
    - `pnpm --filter @contractor-ops/web test --run onboarding-consent-step` GREEN for all cases (GB + DE + PL + AE).
    - `packages/logger/src/pii-mask.ts` contains entries for SV-Nummer keys; logger mask test passes.
    - Organization-creation code path sets `language='de'` for DE orgs absent explicit user override.
    - AE/SA onboarding flow untouched (regression test for existing Phase 51 behaviour passes).
  </acceptance_criteria>
  <done>
    Onboarding consent step extended for UK/DE; SV-Nummer masked in logs; DE orgs default language=de; all Wave 0 consent tests GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Steuerberater review deliverable + phase verification gate</name>
  <files>
    .planning/phases/56-country-foundations-german-i18n/56-STEUERBERATER-REVIEW.md
  </files>
  <read_first>
    - .planning/STATE.md (blocker item: "German Steuerberater review of tax terminology should be commissioned during Phase 56")
    - .planning/phases/56-country-foundations-german-i18n/56-CONTEXT.md D-13 (review workflow)
    - .planning/phases/56-country-foundations-german-i18n/56-VALIDATION.md §Manual-Only Verifications (rows 1, 3, 4 are Steuerberater items)
    - apps/web/messages/de.json (content to review)
    - packages/validators/src/legal/de.ts (9 locked phrases)
    - packages/validators/src/privacy-notices/de.ts (DE Datenschutzerklärung content)
    - packages/validators/src/de-validators.ts (A3 SV-Nummer weight array — flagged for vector verification)
  </read_first>
  <action>
    Create `.planning/phases/56-country-foundations-german-i18n/56-STEUERBERATER-REVIEW.md` with the structure below:

    ```markdown
    # Phase 56 — Steuerberater Review Deliverable

    **Status:** commissioned — awaiting sign-off
    **Commissioned:** YYYY-MM-DD
    **Target completion:** before Phase 56 ships
    **Reviewer:** {name + qualification to be filled by project owner}

    ## Review Scope

    Per CONTEXT.md D-13, this review covers:

    1. **Locked legal phrases (9 constants):** verify exact forms in `packages/validators/src/legal/de.ts`:
       - GDPR_CONTROLLER_LABEL — "Verantwortlicher im Sinne der DSGVO"
       - GDPR_RIGHTS_HEADING — "Ihre Rechte als betroffene Person"
       - GDPR_DPO_LABEL — "Datenschutzbeauftragter"
       - GDPR_COMPLAINT_HEADING — "Beschwerderecht bei der Aufsichtsbehörde"
       - TAX_USTIDNR_LABEL — "Umsatzsteuer-Identifikationsnummer (USt-IdNr)"
       - TAX_STEUERNUMMER_LABEL — "Steuernummer"
       - TAX_HANDELSREGISTER_LABEL — "Handelsregisternummer"
       - TAX_SOZIALVERSICHERUNGSNUMMER_LABEL — "Sozialversicherungsnummer"
       - TAX_KLEINUNTERNEHMER_LABEL — "Kleinunternehmer gemäß § 19 UStG"

    2. **German Datenschutzerklärung text:** full review of `packages/validators/src/privacy-notices/de.ts` and `apps/web/src/app/[locale]/(legal)/privacy/(content)/de.mdx` against:
       - DSGVO Art. 13 / 14 required elements
       - BfDI Mustertexte alignment
       - BDSG-specific clauses

    3. **Formal register (Sie) across `apps/web/messages/de.json`:** spot-check and confirm no slip into Du-register; automated CI check (`locked-phrases-guard` Du/Dir/Dein regex) is pre-verified but human eye catches borderline cases (e.g. Duzen in nouns).

    4. **SV-Nummer checksum vector verification (ASSUMPTION A3):** validate 5 real-world SV-Nummern (with owner consent) OR synthetic DRV test vectors against the algorithm in `packages/validators/src/de-validators.ts`. Weight array under review: `[2, 1, 2, 5, 7, 1, 2, 1, 2, 1, 2, 1]`.

    5. **Steuernummer per-Bundesland regex:** confirm 16-state table in `packages/validators/src/steuernummer-formats.ts` matches current ELSTER/Finanzamt practice. Flag any states where format changed since research date 2026-04-12.

    6. **Handelsregister court list completeness:** cross-reference `packages/validators/src/handelsregister-courts.ts` against `https://www.justiz.de/onlinedienste/registerportal_der_laender/index.php` — flag any missing or dead courts.

    7. **UI copy sanity (DE profile surfaces):** render contractor profile in DE locale, screenshot the Country Compliance card + onboarding consent step + footer, check labels feel natural to a German-speaking B2B user.

    ## Deliverable Format

    Reviewer returns this document with the following filled in per item:

    | Item | Status | Corrections required | Notes |
    |------|--------|----------------------|-------|
    | 1. Locked phrases | ☐ approved ☐ changes | ... | ... |
    | 2. Datenschutzerklärung | ☐ approved ☐ changes | ... | ... |
    | 3. Sie register | ☐ approved ☐ changes | ... | ... |
    | 4. SV-Nummer vectors | ☐ approved ☐ changes | ... | ... |
    | 5. Steuernummer regex | ☐ approved ☐ changes | ... | ... |
    | 6. Handelsregister list | ☐ approved ☐ changes | ... | ... |
    | 7. UI copy | ☐ approved ☐ changes | ... | ... |

    ## Integration Plan After Sign-Off

    1. Apply reviewer corrections to the relevant source files (de.json / legal/de.ts / privacy-notices/de.ts).
    2. Re-run `pnpm turbo run test` — all tests must remain GREEN (modifying locked phrases forces updates to LOCKED_DE_PHRASES constants; the CI guard keeps everything synced).
    3. Update this document with `Status: approved YYYY-MM-DD` once all items are ☐ approved.
    4. Update `.planning/STATE.md` — move the "German Steuerberater review" blocker from pending to resolved.
    5. Ship Phase 56.

    ## Sign-Off

    Reviewer signature: ______________________
    Date: ______________________
    ```

    This document is not a human interactive task — it's a commissioning artefact. The actual review is a Task 3 checkpoint that pauses the phase for human action.
  </action>
  <verify>
    <automated>test -f .planning/phases/56-country-foundations-german-i18n/56-STEUERBERATER-REVIEW.md && grep -c "☐" .planning/phases/56-country-foundations-german-i18n/56-STEUERBERATER-REVIEW.md | awk '$1 >= 14'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/56-country-foundations-german-i18n/56-STEUERBERATER-REVIEW.md` exists.
    - Document lists all 7 review items with checkbox markers.
    - Document references the exact files the reviewer must inspect.
    - Document includes sign-off slot and integration plan.
    - `grep "Verantwortlicher im Sinne der DSGVO" 56-STEUERBERATER-REVIEW.md` matches (all 9 locked phrases enumerated).
  </acceptance_criteria>
  <done>
    Steuerberater review commissioning document committed; ready for external reviewer engagement.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Steuerberater review completion checkpoint</name>
  <what-built>
    Automated pipeline has delivered:
    - Typed constant module `legal/de.ts` with 9 locked German legal phrases
    - German Datenschutzerklärung (MDX + privacy-notices data module)
    - Full `messages/de.json` (AI first-pass, formal Sie register)
    - DE validators (USt-IdNr, SV-Nummer, Steuernummer per-Bundesland, Handelsregister)
    - `.planning/phases/56-country-foundations-german-i18n/56-STEUERBERATER-REVIEW.md` commissioning document
  </what-built>
  <action>Human-only checkpoint. Claude pauses here; resume when the Steuerberater sign-off document is committed per the steps in how-to-verify.</action>
  <how-to-verify>
    1. **Commission the reviewer.** Send the Steuerberater the review document + repo access (or a zip of the relevant files):
       - `packages/validators/src/legal/de.ts`
       - `packages/validators/src/privacy-notices/de.ts`
       - `apps/web/src/app/[locale]/(legal)/privacy/(content)/de.mdx`
       - `apps/web/messages/de.json`
       - `packages/validators/src/de-validators.ts` (SV-Nummer algorithm)
       - `packages/validators/src/steuernummer-formats.ts` (16-state regex table)
       - `packages/validators/src/handelsregister-courts.ts` (~120 courts)
       - Screenshots of DE-locale profile UI, onboarding consent step, privacy notice, footer

    2. **Wait for reviewer sign-off.** Review typically takes 1-2 weeks. During the wait:
       - The AI-draft code ships to staging (all automated tests GREEN — verified in Task 4).
       - Production release is BLOCKED until this checkpoint resolves.

    3. **Apply corrections.** When the reviewer returns the document with "changes required" items, open a follow-up implementation pass:
       - For locked-phrase corrections → edit `packages/validators/src/legal/de.ts` + regenerate the downstream consumers (privacy-notices/de.ts, de.mdx — CI guard will fail until they're re-synced).
       - For `de.json` translation corrections → edit and re-run `pnpm --filter @contractor-ops/validators test --run locked-phrases-guard`.
       - For SV-Nummer algorithm corrections → edit `packages/validators/src/de-validators.ts` + update test vectors.
       - For court list corrections → edit `packages/validators/src/handelsregister-courts.ts`.

    4. **Final sign-off.** Fill `Status: approved YYYY-MM-DD` in 56-STEUERBERATER-REVIEW.md; remove the blocker from `.planning/STATE.md`.

    5. **Type "approved" into the checkpoint resume prompt** when sign-off document is committed.
  </how-to-verify>
  <resume-signal>Type "approved" after Steuerberater sign-off is committed to 56-STEUERBERATER-REVIEW.md; OR "defer" to ship Phase 56 to staging with this blocker still open (production release remains gated).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Full phase verification — run all test suites + smoke checklist</name>
  <files>
    .planning/phases/56-country-foundations-german-i18n/56-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/56-country-foundations-german-i18n/56-VALIDATION.md (22-row test map + Manual-Only Verifications + Sampling Rate contract)
    - .planning/ROADMAP.md §Phase 56 Success Criteria (5 items)
    - All 8 Plan SUMMARY files (01..07) — verify every plan completed successfully
  </read_first>
  <action>
    Run the full phase verification per VALIDATION.md §Sampling Rate "Before `/gsd-verify-work`" row:

    1. **Automated test gate (required):**
       - `pnpm --filter @contractor-ops/validators test --run` — all validators + locked-phrases guard GREEN
       - `pnpm --filter @contractor-ops/web test --run` — all apps/web tests GREEN
       - `pnpm turbo run test` — full monorepo GREEN (no regressions in other packages)
       - `pnpm --filter @contractor-ops/web build` — production build GREEN
       - TypeScript: `pnpm --filter @contractor-ops/web exec tsc --noEmit` GREEN
       - Biome/lint: `pnpm --filter @contractor-ops/web lint` GREEN

    2. **Manual smoke checklist (executor runs in dev server):**
       - [ ] Visit `/en/legal/privacy/gb` → UK GDPR notice renders with 8 Article 13 sections + skip-link + TOC + download CTA.
       - [ ] Visit `/de/legal/privacy/de` → Datenschutzerklärung renders with all 9 locked phrases visible.
       - [ ] Visit `/en/legal/privacy/eu` → EU fallback renders.
       - [ ] Log in as a DE-org user; `/legal/privacy` auto-redirects to `/de/legal/privacy/de` (or locale-prefixed equivalent).
       - [ ] Switch platform to German via user-menu → layout flips, menus in DE, footer reads "Datenschutz".
       - [ ] Cycle through all 4 locales via user-menu → pl → en → ar → de → pl returns without gaps.
       - [ ] Create contractor in a GB-country org → UK field group renders with UTR/CH/VAT fields and required-markers by entity type.
       - [ ] Create contractor in a DE-country org → DE field group renders; select Bundesland=BY → Steuernummer placeholder updates to "181/815/08155"; select GmbH → Handelsregister fieldset appears; flip Kleinunternehmer toggle → USt-IdNr visibility changes.
       - [ ] Save compliance fields → tRPC mutation succeeds with Zod-validated payload.
       - [ ] Trigger privacy PDF download → signed URL returned; PDF opens with correct jurisdiction content.
       - [ ] Attempt IDOR: modify network request to `generatePrivacyNoticePdf` adding `{ jurisdiction: 'SA' }` — server still returns the session-derived jurisdiction PDF.
       - [ ] Onboarding wizard for UK/DE org shows acknowledgement checkbox; Continue disabled until checked.

    3. **Update VALIDATION.md frontmatter and test-row TBDs:** fill the Task ID and Plan columns for all 22 rows (executor walks the file and maps each row to the Plan that delivered its test); set `nyquist_compliant: true` + `wave_0_complete: true` + `approved: 2026-MM-DD` once all rows resolve.

    4. **Update STATE.md:** on success, set `status: complete`, move Phase 56 blocker items to resolved, bump `progress.completed_phases: 1` + `completed_plans: 8`.

    5. **Update ROADMAP.md:** mark Phase 56 checkbox as `[x]`, set plan count to `8 plans`, list plans and status.

    Emit final report with any leftover concerns (e.g. if Steuerberater review is "deferred" from Task 3, record as a production-release gate in STATE.md).
  </action>
  <verify>
    <automated>pnpm turbo run test 2>&1 | tail -20 | grep -E "Tasks:.*success" && pnpm --filter @contractor-ops/web build 2>&1 | tail -5 | grep -E "Compiled successfully|Generating static pages"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm turbo run test` exits 0 across all packages.
    - `pnpm --filter @contractor-ops/web build` succeeds.
    - `pnpm --filter @contractor-ops/web exec tsc --noEmit` clean.
    - All 22 rows in VALIDATION.md §Per-Task Verification Map have non-TBD Task ID and Plan columns.
    - VALIDATION.md frontmatter: `nyquist_compliant: true`, `wave_0_complete: true`, `approved: 2026-MM-DD`.
    - Manual smoke checklist in the SUMMARY records each bullet as ✅ or notes the gap.
    - IDOR attempt on PDF mutation is documented as blocked (recorded in SUMMARY).
    - STATE.md and ROADMAP.md updated for Phase 56 completion (or staging-ship if Task 3 is deferred).
    - All 5 Phase 56 success criteria from ROADMAP are satisfied and documented.
  </acceptance_criteria>
  <done>
    Phase 56 end-to-end verified; every automated test GREEN; every manual smoke item confirmed; Steuerberater review document commissioned (complete if Task 3 signed off, otherwise flagged as production-release blocker).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
This plan extends the existing Phase 51 ConsentRecord write path (no new tRPC mutations — reuses `trpc.consent.bulkGrant` plus added acknowledgement fields). PII mask update touches server-only logger configuration. Organization-creation mutation runs inside existing auth/authz guards.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-30 | Repudiation (consent bypass) | onboarding-consent-step.tsx | mitigate | Composite gate (REQUIRED_PURPOSES + privacyAck) enforced on both client disable-state AND server-side in the existing `consent.bulkGrant` mutation (executor verifies the server path also asserts acknowledgement fields; if the existing Phase 51 mutation does not, raise in SUMMARY as Plan-09 follow-up). |
| T-56-31 | Information Disclosure (SV-Nummer in logs) | packages/logger/src/pii-mask.ts | mitigate | New mask entries; logger test verifies masking (ASVS V8). |
| T-56-32 | Authorization (language default bypass) | organization-creation mutation | accept | `language='de'` is a UX default; users can always switch via user-menu. No security boundary. |
| T-56-33 | Compliance exposure (AI-draft de.json in prod without review) | Phase 56 release | mitigate | Task 3 checkpoint blocks production release until Steuerberater sign-off; staging-ship is acceptable without sign-off but STATE.md records the gate. |
| T-56-34 | Tampering (Steuerberater correction integration) | Post-review edits | mitigate | Integration plan in 56-STEUERBERATER-REVIEW.md §Integration Plan After Sign-Off prescribes the correct ordering (constants first, consumers regenerated, tests re-run). |
</threat_model>

<verification>
- Onboarding consent shows for UK/DE/AE/SA; not for PL/other
- Acknowledgement checkbox required for UK/DE
- SV-Nummer masked in logger output
- DE orgs default language=de
- Steuerberater review document committed
- Phase verification: `pnpm turbo run test` GREEN; build GREEN; tsc clean; smoke checklist satisfied
- VALIDATION.md fully resolved
- STATE.md + ROADMAP.md reflect Phase 56 completion (or staging-shipped with open reviewer gate)
</verification>

<success_criteria>
Every Phase 56 requirement (FOUND-01..06) closed. Every CONTEXT.md decision (D-01..D-14) implemented. Onboarding acknowledgement enforced for UK/DE. SV-Nummer logging hardened. DE orgs ship with language=de default. Steuerberater review commissioned with explicit sign-off gate. All 22 VALIDATION.md test rows have an owning Plan. All 5 ROADMAP §Phase 56 success criteria are verifiably true.
</success_criteria>

<output>
Create `.planning/phases/56-country-foundations-german-i18n/56-08-SUMMARY.md` covering: onboarding consent diff summary, PII-mask entries added, org-creation DE-default diff, Steuerberater review commissioning status, full automated test run output summary (pass/fail counts per workspace), manual smoke checklist results with screenshots (or file links), ASVS compliance matrix (V4, V5, V7, V8, V10, V12, V13, V14) — each category + evidence of mitigation from the 8 plans, outstanding risks (if any), Phase 56 sign-off recommendation (ship-to-staging vs ship-to-production).
</output>
