---
phase: 58
plan: 04
type: execute
wave: 4
depends_on: [58-01, 58-02, 58-03]
files_modified:
  - apps/web/src/app/[locale]/contractors/[id]/engagements/[engagementId]/classification/page.tsx
  - apps/web/src/components/contractors/classification/wizard/classification-wizard-shell.tsx
  - apps/web/src/components/contractors/classification/wizard/classification-step-indicator.tsx
  - apps/web/src/components/contractors/classification/wizard/classification-progress-bar.tsx
  - apps/web/src/components/contractors/classification/wizard/classification-autosave-indicator.tsx
  - apps/web/src/components/contractors/classification/wizard/wizard-question.tsx
  - apps/web/src/components/contractors/classification/wizard/legal-reference-collapsible.tsx
  - apps/web/src/components/contractors/classification/wizard/answers/yes-no-answer.tsx
  - apps/web/src/components/contractors/classification/wizard/answers/likert-answer.tsx
  - apps/web/src/components/contractors/classification/wizard/answers/score-03-answer.tsx
  - apps/web/src/components/contractors/classification/wizard/answers/rationale-textarea.tsx
  - apps/web/src/components/contractors/classification/wizard/schein/economic-dependency-input.tsx
  - apps/web/src/components/contractors/classification/wizard/ir35/ir35-wizard-steps.tsx
  - apps/web/src/components/contractors/classification/wizard/schein/schein-wizard-steps.tsx
  - apps/web/src/components/contractors/classification/wizard/__tests__/classification-wizard-shell.test.tsx
  - apps/web/src/components/contractors/classification/wizard/__tests__/a11y.test.tsx
  - apps/web/messages/en.json
  - apps/web/messages/pl.json
  - apps/web/messages/de.json
  - apps/web/messages/ar.json
autonomous: true
requirements: [CLASS-02, CLASS-05]
requirements_addressed: [CLASS-02, CLASS-05]

must_haves:
  truths:
    - "Navigating to /[locale]/contractors/[id]/engagements/[engagementId]/classification loads the correct wizard (IR35 5-step for GB, Scheinselbständigkeit 4-step for DE) and auto-creates a draft on first visit."
    - "Wizard enforces sequential step order with step-guard: Next button is disabled until current-step Zod validation passes."
    - "Every answer change debounces 500ms (textareas) or fires on blur/select (radio/number) and persists via trpc.classification.saveAnswer; optimistic-concurrency guard is honoured."
    - "Autosave indicator shows 'Not saved yet' → 'Saving…' → 'Saved {relativeTime}' → 'Save failed — retrying' states with aria-live='polite'."
    - "Disclaimer modal IS NOT rendered by this plan — it is owned by Plan 05. On final step, clicking Submit invokes trpc.classification.submit and navigates to the outcome URL; Plan 05 renders the modal there."
    - "Wizard shell is WCAG 2.1 AA: progress bar aria-valuenow uses fractional completion, step indicator aria-current='step', focus-visible rings, no keyboard traps."
    - "No answer-input component, no wizard component, and no page file imports from packages/classification/src/profiles/*/scoring.* — enforced by grep assertion (Pitfall 2)."
    - "Step titles, step counter, Next/Previous, autosave states are translated across en/pl/de/ar via the new Classification namespace in messages/*.json."
    - "Rule-set drift surfaced from tRPC getDraft PRECONDITION_FAILED is presented to the user as a blocking notification with the 'Resume draft blocked — rule-set version mismatch' copy from UI-SPEC §Error states."
  artifacts:
    - path: apps/web/src/components/contractors/classification/wizard/classification-wizard-shell.tsx
      provides: "Multi-step RHF + Zod wizard with step indicator + progress bar + autosave + server-side submit"
      contains: "trpc.classification.saveAnswer.useMutation"
      min_lines: 150
    - path: apps/web/src/components/contractors/classification/wizard/classification-progress-bar.tsx
      provides: "Radix Progress wrapper with fractional aria-valuenow"
      contains: "aria-valuenow"
    - path: apps/web/src/components/contractors/classification/wizard/wizard-question.tsx
      provides: "Generic question card dispatching by answerType"
      contains: "answerType"
    - path: apps/web/src/components/contractors/classification/wizard/answers/score-03-answer.tsx
      provides: "Score03 radio group with explicit Nicht anwendbar option (rawScore 0 + isNotApplicable:true)"
      contains: "isNotApplicable"
    - path: apps/web/src/components/contractors/classification/wizard/schein/economic-dependency-input.tsx
      provides: "0-100 numeric input with % suffix; blur-fires saveAnswer"
      contains: "inputMode=\"numeric\""
    - path: apps/web/src/app/[locale]/contractors/[id]/engagements/[engagementId]/classification/page.tsx
      provides: "Wizard entry route — creates/fetches draft, selects profile by engagement country, handles rule-set drift"
      contains: "createDraft"
  key_links:
    - from: apps/web/src/components/contractors/classification/wizard/classification-wizard-shell.tsx
      to: packages/api/src/routers/classification.ts
      via: "trpc.classification.saveAnswer / submit mutations"
      pattern: "trpc\\.classification\\."
    - from: apps/web/src/components/contractors/classification/wizard/classification-wizard-shell.tsx
      to: packages/classification/src/index.ts
      via: "import of rule-set constants + question types (client-safe: only types + rule-set constants, NEVER scoring)"
      pattern: "@contractor-ops/classification"
    - from: apps/web/src/components/contractors/classification/wizard/answers/score-03-answer.tsx
      to: packages/validators/src/legal/de.ts
      via: "CLASSIFICATION_SCHEIN_NOT_APPLICABLE verbatim label"
      pattern: "CLASSIFICATION_SCHEIN_NOT_APPLICABLE"
---

<objective>
Build the multi-step assessment wizard (UI-SPEC §Interaction 1–4): the page entry point that creates/fetches a draft via tRPC, the wizard shell with progress bar + step indicator + Previous/Next + autosave indicator, the 4 answer-input components (Yes/No, Likert-5, Score-0-3 with Nicht-anwendbar, Rationale textarea), the DRV-specific EconomicDependencyInput, the LegalReferenceCollapsible, and the IR35/DRV step compositions that render questions from the rule-set constants.

Purpose: Users can complete a full assessment end-to-end across all 5 (IR35) or 4 (DRV) steps with draft autosave, resume support, and accessibility compliance. Plan 05 takes over at Submit by navigating to the outcome route + rendering the blocking disclaimer modal.

Output: A live wizard at `/[locale]/contractors/[id]/engagements/[engagementId]/classification` that progresses the ClassificationAssessment row from absent → draft → (post-submit) completed with the outcome stored server-side.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.planning/phases/58-classification-engine-rule-sets/58-UI-SPEC.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.planning/phases/58-classification-engine-rule-sets/58-CONTEXT.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.planning/phases/58-classification-engine-rule-sets/58-RESEARCH.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.planning/phases/58-classification-engine-rule-sets/58-VALIDATION.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.planning/phases/58-classification-engine-rule-sets/58-03-SUMMARY.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/CLAUDE.md

<interfaces>
<!-- shadcn primitives already installed (verified Plan 01 readme) -->

From apps/web/src/components/ui/:
- Progress (Radix) — `<Progress value={number} aria-label="..." />`
- RadioGroup + RadioGroupItem
- Checkbox
- Textarea
- Input + InputGroup (for billing-ratio % suffix)
- Label
- Button
- Card + CardHeader + CardTitle + CardContent + CardFooter
- Collapsible + CollapsibleTrigger + CollapsibleContent
- Alert (for inline Plan 04 error states)

From tRPC client (apps/web/src/lib/trpc.ts — existing):
```typescript
trpc.classification.createDraft.useMutation()
trpc.classification.getDraft.useQuery({ contractorAssignmentId })
trpc.classification.saveAnswer.useMutation()
trpc.classification.submit.useMutation()
trpc.classification.acknowledgeDisclaimer.useMutation()  // Plan 05 uses this
```
(Names match Plan 03 router.)

From next-intl:
```typescript
import { useTranslations } from 'next-intl';
const t = useTranslations('Classification');
```

From packages/classification/src/index.ts (Plans 01+02):
```typescript
// CLIENT-SAFE imports (rule-set constants + types):
import { IR35_QUESTIONS, RULE_SET_VERSION as IR35_VERSION } from '@contractor-ops/classification/profiles/ir35/rule-set';
import { SCHEIN_QUESTIONS, CATEGORY_WEIGHTS, CATEGORY_TITLES } from '@contractor-ops/classification/profiles/scheinselbstandigkeit/rule-set';
import type { RuleSetQuestion, Ir35Area, ScheinCategory } from '@contractor-ops/classification';
import { getAnswerSchemaForType } from '@contractor-ops/classification';

// FORBIDDEN in client bundles: scoreIr35, scoreSchein — never import these into apps/web
```

From packages/validators/src/legal/de.ts (Plan 01):
```typescript
import { CLASSIFICATION_SCHEIN_NOT_APPLICABLE, CLASSIFICATION_SCHEIN_INTEGRATION, /* etc. */ } from '@contractor-ops/validators/legal/de';
```

Existing auth-gated server-component pattern in apps/web (see apps/web/src/app/[locale]/contractors/[id]/page.tsx for auth + tRPC prefetch + 'use client' boundary).

Existing Phase-56 pattern for country fields in apps/web/src/components/contractors/compliance/country-compliance-section.tsx — reuse its `<Bdi>` + locale handling for DE expansion.
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → tRPC | Wizard posts answers; server Zod re-validates — trust boundary crossed at route |
| client bundle → scoring | Scoring must NEVER ship to client; client only sends raw answers |

## STRIDE Threat Register (applicable ASVS: V5, V10, WCAG 2.1 AA)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-18 | Tampering | client outcome computation | mitigate | Client imports ONLY rule-set constants + types + answer schemas from @contractor-ops/classification. A CI grep assertion bans imports of profiles/*/scoring.*. (V5/Pitfall 2) |
| T-58-19 | Tampering | rationale free-text → XSS | mitigate | Rationale rendered via React (auto-escape); no `dangerouslySetInnerHTML` anywhere in wizard tree. (V10) |
| T-58-20 | Input Validation | answer payload | mitigate | Client-side Zod + RHF resolver; server-side re-validate in Plan 03. (V5) |
| T-58-21 | Info Disclosure | rule-set drift | mitigate | When tRPC getDraft returns PRECONDITION_FAILED, UI shows UI-SPEC Error-states copy and offers 'Start a new assessment' button. Old draft is preserved server-side (not deleted). |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wizard entry page + shell + progress bar + step indicator + autosave indicator + answer inputs + a11y tests</name>
  <files>apps/web/src/app/[locale]/contractors/[id]/engagements/[engagementId]/classification/page.tsx, apps/web/src/components/contractors/classification/wizard/classification-wizard-shell.tsx, apps/web/src/components/contractors/classification/wizard/classification-step-indicator.tsx, apps/web/src/components/contractors/classification/wizard/classification-progress-bar.tsx, apps/web/src/components/contractors/classification/wizard/classification-autosave-indicator.tsx, apps/web/src/components/contractors/classification/wizard/wizard-question.tsx, apps/web/src/components/contractors/classification/wizard/legal-reference-collapsible.tsx, apps/web/src/components/contractors/classification/wizard/answers/yes-no-answer.tsx, apps/web/src/components/contractors/classification/wizard/answers/likert-answer.tsx, apps/web/src/components/contractors/classification/wizard/answers/score-03-answer.tsx, apps/web/src/components/contractors/classification/wizard/answers/rationale-textarea.tsx, apps/web/src/components/contractors/classification/wizard/schein/economic-dependency-input.tsx, apps/web/src/components/contractors/classification/wizard/__tests__/classification-wizard-shell.test.tsx, apps/web/src/components/contractors/classification/wizard/__tests__/a11y.test.tsx, apps/web/messages/en.json, apps/web/messages/pl.json, apps/web/messages/de.json, apps/web/messages/ar.json</files>
  <read_first>
    - .planning/phases/58-classification-engine-rule-sets/58-UI-SPEC.md §Component Inventory, §Interaction 1–4, §Typography, §Color, §Copywriting Contract, §Accessibility Contract
    - .planning/phases/58-classification-engine-rule-sets/58-RESEARCH.md §Code Examples → Multi-step wizard form pattern (RHF + Zod)
    - apps/web/src/components/contractors/compliance/country-compliance-section.tsx (Phase 56 — Bdi + locale + shadcn patterns)
    - apps/web/src/components/ui/progress.tsx, radio-group.tsx, checkbox.tsx, textarea.tsx, input-group.tsx, collapsible.tsx, card.tsx
    - apps/web/src/lib/trpc.ts (client export)
    - apps/web/messages/en.json (base structure — identify namespace keys to add)
    - .planning/phases/58-classification-engine-rule-sets/58-VALIDATION.md rows D-07 / D-09 / D-10 / D-16
  </read_first>
  <behavior>
    ### RTL integration tests (`classification-wizard-shell.test.tsx`):
    - TEST WS-1: Render wizard with mocked tRPC (MSW or tRPC testing utils) — initial state shows step 1 of 5 (IR35) or 1 of 4 (DRV) based on mock.
    - TEST WS-2: Click Next without answering → Next remains disabled OR focus moves to first invalid field with inline error.
    - TEST WS-3: Answer all Step 1 questions → Next becomes enabled → click → progresses to Step 2; progress bar aria-valuenow updates.
    - TEST WS-4: Change an answer → assert trpc.classification.saveAnswer was invoked with { assessmentId, questionId, answer } (debounced for textarea, immediate for radio).
    - TEST WS-5: Rule-set drift — mock getDraft to return PRECONDITION_FAILED TRPCError → wizard shows the UI-SPEC drift error copy + a "Start new assessment" button (not the wizard form).
    - TEST WS-6: Score03Answer — selecting 'Nicht anwendbar' writes { rawScore: 0, isNotApplicable: true } to the form state.
    - TEST WS-7: EconomicDependencyInput — typing 120 → blur → inline error "Billing ratio must be between 0 and 100." and saveAnswer is NOT fired.
    - TEST WS-8: On final step click Submit → trpc.classification.submit invoked; on success, router.push called with outcome URL (`/classification/${resultId}`).

    ### A11y tests (`a11y.test.tsx`):
    - TEST A11Y-1: axe-core scan of the shell returns 0 violations.
    - TEST A11Y-2: Progress bar has role="progressbar", aria-valuenow (fractional), aria-valuemin=0, aria-valuemax=totalSteps, aria-label present.
    - TEST A11Y-3: Step indicator: active <li> has aria-current="step"; each step has a visible label ≥1024px and sr-only label < 640 px (assert .sr-only class + full text).
    - TEST A11Y-4: Autosave indicator has aria-live="polite" + aria-atomic="true".
    - TEST A11Y-5: Legal-reference Collapsible has correct aria-expanded managed by Radix.
    - TEST A11Y-6: Every radio option card is ≥ 44px tall on touch breakpoints (assert `h-11` class).
  </behavior>
  <action>
1. Extend `apps/web/messages/{en,pl,de,ar}.json` with a top-level `"Classification"` namespace containing all chrome keys used by the wizard: progress.ariaLabel, progress.announce, previous, next, submitAssessment, stepCounter, savedAgo, notSavedYet, saving, savedFailed, stepNotApplicable, error.draftDrift, error.draftDriftBody, error.startNew, error.ratioOutOfRange, error.nonNumeric, error.rationaleTooLong, emptyState.noAssessment, emptyState.notSupported, cancelDraft, backToEngagement, ir35.step.substitution, ir35.step.control, ir35.step.financialRisk, ir35.step.partAndParcel, ir35.step.moo, schein.step.integration, schein.step.entrepreneurial, schein.step.personalDep, schein.step.economicDep, schein.weightLabel ('30 % Gewichtung' etc. — for DE keep formal Sie — but this is chrome, not a locked phrase), legalReference.show, legalReference.hide, legalReference.ariaLabel.
   - DE strings MUST use formal Sie register per UI-SPEC.
   - Include `"_NOTE": "Legal-locked phrases live in packages/validators/src/legal/{de,disclaimers}.ts. Do NOT duplicate CLASSIFICATION_* or DISCLAIMER_* values here."` at the top of the Classification namespace in every locale (per Pitfall 9).
   - Copy the exact EN/PL/DE strings from UI-SPEC §Copywriting Contract → Primary CTAs + Step labels + Empty states + Error states. For AR, use good-faith translations and mark with `/* REVIEW:AR */` (Arabic is not in scope for Phase 58 but keys must exist to prevent next-intl runtime errors).

2. Create `apps/web/src/app/[locale]/contractors/[id]/engagements/[engagementId]/classification/page.tsx` (RSC):
   - Auth-gate via existing session helper.
   - Load ContractorAssignment server-side (via trpc.ssrHelpers or a direct `ctx.db` call per repo pattern — match existing engagement page).
   - If assignment.contractor.countryCode not in {'GB','DE'} → render the `Classification not available` empty state (UI-SPEC) + return early.
   - Call `trpc.classification.createDraft` (idempotent — returns existing draft if any) via a server-side prefetch + hydrate, OR render a client component that invokes the mutation on mount. Match the repo's existing prefetch pattern (check a similar engagement-scoped page).
   - Pass { assessmentId, countryCode, contractorAssignmentId } to `<ClassificationWizardShell />`.
   - Handle PRECONDITION_FAILED from getDraft by rendering the drift-blocked UI (UI-SPEC §Error states copy — `t('error.draftDriftBody', { oldVersion, newVersion })` + "Start a new assessment" button that calls a different tRPC mutation — since createDraft already returns existing, provide an escape via a `forceNewAfterDrift: true` flag OR a small helper mutation `recreateDraftAfterDrift` (DOCUMENT: this is a compensating action — the old draft row is preserved; a new row is created with the current ruleSetVersion. If Plan 03 didn't expose this, add it now to packages/api/src/routers/classification.ts as a minor extension and note it in the summary).

3. Create `classification-wizard-shell.tsx` ('use client') per RESEARCH §Code Examples → Multi-step wizard pattern + UI-SPEC §Interaction 1–4:
   - Top-level `<FormProvider {...form}>` with a dynamic resolver swap per step (use `useForm<any>` + call `form.trigger()` before advancing).
   - Step order derived from countryCode: `IR35_STEP_ORDER = ['substitution', 'control', 'financial-risk', 'part-and-parcel', 'moo']` (matches Ir35Area type order); `SCHEIN_STEP_ORDER = ['integration', 'entrepreneurial', 'personal-dep', 'economic-dep']`.
   - Render `<ClassificationProgressBar />`, `<ClassificationStepIndicator />`, `<Card>` containing current step's questions, footer with Previous/Next (or Submit on final step).
   - Autosave via `form.watch()` + debounced effect: for 'rationale' answerType, debounce 500ms; for 'yes-no' | 'likert-5' | 'score-0-3' | 'billing-ratio', fire on change/blur immediately. Every saveAnswer mutation passes `expectedUpdatedAt` from the last successful response's `updatedAt` (Pitfall 10 guard).
   - Track `lastSavedAt` + saveStatus from the mutation and feed into `<ClassificationAutosaveIndicator />`.
   - On Submit: `await submit.mutateAsync({ assessmentId })` → redirect to `/[locale]/contractors/[id]/engagements/[engagementId]/classification/${result.id}` (Plan 05 route).
   - On submit error (server-side missing answer per Pitfall 5), show toast with server error and keep wizard mounted.

4. `classification-progress-bar.tsx`: Radix Progress wrapper. Props: { currentStep: number, totalSteps: number, currentStepCompletion: number }. Compute aria-valuenow = (currentStep - 1) + currentStepCompletion (fractional). Include `<span className="sr-only" aria-live="polite" aria-atomic="true">{t('progress.announce', { current, total })}</span>` that only updates when `currentStep` changes (ref guard + useEffect). 4px height, teal fill per UI-SPEC.

5. `classification-step-indicator.tsx`: horizontal `<ol role="list" aria-label={t('progress.ariaLabel')}>` per UI-SPEC §Interaction 2. Step dot styles: filled teal for visited/current, outlined for future. `aria-current="step"` on current `<li>`. Screen-reader text `<span className="sr-only">Step 2 of 5: Control</span>` always present. Hide labels < 640px except current.

6. `classification-autosave-indicator.tsx`: Small pill styled per UI-SPEC §Interaction 4. Four states: not-saved (muted), saving (Loader2 spin + 'Saving…'), saved ('Saved {relativeTime}' using Intl.RelativeTimeFormat with current locale, re-rendering every 30s via setInterval ref), error (destructive + 'Save failed — retrying' + sonner toast). Wrapping `<div>` has aria-live="polite" + aria-atomic="true".

7. `wizard-question.tsx`: Generic question card per UI-SPEC §Component Inventory. Reads `question: RuleSetQuestion` + `locale`. Renders: prompt from `question.prompt[locale]`, help text in a Collapsible from `question.helpText[locale]`, legal-reference Collapsible below (caseLawCitation for IR35 or drvReference for DRV), and dispatches to the correct answer component by `question.answerType`. Also renders an optional `<RationaleTextarea />` if a `rationale` is allowed for the question (document with a `question.allowsRationale?: boolean` addition if rule-set doesn't already have it; otherwise gate rationale on the IR35 dispositive areas only — CONFIRM before writing).

8. `legal-reference-collapsible.tsx`: shadcn Collapsible wrapping the citation with the `--info` tinted callout per UI-SPEC §Interaction 5. Accepts a `citation` prop (string) + `kind: 'case-law' | 'drv'`.

9. Answer components (`answers/*.tsx`):
   - `yes-no-answer.tsx`: 2-option RadioGroup (horizontal ≥640px, vertical below). Selected option: teal border + filled dot. Value type: 'yes' | 'no'.
   - `likert-answer.tsx`: 5-option RadioGroup with labels "Strongly disagree"…"Strongly agree" (translated). Value type: number 1-5.
   - `score-03-answer.tsx`: 4-option RadioGroup with labels per UI-SPEC §Interaction 3 (0='Nicht anwendbar / Not applicable', 1='Schwacher Hinweis / Weak indicator', 2='Mäßiger Hinweis / Moderate indicator', 3='Starker Hinweis / Strong indicator'). When 0 selected, emit the value `{ rawScore: 0, isNotApplicable: true }`; for 1/2/3 emit `{ rawScore: N, isNotApplicable: false }`. Import and render `CLASSIFICATION_SCHEIN_NOT_APPLICABLE` as the DE label for the 0 option (verbatim — the locked-phrase guard will catch any duplication).
   - `rationale-textarea.tsx`: shadcn Textarea with maxLength=1000, character counter (muted 12 px, destructive at > 950), `aria-describedby` wired to the counter id.

10. `schein/economic-dependency-input.tsx`: `<InputGroup>` with numeric `<Input type="number" min="0" max="100" step="1" inputMode="numeric" />` + `%` suffix per UI-SPEC §Interaction 3. Zod validation via z.number().int().min(0).max(100). On blur, if valid, fire saveAnswer with { value: parsed }. On invalid, show inline error with UI-SPEC `error.ratioOutOfRange` copy.

11. `ir35/ir35-wizard-steps.tsx` + `schein/schein-wizard-steps.tsx`: compose the per-step question lists by filtering the rule-set arrays (IR35_QUESTIONS or SCHEIN_QUESTIONS) by area/category. Render `<WizardQuestion>` per question. DRV step 4 (economic-dep) additionally renders `<EconomicDependencyInput>` for DRV-ECO-01 (override the default dispatcher for that questionId).

12. Tests per the behavior contract above. Use @testing-library/react + vitest + MSW for tRPC mocking. a11y.test.tsx uses axe-core.

13. Grep assertion — add a one-liner to the wizard shell as a leading comment:
    ```typescript
    // IMPORTANT: Do NOT import from @contractor-ops/classification/profiles/*/scoring.*
    // Scoring is server-only (Pitfall 2). Enforced by Plan 05 grep check.
    ```
  </action>
  <acceptance_criteria>
    - `grep -r "profiles/.*/scoring" apps/web/src/` returns NO matches (scoring never bundled to client — Pitfall 2).
    - `grep -l "trpc.classification.saveAnswer" apps/web/src/components/contractors/classification/wizard/classification-wizard-shell.tsx` returns the file.
    - `grep -l "trpc.classification.submit" apps/web/src/components/contractors/classification/wizard/classification-wizard-shell.tsx` returns the file.
    - `grep -l "CLASSIFICATION_SCHEIN_NOT_APPLICABLE" apps/web/src/components/contractors/classification/wizard/answers/score-03-answer.tsx` returns the file.
    - `grep -l "isNotApplicable" apps/web/src/components/contractors/classification/wizard/answers/score-03-answer.tsx` returns the file.
    - `grep -l "inputMode=\"numeric\"" apps/web/src/components/contractors/classification/wizard/schein/economic-dependency-input.tsx` returns the file.
    - `grep -l "aria-valuenow" apps/web/src/components/contractors/classification/wizard/classification-progress-bar.tsx` returns the file.
    - `grep -l "aria-current=\"step\"" apps/web/src/components/contractors/classification/wizard/classification-step-indicator.tsx` returns the file.
    - `grep -l "aria-live=\"polite\"" apps/web/src/components/contractors/classification/wizard/classification-autosave-indicator.tsx` returns the file.
    - `grep -l "\"Classification\":" apps/web/messages/de.json` returns the file (namespace present).
    - `grep -l "\"_NOTE\"" apps/web/messages/de.json` returns the file (Pitfall 9 reminder present).
    - `pnpm --filter @contractor-ops/web test -- classification/wizard` runs and passes all 8 WS + 6 A11Y tests.
    - `pnpm --filter @contractor-ops/validators test` (locked-phrases guard) STILL GREEN — confirms no legal phrase was accidentally duplicated into messages/*.json.
    - `pnpm build` in apps/web completes (or `pnpm --filter @contractor-ops/web typecheck`). The wizard route compiles.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @contractor-ops/web test -- classification/wizard && pnpm --filter @contractor-ops/validators test && pnpm --filter @contractor-ops/web typecheck</automated>
  </verify>
  <done>Wizard renders the correct rule-set for GB vs DE, enforces step guard, autosaves on change/blur with optimistic concurrency, shows autosave indicator with aria-live, handles rule-set drift as a blocking UI state, is WCAG 2.1 AA compliant, and never imports scoring.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @contractor-ops/web test -- classification/wizard` — all integration + a11y tests green
- `pnpm --filter @contractor-ops/web typecheck` — no `any`, strict types
- `pnpm --filter @contractor-ops/validators test` — locked-phrase guard still green (no accidental message-file duplication of CLASSIFICATION_* / DISCLAIMER_*)
- Manual grep audit: no scoring imports in any apps/web file (command above)
- Run `pnpm dev` locally (manual smoke not required for auto-verification, but documented for reviewer)
</verification>

<success_criteria>
- Wizard entry page creates/fetches draft, selects profile, handles drift, and renders the correct shell.
- All 13 wizard/answer files exist and implement UI-SPEC exactly.
- Score-0-3 answer distinguishes N/A from missing via isNotApplicable flag (Pitfall 5).
- EconomicDependencyInput validates 0-100 numeric and fires saveAnswer on blur (not keystroke).
- Autosave indicator uses Intl.RelativeTimeFormat with locale-aware output.
- No scoring code ships to client bundle.
- All four message files carry the Classification namespace with the Pitfall-9 `_NOTE` reminder.
</success_criteria>

<output>
After completion, create `.planning/phases/58-classification-engine-rule-sets/58-04-SUMMARY.md` summarizing: wizard components created, autosave debounce config, tests passing, any deviations from UI-SPEC, rule-set-drift handling path (incl. whether a new tRPC mutation was added), and a grep-audit confirmation that scoring isn't bundled to client.
</output>
