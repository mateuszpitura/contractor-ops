---
phase: 47-vat-engine-wht-calculator-country-fields
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/invoices/invoice-detail/invoice-metadata-form.tsx
  - apps/web/src/app/[locale]/(dashboard)/invoices/[id]/page.tsx
  - apps/web/src/components/payments/payment-run-side-panel.tsx
  - apps/web/src/app/[locale]/(dashboard)/page.tsx
  - apps/web/src/components/contractors/contractor-profile/tab-compliance.tsx
  - apps/web/src/components/invoices/vat-rate-selector.tsx
  - apps/web/src/components/invoices/reverse-charge-banner.tsx
  - apps/web/src/components/payments/wht-summary-card.tsx
  - apps/web/src/components/payments/wht-certificate-preview-dialog.tsx
  - apps/web/src/components/dashboard/tax-obligations-widget.tsx
  - apps/web/src/components/contractors/country-compliance-section.tsx
autonomous: true
gap_closure: true
requirements:
  - TAX-01
  - TAX-02
  - TAX-04
  - TAX-05
  - TAX-03
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04

must_haves:
  truths:
    - "VatRateSelector renders in the invoice metadata form replacing hardcoded dropdown"
    - "ReverseChargeBanner renders on invoice detail page when isReverseCharge is true"
    - "WhtSummaryCard renders in payment run side panel when WHT items exist"
    - "TaxObligationsWidget renders on the main dashboard"
    - "CountryComplianceSection renders in contractor profile compliance tab"
  artifacts:
    - path: "apps/web/src/components/invoices/invoice-detail/invoice-metadata-form.tsx"
      provides: "VatRateSelector integrated into invoice form"
      contains: "VatRateSelector"
    - path: "apps/web/src/app/[locale]/(dashboard)/invoices/[id]/page.tsx"
      provides: "ReverseChargeBanner in invoice detail"
      contains: "ReverseChargeBanner"
    - path: "apps/web/src/components/payments/payment-run-side-panel.tsx"
      provides: "WhtSummaryCard in payment run panel"
      contains: "WhtSummaryCard"
    - path: "apps/web/src/app/[locale]/(dashboard)/page.tsx"
      provides: "TaxObligationsWidget on dashboard"
      contains: "TaxObligationsWidget"
    - path: "apps/web/src/components/contractors/contractor-profile/tab-compliance.tsx"
      provides: "CountryComplianceSection in contractor compliance tab"
      contains: "CountryComplianceSection"
  key_links:
    - from: "apps/web/src/components/invoices/invoice-detail/invoice-metadata-form.tsx"
      to: "apps/web/src/components/invoices/vat-rate-selector.tsx"
      via: "import and render in form"
      pattern: "import.*VatRateSelector"
    - from: "apps/web/src/app/[locale]/(dashboard)/invoices/[id]/page.tsx"
      to: "apps/web/src/components/invoices/reverse-charge-banner.tsx"
      via: "import and conditional render"
      pattern: "import.*ReverseChargeBanner"
    - from: "apps/web/src/components/payments/payment-run-side-panel.tsx"
      to: "apps/web/src/components/payments/wht-summary-card.tsx"
      via: "import and render above items"
      pattern: "import.*WhtSummaryCard"
    - from: "apps/web/src/app/[locale]/(dashboard)/page.tsx"
      to: "apps/web/src/components/dashboard/tax-obligations-widget.tsx"
      via: "import and render in right column"
      pattern: "import.*TaxObligationsWidget"
    - from: "apps/web/src/components/contractors/contractor-profile/tab-compliance.tsx"
      to: "apps/web/src/components/contractors/country-compliance-section.tsx"
      via: "import and render at top of tab"
      pattern: "import.*CountryComplianceSection"
---

<objective>
Wire all orphaned Phase 47 UI components into their parent pages and components.

Purpose: Close Gap 2 from VERIFICATION.md — five components were built but never imported anywhere. This plan integrates each component into its correct parent. Also fixes broken tRPC import paths (`@/lib/trpc` and `@/trpc/react` do not exist; must use `@/trpc/init`) and a broken `@/lib/format` import in WhtSummaryCard/WhtCertificatePreviewDialog.
Output: All tax UI components visible to users in their correct locations.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-vat-engine-wht-calculator-country-fields/47-VERIFICATION.md
@.planning/phases/47-vat-engine-wht-calculator-country-fields/47-UI-SPEC.md
</context>

<interfaces>
<!-- Component signatures for integration -->

VatRateSelector (apps/web/src/components/invoices/vat-rate-selector.tsx):
```typescript
interface VatRateSelectorProps {
  value?: string;
  onChange: (code: string) => void;
  disabled?: boolean;
}
// BROKEN: uses `import { api } from "@/lib/trpc"` — must fix to `import { trpc } from "@/trpc/init"`
```

ReverseChargeBanner (apps/web/src/components/invoices/reverse-charge-banner.tsx):
```typescript
interface ReverseChargeBannerProps {
  invoiceId: string;
  isReverseCharge: boolean;
  onToggle?: (newValue: boolean) => void;
}
// BROKEN: uses `import { api } from "@/lib/trpc"` — must fix to `import { trpc } from "@/trpc/init"`
```

WhtSummaryCard (apps/web/src/components/payments/wht-summary-card.tsx):
```typescript
interface WhtSummaryCardProps {
  paymentRunId: string;
  items: Array<{
    id: string;
    amountMinor: number;
    grossAmountMinor?: number | null;
    whtAmountMinor?: number | null;
    whtRate?: number | null;
    whtTreatyApplied?: boolean | null;
    currency: string;
  }>;
}
// BROKEN: uses `import { api } from "@/lib/trpc"` and `import { formatMinorAmount } from "@/lib/format"`
// Must fix to: `import { trpc } from "@/trpc/init"` and `import { formatMinorUnits } from "@/lib/format-currency"`
```

WhtCertificatePreviewDialog (apps/web/src/components/payments/wht-certificate-preview-dialog.tsx):
```typescript
interface WhtCertificatePreviewProps {
  open: boolean;
  onClose: () => void;
  certificate: { ... } | null;
  onDownload?: () => void;
}
// BROKEN: uses `import { formatMinorAmount } from "@/lib/format"`
// Must fix to: `import { formatMinorUnits } from "@/lib/format-currency"` and rename all formatMinorAmount calls to formatMinorUnits
```

TaxObligationsWidget (apps/web/src/components/dashboard/tax-obligations-widget.tsx):
```typescript
export function TaxObligationsWidget(): JSX.Element | null
// BROKEN: uses `import { api } from "@/trpc/react"` — must fix to `import { trpc } from "@/trpc/init"`
```

CountryComplianceSection (apps/web/src/components/contractors/country-compliance-section.tsx):
```typescript
interface CountryComplianceSectionProps {
  contractorId: string;
}
// BROKEN: uses `import { api } from "@/trpc/react"` — must fix to `import { trpc } from "@/trpc/init"`
```

Integration targets:
- invoice-metadata-form.tsx: Replace hardcoded getVatRateOptions/VAT_RATE_OPTIONS with VatRateSelector
- invoices/[id]/page.tsx: Add ReverseChargeBanner after match card, before submit button
- payment-run-side-panel.tsx: Add WhtSummaryCard after metadata grid, before action buttons
- page.tsx (dashboard): Add TaxObligationsWidget in right column after EInvoiceComplianceWidget
- tab-compliance.tsx: Add CountryComplianceSection at top

Existing format utility:
```typescript
// apps/web/src/lib/format-currency.ts
export function formatMinorUnits(minor: number, currency: string): string;
export function formatAmount(minor: number, currency: string): string;
```

Existing tRPC pattern (182 files use this):
```typescript
import { trpc } from "@/trpc/init";
// Usage: trpc.tax.getRates.useQuery(), trpc.invoice.toggleReverseCharge.useMutation(), etc.
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix broken imports in all 6 orphaned components</name>
  <files>
    apps/web/src/components/invoices/vat-rate-selector.tsx
    apps/web/src/components/invoices/reverse-charge-banner.tsx
    apps/web/src/components/payments/wht-summary-card.tsx
    apps/web/src/components/payments/wht-certificate-preview-dialog.tsx
    apps/web/src/components/dashboard/tax-obligations-widget.tsx
    apps/web/src/components/contractors/country-compliance-section.tsx
  </files>
  <read_first>
    - apps/web/src/components/invoices/vat-rate-selector.tsx
    - apps/web/src/components/invoices/reverse-charge-banner.tsx
    - apps/web/src/components/payments/wht-summary-card.tsx
    - apps/web/src/components/payments/wht-certificate-preview-dialog.tsx
    - apps/web/src/components/dashboard/tax-obligations-widget.tsx
    - apps/web/src/components/contractors/country-compliance-section.tsx
    - apps/web/src/lib/format-currency.ts
  </read_first>
  <action>
Fix tRPC imports in all 6 components. The paths `@/lib/trpc` and `@/trpc/react` do not exist. The codebase standard (182 files) is:

```typescript
import { trpc } from "@/trpc/init";
```

And usage pattern is `trpc.routerName.procedure.useQuery()` / `trpc.routerName.procedure.useMutation()`.

**vat-rate-selector.tsx:**
- Replace `import { api } from "@/lib/trpc";` with `import { trpc } from "@/trpc/init";`
- Replace all `api.` with `trpc.` (e.g., `api.tax.getRates.useQuery()` becomes `trpc.tax.getRates.useQuery()`)

**reverse-charge-banner.tsx:**
- Replace `import { api } from "@/lib/trpc";` with `import { trpc } from "@/trpc/init";`
- Replace all `api.` with `trpc.` (e.g., `api.invoice.toggleReverseCharge.useMutation(...)` becomes `trpc.invoice.toggleReverseCharge.useMutation(...)`)

**wht-summary-card.tsx:**
- Replace `import { api } from "@/lib/trpc";` with `import { trpc } from "@/trpc/init";`
- Replace all `api.` with `trpc.`
- Replace `import { formatMinorAmount } from "@/lib/format";` with `import { formatMinorUnits } from "@/lib/format-currency";`
- Replace all `formatMinorAmount(` calls with `formatMinorUnits(` — note that `formatMinorUnits` takes two args: `(minor: number, currency: string)`. Check each call site and ensure the currency argument is passed. If the component has inline `formatMinorAmount(minor)` calls, change to `formatMinorUnits(minor, currency)` using the item's `currency` field.

**wht-certificate-preview-dialog.tsx:**
- Replace `import { formatMinorAmount } from "@/lib/format";` with `import { formatMinorUnits } from "@/lib/format-currency";`
- Replace all `formatMinorAmount(` calls with `formatMinorUnits(`. The dialog has a `certificate` prop with a `currency` field — use `formatMinorUnits(certificate.grossAmountMinor, certificate.currency)` etc.

**tax-obligations-widget.tsx:**
- Replace `import { api } from "@/trpc/react";` with `import { trpc } from "@/trpc/init";`
- Replace all `api.` with `trpc.`

**country-compliance-section.tsx:**
- Replace `import { api } from "@/trpc/react";` with `import { trpc } from "@/trpc/init";`
- Replace all `api.` with `trpc.`
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && grep -r "@/lib/trpc\|@/trpc/react\|formatMinorAmount" apps/web/src/components/invoices/vat-rate-selector.tsx apps/web/src/components/invoices/reverse-charge-banner.tsx apps/web/src/components/payments/wht-summary-card.tsx apps/web/src/components/payments/wht-certificate-preview-dialog.tsx apps/web/src/components/dashboard/tax-obligations-widget.tsx apps/web/src/components/contractors/country-compliance-section.tsx 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `@/lib/trpc` in any of the 6 component files
    - Zero occurrences of `@/trpc/react` in any of the 6 component files
    - Zero occurrences of `formatMinorAmount` in wht-summary-card.tsx or wht-certificate-preview-dialog.tsx
    - All 6 files contain `import { trpc } from "@/trpc/init"` (except wht-certificate-preview-dialog.tsx which has no tRPC calls, only format fix)
    - wht-summary-card.tsx contains `import { formatMinorUnits } from "@/lib/format-currency"`
    - wht-certificate-preview-dialog.tsx contains `import { formatMinorUnits } from "@/lib/format-currency"`
    - `grep -c "from \"@/trpc/init\"" apps/web/src/components/invoices/vat-rate-selector.tsx` returns 1
    - `grep -c "from \"@/trpc/init\"" apps/web/src/components/dashboard/tax-obligations-widget.tsx` returns 1
  </acceptance_criteria>
  <done>All 6 orphaned components use the correct tRPC import path (@/trpc/init) and correct format utility (@/lib/format-currency). Components can now be imported without build errors.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate VatRateSelector into invoice metadata form</name>
  <files>apps/web/src/components/invoices/invoice-detail/invoice-metadata-form.tsx</files>
  <read_first>
    - apps/web/src/components/invoices/invoice-detail/invoice-metadata-form.tsx
    - apps/web/src/components/invoices/vat-rate-selector.tsx
  </read_first>
  <action>
In `apps/web/src/components/invoices/invoice-detail/invoice-metadata-form.tsx`:

1. Add import at top:
```typescript
import { VatRateSelector } from "@/components/invoices/vat-rate-selector";
```

2. Remove the `getVatRateOptions` function (lines ~82-91) — it returns hardcoded PL-only rates and is no longer needed.

3. Remove the `VAT_RATE_OPTIONS` const that calls `getVatRateOptions(...)` (around line 154).

4. Find the VAT rate Select section in the JSX (search for `t("detail.vatRate")` around line 424). Replace the entire static Select block:

```tsx
{/* OLD — hardcoded Select with VAT_RATE_OPTIONS */}
<div>
  <Label>{t("detail.vatRate")}</Label>
  <Select
    value={vatRateValue ?? undefined}
    onValueChange={(val) => setValue("vatRate", val ?? undefined)}
    disabled={!isEditable}
  >
    ...
  </Select>
</div>
```

With the VatRateSelector component:

```tsx
<div>
  <Label>{t("detail.vatRate")}</Label>
  <VatRateSelector
    value={vatRateValue ?? undefined}
    onChange={(code) => setValue("vatRate", code)}
    disabled={!isEditable}
  />
</div>
```

This replaces the hardcoded Polish-only rate options with the dynamic country-specific rate selector.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && grep -c "VatRateSelector" apps/web/src/components/invoices/invoice-detail/invoice-metadata-form.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `invoice-metadata-form.tsx` contains `import { VatRateSelector } from "@/components/invoices/vat-rate-selector"`
    - `invoice-metadata-form.tsx` contains `<VatRateSelector` JSX usage
    - `invoice-metadata-form.tsx` does NOT contain the `getVatRateOptions` function
    - `invoice-metadata-form.tsx` does NOT contain `VAT_RATE_OPTIONS` const
    - `grep -c "VatRateSelector" apps/web/src/components/invoices/invoice-detail/invoice-metadata-form.tsx` returns 2 or more
  </acceptance_criteria>
  <done>VatRateSelector integrated into invoice metadata form, replacing hardcoded PL-only rate dropdown with dynamic country-specific rates from the TaxRate table.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate ReverseChargeBanner, WhtSummaryCard, TaxObligationsWidget, and CountryComplianceSection into pages</name>
  <files>
    apps/web/src/app/[locale]/(dashboard)/invoices/[id]/page.tsx
    apps/web/src/components/payments/payment-run-side-panel.tsx
    apps/web/src/app/[locale]/(dashboard)/page.tsx
    apps/web/src/components/contractors/contractor-profile/tab-compliance.tsx
  </files>
  <read_first>
    - apps/web/src/app/[locale]/(dashboard)/invoices/[id]/page.tsx
    - apps/web/src/components/payments/payment-run-side-panel.tsx
    - apps/web/src/app/[locale]/(dashboard)/page.tsx
    - apps/web/src/components/contractors/contractor-profile/tab-compliance.tsx
    - apps/web/src/components/invoices/reverse-charge-banner.tsx
    - apps/web/src/components/payments/wht-summary-card.tsx
    - apps/web/src/components/dashboard/tax-obligations-widget.tsx
    - apps/web/src/components/contractors/country-compliance-section.tsx
  </read_first>
  <action>
**A. Invoice detail page — ReverseChargeBanner**

In `apps/web/src/app/[locale]/(dashboard)/invoices/[id]/page.tsx`:

1. Add import:
```typescript
import { ReverseChargeBanner } from "@/components/invoices/reverse-charge-banner";
```

2. In the JSX, after the ReconciliationCard section (around line 309, after the `{reconciliation && ...}` block) and before the "Submit for approval" button, add:

```tsx
{/* Reverse charge banner (Phase 47) */}
{invoice.isReverseCharge && (
  <ReverseChargeBanner
    invoiceId={invoice.id}
    isReverseCharge={invoice.isReverseCharge}
    onToggle={() => {
      queryClient.invalidateQueries({
        queryKey: trpc.invoice.getById.queryKey({ id: invoice.id }),
      });
    }}
  />
)}
```

**B. Payment run side panel — WhtSummaryCard**

In `apps/web/src/components/payments/payment-run-side-panel.tsx`:

1. Add import:
```typescript
import { WhtSummaryCard } from "@/components/payments/wht-summary-card";
```

2. In the JSX, after the metadata grid `<Separator />` (around line 250) and before the action buttons section (around line 252 "Action buttons" comment), add:

```tsx
{/* WHT Summary (Phase 47) */}
<WhtSummaryCard paymentRunId={runId!} items={items} />
```

The `items` array is already typed and available in the component (line 194). WhtSummaryCard internally filters for items with `whtAmountMinor > 0` and returns null if none found.

**C. Dashboard — TaxObligationsWidget**

In `apps/web/src/app/[locale]/(dashboard)/page.tsx`:

1. Add import:
```typescript
import { TaxObligationsWidget } from "@/components/dashboard/tax-obligations-widget";
```

2. In the JSX, in the right column, after the `<EInvoiceComplianceWidget />` (around line 131-132), add:

```tsx
<AnimateIn delay={5}>
  <TaxObligationsWidget />
</AnimateIn>
```

**D. Contractor compliance tab — CountryComplianceSection**

In `apps/web/src/components/contractors/contractor-profile/tab-compliance.tsx`:

1. Add import:
```typescript
import { CountryComplianceSection } from "@/components/contractors/country-compliance-section";
```

2. At the very beginning of the `TabCompliance` component's return JSX, before the existing compliance items content, wrap everything in a fragment and prepend the CountryComplianceSection. The section self-hides when the org has no country fields:

```tsx
export function TabCompliance({ contractor }: TabComplianceProps) {
  const t = useTranslations("ContractorProfile.compliance");

  return (
    <div className="space-y-6">
      {/* Country-specific compliance fields (Phase 47) */}
      <CountryComplianceSection contractorId={contractor.id} />

      {/* Existing compliance items */}
      {contractor.complianceItems.length === 0 ? (
        // ... existing empty state
      ) : (
        // ... existing compliance items list
      )}
    </div>
  );
}
```

Note: The component returns null when org has no supported country fields, so it's safe to always render.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && grep -l "ReverseChargeBanner" apps/web/src/app/*/\(dashboard\)/invoices/*/page.tsx && grep -l "WhtSummaryCard" apps/web/src/components/payments/payment-run-side-panel.tsx && grep -l "TaxObligationsWidget" apps/web/src/app/*/\(dashboard\)/page.tsx && grep -l "CountryComplianceSection" apps/web/src/components/contractors/contractor-profile/tab-compliance.tsx && echo "ALL_WIRED"</automated>
  </verify>
  <acceptance_criteria>
    - `invoices/[id]/page.tsx` contains `import { ReverseChargeBanner }` AND `<ReverseChargeBanner`
    - `payment-run-side-panel.tsx` contains `import { WhtSummaryCard }` AND `<WhtSummaryCard`
    - `(dashboard)/page.tsx` contains `import { TaxObligationsWidget }` AND `<TaxObligationsWidget`
    - `tab-compliance.tsx` contains `import { CountryComplianceSection }` AND `<CountryComplianceSection`
    - `grep "ReverseChargeBanner" apps/web/src/app/\[locale\]/\(dashboard\)/invoices/\[id\]/page.tsx | wc -l` returns 2 or more
    - `grep "TaxObligationsWidget" apps/web/src/app/\[locale\]/\(dashboard\)/page.tsx | wc -l` returns 2 or more
  </acceptance_criteria>
  <done>All 4 remaining UI components are wired into their parent pages: ReverseChargeBanner in invoice detail, WhtSummaryCard in payment run panel, TaxObligationsWidget on dashboard, CountryComplianceSection in contractor compliance tab.</done>
</task>

</tasks>

<verification>
- `grep -r "@/lib/trpc" apps/web/src/components/invoices/ apps/web/src/components/payments/ apps/web/src/components/dashboard/tax-obligations-widget.tsx apps/web/src/components/contractors/country-compliance-section.tsx` returns no results
- `grep -rn "import.*VatRateSelector" apps/web/src/components/invoices/invoice-detail/invoice-metadata-form.tsx` returns a match
- `grep -rn "import.*ReverseChargeBanner" apps/web/src/app/` returns a match
- `grep -rn "import.*WhtSummaryCard" apps/web/src/components/payments/payment-run-side-panel.tsx` returns a match
- `grep -rn "import.*TaxObligationsWidget" apps/web/src/app/` returns a match
- `grep -rn "import.*CountryComplianceSection" apps/web/src/components/contractors/contractor-profile/tab-compliance.tsx` returns a match
</verification>

<success_criteria>
- All 6 orphaned components have correct import paths and are integrated into their parent pages
- VatRateSelector replaces hardcoded VAT dropdown in invoice form
- ReverseChargeBanner shows on invoice detail when reverse charge is flagged
- WhtSummaryCard shows in payment run panel when WHT items exist
- TaxObligationsWidget shows on the main dashboard
- CountryComplianceSection shows in contractor profile compliance tab
</success_criteria>

<output>
After completion, create `.planning/phases/47-vat-engine-wht-calculator-country-fields/47-07-SUMMARY.md`
</output>
