---
phase: 54-regional-routing-adoption-gov-api-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/api/src/routers/zatca.ts
  - packages/api/src/routers/peppol.ts
  - packages/api/src/routers/einvoice.ts
  - packages/api/src/routers/ksef.ts
  - packages/api/src/routers/tax.ts
  - packages/api/src/routers/consent.ts
  - packages/api/src/routers/exchange-rate.ts
  - packages/api/src/routers/billing.ts
  - packages/api/src/routers/equipment.ts
  - packages/api/src/routers/equipment-couriers.ts
  - packages/api/src/routers/equipment-returns.ts
  - packages/api/src/routers/equipment-shipments.ts
  - packages/api/src/routers/equipment-shared.ts
  - packages/api/src/routers/time.ts
  - packages/api/src/routers/jira.ts
  - packages/api/src/routers/linear.ts
  - packages/api/src/routers/teams.ts
  - packages/api/src/routers/esign.ts
  - packages/api/src/routers/ocr.ts
  - packages/api/src/routers/integration.ts
  - packages/api/src/routers/onboarding-import.ts
  - packages/api/src/routers/google-workspace.ts
  - packages/api/src/routers/calendar.ts
  - packages/api/src/routers/docs.ts
autonomous: true
requirements: [INFRA-01, INFRA-02]
gap_closure: true

must_haves:
  truths:
    - "All remaining 21 routers use ctx.db instead of importing prisma directly"
    - "No router file in packages/api/src/routers/ imports { prisma } from @contractor-ops/db"
    - "All 43 routers combined use ctx.db for database access"
  artifacts:
    - path: "packages/api/src/routers/zatca.ts"
      provides: "ZATCA router using ctx.db"
      contains: "ctx.db"
    - path: "packages/api/src/routers/peppol.ts"
      provides: "Peppol router using ctx.db"
      contains: "ctx.db"
    - path: "packages/api/src/routers/consent.ts"
      provides: "Consent router using ctx.db"
      contains: "ctx.db"
  key_links:
    - from: "packages/api/src/routers/*.ts"
      to: "packages/api/src/middleware/tenant.ts"
      via: "tenantProcedure provides ctx.db"
      pattern: "ctx\\.db\\."
---

<objective>
Migrate the remaining 21 integration/feature routers from direct `prisma` import to `ctx.db` (regional Prisma client), completing the full codebase migration to regional database routing.

Purpose: INFRA-01/INFRA-02 gap — completes the router migration started in plan 01. After this plan, zero routers will import `prisma` directly.
Output: All 43 routers across the API package use `ctx.db` for region-aware database access.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-regional-routing-adoption-gov-api-wiring/54-CONTEXT.md
@.planning/phases/54-regional-routing-adoption-gov-api-wiring/54-RESEARCH.md

<interfaces>
From packages/api/src/middleware/tenant.ts:
```typescript
// ctx.db is a region-aware, tenant-scoped PrismaClient
export const tenantProcedure = authedProcedure.use(tenantMiddleware);
// ctx.db available in every handler using tenantProcedure
```
</interfaces>
</context>

## Tasks

### Task 1: Migrate v4.0 routers to ctx.db (zatca, peppol, einvoice, ksef, tax, consent, exchange-rate)

<read_first>
- packages/api/src/routers/zatca.ts
- packages/api/src/routers/peppol.ts
- packages/api/src/routers/einvoice.ts
- packages/api/src/routers/ksef.ts
- packages/api/src/routers/tax.ts
- packages/api/src/routers/consent.ts
- packages/api/src/routers/exchange-rate.ts
</read_first>

<action>
For each of the 7 routers:

1. **Remove** `import { prisma } from "@contractor-ops/db"` (keep `import type { Prisma }` if present)
2. **Replace** every `prisma.` call in handler bodies with `ctx.db.`

**Specific notes:**

**zatca.ts** (imports prisma but handlers mostly delegate to services):
- Remove prisma import. Replace any direct `prisma.` usage with `ctx.db.`
- Service calls like `saveTaxDetails(ctx.organizationId, ...)` do NOT need to change here — services will be wired in plan 03.

**peppol.ts** (~20 prisma occurrences):
- Replace all `prisma.peppolParticipant.`, `prisma.peppolTransmission.`, `prisma.integrationConnection.`, `prisma.invoice.` with `ctx.db.` equivalents.

**einvoice.ts** (small router, ~5 prisma occurrences):
- Replace `prisma.integrationConnection.`, `prisma.integrationSyncLog.` with `ctx.db.` equivalents.

**consent.ts** (~10 prisma occurrences):
- Replace all `prisma.` with `ctx.db.` in handlers.
- Service calls like `grantConsent(...)`, `getCurrentConsent(...)` delegate to services — leave those unchanged here.

**tax.ts** (~5 prisma occurrences):
- Replace all `prisma.taxRate.`, `prisma.withholdingTaxRate.` with `ctx.db.` equivalents.

**exchange-rate.ts** (~3 prisma occurrences):
- Replace all `prisma.exchangeRate.` with `ctx.db.exchangeRate.`
</action>

<acceptance_criteria>
- `grep "import { prisma }" packages/api/src/routers/zatca.ts packages/api/src/routers/peppol.ts packages/api/src/routers/einvoice.ts packages/api/src/routers/ksef.ts packages/api/src/routers/tax.ts packages/api/src/routers/consent.ts packages/api/src/routers/exchange-rate.ts` returns empty
- `grep "ctx\.db\." packages/api/src/routers/peppol.ts` returns at least 15 matches
- `grep "ctx\.db\." packages/api/src/routers/einvoice.ts` returns at least 3 matches
</acceptance_criteria>

### Task 2: Migrate integration routers to ctx.db (billing, jira, linear, teams, esign, ocr, integration, onboarding-import, google-workspace, calendar, docs)

<read_first>
- packages/api/src/routers/billing.ts
- packages/api/src/routers/jira.ts
- packages/api/src/routers/linear.ts
- packages/api/src/routers/teams.ts
- packages/api/src/routers/esign.ts
- packages/api/src/routers/ocr.ts
- packages/api/src/routers/integration.ts
- packages/api/src/routers/onboarding-import.ts
- packages/api/src/routers/google-workspace.ts
- packages/api/src/routers/calendar.ts
- packages/api/src/routers/docs.ts
</read_first>

<action>
For each of the 11 routers:

1. **Remove** `import { prisma } from "@contractor-ops/db"` (keep type imports)
2. **Replace** every `prisma.` call in handler bodies with `ctx.db.`

These are all straightforward mechanical migrations. Each router already uses `tenantProcedure` so `ctx.db` is available.
</action>

<acceptance_criteria>
- `grep "import { prisma }" packages/api/src/routers/billing.ts packages/api/src/routers/jira.ts packages/api/src/routers/linear.ts packages/api/src/routers/teams.ts packages/api/src/routers/esign.ts packages/api/src/routers/ocr.ts packages/api/src/routers/integration.ts packages/api/src/routers/onboarding-import.ts packages/api/src/routers/google-workspace.ts packages/api/src/routers/calendar.ts packages/api/src/routers/docs.ts` returns empty
- `grep "ctx\.db\." packages/api/src/routers/jira.ts` returns at least 10 matches
- `grep "ctx\.db\." packages/api/src/routers/integration.ts` returns at least 10 matches
</acceptance_criteria>

### Task 3: Migrate equipment and time tracking routers to ctx.db

<read_first>
- packages/api/src/routers/equipment.ts
- packages/api/src/routers/equipment-couriers.ts
- packages/api/src/routers/equipment-returns.ts
- packages/api/src/routers/equipment-shipments.ts
- packages/api/src/routers/equipment-shared.ts
- packages/api/src/routers/time.ts
</read_first>

<action>
For each of the 6 routers:

1. **Remove** `import { prisma } from "@contractor-ops/db"` (keep type imports)
2. **Replace** every `prisma.` call in handler bodies with `ctx.db.`

**Note for equipment-shared.ts:** This may export helper functions. If helpers use `prisma` directly (outside handlers), refactor to accept a db parameter and inject `ctx.db` from callers.
</action>

<acceptance_criteria>
- `grep "import { prisma }" packages/api/src/routers/equipment.ts packages/api/src/routers/equipment-couriers.ts packages/api/src/routers/equipment-returns.ts packages/api/src/routers/equipment-shipments.ts packages/api/src/routers/equipment-shared.ts packages/api/src/routers/time.ts` returns empty
- `grep "ctx\.db\." packages/api/src/routers/equipment-couriers.ts` returns at least 15 matches
- `grep "ctx\.db\." packages/api/src/routers/time.ts` returns at least 10 matches
</acceptance_criteria>

## Verification

```bash
# CRITICAL: Verify ZERO routers import prisma directly
grep -rn "import { prisma }" packages/api/src/routers/*.ts
# Expected: no output — all 43 routers migrated

# Count ctx.db usage across all routers (should be high)
grep -rc "ctx\.db\." packages/api/src/routers/*.ts | awk -F: '{sum+=$2} END {print "Total ctx.db usages:", sum}'
# Expected: 400+ usages

# TypeScript build check
cd packages/api && npx tsc --noEmit 2>&1 | head -20
```
