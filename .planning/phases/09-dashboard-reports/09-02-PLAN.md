---
phase: 09-dashboard-reports
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/web/src/app/[locale]/(dashboard)/page.tsx
  - apps/web/src/components/dashboard/kpi-cards.tsx
  - apps/web/src/components/dashboard/spend-chart.tsx
  - apps/web/src/components/dashboard/deadlines-widget.tsx
  - apps/web/src/components/dashboard/approval-queue-widget.tsx
  - apps/web/src/components/dashboard/activity-feed.tsx
  - package.json
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05

must_haves:
  truths:
    - "User sees 5 clickable KPI cards with values and trend indicators"
    - "User sees month-over-month spend area chart with 6m/12m/YTD toggle"
    - "User sees upcoming deadlines widget with sorted urgent items"
    - "User sees approval queue widget with top 5 pending approvals"
    - "User sees activity feed grouped by today/yesterday/earlier"
    - "Dashboard shows empty state when no data exists"
  artifacts:
    - path: "apps/web/src/app/[locale]/(dashboard)/page.tsx"
      provides: "Full dashboard page replacing placeholder"
      min_lines: 30
    - path: "apps/web/src/components/dashboard/kpi-cards.tsx"
      provides: "5 clickable KPI cards with trend indicators"
      exports: ["KpiCards"]
    - path: "apps/web/src/components/dashboard/spend-chart.tsx"
      provides: "Recharts AreaChart with month toggle"
      exports: ["SpendChart"]
    - path: "apps/web/src/components/dashboard/deadlines-widget.tsx"
      provides: "Grouped deadline items with type badges"
      exports: ["DeadlinesWidget"]
    - path: "apps/web/src/components/dashboard/approval-queue-widget.tsx"
      provides: "Top 5 pending approvals with SLA badges"
      exports: ["ApprovalQueueWidget"]
    - path: "apps/web/src/components/dashboard/activity-feed.tsx"
      provides: "Last 20 events grouped by date"
      exports: ["ActivityFeed"]
  key_links:
    - from: "apps/web/src/components/dashboard/kpi-cards.tsx"
      to: "dashboard.kpis"
      via: "trpc.dashboard.kpis.queryOptions()"
      pattern: "trpc\\.dashboard\\.kpis"
    - from: "apps/web/src/components/dashboard/spend-chart.tsx"
      to: "dashboard.spendTrend"
      via: "trpc.dashboard.spendTrend.queryOptions()"
      pattern: "trpc\\.dashboard\\.spendTrend"
---

<objective>
Build the full dashboard page with 5 KPI cards, spend area chart, deadlines widget, approval queue widget, and activity feed. Install Recharts. Replace the existing placeholder dashboard.

Purpose: Give users an at-a-glance overview of their contractor operations with actionable navigation shortcuts.
Output: Complete dashboard page with 5 components consuming tRPC queries from Plan 01.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-dashboard-reports/09-CONTEXT.md
@.planning/phases/09-dashboard-reports/09-RESEARCH.md
@.planning/phases/09-dashboard-reports/09-UI-SPEC.md
@.planning/phases/09-dashboard-reports/09-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 dashboard router -->
```typescript
// packages/api/src/routers/dashboard.ts exports:
// dashboard.kpis -> { activeContractors: { value, prevValue }, pendingApprovals: { value, prevValue }, readyToPayTotal: { valueGrosze, prevValueGrosze }, expiringContracts: { value }, openTasks: { value } }
// dashboard.spendTrend -> Array<{ month: string, currency: string, totalGrosze: number }>
// dashboard.deadlines -> Array<{ type: "CONTRACT_EXPIRING"|"TASK_OVERDUE"|"INVOICE_DUE", entityId, entityName, dueDate, daysRemaining|daysOverdue }>
// dashboard.activity -> { items: Array<{ id, actorName, actorType, action, resourceType, resourceId, resourceName, createdAt }> }
```

<!-- From existing approval router -->
```typescript
// approval.listPending -> existing procedure for approval queue
```

<!-- From navigation.ts -->
```typescript
export const navigationItems: NavItem[] = [
  { key: "dashboard", label: "Dashboard", href: "/dashboard", icon: LayoutDashboard, permission: null },
  // ...
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts and create KPI cards + spend chart components</name>
  <files>
    apps/web/src/components/dashboard/kpi-cards.tsx,
    apps/web/src/components/dashboard/spend-chart.tsx
  </files>
  <read_first>
    apps/web/src/app/[locale]/(dashboard)/page.tsx,
    apps/web/src/components/invoices/status-chip-bar.tsx,
    apps/web/src/components/ui/card.tsx,
    apps/web/src/components/ui/toggle-group.tsx,
    apps/web/src/components/ui/skeleton.tsx,
    apps/web/src/trpc/init.ts,
    packages/api/src/routers/dashboard.ts,
    apps/web/src/i18n/navigation.ts,
    apps/web/src/hooks/use-permissions.ts
  </read_first>
  <action>
First install recharts: `pnpm add recharts --filter @contractor-ops/web` (per D-05).

Create `apps/web/src/components/dashboard/kpi-cards.tsx` ("use client"):
- Export `KpiCards` component.
- Use `useQuery(trpc.dashboard.kpis.queryOptions())` to fetch KPI data.
- Use `useTranslations("Dashboard")` for all text.
- Render 5 cards in a CSS grid: `grid-cols-5` at desktop (xl+), `grid-cols-3` at lg, `grid-cols-2` at md, `grid-cols-1` at sm. Gap: 16px (md spacing).
- Each card is a shadcn `Card` with `cursor-pointer` and hover border transition to `hsl(var(--primary) / 0.5)`. Use `Link` from `@/i18n/navigation` wrapping each card.
- Card content per D-01: Label (12px, text-muted-foreground), Display value (28px semibold), trend row below.
- Trend calculation: `const change = prevValue > 0 ? ((value - prevValue) / prevValue * 100) : 0`. Display with TrendingUp (green) / TrendingDown (red) / Minus (muted) icons from lucide-react. Format: "+12% vs last month" in 12px semibold.
- For readyToPayTotal: format valueGrosze as currency using `Intl.NumberFormat('pl-PL', { style: 'currency', currency: 'PLN' })` dividing by 100. Other KPIs display raw numbers.
- Click navigation per D-03:
  - Active contractors -> `/contractors?status=active`
  - Pending approvals -> `/approvals?tab=my&status=pending`
  - Ready to pay -> `/payments?status=ready`
  - Expiring contracts -> `/contracts?status=expiring`
  - Open tasks -> `/workflows?tab=my-tasks`
- Loading state: 5 Skeleton cards matching dimensions (height ~100px).
- Empty/zero state: Show "0" with neutral trend indicator (Minus icon, text-muted-foreground).
- KPI card minimum width: 200px via `min-w-[200px]`.

Create `apps/web/src/components/dashboard/spend-chart.tsx` ("use client"):
- Export `SpendChart` component.
- Use nuqs `parseAsString` with `useQueryState("spend", parseAsString.withDefault("6"))` for 6m/12m/YTD toggle per D-07.
- Use `useQuery(trpc.dashboard.spendTrend.queryOptions({ months: spendRange as "6"|"12"|"ytd" }))` to fetch data.
- Wrap in shadcn Card. CardHeader: title "Monthly spend" (20px semibold) + ToggleGroup (3 items: "6 months", "12 months", "Year to date") aligned right in header.
- Transform data for Recharts: pivot array of { month, currency, totalGrosze } into array of { month: "MMM YYYY", PLN: number, EUR: number } using `useMemo`.
- Render Recharts `ResponsiveContainer` (width 100%, height 280px) containing `AreaChart`:
  - `CartesianGrid` with strokeDasharray="3 3", stroke at muted-foreground 15%.
  - `XAxis` dataKey="month", fontSize 12.
  - `YAxis` with tickFormatter dividing by 100 and formatting as currency, fontSize 12.
  - `Tooltip` with custom content showing month, total amount formatted.
  - Primary `Area`: type="monotone", dataKey="PLN", stackId="1", stroke=`hsl(var(--primary))`, fill=`hsl(var(--primary) / 0.2)`, strokeWidth=2.
  - If data has EUR entries: secondary `Area` with `hsl(220 70% 55%)` colors per UI-SPEC chart palette.
- Loading: Skeleton rectangle 280px height.
- Empty: Card with centered text "No spend data yet".
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "recharts" apps/web/package.json
    - grep -q "KpiCards" apps/web/src/components/dashboard/kpi-cards.tsx
    - grep -q "SpendChart" apps/web/src/components/dashboard/spend-chart.tsx
    - grep -q "trpc.dashboard.kpis" apps/web/src/components/dashboard/kpi-cards.tsx
    - grep -q "trpc.dashboard.spendTrend" apps/web/src/components/dashboard/spend-chart.tsx
    - grep -q "AreaChart" apps/web/src/components/dashboard/spend-chart.tsx
    - grep -q "TrendingUp" apps/web/src/components/dashboard/kpi-cards.tsx
    - grep -q "grid-cols-5" apps/web/src/components/dashboard/kpi-cards.tsx
    - grep -q "ResponsiveContainer" apps/web/src/components/dashboard/spend-chart.tsx
    - grep -q "useQueryState" apps/web/src/components/dashboard/spend-chart.tsx
  </acceptance_criteria>
  <done>Recharts installed. KPI cards display 5 metrics with trend indicators and click navigation. Spend chart shows area chart with currency stacking and 6m/12m/YTD toggle. Both have loading and empty states. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create widget components and wire dashboard page</name>
  <files>
    apps/web/src/components/dashboard/deadlines-widget.tsx,
    apps/web/src/components/dashboard/approval-queue-widget.tsx,
    apps/web/src/components/dashboard/activity-feed.tsx,
    apps/web/src/app/[locale]/(dashboard)/page.tsx
  </files>
  <read_first>
    apps/web/src/components/dashboard/kpi-cards.tsx,
    apps/web/src/components/dashboard/spend-chart.tsx,
    apps/web/src/components/ui/card.tsx,
    apps/web/src/components/ui/badge.tsx,
    apps/web/src/components/ui/scroll-area.tsx,
    apps/web/src/app/[locale]/(dashboard)/page.tsx,
    packages/api/src/routers/dashboard.ts,
    packages/api/src/routers/approval.ts,
    apps/web/src/i18n/navigation.ts,
    apps/web/src/hooks/use-permissions.ts
  </read_first>
  <action>
Create `apps/web/src/components/dashboard/deadlines-widget.tsx` ("use client"):
- Export `DeadlinesWidget` component.
- Use `useQuery(trpc.dashboard.deadlines.queryOptions())`.
- Use `useTranslations("Dashboard")`.
- Render shadcn Card. CardHeader: title "Upcoming deadlines" (20px semibold) + "See all deadlines" Link to `/reports?report=expiring-contracts` aligned right (text-sm, text-primary).
- CardContent: ScrollArea with max-h-[320px].
- Each item: horizontal row with type Badge (colored per UI-SPEC deadline type badges -- CONTRACT_EXPIRING: warning bg/text, TASK_OVERDUE: destructive bg/text, INVOICE_DUE: primary bg/text), entity name as Link (to appropriate entity page based on type), time text.
- Time text: for overdue items "Overdue by {days} days" in text-destructive. For upcoming: "in {days} days" in text-muted-foreground. Use date-fns differenceInDays.
- Items sorted by urgency: overdue first (most overdue at top), then soonest upcoming.
- Loading: 5 skeleton lines (h-10 each).
- Empty: "No upcoming deadlines" centered text.

Create `apps/web/src/components/dashboard/approval-queue-widget.tsx` ("use client"):
- Export `ApprovalQueueWidget` component.
- Use `useQuery(trpc.approval.listPending.queryOptions({ page: 1, pageSize: 5 }))` (reuse existing approval router, limit to 5 items per D-04 widget spec).
- Render shadcn Card. CardHeader: title "Approval queue" + "See all approvals" Link to `/approvals`.
- CardContent: ScrollArea with max-h-[280px].
- Each item: contractor name, formatted amount (`Intl.NumberFormat` dividing grosze by 100), SLA badge. SLA badge colors: green (within time), yellow (approaching), red (breached) -- follow existing SLA pattern from Phase 6 approval components.
- Click on item navigates to invoice detail: `/invoices/{invoiceId}`.
- Loading: 5 skeleton lines.
- Empty: "No pending approvals" centered text.

Create `apps/web/src/components/dashboard/activity-feed.tsx` ("use client"):
- Export `ActivityFeed` component.
- Use `useQuery(trpc.dashboard.activity.queryOptions())`.
- Use `useTranslations("Dashboard")`.
- Render shadcn Card. CardHeader: title "Recent activity" + "See all activity" Link to `/settings?tab=audit-log`.
- CardContent: ScrollArea with max-h-[400px].
- Group items by day: today / yesterday / earlier per D-04. Group headers: 12px text-muted-foreground uppercase.
- Each event: two-line display:
  - Line 1: actorName + action verb (action in font-semibold, 14px). Action verbs: map action string to readable label ("CREATE" -> "Created", "UPDATE" -> "Updated", etc.).
  - Line 2: resourceType Badge (small, muted variant) + resourceName as Link (navigate to entity using resourceType + resourceId mapping: CONTRACTOR -> /contractors/{id}, CONTRACT -> /contracts/{id}, INVOICE -> /invoices/{id}, etc.) + relative timestamp (12px, text-muted-foreground, use date-fns formatDistanceToNow).
- Loading: 8 skeleton lines grouped.
- Empty: "No recent activity" centered.

Replace `apps/web/src/app/[locale]/(dashboard)/page.tsx` entirely:
- "use client" at top.
- Import Suspense from React.
- Import all 5 dashboard components.
- Import useTranslations, usePermissions.
- Check if user has `report.read` permission to determine full vs simplified dashboard.
- Wrap in Suspense boundary (required for nuqs in SpendChart).
- Layout structure per D-01 and D-02:
  - Page has no visible h1 (self-evident per UI-SPEC). Set document title via metadata or useEffect.
  - KPI cards row at top.
  - Below with 32px gap (xl spacing): two-column grid `grid-cols-2` at lg+, `grid-cols-1` at mobile.
  - Left column (24px gap between widgets): SpendChart + DeadlinesWidget.
  - Right column (24px gap): ApprovalQueueWidget + ActivityFeed.
- Empty state: If all KPIs return zero values, show centered empty state per UI-SPEC: LayoutDashboard icon (48px, text-muted-foreground), heading "Your dashboard will come alive as you add data", body text, "Add contractor" primary button linking to `/contractors/new`.
- Each widget renders independently -- if one fails, show inline error card with AlertCircle + error message + "Try again" button per UI-SPEC. Other widgets continue loading.
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "DeadlinesWidget" apps/web/src/components/dashboard/deadlines-widget.tsx
    - grep -q "ApprovalQueueWidget" apps/web/src/components/dashboard/approval-queue-widget.tsx
    - grep -q "ActivityFeed" apps/web/src/components/dashboard/activity-feed.tsx
    - grep -q "KpiCards" apps/web/src/app/[locale]/(dashboard)/page.tsx
    - grep -q "SpendChart" apps/web/src/app/[locale]/(dashboard)/page.tsx
    - grep -q "DeadlinesWidget" apps/web/src/app/[locale]/(dashboard)/page.tsx
    - grep -q "ApprovalQueueWidget" apps/web/src/app/[locale]/(dashboard)/page.tsx
    - grep -q "ActivityFeed" apps/web/src/app/[locale]/(dashboard)/page.tsx
    - grep -q "grid-cols-2" apps/web/src/app/[locale]/(dashboard)/page.tsx
    - grep -q "ScrollArea" apps/web/src/components/dashboard/activity-feed.tsx
    - grep -q "trpc.dashboard.deadlines" apps/web/src/components/dashboard/deadlines-widget.tsx
    - grep -q "trpc.dashboard.activity" apps/web/src/components/dashboard/activity-feed.tsx
  </acceptance_criteria>
  <done>Dashboard page fully wired with all 5 widgets in two-column layout. Deadlines, approval queue, and activity feed components render with loading/empty states. Dashboard replaces placeholder. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @contractor-ops/web exec tsc --noEmit` passes
- Dashboard page imports and renders all 5 widget components
- All widgets use tRPC queries for data fetching
- KPI cards are clickable with correct navigation targets
- Spend chart uses Recharts with AreaChart
- Two-column layout with responsive breakpoints
</verification>

<success_criteria>
- Dashboard page shows 5 KPI cards, spend chart, deadlines, approval queue, activity feed
- KPI cards navigate to correct filtered pages on click (per D-03)
- Spend chart toggles between 6m/12m/YTD (per D-07)
- Activity feed groups by today/yesterday/earlier (per D-04)
- All components have loading skeletons and empty states
- Responsive: stacks to single column on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-reports/09-02-SUMMARY.md`
</output>
