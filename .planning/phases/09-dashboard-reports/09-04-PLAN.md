---
phase: 09-dashboard-reports
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - apps/web/src/components/settings/audit-log-tab.tsx
  - apps/web/src/components/settings/audit-log-table.tsx
  - apps/web/src/components/settings/audit-log-diff-viewer.tsx
  - apps/web/src/app/[locale]/(dashboard)/settings/page.tsx
autonomous: true
requirements:
  - ORG-10

must_haves:
  truths:
    - "Admin can view searchable, filterable audit log in Settings"
    - "Admin can expand rows to see before/after field diffs"
    - "Admin can filter by actor, action type, resource type, and date range"
    - "Admin can export audit log as CSV"
  artifacts:
    - path: "apps/web/src/components/settings/audit-log-tab.tsx"
      provides: "Audit log tab with search, filters, table, export"
      exports: ["AuditLogTab"]
    - path: "apps/web/src/components/settings/audit-log-table.tsx"
      provides: "TanStack Table with expandable rows"
      exports: ["AuditLogTable"]
    - path: "apps/web/src/components/settings/audit-log-diff-viewer.tsx"
      provides: "Before/after diff view for expanded rows"
      exports: ["AuditLogDiffViewer"]
    - path: "apps/web/src/app/[locale]/(dashboard)/settings/page.tsx"
      provides: "Settings page with Audit Log tab added"
      contains: "audit-log"
  key_links:
    - from: "apps/web/src/components/settings/audit-log-tab.tsx"
      to: "audit.list"
      via: "trpc.audit.list.queryOptions()"
      pattern: "trpc\\.audit\\.list"
    - from: "apps/web/src/app/[locale]/(dashboard)/settings/page.tsx"
      to: "apps/web/src/components/settings/audit-log-tab.tsx"
      via: "import and render in TabsContent"
      pattern: "AuditLogTab"
---

<objective>
Build the audit log viewer as a new tab in Settings with searchable, filterable TanStack Table with expandable rows for before/after diffs, and CSV export.

Purpose: Give admins a complete audit trail of all critical actions for compliance and accountability.
Output: Audit Log tab in Settings with search, filters, expandable diff rows, and CSV export.
</objective>

<execution_context>
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateusz.pitura/Repos/projects/contractor-ops/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-dashboard-reports/09-CONTEXT.md
@.planning/phases/09-dashboard-reports/09-RESEARCH.md
@.planning/phases/09-dashboard-reports/09-UI-SPEC.md
@.planning/phases/09-dashboard-reports/09-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 audit router -->
```typescript
// audit.list -> { items: Array<{ id, organizationId, actorType, actorId, actorName, action, resourceType, resourceId, resourceName, oldValuesJson, newValuesJson, metadataJson, ipAddress, userAgent, createdAt }>, totalCount, page, pageSize }
// audit.actors -> Array<{ actorId, actorName }>
// audit.export -> { data: base64, filename, mimeType }
```

<!-- From existing settings page -->
```typescript
// apps/web/src/app/[locale]/(dashboard)/settings/page.tsx
// Tabs: general, approvals, notifications, integrations, members
// Uses nuqs parseAsString for tab state with ?tab= URL param
// usePermissions().can() for conditional tab rendering
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create audit log diff viewer and table components</name>
  <files>
    apps/web/src/components/settings/audit-log-diff-viewer.tsx,
    apps/web/src/components/settings/audit-log-table.tsx
  </files>
  <read_first>
    apps/web/src/components/contractors/contractor-table/data-table.tsx,
    apps/web/src/components/ui/table.tsx,
    apps/web/src/components/ui/badge.tsx,
    apps/web/src/components/ui/collapsible.tsx,
    packages/api/src/routers/audit.ts,
    packages/db/prisma/schema/audit.prisma,
    apps/web/src/trpc/init.ts
  </read_first>
  <action>
Create `apps/web/src/components/settings/audit-log-diff-viewer.tsx` ("use client"):
- Export `AuditLogDiffViewer` component. Props: `{ oldValues: Record<string, unknown> | null, newValues: Record<string, unknown> | null }`.
- Per D-14: two-column grid showing before/after field changes.
- Compute changed fields: get all keys from both objects, filter where values differ (JSON.stringify comparison).
- If no oldValues and no newValues: show "No field changes recorded" in text-muted-foreground.
- Layout: `grid grid-cols-2 gap-4 p-4 border-l border-primary/20` (16px padding with 1px left border accent per UI-SPEC spacing).
- Left column: heading "Before" (12px semibold text-muted-foreground). For each changed key: `key:` in text-muted-foreground + old value in `text-destructive/50 line-through`.
- Right column: heading "After" (12px semibold text-muted-foreground). For each changed key: `key:` in text-muted-foreground + new value in `text-green-600 dark:text-green-400`.
- Use `useTranslations("Settings.auditLog")` for "Before", "After", "No field changes recorded".

Create `apps/web/src/components/settings/audit-log-table.tsx` ("use client"):
- Export `AuditLogTable` component. Props: `{ data, totalCount, page, pageSize, onPageChange, sortOrder, onSortOrderChange, expandedRows, onToggleRow }`.
- TanStack Table with columns per D-14/D-15 UI-SPEC:
  1. **Timestamp**: relative date using date-fns `formatDistanceToNow` with absolute date in Tooltip. Width: 140px.
  2. **Actor**: actorName + role Badge (12px, from metadataJson.role if available). Width: 180px.
  3. **Action**: action label in font-semibold 14px per UI-SPEC. Map action strings to human-readable labels: CREATE -> "Created", UPDATE -> "Updated", DELETE -> "Deleted", APPROVE -> "Approved", REJECT -> "Rejected", SUBMIT -> "Submitted", EXPORT -> "Exported", INVITE -> "Invited", DEACTIVATE -> "Deactivated", LOGIN -> "Logged in". Use `useTranslations("Settings.auditLog.actions")`.
  4. **Resource**: resourceType Badge (small, variant outline) + resourceName as Link. Map resourceType to URL: CONTRACTOR -> /contractors/{id}, CONTRACT -> /contracts/{id}, INVOICE -> /invoices/{id}, WORKFLOW_RUN -> /workflows/{id}, PAYMENT_RUN -> /payments, USER -> /settings/members, ORGANIZATION -> /settings. Use `useTranslations("Settings.auditLog.resources")`.
  5. **Details**: ChevronRight icon that rotates 90deg when expanded. Click toggles row expansion.
- Below each expanded row: render AuditLogDiffViewer with oldValuesJson and newValuesJson. Use Collapsible or conditional rendering with row colSpan.
- Pagination footer: Previous/Next buttons with "Page {page} of {totalPages}" indicator. 25 items per page per UI-SPEC.
- Loading: 10 skeleton rows per UI-SPEC.
- Sort: single-column sort on createdAt only (asc/desc toggle).
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "AuditLogDiffViewer" apps/web/src/components/settings/audit-log-diff-viewer.tsx
    - grep -q "AuditLogTable" apps/web/src/components/settings/audit-log-table.tsx
    - grep -q "grid-cols-2" apps/web/src/components/settings/audit-log-diff-viewer.tsx
    - grep -q "line-through" apps/web/src/components/settings/audit-log-diff-viewer.tsx
    - grep -q "useReactTable" apps/web/src/components/settings/audit-log-table.tsx
    - grep -q "ChevronRight" apps/web/src/components/settings/audit-log-table.tsx
    - grep -q "formatDistanceToNow" apps/web/src/components/settings/audit-log-table.tsx
    - grep -q "oldValues" apps/web/src/components/settings/audit-log-diff-viewer.tsx
    - grep -q "newValues" apps/web/src/components/settings/audit-log-diff-viewer.tsx
  </acceptance_criteria>
  <done>Audit log diff viewer renders before/after field changes with visual formatting. Audit log table has 5 columns with expandable rows, pagination, and sort. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Create audit log tab and wire into Settings page</name>
  <files>
    apps/web/src/components/settings/audit-log-tab.tsx,
    apps/web/src/app/[locale]/(dashboard)/settings/page.tsx
  </files>
  <read_first>
    apps/web/src/components/settings/audit-log-table.tsx,
    apps/web/src/components/settings/audit-log-diff-viewer.tsx,
    apps/web/src/app/[locale]/(dashboard)/settings/page.tsx,
    apps/web/src/components/settings/approval-chains-tab.tsx,
    apps/web/src/components/contractors/contractor-table/data-table-bulk-actions.tsx,
    packages/api/src/routers/audit.ts,
    apps/web/src/hooks/use-permissions.ts,
    apps/web/src/trpc/init.ts
  </read_first>
  <action>
Create `apps/web/src/components/settings/audit-log-tab.tsx` ("use client"):
- Export `AuditLogTab` component.
- URL state via nuqs (all synced to URL for deep-linking per D-15):
  - `search`: parseAsString, default ""
  - `actorId`: parseAsString, optional
  - `actionFilter`: parseAsString, optional (renamed to avoid collision with HTML action)
  - `resourceType`: parseAsString, optional
  - `dateFrom`/`dateTo`: parseAsString, optional
  - `auditPage`: parseAsString (converted to number), default "1"
  - `auditSort`: parseAsString, default "desc"
- Data fetching:
  - `useQuery(trpc.audit.list.queryOptions({ page, pageSize: 25, search: debouncedSearch, actorId, action: actionFilter, resourceType, dateFrom, dateTo, sortOrder: auditSort }))`.
  - `useQuery(trpc.audit.actors.queryOptions())` for actor dropdown options.
  - `useMutation(trpc.audit.export.mutationOptions())` for CSV export.
- Search bar: full-width Input with Search icon prefix, 300ms debounce (use local state + useEffect to debounce before setting nuqs). Placeholder: "Search audit log..." per UI-SPEC.
- Filter row below search: 3 Select dropdowns side-by-side + DatePicker range.
  - Actor select: options from audit.actors query. "All actors" as default/empty option.
  - Action type select: hardcoded options: CREATE, UPDATE, DELETE, APPROVE, REJECT, SUBMIT, EXPORT, INVITE, DEACTIVATE, LOGIN. "All actions" as default.
  - Resource type select: hardcoded options from EntityType enum values. "All resources" as default.
  - Date range: reuse Popover + Calendar range pattern from date-range-filter component. Or use two inline date inputs.
- AuditLogTable component below filters.
- Manage expandedRows state: `Record<string, boolean>`. Toggle on row click.
- CSV export button: "Export audit log" outline button, top-right aligned with search bar. On click: call audit.export mutation with current filter params. Use base64 Blob download pattern: atob, Uint8Array, Blob, URL.createObjectURL, trigger download, revokeObjectURL. Show sonner toast: "Audit log exported ({count} entries)".
- Loading: skeleton search bar, 3 skeleton selects, 10 skeleton table rows per UI-SPEC.
- Empty: "No audit log entries" heading + "Audit events will appear here as actions are performed in the system." body.

Modify `apps/web/src/app/[locale]/(dashboard)/settings/page.tsx`:
- Add import: `import { AuditLogTab } from "@/components/settings/audit-log-tab";`
- Add new TabsTrigger after integrations (or members): `<TabsTrigger value="audit-log">{t("tabs.auditLog")}</TabsTrigger>`. Conditionally render only for admin users: check `can("settings", ["read"])` or a specific admin check.
- Add new TabsContent: `<TabsContent value="audit-log" className="mt-6"><AuditLogTab /></TabsContent>`.
- Place the audit-log tab AFTER integrations but BEFORE members per D-13 (admin-focused, alongside org settings).
  </action>
  <verify>
    <automated>cd /Users/mateusz.pitura/Repos/projects/contractor-ops && pnpm --filter @contractor-ops/web exec tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "AuditLogTab" apps/web/src/components/settings/audit-log-tab.tsx
    - grep -q "AuditLogTab" apps/web/src/app/[locale]/(dashboard)/settings/page.tsx
    - grep -q "audit-log" apps/web/src/app/[locale]/(dashboard)/settings/page.tsx
    - grep -q "trpc.audit.list" apps/web/src/components/settings/audit-log-tab.tsx
    - grep -q "trpc.audit.actors" apps/web/src/components/settings/audit-log-tab.tsx
    - grep -q "trpc.audit.export" apps/web/src/components/settings/audit-log-tab.tsx
    - grep -q "URL.createObjectURL" apps/web/src/components/settings/audit-log-tab.tsx
    - grep -q "AuditLogTable" apps/web/src/components/settings/audit-log-tab.tsx
    - grep -q "useQueryState" apps/web/src/components/settings/audit-log-tab.tsx
    - grep -q "TabsTrigger" apps/web/src/app/[locale]/(dashboard)/settings/page.tsx
  </acceptance_criteria>
  <done>Audit log tab added to Settings page with search, structured filters, expandable table with diff viewer, and CSV export. All filter state synced to URL. Admin-only visibility. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @contractor-ops/web exec tsc --noEmit` passes
- Settings page shows Audit Log tab for admins
- Audit log table has expandable rows with diff view
- Search, actor/action/resource filters work via URL state
- CSV export triggers file download
</verification>

<success_criteria>
- Audit log lives in Settings > Audit Log tab (per D-13)
- Expandable rows show before/after diffs (per D-14)
- Full-text search + structured filters for actor, action, resource, date (per D-15)
- CSV export with compliance-focused columns (per D-16)
- 25 items per page with server-side pagination
- Admin-only access
</success_criteria>

<output>
After completion, create `.planning/phases/09-dashboard-reports/09-04-SUMMARY.md`
</output>
