/**
 * PORTED STUB — apps/web/src/components/workflows/template-builder/__tests__/task-card.test.tsx migrated as a deferred skip-stub.
 * Typecheck-failing (prop signature / module-shape divergence). Adapt when
 * next touched.
 */

import { describe } from 'vitest';

describe.skip('[DEFERRED] task-card', () => {
  // Deferred port from apps/web — re-enable when component API stabilizes.
});
