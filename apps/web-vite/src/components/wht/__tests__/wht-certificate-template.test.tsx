/**
 * OBSOLETE STUB — the WHT certificate React-PDF template lives in
 * `packages/api/src/pdf-templates/wht-certificate.tsx` (server-side
 * rendered into a Buffer + uploaded to R2). It was never client-side
 * code in the new SPA layout, so this client-test stub has no target
 * to exercise. Kept as a marker so the test name doesn't collide if
 * anyone re-ports the component into the SPA layer.
 */

import { describe } from 'vitest';

describe.skip('[OBSOLETE — component is server-side in packages/api]', () => {
  // noop
});
