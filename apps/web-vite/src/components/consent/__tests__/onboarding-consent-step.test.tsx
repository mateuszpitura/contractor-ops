/**
 * PORTED STUB — apps/web/src/components/consent/__tests__/onboarding-consent-step.test.tsx migrated as deferred skip-stub.
 *
 * REASON: matching component not present in apps/web-vite — feature was not
 * ported during the web → web-vite migration. Decide per case: port the
 * component (then unskip + adapt this test) or delete both as dead code.
 */

import { describe } from 'vitest';

describe.skip('[DEFERRED — component missing] onboarding-consent-step', () => {
  // Body intentionally empty — port or delete the component, then revisit.
});
