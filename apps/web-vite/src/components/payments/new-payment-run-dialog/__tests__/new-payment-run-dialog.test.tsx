/**
 * PORTED STUB — apps/web/src/components/payments/new-payment-run-dialog/__tests__/new-payment-run-dialog.test.tsx migrated as deferred skip-stub.
 *
 * REASON: matching component not present in apps/web-vite — feature was not
 * ported during the web → web-vite migration. Decide per case: port the
 * component (then unskip + adapt this test) or delete both as dead code.
 */

import { describe } from 'vitest';

describe.skip('[DEFERRED — component missing] new-payment-run-dialog', () => {
  // Placeholder; restore once the component lands in web-vite.
});
