.next/
node_modules/
out/
media/
*.tsbuildinfo
.env
.env.local
# src/payload-types.ts is committed — generated by `payload generate:types`
# but pinned so cold builds + downstream typed imports (legal-content.ts,
# payload-queries.ts) work without first wiring a CMS database connection.
# Re-run `pnpm --filter @contractor-ops/cms generate:types` after any
# collection-schema change and commit the diff.
