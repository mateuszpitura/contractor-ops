// ---------------------------------------------------------------------------
// Phase 60 · CLASS-10 — market-card scaffold (VALIDATION 60-04-07).
// ---------------------------------------------------------------------------

import { describe, it } from 'vitest';

describe('MarketCard', () => {
  it.todo('renders market title from i18n (UK — IR35 / Germany — Scheinselbständigkeit)');
  it.todo('renders 4 tile children in a 2x2 responsive grid per UI-SPEC D-13');
  it.todo('renders download CSV CTA for the given market');
});
