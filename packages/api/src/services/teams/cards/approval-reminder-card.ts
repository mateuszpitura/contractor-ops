// ---------------------------------------------------------------------------
// Stub: Approval Reminder Card Builder
// ---------------------------------------------------------------------------
// Placeholder for Plan 02. Provides correct export signature.
// ---------------------------------------------------------------------------

export function buildApprovalReminderCard(_params: {
  overdueInDays: number;
  invoiceNumber: string;
  contractorName: string;
  amount: string;
  currency: string;
  dueDate: string;
  invoiceId: string;
  flowId: string;
}): Record<string, unknown> {
  throw new Error("buildApprovalReminderCard stub -- Plan 02 not yet merged");
}
