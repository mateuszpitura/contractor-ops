// ---------------------------------------------------------------------------
// Phase 60 · CLASS-07 — economicDependencyAlert router tests (Wave-0 scaffolds).
// ---------------------------------------------------------------------------

import { describe, it } from 'vitest';

describe('economicDependencyAlert router', () => {
  describe.todo('listByEngagement returns only org-scoped alerts');
  describe.todo('list respects contractor:read permission gate');
  describe.todo('cross-org contractorAssignmentId is rejected');
});
