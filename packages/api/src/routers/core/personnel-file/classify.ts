import { router } from '../../../init';

// Document → section classification procedures for the personnel file.
// Empty for now so the personnelFile namespace can mount its read surface; the
// classify procedures are filled by a later plan that wires the hybrid
// taxonomy → AI → admin classifier onto this router.
export const classifyRouter = router({});
