import { describe, it } from "vitest";

describe("DocuSignAdapter", () => {
  it.todo("creates an envelope and returns external ID");
  it.todo("generates embedded signing URL for recipient");
  it.todo("downloads signed document as base64");
  it.todo("voids an in-progress envelope");
  it.todo("normalizes DocuSign Connect webhook to NormalizedSigningEvent");
  it.todo("sets supportsEmbeddedSigning to true");
});
