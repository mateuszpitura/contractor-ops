// ---------------------------------------------------------------------------
// Pipeline step identifiers (snake_case for multi-word values, per domain convention)
// ---------------------------------------------------------------------------

export type PipelineStep = "generate" | "validate" | "sign" | "qr_code";
